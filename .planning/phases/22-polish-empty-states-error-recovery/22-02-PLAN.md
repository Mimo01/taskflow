---
phase: 22-polish-empty-states-error-recovery
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - taskflow/src/routes/dashboard/MyTasksTab.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.tsx
  - taskflow/src/routes/dashboard/SprintProgressTab.tsx
  - taskflow/src/routes/dashboard/BacklogPage.tsx
  - taskflow/src/routes/dashboard/MrAttentionTab.tsx
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02

must_haves:
  truths:
    - "My Tasks shows ClipboardList empty state with 'You're all caught up!' when no tasks"
    - "Sprint Board shows Columns3 empty state with 'No sprint issues' when board is empty"
    - "Sprint Progress shows BarChart3 empty state with 'No sprint data yet' when no data"
    - "Backlog shows Inbox empty state with 'Backlog is empty' and 'Create Issue' CTA button"
    - "MR Attention shows GitMerge empty state with 'No merge requests need attention'"
    - "All 5 views show ErrorState with Retry on fetch failure (when no cached data)"
    - "All 5 views show StaleDataBanner above stale content when refetch fails with cached data"
  artifacts:
    - path: "taskflow/src/routes/dashboard/MyTasksTab.tsx"
      provides: "EmptyState + ErrorState + StaleDataBanner integration"
      contains: "EmptyState"
    - path: "taskflow/src/routes/dashboard/SprintBoardTab.tsx"
      provides: "EmptyState + ErrorState + StaleDataBanner integration"
      contains: "EmptyState"
    - path: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      provides: "EmptyState with Create Issue CTA"
      contains: "Create Issue"
    - path: "taskflow/src/routes/dashboard/MrAttentionTab.tsx"
      provides: "EmptyState with conditional Connect GitLab CTA"
      contains: "EmptyState"
  key_links:
    - from: "taskflow/src/routes/dashboard/MyTasksTab.tsx"
      to: "taskflow/src/components/ui/empty-state.tsx"
      via: "import { EmptyState }"
      pattern: "import.*EmptyState.*empty-state"
    - from: "taskflow/src/routes/dashboard/MyTasksTab.tsx"
      to: "taskflow/src/components/ui/error-state.tsx"
      via: "import { ErrorState }"
      pattern: "import.*ErrorState.*error-state"
---

<objective>
Replace inline empty and error JSX in 5 dashboard views (My Tasks, Sprint Board, Sprint Progress, Backlog, MR Attention) with the shared EmptyState, ErrorState, and StaleDataBanner components from Plan 01.

Purpose: Consistent, user-friendly empty/error states across the first half of data views, with per-view icons, copy, and CTAs per the UI-SPEC.
Output: 5 modified view files using shared components.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-polish-empty-states-error-recovery/22-CONTEXT.md
@.planning/phases/22-polish-empty-states-error-recovery/22-UI-SPEC.md
@.planning/phases/22-polish-empty-states-error-recovery/22-01-SUMMARY.md

<interfaces>
<!-- Shared components from Plan 01 -->
From taskflow/src/components/ui/empty-state.tsx:
```typescript
export function EmptyState({ icon, title, subtitle, action }: {
  icon: LucideIcon; title: string; subtitle?: string; action?: ReactNode;
}): JSX.Element
```

From taskflow/src/components/ui/error-state.tsx:
```typescript
export function ErrorState({ error, onRetry, viewName }: {
  error: Error | unknown; onRetry: () => void; viewName: string;
}): JSX.Element
// Auto-detects auth errors (401/403) and shows Reconnect CTA
```

From taskflow/src/components/ui/stale-data-banner.tsx:
```typescript
export function StaleDataBanner({ onRetry, onDismiss }: {
  onRetry: () => void; onDismiss: () => void;
}): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace empty/error states in MyTasksTab, SprintBoardTab, and SprintProgressTab</name>
  <files>
    taskflow/src/routes/dashboard/MyTasksTab.tsx,
    taskflow/src/routes/dashboard/SprintBoardTab.tsx,
    taskflow/src/routes/dashboard/SprintProgressTab.tsx
  </files>
  <read_first>
    taskflow/src/routes/dashboard/MyTasksTab.tsx,
    taskflow/src/routes/dashboard/SprintBoardTab.tsx,
    taskflow/src/routes/dashboard/SprintProgressTab.tsx,
    taskflow/src/components/ui/empty-state.tsx,
    taskflow/src/components/ui/error-state.tsx,
    taskflow/src/components/ui/stale-data-banner.tsx
  </read_first>
  <action>
    For each of these 3 files, apply the three-state detection pattern:

    **MyTasksTab.tsx:**
    - Add imports: EmptyState from '@/components/ui/empty-state', ErrorState from '@/components/ui/error-state', StaleDataBanner from '@/components/ui/stale-data-banner', ClipboardList from 'lucide-react'
    - Ensure useQuery destructuring includes: data, isLoading, isError, error, refetch
    - Add state: const [bannerDismissed, setBannerDismissed] = useState(false); useEffect(() => { setBannerDismissed(false); }, [error]);
    - Replace the existing error div (className containing "border-destructive") with TWO cases:
      - {isError && !data && <ErrorState error={error} onRetry={refetch} viewName="tasks" />}
      - {isError && data && !bannerDismissed && <StaleDataBanner onRetry={refetch} onDismiss={() => setBannerDismissed(true)} />}
    - Replace the existing empty div ("No tasks" text) with:
      {!isLoading && !isError && data && data.length === 0 && <EmptyState icon={ClipboardList} title="You're all caught up!" subtitle="No tasks assigned to you in the active sprint" />}

    **SprintBoardTab.tsx:**
    - Same import pattern. Icon: Columns3 from lucide-react
    - Ensure isError, error, refetch in useQuery destructuring
    - Replace inline error state with: {isError && !data && <ErrorState error={error} onRetry={refetch} viewName="sprint board" />}
    - Add stale banner: {isError && data && !bannerDismissed && <StaleDataBanner ... />}
    - Replace empty state with: <EmptyState icon={Columns3} title="No sprint issues" subtitle="This board will populate when issues are added to the active sprint" />
    - Add useState/useEffect for bannerDismissed

    **SprintProgressTab.tsx:**
    - Same import pattern. Icon: BarChart3 from lucide-react
    - Ensure isError, error, refetch in useQuery destructuring (check if it shares SprintBoard's query or has its own)
    - Replace error/empty with ErrorState(viewName="sprint progress") and EmptyState(icon=BarChart3, title="No sprint data yet", subtitle="Sprint progress will appear once a sprint is active")
    - Add StaleDataBanner + bannerDismissed state

    IMPORTANT: Do NOT remove any existing loading skeleton logic. Only replace the error and empty state JSX blocks.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/MyTasksTab.test.tsx src/routes/dashboard/SprintBoardTab.test.tsx src/routes/dashboard/SprintProgressTab.test.tsx --reporter=verbose 2>&1; echo "---"; npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/dashboard/MyTasksTab.tsx contains "import { EmptyState }" or "import {EmptyState}"
    - taskflow/src/routes/dashboard/MyTasksTab.tsx contains "import { ErrorState }" or "import {ErrorState}"
    - taskflow/src/routes/dashboard/MyTasksTab.tsx contains "ClipboardList"
    - taskflow/src/routes/dashboard/MyTasksTab.tsx contains "You're all caught up!"
    - taskflow/src/routes/dashboard/MyTasksTab.tsx contains "StaleDataBanner"
    - taskflow/src/routes/dashboard/MyTasksTab.tsx does NOT contain "border-destructive/30 bg-destructive/10" (old inline error pattern removed)
    - taskflow/src/routes/dashboard/SprintBoardTab.tsx contains "EmptyState" and "ErrorState" and "Columns3"
    - taskflow/src/routes/dashboard/SprintBoardTab.tsx contains "No sprint issues"
    - taskflow/src/routes/dashboard/SprintProgressTab.tsx contains "EmptyState" and "ErrorState" and "BarChart3"
    - taskflow/src/routes/dashboard/SprintProgressTab.tsx contains "No sprint data yet"
    - Full vitest suite passes
  </acceptance_criteria>
  <done>MyTasksTab, SprintBoardTab, SprintProgressTab all use shared EmptyState/ErrorState/StaleDataBanner with correct per-view icons and copy</done>
</task>

<task type="auto">
  <name>Task 2: Replace empty/error states in BacklogPage and MrAttentionTab</name>
  <files>
    taskflow/src/routes/dashboard/BacklogPage.tsx,
    taskflow/src/routes/dashboard/MrAttentionTab.tsx
  </files>
  <read_first>
    taskflow/src/routes/dashboard/BacklogPage.tsx,
    taskflow/src/routes/dashboard/MrAttentionTab.tsx,
    taskflow/src/components/ui/empty-state.tsx,
    taskflow/src/components/ui/error-state.tsx,
    taskflow/src/components/ui/stale-data-banner.tsx,
    taskflow/src/stores/auth.store.ts
  </read_first>
  <action>
    **BacklogPage.tsx:**
    - Add imports: EmptyState, ErrorState, StaleDataBanner, Button, Inbox from 'lucide-react'
    - Ensure useQuery destructuring includes isError, error, refetch (research notes BacklogPage may be missing these)
    - Replace any existing empty state with:
      ```
      <EmptyState
        icon={Inbox}
        title="Backlog is empty"
        subtitle="All issues are assigned to sprints"
        action={<Button onClick={handleCreateIssue}>Create Issue</Button>}
      />
      ```
      where handleCreateIssue opens the CreateEditIssueModal (find the existing create issue trigger pattern in BacklogPage)
    - Replace error state with: {isError && !data && <ErrorState error={error} onRetry={refetch} viewName="backlog" />}
    - Add StaleDataBanner for stale data case
    - Add bannerDismissed state + reset effect

    **MrAttentionTab.tsx:**
    - Add imports: EmptyState, ErrorState, StaleDataBanner, Button, GitMerge from 'lucide-react'
    - Import useNavigate from 'react-router-dom' and useAuthStore (to check gitlabBaseUrl)
    - Ensure isError, error, refetch in useQuery destructuring
    - Replace empty state with conditional CTA:
      ```
      const { gitlabBaseUrl } = useAuthStore();
      const navigate = useNavigate();

      // Empty state with conditional CTA
      <EmptyState
        icon={GitMerge}
        title="No merge requests need attention"
        subtitle="MRs requiring your review will appear here"
        action={!gitlabBaseUrl ? <Button onClick={() => navigate('/settings')}>Connect GitLab</Button> : undefined}
      />
      ```
    - Replace inline error with: {isError && !data && <ErrorState error={error} onRetry={refetch} viewName="merge requests" />}
    - Add StaleDataBanner + bannerDismissed state

    IMPORTANT: Check if useAuthStore is already imported in MrAttentionTab before adding a duplicate import. Check if useNavigate is already imported. Only add what is missing.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/BacklogPage.test.tsx src/routes/dashboard/MrAttentionTab.test.tsx --reporter=verbose 2>&1; echo "---"; npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/dashboard/BacklogPage.tsx contains "EmptyState" and "ErrorState"
    - taskflow/src/routes/dashboard/BacklogPage.tsx contains "Inbox" (icon import)
    - taskflow/src/routes/dashboard/BacklogPage.tsx contains "Backlog is empty"
    - taskflow/src/routes/dashboard/BacklogPage.tsx contains "Create Issue"
    - taskflow/src/routes/dashboard/BacklogPage.tsx contains "refetch" (wired to ErrorState/StaleDataBanner)
    - taskflow/src/routes/dashboard/MrAttentionTab.tsx contains "EmptyState" and "ErrorState"
    - taskflow/src/routes/dashboard/MrAttentionTab.tsx contains "GitMerge"
    - taskflow/src/routes/dashboard/MrAttentionTab.tsx contains "No merge requests need attention"
    - taskflow/src/routes/dashboard/MrAttentionTab.tsx contains "Connect GitLab"
    - taskflow/src/routes/dashboard/MrAttentionTab.tsx contains "StaleDataBanner"
    - Full vitest suite passes
  </acceptance_criteria>
  <done>BacklogPage shows empty state with Create Issue CTA, MrAttentionTab shows empty state with conditional Connect GitLab CTA, both have ErrorState and StaleDataBanner</done>
</task>

</tasks>

<verification>
cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run --reporter=verbose
- All 5 views now use shared EmptyState/ErrorState/StaleDataBanner
- No inline error/empty JSX remains in these 5 files
- All existing tests pass (no regressions)
</verification>

<success_criteria>
- 5 view files updated with shared components
- Each view uses the correct Lucide icon per UI-SPEC
- Each view uses the correct copy per copywriting contract
- BacklogPage has "Create Issue" CTA, MrAttentionTab has conditional "Connect GitLab" CTA
- All views have three-state detection: empty, error (no cache), stale error (with cache)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/22-polish-empty-states-error-recovery/22-02-SUMMARY.md`
</output>
