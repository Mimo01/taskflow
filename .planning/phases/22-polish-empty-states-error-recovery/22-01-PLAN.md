---
phase: 22-polish-empty-states-error-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/lib/api-error.ts
  - taskflow/src/lib/api-error.test.ts
  - taskflow/src/components/ui/empty-state.tsx
  - taskflow/src/components/ui/empty-state.test.tsx
  - taskflow/src/components/ui/error-state.tsx
  - taskflow/src/components/ui/error-state.test.tsx
  - taskflow/src/components/ui/stale-data-banner.tsx
  - taskflow/src/components/ui/stale-data-banner.test.tsx
  - taskflow/src/services/jira.ts
  - taskflow/src/services/gitlab.ts
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-03

must_haves:
  truths:
    - "ApiError class preserves HTTP status and source ('jira'|'gitlab') for auth detection"
    - "isAuthError correctly identifies 401/403 from ApiError, raw Response, and plain Error"
    - "EmptyState renders a Lucide icon, title, optional subtitle, and optional action button"
    - "ErrorState renders a destructive Alert with view-specific message and Retry button"
    - "ErrorState auto-detects auth errors and shows Reconnect CTA navigating to /settings"
    - "StaleDataBanner renders a dismissible warning with Retry button"
  artifacts:
    - path: "taskflow/src/lib/api-error.ts"
      provides: "ApiError class, isAuthError, getErrorSource helpers"
      exports: ["ApiError", "isAuthError", "getErrorSource"]
    - path: "taskflow/src/components/ui/empty-state.tsx"
      provides: "EmptyState shared component"
      exports: ["EmptyState"]
    - path: "taskflow/src/components/ui/error-state.tsx"
      provides: "ErrorState shared component with auth detection"
      exports: ["ErrorState"]
    - path: "taskflow/src/components/ui/stale-data-banner.tsx"
      provides: "StaleDataBanner shared component"
      exports: ["StaleDataBanner"]
  key_links:
    - from: "taskflow/src/components/ui/error-state.tsx"
      to: "taskflow/src/lib/api-error.ts"
      via: "import { isAuthError, getErrorSource }"
      pattern: "isAuthError\\(error\\)"
    - from: "taskflow/src/components/ui/error-state.tsx"
      to: "/settings"
      via: "useNavigate for Reconnect CTA"
      pattern: "navigate.*settings"
    - from: "taskflow/src/services/jira.ts"
      to: "taskflow/src/lib/api-error.ts"
      via: "throw new ApiError on 401/403"
      pattern: "throw new ApiError"
    - from: "taskflow/src/services/gitlab.ts"
      to: "taskflow/src/lib/api-error.ts"
      via: "throw new ApiError on 401/403"
      pattern: "throw new ApiError"
---

<objective>
Create the foundation layer for Phase 22: the ApiError class for HTTP status preservation, and three shared UI components (EmptyState, ErrorState, StaleDataBanner). Also retrofit 401/403 throw sites in jira.ts and gitlab.ts to use ApiError so downstream views can detect auth errors.

Purpose: All 10 data views will consume these shared components in Plans 02 and 03. Without this foundation, there is no consistent error detection or standardized empty/error rendering.
Output: 4 new source files, 4 new test files, 2 modified service files.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-polish-empty-states-error-recovery/22-CONTEXT.md
@.planning/phases/22-polish-empty-states-error-recovery/22-RESEARCH.md
@.planning/phases/22-polish-empty-states-error-recovery/22-UI-SPEC.md

<interfaces>
<!-- Existing shadcn Alert component exports (used by ErrorState) -->
From taskflow/src/components/ui/alert.tsx:
```typescript
export { Alert, AlertTitle, AlertDescription, AlertAction }
// Alert accepts variant: "default" | "destructive"
// AlertAction renders absolute top-2 right-2
```

From taskflow/src/components/ui/button.tsx:
```typescript
// Standard shadcn Button with variant="default"|"secondary"|"ghost"|"destructive"|"outline"|"link"
// and size="default"|"sm"|"lg"|"icon"
```

From taskflow/src/lib/apiFetch.ts:
```typescript
// apiFetch already calls markDisconnected(source) on 401
// Service functions use apiFetch('jira'|'gitlab', url, init) for HTTP calls
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ApiError class + isAuthError/getErrorSource helpers + EmptyState + StaleDataBanner components</name>
  <files>
    taskflow/src/lib/api-error.ts,
    taskflow/src/lib/api-error.test.ts,
    taskflow/src/components/ui/empty-state.tsx,
    taskflow/src/components/ui/empty-state.test.tsx,
    taskflow/src/components/ui/stale-data-banner.tsx,
    taskflow/src/components/ui/stale-data-banner.test.tsx
  </files>
  <read_first>
    taskflow/src/components/ui/alert.tsx,
    taskflow/src/components/ui/button.tsx,
    taskflow/src/lib/utils.ts,
    taskflow/src/services/jira.ts,
    taskflow/src/services/gitlab.ts
  </read_first>
  <behavior>
    ApiError tests (api-error.test.ts):
    - ApiError extends Error, has name='ApiError', status, source fields
    - new ApiError('msg', 401, 'jira') => status=401, source='jira', message='msg'
    - isAuthError(new ApiError('x', 401, 'jira')) => true
    - isAuthError(new ApiError('x', 403, 'gitlab')) => true
    - isAuthError(new ApiError('x', 500, 'jira')) => false
    - isAuthError({ status: 401 }) => true (raw Response shape)
    - isAuthError(new Error('status 401')) => true (message heuristic)
    - isAuthError(new Error('token has expired')) => true
    - isAuthError(new Error('something else')) => false
    - isAuthError(null) => false
    - getErrorSource(new ApiError('x', 401, 'gitlab')) => 'gitlab'
    - getErrorSource(new Error('x')) => null

    EmptyState tests (empty-state.test.tsx):
    - Renders icon element (svg present)
    - Renders title text
    - Renders subtitle when provided
    - Does not render subtitle when omitted
    - Renders action ReactNode when provided
    - Does not render action wrapper when omitted

    StaleDataBanner tests (stale-data-banner.test.tsx):
    - Renders "Couldn't refresh" text
    - Renders Retry button that calls onRetry on click
    - Renders dismiss button that calls onDismiss on click
  </behavior>
  <action>
    1. Create taskflow/src/lib/api-error.ts:
       - export class ApiError extends Error { status: number; source: 'jira' | 'gitlab'; constructor(message: string, status: number, source: 'jira' | 'gitlab') { super(message); this.name = 'ApiError'; this.status = status; this.source = source; } }
       - export function isAuthError(error: unknown): boolean — checks ApiError.status 401/403, then raw object with .status 401/403, then Error.message regex /\b(401|403)\b/ or includes('token has expired') or includes('lacks required permissions')
       - export function getErrorSource(error: unknown): 'jira' | 'gitlab' | null — returns ApiError.source or null

    2. Create taskflow/src/components/ui/empty-state.tsx:
       - Props: { icon: LucideIcon; title: string; subtitle?: string; action?: ReactNode }
       - Layout: div with className="flex flex-col items-center justify-center py-8 text-center"
       - Icon: React.createElement(icon, { className: "size-12 text-muted-foreground mb-4" })
       - Title: p with className="text-base font-medium text-foreground"
       - Subtitle (conditional): p with className="mt-1 text-sm text-muted-foreground max-w-xs"
       - Action (conditional): div with className="mt-4" wrapping the action ReactNode

    3. Create taskflow/src/components/ui/stale-data-banner.tsx:
       - Props: { onRetry: () => void; onDismiss: () => void }
       - Layout: div className="flex items-center gap-2 rounded-lg border bg-muted px-3 py-2 text-sm"
       - Icon: RefreshCw from lucide-react, className="size-4 text-muted-foreground shrink-0"
       - Text: span className="text-muted-foreground flex-1" with "Couldn't refresh — showing cached data"
       - Retry: Button variant="secondary" size="sm" onClick={onRetry} with text "Retry"
       - Dismiss: Button variant="ghost" size="sm" onClick={onDismiss} with X icon from lucide-react

    4. Write all test files FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/lib/api-error.test.ts src/components/ui/empty-state.test.tsx src/components/ui/stale-data-banner.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/lib/api-error.ts contains "export class ApiError extends Error"
    - taskflow/src/lib/api-error.ts contains "export function isAuthError"
    - taskflow/src/lib/api-error.ts contains "export function getErrorSource"
    - taskflow/src/lib/api-error.ts contains "status === 401 || error.status === 403" (or equivalent)
    - taskflow/src/components/ui/empty-state.tsx contains "export function EmptyState"
    - taskflow/src/components/ui/empty-state.tsx contains "size-12 text-muted-foreground"
    - taskflow/src/components/ui/empty-state.tsx contains "text-base font-medium text-foreground"
    - taskflow/src/components/ui/stale-data-banner.tsx contains "export function StaleDataBanner"
    - taskflow/src/components/ui/stale-data-banner.tsx contains "Couldn't refresh"
    - All 3 test files pass with exit code 0
  </acceptance_criteria>
  <done>ApiError class with isAuthError/getErrorSource helpers tested, EmptyState renders icon/title/subtitle/action, StaleDataBanner renders dismiss + retry</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ErrorState component + ApiError retrofit in jira.ts and gitlab.ts</name>
  <files>
    taskflow/src/components/ui/error-state.tsx,
    taskflow/src/components/ui/error-state.test.tsx,
    taskflow/src/services/jira.ts,
    taskflow/src/services/gitlab.ts
  </files>
  <read_first>
    taskflow/src/lib/api-error.ts,
    taskflow/src/components/ui/alert.tsx,
    taskflow/src/components/ui/button.tsx,
    taskflow/src/services/jira.ts,
    taskflow/src/services/gitlab.ts,
    taskflow/src/lib/apiFetch.ts
  </read_first>
  <behavior>
    ErrorState tests (error-state.test.tsx):
    - Generic error: renders "Couldn't load {viewName}" title
    - Generic error: renders Retry button that calls onRetry on click
    - Generic error: uses AlertCircle icon (check svg or role)
    - Auth error (ApiError with status 401): renders "Session expired" title
    - Auth error: renders "Reconnect" button (not "Retry")
    - Auth error with source 'jira': description contains "Jira"
    - Auth error with source 'gitlab': description contains "GitLab"
    - Auth error: Reconnect button triggers navigation to /settings
    - Plain Error (non-auth): renders Retry, not Reconnect
    - Console.error is called with the raw error (not shown to user)
  </behavior>
  <action>
    1. Create taskflow/src/components/ui/error-state.tsx:
       - Props: { error: Error | unknown; onRetry: () => void; viewName: string }
       - Import { isAuthError, getErrorSource } from '@/lib/api-error'
       - Import { Alert, AlertTitle, AlertDescription, AlertAction } from '@/components/ui/alert'
       - Import { Button } from '@/components/ui/button'
       - Import { AlertCircle, ShieldAlert } from 'lucide-react'
       - Import { useNavigate } from 'react-router-dom'
       - Detect auth: const authError = isAuthError(error); const source = getErrorSource(error);
       - Log raw error: useEffect(() => { console.error(`[ErrorState] ${viewName}:`, error); }, [error, viewName]);
       - If authError: Alert variant="destructive" with ShieldAlert icon, AlertTitle "Session expired", AlertDescription "Your {source === 'gitlab' ? 'GitLab' : 'Jira'} token may have been revoked or expired" (default to 'Jira' if source is null), AlertAction with Button variant="default" size="sm" onClick={() => navigate('/settings')} text "Reconnect"
       - Else: Alert variant="destructive" with AlertCircle icon, AlertTitle "Couldn't load {viewName}", AlertDescription is empty or minimal (never raw error message), AlertAction with Button variant="secondary" size="sm" onClick={onRetry} text "Retry"

    2. Retrofit taskflow/src/services/jira.ts — at EVERY `!response.ok` throw site where status could be 401 or 403:
       - Add `import { ApiError } from '../lib/api-error';` at top
       - At line ~62 (validateJira 401): change `throw new Error('Invalid token...')` to `throw new ApiError('Invalid token or token has expired', 401, 'jira')`
       - At line ~66 (validateJira 403): change to `throw new ApiError('Token valid but lacks required permissions', 403, 'jira')`
       - At line ~100 (validateJiraWithProject 401): same pattern
       - At line ~104 (validateJiraWithProject 403): same pattern
       - At line ~195 (fetchSprintIssues raw Response throw): change `throw response` to `if (response.status === 401 || response.status === 403) throw new ApiError(response.status === 401 ? 'Token expired' : 'Insufficient permissions', response.status, 'jira'); throw new ApiError('Failed to fetch sprint issues', response.status, 'jira');`
       - At other throw sites with `status ${response.status}`: wrap in ApiError for 401/403 cases. For non-auth statuses, keep as `throw new Error(...)` or optionally use ApiError.
       - Keep ALL existing non-HTTP throw messages unchanged (e.g., 'Cannot reach' network errors stay as plain Error).

    3. Retrofit taskflow/src/services/gitlab.ts — same pattern:
       - Add `import { ApiError } from '../lib/api-error';` at top
       - At line ~65 (validateGitLab 401): `throw new ApiError('Invalid token or token has expired', 401, 'gitlab')`
       - At line ~69 (validateGitLab 403): `throw new ApiError('Token valid but lacks required permissions', 403, 'gitlab')`
       - At line ~103, ~107 (validateGitLabProject): same
       - At line ~141, ~145 (validateGitLabMrAccess): same
       - At line ~228 (fetchAssignedMRs): `throw new ApiError('Failed to fetch assigned MRs', response.status, 'gitlab')` — and add 401/403 special case
       - At other fetchX throw sites: add 401/403 ApiError cases before generic throw
       - Keep 'Cannot reach' network errors as plain Error

    4. Write error-state.test.tsx FIRST (RED), then implement ErrorState (GREEN), then retrofit services.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/components/ui/error-state.test.tsx --reporter=verbose && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/components/ui/error-state.tsx contains "export function ErrorState"
    - taskflow/src/components/ui/error-state.tsx contains "isAuthError"
    - taskflow/src/components/ui/error-state.tsx contains "Session expired"
    - taskflow/src/components/ui/error-state.tsx contains "Couldn't load"
    - taskflow/src/components/ui/error-state.tsx contains "Reconnect"
    - taskflow/src/components/ui/error-state.tsx contains "navigate('/settings')" or "navigate(\"/settings\")"
    - taskflow/src/services/jira.ts contains "import { ApiError }" or "import {ApiError}"
    - taskflow/src/services/jira.ts contains "throw new ApiError("
    - taskflow/src/services/gitlab.ts contains "import { ApiError }" or "import {ApiError}"
    - taskflow/src/services/gitlab.ts contains "throw new ApiError("
    - error-state.test.tsx passes with exit code 0
    - Full vitest suite passes (no regressions from service refactor)
  </acceptance_criteria>
  <done>ErrorState component renders auth-aware error UI with Reconnect/Retry CTAs, jira.ts and gitlab.ts throw ApiError on 401/403, all tests green</done>
</task>

</tasks>

<verification>
cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run --reporter=verbose
- All new test files pass
- All existing test files still pass (no regressions from ApiError retrofit)
</verification>

<success_criteria>
- 4 new shared components exist: api-error.ts, empty-state.tsx, error-state.tsx, stale-data-banner.tsx
- 4 new test files all pass
- ErrorState auto-detects auth errors and renders Reconnect to /settings
- jira.ts and gitlab.ts throw ApiError on 401/403 responses
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/22-polish-empty-states-error-recovery/22-01-SUMMARY.md`
</output>
