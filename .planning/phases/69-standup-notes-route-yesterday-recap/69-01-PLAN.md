---
phase: 69-standup-notes-route-yesterday-recap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/lib/standup-date.ts
  - taskflow/src/lib/standup-date.test.ts
  - taskflow/src/services/jira.ts
  - taskflow/src/services/jira-standup.test.ts
autonomous: true
requirements: [STAND-02, STAND-04, STAND-05]
must_haves:
  truths:
    - "resolveYesterdayDate() returns the last working day, skipping weekends (Monday resolves to Friday)"
    - "resolveYesterdayDate() additionally skips days marked HOLIDAY when a Tempo schedule map is provided"
    - "extractJiraKeyFromMessage()/extractJiraKeyFromBranch() return the first [A-Z][A-Z0-9]+-\\d+ match or null"
    - "fetchYesterdayJiraActivity() returns only issues with at least one author-and-date matching transition or comment"
  artifacts:
    - path: "taskflow/src/lib/standup-date.ts"
      provides: "resolveYesterdayDate, getScheduleLookbackRange, extractJiraKeyFromMessage, extractJiraKeyFromBranch"
      exports: ["resolveYesterdayDate", "getScheduleLookbackRange", "extractJiraKeyFromMessage", "extractJiraKeyFromBranch"]
      min_lines: 40
    - path: "taskflow/src/lib/standup-date.test.ts"
      provides: "Unit coverage for STAND-02 (weekend + holiday skip) and STAND-05 (Jira key extraction)"
      contains: "resolveYesterdayDate"
    - path: "taskflow/src/services/jira.ts"
      provides: "fetchYesterdayJiraActivity + JiraActivityItem export"
      contains: "export async function fetchYesterdayJiraActivity"
    - path: "taskflow/src/services/jira-standup.test.ts"
      provides: "Unit coverage for STAND-04 client-side author+date filtering"
      contains: "fetchYesterdayJiraActivity"
  key_links:
    - from: "taskflow/src/lib/standup-date.ts"
      to: "@/services/tempo (ScheduleDayType)"
      via: "type import"
      pattern: "import type \\{ ScheduleDayType \\}"
    - from: "taskflow/src/services/jira.ts (fetchYesterdayJiraActivity)"
      to: "apiFetch('jira', ...)"
      via: "JQL search + per-issue comment fetch"
      pattern: "apiFetch\\('jira'"
---

<objective>
Build the two pure-data foundations the Yesterday recap depends on: (1) the `standup-date.ts` utility that resolves "yesterday" to the last working day (weekend + Tempo holiday skip) and extracts Jira keys from commit messages/branch names, and (2) the `fetchYesterdayJiraActivity()` service function that returns the status transitions + comments I authored on the last working day.

Purpose: These are pure, testable units with no UI dependency. Isolating them in Wave 1 lets the page and column work (Plan 04) consume verified contracts instead of exploring the codebase. STAND-02 (date resolution), STAND-04 (Jira activity), and the STAND-05 key-extraction helper all live here.
Output: `src/lib/standup-date.ts` + test, and a new `fetchYesterdayJiraActivity()` export in `src/services/jira.ts` + test.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-standup-notes-route-yesterday-recap/69-RESEARCH.md
@.planning/phases/69-standup-notes-route-yesterday-recap/69-PATTERNS.md

<interfaces>
<!-- Verified from codebase scan. Use these directly — no exploration needed. -->

From taskflow/src/services/tempo/schedule.ts:
```typescript
export type ScheduleDayType = 'WORKING_DAY' | 'NON_WORKING_DAY' | 'HOLIDAY';
export async function fetchUserSchedule(
  baseUrl: string, token: string, from: string, to: string, userKey: string,
): Promise<Map<string, ScheduleDayType>>;
```
Barrel: `import type { ScheduleDayType } from '@/services/tempo';` (re-exported from tempo/index.ts).

From taskflow/src/services/jira.ts (existing, to extend):
```typescript
import { ApiError } from '../lib/api-error';        // line 20
import { apiFetch } from '../lib/apiFetch';          // line 21
import type { ChangelogHistory } from './jira-changelog'; // line 29
export interface JiraComment {                        // line 798
  id: string;
  author: { displayName: string; name?: string };
  body: string;
  created: string;  // ISO 8601
  updated: string;
}
export async function fetchComments(baseUrl, token, issueKey): Promise<JiraComment[]>; // line 806
export async function searchJira(baseUrl, token, projectKey, query): Promise<JiraIssue[]>; // line 1061
```

From taskflow/src/services/jira-changelog.ts:
```typescript
export interface ChangelogHistory {
  id: string;
  created: string;            // ISO 8601
  author: { displayName: string; name?: string; avatarUrls?: {...} };
  items: Array<{ field: string; fieldtype?: string; fromString: string | null; toString: string | null }>;
}
```

Existing getLastWorkingDay() to extend (taskflow/src/routes/worklogs/WorklogsPage.tsx line 182):
```typescript
function getLastWorkingDay(): string {
  const today = new Date();
  const dow = today.getDay(); // 0=Sun, 1=Mon
  const daysBack = dow === 1 ? 3 : dow === 0 ? 2 : 1;
  const d = new Date(today);
  d.setDate(today.getDate() - daysBack);
  return d.toISOString().slice(0, 10);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create standup-date.ts utility (date resolution + Jira key extraction)</name>
  <read_first>
    - taskflow/src/lib/aioUtils.ts (utility file structure + JSDoc convention this file must follow)
    - taskflow/src/routes/worklogs/WorklogsPage.tsx lines 110-190 (getLastWorkingDay + the `.toISOString().slice(0,10)` date discipline this must replicate)
    - taskflow/src/services/tempo/schedule.ts (ScheduleDayType definition + Map<string, ScheduleDayType> shape returned by fetchUserSchedule)
    - .planning/phases/69-standup-notes-route-yesterday-recap/69-RESEARCH.md Pattern 4 (full resolveYesterdayDate reference) + Pattern 6 (Jira key extraction)
  </read_first>
  <behavior>
    - resolveYesterdayDate() with no argument: starts at today-1, returns the first weekday going backward (dow not 0 and not 6). Verify Monday input resolves to Friday (subtract weekend).
    - resolveYesterdayDate(scheduleMap) where the candidate weekday is marked 'HOLIDAY': skips it and returns the prior working day.
    - resolveYesterdayDate(scheduleMap) where the map has only 'WORKING_DAY' entries: behaves identically to the no-arg weekend-only case.
    - 14-iteration safety cap prevents infinite loop; returns a YYYY-MM-DD string in all paths.
    - getScheduleLookbackRange() returns { from, to } where `from` is 14 days before today and `to` is today, both YYYY-MM-DD.
    - extractJiraKeyFromMessage('fix [PROJ-123] bug') returns 'PROJ-123'; extractJiraKeyFromMessage('no key here') returns null.
    - extractJiraKeyFromBranch('feature/PROJ-456-thing') returns 'PROJ-456'; extractJiraKeyFromBranch('main') returns null.
    - Regex must not match lowercase project prefixes (e.g. 'abc-1' returns null) and must match multi-segment prefixes like 'AB12-9'.
  </behavior>
  <action>
    Create `taskflow/src/lib/standup-date.ts` exporting four functions: `resolveYesterdayDate(tempoSchedule?: Map<string, ScheduleDayType>): string`, `getScheduleLookbackRange(): { from: string; to: string }`, `extractJiraKeyFromMessage(message: string): string | null`, and `extractJiraKeyFromBranch(branchName: string): string | null`. Implement `resolveYesterdayDate` per RESEARCH.md Pattern 4 (D-15): start `candidate = today - 1`, loop max 14 iterations, skip days where `getDay()` is 0 or 6, then skip days where `tempoSchedule?.get(dateStr) === 'HOLIDAY'`, return the first surviving `dateStr` computed via `candidate.toISOString().slice(0, 10)`. NEVER use `toLocaleDateString()` for any date computation (Phase 62 standing rule). Import the type via `import type { ScheduleDayType } from '@/services/tempo';`. For the two extraction helpers use the shared regex `/[A-Z][A-Z0-9]+-\d+/` — use a non-global regex with `.match()` (or `.exec()` on a fresh RegExp instance) so repeated calls do not carry `lastIndex` state. Return `match ? match[0] : null`. Add file-level + per-function JSDoc matching the `aioUtils.ts` convention.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- standup-date</automated>
  </verify>
  <acceptance_criteria>
    - `standup-date.ts` contains `export function resolveYesterdayDate`, `export function getScheduleLookbackRange`, `export function extractJiraKeyFromMessage`, `export function extractJiraKeyFromBranch`
    - `standup-date.ts` contains `import type { ScheduleDayType }` and contains no occurrence of `toLocaleDateString`
    - `standup-date.ts` contains `.toISOString().slice(0, 10)`
    - `npm run test -- standup-date` exits 0 with the behaviors above covered
  </acceptance_criteria>
  <done>standup-date.ts exports all four functions; weekend-skip, holiday-skip, and key-extraction behaviors are covered by passing unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fetchYesterdayJiraActivity() to jira.ts</name>
  <read_first>
    - taskflow/src/services/jira.ts (the file being modified — read fetchComments at ~806, searchJira at ~1061, the ApiError import at line 20, the apiFetch import at line 21, and the ChangelogHistory import at line 29 before editing)
    - taskflow/src/services/jira-changelog.ts lines 14-28 (ChangelogHistory shape — author.name + items[].field/fromString/toString)
    - taskflow/src/services/jira.test.ts lines 1-45 (vi.mock('@tauri-apps/plugin-http') setup pattern the new test file must replicate)
    - .planning/phases/69-standup-notes-route-yesterday-recap/69-RESEARCH.md Pattern 5 §fetchYesterdayJiraActivity (full reference implementation, D-01/D-02/D-03)
  </read_first>
  <behavior>
    - JQL search succeeds, one issue has a changelog history with author.name === jiraUsername, created date === target date, and an item where field === 'status': the returned item includes a transition { fromStatus, toStatus, at }.
    - A changelog history authored by a DIFFERENT user on the same date is excluded from transitions.
    - A changelog history authored by jiraUsername but on a DIFFERENT date is excluded.
    - Per-issue comment fetch returns a comment authored by jiraUsername on the target date: it appears in the issue's comments array; comments by other authors or other dates are excluded.
    - An issue with zero matching transitions AND zero matching comments is omitted entirely from the result array.
    - The comment fetch for one issue throwing/failing does not abort the whole call (graceful per-issue degradation) — other issues still return.
    - A 401/403 on the JQL search throws an ApiError with source 'jira'.
  </behavior>
  <action>
    Add `export interface JiraActivityItem { issueKey: string; summary: string; transitions: Array<{ fromStatus: string; toStatus: string; at: string }>; comments: Array<{ body: string; at: string }>; }` and `export async function fetchYesterdayJiraActivity(baseUrl: string, token: string, projectKey: string, date: string, jiraUsername: string): Promise<JiraActivityItem[]>` to `taskflow/src/services/jira.ts` after the existing `fetchComments` function. Implement per RESEARCH.md Pattern 5: (1) JQL `project = {projectKey} AND updated >= "{date}" ORDER BY updated DESC` against `/rest/api/2/search?jql=...&maxResults=50&expand=changelog&fields=summary,status,issuetype` (D-01, D-03), encoding the JQL with `encodeURIComponent`; use `apiFetch('jira', url, { headers: { Authorization: \`Bearer ${token}\`, 'Content-Type': 'application/json' } }, 'Load Standup Jira Activity')`; throw `ApiError(..., response.status, 'jira')` on 401/403 and a plain `Error` on other non-ok statuses. (2) For each returned issue, filter `changelog.histories` to entries where `h.author.name === jiraUsername` AND `h.created.slice(0, 10) === date` AND `h.items.some(i => i.field === 'status')`, mapping each to `{ fromStatus: statusItem.fromString ?? '', toStatus: statusItem.toString ?? '', at: h.created }` (D-02). (3) For each issue, fetch `/rest/api/2/issue/{key}/comment` inside a `try/catch` (graceful degradation), filtering returned comments to `c.author.name === jiraUsername && c.created.slice(0, 10) === date`, mapping to `{ body: c.body, at: c.created }`. (4) Push an item only when `transitions.length > 0 || comments.length > 0`. Use `apiFetch('jira', ...)` for every request and `.slice(0, 10)` for every date comparison — never `toLocaleDateString()`. Reuse the existing `ChangelogHistory` and `JiraComment` types already imported/declared in the file.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- jira-standup</automated>
  </verify>
  <acceptance_criteria>
    - `jira.ts` contains `export async function fetchYesterdayJiraActivity(` and `export interface JiraActivityItem`
    - `jira.ts` fetchYesterdayJiraActivity body contains `expand=changelog`, `maxResults=50`, and `apiFetch('jira'`
    - `jira-standup.test.ts` mocks `@tauri-apps/plugin-http` and asserts: author+date filtering on transitions, per-issue comment filtering, omission of issues with no matching activity, and ApiError on 401
    - `npm run test -- jira-standup` exits 0
    - `cd taskflow && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>fetchYesterdayJiraActivity is exported from jira.ts, returns only author+date matched activity grouped per issue, degrades gracefully on per-issue comment failures, and is covered by passing unit tests in jira-standup.test.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri webview → Jira Server (apiFetch) | Outbound authenticated API call; token from Stronghold |
| date param → JQL/URL string | Internally computed date string interpolated into JQL + URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Tampering | JQL injection via `date` param in fetchYesterdayJiraActivity | mitigate | `date` is internally computed by resolveYesterdayDate (`toISOString().slice(0,10)`), never user-typed; format is fixed YYYY-MM-DD. JQL value is wrapped in quotes and the whole JQL is `encodeURIComponent`-encoded. |
| T-69-02 | Information Disclosure | Jira PAT leak | mitigate | Token passed as function arg from `readSecret('jira-pat')` (Stronghold); never logged, never persisted to Zustand, never placed in any returned object. |
| T-69-03 | Denial of Service | Per-issue comment fetch fan-out (up to 50 sequential/parallel calls) | accept | D-03 caps JQL at maxResults=50; per-issue comment failures are caught individually so one slow/failing issue cannot abort the recap. Daily-standup volume is low. |
| T-69-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this phase (RESEARCH Package Legitimacy Audit: zero installs). No install task → no slopcheck gate required. |
</threat_model>

<verification>
- `cd taskflow && npm run test -- standup-date` passes (STAND-02 weekend+holiday skip, STAND-05 key extraction)
- `cd taskflow && npm run test -- jira-standup` passes (STAND-04 author+date filtering)
- `cd taskflow && npx tsc --noEmit` passes (no type regressions in jira.ts)
- No new dependencies added to package.json
</verification>

<success_criteria>
- `resolveYesterdayDate()` resolves Monday → Friday and skips Tempo holidays when a schedule map is supplied (STAND-02)
- Jira key extraction helpers correctly parse `[A-Z][A-Z0-9]+-\d+` from messages and branch names, returning null on miss (STAND-05 helper)
- `fetchYesterdayJiraActivity()` returns per-issue transitions + comments filtered to the authoring user and the target date, omitting issues with no matching activity (STAND-04)
- All new unit tests pass; `tsc --noEmit` is green
</success_criteria>

<output>
Create `.planning/phases/69-standup-notes-route-yesterday-recap/69-01-SUMMARY.md` when done
</output>
