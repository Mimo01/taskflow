---
phase: 69-standup-notes-route-yesterday-recap
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/routes.tsx
  - taskflow/src/components/app/sidebar-items.ts
  - taskflow/src/main.tsx
  - taskflow/src/routes/standup-notes/StandupNotesPage.tsx
  - taskflow/src/routes/standup-notes/StandupPageHeader.tsx
  - taskflow/src/routes/standup-notes/TodayColumnPlaceholder.tsx
  - taskflow/src/routes/routes.test.ts
autonomous: true
requirements: [STAND-01]
must_haves:
  truths:
    - "A 'Standup Notes' item appears in the sidebar (section main) and is visible to all users"
    - "Navigating to /standup-notes renders the two-column page shell with header"
    - "The page header shows 'Standup notes' title, the resolved date, sync status + Refresh, and a Copy markdown button"
    - "The Today column renders the Phase 70 placeholder (heading + EmptyState)"
  artifacts:
    - path: "taskflow/src/routes/standup-notes/StandupNotesPage.tsx"
      provides: "Top-level page: 2-column shell + header; renders left (Yesterday) + right (Today placeholder) regions"
      contains: "StandupNotesPage"
      min_lines: 40
    - path: "taskflow/src/routes/standup-notes/StandupPageHeader.tsx"
      provides: "Header: title + date + sync status + Refresh + Copy markdown"
      contains: "Copy markdown"
    - path: "taskflow/src/routes/standup-notes/TodayColumnPlaceholder.tsx"
      provides: "Phase 70 placeholder column shell"
      contains: "Today section coming soon"
    - path: "taskflow/src/routes/routes.tsx"
      provides: "Lazy /standup-notes route"
      contains: "standup-notes"
    - path: "taskflow/src/components/app/sidebar-items.ts"
      provides: "standup-notes nav entry"
      contains: "standup-notes"
    - path: "taskflow/src/main.tsx"
      provides: "routeLabel case for /standup-notes"
      contains: "Standup Notes"
  key_links:
    - from: "taskflow/src/routes/routes.tsx"
      to: "./standup-notes/StandupNotesPage"
      via: "lazy import + withLazy route entry"
      pattern: "standup-notes/StandupNotesPage"
    - from: "taskflow/src/components/app/sidebar-items.ts"
      to: "/standup-notes"
      via: "SIDEBAR_NAV_ITEMS entry (auto-included by getDefaultSidebarItems map)"
      pattern: "'/standup-notes'"
---

<objective>
Register the `/standup-notes` route + sidebar entry + breadcrumb label (STAND-01), and build the two-column page shell: `StandupNotesPage` (owns the 50/50 layout + header), `StandupPageHeader` (title + date + sync status + Refresh + Copy markdown button), and `TodayColumnPlaceholder` (the Phase 70 stub). The left/Yesterday region is rendered as a labelled container in this plan; its data sections are filled in by Plan 04.

Purpose: This is the interface-first / shell plan. It defines `StandupNotesPage.tsx` as the contract that Plan 04 extends with the four useQuery hooks + YesterdayColumn. Building the route + shell now means STAND-01 is fully verifiable in Wave 1 (route reachable, sidebar item present, header + Today placeholder render) and Plan 04 only adds the Yesterday data wiring.
Output: three new components under `src/routes/standup-notes/`, route/sidebar/label registration across three config files, and a route-presence test in `routes.test.ts`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-standup-notes-route-yesterday-recap/69-UI-SPEC.md
@.planning/phases/69-standup-notes-route-yesterday-recap/69-PATTERNS.md

<interfaces>
<!-- Verified from codebase scan. Use these directly. -->

routes.tsx (line 23 lazy import, line 45 /worklogs entry, lines 25-33 withLazy):
```typescript
const WorklogsPage = lazy(() => import('./worklogs/WorklogsPage'));
// route array:
{ path: '/worklogs', element: withLazy(WorklogsPage) },
function withLazy(Component: ComponentType) { /* ChunkErrorBoundary + Suspense + RouteSpinner */ }
```

sidebar-items.ts (my-tasks entry lines 45-49; getDefaultSidebarItems maps SIDEBAR_NAV_ITEMS at line 96 → auto-includes, no migration):
```typescript
{ id: 'my-tasks', label: 'My Tasks', path: '/my-tasks', iconName: 'CheckSquare', section: 'main' },
export function getDefaultSidebarItems(): SidebarItem[] {
  return SIDEBAR_NAV_ITEMS.map((item) => ({ id: item.id, visible: true }));
}
```

main.tsx routeLabel (lines 285-298, add before `return 'Home'`):
```typescript
function routeLabel(pathname: string): string {
  if (pathname.startsWith('/worklogs')) return 'Worklogs';
  // ...
  return 'Home';
}
```

WorklogsPage header pattern (line 738):
```typescript
<div className="flex flex-col h-full">
  <header className="px-6 py-4 border-b border-border"> <h1 className="text-xl font-semibold">Worklogs</h1> </header>
```

UI-SPEC contract (taskflow/.planning/.../69-UI-SPEC.md):
- Header container: `<header className="px-6 py-4 border-b border-border flex items-center justify-between">`
- Title: `<h1 className="text-2xl font-semibold">Standup notes</h1>` (lowercase 'n' in notes)
- Date line: `<p className="text-xs text-muted-foreground">{full weekday, day month year}</p>`
- Sync status: `<span className="text-xs text-muted-foreground">• synced {N}m ago</span>`
- Refresh: `<Button variant="ghost" size="sm">Refresh</Button>` (no icon)
- Copy markdown: `<Button variant="default" size="sm">` with Lucide `Copy` icon left of label
- Today placeholder: heading `Today` (`text-xl font-semibold`) + current date muted + `<EmptyState icon={Clock} title="Today section coming soon" subtitle="Planned tasks, pinned issues, and worklog targets will appear here." />`
- 2-column shell: 50/50, vertical 1px `border-border` divider, both columns independently scrollable
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register /standup-notes route, sidebar entry, and breadcrumb label</name>
  <read_first>
    - taskflow/src/routes/routes.tsx (the file being modified — read the lazy-import block ~line 23, the route array, and the withLazy wrapper lines 25-33)
    - taskflow/src/components/app/sidebar-items.ts (the file being modified — read SIDEBAR_NAV_ITEMS, the my-tasks entry at lines 45-49, and getDefaultSidebarItems at line 95)
    - taskflow/src/main.tsx lines 285-298 (the routeLabel function being modified)
    - taskflow/src/routes/routes.test.ts (the existing absence-guard test style this plan adds a presence test to)
    - .planning/phases/69-standup-notes-route-yesterday-recap/69-PATTERNS.md sections for routes.tsx / sidebar-items.ts / main.tsx (exact insertion points)
  </read_first>
  <action>
    Make three config edits and one test edit. (1) In `taskflow/src/routes/routes.tsx`: add `const StandupNotesPage = lazy(() => import('./standup-notes/StandupNotesPage'));` alongside the existing lazy imports, and add `{ path: '/standup-notes', element: withLazy(StandupNotesPage) },` to the route array directly after the `/worklogs` entry. Do not modify the `withLazy` wrapper. (2) In `taskflow/src/components/app/sidebar-items.ts`: add `{ id: 'standup-notes', label: 'Standup Notes', path: '/standup-notes', iconName: 'ClipboardList', section: 'main' }` to `SIDEBAR_NAV_ITEMS` directly after the `my-tasks` entry. Do NOT modify `getDefaultSidebarItems()` — it maps over SIDEBAR_NAV_ITEMS so the new item is auto-included with `visible: true` (no store migration needed). (3) In `taskflow/src/main.tsx`: add `if (pathname.startsWith('/standup-notes')) return 'Standup Notes';` inside `routeLabel()` immediately before `return 'Home';`. (4) In `taskflow/src/routes/routes.test.ts`: add a presence-guard test asserting the source contains a `/standup-notes` route entry and a `StandupNotesPage` lazy import (mirror the existing `/dashboard` preserved-route assertion style — read source file, assert `toMatch(/path:\s*['"]\/standup-notes['"]/)` and `toMatch(/StandupNotesPage/)`). Use `ClipboardList` as the iconName per UI-SPEC. The route placement after `/worklogs` + section `main` places the nav item with Dashboard and My Tasks; no role gate is needed (post-ROLES-06, all items visible to all users).
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- routes</automated>
  </verify>
  <acceptance_criteria>
    - `routes.tsx` contains `import('./standup-notes/StandupNotesPage')` and `path: '/standup-notes'`
    - `sidebar-items.ts` contains `id: 'standup-notes'` with `path: '/standup-notes'` and `section: 'main'`
    - `main.tsx` routeLabel contains `pathname.startsWith('/standup-notes')` returning `'Standup Notes'`
    - `routes.test.ts` contains a passing assertion that `/standup-notes` route + StandupNotesPage import are present
    - `npm run test -- routes` exits 0
  </acceptance_criteria>
  <done>The /standup-notes route is registered with a lazy StandupNotesPage import, a 'Standup Notes' nav item exists in section main (visible to all by default), the breadcrumb label resolves, and a route-presence test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build StandupNotesPage shell + StandupPageHeader + TodayColumnPlaceholder</name>
  <read_first>
    - taskflow/src/routes/worklogs/WorklogsPage.tsx lines 735-760 (header container + `flex flex-col h-full` page-shell pattern to replicate)
    - taskflow/src/components/ui/button.tsx (Button variant/size props — confirm `variant="default" | "ghost"` and `size="sm"`)
    - taskflow/src/components/ui/empty-state.tsx (EmptyState props: icon, title, subtitle)
    - .planning/phases/69-standup-notes-route-yesterday-recap/69-UI-SPEC.md "Page Shell", "Page Header Anatomy (D-11)", "Today column placeholder", "Copywriting Contract", "Typography", "Spacing Scale", "Color" sections (this is the binding visual contract — match exact class names, copy strings, and the lowercase 'Standup notes' title)
    - .planning/phases/69-standup-notes-route-yesterday-recap/69-PATTERNS.md StandupPageHeader + Copy markdown clipboard pattern
  </read_first>
  <action>
    Create three components under `taskflow/src/routes/standup-notes/`.

    (A) `TodayColumnPlaceholder.tsx`: a default-exported component rendering the Today column shell per UI-SPEC — a column heading `Today` in `text-xl font-semibold`, the current date below in `text-xs text-muted-foreground` (full weekday/day/month/year format), and an `EmptyState` with the Lucide `Clock` icon, title `Today section coming soon`, subtitle `Planned tasks, pinned issues, and worklog targets will appear here.` (exact strings from the Copywriting Contract).

    (B) `StandupPageHeader.tsx`: a component accepting props `{ dateLabel: string; syncedMinutesAgo: number | null; onRefresh: () => void; onCopyMarkdown: () => void; copied: boolean }`. Render the header per UI-SPEC Page Header Anatomy: container `<header className="px-6 py-4 border-b border-border flex items-center justify-between">`; left side = `<h1 className="text-2xl font-semibold">Standup notes</h1>` (lowercase 'n') plus the muted `dateLabel` line in `text-xs text-muted-foreground`; right side = a flex row with the sync-status span `• synced {syncedMinutesAgo}m ago` (`text-xs text-muted-foreground`, render only when syncedMinutesAgo is non-null), a `<Button variant="ghost" size="sm" onClick={onRefresh}>Refresh</Button>`, and a `<Button variant="default" size="sm" onClick={onCopyMarkdown}>` containing the Lucide `Copy` icon and the label `copied ? 'Copied!' : 'Copy markdown'`. Accent color (`primary`) is reserved for the Copy markdown button only.

    (C) `StandupNotesPage.tsx`: the default-exported top-level page. Render the WorklogsPage-style outer shell `<div className="flex flex-col h-full">` containing `StandupPageHeader` then a two-column body: a flex row (`flex flex-1 min-h-0 overflow-hidden`) with a left region (50% width, `overflow-auto`, `border-r border-border`) and a right region (50% width, `overflow-auto`) holding `<TodayColumnPlaceholder />`. The left region in THIS plan is a labelled container only: render the `Yesterday` column heading (`text-xl font-semibold`) and reserve a child slot/region for the Yesterday content — leave an explicit comment `{/* Plan 04 mounts YesterdayColumn + four useQuery sections here */}`. Compute a local `yesterdayDate`/`dateLabel` placeholder for the header using `resolveYesterdayDate()` from `@/lib/standup-date` (Plan 01 export) wrapped in `useMemo(() => resolveYesterdayDate(), [])` for now — Plan 04 will replace this with the schedule-aware memo. Wire `StandupPageHeader` props: `dateLabel` = a formatted full-date string derived from `yesterdayDate` (build it with explicit `Intl.DateTimeFormat`/array lookups, NOT a date comparison — display formatting only, never use `toLocaleDateString` for any date *comparison*), `syncedMinutesAgo={null}` for now, `onRefresh` = a no-op stub `() => {}` (Plan 04 wires refetch), `onCopyMarkdown` = a handler that calls `navigator.clipboard.writeText('')` inside try/catch and toggles a `copied` state true→false after 2s, `copied` from local `useState`. Keep all four-section data fetching OUT of this plan — Plan 04 owns it. Use fine-grained store selectors if any store value is read (none required here).
  </action>
  <verify>
    <automated>cd taskflow && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `StandupNotesPage.tsx` has a default export, contains `flex flex-col h-full`, renders `StandupPageHeader` and `TodayColumnPlaceholder`, and contains the comment marker `Plan 04 mounts YesterdayColumn`
    - `StandupNotesPage.tsx` imports `resolveYesterdayDate` from `@/lib/standup-date`
    - `StandupPageHeader.tsx` contains the string `Standup notes` (lowercase 'n'), `Copy markdown`, `Refresh`, and `text-2xl font-semibold`
    - `TodayColumnPlaceholder.tsx` contains `Today section coming soon` and `Planned tasks, pinned issues, and worklog targets will appear here.`
    - Neither StandupNotesPage nor any new file contains `toLocaleDateString` used for date comparison
    - `cd taskflow && npm run build` exits 0 (Phase 59 rule: build, not just tsc)
  </acceptance_criteria>
  <done>StandupNotesPage renders the 50/50 two-column shell with the header and the Today placeholder; the Yesterday region is a labelled container with an explicit Plan-04 mount point; `npm run build` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route registration → router | New lazy route exposed to all users (no auth gate by design, post-ROLES-06) |
| navigator.clipboard | Page-originated clipboard write (placeholder content in this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-07 | Elevation of Privilege | /standup-notes reachable without role gate | accept | Post-ROLES-06 universal-access model (REQUIREMENTS ROLES-06): all routes/nav items are intentionally visible to all users. No sensitive action is exposed by the shell. |
| T-69-08 | Information Disclosure | Clipboard write | accept | This plan writes empty placeholder content via navigator.clipboard inside try/catch; no PII. Real content composed in Plan 04. |
| T-69-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit: zero installs; all UI components pre-installed). No install task → no slopcheck gate required. |
</threat_model>

<verification>
- `cd taskflow && npm run test -- routes` passes (STAND-01 route presence)
- `cd taskflow && npm run build` passes (Phase 59 standing rule — build catches CSS/import issues tsc misses)
- Manual: navigating to /standup-notes renders header + two columns + Today placeholder (covered functionally in Plan 04 human-verify)
- No new dependencies added to package.json
</verification>

<success_criteria>
- `/standup-notes` is registered as a lazy route and reachable from a 'Standup Notes' sidebar item in section main, visible to all users (STAND-01)
- The page renders the 50/50 two-column shell with the page header (title, date, sync status, Refresh, Copy markdown) and the Today placeholder
- The Yesterday region exists as a labelled container with an explicit Plan-04 mount point
- `npm run build` is green
</success_criteria>

<output>
Create `.planning/phases/69-standup-notes-route-yesterday-recap/69-03-SUMMARY.md` when done
</output>
