---
phase: 69-standup-notes-route-yesterday-recap
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/gitlab.ts
  - taskflow/src/services/gitlab.test.ts
autonomous: true
requirements: [STAND-05, STAND-06]
must_haves:
  truths:
    - "fetchUserCommits() returns commits authored by the given user on the target project for the target day"
    - "fetchUserCommits() filters by author (GitLab author param + case-insensitive client-side fallback)"
    - "fetchUserMREvents() returns merged commented + approved MR events for the target user filtered to the target date"
    - "Both functions surface 401/403 as ApiError(source='gitlab') and degrade gracefully on unreachable host"
  artifacts:
    - path: "taskflow/src/services/gitlab.ts"
      provides: "fetchUserCommits, fetchUserMREvents + GitLabCommit, GitLabUserMREvent exports"
      contains: "export async function fetchUserCommits"
    - path: "taskflow/src/services/gitlab.test.ts"
      provides: "Unit coverage for STAND-05 commit fetch/filter and STAND-06 MR event merge/filter"
      contains: "fetchUserMREvents"
  key_links:
    - from: "taskflow/src/services/gitlab.ts (fetchUserCommits)"
      to: "apiFetch('gitlab', ...) /api/v4/projects/:id/repository/commits"
      via: "since/until day-window query"
      pattern: "repository/commits"
    - from: "taskflow/src/services/gitlab.ts (fetchUserMREvents)"
      to: "apiFetch('gitlab', ...) /api/v4/users/:id/events"
      via: "Promise.allSettled commented+approved"
      pattern: "/events\\?action="
---

<objective>
Add the two new GitLab service functions the Yesterday recap depends on: `fetchUserCommits()` (STAND-05 — Git commits I authored on the configured project for the last working day) and `fetchUserMREvents()` (STAND-06 — MR comments + approvals I performed, via the GitLab User Events API).

Purpose: These are independent request-response service functions with no UI dependency, so they belong in Wave 1 alongside the Jira service work. Isolating them lets Plan 04 (the column) consume verified, tested function signatures. They only touch `gitlab.ts` + its test file — zero overlap with any other Wave 1 plan, so they run fully in parallel.
Output: `fetchUserCommits()` + `fetchUserMREvents()` (and their result-shape interfaces) exported from `src/services/gitlab.ts`, covered by new tests in `src/services/gitlab.test.ts`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-standup-notes-route-yesterday-recap/69-RESEARCH.md
@.planning/phases/69-standup-notes-route-yesterday-recap/69-PATTERNS.md

<interfaces>
<!-- Verified from codebase scan. Use these directly. -->

From taskflow/src/services/gitlab.ts (existing, to extend):
```typescript
import { ApiError } from '../lib/api-error';   // line 16
import { apiFetch } from '../lib/apiFetch';     // line 17
export interface GitLabUser { id: number; name: string; username: string; }
export async function validateGitLab(baseUrl: string, token: string): Promise<GitLabUser>; // line 46 — error-handling reference
export async function fetchMRCommits(...): ...; // line 406 — commit-fetch reference
```

validateGitLab error pattern (the contract all new functions follow):
```typescript
let response: Response;
try {
  response = await apiFetch('gitlab', url,
    { headers: { 'PRIVATE-TOKEN': token, 'Content-Type': 'application/json' } },
    'Validate Connection');
} catch {
  throw new Error(`Cannot reach ${baseUrl} — check the base URL`);
}
if (response.status === 401) throw new ApiError('Invalid token or token has expired', 401, 'gitlab');
if (response.status === 403) throw new ApiError('Token valid but lacks required permissions', 403, 'gitlab');
```

From taskflow/src/stores/auth.store.ts (caller context — values passed in by Plan 04):
```typescript
activeGitlabProject: number | null;   // project id used as projectId arg
gitlabUserId: number | null;          // userId arg for the events API
gitlabUsername: string | null;        // author filter for commits
gitlabBaseUrl: string | null;
```

gitlab.test.ts mock setup (the new tests must follow this exact pattern):
```typescript
vi.mock('@tauri-apps/plugin-http', () => ({ fetch: vi.fn() }));
import { fetch as mockFetch } from '@tauri-apps/plugin-http';
// vi.mocked(mockFetch).mockResolvedValue({ ok, status, json: async () => (...) } as Response);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fetchUserCommits() to gitlab.ts</name>
  <read_first>
    - taskflow/src/services/gitlab.ts (the file being modified — read validateGitLab at line 46 for the error-handling contract, fetchMRCommits at line 406 for the commits-endpoint shape, ApiError import at line 16, apiFetch import at line 17)
    - .planning/phases/69-standup-notes-route-yesterday-recap/69-RESEARCH.md Pattern 5 §fetchUserCommits (reference implementation, D-13/D-14) + Pitfall 5 (case-insensitive author matching)
  </read_first>
  <behavior>
    - Success path: GET /api/v4/projects/:projectId/repository/commits returns commits; the function returns the array typed as GitLabCommit[].
    - Author filter: a commit whose author_name case-insensitively equals authorUsername is included; a commit whose author_email contains authorUsername (case-insensitive) is included; an unrelated commit is excluded.
    - The URL contains since={date}T00:00:00.000Z and until={date}T23:59:59.999Z (full UTC day window) and per_page=100.
    - 401 throws ApiError(status=401, source='gitlab'); 403 throws ApiError(status=403, source='gitlab').
    - apiFetch throwing (network) surfaces as `Error('Cannot reach ... — check the base URL')`.
  </behavior>
  <action>
    Add `export interface GitLabCommit { id: string; short_id: string; title: string; message: string; author_name: string; author_email: string; authored_date: string; web_url: string; }` and `export async function fetchUserCommits(baseUrl: string, token: string, projectId: number, date: string, authorUsername: string): Promise<GitLabCommit[]>` to `taskflow/src/services/gitlab.ts`. Implement per RESEARCH.md Pattern 5: strip trailing slash from baseUrl; build `since = \`${date}T00:00:00.000Z\`` and `until = \`${date}T23:59:59.999Z\``; call `GET ${base}/api/v4/projects/${projectId}/repository/commits?since=...&until=...&per_page=100&with_stats=false` with `encodeURIComponent` on the since/until values; use `apiFetch('gitlab', url, { headers: { 'PRIVATE-TOKEN': token, 'Content-Type': 'application/json' } }, 'Load Standup Commits')`; wrap the apiFetch call in `try/catch` that throws `Error(\`Cannot reach ${baseUrl} — check the base URL\`)`; throw `ApiError(..., response.status, 'gitlab')` on 401/403 and a plain `Error` on other non-ok statuses (mirror the validateGitLab contract exactly). Then filter the returned commits client-side (Pitfall 5, case-insensitive): keep a commit when `c.author_name.toLowerCase() === authorUsername.toLowerCase()` OR `c.author_email.toLowerCase().includes(authorUsername.toLowerCase())`. Scope is `activeGitlabProject` only (D-14 resolution: single project). Always use `'PRIVATE-TOKEN'` header — never `Authorization: Bearer`.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- gitlab</automated>
  </verify>
  <acceptance_criteria>
    - `gitlab.ts` contains `export async function fetchUserCommits(` and `export interface GitLabCommit`
    - `fetchUserCommits` body contains `repository/commits`, `'PRIVATE-TOKEN'`, and `apiFetch('gitlab'`
    - `fetchUserCommits` body contains `.toLowerCase()` (case-insensitive author match)
    - `gitlab.test.ts` asserts: included-by-name, included-by-email, excluded-unrelated, 401 → ApiError
    - `npm run test -- gitlab` exits 0
  </acceptance_criteria>
  <done>fetchUserCommits is exported from gitlab.ts, queries the configured project's commits for the day window, filters by author case-insensitively, surfaces 401/403 as ApiError, and is covered by passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fetchUserMREvents() to gitlab.ts</name>
  <read_first>
    - taskflow/src/services/gitlab.ts (read the file again post-Task-1 if needed — reuse the same ApiError/apiFetch imports; place this function after fetchUserCommits)
    - .planning/phases/69-standup-notes-route-yesterday-recap/69-RESEARCH.md Pattern 5 §fetchUserMREvents (reference, D-04/D-05) + Pitfall 4 (GitLab `after` param is exclusive)
    - .planning/phases/69-standup-notes-route-yesterday-recap/69-PATTERNS.md "Parallel fetch pattern" (Promise.allSettled shape)
  </read_first>
  <behavior>
    - Fires two requests in parallel (Promise.allSettled): action=commented and action=approved, both target_type=merge_request, against /api/v4/users/:userId/events.
    - Merges results from both requests into one GitLabUserMREvent[].
    - Filters merged events to `e.created_at.slice(0, 10) === date` and `e.target_type === 'MergeRequest'` — events from a neighboring day (returned because GitLab `after` is exclusive/loose) are dropped.
    - If one of the two requests rejects or returns non-ok, the other request's events are still returned (allSettled isolation).
    - Returns an empty array when both requests return zero same-day events (no throw).
  </behavior>
  <action>
    Add `export interface GitLabUserMREvent { id: number; action_name: 'commented' | 'approved'; target_type: 'MergeRequest'; target_id: number; target_iid: number; target_title: string; created_at: string; project_id: number; }` and `export async function fetchUserMREvents(baseUrl: string, token: string, userId: number, date: string): Promise<GitLabUserMREvent[]>` to `taskflow/src/services/gitlab.ts`. Implement per RESEARCH.md Pattern 5 (D-04, D-05): strip trailing slash; define `headers = { 'PRIVATE-TOKEN': token, 'Content-Type': 'application/json' }`. To handle Pitfall 4 (the events `after` param is exclusive), compute `dayBefore` = the date one calendar day before `date` (via a Date minus 1 then `.toISOString().slice(0,10)`) and pass `after=${dayBefore}` so yesterday's events are included. Fire both requests with `Promise.allSettled([ apiFetch('gitlab', \`${base}/api/v4/users/${userId}/events?action=commented&target_type=merge_request&after=${dayBefore}&per_page=100\`, { headers }, 'Load Standup MR Events'), apiFetch('gitlab', \`${base}/api/v4/users/${userId}/events?action=approved&target_type=merge_request&after=${dayBefore}&per_page=100\`, { headers }, 'Load Standup MR Events') ])`. For each settled result that is `fulfilled` AND `value.ok`, parse the JSON and push entries that pass `e.created_at.slice(0, 10) === date && e.target_type === 'MergeRequest'` into a combined array. Return the combined array. Do NOT throw on a single failed request — allSettled isolates failures per D-04's graceful-degradation intent. Use `apiFetch('gitlab', ...)` and `.slice(0, 10)` (never `toLocaleDateString()`).
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- gitlab</automated>
  </verify>
  <acceptance_criteria>
    - `gitlab.ts` contains `export async function fetchUserMREvents(` and `export interface GitLabUserMREvent`
    - `fetchUserMREvents` body contains `Promise.allSettled`, `action=commented`, `action=approved`, and `/events?`
    - `fetchUserMREvents` body contains `.slice(0, 10)` (same-day client filter)
    - `gitlab.test.ts` asserts: merge of both event types, same-day filtering drops neighbor-day events, one-request-failure isolation still returns the other's events
    - `npm run test -- gitlab` exits 0
    - `cd taskflow && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>fetchUserMREvents is exported from gitlab.ts, merges commented+approved MR events via Promise.allSettled, filters to the exact target date, isolates per-request failures, and is covered by passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri webview → GitLab API (apiFetch) | Outbound authenticated API calls; token from Stronghold |
| date/userId/projectId params → URL string | Internally computed values interpolated into URL query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-04 | Tampering | URL param injection via date/since/until in fetchUserCommits | mitigate | `date` is internally computed (`toISOString().slice(0,10)`), not user input; since/until values are `encodeURIComponent`-encoded; projectId/userId are numeric from auth store. |
| T-69-05 | Information Disclosure | GitLab PAT leak | mitigate | Token passed as `'PRIVATE-TOKEN'` header from `readSecret('gitlab-pat')`; never logged, never persisted to Zustand, never included in returned objects. |
| T-69-06 | Denial of Service | Events API fan-out | accept | Only two parallel requests per call (commented + approved), each `per_page=100`; allSettled bounds failure blast radius. Daily-standup volume is low. |
| T-69-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this phase (RESEARCH Package Legitimacy Audit: zero installs). No install task → no slopcheck gate required. |
</threat_model>

<verification>
- `cd taskflow && npm run test -- gitlab` passes (STAND-05 commit fetch/filter, STAND-06 MR event merge/filter)
- `cd taskflow && npx tsc --noEmit` passes (no type regressions in gitlab.ts)
- No new dependencies added to package.json
</verification>

<success_criteria>
- `fetchUserCommits()` returns the configured project's commits for the day window, filtered by author case-insensitively, surfacing 401/403 as ApiError (STAND-05)
- `fetchUserMREvents()` merges commented + approved MR events and filters them to the exact target date, isolating per-request failures (STAND-06)
- All new unit tests pass; `tsc --noEmit` is green
</success_criteria>

<output>
Create `.planning/phases/69-standup-notes-route-yesterday-recap/69-02-SUMMARY.md` when done
</output>
