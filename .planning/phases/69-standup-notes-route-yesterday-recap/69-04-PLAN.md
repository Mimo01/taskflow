---
phase: 69-standup-notes-route-yesterday-recap
plan: 04
type: execute
wave: 2
depends_on: ["69-01", "69-02", "69-03"]
files_modified:
  - taskflow/src/routes/standup-notes/StandupNotesPage.tsx
  - taskflow/src/routes/standup-notes/YesterdayColumn.tsx
  - taskflow/src/routes/standup-notes/IssueActivityGroup.tsx
  - taskflow/src/routes/standup-notes/OtherCommitsGroup.tsx
  - taskflow/src/routes/standup-notes/StandaloneMrGroup.tsx
autonomous: false
requirements: [STAND-02, STAND-03, STAND-04, STAND-05, STAND-06]
must_haves:
  truths:
    - "Yesterday resolves to the last working day, skipping weekends + Tempo holidays (when Tempo enabled) via the schedule-aware memo"
    - "Four data sources load independently via four useQuery hooks; one source failing/disabled does not block the others"
    - "Yesterday entries are grouped by Jira issue: worklogs + Jira transitions/comments + commits + MR events join under the issue key"
    - "Commits with no Jira key (message then branch fallback) land in the 'Other commits' catch-all group"
    - "MR events not linked to any issue render as standalone MR groups (MR IID heading)"
    - "Each section renders loading/empty/error/populated states; disabled integrations show an inline muted notice, not ErrorState"
    - "Copy markdown writes a formatted standup summary to the clipboard; Refresh refetches all four queries"
  artifacts:
    - path: "taskflow/src/routes/standup-notes/YesterdayColumn.tsx"
      provides: "Stat line + data join + ordered group list rendering"
      contains: "YesterdayColumn"
      min_lines: 60
    - path: "taskflow/src/routes/standup-notes/IssueActivityGroup.tsx"
      provides: "Per-issue group header + typed sub-item list"
      contains: "IssueActivityGroup"
    - path: "taskflow/src/routes/standup-notes/OtherCommitsGroup.tsx"
      provides: "Catch-all commits group (D-08)"
      contains: "Other commits"
    - path: "taskflow/src/routes/standup-notes/StandaloneMrGroup.tsx"
      provides: "MR-not-linked-to-issue group (D-09)"
      contains: "StandaloneMrGroup"
    - path: "taskflow/src/routes/standup-notes/StandupNotesPage.tsx"
      provides: "Four useQuery hooks + schedule-aware yesterdayDate memo + refetch-all + real Copy markdown"
      contains: "useQuery"
  key_links:
    - from: "taskflow/src/routes/standup-notes/StandupNotesPage.tsx"
      to: "fetchWorklogs / fetchYesterdayJiraActivity / fetchUserCommits / fetchUserMREvents"
      via: "four independent useQuery hooks (token NOT in queryKey)"
      pattern: "useQuery\\("
    - from: "taskflow/src/routes/standup-notes/StandupNotesPage.tsx"
      to: "resolveYesterdayDate + fetchUserSchedule"
      via: "useMemo(() => resolveYesterdayDate(scheduleData ?? undefined), [scheduleData])"
      pattern: "resolveYesterdayDate\\(scheduleData"
    - from: "taskflow/src/routes/standup-notes/YesterdayColumn.tsx"
      to: "IssueActivityGroup / OtherCommitsGroup / StandaloneMrGroup"
      via: "grouped data join render"
      pattern: "IssueActivityGroup"
---

<objective>
Complete the Yesterday recap: wire the four independent `useQuery` hooks (Tempo worklogs, Jira activity, GitLab commits, GitLab MR events) plus the schedule-aware `yesterdayDate` memo into `StandupNotesPage`, then build `YesterdayColumn` (data-join by Jira issue + summary stat line + ordered group list) and its three child group components (`IssueActivityGroup`, `OtherCommitsGroup`, `StandaloneMrGroup`). Implement per-section loading/empty/error states, the disabled-integration inline notice, the real Copy markdown generator, and Refresh-all.

Purpose: This is the data + rendering plan that makes STAND-02 through STAND-06 observable. It depends on Plan 01 (date utility + Jira service), Plan 02 (GitLab services), and Plan 03 (page shell + mount point) — hence Wave 2. It is the only plan with graceful-degradation logic, so it carries the human-verify checkpoint for the four-section behavior.
Output: four new group/column components and the fully wired `StandupNotesPage`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-standup-notes-route-yesterday-recap/69-RESEARCH.md
@.planning/phases/69-standup-notes-route-yesterday-recap/69-PATTERNS.md
@.planning/phases/69-standup-notes-route-yesterday-recap/69-UI-SPEC.md

<interfaces>
<!-- Contracts produced by Plans 01-03 (Wave 1). Use these directly. -->

From @/lib/standup-date (Plan 01):
```typescript
export function resolveYesterdayDate(tempoSchedule?: Map<string, ScheduleDayType>): string;
export function getScheduleLookbackRange(): { from: string; to: string };
export function extractJiraKeyFromMessage(message: string): string | null;
export function extractJiraKeyFromBranch(branchName: string): string | null;
```

From @/services/jira (Plan 01):
```typescript
export interface JiraActivityItem {
  issueKey: string; summary: string;
  transitions: Array<{ fromStatus: string; toStatus: string; at: string }>;
  comments: Array<{ body: string; at: string }>;
}
export async function fetchYesterdayJiraActivity(baseUrl, token, projectKey, date, jiraUsername): Promise<JiraActivityItem[]>;
```

From @/services/gitlab (Plan 02):
```typescript
export interface GitLabCommit { id; short_id; title; message; author_name; author_email; authored_date; web_url; }
export interface GitLabUserMREvent { id; action_name: 'commented' | 'approved'; target_type: 'MergeRequest'; target_id; target_iid; target_title; created_at; project_id; }
export async function fetchUserCommits(baseUrl, token, projectId, date, authorUsername): Promise<GitLabCommit[]>;
export async function fetchUserMREvents(baseUrl, token, userId, date): Promise<GitLabUserMREvent[]>;
```

From @/services/tempo (existing):
```typescript
export interface TempoWorklog { issue: { key: string; summary?: string }; author: { name: string }; timeSpentSeconds: number; dateStarted: string; comment?: string; }
export async function fetchWorklogs(baseUrl, token, usernames, from, to): Promise<TempoWorklog[]>;
export async function fetchUserSchedule(baseUrl, token, from, to, userKey): Promise<Map<string, ScheduleDayType>>;
```

From @/stores/auth.store + @/stores/settings.store:
```typescript
jiraBaseUrl, gitlabBaseUrl: string | null
activeJiraProject: string | null; activeGitlabProject: number | null
jiraUsername, jiraUserKey, gitlabUsername: string | null; gitlabUserId: number | null
useSettingsStore((s) => s.tempoEnabled): boolean   // fine-grained selector ONLY
```

From Plan 03 (the shell to extend): StandupNotesPage.tsx renders the header + 2-column shell with a `{/* Plan 04 mounts YesterdayColumn ... */}` marker in the left region; StandupPageHeader accepts { dateLabel, syncedMinutesAgo, onRefresh, onCopyMarkdown, copied }.

UI components (pre-installed): `EmptyState` (@/components/ui/empty-state), `ErrorState` (@/components/ui/error-state), `Skeleton` (@/components/ui/skeleton), `Badge` (@/components/ui/badge). Lucide icons: GitBranch (commits), ArrowRight (transitions), MessageSquare (MR comments), CheckCircle (approvals), MessageCircle (Jira comments), GitMerge (MR opened/merged), CircleDot/Bug/ChevronRight/Layers (issue type), Clock (empty).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire four independent useQuery hooks + schedule-aware yesterdayDate into StandupNotesPage</name>
  <files>taskflow/src/routes/standup-notes/StandupNotesPage.tsx</files>
  <read_first>
    - taskflow/src/routes/standup-notes/StandupNotesPage.tsx (the Plan 03 shell being extended — read it fully before editing; preserve the header + 2-column structure and replace the placeholder memo + stub handlers)
    - taskflow/src/routes/worklogs/WorklogsPage.tsx lines 1-12 + 255 + 305-364 + 490-495 (T-62-06 token-not-in-queryKey rule, fine-grained tempoEnabled selector, token-loading useEffect, the worklogs useQuery, and the schedule useQuery with 24h staleTime)
    - .planning/phases/69-standup-notes-route-yesterday-recap/69-RESEARCH.md Pattern 2 (token loading), Pattern 3 (all four queryKey shapes), Pattern 4 (yesterdayDate memo), Pitfall 1/2 (queryKey + memo ordering)
    - .planning/phases/69-standup-notes-route-yesterday-recap/69-PATTERNS.md "Token Loading", "T-62-06", "useMemo for derived date" sections
  </read_first>
  <action>
    Extend `taskflow/src/routes/standup-notes/StandupNotesPage.tsx`. (1) Read auth fields via `useAuthStore` and `const tempoEnabled = useSettingsStore((s) => s.tempoEnabled)` (fine-grained selector — never destructure the whole store). (2) Add two token-loading `useEffect`/`useState` pairs (jiraToken from `readSecret('jira-pat')` keyed on jiraBaseUrl; gitlabToken from `readSecret('gitlab-pat')` keyed on gitlabBaseUrl) exactly per RESEARCH Pattern 2. (3) Add the Tempo schedule query first: `useQuery({ queryKey: ['standup','schedule', jiraBaseUrl, jiraUserKey ?? ''], queryFn: () => fetchUserSchedule(jiraBaseUrl!, jiraToken!, getScheduleLookbackRange().from, getScheduleLookbackRange().to, jiraUserKey!), enabled: !!jiraBaseUrl && !!jiraToken && !!jiraUserKey && tempoEnabled, staleTime: 24*60*60*1000 })`. (4) Derive the date: `const yesterdayDate = useMemo(() => resolveYesterdayDate(scheduleData ?? undefined), [scheduleData])` (Pitfall 2 — memo on scheduleData so Tempo-enabled users get holiday-skip). (5) Add the four data queries per RESEARCH Pattern 3, with `staleTime: 5*60*1000` and the token NEVER in queryKey (T-62-06): `tempoQuery` (queryKey `['standup','tempo', jiraBaseUrl, yesterdayDate, jiraUsername ?? '']`, calls `fetchWorklogs(jiraBaseUrl!, jiraToken!, [jiraUsername!], yesterdayDate, yesterdayDate)`, enabled requires `tempoEnabled`); `jiraActivityQuery` (queryKey `['standup','jira', jiraBaseUrl, activeJiraProject, yesterdayDate, jiraUsername ?? '']`, calls `fetchYesterdayJiraActivity(jiraBaseUrl!, jiraToken!, activeJiraProject!, yesterdayDate, jiraUsername!)`); `commitsQuery` (queryKey `['standup','commits', gitlabBaseUrl, activeGitlabProject, yesterdayDate, gitlabUsername ?? '']`, calls `fetchUserCommits(gitlabBaseUrl!, gitlabToken!, activeGitlabProject!, yesterdayDate, gitlabUsername!)`); `mrEventsQuery` (queryKey `['standup','mr-events', gitlabBaseUrl, gitlabUserId, yesterdayDate]`, calls `fetchUserMREvents(gitlabBaseUrl!, gitlabToken!, gitlabUserId!, yesterdayDate)`). Each `enabled` guard checks the relevant base URL + token + identity fields + `!!yesterdayDate`. (6) Build a `handleRefresh` that calls `.refetch()` on all four data queries (UI-SPEC: stale data stays visible during refetch — do not reset). (7) Replace the Plan-03 `dateLabel` placeholder with a label derived from `yesterdayDate` (display formatting only). (8) Pass `tempoEnabled`, all four query objects (data/isLoading/isError/error/refetch), `yesterdayDate`, and the commit/branch data down to `<YesterdayColumn />` mounted at the Plan-03 marker. (9) Leave the real Copy-markdown generation to Task 2's `generateMarkdown`, importing it once available; for this task pass a working `onCopyMarkdown` that calls `navigator.clipboard.writeText(generateMarkdown(...))`. Do NOT combine sources into one query (RESEARCH anti-pattern) — four independent queries are mandatory for graceful degradation.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `StandupNotesPage.tsx` contains exactly four data `useQuery` calls plus one schedule `useQuery` (grep `useQuery(` count is 5)
    - No `queryKey` array in the file contains `jiraToken` or `gitlabToken` (T-62-06): `grep -n "queryKey" StandupNotesPage.tsx` shows no token identifiers inside the arrays
    - `StandupNotesPage.tsx` contains `resolveYesterdayDate(scheduleData ?? undefined)` inside a `useMemo`
    - `StandupNotesPage.tsx` contains `useSettingsStore((s) => s.tempoEnabled)` (fine-grained selector)
    - `StandupNotesPage.tsx` contains a refresh handler calling `.refetch()` on all four data queries
    - `cd taskflow && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>StandupNotesPage owns five useQuery hooks (schedule + four sources), derives yesterdayDate from schedule data via useMemo, never places tokens in queryKey, exposes a refetch-all handler, and passes all query state to YesterdayColumn; tsc is green.</done>
</task>

<task type="auto">
  <name>Task 2: Build YesterdayColumn (data join + stat line + Copy markdown) and the three group components</name>
  <files>taskflow/src/routes/standup-notes/YesterdayColumn.tsx, taskflow/src/routes/standup-notes/IssueActivityGroup.tsx, taskflow/src/routes/standup-notes/OtherCommitsGroup.tsx, taskflow/src/routes/standup-notes/StandaloneMrGroup.tsx</files>
  <read_first>
    - taskflow/src/routes/standup-notes/StandupNotesPage.tsx (post-Task-1 — read the props YesterdayColumn now receives)
    - taskflow/src/routes/worklogs/WorklogsPage.tsx lines 92-98 (hours formatting) + 507-656 (Map-based grouping useMemo) + 953-1012 (loading/error/empty cascade) + 1054-1177 (epic/story/no-epic row JSX the group components adapt)
    - taskflow/src/components/ui/empty-state.tsx, error-state.tsx, skeleton.tsx, badge.tsx (props for the four section states)
    - @/lib/standup-date (extractJiraKeyFromMessage + extractJiraKeyFromBranch — the commit grouping fallback chain D-08)
    - .planning/phases/69-standup-notes-route-yesterday-recap/69-RESEARCH.md Pattern 6 (Jira key extraction + branch-refs note), Pattern 7 (Copy markdown), Pattern 8 (disabled-integration inline notice)
    - .planning/phases/69-standup-notes-route-yesterday-recap/69-UI-SPEC.md "Issue group structure (D-07)", "Summary stat line (D-10)", "States per Section", "Copywriting Contract" (exact icon assignments, stat-line template, empty/error copy strings) — binding contract
  </read_first>
  <action>
    Create four components under `taskflow/src/routes/standup-notes/`.

    (A) `IssueActivityGroup.tsx`: props `{ issueKey: string; summary: string; issueType?: string; totalSeconds: number; subItems: Array<{ kind: 'commit' | 'transition' | 'mr-comment' | 'approval' | 'jira-comment' | 'mr-open'; label: string }> }`. Render the D-07 group header — `[type icon] [issueKey] [summary] [Xh right-aligned]` — using `text-sm font-semibold` for the header (per UI-SPEC Typography), a `Badge` for the issue type, and hours via `(totalSeconds/3600).toFixed(1) + 'h'` right-aligned (omit when 0). Render sub-items at `pl-8`, `text-xs`, each with its Lucide icon by `kind` (commit→GitBranch, transition→ArrowRight, mr-comment→MessageSquare, approval→CheckCircle, jira-comment→MessageCircle, mr-open→GitMerge) per the UI-SPEC icon table.

    (B) `OtherCommitsGroup.tsx`: props `{ commits: GitLabCommit[] }`. Render a muted/italic catch-all group (adapt the NO_EPIC pattern) with heading `Other commits` and subtitle `Commits without a linked Jira issue` (exact copy), each commit as a `GitBranch` sub-item showing `title` + `short_id`.

    (C) `StandaloneMrGroup.tsx`: props `{ iid: number; title: string; events: GitLabUserMREvent[] }`. Render a group whose heading is `!{iid} {title}` (D-09) with a `GitMerge` icon at the issue-icon level, sub-items labeled "Commented on !{iid}" / "Approved !{iid}" per `action_name` (D-05).

    (D) `YesterdayColumn.tsx`: props = the four query objects + `tempoEnabled` + `yesterdayDate` (passed from Task 1). It (1) builds the issue-keyed join in a `useMemo`: a `Map<string, group>` keyed by Jira issue key. Seed from Tempo worklogs (`w.issue.key`, accumulate `timeSpentSeconds`, summary from `w.issue.summary`). Add Jira `transitions`/`comments` under their `issueKey`. For each commit, resolve a key via the D-08 fallback chain: `extractJiraKeyFromMessage(commit.message)` → if null, leave for branch fallback (see note), → if still null, route to the Other-commits bucket. For each MR event, attempt to extract a key from `target_title`; if none, route to a standalone-MR bucket keyed by `target_iid`. Use `.slice(0,10)` for any date comparison — never `toLocaleDateString()`. (2) Renders the four section states per UI-SPEC "States per Section": for each source, if its integration is disabled (e.g. `!tempoEnabled` for Tempo) render the inline muted notice `Tempo is disabled. Enable it in Settings → Integrations.` (NOT ErrorState — RESEARCH Pattern 8); else run the cascade `isError → <ErrorState error={...} onRetry={refetch} viewName="..."/>`, `isLoading && !data → 3× <Skeleton className="h-4 w-full" /> with gap-2`, `data empty → <EmptyState>` with the section-specific copy from the Copywriting Contract, else the populated group list. (3) Renders the D-10 summary stat line beneath the `Yesterday` heading in `text-xs text-muted-foreground` only when at least one source has data: `{X}h logged across {N} stories · {N} commits · {N} MR events` (middle-dot `·` separators, hidden entirely when all empty). (4) Renders the joined groups in order: issue groups, then standalone MR groups, then the Other-commits group last. When ALL four sources are empty/disabled, render the full-column empty state title `Nothing to recap` / subtitle `All integrations returned empty results for {date}.`. (5) Export a `generateMarkdown(groups, date): string` helper (Pattern 7 — planner discretion format: `## Yesterday (date)` then per-group `### {issueKey}: {summary}` + `- {sub-item label}` lines) that StandupNotesPage's onCopyMarkdown consumes.

    Branch-fallback note (D-08 step 2 / D-14): for commits with no Jira key in the message, the branch name is not present on the commit object returned by `fetchUserCommits`. Resolve it by the message only in this pass and route no-key commits to "Other commits"; do NOT add per-commit `/refs` API calls in this plan (avoids the Pitfall 7 fan-out and keeps Plan 04 within the 2-source-join budget). Document this in the SUMMARY as the chosen D-14 interpretation (message-extraction only; branch-refs deferred as a known limitation).
  </action>
  <verify>
    <automated>cd taskflow && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under `src/routes/standup-notes/` with the named exports/components
    - `YesterdayColumn.tsx` contains `extractJiraKeyFromMessage`, an `EmptyState`, an `ErrorState`, a `Skeleton`, and the inline disabled notice string `Tempo is disabled. Enable it in Settings → Integrations.`
    - `YesterdayColumn.tsx` contains the stat-line template tokens (`logged across`, `commits`, `MR events`) and the full-column empty title `Nothing to recap`
    - `YesterdayColumn.tsx` exports `generateMarkdown`
    - `OtherCommitsGroup.tsx` contains `Other commits` and `Commits without a linked Jira issue`
    - `IssueActivityGroup.tsx` imports the Lucide icons GitBranch, ArrowRight, MessageSquare, CheckCircle, MessageCircle, GitMerge
    - No new file uses `toLocaleDateString` for date comparison
    - `cd taskflow && npm run build` exits 0
  </acceptance_criteria>
  <done>YesterdayColumn joins all four sources by Jira issue, renders per-section loading/empty/error/disabled states, the D-10 stat line, ordered issue → standalone-MR → other-commits groups, the full-column empty state, and a generateMarkdown helper; the three group components render per the UI-SPEC icon/copy contract; `npm run build` is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Yesterday recap four-section behavior + Copy markdown</name>
  <files>taskflow/ (verification only — no source edits in this task)</files>
  <action>Verification-only task. Run the app and walk through the how-to-verify steps below; no code changes are made here. Resume on "approved" or fix-and-recheck if issues are reported.</action>
  <what-built>
    The complete Standup Notes page: `/standup-notes` route + sidebar entry, the two-column shell, and the fully populated Yesterday recap aggregating Tempo worklogs, Jira changelog activity, Git commits, and MR activity grouped by Jira issue — with per-section loading/empty/error/disabled states, the summary stat line, the Copy markdown button, and Refresh. The Today column shows the Phase 70 placeholder.
  </what-built>
  <how-to-verify>
    1. Run `cd taskflow && npm run tauri dev` (or the project's standard dev command).
    2. Confirm a "Standup Notes" item appears in the sidebar under the main section (with Dashboard / My Tasks). Click it → the URL is `/standup-notes`.
    3. Confirm the page header shows "Standup notes" (lowercase 'n'), the resolved date, "synced Xm ago · Refresh", and a primary "Copy markdown" button. The page is split 50/50 with a vertical divider; the Today column shows "Today section coming soon".
    4. Verify the resolved "Yesterday" date: on a Monday it should be Friday; weekends are skipped. If Tempo is enabled and a holiday fell on the last working day, it should skip the holiday too.
    5. Confirm the Yesterday column shows up to four kinds of content grouped by Jira issue: Tempo hours (right-aligned per group), status transitions (arrow icon), Jira comments (chat icon), Git commits (branch icon), and MR activity (commented/approved). Commits with no Jira key fall under "Other commits". MRs not linked to an issue appear as standalone "!IID" groups.
    6. Verify graceful degradation: disable Tempo in Settings → Integrations and reload — the Tempo section shows the inline "Tempo is disabled…" notice (NOT a red error), and the other three sections still load. If a source has zero items for the day, that section shows its empty state.
    7. Click "Copy markdown" → button reads "Copied!" for ~2s; paste into any text field and confirm a readable markdown summary of the Yesterday groups is on the clipboard.
    8. Click "Refresh" → all sections refetch; existing data stays visible during the refetch (no full skeleton flash).
  </how-to-verify>
  <verify><human-check>Operator confirms STAND-02 date resolution, STAND-03..06 four-section rendering, graceful degradation, Copy markdown, and Refresh per the steps above.</human-check></verify>
  <done>Operator types "approved" confirming the page renders correctly, degrades gracefully per section, resolves the yesterday date correctly, and Copy markdown + Refresh work; or issues are reported and fixed before proceeding.</done>
  <resume-signal>Type "approved" or describe any section that renders incorrectly, fails to degrade gracefully, or mis-resolves the yesterday date.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri webview → Jira/GitLab/Tempo APIs | Four independent authenticated fetches; tokens from Stronghold |
| navigator.clipboard | Real standup summary written to clipboard via Copy markdown |
| API response → issue-grouping join | Untrusted external data (commit messages, MR titles, summaries) rendered into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-09 | Information Disclosure | Tokens leaking via queryKey cache entries | mitigate | T-62-06: jiraToken/gitlabToken never appear in any queryKey; `enabled` guards gate on `!!token`; fresh token used inside queryFn. Verified by acceptance criterion grep. |
| T-69-10 | Tampering | XSS via commit message / MR title / Jira summary rendered in groups | mitigate | All external strings rendered as React text children (auto-escaped) — never via `dangerouslySetInnerHTML`. No raw HTML injection path. |
| T-69-11 | Denial of Service | One slow/failing source blocking the page | mitigate | Four independent useQuery hooks with isolated enabled/error states; a single source failing renders its own ErrorState while others load (graceful degradation, STAND-03..06). |
| T-69-12 | Information Disclosure | Clipboard content (standup summary may include internal issue keys/comments) | accept | User explicitly invokes Copy markdown (D-12); content is the user's own activity summary; clipboard write is intentional and user-initiated. |
| T-69-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit: zero installs; clipboard uses navigator.clipboard, no plugin). No install task → no slopcheck gate required. |
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` passes after Task 1
- `cd taskflow && npm run build` passes after Task 2 (Phase 59 standing rule)
- `cd taskflow && npm run test` full suite green (no regressions in routes/jira/gitlab/standup-date tests)
- Human-verify checkpoint (Task 3) confirms STAND-02 date resolution + STAND-03..06 four-section behavior + graceful degradation + Copy markdown + Refresh
- No new dependencies added to package.json
</verification>

<success_criteria>
- Yesterday resolves to the last working day (weekend + Tempo-holiday skip) via the schedule-aware memo (STAND-02)
- Four data sources load independently; one disabled/failing source does not block the others (STAND-03, STAND-04, STAND-05, STAND-06)
- Tempo worklogs, Jira transitions+comments, Git commits, and MR comments+approvals render grouped by Jira issue, with Other-commits catch-all and standalone-MR groups
- Each section renders loading/empty/error states; disabled integrations show an inline muted notice (not ErrorState)
- Copy markdown writes a formatted summary; Refresh refetches all four queries keeping stale data visible
- `npm run build` + full test suite green; human-verify approved
</success_criteria>

<output>
Create `.planning/phases/69-standup-notes-route-yesterday-recap/69-04-SUMMARY.md` when done
</output>
