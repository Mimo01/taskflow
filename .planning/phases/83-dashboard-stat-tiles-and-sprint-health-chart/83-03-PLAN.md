---
phase: 83-dashboard-stat-tiles-and-sprint-health-chart
plan: 03
type: execute
wave: 3
depends_on: ["83-01", "83-02"]
files_modified:
  - taskflow/src/routes/dashboard/index.tsx
  - taskflow/src/routes/dashboard/SmokeTestChart.tsx
  - taskflow/src/routes/dashboard/DashboardSprintCard.tsx
  - taskflow/src/routes/dashboard/DashboardInProgressCard.tsx
  - taskflow/src/routes/dashboard/SmokeTestChart.test.tsx
  - taskflow/src/routes/dashboard/DashboardSprintCard.test.tsx
  - taskflow/src/routes/dashboard/DashboardInProgressCard.test.tsx
  - taskflow/src/routes/dashboard/index.test.tsx
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-07]
must_haves:
  truths:
    - "Dashboard no longer shows DashboardSprintCard, DashboardInProgressCard, or the SmokeTestChart scaffold; those files are deleted"
    - "The gradient hero greeting + en-GB date and the next-release countdown are retained"
    - "Dashboard renders the 4-tile stat grid (Open/InProgress/Overdue/SP Done) and the sprint-health section"
    - "Stat-tiles row and sprint-health section each have their own skeleton/error/empty state — one failing section does not blank the other"
  artifacts:
    - path: "taskflow/src/routes/dashboard/index.tsx"
      provides: "Rewritten Dashboard: hero + stat tiles + sprint health + release countdown"
      contains: "SprintHealthSection"
  key_links:
    - from: "taskflow/src/routes/dashboard/index.tsx"
      to: "computePersonalTileCounts / computeSpDone"
      via: "import from ./dashboardMetrics for the tile-row derivation"
      pattern: "from './dashboardMetrics'"
    - from: "taskflow/src/routes/dashboard/index.tsx"
      to: "StatTile / SprintHealthSection / DashboardReleaseCard"
      via: "component composition in the route body"
      pattern: "SprintHealthSection"
user_setup: []
---

<objective>
Integrate Phase 83: rewrite `dashboard/index.tsx` to remove the 3-card grid and the `SmokeTestChart` mount, retain the gradient hero (+ en-GB date) and the next-release countdown, and render the new 4-tile stat grid + `SprintHealthSection`. Delete `SmokeTestChart.tsx`, `DashboardSprintCard.tsx`, and `DashboardInProgressCard.tsx` along with their tests. The Wave-1 widget-removal guard turns GREEN here. Close with a human UAT in a real Tauri WebKit build confirming the new layout renders correctly with no new network calls.

Purpose: Land the user-visible Dashboard refactor and prove DASH-01/02/03/07 end-to-end.
Output: Rewritten `index.tsx`, three deleted components + tests, updated `index.test.tsx`, green widget-removal guard, human-verified Tauri render.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-CONTEXT.md
@.planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-PATTERNS.md
@.planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-UI-SPEC.md
@taskflow/src/routes/dashboard/dashboardMetrics.ts
@taskflow/src/routes/dashboard/StatTile.tsx
@taskflow/src/routes/dashboard/SprintHealthSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite dashboard/index.tsx (remove 3-card grid + SmokeTestChart; add stat tiles + SprintHealthSection; retain hero + release countdown)</name>
  <read_first>
    - taskflow/src/routes/dashboard/index.tsx (retain lines 1-101 hero/date/firstName/useBoardId/jiraToken effect; remove imports of the 3 cards + SmokeTestChart; remove lines 103-105 SmokeTestChart mount + 107-130 3-card grid)
    - taskflow/src/routes/dashboard/index.test.tsx (existing render-test structure to update)
    - taskflow/src/routes/dashboard/DashboardReleaseCard.tsx (retained countdown component + its props)
    - .planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-PATTERNS.md (§index.tsx — exact retain/remove line list, new imports, stat-tiles row pattern with showTileSkeleton/tileError/refetchTiles, new sections layout)
    - .planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-UI-SPEC.md (Layout assembly, grid grid-cols-2 sm:grid-cols-4 gap-4, tile icons/colors, copy)
    - taskflow/src/routes/dashboard/dashboardMetrics.ts, StatTile.tsx, SprintHealthSection.tsx (the consumed modules)
    - taskflow/src/stores/auth.store.ts (jiraUserDisplayName), useSettingsStore (storyPointsFieldKey)
  </read_first>
  <action>
    Rewrite `taskflow/src/routes/dashboard/index.tsx` per 83-PATTERNS.md §index.tsx:
    1. RETAIN unchanged: getTimeGreeting, AMBIENT_CURVES, auth/settings/readSecret/useEffect/useState imports, useOutletContext, useBoardId, the jiraToken readSecret effect, the en-GB `today` formatting, firstName parse, and the entire hero `<section>`.
    2. REMOVE imports of `DashboardInProgressCard`, `DashboardSprintCard`, `SmokeTestChart`; REMOVE the `<SmokeTestChart />` mount block and the 3-card grid block.
    3. ADD imports: `StatTile`, `SprintHealthSection`, `DashboardReleaseCard` (retained for countdown), `{ computePersonalTileCounts, computeSpDone }` from `./dashboardMetrics`, `useQuery`, `fetchSprintIssues`, lucide icons `{ Activity, CheckCircle2, Clock, Zap }`, `useDelayedLoading`, `ErrorState`.
    4. In the component, read `jiraUserDisplayName` from auth store and `storyPointsFieldKey` from settings store. Mount ONE sprint-board `useQuery` for the tile row using the sacred key `['jira-issues','sprint-board',activeJiraProject,storyPointsFieldKey]`, `queryFn: () => fetchSprintIssues(jiraBaseUrl, jiraToken, activeJiraProject, false, storyPointsFieldKey)`, `staleTime: 30_000`, `enabled: !!jiraBaseUrl && !!jiraToken && !!activeJiraProject` (this dedupes against the SprintHealthSection query — same key). Derive `tileCounts = computePersonalTileCounts(sprintIssues, jiraUserDisplayName ?? '', new Date().toISOString().slice(0,10))` and `spDone = computeSpDone(sprintIssues, storyPointsFieldKey)`. `showTileSkeleton = useDelayedLoading(tileLoading)`.
    5. Render layout below the hero: a stat-tiles row `<div className="relative px-6 pb-6">` with its own skeleton/error/data branches (4 skeleton tiles when showTileSkeleton; `ErrorState` when tileError; otherwise the 4-tile grid `grid grid-cols-2 sm:grid-cols-4 gap-4` with the four `StatTile`s — Open=Activity/text-sky-500, In Progress=Zap/text-amber-500, Overdue=Clock/text-destructive with destructive valueClass when >0, SP Done=CheckCircle2/text-green-500). Then `<SprintHealthSection ... />` in its own `px-6 pb-6` wrapper (passing jiraBaseUrl/jiraToken/activeJiraProject/storyPointsFieldKey/boardId). Then `<DashboardReleaseCard ... />` in its own `px-6 pb-6` wrapper (retained countdown). Each section degrades independently (DASH-07). Use the exact copy from UI-SPEC. Implements DASH-01/02/03/07.

    Update `taskflow/src/routes/dashboard/index.test.tsx`: remove any assertions referencing the deleted cards / SmokeTestChart; add assertions that the stat-tiles grid and SprintHealthSection render (seed the sprint-board cache via setQueryData in a QueryClientProvider as in the existing test), and that the hero greeting still renders. Keep it a smoke-level render test consistent with the existing file.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- "dashboard/index" --run</automated>
  </verify>
  <acceptance_criteria>
    - index.tsx contains no import of `SmokeTestChart`, `DashboardSprintCard`, or `DashboardInProgressCard`
    - index.tsx imports and renders `StatTile`, `SprintHealthSection`, and `DashboardReleaseCard`
    - index.tsx tile-row sprint-board query uses key `['jira-issues', 'sprint-board', activeJiraProject, storyPointsFieldKey]` with `staleTime: 30_000`
    - index.tsx derives tiles via `computePersonalTileCounts` and `computeSpDone` (no inline re-derivation of SP/subtask logic)
    - index.tsx grid uses `grid grid-cols-2 sm:grid-cols-4 gap-4`
    - `npm run test -- "dashboard/index" --run` exits 0
    - `npx tsc --noEmit` reports no new errors in index.tsx
  </acceptance_criteria>
  <done>index.tsx is rewritten with hero + tile row + sprint-health + release countdown, derives tiles via the metrics module over the shared cache key, and its render test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete SmokeTestChart, DashboardSprintCard, DashboardInProgressCard (+ their tests); turn widget-removal guard GREEN; run full suite</name>
  <read_first>
    - taskflow/src/routes/dashboard/widget-removal.guard.test.ts (the Phase 83 block added in 83-01 — must go GREEN after deletions)
    - taskflow/src/routes/dashboard/index.tsx (confirm no residual imports of the three files before deleting)
    - .planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-CONTEXT.md (D-01 — these three are removed; DashboardReleaseCard is RETAINED)
  </read_first>
  <action>
    Delete these files with `git rm`: `taskflow/src/routes/dashboard/SmokeTestChart.tsx`, `taskflow/src/routes/dashboard/DashboardSprintCard.tsx`, `taskflow/src/routes/dashboard/DashboardInProgressCard.tsx`, and their colocated tests `SmokeTestChart.test.tsx`, `DashboardSprintCard.test.tsx`, `DashboardInProgressCard.test.tsx` (delete only the test files that exist — confirm via `ls`). Do NOT delete `DashboardReleaseCard.tsx`/`DashboardReleaseCard.test.tsx` (retained per D-01). Before deleting, grep the whole `taskflow/src` tree to confirm no remaining imports of the three deleted components outside their own test files and the widget-removal guard; if any consumer other than index.tsx imports them, STOP and report (the plan assumes index.tsx was the only consumer). After deletion, run the widget-removal guard — its Phase 83 block must now be GREEN. Then run the full test suite to confirm no regressions from the removals.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- widget-removal --run && npm run test --run</automated>
  </verify>
  <acceptance_criteria>
    - SmokeTestChart.tsx, DashboardSprintCard.tsx, DashboardInProgressCard.tsx no longer exist on disk
    - No file under taskflow/src imports the three deleted components except the widget-removal guard (which asserts their absence)
    - `npm run test -- widget-removal --run` exits 0 (Phase 83 block now GREEN)
    - `npm run test --run` (full suite) exits 0
  </acceptance_criteria>
  <done>The three scaffold/legacy components and their tests are deleted, the widget-removal guard is fully green, and the entire Vitest suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — Dashboard renders in real Tauri WebKit with zero new API calls</name>
  <what-built>
    The rewritten Dashboard: gradient hero greeting + en-GB date (retained), a 4-tile stat row (Open / In Progress / Overdue / SP Done), a Sprint Health section (days remaining + %-complete progress bar + points-by-status donut), and the retained next-release countdown. All figures derive from the warm sprint-board + active-sprint caches; donut colors come from `var(--chart-1..3)`.
  </what-built>
  <how-to-verify>
    1. Build/run the real Tauri app: `cd taskflow && npm run tauri dev` (or the project's standard dev build).
    2. First visit Sprint Board (or hover its sidebar entry) to warm caches, then open Network/devtools and navigate to Dashboard. Confirm NO new `/sprint` or `/search` request fires on Dashboard load (criterion 3, DASH-03).
    3. Confirm the gradient hero greeting + en-GB date still render, and the next-release countdown is present (criterion 1, DASH-01).
    4. Confirm the 4 stat tiles render (Open/In Progress/Overdue/SP Done); Overdue value is red when > 0; tiles are static (no hover/cursor-pointer/click) (criterion 2, DASH-02, D-06).
    5. Confirm the Sprint Health section shows days-remaining copy, a progress bar at the correct %, and the donut with To Do/In Progress/Done segments in the theme `--chart-1..3` colors (no 0×0 collapse) in BOTH light and dark themes; donut center shows total sprint SP (criterion 3, DASH-03).
    6. Optionally simulate a slow/failed section (e.g. throttle network on a cold cache) and confirm one section's skeleton/error does not blank the others (criterion 4, DASH-07).
  </how-to-verify>
  <action>Run the real Tauri build and walk the verification steps above; this is a verify-after-automation checkpoint — no code is written here. Pause for the user to confirm the rendered Dashboard, theme-correct donut, zero-new-API-calls behavior, and independent section degradation.</action>
  <verify>
    <human-check>User runs the Tauri build and confirms criteria 1-4 (hero+countdown retained, 4 static tiles, sprint-health donut/progress/days in both themes, zero new API calls, independent degradation).</human-check>
  </verify>
  <done>User replies "approved" after confirming the Dashboard renders correctly in a real Tauri WebKit build with no new network requests on load and independently degrading sections.</done>
  <resume-signal>Type "approved" or describe issues (e.g. donut color wrong, a new request fired, tile interactive).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TanStack Query warm cache → Dashboard UI | Read-only render of already-authenticated cached Jira data; no new network input or user input introduced |
| Jira issue text → JSX | Rendered via React JSX auto-escaping (no dangerouslySetInnerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-05 | Information disclosure | index.tsx tile-row cache read | mitigate | Sprint-board cache key includes `activeJiraProject`; tile/donut figures are project-scoped and cannot leak across Jira projects |
| T-83-06 | Cross-site scripting | tile/section text from Jira | mitigate | All Jira-derived text rendered via JSX auto-escaping; no dangerouslySetInnerHTML added |
| T-83-SC | Tampering | supply chain | accept | No package installs in this phase |
</threat_model>

<verification>
- `npm run test -- "dashboard/index" --run` green; `npm run test -- widget-removal --run` GREEN after deletions; `npm run test --run` full suite green.
- `npx tsc --noEmit` clean for the dashboard route.
- Human UAT confirms Tauri render, theme-correct donut, zero new API calls, independent degradation.
</verification>

<success_criteria>
- DASH-01: 3 cards + SmokeTestChart removed (files deleted, guard green); hero + release countdown retained.
- DASH-02: 4 stat tiles render with subtask-excluded SP Done.
- DASH-03: sprint-health donut + progress + days-remaining from warm cache, CSS-var colors, zero new API calls.
- DASH-07: each section degrades independently — confirmed in UAT.
</success_criteria>

<output>
Create `.planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-03-SUMMARY.md` when done.
</output>
