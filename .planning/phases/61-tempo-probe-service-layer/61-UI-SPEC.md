---
phase: 61
slug: tempo-probe-service-layer
status: draft
shadcn_initialized: true
preset: base-nova / neutral / cssVariables
created: 2026-05-21
---

# Phase 61 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Phase Scope

Phase 61 delivers one UI surface: a **Tempo Timesheets toggle** in Settings → Integrations. The rest of the phase is backend (curl probe, service module, store migration) with no new visual components. This contract is narrow by design — the toggle reuses the existing AIO checkbox pattern verbatim.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova, baseColor: neutral, cssVariables: true |
| Style | base-nova (detected from components.json) |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react |
| Font | Geist Variable (--font-sans) |

Source: `taskflow/components.json` + `npx shadcn info`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing (gap-2) |
| md | 16px | Default element spacing (gap-4) |
| lg | 24px | Section padding (gap-6) |
| xl | 32px | Layout gaps (gap-8) |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

**Phase-specific usage:**
- Settings section group gap: `gap-8` (32px) — mirrors existing `flex flex-col gap-8` on `section-integrations` wrapper
- Toggle label inner gap: `gap-4` (16px) — mirrors existing AIO `<label>` element
- Tempo section inner wrapper: `gap-4` (16px) — `<div className="flex flex-col gap-4">` wraps the `<h3>` and `<label>` pair

Exceptions: none — this phase introduces no non-4-multiple spacing values.

**Out of Scope / Inherited (not part of this phase's design surface):**
`gap-1.5` (6px) exists in `IntegrationsSection.tsx` line 66 within the existing AIO block. This phase does not touch that element. It is documented here for traceability only and is explicitly excluded from this phase's spacing contract.

Source: `IntegrationsSection.tsx` line 60 (gap-8 wrapper).

---

## Typography

| Role | Size | Weight | Line Height | Tailwind Class |
|------|------|--------|-------------|----------------|
| Section heading | 18px (text-lg) | 600 (font-semibold) | 1.2 | `text-lg font-semibold` |
| Subsection label | 14px (text-sm) | 600 (font-semibold) | 1.4 | `text-sm font-semibold text-muted-foreground uppercase tracking-wide` |
| Toggle label | 14px (text-sm) | 600 (font-semibold) | 1.4 | `text-sm font-semibold` |
| Toggle description | 12px (text-xs) | 400 (font-normal) | 1.4 | `text-xs text-muted-foreground` |

Declared weights: 400 (font-normal) and 600 (font-semibold) only. font-medium (500) is not used in this phase.

Font: Geist Variable, sans-serif (inherited from `html { @apply font-sans }` in index.css).

Source: `IntegrationsSection.tsx` lines 61–79 — mirrored exactly for Tempo section, with font-medium collapsed to font-semibold.

---

## Color

| Role | CSS Variable | OKLCH (light) | Usage |
|------|-------------|---------------|-------|
| Dominant (60%) | `--background` | oklch(1 0 0) | Page background |
| Secondary (30%) | `--card` / `--sidebar` | oklch(1 0 0) / oklch(0.985 0 0) | Settings panel surface |
| Accent (10%) | `--primary` | oklch(0.205 0 0) | Checkbox `accent-primary` fill when checked |
| Muted foreground | `--muted-foreground` | oklch(0.556 0 0) | Description text, subsection labels |
| Destructive | `--destructive` | oklch(0.577 0.245 27.325) | Not used in this phase |

Accent reserved for: checkbox fill on `<input type="checkbox" className="h-4 w-4 accent-primary">` only.

Source: `index.css` lines 80–113, `IntegrationsSection.tsx` line 78.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| New section heading (h3) | `Tempo Timesheets` |
| Toggle label (p.font-semibold) | `Enable Tempo Timesheets` |
| Toggle description (p.text-xs) | `Show worklog data from Jira Tempo Timesheets. Requires Tempo plugin on your Jira instance.` |
| Checkbox aria-label | `Enable Tempo Timesheets` |
| Empty state | Not applicable — toggle has no sub-UI when enabled in Phase 61 |
| Error state | Not applicable — no data fetch in this phase's toggle |
| Destructive confirmation | Not applicable — no destructive actions in this phase |

Source: CONTEXT.md D-11, D-12 (locked decisions).

---

## Component Inventory

No new shadcn components required. All elements are native HTML or already-installed components.

| Component | Source | Status |
|-----------|--------|--------|
| `<input type="checkbox">` | Native HTML | Already used in AIO toggle |
| `text-sm font-semibold` label | Tailwind utility | Already used |
| `text-xs text-muted-foreground` description | Tailwind utility | Already used |
| `h3` subsection heading | Native HTML | Already used |

No `npx shadcn add` commands needed for this phase.

---

## Interaction Contract

### Tempo Toggle

| State | Visual |
|-------|--------|
| Default (off) | Checkbox unchecked, `tempoEnabled: false` in store |
| Checked | Checkbox checked with `accent-primary` fill; store persists `tempoEnabled: true` |
| No sub-UI on enable | Unlike AIO toggle, no dropdown or child section appears when enabled — Phase 62 adds the sidebar link |
| App restart | `tempoEnabled` survives via zustand-persist (store v20 migration guard) |

### Checkbox placement

```
<label className="flex items-center justify-between gap-4 cursor-pointer">
  <div>
    <p className="text-sm font-semibold">Enable Tempo Timesheets</p>
    <p className="text-xs text-muted-foreground">
      Show worklog data from Jira Tempo Timesheets. Requires Tempo plugin on your Jira instance.
    </p>
  </div>
  <input
    type="checkbox"
    aria-label="Enable Tempo Timesheets"
    checked={tempoEnabled}
    onChange={(e) => setTempoEnabled(e.target.checked)}
    className="h-4 w-4 accent-primary"
  />
</label>
```

Placement: below the AIO block inside `IntegrationsSection.tsx`, separated by the existing `flex flex-col gap-8` container rhythm. A new `<div className="flex flex-col gap-4">` wraps the `<h3>` and `<label>` pair, identical to the AIO section structure.

Source: CONTEXT.md D-11; mirrored from `IntegrationsSection.tsx` lines 62–80.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | alert, badge, button, command, dialog, input, label, popover, progress, radio-group, select, sheet, skeleton, switch, tabs, textarea | not required |
| Third-party | none | not applicable |

No third-party registries. No new shadcn components added in this phase.

Source: `npx shadcn info` — registries: `{ @shadcn: "https://ui.shadcn.com/r/..." }` only.

---

## Accessibility

| Requirement | Implementation |
|-------------|----------------|
| Checkbox label association | `<label>` wraps both text and `<input>` (implicit association) |
| Checkbox aria-label | `aria-label="Enable Tempo Timesheets"` on `<input>` (explicit redundancy, same as AIO) |
| Keyboard | Native checkbox receives tab focus, spacebar toggles |
| Touch target | `h-4 w-4` (16px) checkbox — label wrapper provides extended click area |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
