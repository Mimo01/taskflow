---
phase: 61-tempo-probe-service-layer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/61-tempo-probe-service-layer/61-PROBE-RESULT.md
autonomous: false
requirements:
  - TEMPO-06
must_haves:
  truths:
    - "A live curl probe against /rest/tempo-timesheets/4/worklogs returns 200 with a JSON body, OR a documented failure mode and blocker note exists"
    - "The exact Tempo DC response envelope shape (item field name: worklogs|results|items; pagination sentinel: isLast|metadata.count|length<limit) is captured"
    - "The exact `author` field shape on this DC instance (string vs object) is captured"
    - "If probe returns 401 on both /4/ and /3/, TEMPO-06 is marked blocked and downstream plans 02-04 do NOT execute"
  artifacts:
    - path: ".planning/phases/61-tempo-probe-service-layer/61-PROBE-RESULT.md"
      provides: "Probe outcome record: working API path, response shape, author field type, pagination sentinel, blocker status"
      contains: "## Probe Result"
  key_links:
    - from: "61-PROBE-RESULT.md"
      to: "61-02-PLAN.md client.ts"
      via: "TEMPO_API_PATH constant value + author field type + pagination sentinel"
      pattern: "TEMPO_API_PATH"
---

<objective>
Run a live curl probe against the Jira DC Tempo Timesheets REST API using `Authorization: Bearer <jira-pat>`. Try `/rest/tempo-timesheets/4/` first, then `/rest/tempo-timesheets/3/` as a fallback (D-01). Capture the response JSON shape (item array field name, pagination envelope, author field type) so Wave 1 service code is written against ground truth, not assumed Cloud-API shapes.

Purpose: Phase 61 is a hard gate (D-03). If Bearer PAT returns 401 on both versions, no service module files are built — the failure is documented and TEMPO-06 is marked blocked until a separate Tempo plugin token is added to Stronghold. This plan is the gate.

Output: `.planning/phases/61-tempo-probe-service-layer/61-PROBE-RESULT.md` containing: working API path, full sanitized response JSON, identified `author` field shape, identified pagination sentinel, and a GO/NO-GO decision for Wave 1.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-tempo-probe-service-layer/61-CONTEXT.md
@.planning/phases/61-tempo-probe-service-layer/61-RESEARCH.md
@.planning/STATE.md

# AIO probe precedent — same pattern, same shape of comment block
@taskflow/src/services/aio/client.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Live curl probe — capture working Tempo API path and response shape</name>
  <read_first>
    - .planning/phases/61-tempo-probe-service-layer/61-CONTEXT.md (D-01, D-02, D-03)
    - .planning/phases/61-tempo-probe-service-layer/61-RESEARCH.md (Probe curl commands section; Pitfall 1; Open Questions)
    - taskflow/src/services/aio/client.ts (header comment block style — mirror format in PROBE-RESULT.md)
  </read_first>
  <what-built>
    Claude has prepared the curl commands and the PROBE-RESULT.md scaffold. The user must (a) supply the Jira base URL, their Jira PAT, and a known Jira username from their instance, and (b) run the two curl probes below and paste the responses back into the chat. This task cannot be Claude-automated because: the secret PAT lives in Stronghold (Tauri-only access, no shell), and the live Jira DC instance is the user's own corporate host.
  </what-built>
  <how-to-verify>
    Execute these probes from the developer workstation and capture both HTTP status code and JSON body. Substitute `&lt;JIRA_BASE_URL&gt;`, `&lt;JIRA_PAT&gt;`, and `&lt;YOUR_USERNAME&gt;` with real values; never paste the PAT into chat — only paste the JSON response body and HTTP status.

    Step 1 — Probe v4 (try first, per D-01):
    ```
    curl -s -w "\nHTTP_STATUS:%{http_code}\n" \
      -H "Authorization: Bearer &lt;JIRA_PAT&gt;" \
      -H "Content-Type: application/json" \
      "&lt;JIRA_BASE_URL&gt;/rest/tempo-timesheets/4/worklogs?dateFrom=2026-05-01&dateTo=2026-05-07&username=&lt;YOUR_USERNAME&gt;"
    ```

    Step 2 — If v4 returned 401, probe v3:
    ```
    curl -s -w "\nHTTP_STATUS:%{http_code}\n" \
      -H "Authorization: Bearer &lt;JIRA_PAT&gt;" \
      -H "Content-Type: application/json" \
      "&lt;JIRA_BASE_URL&gt;/rest/tempo-timesheets/3/worklogs?dateFrom=2026-05-01&dateTo=2026-05-07&username=&lt;YOUR_USERNAME&gt;"
    ```

    Expected outcomes (any one of):
    1. v4 returns HTTP 200 + JSON body — GO for Wave 1; record v4 path
    2. v4 returns 401, v3 returns HTTP 200 + JSON body — GO for Wave 1; record v3 path
    3. Both return 401 — NO-GO; phase ends at this task; mark TEMPO-06 blocked
    4. Both return 404 — NO-GO; Tempo plugin not installed on this instance; mark TEMPO-06 blocked

    After running, paste back into the chat:
    - The HTTP_STATUS line for each call (e.g. `HTTP_STATUS:200`)
    - The full JSON body (DO NOT paste the curl command itself; only the response)
  </how-to-verify>
  <resume-signal>Paste the HTTP_STATUS lines and the full JSON response body (or error text) for each probe attempted. Type "v4 401, v3 401" only if both failed (Claude will then write a blocker note and stop the phase).</resume-signal>
  <action>Claude prepares this task by reading `taskflow/src/services/aio/client.ts` header comment for the comment-block style precedent, listing the two probe commands above, and waiting for the user to execute them. Do NOT proceed to Task 2 until the user has pasted concrete HTTP status codes and response bodies (or confirmed both 401/404).</action>
  <verify>
    <human-check>User has pasted at least one HTTP_STATUS code AND a corresponding response body (JSON or error) for one of the two probe URLs.</human-check>
  </verify>
  <acceptance_criteria>
    - The chat transcript contains at least one `HTTP_STATUS:&lt;code&gt;` line returned by curl
    - For any 200 response: the chat transcript contains the JSON body (or a clear "[no worklogs returned for date range]" note + the envelope shape)
    - For any 401/404 response: user has confirmed which path(s) failed
    - Claude has NOT written any file in `taskflow/src/services/tempo/` during this task
  </acceptance_criteria>
  <done>User has provided concrete HTTP status codes for the probe attempts and (for any 200) the response JSON body, enabling Claude to identify the working path and response shape.</done>
</task>

<task type="auto">
  <name>Task 2: Write 61-PROBE-RESULT.md capturing path, shape, and GO/NO-GO decision</name>
  <files>.planning/phases/61-tempo-probe-service-layer/61-PROBE-RESULT.md</files>
  <read_first>
    - .planning/phases/61-tempo-probe-service-layer/61-CONTEXT.md (D-01, D-02, D-03, D-06, D-09)
    - .planning/phases/61-tempo-probe-service-layer/61-RESEARCH.md (Assumptions Log A1, A2, A3; Open Questions 1-3)
    - taskflow/src/services/aio/client.ts (lines 1-18 header comment block format to mirror)
  </read_first>
  <action>Create `.planning/phases/61-tempo-probe-service-layer/61-PROBE-RESULT.md` with these exact sections in order: (1) `# Phase 61 — Tempo Probe Result` heading + `Run date:` line. (2) `## Probe Attempts` table with columns: `API Path | HTTP Status | Outcome` covering `/rest/tempo-timesheets/4/worklogs` and `/rest/tempo-timesheets/3/worklogs`. (3) `## Working API Path` section: single fenced block stating `TEMPO_API_PATH = '/rest/tempo-timesheets/4'` or `'/rest/tempo-timesheets/3'` based on probe; if both failed, write `TEMPO_API_PATH = NOT FOUND`. (4) `## Response Envelope Shape` section: paste the sanitized JSON response body (replace any PAT, real usernames, or email addresses with `&lt;redacted&gt;`); identify which top-level field carries the array (`worklogs` / `results` / `items`); identify the pagination sentinel (`isLast` boolean / `metadata.count` / fallback to `items.length < limit`). (5) `## author Field Shape` section: state whether `author` is `string` (plain username) or `{ name: string }` (object) based on probe — this resolves Assumption A1. (6) `## username Query Param` section: state the confirmed query param name used for user filtering (`username` / `worker` / `userKey`) — this resolves Assumption A2. (7) `## GO/NO-GO Decision for Wave 1` section: write `GO` if any probe returned 200; write `NO-GO — TEMPO-06 blocked` if both returned 401/404; if NO-GO, also list the recommended next step (e.g. "add `tempo-api-token` to Stronghold; re-probe in Phase 61.1"). Do NOT include any actual PAT, real password, or full email address in this file. Resolve Assumptions A1/A2/A3 from RESEARCH.md by writing concrete values here.</action>
  <verify>
    <automated>test -f .planning/phases/61-tempo-probe-service-layer/61-PROBE-RESULT.md &amp;&amp; grep -q "## Working API Path" .planning/phases/61-tempo-probe-service-layer/61-PROBE-RESULT.md &amp;&amp; grep -q "## GO/NO-GO Decision" .planning/phases/61-tempo-probe-service-layer/61-PROBE-RESULT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/61-tempo-probe-service-layer/61-PROBE-RESULT.md` exists
    - File contains a heading exactly matching `## Working API Path`
    - File contains a heading exactly matching `## Response Envelope Shape`
    - File contains a heading exactly matching `## author Field Shape`
    - File contains a heading exactly matching `## GO/NO-GO Decision for Wave 1`
    - The GO/NO-GO section contains either the literal token `GO` or the literal token `NO-GO`
    - `grep -i "Bearer [A-Za-z0-9_.-]\\{20,\\}" .planning/phases/61-tempo-probe-service-layer/61-PROBE-RESULT.md` returns no lines (no leaked PAT)
    - The Working API Path section contains a string starting with `/rest/tempo-timesheets/` OR the literal `NOT FOUND`
  </acceptance_criteria>
  <done>61-PROBE-RESULT.md exists with all five required sections; GO/NO-GO decision is explicit; no secret credentials are leaked into the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → Jira DC host | Bearer PAT crosses this boundary in the `Authorization` header during the probe |
| Probe response → committed planning artifact (PROBE-RESULT.md) | Response body is recorded into the repo and committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Information Disclosure | curl probe command | mitigate | User pastes only HTTP status + JSON body into chat; never the curl command (PAT). Task instructions explicitly forbid pasting the PAT. |
| T-61-02 | Information Disclosure | 61-PROBE-RESULT.md committed file | mitigate | Task 2 action mandates sanitizing JSON (replace real usernames, emails, any leaked tokens with `&lt;redacted&gt;`). Acceptance criterion includes a `grep` gate against Bearer tokens longer than 20 chars. |
| T-61-03 | Tampering | jiraBaseUrl supplied by user | accept | jiraBaseUrl comes from the user's own workstation — already trusted. No untrusted-input crossing. |
| T-61-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `.planning/phases/61-tempo-probe-service-layer/61-PROBE-RESULT.md` exists and contains all five required sections
- The file commits a clear GO or NO-GO decision
- No PAT, password, or full email is present in the committed file (grep gate)
- Downstream Plans 02-04 can read TEMPO_API_PATH and the author field shape directly from PROBE-RESULT.md
</verification>

<success_criteria>
- Working Tempo API path is confirmed and recorded (or definitive NO-GO is recorded)
- Wave 1 plans (61-02, 61-03) have a concrete TEMPO_API_PATH value, `author` field type, and pagination sentinel to code against
- TEMPO-06 success criterion 1 (live curl probe returns 200 OR auth mechanism documented if Bearer PAT fails) is satisfied
- If NO-GO: phase ends here; Plans 02-04 are deferred and a blocker note is added to STATE.md (handled at phase wrap-up, not in this plan)
</success_criteria>

<output>
Create `.planning/phases/61-tempo-probe-service-layer/61-01-SUMMARY.md` when done, citing the working API path (or NO-GO status) and the resolved Assumption values (A1 author shape, A2 username param, A3 pagination sentinel).
</output>
