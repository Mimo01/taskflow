---
phase: 51-aio-service-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/51-aio-service-layer/51-CONTEXT.md
autonomous: false
requirements:
  - AION-05

must_haves:
  truths:
    - "The working AIO REST base path variant is identified (one of 3 candidates returns 200)"
    - "Bearer PAT auth scheme is confirmed or refuted (200 = confirmed, 401 = wrong scheme)"
    - "Whether GET /testrun?issueKey= works without a project ID is confirmed"
    - "Actual JSON response shapes for /project and /testrun are captured"
    - "All findings are recorded as Key Decisions in CONTEXT.md before any TypeScript is written"
  artifacts:
    - path: ".planning/phases/51-aio-service-layer/51-CONTEXT.md"
      provides: "Key Decisions section with probe findings (base path, auth, testrun param)"
      contains: "KEY DECISION — Phase 51 Probe"
  key_links:
    - from: "curl probe results (base path)"
      to: "aio/client.ts AIO_API_PATH constant"
      via: "Key Decision recorded in CONTEXT.md"
      pattern: "KEY DECISION.*Phase 51 Probe"
    - from: "curl probe results (testrun?issueKey= shape)"
      to: "aio/issue-runs.ts response guard"
      via: "Key Decision recorded in CONTEXT.md"
      pattern: "testrun.*issueKey"
---

<objective>
Run the live AIO instance curl probe and record findings as Key Decisions.

Purpose: The AIO REST base path is installation-specific (three known variants) and Bearer PAT auth has not been verified against this instance. No AIO TypeScript service code can be written until these facts are confirmed. This plan is the hard blocker for Plans 02 and 03.

Output: CONTEXT.md updated with a "KEY DECISION — Phase 51 Probe" section recording: working base path variant, confirmed auth scheme, whether GET /testrun?issueKey= works without a project ID, and the actual JSON shapes of /project and /testrun responses.
</objective>

<execution_context>
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/51-aio-service-layer/51-CONTEXT.md
@.planning/phases/51-aio-service-layer/51-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run curl probe against live AIO instance</name>
  <files>.planning/phases/51-aio-service-layer/51-CONTEXT.md</files>
  <read_first>
    - .planning/phases/51-aio-service-layer/51-CONTEXT.md — D-02 specifies exact curl commands and the three base path variants to test; read before running to confirm you have the correct JIRA_BASE and PAT values
    - .planning/phases/51-aio-service-layer/51-RESEARCH.md — "Curl Probe Commands" section contains the exact curl invocations and expected outcomes table
  </read_first>
  <what-built>
    The executor has prepared the curl commands. You must run them manually against the live Jira/AIO instance because the instance is not reachable from the test environment.
  </what-built>
  <how-to-verify>
    Run the following commands in your terminal, substituting your values:

    export JIRA_BASE="https://your-jira-instance.example.com"
    export PAT="your-jira-personal-access-token"

    --- Variant 1: /rest/aio-tcms/1.0/ ---
    curl -s -o /dev/null -w "%{http_code}" -H "Authorization: Bearer $PAT" "$JIRA_BASE/rest/aio-tcms/1.0/project"
    curl -s -o /dev/null -w "%{http_code}" -H "Authorization: Bearer $PAT" "$JIRA_BASE/rest/aio-tcms/1.0/testrun?issueKey=PROJ-1"

    --- Variant 2: /rest/aio-tcms-api/1.0/ ---
    curl -s -o /dev/null -w "%{http_code}" -H "Authorization: Bearer $PAT" "$JIRA_BASE/rest/aio-tcms-api/1.0/project"
    curl -s -o /dev/null -w "%{http_code}" -H "Authorization: Bearer $PAT" "$JIRA_BASE/rest/aio-tcms-api/1.0/testrun?issueKey=PROJ-1"

    --- Variant 3: /plugins/servlet/aio/ ---
    curl -s -o /dev/null -w "%{http_code}" -H "Authorization: Bearer $PAT" "$JIRA_BASE/plugins/servlet/aio/project"
    curl -s -o /dev/null -w "%{http_code}" -H "Authorization: Bearer $PAT" "$JIRA_BASE/plugins/servlet/aio/testrun?issueKey=PROJ-1"

    For the variant that returns 200, ALSO capture the full response bodies:
    curl -s -H "Authorization: Bearer $PAT" "$JIRA_BASE/<working-base-path>/project" | python3 -m json.tool
    curl -s -H "Authorization: Bearer $PAT" "$JIRA_BASE/<working-base-path>/testrun?issueKey=<real-issue-key>" | python3 -m json.tool

    Record for each command: HTTP status code returned.
    Record for the working variant: full JSON response body (project list and testrun response).

    Expected outcome: Exactly one variant returns 200 for /project. The other two return 404 or redirect.
    If all return 401: Bearer PAT is wrong auth — try Basic auth as fallback: -u "your-username:$PAT"
  </how-to-verify>
  <resume-signal>
    Type "probe done" and provide:
    1. Working base path (e.g., "/rest/aio-tcms/1.0") — or "none" if all 401/404
    2. Auth scheme that worked ("Bearer PAT" or "Basic auth" or "failed — all 401")
    3. HTTP status from GET /testrun?issueKey= on the working variant ("200" or "404" or other)
    4. Paste the JSON body of /project response (or first 20 lines if large)
    5. Paste the JSON body of /testrun?issueKey= response (or first 20 lines if large)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record probe findings as Key Decisions in CONTEXT.md</name>
  <files>.planning/phases/51-aio-service-layer/51-CONTEXT.md</files>
  <read_first>
    - .planning/phases/51-aio-service-layer/51-CONTEXT.md — read the full file first; the Key Decisions section (under ## Implementation Decisions) is where the new probe findings block goes, appended after D-12
    - .planning/phases/51-aio-service-layer/51-RESEARCH.md — "Expected outcomes to record" list confirms what must be captured
  </read_first>
  <action>
    Append a new "## Probe Findings (KEY DECISIONS — Phase 51)" subsection to the decisions block in CONTEXT.md immediately after D-12. The section must contain exactly these items (fill in from the developer's probe results provided at the checkpoint):

    D-13: Working AIO REST base path — the exact path string confirmed by probe (e.g., "/rest/aio-tcms/1.0"). State which of the three variants returned 200 for GET /project. State that the other two returned 404/401/redirect (with the actual codes).

    D-14: Auth scheme confirmed — "Bearer PAT (same jira-pat Stronghold key) returns 200" or document the actual auth required if different from expected.

    D-15: GET /testrun?issueKey= result — whether it works without a project ID (state the HTTP status returned). If it returned 200, confirm the response is usable for Phase 54. If it returned 404 or requires project ID, document the required fallback (extract project key from issueKey, fetch project list, match by name).

    D-16: AioProject response shape — paste the field names and types observed in the /project JSON response. This drives types.ts AioProject interface. Mark assumed fields that were not present in the actual response.

    D-17: AioIssueRun response shape — paste the field names and types observed in the /testrun JSON response. This drives types.ts AioIssueRun interface. Note whether the response is a direct array or a paginated wrapper (e.g., { values: [...] }).

    Format: Use the same numbered D-XX style as existing decisions. Add the probe date (today's date) at the top of the section: "Probe run: YYYY-MM-DD".
  </action>
  <verify>
    <automated>grep -c "D-13\|D-14\|D-15\|D-16\|D-17" .planning/phases/51-aio-service-layer/51-CONTEXT.md</automated>
    Expected: 5 (each decision key appears at least once)
    Also verify: grep -c "KEY DECISION\|Probe run" .planning/phases/51-aio-service-layer/51-CONTEXT.md returns ≥ 1
  </verify>
  <acceptance_criteria>
    - CONTEXT.md contains a new "Probe Findings" section with D-13 through D-17
    - D-13 identifies the exact working base path string (not "TBD" or "to be confirmed")
    - D-14 states whether Bearer PAT or an alternative auth scheme was confirmed
    - D-15 states the HTTP status of GET /testrun?issueKey= and whether project ID is required
    - D-16 contains actual field names from the /project JSON response (not the placeholder [ASSUMED] names from PATTERNS.md)
    - D-17 contains actual field names from the /testrun JSON response and states whether response is array or wrapped
    - All five decisions are present and non-placeholder before Plan 03 may begin
  </acceptance_criteria>
  <done>
    CONTEXT.md updated with probe findings recorded as D-13–D-17. Plan 02 (settings store + UI) may proceed in parallel. Plan 03 (AIO service module) may proceed only after this task completes, since AIO_API_PATH and types.ts interfaces depend on D-13 and D-16/D-17.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → live AIO instance | Curl commands cross this boundary; PAT travels over HTTPS |
| CONTEXT.md content → aio/client.ts | The D-13 base path value is hard-coded into the AIO_API_PATH constant — if recorded incorrectly, all AIO calls will fail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51P-01 | Information Disclosure | PAT in curl command history | accept | PAT is in shell history only; same exposure as all other curl-based probe work; not stored anywhere in the codebase |
| T-51P-02 | Tampering | Incorrect base path recorded in D-13 | mitigate | Acceptance criteria require non-placeholder value; Plan 03 executor must verify AIO_API_PATH matches D-13 exactly before writing service code |
| T-51P-03 | Elevation of Privilege | PAT sent to wrong host (typo in JIRA_BASE) | accept | Developer controls JIRA_BASE value; typo risk is low; no programmatic construction of the URL |
</threat_model>

<verification>
Phase 51 Plan 01 is complete when:
- Developer has run all 6 curl probe commands (3 variants × 2 endpoints)
- Developer has captured full JSON response bodies for the working variant
- CONTEXT.md contains D-13 through D-17 with non-placeholder values
- grep confirms all five decision keys are present in CONTEXT.md

Full suite run: not required for Plan 01 (no TypeScript changes). Plans 02 and 03 carry the test verification.
</verification>

<success_criteria>
1. Exactly one AIO REST base path variant confirmed as returning 200 for GET /project with Bearer PAT
2. Auth scheme confirmed or documented with fallback
3. GET /testrun?issueKey= behavior confirmed (works without project ID, or fallback documented)
4. Actual JSON field names captured for AioProject and AioIssueRun interfaces
5. All findings in CONTEXT.md as D-13–D-17, non-placeholder, before any TypeScript in Plans 02/03
</success_criteria>

<output>
After completion, create `.planning/phases/51-aio-service-layer/51-01-SUMMARY.md`
</output>
