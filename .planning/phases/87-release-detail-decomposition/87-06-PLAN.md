---
phase: 87-release-detail-decomposition
plan: 06
type: execute
wave: 6
depends_on: ["87-05"]
files_modified:
  - taskflow/src/routes/dashboard/release-detail/EditReleaseModal.tsx
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
autonomous: false
requirements: [FOUND-01]

must_haves:
  truths:
    - "The edit modal still opens from the sidebar Edit button, edits all six fields, and saves"
    - "The Save button is still disabled while saving, when the name is empty, when nothing changed, or when a matched milestone title was cleared"
    - "The release detail page renders identically to its pre-refactor form (verified by manual UAT click-through)"
    - "release-detail/ contains one file per section/hook, mirroring the issue-detail/ convention"
    - "D-15: the existing suites pass with zero regressions — specifically ReleasesTab.test.tsx and UpcomingReleasesTimeline.test.tsx, which share query state and release-linking code paths with this page"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/EditReleaseModal.tsx"
      provides: "Edit Release dialog (D-01)"
      contains: "Dialog.Root"
    - path: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      provides: "Slim page shell delegating to release-detail/ (D-04, D-06)"
      max_lines: 260
  key_links:
    - from: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      to: "./release-detail/EditReleaseModal"
      via: "direct-path import, props sourced from useEditRelease"
      pattern: "from './release-detail/EditReleaseModal'"
---

<objective>
Extract the Edit Release dialog into `release-detail/EditReleaseModal.tsx`, confirm the page shell has landed in D-06's target range, and close the phase with the manual UAT click-through that verifies Success Criterion 1.

Purpose: last extraction plus the phase's only means of verifying "renders identically" — `ReleaseDetailPage` has no automated coverage and D-14 deliberately declined adding a characterization test.

Output: `EditReleaseModal.tsx`, a finished `release-detail/` folder, and a recorded manual verification result.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-release-detail-decomposition/87-CONTEXT.md
@.planning/phases/87-release-detail-decomposition/87-RESEARCH.md
@.planning/phases/87-release-detail-decomposition/87-PATTERNS.md
@.planning/phases/87-release-detail-decomposition/87-VALIDATION.md
@.planning/phases/87-release-detail-decomposition/87-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract EditReleaseModal.tsx</name>
  <files>taskflow/src/routes/dashboard/release-detail/EditReleaseModal.tsx, taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx (the `{/* Edit modal ... */}` block from `<Dialog.Root open={editing}` through `</Dialog.Root>` — pre-refactor L1305-1479)
    - taskflow/src/routes/dashboard/BulkCreateSubtasksModal.tsx (the `@base-ui/react/dialog` usage analog — confirms the primitive to keep)
    - taskflow/src/routes/dashboard/release-detail/useEditRelease.ts (the hook whose values become props)
    - .planning/phases/87-release-detail-decomposition/87-RESEARCH.md (§2 EditReleaseModal — the full prop inventory)
  </read_first>
  <action>
    Create `release-detail/EditReleaseModal.tsx` with `interface EditReleaseModalProps` and `export function EditReleaseModal(...)`.

    Props: `open: boolean`, `onOpenChange: (o: boolean) => void`, `editName`/`setEditName`, `editDate`/`setEditDate`, `editDescription`/`setEditDescription`, `editReleased`/`setEditReleased`, `editMilestoneTitle`/`setEditMilestoneTitle`, `editMilestoneDescription`/`setEditMilestoneDescription`, `isSaving: boolean`, `jiraError: string | null`, `gitlabError: string | null`, `showMilestoneSection: boolean`, `isSaveDisabled: boolean`, `onCancel: () => void`, `onSave: () => void`. Setter props type as `(v: string) => void` / `(v: boolean) => void` (or the matching `Dispatch<SetStateAction<T>>` if the JSX passes them directly to a controlled input's updater).

    Move the block verbatim. Keep exactly:
    - `import { Dialog } from '@base-ui/react/dialog'` — do NOT swap to another dialog primitive
    - `Dialog.Root` / `Dialog.Portal` / `Dialog.Backdrop` / `Dialog.Popup` / `Dialog.Close` structure and every class string, notably `fixed inset-0 z-50 bg-black/40 backdrop-blur-sm` and `fixed left-1/2 top-1/2 z-50 -translate-x-1/2 -translate-y-1/2 w-[680px] max-h-[85vh] overflow-y-auto bg-background border rounded-lg shadow-xl flex flex-col`
    - the header `Edit Release` with its `X`-icon `Dialog.Close` render prop and `aria-label="Close"`
    - all six controlled fields with their `id`/`htmlFor` pairs (e.g. `release-name`), `disabled={isSaving}` flags, and `required` attributes
    - the GitLab milestone fieldset, gated on `showMilestoneSection` (the shell computes `gitlabMatch.type !== 'none' && !!matchedMilestone`)
    - both error banners: `Jira: {jiraError}` and `GitLab: {gitlabError}` with `rounded-md bg-destructive/10 px-3 py-2 text-sm text-destructive`
    - the footer with Cancel (`onCancel`, `disabled={isSaving}`) and Save (`onSave`, `disabled={isSaveDisabled}`), including the `isSaving ? <Loader2 className="size-3.5 animate-spin" /> Saving... : <Check className="size-3.5" /> Save` swap

    The `onOpenChange` body currently is `(o) => { if (!o) cancelEditing(); }`. Keep that logic in the shell's passed callback; the component just forwards `onOpenChange` to `Dialog.Root`.

    Do NOT import `useEditRelease`, `useQuery`, or any store hook into this file — it is presentational (D-08).

    In `ReleaseDetailPage.tsx`: replace the block with `<EditReleaseModal ... />` at its exact position (last child of the `<div ref={containerRef} ...>` wrapper, after the sidebar). Compute in the shell: `showMilestoneSection={gitlabMatch.type !== 'none' && !!matchedMilestone}` and `isSaveDisabled={isSaving || !editName.trim() || !isEditDirty || isMilestoneTitleInvalid}` — that boolean expression must be copied character-for-character from the current Save button. Drop the page's now-unused `Dialog`, `Input`, `Textarea`, `X`, `Loader2`, `Check` imports.
  </action>
  <acceptance_criteria>
    - `grep -c "export function EditReleaseModal" taskflow/src/routes/dashboard/release-detail/EditReleaseModal.tsx` returns 1
    - `grep -c "from '@base-ui/react/dialog'" taskflow/src/routes/dashboard/release-detail/EditReleaseModal.tsx` returns 1
    - File contains all of: `Edit Release`, `w-[680px] max-h-[85vh]`, `aria-label="Close"`, `Jira: {jiraError}`, `GitLab: {gitlabError}`, `animate-spin`
    - `grep -c "Dialog.Root\|Dialog.Popup" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0
    - `grep -c "isSaving || !editName.trim() || !isEditDirty || isMilestoneTitleInvalid" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 1
    - `grep -nE "useEditRelease|useQuery|useAuthStore" taskflow/src/routes/dashboard/release-detail/EditReleaseModal.tsx` returns no match
    - `cd taskflow && npm run check` shows no more than the 2 baseline errors
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && npm run check</automated>
  </verify>
  <done>The edit dialog renders from `EditReleaseModal.tsx` with identical markup and identical save-disabled logic; the page shell holds no dialog JSX.</done>
</task>

<task type="auto">
  <name>Task 2: Final structural audit and full-suite regression gate</name>
  <files>taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx (the finished shell — read it end to end)
    - taskflow/src/routes/dashboard/IssueDetailContent.tsx (the page-shell precedent to compare shape against, D-04)
    - .planning/phases/87-release-detail-decomposition/87-VALIDATION.md (the gate definitions and the 2-error baseline)
  </read_first>
  <action>
    Audit and, where needed, tidy the finished shell. Permitted changes in this task are limited to: removing imports that are now unused, and removing stale comments that referred to deleted code. Do NOT restructure JSX, do NOT rename anything, do NOT add abstractions.

    Confirm and record in the SUMMARY:
    - `wc -l taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` versus D-06's 150-250 target. If it lands above 250, report the actual number and the reason rather than inventing a further split — D-06 is an explicitly stated health indicator, not a hard gate.
    - The contents of `taskflow/src/routes/dashboard/release-detail/` — expected: `releaseSummaries.ts`, `releaseSummaries.test.ts`, `useReleaseDetail.ts`, `useEditRelease.ts`, `MetaRow.tsx`, `ReleaseDetailSkeleton.tsx`, `ReleaseHeader.tsx`, `DescriptionsSection.tsx`, `LabelSummarySection.tsx`, `IssuesSection.tsx`, `UnmatchedMRsSection.tsx`, `ReleaseDetailSidebar.tsx`, `EditReleaseModal.tsx`. No `index.ts` (D-05: omitted; the shell imports by direct path).
    - No speculative or placeholder file exists (D-16). Every file in the folder must contain code moved from `ReleaseDetailPage.tsx` or the tests for it.

    Run the phase gate: `npm run check` and the full `npm test` suite. `npm run check` MUST show no more than the 2 known pre-existing biome formatting errors in `src/routes/dashboard/BacklogPage.tsx` and `src/routes/dashboard/BacklogRow.tsx`. Do NOT fix those two files — they are out of scope (D-16) and fixing them would obscure the baseline comparison. If a NEW error appears in any `release-detail/` file, `ReleaseDetailPage.tsx`, or `services/jira.ts`, fix that file.

    Confirm the route entry for `/release/:versionId` is untouched — `ReleaseDetailPage.tsx` still lives at `routes/dashboard/ReleaseDetailPage.tsx` with a default export (D-04, no router change).
  </action>
  <acceptance_criteria>
    - `cd taskflow && npm test` exits 0 (full suite, zero regressions — D-15)
    - `cd taskflow && npm run check` output lists errors ONLY in `BacklogPage.tsx` and `BacklogRow.tsx`, and no more than 2 total
    - `ls taskflow/src/routes/dashboard/release-detail/` lists exactly the 13 expected files and no `index.ts`
    - `grep -c "export default function ReleaseDetailPage" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 1
    - `grep -c "useQuery(\|Dialog.Root\|cursor-ew-resize\|async function fetch" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0
    - `grep -rc "TODO\|placeholder\|Phase 88\|Phase 89\|Phase 90\|Phase 91" taskflow/src/routes/dashboard/release-detail/` shows 0 matches in every file (D-16: no forward-shaping)
    - `wc -l taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` is recorded in the SUMMARY
    - `git grep -n "ReleaseDetailPage" -- taskflow/src` shows the route import path unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && npm run check && npm test</automated>
  </verify>
  <done>Full suite green, `npm run check` at the 2-error baseline, `release-detail/` holds 13 real files with no barrel and no forward-shaped placeholders, and the shell's LOC is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT click-through of the release detail page</name>
  <what-built>
    `ReleaseDetailPage.tsx` (1518 LOC) has been decomposed into 13 files under `taskflow/src/routes/dashboard/release-detail/`: a pure computations module with unit tests, two co-located hooks (`useReleaseDetail`, `useEditRelease`), and eight presentational components. Two Jira fetchers moved to `services/jira.ts` behind `apiFetch`. Full test suite is green and `npm run check` is at its known 2-error baseline.
  </what-built>
  <files>taskflow/src/routes/dashboard/ReleaseDetailPage.tsx, taskflow/src/routes/dashboard/release-detail/</files>
  <read_first>
    - .planning/phases/87-release-detail-decomposition/87-VALIDATION.md (§ Manual-Only Verifications — the authoritative click-through path)
    - .planning/phases/87-release-detail-decomposition/87-06-SUMMARY.md drafts from Tasks 1-2 (what changed, for framing the walkthrough)
  </read_first>
  <action>
    Start the app with the project's normal dev command from `taskflow/`, navigate to the Releases tab, and hand the click-through below to the user. Do not substitute an automated assertion for this step — D-14 declined a page-level characterization test and 87-VALIDATION.md records this as the phase's known validation ceiling. Capture every observation the user reports verbatim into the SUMMARY, including "no differences observed" if that is the result.
  </action>
  <how-to-verify>
    `ReleaseDetailPage` has no automated test coverage and D-14 deliberately declined adding a characterization test, so Success Criterion 1 ("renders identically") can only be verified by hand. Run the app and walk `/release/:versionId` (Releases tab → click a release):

    1. Loading: confirm the skeleton appears, and that the back/breadcrumb bar is visible WHILE the skeleton is showing (not only after load).
    2. Header: Rocket icon, `v{id}` in mono, release name as the heading.
    3. Descriptions: check a release where only Jira has a description; one where a GitLab milestone matched and both have descriptions (headings should read "Jira Description" and "GitLab Description", GitLab rendered as markdown); and one where a milestone matched but neither has text (should collapse to a single "Description" section with italic "No description").
    4. Labels: label chips render with their GitLab colors and `(count)` suffixes.
    5. Issues: count badge, green progress bar, and the orange "no milestone" warning on an unmatched release. Confirm all four MR-match states appear across rows: matched MR, no MR, wrong-milestone warning, and the "Missing MR" badge.
    6. Unmatched MRs: confirm this block still sits INSIDE the Issues section — separated by the thin top border, indented in the same flow, NOT as a separate card below the section. Open the browser devtools element inspector and confirm the Unmatched-MRs `<div>` is a descendant of the Issues `<section>`.
    7. Click a ticket key inside an unmatched MR title → navigates to the issue with the release added to the breadcrumb trail. Click a row in the issues table → opens the issue.
    8. Sidebar: all Details rows render; MR-state, issue-status and story-point distribution blocks render; drag the left edge to resize and confirm the handle highlights on hover and while dragging.
    9. Edit modal: click Edit, change the name, confirm Save enables only when something changed; clear the name → Save disables; clear a matched milestone title → Save disables. Save and confirm the page refreshes with the new values.
    10. Pin/unpin the release from the action buttons, and use "Open in Jira".
    11. Cache sharing: go Releases tab → release detail → back to Releases tab and confirm no visible refetch/loading flash for the releases list (query keys `jira-fix-versions`, `jira-version-counts`, `gitlab-milestones` must still be shared).

    Report any visual or behavioral difference from before the refactor, however small.
  </how-to-verify>
  <acceptance_criteria>
    - Every one of the 11 steps above is exercised and its result reported
    - Step 6's DOM check explicitly confirms Unmatched MRs is a descendant of the Issues `<section>` (D-12b)
    - Step 11 confirms no refetch waterfall when returning to the Releases tab (D-11)
    - Any reported difference is either fixed or recorded in the SUMMARY as an accepted deviation with a reason
  </acceptance_criteria>
  <verify>
    <human-check>User walks all 11 steps and confirms the page is visually and behaviorally identical to its pre-refactor form</human-check>
    <automated>cd taskflow && npm test</automated>
  </verify>
  <done>The user has exercised all 11 steps and either approved the page as identical, or every reported difference has been fixed or recorded as an accepted deviation in the SUMMARY.</done>
  <resume-signal>Type "approved" if the page behaves identically, or describe every difference you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → Jira/GitLab write APIs | Edit modal fields feed `updateFixVersion` / `updateMilestone` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-13 | Tampering | Empty/blank release name or milestone title submitted to Jira/GitLab | mitigate | Preserve the `isSaveDisabled` expression verbatim (`isSaving \|\| !editName.trim() \|\| !isEditDirty \|\| isMilestoneTitleInvalid`) and the `required` attribute on the name Input; these are the existing client-side guards |
| T-87-14 | Elevation of Privilege | Modal rendered outside the app's dialog primitive, bypassing focus trapping | accept | `@base-ui/react/dialog` is preserved verbatim; no primitive swap is permitted by this plan |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `cd taskflow && npm test` exits 0 (full suite)
- `cd taskflow && npm run check` reports at most the 2 known `BacklogPage.tsx` / `BacklogRow.tsx` baseline errors
- Manual UAT click-through completed per 87-VALIDATION.md § Manual-Only Verifications, with the D-12b DOM nesting check and the D-11 cache-sharing check both explicitly confirmed
</verification>

<success_criteria>
- FOUND-01 SC1: the release detail page renders and behaves identically before and after the refactor (manual UAT approved)
- FOUND-01 SC2: release detail code lives in `release-detail/` with one file per section/hook, mirroring `issue-detail/`; the shell stays at `routes/dashboard/ReleaseDetailPage.tsx` with no router change
- FOUND-01 SC3: the full suite passes with zero regressions, including `ReleasesTab.test.tsx` and `UpcomingReleasesTimeline.test.tsx`
- Page shell LOC recorded against D-06's 150-250 target
- No `index.ts` barrel, no placeholder files, no Phase 88-91 seams (D-05, D-16)
</success_criteria>

<output>
Create `.planning/phases/87-release-detail-decomposition/87-06-SUMMARY.md` when done. Record: final `ReleaseDetailPage.tsx` LOC, the full `release-detail/` file listing, the `npm run check` error count with filenames, and the manual UAT result including the step-6 DOM nesting confirmation and the step-11 cache-sharing confirmation.
</output>
