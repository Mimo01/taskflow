---
phase: 87-release-detail-decomposition
plan: 05
type: execute
wave: 5
depends_on: ["87-04"]
files_modified:
  - taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx
  - taskflow/src/routes/dashboard/release-detail/useEditRelease.ts
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
autonomous: true
requirements: [FOUND-01]

must_haves:
  truths:
    - "The right sidebar still shows Details metadata plus the MR-state, issue-status and story-point distribution blocks, and still drag-resizes"
    - "Saving a release still writes Jira and GitLab independently and keeps the modal open with a per-source error on partial failure"
    - "The four post-save cache invalidations still fire with their original query keys"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx"
      provides: "Right sidebar incl. drag handle, Details rows and distribution blocks (D-01)"
      contains: "cursor-ew-resize"
    - path: "taskflow/src/routes/dashboard/release-detail/useEditRelease.ts"
      provides: "Edit-modal state, diff builders and combined save with invalidations"
      contains: "export function useEditRelease"
  key_links:
    - from: "taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx"
      to: "./MetaRow"
      via: "same-folder relative import of the private MetaRow copy"
      pattern: "from '\\./MetaRow'"
    - from: "taskflow/src/routes/dashboard/release-detail/useEditRelease.ts"
      to: "queryClient.invalidateQueries"
      via: "four invalidations preserved verbatim after a successful save"
      pattern: "invalidateQueries"
---

<objective>
Extract the right sidebar into `release-detail/ReleaseDetailSidebar.tsx`, and lift the edit-modal state machine out of the page shell into `release-detail/useEditRelease.ts`.

Purpose: these are the two remaining heavyweight blocks in the shell. Extracting `useEditRelease` first also collapses the ~22-prop surface `EditReleaseModal` would otherwise need in Plan 06, and gets the shell into D-06's 150-250 LOC range.

Output: one sidebar component and one co-located edit hook; the page shell keeps only routing, breadcrumb, pin state, `useResizable`, and JSX composition.

Scope note (D-16): `useEditRelease` is a MOVE of existing state and handlers, not new structure. Do not add validation, optimistic updates, mutation libraries, or memoization that does not exist today.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-release-detail-decomposition/87-CONTEXT.md
@.planning/phases/87-release-detail-decomposition/87-RESEARCH.md
@.planning/phases/87-release-detail-decomposition/87-PATTERNS.md
@.planning/phases/87-release-detail-decomposition/87-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ReleaseDetailSidebar.tsx</name>
  <files>taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx, taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx (the `{/* Right sidebar */}` block — pre-refactor L1104-1303 — from the `<div className={\`relative border-l overflow-auto p-4 shrink-0...\`}` through its closing `</div>`)
    - taskflow/src/routes/dashboard/issue-detail/IssueDetailSidebar.tsx (the direct structural precedent: interface above component, composes sub-blocks, keeps small groupings inline)
    - taskflow/src/routes/dashboard/release-detail/MetaRow.tsx (the private copy this sidebar consumes)
    - .planning/phases/87-release-detail-decomposition/87-RESEARCH.md (§2 ReleaseDetailSidebar props contract, §9 hazard 7)
  </read_first>
  <action>
    Create `release-detail/ReleaseDetailSidebar.tsx` with `interface ReleaseDetailSidebarProps` and `export function ReleaseDetailSidebar(...)`.

    Props (per RESEARCH §2): `width: number`, `isDragging: boolean`, `onResizeMouseDown: (e: React.MouseEvent) => void`, `handleHovered: boolean`, `setHandleHovered: (b: boolean) => void`, `onStartEditing: () => void`, `version: FixVersion`, `gitlabMatch: ReleaseMatch`, `matchedMilestone: GitLabMilestone | null`, `milestoneMRsLoaded: boolean`, `labelCoverage: LabelCoverage | null`, `mrStateCounts: { merged: number; opened: number; closed: number }`, `hasMrs: boolean`, `hasIssues: boolean`, `issueStatusCounts: { new: number; indeterminate: number; done: number }`, `hasStoryPoints: boolean`, `storyPoints: { total: number; completed: number }`. Adjust this list only if the actual JSX reads something not covered — do not drop anything it reads.

    Move the entire block verbatim, including:
    - the outer `<div className={\`relative border-l overflow-auto p-4 shrink-0${isDragging ? '' : ' transition-all duration-200'}\`} style={{ width }}>` — keep the template-literal class expression and the inline `style={{ width }}` exactly
    - the drag-handle `<div aria-hidden="true" ... className="absolute left-0 top-0 h-full w-3 cursor-ew-resize z-20 border-l border-border transition-colors duration-100" />` with its `onMouseDown` / `onMouseEnter` / `onMouseLeave` and the `borderColor: isDragging || handleHovered ? 'var(--ring)' : undefined` inline style
    - the `Details` heading row with its ghost `Edit` button (`Pencil` icon) calling `onStartEditing`
    - every `MetaRow` in the Details group and the MR-state / issue-status / story-point distribution blocks, each with its exact class strings

    `useResizable` STAYS in the page shell — `containerRef` is attached to the outer flex wrapper spanning both columns, which the sidebar does not own (hazard 7). Only `width`, `isDragging` and `handleMouseDown` cross the prop boundary. Do NOT import `useResizable`, `useSettingsStore`, or any query hook into this file — the sidebar is presentational and props-driven (D-08).

    Keep the distribution blocks INLINE in this one file — do not create separate files for them (matching `IssueDetailSidebar`'s granularity per PATTERNS.md). Import `MetaRow` from `./MetaRow`, `statusPillClass` from `@/lib/statusStyles` if the JSX uses it, and `LabelCoverage` type from `./releaseSummaries`.

    In `ReleaseDetailPage.tsx`: replace the block with `<ReleaseDetailSidebar ... />` at its exact position — the second child of `<div ref={containerRef} className="flex flex-1 overflow-hidden">`, after the left column div. Pass `milestoneMRsLoaded={!!milestoneMRs}`, `hasMrs={releaseMrs.length > 0}`, `hasIssues={releaseIssues.length > 0}`. Drop page imports that become unused (`Pencil`, `MetaRow`, `statusPillClass`, `Calendar`, `Check`, `Tag`, `GitMerge` — only those genuinely no longer referenced).
  </action>
  <acceptance_criteria>
    - `grep -c "export function ReleaseDetailSidebar" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx` returns 1
    - `grep -c "interface ReleaseDetailSidebarProps" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx` returns 1
    - File contains all of: `cursor-ew-resize`, `relative border-l overflow-auto p-4 shrink-0`, `var(--ring)`, `aria-hidden="true"`, `style={{ width }}`
    - `grep -c "from './MetaRow'" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx` returns 1
    - `grep -nE "useResizable|useQuery|useSettingsStore|useAuthStore" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx` returns no match
    - `grep -c "useResizable(" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 1 (stayed in the shell)
    - `grep -c "cursor-ew-resize" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0
    - `grep -c "<ReleaseDetailSidebar" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 1
    - `cd taskflow && npm run check` shows no more than the 2 baseline errors
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && npm run check</automated>
  </verify>
  <done>The sidebar renders from its own file with identical markup and drag behavior; `useResizable`/`containerRef` remain in the shell.</done>
</task>

<task type="auto">
  <name>Task 2: Extract useEditRelease.ts (edit state, diff builders, combined save)</name>
  <files>taskflow/src/routes/dashboard/release-detail/useEditRelease.ts, taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx (the edit state declarations, `startEditing`, `cancelEditing`, `buildJiraDiff`, `buildGitlabDiff`, `isEditDirty`, `isMilestoneTitleInvalid`, and the full `handleSave` including its four `queryClient.invalidateQueries` calls — pre-refactor L186-198 and L478-618)
    - taskflow/src/routes/dashboard/issue-detail/useFieldMutation.ts (the co-located hook + invalidation-commenting convention)
    - .planning/phases/87-release-detail-decomposition/87-RESEARCH.md (§2 EditReleaseModal prop inventory, §3 the four invalidation keys)
  </read_first>
  <action>
    Create `release-detail/useEditRelease.ts` — co-located in `release-detail/`, NOT `src/hooks/` (D-10) — with a single named export `export function useEditRelease(args)` taking what the moved code closes over: `version: FixVersion | null`, `matchedMilestone: GitLabMilestone | null`, `versionId: string | undefined`, `jiraBaseUrl`, `activeJiraProject`, `gitlabBaseUrl`, `activeGitlabProject`, `gitlabToken`. Call `useQueryClient()` inside the hook.

    Move VERBATIM: the ten `useState` declarations (`editing`, `editName`, `editDate`, `editDescription`, `editReleased`, `editMilestoneTitle`, `editMilestoneDescription`, `jiraError`, `gitlabError`, `isSaving`), `startEditing`, `cancelEditing`, `buildJiraDiff`, `buildGitlabDiff`, `isEditDirty`, `isMilestoneTitleInvalid`, and `handleSave`.

    Preserve exactly, with no restructuring:
    - `buildJiraDiff`'s field-by-field comparisons including `editDate !== (version?.releaseDate ?? '')` producing `fields.releaseDate = editDate || null`
    - `buildGitlabDiff`'s early `if (!matchedMilestone) return fields;`
    - `isEditDirty` recomputing both diffs on every render (do NOT memoize)
    - `isMilestoneTitleInvalid = !!matchedMilestone && editMilestoneTitle.trim() === ''`
    - `handleSave`'s "nothing changed → just close" early return, the `Promise.allSettled([jiraPromise ?? Promise.resolve(null), gitlabPromise ?? Promise.resolve(null)])` shape, the per-source rejection handling with the `'Failed to update Jira'` / `'Failed to update GitLab milestone'` default messages, the no-rollback partial-failure semantics, and the `readSecret('jira-pat').catch(() => null)` + `throw new Error('No credentials')` preamble inside the Jira promise
    - all four `queryClient.invalidateQueries` calls with their exact keys: `['jira-fix-versions', activeJiraProject]`, `['jira-version-counts', versionId]`, `['gitlab-milestones', activeGitlabProject]`, `['gitlab-milestone-mrs', activeGitlabProject]`, plus the existing explanatory comment above the milestone-MRs invalidation. Keep them gated on their respective `status === 'fulfilled'` branches
    - whatever `handleSave` does after the invalidations (closing the modal / clearing `isSaving`) exactly as written

    Do NOT convert `handleSave` to `useMutation`. Do NOT add `useCallback`/`useMemo`. Do NOT change error strings.

    Return one flat object with every value the modal and sidebar need: `editing`, `setEditing`, `editName`, `setEditName`, `editDate`, `setEditDate`, `editDescription`, `setEditDescription`, `editReleased`, `setEditReleased`, `editMilestoneTitle`, `setEditMilestoneTitle`, `editMilestoneDescription`, `setEditMilestoneDescription`, `jiraError`, `gitlabError`, `isSaving`, `isEditDirty`, `isMilestoneTitleInvalid`, `startEditing`, `cancelEditing`, `handleSave`.

    In `ReleaseDetailPage.tsx`: delete all the moved declarations, call `useEditRelease({ ... })` unconditionally alongside `useReleaseDetail(versionId)` (both above the `if (!versionId) return null;` guard — hazard 9), and destructure what the JSX uses. Remove `useQueryClient`, `updateFixVersion`, `updateMilestone`, and `readSecret` imports from the page if they become unused. The edit-modal JSX stays inline in the page for now; Plan 06 extracts it.
  </action>
  <acceptance_criteria>
    - `grep -c "export function useEditRelease" taskflow/src/routes/dashboard/release-detail/useEditRelease.ts` returns 1
    - `grep -c "invalidateQueries" taskflow/src/routes/dashboard/release-detail/useEditRelease.ts` returns 4
    - `useEditRelease.ts` contains each of: `'jira-fix-versions'`, `'jira-version-counts'`, `'gitlab-milestones'`, `'gitlab-milestone-mrs'`, `Promise.allSettled`, `'Failed to update GitLab milestone'`, `'No credentials'`
    - `grep -c "useMutation\|useCallback\|useMemo" taskflow/src/routes/dashboard/release-detail/useEditRelease.ts` returns 0
    - `grep -c "useState(" taskflow/src/routes/dashboard/release-detail/useEditRelease.ts` returns 10
    - `grep -c "invalidateQueries\|buildJiraDiff\|handleSave = async" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0
    - `grep -c "useEditRelease(" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 1
    - In `ReleaseDetailPage.tsx`, both `useReleaseDetail(` and `useEditRelease(` appear at line numbers BELOW no conditional return — confirm `if (!versionId) return null` has a higher line number than both (`grep -n`)
    - `cd taskflow && npm run check` shows no more than the 2 baseline errors
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && npm run check && npm test -- ReleasesTab.test.tsx UpcomingReleasesTimeline.test.tsx releaseSummaries.test.ts</automated>
  </verify>
  <done>All edit-modal state, diff builders, and the combined save (with its four verbatim invalidations) live in `useEditRelease.ts`; the page shell calls it unconditionally and holds no edit logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Jira/GitLab write APIs | `handleSave` performs authenticated writes (`updateFixVersion`, `updateMilestone`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-10 | Tampering | `handleSave` sending unintended fields on write | mitigate | Preserve `buildJiraDiff` / `buildGitlabDiff` verbatim so only changed fields are transmitted; do not replace the diff with a full-object PUT |
| T-87-11 | Information Disclosure | `readSecret('jira-pat')` moving into `useEditRelease.ts` | mitigate | Token stays function-local inside the Jira promise; it is never returned from the hook, stored in state, or logged |
| T-87-12 | Repudiation | Partial-failure save leaving Jira and GitLab out of sync | accept | Existing, deliberate design (per-source error, no rollback) — preserved unchanged; this phase makes no behavior decision here |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `cd taskflow && npm run check` shows zero NEW errors beyond the 2-error baseline
- `cd taskflow && npm test -- ReleasesTab.test.tsx UpcomingReleasesTimeline.test.tsx releaseSummaries.test.ts` exits 0
- No `release-detail/*.tsx` presentational file imports `useResizable`, `useQuery`, `useAuthStore`, or `useSettingsStore`
</verification>

<success_criteria>
- `ReleaseDetailSidebar.tsx` renders the sidebar with identical markup, including the drag handle and its hover/drag border color
- `useResizable` and `containerRef` remain in the page shell
- `useEditRelease.ts` owns all edit state and the combined save, with the four invalidation keys unchanged
- No memoization or mutation-library conversion was introduced
</success_criteria>

<output>
Create `.planning/phases/87-release-detail-decomposition/87-05-SUMMARY.md` when done. Record `useEditRelease`'s exact return-object field list so Plan 06 can wire `EditReleaseModal` props without re-reading the hook, and note the current `wc -l` of `ReleaseDetailPage.tsx`.
</output>
