---
phase: 87-release-detail-decomposition
plan: 04
type: execute
wave: 4
depends_on: ["87-03"]
files_modified:
  - taskflow/src/routes/dashboard/release-detail/UnmatchedMRsSection.tsx
  - taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
autonomous: true
requirements: [FOUND-01]

must_haves:
  truths:
    - "The Unmatched MRs block still renders INSIDE the Issues <section> wrapper, not as a sibling (D-12b)"
    - "All four MR-match states (matched / none / wrong-milestone / missing MR) still render in the issues table"
    - "Clicking a ticket key inside an unmatched MR title still pushes the release breadcrumb and navigates to the issue"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx"
      provides: "Issues heading, progress bar, milestone warning, issues table, and the nested UnmatchedMRsSection"
      contains: "<UnmatchedMRsSection"
    - path: "taskflow/src/routes/dashboard/release-detail/UnmatchedMRsSection.tsx"
      provides: "Unmatched MRs list, rendered as a child of IssuesSection"
      contains: "mt-4 pt-4 border-t border-border/50"
  key_links:
    - from: "taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx"
      to: "./UnmatchedMRsSection"
      via: "same-folder relative import, rendered inside the <section> element"
      pattern: "from '\\./UnmatchedMRsSection'"
    - from: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      to: "./release-detail/IssuesSection"
      via: "single <IssuesSection /> render; UnmatchedMRsSection is NOT referenced by the shell"
      pattern: "<IssuesSection"
---

<objective>
Extract the Issues block — heading + badge, progress bar, milestone warning, issues table with MR matching — into `release-detail/IssuesSection.tsx`, with the Unmatched MRs list as `release-detail/UnmatchedMRsSection.tsx` rendered as a CHILD from inside `IssuesSection`'s JSX.

Purpose: D-01's largest section. This plan carries the phase's single highest-risk extraction trap (D-12b / RESEARCH §9 hazard 1).

Output: two new files; the page shell renders `<IssuesSection />` and never references `UnmatchedMRsSection`.

D-02: do NOT split `IssuesSection` further. No separate `IssuesTable`, `IssueRow`, `ReleaseProgressBar`, or `MilestoneWarning` files — those are explicitly deferred to Phase 89/90.
D-03 decision (planner's call, recorded): the Action Buttons block (Pin / Open in Jira) STAYS in the page shell. It sits after the Issues `</section>` as a sibling in the left column's `space-y-6` flow, structurally unrelated to the header, and moving it into `ReleaseHeader` would change DOM order. It also carries all five `usePinnedTabsStore` selector reads (hazard 6), which stay together in the shell.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-release-detail-decomposition/87-CONTEXT.md
@.planning/phases/87-release-detail-decomposition/87-RESEARCH.md
@.planning/phases/87-release-detail-decomposition/87-PATTERNS.md
@.planning/phases/87-release-detail-decomposition/87-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UnmatchedMRsSection.tsx as a presentational leaf</name>
  <files>taskflow/src/routes/dashboard/release-detail/UnmatchedMRsSection.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx (the `{/* Unmatched MRs section */}` block — `{unmatchedMRs.length > 0 && (<div className="mt-4 pt-4 border-t border-border/50">...)}` through its closing `)}`)
    - taskflow/src/routes/dashboard/issue-detail/MergeRequestsSection.tsx (the leaf-section analog: named export, interface above component, callback props instead of routing hooks)
    - .planning/phases/87-release-detail-decomposition/87-RESEARCH.md (§2 UnmatchedMRsSection props subset, §9 hazard 4)
  </read_first>
  <action>
    Create `release-detail/UnmatchedMRsSection.tsx` with `interface UnmatchedMRsSectionProps { unmatchedMRs: GitLabMR[]; onNavigateToIssueFromMR: (key: string) => void }` and `export function UnmatchedMRsSection(...)`.

    Move the block verbatim. Keep, exactly:
    - the `unmatchedMRs.length > 0` gate — implement as `if (unmatchedMRs.length === 0) return null;` at the top so the caller renders it unconditionally
    - the wrapper `<div className="mt-4 pt-4 border-t border-border/50">` (this top border is what visually separates it from the issues table inside the shared `<section>` — do not change or drop it)
    - the `Info` icon heading with `<h4 className="text-sm font-medium">Unmatched MRs` plus the count `<Badge variant="secondary" className="ml-1.5 text-xs">`
    - the subtitle `<p className="text-xs text-muted-foreground mb-2">MRs in milestone not linked to any Jira task</p>`
    - the `space-y-1` list, each row's `GitMerge` icon with its merged/opened/else color ternary, the `!{mr.iid}` button calling `openUrl(mr.web_url)`, and the `line-clamp-1` title span
    - the inline IIFE that runs `extractTicketKeys(mr.title)` and splices `<button>` elements around each key, including `e.stopPropagation()` and the trailing-`remaining` push

    Replace ONLY the ticket-key button's onClick body: today it calls `breadcrumbPush({ path: \`/release/${versionId}\`, label: version.name })` then `navigate(\`/issue/${key}\`)`. That two-step closure moves to the caller; here the handler becomes `onNavigateToIssueFromMR(key)` after `e.stopPropagation()`. Do NOT import `useNavigate` or `useBreadcrumbStore` in this file (D-08, hazard 4). `openUrl` from `@tauri-apps/plugin-opener` and `extractTicketKeys` from `@/services/linkEngine` DO move into this file — they are not React hooks.

    Do NOT wire this component into the page or into `IssuesSection` in this task; Task 2 does that.
  </action>
  <acceptance_criteria>
    - `grep -c "export function UnmatchedMRsSection" taskflow/src/routes/dashboard/release-detail/UnmatchedMRsSection.tsx` returns 1
    - File contains the literal strings `mt-4 pt-4 border-t border-border/50`, `MRs in milestone not linked to any Jira task`, `line-clamp-1`, `extractTicketKeys`, and `e.stopPropagation()`
    - `grep -c "onNavigateToIssueFromMR(key)" taskflow/src/routes/dashboard/release-detail/UnmatchedMRsSection.tsx` returns 1
    - `grep -nE "useNavigate|useBreadcrumbStore|useQuery" taskflow/src/routes/dashboard/release-detail/UnmatchedMRsSection.tsx` returns no match
    - `grep -c "return null" taskflow/src/routes/dashboard/release-detail/UnmatchedMRsSection.tsx` is at least 1 (the empty-list early return)
    - `cd taskflow && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit</automated>
  </verify>
  <done>`UnmatchedMRsSection.tsx` exists as a props-driven leaf with the original markup and no routing/store hook imports.</done>
</task>

<task type="auto">
  <name>Task 2: Create IssuesSection.tsx with UnmatchedMRsSection nested inside its section wrapper, and wire the page</name>
  <files>taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx, taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx (the full `{/* Issues with MR matching */}` block from `<section>` through its matching `</section>` — pre-refactor L778-1064 — plus the `seedReleaseBreadcrumb` and `openIssueFull` closures and the `useOutletContext` destructure)
    - taskflow/src/routes/dashboard/release-detail/UnmatchedMRsSection.tsx (Task 1's props interface)
    - taskflow/src/routes/dashboard/issue-detail/IssueDetailSidebar.tsx (the analog for "a section component that imports and renders a sibling section file as a JSX child")
    - .planning/phases/87-release-detail-decomposition/87-RESEARCH.md (§2 IssuesSection props contract, §9 hazards 1, 3, 4, 5)
  </read_first>
  <action>
    Create `release-detail/IssuesSection.tsx` with `interface IssuesSectionProps` and `export function IssuesSection(...)`.

    Props (per RESEARCH §2): `issueCounts: { issuesFixed: number; issuesTotal: number } | undefined`, `gitlabMatchType: ReleaseMatch['type']`, `hasReleaseDate: boolean`, `isLoadingIssues: boolean`, `matchedRows: Array<{ issue: JiraIssue; mr: GitLabMR | null }>`, `wrongMilestoneByKey: Map<string, GitLabMR>`, `unmatchedMRs: GitLabMR[]`, `onOpenIssue: (key: string) => void`, `onSeedBreadcrumb: () => void`, `onNavigateToIssueFromMR: (key: string) => void`.

    Move the entire `<section>...</section>` verbatim: the Issues `<h3>` + `issueCounts` badge (`{issuesFixed} / {issuesTotal} done`), the `Progress` bar gated on `issueCounts && issueCounts.issuesTotal > 0` with `Math.round((issuesFixed / issuesTotal) * 100)` and `indicatorClassName="bg-green-500"`, the `gitlabMatch.type === 'none'` milestone-warning div with its `AlertTriangle` and orange border/background classes, the loading state, the issues table with every column and all four MR-match states (matched MR link, no-MR, wrong-milestone warning via `wrongMilestoneByKey`, and the `Missing MR` badge with `title="No merge request found in milestone"`).

    THE CRITICAL CONSTRAINT (D-12b, RESEARCH §9 hazard 1): render `<UnmatchedMRsSection unmatchedMRs={unmatchedMRs} onNavigateToIssueFromMR={onNavigateToIssueFromMR} />` as the LAST CHILD INSIDE the `<section>` element, in the exact position the `{/* Unmatched MRs section */}` block occupies today — after the issues table's closing `)}` and before `</section>`. Do NOT close `</section>` before it. Do NOT return a fragment with two siblings. Do NOT render it from the page shell. Import it via same-folder relative path `./UnmatchedMRsSection`.

    Row click handlers: today the row onClick calls `seedReleaseBreadcrumb()` then `(onOpenIssue ?? openIssueFull)(issue.key)`. In the component these become `onSeedBreadcrumb()` then `onOpenIssue(key)` — the shell resolves the `??` fallback before passing (hazard 3). Do NOT call `useOutletContext`, `useNavigate`, or `useBreadcrumbStore` inside this file.

    In `ReleaseDetailPage.tsx`: replace the whole block with a single `<IssuesSection ... />` at its exact position inside `<div className="p-6 space-y-6">` — immediately after `<LabelSummarySection />` and immediately before the `{/* Action buttons */}` div. Define in the shell (or reuse the existing closures): `resolvedOnOpenIssue = onOpenIssue ?? openIssueFull`, and `handleNavigateToIssueFromMR = (key: string) => { breadcrumbPush({ path: \`/release/${versionId}\`, label: version.name }); navigate(\`/issue/${key}\`); }`. Keep `seedReleaseBreadcrumb` a single shared closure passed as `onSeedBreadcrumb` — do not duplicate it per section (hazard 5). Pass `hasReleaseDate={!!version.releaseDate}`.

    The Action Buttons block and all five `usePinnedTabsStore` reads STAY in the page shell, unchanged (D-03 decision above).

    Drop page imports that become unused (`Progress`, `AlertTriangle`, `Info`, `GitMerge`, `extractTicketKeys`, `openUrl` — but keep `openUrl` if `handleOpenInJira` still uses it, and keep `ExternalLink`/`Pin`/`Button` for the action buttons).
  </action>
  <acceptance_criteria>
    - `grep -c "export function IssuesSection" taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx` returns 1
    - `grep -c "from './UnmatchedMRsSection'" taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx` returns 1
    - In `IssuesSection.tsx`, the line number of `<UnmatchedMRsSection` is LESS than the line number of `</section>` — verify with `grep -n "<UnmatchedMRsSection\|</section>" taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx` and confirm the ordering (this is the D-12b gate)
    - `grep -c "UnmatchedMRsSection" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0 (the shell must NOT reference it)
    - `grep -c "<IssuesSection" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 1
    - `IssuesSection.tsx` contains all of: `indicatorClassName="bg-green-500"`, `title="No merge request found in milestone"`, `wrongMilestoneByKey`, `border-orange-300 dark:border-orange-700`
    - `grep -nE "useOutletContext|useNavigate|useBreadcrumbStore|useQuery" taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx` returns no match
    - No separate `IssuesTable.tsx`, `IssueRow.tsx`, `ReleaseProgressBar.tsx`, or `MilestoneWarning.tsx` file exists in `release-detail/` (D-02)
    - `grep -c "Open in Jira" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 1 (action buttons stayed in the shell per D-03)
    - `cd taskflow && npm run check` shows no more than the 2 baseline errors
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && npm run check && npm test -- ReleasesTab.test.tsx UpcomingReleasesTimeline.test.tsx releaseSummaries.test.ts</automated>
  </verify>
  <done>`IssuesSection` renders the full issues block with `UnmatchedMRsSection` nested inside the same `<section>` wrapper; the page shell renders only `<IssuesSection />` and retains the action buttons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote content → DOM / navigation | GitLab MR titles and Jira issue keys drive rendered text and in-app navigation targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-08 | Tampering | `openUrl(mr.web_url)` in `UnmatchedMRsSection` opens a remote-supplied URL in the OS browser | accept | Pre-existing behavior moved verbatim; `web_url` originates from the user's own authenticated GitLab instance. No change to the call in this phase |
| T-87-09 | Tampering | `extractTicketKeys(mr.title)` result interpolated into a `/issue/${key}` route | mitigate | Keep `extractTicketKeys` as the only key source and pass the key through the shell-provided `onNavigateToIssueFromMR` closure unchanged — do not construct routes from raw `mr.title` substrings |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `cd taskflow && npm run check` shows zero NEW errors beyond the 2-error baseline
- `cd taskflow && npm test -- ReleasesTab.test.tsx UpcomingReleasesTimeline.test.tsx releaseSummaries.test.ts` exits 0
- D-12b structural gate: `<UnmatchedMRsSection` appears above `</section>` inside `IssuesSection.tsx`, and `UnmatchedMRsSection` is absent from `ReleaseDetailPage.tsx`
</verification>

<success_criteria>
- `IssuesSection.tsx` and `UnmatchedMRsSection.tsx` exist as presentational named exports
- Unmatched MRs renders as a nested child inside the Issues `<section>`, preserving the `mt-4 pt-4 border-t border-border/50` separator in the same flow
- `IssuesSection` is not split further (D-02)
- Action buttons and pinned-tab state remain in the page shell (D-03)
- No routing or store hooks are imported by either new file
</success_criteria>

<output>
Create `.planning/phases/87-release-detail-decomposition/87-04-SUMMARY.md` when done. Explicitly confirm the D-12b nesting: quote the `grep -n` output showing `<UnmatchedMRsSection` preceding `</section>` in `IssuesSection.tsx`.
</output>
