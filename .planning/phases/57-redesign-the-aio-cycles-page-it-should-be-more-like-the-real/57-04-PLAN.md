---
phase: 57
plan: 04
type: execute
wave: 2
depends_on: [57-02, 57-03]
files_modified:
  - taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx
  - taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx
  - taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx
autonomous: true
requirements: [AION-03]

must_haves:
  truths:
    - "The page renders a two-panel layout: left = folder tree (w-64), right = cycle list table"
    - "The left panel renders a recursive folder tree from fetchAioFolderTree, with expand/collapse chevrons and a count badge per folder showing fetchAioFolderCycleCounts[folder.ID]"
    - "An 'Ungrouped' entry appears at the bottom of the tree iff countMap['-1'] > 0"
    - "On first load, the first root folder auto-expands and the first folder with count > 0 is auto-selected"
    - "Clicking a folder selects it and the right panel shows that folder's cycles (filtered per 57-PROBE-FINDINGS.md A5: server-side via folderID param OR client-side from the all-cycles list)"
    - "The cycle list table has exactly 5 columns: Key (mono w-28) | Name (NavLink flex-1) | Owner (w-32) | Total tests (w-20 text-right) | Progress bar (w-44)"
    - "Each cycle row's Owner cell shows the JiraAssignableUser.displayName from a useQuery on fetchJiraUserByUsername(cycle.detail.ownedByID), or a Skeleton while loading, or the raw ownedByID string on null (404/error per D-08)"
    - "The Owner column dedupes by unique ownedByID — for N cycles with K unique owners, exactly K useQuery calls fire (NOT N)"
    - "The Progress column renders a h-1.5 rounded-full bar with 5 segments (green pass / red fail / orange blocked / blue inProgress / muted notRun) whose widths sum to summary.totalTests percentage, driven by AIO_STATUS_MAP lookup on numeric-string testRunDistribution keys"
    - "A 'Show closed' Switch (label: 'Show closed') in the page header — default false — when off hides cycles where detail.isClosed === true; when on, those cycles render with text-muted-foreground on Key+Name plus a <Badge>Closed</Badge>"
    - "When no folder is selected (e.g. user collapsed the parent of the selected folder), the right panel shows <EmptyState>Select a folder</EmptyState>"
    - "When the selected folder has zero cycles, the right panel shows <EmptyState>No cycles in this folder</EmptyState>"
    - "When folder tree query errors, the page renders <ErrorState> with retry that invalidates the folder tree query key"
    - "All useQuery calls follow the credential gate: enabled: !!jiraBaseUrl && !!token && !tokenLoading && !!projectKey"
    - "All AIO query keys use the prefix ['aio', jiraBaseUrl, ...]; all Jira user queries use ['jira', jiraBaseUrl, 'user-by-username', ownedByID]; token is NEVER part of any query key"
    - "The new AioCyclesSkeleton renders the two-panel layout shape (left-panel skeleton rows + right-panel table skeleton rows) so there is NO layout jump from skeleton → data"
    - "AioProjectOverviewPage.test.tsx is fully rewritten — all old CycleStatsCell + accordion tests removed; new tests cover folder tree render, folder selection, Show-closed toggle, owner column states, progress bar from testRunDistribution, count badge, empty states, error state"
    - "Existing usages of fetchAioCycles / CycleStatsCell elsewhere in the codebase are NOT broken (the old AioCycle type and fetchAioCycles function remain exported; no other component is rewritten by this plan)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx"
      provides: "Two-panel folder tree + cycle list page with summary-driven progress bars and owner resolution"
      contains: "fetchAioFolderTree"
    - path: "taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx"
      provides: "Rewritten test suite covering tree render, folder select, toggle, owner states, progress, empty/error states"
      contains: "folder-node"
    - path: "taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx"
      provides: "Two-panel layout-matching skeleton (left tree rows + right table rows)"
      contains: "flex flex-row"
  key_links:
    - from: "AioProjectOverviewPage.tsx"
      to: "@/services/aio (fetchAioFolderTree, fetchAioFolderCycleCounts, fetchAioCyclesWithDetail, fetchAioCycleSummaries)"
      via: "import + useQuery"
      pattern: "fetchAioFolderTree|fetchAioCyclesWithDetail"
    - from: "AioProjectOverviewPage.tsx"
      to: "@/services/jira/users (fetchJiraUserByUsername)"
      via: "import + useQuery per unique ownedByID"
      pattern: "fetchJiraUserByUsername"
    - from: "AioProjectOverviewPage.tsx"
      to: "@/lib/aioUtils (AIO_STATUS_MAP, normalizeStatusById)"
      via: "import + lookup in progress-bar builder"
      pattern: "normalizeStatusById|AIO_STATUS_MAP"
    - from: "AioProjectOverviewPage.tsx"
      to: "@/hooks/useAioCredentials"
      via: "hook call providing { token, isLoading } for the credential gate"
      pattern: "useAioCredentials\\(\\)"
---

<objective>
Replace the existing `AioProjectOverviewPage.tsx` (flat accordion + N+1 run fetches) with a two-panel layout (recursive folder tree + cycle list table) backed by the four new service functions from Plan 02 and the Jira user lookup from Plan 03. Update the matching skeleton and rewrite the test suite from scratch. This is the user-visible deliverable for Phase 57.

Closes D-01 (two-panel layout), D-02 (count badge per folder), D-03 (summary endpoint replaces N+1), D-06 (5-column row), D-07 (deduped owner queries), D-08 (null fallback), D-09 (Show closed toggle), D-10 (closed visual treatment).

Purpose: This phase exists because the old page diverges from the real AIO product in three ways (flat accordion vs tree, N+1 run fetches vs summary endpoint, missing owner column). This plan ships all three changes in one component rewrite while preserving every shared pattern documented in 57-PATTERNS.md (credential gate, query key prefix, useDelayedLoading, NavLink to cycle detail, EmptyState/ErrorState).

Output: 3 files — fully rewritten component, fully rewritten skeleton, fully rewritten test file.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-CONTEXT.md
@.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-RESEARCH.md
@.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PATTERNS.md
@.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-UI-SPEC.md
@.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/API-EXAMPLES.md
@.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PROBE-FINDINGS.md
@.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-02-SUMMARY.md
@.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-03-SUMMARY.md

<interfaces>
<!-- New exports available from Wave 1 (Plans 02 + 03): -->
From @/services/aio (re-exported through index.ts):
  - export async function fetchAioFolderTree(baseUrl, token, projectKey): Promise<AioFolder[]>
  - export async function fetchAioFolderCycleCounts(baseUrl, token, projectKey): Promise<Record<string, number>>
  - export async function fetchAioCyclesWithDetail(baseUrl, token, projectKey, folderID?: number): Promise<AioCycleDetailPagedResponse>
  - export async function fetchAioCycleSummaries(baseUrl, token, projectKey, ids: number[]): Promise<AioCycleSummaryItem[]>
  - export interface AioFolder { ID: number; name: string; description: string | null; parentID: number | null; rankOrder: number | null; children: AioFolder[] }
  - export interface AioCycleDetailItem { ID: number; jiraProjectID: number; permission?: {value: number}; detail: { key: string; title: string; ownedByID: string; folder: number | null; isClosed: boolean; startDate: string | null; endDate: string | null; objective?: string | null }; summary: null }
  - export interface AioCycleSummaryItem { ID: number; detail: null; summary: { totalTests: number; testRunDistribution: Record<string, number>; ... } }
  - export interface AioCycleDetailPagedResponse { items: AioCycleDetailItem[]; allIDs: number[]; startAt: number; maxResults: number; total?: number; isLast: boolean }

From @/services/jira/users:
  - export async function fetchJiraUserByUsername(baseUrl, token, username): Promise<JiraAssignableUser | null>

From @/lib/aioUtils:
  - export const AIO_STATUS_MAP: Record<number, 'pass' | 'fail' | 'blocked' | 'notRun' | 'inProgress'>
  - export function normalizeStatusById(id: number): 'pass' | 'fail' | 'blocked' | 'notRun' | 'inProgress'

<!-- Existing patterns to reuse: -->
From @/hooks/useAioCredentials:
  - export function useAioCredentials(): { token: string | null; isLoading: boolean }
From @/hooks/useDelayedLoading:
  - export function useDelayedLoading(isLoading: boolean): boolean   // 200ms threshold
From @/stores/auth.store:
  - useAuthStore() returns { jiraBaseUrl: string | undefined; ... }
From @/components/ui/*:
  - <Skeleton className="..." />
  - <Badge variant="secondary | default | ...">{children}</Badge>
  - <Switch id checked onCheckedChange />
  - <EmptyState icon={LucideIcon} title="..." subtitle="..." />
  - <ErrorState error={Error} onRetry={() => void} viewName="..." />
From react-router-dom:
  - <NavLink to={path} className="..."> + useParams<{projectKey: string}>()
From lucide-react:
  - ChevronRight, FlaskConical
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite AioProjectOverviewPage.tsx as two-panel folder tree + cycle list</name>
  <files>taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx, taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx (current file — to be entirely replaced; read for credential gate pattern + query-key pattern + error/retry pattern + skeleton wrapper pattern to PRESERVE)
    - taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx (current single-folder skeleton — to be replaced with two-panel skeleton)
    - taskflow/src/routes/dashboard/AioCycleDetailPage.tsx (NavLink target — no changes; just confirm route path is `/aio-cycle/:projectKey/:cycleKey`)
    - taskflow/src/hooks/useAioCredentials.ts (returns `{ token, isLoading }` — wire into credential gate)
    - taskflow/src/hooks/useDelayedLoading.ts (200ms skeleton flicker prevention — wrap BOTH folder tree loading and cycle list loading)
    - taskflow/src/components/ui/empty-state.tsx (props: icon, title, subtitle)
    - taskflow/src/components/ui/error-state.tsx (props: error, onRetry, viewName)
    - taskflow/src/components/ui/badge.tsx + switch.tsx + skeleton.tsx
    - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-UI-SPEC.md (layout, spacing, color, copywriting — this is the visual contract)
    - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PATTERNS.md (AioProjectOverviewPage.tsx pattern section — provides verbatim code snippets for FolderNode, progress bar, auto-expand ref guard, EmptyState text)
    - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-RESEARCH.md (Patterns 1–5; Pitfalls 1–6; Data Flow: Initial Load Sequence)
    - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PROBE-FINDINGS.md (A5 controls whether folderID is server-filter or client-filter — this drives whether `fetchAioCyclesWithDetail` is called once per folder selection OR once on mount)
    - taskflow/src/services/aio/index.ts (verify all four new fetch* functions are re-exported)
    - taskflow/src/lib/aioUtils.ts (AIO_STATUS_MAP + normalizeStatusById usage)
    - taskflow/src/routes/dashboard/AioCycleDetailPage.tsx (HOW the existing AIO cycle detail page is structured — for stylistic consistency only)
  </read_first>
  <behavior>
    Page-level behavior to satisfy (each maps to one or more tests in Task 2):

    **Layout & shell**
    - Root: `<div className="flex flex-col h-full">` containing header + content.
    - Header: `<div className="flex items-center justify-between px-6 py-4 border-b border-border flex-shrink-0">` with `<h1 className="text-xl font-semibold">Cycles — {projectKey ?? ''}</h1>` on the left and the Show-closed Switch + label on the right.
    - Content: `<div className="flex flex-row flex-1 overflow-hidden">` containing left panel + right panel.
    - Left panel: `<aside className="w-64 shrink-0 overflow-y-auto border-r border-border bg-muted/10">` (W-64 fixed width per UI-SPEC).
    - Right panel: `<div className="flex-1 overflow-auto">`.

    **Folder tree (left panel)**
    - One `<FolderNode>` per root folder (recursive — node renders itself + children when expanded).
    - Folder button styling: `w-full flex items-center gap-1 px-3 py-2 text-left text-sm`, indentation `pl-{3 + depth * 4}` (12px base + 16px per level — UI-SPEC).
    - Selected folder row: `bg-primary text-primary-foreground`.
    - Hover (not selected): `hover:bg-muted/30`.
    - Chevron: `<ChevronRight className="size-4 shrink-0 transition-transform {isExpanded ? 'rotate-90' : ''}" />` — rendered ONLY when node has children.
    - Count badge: `<Badge variant="secondary">{count}</Badge>` rendered after folder name iff count > 0.
    - Clicking a folder with children: toggle expand AND select (per UI-SPEC Interaction Contract).
    - Clicking a leaf folder: just select.
    - Keyboard: Enter / Space activate same as click (button is native button — automatic).
    - Auto-expand: on first load, the first root folder auto-expands. Use a `useRef<boolean>(false)` guard to avoid double-expansion under React StrictMode.
    - Auto-select: on first load, the first folder with `countMap[String(folder.ID)] > 0` is auto-selected (CONTEXT.md D-01 "auto-select first non-empty folder").
    - "Ungrouped" entry: rendered at BOTTOM of the tree iff `countMap['-1'] > 0`. Selecting it makes the right panel filter by "no folder assignment" (per 57-PROBE-FINDINGS.md A5 convention — see below).
    - Collapse-clears-selection: if user collapses a folder whose descendant is currently selected, clear `selectedFolderID` so the right panel shows the "Select a folder" empty state. Use an `isDescendant(tree, ancestorID, candidateID): boolean` helper (recursive walk) — Pitfall 6 in 57-RESEARCH.md.

    **Folder filter convention (A5-dependent)**
    - If 57-PROBE-FINDINGS.md A5 says SERVER-FILTER: call `fetchAioCyclesWithDetail(baseUrl, token, projectKey, selectedFolderID ?? undefined)` once per folder selection. Query key is `['aio', jiraBaseUrl, 'cycles-detail', projectKey, selectedFolderID]`.
    - If A5 says CLIENT-FILTER: call `fetchAioCyclesWithDetail(baseUrl, token, projectKey)` ONCE on mount and filter the resulting `items[]` client-side by `selectedFolderID`. Since `detail.folder` is `null` per API-EXAMPLES.md, the filter must come from a folderID→cycleID map derived from `countMap` + `allIDs` — or a separate per-folder server-filter call regardless of A5. Resolve this in the implementation by defaulting to SERVER-FILTER if A5 explicitly confirms `?folderID=` works; otherwise fall back to a per-folder server call only when `selectedFolderID` is non-null (no `selectedFolderID` ⇒ skip the query entirely). Query key in client-filter mode: `['aio', jiraBaseUrl, 'cycles-detail', projectKey]`.

    **Right panel: cycle list table**
    - When no folder selected: render `<EmptyState icon={FlaskConical} title="Select a folder" subtitle="Choose a folder from the left to view its cycles." />`.
    - When folder selected and cycles loading and showSkeleton: render the cycle-rows portion of `<AioCyclesSkeleton>` (or inline skeleton rows).
    - When folder selected and result is empty (`visibleCycles.length === 0`): render `<EmptyState icon={FlaskConical} title="No cycles in this folder" subtitle="This folder has no test cycles yet." />`.
    - When error: render `<ErrorState error={cycleListQuery.error} onRetry={() => queryClient.invalidateQueries({queryKey: ['aio', jiraBaseUrl, 'cycles-detail', projectKey]})} viewName="cycles" />`.
    - When folder selected and result populated: render the table per UI-SPEC column spec.

    **Table columns (exactly 5)**
    | Column | Class | Cell content |
    |--------|-------|--------------|
    | Key | `w-28 px-3 py-3 text-xs text-muted-foreground font-mono` | `{cycle.detail.key}` |
    | Name | `px-4 py-3` | `<NavLink to={\`/aio-cycle/${projectKey}/${cycle.detail.key}\`} className="hover:underline">{cycle.detail.title}</NavLink>` followed by `<Badge variant="secondary">Closed</Badge>` iff `cycle.detail.isClosed && showClosed`. When closed, also add `text-muted-foreground` to the row's Key + Name cells. |
    | Owner | `w-32 px-3 py-3` | `<OwnerCell ownedByID={cycle.detail.ownedByID} token={token} jiraBaseUrl={jiraBaseUrl} />` — sub-component below |
    | Total tests | `w-20 px-3 py-3 text-right text-xs text-muted-foreground` | `{summaryByID[cycle.ID]?.summary.totalTests ?? '—'}` |
    | Progress | `w-44 px-3 py-3` | `<ProgressBarCell summary={summaryByID[cycle.ID]?.summary} />` — sub-component below |
    Header: `<thead className="bg-muted/10"><tr>` with `<th className="...text-xs font-medium text-muted-foreground">` per column.

    **OwnerCell sub-component** (defined in this file, NOT a separate file)
    - Props: `{ ownedByID: string; token: string | null; jiraBaseUrl: string | undefined }`
    - useQuery: `{ queryKey: ['jira', jiraBaseUrl, 'user-by-username', ownedByID], queryFn: () => fetchJiraUserByUsername(jiraBaseUrl!, token!, ownedByID), enabled: !!jiraBaseUrl && !!token && !!ownedByID, staleTime: 5 * 60 * 1000 }`
    - Loading: `<Skeleton className="h-4 w-24" />`
    - Error or null result: render `{ownedByID}` as plain text (D-08 fallback)
    - Success with user: render `{user.displayName}`
    - **Dedup guarantee:** TanStack Query automatically dedupes concurrent queries with identical keys; so rendering N OwnerCell components with K unique ownedByIDs results in K network requests. NO need for parent-level dedup logic — verified in Task 2 test.

    **ProgressBarCell sub-component** (defined in this file)
    - Props: `{ summary: AioCycleSummaryItem['summary'] | undefined }`
    - Loading (summary undefined and cycleSummariesQuery.isLoading): `<Skeleton className="h-1.5 w-full rounded-full" />`
    - No data (summary undefined and not loading): render `—` text
    - Success: build counts via:
      ```
      const counts = { pass: 0, fail: 0, blocked: 0, notRun: 0, inProgress: 0 };
      for (const [idStr, count] of Object.entries(summary.testRunDistribution)) {
        counts[normalizeStatusById(Number(idStr))] += count;
      }
      ```
      Then render the 5-segment bar with the exact classes from UI-SPEC Status Color Palette (`bg-green-500` / `bg-red-500` / `bg-orange-400` / `bg-blue-400` / `bg-muted`). Each segment width is `Math.round((count / summary.totalTests) * 100) + '%'`. Skip segments with count === 0.
    - Below the bar, render a small `text-xs text-muted-foreground` line with the counts: `{counts.pass}P {counts.fail}F {counts.blocked}B {counts.inProgress}IP {counts.notRun}N` (only show segments with count > 0).

    **Queries fired (state machine)**
    - On mount, when token resolves: fire `useQuery(folders)` and `useQuery(counts)` in parallel — keys `['aio', jiraBaseUrl, 'folders', projectKey]` and `['aio', jiraBaseUrl, 'cycle-count', projectKey]`. Both gated on `!!jiraBaseUrl && !!token && !tokenLoading && !!projectKey`.
    - On folder selection (selectedFolderID non-null): fire `useQuery(cyclesWithDetail)` — key `['aio', jiraBaseUrl, 'cycles-detail', projectKey, selectedFolderID]` (or no selectedFolderID in key if client-filter mode). Same gate.
    - When cyclesWithDetail.data?.allIDs is non-empty: fire `useQuery(cycleSummaries)` — key `['aio', jiraBaseUrl, 'cycle-summaries', projectKey, allIDs.join(',')]`. Gated additionally on `(cyclesWithDetail.data?.allIDs?.length ?? 0) > 0`.
    - Per unique ownedByID rendered in the current visible cycles: one OwnerCell fires `useQuery(userByUsername)` — key `['jira', jiraBaseUrl, 'user-by-username', ownedByID]`. Same gate.

    **Show closed toggle**
    - State: `const [showClosed, setShowClosed] = useState<boolean>(false)`
    - UI: `<div className="flex items-center gap-2"><Switch id="show-closed" checked={showClosed} onCheckedChange={setShowClosed} /><label htmlFor="show-closed" className="text-sm">Show closed</label></div>` — right side of header.
    - Filter: `const visibleCycles = (cycleList ?? []).filter(c => showClosed || !c.detail.isClosed)`

    **Loading states**
    - `useDelayedLoading` wraps BOTH folder tree query loading AND cycle list query loading.
    - When folder tree loading: left panel shows skeleton rows (delegate to AioCyclesSkeleton's left-panel section); right panel shows the "Select a folder" empty state (no data yet to render).
    - When cycle list loading (folder selected): right panel shows skeleton rows.

    **No hidden hooks**
    - Every `useQuery` is called unconditionally — `enabled: false` gates them. Sub-components (`OwnerCell`, `ProgressBarCell`) call hooks only at their top level. No conditional `useQuery`.

    **AioCyclesSkeleton update**
    Rewrite to match the two-panel layout:
    - Outer: `<div className="flex flex-row h-full">`
    - Left (`w-64 shrink-0 border-r border-border bg-muted/10 p-2 space-y-1`): 6 `<Skeleton className="h-6 w-{40/36/32/28/40/36}" />` rows at increasing indentation (via inline `style={{paddingLeft: ...}}`).
    - Right (`flex-1 p-4`): 5 rows of `<div className="flex items-center gap-3 border-b border-border py-3">` each with `<Skeleton className="h-4 w-20" />`, `<Skeleton className="h-4 flex-1" />`, `<Skeleton className="h-4 w-24" />`, `<Skeleton className="h-4 w-12" />`, `<Skeleton className="h-1.5 w-32 rounded-full" />`.
    The skeleton's visual proportions must match the live layout so the transition from skeleton → data does not jump (verified manually in Wave 3 checkpoint).
  </behavior>
  <action>
    Rewrite TWO files:

    **File 1: `taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx`** — complete replacement.

    Structure:
    1. Imports: React (`useEffect`, `useMemo`, `useRef`, `useState`), `useQuery` + `useQueryClient` from `@tanstack/react-query`, `ChevronRight` + `FlaskConical` from `lucide-react`, `NavLink` + `useParams` from `react-router-dom`, all the UI primitives (`Skeleton`, `Badge`, `Switch`, `EmptyState`, `ErrorState`), the hooks (`useAioCredentials`, `useDelayedLoading`), `useAuthStore`, the new service functions (`fetchAioFolderTree`, `fetchAioFolderCycleCounts`, `fetchAioCyclesWithDetail`, `fetchAioCycleSummaries`) from `@/services/aio`, `fetchJiraUserByUsername` from `@/services/jira/users`, types `AioFolder` + `AioCycleDetailItem` + `AioCycleSummaryItem` from `@/services/aio/types` (or `@/services/aio` via the barrel), `normalizeStatusById` from `@/lib/aioUtils`, and the new `AioCyclesSkeleton` from `./AioCyclesSkeleton`.

    2. Helper functions (file-scope, above the component):
       - `function isDescendant(tree: AioFolder[], ancestorID: number, candidateID: number): boolean` — recursive walk; returns true iff candidateID is found in the subtree rooted at the node with ID === ancestorID.
       - `function findFirstNonEmptyFolder(tree: AioFolder[], countMap: Record<string, number>): number | null` — walks the tree depth-first; returns the ID of the first folder with `countMap[String(folder.ID)] > 0`, or null.

    3. Sub-components (file-scope, above the default export):
       - `function FolderNode({ node, depth, countMap, expandedIDs, selectedFolderID, onToggle, onSelect }: ...)` — see 57-PATTERNS.md section "Folder tree button pattern" for the verbatim button JSX.
       - `function OwnerCell({ ownedByID, token, jiraBaseUrl }: ...)` — single useQuery on `fetchJiraUserByUsername`; renders Skeleton / displayName / raw ID.
       - `function ProgressBarCell({ summary, isLoading }: ...)` — builds counts via `normalizeStatusById(Number(idStr))` loop; renders the 5-segment bar.

    4. Default export `AioProjectOverviewPage`:
       - `const { projectKey } = useParams<{projectKey: string}>();`
       - `const { jiraBaseUrl } = useAuthStore();`
       - `const { token, isLoading: tokenLoading } = useAioCredentials();`
       - `const queryClient = useQueryClient();`
       - `const [selectedFolderID, setSelectedFolderID] = useState<number | null>(null);`
       - `const [expandedIDs, setExpandedIDs] = useState<Set<number>>(new Set());`
       - `const [showClosed, setShowClosed] = useState<boolean>(false);`
       - 4 `useQuery` calls (folders, countMap, cyclesWithDetail, cycleSummaries) per the keys above.
       - `const visibleCycles = useMemo(...)` filtering by isClosed (per showClosed) and limiting to those in the selected folder (per A5 mode).
       - `const summaryByID = useMemo(() => Object.fromEntries((cycleSummaries.data ?? []).map(s => [s.ID, s])), [cycleSummaries.data])`.
       - `useEffect` auto-expand+auto-select guard (one-time via `useRef<boolean>(false)`); fires when both `foldersQuery.data` and `countMapQuery.data` are first available.
       - `const showFolderSkeleton = useDelayedLoading(foldersQuery.isLoading);`
       - `const showCycleSkeleton = useDelayedLoading(cyclesWithDetailQuery.isLoading);`
       - Handlers: `const toggleFolder = (id: number) => { setExpandedIDs(prev => { const next = new Set(prev); next.has(id) ? next.delete(id) : next.add(id); return next; }); /* if collapsing and selectedFolderID is descendant → clear selection */ };` and `const selectFolder = (id: number) => setSelectedFolderID(id);`.
       - Return the JSX shell described in Behavior above.

    All AIO query keys MUST be `['aio', jiraBaseUrl, ...]`. The user-by-username key MUST be `['jira', jiraBaseUrl, 'user-by-username', ownedByID]`. Token MUST NOT appear in any query key (security requirement per 56-PATTERNS.md).

    **File 2: `taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx`** — complete replacement.

    Replace the existing single-folder skeleton with the two-panel skeleton described in Behavior. Component still exports as `function AioCyclesSkeleton()` default-or-named (match existing — currently named export). Optionally split into `AioFolderTreeSkeleton` + `AioCycleListSkeleton` sub-components if the page imports them separately; otherwise keep `AioCyclesSkeleton` as the single export and let the page render it inline.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit 2>&1 | grep -E "AioProjectOverviewPage|AioCyclesSkeleton" || echo "page files compile cleanly"</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx` exists
    - File contains `import { fetchAioFolderTree` (folder tree fetch wired)
    - File contains `import { fetchAioCyclesWithDetail` (cycle list fetch wired)
    - File contains `import { fetchAioCycleSummaries` (summary fetch wired)
    - File contains `import { fetchJiraUserByUsername` (owner lookup wired)
    - File contains `normalizeStatusById` (status mapping used)
    - File contains `useAioCredentials()` (credential gate)
    - File contains the literal string `['aio', jiraBaseUrl, 'folders'` (query key prefix correct)
    - File contains the literal string `['aio', jiraBaseUrl, 'cycle-count'` 
    - File contains the literal string `['aio', jiraBaseUrl, 'cycles-detail'`
    - File contains the literal string `['aio', jiraBaseUrl, 'cycle-summaries'`
    - File contains the literal string `['jira', jiraBaseUrl, 'user-by-username'`
    - File does NOT contain any query key with `token` as an element: `grep -E "queryKey: \[.*token" taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx | grep -v '//'` returns nothing
    - File contains exactly one `<Switch` element (Show closed)
    - File contains `function FolderNode` and `function OwnerCell` and `function ProgressBarCell` declarations (or const arrow equivalents — three sub-components)
    - File contains the literal strings `flex flex-row` (two-panel layout) and `w-64 shrink-0` (left panel width)
    - File does NOT contain references to the old `fetchAioTestRunsForCycle` or `CycleStatsCell` or `groupCyclesByFolder` (old implementation gone)
    - File contains both `<EmptyState` occurrences for "Select a folder" and "No cycles in this folder"
    - `taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx` contains `flex flex-row` (two-panel skeleton)
    - `cd taskflow && npx tsc --noEmit` reports no new errors in either file
  </acceptance_criteria>
  <done>Component rewritten as two-panel folder tree + cycle list; skeleton matches new layout; tsc clean; all required imports + query keys + sub-components present; no references to removed symbols.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite AioProjectOverviewPage.test.tsx covering tree render, folder select, toggle, owner states, progress, empty/error states</name>
  <files>taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx (existing test file — read for the test infrastructure pattern; full rewrite of test cases)
    - taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx (just written in Task 1 — the assertion targets)
    - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PATTERNS.md (AioProjectOverviewPage.test.tsx COMPLETE REWRITE section — provides vi.mock template, render helper, userEvent pattern)
    - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/API-EXAMPLES.md (folder + paged + paged2 shapes — use as mock return values)
    - taskflow/src/services/aio/types.ts (AioFolder, AioCycleDetailItem, AioCycleSummaryItem — for typing the mock values)
    - taskflow/src/services/jira/types.ts (JiraAssignableUser shape)
    - taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx (if it exists — for an existing test pattern reference on AIO pages)
  </read_first>
  <behavior>
    Tests to cover (each in its own `it` block):

    1. **`renders folder tree with root and child nodes from fetchAioFolderTree`** — mock fetchAioFolderTree returning two roots (one with children); assert both root names render; expand the first root via user click and assert child names appear.

    2. **`shows cycle count badge per folder from fetchAioFolderCycleCounts`** — mock countMap with `{ "201": 5, "301": 240 }`; assert badges with text `5` and `240` render adjacent to the respective folder names.

    3. **`renders "Ungrouped" entry when countMap['-1'] > 0`** — mock countMap with `{ "201": 5, "-1": 3 }`; assert an element with text `Ungrouped` appears.

    4. **`does NOT render "Ungrouped" entry when countMap['-1'] is 0 or absent`** — mock countMap with `{ "201": 5 }`; assert no element with text `Ungrouped` exists.

    5. **`auto-expands first root folder and auto-selects first folder with non-zero count on initial load`** — mock 2-root tree + countMap `{ "201": 5, "202": 3 }`; assert folder 202 (child of expanded root 201) is the auto-selected folder (visually `bg-primary` class on its button — assert via testid or class match).

    6. **`clicking a folder selects it and loads its cycles in the right panel`** — start with folder X auto-selected (mock cyclesWithDetail returns cycles for X); user clicks folder Y; mock cyclesWithDetail is called with folderID = Y.ID (server-filter mode per A5); assert Y's cycle title appears in the table.

    7. **`renders all 5 columns in the cycle table: Key, Name, Owner, Total tests, Progress`** — assert table thead has 5 `<th>` elements with the correct text content.

    8. **`renders cycle row Name as a NavLink to /aio-cycle/{projectKey}/{cycleKey}`** — assert the rendered `<a>` element has `href="/aio-cycle/PROJ/PROJ-CY-101"` (or whatever the mock cycle key is).

    9. **`Owner cell shows Skeleton while user query pending, displayName on resolve, raw ownedByID on null`** — three sub-cases (might be combined into one test with multiple cycles):
       - Cycle with ownedByID `user_a`, mock fetchJiraUserByUsername resolves to `{displayName: 'Alice', name: 'user_a'}` → assert `Alice` rendered.
       - Cycle with ownedByID `user_b`, mock fetchJiraUserByUsername resolves to `null` (404 fallback) → assert `user_b` rendered as plain text.
       - Pending case: assert that when fetchJiraUserByUsername has not resolved, a Skeleton element (`role="status"` or `data-slot="skeleton"` per shadcn skeleton convention) renders inside the Owner cell.

    10. **`Owner queries dedupe — N cycles with K unique ownedByIDs fire K fetchJiraUserByUsername calls`** — mock cyclesWithDetail with 3 cycles where owner IDs are `[user_a, user_a, user_b]`; wait for all queries to settle; assert `fetchJiraUserByUsername` was called exactly 2 times. This proves TanStack Query dedup works at the level required by D-07.

    11. **`Total tests cell shows summary.totalTests from fetchAioCycleSummaries result keyed by cycle ID`** — mock summaries `[{ID: 1001, summary: {totalTests: 261, testRunDistribution: {...}}}, {ID: 1002, summary: {totalTests: 173, ...}}]`; assert the rows for cycle 1001 and 1002 show `261` and `173` in the Total tests cell.

    12. **`Progress bar builds from testRunDistribution via AIO_STATUS_MAP — green/red/orange/blue/muted segments correctly proportioned`** — mock summary `{totalTests: 100, testRunDistribution: {"901": 60, "51": 20, "55": 10, "54": 5, "53": 5}}`; assert the rendered progress bar contains 5 segments with classes `bg-green-500`, `bg-red-500`, `bg-orange-400`, `bg-blue-400`, `bg-muted`; assert each segment's inline width is the correct percentage (`60%`, `20%`, etc.). Also assert the legend line `60P 20F 10B 5IP 5N` (or similar — match the implementation's exact format).

    13. **`Show closed toggle hides closed cycles by default and reveals them with Closed badge when on`** — mock cyclesWithDetail with 3 cycles, 1 of which has `isClosed: true`; assert default render shows 2 cycles (closed hidden). Use `userEvent` to click the Switch; assert all 3 cycles render and the closed one has a `Closed` badge near its name with `text-muted-foreground` class applied to its Key+Name cells.

    14. **`right panel shows "Select a folder" EmptyState when no folder is selected`** — mock folderTree empty `[]` so auto-select can't pick a folder; assert `Select a folder` text renders.

    15. **`right panel shows "No cycles in this folder" EmptyState when selected folder has zero cycles`** — mock cyclesWithDetail returning `{items: [], allIDs: [], ...}` for the selected folder; assert `No cycles in this folder` text renders.

    16. **`renders ErrorState when fetchAioFolderTree rejects, with retry button that invalidates the folder tree query`** — mock fetchAioFolderTree to reject; assert ErrorState renders; click retry; assert `fetchAioFolderTree` is called again (or queryClient.invalidateQueries was called with the folder tree key — use a spy on the QueryClient if accessible, otherwise re-assert mock call count).

    17. **`credential gate: no service functions called until token resolves and tokenLoading is false`** — supply `readSecret` mock that resolves AFTER a small delay; assert that during the loading window NO `fetchAioFolderTree` / `fetchAioCyclesWithDetail` / etc. are called. After token resolves, they fire.

    Test infrastructure:
    - Replace all `vi.mock('@/services/aio', ...)` to mock the four NEW fetch functions: `fetchAioFolderTree`, `fetchAioFolderCycleCounts`, `fetchAioCyclesWithDetail`, `fetchAioCycleSummaries` (and OMIT `fetchAioCycles`, `fetchAioTestRunsForCycle` from the mock factory — the new component doesn't use them).
    - Add `vi.mock('@/services/jira/users', () => ({ fetchJiraUserByUsername: vi.fn() }))`.
    - Keep `vi.mock('@/stores/auth.store', ...)` with `jiraBaseUrl: 'https://jira.example.com'`.
    - Keep `vi.mock('@/services/stronghold', () => ({ readSecret: vi.fn().mockResolvedValue('test-jira-token') }))`.
    - Render helper unchanged in shape: `QueryClientProvider` → `MemoryRouter` → `Routes`. Each test that imports fetch functions must use the dynamic-import pattern `const { fetchAioFolderTree } = await import('@/services/aio')` before casting to `ReturnType<typeof vi.fn>`.

    All old tests covering `fetchAioCycles`, `CycleStatsCell`, `cycle-stats-loading` / `cycle-stats-loaded` / `cycle-stats-error` testids, and the accordion `folder-toggle-*` testids MUST be removed — the new component does not render any of those.
  </behavior>
  <action>
    Replace `taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx` entirely with a new test file covering the 17 cases above. Use the mock factory + render helper templates from `57-PATTERNS.md` under "AioProjectOverviewPage.test.tsx (test) — COMPLETE REWRITE".

    Constraints:
    - Mock JSON values must use the API-EXAMPLES.md shapes (lowercase `detail`, `summary`, with `ID` capitalised per AIO convention).
    - Use `userEvent.setup()` for all clicks; use `screen.findByText` / `waitFor` for async assertions.
    - Each `it` block is independent (`beforeEach(() => vi.clearAllMocks())`).
    - At least one test uses `data-testid` on the FolderNode button for stable folder identification (e.g. `data-testid={\`folder-node-${node.ID}\`}` — Task 1 should add this testid to FolderNode for testability).
    - At least one test uses `data-testid` on the cycle row (e.g. `data-testid={\`cycle-row-${cycle.ID}\`}`) — Task 1 should add this too.
    - At least one test uses `data-testid="owner-cell"` on the OwnerCell wrapper (Task 1 adds this).
    - At least one test uses `data-testid="progress-bar"` on the ProgressBarCell wrapper (Task 1 adds this).

    If Task 1 forgot to add these testids, this Task 2 should add them BACK to AioProjectOverviewPage.tsx (Task 1 and 2 share both files in `files_modified`, so this is in-scope).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/AioProjectOverviewPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npx vitest run src/routes/dashboard/AioProjectOverviewPage.test.tsx` exits 0 (every test passes)
    - Test file contains at least 17 distinct `it(` calls (`grep -c "^  it(" taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx` returns >= 17)
    - Test file does NOT contain `cycle-stats-loading` / `cycle-stats-loaded` / `cycle-stats-error` testids (old `CycleStatsCell` symbols)
    - Test file does NOT contain `fetchAioTestRunsForCycle` references
    - Test file mocks `fetchAioFolderTree`, `fetchAioFolderCycleCounts`, `fetchAioCyclesWithDetail`, `fetchAioCycleSummaries`, `fetchJiraUserByUsername` (5 mock declarations or in the mock factory)
    - Test file contains assertions for `bg-green-500`, `bg-red-500`, `bg-orange-400`, `bg-blue-400`, `bg-muted` (verifies progress bar segments) — `grep -cE "bg-(green|red|orange|blue)-(500|400)|bg-muted" taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx` returns at least 5
    - Test file contains a `userEvent` import and at least 3 `user.click` calls (verifying interactivity tests exist)
    - Test file contains assertions referencing the literal text `Select a folder`, `No cycles in this folder`, `Show closed`, and `Closed`
    - Test file's full suite runtime is < 30s (matches 57-VALIDATION.md feedback latency target)
  </acceptance_criteria>
  <done>Test file rewritten with 17+ test cases, all green; old CycleStatsCell tests removed; new functions all mocked correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser → AIO + Jira REST APIs | PAT crosses to two APIs (AIO TCMS + Jira DC) via apiFetch / aioFetch |
| AIO response data → React component tree | Untrusted JSON renders directly into DOM via React (XSS-safe by default — text content escaped) |
| Cycle detail fields (title, ownedByID) → URL construction for NavLink | cycle.detail.key is interpolated into NavLink href |
| User-controlled folder ID → URL query param | selectedFolderID (number from API) → folderID query param on cycle-list fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-04-01 | T (Tampering) | NavLink href construction `\`/aio-cycle/${projectKey}/${cycle.detail.key}\`` | mitigate | projectKey comes from useParams (router-validated path segment); cycle.detail.key comes from AIO API (trusted backend). React Router treats the template literal as a path string with auto-encoding for unsafe characters. No URL injection risk. Acceptance criteria require NavLink wrapper (not raw `<a href>`). |
| T-57-04-02 | I (Information Disclosure) | PAT in query keys | mitigate | All useQuery calls have token excluded from queryKey. Acceptance criteria assert this via grep (no `token` element in any `queryKey:` line). |
| T-57-04-03 | I | PAT in browser DevTools network tab | accept | apiFetch / aioFetch send Bearer in Authorization header — visible in DevTools. This is the standard Phase 51 pattern; same exposure as every other AIO page. No additional mitigation in this plan. |
| T-57-04-04 | S (Spoofing) | OwnerCell rendering arbitrary displayName text | mitigate | React text node interpolation escapes all HTML/JS — XSS-safe by default. Acceptance criteria use `screen.getByText` (text-content match, not innerHTML). |
| T-57-04-05 | D (Denial of Service) | Many simultaneous Jira user queries for a large team | mitigate | TanStack Query dedupes by query key. Acceptance criteria Test #10 verifies dedup at the test level — proves K unique IDs → K calls, not N calls. Additional safety: `staleTime: 5 * 60 * 1000` on OwnerCell queries means subsequent renders within 5 minutes hit the cache. |
| T-57-04-06 | E (Elevation of Privilege) | aioEnabled gate bypass | mitigate | Page is rendered inside the existing `/aio-project/:projectKey` route which the routes.tsx config already gates on aioEnabled (Phase 51/52). This plan does NOT change routing — the gate is unaffected. No additional mitigation needed in component code. |
| T-57-04-07 | T | "Show closed" toggle state survives session navigation | accept | Toggle defaults to off on mount (in-component useState). User loses preference between sessions. CONTEXT.md D-09 / Claude's Discretion confirms in-memory state is the simplest approach; deferred to a future phase if persistence needed. |
| T-57-04-08 | I | Rendering ownedByID raw as fallback might leak internal usernames | accept | The fallback shows the same string that AIO already returns. Internal usernames like `JIRAUSER23429` are not secrets in the Jira DC context (visible in every issue assignee field already). D-08 explicitly mandates this fallback. |

No high-severity threats. All ASVS L1 controls satisfied: input validation at API boundary (TypeScript types + apiFetch error handling), credential gating, no token in query keys, default-escaped text rendering.
</threat_model>

<verification>
After both tasks complete, run:

```bash
cd taskflow && npx vitest run src/routes/dashboard/AioProjectOverviewPage.test.tsx
```

Expected: all 17+ tests pass.

```bash
cd taskflow && npx vitest run
```

Expected: full suite green (no regressions in other test files — particularly AioCycleDetailPage.test.tsx, sidebar tests, settings tests should be unaffected).

```bash
cd taskflow && npx tsc --noEmit
```

Expected: zero new errors.

Manual visual check (deferred to Plan 05 Wave 3 checkpoint):
- Load the page against a live AIO instance
- Verify folder tree renders, expand/collapse works, count badges show, Show-closed toggle works, owner names resolve, progress bars fill with correct colors and proportions
</verification>

<success_criteria>
- AioProjectOverviewPage.tsx implements the two-panel layout per UI-SPEC
- AioCyclesSkeleton.tsx matches the new layout (no skeleton-to-data jump)
- AioProjectOverviewPage.test.tsx has 17+ tests covering all listed behaviors, all green
- Full vitest suite green
- tsc --noEmit clean
- Every locked decision D-01 through D-10 implemented (verified by tests)
</success_criteria>

<output>
After completion, create `.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-04-SUMMARY.md` recording:
- Final query-key map (which key each useQuery uses)
- A5 mode implemented (server-filter or client-filter) and how cycle list filtering works
- Sub-component breakdown (FolderNode, OwnerCell, ProgressBarCell — line counts, testids)
- Test count and pass count
- Any tests that required custom waitFor timeouts (flakiness signals)
- Remaining items for Wave 3 human-verification checkpoint
</output>
