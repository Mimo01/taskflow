---
phase: 90-per-mr-corrective-actions
plan: 04
type: execute
wave: 4
depends_on: [90-03]
files_modified:
  - .planning/phases/90-per-mr-corrective-actions/90-VALIDATION.md
  - .planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md
autonomous: false
requirements: [MRFIX-01, MRFIX-02, MRFIX-03, MRFIX-04]

must_haves:
  truths:
    - "The full suite is green and `npm run check` introduces zero new diagnostics beyond the known 2-error BacklogPage/BacklogRow baseline"
    - "Every row of 90-VALIDATION.md's per-task verification map resolves to a real, named, passing test — no `TBD`, no `⬜ pending` left"
    - "A real MR's `target_branch` actually changes on GitLab from a single cell click, and the BR flag is gone after a reload (MRFIX-01, live)"
    - "A real MR gains the release milestone from a single cell click and it persists on GitLab (MRFIX-02, live)"
    - "A live failure renders a readable GitLab message in the cell tooltip — never `[object Object]` — and retries on click (D-07, D-10)"
    - "Both cells on one row can be fired concurrently, the row does not re-sort after a fix, and the MR Drift badge decrements immediately (D-09, D-11, D-12)"
    - "The BR cell is inert with an explanatory tooltip on a release whose branch does not exist (MRFIX-04, D-14)"
    - "No confirm dialog and no warning appears on retarget, regardless of the Plan 01 probe outcome (D-16)"
  artifacts:
    - path: ".planning/phases/90-per-mr-corrective-actions/90-VALIDATION.md"
      provides: "the completed per-task verification map with real task ids and green statuses"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "90-VALIDATION.md rows"
      to: "actual vitest -t filters"
      via: "each row's automated command resolving to >=1 passing test"
      pattern: "npx vitest run"
---

<objective>
Close the phase: prove the suite is green against the recorded biome baseline, finish the validation ledger, and put the four requirements in front of the user on live GitLab data.

Three of the four requirements can only be *fully* proven live — no fixture can show that GitLab accepts the payload, that `milestone_id` really is the global id, or that the drift flag clears after a refetch (90-VALIDATION.md § Manual-Only Verifications). This plan is where that happens.

**D-16 is re-stated here as a guard, not a question:** the retarget action must apply with no confirm dialog and no warning. If the Plan 01 probe found `reset_approvals_on_push: true` or a protected `release/*` pattern, that is a recorded fact — it does not license adding friction, and the UAT script below explicitly checks that none was added.

Purpose: MRFIX-01…04 are user-facing claims; the phase does not ship on unit tests alone.
Output: an updated `90-VALIDATION.md` ledger and an approved live UAT run.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-per-mr-corrective-actions/90-CONTEXT.md
@.planning/phases/90-per-mr-corrective-actions/90-VALIDATION.md
@.planning/phases/90-per-mr-corrective-actions/90-UI-SPEC.md
@.planning/phases/90-per-mr-corrective-actions/90-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Green the full suite against the recorded baseline and complete the validation ledger</name>
  <files>.planning/phases/90-per-mr-corrective-actions/90-VALIDATION.md</files>

  <read_first>
    - `.planning/phases/90-per-mr-corrective-actions/90-VALIDATION.md` (the map to fill in: Task ID / Plan / Wave / Status columns and the frontmatter flags)
    - `.planning/phases/90-per-mr-corrective-actions/90-01-SUMMARY.md`, `90-02-SUMMARY.md`, `90-03-SUMMARY.md` (the real task ids and test names to reference)
    - `taskflow/package.json` (the `check` script is `biome check ./src && tsc --noEmit`; `test` is `vitest run`)
  </read_first>

  <action>
    Run `cd taskflow && npm run test` (full suite, ~2300 tests, ~90s) and `cd taskflow && npm run check`.

    Baseline rule, non-negotiable: `npm run check` has **2 pre-existing biome formatting errors in `BacklogPage.tsx` and `BacklogRow.tsx`**. Gate on **zero NEW** diagnostics — do not "fix" the Backlog files here, and do not report a red gate because the run is not clean. Any diagnostic naming a file other than those two is a real failure this plan must fix in the file it belongs to. `tsc --noEmit` must be genuinely clean.

    Then update `90-VALIDATION.md`: for each row of the per-task verification map, replace `TBD` with the real `{plan}-{task}` id and wave, set Status to `✅ green`, and replace any `❌ W0` File-Exists marker with `✅` naming the file that now holds the test. Where a row's command drifted from what was actually built (for example the D-13 invalidation assertions living in `useMrFixMutation.test.tsx` rather than `useReleaseDetail.test.tsx`), correct the command to the one that really passes and add a one-line note in the row explaining the relocation. Every automated command must use `npx vitest run` — never watch mode. Tick the six Validation Sign-Off boxes and set `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter.

    Record the actual full-suite pass count and the exact biome diagnostic count in a short `## Run record` section at the end of the file, with the date.
  </action>

  <verify>
    <automated>cd taskflow &amp;&amp; npm run test &amp;&amp; npx tsc --noEmit &amp;&amp; npx biome check ./src 2>&amp;1 | grep -E '^src/' | grep -vE 'BacklogPage\.tsx|BacklogRow\.tsx' | wc -l | grep -qx '0'</automated>
  </verify>

  <acceptance_criteria>
    - `npm run test` exits 0 with zero failing tests
    - `npx tsc --noEmit` exits 0
    - `npx biome check ./src` reports diagnostics only for `BacklogPage.tsx` / `BacklogRow.tsx` — the piped count of any other flagged `src/` path is exactly 0
    - `grep -c 'TBD' 90-VALIDATION.md` returns 0
    - `grep -c '⬜ pending' 90-VALIDATION.md` returns 0
    - `grep -c 'nyquist_compliant: true' 90-VALIDATION.md` returns 1 and `grep -c 'wave_0_complete: true'` returns 1
    - `grep -cE 'vitest (?!run)' 90-VALIDATION.md` finds no watch-mode invocation (every command is `npx vitest run …`)
    - Every command in the map, run verbatim, matches at least one test (spot-check all rows; a `-t` filter matching zero tests is a failure)
    - A `## Run record` section names the full-suite pass count and the biome diagnostic count
  </acceptance_criteria>

  <done>The suite is green against the recorded baseline and the validation ledger has no unresolved rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT of both corrective actions on real GitLab data</name>
  <files>.planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md</files>

  <read_first>
    - `.planning/phases/90-per-mr-corrective-actions/90-UI-SPEC.md` § "Interaction Contract" (the per-state expectations the user is checking against)
    - `.planning/phases/90-per-mr-corrective-actions/90-VALIDATION.md` § "Manual-Only Verifications" (the four behaviours that cannot be automated and why)
    - `.planning/phases/90-per-mr-corrective-actions/90-CONTEXT.md` § D-16 (the friction that must NOT have appeared)
    - `.planning/phases/90-per-mr-corrective-actions/90-03-SUMMARY.md` (which focus-reveal mechanism shipped — step 3 checks it by keyboard)
  </read_first>

  <what-built>
    Everything: `updateMergeRequest` + `flattenGitLabError` (Plan 01), the per-cell optimistic mutation hook (Plan 02), and the hover-revealed BR/MS action cells with frozen sort order (Plan 03). The full suite is green. What automation cannot prove is that GitLab actually accepts the two payloads and that the drift flags clear after a real refetch — that needs a live run against the team's project.
  </what-built>

  <how-to-verify>
    Run the app (`cd taskflow && npm run tauri dev`) and open a release detail page whose MR Drift section shows at least one flagged row with BOTH the BR and MS columns flagged. Note the `MR Drift` badge count before starting.

    1. **Reveal (D-01/D-02/D-03):** hover the row. Every flagged BR/MS cell on that row should swap `⚠` -> an icon at once (branch icon in BR, milestone icon in MS). Move the pointer onto one cell — only that cell gains a background/ring. Confirm the row is not wider than before and nothing shifted.
    2. **Tooltips (D-03):** rest on the BR icon — the tooltip must name the exact change, e.g. `Retarget to release/33.7.0`; the MS icon must read `Assign milestone <milestone title>`.
    3. **Keyboard (D-04):** Tab into the row. The action buttons must be reachable and reveal their icon on focus (same as hover). Tab order within the row: `!iid` -> Jira key -> title keys -> BR -> MS.
    4. **Retarget (MRFIX-01, D-06):** click the BR icon. A spinner must appear **and no dialog, no warning, no toast** may appear (D-16). When it settles the cell turns green `✓` **in place** — the row must NOT jump to the clean partition (D-11) — and the `MR Drift` badge must drop by one immediately (D-12). Open the MR in GitLab and confirm its target branch is now the release branch.
    5. **Assign milestone (MRFIX-02):** click the MS icon on another flagged row. Same spinner-then-check behaviour; confirm on GitLab that the release milestone is attached.
    6. **Independence (MRFIX-03, D-09):** on a row flagged in both columns, click BR then immediately click MS. Both must spin independently and both must settle; neither may cancel or clobber the other.
    7. **Failure + retry (D-07/D-08/D-10):** force a failure — easiest is to disconnect the network (or point at a closed/locked MR) and click an action. The cell must turn **red** with a **readable** GitLab message in its tooltip. Report the exact tooltip text: if it says `[object Object]`, this is a hard fail (D-10). Nothing may render outside the row — no toast, no error line under the row. Then trigger a background refetch (switch away from the window and back, or wait past the 5-minute staleTime) — the red state must survive. Click the red cell again: it must retry and clear.
    8. **Unavailable (MRFIX-04, D-14):** open a release whose release branch does not exist yet (the create-branch control is offered in the sidebar). Its flagged BR cells must stay orange `⚠`, must NOT swap to an icon on hover, must not be clickable, and their tooltip must explain the blocker and point at the create-branch control.
    9. **Degraded (D-15):** open a release with no matched GitLab milestone — the degraded banner shows, BR/MS render `—`, and no action appears anywhere.
    10. **Approval side effect (D-16, observation only):** if the Plan 01 probe reported approvals in use, check whether the retargeted MR's approvals were reset on GitLab. Report what you see — this is recorded as a fact, not acted on.

    Type "approved" if all ten pass, or describe exactly which step failed and what you saw.
  </how-to-verify>

  <action>
    After approval, append a `## Live UAT (Plan 04)` section to `.planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md` recording: the release and MR iids exercised, the verbatim failure tooltip text observed in step 7 (the real-world proof for D-10), the step-10 approval-reset observation, and an explicit line `D-16 honoured: no confirm dialog, no warning, no toast on retarget`. If any step failed, record it verbatim instead of an approval line and stop — do not paper over a gap.
  </action>

  <acceptance_criteria>
    - The user has explicitly approved all ten steps, or every failure is recorded verbatim
    - `90-PROBE-RESULTS.md` contains a `## Live UAT (Plan 04)` section naming the MR iids exercised
    - The recorded step-7 tooltip text does not contain `[object Object]`
    - The recorded section contains the line `D-16 honoured: no confirm dialog, no warning, no toast on retarget`
    - The step-10 approval-reset observation is recorded even when the answer is "no approvals configured"
    - No source file under `taskflow/src` is modified by this task
  </acceptance_criteria>

  <verify>
    <automated>grep -q '## Live UAT (Plan 04)' .planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md &amp;&amp; grep -q 'D-16 honoured' .planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md &amp;&amp; ! grep -q '\[object Object\]' .planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md</automated>
  </verify>

  <done>All four requirements are confirmed against live GitLab, the real error-message text is on record, and D-16 is confirmed honoured.</done>

  <resume-signal>Type "approved", or describe which of the ten steps failed and what you saw</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App -> live GitLab (real writes) | The first time this phase's write path runs against production data |
| UAT notes -> repository | Live output pasted into a committed planning document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-16 | Tampering | live retarget on a real MR | mitigate | Writes are limited to the resolved release branch and the matched release milestone (structurally enforced in Plans 01-03), and both are reversible in GitLab's own UI. The UAT is run by the developer on MRs they choose, behind a blocking checkpoint. Step 4/5 include reading back the value on GitLab so an unintended target is caught immediately. |
| T-90-17 | Information Disclosure | UAT notes committed to `.planning/` | mitigate | The recorded section captures only the tooltip text, MR iids and the approval observation — the acceptance criteria forbid pasting any `PRIVATE-TOKEN` value, and Plan 01's redaction rule for `90-PROBE-RESULTS.md` continues to apply to this appended section. |
| T-90-18 | Repudiation | undetected approval reset | accept | Per D-16 (reaffirmed under direct challenge) retarget carries no warning; the approval-reset consequence is recorded in the phase docs rather than mitigated in the UI. Step 10 makes it an observed, written-down fact. Accepted risk, user-owned decision. |
| T-90-19 | Elevation of Privilege | 403 on write | accept | Permission gating is explicitly out of scope (team is all Developer+); a 403 surfaces as a normal `ApiError` and lands in the red-cell tooltip. GitLab's server-side role check remains the sole authority. |
| T-90-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in this phase; an executor reaching for an install must stop. |
</threat_model>

<verification>
- `cd taskflow && npm run test` — full suite green
- `cd taskflow && npx tsc --noEmit` — exits 0
- `cd taskflow && npx biome check ./src` — only the 2-error `BacklogPage.tsx`/`BacklogRow.tsx` baseline
- `90-VALIDATION.md` has no `TBD` / `⬜ pending` rows and `nyquist_compliant: true`
- `90-PROBE-RESULTS.md` carries the `## Live UAT (Plan 04)` record
</verification>

<success_criteria>
- MRFIX-01 and MRFIX-02 confirmed against real GitLab state (target branch and milestone actually changed)
- MRFIX-03 confirmed by firing both cells on one row concurrently and failing one independently
- MRFIX-04 confirmed on a release with no branch: inert cell, explanatory tooltip
- No `[object Object]` reached the user, and no confirm dialog or warning appeared on retarget (D-10, D-16)
</success_criteria>

<output>
Create `.planning/phases/90-per-mr-corrective-actions/90-04-SUMMARY.md` when done. Record the live UAT verdict per step and anything the user asked to change (candidate follow-ups, not silent scope additions).
</output>
