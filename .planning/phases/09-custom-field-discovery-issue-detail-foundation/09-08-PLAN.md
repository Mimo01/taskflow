---
phase: 09-custom-field-discovery-issue-detail-foundation
plan: "08"
type: execute
wave: 5
depends_on:
  - "09-07"
files_modified:
  - taskflow/src/routes/dashboard/index.tsx
autonomous: false
requirements:
  - ISSUE-01

must_haves:
  truths:
    - "IssueDetailSheet is accessible from search results and notification rows in the Dashboard"
    - "Clicking any item in all four entry points (sprint board, my tasks, search, notifications) opens the correct issue"
    - "The app compiles cleanly and all tests pass before checkpoint"
    - "User confirms the sheet opens correctly from all four entry points in a live test"
  artifacts:
    - path: "taskflow/src/routes/dashboard/index.tsx"
      provides: "selectedIssueKey state + IssueDetailSheet at Dashboard root; search result and notification rows trigger it"
  key_links:
    - from: "taskflow/src/routes/dashboard/index.tsx"
      to: "taskflow/src/routes/dashboard/IssueDetailSheet.tsx"
      via: "<IssueDetailSheet issueKey={selectedIssueKey} onClose={...} onOpenIssue={...} />"
      pattern: "IssueDetailSheet"
---

<objective>
Wire `IssueDetailSheet` into the remaining two entry points (search results and notification rows) via the Dashboard root component, then ask the user to verify the complete phase by testing all four entry points in the running app.

Purpose: Complete ISSUE-01 — the sheet must be accessible from ANY view in the app.
Output: `index.tsx` wired; human verification checkpoint confirms all requirements are met.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-custom-field-discovery-issue-detail-foundation/09-CONTEXT.md
@.planning/phases/09-custom-field-discovery-issue-detail-foundation/09-07-SUMMARY.md

<interfaces>
<!-- Dashboard index.tsx — read before modifying -->
<!-- Search and notification entry points vary by existing implementation -->
<!-- Pattern to follow (same as plans 07): -->
// 1. Add useState<string | null>(null) for selectedIssueKey at Dashboard root
// 2. Pass setSelectedIssueKey callback to SearchResultPanel and notification row components
// 3. Render <IssueDetailSheet> at root of Dashboard JSX
// 4. Those components already receive props for callbacks (check existing interfaces first)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire IssueDetailSheet into Dashboard (search + notifications entry points)</name>
  <files>taskflow/src/routes/dashboard/index.tsx</files>
  <action>
Read `taskflow/src/routes/dashboard/index.tsx` completely before making changes. Understand how SearchResultPanel and notification rows are currently rendered.

Add `selectedIssueKey` state and wire IssueDetailSheet:

1. Add `import { useState } from 'react'` if not already present
2. Add `import { IssueDetailSheet } from './IssueDetailSheet'`
3. Add `const [selectedIssueKey, setSelectedIssueKey] = useState<string | null>(null)` in the Dashboard component
4. Pass `onIssueClick={(key: string) => setSelectedIssueKey(key)}` (or equivalent) to `SearchResultPanel` and to any notification row component that renders Jira issues. Check the existing prop interfaces of those components — if they don't have an `onIssueClick` prop yet, add it (optional prop, same pattern as TaskRow)
5. Render at the root of Dashboard's return:

```tsx
<IssueDetailSheet
  issueKey={selectedIssueKey}
  onClose={() => setSelectedIssueKey(null)}
  onOpenIssue={setSelectedIssueKey}
/>
```

Verify with TypeScript: `npx tsc --noEmit` from taskflow/.

If SearchResultPanel currently opens issues in Jira directly via `openUrl`, change that behavior: route through `onIssueClick` instead. The "Open in Jira" deep link is now available INSIDE the detail sheet (ISSUE-09), so the search panel click should open the sheet, not directly open the browser.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit && npx vitest run --reporter=dot</automated>
  </verify>
  <done>
    - IssueDetailSheet rendered in Dashboard root
    - Search results and notification rows trigger selectedIssueKey state when clicked
    - TypeScript clean compile
    - Full test suite green
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Full Phase 9 implementation — IssueDetailSheet accessible from all four entry points:
1. Sprint Board (SprintBoardTab) — click any card
2. My Tasks (MyTasksTab) — click any task title
3. Search results (Dashboard SearchResultPanel) — click any search result
4. Notifications (Dashboard notification rows) — click any Jira notification item

Features in the sheet:
- Two-column layout: description + subtasks + comments (left) / metadata sidebar (right)
- Wiki markup rendered as formatted text (bold, italic, code blocks, lists, tables)
- All metadata visible: priority, assignee, reporter, story points, epic, sprint, labels, fix versions, dates
- Linked issues shown in sidebar with inward/outward type labels
- Inline editing for: assignee (typeahead), priority (select), story points (number input), labels (badge chips)
- Optimistic updates with rollback on failure
- Comment thread (newest-first) with author + relative timestamp + rendered body
- Comment compose box with bold/italic/code/bullet toolbar
- "Open in Jira" deep link button
- Subtask click-through opens subtask in same sheet
  </what-built>
  <how-to-verify>
Run the app: `cd taskflow && npm run tauri dev` (or use the running dev instance if already open).

Check each entry point:
1. Go to Sprint Board. Click any issue card. Confirm the sheet slides in from the right without leaving the board.
2. Go to My Tasks. Click any task title. Confirm the sheet opens with that issue's detail.
3. Use the search bar. Click any Jira issue in the results. Confirm the sheet opens (not a browser tab).
4. Open Notifications. Click any Jira comment notification. Confirm the sheet opens with the relevant issue.

In the open sheet:
- Confirm the two-column layout is visible (description left, metadata right)
- Confirm the description is rendered as formatted text (not raw `*bold*` wiki markup)
- Confirm the assignee/priority fields show a pencil or are click-to-edit
- Click a subtask row and confirm it opens the subtask's detail in the same sheet
- Click "Open in Jira" and confirm a browser tab opens to the correct Jira issue URL
- Type a comment and click Submit — confirm it appears in the thread (newest-first)
  </how-to-verify>
  <resume-signal>Type "approved" if all entry points work correctly. Or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All four entry points open IssueDetailSheet.
Sheet displays correct issue details from the live Jira API.
Wiki markup renders as formatted text.
Inline editing, comment posting, and "Open in Jira" all work.
</verification>

<success_criteria>
- IssueDetailSheet opens from sprint board, my tasks, search results, and notification rows
- Sheet shows full issue detail without leaving the current view
- All interactive features (edit, comment, deep link, subtask nav) confirmed working
- User has typed "approved" at the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/09-custom-field-discovery-issue-detail-foundation/09-08-SUMMARY.md`
</output>
