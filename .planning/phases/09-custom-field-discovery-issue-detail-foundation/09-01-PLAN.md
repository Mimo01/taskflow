---
phase: 09-custom-field-discovery-issue-detail-foundation
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/IssueDetailSheet.test.tsx
  - taskflow/src/routes/dashboard/WikiRenderer.test.tsx
  - taskflow/src/services/jira.test.ts
autonomous: true
requirements:
  - ISSUE-01
  - ISSUE-02
  - ISSUE-03
  - ISSUE-04
  - ISSUE-05
  - ISSUE-06
  - ISSUE-07
  - ISSUE-08
  - ISSUE-09

must_haves:
  truths:
    - "All Wave 0 test scaffold files exist and the test suite runs (even if tests are pending/skipped)"
    - "jira2md, react-markdown, remark-gfm, and @tailwindcss/typography are installed"
    - "shadcn Sheet component is installed in src/components/ui/sheet.tsx"
    - "New jira.test.ts tests for fetchIssueDetail and discoverCustomFields are present"
  artifacts:
    - path: "taskflow/src/routes/dashboard/IssueDetailSheet.test.tsx"
      provides: "Test stubs for ISSUE-01, 04, 05, 06, 07, 08, 09"
    - path: "taskflow/src/routes/dashboard/WikiRenderer.test.tsx"
      provides: "Test stubs for ISSUE-02"
    - path: "taskflow/src/services/jira.test.ts"
      provides: "Test stubs for fetchIssueDetail and discoverCustomFields (ISSUE-03)"
    - path: "taskflow/src/components/ui/sheet.tsx"
      provides: "shadcn Sheet component"
  key_links:
    - from: "IssueDetailSheet.test.tsx"
      to: "IssueDetailSheet.tsx (future)"
      via: "import stub that allows test file to exist before implementation"
      pattern: "vi.mock"
    - from: "WikiRenderer.test.tsx"
      to: "WikiRenderer.tsx (future)"
      via: "import stub"
      pattern: "vi.mock"
---

<objective>
Install dependencies and create all Wave 0 test scaffold files so every subsequent wave can run `vitest run` immediately after each task.

Purpose: Nyquist compliance — no implementation wave should ship without an automated verify command. All test files for Phase 9 are new (none exist yet), so they must be created before implementation begins.
Output: Installed packages, shadcn Sheet primitive, and three test files with describe-blocks and todo/skip stubs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-custom-field-discovery-issue-detail-foundation/09-CONTEXT.md
@.planning/phases/09-custom-field-discovery-issue-detail-foundation/09-RESEARCH.md
@.planning/phases/09-custom-field-discovery-issue-detail-foundation/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and shadcn Sheet</name>
  <files>taskflow/package.json, taskflow/src/components/ui/sheet.tsx, taskflow/src/index.css</files>
  <action>
Run the following commands from the repo root (the taskflow/ subdirectory):

```bash
cd /Users/mimo/Desktop/Tasker/taskflow
npm install jira2md react-markdown remark-gfm @tailwindcss/typography
npx shadcn@latest add sheet --yes
```

After install, add the typography plugin to `src/index.css`. Find the `@import "tailwindcss"` line (or the Tailwind CSS v4 entry) and add `@plugin "@tailwindcss/typography";` immediately after it. This enables the `prose` utility class used by WikiRenderer.

Verify jira2md can be imported: create a quick smoke test by checking that `node -e "require('jira2md')"` exits 0 (or that the package is in package.json dependencies).
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && node -e "require('jira2md'); require('react-markdown'); console.log('ok')" 2>/dev/null || (ls node_modules/jira2md && ls node_modules/react-markdown && echo ok)</automated>
  </verify>
  <done>jira2md, react-markdown, remark-gfm, @tailwindcss/typography present in node_modules; taskflow/src/components/ui/sheet.tsx exists; @plugin "@tailwindcss/typography" in index.css</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test scaffolds</name>
  <files>
    taskflow/src/routes/dashboard/IssueDetailSheet.test.tsx,
    taskflow/src/routes/dashboard/WikiRenderer.test.tsx,
    taskflow/src/services/jira.test.ts
  </files>
  <action>
Create two new test files and extend the existing jira.test.ts. All stubs use `it.todo()` so the suite stays green (pending, not failing) before implementation.

**File 1: `taskflow/src/routes/dashboard/IssueDetailSheet.test.tsx`** — new file

```tsx
import { describe, it } from 'vitest'

// Wave 0 scaffold — implementation in plans 04-08.
// These stubs exist so `vitest run` passes on every wave before the component ships.

describe('IssueDetailSheet', () => {
  describe('ISSUE-01: open/close', () => {
    it.todo('renders sheet open when issueKey is a non-null string')
    it.todo('renders sheet closed when issueKey is null')
    it.todo('calls onClose when Sheet onOpenChange fires with false')
  })

  describe('ISSUE-04: optimistic field update', () => {
    it.todo('applies optimistic update to assignee field immediately')
    it.todo('rolls back assignee to previous value when mutation errors')
    it.todo('applies optimistic update to priority field immediately')
    it.todo('applies optimistic update to story points field immediately')
    it.todo('applies optimistic update to labels field immediately')
  })

  describe('ISSUE-05: subtask list', () => {
    it.todo('renders each subtask with key, summary, and status badge')
    it.todo('clicking a subtask calls onOpenIssue with the subtask key')
  })

  describe('ISSUE-06: linked issues', () => {
    it.todo('renders inward linked issues with type.inward label')
    it.todo('renders outward linked issues with type.outward label')
  })

  describe('ISSUE-07: comment thread', () => {
    it.todo('renders comments ordered newest-first')
    it.todo('each comment shows author displayName and relative timestamp')
    it.todo('renders comment body through WikiRenderer (wiki markup converted)')
  })

  describe('ISSUE-08: post comment', () => {
    it.todo('calls postComment with issueKey and compose box text on submit')
    it.todo('clears compose box after successful submission')
  })

  describe('ISSUE-09: open in Jira deep link', () => {
    it.todo('calls openUrl with ${jiraBaseUrl}/browse/${issueKey} when button clicked')
  })
})
```

**File 2: `taskflow/src/routes/dashboard/WikiRenderer.test.tsx`** — new file

```tsx
import { describe, it, expect } from 'vitest'

// Wave 0 scaffold — implementation in plan 03.

describe('WikiRenderer', () => {
  describe('ISSUE-02: wiki markup rendering', () => {
    it.todo('renders bold text from *bold* wiki markup')
    it.todo('renders italic text from _italic_ wiki markup')
    it.todo('renders code block from {code} wiki markup')
    it.todo('renders bullet list from * item wiki markup')
    it.todo('returns empty output for null/undefined input (no throw)')
    it.todo('renders plain text unchanged')
  })
})
```

**Extend: `taskflow/src/services/jira.test.ts`** — append new describe blocks after the final existing describe block. Do NOT remove or alter any existing tests. Read the file first to find the correct insertion point (after the last `})` closing the outermost describe, or as new top-level describes). Add:

```ts
// ─── Phase 9 scaffolds ────────────────────────────────────────────────────────

describe('ISSUE-03: fetchIssueDetail', () => {
  it.todo('requests the issue with an explicit fields= param including epicLinkFieldKey and sprintFieldKey')
  it.todo('returns all expected metadata fields from the response')
  it.todo('throws when response is not ok')
})

describe('ISSUE-03: discoverCustomFields', () => {
  it.todo('returns epicLinkFieldKey matching schema.custom gh-epic-link field')
  it.todo('returns sprintFieldKey matching schema.custom gh-sprint field')
  it.todo('returns storyPointsFieldKey matching Story Points float field')
  it.todo('returns defaults when API call fails')
  it.todo('returns defaults when response is not ok')
})
```
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `taskflow/src/routes/dashboard/IssueDetailSheet.test.tsx` exists with describe blocks for ISSUE-01, 04, 05, 06, 07, 08, 09
    - `taskflow/src/routes/dashboard/WikiRenderer.test.tsx` exists with describe block for ISSUE-02
    - `taskflow/src/services/jira.test.ts` has new describe blocks for fetchIssueDetail and discoverCustomFields
    - `npx vitest run` passes (all new tests pending, existing tests green)
  </done>
</task>

</tasks>

<verification>
`cd taskflow && npx vitest run` exits 0 — no failures (pending todos are not failures).
`ls taskflow/src/components/ui/sheet.tsx` — file exists.
`ls taskflow/node_modules/jira2md` — directory exists.
</verification>

<success_criteria>
- All 4 packages installed: jira2md, react-markdown, remark-gfm, @tailwindcss/typography
- shadcn Sheet installed at taskflow/src/components/ui/sheet.tsx
- @plugin "@tailwindcss/typography" present in taskflow/src/index.css
- IssueDetailSheet.test.tsx, WikiRenderer.test.tsx created; jira.test.ts extended
- Full vitest suite green (pending todos do not cause failures)
</success_criteria>

<output>
After completion, create `.planning/phases/09-custom-field-discovery-issue-detail-foundation/09-01-SUMMARY.md`
</output>
