---
phase: 13-epic-management
plan: "05"
type: execute
wave: 4
depends_on:
  - "13-02"
  - "13-03"
  - "13-04"
files_modified: []
autonomous: false
requirements:
  - EPIC-01
  - EPIC-02
  - EPIC-03
  - EPIC-04

must_haves:
  truths:
    - "User can navigate to /epics and see all epics with name, status badge, story count, total points, and progress bar"
    - "User can filter the sprint board by epic — selecting an epic hides non-matching stories"
    - "User can click an epic row on /epics and see a slide-over with stories under that epic"
    - "User can create a new epic from the /epics page and see it appear after refresh"
  artifacts:
    - path: "taskflow/src/routes/dashboard/EpicsPage.tsx"
      provides: "Working /epics route"
    - path: "taskflow/src/routes/dashboard/EpicDetailSheet.tsx"
      provides: "Working epic detail slide-over"
    - path: "taskflow/src/routes/dashboard/CreateEpicDialog.tsx"
      provides: "Working create epic dialog"
    - path: "taskflow/src/routes/dashboard/SprintBoardTab.tsx"
      provides: "Working epic filter bar on sprint board"
  key_links:
    - from: "EpicsPage epic name button"
      to: "EpicDetailSheet"
      via: "onEpicClick -> setSelectedEpicKey in AppLayout"
      pattern: "onEpicClick"
    - from: "EpicDetailSheet story row button"
      to: "IssueDetailSheet"
      via: "onOpenIssue -> setSelectedIssueKey in AppLayout"
      pattern: "onOpenIssue"
---

<objective>
Human verification checkpoint for all four EPIC requirements. Verify on the live Orange Jira instance that the epics list, epic detail sheet, sprint board filter, and create epic dialog all work correctly with real data.

Purpose: Unit tests confirm component behavior with mocks; human verification confirms the full integration works with the real Jira DC instance.
Output: EPIC-01 through EPIC-04 confirmed working or issues surfaced for gap closure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-epic-management/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite gate — all EPIC tests GREEN</name>
  <files></files>
  <action>
Run the full Vitest test suite and confirm:
1. All four EPIC requirement test files pass GREEN
2. Zero regressions in the full suite (351+ tests passing, same baseline as Phase 12 completion)

Commands:
```bash
cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run --reporter=verbose EpicsPage EpicDetailSheet CreateEpicDialog SprintBoardTab 2>&1 | tail -40
cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run --reporter=verbose 2>&1 | tail -10
cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1
```

If any test is RED or any TypeScript error exists, stop and fix before proceeding to the human verification checkpoint.</action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass. TypeScript clean. Test count is at least 351 (Phase 12 baseline) — new EPIC tests added on top. Zero TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — all four EPIC requirements on Orange Jira instance</name>
  <action>
Start the dev server: `cd /Users/mimo/Desktop/Tasker/taskflow && npm run dev`

Then verify each EPIC requirement against the live Orange Jira instance using the steps in how-to-verify below.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms all four EPIC requirements (EPIC-01 through EPIC-04) pass on the Orange Jira instance and types "approved".</done>
  <what-built>
Four EPIC requirements fully implemented:
- EPIC-01: /epics page listing all epics with name, status, story count, points, and progress bar
- EPIC-02: Sprint board epic filter bar — selecting an epic filters to matching stories only
- EPIC-03: EpicDetailSheet slide-over opens from /epics, shows stories list with key/summary/status/points
- EPIC-04: "+ Create Epic" button on /epics opens dialog; submits to Jira with Epic type

Sidebar has /epics NavLink visible regardless of role. Story rows in EpicDetailSheet click through to IssueDetailSheet.
  </what-built>
  <how-to-verify>

**Verify EPIC-01 — Epic list:**
1. Open the app. Click "Epics" in the sidebar (should appear above the role-specific section).
2. Confirm: /epics page loads and shows a list of epics from the Orange Jira instance.
3. For each epic row, confirm: epic name is visible, status badge shown, story count column, story points column, and a progress bar.
4. If project has no epics, confirm an empty state message is shown (not a blank/broken page).

**Verify EPIC-02 — Sprint board epic filter:**
1. Navigate to /sprint-board.
2. Confirm: an "Epic:" filter control appears at the top of the board (above the column headers).
3. Select an epic from the filter. Confirm: stories not belonging to that epic are hidden from the board.
4. Confirm: stories with no epic are also hidden when a filter is active.
5. Clear the filter (click "Clear" or select "All epics"). Confirm: all stories return.
6. If no epics are linked to sprint stories, the filter control should not appear (epicOptions.length === 0).

**Verify EPIC-03 — Epic detail sheet:**
1. On the /epics page, click an epic name.
2. Confirm: a wide slide-over panel (85vw) opens from the right.
3. Confirm: the stories list is visible in the left column with issue keys, summaries, status badges, and story points.
4. Click a story row. Confirm: IssueDetailSheet opens for that story (existing behavior).
5. Close the EpicDetailSheet (X or click outside). Confirm: it closes without errors.

**Verify EPIC-04 — Create epic:**
1. On the /epics page, click "+ Create Epic" button in the page header.
2. Confirm: the Create Epic dialog opens with an "Epic Name" field (required).
3. Enter an epic name (e.g. "Test Epic from App"). Optionally add a description.
4. Click "Create Epic". Confirm: the dialog closes and the new epic appears in the list on next load (may require page reload or cache invalidation).
5. Verify the epic was created in the real Jira instance.

**Negative checks:**
- Navigating to /epics without Jira configured should not crash (shows loading or error state gracefully).
- TypeScript: `cd taskflow && npx tsc --noEmit` exits 0.
  </how-to-verify>
  <resume-signal>Type "approved" if all four EPIC requirements pass on the Orange Jira instance, or describe specific issues found.</resume-signal>
</task>

</tasks>

<verification>
All four EPIC requirements (EPIC-01 through EPIC-04) confirmed working on the live Orange Jira instance.
Full test suite GREEN with zero regressions.
</verification>

<success_criteria>
- EPIC-01: /epics page shows real epics from Orange Jira with all required columns
- EPIC-02: Sprint board epic filter hides non-matching stories
- EPIC-03: EpicDetailSheet opens and shows real stories under a real epic
- EPIC-04: Create Epic dialog successfully creates an epic in Jira
- Human verified on Orange Jira instance
- Full test suite GREEN, TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/13-epic-management/13-05-SUMMARY.md`
</output>
