---
phase: 53
plan: "04"
type: execute
wave: 3
depends_on: ["53-03"]
files_modified: []
autonomous: false
requirements:
  - AION-04
  - AIOC-01
  - AIOC-02
  - AIOC-03
  - AIOP-01
  - AIOP-02
  - AIOP-03

must_haves:
  truths:
    - "User can navigate to a cycle detail page and see the cycle name in the heading"
    - "Progress bar renders with colored segments sized by status counts"
    - "Test run list shows test case names, status badges, and dates"
    - "Filter chips start all-active; toggling chips filters the run list"
    - "Pin cycle button pins the cycle and it appears in the header tab strip"
    - "Pinned cycle tab click navigates back to the cycle detail page"
    - "Unpin cycle tab right-click → Unpin removes it from the strip"
    - "Pinned cycle tab persists after app restart"
    - "If any runs have defects, the Defects section renders clickable Jira issue links"
    - "All 53-0x tests are GREEN (no todo stubs left that cover implemented behavior)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/AioCycleDetailPage.tsx"
      provides: "Verified live cycle detail page"
    - path: "taskflow/src/components/app/PinnedTabStrip.tsx"
      provides: "Verified cycle tab rendering in the header"
    - path: "taskflow/src/stores/pinned-tabs.store.ts"
      provides: "Verified persistence across restart"
  key_links:
    - from: "Header tab strip (PinnedTabStrip)"
      to: "/aio-cycle/:projectKey/:cycleKey route"
      via: "click handler in main.tsx onTabClick"
      pattern: "manual"
---

<objective>
Wave 3: Human verification checkpoint. Developer manually exercises the full Phase 53 feature set against the running app with real AIO data, then confirms sign-off or reports issues.

Purpose: Automated tests cover unit behavior and component rendering. This checkpoint verifies the integrated end-to-end flow (real API data, real navigation, real persistence across restart) that cannot be covered by Vitest.
Output: Developer sign-off on all 5 success criteria; or a list of issues for gap closure.
</objective>

<execution_context>
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/53-cycle-detail-header-pinning/53-CONTEXT.md
@.planning/phases/53-cycle-detail-header-pinning/53-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill in Wave 0 test stubs and run full suite</name>
  <files>
    taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx
    taskflow/src/stores/pinned-tabs.store.test.ts
    taskflow/src/components/app/PinnedTabStrip.test.tsx
  </files>
  <read_first>
    - taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx — read current file; identify all it.todo stubs; replace with real test implementations now that the component exists
    - taskflow/src/stores/pinned-tabs.store.test.ts — read current file; identify the new todo stubs for setPinnedCycleMeta, clearCycleMeta, and migration; implement them
    - taskflow/src/components/app/PinnedTabStrip.test.tsx — read current file; implement the cycle tab rendering tests now that the component has been refactored
    - taskflow/src/routes/dashboard/AioCycleDetailPage.tsx — read to understand mock data shapes needed for tests
    - taskflow/src/stores/pinned-tabs.store.ts — read final implementation to write correct test assertions
    - taskflow/src/components/app/PinnedTabStrip.tsx — read final props interface and render logic for correct test data
  </read_first>
  <action>
    Replace all `it.todo(...)` stubs in the three test files with real test implementations. Use the existing passing tests in `AioProjectOverviewPage.test.tsx` and `pinned-tabs.store.test.ts` as patterns.

    FOR AioCycleDetailPage.test.tsx:
    - Mock `fetchAioTestRunsForCycle` to return an array of 3 AioTestRun objects with statuses PASS, FAIL, NOT_EXECUTED and testCase.title values.
    - Mock `fetchAioCycleDetail` to return an AioCycle object with name and status.
    - progress bar test: render with mocked runs → assert green segment is visible (by class or test-id) and counts show Pass:1, Fail:1, Not Run:1.
    - no-runs test: mock empty runs array → assert "No runs recorded" text is in document.
    - filter chip test: render, find "Fail" chip, click it, assert only Fail-status row remains visible.
    - defects test: render runs where one run has `defects: ['PROJ-42']` → assert NavLink with text "PROJ-42" and href "/issue/PROJ-42" is in document.
    - no-defects test: render runs with empty defects → assert "Defects" heading is NOT in document.
    - pin button label test: mock isPinned returning false → assert button text "Pin cycle".
    - pin button click test: click "Pin cycle" button → assert togglePin was called with cycleKey and setPinnedCycleMeta was called.

    FOR pinned-tabs.store.test.ts:
    - setPinnedCycleMeta test: call `usePinnedTabsStore.getState().setPinnedCycleMeta('PROJ-CY-2', { name: 'Sprint 2', projectKey: 'PROJ' })` → assert `usePinnedTabsStore.getState().pinnedCycleMeta['PROJ-CY-2']` equals `{ name: 'Sprint 2', projectKey: 'PROJ' }`.
    - clearCycleMeta test: set meta, then call `clearCycleMeta('PROJ-CY-2')` → assert key is removed from pinnedCycleMeta.
    - migration v0→v1 test: create a mock persisted state without pinnedCycleMeta, run through the migrate callback with version=0 → assert result has `pinnedCycleMeta: {}`.
    - default value test: call `usePinnedTabsStore.getState().pinnedCycleMeta` on a reset store → assert it equals `{}`.

    FOR PinnedTabStrip.test.tsx:
    - Cycle tab render test: render `<PinnedTabStrip pinnedKeys={['PROJ-CY-2']} resolvedTabs={new Map([['PROJ-CY-2', { type: 'cycle', name: 'Sprint 2', projectKey: 'PROJ' }]])} activeKey={null} onTabClick={vi.fn()} onTabClose={vi.fn()} onReorder={vi.fn()} />` → assert FlaskConical test-id is present and text "Sprint 2" is in document.
    - Aria-label test: find strip container → assert aria-label is "Pinned tabs".
    - Tab click test: render with one tab → click it → assert onTabClick was called with the tab key.

    After all stubs are replaced: run full suite. All tests must be GREEN.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npm test` exits 0 (all tests pass)
    - AioCycleDetailPage.test.tsx has zero `it.todo` entries — all converted to real tests
    - pinned-tabs.store.test.ts has zero `it.todo` entries in the new describe blocks
    - PinnedTabStrip.test.tsx has zero `it.todo` entries — all converted to real tests
    - Total passing test count is greater than before Phase 53 started (net addition of tests)
    - `cd taskflow && npm test -- --reporter=verbose 2>&1 | grep -c "✓"` returns a count higher than the Wave 0 baseline
  </acceptance_criteria>
  <done>All Wave 0 stubs implemented; full test suite green with no todos remaining in new test files</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 53-00 through 53-03 built:
    - AioCycleDetailPage at /aio-cycle/:projectKey/:cycleKey with progress bar, filter chips, test run table, defects section, and Pin/Unpin button
    - AioCycleDetailSkeleton for 200ms flicker-free loading
    - usePinnedTabsStore extended with pinnedCycleMeta (version 0→1, migration)
    - PinnedTabStrip refactored with discriminated union resolvedTabs prop
    - main.tsx wired: cycle keys bypass useQueries, resolvedPinnedTabs includes CycleTab entries, activeCycleKey derived from URL, cycle tab click navigates correctly
    - Full test suite green (unit + component tests for all new behavior)
  </what-built>
  <how-to-verify>
    Run the app: `cd taskflow && npm run tauri dev`

    **Verify 1 — Cycle detail page renders (AION-04, AIOC-01, AIOC-02):**
    1. Enable AIO from Settings → Integrations → AIO toggle ON
    2. Navigate: sidebar → AIO Projects → pick a project → click a cycle
    3. Confirm URL is `/aio-cycle/:projectKey/:cycleKey`
    4. Confirm cycle name appears in the h1 heading with a status badge
    5. Confirm progress bar shows colored segments (or "No runs recorded" if empty)
    6. Confirm test run table has rows with test case names, status badges, and dates
    7. Confirm skeleton appears briefly on first load then transitions to data

    **Verify 2 — Filter chips (AIOC-02):**
    1. All 4 chips (Not Run / Pass / Fail / Blocked) are visible and active by default
    2. Click "Fail" chip to deactivate it — confirm Fail-status rows disappear
    3. Click it again to re-activate — rows return
    4. Deactivate all chips — confirm "No runs match the selected filters" message appears

    **Verify 3 — Defects section (AIOC-03):**
    1. If any runs have defects, confirm "Defects" section appears below the run table
    2. Confirm defect keys render as links
    3. Click a defect link — confirm navigation to `/issue/:key` (Jira issue detail)
    4. If no runs have defects, confirm "Defects" section is absent (not shown as empty)

    **Verify 4 — Pin and header tab (AIOP-01, AIOP-02):**
    1. On the cycle detail page, click "Pin cycle" button
    2. Confirm button label changes to "Unpin cycle"
    3. Confirm a cycle tab appears in the header tab strip with FlaskConical icon, cycle key, and cycle name
    4. Confirm the cycle tab is highlighted (active state) while on the cycle detail page
    5. Navigate to a different page — confirm cycle tab is no longer highlighted but remains in the strip
    6. Click the cycle tab in the header — confirm navigation back to the cycle detail page
    7. Right-click the cycle tab → Unpin — confirm tab is removed from the strip and button returns to "Pin cycle"

    **Verify 5 — Persistence across restart (AIOP-03):**
    1. Pin a cycle (from Verify 4)
    2. Quit the app completely (Cmd+Q on macOS)
    3. Relaunch the app
    4. Confirm the cycle tab is still in the header tab strip
    5. Click the tab — confirm navigation to the correct cycle detail page

    **Verify 6 — Issue tab behavior unchanged:**
    1. Pin a Jira issue (from any issue detail page)
    2. Confirm issue tab still appears alongside cycle tab in the strip
    3. Issue tab click still navigates to the issue detail — not broken by the refactor
    4. Drag to reorder issue and cycle tabs — confirm both can be interleaved
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 6 verifications pass.
    Or describe specific failures (e.g., "Verify 3 fails — defects section visible even with no defects") for gap closure.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → running app | Manual verification against real AIO data and real Tauri persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04-01 | Information Disclosure | pinned-tabs.json on disk | accept | Tauri app data directory; OS-level sandboxing; cycle names are non-sensitive display data |
</threat_model>

<verification>
Automated gate (before human checkpoint):
```
cd taskflow && npm test
```
Must exit 0 with full green suite before presenting the human checkpoint.
</verification>

<success_criteria>
- Full test suite green (all previously passing tests + all new Phase 53 tests)
- Developer confirms "approved" for all 6 verification scenarios
- Phase 53 can be marked complete in STATE.md and ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/53-cycle-detail-header-pinning/53-04-SUMMARY.md`
</output>
