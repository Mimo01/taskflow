---
phase: 53
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx
  - taskflow/src/stores/pinned-tabs.store.test.ts
  - taskflow/src/components/app/PinnedTabStrip.test.tsx
autonomous: true
requirements:
  - AION-04
  - AIOC-01
  - AIOC-02
  - AIOC-03
  - AIOP-01
  - AIOP-02
  - AIOP-03

must_haves:
  truths:
    - "All three test stub files exist and their stub tests run (and fail with 'not yet implemented' or import errors) — no passing tests on non-existent behavior"
    - "Running `cd taskflow && npm test` with only stub files does not throw a module-resolution error (imports are correct paths)"
    - "pinned-tabs.store.test.ts has new describe blocks for setPinnedCycleMeta, clearCycleMeta, and v0→v1 migration"
    - "AioCycleDetailPage.test.tsx has stub tests for render, progress bar, filter chips, defects, and pin button"
    - "PinnedTabStrip.test.tsx has stub tests for cycle tab rendering with FlaskConical icon"
  artifacts:
    - path: "taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx"
      provides: "RED test stubs for AION-04, AIOC-01, AIOC-02, AIOC-03, AIOP-01"
    - path: "taskflow/src/stores/pinned-tabs.store.test.ts"
      provides: "Extended RED stubs for AIOP-02, AIOP-03, store migration"
    - path: "taskflow/src/components/app/PinnedTabStrip.test.tsx"
      provides: "RED stubs for AIOP-01 cycle tab rendering"
  key_links:
    - from: "AioCycleDetailPage.test.tsx"
      to: "AioCycleDetailPage.tsx"
      via: "import — will fail until Wave 1 creates the component"
      pattern: "import.*AioCycleDetailPage"
    - from: "PinnedTabStrip.test.tsx"
      to: "PinnedTabStrip.tsx"
      via: "import — must reference resolvedTabs prop (not yet renamed)"
      pattern: "import.*PinnedTabStrip"
---

<objective>
Wave 0: Create RED test stub files for all three test targets in Phase 53 before any implementation begins. Stubs define the test shape without asserting behavior that doesn't exist yet — they fail gracefully (import error or `todo()` marker), establishing the Nyquist sampling grid.

Purpose: Executor agents in Wave 1+ work against existing test files rather than creating tests and implementation simultaneously. The stub files lock the test API surface and ensure CI catches regressions during implementation.
Output: Three test files — two extended (store tests), one new (PinnedTabStrip), one new (AioCycleDetailPage).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/53-cycle-detail-header-pinning/53-CONTEXT.md
@.planning/phases/53-cycle-detail-header-pinning/53-VALIDATION.md

<!-- Key test infrastructure pattern: store tests co-located with store files; page tests co-located with page files; component tests co-located with component files -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AioCycleDetailPage test stubs (RED)</name>
  <files>taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx — test file structure, mock patterns (vi.mock for stronghold, auth store, react-query), render helper setup; copy mock structure verbatim
    - taskflow/src/routes/dashboard/AioProjectsPage.test.tsx — second reference for describe/it block naming conventions in this directory
    - taskflow/src/routes/dashboard/AioCycleDetailPage.tsx — does NOT exist yet; the import will fail until Wave 1 creates it; that is expected (RED)
  </read_first>
  <behavior>
    - Stub test: renders AioCycleDetailPage without crashing (will fail: module not found)
    - Stub test: shows progress bar with correct Pass/Fail/Blocked/Not Run counts
    - Stub test: shows "No runs recorded" when runs array is empty
    - Stub test: filter chips default to all-active; toggling Fail chip hides Pass/Blocked/Not-Run rows
    - Stub test: renders defects section with NavLinks when runs have defects
    - Stub test: hides defects section when no runs have non-empty defects array
    - Stub test: Pin button reads "Pin cycle" when cycle is not pinned
    - Stub test: clicking Pin button calls togglePin and setPinnedCycleMeta
    - Stub test: shows "No runs match the selected filters" when all chips toggled off
  </behavior>
  <action>
    Create `taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx`.

    Copy the vi.mock block structure from `AioProjectOverviewPage.test.tsx` exactly: mock `@tauri-apps/plugin-stronghold` (readSecret returns 'fake-token'), mock `@/stores/auth.store` (jiraBaseUrl = 'https://jira.example.com'), mock `@tanstack/react-query` useQuery to return configurable data.

    Add a mock for `@/stores/pinned-tabs.store` returning: `isPinned: vi.fn().mockReturnValue(false)`, `togglePin: vi.fn()`, `setPinnedCycleMeta: vi.fn()`, `removePin: vi.fn()`, `clearCycleMeta: vi.fn()`.

    Use `vi.fn()` for `fetchAioTestRunsForCycle` from `@/services/aio`. Use `vi.fn()` for a cycle detail fetch function (name TBD in Wave 1 — use `fetchAioCycleDetail`).

    All `it()` / `test()` calls use `it.todo('...')` format so they are skipped but listed. The import of `AioCycleDetailPage` is a live import (not a todo) so the file fails with import error until Wave 1 ships the component — this is the expected RED state.

    File path for import: `import { AioCycleDetailPage } from './AioCycleDetailPage'`.

    Use `describe('AioCycleDetailPage', () => { ... })` as root block. Nest sub-describe blocks: `describe('progress bar', ...)`, `describe('filter chips', ...)`, `describe('defects section', ...)`, `describe('pin button', ...)`.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --reporter=verbose AioCycleDetailPage 2>&1 | grep -E "FAIL|cannot find module|AioCycleDetailPage|todo" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx`
    - File imports `AioCycleDetailPage` from `./AioCycleDetailPage` (non-todo import)
    - File contains at least 8 `it.todo(...)` entries across the 4 describe blocks (progress bar, filter chips, defects, pin button)
    - Running the test produces "cannot find module" or similar import failure — NOT a passing suite (the component doesn't exist yet)
    - vi.mock blocks are present for: stronghold, auth.store, tanstack/react-query, pinned-tabs.store, services/aio
  </acceptance_criteria>
  <done>AioCycleDetailPage.test.tsx exists with correct mock structure and stub todos; test runner reports it (fails on import or shows todo stubs)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend pinned-tabs.store.test.ts with RED stubs</name>
  <files>taskflow/src/stores/pinned-tabs.store.test.ts</files>
  <read_first>
    - taskflow/src/stores/pinned-tabs.store.test.ts — read the FULL current file; understand the existing 5 tests (togglePin, isPinned, removePin, reorder) and the beforeEach reset pattern using usePinnedTabsStore.setState; MUST preserve all existing tests exactly
    - taskflow/src/stores/pinned-tabs.store.ts — current interface: version 0, no pinnedCycleMeta field yet; the new test stubs reference fields that will be added in Wave 1
  </read_first>
  <behavior>
    - Existing 5 tests: preserved exactly, still pass
    - New stub: setPinnedCycleMeta stores {name, projectKey} under the key in pinnedCycleMeta
    - New stub: clearCycleMeta removes the key from pinnedCycleMeta
    - New stub: v0→v1 migration sets pinnedCycleMeta = {} on persisted state with version < 1
    - New stub: fresh store (no persisted state) has pinnedCycleMeta = {} as default
  </behavior>
  <action>
    Read `taskflow/src/stores/pinned-tabs.store.test.ts` first. Append (do NOT replace) a new `describe('cycle metadata actions', () => { ... })` block after the existing tests.

    Inside `cycle metadata actions`, add `beforeEach` that calls `usePinnedTabsStore.setState({ pinnedCycleMeta: {} })` to reset cycle meta between tests. Then add `it.todo(...)` stubs for: setPinnedCycleMeta, clearCycleMeta, migration (v0→v1), and default value on fresh install.

    Add a second `describe('v0→v1 migration', () => { ... })` block with an `it.todo(...)` stub for the migration test.

    The existing tests must remain untouched and still pass. Run the test after editing to confirm existing 5 tests are GREEN and the new blocks appear as TODO.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --reporter=verbose pinned-tabs.store 2>&1 | grep -E "✓|✗|todo|PASS|FAIL" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All previously passing tests (5) still pass after the file edit
    - New `describe('cycle metadata actions', ...)` block is present with at least 4 `it.todo(...)` entries
    - New `describe('v0→v1 migration', ...)` block is present with at least 1 `it.todo(...)` entry
    - Test suite does NOT have new failing (non-todo) tests
  </acceptance_criteria>
  <done>pinned-tabs.store.test.ts has 5 green existing tests + new todo stubs for cycle meta and migration behavior</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: New PinnedTabStrip.test.tsx with RED stubs</name>
  <files>taskflow/src/components/app/PinnedTabStrip.test.tsx</files>
  <read_first>
    - taskflow/src/components/app/PinnedTabStrip.tsx — current props interface (resolvedIssues, not yet renamed); current ResolvedIssue type; aria-label="Pinned issues" (line ~201); tab rendering paths (lines ~263–280)
    - taskflow/src/components/app/Sidebar.test.tsx — co-located test file for mock patterns in this directory; copy vi.mock structure for lucide-react if needed
    - taskflow/src/components/app/TopBar.test.tsx — second reference for render setup patterns
  </read_first>
  <behavior>
    - Stub test: renders issue tab with IssueTypeIcon + key + summary when resolvedTabs has type='issue' entry
    - Stub test: renders cycle tab with FlaskConical icon + key + cycle name when resolvedTabs has type='cycle' entry
    - Stub test: active cycle tab has border-primary class
    - Stub test: clicking a tab calls onTabClick with the tab key
    - Stub test: aria-label on strip element equals "Pinned tabs"
  </behavior>
  <action>
    Create `taskflow/src/components/app/PinnedTabStrip.test.tsx` (new file — no existing file).

    Import `PinnedTabStrip` from `./PinnedTabStrip` — the current file uses `resolvedIssues` prop. The stub tests must reference the FUTURE prop name `resolvedTabs` (as it will be named after Wave 1). This means the test file will have TypeScript errors until Wave 1 renames the prop — that is acceptable RED state.

    Mock lucide-react icons: `vi.mock('lucide-react', () => ({ FlaskConical: () => <span data-testid="flask-icon" />, IssueTypeIcon: () => <span />, Loader2: () => <span />, ... }))` — include all icons referenced in PinnedTabStrip.tsx import block (read the file to enumerate them).

    Use `it.todo(...)` for all behavioral stubs. The import itself is a live import (will compile once Wave 1 renames the prop and updates the type).

    Root describe: `describe('PinnedTabStrip', () => { ... })`. Sub-describe: `describe('cycle tab rendering', ...)` with 3 todos. Top-level todos for: tab click handler, aria-label.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --reporter=verbose PinnedTabStrip 2>&1 | grep -E "✓|todo|PASS|FAIL|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `taskflow/src/components/app/PinnedTabStrip.test.tsx`
    - File imports from `./PinnedTabStrip`
    - File contains at least 5 `it.todo(...)` entries covering: cycle tab FlaskConical icon, cycle tab name display, active tab styling, tab click handler, aria-label
    - Running the test does not crash the test runner (TypeScript errors are acceptable; crash is not)
    - vi.mock for lucide-react is present with FlaskConical stub
  </acceptance_criteria>
  <done>PinnedTabStrip.test.tsx exists with stub tests for cycle tab rendering and aria-label update</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test stubs → source imports | Test files import source that does not exist yet (AioCycleDetailPage); controlled failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-W0-01 | Tampering | Test mock setup | accept | Mocks are test-only; no production path affected |
</threat_model>

<verification>
After all three tasks:

```
cd taskflow && npm test -- --reporter=dot
```

Expected: existing suite GREEN, new stub files appear as TODO or fail on import of missing component (AioCycleDetailPage). No regressions in `issue-runs.test.ts`, `cycles.test.ts`, `AioProjectOverviewPage.test.tsx`, `AioProjectsPage.test.tsx`.
</verification>

<success_criteria>
- Three test stub files exist in correct locations
- Existing test suite (all previously passing tests) remains GREEN
- New stub files register as todo or fail-on-import (RED state) — not silent omission
- No watch-mode flags used
</success_criteria>

<output>
After completion, create `.planning/phases/53-cycle-detail-header-pinning/53-00-SUMMARY.md`
</output>
