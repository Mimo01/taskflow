---
phase: 81-charting-foundation
plan: 03
type: execute
wave: 3
depends_on: ["81-02"]
files_modified:
  - taskflow/src/routes/dashboard/SmokeTestChart.tsx
  - taskflow/src/routes/dashboard/index.tsx
  - taskflow/src/routes/routes.tsx
autonomous: false
requirements: [CHART-02, CHART-01, CHART-03]
user_setup: []

must_haves:
  truths:
    - "A smoke-test chart renders on /dashboard using ChartWrapper and the Recharts responsive prop"
    - "The smoke chart series colors come from var(--chart-N) CSS-var strings, not hardcoded hex"
    - "The Dashboard route is React.lazy() so recharts lands in the Dashboard chunk, not vendor/main"
    - "The smoke chart renders at its expected dimensions in a real macOS Tauri WebKit build with no 0x0 collapse"
  artifacts:
    - path: "taskflow/src/routes/dashboard/SmokeTestChart.tsx"
      provides: "Throwaway smoke-test chart (SCAFFOLD-marked) exercising ChartWrapper + chart.tsx + recharts responsive prop"
      contains: "SCAFFOLD"
    - path: "taskflow/src/routes/routes.tsx"
      provides: "Dashboard route converted to React.lazy() + withLazy()"
      contains: "withLazy(Dashboard)"
  key_links:
    - from: "taskflow/src/routes/dashboard/index.tsx"
      to: "SmokeTestChart"
      via: "import + render in the Dashboard page"
      pattern: "SmokeTestChart"
    - from: "taskflow/src/routes/routes.tsx"
      to: "withLazy(Dashboard)"
      via: "lazy route element"
      pattern: "withLazy\\(Dashboard\\)"
---

<objective>
Prove the charting stack end-to-end: mount a throwaway smoke-test chart on /dashboard through ChartWrapper + the shadcn chart primitive + the Recharts `responsive` prop, convert the Dashboard route to `React.lazy()` so recharts is code-split out of vendor/main, confirm the split via bundle analysis, and verify the chart actually renders (no 0x0 collapse, correct theme colors) in a real macOS Tauri WebKit build.

Purpose: CHART-02 (renders correctly in Tauri WebKit — explicit height + responsive prop + animations off) and CHART-01/CHART-03 bundle protection (lazy-loaded chunk). This is the only plan that produces user-visible output and the only one requiring a human checkpoint (the WebKit render + theme colors can only be verified in a real Tauri build per 81-VALIDATION.md Manual-Only Verifications).
Output: src/routes/dashboard/SmokeTestChart.tsx (SCAFFOLD-marked throwaway), SmokeTestChart mounted in src/routes/dashboard/index.tsx, Dashboard route converted to React.lazy() in routes.tsx, bundle-analysis confirmation, real-Tauri-build human UAT.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-charting-foundation/81-CONTEXT.md
@.planning/phases/81-charting-foundation/81-UI-SPEC.md
@.planning/phases/81-charting-foundation/81-PATTERNS.md
@.planning/phases/81-charting-foundation/81-VALIDATION.md
@taskflow/src/routes/routes.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the SCAFFOLD smoke-test chart and mount it on the Dashboard (CHART-02, CHART-01)</name>
  <files>taskflow/src/routes/dashboard/SmokeTestChart.tsx, taskflow/src/routes/dashboard/index.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/index.tsx (the Dashboard page — where SmokeTestChart mounts; choose a visible spot in the existing layout)
    - taskflow/src/routes/dashboard/DashboardInProgressCard.tsx (dashboard sub-component analog; 81-PATTERNS.md SmokeTestChart.tsx section)
    - taskflow/src/components/chart-wrapper.tsx (the wrapper SmokeTestChart renders through — from Plan 02)
    - taskflow/src/components/ui/chart.tsx (the shadcn primitive — confirm actual exports ChartContainer/ChartTooltip/ChartTooltipContent; from Plan 01)
    - .planning/phases/81-charting-foundation/81-UI-SPEC.md Smoke-test chart section (height 240, var(--chart-1/2) only, isAnimationActive={false}, SCAFFOLD comment, copy)
    - .planning/phases/81-charting-foundation/81-PATTERNS.md SmokeTestChart.tsx section (imports, ChartConfig, responsive prop usage)
  </read_first>
  <action>
    Create src/routes/dashboard/SmokeTestChart.tsx as a named export `export function SmokeTestChart()`. The FIRST line must be the comment `// SCAFFOLD: smoke-test only — remove when Phase 83 rebuilds Dashboard` (D-06 — throwaway scaffold removed in Phase 83). Import Bar, BarChart, XAxis, YAxis from recharts (bar is the documented default; donut is allowed per D-06 Claude's Discretion); import ChartContainer, ChartTooltip, ChartTooltipContent and the ChartConfig type from @/components/ui/chart; import ChartWrapper from @/components/chart-wrapper. Use a hardcoded static data array of 3 to 5 entries (no API fetch). Define a ChartConfig using `var(--chart-1)` (optionally also `var(--chart-2)`) CSS-var strings only — NEVER hardcoded hex, NEVER getComputedStyle (D-08). Render the chart with the Recharts `responsive` prop and an explicit `height={240}` — NEVER ResponsiveContainer (D-02), NEVER height="100%" (D-03). Every Recharts series component carries `isAnimationActive={false}` (D-04). Pass `fill="var(--chart-1)"` to the Bar. Wrap the chart in `<ChartWrapper title="Chart smoke test" description="Verifies charting stack — temporary scaffold" height={240}>` (copy per UI-SPEC). Then import and render `<SmokeTestChart />` in a visible location in src/routes/dashboard/index.tsx (D-06 temporary mount; do not delete or restructure existing Dashboard content beyond adding this one component).
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit && head -1 src/routes/dashboard/SmokeTestChart.tsx | grep -c "SCAFFOLD"</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/dashboard/SmokeTestChart.tsx line 1 contains SCAFFOLD (`head -1 ... | grep -c SCAFFOLD` is 1)
    - Uses the Recharts responsive prop and never ResponsiveContainer (`grep -c ResponsiveContainer SmokeTestChart.tsx` is 0; `grep -c responsive SmokeTestChart.tsx` >= 1)
    - Never uses height="100%" and never hardcodes hex in chart props (`grep -c 'height="100%"'` is 0; `grep -Ec '#[0-9a-fA-F]{3,6}' SmokeTestChart.tsx` is 0)
    - Passes var(--chart- CSS-var strings (`grep -c "var(--chart-" SmokeTestChart.tsx` >= 1) and sets isAnimationActive={false} (`grep -c "isAnimationActive={false}"` >= 1)
    - SmokeTestChart imported and rendered in src/routes/dashboard/index.tsx (`grep -c SmokeTestChart src/routes/dashboard/index.tsx` >= 2)
    - cd taskflow && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>SmokeTestChart is a SCAFFOLD-marked throwaway rendering through ChartWrapper with the responsive prop, explicit height, var(--chart-N) colors, and animations off; mounted on /dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the Dashboard route to React.lazy() and confirm recharts is code-split (CHART-01, CHART-03)</name>
  <files>taskflow/src/routes/routes.tsx</files>
  <read_first>
    - taskflow/src/routes/routes.tsx (current state: static `import Dashboard from './dashboard/index'` at line 5; route at line 36; existing withLazy() helper at lines 24-32; existing lazy() pattern at lines 11-22; `lazy` already imported at line 1)
    - .planning/phases/81-charting-foundation/81-PATTERNS.md routes.tsx section (exact before/after diff)
    - .planning/phases/81-charting-foundation/81-RESEARCH.md Pattern 4, Pitfall 4 (recharts must not be in vendor/main), and Bundle analysis command
  </read_first>
  <action>
    In src/routes/routes.tsx: remove the static `import Dashboard from './dashboard/index';` (line 5) and declare `const Dashboard = lazy(() => import('./dashboard/index'));` alongside the other lazy() declarations (lines 11-22) (D-07). Change the route element from `<Dashboard />` to `withLazy(Dashboard)` (line 36) — withLazy() already wraps with ChunkErrorBoundary + Suspense + RouteSpinner; reuse it, no changes to that helper. The `lazy` identifier is already imported at line 1. Then run `ANALYZE=true npm run build` from taskflow/ (rollup-plugin-visualizer is wired at vite.config.ts:38-40) and confirm recharts appears inside a Dashboard-named chunk, NOT in vendor or the main/index chunk (D-07 / criterion 3 / assumption A3 — if the visualizer misidentifies chunks, fall back to inspecting dist/ chunk filenames as documented in RESEARCH.md). Record which chunk recharts landed in (chunk filename or visualizer node) in the SUMMARY.
  </action>
  <verify>
    <automated>cd taskflow && grep -c "withLazy(Dashboard)" src/routes/routes.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/routes.tsx no longer has the static default import (`grep -c "^import Dashboard from './dashboard/index'" src/routes/routes.tsx` is 0)
    - src/routes/routes.tsx declares `const Dashboard = lazy(() => import('./dashboard/index'))` (`grep -c "lazy(() => import('./dashboard/index'))" src/routes/routes.tsx` is 1)
    - The Dashboard route uses withLazy (`grep -c "withLazy(Dashboard)" src/routes/routes.tsx` is 1)
    - cd taskflow && npx tsc --noEmit exits 0
    - cd taskflow && ANALYZE=true npm run build completes exit 0 AND the SUMMARY records that recharts is in a Dashboard/route chunk, NOT in vendor or main/index (criterion 3 / D-07)
  </acceptance_criteria>
  <done>Dashboard route is React.lazy() + withLazy(); the production build succeeds and bundle analysis confirms recharts is code-split into the Dashboard chunk, not vendor/main.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — smoke chart renders in real macOS Tauri WebKit build (CHART-02)</name>
  <action>Human-verify checkpoint — no automated implementation. The executor automates everything first (Tasks 1 and 2: scaffold, lazy route, tsc, Vitest, bundle analysis all green), then pauses for the user to confirm the smoke chart renders correctly in a real macOS Tauri WebKit build (the only verification automation cannot perform). See what-built / how-to-verify below.</action>
  <what-built>
    The SmokeTestChart is mounted on /dashboard, rendering through ChartWrapper with the Recharts responsive prop, explicit 240px height, var(--chart-1/2) theme colors, and isAnimationActive={false}. The Dashboard route is now lazy-loaded. All automated checks (tsc, Vitest, bundle analysis) have passed. The only thing automation cannot verify is the real-WebKit render: jsdom and the dev Chrome server do not reproduce the WebKit 0x0-collapse / ResizeObserver timing failure (81-VALIDATION.md Manual-Only Verifications; D-03).
  </what-built>
  <how-to-verify>
    1. From taskflow/, run a real Tauri build/run: `npm run tauri build` (or a Tauri dev run if faster on this machine) — NOT `npm run dev` in a browser, which does not reproduce WebKit timing.
    2. Open the app and navigate to /dashboard. First load shows the RouteSpinner Suspense fallback briefly (expected — Dashboard is now lazy), then the page renders.
    3. Confirm the "Chart smoke test" card is visible and the bar chart inside it is drawn at roughly 240px tall and fills the card width — NOT collapsed to 0x0, NOT an invisible/blank SVG.
    4. Toggle the app theme between light and dark (Settings). Confirm the chart bars and axis labels remain visible in BOTH themes and the series colors are the blue-spectrum --chart-N tokens (not invisible, not wrong color).
  </how-to-verify>
  <resume-signal>Type "approved" if the smoke chart renders at expected size with correct colors in both themes, or describe what you see (e.g. blank SVG, 0x0, wrong colors in dark mode) so the issue can be triaged.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | The smoke chart renders a hardcoded static data array. No user input, no network request, no auth, no secrets cross any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-03-D | Denial of Service | Lazy Dashboard chunk load failure | accept | withLazy() already wraps the route in ChunkErrorBoundary (existing, production-proven on SprintBoardTab/BacklogPage); a failed chunk fetch surfaces the existing error boundary, not a blank app. No new mitigation needed. |

**Low-risk assessment:** Throwaway scaffold chart + a route-lazy conversion that reuses the established withLazy pattern. No input validation, auth, network, or crypto surface (consistent with 81-RESEARCH.md Security Domain). No high-severity threats.
</threat_model>

<verification>
- cd taskflow && npx tsc --noEmit exits 0
- cd taskflow && npm run test exits 0 (full suite still green after route + scaffold changes)
- cd taskflow && ANALYZE=true npm run build exits 0; recharts confirmed in the Dashboard chunk, not vendor/main
- Human UAT: smoke chart renders at expected dimensions with correct light/dark colors in a real macOS Tauri build
</verification>

<success_criteria>
- Smoke chart renders on /dashboard via ChartWrapper + responsive prop + explicit height + animations off (CHART-02, D-02/03/04)
- Series colors from var(--chart-N), never hex (CHART-01, D-08)
- Dashboard route is React.lazy(); recharts is code-split out of vendor/main (CHART-01/CHART-03, D-07)
- Real-Tauri-build WebKit render verified by human (CHART-02 — no 0x0 collapse, correct theme colors)
</success_criteria>

<output>
Create `.planning/phases/81-charting-foundation/81-03-SUMMARY.md` when done. Record the bundle-analysis result (which chunk recharts landed in) and the human-UAT outcome.
</output>
