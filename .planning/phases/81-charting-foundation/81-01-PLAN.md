---
phase: 81-charting-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/package.json
  - taskflow/package-lock.json
  - taskflow/src/components/ui/chart.tsx
  - taskflow/src/test/setup.ts
autonomous: true
requirements: [CHART-01]
user_setup: []

must_haves:
  truths:
    - "recharts and react-is appear in package.json dependencies"
    - "src/components/ui/chart.tsx exists and exports ChartContainer + ChartConfig"
    - "Vitest can mount a component using the Recharts responsive prop without ReferenceError: ResizeObserver is not defined"
  artifacts:
    - path: "taskflow/src/components/ui/chart.tsx"
      provides: "shadcn chart primitive (ChartContainer, ChartTooltip, ChartTooltipContent, ChartLegend, ChartLegendContent, ChartConfig type)"
      contains: "ChartContainer"
    - path: "taskflow/src/test/setup.ts"
      provides: "global ResizeObserver no-op mock for jsdom chart-render tests"
      contains: "ResizeObserver"
    - path: "taskflow/package.json"
      provides: "recharts + react-is runtime dependencies"
      contains: "recharts"
  key_links:
    - from: "taskflow/src/test/setup.ts"
      to: "global.ResizeObserver"
      via: "global assignment of a no-op class"
      pattern: "global\\.ResizeObserver"
---

<objective>
Lay the Wave 0 foundation for all charting work: install the charting dependencies, generate the shadcn chart primitive, and add the jsdom ResizeObserver mock that every chart-render test depends on. This plan creates no rendered UI — it makes the stack installable and the test environment chart-capable.

Purpose: All downstream chart tests (ChartWrapper render tests in Plan 02, smoke chart in Plan 03) fail with `ReferenceError: ResizeObserver is not defined` until the mock exists, and cannot import `@/components/ui/chart` until the shadcn primitive is generated. This is strictly first (Wave 0 per 81-VALIDATION.md).
Output: recharts@^3.8.1 + react-is@^19.1.0 in package.json; src/components/ui/chart.tsx (shadcn copy-paste); ResizeObserver mock appended to src/test/setup.ts.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-charting-foundation/81-CONTEXT.md
@.planning/phases/81-charting-foundation/81-RESEARCH.md
@.planning/phases/81-charting-foundation/81-PATTERNS.md
@.planning/phases/81-charting-foundation/81-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts + react-is and generate the shadcn chart primitive (CHART-01)</name>
  <files>taskflow/package.json, taskflow/package-lock.json, taskflow/src/components/ui/chart.tsx</files>
  <read_first>
    - taskflow/package.json (the dependencies block — confirm recharts/react-is absent; alphabetical placement per 81-PATTERNS.md §package.json)
    - taskflow/src/components/ui/skeleton.tsx (the simplest shadcn copy-paste analog — named export, uses cn() from @/lib/utils; 81-PATTERNS.md §chart.tsx)
    - taskflow/components.json (shadcn config — confirm aliases so the CLI writes to src/components/ui/chart.tsx)
    - .planning/phases/81-charting-foundation/81-RESEARCH.md §Installation Commands and §Package Legitimacy Audit (D-01)
  </read_first>
  <action>
    From the taskflow/ directory run `npm install recharts react-is` (D-01). Recharts is the locked library (Recharts v3.8, react-is ^19.1.0 is the required React 19 peer dep — both Approved in the Package Legitimacy Audit, no [ASSUMED]/[SUS]/[SLOP] packages, so no install checkpoint is required). Confirm recharts resolves to ^3.8.x and react-is to ^19.x in package.json dependencies (NOT devDependencies — react-is is a runtime peer dep). Then run `npx shadcn@latest add chart` from taskflow/ to generate src/components/ui/chart.tsx as a project-owned copy-paste file — do NOT hand-write it. The generated file must export ChartContainer, ChartTooltip, ChartTooltipContent, ChartLegend, ChartLegendContent, and the ChartConfig type (assumption A1 in RESEARCH.md — if the shadcn export shape differs, record the actual exports in the SUMMARY). Do NOT import or reference ResponsiveContainer anywhere (D-02). Do NOT add any vite.config.ts babel exclusion (D-02 — none is needed as long as ResponsiveContainer is never imported).
  </action>
  <verify>
    <automated>cd taskflow && node -e "const p=require('./package.json'); if(!p.dependencies.recharts) throw new Error('recharts missing from deps'); if(!p.dependencies['react-is']) throw new Error('react-is missing from deps'); console.log('deps ok', p.dependencies.recharts, p.dependencies['react-is'])"</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/package.json` dependencies contains a `recharts` key resolving to a `^3.8` range
    - `taskflow/package.json` dependencies contains a `react-is` key resolving to a `^19` range (in dependencies, not devDependencies)
    - `taskflow/src/components/ui/chart.tsx` exists
    - `grep -c "export" taskflow/src/components/ui/chart.tsx` is greater than 0 and the file exports an identifier named `ChartContainer`
    - `grep -c "ResponsiveContainer" taskflow/src/components/ui/chart.tsx` filtering out comment lines (`grep -v '^[[:space:]]*//' | grep -c ResponsiveContainer`) is 0
    - `cd taskflow && npx tsc --noEmit` exits 0 (chart.tsx type-checks against installed recharts)
  </acceptance_criteria>
  <done>recharts + react-is are installed runtime deps; src/components/ui/chart.tsx exists, exports ChartContainer, type-checks, and never references ResponsiveContainer.</done>
</task>

<task type="auto">
  <name>Task 2: Add no-op ResizeObserver mock to test setup (Wave 0 — unblocks all chart-render tests)</name>
  <files>taskflow/src/test/setup.ts</files>
  <read_first>
    - taskflow/src/test/setup.ts (existing global mocks — the window.crypto Object.defineProperty pattern at lines 5-8 and the vi.mock Tauri-store pattern; 81-PATTERNS.md §setup.ts)
    - .planning/phases/81-charting-foundation/81-RESEARCH.md §Pitfall 5 and §ResizeObserver Mock (why this is required: jsdom lacks ResizeObserver; Recharts responsive prop attaches one)
    - .planning/phases/81-charting-foundation/81-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <action>
    Append a no-op global ResizeObserver mock to src/test/setup.ts after the existing Tauri-store vi.mock block (after line 46). Define `global.ResizeObserver` as a class with empty `observe()`, `unobserve()`, and `disconnect()` methods (RESEARCH.md Option A — global mock in setup.ts is preferred over per-test vi.stubGlobal because every future chart test inherits it at zero cost for non-chart tests). Use the `global.ResizeObserver =` assignment style to match the file's existing idiom (per 81-PATTERNS.md §setup.ts — global is idiomatic for jsdom in Vitest). Add a one-line comment explaining it is required for the Recharts responsive prop in jsdom. Do not modify the existing crypto or Tauri-store mocks.
  </action>
  <verify>
    <automated>cd taskflow && grep -v '^[[:space:]]*//' src/test/setup.ts | grep -c "global.ResizeObserver"</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/test/setup.ts` contains `global.ResizeObserver` (non-comment line)
    - The mock class declares `observe`, `unobserve`, and `disconnect` methods (`grep -c "observe\|unobserve\|disconnect" src/test/setup.ts` >= 3)
    - The existing `window.crypto` mock and `@tauri-apps/plugin-store` vi.mock block are still present (`grep -c "@tauri-apps/plugin-store" src/test/setup.ts` is 1)
    - `cd taskflow && npm run test` exits 0 (the existing suite still passes with the new global mock — confirms the mock does not break non-chart tests)
  </acceptance_criteria>
  <done>src/test/setup.ts defines a global no-op ResizeObserver with observe/unobserve/disconnect; existing mocks untouched; the full existing Vitest suite still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Two new packages (recharts, react-is) are pulled from the public npm registry during install |

No user input, no network requests at runtime, no auth surface, no secrets are introduced by this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-SC | Tampering | npm install of recharts + react-is | accept | Both packages Approved in 81-RESEARCH.md §Package Legitimacy Audit — recharts (9+ yrs, ~3M/wk, shadcn official dep), react-is (Meta/React official under facebook/react). No [ASSUMED]/[SUS]/[SLOP] packages, so no blocking legitimacy checkpoint is required. No postinstall network scripts expected. |

**Low-risk assessment:** This is a dependency-adding + test-infra foundation plan with negligible attack surface. The only external operation is installing two long-established, trusted packages. No high-severity threats apply.
</threat_model>

<verification>
- `cd taskflow && node -e "..."` confirms recharts + react-is in dependencies
- `cd taskflow && npm run test` passes (existing suite green with new ResizeObserver mock)
- `cd taskflow && npx tsc --noEmit` exits 0 (chart.tsx type-checks)
</verification>

<success_criteria>
- recharts@^3.8 and react-is@^19 are runtime dependencies (CHART-01 prerequisite)
- src/components/ui/chart.tsx exists, exports ChartContainer + ChartConfig, never references ResponsiveContainer (D-01/D-02)
- src/test/setup.ts has a global no-op ResizeObserver mock; existing suite still green (Wave 0 unblocked)
</success_criteria>

<output>
Create `.planning/phases/81-charting-foundation/81-01-SUMMARY.md` when done. Record the actual exports of the generated chart.tsx (verifies assumption A1).
</output>
