---
phase: 81-charting-foundation
plan: 02
type: execute
wave: 2
depends_on: ["81-01"]
files_modified:
  - taskflow/src/components/chart-wrapper.tsx
  - taskflow/src/components/chart-wrapper.test.tsx
autonomous: true
requirements: [CHART-03, CHART-01]

must_haves:
  truths:
    - "ChartWrapper renders a Skeleton when isLoading is true"
    - "ChartWrapper renders ErrorState when error is non-null"
    - "ChartWrapper renders EmptyState when isEmpty is true"
    - "ChartWrapper renders its children inside an explicit-height div in the success state"
    - "ChartWrapper mounts and renders without error in the Vitest jsdom environment"
    - "D-05: ChartWrapper is the status-prop card (title/description + isLoading/error/isEmpty) composing existing Skeleton/ErrorState/EmptyState inside consistent card chrome; chart children render only on success"
  artifacts:
    - path: "taskflow/src/components/chart-wrapper.tsx"
      provides: "Status-prop card wrapper composing Skeleton/ErrorState/EmptyState with explicit-height chart area and 'use no memo' directive"
      exports: ["ChartWrapper"]
      min_lines: 40
    - path: "taskflow/src/components/chart-wrapper.test.tsx"
      provides: "Render tests for loading/error/empty/success states"
      contains: "ChartWrapper"
  key_links:
    - from: "taskflow/src/components/chart-wrapper.tsx"
      to: "@/components/ui/{skeleton,error-state,empty-state}"
      via: "imports of the three state primitives"
      pattern: "@/components/ui/(skeleton|error-state|empty-state)"
---

<objective>
Build the reusable ChartWrapper — the status-prop card that every downstream chart (Phases 83–85) composes — and its Vitest render tests. ChartWrapper owns the card chrome, the explicit-height chart area (WebKit 0×0 guard), the `'use no memo'` React Compiler escape hatch, and the loading/error/empty/success state branching.

Purpose: CHART-03 (reusable wrapper with consistent sizing/theming/states) and the automatable subset of CHART-01 (importable, renders without error in Vitest). This is the load-bearing deliverable of the phase — Plan 03's smoke chart and all future charts render their children through this wrapper.
Output: src/components/chart-wrapper.tsx (named export, `'use no memo'` at line 1) + src/components/chart-wrapper.test.tsx covering all four states.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-charting-foundation/81-CONTEXT.md
@.planning/phases/81-charting-foundation/81-UI-SPEC.md
@.planning/phases/81-charting-foundation/81-PATTERNS.md
@.planning/phases/81-charting-foundation/81-VALIDATION.md
@taskflow/src/components/ui/error-state.tsx
@taskflow/src/components/ui/empty-state.tsx
@taskflow/src/components/ui/skeleton.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ChartWrapper status-prop card (CHART-03, CHART-01)</name>
  <files>taskflow/src/components/chart-wrapper.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/DashboardInProgressCard.tsx (closest analog — card chrome + loading-state branching; 81-PATTERNS.md §chart-wrapper.tsx, card chrome at line 108)
    - taskflow/src/components/ui/error-state.tsx (exact prop signature: error: unknown, onRetry: () => void, viewName: string; calls useNavigate internally)
    - taskflow/src/components/ui/empty-state.tsx (exact prop signature: icon: LucideIcon, title: string, subtitle?: string, action?: ReactNode)
    - taskflow/src/components/ui/skeleton.tsx (named export, data-slot="skeleton" + animate-pulse)
    - .planning/phases/81-charting-foundation/81-UI-SPEC.md §ChartWrapper (full visual contract: card chrome, state table, copy)
    - .planning/phases/81-charting-foundation/81-PATTERNS.md §chart-wrapper.tsx (props interface, render branching, 'use no memo' position)
  </read_first>
  <behavior>
    - isLoading={true} → renders Skeleton (a div with data-slot="skeleton"); does NOT render children
    - error non-null → renders ErrorState (passes error, onRetry, viewName={title}); does NOT render children
    - isEmpty={true} → renders EmptyState (icon BarChart2, title "No data yet", subtitle "Data will appear here once available."); does NOT render children
    - none of the above → renders children inside the explicit-height div
    - title always renders as the card heading text; description renders only when provided
  </behavior>
  <action>
    Create src/components/chart-wrapper.tsx as a named export `export function ChartWrapper(...)` (NOT default — matches the ui-component convention per 81-PATTERNS.md). Place the `'use no memo'` directive as the FIRST line of the file, before all imports (D-04 React Compiler escape hatch — this is the only file in the codebase that uses it). Props per the UI-SPEC/PATTERNS interface: title: string (required), description?: string, height?: number (default 240), isLoading?: boolean, error?: unknown, isEmpty?: boolean, onRetry?: () => void, children: ReactNode. Card chrome: outer div `bg-card rounded-[var(--radius)] border border-border p-6` (p-6 = lg/24px spacing token per UI-SPEC). Title: `text-base font-semibold text-foreground`. Description (when present): `text-sm text-muted-foreground mt-1 mb-4`. Chart area: a div with `style={{ height }}` (explicit PIXEL height — D-03 WebKit 0×0 guard; NEVER height="100%") and className `w-full`. Branch the chart area content: isLoading → `<Skeleton className="w-full h-full rounded-md" />`; error → `<ErrorState error={error} onRetry={onRetry ?? (() => {})} viewName={title} />`; isEmpty → `<EmptyState icon={BarChart2} title="No data yet" subtitle="Data will appear here once available." />` (BarChart2 from lucide-react); otherwise render `children`. Use the `@/` import alias for all imports. Do NOT import recharts or chart.tsx here — ChartWrapper is chart-library-agnostic and only composes the three ui/ state primitives; chart children are passed in by consumers.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit && head -1 src/components/chart-wrapper.tsx | grep -c "use no memo"</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/components/chart-wrapper.tsx` line 1 is the `'use no memo'` directive (`head -1 ... | grep -c "use no memo"` is 1)
    - File contains `export function ChartWrapper` (named export, `grep -c "export function ChartWrapper"` is 1)
    - File imports all three state primitives: `grep -c "@/components/ui/skeleton\|@/components/ui/error-state\|@/components/ui/empty-state"` is 3
    - Chart area uses an explicit numeric height via `style={{ height` and never `height="100%"` (`grep -c 'height="100%"'` is 0)
    - `grep -c "ResponsiveContainer\|recharts" src/components/chart-wrapper.tsx` is 0 (wrapper is library-agnostic)
    - `cd taskflow && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ChartWrapper exists as a named export with 'use no memo' at line 1, composes Skeleton/ErrorState/EmptyState, renders children in an explicit-height div, and type-checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChartWrapper render tests — all four states (CHART-03)</name>
  <files>taskflow/src/components/chart-wrapper.test.tsx</files>
  <read_first>
    - taskflow/src/components/ui/error-state.test.tsx (exact test analog — render/screen from @testing-library/react, vi.mock for react-router-dom useNavigate, describe/it, console.error spy; 81-PATTERNS.md §chart-wrapper.test.tsx)
    - taskflow/src/components/chart-wrapper.tsx (the component under test — created in Task 1)
    - .planning/phases/81-charting-foundation/81-RESEARCH.md §Validation Architecture (test map: isLoading/error/isEmpty/success) and Open Question 2 (ErrorState needs MemoryRouter)
    - .planning/phases/81-charting-foundation/81-VALIDATION.md §Per-Task Verification Map
  </read_first>
  <behavior>
    - Test 1 (loading): render with isLoading → document.querySelector('[data-slot="skeleton"]') is truthy; provided child text is NOT in the document
    - Test 2 (success): render with no loading/error/empty → child text IS in the document; no skeleton element present
    - Test 3 (error): render wrapped in MemoryRouter with error={new Error('oops')} → ErrorState copy ("Couldn't load {title}" / Retry) is present; child text is NOT rendered
    - Test 4 (empty): render with isEmpty → "No data yet" text is present; child text is NOT rendered
  </behavior>
  <action>
    Create src/components/chart-wrapper.test.tsx modeled on error-state.test.tsx. Import render and screen from @testing-library/react and describe/it/expect/beforeEach/vi from vitest. The error-state branch test MUST wrap the render in `<MemoryRouter>` (from react-router-dom — already a project dependency) because ErrorState calls useNavigate() internally (RESEARCH.md Open Question 2); the loading/empty/success branches do not need router context. Add a beforeEach that clears mocks and spies on console.error with a no-op (matches error-state.test.tsx, suppresses ErrorState's logging side-effect). Write the four tests described in the behavior block, each rendering ChartWrapper with a distinguishable child like `<div>my chart</div>` so absence/presence of the child distinguishes states. Use `.toBeTruthy()` / `screen.getByText(...)` / `screen.queryByText(...)` assertions (the project's existing style). The ResizeObserver mock from Plan 01 is already global in setup.ts — do not re-mock it here.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/components/chart-wrapper.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npx vitest run src/components/chart-wrapper.test.tsx` exits 0
    - The test file contains at least 4 `it(`/`test(` cases (`grep -c "it(\|test(" src/components/chart-wrapper.test.tsx` >= 4)
    - The error-branch test wraps render in `MemoryRouter` (`grep -c "MemoryRouter" src/components/chart-wrapper.test.tsx` >= 1)
    - Tests assert the loading state via `data-slot="skeleton"` and the empty state via the text "No data yet" (`grep -c 'data-slot="skeleton"\|No data yet' src/components/chart-wrapper.test.tsx` >= 2)
    - `cd taskflow && npm run test` exits 0 (full suite still green)
  </acceptance_criteria>
  <done>chart-wrapper.test.tsx covers loading/success/error/empty; all four pass under Vitest; the error branch is wrapped in MemoryRouter; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | ChartWrapper renders only props passed by trusted in-app callers. No user input, no network, no auth, no secrets, no untrusted data crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-02-I | Information Disclosure | ChartWrapper error prop rendering | accept | ErrorState (existing, already in production) renders error UI; ChartWrapper passes the error through unchanged. No new disclosure surface beyond the already-shipped ErrorState component. |

**Low-risk assessment:** Pure frontend presentational component composing three already-shipped UI primitives. No network, input validation, auth, or crypto applies (consistent with 81-RESEARCH.md §Security Domain). No high-severity threats.
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/components/chart-wrapper.test.tsx` exits 0 (all four state tests pass)
- `cd taskflow && npm run test` exits 0 (full suite)
- `cd taskflow && npx tsc --noEmit` exits 0
- `head -1 src/components/chart-wrapper.tsx` is the 'use no memo' directive
</verification>

<success_criteria>
- ChartWrapper is a named export with 'use no memo' at line 1, composing Skeleton/ErrorState/EmptyState (CHART-03, D-04, D-05)
- Chart area uses an explicit pixel height, never height="100%" (D-03)
- All four state branches verified by passing Vitest tests; component mounts cleanly in jsdom (CHART-01 automatable subset)
</success_criteria>

<output>
Create `.planning/phases/81-charting-foundation/81-02-SUMMARY.md` when done.
</output>
