---
phase: 02-developer-dashboard
task: UAT
status: in_progress
last_updated: 2026-03-11T20:33:38.232Z
---

<current_state>
Running Phase 2 UAT via /gsd:verify-work 2. Fixed multiple infrastructure issues during UAT.
Paused mid-test — Test 1 (Dashboard Tab Navigation) has not yet received a pass/fail from user.
UAT file: .planning/phases/02-developer-dashboard/02-UAT.md
</current_state>

<completed_work>

- Fixed Tailwind v4 setup: upgraded tailwindcss to v4, restored @tailwindcss/vite plugin, fixed postcss.config.js
- Added missing `import './index.css'` to main.tsx
- Switched jira.ts + gitlab.ts to @tauri-apps/plugin-http fetch (CORS fix)
- Updated jira.test.ts + gitlab.test.ts to mock @tauri-apps/plugin-http
- Added http capability scope in capabilities/default.json (allow-fetch, allow-fetch-send, etc.)
- Restored auth.store.ts with persist middleware (jiraBaseUrl/jiraConnected now survive restarts)
- Rebuilt dashboard/index.tsx three-tab shell (My Tasks | Sprint Board | MR Attention)
- Extended DashTab type to include 'mr-attention'
- Fixed JiraStep.test.tsx mock (added jiraProjects, reset in beforeEach)
- Staged and committed all Phase 2 untracked UI components
- All committed: 2b2da50 "fix(uat): resolve Phase 2 UAT issues"
- 106/106 tests passing

</completed_work>

<remaining_work>

UAT tests still pending (02-UAT.md):
- Test 1: Dashboard Tab Navigation — not yet confirmed (user was on this when we paused)
- Test 2: My Tasks: Loading State
- Test 3: My Tasks: Task List
- Test 4: My Tasks: Last-Refreshed Timestamp
- Test 5: Sprint Board: Kanban Columns
- Test 6: MR Attention: Stale Badge
- Test 7: Settings: Stale MR Threshold Selector
- Test 8: MR Health Chips on Task Rows
- Test 9: MR Attention: Linked Task Badge
- Test 10: Status Transition via Popover
- Test 11: Inline Comment on Task

</remaining_work>

<decisions_made>

- Used @tauri-apps/plugin-http fetch (not global fetch) for jira.ts and gitlab.ts — plain fetch causes CORS 401 preflight failures on Jira Server
- Used wildcard scope `https://**` + `http://**` for http capability — Jira/GitLab hostnames are user-configured at runtime, can't be hardcoded
- auth.store.ts persisted to auth.json via Tauri Store — URLs and connection state must survive restarts
- dashboard/index.tsx rebuilt from scratch using Tabs/TabsList/TabsTrigger/TabsContent from @base-ui/react
- postcss.config.js reverted to empty plugins (Tailwind v4 uses vite plugin, not postcss)

</decisions_made>

<blockers>

None currently — all issues resolved, app running with tauri dev.

</blockers>

<context>
The user ran /gsd:verify-work 2 to UAT Phase 2 (developer dashboard). We hit several infra issues:
1. Missing CSS import → app had no styles
2. Tailwind v4 downgraded to v3 → CSS broke
3. jira.ts/gitlab.ts used plain fetch → CORS errors in Tauri webview
4. http capability lacked scope/fetch-send → Tauri blocked requests
5. auth.store not persisted → settings disappeared on restart
6. dashboard/index.tsx was placeholder in HEAD (Phase 2 commits never pushed)

All fixed and committed. App is now functional enough to test Phase 2 features.
User's app is connected to https://jira.orange.sk (real instance).
</context>

<next_action>
Resume UAT with /gsd:verify-work 2 — start at Test 1 (Dashboard Tab Navigation).
The UAT file is at .planning/phases/02-developer-dashboard/02-UAT.md.
Present test 1 and wait for user response.
</next_action>
