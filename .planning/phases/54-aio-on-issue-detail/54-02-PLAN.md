---
phase: 54-aio-on-issue-detail
plan: "02"
type: tdd
wave: 2
depends_on: ["54-01"]
files_modified:
  - taskflow/src/services/aio/issue-steps.test.ts
  - taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx
autonomous: true
requirements: [AIOI-01, AIOI-02, AIOI-03]

must_haves:
  truths:
    - "Service unit tests exist for fetchAioTestCasesForIssue (200, 200 empty, 401, 404)"
    - "Service unit tests exist for fetchAioTestRunSteps (200 with steps, 404 returns empty)"
    - "Component test stubs exist for AioTestRunsSection covering aioEnabled gate, skeleton, empty states, step table, lightbox"
    - "All tests in this plan are RED — they fail because the component does not yet exist"
  artifacts:
    - path: "taskflow/src/services/aio/issue-steps.test.ts"
      provides: "Unit tests for both service functions"
      contains: "fetchAioTestCasesForIssue"
    - path: "taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx"
      provides: "Component integration test stubs"
      contains: "AioTestRunsSection"
  key_links:
    - from: "taskflow/src/services/aio/issue-steps.test.ts"
      to: "taskflow/src/services/aio/issue-steps.ts"
      via: "import { fetchAioTestCasesForIssue, fetchAioTestRunSteps }"
      pattern: "fetchAioTestCasesForIssue"
    - from: "taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx"
      to: "taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx"
      via: "import { AioTestRunsSection }"
      pattern: "AioTestRunsSection"
---

<objective>
Create test files for both the service layer and the section component. The service tests will go GREEN after Plan 54-01 ships. The component test stubs will be RED (import will fail because `AioTestRunsSection.tsx` does not exist yet) — they turn GREEN in Plan 54-04.

Purpose: Writing tests before implementation forces the API contract to be declared explicitly. Service tests can be run and go GREEN immediately (the service is already implemented in Plan 54-01). Component stubs serve as the acceptance gate for Plan 54-04.

Output: Two test files — `issue-steps.test.ts` and `AioTestRunsSection.test.tsx`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-aio-on-issue-detail/54-CONTEXT.md
@.planning/phases/54-aio-on-issue-detail/54-RESEARCH.md
@.planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md

<interfaces>
<!-- Key contracts the executor needs. -->

From taskflow/src/services/aio/issue-steps.ts (implemented in Plan 54-01):
```typescript
export async function fetchAioTestCasesForIssue(
  baseUrl: string, token: string, projectKey: string, issueKey: string
): Promise<AioTestCase[]>

export async function fetchAioTestRunSteps(
  baseUrl: string, token: string, projectKey: string, cycleKey: string, runId: string
): Promise<AioTestRunStep[]>
```

From taskflow/src/services/aio/types.ts (confirmed from Plan 54-01):
```typescript
export interface AioTestCase { id: number; key: string; title: string; projectKey?: string; }
export interface AioTestRunStep {
  id: number;
  // probe-confirmed field names — see 54-PROBE-FINDINGS.md
  stepAction: string; expectedResult?: string; actualResult?: string;
  status?: string; attachments?: AioStepAttachment[];
}
export interface AioStepAttachment { url?: string; fileName?: string; }
```

Vitest pattern from existing test files (check taskflow/src/*.test.ts for vi.fn() mock pattern):
```typescript
import { vi, describe, it, expect, beforeEach } from 'vitest';
// mock aioFetch:
vi.mock('../services/aio/client', () => ({ aioFetch: vi.fn() }));
import { aioFetch } from '../services/aio/client';
const mockFetch = vi.mocked(aioFetch);
```

@testing-library/react pattern (check existing *.test.tsx for render/screen pattern):
```typescript
import { render, screen } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
// wrap component: <QueryClientProvider client={new QueryClient()}><Component /></QueryClientProvider>
```
</interfaces>
</context>

<feature>
  <name>AIO issue-steps service + AioTestRunsSection component</name>
  <files>
    taskflow/src/services/aio/issue-steps.test.ts,
    taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx
  </files>
  <behavior>
    Service tests — fetchAioTestCasesForIssue:
    - 200 with AioPage wrapper containing one test case → returns AioTestCase[] of length 1 with correct key and title
    - 200 with direct array (no pagination wrapper) → returns AioTestCase[]
    - 200 with empty items array → returns []
    - 401 → throws ApiError with status 401
    - 404 → returns []

    Service tests — fetchAioTestRunSteps:
    - 200 with steps embedded at probe-confirmed path (e.g. response.steps[]) → returns AioTestRunStep[] of correct length
    - 404 → returns []
    - step with no attachments → AioTestRunStep.attachments is undefined or []

    Component tests — AioTestRunsSection (stubs — RED until Plan 54-03 creates the component):
    - When aioEnabled=false → renders nothing (null)
    - When aioEnabled=true and query is loading → renders skeleton (AioTestRunsSkeleton)
    - When query returns [] (no linked test cases) → renders nothing (section hidden, D-04)
    - When query returns data with one run having 0 steps → renders empty state "No test runs in active cycle"
    - When query returns one run with steps → renders table with headers Step, Expected, Actual, Status
    - When a step has status NOT_EXECUTED → Actual cell contains "—"
    - When a step has status PASS → Status cell contains "Pass" chip
    - When there are multiple test cases → renders collapsible blocks (each has the test case name)
    - When a step has attachments → renders thumbnail images
    - When a thumbnail is clicked → ImageLightbox opens with correct src
  </behavior>
  <implementation>
    Service tests: mock `aioFetch` with `vi.mock`. Use a helper `makePageResponse` to build AioPage<T> fixtures.

    Component tests: mock `useSettingsStore`, `useAuthStore`, `readSecret`, and the aio service functions (`fetchAioTestCasesForIssue`, `fetchAioTestCasesForIssue`, `fetchAioCycles`, `fetchAioTestRunsForCycle`, `fetchAioTestRunSteps`) using vi.mock. Wrap component in QueryClientProvider with `retry: false`. Use `waitFor` for async query resolution.

    For the component test file, import `AioTestRunsSection` from `./AioTestRunsSection`. This import will fail until Plan 54-03 creates the file — that is the intended RED state.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 0: Write service unit tests (issue-steps.test.ts) — RED then GREEN</name>
  <files>taskflow/src/services/aio/issue-steps.test.ts</files>
  <read_first>
    taskflow/src/services/aio/issue-steps.ts (implemented in Plan 54-01 — read before writing tests)
    taskflow/src/services/aio/issue-runs.ts (how the existing test analog is structured — check if issue-runs.test.ts exists)
    .planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md (use confirmed field names in test fixtures)
  </read_first>
  <behavior>
    RED phase: `cd taskflow && npm test -- issue-steps` — runs but tests may fail if service has a bug
    GREEN phase: all 7 tests pass

    Tests to write:
    1. fetchAioTestCasesForIssue — 200 AioPage wrapper → AioTestCase[] with correct key/title
    2. fetchAioTestCasesForIssue — 200 direct array → AioTestCase[]
    3. fetchAioTestCasesForIssue — 200 empty items → []
    4. fetchAioTestCasesForIssue — 401 → throws ApiError (status === 401)
    5. fetchAioTestCasesForIssue — 404 → []
    6. fetchAioTestRunSteps — 200 with steps at confirmed path → AioTestRunStep[] correct length
    7. fetchAioTestRunSteps — 404 → []
  </behavior>
  <action>
Create `taskflow/src/services/aio/issue-steps.test.ts`.

Mock strategy: `vi.mock('../../lib/api-error', ...)` is not needed — import `ApiError` directly. Mock `aioFetch` via `vi.mock('./client', () => ({ aioFetch: vi.fn() }))`. In each test, configure `mockFetch.mockResolvedValueOnce` with a `Response`-like object that has `ok`, `status`, and `json()` properties.

Fixture shape for test case: use the probe-confirmed field names from 54-PROBE-FINDINGS.md. If the probe confirmed `title` as the display field, use `{ id: 1, key: 'PROJ-TC-1', title: 'Login flow test', projectKey: 'PROJ' }`.

Fixture shape for step: use probe-confirmed field names. If `stepAction` is confirmed, use `{ id: 1, stepAction: 'Click login', expectedResult: 'Login page appears', status: 'PASS' }`.

For the steps endpoint response: wrap steps at the confirmed path (e.g., `{ steps: [stepFixture] }`) matching how the actual endpoint returns data per the probe.

After writing all tests, run:
```
cd taskflow && npm test -- --reporter=dot issue-steps
```
Service tests should all pass GREEN (service is already implemented in Plan 54-01). If any fail, fix the service implementation in `issue-steps.ts` rather than weakening the tests.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --reporter=dot issue-steps 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/services/aio/issue-steps.test.ts` exists with at least 7 `it()` blocks
    - `cd taskflow && npm test -- issue-steps` exits 0 (all 7 tests GREEN)
    - Test for 401 asserts `error.status === 401` (not just that an error was thrown)
    - Test for paginated response asserts the returned array has the correct length (not just truthy)
    - Test fixtures use probe-confirmed field names from 54-PROBE-FINDINGS.md (not ASSUMED names)
  </acceptance_criteria>
  <done>All 7 service unit tests pass. fetchAioTestCasesForIssue and fetchAioTestRunSteps are verified against key behaviors including 401 and 404 handling.</done>
</task>

<task type="tdd">
  <name>Task 1: Write component test stubs (AioTestRunsSection.test.tsx) — intentionally RED</name>
  <files>taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx</files>
  <read_first>
    .planning/phases/54-aio-on-issue-detail/54-CONTEXT.md (D-04 two empty states, D-07 step table columns, D-09 NOT_EXECUTED dash, D-10 collapsible, D-15 parallel loading)
    .planning/phases/54-aio-on-issue-detail/54-UI-SPEC.md (Interaction Contract section — aioEnabled gate, empty state copy, thumbnail a11y)
    taskflow/src/routes/dashboard/AioCycleDetailPage.tsx (test query pattern to mock)
  </read_first>
  <behavior>
    Expected RED state: `cd taskflow && npm test -- AioTestRunsSection` fails with "Cannot find module './AioTestRunsSection'" — this is correct since Plan 54-03 creates the component.

    Tests to write (all in a single describe block):
    1. renders null when aioEnabled=false
    2. renders AioTestRunsSkeleton while loading (useDelayedLoading returns true)
    3. renders null when query returns [] (no linked test cases — section hidden per D-04)
    4. renders EmptyState "No test runs in active cycle" when test cases linked but matched runs have zero steps
    5. renders table with headers "Step", "Expected", "Actual", "Status"
    6. shows "—" in Actual cell for NOT_EXECUTED step status (per D-09)
    7. renders collapsible blocks when multiple test cases (per D-10)
    8. renders thumbnail for step with attachment
    9. clicking thumbnail opens lightbox with correct src (per D-13)
  </behavior>
  <action>
Create `taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx`.

Mock the entire aio service module: `vi.mock('@/services/aio', () => ({ fetchAioTestCasesForIssue: vi.fn(), fetchAioCycles: vi.fn(), fetchAioTestRunsForCycle: vi.fn(), fetchAioTestRunSteps: vi.fn() }))`.

Mock stores: `vi.mock('@/stores/settings.store', () => ({ useSettingsStore: vi.fn() }))` and `vi.mock('@/stores/auth.store', () => ({ useAuthStore: vi.fn() }))`.

Mock `readSecret`: `vi.mock('@/services/stronghold', () => ({ readSecret: vi.fn().mockResolvedValue('mock-token') }))`.

Mock `useDelayedLoading`: `vi.mock('@/hooks/useDelayedLoading', () => ({ useDelayedLoading: vi.fn().mockReturnValue(false) }))`.

Wrap each test render: `<QueryClientProvider client={new QueryClient({ defaultOptions: { queries: { retry: false } } })}><AioTestRunsSection issueKey="PROJ-123" jiraBaseUrl="https://jira.example.com" /></QueryClientProvider>`.

For the empty state test (test 4): return one test case from fetchAioTestCasesForIssue, one active cycle from fetchAioCycles, one matching run from fetchAioTestRunsForCycle, and an empty array from fetchAioTestRunSteps.

For the step table test (test 5): same setup but fetchAioTestRunSteps returns one step with probe-confirmed field names and status "PASS".

For thumbnail test (test 8-9): return a step with `attachments: [{ url: 'https://jira.example.com/attach/1.png', fileName: 'screenshot.png' }]`.

The import of AioTestRunsSection will fail (file doesn't exist) — commit the test file anyway. Plan 54-03 creates the component.

After writing, confirm the test file has valid syntax (run the test to see the import error, not a syntax error):
```
cd taskflow && npm test -- --reporter=dot AioTestRunsSection 2>&1 | head -20
```
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --reporter=dot AioTestRunsSection 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx` exists
    - File contains at least 9 `it()` blocks
    - Running the test exits non-zero with "Cannot find module" or "AioTestRunsSection" error — NOT a syntax error (syntax is valid TypeScript)
    - Test for NOT_EXECUTED step asserts `screen.getByText('—')` or equivalent
    - Test for aioEnabled=false asserts the component returns null (container has no AIO section content)
    - Test for collapsible blocks asserts multiple test case name strings appear in the DOM
    - Test for thumbnail click asserts lightbox opens (checks for the `aria-modal="true"` dialog or the `alt` of the lightbox image)
  </acceptance_criteria>
  <done>Component test file exists with 9 stubs, all syntactically valid. The file fails at import (RED) because the component doesn't exist yet — this is the intended state going into Plan 54-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → component under test | No real network calls in tests — all aio service functions are mocked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-02-01 | Tampering | Test fixtures | accept | Test fixtures use controlled data; no injection vectors in test environment |
</threat_model>

<verification>
- `cd taskflow && npm test -- issue-steps` exits 0 (service tests GREEN)
- `cd taskflow && npm test -- AioTestRunsSection 2>&1 | grep -i "cannot find module"` prints non-empty (expected RED)
</verification>

<success_criteria>
Service tests are GREEN. Component test file is syntactically valid but RED (import failure). The RED state is the intended outcome — it will turn GREEN in Plan 54-04.
</success_criteria>

<output>
After completion, create `.planning/phases/54-aio-on-issue-detail/54-02-SUMMARY.md`
</output>
