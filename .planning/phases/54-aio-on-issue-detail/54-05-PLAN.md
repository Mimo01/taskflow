---
phase: 54-aio-on-issue-detail
plan: "05"
type: execute
wave: 4
depends_on: ["54-04"]
files_modified: []
autonomous: false
requirements: [AIOI-01, AIOI-02, AIOI-03]

must_haves:
  truths:
    - "AIO Test Runs section appears on issue detail page when aioEnabled=true"
    - "Section is absent when aioEnabled=false"
    - "Step table shows correct Step/Expected/Actual/Status columns"
    - "Status chips are colored correctly per status"
    - "NOT_EXECUTED steps show dash in Actual column"
    - "Step attachment thumbnails are visible and open the in-app lightbox on click"
    - "Multiple test cases show as collapsible blocks; PASS collapsed, FAIL/BLOCKED expanded"
    - "Section loads in parallel with the main issue data (no blocking)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/IssueDetailPage.tsx"
      provides: "Renders AioTestRunsSection below ActivityTimeline"
      contains: "AioTestRunsSection"
    - path: "taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx"
      provides: "Full section with step table, thumbnails, lightbox, and collapsible blocks"
      contains: "export function AioTestRunsSection"
  key_links:
    - from: "AioTestRunsSection in browser"
      to: "AIO REST API /testcase + /testrun + /testrun/{id}"
      via: "useQuery firing in parallel with Jira issue query"
      pattern: "issue-steps"
---

<objective>
Human verification of the complete Phase 54 feature in the running application. This is a visual and interactive check — automated tests verified correctness, but only the developer can confirm the AIO section renders correctly, thumbnails open the lightbox, and the section placement in the issue detail layout looks right.

Purpose: Catch visual regressions, layout issues, or probe-assumption mismatches that automated tests cannot detect (e.g., real step data showing wrong fields because a probe assumption was off).

Output: Human confirmation that all five success criteria from the ROADMAP are met, or issue report that feeds back into gap closure.
</objective>

<execution_context>
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/54-aio-on-issue-detail/54-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Verify AIO section renders correctly on a real issue</name>
  <read_first>
    .planning/ROADMAP.md (Phase 54 success criteria)
    .planning/phases/54-aio-on-issue-detail/54-CONTEXT.md (D-04, D-07, D-09, D-10, D-12, D-16)
  </read_first>
  <what-built>
    - AioTestRunsSection component added to IssueDetailPage below ActivityTimeline
    - Step table with 4 columns: Step, Expected, Actual, Status
    - Status chips colored per status using aioRunStatusBadgeClass
    - NOT_EXECUTED steps show dash in Actual cell
    - Multiple test cases show as collapsible run blocks
    - Step attachment thumbnails clickable to open ImageLightbox
    - Section hidden when aioEnabled=false; hidden (not an error) when no AIO test cases linked
  </what-built>
  <how-to-verify>
    1. **Start the app** and navigate to Settings → Integrations. Verify `aioEnabled` is ON.

    2. **Open an issue detail page** for a Jira issue that is known to have AIO test cases linked (from your probe in Plan 54-00, you know which issue that is). Scroll below the activity timeline.
       - Expected: "AIO Test Runs" section heading with FlaskConical icon appears
       - Expected: A step table with headers Step | Expected | Actual | Status is visible
       - Expected: Status chips show correct colors (green=Pass, red=Fail, orange=Blocked, gray=Not Run)
       - Expected: NOT_EXECUTED steps show "—" in the Actual column, not blank/undefined

    3. **If the issue has multiple linked test cases:** verify each shows as a collapsible block with the test case name in the header, plus a run status chip. Verify PASS runs are collapsed by default and FAIL/BLOCKED runs are expanded.

    4. **If any step has attachment images:** verify thumbnails appear in the Actual column row, at ~48px height. Click a thumbnail and verify the ImageLightbox opens showing the full image. Press Escape to close.

    5. **Open an issue that has NO AIO test cases linked.** Verify the AIO Test Runs section does NOT appear (section hidden — no empty state message, no error state).

    6. **Go to Settings → Integrations and toggle aioEnabled OFF.** Navigate back to the issue from step 2. Verify the AIO Test Runs section is completely absent.

    7. **Toggle aioEnabled back ON** and reload the issue. Verify the section reappears without requiring a hard refresh.

    8. **Verify parallel loading:** Open a fresh issue detail page (not cached). Observe that the main Jira issue content (title, description, assignee) loads and renders before the AIO section. The AIO section may briefly show its skeleton (if load takes >200ms) then the step data. The issue content must NOT be blocked by the AIO query.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 54, or describe any issues found (include which step failed and what you saw vs. expected).</resume-signal>
  <action>
No automated steps — this is a pure human visual verification. After verifying all 8 steps above, record the result. If issues are found, note which decision (D-XX) or ROADMAP success criterion failed so gap closure can be targeted.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - "AIO Test Runs" section heading with FlaskConical icon appears below ActivityTimeline on a linked issue
    - Step table columns are labeled Step, Expected, Actual, Status
    - Status chips are colored (at minimum: Pass = green, Fail = red)
    - NOT_EXECUTED steps show "—" in Actual column
    - Section is absent on issues with no linked AIO test cases (D-04, first case)
    - Section is absent when aioEnabled=false (AIOI-01)
    - Step attachment thumbnails (if any exist) open the in-app ImageLightbox (AIOI-03)
    - Full test suite still passes: `npm test` exits 0
  </acceptance_criteria>
  <done>Developer has confirmed all 8 verification steps pass in the running application and typed "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer browser → running Tauri app | Human verification only; no new attack surface introduced in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-05-01 | Information Disclosure | AIO step data visible in running app | accept | Data is from AIO TCMS on the same Jira host; same auth boundary as existing Jira issue data |
</threat_model>

<verification>
Phase 54 complete when developer approves this checkpoint and `npm test` exits 0.
</verification>

<success_criteria>
All four ROADMAP success criteria confirmed in the running application:
1. AIO test runs section appears on issue detail page only when aioEnabled=true and loads without blocking Jira data
2. Step table renders with step / expected / actual columns and colored failure markers per row
3. Section hidden (not error state) when issue has no associated AIO test runs
4. Attachment images open in the existing in-app ImageLightbox via authenticated fetch
</success_criteria>

<output>
After completion, create `.planning/phases/54-aio-on-issue-detail/54-05-SUMMARY.md`
</output>
