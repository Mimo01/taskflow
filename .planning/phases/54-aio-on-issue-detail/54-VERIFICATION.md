---
phase: 54-aio-on-issue-detail
verified: 2026-05-14T00:25:00Z
status: human_needed
score: 4/4 ROADMAP success criteria verified in code; 3 plan 54-07 gap-closure behaviours need running-app UAT
overrides_applied: 0
human_verification:
  - test: "Gap 1 — Impacted executions list renders on no-runs path with real per-run status chips"
    expected: "When an issue has linked test cases but no runs in the latest active (primary) cycle, the bare 'No test runs in active cycle' EmptyState is GONE and is replaced by a compact list showing one row per impacted execution (test case key + title, cycle key, run ID, colored status chip). Chip colors differ between PASS/FAIL/BLOCKED rows — not all gray 'Not Run'. Rows are read-only."
    why_human: "Requires a real ESHOP issue whose linked test cases have runs only in non-primary cycles; chip color verification is visual, and the live data shape is the only source of truth for whether `detail.run.status` flows correctly to the chip."
  - test: "Gap 2 — AioAttachmentsGrid populates on no-runs path"
    expected: "On a no-runs ESHOP issue whose impacted-execution step content contains inline `[file.png|url]` refs, the 'AIO attachments' collapsible header is visible AND ≥1 thumbnail appears in the grid. When no inline image refs exist, the header still renders with the empty-state 'No inline image attachments found in linked test runs.' inside. Clicking a thumbnail opens the in-app ImageLightbox via the AuthImage bridge-URL translation path."
    why_human: "Cross-cycle bounded fan-out (MAX_IMPACTED_EXECUTIONS=20, MAX_PARALLEL=6, T-54-07-02) interacts with live AIO instance data. Image rendering also depends on AuthImage's bridge-URL translation, which only works against live AIO data."
  - test: "Gap 3 — Nested wiki (`{panel}` with embedded `[name|url]` list) renders inside table cells"
    expected: "Open an ESHOP issue whose failed test runs contain step content matching the verbatim Finding 1 fixture (`{panel}` block inside a `|cell|` table row). The step table renders without breaking — the panel content and the `VAS.png` text anchor render INSIDE the table cell. Clicking the link opens the in-app ImageLightbox (not the OS browser)."
    why_human: "Branch 3-A is a preprocess heuristic — automated test passes against verbatim ESHOP fixture, but real ESHOP runs may have variants the heuristic does not cover. Visual verification is the final acceptance gate per plan 54-07 Task 3."
  - test: "ROADMAP SC verification on a fresh run"
    expected: "All four ROADMAP SCs visually confirmed on a real ESHOP issue: (1) section appears only when aioEnabled=true and loads lazily without blocking issue body; (2) step table renders Step/Expected/Actual columns with colored failure chips; (3) section is hidden (no error) when no AIO test cases linked; (4) attachment images open in the existing in-app ImageLightbox."
    why_human: "ROADMAP SC verification is contractually a human UAT step; automated tests verify intermediate behavior but the integrated user experience needs a developer to confirm in `npm run tauri dev`."
re_verification:
  previous_status: never_verified
  previous_score: n/a
  note: "Initial verification — phase 54 has not been previously gsd-verified. Verifier was deferred until after 54-06 UAT and 54-07 gap closure (per .continue-here.md handoff)."
---

# Phase 54: AIO on Issue Detail — Verification Report

**Phase Goal:** Issue detail pages surface AIO test run data inline — step table with failure markers and attachment images openable in the existing lightbox
**Verified:** 2026-05-14T00:25:00Z
**Status:** human_needed
**Re-verification:** No — initial verification after Plans 54-00 through 54-07 executed (note: 54-02, 54-03, 54-05 have no individual SUMMARY.md but their work shipped via the codebase artifacts cited below).

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #   | Truth                                                                                                                                                | Status     | Evidence                                                                                                                                                                                                                                                                                                          |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | An AIO test runs section appears on the issue detail page only when `aioEnabled` is true, and loads lazily without blocking the main issue data       | ✓ VERIFIED | `AioTestRunsSection.tsx:572` — `if (!aioEnabled) return null` early-return gate; `AioTestRunsSection.tsx:542` — `enabled: !!jiraBaseUrl && !!issueKey && !!aioEnabled` ensures no AIO API calls fire when disabled. Component is rendered as a sibling to `ActivityTimeline` inside `IssueDetailPage.tsx:393`; uses `useQuery` (parallel) + `useDelayedLoading` (200ms skeleton flicker prevention at line 546) — does not block Jira issue data render. Component test confirms null-render when `aioEnabled=false` (`AioTestRunsSection.test.tsx:142`). |
| 2   | The AIO section renders a step table with step / expected / actual columns and colored failure markers per row                                       | ✓ VERIFIED | `AioTestRunsSection.tsx:240-304` — `StepTable` component renders a 4-column table (`Step | Expected | Actual | Status`); `Status` chip uses `aioRunStatusBadgeClass(step.status)` (line 294) — colored by status (PASS green / FAIL red / BLOCKED orange / NOT_EXECUTED gray per `statusStyles.ts:50`); each cell renders via `WikiRenderer` for Jira-wiki support (lines 261, 264, 271); test `AioTestRunsSection.test.tsx:219` asserts headers "Step", "Expected", "Actual", "Status" appear. |
| 3   | When the issue has no associated AIO test runs, the section is hidden (not an error state)                                                            | ✓ VERIFIED | `AioTestRunsSection.tsx:595` — `if (data === null) return null` (D-04 first case: no linked test cases → section hidden); `queryFn` returns `null` (not error, not empty state) when `linkedTestCases.length === 0` on both Branch A1 (line 388) and the legacy path (line 510). Defensive null also at line 606 covers the no-in-cycle-runs / no-impacted-executions defensive branch. Plan 54-07 widened the no-runs-but-impacted-executions case to render `ImpactedExecutionsList` (line 629) instead of an EmptyState — this is more informative than blank but is explicitly net-new product behavior, not an error state. |
| 4   | Attachment images within test run steps are fetched via the authenticated HTTP client and open in the existing in-app lightbox                       | ✓ VERIFIED | `AioTestRunsSection.tsx:232` — `<AuthImage src={url} alt={fileName} className="h-full w-auto object-contain" />` inside `StepThumbnail` (line 215) wired through `ImageLightbox` (line 234); also `AioAttachmentsGrid` at lines 193, 203 uses `<AuthImage>` + `<ImageLightbox>` for the inline image attachment grid. `AuthImage` (`AuthImage.tsx:6-115`) translates AIO bridge URLs to the direct download endpoint and fetches with Bearer auth when src starts with jiraBaseUrl (lines 24-39, 60). `WikiRenderer.tsx:333-342` routes inline `[file.ext|url]` image links to in-app lightbox via `setLightboxSrc(href)` — not OS browser. |

**Score:** 4/4 ROADMAP success criteria verified

### Required Artifacts

| Artifact                                                                                  | Expected                                              | Status     | Details                                                                                                                              |
| ----------------------------------------------------------------------------------------- | ----------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `taskflow/src/services/aio/types.ts`                                                       | AIO type definitions (AioTestCase, AioTestRunStep, AioTraceabilityRunRef, AioTestCaseWithRuns) | ✓ VERIFIED | 9 exported `AioXxx` interfaces (`grep -c "export interface Aio"` = 9). Probe-confirmed field names (`step`, `expectedResult`, `actualResult`, `testRunStepStatus.name`). |
| `taskflow/src/services/aio/issue-steps.ts`                                                 | Service functions: fetchAioTestCasesForIssue, fetchAioTestRunSteps, fetchAioTestRunDetail | ✓ VERIFIED | 3 exported async functions at lines 104, 163, 244. `fetchAioTestRunDetail` (Plan 54-06 addition) returns `{ run, steps } | null` in a single request. |
| `taskflow/src/services/aio/projects.ts`                                                    | fetchAioProjects + widened fetchAioTraceabilityTestCases | ✓ VERIFIED | `fetchAioTraceabilityTestCases` at line 55 returns `AioTestCaseWithRuns[]` with embedded run refs (Plan 54-06 Branch A1 widening). |
| `taskflow/src/services/aio/index.ts`                                                       | Barrel exports                                       | ✓ VERIFIED | Re-exports `types`, `projects`, `issue-runs`, `cycles`, `issue-steps`.                                                                |
| `taskflow/src/routes/dashboard/issue-detail/AioTestRunsSkeleton.tsx`                       | Loading skeleton                                     | ✓ VERIFIED | 13-line component exporting `AioTestRunsSkeleton`; renders 6 Skeleton elements (heading + table header + 4 step rows).               |
| `taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx`                        | Main AIO section component                          | ✓ VERIFIED | 696 lines; exports `AioTestRunsSection`. Sub-components: `StepTable`, `StepThumbnail`, `CollapsibleRunBlock`, `AioAttachmentsGrid`, `ImpactedExecutionsList`. aioEnabled gate + parallel useQuery + skeleton/error/null/data state waterfall + Branch A1 direct-lookup + cross-cycle bounded fan-out (T-54-07-02). |
| `taskflow/src/routes/dashboard/IssueDetailPage.tsx`                                        | Integration point                                    | ✓ VERIFIED | Import at line 36; render at line 393 (`<AioTestRunsSection issueKey={issueKey} jiraBaseUrl={jiraBaseUrl!} jiraIssueId={issue.id} />`). NOTE — placement deviation: section is ABOVE `ActivityTimeline` instead of below as originally specified in D-16. Comment at line 392 says "above comments per user preference". This deviation does not violate any ROADMAP SC (placement is not in the goal text) and was an explicit user preference. |
| `taskflow/src/routes/dashboard/WikiRenderer.tsx`                                           | Wiki rendering with openUrl for external links + nested-table-cell support + rehype-sanitize XSS guard | ✓ VERIFIED | 374 lines; `<a>` override at line 310 routes external links through `openUrl` (line 346) and image-extension links to in-app lightbox (line 339); `mergeOpenTableRows` (line 116) + `flattenInlineCalloutsForTableRow` (line 86) + `<span data-callout>` override (line 284) for nested wiki inside `\|cell\|` rows; `rehypeSanitize` wired at line 365 with custom schema preserving `mention`/`span`/`br`/`data-callout`/`data-title` while stripping `<script>`/`<iframe>`/on-* attrs (T-54-07-01). |

### Key Link Verification

| From                                              | To                                                       | Via                                                                                  | Status | Details                                                                                                              |
| ------------------------------------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------ | ------ | -------------------------------------------------------------------------------------------------------------------- |
| IssueDetailPage.tsx                               | AioTestRunsSection                                       | `import { AioTestRunsSection } from './issue-detail/AioTestRunsSection'` + JSX render at line 393 | WIRED  | Both import and render present; props `issueKey`, `jiraBaseUrl!`, `jiraIssueId={issue.id}` passed correctly.          |
| AioTestRunsSection                                | AIO service layer                                       | `fetchAioProjects`, `fetchAioTraceabilityTestCases`, `fetchAioTestRunDetail`, `fetchAioCycles`, `fetchAioTestRunsForCycle`, `fetchAioTestCasesForIssue`, `fetchAioTestRunSteps` | WIRED  | All 7 service functions imported at lines 14-22 from `@/services/aio` barrel; called in queryFn at lines 367, 373, 430, 453, 504, 512, 516, 530.  |
| AioTestRunsSection.StepTable                      | WikiRenderer                                             | `<WikiRenderer wikiText={step.step\|expectedResult\|actualResult} />` in Step/Expected/Actual cells | WIRED  | 3 cells render via WikiRenderer (lines 261, 264, 271); wiki markup tables, `{color}`, `{panel}`, headings, hard breaks all flow through. |
| AioTestRunsSection.StepThumbnail                  | ImageLightbox (per-thumbnail)                            | `<ImageLightbox src={url} alt={fileName} open={open} onClose={...} />` (D-14)        | WIRED  | Independent useState per thumbnail (line 216); lightbox call at line 234. AuthImage handles authenticated fetch (line 232). |
| AioTestRunsSection.AioAttachmentsGrid             | ImageLightbox + AuthImage                                | Grid aggregates inline image refs from in-cycle + cross-cycle step content via `collectAioImageAttachments`; lightbox opens on thumbnail click | WIRED  | Lines 150-212 wire AuthImage thumbnails (line 193) into ImageLightbox (line 203); attachments memo at line 552 aggregates from BOTH `data.runs` and `data.impactedExecutions`. |
| AioTestRunsSection.queryFn (Branch A1)            | fetchAioTraceabilityTestCases (widened) + fetchAioTestRunDetail | Direct-lookup path bypasses cycle scan (lines 366-499)                                | WIRED  | Branch A1 fetches traceability for defect+requirement in parallel (lines 373-382), extracts embedded run refs, partitions by primary cycle, fetches detail per run (in-cycle uncapped; cross-cycle capped at MAX_IMPACTED_EXECUTIONS=20 / MAX_PARALLEL=6 — T-54-07-02). |
| WikiRenderer `<a>` override                       | @tauri-apps/plugin-opener openUrl                        | `e.preventDefault(); openUrl(href).catch(() => {})` for non-image external links     | WIRED  | Line 344-346; image-extension links instead open in-app via `setLightboxSrc` (line 336).                              |
| WikiRenderer markdown pipeline                    | rehype-sanitize                                          | `rehypePlugins={[rehypeRaw, [rehypeSanitize, wikiSanitizeSchema]]}` (T-54-07-01)     | WIRED  | Line 365; schema at lines 26-41 preserves wiki surface (data-callout, mention, span, br, img, a, href) while stripping `<script>`/`<iframe>`/on-*. |
| AuthImage (used by StepThumbnail + AioAttachmentsGrid + WikiRenderer img override) | AIO bridge → direct-download endpoint | `resolveAttachmentUrl(src, jiraBaseUrl, activeJiraProject)` at line 22-42         | WIRED  | Bridge URL translation: `/plugins/servlet/aio-tcms/bridge/tcms/browse?...` → `/rest/aio-tcms-api/1.0/project/{projectKey}/attachment/{attachmentId}` (Probe D in 54-06 UAT follow-up). Authenticated fetch only when resolved URL starts with `jiraBaseUrl` (line 60). |
| useSettingsStore.aioEnabled                       | AioTestRunsSection (gate + query.enabled)                | `useSettingsStore((s) => s.aioEnabled)` at line 342; gate at line 572; query enabled at line 542 | WIRED  | Belt-and-braces: both the render gate AND the useQuery enabled flag respect aioEnabled. No AIO API calls fire when disabled (ROADMAP Phase 51 SC-2 compliance).        |

### Data-Flow Trace (Level 4)

| Artifact                                                                          | Data Variable                       | Source                                                                                                                                          | Produces Real Data | Status      |
| --------------------------------------------------------------------------------- | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | ----------- |
| `AioTestRunsSection` (in-cycle path, Branch A1)                                   | `data.runs` (AioIssueRunData[])     | `fetchAioTraceabilityTestCases` (defect + requirement) → embedded run refs → `fetchAioTestRunDetail` per ref (uncapped Promise.all)              | Yes — REST API     | ✓ FLOWING   |
| `AioTestRunsSection` (impacted executions path, Plan 54-07)                       | `data.impactedExecutions[]`         | Cross-cycle run refs sliced to MAX_IMPACTED_EXECUTIONS=20, chunked Promise.all of MAX_PARALLEL=6 calling `fetchAioTestRunDetail`                | Yes — REST API     | ✓ FLOWING   |
| `AioTestRunsSection` (legacy path, no jiraIssueId)                                | `data.runs`                         | `fetchAioTestCasesForIssue` → `fetchAioCycles` + `pickLatestActiveCycle` → `fetchAioTestRunsForCycle` + client-side filter → `fetchAioTestRunSteps` per matched run | Yes — REST API     | ✓ FLOWING   |
| `AioAttachmentsGrid` thumbnails                                                   | `aioAttachments` (memoized)         | `collectAioImageAttachments([...data.runs, ...impactedAsRunData])` — regex-extract inline `[name.ext|url]` from `step.step \| expectedResult \| actualResult`, dedupe by URL | Yes — derived from step content | ✓ FLOWING   |
| `ImpactedExecutionsList` chip                                                     | `row.status`                        | Set in queryFn at line 494: `r.detail?.run.status ?? 'NOT_EXECUTED'` (already normalised by `toRunChipStatus` in `issue-steps.ts`)               | Yes — REST API     | ✓ FLOWING   |
| `StepTable` Step/Expected/Actual cell                                              | `step.step / step.expectedResult / step.actualResult` | `fetchAioTestRunDetail.run.testRunSteps[]` (probe-confirmed field names)                                                                       | Yes — REST API     | ✓ FLOWING   |
| `AioAttachmentsGrid` empty-state header (Gap 2)                                   | `attachments.length === 0`          | Always rendered when AIO data present, even when no inline image refs found; per-Plan 54-06 commit `8a92dbb` and Plan 54-07 architecture        | Yes — branch always reached when data present | ✓ FLOWING   |

### Behavioral Spot-Checks

| Behavior                                                       | Command                                                                                              | Result                              | Status |
| -------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ----------------------------------- | ------ |
| Phase 54 TypeScript compiles cleanly                            | `cd taskflow && ./node_modules/.bin/tsc --noEmit`                                                    | Exit code 0; no errors              | ✓ PASS |
| Full test suite passes (regression-free)                        | `cd taskflow && ./node_modules/.bin/vitest run --reporter=dot`                                       | 1002 passed, 2 skipped, 39 todo across 102 test files (+5 test files skipped); 0 failures | ✓ PASS |
| AIO service barrel exports all required functions               | `grep -n "fetchAioTraceabilityTestCases\|fetchAioTestRunDetail" services/aio/index.ts (via re-export)` | Both functions present (projects.ts + issue-steps.ts both exported)              | ✓ PASS |
| AioTestRunsSection integrated into IssueDetailPage              | `grep -n "AioTestRunsSection" IssueDetailPage.tsx`                                                   | 2 matches: import (L36) + render (L393)                                       | ✓ PASS |
| Sticky bottom-0 composer present exactly once (no duplication)  | `grep -c "sticky bottom-0" IssueDetailPage.tsx`                                                      | 1                                                                            | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan                            | Description                                                                                | Status        | Evidence                                                                                                                                                       |
| ----------- | -------------------------------------- | ------------------------------------------------------------------------------------------ | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AIOI-01     | 54-00 through 54-07                    | Issue detail page shows a lazy-loaded AIO test runs section (only when aioEnabled is true) | ✓ SATISFIED   | aioEnabled gate at `AioTestRunsSection.tsx:572`; query.enabled at line 542; lazy-loaded via `useQuery` (parallel with Jira data); integrated at `IssueDetailPage.tsx:393`. |
| AIOI-02     | 54-00 through 54-07                    | AIO test run section renders a step table (step / expected / actual columns, colored failure markers) | ✓ SATISFIED   | `StepTable` at `AioTestRunsSection.tsx:240-304` — 4-column table; status chip via `aioRunStatusBadgeClass`. WikiRenderer enriches step text with Jira wiki (54-06). |
| AIOI-03     | 54-00 through 54-07                    | AIO attachment images are fetched via authenticated HTTP client and open in the existing in-app lightbox | ✓ SATISFIED   | `StepThumbnail` (line 215) + `AioAttachmentsGrid` (line 150) + `WikiRenderer <a>` image-extension override (line 333) all use `AuthImage` + `ImageLightbox`. AuthImage fetches with Bearer + translates AIO bridge URLs to direct-download endpoint. |

**ORPHANED requirements check:** None. REQUIREMENTS.md maps AIOI-01/02/03 to Phase 54 (lines 76-78); all three are declared in every plan's `requirements:` field across 54-00 through 54-07 and all three have implementation evidence.

### Probe Execution

| Probe                                                              | Command                                                                                 | Result                                                                                                | Status         |
| ------------------------------------------------------------------ | --------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | -------------- |
| (No formal probe scripts — phase used human-action probes A/B/C/D/E with output recorded in `54-PROBE-FINDINGS.md`) | n/a                                                                                     | All probes were live-curl human checkpoints whose output is preserved in `54-PROBE-FINDINGS.md` and consumed by each subsequent plan. Probes A (testcase field names), B (step field names), C (direct run lookup → Branch A1), D (AIO bridge URL → direct-download), E (nested wiki → Branch 3-A) all yielded explicit `### Decision` lines that drove implementation. | n/a — none applicable |

### Anti-Patterns Found

| File                                                                                 | Line | Pattern                                                                                        | Severity | Impact                                                                                                                                       |
| ------------------------------------------------------------------------------------ | ---- | ---------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| (none in modified files)                                                              | —    | No `TBD`/`FIXME`/`XXX` debt markers in Phase 54 modified files (`AioTestRunsSection.tsx`, `WikiRenderer.tsx`, `issue-steps.ts`, `projects.ts`, `types.ts`, `AuthImage.tsx`, `WikiRenderer.test.tsx`, `AioTestRunsSection.test.tsx`)  | —        | Clean. Phase 54 introduced no unreferenced debt markers.                                                                                     |

(Code review at `54-REVIEW.md` flagged 1 critical (`CR-01: queryKey omits jiraIssueId — stale data risk`), 7 warnings, 5 info. These are advisory and do not block ROADMAP SC verification. Most notably, CR-01 is a real correctness concern for cross-issue navigation; it is NOT a Phase 54 ROADMAP SC violation but should be addressed before phase close or routed to a follow-up. WR-01 — `{info}/{warning}/{note}` not balanced in `mergeOpenTableRows` — is similarly an edge case that does not block ROADMAP SCs.)

### Human Verification Required

See `human_verification` section in frontmatter for structured details. Summary:

1. **Gap 1 — Impacted executions list on real ESHOP issue** (Plan 54-07 Task 3 sub-item 1). What to do: open an ESHOP issue with linked test cases but no runs in primary cycle; confirm `ImpactedExecutionsList` renders with per-row PASS/FAIL/BLOCKED colored chips (not all gray).
2. **Gap 2 — AioAttachmentsGrid populated on no-runs path** (Plan 54-07 Task 3 sub-item 2). What to do: open an ESHOP issue whose impacted executions contain inline `[file.png|url]` refs; confirm grid header + thumbnails visible + click opens in-app lightbox.
3. **Gap 3 — Nested wiki inside table cells on real ESHOP issue** (Plan 54-07 Task 3 sub-item 3). What to do: open the canonical Finding 1 ESHOP fixture issue; confirm the `{panel}` block + `VAS.png` link render INSIDE the failed-step table cell, without splitting the table.
4. **ROADMAP SC end-to-end UAT on a happy-path issue** (overlap with Plan 54-05 Task 0 + Plan 54-07 Task 3 sub-item 6). What to do: open a known good issue (e.g. 393120 ESHOP), confirm all four ROADMAP SCs visually; toggle aioEnabled OFF/ON; confirm section appears/disappears.

### Gaps Summary

No automation-discoverable gaps blocking ROADMAP SCs. All four ROADMAP success criteria are verified in the codebase. All three Phase 54 requirements (AIOI-01, AIOI-02, AIOI-03) are satisfied with concrete artifact + key-link + data-flow evidence. Full test suite GREEN (1002 passing, 0 failures); TypeScript clean.

The phase status is `human_needed` solely because Plan 54-07 Task 3 (visual + interactive UAT of the three gap-closure items) is documented as still pending and is the contract for Phase 54 close. The 54-07 SUMMARY explicitly states: "Task 3 (human UAT) remains — the plan's third task is `checkpoint:human-verify` for a developer to run `npm run tauri dev` and verify the three gaps on a real ESHOP issue. … Once approved, Phase 54 is closeable." Plan 54-05 Task 0 (the original 8-scenario UAT) is similarly pending unless previously approved by the developer (no 54-05-SUMMARY.md present — implicit pending state per `.continue-here.md`).

Additionally — but informational, not blocking: code review `54-REVIEW.md` (advisory) raised CR-01 (queryKey omits `jiraIssueId`) and WR-01 (callout-balance regex covers only `{panel}`, not `{info}/{warning}/{note}`). These are real correctness concerns that should be triaged into a 54-08 follow-up or resolved in-band; they do not violate any ROADMAP SC and are out of scope for this goal-backward verification.

---

_Verified: 2026-05-14T00:25:00Z_
_Verifier: Claude (gsd-verifier)_
