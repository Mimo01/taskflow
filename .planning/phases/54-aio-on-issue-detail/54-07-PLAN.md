---
phase: 54-aio-on-issue-detail
plan: "07"
type: execute
wave: 6
depends_on: ["54-06"]
files_modified:
  - .planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md
  - taskflow/src/routes/dashboard/WikiRenderer.tsx
  - taskflow/src/routes/dashboard/WikiRenderer.test.tsx
  - taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx
  - taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx
autonomous: false
gap_closure: true
tags: [aio, tcms, jira-wiki, react-markdown, jira2md, traceability, impacted-executions, attachments]

requirements: [AIOI-02, AIOI-03]

must_haves:
  truths:
    - "When an issue has linked test cases but zero runs in the active cycle, the section renders a compact 'Impacted executions' list (one row per traceability item with embedded testRun.ID) instead of the bare 'No test runs in active cycle' EmptyState"
    - "Each impacted-execution row shows test case key + title, cycle key, run ID, and a status chip whose styling matches D-08 (aioRunStatusBadgeClass + normalizeStatusLabel); the chip status is sourced from the per-run `fetchAioTestRunDetail` response field `detail.run.status` (already normalized by `toRunChipStatus` to one of `PASS` / `FAIL` / `BLOCKED` / `NOT_EXECUTED`) — NOT defaulted to `'NOT_EXECUTED'` on the render layer"
    - "AioAttachmentsGrid is rendered on the no-runs path too — when impacted executions exist, the grid header is always visible AND the grid populates with inline `[name.ext|url]` images aggregated from the impacted-executions' step data (which `fetchAioTestRunDetail` returned alongside `run.status` in Sub-change 1's widened data shape); same dedupe pass as the in-cycle path via `collectAioImageAttachments`"
    - "Fan-out cap (T-54-07-02) applies ONLY to the cross-cycle impacted-executions fetch loop; in-cycle runs (runRefs whose `cycleKey` matches the dominant/active cycle) are NOT capped — preserving today's behaviour where every in-cycle linked run renders. `MAX_IMPACTED_EXECUTIONS=20` slices the cross-cycle list; `MAX_PARALLEL=6` chunks the cross-cycle Promise.all"
    - "Nested wiki rendering inside table cells works on the verbatim ESHOP fixture from 54-06-UAT-FINDINGS.md (Finding 1 example: a `{panel}…{panel}` containing `# [VAS.png|...]` embedded INSIDE a `|...|` table row renders without losing the panel block or the link)"
    - "Probe E is recorded in 54-PROBE-FINDINGS.md and ends with a single one-line decision: `### Decision: Branch 3-{A|B|C} selected — {one-line rationale}`"
    - "All existing 54-* tests still pass (regression-free); WikiRenderer + AioTestRunsSection test suites remain GREEN"
    - "Out-of-scope items are NOT introduced: no editing test runs from issue detail; no `step.attachments` field handling (Probe B confirmed absent); no navigation from impacted-executions to cycle/run pages (lives in phase 53)"
    - "D-08 status chip styling and D-10 collapsible behaviour unchanged for the in-cycle render path"
    - "T-54-07-01 mitigated: any new regex in preprocessJiraMarkup is asserted XSS-safe via a `<script>` payload test that renders as literal text (no script execution)"
    - "T-54-07-02 mitigated: the cross-cycle (no-runs-path) step fetch is capped at MAX_IMPACTED_EXECUTIONS=20 and MAX_PARALLEL=6; a test asserts fetchAioTestRunDetail call count is bounded on a 30-cross-cycle-case fixture; a no-regression test asserts 21 in-cycle linked runs all render (cap does NOT apply to in-cycle path)"
  artifacts:
    - path: ".planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md"
      provides: "Probe E section with nested-wiki branch selection decision"
      contains: "Probe E — 54-07 Nested Wiki Rendering"
    - path: "taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx"
      provides: "ImpactedExecutionsList sub-component + cross-cycle bounded step fetch + AioAttachmentsGrid always rendered when AIO data is present"
      contains: "ImpactedExecutionsList"
    - path: "taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx"
      provides: "Tests for impacted-executions list render with per-run status chips, no-runs-path attachment grid population from impacted-executions step data, cross-cycle bounded fetch ceiling, in-cycle no-cap regression, and (depending on branch) wiki-cell nesting behaviour"
      contains: "Impacted executions"
    - path: "taskflow/src/routes/dashboard/WikiRenderer.tsx"
      provides: "Nested-wiki rendering handled per Probe E branch (3-A preprocess heuristic OR 3-B td override OR 3-C parser swap)"
      contains: "WikiRenderer"
    - path: "taskflow/src/routes/dashboard/WikiRenderer.test.tsx"
      provides: "Test asserting nested wiki content (panel + image-link list) inside a table cell renders correctly on the ESHOP fixture, AND a `<script>` payload renders as literal text"
      contains: "table cell"
  key_links:
    - from: "AioTestRunsSection (data.runs.length === 0 && data.impactedCases.length > 0 branch)"
      to: "ImpactedExecutionsList"
      via: "render <ImpactedExecutionsList rows={impactedRowsWithStatus} /> when aioCases.length > 0 && activeCycleRuns.length === 0; replaces the EmptyState 'No test runs in active cycle'; each row carries the fetched run.status from fetchAioTestRunDetail"
      pattern: "ImpactedExecutionsList"
    - from: "ImpactedExecutionsList rows"
      to: "AioAttachmentsGrid (no-runs path)"
      via: "queryFn already fetched run+steps for each cross-cycle impacted execution (capped); aioAttachments useMemo aggregates inline image refs from data.runs UNION the impacted-executions step data exposed on the widened return shape"
      pattern: "fetchAioTestRunDetail"
    - from: "WikiRenderer (table cell content)"
      to: "Probe E selected branch (3-A | 3-B | 3-C)"
      via: "Probe E Decision line determines the impl; Task 2 wires exactly that branch"
      pattern: "Probe E"
---

<objective>
Close the three net-new pieces of scope surfaced during 54-06 UAT (verbatim from `54-06-SUMMARY.md` "Task 3 — UAT Outcome" section, routed there explicitly rather than retroactively expanding 54-06):

1. **Gap 1 — Impacted executions list across all cycles.** When `aioCases.length > 0 && activeCycleRuns.length === 0`, replace the bare `EmptyState "No test runs in active cycle"` with a compact list of impacted executions (test × cycle × run + status chip) sourced from the already-widened `fetchAioTraceabilityTestCases` response. No new endpoint needed — 54-06 Probe C1 already proved `testRun.ID` + `testCycle.detail.key` are embedded on traceability items.

2. **Gap 2 — AIO attachments grid visibility on no-runs path.** The `AioAttachmentsGrid` scaffolded in 54-06 (commits `dbfd361` + `8a92dbb`) currently renders an empty header on the no-runs path because no step data is fetched. Fetch step data for impacted executions (cross-cycle fetch loop bounded by `MAX_IMPACTED_EXECUTIONS=20`, `MAX_PARALLEL=6` per security threat model — NOT applied to in-cycle runs) using `fetchAioTestRunDetail`, then feed both the in-cycle data and the impacted-executions data into `collectAioImageAttachments` so the grid populates uniformly across both render paths.

3. **Gap 3 — Nested wiki rendering inside table cells.** Real ESHOP step content embeds `{panel}…{panel}` blocks (multi-line) and `# [name.ext|url]` lists INSIDE `||header||`/`|cell|` table rows. Jira's native renderer handles this; `jira2md` breaks on the embedded newlines (the row terminator `\n` ends the `|...|` row before the `{panel}` block is consumed). Probe E (Task 1) picks ONE of three documented branches (3-A preprocess heuristic / 3-B custom td renderer / 3-C swap to a proper wiki parser) against verbatim ESHOP fixtures, recorded as a single `### Decision` line. Task 2 wires exactly that branch.

Purpose: 54-06 closed both of the original UAT findings (wiki for simple constructs + direct lookup perf path). These three items are blockers for declaring Phase 54 fully shipping-ready. Out-of-scope items from `54-06-UAT-FINDINGS.md` (step attachment thumbnails on this instance, edit-run-from-issue-detail) and phase-boundary items (impacted-execution → cycle navigation, which lives in phase 53) MUST NOT leak in.

Output: ImpactedExecutionsList renders on no-runs path with real per-run status chips; AioAttachmentsGrid populates uniformly via cross-cycle bounded `fetchAioTestRunDetail` calls (in-cycle path stays uncapped); nested wiki content renders correctly per Probe E branch; XSS guard test passes; full test suite GREEN.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-aio-on-issue-detail/54-06-SUMMARY.md
@.planning/phases/54-aio-on-issue-detail/54-06-UAT-FINDINGS.md
@.planning/phases/54-aio-on-issue-detail/54-CONTEXT.md
@.planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From taskflow/src/services/aio/types.ts (already shipped in 54-06):
```typescript
export interface AioTraceabilityRunRef {
  runId: string;     // String form of numeric run ID (e.g. "263794")
  cycleKey: string;  // e.g. "ESHOP-CY-1011"
}
export interface AioTestCaseWithRuns extends AioTestCase {
  runs: AioTraceabilityRunRef[];  // Embedded run references — 54-06 Probe C1 confirmed
}
// Note: a traceability item with NO embedded runs returns runs: [] (Probe C1
// found ≥90% coverage — the gap row case is the <10% tail). The
// ImpactedExecutionsList must handle the empty `runs` case gracefully
// (omit the row OR show "no recorded executions" — see Task 2 acceptance).

// AioTestRun (also from types.ts) — relevant for Sub-change 1's status chip wiring:
export interface AioTestRun {
  id: string;
  status: string;        // ALREADY normalized by fetchAioTestRunDetail's toRunChipStatus()
                         // to one of "PASS" | "FAIL" | "BLOCKED" | "NOT_EXECUTED".
                         // This is the field Sub-change 1 reads — NOT detail.run.testRunStatus.name
                         // (that is the RAW field path, normalised by the service layer before return).
  testCaseKey: string;
  cycleKey: string;
  // ...other fields omitted
}
```

From taskflow/src/services/aio/projects.ts (already widened in 54-06):
```typescript
export async function fetchAioTraceabilityTestCases(
  baseUrl: string, token: string, aioProjectId: number,
  jiraIssueNumericId: number, type: 'defect' | 'requirement'
): Promise<AioTestCaseWithRuns[]>;
// Returns items with embedded testRun.ID + testCycle.detail.key normalized to runs[].
// Status is NOT included in the run reference — needs fetchAioTestRunDetail to
// resolve. This is the reason Task 2 fetches detail on the no-runs path.
```

From taskflow/src/services/aio/issue-steps.ts (already shipped in 54-06):
```typescript
export async function fetchAioTestRunDetail(
  baseUrl: string, token: string, projectKey: string, cycleKey: string, runId: string
): Promise<{ run: AioTestRun; steps: AioTestRunStep[] } | null>;
// Returns run-level status + steps in one request. run.status is ALREADY normalized
// (PASS/FAIL/BLOCKED/NOT_EXECUTED) by toRunChipStatus() — Sub-change 1 consumes
// `detail.run.status` directly. Already parallelised via Promise.all in
// AioTestRunsSection.tsx for the in-cycle path. Reuse the same pattern for
// cross-cycle impacted executions; CAP parallelism per T-54-07-02 (cross-cycle ONLY).
```

From taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx (the primary file you edit):
- Lines 66-85: `INLINE_IMAGE_ATTACHMENT_RE`, `IMAGE_EXT_RE`, `extractInlineImageAttachments` — REUSE; do not duplicate.
- Lines 87-105: `collectAioImageAttachments(runs: AioIssueRunData[])` — REUSE; both in-cycle data and impacted-executions data should funnel through it. The interface accepts `AioIssueRunData[]` (each has `run`, `testCase`, `steps`).
- Lines 111-174: `AioAttachmentsGrid` — REUSE unchanged. Currently rendered only on the data-path (line 499). Task 2 moves the call site / adjusts the data feeding it so it also populates on the no-runs path.
- Lines 202-266: `StepTable` — UNCHANGED on this plan (renders step rows via WikiRenderer; the table-cell nesting fix lives in WikiRenderer itself, not here).
- Lines 269-296: `CollapsibleRunBlock` — UNCHANGED.
- Lines 298-502: `AioTestRunsSection` (the main component).
  - Lines 322-384: `jiraNumericId !== null` branch — already collects `linkedTestCases` (`AioTestCaseWithRuns[]`) and flattens to `runRefs`. CRITICAL: today this branch ONLY uses `runRefs` (filters out cases with `runs: []`). For Gap 1, you need the FULL `linkedTestCases` list available to the render layer, NOT just the flattened runRefs. The `data === []` empty-array return at line 382 happens when `withSteps.length === 0` — that is the exact branch ImpactedExecutionsList renders into.
  - Lines 444-470: render waterfall (aioEnabled / skeleton / error / null / undefined / empty-array).
  - Lines 473-481: the `data.length === 0` EmptyState branch — THIS IS THE SWAP TARGET. Replace the EmptyState with `<ImpactedExecutionsList ... />`. The list needs the `linkedTestCases` array (currently held only inside `queryFn`).
- Lines 484-501: data-available render. Line 499 calls `<AioAttachmentsGrid attachments={aioAttachments} />`.

From taskflow/src/routes/dashboard/WikiRenderer.tsx:
- Lines 34-105: `preprocessJiraMarkup(wiki, attachments, users): string` — runs BEFORE jira2md. Branch 3-A edits happen here.
- Lines 84-103: existing panel/info/warning/note callout substitution — note the `[\s\S]*?` non-greedy match crosses newlines. Branch 3-A heuristic must run AFTER table-row detection but BEFORE the panel substitution (or it must work in concert with it). Read the function in full before editing.
- Lines 120-204: `markdownComponents` map. Branch 3-B adds a `td` override here.
- Tests live at WikiRenderer.test.tsx (267 lines); follow the existing assertion style.

From taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx (lines 30-38 are the service mock factory — already includes everything; no changes needed):
```typescript
vi.mock('@/services/aio', () => ({
  fetchAioProjects: vi.fn(),
  fetchAioTestCasesForIssue: vi.fn(),
  fetchAioTraceabilityTestCases: vi.fn(),
  fetchAioCycles: vi.fn(),
  fetchAioTestRunsForCycle: vi.fn(),
  fetchAioTestRunSteps: vi.fn(),
  fetchAioTestRunDetail: vi.fn(),  // 54-06 added; reuse for no-runs-path fetch in Task 2
}));
```
Fixture `TEST_CASE_WITH_RUNS` (lines 84-88) already exists. Reuse as a base; build a no-runs variant for the impacted-executions test (a case whose `runs[0].cycleKey !== activeCycle.key`, simulating "linked, but not in the active cycle").

From taskflow/src/lib/statusStyles.ts (existing helper, do not duplicate):
```typescript
export function aioRunStatusBadgeClass(status: string): string;
// Returns Tailwind classes for PASS / FAIL / BLOCKED / NOT_EXECUTED.
// Reuse for impacted-execution row status chips (D-08).
```

From taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx `normalizeStatusLabel` (lines 51-64):
```typescript
function normalizeStatusLabel(raw: string | undefined): string;
// PASS → "Pass", FAIL → "Fail", BLOCKED → "Blocked", NOT_EXECUTED → "Not Run"
// Reuse for impacted-execution row labels.
```

The verbatim Finding 1 ESHOP fixture from 54-06-UAT-FINDINGS.md (lines 14-25) is the canonical Probe E + Branch 3 test input. Use it AS-IS; do not paraphrase.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Probe E — Nested wiki rendering inside table cells (branch selection)</name>
  <read_first>
    .planning/phases/54-aio-on-issue-detail/54-06-UAT-FINDINGS.md (Finding 1 — the verbatim ESHOP fixture at lines 14-25 IS the probe input; do not paraphrase)
    .planning/phases/54-aio-on-issue-detail/54-06-SUMMARY.md (Task 3 UAT Outcome item 3 — the gap text routed here)
    .planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md (Probe C decision-line convention — Probe E mirrors that single-line decision pattern)
    taskflow/src/routes/dashboard/WikiRenderer.tsx (preprocessJiraMarkup + markdownComponents — the surfaces Branches 3-A and 3-B would touch)
  </read_first>
  <what-human-must-do>
This is a probe (no production code changes). Goal: pick exactly one of three documented branches for closing Gap 3 (nested wiki rendering inside table cells) and write a one-line `### Decision` to `54-PROBE-FINDINGS.md` that Task 2 reads verbatim.

**Step 1 — Reproduce the bug locally.** In a scratch test (or a quick repl in `node` / a temporary `WikiRenderer.test.tsx` test that you'll delete before commit), pass the verbatim ESHOP fixture below through the EXISTING `WikiRenderer` (no code changes) and confirm `jira2md` mangles the nested `{panel}` inside the `|cell|` row:

```
||*S.No.*||*Step*||*Expected Result*||*Actual Result*||
|1. |Nacitanie eshop home page |Kontrola OK |Works as expected|
|2. |{color:#d04437}*FAILED:*{color} Plati pre paušály S, M, L: \\ • 5 GB (12657037, 5,13 €) |Kontrola OK |V kosiku mam Pro Biznis M a device na splatky...
{panel}
# [VAS.png|https://jira.orange.sk/plugins/servlet/aio-tcms/bridge/tcms/browse?c_pId=10134&page=run-details-attachment&params=%7B%22cycleId%22:14041,%22caseId%22:68141,%22runId%22:263794,%22attachmentId%22:150383,%22projectId%22:10134%7D]
{panel}|
```

Record the symptom in 1-2 sentences (e.g. "the `{panel}` block renders outside the table row" or "the row terminator `|` is consumed before the `{panel}` block, splitting the table"). This is your baseline — every branch must visibly improve on it.

**Step 2 — Try Branch 3-A first (cheapest).** Sketch a preprocessing heuristic in `preprocessJiraMarkup`: when a `|...|` table row contains an unclosed `{panel}` (the row terminator appears AFTER a subsequent `{panel}` closer), inline-flatten the embedded newlines (`\n`) inside the panel content to `<br/>` OR to the markdown hard-break form (`  \n`) AND escape the `{panel}` markers (or substitute the callout-`<div>` upfront so the row stays one logical line for jira2md). Test against the verbatim fixture in a throwaway WikiRenderer test. Outcome:
- ✓ Renders panel content inside the cell with the image-link list visible → **3-A wins**.
- ✗ Heuristic too brittle (e.g. only works when there's exactly one `{panel}` per cell, breaks on the real fixture's mixed-content rows) → continue to 3-B.

**Step 3 — Try Branch 3-B if 3-A failed.** In `markdownComponents`, add a `td` override that receives the cell's children and re-runs `jira2md.to_markdown(rawCellSource)` then re-renders. The problem: react-markdown's `td` component receives already-parsed children (HAST/MDAST nodes), NOT the raw source. You'd need to either (a) preserve raw source through a custom remark plugin that attaches `node.raw` on table cells, OR (b) reconstruct an approximation of the raw cell text from the children — both are non-trivial. Time-box at 30 minutes; if no working render after that, 3-B loses.

**Step 4 — Branch 3-C as last resort.** Evaluate `jira-wiki-markup` (npm) OR `marsdown-jira-wiki`: install in a scratch dir, pipe the verbatim fixture through it, compare output to Jira's native render. Blast radius is the highest of the three: every WikiRenderer caller (issue descriptions, comments, MR descriptions, AIO content) would now use the new parser. Only pick this if 3-A and 3-B both demonstrably fail on the real fixture.

**Step 5 — Append the probe to `.planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md`** (do NOT overwrite existing Probe A/B/C/D sections). Use this exact section structure so Task 2 can grep it deterministically:

```markdown

## Probe E — 54-07 Nested Wiki Rendering
Probed: [date]

### Fixture
[the verbatim ESHOP fixture pasted from 54-06-UAT-FINDINGS.md — do not paraphrase]

### Baseline behaviour (no fix)
[1-2 sentence symptom — what jira2md does to the embedded {panel}]

### Branch 3-A — preprocess heuristic
[outcome: works / fails — short rationale and any diff sketch if works]

### Branch 3-B — custom td renderer
[outcome: works / fails / not attempted (only valid if 3-A wins) — short rationale]

### Branch 3-C — swap to proper wiki parser
[outcome: evaluated / not evaluated (only valid if 3-A or 3-B wins) — if evaluated, name the package + a 1-line summary of output quality vs blast radius]

### Decision: Branch 3-{A|B|C} selected — {one-line rationale}
```

The `### Decision` line is read verbatim by Task 2 to branch deterministically. Pattern MUST match `^### Decision: Branch 3-[ABC] selected — `.

**Hard rules:**
- Do NOT skip the probe and jump to a branch — Task 2 reads the Decision line to choose its impl.
- Do NOT pick 3-C unless 3-A AND 3-B both fail on the real fixture. 3-C touches every WikiRenderer surface; the regression risk is the highest of the three.
- If 3-A succeeds, do NOT also attempt 3-B "to confirm" — winning branch terminates the probe.
- If a `<script>` payload inside the fixture appears to render as anything other than literal text in any branch, STOP and document the failure — that's a T-54-07-01 violation and Task 2's XSS test would catch it anyway, but better to know now.
  </what-human-must-do>
  <action>
Record Probe E in `.planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md` under a new `## Probe E — 54-07 Nested Wiki Rendering` heading. Do not modify Probe A/B/C/D content. End with a single `### Decision: Branch 3-{A|B|C} selected — {one-line rationale}` line whose pattern is exactly `^### Decision: Branch 3-[ABC] selected — `.

Hard rules:
- Use the verbatim Finding 1 fixture from 54-06-UAT-FINDINGS.md lines 14-25 — do not paraphrase, abbreviate, or modify the fixture content.
- Pick the cheapest branch that demonstrably renders the panel block inside the table cell on the real fixture.
- Prefer 3-A over 3-B over 3-C (lowest blast radius wins on ties).
- The Decision line is the only thing Task 2 reads — phrase it so the rationale fits on one line.
  </action>
  <verify>
    <automated>grep -q "^## Probe E — 54-07 Nested Wiki Rendering" .planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md && grep -qE "^### Decision: Branch 3-[ABC] selected — " .planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md` contains a `## Probe E — 54-07 Nested Wiki Rendering` section
    - The Probe E section includes a verbatim copy of the Finding 1 fixture from 54-06-UAT-FINDINGS.md (grep for a distinctive token: `Plati pre paušály S, M, L`)
    - All three branch evaluations (3-A, 3-B, 3-C) are documented — at least one must have `outcome: works`; the others may have `outcome: not attempted` only when an earlier branch already succeeded
    - The section ends with exactly one line matching `^### Decision: Branch 3-[ABC] selected — `
    - No existing Probe A/B/C/D section text was modified (`git diff` on the file shows only additions after the existing content)
    - No XSS regression was observed during the probe (if a `<script>` test was attempted in any branch, the output renders as literal text — documented in the section)
  </acceptance_criteria>
  <done>Probe E section appended to 54-PROBE-FINDINGS.md; Decision line is one of `Branch 3-A`, `Branch 3-B`, or `Branch 3-C`; Task 2 can read the line and branch without interpretation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Gap 1 (ImpactedExecutionsList) + Gap 2 (no-runs-path attachment grid) + Gap 3 (nested wiki per Probe E)</name>
  <files>taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx, taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx, taskflow/src/routes/dashboard/WikiRenderer.tsx, taskflow/src/routes/dashboard/WikiRenderer.test.tsx</files>
  <read_first>
    .planning/phases/54-aio-on-issue-detail/54-06-SUMMARY.md (Task 3 UAT Outcome — gap text for items 1/2/3 verbatim)
    .planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md (Probe E `### Decision` line — read in full; the line determines which branch of Gap 3 you wire; if the line is missing or does not match `^### Decision: Branch 3-[ABC] selected — `, STOP and surface the issue rather than guess)
    .planning/phases/54-aio-on-issue-detail/54-06-UAT-FINDINGS.md (Finding 1 fixture — used as the Branch 3 test input)
    taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx (full file — note the queryFn data shape, the AioAttachmentsGrid call site, and the `data.length === 0` EmptyState branch you swap)
    taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx (full file — note the existing mock factory and TEST_CASE_WITH_RUNS fixture)
    taskflow/src/routes/dashboard/WikiRenderer.tsx (full file — Branch 3-A/3-B/3-C target surfaces, depending on Probe E decision)
    taskflow/src/routes/dashboard/WikiRenderer.test.tsx (existing assertion style — match it for the new nested-cell test)
    taskflow/src/services/aio/types.ts (AioTraceabilityRunRef, AioTestCaseWithRuns, AioTestRun.status — already shipped in 54-06; reuse the runs[] shape and `run.status` already normalised by toRunChipStatus)
    taskflow/src/services/aio/issue-steps.ts (fetchAioTestRunDetail — reuse for impacted-executions step fetch; returns `{ run, steps } | null`; note `run.status` is already normalised)
    taskflow/src/lib/statusStyles.ts (aioRunStatusBadgeClass — reuse for impacted-execution row chip)
  </read_first>
  <action>
Three sub-changes in one task, sequenced so each one's tests fail independently before the next is added. Read the Probe E `### Decision` line FIRST — Sub-change 3 branches on it.

**Sub-change 1 — Gap 1: ImpactedExecutionsList on the no-runs path.**

The data shape problem: the current queryFn (`AioTestRunsSection.tsx` lines 322-384) returns `AioIssueRunData[] | null`, where `null` = no linked test cases (D-04 first empty case) and `[]` = linked test cases exist but no runs have usable steps in the active cycle (D-04 second empty case). The `linkedTestCases` array is computed inside the queryFn (lines 330-345) and not surfaced to the render layer.

Widen the queryFn return shape to ALSO carry the `linkedTestCases` array on the empty-array path PLUS the per-impacted-execution step data and `run.status` aggregated from `fetchAioTestRunDetail`. Add a local composite return type:

```typescript
// One row per (testCase × run) impacted execution — carries the fetched run.status
// AND the fetched steps so the attachments grid can aggregate from this data too.
interface AioImpactedExecution {
  testCase: AioTestCaseWithRuns;
  runRef: AioTraceabilityRunRef;
  status: string;              // normalised: "PASS" | "FAIL" | "BLOCKED" | "NOT_EXECUTED"
  steps: AioTestRunStep[];     // for attachments aggregation; may be []
}

type AioTestRunsQueryResult =
  | null
  | { runs: AioIssueRunData[]; impactedExecutions: AioImpactedExecution[] }
```

Update the queryFn to ALWAYS return both arrays (no more bare `[]` sentinel):
- `null` — no linked test cases at all (D-04 first case; section hidden).
- `{ runs: withSteps, impactedExecutions: [...] }` — `withSteps` is the in-cycle render data (unchanged shape); `impactedExecutions` is the new array populated from `fetchAioTestRunDetail` results that did NOT make it into `withSteps` (cross-cycle runs OR in-cycle runs with empty steps). For each cross-cycle ref the queryFn extracts `{ testCase, runRef, status: detail.run.status, steps: detail.steps }`.

Adjust the render layer:
- `data === null` → section hidden (unchanged behaviour).
- `data.runs.length === 0 && data.impactedExecutions.length > 0` → render `<ImpactedExecutionsList rows={data.impactedExecutions} />` instead of `<EmptyState ...>`.
- `data.runs.length === 0 && data.impactedExecutions.length === 0` → defensive `return null` (cannot happen if linkedTestCases > 0).
- `data.runs.length > 0` → existing render path (StepTable / CollapsibleRunBlock list); the EmptyState branch is gone.

Add a new `ImpactedExecutionsList` sub-component inside `AioTestRunsSection.tsx` (do not extract to a new file — the section keeps its sub-components inline per the existing pattern with `StepTable`, `CollapsibleRunBlock`, `AioAttachmentsGrid`, `StepThumbnail`). Signature: `function ImpactedExecutionsList({ rows }: { rows: AioImpactedExecution[] }): JSX.Element`. Behaviour:
- Heading: small subdued header, e.g. "Impacted executions (across all cycles)" — match the visual weight of the existing `<div className="flex items-center gap-1.5 text-sm font-semibold mb-2">` heading style used by `AioAttachmentsGrid`.
- For each `row` in `rows`: render one row showing: test case key (`row.testCase.key`), test case title (`row.testCase.title`), cycle key (`row.runRef.cycleKey`), run ID (`row.runRef.runId`), and a status chip using `aioRunStatusBadgeClass(row.status)` + `normalizeStatusLabel(row.status)`.
- The status chip MUST be driven by `row.status` (the fetched, normalised value from `detail.run.status`). Do NOT default to `'NOT_EXECUTED'` on the render layer — the queryFn already wrote the real status into each row.
- Row layout: read-only table-ish (use the same `<table>` / `<tbody>` / `<tr>` pattern as `StepTable` for visual consistency, OR a simple flex grid — your choice; D-08 chip styling and Tailwind border conventions match the rest of the section). No buttons, no navigation, no click handlers — read-only per gap spec.
- Reuse `aioRunStatusBadgeClass` + `normalizeStatusLabel` for the chip.

Component tests (in `AioTestRunsSection.test.tsx`, new `describe('Impacted executions list (Gap 1)', () => { ... })` block):
- Setup: `mockFetchTraceability.mockResolvedValue([{ ...TEST_CASE_WITH_RUNS, runs: [{ runId: '99999', cycleKey: 'PROJ-CY-OLD' }] }])` (case linked, run in a NON-active cycle). Mock `fetchAioTestRunDetail` to return `{ run: { ...baseRun, status: 'PASS' }, steps: [] }` (steps empty → `withSteps` filtered out → render layer hits the `data.runs.length === 0 && data.impactedExecutions.length > 0` branch).
- Assert: `screen.getByText(/Impacted executions/i)` is present; `screen.getByText('PROJ-TC-1')` is present; `screen.getByText('PROJ-CY-OLD')` is present; `screen.getByText('99999')` (the run ID) is present; the `EmptyState` "No test runs in active cycle" text is NOT in the DOM.
- Assert: no click handler is wired on rows (`screen.getByText('PROJ-TC-1').closest('button')` returns null) — read-only per gap spec.
- **Status chip wiring test (per checker WARNING 1):** add a second test that mocks TWO traceability cases — one whose `fetchAioTestRunDetail` resolves with `status: 'PASS'`, the other with `status: 'FAIL'`. Assert that the two rendered rows show non-gray chips matching `aioRunStatusBadgeClass('PASS')` and `aioRunStatusBadgeClass('FAIL')` respectively (e.g. by reading the className token list of the chip element — assert it contains the green/red token from the helper). Assert that NEITHER row shows the gray `NOT_EXECUTED` / "Not Run" chip. This proves the queryFn threads `detail.run.status` through to the render layer instead of defaulting.

**Sub-change 2 — Gap 2: AioAttachmentsGrid populates on the no-runs path.**

The current data flow:
- `aioAttachments` (line 435-441) memos `collectAioImageAttachments(stepsQuery.data)` IF `stepsQuery.data.length > 0`.
- `<AioAttachmentsGrid attachments={aioAttachments} />` is called at line 499 only inside the data-available render branch.

Decision (recorded here per gap spec — no extra probe needed, since `fetchAioTestRunDetail` is already proven by Probe B): **the queryFn already fetched step data for cross-cycle impacted executions in Sub-change 1 — feed that step data into `collectAioImageAttachments` so the no-runs-path grid populates with real thumbnails (NOT just an empty-state).**

Implementation:
- Render `<AioAttachmentsGrid attachments={aioAttachments} />` on BOTH branches (data-available branch AND the no-runs-with-impacted-executions branch). Either:
  - (a) move the call site to a position inside the `<section>` wrapper that is rendered by both branches; OR
  - (b) duplicate the call in both branches.
  Pick (a) — single call site — to keep `grep -c "AioAttachmentsGrid"` low and the render tree predictable.
- Update the `aioAttachments` memo to aggregate from `data.runs` UNION the impacted-executions step data. `collectAioImageAttachments` already accepts `AioIssueRunData[]`; map `data.impactedExecutions` to that shape (`{ run: <synthetic from runRef + status>, testCase: row.testCase, steps: row.steps }`) and concatenate before calling. Reuse the existing dedupe-by-URL pass — no new helper needed.
- Net effect (per checker WARNING 3): on the no-runs path, when ≥1 impacted execution's step content contains `[file.png|...]` refs, the AioAttachmentsGrid renders ≥1 thumbnail. When no inline image refs exist, the grid header still renders with the existing "No inline image attachments found in linked test runs." empty-state inside — the header is ALWAYS present whenever the section renders. The grid is NEVER conditionally hidden based on the render branch.
- **Cap enforcement (T-54-07-02) — scoped to the cross-cycle fetch loop ONLY (per checker WARNING 2):** add two constants at the top of `AioTestRunsSection.tsx` near `INLINE_IMAGE_ATTACHMENT_RE`:
  ```typescript
  const MAX_IMPACTED_EXECUTIONS = 20;
  const MAX_PARALLEL = 6;
  ```
  In the queryFn, partition `runRefs` into two arrays by cycle key:
  - The "primary" cycle is the most frequently referenced `cycleKey` across `runRefs` (tie-break by lexicographically-highest CY-N suffix using the same numeric parser as `pickLatestActiveCycle`). This stands in for the active cycle since the dominant Branch A1 path does not call `fetchAioCycles`.
  - `inCycleRefs = runRefs.filter(r => r.runRef.cycleKey === primaryCycleKey)` — fetched UNCAPPED, preserving today's behaviour where every in-cycle linked run renders.
  - `crossCycleRefs = runRefs.filter(r => r.runRef.cycleKey !== primaryCycleKey).slice(0, MAX_IMPACTED_EXECUTIONS)` — capped at 20.
  Fetch `inCycleRefs` via the existing `Promise.all` (unchanged). Fetch `crossCycleRefs` via a chunked-parallel helper: a `for` loop over `Math.ceil(crossCycleRefs.length / MAX_PARALLEL)` slices, each slice resolved via `Promise.all` — this caps in-flight cross-cycle requests at 6 simultaneously per the threat model. Add a code comment naming `T-54-07-02` so the threat traceback is explicit.
  After both fetches resolve, partition the results: in-cycle results with non-empty steps → `withSteps` (the existing in-cycle render array, unchanged); cross-cycle results (regardless of steps length) → `impactedExecutions` for the new Sub-change 1 render path.

Component tests (in `AioTestRunsSection.test.tsx`, new test inside `describe('Impacted executions list (Gap 1)', () => { ... })` OR a new `describe('Attachments grid on no-runs path (Gap 2)', () => { ... })` block):

1. "no-runs path renders the AioAttachmentsGrid header with empty state inside when no inline images exist" — setup: linked case, run in out-of-cycle, mock `fetchAioTestRunDetail` returns `{ run: { ...baseRun, status: 'FAIL' }, steps: [{ id: 1, step: 'plain text', status: 'FAIL' }] }`. Assert `screen.getByText(/AIO attachments/i)` IS in the DOM, AND `screen.getByText(/No inline image attachments found in linked test runs/i)` IS in the DOM.
2. "no-runs path with inline image refs in impacted-execution step content renders thumbnails in the grid" (per checker WARNING 3) — setup: linked case, run in out-of-cycle, mock `fetchAioTestRunDetail` returns `{ run: { ...baseRun, status: 'FAIL' }, steps: [{ id: 1, step: 'see [foo.png|https://example.com/foo.png]', status: 'FAIL' }] }`. Assert `screen.getByText(/AIO attachments \(1\)/i)` (or the equivalent count token) IS in the DOM, AND at least one `<img>` (or `data-testid` thumbnail wrapper) with `alt="foo.png"` IS in the DOM. This is the canonical "grid populated on no-runs path" assertion.
3. "cross-cycle fetch is capped at MAX_IMPACTED_EXECUTIONS" — setup: mock `fetchAioTraceabilityTestCases` to return 30 cases, each with one embedded run ref whose `cycleKey` is in a DIFFERENT cycle (i.e. all 30 are cross-cycle — ensure the "primary cycle" picker selects a key NOT shared with these 30, e.g. by including a 31st case whose `cycleKey` is the dominant one but whose `fetchAioTestRunDetail` returns empty steps). Assert `vi.mocked(fetchAioTestRunDetail).mock.calls.length <= 21` (20 cross-cycle + 1 dominant) — i.e. the cross-cycle slice is capped at 20.
4. **In-cycle no-regression test (per checker WARNING 2):** setup: 21 traceability cases, ALL with the SAME `cycleKey` (so they all flow through the in-cycle path uncapped); mock `fetchAioTestRunDetail` to return non-empty steps for each. Assert that `vi.mocked(fetchAioTestRunDetail).mock.calls.length === 21` (no cap on in-cycle) AND that all 21 StepTable rows render — find the section by `[data-testid="aio-test-runs-section"]` and assert it contains 21 `CollapsibleRunBlock` (or 21 step-table containers). This proves `MAX_IMPACTED_EXECUTIONS` does NOT apply to in-cycle runs.

**Sub-change 3 — Gap 3: nested wiki rendering per Probe E branch.**

Read `54-PROBE-FINDINGS.md` Probe E `### Decision` line. The line MUST match `^### Decision: Branch 3-[ABC] selected — `. If it does not match, STOP — do NOT pick a branch yourself, do NOT skip the gap. Surface the issue to the user.

**Branch 3-A — preprocess heuristic (extend `preprocessJiraMarkup`):**

The current `preprocessJiraMarkup` (WikiRenderer.tsx lines 34-105) handles `{panel}…{panel}` globally via `result.replace(/\{panel\}([\s\S]*?)\{panel\}/g, '<div data-callout="panel">$1</div>')`. The bug is that this substitution happens BEFORE jira2md, but jira2md does NOT re-parse the resulting HTML inside table-row contexts — the `|...|` row terminator is consumed by jira2md before the `<div>` block is recognised, splitting the table.

Implement Probe E's exact 3-A heuristic. Two plausible shapes (use whichever the probe validated):
- (a) Detect `|...{panel}...{panel}...|` patterns BEFORE the global `{panel}` substitution and inline-flatten embedded newlines (`\n` inside the panel content → `<br/>` OR `  \n`) so the row stays one logical line.
- (b) Substitute the in-row `{panel}` to a single-line `<span data-callout="panel">…</span>` (or equivalent) BEFORE the global substitution runs, and skip the global substitution for already-substituted blocks.

Whichever shape: the heuristic MUST be regex-based, MUST run inside `preprocessJiraMarkup`, MUST NOT introduce new dependencies, and MUST NOT enable HTML injection (T-54-07-01 mitigation — see test below). Wrap any user-content interpolation through React (no `dangerouslySetInnerHTML` outside what `rehypeRaw` already permits at the markdown layer).

**Branch 3-B — custom td renderer:**

Add a `td` override in `markdownComponents` (WikiRenderer.tsx around line 120). The override receives `children` (parsed nodes). The Probe E result MUST have demonstrated a working approach for accessing the raw cell source — implement whichever approach won (raw-source preservation via custom remark plugin, OR best-effort reconstruction from children). Test against the verbatim fixture.

**Branch 3-C — swap jira2md for a proper wiki parser:**

Install `jira-wiki-markup` OR `marsdown-jira-wiki` (whichever the probe validated). Update `package.json`. Replace the `j2m.to_markdown(preprocessed)` call at WikiRenderer.tsx line 118 with the new parser's equivalent. Re-run ALL existing WikiRenderer tests — every one of them MUST still pass; this branch's blast radius is every wiki rendering surface in the app (issue descriptions, comments, MR descriptions, AIO step content).

**Branch 3 tests (in `WikiRenderer.test.tsx`):**

Add a new `describe('Gap 3 — nested wiki inside table cells (Branch 3-{A|B|C})', () => { ... })` block. Use the Probe E branch as the suffix. Tests:

1. "renders nested `{panel}` block with embedded image link inside a table cell" — use the verbatim Finding 1 fixture from 54-06-UAT-FINDINGS.md (lines 14-25). Assert that:
   - `screen.getByRole('table')` is in the document (the table did not break).
   - The `{panel}` content is inside the cell (use `screen.getByRole('cell', { name: /Plati pre paušály/ })` or similar — find the cell containing the FAILED content and assert the panel content is a descendant of it; you can also assert `getByRole('table').querySelector('[data-callout="panel"]')` is not null for Branches 3-A and 3-B).
   - The `VAS.png` text anchor is rendered (use `screen.getByText('VAS.png')` — note that 54-06 made image links render as text anchors that open the lightbox, NOT inline images; do not regress that behaviour).
2. "T-54-07-01 — `<script>` payload inside a table cell renders as literal text" — pass a fixture like ```|cell with <script>alert(1)</script> payload|``` through WikiRenderer; assert `screen.getByText(/<script>alert\(1\)<\/script>/)` IS in the document (literal text), AND `document.querySelector('script[data-test="injected"]')` returns null (no script tag was actually injected). This test MUST pass for whichever branch was selected.

**Common requirements across all three sub-changes:**

- The Task 1 Probe E `### Decision` line MUST be reflected in the commit message body (e.g. "Gap 3 branch: 3-A — preprocess heuristic"). Match the 54-06 commit-trace pattern.
- The Sub-change 1 + 2 commits MUST NOT regress the in-cycle data path tests added in 54-06 (Branch A1 perf test, wiki rendering tests). Run `cd taskflow && npm test -- AioTestRunsSection WikiRenderer --reporter=dot` after each sub-change.
- D-04 sentinels are PRESERVED in spirit: `data === null` ⇒ section hidden (unchanged); the new shape `{ runs, impactedExecutions }` replaces the bare `[]` sentinel but keeps the semantic equivalence (`runs.length === 0 && impactedExecutions.length > 0` is the new "no runs in active cycle but linked cases exist" branch).
- D-08 status chip styling and D-10 collapsible behaviour are UNCHANGED — Sub-change 1's ImpactedExecutionsList rows are read-only (no collapsible state, no buttons), so D-10 does not apply; D-08 chip styling is reused via `aioRunStatusBadgeClass`.
- No new external dependencies UNLESS Branch 3-C is selected (in which case `package.json` is updated and the new dependency is listed in the commit body).
- After all sub-changes: `cd taskflow && npm run typecheck && npm test --reporter=dot 2>&1 | tail -20` exits 0 with no regressions.

**Commit pattern:** atomic per sub-change:
- `feat(54-07): add ImpactedExecutionsList for no-runs path with per-run status chips (Gap 1)` — Sub-change 1
- `feat(54-07): populate AIO attachments grid on no-runs path with cross-cycle bounded fetch (Gap 2, T-54-07-02 mitigation)` — Sub-change 2
- `fix(54-07): nested wiki rendering inside table cells via Branch 3-{A|B|C} (Gap 3, T-54-07-01 mitigation)` — Sub-change 3

Lint hook `--no-verify` is approved per `feedback_no_verify_lint.md` IF the hook fails on pre-existing baseline warnings on files this plan did NOT touch. Biome on the 4 files this plan touches MUST be clean.
  </action>
  <verify>
    <automated>cd taskflow && npm run typecheck && npm test --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ImpactedExecutionsList" taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx` returns at least 2 matches (definition + usage)
    - `grep -nE "EmptyState[^A-Za-z]" taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx` — the previous EmptyState usage for "No test runs in active cycle" is REPLACED by ImpactedExecutionsList; if EmptyState is still imported, it must not be invoked in the data-empty branch
    - `grep -c "MAX_IMPACTED_EXECUTIONS" taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx` ≥ 2 (constant + usage in cross-cycle slice)
    - `grep -c "MAX_PARALLEL" taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx` ≥ 2 (constant + usage in chunked cross-cycle Promise.all)
    - `grep -n "T-54-07-02" taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx` returns at least 1 match (comment naming the threat in the cap code, alongside the cross-cycle partition)
    - `grep -c "AioAttachmentsGrid" taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx` ≥ 2 (component definition + at least one usage) — single render position is the preferred shape per Sub-change 2 (option a)
    - A component test in `AioTestRunsSection.test.tsx` asserts the AioAttachmentsGrid renders on BOTH the data-available branch AND the no-runs branch (NOT a grep count — the test fixtures listed in Sub-change 2 cases 1 + 2 cover the no-runs branch; the in-cycle render path already has 54-06 coverage that exercises the data-available branch — link the existing test by name in the new describe block if helpful)
    - `AioTestRunsSection.test.tsx` contains tests covering: ImpactedExecutionsList render on no-runs path (assert "Impacted executions" heading + at least one row with test case key + cycle key + run ID + no buttons/click handlers); status chip wiring test asserting PASS row chip className contains the green token from `aioRunStatusBadgeClass('PASS')` AND FAIL row chip className contains the red token (NOT defaulted to gray "Not Run"); AioAttachmentsGrid header with empty state on no-runs path when no inline images; AioAttachmentsGrid populated with ≥1 thumbnail on no-runs path when impacted-execution step content contains `[name.png|...]`; cross-cycle cap (assert `fetchAioTestRunDetail` cross-cycle call count ≤ 20 with a 30-cross-cycle-case fixture); in-cycle no-regression test (21 in-cycle linked runs all render — `fetchAioTestRunDetail` called 21 times, 21 step-table containers in the section)
    - `WikiRenderer.test.tsx` contains a new `describe` block whose name includes "Branch 3-A", "Branch 3-B", or "Branch 3-C" matching the Probe E Decision line
    - WikiRenderer.test.tsx contains the verbatim Finding 1 fixture (grep for `Plati pre paušály` — distinctive token from the fixture)
    - WikiRenderer.test.tsx contains a `<script>` XSS test that asserts literal-text render (T-54-07-01 mitigation; grep for `<script>alert(1)</script>` as a string literal)
    - The Probe E Decision line is quoted (verbatim) in the Gap 3 commit message body
    - `cd taskflow && npm run typecheck` exits 0
    - `cd taskflow && npm test` exits 0 (full suite — no regressions)
    - No new dependencies added UNLESS Branch 3-C was selected; if 3-C was selected, `package.json` shows the new wiki-parser dependency added and the change is documented in the commit body
  </acceptance_criteria>
  <done>ImpactedExecutionsList renders on no-runs path with per-row status chip sourced from the fetched `detail.run.status` (no gray "Not Run" default); AioAttachmentsGrid is visible on both render paths and populates with thumbnails from impacted-execution step data on the no-runs path; cross-cycle fetch is capped at MAX_IMPACTED_EXECUTIONS=20 / MAX_PARALLEL=6 while in-cycle fetches stay uncapped (T-54-07-02); nested wiki content renders inside table cells per Probe E branch (T-54-07-01 mitigated with literal-text-script assertion); full test suite is GREEN; commit body cites Probe E Decision line.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — impacted executions + attachments grid + nested wiki on a real ESHOP issue</name>
  <read_first>
    .planning/phases/54-aio-on-issue-detail/54-06-SUMMARY.md (Task 3 UAT Outcome — the three gaps you are verifying are listed verbatim there)
    .planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md (Probe E — confirm the implemented branch matches the recorded decision)
  </read_first>
  <what-built>
    - Gap 1: `ImpactedExecutionsList` renders on the no-runs path with one row per traceability item whose `runs[]` is non-empty (test case key, title, cycle key, run ID, status chip). Status chip color is driven by the fetched per-run `detail.run.status` (PASS/FAIL/BLOCKED/NOT_EXECUTED) — NOT defaulted to gray "Not Run". Replaces the bare `EmptyState "No test runs in active cycle"`.
    - Gap 2: `AioAttachmentsGrid` header is rendered on the no-runs path too AND populates with thumbnails aggregated from impacted-execution step content (`[name.png|...]` refs); empty state inside ONLY when no inline image refs exist. On large traceability responses, CROSS-CYCLE step fetches are capped at MAX_IMPACTED_EXECUTIONS=20 and MAX_PARALLEL=6 (T-54-07-02 mitigation); in-cycle runs stay uncapped.
    - Gap 3: Nested wiki content (`{panel}…{panel}` blocks with `# [name.ext|url]` lists embedded INSIDE `|cell|` table rows) renders correctly via Probe E's selected branch. XSS guard test passes — any `<script>` payload renders as literal text (T-54-07-01 mitigation).
    - Out-of-scope items are absent: no editing test runs from issue detail; no step-attachment field handling; no navigation from impacted-execution rows to cycle/run pages.
    - D-08 status chip styling and D-10 collapsible behaviour unchanged for the in-cycle render path.
  </what-built>
  <how-to-verify>
    1. **Start the app**, confirm `aioEnabled` is ON in Settings → Integrations.

    2. **Find an ESHOP issue with linked test cases but no runs in the latest active cycle.** Use the AIO Jira panel on the issue to confirm there ARE linked test cases and that their runs live in a non-active / older cycle. Examples from prior UAT sessions had issue 393120 in this state at points.

    3. **Gap 1 verification:**
       - Expected: the AIO section is visible (not hidden).
       - Expected: the bare "No test runs in active cycle" empty state is GONE.
       - Expected: in its place, a compact list shows one row per impacted execution with test case key + title, cycle key, run ID, and a status chip whose color matches D-08 styling (PASS green / FAIL red / BLOCKED orange / NOT_EXECUTED gray).
       - Expected: each row's chip color reflects the ACTUAL run status (not all gray). Find at least one row that is PASS and at least one that is FAIL (if both exist on the issue) — colors must differ accordingly.
       - Expected: rows are read-only — clicking a row does nothing (no navigation, no expansion).

    4. **Gap 2 verification:**
       - Expected: the "AIO attachments" collapsible header is visible on this no-runs page.
       - Expected: clicking the header expands/collapses the section.
       - Expected: when expanded, IF any impacted execution's step content contains inline `[name.ext|url]` image refs, thumbnails appear in the grid. Otherwise the empty state "No inline image attachments found in linked test runs." is shown.
       - Find an ESHOP issue whose impacted executions contain image refs (the Finding 1 example is a known candidate) and confirm thumbnails appear in the header — this is the canonical "populated grid on no-runs path" check.
       - If you can find an issue with ≥21 cross-cycle impacted executions (rare on ESHOP — there may not be a viable one), confirm the section still loads (no infinite spinner, no rate-limit error). Otherwise skip this sub-step and rely on the automated `MAX_IMPACTED_EXECUTIONS` test.

    5. **Gap 3 verification — open an ESHOP issue whose linked test runs contain step content with embedded `{panel}` inside a table cell** (the Finding 1 example from 54-06-UAT-FINDINGS.md lines 14-25 is the canonical case; any failed/blocked test run with multi-line step content + attachment list is likely to have this structure):
       - Expected: the step table renders without breaking — the row containing the `{panel}` block remains in the table (no split).
       - Expected: the `{panel}` content (including the `# [VAS.png|...]` list inside) is visible WITHIN the table cell.
       - Expected: the image-attachment link inside the panel renders as a clickable text anchor; clicking it opens the in-app ImageLightbox (NOT the OS browser — same behaviour as 54-06).

    6. **No regressions:**
       - Open an issue WITH runs in the active cycle (the 54-06 happy-path issue). Confirm the in-cycle StepTable render path still works (tables, `{color}`, single-line `{panel}`, `h4.`, `*bold*`, `\\` hard breaks — none of these appear as literal text). The Branch A1 direct-lookup perf path is still active.
       - Confirm `<details>`/collapsible blocks (D-10) still expand/collapse correctly for multi-test-case issues.
       - Confirm status chip colors (D-08) are unchanged in both the in-cycle render and the new impacted-executions list.
       - Toggle `aioEnabled` OFF — section disappears entirely.

    7. **Other WikiRenderer surfaces NOT regressed:** open a regular Jira issue and confirm its description / a comment with wiki markup still renders correctly. External links still open via `openUrl`. If Branch 3-C was selected, this check is the most critical — the parser swap touches every surface.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 54, or describe any issues (include which Gap 1/2/3 it falls under and what you saw vs. expected — that lets a follow-up plan 54-08 be scoped narrowly if needed).</resume-signal>
  <action>
No automated steps — this is the human visual + interactive verification gate. After verifying all 7 steps above, record approval. If any step fails, note which Gap (1, 2, or 3) it falls under so a follow-up gap-closure plan (54-08) can be scoped narrowly.
  </action>
  <verify>
    <automated>cd taskflow && npm test --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Gap 1: ImpactedExecutionsList visible on no-runs path with at least one row showing test case key + cycle key + run ID + status chip whose color matches the actual run status (PASS/FAIL/BLOCKED chips differentiate visually — not all gray "Not Run"). "No test runs in active cycle" EmptyState is gone.
    - Gap 2: AioAttachmentsGrid header visible on no-runs path (collapsible). When impacted-execution step content contains inline image refs, ≥1 thumbnail is visible inside; otherwise the empty state is shown.
    - Gap 3: Nested `{panel}` + image-link list inside a table cell renders correctly on the verbatim Finding 1 fixture (or an analogous real ESHOP issue). No table-split. Image link still opens in-app via lightbox.
    - No regressions in in-cycle render path (D-08, D-10, Branch A1 direct lookup, uncapped in-cycle fetch with ≥21 linked runs if such an issue exists) or other WikiRenderer surfaces (issue descriptions, comments).
    - `cd taskflow && npm test` exits 0.
  </acceptance_criteria>
  <done>Developer has confirmed all 7 verification steps pass in the running app and typed "approved". Phase 54 is closeable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AIO REST API → Tauri webview | Raw Jira wiki markup in step content reaches the renderer. `rehypeRaw` is already enabled in WikiRenderer to allow the preprocessor's `<div data-callout>` / `<img>` / `<mention>` HTML through. Step content from AIO is in the same trust class as issue descriptions / comments (same AIO instance, same auth boundary). |
| Wiki preprocessor regex → markdown pipeline | Branch 3-A extends `preprocessJiraMarkup` with new regex substitutions on untrusted Jira content. Any new pattern must NOT enable HTML injection (T-54-07-01). |
| Cross-cycle impacted-executions fetch → AIO REST API | Sub-change 2 fans out one `fetchAioTestRunDetail` per CROSS-CYCLE impacted execution (in-cycle runs continue to use the existing uncapped Promise.all from 54-06). Unbounded fan-out on a traceability response with many CROSS-CYCLE items would create rate-limit pressure on the AIO instance (T-54-07-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-07-01 | Tampering / Spoofing | `preprocessJiraMarkup` regex changes for nested wiki (Gap 3, Branch 3-A or 3-B) | mitigate | Any new regex MUST NOT introduce new `<script>` or attribute-injection vectors. WikiRenderer.test.tsx adds a `<script>alert(1)</script>` payload test that asserts the script renders as literal text (`screen.getByText(/<script>alert\(1\)<\/script>/)`) and that no script element was actually injected (`document.querySelector('script[data-test="injected"]')` returns null). React's default escaping + `rehypeRaw`'s sanitisation posture is preserved; no `dangerouslySetInnerHTML` added outside the existing markdown pipeline. If Branch 3-C is selected, the same XSS guard test runs against the new parser. |
| T-54-07-02 | Denial of Service | Unbounded `fetchAioTestRunDetail` fan-out on the no-runs path (Gap 2) — CROSS-CYCLE fetch loop only | mitigate | Partition `runRefs` by `cycleKey` against the primary cycle (most-frequent, tie-break by highest CY-N). In-cycle refs are fetched UNCAPPED via the existing Promise.all (preserving today's behaviour). Cross-cycle refs are capped at `MAX_IMPACTED_EXECUTIONS = 20` (slice the cross-cycle array before Promise.all) AND `MAX_PARALLEL = 6` (chunk the cross-cycle Promise.all into batches of 6 in-flight). Both constants are inline in `AioTestRunsSection.tsx` with a `// T-54-07-02` comment naming the threat. Tests assert (a) cross-cycle `fetchAioTestRunDetail` call count ≤ 20 even when the cross-cycle slice contains 30 items, AND (b) in-cycle fetch is uncapped: 21 in-cycle linked runs all render with 21 detail calls. The cross-cycle cap is generous enough not to break the 99th-percentile ESHOP issue (most issues have <5 linked cross-cycle test cases) but bounded enough to prevent runaway fan-out on a pathological traceability response. |
| T-54-07-03 | Information Disclosure | Impacted-execution rows display run IDs + cycle keys to the user | accept | This is the intended product behaviour (the gap spec explicitly calls for showing run ID + cycle key per row). All values come from the same AIO instance the user is already authenticated to via PAT — no new disclosure surface. Same trust class as the cycle detail page (Phase 53) which displays the same fields. |
| T-54-07-04 | Information Disclosure | Probe E findings (54-PROBE-FINDINGS.md) | accept | Inherits Probe A/B/C/D disposition — endpoint URLs and HTTP status codes only; no secrets, no PII. Safe to commit. |
| T-54-07-05 | Tampering / Spoofing | wiki `<a href>` payload in step content (carryover from 54-06) | mitigate (pre-existing) | Already mitigated in 54-06 — `openUrl` routes external links to OS shell; image-extension links open in-app via ImageLightbox. This plan does NOT widen that surface. No new mitigation needed; named here for completeness so Task 2's Branch 3-A regex changes are checked against this prior posture. |
</threat_model>

<verification>
Plan 54-07 closes Phase 54 when:
- `.planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md` contains a `## Probe E — 54-07 Nested Wiki Rendering` section ending in a `### Decision: Branch 3-{A|B|C} selected — …` line that matches `^### Decision: Branch 3-[ABC] selected — `
- `grep -c "ImpactedExecutionsList" taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx` ≥ 2
- `grep -c "MAX_IMPACTED_EXECUTIONS" taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx` ≥ 2 AND `grep -c "MAX_PARALLEL" taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx` ≥ 2
- `grep -n "T-54-07-02" taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx` returns at least 1 match
- AioTestRunsSection.test.tsx asserts the cross-cycle fetch is capped at 20 AND the in-cycle path is uncapped (21 in-cycle linked runs all render)
- AioTestRunsSection.test.tsx asserts ImpactedExecutionsList chip colors differ between PASS and FAIL rows (no gray "Not Run" default)
- AioTestRunsSection.test.tsx asserts AioAttachmentsGrid populates with ≥1 thumbnail on the no-runs path when impacted-execution step content contains an inline image ref
- WikiRenderer.test.tsx contains the verbatim Finding 1 fixture (`grep -l "Plati pre paušály" taskflow/src/routes/dashboard/WikiRenderer.test.tsx`) AND the `<script>alert(1)</script>` XSS guard test
- `cd taskflow && npm test` exits 0
- `cd taskflow && npm run typecheck` exits 0
- Task 3 human-verify checkpoint approved
</verification>

<success_criteria>
All three Task 3 UAT follow-up gaps are closed without scope creep:
1. **Gap 1 closed:** ImpactedExecutionsList renders on the no-runs path with one row per traceability item containing test case key, cycle key, run ID, and a status chip whose color reflects the fetched `detail.run.status` (PASS/FAIL/BLOCKED differ visually — not all gray); verified on a real ESHOP issue with linked-but-not-in-active-cycle test cases.
2. **Gap 2 closed:** AioAttachmentsGrid header is rendered on the no-runs path AND populates with thumbnails aggregated from impacted-execution step content when inline image refs exist; CROSS-CYCLE step fetch is capped at MAX_IMPACTED_EXECUTIONS=20 and MAX_PARALLEL=6 while IN-CYCLE fetches stay uncapped; T-54-07-02 mitigation verified by automated tests (cross-cycle call count ≤ 20 on a 30-cross-cycle-case fixture AND 21 in-cycle linked runs all render).
3. **Gap 3 closed:** Nested `{panel}…{panel}` blocks with embedded `# [name.ext|url]` lists inside `|cell|` table rows render correctly via Probe E's selected branch (3-A, 3-B, or 3-C); T-54-07-01 mitigation verified by automated test asserting `<script>` payloads render as literal text.
4. **No scope creep:** out-of-scope items (step attachment thumbnails on this instance, edit-runs-from-issue-detail, impacted-execution → cycle/run navigation) are absent. D-08 status chip styling and D-10 collapsible behaviour are unchanged. The 54-06 Branch A1 direct-lookup perf path remains active.
</success_criteria>

<output>
After completion, create `.planning/phases/54-aio-on-issue-detail/54-07-SUMMARY.md`
</output>
</output>
