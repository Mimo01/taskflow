---
phase: 54-aio-on-issue-detail
plan: "04"
type: execute
wave: 3
depends_on: ["54-02", "54-03"]
files_modified:
  - taskflow/src/routes/dashboard/IssueDetailPage.tsx
autonomous: true
requirements: [AIOI-01, AIOI-02, AIOI-03]

must_haves:
  truths:
    - "AioTestRunsSection appears below ActivityTimeline in IssueDetailPage — not before, not after the sticky composer"
    - "All component tests from Plan 54-02 pass GREEN"
    - "Full test suite passes (npm test exits 0)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/IssueDetailPage.tsx"
      provides: "Integration point — renders AioTestRunsSection below ActivityTimeline"
      contains: "AioTestRunsSection"
  key_links:
    - from: "taskflow/src/routes/dashboard/IssueDetailPage.tsx"
      to: "taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx"
      via: "import { AioTestRunsSection } from './issue-detail/AioTestRunsSection'"
      pattern: "AioTestRunsSection"
---

<objective>
Wire `AioTestRunsSection` into `IssueDetailPage.tsx` at the correct placement point (below `ActivityTimeline`, above the sticky comment composer — D-16). Then fill the component test stubs from Plan 54-02 with real assertions and confirm all tests pass GREEN.

Purpose: The component exists (Plan 54-03) and the tests exist (Plan 54-02). This plan completes the circuit: issue detail page shows AIO data, and the test suite verifies all behaviors end-to-end.

Output: Updated `IssueDetailPage.tsx` with the AIO section import and render. Full test suite GREEN.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-aio-on-issue-detail/54-CONTEXT.md
@.planning/phases/54-aio-on-issue-detail/54-PATTERNS.md

<interfaces>
<!-- Key insertion point from IssueDetailPage.tsx, extracted for executor. -->

From taskflow/src/routes/dashboard/IssueDetailPage.tsx (lines 388-432 — the relevant section):
```typescript
            {/* Activity timeline + comment composer */}
            <div className="px-6">
              <ActivityTimeline
                ...many props...
              />

              {(timelineFilter === 'comment' || timelineFilter === 'all') && (
                <div className="sticky bottom-0 border-t py-3 -mx-6 px-6 bg-background">
                  <CommentComposer issueKey={issueKey} jiraBaseUrl={jiraBaseUrl!} />
                </div>
              )}
            </div>
```

Insert `<AioTestRunsSection>` BETWEEN the closing `/>` of `<ActivityTimeline>` and the `{(timelineFilter === ...}` sticky composer div.

Import to add at the top of IssueDetailPage.tsx (alongside other issue-detail section imports):
```typescript
import { AioTestRunsSection } from './issue-detail/AioTestRunsSection';
```

Props to pass: `issueKey={issueKey}` and `jiraBaseUrl={jiraBaseUrl!}` — both already available in scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Integrate AioTestRunsSection into IssueDetailPage</name>
  <files>taskflow/src/routes/dashboard/IssueDetailPage.tsx</files>
  <read_first>
    taskflow/src/routes/dashboard/IssueDetailPage.tsx (read full file — identify the exact location of the ActivityTimeline closing tag and the sticky composer div)
    .planning/phases/54-aio-on-issue-detail/54-PATTERNS.md (IssueDetailPage.tsx pattern section — insertion point documented)
    .planning/phases/54-aio-on-issue-detail/54-CONTEXT.md (D-16: placement below ActivityTimeline)
  </read_first>
  <action>
Make two changes to `IssueDetailPage.tsx`:

**Change 1 — Import:** Add `import { AioTestRunsSection } from './issue-detail/AioTestRunsSection';` after the existing import `import { ActivityTimeline } from './issue-detail/ActivityTimeline';` on line 35 (keep alphabetical order within the issue-detail import group).

**Change 2 — JSX insertion:** Between the closing `/>` of `<ActivityTimeline ...>` and the `{(timelineFilter === 'comment' || timelineFilter === 'all') && (` line, insert:
```tsx
              {/* AIO Test Runs — loads in parallel with Jira data; gated by aioEnabled inside the component (D-15, D-16) */}
              <AioTestRunsSection issueKey={issueKey} jiraBaseUrl={jiraBaseUrl!} />
```

Placement rule (from RESEARCH.md Pitfall 5): the AIO section must be inside the `<div className="px-6">` wrapper (after ActivityTimeline) but BEFORE the sticky bottom div. The sticky `bottom-0` comment composer div must remain the last element inside `<div className="px-6">`.

After making changes, run `cd taskflow && npm run typecheck` to confirm no TypeScript errors.
  </action>
  <verify>
    <automated>cd taskflow && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AioTestRunsSection" taskflow/src/routes/dashboard/IssueDetailPage.tsx` returns at least 2 matches (import line + JSX render line)
    - The JSX render of `<AioTestRunsSection>` appears AFTER the closing `/>` of `<ActivityTimeline` (verify line ordering with grep -n)
    - The sticky composer `className="sticky bottom-0` line appears AFTER the `<AioTestRunsSection>` line in the file
    - Props are `issueKey={issueKey}` and `jiraBaseUrl={jiraBaseUrl!}` (both non-null asserted)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>IssueDetailPage.tsx imports AioTestRunsSection and renders it below ActivityTimeline but above the sticky comment composer. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 1: Fill component tests GREEN and run full suite</name>
  <files>taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx</files>
  <read_first>
    taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx (the stub file from Plan 54-02 — read current state)
    taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx (read full implementation — tests must match component's actual behavior)
    .planning/phases/54-aio-on-issue-detail/54-PROBE-FINDINGS.md (step field names for test fixtures)
    .planning/phases/54-aio-on-issue-detail/54-CONTEXT.md (D-04 two empty states, D-09 NOT_EXECUTED dash, D-10 collapsible defaults)
  </read_first>
  <action>
Run the component tests first to see which are failing and why:
```
cd taskflow && npm test -- --reporter=verbose AioTestRunsSection 2>&1 | head -60
```

The test file should now import successfully (AioTestRunsSection.tsx exists). Fix tests that fail due to:

1. **Mock mismatch** — the component may import from `@/services/aio` using named exports; ensure the vi.mock covers all imported functions (`fetchAioTestCasesForIssue`, `fetchAioCycles`, `fetchAioTestRunsForCycle`, `fetchAioTestRunSteps`).

2. **QueryClient / Provider** — ensure each test wraps the component in a fresh `QueryClient` with `defaultOptions: { queries: { retry: false } }` to prevent retry delays in tests.

3. **aioEnabled mock** — `useSettingsStore` mock must return `{ aioEnabled: true }` for tests that expect the section to render, and `{ aioEnabled: false }` for the gate test.

4. **Async query resolution** — use `await waitFor(() => expect(screen.getByText('Step')).toBeInTheDocument())` for tests that wait for query data to resolve.

5. **Sentinel null vs []** — the component returns null for `data === null` (no test cases) and EmptyState for `data.length === 0` (no runs). Test 3 (section hidden) must mock `fetchAioTestCasesForIssue` to return `[]`, which causes the queryFn to return `null`. Test 4 (empty state) must mock all fetches to produce a matched run with zero steps, triggering the `[]` return.

6. **Thumbnail click lightbox** — after clicking a thumbnail, assert `screen.getByRole('dialog')` appears or assert the `aria-modal="true"` attribute, since `ImageLightbox` renders with `role="dialog" aria-modal="true"`.

Fix each failing test while preserving the test intent. Do NOT weaken assertions to make tests trivially pass. If the component behavior differs from the test expectation, fix the component — not the test.

After all 9 tests pass, run the full suite:
```
cd taskflow && npm test 2>&1 | tail -20
```

Fix any regressions in other test files caused by the Phase 54 additions.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npm test -- AioTestRunsSection 2>&1 | grep -E "✓|pass" | wc -l` returns 9 or more
    - `cd taskflow && npm test` exits 0 (full suite passes — no regressions)
    - Test for aioEnabled=false asserts container renders nothing related to AIO (no "AIO Test Runs" text, no table headers)
    - Test for NOT_EXECUTED step has `expect(screen.getByText('—')).toBeInTheDocument()`
    - Test for lightbox has `expect(screen.getByRole('dialog')).toBeInTheDocument()` after thumbnail click
    - Test for collapsible blocks has at least two test case name assertions in the DOM
    - No test uses `toBeTruthy()` or `toBeDefined()` as the sole assertion (all assertions are specific)
  </acceptance_criteria>
  <done>All 9 component tests pass GREEN. Full test suite exits 0. No regressions introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IssueDetailPage → AioTestRunsSection | issueKey from useParams — comes from React Router URL, not user input field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-04-01 | Tampering | issueKey from route params → AioTestRunsSection prop | accept | issueKey is from React Router params (same trust level as other Jira calls in IssueDetailPage); encodeURIComponent applied inside service layer |
| T-54-04-02 | Information Disclosure | AIO section visible when aioEnabled=false | mitigate | AioTestRunsSection first line: `if (!aioEnabled) return null` — enforced and tested in component test suite |
</threat_model>

<verification>
Phase 54 feature complete when:
- `cd taskflow && npm test` exits 0
- `grep -n "AioTestRunsSection" taskflow/src/routes/dashboard/IssueDetailPage.tsx` shows import + JSX render
- `grep -n "AioTestRunsSection" taskflow/src/routes/dashboard/IssueDetailPage.tsx` — JSX render line number is greater than ActivityTimeline closing line number (placement check)
- `grep -c "sticky bottom-0" taskflow/src/routes/dashboard/IssueDetailPage.tsx` returns 1 (sticky composer not duplicated)
</verification>

<success_criteria>
The AIO Test Runs section is wired into the issue detail page, all component tests pass, and the full test suite is green. Phase 54 is ready for human verification (Plan 54-05).
</success_criteria>

<output>
After completion, create `.planning/phases/54-aio-on-issue-detail/54-04-SUMMARY.md`
</output>
