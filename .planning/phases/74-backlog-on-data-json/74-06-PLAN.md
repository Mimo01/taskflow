---
phase: 74-backlog-on-data-json
plan: 06
type: execute
wave: 4
depends_on:
  - 74-03
  - 74-04
  - 74-05
files_modified:
  - taskflow/src/services/jira/backlog.ts
  - taskflow/src/services/jira.ts
  - taskflow/package.json
autonomous: true
requirements:
  - GH-BACKLOG-01
  - GH-BACKLOG-02
must_haves:
  truths:
    - "fetchBacklogIssues and fetchBacklogSprintStories are deleted from services/jira/backlog.ts and from any re-export in services/jira.ts (D-09)"
    - "fetchBacklogView is deleted (services/jira/backlog.ts AND services/jira.ts duplicate) if a fresh grep confirms zero non-test callers per D-09 / RESEARCH Pitfall 3"
    - "BacklogViewData interface is deleted from both services/jira.ts and services/jira/backlog.ts if it has no remaining importers (RESEARCH A4 already confirmed only services/jira/backlog.ts:8)"
    - "fetchSprintList is KEPT (D-09a — still imported by FieldsSection.tsx)"
    - "scripts/check-legacy-backlog-keys.mjs exits 0 against the entire src/ tree and is wired into package.json scripts (npm run lint:legacy-backlog or postlint hook)"
    - "Full vitest suite GREEN; biome 0/0; tsc clean"
  artifacts:
    - path: taskflow/src/services/jira/backlog.ts
      provides: "Trimmed legacy backlog service — only fetchSprintList survives"
    - path: taskflow/src/services/jira.ts
      provides: "Trimmed re-export surface — three legacy fetcher re-exports removed; BacklogViewData removed"
    - path: taskflow/package.json
      provides: "Script entry running check-legacy-backlog-keys.mjs"
      contains: "check-legacy-backlog-keys"
  key_links:
    - from: taskflow/package.json
      to: taskflow/scripts/check-legacy-backlog-keys.mjs
      via: "scripts entry"
      pattern: "check-legacy-backlog-keys"
---

<objective>
Hard-cutover legacy backlog REST fetchers per GH-CUT-01 and D-09: delete `fetchBacklogIssues`, `fetchBacklogSprintStories`, and (if zero callers) `fetchBacklogView` + `BacklogViewData`. Remove their re-exports from the dual `services/jira.ts`. Wire the Plan 01 static-grep script into the CI lint surface so the symbols cannot reappear.

Purpose: GH-CUT-01 is satisfied only when the old paths are gone from the codebase, not merely unreferenced. The grep guard locks the deletion in.
Output: Trimmed backlog service file, trimmed `jira.ts` re-export surface, package.json script entry running the guard.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-backlog-on-data-json/74-CONTEXT.md
@.planning/phases/74-backlog-on-data-json/74-RESEARCH.md
@.planning/phases/74-backlog-on-data-json/74-PATTERNS.md
@taskflow/src/services/jira/backlog.ts
@taskflow/src/services/jira.ts
@taskflow/scripts/check-legacy-backlog-keys.mjs
@taskflow/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-delete grep audit — verify zero remaining callers</name>
  <read_first>
    - taskflow/src/services/jira/backlog.ts (full — confirm function locations and exports)
    - taskflow/src/services/jira.ts (lines 2040-2200 — confirm legacy re-exports + BacklogViewData definition position)
    - .planning/phases/74-backlog-on-data-json/74-RESEARCH.md §"Common Pitfalls #3" (BacklogViewData consumer grep — RESEARCH already confirmed only services/jira/backlog.ts:8; planner re-grep is mandatory before delete)
    - .planning/phases/74-backlog-on-data-json/74-CONTEXT.md D-09 + D-09a
  </read_first>
  <behavior>
    - Confirms that NO files under `taskflow/src/` (excluding the legacy `jira.ts` and `services/jira/backlog.ts` themselves) import: `fetchBacklogIssues`, `fetchBacklogSprintStories`, `fetchBacklogView`, or `BacklogViewData`.
    - Confirms that `fetchSprintList` is STILL imported by `taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx` (D-09a — must NOT be deleted).
    - Confirms `BacklogViewData` is referenced only inside `services/jira/backlog.ts` and its declaration inside `services/jira.ts`.
    - Output is a brief plain-text grep report committed to the working tree as a transient verification artifact (NOT a markdown file). If any caller remains, this task fails and the executor stops.
  </behavior>
  <action>
    Run grep audits and capture exit codes. Commands to run (recorded as a single bash sequence):
    `grep -rnE "fetchBacklogIssues|fetchBacklogSprintStories|fetchBacklogView" taskflow/src --include="*.ts" --include="*.tsx" | grep -vE "services/jira/backlog\\.ts|services/jira\\.ts"` — expected: empty (exit 1).
    `grep -rnE "BacklogViewData" taskflow/src --include="*.ts" --include="*.tsx" | grep -vE "services/jira/backlog\\.ts|services/jira\\.ts"` — expected: empty (exit 1).
    `grep -rnE "fetchSprintList" taskflow/src --include="*.ts" --include="*.tsx"` — expected: at least one hit in `routes/dashboard/issue-detail/FieldsSection.tsx`.
    If any of the first two greps returns a hit, halt and surface the file:line; the executor must investigate before proceeding to Task 2.
  </action>
  <acceptance_criteria>
    - First grep (external callers of the three doomed fetchers) exits with code 1 (no matches).
    - Second grep (external callers of `BacklogViewData`) exits with code 1.
    - Third grep (callers of `fetchSprintList`) exits 0 with at least one hit in `FieldsSection.tsx`.
  </acceptance_criteria>
  <verify>
    <automated>! grep -rnE "fetchBacklogIssues|fetchBacklogSprintStories|fetchBacklogView" taskflow/src --include="*.ts" --include="*.tsx" | grep -vE "services/jira/backlog\\.ts|services/jira\\.ts" && ! grep -rnE "BacklogViewData" taskflow/src --include="*.ts" --include="*.tsx" | grep -vE "services/jira/backlog\\.ts|services/jira\\.ts" && grep -rnE "fetchSprintList" taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx | grep -q "fetchSprintList"</automated>
  </verify>
  <done>Zero external callers for the three doomed symbols + BacklogViewData; fetchSprintList still wired to FieldsSection.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Delete legacy fetchers from services/jira/backlog.ts (keep fetchSprintList)</name>
  <read_first>
    - taskflow/src/services/jira/backlog.ts (full — line 8 import block, line 20 fetchBacklogIssues, line 83 fetchSprintList, line 132 fetchBacklogSprintStories, line 195 fetchBacklogView)
    - .planning/phases/74-backlog-on-data-json/74-PATTERNS.md §"services/jira/backlog.ts (MODIFY)"
  </read_first>
  <behavior>
    - Delete: `fetchBacklogIssues` (and its JSDoc), `fetchBacklogSprintStories` (and its JSDoc), `fetchBacklogView` (and its JSDoc).
    - Delete the `BacklogViewData` import name from line 8's `import type` block. If after removal the import line has no remaining named type imports, delete the entire import line; otherwise keep `JiraActiveSprint` / `JiraIssue` if still referenced by `fetchSprintList`.
    - Keep: `fetchSprintList` (lines ~83-118).
    - Result: `backlog.ts` contains only `fetchSprintList` and its supporting imports.
  </behavior>
  <action>
    Open `taskflow/src/services/jira/backlog.ts`. Delete the three function declarations + JSDoc blocks per the line ranges in <read_first>. Update the top `import type` block on line 8 to drop `BacklogViewData`; verify remaining type imports are still referenced by `fetchSprintList` before keeping. Save. Do NOT touch any other file in this task.
  </action>
  <acceptance_criteria>
    - `grep -cE "^export (async )?function (fetchBacklogIssues|fetchBacklogSprintStories|fetchBacklogView)" taskflow/src/services/jira/backlog.ts` returns 0.
    - `grep -cE "^export (async )?function fetchSprintList" taskflow/src/services/jira/backlog.ts` returns 1.
    - `grep -cE "BacklogViewData" taskflow/src/services/jira/backlog.ts` returns 0.
    - `cd taskflow && pnpm tsc --noEmit` exits 0.
    - `cd taskflow && pnpm biome check src/services/jira/backlog.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && pnpm tsc --noEmit && pnpm biome check src/services/jira/backlog.ts && (grep -cE "^export (async )?function (fetchBacklogIssues|fetchBacklogSprintStories|fetchBacklogView)" src/services/jira/backlog.ts | grep -qx 0) && (grep -cE "^export (async )?function fetchSprintList" src/services/jira/backlog.ts | grep -qx 1)</automated>
  </verify>
  <done>Legacy fetchers removed; fetchSprintList intact.</done>
</task>

<task type="auto">
  <name>Task 3: Delete legacy re-exports and BacklogViewData from services/jira.ts</name>
  <read_first>
    - taskflow/src/services/jira.ts (lines 2040-2200 — locate `fetchBacklogIssues` definition/re-export at ~2048, `BacklogViewData` interface at ~2149, `fetchBacklogView` definition/re-export at ~2177; planner verifies exact line numbers before edit)
    - .planning/phases/74-backlog-on-data-json/74-PATTERNS.md §"services/jira.ts (MODIFY)" Deletions section
  </read_first>
  <behavior>
    - Delete the `fetchBacklogIssues` function (or re-export) and its JSDoc in `jira.ts`.
    - Delete the `fetchBacklogSprintStories` function/re-export if present in `jira.ts`.
    - Delete the `BacklogViewData` interface declaration.
    - Delete the `fetchBacklogView` function (or re-export) and its JSDoc.
    - Any named re-export blocks (`export { fetchBacklogIssues } from './jira/backlog';` etc.) referencing the three doomed symbols are removed.
    - `fetchSprintList` re-export is KEPT.
    - `useGhBacklogData` / `getGhBacklogData` / `invalidateGhBacklogData` re-exports added in Plan 02 are UNTOUCHED.
  </behavior>
  <action>
    Open `taskflow/src/services/jira.ts`. Search for `fetchBacklogIssues`, `fetchBacklogSprintStories`, `fetchBacklogView`, and `BacklogViewData` to locate every declaration and re-export. Delete each per <behavior>. After deletion, run `pnpm tsc --noEmit` — any new error names a file the audit missed; fix by going back to Plan 03/04 and patching the missed call site (do NOT re-introduce the deleted symbol).
  </action>
  <acceptance_criteria>
    - `grep -cE "fetchBacklogIssues|fetchBacklogSprintStories|fetchBacklogView|BacklogViewData" taskflow/src/services/jira.ts` returns 0.
    - `grep -cE "fetchSprintList" taskflow/src/services/jira.ts` returns at least 1 (kept).
    - `grep -cE "useGhBacklogData|getGhBacklogData|invalidateGhBacklogData" taskflow/src/services/jira.ts` returns at least 3 (Plan 02 additions intact).
    - `cd taskflow && pnpm tsc --noEmit` exits 0.
    - `cd taskflow && pnpm biome check src/services/jira.ts` exits 0 (existing file-wide warnings out of scope, but the edited regions must be clean).
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && pnpm tsc --noEmit && (grep -cE "fetchBacklogIssues|fetchBacklogSprintStories|fetchBacklogView|BacklogViewData" src/services/jira.ts | grep -qx 0) && grep -q "fetchSprintList" src/services/jira.ts && grep -q "useGhBacklogData" src/services/jira.ts</automated>
  </verify>
  <done>Legacy backlog surface gone from dual-file jira.ts; new GH surface intact; fetchSprintList preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Activate static-grep guard via package.json script + full-suite green</name>
  <read_first>
    - taskflow/package.json (scripts block)
    - taskflow/scripts/check-legacy-backlog-keys.mjs (from Plan 01)
    - .planning/phases/74-backlog-on-data-json/74-VALIDATION.md §"Wave 0 Requirements" final bullet
  </read_first>
  <behavior>
    - `taskflow/package.json` gains a script entry `"check:legacy-backlog": "node scripts/check-legacy-backlog-keys.mjs"`.
    - Running `pnpm check:legacy-backlog` exits 0 (all four banned tokens absent from src/ now that Plans 02-06 deleted them).
    - Full `pnpm vitest run` GREEN.
    - `pnpm biome check .` returns 0 errors / 0 warnings (project baseline per memory [[project_biome_state]]).
    - `pnpm tsc --noEmit` exits 0.
  </behavior>
  <action>
    Edit `taskflow/package.json` scripts block: add the `check:legacy-backlog` entry alphabetically near other check scripts. Run the four commands listed in <behavior> in sequence; if any fail, surface the file:line and fix. Do NOT skip biome or vitest. The full-suite gate is mandatory here because this is the last plan in the wave.
  </action>
  <acceptance_criteria>
    - `grep -cE "check:legacy-backlog" taskflow/package.json` returns 1.
    - `cd taskflow && pnpm check:legacy-backlog` exits 0.
    - `cd taskflow && pnpm tsc --noEmit` exits 0.
    - `cd taskflow && pnpm vitest run --reporter=basic` exits 0 (FULL suite, not just plan-scoped tests).
    - `cd taskflow && pnpm biome check .` exits 0 with 0 errors and 0 warnings.
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && pnpm check:legacy-backlog && pnpm tsc --noEmit && pnpm vitest run --reporter=basic && pnpm biome check .</automated>
  </verify>
  <done>Static guard active; project baselines preserved; legacy backlog REST surface fully purged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source→CI | The grep guard is a build-time defense against accidental reintroduction of legacy symbols. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-13 | Tampering | Future PR re-introduces `fetchBacklogIssues` (e.g. revert, merge conflict) silently re-enabling the legacy REST path | mitigate | `check:legacy-backlog` script wired to package.json; CI / pre-commit invocation flags the file:line. |
| T-74-14 | Integrity | Deletion misses a re-export, leaving the symbol importable from `jira.ts` | mitigate | Task 3 grep gates on `services/jira.ts` return 0 for all four banned identifiers. |
| T-74-15 | Integrity | Deletion accidentally removes `fetchSprintList`, breaking FieldsSection sprint picker | mitigate | Task 1 grep audit confirms FieldsSection still imports it; Task 2 grep gate confirms `fetchSprintList` declaration remains. |
| T-74-SC | Tampering | npm/pnpm installs | mitigate | No new dependencies — guard is a vanilla Node ESM script using only `node:fs/promises` and `node:path`. |
</threat_model>

<verification>
- `cd taskflow && pnpm check:legacy-backlog` — exits 0.
- `cd taskflow && pnpm tsc --noEmit` — exits 0.
- `cd taskflow && pnpm vitest run --reporter=basic` — full suite GREEN.
- `cd taskflow && pnpm biome check .` — 0 errors / 0 warnings (project baseline).
- Manual: open backlog page; confirm move-to-sprint, create story, rank, filter-by-epic, filter-by-assignee still work end-to-end on the new data source (per GH-BACKLOG-02).
</verification>

<success_criteria>
- `fetchBacklogIssues`, `fetchBacklogSprintStories`, `fetchBacklogView`, `BacklogViewData` are deleted from both files.
- `fetchSprintList` is retained.
- Static-grep guard is executable via `pnpm check:legacy-backlog` and exits 0.
- Full project suite + lint + typecheck GREEN.
- GH-BACKLOG-01 and GH-BACKLOG-02 satisfied end-to-end across Plans 01-06.
</success_criteria>

<output>
Create `.planning/phases/74-backlog-on-data-json/74-06-SUMMARY.md` when done.
</output>
