---
phase: 74-backlog-on-data-json
plan: 03
type: execute
wave: 2
depends_on:
  - 74-02
files_modified:
  - taskflow/src/routes/dashboard/BacklogPage.tsx
autonomous: true
requirements:
  - GH-BACKLOG-01
  - GH-BACKLOG-02
must_haves:
  truths:
    - "Opening /backlog issues exactly one GET to /rest/greenhopper/1.0/xboard/plan/backlog/data.json and zero GETs to legacy backlog REST paths (Plan 01 BacklogPage.network.test.tsx flips GREEN)"
    - "Backlog list = adapted issues whose id is not referenced by any data.sprints[].issuesIds[] (D-01)"
    - "Sprint sections render in data.sprints[] order, filtered to state === 'ACTIVE' or 'FUTURE' uppercase (D-01a + RESEARCH A5)"
    - "Each adapted issue has fields.sprint = { id } iff issueId appears in any sprint's issuesIds[] (D-04b reverse-index)"
    - "All mutation handlers (move-to-sprint, move-to-backlog, create story, rank) keep their REST POSTs; only invalidation keys change — they call invalidateGhBacklogData(queryClient, boardId) and NO LONGER call invalidateQueries with ['jira-backlog-issues'], ['jira-backlog-sprint-stories'], or ['jira-sprint-list'] (D-06)"
    - "Optimistic updates mutate ['gh-backlog', boardId] cache's data.sprints[].issuesIds[] in place (D-06a + RESEARCH Pattern 3)"
    - "Label filter chip, subtask count chip, and flagged indicator are removed from the backlog surface (D-05a/b/c)"
    - "Per-section refetchBacklog/refetchStories callbacks are removed (D-07a); the toolbar Reload action shipped in Plan 05 will own all manual refreshes"
  artifacts:
    - path: taskflow/src/routes/dashboard/BacklogPage.tsx
      provides: "Rewritten data layer + adapter pass + mutation invalidation swap + dropped chips"
      contains: "useGhBacklogData"
  key_links:
    - from: taskflow/src/routes/dashboard/BacklogPage.tsx
      to: taskflow/src/services/jira.ts
      via: "named import from @/services/jira (D-09b)"
      pattern: "from '@/services/jira'"
    - from: taskflow/src/routes/dashboard/BacklogPage.tsx
      to: taskflow/src/services/jira/greenhopper/adapter.ts
      via: "buildEntityMaps + createAdapter call-site useMemo chain (Pattern S3)"
      pattern: "createAdapter"
---

<objective>
Rewrite BacklogPage's data layer: replace the three legacy queries (`jira-sprint-list`, `jira-backlog-sprint-stories`, `jira-backlog-issues`) with one `useGhBacklogData(boardId)` call, derive backlog + sprint sections via a useMemo chain (adapter + sprint reverse-index), and swap every mutation handler's invalidation keys to `invalidateGhBacklogData`. Drop label filter, subtask chip, and flagged indicator per D-05a/b/c. This plan flips Plan 01's `BacklogPage.network.test.tsx` from RED to GREEN and satisfies GH-BACKLOG-01 literally.

Purpose: The 3→1 request collapse is the central claim of GH-BACKLOG-01. Mutation invalidation parity is the central claim of GH-BACKLOG-02 (combined with the toolbar in Plan 05).
Output: One large rewrite to `BacklogPage.tsx` (data layer + mutation invalidations + chip removals). No new files.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/74-backlog-on-data-json/74-CONTEXT.md
@.planning/phases/74-backlog-on-data-json/74-RESEARCH.md
@.planning/phases/74-backlog-on-data-json/74-PATTERNS.md
@.planning/phases/74-backlog-on-data-json/74-UI-SPEC.md
@taskflow/src/routes/dashboard/BacklogPage.tsx
@taskflow/src/routes/dashboard/SprintBoardTab.tsx
@taskflow/src/services/jira/greenhopper/adapter.ts
@taskflow/src/services/jira/greenhopper/entityMaps.ts
@taskflow/src/services/jira/greenhopper/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace three useQuery blocks with useGhBacklogData + adapter chain</name>
  <read_first>
    - taskflow/src/routes/dashboard/BacklogPage.tsx (full — focus lines 20-48 imports, 220-310 the three queries + projectStatuses, 340-360 refetch callbacks, 590-810 mutation handlers, 820+ render of toolbar chips + virtualized list)
    - taskflow/src/routes/dashboard/SprintBoardTab.tsx (lines 41 imports, 610-642 hook + adapter useMemo chain — Pattern S3 source)
    - taskflow/src/services/jira/greenhopper/adapter.ts (createAdapter / adaptIssue / buildEntityMaps signatures)
    - taskflow/src/services/jira/greenhopper/types.ts (widened GhBacklogResponse + GhSprintBacklog from Plan 01)
    - .planning/phases/74-backlog-on-data-json/74-PATTERNS.md §"BacklogPage.tsx (REWRITE data layer)" — Hook + adapter useMemo chain + deletions
    - .planning/phases/74-backlog-on-data-json/74-RESEARCH.md §"Pattern 2: Sprint membership reverse-index" + §"Pitfall 5"
  </read_first>
  <behavior>
    - Imports: remove `fetchBacklogIssues`, `fetchBacklogSprintStories`, `fetchSprintList` from their existing import statements. Add named imports from `@/services/jira` (D-09b — NEVER from `@/services/jira/greenhopper`): `useGhBacklogData`, `buildEntityMaps`, `createAdapter`.
    - Delete the three `useQuery` blocks keyed on `['jira-sprint-list', ...]`, `['jira-backlog-sprint-stories', ...]`, `['jira-backlog-issues', ...]`.
    - Add: `const { data: backlog, isLoading: backlogLoading, isFetching: backlogFetching, isError, error } = useGhBacklogData(boardId ?? null);`
    - Add useMemo chain (entityMaps → adapt → issueIdToSprintId → adaptedIssues → backlogIssuesAdapted → sprintSections) per PATTERNS §"Hook + adapter useMemo chain".
    - `issueIdToSprintId`: `Map<number, number>` built from `backlog?.sprints ?? []` × `s.issuesIds`.
    - `adaptedIssues`: maps `backlog.issues` through `adapt(gh) as JiraIssue` then attaches `fields.sprint = { id: sprintId }` when `issueIdToSprintId.get(gh.id)` is defined.
    - `backlogIssuesAdapted`: filters adaptedIssues where `fields.sprint` is undefined.
    - `sprintSections`: filters `backlog.sprints` to `state === 'ACTIVE' || state === 'FUTURE'` (uppercase per RESEARCH A5), then maps to `{ sprint, issues: adaptedIssues.filter(i => i.fields.sprint?.id === s.id) }`.
    - Existing downstream render code consuming `backlogIssues` / `sprintStories` / `sprintList` variables is rewired to consume `backlogIssuesAdapted` / `sprintSections`. Render JSX shape is otherwise unchanged.
    - Delete `refetchBacklog` and `refetchStories` callbacks and any prop drilling of them; the Plan 05 toolbar will own reloads.
    - Loading + error states: drive from `backlogLoading || (!backlog && backlogFetching)` for initial-load skeleton; from `isError` for the error pane.
    - `fetchProjectStatuses` / `useQuery(['project-statuses', ...])` for the status-filter dropdown STAYS (D-09a + RESEARCH Open Question #2). `fetchEpicsBasic` / `['jira-epics-basic', ...]` STAYS for the epic-filter dropdown.
  </behavior>
  <action>
    Open `taskflow/src/routes/dashboard/BacklogPage.tsx`. Execute the rewrite per PATTERNS §"BacklogPage.tsx (REWRITE data layer)" in this order so the file compiles incrementally: (1) update imports — remove the three legacy fetchers, add the three new symbols from `@/services/jira`; (2) delete the three `useQuery` blocks for sprint-list / backlog-sprint-stories / backlog-issues; (3) insert the `useGhBacklogData(boardId ?? null)` call and the six `useMemo` derivations described above; (4) rewire every downstream consumer of the old variable names (search for any reference to `sprintList`, `sprintStories`, `backlogIssues` outside the new names and update to `sprintSections` / `backlogIssuesAdapted`); (5) delete `refetchBacklog`/`refetchStories` callback declarations and their prop wiring; (6) delete the label-filter chip UI (D-05a), the subtask count chip on backlog rows (D-05b) if currently rendered, and the flagged indicator on backlog rows (D-05c) if currently rendered. Mutation handlers stay in place in this task — Task 2 swaps their invalidation keys. Do NOT touch the toolbar Reload control yet — that ships in Plan 05.
  </action>
  <acceptance_criteria>
    - `grep -cE "useGhBacklogData" taskflow/src/routes/dashboard/BacklogPage.tsx` returns at least 1.
    - `grep -cE "createAdapter|buildEntityMaps" taskflow/src/routes/dashboard/BacklogPage.tsx` returns at least 2 (both imports present).
    - `grep -cE "fetchBacklogIssues|fetchBacklogSprintStories|fetchSprintList" taskflow/src/routes/dashboard/BacklogPage.tsx` returns 0 (legacy imports removed).
    - `grep -cE "state === 'ACTIVE'|state === 'FUTURE'" taskflow/src/routes/dashboard/BacklogPage.tsx` returns at least 1 (uppercase check).
    - `grep -cE "issueIdToSprintId" taskflow/src/routes/dashboard/BacklogPage.tsx` returns at least 2 (declared + used).
    - `cd taskflow && pnpm tsc --noEmit` exits 0.
    - `cd taskflow && pnpm vitest run src/routes/dashboard/__tests__/BacklogPage.network.test.tsx --reporter=basic` exits 0 (Plan 01 RED gate now GREEN).
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && pnpm tsc --noEmit && pnpm vitest run src/routes/dashboard/__tests__/BacklogPage.network.test.tsx --reporter=basic</automated>
  </verify>
  <done>Data layer rewired; exactly 1 data.json request on backlog open; legacy queryKeys removed from the file; label/subtask/flagged chips dropped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swap mutation invalidations + optimistic updates to single GH cache</name>
  <read_first>
    - taskflow/src/routes/dashboard/BacklogPage.tsx (after Task 1 — focus mutation handlers near lines 590-810: move-to-sprint, move-to-backlog, create story, rank/reorder, toggle flag if still present)
    - .planning/phases/74-backlog-on-data-json/74-PATTERNS.md §"Mutation invalidation swap" + §"Optimistic-update rewrite"
    - .planning/phases/74-backlog-on-data-json/74-RESEARCH.md §"Pattern 3: Optimistic update on the new single cache" + §"Pitfall 5"
    - .planning/phases/74-backlog-on-data-json/74-CONTEXT.md D-06 + D-06a
  </read_first>
  <behavior>
    - Every `queryClient.invalidateQueries({ queryKey: ['jira-backlog-issues', ...] })`, `['jira-backlog-sprint-stories', ...]`, `['jira-sprint-list', ...]` call site in mutation handlers is replaced by a single `invalidateGhBacklogData(queryClient, boardId)`.
    - Every `queryClient.setQueryData<JiraIssue[]>(['jira-backlog-issues', ...], ...)` and analogous setQueryData on the legacy keys is rewritten to mutate the unified `GhBacklogResponse` via `setQueryData<GhBacklogResponse>(['gh-backlog', boardId], (old) => ...)`:
      - move-to-sprint: shift `issueId` between `data.sprints[].issuesIds[]` (remove from any current sprint membership, add to the destination sprint's `issuesIds[]`).
      - move-to-backlog: remove `issueId` from every sprint's `issuesIds[]`.
      - rank/reorder within the backlog: reorder elements in `data.issues[]` (or — if simpler — rely on invalidation refetch for reorder; planner discretion per RESEARCH Open Question #1; default = refetch only, no optimistic reorder, because rank persistence shape is unchanged from the REST POST).
      - create story: skip optimistic; rely on invalidation refetch (RESEARCH Open Question #1 recommendation).
    - `invalidateGhBacklogData` is imported from `@/services/jira` (D-09b).
    - Keep any unrelated invalidations the handlers also fire (e.g. `['jira-issue-detail', ...]` for cross-surface freshness) — only the three legacy backlog keys are stripped.
    - If a `handleToggleFlag` exists and depended on backlog-cache mutation, remove the backlog-cache-flag mutation (D-05c — flagged indicator dropped). If `handleToggleFlag` also services non-backlog surfaces (issue-detail), keep that path; otherwise delete the whole handler.
  </behavior>
  <action>
    In `BacklogPage.tsx`, locate every `invalidateQueries` and `setQueryData` call referencing the three legacy keys (`'jira-backlog-issues'`, `'jira-backlog-sprint-stories'`, `'jira-sprint-list'`). For each invalidation: replace with `invalidateGhBacklogData(queryClient, boardId);` (one call covers all backlog freshness — single cache). For each setQueryData: rewrite per the bullets in <behavior> above, operating on `(old: GhBacklogResponse | undefined)` and spreading `{ ...old, sprints: old.sprints.map(...) }` or `{ ...old, issues: old.issues.filter(...) }` as appropriate. Add the import `invalidateGhBacklogData` to the named-import block from `@/services/jira` if not already added by Task 1. Audit with the grep gates below; any residual hit means a missed site.
  </action>
  <acceptance_criteria>
    - `grep -cE "invalidateGhBacklogData" taskflow/src/routes/dashboard/BacklogPage.tsx` returns at least 1 (import) plus at least one per mutation handler (move-to-sprint, move-to-backlog, create story, rank — minimum total 4 across import + uses).
    - `grep -cE "'jira-backlog-issues'|'jira-backlog-sprint-stories'|'jira-sprint-list'" taskflow/src/routes/dashboard/BacklogPage.tsx` returns 0 (no raw matches — legacy keys must not appear even in comments).
    - `grep -cE "setQueryData<GhBacklogResponse>" taskflow/src/routes/dashboard/BacklogPage.tsx` returns at least 1 (at minimum the move-to-sprint optimistic update).
    - `cd taskflow && pnpm tsc --noEmit` exits 0.
    - `cd taskflow && pnpm vitest run src/routes/dashboard/__tests__/BacklogPage.network.test.tsx --reporter=basic` exits 0 (still GREEN).
    - `cd taskflow && pnpm vitest run src/routes/dashboard/__tests__ --reporter=basic` exits 0 (all BacklogPage-scoped tests pass).
    - `cd taskflow && pnpm biome check src/routes/dashboard/BacklogPage.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && pnpm tsc --noEmit && pnpm biome check src/routes/dashboard/BacklogPage.tsx && pnpm vitest run src/routes/dashboard/__tests__ --reporter=basic && (grep -cE "'jira-backlog-issues'|'jira-backlog-sprint-stories'|'jira-sprint-list'" src/routes/dashboard/BacklogPage.tsx | grep -qx 0)</automated>
  </verify>
  <done>Every mutation invalidates the single GH cache; legacy keys absent from BacklogPage; optimistic updates operate on `['gh-backlog', boardId]`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→GH API | One read endpoint (`/data.json`) replaces three legacy read endpoints. Token surface unchanged. |
| user→mutations | Move-to-sprint, create-story, rank still POST through the existing REST mutation handlers (D-06). No new write surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-06 | Tampering | Optimistic update writes to the wrong cache key (Pitfall 5) silently desyncing UI from server state | mitigate | Acceptance criterion greps for ZERO live references to the three legacy keys in `BacklogPage.tsx` (comments excluded); all optimistic updates funnel through `setQueryData<GhBacklogResponse>(['gh-backlog', boardId], …)`. |
| T-74-07 | Integrity | Adapter mis-maps `epicId` / `statusId` resulting in mis-grouped backlog rows | mitigate | Reuse Phase 71 `adaptIssue` (already covered by adapter tests + extended by Plan 01 adapter-backlog.test.ts); warnOnce surfaces lookup misses. |
| T-74-08 | Information Disclosure | Removed label/subtask/flagged surfaces could leak through stale memoization | accept | Components literally removed from JSX; no conditional render path remains. UAT confirms (Plan 05 manual checks). |
| T-74-SC | Tampering | npm/pnpm installs | mitigate | No new dependencies — all imports resolve to in-repo modules introduced in Plans 01-02. |
</threat_model>

<verification>
- `cd taskflow && pnpm vitest run src/routes/dashboard/__tests__/BacklogPage.network.test.tsx --reporter=basic` — GREEN (1 data.json call, 0 legacy backlog REST per RESEARCH Validation Architecture).
- `cd taskflow && pnpm vitest run --reporter=basic` — full suite GREEN.
- `cd taskflow && pnpm tsc --noEmit` — exits 0.
- `cd taskflow && pnpm biome check src/routes/dashboard/BacklogPage.tsx` — 0 errors / 0 warnings.
- Plan 01's other GREEN-from-day-one tests (types-fixture, useGhBacklogData) still GREEN.
</verification>

<success_criteria>
- BacklogPage reads from a single `['gh-backlog', boardId]` cache.
- All mutations invalidate via `invalidateGhBacklogData` only; legacy backlog keys absent from the file.
- Label / subtask / flagged surfaces removed.
- Plan 01 BacklogPage.network.test.tsx flips GREEN.
</success_criteria>

<output>
Create `.planning/phases/74-backlog-on-data-json/74-03-SUMMARY.md` when done.
</output>
