---
phase: 74-backlog-on-data-json
plan: 05
type: execute
wave: 3
depends_on:
  - 74-03
files_modified:
  - taskflow/src/routes/dashboard/BacklogPage.tsx
autonomous: false
requirements:
  - GH-BACKLOG-02
user_setup: []
must_haves:
  truths:
    - "Backlog toolbar exposes a single 'Reload backlog' control (Lucide RefreshCw icon, secondary styling) per UI-SPEC §'Interaction Contract'"
    - "Click invalidates three keys per D-07: ['gh-backlog', boardId], ['jira-epics-basic', projectKey], and the project-statuses key tuple already used by BacklogPage's status filter dropdown"
    - "While reloading, button is disabled and icon spins (animate-spin); aria-live='polite' status region announces 'Backlog reloaded' or 'Failed to reload backlog' and auto-clears after 3000ms"
    - "Any pre-existing per-section refresh affordances are removed (D-07a) — this control owns all backlog reloads"
    - "Copy verbatim from 74-UI-SPEC.md §Copywriting Contract"
  artifacts:
    - path: taskflow/src/routes/dashboard/BacklogPage.tsx
      provides: "Reload backlog toolbar action + status region"
      contains: "Reload backlog"
  key_links:
    - from: taskflow/src/routes/dashboard/BacklogPage.tsx
      to: taskflow/src/services/jira.ts
      via: "invalidateGhBacklogData import (already present from Plan 03)"
      pattern: "invalidateGhBacklogData"
---

<objective>
Add the "Reload backlog" toolbar action per 74-UI-SPEC and D-07. Mirrors Phase 73's "Reload board" surface in `SprintBoardTab.tsx`. Includes a human-verify checkpoint because UI placement, spinner state, and aria-live announcement are visual behaviors not fully covered by automated tests.

Purpose: Manual reload is a baseline expectation; per-section refresh callbacks were removed in Plan 03, so this control owns the surface.
Output: One handler + button + status region inside `BacklogPage.tsx`. No new files.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-backlog-on-data-json/74-CONTEXT.md
@.planning/phases/74-backlog-on-data-json/74-UI-SPEC.md
@.planning/phases/74-backlog-on-data-json/74-PATTERNS.md
@taskflow/src/routes/dashboard/SprintBoardTab.tsx
@taskflow/src/routes/dashboard/BacklogPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement handleReloadBacklog + state + 3s auto-clear effect</name>
  <read_first>
    - taskflow/src/routes/dashboard/SprintBoardTab.tsx (lines 776-810 — verbatim model: handler + status state + auto-clear useEffect)
    - taskflow/src/routes/dashboard/BacklogPage.tsx (after Plan 03 — find the toolbar JSX, identify the existing project-statuses useQuery cache-key tuple and the existing epic-filter useQuery key tuple, both of which the handler will reference verbatim)
    - .planning/phases/74-backlog-on-data-json/74-PATTERNS.md §"Reload backlog toolbar action" + §"Pattern S5"
    - .planning/phases/74-backlog-on-data-json/74-UI-SPEC.md §"Interaction Contract" + §"Copywriting Contract"
  </read_first>
  <behavior>
    - Local state: `reloadStatus: string | null`, initialized null.
    - Effect: when `reloadStatus` non-null, setTimeout 3000ms to setReloadStatus(null); cleanup clears timeout.
    - Handler `handleReloadBacklog`: async; if boardId truthy calls `invalidateGhBacklogData(queryClient, boardId)`; then awaits `queryClient.invalidateQueries({ queryKey: <projectStatusesKey> })` and `queryClient.invalidateQueries({ queryKey: ['jira-epics-basic', activeJiraProject, jiraBaseUrl] })`. try/catch: success `setReloadStatus('Backlog reloaded')`; failure `setReloadStatus('Failed to reload backlog')`.
    - `<projectStatusesKey>` is read verbatim from the existing BacklogPage status-filter useQuery (executor does not invent a new tuple).
    - No `gh-transitions`, `jira-board-quickfilters`, or `jira-active-sprint` invalidation (board-only, not backlog).
  </behavior>
  <action>
    In `BacklogPage.tsx`, add `const [reloadStatus, setReloadStatus] = useState<string | null>(null);` near sibling state. Add a `useEffect` clearing `reloadStatus` after 3000ms (cleanup function clears the timeout). Add `const handleReloadBacklog = useCallback(async () => { try { if (boardId) invalidateGhBacklogData(queryClient, boardId); await queryClient.invalidateQueries({ queryKey: <projectStatusesKey copied verbatim> }); await queryClient.invalidateQueries({ queryKey: ['jira-epics-basic', activeJiraProject, jiraBaseUrl] }); setReloadStatus('Backlog reloaded'); } catch { setReloadStatus('Failed to reload backlog'); } }, [queryClient, boardId, activeJiraProject, jiraBaseUrl]);`. Strings copied character-for-character from 74-UI-SPEC.md.
  </action>
  <acceptance_criteria>
    - `grep -cE "handleReloadBacklog" taskflow/src/routes/dashboard/BacklogPage.tsx` returns at least 2 (declaration + later button onClick reference; Task 2 adds the button).
    - `grep -cE "Backlog reloaded" taskflow/src/routes/dashboard/BacklogPage.tsx` returns at least 1.
    - `grep -cE "Failed to reload backlog" taskflow/src/routes/dashboard/BacklogPage.tsx` returns at least 1.
    - `cd taskflow && pnpm tsc --noEmit` exits 0.
    - `cd taskflow && pnpm biome check src/routes/dashboard/BacklogPage.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && pnpm tsc --noEmit && pnpm biome check src/routes/dashboard/BacklogPage.tsx && grep -q "Backlog reloaded" src/routes/dashboard/BacklogPage.tsx && grep -q "Failed to reload backlog" src/routes/dashboard/BacklogPage.tsx</automated>
  </verify>
  <done>Handler + state + auto-clear effect live; copy strings present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add toolbar button + aria-live status region; remove leftover per-section refresh affordances</name>
  <read_first>
    - taskflow/src/routes/dashboard/SprintBoardTab.tsx (lines 1186-1207 — verbatim JSX model for the button + aria-live sr-only span)
    - taskflow/src/routes/dashboard/BacklogPage.tsx (after Task 1 — locate the toolbar JSX region; identify any residual refresh affordances on per-section headers that escaped Plan 03 and remove them)
    - .planning/phases/74-backlog-on-data-json/74-UI-SPEC.md §"Interaction Contract"
  </read_first>
  <behavior>
    - Toolbar JSX gains a button with: `aria-label="Reload backlog"`, `title="Reload backlog"`, secondary styling (text-xs text-muted-foreground hover:text-foreground), disabled when `backlogFetching`, onClick `() => { void handleReloadBacklog(); }`.
    - Icon: `<RefreshCw className={backlogFetching ? 'size-3 animate-spin' : 'size-3'} />` from `lucide-react`.
    - Adjacent: `<span aria-live="polite" className="sr-only">{reloadStatus ?? ''}</span>`.
    - Any remaining per-section refresh buttons or icons inside sprint section headers / backlog section header are removed (D-07a).
    - Button placement mirrors the SprintBoardTab "Reload board" relative slot (UI-SPEC §"Interaction Contract").
  </behavior>
  <action>
    Open the toolbar JSX in `BacklogPage.tsx`. Verify `RefreshCw` is imported from `lucide-react` (add to existing import if missing). Insert the aria-live span and the button per <behavior>. Grep the file for any leftover refresh affordances — patterns to remove if present: occurrences of `refetchBacklog()` / `refetchStories()` JSX handlers that survived Plan 03, any per-sprint-section RefreshCw button, any "Reload" / "Refresh" string in section headers. Visual styling follows the SprintBoardTab.tsx:1186-1207 secondary-icon-button convention; do NOT introduce primary accent styling.
  </action>
  <acceptance_criteria>
    - `grep -cE "RefreshCw" taskflow/src/routes/dashboard/BacklogPage.tsx` returns at least 2 (import + render).
    - `grep -cE "aria-label=\"Reload backlog\"" taskflow/src/routes/dashboard/BacklogPage.tsx` returns at least 1.
    - `grep -cE "aria-live=\"polite\"" taskflow/src/routes/dashboard/BacklogPage.tsx` returns at least 1.
    - `grep -cE "refetchBacklog|refetchStories" taskflow/src/routes/dashboard/BacklogPage.tsx` returns 0 (all per-section refresh callbacks fully removed).
    - `cd taskflow && pnpm tsc --noEmit` exits 0.
    - `cd taskflow && pnpm biome check src/routes/dashboard/BacklogPage.tsx` exits 0.
    - `cd taskflow && pnpm vitest run src/routes/dashboard/__tests__ --reporter=basic` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && pnpm tsc --noEmit && pnpm biome check src/routes/dashboard/BacklogPage.tsx && pnpm vitest run src/routes/dashboard/__tests__ --reporter=basic && (grep -cE "refetchBacklog|refetchStories" src/routes/dashboard/BacklogPage.tsx | grep -qx 0)</automated>
  </verify>
  <done>Button visible in toolbar, status region wired, per-section refresh callbacks fully purged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Reload backlog toolbar UX</name>
  <what-built>Reload backlog toolbar action — single icon button in the backlog toolbar that invalidates the GH backlog cache + epic-filter cache + project-statuses cache, shows a spinning RefreshCw icon while reloading, and announces success/failure into an aria-live region that auto-clears after 3 seconds.</what-built>
  <how-to-verify>
    1. `cd taskflow && pnpm dev` — start the app.
    2. Open the backlog page (`/backlog`).
    3. Locate the "Reload backlog" icon button in the toolbar — it should sit in the same relative position the "Reload board" button occupies on the sprint board page (Phase 73 precedent). Hovering should show the "Reload backlog" tooltip.
    4. Click the button. Expected behavior:
       - Icon starts spinning (Tailwind animate-spin).
       - Button is disabled during reload (cursor / aria-disabled).
       - On success, a screen-reader-only status announces "Backlog reloaded"; the toast/visible UI is not required, but a real screen-reader test (VoiceOver/NVDA) confirms the announcement.
       - After ~3 seconds, status clears (no visible state change for sighted users; aria-live region empties).
    5. Confirm the label-filter chip, subtask count chip on backlog rows, and flagged indicator on backlog rows are absent (D-05a/b/c).
    6. Confirm no per-section "Refresh" / reload buttons remain on sprint section headers or the backlog section header.
    7. Open DevTools Network tab, clear it, refresh `/backlog`. Confirm exactly one GET to `/rest/greenhopper/1.0/xboard/plan/backlog/data.json` and zero GETs to `/rest/agile/1.0/board/*/backlog`, `/rest/agile/1.0/board/*/sprint`, or any URL matching `jira-backlog-issues` / `jira-backlog-sprint-stories`.
    8. Click "Reload backlog" again and confirm one additional `data.json` GET fires, plus the project-statuses and epic-basic fetches.
    9. Failure path: temporarily edit the GH endpoint to invalid URL in DevTools throttling or block via DevTools Network tab → click reload → confirm "Failed to reload backlog" is announced.
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 checks pass, or describe issues (placement wrong, copy off, spinner state buggy, network log shows extra calls, etc.).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→UI | Click handler triggers cache invalidation only; no new write path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-11 | Denial of Service | Rapid-fire reload clicks flood the GH API | mitigate | Button disabled while `backlogFetching` true. React Query also dedupes inflight requests on the same key. |
| T-74-12 | Repudiation | Failure not visible to sighted users | accept | aria-live status region announces both states; visible toast is out of scope per UI-SPEC. |
| T-74-SC | Tampering | npm/pnpm installs | mitigate | No new dependencies — `RefreshCw` already imported by sibling SprintBoardTab from existing `lucide-react`. |
</threat_model>

<verification>
- `cd taskflow && pnpm tsc --noEmit` — exits 0.
- `cd taskflow && pnpm biome check src/routes/dashboard/BacklogPage.tsx` — 0 errors / 0 warnings.
- `cd taskflow && pnpm vitest run --reporter=basic` — full suite GREEN.
- Human-verify Task 3 returns "approved".
</verification>

<success_criteria>
- One Reload backlog button in toolbar, secondary styling, matching UI-SPEC copy verbatim.
- aria-live polite status region announces success/failure and auto-clears.
- No per-section refresh affordances remain.
- Human-verify checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/74-backlog-on-data-json/74-05-SUMMARY.md` when done.
</output>
