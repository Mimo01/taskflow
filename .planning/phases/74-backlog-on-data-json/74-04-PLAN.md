---
phase: 74-backlog-on-data-json
plan: 04
type: execute
wave: 2
depends_on:
  - 74-02
files_modified:
  - taskflow/src/components/app/Sidebar.tsx
autonomous: true
requirements:
  - GH-BACKLOG-01
must_haves:
  truths:
    - "Hovering / mounting the Sidebar with /backlog as the active or hovered route issues exactly one prefetch chain: fetchBoardId then getGhBacklogData(qc, baseUrl, token, boardId) — collapsing the prior three legacy prefetches (D-08)"
    - "The warmed cache key matches what BacklogPage reads: ['gh-backlog', boardId] (Plan 01 Sidebar.prefetch.test.tsx flips GREEN)"
    - "When boardId is null/unknown, prefetch silently skips (D-08a) — no fetch is issued and no error toast surfaces"
    - "Sidebar no longer imports fetchBacklogIssues, fetchBacklogSprintStories, or fetchSprintList"
  artifacts:
    - path: taskflow/src/components/app/Sidebar.tsx
      provides: "Collapsed /backlog prefetch branch matching the /sprint-board precedent"
      contains: "getGhBacklogData"
  key_links:
    - from: taskflow/src/components/app/Sidebar.tsx
      to: taskflow/src/services/jira.ts
      via: "named import (D-09b)"
      pattern: "getGhBacklogData"
---

<objective>
Collapse the Sidebar's `/backlog` prefetch chain from three legacy fetchers to a single `getGhBacklogData(boardId)` warm-up. Direct port of the Phase 73 `/sprint-board` branch already present in the same file. Flips Plan 01's `Sidebar.prefetch.test.tsx` from RED to GREEN.

Purpose: The warmed cache key MUST match what BacklogPage reads after Plan 03; mismatched keys make the prefetch dead weight (Pitfall 4).
Output: Modified `Sidebar.tsx` only — imports tightened, `/backlog` block rewritten.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/74-backlog-on-data-json/74-CONTEXT.md
@.planning/phases/74-backlog-on-data-json/74-RESEARCH.md
@.planning/phases/74-backlog-on-data-json/74-PATTERNS.md
@taskflow/src/components/app/Sidebar.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse /backlog prefetch chain to single getGhBacklogData call</name>
  <read_first>
    - taskflow/src/components/app/Sidebar.tsx (full — focus lines 32-43 imports, 127-142 the /sprint-board precedent branch, 179-246 the current /backlog branch to rewrite)
    - .planning/phases/74-backlog-on-data-json/74-PATTERNS.md §"Sidebar.tsx (MODIFY)" + §"Pattern S4: Sidebar prefetch warm — boardId chain"
    - .planning/phases/74-backlog-on-data-json/74-CONTEXT.md D-08 + D-08a
    - .planning/phases/74-backlog-on-data-json/74-RESEARCH.md §"Pitfall 4"
  </read_first>
  <behavior>
    - Imports: remove the named-import block `{ fetchBacklogIssues, fetchBacklogSprintStories, fetchSprintList } from '@/services/jira/backlog'`. Add `getGhBacklogData` to the existing named-import block from `@/services/jira` (alongside the Phase 73 `getGhAllData`).
    - `/backlog` prefetch branch (current lines ~181-200 plus the chained 203-245 fetchBoardId → fetchSprintList → fetchBacklogSprintStories) is replaced verbatim by the same shape as the file's existing `/sprint-board` branch (lines ~131-141), swapping `getGhAllData` for `getGhBacklogData`.
    - The branch calls `queryClient.fetchQuery({ queryKey: ['jira-board-id', activeJiraProject, jiraBaseUrl], queryFn: () => fetchBoardId(jiraBaseUrl, jiraToken, activeJiraProject), staleTime: Infinity })`, then `.then(boardId => { if (boardId == null) return; return getGhBacklogData(queryClient, jiraBaseUrl, jiraToken, boardId); }).catch(() => {})`.
    - The surrounding `jira-epics-basic` prefetch (currently at lines ~167-178) STAYS — backlog still uses it for the epic-filter dropdown's full project epic list (RESEARCH Open Question #2).
    - No change to any other route branch (`/sprint-board`, `/standup`, etc.).
  </behavior>
  <action>
    Open `taskflow/src/components/app/Sidebar.tsx`. Update the import block at lines 32-43: delete the import statement bringing in `fetchBacklogIssues`, `fetchBacklogSprintStories`, `fetchSprintList` (planner verifies the exact statement before edit — it may be a single named-import line or split across multiple). Add `getGhBacklogData` to the existing `from '@/services/jira'` import group (alphabetical placement matches sibling `getGhAllData`). Replace the `if (path === '/backlog') { ... }` block (currently spanning ~lines 179-246) with the Pattern S4 skeleton: `fetchBoardId` → `getGhBacklogData(queryClient, jiraBaseUrl, jiraToken, boardId)` with the `boardId == null` guard and `.catch(() => {})` silent-failure. Leave the `jira-epics-basic` prefetch immediately above untouched. Do NOT touch other route branches.
  </action>
  <acceptance_criteria>
    - `grep -cE "fetchBacklogIssues|fetchBacklogSprintStories" taskflow/src/components/app/Sidebar.tsx` returns 0.
    - `grep -cE "from '@/services/jira/backlog'" taskflow/src/components/app/Sidebar.tsx` returns 0 (the legacy import path removed).
    - `grep -cE "getGhBacklogData" taskflow/src/components/app/Sidebar.tsx` returns at least 1 (the prefetch call).
    - `grep -cE "boardId == null" taskflow/src/components/app/Sidebar.tsx` returns at least 1 (D-08a guard preserved — may match the /sprint-board branch too).
    - `cd taskflow && pnpm tsc --noEmit` exits 0.
    - `cd taskflow && pnpm biome check src/components/app/Sidebar.tsx` exits 0.
    - `cd taskflow && pnpm vitest run src/components/app/__tests__/Sidebar.prefetch.test.tsx --reporter=basic` exits 0 (Plan 01 RED gate now GREEN).
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && pnpm tsc --noEmit && pnpm biome check src/components/app/Sidebar.tsx && pnpm vitest run src/components/app/__tests__/Sidebar.prefetch.test.tsx --reporter=basic</automated>
  </verify>
  <done>Three legacy prefetches collapsed to one GH call; Plan 01 Sidebar.prefetch.test.tsx GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→GH API | Prefetch warms the same `['gh-backlog', boardId]` cache the page reads — no extra network call when boardId is known. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-09 | Denial of Service | Prefetch fires with `boardId == null`, polluting cache with a key the page never reads (Pitfall 4) | mitigate | Early return guard `if (boardId == null) return;` per D-08a, copied verbatim from the Phase 73 `/sprint-board` branch. |
| T-74-10 | Information Disclosure | Sidebar's `.catch(() => {})` silently swallows auth failures | accept | Phase 73 precedent; prefetch is best-effort, page-level handling owns the user-visible error path. Not a regression. |
| T-74-SC | Tampering | npm/pnpm installs | mitigate | No new dependencies — `getGhBacklogData` resolves through existing barrel. |
</threat_model>

<verification>
- `cd taskflow && pnpm vitest run src/components/app/__tests__/Sidebar.prefetch.test.tsx --reporter=basic` — GREEN.
- `cd taskflow && pnpm tsc --noEmit` — exits 0.
- `cd taskflow && pnpm biome check src/components/app/Sidebar.tsx` — 0 errors / 0 warnings.
- Manual: hover the backlog nav link in DevTools Network panel — single `data.json` GET; the previous burst of three legacy GETs no longer appears.
</verification>

<success_criteria>
- Sidebar `/backlog` branch executes exactly one chained prefetch via `getGhBacklogData`.
- Legacy fetcher imports removed from Sidebar.
- Plan 01 Sidebar.prefetch.test.tsx GREEN.
</success_criteria>

<output>
Create `.planning/phases/74-backlog-on-data-json/74-04-SUMMARY.md` when done.
</output>
