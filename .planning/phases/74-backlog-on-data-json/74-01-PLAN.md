---
phase: 74-backlog-on-data-json
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - taskflow/src/services/jira/greenhopper/types.ts
  - taskflow/src/services/jira/greenhopper/__tests__/types-fixture.test.ts
  - taskflow/src/services/jira/greenhopper/__tests__/useGhBacklogData.test.tsx
  - taskflow/src/services/jira/greenhopper/__tests__/adapter-backlog.test.ts
  - taskflow/src/routes/dashboard/__tests__/BacklogPage.network.test.tsx
  - taskflow/src/components/app/__tests__/Sidebar.prefetch.test.tsx
  - taskflow/scripts/check-legacy-backlog-keys.mjs
autonomous: true
requirements:
  - GH-BACKLOG-01
  - GH-BACKLOG-02
must_haves:
  truths:
    - "GhBacklogResponse type pins to the real fixture envelope (entityData, sprints[].issuesIds, rankCustomFieldId, projects, versionData, canManageSprints, canCreateIssue, supportsPages, cardColorStrategy, emptyFilterBoard, hasBulkChangePermission, issueArchivingEnabled) per D-04a"
    - "All six Wave 0 scaffolds in 74-VALIDATION.md exist on disk and fail loudly (red) before any production code in later plans"
    - "scripts/check-legacy-backlog-keys.mjs exits non-zero when ['jira-backlog-issues' / ['jira-backlog-sprint-stories' / fetchBacklogIssues / fetchBacklogSprintStories appear in src/"
  artifacts:
    - path: taskflow/src/services/jira/greenhopper/types.ts
      provides: "Widened GhBacklogResponse + GhSprintBacklog interface"
      contains: "export interface GhSprintBacklog"
    - path: taskflow/src/services/jira/greenhopper/__tests__/types-fixture.test.ts
      provides: "Compile + runtime pin of GhBacklogResponse to data.real.json"
    - path: taskflow/src/services/jira/greenhopper/__tests__/useGhBacklogData.test.tsx
      provides: "RED hook test suite (will go GREEN in Plan 02)"
    - path: taskflow/src/services/jira/greenhopper/__tests__/adapter-backlog.test.ts
      provides: "Backlog-shape adapter case + sprint reverse-index assertion"
    - path: taskflow/src/routes/dashboard/__tests__/BacklogPage.network.test.tsx
      provides: "RED network-call invariant gate (1 data.json, 0 legacy REST)"
    - path: taskflow/src/components/app/__tests__/Sidebar.prefetch.test.tsx
      provides: "RED prefetch-collapse assertion (1 GH call, not 3 REST)"
    - path: taskflow/scripts/check-legacy-backlog-keys.mjs
      provides: "Static-grep guard preventing reintroduction of legacy backlog symbols"
  key_links:
    - from: taskflow/src/services/jira/greenhopper/types.ts
      to: taskflow/src/services/jira/greenhopper/__fixtures__/data.real.json
      via: "type-level cast in types-fixture.test.ts"
      pattern: "fixture as unknown as GhBacklogResponse"
---

<objective>
Wave 0 scaffolding for Phase 74. Widen `GhBacklogResponse` (D-04a) so downstream call sites can type-resolve `data.entityData` / `data.sprints[].issuesIds`, and lay down the six RED test fixtures listed in 74-VALIDATION.md. Every later plan compiles against this widened type and pushes a RED test to GREEN.

Purpose: Without the widened type, Plans 02-05 cannot compile. Without the RED tests, Nyquist sampling has no failing baseline to flip to GREEN per wave.
Output: Modified `types.ts`, five new test files under `__tests__/`, and one static-grep script under `scripts/`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-backlog-on-data-json/74-CONTEXT.md
@.planning/phases/74-backlog-on-data-json/74-RESEARCH.md
@.planning/phases/74-backlog-on-data-json/74-PATTERNS.md
@.planning/phases/74-backlog-on-data-json/74-VALIDATION.md
@taskflow/src/services/jira/greenhopper/types.ts
@taskflow/src/services/jira/greenhopper/useGhAllData.test.ts
@taskflow/src/services/jira/greenhopper/__fixtures__/data.real.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen GhBacklogResponse and add GhSprintBacklog (D-04a)</name>
  <read_first>
    - taskflow/src/services/jira/greenhopper/types.ts (full, focus lines 125-167 for GhAllDataResponse + GhBacklogResponse)
    - taskflow/src/services/jira/greenhopper/__fixtures__/data.real.json (envelope tail, lines 6674-8197 — entityData, sprints, rankCustomFieldId, versionData, projects, top-level flags)
    - .planning/phases/74-backlog-on-data-json/74-RESEARCH.md §"Code Examples — Widening GhBacklogResponse" + §"Assumptions Log A3, A5, A6"
    - .planning/phases/74-backlog-on-data-json/74-PATTERNS.md §"types.ts (MODIFY — widen GhBacklogResponse)"
  </read_first>
  <behavior>
    - GhBacklogResponse declares: issues, entityData (reused from GhAllDataResponse['entityData']), rankCustomFieldId, sprints (GhSprintBacklog[]), supportsPages, projects, canManageSprints, canCreateIssue, versionData (versionsPerProject + canCreateVersion + isLinkToDevStatusVersionAvailable), hasBulkChangePermission, issueArchivingEnabled, emptyFilterBoard, cardColorStrategy
    - GhSprintBacklog includes: id, sequence, rapidViewId, name, state ('ACTIVE' | 'CLOSED' | 'FUTURE'), autoStartStop, synced, startDate, endDate, activatedDate, completeDate, canUpdateSprint, canStartStopSprint, canUpdateDates, remoteLinks (unknown[]), daysRemaining, timeRemaining? ({ text, isFuture }), goal?, issuesIds (number[])
    - The misleading file comment "does NOT carry entity maps" is removed.
    - Existing GhBoardIssue / GhAllDataResponse / GhIssue declarations are unchanged in this task.
  </behavior>
  <action>
    Replace the existing `GhBacklogResponse` interface block (and its preceding JSDoc) in `taskflow/src/services/jira/greenhopper/types.ts` per D-04a. Add a new exported `GhSprintBacklog` interface immediately above the widened `GhBacklogResponse`. Reuse `GhAllDataResponse['entityData']` as the `entityData` field type (do not redeclare the entity-map sub-shape — research A3 confirms structural equality). Sprint `state` is an uppercase string literal union (`'ACTIVE' | 'CLOSED' | 'FUTURE'`) per research A5; sprint `issuesIds` is `number[]` per research A2. Delete the outdated JSDoc clause claiming "does NOT carry entity maps". Do NOT modify any other type in this file. Do NOT add runtime code.
  </action>
  <acceptance_criteria>
    - `grep -nE "does NOT carry entity maps" taskflow/src/services/jira/greenhopper/types.ts` returns exit code 1 (no matches).
    - `grep -nE "export interface GhSprintBacklog" taskflow/src/services/jira/greenhopper/types.ts` returns exactly one hit.
    - `grep -nE "issuesIds: number\\[\\]" taskflow/src/services/jira/greenhopper/types.ts` returns at least one hit (inside GhSprintBacklog).
    - `grep -nE "entityData: GhAllDataResponse\\['entityData'\\]" taskflow/src/services/jira/greenhopper/types.ts` returns exactly one hit (inside GhBacklogResponse).
    - `cd taskflow && pnpm tsc --noEmit` exits 0 (no new type errors introduced).
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && pnpm tsc --noEmit</automated>
  </verify>
  <done>GhBacklogResponse + GhSprintBacklog reflect the real fixture envelope; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: types-fixture.test.ts pins GhBacklogResponse to data.real.json</name>
  <read_first>
    - taskflow/src/services/jira/greenhopper/types.ts (after Task 1's widening)
    - taskflow/src/services/jira/greenhopper/__fixtures__/data.real.json (envelope shape, top-level keys)
    - taskflow/src/services/jira/greenhopper/allData.test.ts (existing vitest import style + JSON import precedent)
    - .planning/phases/74-backlog-on-data-json/74-RESEARCH.md §"Code Examples — Type test loading the real fixture"
  </read_first>
  <behavior>
    - Test "GhBacklogResponse is structurally compatible with the real fixture" loads the JSON, casts to GhBacklogResponse, and asserts: entityData.statuses defined; every sprint has Array.isArray(issuesIds) true; typeof rankCustomFieldId === 'number'; canManageSprints + canCreateIssue are booleans; versionData.versionsPerProject is an object.
    - Test fails (red) if any of those fields are missing post-widening (regression gate).
  </behavior>
  <action>
    Create `taskflow/src/services/jira/greenhopper/__tests__/types-fixture.test.ts`. Import fixture via `import fixture from '../__fixtures__/data.real.json'` (vitest + tsconfig support resolveJsonModule per existing allData.test.ts pattern). Import the `GhBacklogResponse` type from `../types`. Single `describe('GhBacklogResponse fixture pin', ...)` with one `it` covering the structural assertions above. Cast through `unknown` to satisfy strict typing (`fixture as unknown as GhBacklogResponse`). No mocks; no QueryClient; pure type+runtime structural assertion.
  </action>
  <acceptance_criteria>
    - File exists at `taskflow/src/services/jira/greenhopper/__tests__/types-fixture.test.ts`.
    - `cd taskflow && pnpm vitest run src/services/jira/greenhopper/__tests__/types-fixture.test.ts --reporter=basic` exits 0 (test passes GREEN against the widened type + real fixture).
    - File contains exactly one `it(` call.
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && pnpm vitest run src/services/jira/greenhopper/__tests__/types-fixture.test.ts --reporter=basic</automated>
  </verify>
  <done>Type-pin GREEN; any future drift in either the fixture or the widened type produces a loud test failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: useGhBacklogData.test.tsx (RED hook suite mirroring useGhAllData.test.ts)</name>
  <read_first>
    - taskflow/src/services/jira/greenhopper/useGhAllData.test.ts (full — verbatim model per 74-PATTERNS S7)
    - .planning/phases/74-backlog-on-data-json/74-PATTERNS.md §"useGhBacklogData.test.ts (NEW — test)"
    - .planning/phases/74-backlog-on-data-json/74-RESEARCH.md §"Wave 0 Gaps" (8 ports)
  </read_first>
  <behavior>
    - Test 1: does NOT call fetchBacklogData when boardId is null
    - Test 2: does NOT call fetchBacklogData when route is inactive (useIsActiveRoute('/backlog') returns false)
    - Test 3: does NOT call fetchBacklogData when token is missing (readSecret resolves null)
    - Test 4: calls fetchBacklogData once with (baseUrl, token, boardId) when enabled; returns the raw envelope unchanged
    - Test 5: getGhBacklogData uses ensureQueryData with key ['gh-backlog', boardId] and STALE_TIME_MS
    - Test 6: invalidateGhBacklogData(qc) invalidates all boards (queryKey ['gh-backlog'])
    - Test 7: invalidateGhBacklogData(qc, boardId) invalidates exactly ['gh-backlog', boardId]
    - Test 8: query result does NOT carry refetchInterval behavior (no recurring refetch after staleTime, confirming D-02 no-polling — assert via vi.useFakeTimers + advance > STALE_TIME_MS + fetchBacklogData call count stays 1)
  </behavior>
  <action>
    Create `taskflow/src/services/jira/greenhopper/__tests__/useGhBacklogData.test.tsx`. Mock `../data` to provide `fetchBacklogData: vi.fn()`, mock `../../../stronghold` for `readSecret`, mock `../../../../hooks/useIsActiveRoute` for `useIsActiveRoute` (path depth from `__tests__/`). Import `useGhBacklogData`, `getGhBacklogData`, `invalidateGhBacklogData` from `../useGhBacklogData`. Import `GhBacklogResponse` from `../types`. Build a `makeBacklogResponse()` factory returning an empty-but-valid widened envelope (issues: [], entityData: { statuses: {}, priorities: {}, types: {}, epics: {} }, sprints: [], rankCustomFieldId: 0, projects: [], canManageSprints: false, canCreateIssue: false, versionData: { versionsPerProject: {}, canCreateVersion: false, isLinkToDevStatusVersionAvailable: false }, supportsPages: false, hasBulkChangePermission: false, issueArchivingEnabled: false, emptyFilterBoard: false, cardColorStrategy: 'none'). Use the same `makeWrapper(qc)` helper from useGhAllData.test.ts. Use vitest's `renderHook` + `waitFor`. This file MUST fail compilation/runtime now (because `useGhBacklogData.ts` does not yet exist) — that is the RED state Plan 02 flips to GREEN.
  </action>
  <acceptance_criteria>
    - File exists at `taskflow/src/services/jira/greenhopper/__tests__/useGhBacklogData.test.tsx`.
    - `grep -cE "^[[:space:]]*it\\(" taskflow/src/services/jira/greenhopper/__tests__/useGhBacklogData.test.tsx` returns exactly 8.
    - `cd taskflow && pnpm vitest run src/services/jira/greenhopper/__tests__/useGhBacklogData.test.tsx --reporter=basic` exits NON-ZERO (RED) — module-not-found resolving `../useGhBacklogData` is acceptable.
    - File mocks all three: `../data`, `../../../stronghold`, `../../../../hooks/useIsActiveRoute`.
  </acceptance_criteria>
  <verify>
    <automated>! (cd taskflow && pnpm vitest run src/services/jira/greenhopper/__tests__/useGhBacklogData.test.tsx --reporter=basic) && grep -cE "^[[:space:]]*it\\(" taskflow/src/services/jira/greenhopper/__tests__/useGhBacklogData.test.tsx | grep -qx 8</automated>
  </verify>
  <done>Eight-case RED hook suite committed; Plan 02 GREENs it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: adapter-backlog.test.ts + BacklogPage.network.test.tsx + Sidebar.prefetch.test.tsx (RED gates)</name>
  <read_first>
    - taskflow/src/services/jira/greenhopper/adapter.test.ts (existing structure + adaptIssue usage)
    - taskflow/src/services/jira/greenhopper/adapter.ts (createAdapter / adaptIssue signature)
    - taskflow/src/routes/dashboard/BacklogPage.tsx (lines 200-350 — current 3 useQuery blocks)
    - taskflow/src/components/app/Sidebar.tsx (lines 38-42, 127-142, 179-246 — current /backlog prefetch chain + /sprint-board precedent)
    - .planning/phases/74-backlog-on-data-json/74-VALIDATION.md §"Wave 0 Requirements"
  </read_first>
  <behavior>
    - adapter-backlog.test.ts: feeds one `GhIssue` taken from `data.real.json` issues[0] through `adaptIssue(gh, entityMaps, storyPointsFieldKey)`. Asserts: result has fields.status (resolved name), fields.issuetype (resolved name), synthesized fields.assignee shape when `assignee` present (avatarUrls['48x48'] === gh.avatarUrl, displayName === gh.assigneeName, name === gh.assignee), story points populated at `fields[storyPointsFieldKey]` from `gh.estimateStatistic.statFieldValue.value`. A second `it` builds a `Map<issueId, sprintId>` from `data.sprints[i].issuesIds[]` and asserts the reverse-index lookup is correct for at least one (issueId, sprintId) pair drawn from the fixture.
    - BacklogPage.network.test.tsx: renders BacklogPage with a QueryClient + msw or fetch stub; asserts exactly 1 GET to `/rest/greenhopper/1.0/xboard/plan/backlog/data.json` (rapidViewId param accepted) and 0 hits matching the legacy patterns `/rest/agile/1.0/board/{id}/backlog`, `/rest/agile/1.0/board/{id}/sprint`, or any URL containing `backlog?` (legacy REST). Acceptable scaffolding: use `vi.spyOn(global, 'fetch')` and count by URL substring. Will be RED until Plan 03 rewrites BacklogPage.
    - Sidebar.prefetch.test.tsx: simulates Sidebar with current route `/backlog`; asserts exactly 1 call to a function bound from `@/services/jira`'s `getGhBacklogData` (use `vi.mock('@/services/jira', () => ({ getGhBacklogData: vi.fn(), fetchBoardId: vi.fn().mockResolvedValue(123) }))`) and 0 calls to `fetchBacklogIssues`, `fetchBacklogSprintStories`, or `fetchSprintList`. Will be RED until Plan 04 collapses the chain.
  </behavior>
  <action>
    Create three test files at the VALIDATION-specified paths. Use `vi.mock` + `vi.fn` patterns lifted from existing sibling tests (`adapter.test.ts`, `useGhAllData.test.ts`). For BacklogPage.network.test.tsx, follow the existing render-with-providers pattern used elsewhere in `src/routes/dashboard/__tests__` if any; otherwise inline a minimal `QueryClientProvider` wrapper and stub auth via `vi.mock('@/stores/auth.store', ...)` with a fake `jiraBaseUrl` and `jiraToken`. For Sidebar.prefetch.test.tsx, render `<Sidebar />` inside a route harness with `MemoryRouter` initial `/backlog` (or stub `useLocation` directly via `vi.mock('react-router-dom', ...)` matching project's existing test style). All three tests MUST fail today (RED) — adapter-backlog because the reverse-index helper is not yet wired into Plan 03's call site; BacklogPage.network because the page still uses legacy REST; Sidebar.prefetch because the prefetch chain still calls three legacy fetchers.
  </action>
  <acceptance_criteria>
    - All three files exist at the exact paths in must_haves.artifacts above.
    - `cd taskflow && pnpm vitest run src/services/jira/greenhopper/__tests__/adapter-backlog.test.ts src/routes/dashboard/__tests__/BacklogPage.network.test.tsx src/components/app/__tests__/Sidebar.prefetch.test.tsx --reporter=basic` exits NON-ZERO (RED).
    - `grep -cE "^[[:space:]]*it\\(" taskflow/src/services/jira/greenhopper/__tests__/adapter-backlog.test.ts` returns at least 2.
    - `grep -cE "data\\.json" taskflow/src/routes/dashboard/__tests__/BacklogPage.network.test.tsx` returns at least 1 (the URL substring being asserted).
    - `grep -cE "getGhBacklogData" taskflow/src/components/app/__tests__/Sidebar.prefetch.test.tsx` returns at least 1.
  </acceptance_criteria>
  <verify>
    <automated>! (cd taskflow && pnpm vitest run src/services/jira/greenhopper/__tests__/adapter-backlog.test.ts src/routes/dashboard/__tests__/BacklogPage.network.test.tsx src/components/app/__tests__/Sidebar.prefetch.test.tsx --reporter=basic)</automated>
  </verify>
  <done>Three RED gates committed and ready to flip GREEN across Plans 02–04.</done>
</task>

<task type="auto">
  <name>Task 5: scripts/check-legacy-backlog-keys.mjs static-grep guard</name>
  <read_first>
    - taskflow/scripts/ (existing scripts for style precedent — list contents and pick one as analog)
    - .planning/phases/74-backlog-on-data-json/74-VALIDATION.md §"Wave 0 Requirements" final bullet
    - .planning/phases/74-backlog-on-data-json/74-CONTEXT.md D-09 (delete list) + D-09a (keep list — fetchSprintList stays in FieldsSection.tsx)
  </read_first>
  <behavior>
    - Script scans `taskflow/src/**/*.{ts,tsx}` excluding `**/*.test.*` and `__tests__/**` and the `scripts/` directory itself.
    - Fails (exit 1) and prints offending file + line if any of these tokens are found OUTSIDE allowed files: `'jira-backlog-issues'`, `'jira-backlog-sprint-stories'`, `fetchBacklogIssues`, `fetchBacklogSprintStories`.
    - The script's own file may contain the tokens (they are literals in the patterns array); the script ignores itself.
    - `fetchSprintList` is NOT a banned token (D-09a — FieldsSection.tsx still uses it).
    - During Plan 01 the script is INFORMATIONAL only (will report current legacy hits in BacklogPage.tsx / Sidebar.tsx / backlog.ts / jira.ts — that is correct). It becomes a blocking guard after Plan 06 deletes the symbols. The script does not need to pass on its own day; it just needs to be executable and to correctly enumerate the banned tokens.
  </behavior>
  <action>
    Create `taskflow/scripts/check-legacy-backlog-keys.mjs` as a Node ESM script. Use `node:fs/promises` + `node:path` (no external deps). Walk `taskflow/src` recursively, filter by `.ts`/`.tsx` and exclude paths matching `/__tests__/`, `.test.`, `/scripts/`. For each remaining file, read text and scan for the four banned token strings. Collect hits as `{ file, line, token }`. If hits.length > 0, print one line per hit (`file:line — token`) and `process.exit(1)`. Otherwise print `OK` and `process.exit(0)`. Top of file: JSDoc explaining D-09 rationale and that Plan 06 wires this into CI / pre-commit.
  </action>
  <acceptance_criteria>
    - File exists at `taskflow/scripts/check-legacy-backlog-keys.mjs`, mode includes read for owner.
    - `node taskflow/scripts/check-legacy-backlog-keys.mjs; echo $?` runs without a Node syntax error (exit 1 expected today because legacy code still present; exit 0 expected after Plan 06 deletion — both are valid script behavior).
    - `grep -nE "fetchBacklogIssues|fetchBacklogSprintStories|jira-backlog-issues|jira-backlog-sprint-stories" taskflow/scripts/check-legacy-backlog-keys.mjs` returns at least 4 hits (the four banned tokens declared in the script).
    - `grep -cE "fetchSprintList" taskflow/scripts/check-legacy-backlog-keys.mjs` returns 0 (D-09a — never banned).
  </acceptance_criteria>
  <verify>
    <automated>node taskflow/scripts/check-legacy-backlog-keys.mjs >/dev/null 2>&1; test $? -le 1 && grep -cE "fetchBacklogIssues|fetchBacklogSprintStories|jira-backlog-issues|jira-backlog-sprint-stories" taskflow/scripts/check-legacy-backlog-keys.mjs | awk '$1>=4 {ok=1} END{exit !ok}' && ! grep -qE "fetchSprintList" taskflow/scripts/check-legacy-backlog-keys.mjs</automated>
  </verify>
  <done>Static-grep guard committed; Plan 06 will wire it into CI after the deletions land.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→GH API | Bearer PAT crosses from the renderer (via `readSecret('jira-pat')`) into outbound HTTPS to `/rest/greenhopper/1.0/xboard/plan/backlog/data.json`. Unchanged from Phase 71/73 — token surface is identical. |
| fixture→types | `data.real.json` is the contract source for `GhBacklogResponse`; a malformed fixture would silently widen accepted shapes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Tampering | `types.ts` widening drifts from real API | mitigate | `types-fixture.test.ts` (Task 2) — every CI run pins type to fixture; mismatch fails the build. |
| T-74-02 | Information Disclosure | Test fixture might leak PII if refreshed from a real Jira | accept | Phase 71 established the fixture-redaction discipline; this plan only re-reads the existing already-redacted file. No new capture in this plan. |
| T-74-SC | Tampering | npm/pnpm installs | mitigate | No new dependencies introduced — Wave 0 only touches in-repo source + test files. Package legitimacy audit not triggered. |
</threat_model>

<verification>
- `cd taskflow && pnpm tsc --noEmit` — exits 0 (widened type compiles).
- `cd taskflow && pnpm vitest run src/services/jira/greenhopper/__tests__/types-fixture.test.ts --reporter=basic` — GREEN.
- `cd taskflow && pnpm vitest run src/services/jira/greenhopper/__tests__/useGhBacklogData.test.tsx src/services/jira/greenhopper/__tests__/adapter-backlog.test.ts src/routes/dashboard/__tests__/BacklogPage.network.test.tsx src/components/app/__tests__/Sidebar.prefetch.test.tsx --reporter=basic` — RED (this is the desired state for Wave 0; Plans 02-04 turn them GREEN one-by-one).
- `cd taskflow && pnpm biome check src/services/jira/greenhopper/types.ts` — 0 errors / 0 warnings.
- `node taskflow/scripts/check-legacy-backlog-keys.mjs` — runs without Node syntax error (exit code 0 or 1; both valid pre-cutover).
</verification>

<success_criteria>
- `GhBacklogResponse` matches the real fixture envelope; outdated "does NOT carry entity maps" comment removed.
- All six Wave 0 scaffolds from 74-VALIDATION.md exist on disk with content described above.
- Wave 0 tests are RED in the four expected places and GREEN at the type-pin — exactly the gate Plans 02-04 are designed to flip.
- Biome baseline preserved (0 errors / 0 warnings on modified files).
</success_criteria>

<output>
Create `.planning/phases/74-backlog-on-data-json/74-01-SUMMARY.md` when done.
</output>
