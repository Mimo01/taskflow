---
phase: 76-visual-polish-and-shared-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/lib/issueDisplayUtils.ts
  - taskflow/src/lib/issueDisplayUtils.test.ts
autonomous: true
requirements: [VISUAL-01, VISUAL-04, VISUAL-05]
must_haves:
  truths:
    - "isDoneStatus returns true only for statusCategory.key === 'done'"
    - "doneSummaryClass returns 'line-through' for done status, '' otherwise"
    - "priorityStripeClass returns the exact WCAG-verified border-l class per priority and a default for unknown/null"
  artifacts:
    - path: "taskflow/src/lib/issueDisplayUtils.ts"
      provides: "isDoneStatus, doneSummaryClass, priorityStripeClass shared display primitives"
      exports: ["isDoneStatus", "doneSummaryClass", "priorityStripeClass"]
    - path: "taskflow/src/lib/issueDisplayUtils.test.ts"
      provides: "Unit coverage for all three exports including WCAG palette assertions"
      contains: "priorityStripeClass"
  key_links:
    - from: "taskflow/src/lib/issueDisplayUtils.ts"
      to: "PRIORITY_STRIPE record"
      via: "full static Tailwind class strings (no template literals)"
      pattern: "border-l-yellow-700 dark:border-l-yellow-500"
---

<objective>
Create the shared display-primitive utility `lib/issueDisplayUtils.ts` exporting `isDoneStatus`, `doneSummaryClass`, and `priorityStripeClass`, fully unit-tested. This is the single source of truth for done-state strike (D-05/D-06/D-07) and the priority color stripe palette (D-01/D-02/D-03) that Plan 03 consumers wire into TaskCard, BacklogRow, the Standup Today sections, and DashboardInProgressCard.

Purpose: Centralize the inline `statusCategory?.key === 'done'` check currently duplicated in TaskCard, and provide a WCAG-verified priority→Tailwind-class map so every consumer renders consistent, accessible visuals.
Output: `taskflow/src/lib/issueDisplayUtils.ts` + `taskflow/src/lib/issueDisplayUtils.test.ts` (both new), test suite green.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-visual-polish-and-shared-primitives/76-CONTEXT.md
@.planning/phases/76-visual-polish-and-shared-primitives/76-RESEARCH.md
@.planning/phases/76-visual-polish-and-shared-primitives/76-PATTERNS.md
@.planning/phases/76-visual-polish-and-shared-primitives/76-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for issueDisplayUtils (Wave 0 scaffold)</name>
  <read_first>
    - taskflow/src/lib/formatTimeAgo.test.ts (test-file analog: vitest imports, describe/it structure, no testing-library)
    - taskflow/src/lib/statusStyles.ts (Record<string,string> + nullable-input convention the util mirrors)
    - .planning/phases/76-visual-polish-and-shared-primitives/76-RESEARCH.md § WCAG Contrast Analysis (verified palette)
    - .planning/phases/76-visual-polish-and-shared-primitives/76-PATTERNS.md § issueDisplayUtils.test.ts (required tests list)
  </read_first>
  <behavior>
    - isDoneStatus: true for `{ key: 'done' }`; false for `{ key: 'indeterminate' }`, `{ key: 'new' }`, `null`, `undefined`.
    - doneSummaryClass: returns exactly `'line-through'` for `{ key: 'done' }`; returns exactly `''` for `{ key: 'indeterminate' }`, `null`, `undefined`.
    - priorityStripeClass exact returns: 'Highest' -> 'border-l-red-600 dark:border-l-red-400'; 'High' -> 'border-l-orange-600 dark:border-l-orange-400'; 'Medium' -> 'border-l-yellow-700 dark:border-l-yellow-500'; 'Low' -> 'border-l-gray-500 dark:border-l-gray-400'; 'Lowest' -> 'border-l-gray-600 dark:border-l-gray-300'.
    - priorityStripeClass default: `null`, `undefined`, and an unknown name (e.g. 'Critical') all return 'border-l-gray-600 dark:border-l-gray-300'.
  </behavior>
  <action>
    Create `taskflow/src/lib/issueDisplayUtils.test.ts`. Import `{ describe, expect, it }` from 'vitest' and `{ isDoneStatus, doneSummaryClass, priorityStripeClass }` from './issueDisplayUtils'. Write one `describe` block per exported function. Assert string equality with `.toBe()` using the EXACT verified palette strings from RESEARCH.md (these REPLACE the UI-SPEC starting shades: Medium light is `yellow-700` NOT `yellow-500`; the UI-SPEC `yellow-500`/`yellow-300` Medium and `gray-400` Lowest light shades FAIL WCAG and must not be used). Cover every priority key, the default-fallback path, and all done/non-done/null/undefined cases per the behavior block. The implementation file does not exist yet — run the test and confirm it fails to import (RED). Do NOT create the implementation in this task.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/lib/issueDisplayUtils.test.ts 2>&1 | grep -qiE "fail|error|cannot find module|no test" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/lib/issueDisplayUtils.test.ts` exists and imports the three named exports from './issueDisplayUtils'.
    - Test asserts `priorityStripeClass('Medium')` toBe `'border-l-yellow-700 dark:border-l-yellow-500'` (the corrected shade, NOT yellow-500 light).
    - Test asserts the default branch (`null`/`undefined`/unknown) toBe `'border-l-gray-600 dark:border-l-gray-300'`.
    - Run fails RED because `issueDisplayUtils.ts` does not yet exist.
  </acceptance_criteria>
  <done>issueDisplayUtils.test.ts written with full palette + done-state coverage; vitest run is RED (module missing).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement issueDisplayUtils to make tests pass (GREEN)</name>
  <read_first>
    - taskflow/src/lib/statusStyles.ts (module-scope Record, full static class strings, nullable input + `?? fallback`)
    - taskflow/src/lib/epicColors.ts (secondary Record analog — full Tailwind tokens, no template literals)
    - .planning/phases/76-visual-polish-and-shared-primitives/76-PATTERNS.md § issueDisplayUtils.ts (complete expected file shape)
    - taskflow/src/lib/issueDisplayUtils.test.ts (the tests written in Task 1)
  </read_first>
  <action>
    Create `taskflow/src/lib/issueDisplayUtils.ts` with no imports (pure string utilities). Export `isDoneStatus(statusCategory: { key: string } | null | undefined): boolean` returning `statusCategory?.key === 'done'` (D-07 — exact mirror of kanban check). Export `doneSummaryClass(statusCategory: { key: string } | null | undefined): string` returning `isDoneStatus(statusCategory) ? 'line-through' : ''` — add a doc comment stating that despite the name this class is applied to the issue KEY element, not the summary (D-06 — do NOT rename or retarget to the summary). Define module-scope `const PRIORITY_STRIPE: Record<string, string>` with full static class strings: Highest='border-l-red-600 dark:border-l-red-400', High='border-l-orange-600 dark:border-l-orange-400', Medium='border-l-yellow-700 dark:border-l-yellow-500', Low='border-l-gray-500 dark:border-l-gray-400', Lowest='border-l-gray-600 dark:border-l-gray-300'. Define `const DEFAULT_STRIPE = 'border-l-gray-600 dark:border-l-gray-300'` (D-03 — neutral gray for unset, never absent). Export `priorityStripeClass(priorityName: string | null | undefined): string` returning `PRIORITY_STRIPE[priorityName ?? ''] ?? DEFAULT_STRIPE`. Use full static strings only — no `border-l-${color}` template literals (RESEARCH Pitfall 5: Tailwind JIT would purge them). Function returns color class only; callers add `border-l-4` width (per UI-SPEC contract).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/lib/issueDisplayUtils.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/lib/issueDisplayUtils.ts` exports exactly `isDoneStatus`, `doneSummaryClass`, `priorityStripeClass`.
    - `PRIORITY_STRIPE.Medium` source string is `'border-l-yellow-700 dark:border-l-yellow-500'` (grep: `grep -q "border-l-yellow-700 dark:border-l-yellow-500" taskflow/src/lib/issueDisplayUtils.ts`).
    - No template-literal class construction in the file (grep: `grep -v '^[[:space:]]*\*' taskflow/src/lib/issueDisplayUtils.ts | grep -c 'border-l-\${'` returns 0).
    - `npx vitest run src/lib/issueDisplayUtils.test.ts` passes GREEN.
    - `npm run check` (biome + tsc) is clean.
  </acceptance_criteria>
  <done>All issueDisplayUtils tests GREEN; biome+tsc clean; PRIORITY_STRIPE uses the verified WCAG palette with full static class strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is crossed. This plan adds pure, side-effect-free string utilities operating on already-fetched, already-trusted in-app data (`statusCategory.key`, `priority.name`).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | issueDisplayUtils.ts | n/a | No threats apply at ASVS L1: no auth/session/crypto/access-control/input-deserialization is introduced; functions are pure transforms over trusted in-memory display data (RESEARCH.md § Security Domain). |

No `npm install` occurs in this plan — package legitimacy gate not applicable.
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/lib/issueDisplayUtils.test.ts` — all GREEN
- `cd taskflow && npm run check` — biome + tsc clean
</verification>

<success_criteria>
- `isDoneStatus`, `doneSummaryClass`, `priorityStripeClass` exported from `taskflow/src/lib/issueDisplayUtils.ts`.
- Priority palette matches the RESEARCH.md WCAG-verified table exactly (Medium = yellow-700/yellow-500).
- Full unit coverage GREEN; no Tailwind template-literal classes.
</success_criteria>

<output>
Create `.planning/phases/76-visual-polish-and-shared-primitives/76-01-SUMMARY.md` when done.
</output>
