---
phase: 76-visual-polish-and-shared-primitives
plan: 04
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - taskflow/src/routes/dashboard/TaskCard.tsx
  - taskflow/src/routes/dashboard/BacklogRow.tsx
  - taskflow/src/routes/standup-notes/TodayInProgressSection.tsx
  - taskflow/src/routes/standup-notes/TodayUpNextSection.tsx
  - taskflow/src/routes/dashboard/DashboardInProgressCard.tsx
autonomous: false
requirements: [VISUAL-01, VISUAL-02, VISUAL-03, VISUAL-04, VISUAL-05]
must_haves:
  truths:
    - "Done current-sprint stories show a struck-through issue key on the Backlog active-sprint list (VISUAL-01)"
    - "Done items show a struck-through issue key in the Standup Notes Today section (VISUAL-02)"
    - "Done items show a struck-through issue key in the Dashboard per-story In-Progress card rows (VISUAL-03)"
    - "Sprint board non-subtask cards show a left-edge priority color stripe (border-l-4 + priority class) (VISUAL-04)"
    - "Stripe colors are the WCAG >= 3:1 verified palette, legible in both themes (VISUAL-05)"
    - "TaskCard's inline done-check is replaced by isDoneStatus (single source of truth)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/TaskCard.tsx"
      provides: "border-l-4 + priorityStripeClass on non-subtask outer button; isDoneStatus refactor"
      contains: "priorityStripeClass"
    - path: "taskflow/src/routes/dashboard/BacklogRow.tsx"
      provides: "doneSummaryClass on issue key span"
      contains: "doneSummaryClass"
    - path: "taskflow/src/routes/standup-notes/TodayInProgressSection.tsx"
      provides: "doneSummaryClass on issue key span"
      contains: "doneSummaryClass"
    - path: "taskflow/src/routes/standup-notes/TodayUpNextSection.tsx"
      provides: "doneSummaryClass on issue key span"
      contains: "doneSummaryClass"
    - path: "taskflow/src/routes/dashboard/DashboardInProgressCard.tsx"
      provides: "doneSummaryClass on per-story issue key spans"
      contains: "doneSummaryClass"
  key_links:
    - from: "taskflow/src/routes/dashboard/TaskCard.tsx"
      to: "lib/issueDisplayUtils"
      via: "import isDoneStatus + priorityStripeClass"
      pattern: "from '@/lib/issueDisplayUtils'"
    - from: "taskflow/src/routes/dashboard/BacklogRow.tsx"
      to: "lib/issueDisplayUtils"
      via: "import doneSummaryClass"
      pattern: "doneSummaryClass"
---

<objective>
Wire the Plan-01 display primitives into every consuming surface: apply the key-only `line-through` (D-05/D-06) to the issue-key span on the Backlog list (VISUAL-01), the Standup Today sections (VISUAL-02), and the Dashboard In-Progress per-story rows (VISUAL-03); refactor TaskCard's inline done-check to `isDoneStatus`; and add the `border-l-4` + `priorityStripeClass` left-edge stripe to non-subtask sprint board cards (VISUAL-04/05). A human checkpoint verifies the strike and the WCAG-legible stripe across both themes.

Purpose: Deliver the user-visible visual consistency that is the point of the phase, using the single-source-of-truth utilities from Plan 01.
Output: 5 modified components; a blocking human-verify checkpoint confirming strike + stripe in light and dark themes.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-visual-polish-and-shared-primitives/76-CONTEXT.md
@.planning/phases/76-visual-polish-and-shared-primitives/76-RESEARCH.md
@.planning/phases/76-visual-polish-and-shared-primitives/76-PATTERNS.md
@.planning/phases/76-visual-polish-and-shared-primitives/76-UI-SPEC.md
@.planning/phases/76-visual-polish-and-shared-primitives/76-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TaskCard — refactor done-check to isDoneStatus + add priority stripe</name>
  <read_first>
    - taskflow/src/routes/dashboard/TaskCard.tsx (self — outer button className ~lines 94-99 with `isSubtask && 'border-l-2 border-l-muted'` and `isFlagged`; key span ~lines 112-117; cn imported line 34; import group ~lines 33-35)
    - .planning/phases/76-visual-polish-and-shared-primitives/76-PATTERNS.md § TaskCard.tsx (Change 1, Change 2, import additions — exact before/after)
    - .planning/phases/76-visual-polish-and-shared-primitives/76-RESEARCH.md § Pitfall 2 (subtasks keep muted border, no priority stripe)
    - taskflow/src/lib/issueDisplayUtils.ts (the exports from Plan 01)
  </read_first>
  <action>
    Edit `taskflow/src/routes/dashboard/TaskCard.tsx`:
    1. Add import: `import { isDoneStatus, priorityStripeClass } from '@/lib/issueDisplayUtils';` alongside the existing imports (~lines 33-35; `cn` is already imported at line 34).
    2. Key-span refactor (~lines 112-117): replace the inline `issue.fields.status.statusCategory?.key === 'done'` condition with `isDoneStatus(issue.fields.status.statusCategory)`. PRESERVE the existing branch values verbatim — `'line-through group-hover:[text-decoration-line:underline_line-through]'` for done and `'group-hover:underline'` for not-done. These hover-context decorations are NOT part of `doneSummaryClass`; do not fold them in. This is a pure refactor — rendered classes are unchanged.
    3. Priority stripe on the outer button (~lines 94-99): change the `isSubtask && 'border-l-2 border-l-muted'` line to the ternary `isSubtask ? 'border-l-2 border-l-muted' : ['border-l-4', priorityStripeClass(issue.fields.priority?.name)]`. Subtasks RETAIN the muted nesting border and get NO priority stripe (D-04 + RESEARCH Pitfall 2 — nesting hierarchy outranks priority for subtask cards). Leave the `isFlagged` branch unchanged. `priorityStripeClass` returns color only; the `'border-l-4'` width is added here by the caller per the UI-SPEC contract.
  </action>
  <verify>
    <automated>cd taskflow && grep -q "isDoneStatus(issue.fields.status.statusCategory)" src/routes/dashboard/TaskCard.tsx && grep -q "priorityStripeClass(issue.fields.priority?.name)" src/routes/dashboard/TaskCard.tsx && grep -q "border-l-4" src/routes/dashboard/TaskCard.tsx && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - TaskCard imports `isDoneStatus` and `priorityStripeClass` from `@/lib/issueDisplayUtils`.
    - The key-span condition now calls `isDoneStatus(...)`; both done/not-done class strings are byte-for-byte unchanged from before the edit.
    - Non-subtask cards apply `border-l-4` + `priorityStripeClass(issue.fields.priority?.name)`; subtask cards still render only `border-l-2 border-l-muted`.
    - No inline `statusCategory?.key === 'done'` remains for the key span (grep: `grep -c "statusCategory?.key === 'done'" src/routes/dashboard/TaskCard.tsx` returns 0).
    - `npm run check` clean.
  </acceptance_criteria>
  <done>TaskCard uses isDoneStatus for the key strike and renders border-l-4 + priority stripe on non-subtask cards (subtasks unchanged); biome+tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Apply doneSummaryClass to issue-key spans across list surfaces (VISUAL-01/02/03)</name>
  <read_first>
    - taskflow/src/routes/dashboard/BacklogRow.tsx (key span line 79; cn already imported line 29)
    - taskflow/src/routes/standup-notes/TodayInProgressSection.tsx (key span line 105; cn NOT yet imported — must add)
    - taskflow/src/routes/standup-notes/TodayUpNextSection.tsx (key span line 109; cn NOT yet imported — must add)
    - taskflow/src/routes/dashboard/DashboardInProgressCard.tsx (per-story issue-key spans at lines 134 parentKey, 167 subtask.key, and the orphan span near the bottom; cn NOT yet imported — must add; each row's issue object exposes `fields.status.statusCategory`)
    - .planning/phases/76-visual-polish-and-shared-primitives/76-PATTERNS.md § BacklogRow.tsx / TodayInProgressSection.tsx / TodayUpNextSection.tsx (exact before/after + import notes)
    - .planning/phases/76-visual-polish-and-shared-primitives/76-UI-SPEC.md § Done-State Strike Treatment (key element only, summary stays normal)
  </read_first>
  <action>
    Apply the SAME change to the issue-key span in four files: wrap the existing className with `cn(...)` and append `doneSummaryClass(<the issue's statusCategory>)`. Strike the KEY span only — never the summary (D-05/D-06).
    1. `BacklogRow.tsx` line 79: the span `font-mono text-xs text-muted-foreground` around `{issue.key}` — wrap className with `cn('font-mono text-xs text-muted-foreground', doneSummaryClass(issue.fields.status.statusCategory))`. Add `import { doneSummaryClass } from '@/lib/issueDisplayUtils';` (`cn` already imported line 29).
    2. `TodayInProgressSection.tsx` line 105: the key span `text-xs text-muted-foreground font-mono shrink-0` — wrap with `cn(..., doneSummaryClass(issue.fields.status.statusCategory))`. Add BOTH `import { cn } from '@/lib/utils';` and `import { doneSummaryClass } from '@/lib/issueDisplayUtils';` (cn not yet imported here).
    3. `TodayUpNextSection.tsx` line 109: identical to (2), same two imports to add.
    4. `DashboardInProgressCard.tsx`: apply `doneSummaryClass(...)` to each of the three per-story key spans — the parent-story key (line 134; use `issueByKey.get(parentKey)?.fields.status.statusCategory`), the subtask key (line 167; use `subtask.fields.status.statusCategory`), and the orphan subtask key near the bottom (use `subtask.fields.status.statusCategory`). Add `import { cn } from '@/lib/utils';` and `import { doneSummaryClass } from '@/lib/issueDisplayUtils';`. This card renders only `indeterminate` items in normal operation, so `doneSummaryClass` returns `''` for them — the strike is style-readiness for mid-day transitions (D-08 parallel) and satisfies VISUAL-03 as a real per-story surface (the requirement's aggregate-only escape hatch does NOT apply because per-story key rows exist here). Undefined statusCategory is safe: `doneSummaryClass` returns `''` by contract.
  </action>
  <verify>
    <automated>cd taskflow && grep -q doneSummaryClass src/routes/dashboard/BacklogRow.tsx && grep -q doneSummaryClass src/routes/standup-notes/TodayInProgressSection.tsx && grep -q doneSummaryClass src/routes/standup-notes/TodayUpNextSection.tsx && grep -q doneSummaryClass src/routes/dashboard/DashboardInProgressCard.tsx && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - All four files import and call `doneSummaryClass` on the issue-key span(s) (summary spans untouched).
    - `TodayInProgressSection.tsx`, `TodayUpNextSection.tsx`, and `DashboardInProgressCard.tsx` each gain a `cn` import from `@/lib/utils`.
    - `DashboardInProgressCard.tsx` applies `doneSummaryClass` to all three per-story key spans (parent, subtask, orphan) — grep count >= 3.
    - No summary text span receives `line-through` / `doneSummaryClass`.
    - `npm run check` clean.
  </acceptance_criteria>
  <done>Backlog list, both Standup Today sections, and the Dashboard In-Progress per-story rows strike the issue KEY span only when done; biome+tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Done-state key strike applied app-wide (Backlog list, Standup Today, Dashboard In-Progress per-story rows, sprint board cards) and the priority left-edge color stripe (border-l-4 + WCAG-verified palette) on non-subtask sprint board cards.
  </what-built>
  <how-to-verify>
    1. Run the app (`cd taskflow && npm run tauri dev` or the project's standard dev command).
    2. Sprint board (VISUAL-04/05): confirm non-subtask cards show a left-edge stripe colored by priority — Highest=red, High=orange, Medium=yellow, Low/Lowest/unset=gray (red>orange>yellow>gray hierarchy visible). Subtask cards keep the thin muted nesting border (no priority stripe). Toggle light AND dark theme; confirm every stripe is clearly distinguishable from the card surface in both themes (WCAG >= 3:1 — pay special attention to Medium/yellow in LIGHT mode, which must read as a solid yellow-700, not a pale wash). Optionally confirm via DevTools > Accessibility > Color Contrast that the stripe clears 3:1 against bg-card.
    3. Backlog active-sprint list (VISUAL-01): find a done story; confirm its issue KEY (monospace) is struck-through while the summary text stays normal. Matches the kanban card treatment.
    4. Standup Notes Today (VISUAL-02): if a done item is present in the Today section (e.g. transitioned mid-day), confirm its issue key is struck-through. If none present, confirm normal items are NOT struck (style-readiness only — D-08).
    5. Dashboard "My In Progress" card (VISUAL-03): per-story rows render issue keys; confirm a done item (if present) shows a struck key, normal items do not.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any strike/stripe/theme issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. All changes apply CSS classes (via `cn` / Tailwind) to already-fetched, already-trusted issue data rendered in existing components. No new data source, network call, or persistence.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | TaskCard / BacklogRow / Today sections / DashboardInProgressCard | n/a | No threats apply at ASVS L1: changes are presentation-only Tailwind class applications over trusted in-memory display data; no auth/session/crypto/access-control/input-handling introduced (RESEARCH.md § Security Domain). |

No `npm install` occurs in this plan — package legitimacy gate not applicable.
</threat_model>

<verification>
- `cd taskflow && npm run check` — biome + tsc clean across all 5 modified files
- `cd taskflow && npx vitest run` — full suite GREEN (phase gate)
- Human checkpoint: strike + stripe verified in light AND dark themes
</verification>

<success_criteria>
- Issue-key strike present on Backlog list, both Standup Today sections, Dashboard In-Progress per-story rows, and sprint board cards (key only; summary unchanged).
- Non-subtask sprint board cards show the WCAG-verified priority stripe; subtask cards retain the muted nesting border.
- Stripe legible >= 3:1 in both themes (human-verified; Medium/yellow light-mode specifically checked).
- TaskCard inline done-check replaced by `isDoneStatus`.
</success_criteria>

<output>
Create `.planning/phases/76-visual-polish-and-shared-primitives/76-04-SUMMARY.md` when done.
</output>
