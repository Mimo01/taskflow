---
phase: 76-visual-polish-and-shared-primitives
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/stores/settings.store.ts
  - taskflow/src/stores/settings.store.test.ts
  - taskflow/src/routes/dashboard/BacklogPage.tsx
autonomous: true
requirements: [VISUAL-01]
must_haves:
  truths:
    - "Settings store version is 25 and persists rankFieldKey (default null)"
    - "rankFieldKey survives a 'preferences' reset (discovered field, not user preference)"
    - "BacklogPage writes rankFieldKey = customfield_${rankCustomFieldId} once when the GreenHopper backlog response first arrives"
  artifacts:
    - path: "taskflow/src/stores/settings.store.ts"
      provides: "rankFieldKey state + setRankFieldKey action + v25 migration"
      contains: "version: 25"
    - path: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      provides: "rankFieldKey discovery useEffect from backlog.rankCustomFieldId"
      contains: "setRankFieldKey"
  key_links:
    - from: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      to: "settings.store rankFieldKey"
      via: "useEffect on backlog.rankCustomFieldId composing customfield_${id}"
      pattern: "setRankFieldKey\\(`customfield_"
---

<objective>
Add a persisted `rankFieldKey` to the settings store via a v25 migration (D-10) and populate it from the GreenHopper backlog response's `rankCustomFieldId` in BacklogPage, composed as `customfield_${rankCustomFieldId}` (D-11). Probe-verified value: `rankCustomFieldId: 10105` -> `customfield_10105`.

Purpose: Persist the discovered rank field key so Phase 78 (drag-to-rank) can read it directly without re-discovery. This is non-visual scaffolding consumed downstream only.
Output: `settings.store.ts` at v25 with `rankFieldKey`/`setRankFieldKey`; BacklogPage discovery `useEffect`; a unit test asserting the composed-key contract.

Note on `requirements`: this plan carries VISUAL-01 in frontmatter to satisfy the requirement-coverage gate, but its substantive deliverables are decisions D-10/D-11 (discretion scaffolding for Phase 78). The user-facing VISUAL behaviors are delivered by Plans 01 and 04.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-visual-polish-and-shared-primitives/76-CONTEXT.md
@.planning/phases/76-visual-polish-and-shared-primitives/76-RESEARCH.md
@.planning/phases/76-visual-polish-and-shared-primitives/76-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rankFieldKey + v25 migration to settings store</name>
  <read_first>
    - taskflow/src/stores/settings.store.ts (self — v24 migration block ~line 439, version ~line 340, initialSettings ~line 20, accountFieldKey precedent at lines 33/93/333, setFlaggedFieldKey action ~line 310)
    - .planning/phases/76-visual-polish-and-shared-primitives/76-PATTERNS.md § settings.store.ts (Changes 1–6, exact anchors)
  </read_first>
  <action>
    Apply six edits to `taskflow/src/stores/settings.store.ts`, mirroring the existing discovered-field-key idiom (accountFieldKey / flaggedFieldKey are the precedents):
    1. In `initialSettings` (~line 20, after `quickFilters: []`), add `rankFieldKey: null as string | null,`.
    2. In the `SettingsState` interface (near `accountFieldKey: string | null;` ~line 93), add `rankFieldKey: string | null;` and `setRankFieldKey: (key: string) => void;` with a doc comment "Discovered rank custom field key. Null until populated from GreenHopper backlog response."
    3. In the `create()(persist(...))` actions block, beside `setFlaggedFieldKey` (~line 310), add `setRankFieldKey: (key) => set({ rankFieldKey: key }),`.
    4. In `resetSettings` 'preferences' branch (preserved keys, near `accountFieldKey: s.accountFieldKey,` ~line 333), add `rankFieldKey: s.rankFieldKey,` — discovered keys are preserved on a preferences reset, never cleared (D-11: comes from the Jira instance, not user preference).
    5. Append a v25 migration block immediately after the v24 block (~line 439, before `return persisted as SettingsState`): `if (version < 25) { if (s.rankFieldKey === undefined) s.rankFieldKey = null; }` (D-10).
    6. Bump `version: 24` to `version: 25` (~line 340).
    The store is already at v24, so the roadmap's "bump to persist v24" is implemented as v25 (D-10 — the roadmap text was off by one).
  </action>
  <verify>
    <automated>cd taskflow && grep -q "version: 25" src/stores/settings.store.ts && grep -q "setRankFieldKey: (key) => set({ rankFieldKey: key })" src/stores/settings.store.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `version: 25` present (grep passes); `version: 24` no longer present as the persist version.
    - `rankFieldKey` appears in `initialSettings`, the `SettingsState` interface, and the `resetSettings` 'preferences' preserved keys.
    - `setRankFieldKey: (key) => set({ rankFieldKey: key })` action present.
    - v25 migration block sets `rankFieldKey = null` when undefined.
    - `npm run check` (biome + tsc) clean.
  </acceptance_criteria>
  <done>Settings store at v25 with rankFieldKey persisted, preserved on preferences reset, and backfilled by the v25 migration; biome+tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Discover rankFieldKey in BacklogPage + add D-11 contract test</name>
  <read_first>
    - taskflow/src/routes/dashboard/BacklogPage.tsx (useSettingsStore destructure ~lines 210–217; useGhBacklogData -> `backlog` ~lines 241–247; useEffect already imported line 33; adapter useMemo chain begins ~line 253)
    - taskflow/src/stores/settings.store.test.ts (analog: renderHook + act + vitest; existing version-bump assertion style ~line 44)
    - taskflow/src/main.tsx (custom-field discovery useEffect precedent for the seam shape)
    - .planning/phases/76-visual-polish-and-shared-primitives/76-PATTERNS.md § BacklogPage.tsx (Changes 1–2, exact anchors) and § Pattern 4 (discovery seam)
  </read_first>
  <action>
    Edit `taskflow/src/routes/dashboard/BacklogPage.tsx`:
    1. Extend the existing `useSettingsStore()` destructure (~lines 210–217) to also pull `rankFieldKey` and `setRankFieldKey`.
    2. Add a `useEffect` after the `useGhBacklogData` call (~line 247) and before the adapter `useMemo` chain (~line 253): `useEffect(() => { if (backlog?.rankCustomFieldId && !rankFieldKey) { setRankFieldKey(\`customfield_${backlog.rankCustomFieldId}\`); } }, [backlog?.rankCustomFieldId, rankFieldKey, setRankFieldKey]);` — writes once on first discovery, guarded by `!rankFieldKey` so the background poll cannot re-trigger it. `useEffect` is already imported (line 33) — no import change.
    Then add a contract test to `taskflow/src/stores/settings.store.test.ts` in a new `describe('settings.store — rankFieldKey (Phase 76)')` block: assert (a) the persist version is 25, (b) default `rankFieldKey` is `null`, (c) calling `setRankFieldKey('customfield_10105')` (the composed key for the probe-verified `rankCustomFieldId: 10105`) sets state to `'customfield_10105'`. Follow the existing renderHook + act pattern in the file. The composition itself (`customfield_${id}`) is exercised by asserting the composed value matches the fixture id 10105 per D-11.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/stores/settings.store.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - BacklogPage destructures `rankFieldKey` and `setRankFieldKey` from `useSettingsStore()`.
    - Discovery useEffect present (grep: `grep -q "setRankFieldKey(\`customfield_" src/routes/dashboard/BacklogPage.tsx`), guarded by `!rankFieldKey`.
    - `settings.store.test.ts` has a `rankFieldKey` describe block asserting version 25, default null, and `setRankFieldKey('customfield_10105')` -> `'customfield_10105'`.
    - `npx vitest run src/stores/settings.store.test.ts` GREEN; `npm run check` clean.
  </acceptance_criteria>
  <done>BacklogPage populates rankFieldKey from backlog.rankCustomFieldId once; settings test asserts the v25 + composed-key contract; suite GREEN; biome+tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

`rankCustomFieldId` arrives from the GreenHopper backlog response (Jira -> app), an already-authenticated, already-trusted data path established in prior phases. The only new persisted datum is `rankFieldKey`, a non-secret field-id string.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | rankFieldKey persistence | n/a | No threats apply at ASVS L1: `rankFieldKey` is a non-secret string like `customfield_10105` (RESEARCH.md § Security Domain). No auth/session/crypto/access-control introduced; the discovery seam reuses the existing trusted backlog-response path and the established Tauri-Store persistence used for the other 7 discovered field keys. |

No `npm install` occurs in this plan — package legitimacy gate not applicable.
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/stores/settings.store.test.ts` — GREEN
- `cd taskflow && npm run check` — biome + tsc clean
</verification>

<success_criteria>
- Settings store at v25 persisting `rankFieldKey` (default null), preserved across a preferences reset.
- BacklogPage discovers and writes `customfield_${rankCustomFieldId}` exactly once.
- Contract test asserts the composed key for the probe-verified id 10105.
</success_criteria>

<output>
Create `.planning/phases/76-visual-polish-and-shared-primitives/76-03-SUMMARY.md` when done.
</output>
