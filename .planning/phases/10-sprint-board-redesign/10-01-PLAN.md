---
phase: 10-sprint-board-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/package.json
  - taskflow/package-lock.json
  - taskflow/src/services/jira.ts
  - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
  - taskflow/src/routes/dashboard/QuickCreateInput.test.tsx
  - taskflow/src/services/jira.test.ts
autonomous: true
requirements:
  - BOARD-01
  - BOARD-03
  - BOARD-04

must_haves:
  truths:
    - "@dnd-kit/core and @dnd-kit/utilities are importable in the codebase"
    - "fetchProjectStatuses is exported from jira.ts and flattens statuses across issue types"
    - "createIssue is exported from jira.ts and POSTs the minimal issue body"
    - "Test stubs exist for all Wave 0 MISSING test cases (fail in RED state — no implementation yet)"
  artifacts:
    - path: "taskflow/src/services/jira.ts"
      provides: "fetchProjectStatuses and createIssue exports"
      exports: ["fetchProjectStatuses", "createIssue", "JiraProjectStatus"]
    - path: "taskflow/src/routes/dashboard/SprintBoardTab.test.tsx"
      provides: "RED stubs for story-in-multiple-columns, drag rollback, workflow-API columns"
    - path: "taskflow/src/routes/dashboard/QuickCreateInput.test.tsx"
      provides: "RED stubs for show input on click, submit on Enter, hide on Escape"
    - path: "taskflow/src/services/jira.test.ts"
      provides: "RED stubs for fetchProjectStatuses and createIssue"
  key_links:
    - from: "taskflow/src/services/jira.ts"
      to: "GET /rest/api/2/project/{key}/statuses"
      via: "fetchProjectStatuses"
      pattern: "fetchProjectStatuses"
    - from: "taskflow/src/services/jira.ts"
      to: "POST /rest/api/2/issue"
      via: "createIssue"
      pattern: "createIssue"
---

<objective>
Install @dnd-kit packages, add two new jira.ts service functions, and write Wave 0 test stubs that will be RED until later plans implement the components they test.

Purpose: Establishes the dependency foundation — dnd-kit must be installed before any drag implementation, service functions must exist before BoardColumn and QuickCreateInput can call them, and RED stubs must exist before any wave can satisfy the Nyquist rule.
Output: @dnd-kit/core + @dnd-kit/utilities installed; fetchProjectStatuses and createIssue in jira.ts; RED test stubs in SprintBoardTab.test.tsx, QuickCreateInput.test.tsx, jira.test.ts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sprint-board-redesign/10-CONTEXT.md
@.planning/phases/10-sprint-board-redesign/10-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts already in jira.ts that new functions must align with. -->

From src/services/jira.ts (existing patterns):
```typescript
// apiFetch wrapper used by all service functions
import { apiFetch } from './tauri'  // used as: apiFetch('jira', url, options)

// Existing fetchTransitions signature (reference pattern):
export async function fetchTransitions(baseUrl: string, token: string, issueKey: string): Promise<JiraTransition[]>

// Existing postTransition signature (reference pattern):
export async function postTransition(baseUrl: string, token: string, issueKey: string, transitionId: string): Promise<void>

// JiraTransition interface (already exists):
export interface JiraTransition {
  id: string
  name: string
  to: { id: string; name: string; statusCategory: { key: string } }
}
```

New types to add:
```typescript
export interface JiraProjectStatus {
  id: string
  name: string
  statusCategory: { key: 'new' | 'indeterminate' | 'done' | string }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install @dnd-kit packages</name>
  <files>taskflow/package.json, taskflow/package-lock.json</files>
  <action>
    Run from the taskflow/ directory:
    ```
    npm install @dnd-kit/core @dnd-kit/utilities
    ```
    Verify @dnd-kit/core ^6.x and @dnd-kit/utilities ^3.x appear in package.json dependencies.
    Do NOT install @dnd-kit/react (new API, not production-ready — locked decision from STATE.md).
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && node -e "require('@dnd-kit/core')" && echo "dnd-kit/core ok" && node -e "require('@dnd-kit/utilities')" && echo "dnd-kit/utilities ok"</automated>
  </verify>
  <done>Both packages import without error; package.json lists @dnd-kit/core and @dnd-kit/utilities under dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fetchProjectStatuses and createIssue to jira.ts</name>
  <files>taskflow/src/services/jira.ts, taskflow/src/services/jira.test.ts</files>
  <behavior>
    fetchProjectStatuses:
    - Calls apiFetch('jira', `${baseUrl}/rest/api/2/project/${projectKey}/statuses`, { headers: { Authorization: `Bearer ${token}`, 'Content-Type': 'application/json' } })
    - Response is Array&lt;{ id: string, name: string, statuses: JiraProjectStatus[] }&gt; (one object per issue type)
    - Flattens statuses across all issue type objects, deduplicates by status id (first occurrence wins)
    - Returns JiraProjectStatus[] — never returns duplicates even if status appears in multiple issue type arrays
    - Throws with message "Failed to fetch project statuses: {status}" on non-ok response

    createIssue:
    - Calls apiFetch('jira', `${baseUrl}/rest/api/2/issue`, POST with JSON body)
    - Body: `{ fields: { project: { key: projectKey }, summary, issuetype: { name: 'Story' } } }`
    - Returns `{ id: string; key: string }` parsed from response JSON
    - Throws with message "Failed to create issue: {status}" on non-ok response
  </behavior>
  <action>
    Add after the existing updateIssueField function in jira.ts:

    1. Export interface JiraProjectStatus with fields: id (string), name (string), statusCategory ({ key: 'new' | 'indeterminate' | 'done' | string })

    2. Export async function fetchProjectStatuses(baseUrl, token, projectKey):
       - URL: `${baseUrl.replace(/\/$/, '')}/rest/api/2/project/${projectKey}/statuses`
       - Method: GET (default)
       - Headers: { Authorization: `Bearer ${token}`, 'Content-Type': 'application/json' }
       - Parse response as Array&lt;{ statuses: JiraProjectStatus[] }&gt;
       - Flatten + deduplicate by id using a Set
       - Return JiraProjectStatus[]

    3. Export async function createIssue(baseUrl, token, projectKey, summary):
       - URL: `${baseUrl.replace(/\/$/, '')}/rest/api/2/issue`
       - Method: POST
       - Headers: { Authorization: `Bearer ${token}`, 'Content-Type': 'application/json' }
       - Body: JSON.stringify({ fields: { project: { key: projectKey }, summary, issuetype: { name: 'Story' } } })
       - Return `{ id: string; key: string }` from response.json()

    In jira.test.ts, add describe blocks for fetchProjectStatuses and createIssue with RED stub tests
    (tests that describe the expected behavior but call the real function against a mocked fetch — they
    should pass immediately since the implementation is written before tests in this TDD exception:
    both the function and test are added in the same task, so write the test first, implement to pass).

    Test stubs to add to jira.test.ts (add inside the existing file, do not replace existing tests):
    - "fetchProjectStatuses: flattens statuses across issue types"
      - Mock response: [{ statuses: [{ id: '1', name: 'To Do', statusCategory: { key: 'new' } }] }, { statuses: [{ id: '1', name: 'To Do', statusCategory: { key: 'new' } }, { id: '2', name: 'In Progress', statusCategory: { key: 'indeterminate' } }] }]
      - Expect result.length === 2 (id '1' deduplicated)
      - Expect result[0].name === 'To Do', result[1].name === 'In Progress'
    - "createIssue: POSTs correct body and returns id and key"
      - Mock response: { id: '10001', key: 'PROJ-42' }
      - Expect fetch called with POST, body includes project.key, summary, issuetype.name 'Story'
      - Expect return value equals { id: '10001', key: 'PROJ-42' }
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/services/jira.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>jira.test.ts passes including the new fetchProjectStatuses and createIssue tests; both functions exported from jira.ts with correct TypeScript types.</done>
</task>

<task type="auto">
  <name>Task 3: Write Wave 0 RED test stubs for SprintBoardTab and QuickCreateInput</name>
  <files>taskflow/src/routes/dashboard/SprintBoardTab.test.tsx, taskflow/src/routes/dashboard/QuickCreateInput.test.tsx</files>
  <action>
    Add RED stub tests to SprintBoardTab.test.tsx (append to existing file after existing tests).
    These tests MUST fail (RED) because the components they test have not been rebuilt yet.
    Do not modify the existing passing tests.

    Stubs to add to SprintBoardTab.test.tsx:
    1. "renders workflow-API-derived columns (BOARD-01)" — mock fetchProjectStatuses to return [{ id: '1', name: 'To Do', statusCategory: { key: 'new' } }, { id: '2', name: 'In Progress', statusCategory: { key: 'indeterminate' } }], render SprintBoardTab, expect column headers 'To Do' and 'In Progress' are visible. (RED: current board derives columns from issue statuses, not workflow API)
    2. "story header appears in each column that has its subtasks (BOARD-01)" — mock fetchSprintIssues to return a story (PROJ-1, 'To Do') with one subtask (PROJ-2, parent PROJ-1, 'In Progress'). Expect PROJ-1 header text appears in both 'To Do' column area and 'In Progress' column area. (RED: current board doesn't show story headers in subtask columns)
    3. "onDragEnd moves card optimistically (BOARD-03)" — render SprintBoardTab with mocked issue data, find a board column, call handleDragEnd mock with valid active/over data, assert the card appears in the target column. Use the mock DragEndEvent pattern from RESEARCH.md. (RED: no drag support yet)
    4. "onDragEnd rollback: card reverts when postTransition throws (BOARD-03)" — mock postTransition to reject, call handleDragEnd mock, assert card returns to original column. (RED: no drag support yet)

    Also add mock for fetchProjectStatuses in the vi.mock('@/services/jira') block at top of SprintBoardTab.test.tsx:
    Add `fetchProjectStatuses: vi.fn().mockResolvedValue([])` to the existing mock factory.

    Create NEW file QuickCreateInput.test.tsx:
    - Import describe, it, expect, vi from vitest
    - Import render, screen, fireEvent from @testing-library/react
    - vi.mock('@/services/jira', ...) mocking createIssue
    - vi.mock('@/services/stronghold', ...) mocking readSecret
    - Describe 'QuickCreateInput':
      1. "shows text input when + Add button is clicked" — render component with statusName='To Do', click the '+ Add' button, expect a text input to be visible. (RED: component doesn't exist yet)
      2. "calls createIssue with correct args and hides input after Enter" — render, click Add, type 'My new story', press Enter, expect createIssue called with 'To Do'. (RED: no component yet)
      3. "hides input when Escape is pressed without creating" — render, click Add, type text, press Escape, expect input hidden and createIssue not called. (RED: no component yet)

    All new stubs MUST fail when running vitest — do not add skip() or todo() markers. They should fail because the components/functions they test don't yet implement the behavior.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/SprintBoardTab.test.tsx src/routes/dashboard/QuickCreateInput.test.tsx 2>&1 | grep -E "Tests|FAIL|PASS|failed|passed" | tail -5</automated>
  </verify>
  <done>QuickCreateInput.test.tsx exists. SprintBoardTab.test.tsx has 4 new stubs. Existing passing tests in SprintBoardTab.test.tsx still pass. New stubs fail (RED) because implementation hasn't changed yet.</done>
</task>

</tasks>

<verification>
1. `cd taskflow && node -e "require('@dnd-kit/core')"` exits 0
2. `grep "fetchProjectStatuses\|createIssue\|JiraProjectStatus" src/services/jira.ts` shows all three
3. `npx vitest run src/services/jira.test.ts` — all tests pass including new ones
4. `npx vitest run src/routes/dashboard/SprintBoardTab.test.tsx` — existing tests pass, 4 new stubs fail
5. `ls src/routes/dashboard/QuickCreateInput.test.tsx` — file exists
</verification>

<success_criteria>
- @dnd-kit/core and @dnd-kit/utilities in package.json dependencies
- fetchProjectStatuses exported from jira.ts with JiraProjectStatus interface
- createIssue exported from jira.ts with minimal Story body
- jira.test.ts passes with new fetchProjectStatuses + createIssue tests
- SprintBoardTab.test.tsx has 4 new RED stubs (board tests still pass, drag/layout stubs fail)
- QuickCreateInput.test.tsx exists with 3 RED stubs
</success_criteria>

<output>
After completion, create `.planning/phases/10-sprint-board-redesign/10-01-SUMMARY.md`
</output>
