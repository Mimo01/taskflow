---
phase: 91.1-unified-release-detail-task-table
plan: 04
type: execute
wave: 3
depends_on: ["91.1-03"]
files_modified:
  - taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx
autonomous: true
requirements: [MRFIX-01, MRFIX-02, MRFIX-03, MRFIX-04, DRIFT-05, DRIFT-06, DRIFT-08]
must_haves:
  truths:
    - "Every Phase 90 corrective-action behaviour still has a passing test after moving to the new component"
    - "The four MR-slot states are proven mutually exclusive, with pending beating resolved-empty"
    - "The secondary table is proven absent when there are no uncovered MRs"
    - "Hover-reveal is proven scoped to a single MR sub-line, not the task block"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx"
      provides: "Relocated DriftActionCell state-machine coverage plus new D-04/D-05/D-09/D-13/D-17 cases"
      min_lines: 400
  key_links:
    - from: "UnifiedTaskTable.test.tsx"
      to: "UnifiedTaskTable.tsx"
      via: "render() with primaryRows/secondaryRows fixtures"
      pattern: "UnifiedTaskTable"
---

<objective>
Close the Nyquist Wave 0 component gap: relocate the Phase 90 corrective-action coverage
onto the new component and add the cases the new row shape introduces.

This plan resolves **RESEARCH Open Question 3** (test file consolidation). The strategy is
split-by-tier, not rename-in-place:
  - Pure data-layer cases (attachment, partition, counts) already went to
    `driftDetection.test.ts` in plan 01.
  - Component cases go here, in a new `UnifiedTaskTable.test.tsx`.
  - `MrDriftSection.test.tsx` is deleted in plan 05, after this file proves the coverage has
    landed. It is not renamed, because roughly a third of its content (the `applyHeldOrder`
    block and the `drift-task` assertions) is deliberately being dropped, and a rename would
    obscure which losses were intentional.

**Coverage-preservation rule (91.1-VALIDATION.md):** every `it` in
`MrDriftSection.test.tsx`'s `describe('per-MR corrective actions')` block must have a
passing counterpart here. The only sanctioned losses are the `applyHeldOrder` /
`heldForVersion` block (D-18 deletes the mechanism) and assertions that exist solely to
check the `drift-task` cell (criterion 4 drops the column).

Purpose: makes plan 05's deletion safe.
Output: one new component test file.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91.1-unified-release-detail-task-table/91.1-VALIDATION.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-CONTEXT.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relocate the Phase 90 corrective-action state-machine coverage</name>

  <files>taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx</files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/MrDriftSection.test.tsx` (whole file — its mocks, `QueryClient` wrapper, fixture builders, and especially `describe('per-MR corrective actions')` at :437-755, which is the block being relocated case-for-case)
    - `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` (the component under test — its props contract, test ids, and where `DriftActionCell` is reached from)
    - `taskflow/src/routes/dashboard/release-detail/useMrFixMutation.ts` (what the tests mock around)
  </read_first>

  <behavior>
    One relocated case per existing `it` in `describe('per-MR corrective actions')`:
    - pending: clicking a flagged BR cell whose write never resolves leaves a spinner; a second click does not re-fire
    - a11y: keyboard focus survives pending and the failure, and the error is announced via the live region
    - success: with the write resolved and the cache-derived mark now `ok`, the cell renders the green check and is no longer a button
    - sticky failure: the cell ends red with the exact tooltip and survives an `invalidateQueries` sweep
    - retry: clicking the red cell fires the write a second time and the red state clears while pending
    - independent locks (MRFIX-03, D-09): BR and MS on one MR lock independently and issue two calls with disjoint bodies
    - error + prerequisite gone: the cell stops being a retry button and explains itself
    - error cleared: a refetch showing the field now `ok` replaces the red retry with the green check
    - unavailable (MRFIX-04): with no release branch, `drift-br` is not a button, clicking calls nothing, and `drift-ms` on the same MR is still actionable
    - inert cells: `ok`, `na` and non-evaluated rows render no button
    - degraded: with no matched milestone the banner renders and neither BR nor MS is a button
    - BR and MS carry identical `aria-label` and `title` strings
    - no toast/alert element is rendered outside the row on failure
    Plus the title-rendering block: `matchTicketKeyInTitle` unit cases and the
    "renders the full title verbatim" cases for non-literal ticket keys (CR-01).
  </behavior>

  <action>
Create `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx`.

Port `MrDriftSection.test.tsx`'s test harness — the same module mocks, the same
`QueryClient` wrapper, the same MR/DriftRow fixture builders — adapting only the render
call. Instead of `<MrDriftSection rows={...} />`, render `<UnifiedTaskTable ... />` with the
new props, wrapping each fixture drift row in a `primaryRows` entry
(`[{ issue, mrs: [row] }]`) with a minimal `JiraIssue` fixture, and passing
`isLoadingDrift={false}` so the sub-lines actually render (an MR sub-line is unreachable
while the pending branch holds). Add a small local helper that builds this props object with
sensible defaults and accepts overrides, so each `it` stays as short as its predecessor.

Relocate every case listed in `<behavior>` above, one for one, preserving the original test
names where they still describe the behaviour (they cite `D-XX`/`MRFIX-XX` ids that must
survive). `drift-br` and `drift-ms` test ids are unchanged, so most queries port directly.

**Sanctioned edits during relocation:**
  - Delete assertions on `screen.getByTestId('drift-task')`. Where an `it` asserted BOTH
    drift-task and something else (e.g. the "inert cells" and "non-evaluated row" cases),
    keep the case and drop only the `drift-task` line — do not drop the whole test.
  - Do not port `describe('held sort order (D-11)')` at all, nor the `applyHeldOrder`
    import. That mechanism no longer exists.
  - Replace the "renders rows in the given order without re-sorting" case with an
    equivalent that asserts `primaryRows` order is preserved in the DOM, since row order is
    now the component's contract rather than a frozen-ref side effect.
  - The "renders the empty-state copy when rows is empty" case becomes the primary-table
    empty case asserting `No issues in this fix version`.
  - The "renders the flagged count in the heading badge" case now reads
    `flagged-count-badge`.

Run mode only: `npx vitest run`, never watch.
  </action>

  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `cd taskflow && npx vitest run src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx` exits 0 with zero failures
    - The number of `it(` in `UnifiedTaskTable.test.tsx` is ≥ (number of `it(` in `MrDriftSection.test.tsx`'s `describe('per-MR corrective actions')` block + its title-rendering block), verified by counting both
    - Every `MRFIX-0` and `D-0`/`D-1` id appearing in a test name inside `MrDriftSection.test.tsx`'s corrective-actions block also appears in a test name in the new file, except `D-11` (held order)
    - `grep -c "applyHeldOrder\|heldForVersion\|drift-task" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx` returns 0
    - `grep -c "drift-br" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx` is ≥ 8
    - `grep -cE "vitest\b(?!.*run)|--watch" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx` returns 0 (no watch-mode invocation anywhere)
    - `MrDriftSection.test.tsx` is still present and still green at the end of this task (it is deleted in plan 05, not here)
  </acceptance_criteria>

  <done>Every Phase 90 corrective-action and title-rendering case has a passing counterpart against the new component; only the held-order and TASK-cell assertions were dropped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New cases for the row shape this phase introduces</name>

  <files>taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx</files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx` (the harness built in task 1 — reuse its props-builder helper)
    - `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` (the exact test ids and DOM structure to query)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-VALIDATION.md` §"Wave 0 Requirements" (the case list this task closes) and §"Manual-Only Verifications" (what is deliberately NOT asserted here)
  </read_first>

  <behavior>
    D-17 — four mutually exclusive MR-slot states:
    - `isLoadingDrift: true` with `mrs: []` renders `mr-slot-pending` and neither `mr-slot-none` nor `mr-slot-unavailable`
    - `isLoadingDrift: true` with a task that will resolve to zero MRs still shows no `⚠ No merge request` (the false-alarm flash guard)
    - `isLoadingDrift: false`, `mrs: []`, `hasMatchedMilestone: true` renders `mr-slot-none` only
    - `isLoadingDrift: false`, `mrs: []`, `hasMatchedMilestone: false` renders `mr-slot-unavailable` only, with the "cannot check for MRs" tooltip and no orange warning
    - `isLoadingDrift: false` with two MRs renders two `drift-row` elements and no slot-state line

    D-04 — task row click behaviour:
    - clicking the row body (`task-row-overlay`) calls `onSeedBreadcrumb` then `onOpenIssue` with the issue key, and does NOT call `onOpenIssueFull`
    - clicking the key button calls `onOpenIssueFull` and does NOT call `onOpenIssue`
    - the task row container is not `role="button"` and the overlay is a sibling of the key button, not its ancestor
    - an MR sub-line has no line-wide click target: clicking its title text (away from a linkified key) calls none of the row handlers

    D-05 — hover-reveal scope:
    - `group/row` is present on each MR sub-line element and absent from the task row element
    - with two MR sub-lines under one task, the two sub-lines are separate group roots (assert on the rendered class placement, since jsdom does not evaluate `:hover`)

    D-06 / criterion 4 — task row gutter:
    - a task row renders no `drift-br`/`drift-ms`/`drift-task` element of its own; the only such elements in the tree belong to MR sub-lines

    D-09 — duplication:
    - the same `DriftRow` supplied under two `primaryRows` entries renders two `drift-row` elements, each with its own actionable `drift-br` button, and firing one leaves the other's local error state untouched

    D-13 — secondary table:
    - `secondaryRows: []` renders no `secondary-section` and no "Not covered by tasks above" text at all
    - a non-empty `secondaryRows` renders the section, an out-of-scope-key row showing the key with the `"{KEY} is not in fix version {versionName}"` tooltip, a keyless row showing `—`, and both rows carrying actionable `drift-br`/`drift-ms` cells (D-10)

    D-14/D-15 — header:
    - the done badge and the flagged-count badge both render, the latter showing the passed `flaggedMrCount`

    D-16 — banner:
    - with no matched milestone exactly one `drift-degraded-banner` renders in the whole tree
  </behavior>

  <action>
Append new `describe` blocks to `UnifiedTaskTable.test.tsx` covering every case in
`<behavior>` above, one `it` per bullet, naming the decision id in each test name to match
the file's convention.

For D-05 and D-06, assert on rendered structure rather than simulated hover — jsdom does not
evaluate `:hover`, and 91.1-VALIDATION.md already routes the visual hover confirmation to
manual UAT. Query the sub-line element and assert its `className` contains `group/row`, and
assert the task-row element's `className` does not. Cite that reasoning in a comment so the
assertion is not later "upgraded" into a fake hover test.

For D-09's independent-error case, reuse task 1's failing-write mock: fire the `drift-br`
cell in the first rendered copy, let it settle to the red retry state, and assert the second
copy's `drift-br` is still the orange actionable button. This is the defect CONTEXT D-09
explicitly calls out.

For the D-17 flash guard, assert the negative directly —
`expect(screen.queryByTestId('mr-slot-none')).toBeNull()` while `isLoadingDrift` is true —
rather than inferring it from the presence of the pending line.

Do not add tests for the three items in 91.1-VALIDATION.md's Manual-Only table (real
streaming across three independently-resolving queries, ordering stability under live
mutation, per-row truncation) — those are plan 05's human checkpoint.

Run mode only: `npx vitest run`.
  </action>

  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx && npm test</automated>
  </verify>

  <acceptance_criteria>
    - `cd taskflow && npx vitest run src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx` exits 0
    - `cd taskflow && npm test` exits 0 (full suite, wave gate per 91.1-VALIDATION.md)
    - Test names covering `D-04`, `D-05`, `D-06`, `D-09`, `D-13`, `D-14`, `D-15`, `D-16` and `D-17` all exist in the file — `grep -c "D-17" ...` is ≥ 3
    - A test asserts `queryByTestId('mr-slot-none')` is null while `isLoadingDrift` is true
    - A test asserts `queryByTestId('secondary-section')` is null when `secondaryRows` is empty
    - A test asserts exactly one `drift-degraded-banner` in the tree when no milestone matched
    - A test asserts a red/error `drift-br` in one duplicated copy leaves the other copy's `drift-br` an orange actionable button
    - `grep -c "fireEvent.mouseOver\|userEvent.hover" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx` returns 0 (hover scope is asserted structurally)
  </acceptance_criteria>

  <done>All Wave 0 component gaps from 91.1-VALIDATION.md are closed with passing tests, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → component | Fixtures stand in for remote GitLab/Jira data, including hostile-shaped MR titles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91.1-05 | Tampering | MR title rendering | mitigate | The relocated CR-01 title cases assert non-literal and unmatched ticket keys render the title verbatim rather than being sliced or executed, preserving Phase 89's proof that remote text stays inert |
| T-91.1-06 | Tampering | wrong-MR write under D-09 duplication | mitigate | A dedicated test fires a fix in one duplicated copy and asserts the other copy's cell state is untouched — the direct guard against cross-wired mutation state |
| T-91.1-08 | Repudiation | unannounced failure | mitigate | The relocated a11y case asserts the live region announces the error text and that no toast escapes the row |
| T-91.1-SC | Tampering | package installs | accept | No installs — Vitest and `@testing-library/react` are already configured |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx` green
- `cd taskflow && npm test` green
- Case-for-case coverage parity with `MrDriftSection.test.tsx`'s corrective-actions block confirmed by count
</verification>

<success_criteria>
`UnifiedTaskTable.test.tsx` carries every relocated Phase 90 corrective-action case plus new
passing coverage for D-04, D-05, D-06, D-09, D-13, D-14/D-15, D-16 and D-17, making
`MrDriftSection.test.tsx` safe to delete.
</success_criteria>

<output>
Create `.planning/phases/91.1-unified-release-detail-task-table/91.1-04-SUMMARY.md` when done
</output>
