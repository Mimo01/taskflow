---
phase: 91.1-unified-release-detail-task-table
plan: 03
type: execute
wave: 2
depends_on: ["91.1-01"]
files_modified:
  - taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx
autonomous: true
requirements: [DRIFT-05, DRIFT-06, DRIFT-07, DRIFT-08, MRFIX-01, MRFIX-02, MRFIX-03, MRFIX-04]
must_haves:
  truths:
    - "One table keyed on Jira tasks renders key, summary, assignee and status per task"
    - "Every MR for a task renders as a stacked sub-line inside that task's block — !iid, linkified title, author, state badge — with no interaction needed"
    - "BR and MS fix cells work per MR sub-line with Phase 90's pending/error/retry and independent per-cell locking"
    - "A task with zero MRs shows a warning line when the app verified absence, and a neutral unavailable line when it could not check"
    - "While MR channels are pending, no task shows a false 'No merge request'"
    - "A secondary table lists only uncovered MRs and is absent entirely when there are none"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx"
      provides: "The unified task↔MR table replacing IssuesSection and MrDriftSection"
      exports: ["UnifiedTaskTable", "MrFixContext", "matchTicketKeyInTitle"]
      min_lines: 350
  key_links:
    - from: "UnifiedTaskTable.tsx"
      to: "useMrFixMutation"
      via: "DriftActionCell, one hook instance per (MR, action)"
      pattern: "useMrFixMutation\\("
    - from: "UnifiedTaskTable.tsx"
      to: "driftDetection DriftRow"
      via: "primaryRows/secondaryRows props"
      pattern: "DriftRow"
---

<objective>
Build `UnifiedTaskTable.tsx` — the single div+flex table that replaces both `IssuesSection`
and `MrDriftSection`. Task rows carry Jira identity; MR sub-lines carry the re-hosted
Phase 90 BR/MS corrective-action cells; a conditional secondary table carries the MRs no
task covers.

This plan **creates the new file only**. `IssuesSection.tsx` and `MrDriftSection.tsx` stay
on disk and stay wired to the page until plan 05, so the tree compiles at every wave. That
also means `MrDriftSection.tsx` is readable as the source to copy from — copy its function
bodies, do not paraphrase them.

Purpose: criteria 1-5 of the phase all land in this file.
Output: one new presentational component, fetching nothing and reading no store (P87 D-08).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91.1-unified-release-detail-task-table/91.1-CONTEXT.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-UI-SPEC.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-PATTERNS.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Module scaffold — moved primitives, props contract, header, banner, shell states</name>

  <files>taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx</files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx` (whole file — `MrFixContext` :48-55, `CHANNEL_NAMES` :76-80, `matchTicketKeyInTitle` :93-104, `channelsTitle` :106-111, `DriftMarkCell` :113-137, `DriftActionCell` :139-311, header badge :346-353, degraded banner :358-369, column header strip :385-406)
    - `taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx` (whole file — heading + done Badge + `Progress` :34-52, milestone warning banner :54-63, loading + empty copy :66-72)
    - `taskflow/src/routes/dashboard/release-detail/driftDetection.ts` (`DriftRow`, `DriftMark`, `Channel`, and plan 01's `buildTaskMrAttachment` return shape)
    - `taskflow/src/routes/dashboard/release-detail/useMrFixMutation.ts` (the hook contract `DriftActionCell` consumes — do not modify it)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-UI-SPEC.md` §"Component & Layout Contract" and §"Copywriting Contract" (both LOCKED — copy strings verbatim)
  </read_first>

  <action>
Create `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx`.

**Move verbatim from `MrDriftSection.tsx`** (copy the function bodies unchanged — these are
explicitly "re-hosted, not reimplemented"): `MrFixContext` (keep it `export`ed, same six
fields), `CHANNEL_NAMES`, `matchTicketKeyInTitle` (keep `export`ed — it has its own tests),
`channelsTitle`, `DriftMarkCell`, and `DriftActionCell` including its `lastMarkRef`
render-phase reset, its `liveRegion`, and all five status/mark branches. The **only** edit
permitted to `DriftActionCell` in this plan is none — its `group-hover/row:` /
`group-focus-visible/fix:` class targets stay exactly as written; task 2 changes which
element owns `group/row`, not the cell.

**Do not copy** `applyHeldOrder` (:12-41), the `orderRef` / `heldForVersion` refs
(:330-341), or the `versionId` prop (:73). D-18 deletes that mechanism; it has no
destination. The new file must import `useRef` only for `DriftActionCell`'s `lastMarkRef`.

**Add one local helper** `mrStateBadgeClass(state: string): string` returning the existing
ternary `border-green-500 text-green-600` (merged) / `border-blue-500 text-blue-600`
(opened) / `border-gray-400 text-gray-500` (other). Both source files repeat this ternary;
extract once rather than copying it a third time.

**Column grid constants.** Declare module-level string constants for the shared span widths
so task rows, MR sub-lines and secondary rows provably use the same grid (D-02) — explicit
px throughout, never `%` or an unconstrained narrow `flex-1` (recorded WebKit/Tauri
zero-width-column collapse):
`COL_KEY = 'flex-none w-[72px]'`, `COL_SUMMARY = 'flex-1 min-w-0'`,
`COL_PERSON = 'flex-none w-[140px] min-w-0'`, `COL_STATE = 'flex-none w-[96px]'`.
The BR/MS gutters keep `DriftMarkCell`/`DriftActionCell`'s own inherited
`flex-none w-[28px]` — do not re-declare or change it.

**Props contract.** Export `interface UnifiedTaskTableProps` with exactly:
`issueCounts: { issuesFixed: number; issuesTotal: number } | undefined`,
`versionName: string`, `hasReleaseDate: boolean`, `isLoadingIssues: boolean`,
`isLoadingDrift: boolean`, `hasMatchedMilestone: boolean`,
`primaryRows: Array<{ issue: JiraIssue; mrs: DriftRow[] }>`, `secondaryRows: DriftRow[]`,
`flaggedMrCount: number`, `onOpenIssue: (key: string) => void`,
`onOpenIssueFull: (key: string) => void`, `onSeedBreadcrumb: () => void`,
`onNavigateToIssueFromMR: (key: string) => void`, `fix: MrFixContext`.
No `versionId`, no `gitlabMatchType`, no `matchedRows`, no `wrongMilestoneByKey`.

`versionName` exists solely for D-11's out-of-scope tooltip template
(`"{KEY} is not in fix version {versionName}"`) built in task 3.

**Dropping `gitlabMatchType`:** the banner and the degraded sub-line both key off
`hasMatchedMilestone`. Before finalising, read `taskflow/src/services/releaseLinker.ts`'s
`resolveGitLabMatch` and confirm `gitlabMatch.type === 'none'` holds exactly when
`matchedMilestone === null`. If they are equivalent, use `hasMatchedMilestone` alone and
omit the prop. If they are NOT equivalent, keep `gitlabMatchType: ReleaseMatch['type']` as
a prop and gate the banner on it, preserving today's behaviour — and say so in the summary.

**Render the shell** (`export function UnifiedTaskTable(...)` returning a `<section>`):

1. Header row (D-14/D-15) — `<h3>` "Issues" at `text-sm font-medium text-muted-foreground`,
   then the existing `N / M done` `Badge variant="secondary"` when `issueCounts` is set,
   then the flagged-count `Badge variant="secondary"` carrying `{flaggedMrCount}` placed
   immediately after the done badge with the same `gap`/`ml-1.5` rhythm as
   `MrDriftSection.tsx:349`. Give the flagged badge
   `data-testid="flagged-count-badge"` and `title="Merge requests needing attention"`.
2. `Progress` bar, copied verbatim from `IssuesSection.tsx:46-52` (same `max-w-xs mb-4`,
   same `indicatorClassName="bg-green-500"`, same rounding expression).
3. Degraded banner (D-16) — ONE banner, rendered when the milestone did not match, carrying
   `data-testid="drift-degraded-banner"` (test-id continuity, CONTEXT canonical_refs) and
   `IssuesSection.tsx:55-63`'s exact markup and copy: `"No GitLab milestone matched — MR
   linking is unavailable."` plus `' Set a release date to enable milestone matching.'`
   when `!hasReleaseDate`. Do not render a second banner anywhere in this file.
4. Primary body: when `isLoadingIssues`, the `Loader2` + `"Loading issues..."` block from
   `IssuesSection.tsx:66-70`; else when `primaryRows.length === 0`, the verbatim
   `"No issues in this fix version"` paragraph; else the column header strip followed by an
   empty `<div data-testid="task-list" />` region that task 2 fills.
5. Column header strip: one flex row at `text-xs text-muted-foreground font-medium py-1
   bg-muted/30`, with empty spans at `COL_KEY`, `COL_SUMMARY`, `COL_PERSON`, `COL_STATE`
   and then the two labelled 28px gutters `BR` (title
   `"Target branch matches release branch"`) and `MS` (title
   `"Release milestone assigned"`), copied from `MrDriftSection.tsx:385-399`. **Omit the
   TASK span entirely** (criterion 4) — do not reserve its width.

The component must fetch nothing, read no store, and import no service module other than
types plus `extractTicketKeys` (P87 D-08).
  </action>

  <verify>
    <automated>cd taskflow && npx tsc --noEmit && npx vitest run src/routes/dashboard/release-detail</automated>
  </verify>

  <acceptance_criteria>
    - File `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` exists and exports `UnifiedTaskTable`, `MrFixContext`, `matchTicketKeyInTitle`
    - `grep -c "applyHeldOrder\|orderRef\|heldForVersion\|versionId" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 0
    - `grep -c "drift-task" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 0
    - `grep -c "drift-degraded-banner" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 1
    - `grep -c "No GitLab milestone matched — MR linking is unavailable." taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 1
    - `grep -c "No issues in this fix version" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 1
    - `grep -c "border-green-500 text-green-600" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 1 (state ternary extracted once, not triplicated)
    - `grep -cE "useQuery|useStore|fetch[A-Z]" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 0 (presentational-only, P87 D-08)
    - A diff of the copied `DriftActionCell` body against `MrDriftSection.tsx:149-311` shows no behavioural change (only the surrounding module differs)
    - `cd taskflow && npx tsc --noEmit` reports zero errors
    - `cd taskflow && npx vitest run src/routes/dashboard/release-detail` exits 0 (existing suites unaffected)
  </acceptance_criteria>

  <done>The module compiles with all Phase 90 primitives re-hosted verbatim, the merged header, the single degraded banner, the shared column constants, and the loading/empty shell states.</done>
</task>

<task type="auto">
  <name>Task 2: Task rows, MR sub-lines and the four mutually-exclusive MR-slot states</name>

  <files>taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx</files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` (current state from task 1)
    - `taskflow/src/routes/standup-notes/IssueActivityGroup.tsx` lines 194-240 (`ActivityGroupHeader` — the codebase's proven overlay-button pattern D-04 mandates; copy its structure, including which children get `pointer-events-none` and which get `relative z-10`)
    - `taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx` lines 408-520 (the per-MR row JSX: `!iid` button with `channelsTitle`, the title-linkification IIFE, `CachedAvatar` + author, the state `Badge`, and the `row.evaluated` branch selecting action cells vs `na` mark cells)
    - `taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx` lines 106-130 (task-row column content: summary `line-clamp-1`, assignee/Unassigned avatar treatment, status pill)
    - `taskflow/src/lib/statusStyles.ts` (`statusPillClass`)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-UI-SPEC.md` §"Row anatomy" and §"State line variants inside the MR-slot"
  </read_first>

  <action>
Fill the `data-testid="task-list"` region with `primaryRows.map(...)`, preserving the
incoming array order exactly — no `.sort()`, no `.slice().sort()`, no ref-held order
anywhere in this file (D-18, criterion 6).

**`TaskRow` (D-01/D-03/D-04/D-06).** One `div` per task, `data-testid="task-row"`, classes
`relative flex items-center gap-2 text-sm py-1.5 border-b border-border/50
hover:bg-muted/40`. Children, in grid order:
  - The overlay button FIRST: `<button type="button">` with
    `className="absolute inset-0 rounded cursor-pointer focus-visible:ring-2
    focus-visible:ring-ring"`, `aria-label={`Open ${issue.key} ${issue.fields.summary}`}`,
    `data-testid="task-row-overlay"`, `onClick` calling `onSeedBreadcrumb()` then
    `onOpenIssue(issue.key)`. It is a **sibling** of the key button, never a wrapper, and
    the row `div` must never be `role="button"` — the recorded nested-interactive lesson.
  - Key button: `relative z-10` + `COL_KEY` + `font-mono text-xs text-primary
    hover:underline text-left`, `onClick` calling `onSeedBreadcrumb()` then
    `onOpenIssueFull(issue.key)`. No `stopPropagation()` is needed or wanted — the overlay
    is a sibling, so the click does not bubble through it. Do not port
    `IssuesSection.tsx`'s `<tr onClick>` + `stopPropagation()` handling; that pattern does
    not transfer to div+flex rows.
  - Summary span: `pointer-events-none relative` + `COL_SUMMARY` + `line-clamp-1`.
  - Assignee: `pointer-events-none relative` + `COL_PERSON`, `inline-flex items-center
    gap-1.5 text-xs`, `CachedAvatar size={20}` with the issue assignee's `avatarUrls['48x48']`
    and `displayName`, falling back to the `url={null} name="Unassigned"` treatment from
    `IssuesSection.tsx:120-123`.
  - Status: a `pointer-events-none relative` wrapper `div` with `COL_STATE` plus
    `flex items-center`, containing the `<span className={statusPillClass(...)}>`. The pill
    must sit inside a flex parent — its `min-w`/`text-center` silently collapse on a bare
    inline span (recorded lesson). Do not add geometry classes to the span itself.
  - Two empty 28px gutter spans (`flex-none w-[28px]`, `pointer-events-none`) so the task
    row's grid stays aligned with the sub-lines below. **No rollup glyph, no count chip**
    (D-06) — these spans render nothing.

**MR-slot, immediately after each task row** — exactly one of four mutually exclusive
branches (D-07/D-08/D-17). This is a state-machine correctness requirement, not a visual
preference: branch on `isLoadingDrift` FIRST, so states 2 and 3 can never render while MR
queries are in flight.
  1. `isLoadingDrift` → `data-testid="mr-slot-pending"`, indented sub-line with
     `Loader2 className="size-3.5 animate-spin"` and the copy `loading merge requests…`.
  2. else `mrs.length === 0 && hasMatchedMilestone` → `data-testid="mr-slot-none"`,
     `AlertTriangle className="size-3.5"` in `text-orange-600 dark:text-orange-400` and the
     copy `No merge request`.
  3. else `mrs.length === 0 && !hasMatchedMilestone` → `data-testid="mr-slot-unavailable"`,
     `text-muted-foreground`, copy `— MR status unavailable`, `title="No GitLab milestone
     matched — cannot check for MRs"`. This branch must never render the orange warning —
     the app did not verify absence, it was unable to check.
  4. else `mrs.map(...)` rendering `MrSubLine`, keyed by `row.mr.id`.
  All three state lines use `pl-4 flex items-center gap-2 text-xs py-1` (16px indent per
  D-03) and carry no border.

**`MrSubLine` (D-02/D-03/D-05/D-09, criteria 2/3).** One `div` per MR, keyed `row.mr.id`,
`data-testid="drift-row"` (test-id continuity), classes
`group/row pl-4 flex items-center gap-2 text-xs py-1 text-muted-foreground` — **no
`border-b`** between sibling MR lines; only the task row carries the separator, so a task
plus its MRs reads as one block. `group/row` sits on THIS element, never on the task row —
if it stays on the task row, D-05 breaks silently because the class names still resolve.
Hovering one sub-line must reveal only its own action icons.

Children, using the shared grid constants:
  - `COL_KEY` `!iid` button — `font-mono text-xs hover:underline` with no colour override,
    `onClick={() => openUrl(mr.web_url)}`, `title={channelsTitle(row.channels)}` (the
    channel-provenance tooltip is retained — CONTEXT's conservative default).
  - `COL_SUMMARY` span with `truncate` containing the linkification IIFE copied verbatim
    from `MrDriftSection.tsx:441-475` — `extractTicketKeys` + `matchTicketKeyInTitle`,
    emitting `<button className="text-primary hover:underline font-mono">` per matched key
    calling `onNavigateToIssueFromMR(k)`. Keep the `e.stopPropagation()` in that handler.
    Render title text as React children only — never `dangerouslySetInnerHTML`.
  - `COL_PERSON` author: `CachedAvatar url={mr.author.avatar_url} name={mr.author.name}
    size={20}` plus the name.
  - `COL_STATE` state `Badge variant="outline"` with `text-[10px] justify-center` and
    `mrStateBadgeClass(mr.state)`.
  - BR/MS gutters: the `row.evaluated` branch copied verbatim from
    `MrDriftSection.tsx:493-517` — two `DriftActionCell`s (`action="retarget"`
    `mark={row.br}` `testId="drift-br"`, `action="assign-milestone"` `mark={row.ms}`
    `testId="drift-ms"`, both `fix={fix}`) when evaluated, two `DriftMarkCell mark="na"`
    otherwise. **Delete the third cell** — no `row.task` cell, no `drift-task` test id
    (criterion 4).

The MR sub-line has **no line-wide click target** (D-04) — only `!iid`, the linkified keys
and the BR/MS cells are interactive. Do not add an overlay button or an `onClick` to the
sub-line container.

D-09 note: the same `DriftRow` can be rendered under two different tasks. That is correct
and intended. Each render site instantiates its own `useMrFixMutation` per (MR, action)
inside `DriftActionCell`, and both read the same TanStack query cache, so a successful fix
in one copy settles in the other; the sticky local error stays scoped to the cell the user
actually clicked. Do not attempt to hoist, share, or dedupe the mutation state across
copies — hoisting would break MRFIX-03's independent retry.
  </action>

  <verify>
    <automated>cd taskflow && npx tsc --noEmit && npx vitest run src/routes/dashboard/release-detail</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "group/row" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns exactly 1, and that occurrence is on the MR sub-line container, not the task row
    - `grep -c "data-testid=\"mr-slot-pending\"\|data-testid=\"mr-slot-none\"\|data-testid=\"mr-slot-unavailable\"" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 3
    - The MR-slot branch tests `isLoadingDrift` before any `mrs.length === 0` test — states 2 and 3 are unreachable while pending
    - `grep -c "role=\"button\"\|dangerouslySetInnerHTML" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 0
    - `grep -c "absolute inset-0" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 1 (the task-row overlay button)
    - `grep -cE "\.sort\(|primaryRows\.slice" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 0 (no re-sorting anywhere — D-18)
    - `grep -c "drift-br" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` and `drift-ms` each return 2 (the evaluated action cell and the `na` mark cell)
    - `grep -c "drift-task" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 0
    - The task row renders two empty 28px gutter spans and no drift glyph (D-06)
    - `cd taskflow && npx tsc --noEmit` reports zero errors
  </acceptance_criteria>

  <done>Task rows render with the overlay-button click pattern and an empty gutter; every MR renders as an indented sub-line with working BR/MS cells; the four MR-slot states are mutually exclusive with pending winning.</done>
</task>

<task type="auto">
  <name>Task 3: Secondary table for uncovered MRs</name>

  <files>taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx</files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` (current state from task 2 — `MrSubLine` is the renderer being reused)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-UI-SPEC.md` §"Secondary table (D-10 through D-13)" and the out-of-scope tooltip template in §"Copywriting Contract"
    - `taskflow/src/routes/dashboard/release-detail/driftDetection.ts` (`TaskDriftReason`, and plan 01's note on why a non-evaluated row can be uncovered with `taskReason === null`)
  </read_first>

  <action>
Render the secondary section after the primary table, inside the same `<section>`.

**D-13 — the whole block, heading included, is wrapped in
`{secondaryRows.length > 0 && (...)}`.** When there are no uncovered MRs, nothing renders:
no heading, no empty-state copy, no spacer. Absence is the signal.

Give the block `data-testid="secondary-section"`, an `<h4>` reading
`Not covered by tasks above` at `text-sm font-medium` with a `mt-4 pt-4 border-t
border-border/50` container to separate it from the primary table, and its own copy of the
same column header strip built in task 1 (BR/MS labels included) so the two tables align
straight through the gutter (D-12).

**Reuse `MrSubLine`** for each row rather than writing a second renderer. Give it an
optional prop that swaps the key-column cell — e.g. `keyCell?: React.ReactNode` defaulting
to the `!iid` button, or a `variant: 'primary' | 'secondary'` discriminator; pick one and
apply it consistently. The secondary rows must keep the same `COL_*` widths, the same
linkified title, author, state badge, and **fully working `DriftActionCell` BR/MS cells**
(D-10) — read-only here would be a functional regression against today's drift list, and
these are the MRs most likely to need a fix.

Secondary rows still need the `!iid` GitLab link. Render it in the summary column's leading
position (before the linkified title, separated by the existing `gap-2`) so the key column
is free for D-11's reason marker, and keep its `channelsTitle` tooltip.

**Key-column content (D-11)** — one column carries both cases, derived from `taskKeys`, not
from `taskReason`. A non-evaluated (merged/closed) uncovered MR has `taskReason === null`
even when it carries an out-of-scope key, so gating on `taskReason === 'not-in-fix-version'`
would mislabel it as keyless:
  - `row.taskKeys.length > 0` → `<span>` at `COL_KEY` with
    `font-mono text-xs text-orange-600 dark:text-orange-400 inline-flex items-center gap-1`,
    an `AlertTriangle className="size-3"`, the text `row.taskKeys[0]`, and
    `title={`${row.taskKeys[0]} is not in fix version ${versionName}`}` — the exact template
    from the copywriting contract. Add `data-testid="secondary-key-flagged"`.
  - otherwise → `<span>` at `COL_KEY` with `font-mono text-xs text-muted-foreground`
    containing a plain `—`, no tooltip. Add `data-testid="secondary-key-none"`.

Order the secondary rows exactly as `secondaryRows` arrives — no sort in this file (D-18).
  </action>

  <verify>
    <automated>cd taskflow && npx tsc --noEmit && npx vitest run src/routes/dashboard/release-detail && npx biome check src/routes/dashboard/release-detail/UnifiedTaskTable.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "secondaryRows.length > 0" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 1, and the `<h4>` heading is inside that conditional
    - `grep -c "Not covered by tasks above" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 1
    - `grep -c "is not in fix version" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 1
    - `grep -c "data-testid=\"secondary-key-flagged\"\|data-testid=\"secondary-key-none\"" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 2
    - The secondary key-column branch tests `row.taskKeys.length` and does NOT test `taskReason === 'not-in-fix-version'`
    - Exactly one `MrSubLine` function is defined in the file — the secondary table reuses it rather than duplicating the row JSX
    - `grep -c "DriftActionCell" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` shows the action cells reached from the shared sub-line renderer, not a second read-only variant
    - `cd taskflow && npx biome check src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` reports zero diagnostics for this file
    - `cd taskflow && npx tsc --noEmit` reports zero errors
  </acceptance_criteria>

  <done>Uncovered MRs render in a secondary table sharing the primary grid and renderer, with working fix cells, the key-or-dash reason column, and the whole section absent when empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitLab API → rendered UI | MR titles, author names and branch names are attacker-influenceable remote text rendered inside the Tauri webview |
| Jira API → rendered UI | Issue summaries, keys and status names rendered per task row |
| UI → GitLab write API | BR/MS cells fire `updateMergeRequest` writes against a specific MR id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91.1-05 | Tampering | MR title linkification, out-of-scope key tooltip, task summary | mitigate | All remote text renders as React children / `title` attributes only; `dangerouslySetInnerHTML` is grep-asserted absent, and the linkification IIFE is copied verbatim rather than re-derived (its `matchTicketKeyInTitle` regex already escapes the key before constructing a `RegExp`) |
| T-91.1-06 | Tampering | `DriftActionCell` fired against the wrong MR under D-09 duplication | mitigate | Each cell receives `mr` from its own `DriftRow` and instantiates its own `useMrFixMutation`; sub-lines are keyed by `row.mr.id`, never by array index, so a duplicated MR under two tasks cannot cross-wire a write |
| T-91.1-07 | Elevation of privilege | `openUrl(mr.web_url)` opening a remote-supplied URL in the system browser | accept | Unchanged from today's `MrDriftSection`/`IssuesSection` behaviour; URL originates from the authenticated GitLab instance the user configured, and no new attack surface is introduced by re-hosting the call |
| T-91.1-08 | Repudiation | Silent failure of a corrective action | mitigate | `DriftActionCell`'s `liveRegion` (`role="status" aria-live="polite"`) and sticky error state are copied verbatim, so every failure remains announced and visible until resolved |
| T-91.1-SC | Tampering | package installs | accept | No installs in this plan — zero new external packages phase-wide |
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` clean
- `cd taskflow && npx vitest run src/routes/dashboard/release-detail` green (existing suites unaffected — the old components are still on disk and still wired)
- `cd taskflow && npx biome check src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` clean
</verification>

<success_criteria>
`UnifiedTaskTable.tsx` renders the merged header and single degraded banner, task rows with
overlay-button peek and an empty drift gutter, indented MR sub-lines with re-hosted BR/MS
action cells and no TASK cell, the four mutually-exclusive MR-slot states with pending
winning, and a conditional secondary table sharing the same grid and renderer.
</success_criteria>

<output>
Create `.planning/phases/91.1-unified-release-detail-task-table/91.1-03-SUMMARY.md` when done
</output>
