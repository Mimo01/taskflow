---
phase: 91.1-unified-release-detail-task-table
plan: 01
subsystem: ui
tags: [react, typescript, vitest, drift-detection, release-detail]

# Dependency graph
requires:
  - phase: 89-mr-discovery-and-drift-flagging
    provides: driftDetection.ts (unionMRs, buildDriftRows, DriftRow, countFlaggedMRs, buildIssueMrIndex)
provides:
  - buildTaskMrAttachment(releaseIssues, driftRows) — D-09 task-MR partition (primaryRows per task + secondaryRows for uncovered MRs), total and disjoint
  - countBrMsFlaggedMRs(rows) — D-15/A2 header-badge count (BR-or-MS flags only, excludes TASK-only flags)
  - 18 new unit tests covering both functions' Wave 0 cases
affects: [91.1-02-hook-wiring, 91.1-03-unified-component]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Partition functions built on taskKeys intersection against a covered-key Set, not on the taskReason short-circuit field (which drops non-evaluated MRs)"

key-files:
  created: []
  modified:
    - taskflow/src/routes/dashboard/release-detail/driftDetection.ts
    - taskflow/src/routes/dashboard/release-detail/driftDetection.test.ts

key-decisions:
  - "buildTaskMrAttachment uses taskKeys.some(coveredKeys.has) for coverage, not row.taskReason !== null — the RESEARCH/PATTERNS-sketched rule would silently drop merged/closed MRs whose taskReason is always null by construction, causing them to vanish from both primary and secondary tables"
  - "countBrMsFlaggedMRs deliberately excludes the TASK mark and does not reuse row.flagged, to avoid inflating the header badge by the entire secondary-table row count (every uncovered MR carries task: 'flag' by D-11)"

patterns-established:
  - "Regression-guard test pattern: temporarily swap the correct partition predicate for the rejected one, confirm the targeted test fails, then revert — proves the guard actually catches the regression it documents"

requirements-completed: [DRIFT-04, DRIFT-07, DRIFT-08]

# Metrics
duration: 25min
completed: 2026-08-12
---

# Phase 91.1 Plan 01: Task-MR Data Layer Summary

**Added `buildTaskMrAttachment` (D-09 task↔MR partition) and `countBrMsFlaggedMRs` (D-15 BR-or-MS dedup count) to `driftDetection.ts`, both fully unit-tested including the merged-MR regression guard for the corrected partition rule.**

## Performance

- **Duration:** ~25 min
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments
- `buildTaskMrAttachment` exported: partitions `DriftRow[]` into per-task `primaryRows` (issue + its attached MRs, sorted ascending by `mr.iid`, `releaseIssues` order preserved per D-18) plus `secondaryRows` for uncovered MRs — proven total and disjoint by a dedicated invariant test
- `countBrMsFlaggedMRs` exported: counts rows flagged on BR and/or MS only, excluding TASK-only flags, satisfying D-15/RESEARCH Assumption A2
- 18 new tests added (43 total in `driftDetection.test.ts`, all green), including the load-bearing regression guard proving the `taskReason !== null` rule (rejected by the plan) actually fails without the `taskKeys` intersection fix

## Task Commits

1. **Task 1: Add buildTaskMrAttachment and countBrMsFlaggedMRs to driftDetection.ts** - `65f40a6d` (feat)
2. **Task 2: Wave 0 unit coverage for both new functions** - `24c6ee5e` (test)

## Files Created/Modified
- `taskflow/src/routes/dashboard/release-detail/driftDetection.ts` - added `countBrMsFlaggedMRs` and `buildTaskMrAttachment` exports (82 lines, additions only)
- `taskflow/src/routes/dashboard/release-detail/driftDetection.test.ts` - added `describe('buildTaskMrAttachment', ...)` (9 tests) and `describe('countBrMsFlaggedMRs', ...)` (4 tests) blocks (225 lines, additions only)

## Decisions Made
- Used `taskKeys` intersection against a `Set` of `releaseIssues` keys as the coverage rule for `buildTaskMrAttachment`, per the plan's explicit override of RESEARCH/PATTERNS' `taskReason !== null` sketch. Verified via a temporary swap-and-revert: the swap made the merged-MR regression test fail, confirming the guard is load-bearing, not decorative.
- `countBrMsFlaggedMRs` implemented as a plain `filter().length` with no dedup Set, since `driftRows` is already one entry per unique `mr.id` (guaranteed upstream by `unionMRs`).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

**Worktree base drift (environment, not plan):** The worktree's HEAD was found at `ca59303f` (a stale base, missing the entire Phase 87-91 release-detail decomposition) instead of the expected `bafcdec6`. The mandated `worktree_branch_check` step's `git reset --hard` to the correct base commit was applied before any file edits (working tree was clean at time of reset, no work lost). All subsequent reads/edits used the corrected worktree state.

**No `node_modules` in worktree:** The worktree had an empty `node_modules/` placeholder with no installed dependencies. Symlinked `taskflow/node_modules` to the main checkout's `taskflow/node_modules` (gitignored, not committed) to run `tsc`/`vitest`/`biome` for verification; removed the symlink before finishing.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

`buildTaskMrAttachment` and `countBrMsFlaggedMRs` are exported, typecheck clean, and pass all Wave 0 unit cases from `91.1-VALIDATION.md` (task-MR attachment: single MR, zero-MR task, dual-task attachment, keyless MR, out-of-scope-key MR, merged-MR regression guard, total/disjoint partition invariant, order preservation, no-mutation; BR-or-MS count: task-only=0, both-flagged=1, non-evaluated=0, mixed-list strictly-less-than `countFlaggedMRs`). Ready for plan 02 (hook wiring into `useReleaseDetail.ts`) and plan 03 (the unified task-table component) to consume both exports.

No pre-existing export in `driftDetection.ts` (`buildDriftRows`, `countFlaggedMRs`, `buildIssueMrIndex`, `DriftRow`) was modified — `git diff` on both files shows additions only.

---
*Phase: 91.1-unified-release-detail-task-table*
*Completed: 2026-08-12*
