---
phase: 91.1-unified-release-detail-task-table
plan: 07
type: execute
wave: 2
depends_on: ["91.1-06"]
gap_closure: true
files_modified:
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
  - taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts
  - taskflow/src/routes/dashboard/release-detail/releaseSummaries.test.ts
autonomous: true
requirements: [DRIFT-04]
must_haves:
  truths:
    - "Clicking a ticket key inside an MR sub-line after having already opened a task peek leaves exactly one release entry in the breadcrumb trail"
    - "No unreferenced export encoding the superseded one-MR-per-issue matching rule remains in releaseSummaries.ts"
  artifacts:
    - path: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      provides: "handleNavigateToIssueFromMR delegating to the idempotent seedReleaseBreadcrumb"
      contains: "seedReleaseBreadcrumb"
  key_links:
    - from: "ReleaseDetailPage.tsx handleNavigateToIssueFromMR"
      to: "seedReleaseBreadcrumb"
      via: "direct call, replacing the unguarded breadcrumbPush"
      pattern: "handleNavigateToIssueFromMR[\\s\\S]{0,120}seedReleaseBreadcrumb\\(\\)"
---

<objective>
Close review warnings WR-01 and WR-02 — the two findings that live entirely outside
`UnifiedTaskTable.tsx`/`driftDetection.ts` and can therefore run in parallel with plan 08.

Purpose: the unified table interleaves task rows and linkified MR titles in one list, which
makes the duplicate-breadcrumb path (WR-01) trivially reachable; and the component deletion
in plan 05 orphaned an exported matcher (WR-02) that still encodes the one-MR-per-issue rule
this phase deliberately replaced.

Output: one guarded breadcrumb call site, one deleted dead export, one regression test.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91.1-unified-release-detail-task-table/91.1-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route handleNavigateToIssueFromMR through the idempotent breadcrumb seed (WR-01)</name>
  <files>taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <read_first>
    - `taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` lines 152-190 — `seedReleaseBreadcrumb`
      (159-166, note the `last?.path !== /release/${versionId}` guard and the comment explaining
      why the seed exists), `openIssueFull` (169-172, the correct usage pattern to mirror), and
      `handleNavigateToIssueFromMR` (180-185, the unguarded duplicate)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-REVIEW.md` section WR-01
  </read_first>
  <behavior>
    - Test 1 (or an equivalent assertion if no page-level test harness exists): after
      `seedReleaseBreadcrumb()` has already pushed `/release/{versionId}`, invoking
      `handleNavigateToIssueFromMR` does not push a second entry with the same path
  </behavior>
  <action>
    Replace the body of `handleNavigateToIssueFromMR` (lines 180-185) so it calls
    `seedReleaseBreadcrumb()` followed by `navigate(`/issue/${key}`)`, exactly mirroring
    `openIssueFull`. Delete the inline `if (version) { breadcrumbPush(...) }` block. If
    `breadcrumbPush` becomes unused in this file after the change, remove its import too;
    if it is still used elsewhere in the file, leave the import.
    `ReleaseDetailPage.tsx` has no dedicated test file today — do not create a full page
    harness for this. Assert the fix structurally via the acceptance grep below, and record
    the breadcrumb sequence in the live UAT step in plan 10 instead.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'breadcrumbPush({ path: `/release/' taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns exactly 1 match, and it is inside `seedReleaseBreadcrumb`
    - `sed -n '/const handleNavigateToIssueFromMR/,/^  };/p' taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` contains `seedReleaseBreadcrumb()` and contains no `breadcrumbPush`
    - `npx tsc --noEmit` exits 0; `npm run lint` flags no newly-listed file
  </acceptance_criteria>
  <done>Exactly one unguarded-to-guarded breadcrumb push path remains for the release entry.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the dead matchIssuesToMRs export (WR-02)</name>
  <files>taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts, taskflow/src/routes/dashboard/release-detail/releaseSummaries.test.ts</files>
  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts` lines 107-136 (the
      function and its docstring)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-REVIEW.md` section WR-02
    - `taskflow/src/routes/dashboard/release-detail/releaseSummaries.test.ts` — confirm by
      reading that it contains no `matchIssuesToMRs` case before deleting
  </read_first>
  <action>
    Before deleting, run a repo-wide `grep -rn 'matchIssuesToMRs' taskflow/src` and confirm
    the only hit is the definition itself. If any other caller exists, STOP and report it in
    the SUMMARY instead of deleting.
    Then delete `matchIssuesToMRs` (lines 114-135) and its docstring (107-113) from
    `releaseSummaries.ts`. Remove the `linkMRToTask` import if this was its last consumer in
    the file; leave it otherwise. No replacement is added — `buildTaskMrAttachment` in
    `driftDetection.ts` is the surviving matcher and already carries the correct
    many-MRs-per-issue semantics.
  </action>
  <verify>
    <automated>cd taskflow && grep -rn 'matchIssuesToMRs' src | wc -l | grep -qx 0 && npx vitest run src/routes/dashboard/release-detail/releaseSummaries.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'matchIssuesToMRs' taskflow/src` returns zero matches
    - `npx tsc --noEmit` exits 0 (no orphaned-import or unused-symbol error)
    - `npx vitest run src/routes/dashboard/release-detail/` passes with no reduction in the count of passing assertions other than any removed `matchIssuesToMRs` cases
  </acceptance_criteria>
  <done>The superseded one-MR-per-issue matcher no longer exists to be reintroduced by a future caller.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MR title (remote text) → navigation | A clicked ticket key inside a remote-supplied MR title drives an in-app route change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91.1-12 | Tampering | `navigate(`/issue/${key}`)` with a key parsed from a remote MR title | accept | Unchanged behaviour; `key` comes from `extractTicketKeys`'s constrained `PROJ-123` grammar, and the target is an internal react-router path, not a URL opened externally |
| T-91.1-13 | Repudiation | Breadcrumb trail misrepresenting navigation history | mitigate | Single guarded seed path ensures the trail reflects one release entry per visit |
| T-91.1-SC | Tampering | package installs | accept | No installs in this plan |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/release-detail/` — all green
- `cd taskflow && npx tsc --noEmit` — exits 0
- `grep -rn 'matchIssuesToMRs' taskflow/src` — zero matches
</verification>

<success_criteria>
WR-01 and WR-02 are closed with no behavioural regression elsewhere in the release-detail suite.
</success_criteria>

<output>
Create `.planning/phases/91.1-unified-release-detail-task-table/91.1-07-SUMMARY.md` when done
</output>
