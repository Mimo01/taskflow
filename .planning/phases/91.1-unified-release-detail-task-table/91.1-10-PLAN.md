---
phase: 91.1-unified-release-detail-task-table
plan: 10
type: execute
wave: 4
depends_on: ["91.1-07", "91.1-08", "91.1-09"]
gap_closure: true
files_modified:
  - taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx
  - taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx
  - .planning/phases/91.1-unified-release-detail-task-table/91.1-UAT.md
autonomous: false
requirements: [DRIFT-05, DRIFT-06, DRIFT-08]
must_haves:
  truths:
    - "MR sub-lines render iid, title and the BR/MS cells only — no author avatar, no author name, no state pill"
    - "The author name and MR state are still reachable, via the sub-line's hover tooltip"
    - "A sub-line with no BR or MS flag renders in muted text; a sub-line with either flag renders at full-strength foreground"
    - "No border separates a task row from its own MR sub-lines; a border separates one task group from the next"
    - "Every MR is still visible — nothing is collapsed behind a 'N more' affordance"
    - "The developer confirms on live data that the table reads cleanly and the two badges are actionable"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx"
      provides: "Quiet, three-column MR sub-lines and group-level separators"
      contains: "isFlagged"
  key_links:
    - from: "MrSubLine"
      to: "row.br / row.ms"
      via: "isFlagged deciding the sub-line's text treatment"
      pattern: "row\\.br === 'flag' \\|\\| row\\.ms === 'flag'"
---

<objective>
Close UAT gap **UAT-91.1-A** — "works but it is very cluttered in the table. too much visual
noise." Implement the developer's three agreed changes exactly as written.

**Explicitly rejected and out of scope:** collapsing healthy MRs behind a "N more, all OK"
affordance. The developer chose visible-but-quiet. Every MR stays rendered.

Then re-run the live UAT for both design gaps, since both were reported by the developer
against real data and neither is provable from unit tests alone.

Purpose: at real density (a task with 2-4 MRs, most healthy) every sub-line currently renders
the full cell set at equal visual weight, so the eye cannot find the rows that need action.

Output: three-column sub-lines, a flag-conditional text treatment, group-level separators,
and an updated `91.1-UAT.md` recording the re-run.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91.1-unified-release-detail-task-table/91.1-UAT.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduce MR sub-lines to iid + title + drift cells, with author and state in the tooltip</name>
  <files>taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx, taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx</files>
  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` lines 48-56
      (`COL_*` constants — `COL_PERSON` and `COL_STATE` remain in use by `TaskRow`, do not
      delete them), 94-101 (`mrStateBadgeClass`), 327-432 (`MrSubLine` — the author span at
      403-406 and the state `Badge` at 407-412 are the two blocks to remove)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-UAT.md` gap UAT-91.1-A,
      agreed fix items 1 and 2
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-UI-SPEC.md` sections
      "Primary table — shared column grid (D-02)" and "Visual hierarchy — what the eye lands
      on" (the sub-line is defined as deliberately secondary; this change deepens that, it
      does not contradict it)
    - `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx` line 498-516 —
      the P90 stale-failure test asserts `.text-green-600` inside the `drift-br` ok cell.
      The BR/MS ok check glyph therefore KEEPS its green; the colour reduction in this task
      comes from removing the blue/green/grey state pill, not from muting the drift glyphs.
  </read_first>
  <behavior>
    - Test 1: a sub-line renders the `!iid` link and the MR title, and the tree contains no
      element with the MR state text (`opened`) inside a `drift-row`
    - Test 2: a `drift-row` contains no `img`/avatar element and does not render the author
      name (`Alice`) as visible text
    - Test 3: the `drift-row` element carries a `title` attribute containing both the author
      name and the MR state
    - Test 4: `drift-br` and `drift-ms` cells still render on every sub-line and remain
      buttons when actionable (no regression of the P90 corrective-action suite)
    - Test 5: a task with three MRs still renders three `drift-row` elements — nothing is
      collapsed or hidden
  </behavior>
  <action>
    In `MrSubLine`:
    1. Delete the `COL_PERSON` author span (avatar + name) and the state `Badge` from the
       returned JSX. Leave `TaskRow`'s assignee and status columns untouched — the header
       strip's `Assignee`/`Status` labels added in plan 08 describe task rows and stay accurate.
    2. Add `title={`${mr.author.name} — ${mr.state}`}` to the sub-line container `div` so the
       removed information is reachable on hover (UAT item 2's "moves to the hover tooltip").
       Leave the `!iid` button's own `channelsTitle` title in place.
    3. If `mrStateBadgeClass` has no remaining caller after the removal, delete it and its
       `Badge` import if that import is also orphaned; run a grep before deleting.
    The freed width goes to the title automatically — `COL_SUMMARY` is `flex-1 min-w-0`, so no
    width constant changes. Do not alter the `COL_*` constants or the BR/MS 28px geometry.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^function MrSubLine/,/^}/p' taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` contains no `CachedAvatar`, no `COL_PERSON`, no `COL_STATE`
    - `grep -c 'CachedAvatar' taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` is at least 1 (still used by `TaskRow`)
    - Tests 1-5 pass; the entire `per-MR corrective actions` describe block (lines 287-593) passes unmodified
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sub-lines are three columns wide; author and state survive as tooltip content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Quiet healthy sub-lines and move separators to task-group boundaries</name>
  <files>taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx, taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx</files>
  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` lines 387-392 (the
      sub-line container's current unconditional `text-muted-foreground`), 445-449 (`TaskRow`'s
      `border-b border-border/50`), 634-652 (the per-task group wrapper `<div key={issue.id}>`)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-UAT.md` gap UAT-91.1-A,
      agreed fix items 1 and 3
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-UI-SPEC.md` "Row anatomy"
      — "MR sub-line … **no border** between sibling sub-lines — only the parent task row
      carries the boundary rule"; this task relocates that boundary from the task row to the
      task GROUP, which is what the developer asked for and what the indentation already implies
  </read_first>
  <behavior>
    - Test 1: a sub-line with `br: 'ok', ms: 'ok'` has `text-muted-foreground` in its class list
    - Test 2: a sub-line with `br: 'flag'` does NOT have `text-muted-foreground`, and a
      sub-line with `ms: 'flag'` does not either
    - Test 3: `task-row` elements carry no `border-b` class
    - Test 4: each per-task group wrapper carries `border-b`, so with two tasks there are two
      group separators and zero separators between a task row and its own sub-lines
  </behavior>
  <action>
    1. In `MrSubLine`, derive `const isFlagged = row.br === 'flag' || row.ms === 'flag';` and
       make the container's text treatment conditional: `text-muted-foreground` when not
       flagged, `text-foreground` when flagged. Keep `text-xs`, `pl-4`, `py-1`, `gap-2` and
       the `group/row` class exactly as they are — the hover-reveal scope (D-05) must stay on
       the individual sub-line. Do not touch the BR/MS cell glyph colours: orange for flag and
       green for ok are the established semantics (and the P90 stale-failure test asserts the
       green).
    2. Move the separator: remove `border-b border-border/50` from `TaskRow`'s container class
       (line 448) and add `border-b border-border/50` to the per-task group wrapper
       `<div key={issue.id}>` in the primary map (line 636). Add `data-testid="task-group"` to
       that wrapper so the separator is assertable.
    Secondary-table rows are unaffected by the separator change (they have no task grouping);
    they do pick up the flag-conditional text treatment, which is correct — a healthy row is
    quiet wherever it appears.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/ && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^function TaskRow/,/^}/p' taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` contains no `border-b`
    - `grep -c 'data-testid="task-group"' taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 1, and its element's className contains `border-b`
    - `grep -c "row.br === 'flag' || row.ms === 'flag'" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` is at least 1
    - Tests 1-4 pass; the full `src/routes/dashboard/release-detail/` suite is green
    - `npm run lint` flags no newly-listed file
  </acceptance_criteria>
  <done>Healthy rows recede, flagged rows stand out, and borders group instead of separate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live UAT re-run of both design gaps</name>
  <files>.planning/phases/91.1-unified-release-detail-task-table/91.1-UAT.md</files>
  <read_first>
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-UAT.md` (both gap
      sections and the existing Tests/Summary block format to append to)
  </read_first>
  <what-built>
    Plans 06-10 closed the two blocking verification gaps (settled-state derivation), the five
    review warnings, the four info items, and both UAT design gaps: MR sub-lines are now
    iid + title + drift cells with author/state in a tooltip, healthy rows are muted, borders
    separate task groups rather than every row, and the single grey flagged integer is
    replaced by two clickable warning badges that filter the table.
  </what-built>
  <how-to-verify>
    Run `cd taskflow && npm run tauri dev`, open a release fix version with real data
    (ideally one with a task carrying 2-4 MRs and at least one BR or MS flag), then:
    1. **UAT-91.1-A / clutter.** Confirm each MR sub-line shows only `!iid`, the title and the
       two drift cells. Hover a sub-line — the author name and MR state appear in the tooltip.
    2. Confirm healthy sub-lines read as muted grey while a BR/MS-flagged sub-line reads at
       full strength, and that NO MR is hidden behind a "N more" control.
    3. Confirm there is no horizontal rule between a task row and its own MRs, and there IS
       one between one task group and the next.
    4. **UAT-91.1-B / badges.** Confirm the header shows `⚠ N wrong branch` and/or
       `⚠ N no milestone` in warning styling, and that a category with zero MRs shows no badge
       at all (not a `0`).
    5. Click `⚠ N wrong branch` — the table should show only MRs targeting the wrong branch,
       with the "Showing only…" notice and a `Clear filter` button. Click the badge again to
       toggle it off.
    6. Fix one flagged MR from inside a filtered view and confirm the count decrements and the
       row leaves the filter without the table re-sorting around you.
    7. **WR-01 regression.** Click a task row to open the peek, close it, then click a Jira key
       inside an MR title — the breadcrumb must show the release name ONCE, not twice.
    8. **CR-01/CR-02 regression (best effort).** Reload the page and watch the first paint: no
       "No merge request" and no "not covered by tasks above" content should flash before data
       arrives.
    Report per-step pass/fail. Anything not walked is recorded as `not_reported`, never
    inferred as passing.
  </how-to-verify>
  <action>
    Before pausing: run `cd taskflow && npx vitest run src/routes/dashboard/release-detail/`
    and `npx tsc --noEmit`, then start the app with `npm run tauri dev` so the developer has a
    running build to verify against — do not ask them to build it themselves.
    After the developer responds, append a dated `### Re-run` section to `91.1-UAT.md`
    recording each of the 8 steps with its literal verdict, quote any new complaint verbatim,
    recompute the `## Summary` counts, and update the frontmatter `status:`/`updated:` fields.
    Do not paraphrase the developer's words and do not mark a step passed that they did not walk.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/ && npx tsc --noEmit</automated>
    <human-check>Developer walks steps 1-8 in the running app and reports per-step results</human-check>
  </verify>
  <acceptance_criteria>
    - `91.1-UAT.md` gains a dated re-run section with an explicit per-step result for steps 1-8
    - UAT-91.1-A and UAT-91.1-B are marked `passed` only on an explicit developer confirmation;
      any new observation is recorded verbatim as a new numbered gap with a diagnosis
    - The `## Summary` counts are recomputed to include the re-run
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues per numbered step</resume-signal>
  <done>Both design gaps carry a live-data verdict from the developer, recorded verbatim in 91.1-UAT.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitLab API → rendered UI | Author name and MR state are remote text now rendered into a `title` attribute instead of visible cells |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91.1-19 | Tampering | Remote author name interpolated into a `title` attribute | mitigate | Rendered as a React attribute value, never as HTML; `dangerouslySetInnerHTML` remains grep-asserted absent from the file |
| T-91.1-20 | Repudiation | Quieting a healthy row hiding a real problem | mitigate | The muted treatment keys off `row.br`/`row.ms` only; drift glyph colours are untouched and every row stays rendered (test 5, task 1) |
| T-91.1-SC | Tampering | package installs | accept | No installs in this plan |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/release-detail/` — all green
- `cd taskflow && npx tsc --noEmit` — exits 0
- `cd taskflow && npm run lint` — no NEW files flagged
- Live UAT re-run recorded in `91.1-UAT.md`
</verification>

<success_criteria>
UAT-91.1-A's three agreed changes are implemented as written with no collapsing affordance,
and the developer has re-verified both design gaps plus the WR-01 and CR-01/CR-02 regressions
on live data.
</success_criteria>

<output>
Create `.planning/phases/91.1-unified-release-detail-task-table/91.1-10-SUMMARY.md` when done
</output>
