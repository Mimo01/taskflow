---
phase: 91.1-unified-release-detail-task-table
plan: 08
type: execute
wave: 2
depends_on: ["91.1-06"]
gap_closure: true
files_modified:
  - taskflow/src/routes/dashboard/release-detail/driftDetection.ts
  - taskflow/src/routes/dashboard/release-detail/driftDetection.test.ts
  - taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx
  - taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx
autonomous: true
requirements: [DRIFT-07, DRIFT-08]
must_haves:
  truths:
    - "The secondary table's key cell states a TASK drift only for rows whose own taskReason is 'not-in-fix-version'"
    - "An MR carrying two out-of-scope keys names both of them, not just the first"
    - "A non-evaluated (merged/closed/locked) MR with out-of-scope keys shows its keys without asserting a drift the row marks as not evaluated"
    - "extractMrTaskKeys returns each key at most once even when it appears in both the title and the source branch"
    - "Every column of the header strip carries a visible label"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/driftDetection.ts"
      provides: "Deduped key extraction and a module contract that matches its actual consumers"
      contains: "new Set("
    - path: "taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx"
      provides: "taskReason-driven secondary key cell and a labelled column header strip"
      contains: "secondary-key-unevaluated"
  key_links:
    - from: "UnifiedTaskTable.tsx secondary key cell"
      to: "DriftRow.taskReason"
      via: "direct verdict read, replacing the taskKeys.length re-derivation"
      pattern: "row\\.taskReason === 'not-in-fix-version'"
---

<objective>
Close review warnings WR-03, WR-04, WR-05 and info items IN-01, IN-02 — the contract-drift
and presentation-correctness findings left behind by the component merge.

**WR-03 resolution (decided here, not deferred): option (a) — RETAIN `task`/`taskReason` on
`DriftRow` and give them a real production consumer.** Rationale: WR-04's fix requires the
secondary key cell to read the row's own verdict rather than re-deriving one from
`taskKeys.length`, so `taskReason` stops being write-only the moment WR-04 lands. Dropping
the fields (option b) would force the cell to keep re-deriving a verdict the data layer is
better placed to compute, and would delete the only field that distinguishes
`'no-linked-task'` from `'not-in-fix-version'` — the D-11/D-12 distinction the phase
explicitly relies on. The stale D-12 comment is rewritten to name the real consumer (the
secondary-table key cell) instead of the deleted TASK-column tooltip.

Purpose: make the module's stated contract, its actual consumers, and the rendered claim
agree — so a reader trusting the comments is not sent looking for a TASK tooltip that no
longer exists, and so the cell cannot assert a drift the row itself marks unevaluated.

Output: deduped `extractMrTaskKeys`, corrected module/docstring contract, a
verdict-driven secondary key cell naming all out-of-scope keys, and a labelled header strip.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91.1-unified-release-detail-task-table/91.1-REVIEW.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dedupe extractMrTaskKeys and realign the driftDetection module contract (IN-01, IN-02, WR-03)</name>
  <files>taskflow/src/routes/dashboard/release-detail/driftDetection.ts, taskflow/src/routes/dashboard/release-detail/driftDetection.test.ts</files>
  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/driftDetection.ts` lines 1-60 (module
      header, the D-12 bullet at 25-27, the `DriftRow` interface at 44-56) and lines 146-175
      (`extractMrTaskKeys` docstring + body, `evaluateTaskDrift`) and 281-299
      (`countBrMsFlaggedMRs` docstring reasoning about `row.flagged`)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-REVIEW.md` sections
      WR-03, IN-01, IN-02
    - `taskflow/src/routes/dashboard/release-detail/driftDetection.test.ts` lines 161-215
      (`evaluateTaskDrift` and state-classification suites — the fixture style to reuse)
  </read_first>
  <behavior>
    - Test 1: `extractMrTaskKeys({ title: 'PROJ-1 fix', source_branch: 'feature/PROJ-1' })`
      returns `['PROJ-1']` (length 1, not 2)
    - Test 2: a title key and a distinct branch key both survive, title key first
      (`['PROJ-1', 'PROJ-2']` for title `PROJ-1 …`, branch `feature/PROJ-2`)
    - Test 3: `buildDriftRows` output for such an MR carries `taskKeys` of length 1, so a
      consumer rendering `taskKeys` cannot print the same key twice
  </behavior>
  <action>
    1. IN-01: change `extractMrTaskKeys` (line 154-156) to return
       `[...new Set([...extractTicketKeys(mr.title), ...extractTicketKeys(mr.source_branch)])]`.
       Set iteration preserves insertion order, so the documented "title then source_branch"
       ordering is unchanged.
    2. IN-02: narrow the `extractMrTaskKeys` docstring (lines 146-153). It currently claims
       to be "the single source of truth shared by `evaluateTaskDrift` and the UI tooltip …
       so neither re-derives it independently", which `MrSubLine` contradicts by calling
       `extractTicketKeys(mr.title)` for linkification. Rewrite it to state what is actually
       true: this is the combined title+branch key set used by `evaluateTaskDrift` and by the
       secondary-table key cell; the MR-title linkifier legitimately uses the narrower
       title-only set and is NOT a consumer of this function. Do not split the field on
       `DriftRow` — the two sets are used for different questions and one field per question
       is not warranted here.
    3. WR-03: rewrite the D-12 bullet in the module header (lines 25-27) so it names the real
       consumer: `taskReason` distinguishes `'no-linked-task'` from `'not-in-fix-version'`
       and is consumed by the secondary table's key cell (`UnifiedTaskTable.tsx`) to decide
       whether to assert an out-of-scope claim; the TASK *column* was dropped from the
       presented grid in this phase (criterion 4) and is not coming back. Keep the "do not
       collapse to a boolean" instruction and attach it to the surviving consumer.
    4. WR-03 (cont.): fix the `countBrMsFlaggedMRs` docstring's reference to `row.flagged` so
       it states that `flagged` survives as the sort key in `buildDriftRows`'s comparator,
       not as a rendered value.
    Do not change any predicate semantics, the comparator, or `buildTaskMrAttachment`.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/driftDetection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/export function extractMrTaskKeys/,/^}/p' taskflow/src/routes/dashboard/release-detail/driftDetection.ts` contains `new Set(`
    - `grep -v '^\s*\*' taskflow/src/routes/dashboard/release-detail/driftDetection.ts | grep -c 'UI tooltip'` returns 0
    - `npx vitest run src/routes/dashboard/release-detail/driftDetection.test.ts` passes, including the 3 new cases; every pre-existing case in that file still passes unmodified
  </acceptance_criteria>
  <done>Key extraction dedupes across title and branch, and no comment in the module names a consumer that was deleted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive the secondary key cell from row.taskReason and name every out-of-scope key (WR-04)</name>
  <files>taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx, taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx</files>
  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` lines 656-691 (the
      secondary section's key-cell block, including the unsafe `row.taskKeys[0]` read at 661)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-REVIEW.md` section WR-04
      (its three enumerated problems and the target shape)
    - `taskflow/src/routes/dashboard/release-detail/driftDetection.ts` lines 40-56 and 232-270
      — how `taskReason` is set, and specifically that a non-evaluated MR gets
      `taskReason: null` regardless of its real keys
    - `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx` lines 797-838
      (the existing `D-13: secondary table` suite — its single-key assertions on
      `secondary-key-flagged` MUST keep passing unchanged)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-UI-SPEC.md` copywriting
      row "Out-of-scope-key tooltip (D-11)" — the `{KEY} is not in fix version {version}`
      template is the singular form and must stay byte-identical for the one-key case
  </read_first>
  <behavior>
    - Test 1: `taskReason: 'not-in-fix-version'`, `taskKeys: ['PROJ-9']` renders
      `secondary-key-flagged` with text `PROJ-9` and title `PROJ-9 is not in fix version 33.5.0`
      (existing D-13 assertion, preserved verbatim)
    - Test 2: `taskReason: 'not-in-fix-version'`, `taskKeys: ['PROJ-9', 'PROJ-8']` renders text
      `PROJ-9 +1` and title `PROJ-9, PROJ-8 are not in fix version 33.5.0`
    - Test 3: `taskReason: 'no-linked-task'`, `taskKeys: []` renders `secondary-key-none` with
      an em dash (existing assertion, preserved)
    - Test 4: `evaluated: false`, `taskReason: null`, `taskKeys: ['PROJ-9']` (a merged MR)
      renders `secondary-key-unevaluated` showing `PROJ-9`, with no `.text-orange-600`
      descendant and no `is not in fix version` title
    - Test 5: no rendered element in the secondary section ever contains the string
      `undefined is not in fix version`
  </behavior>
  <action>
    Replace the key-cell IIFE body inside the secondary section map (lines 660-679) with a
    three-way branch driven by the row's own verdict, and remove the pre-guard
    `const key = row.taskKeys[0];` read entirely:
    - `row.taskReason === 'not-in-fix-version'` → `data-testid="secondary-key-flagged"`,
      orange treatment and `AlertTriangle` unchanged, visible text `keys[0]` plus
      `` ` +${keys.length - 1}` `` when `keys.length > 1`, `title` = `keys.join(', ')` followed
      by ` is not in fix version ${versionName}` for one key or ` are not in fix version
      ${versionName}` for more than one.
    - `row.taskKeys.length === 0` → `data-testid="secondary-key-none"`, em dash, unchanged.
    - otherwise (keys present but the row carries no evaluated out-of-scope verdict — the
      merged/closed/locked `task: 'na'` case) → new `data-testid="secondary-key-unevaluated"`,
      `text-muted-foreground` font-mono, same `keys[0]{+N}` text, no glyph, `title` =
      `` `${keys.join(', ')} — not evaluated (MR is ${row.mr.state})` ``. Never assert a drift
      the row marks as not evaluated.
    Derive `const keys = row.taskKeys;` once at the top of the callback and index it only
    inside branches that have already established a non-zero length.
    Add tests 2, 4 and 5 to the `D-13: secondary table` describe block; leave tests 1 and 3
    (already present) untouched.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'row.taskKeys\[0\]' taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns zero matches
    - `grep -c "taskReason === 'not-in-fix-version'" taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 1
    - `grep -c 'secondary-key-unevaluated' taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 1
    - The pre-existing D-13 assertions on `secondary-key-flagged` text `PROJ-9` and title `PROJ-9 is not in fix version 33.5.0` pass without edit
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The key cell's claim is the row's own verdict, safe for keyless rows, and complete for multi-key rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Label the column header strip (WR-05)</name>
  <files>taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx, taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx</files>
  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` lines 48-56 (the
      `COL_*` width constants) and 303-319 (`ColumnHeaderStrip`, with four empty spans)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-REVIEW.md` section WR-05
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-UI-SPEC.md` sections
      "Typography" (the row "Section heading / column header labels — 12px, font-medium"
      already budgets for these labels) and "Row anatomy" ("not `<table>`" — the div+flex
      structure is mandated and MUST stay)
  </read_first>
  <behavior>
    - Test 1: the rendered table contains visible header text `Key`, `Summary`, `Assignee`
      and `Status` (alongside the existing `BR` and `MS`)
    - Test 2: `UnifiedTaskTable` renders no element with `role="table"`, `role="row"` or
      `role="columnheader"` (the decision below, asserted so it is not silently reversed)
  </behavior>
  <action>
    Fill the four empty spans in `ColumnHeaderStrip` with the literal labels `Key`,
    `Summary`, `Assignee`, `Status`, keeping each span's existing `COL_KEY`/`COL_SUMMARY`/
    `COL_PERSON`/`COL_STATE` class and the strip's existing `text-xs font-medium
    text-muted-foreground bg-muted/30` treatment (this is exactly what UI-SPEC's typography
    table already budgets for; no new token, size or weight).

    **Decision on the ARIA half of WR-05: visible labels only — do NOT add
    `role="table"`/`"row"`/`"columnheader"`.** Rationale: the UI-SPEC mandates a div+flex
    structure with no cell elements, so a `role="row"` header over rows that expose no
    `role="cell"` children produces an incomplete grid that assistive technology reports
    worse than plain text. Visible labels restore the missing information for every user,
    screen-reader users included, with no partial-ARIA hazard. Record this rationale as a
    code comment above `ColumnHeaderStrip` so a later reader does not "finish" the ARIA.

    Note for the executor of plan 10: the `Assignee` and `Status` labels describe the TASK
    row columns, which plan 10 does not touch — plan 10 removes those columns from MR
    sub-lines only, so these labels stay accurate.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/function ColumnHeaderStrip/,/^}/p' taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` contains the strings `Key`, `Summary`, `Assignee`, `Status`, `BR`, `MS`
    - `grep -c 'role="table"\|role="row"\|role="columnheader"' taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 0
    - `grep -c '<table' taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 0 (div+flex structure preserved per UI-SPEC)
    - Both new tests pass and the full file suite stays green
  </acceptance_criteria>
  <done>Every column of the strip is labelled; the mandated div+flex structure is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitLab API → rendered UI | MR titles and source branch names supply the extracted Jira keys rendered in the secondary key cell and its tooltip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91.1-14 | Repudiation | Secondary key cell asserting an out-of-scope drift for a row marked not-evaluated | mitigate | Cell branches on `row.taskReason`; the unevaluated branch renders neutral copy naming the MR state (test 4) |
| T-91.1-15 | Tampering | Remote-supplied keys interpolated into a `title` attribute | mitigate | Keys are rendered as React children / attribute values only; `extractTicketKeys`'s grammar constrains them and no `dangerouslySetInnerHTML` exists in the file (grep-asserted) |
| T-91.1-SC | Tampering | package installs | accept | No installs in this plan |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/release-detail/` — all green (109 prior + plan 06's + this plan's new cases)
- `cd taskflow && npx tsc --noEmit` — exits 0
- `cd taskflow && npm run lint` — no NEW files flagged
</verification>

<success_criteria>
WR-03 (with its resolution and rationale recorded in code), WR-04, WR-05, IN-01 and IN-02
are closed. The module's comments name only consumers that exist.
</success_criteria>

<output>
Create `.planning/phases/91.1-unified-release-detail-task-table/91.1-08-SUMMARY.md` when done
</output>
