---
phase: 91.1-unified-release-detail-task-table
plan: 07
subsystem: ui
tags: [react, breadcrumb-navigation, dead-code-removal, gitlab-mr-matching]

# Dependency graph
requires:
  - phase: 91.1-06
    provides: unified release-detail task table with inline MR sub-lines and the extracted releaseSummaries.ts module
provides:
  - handleNavigateToIssueFromMR routed through the idempotent seedReleaseBreadcrumb helper (no duplicate release breadcrumb entry)
  - matchIssuesToMRs (superseded one-MR-per-issue matcher) and its linkMRToTask import deleted from releaseSummaries.ts
affects: [91.1-08, 91.1-09, 91.1-10]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Breadcrumb seeding: all release-entry navigation call sites delegate to a single idempotent seedReleaseBreadcrumb() rather than pushing directly"

key-files:
  created: []
  modified:
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
    - taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts

key-decisions:
  - "matchIssuesToMRs deleted outright with no replacement — buildTaskMrAttachment in driftDetection.ts already carries the correct many-MRs-per-issue semantics and is the surviving matcher"

patterns-established:
  - "Any future breadcrumb-pushing navigation handler in ReleaseDetailPage.tsx must call seedReleaseBreadcrumb() rather than breadcrumbPush() directly"

requirements-completed: [DRIFT-04]

# Metrics
duration: 2min
completed: 2026-08-12
---

# Phase 91.1 Plan 07: WR-01/WR-02 Gap Closure Summary

**Guarded the release-breadcrumb seed against duplicate pushes from MR ticket-key clicks and deleted the orphaned one-MR-per-issue matcher left behind by Phase 89's channel-based rewrite**

## Performance

- **Duration:** ~2 min (task work; commits 58s apart, hooks add full-suite runtime)
- **Started:** 2026-08-12T13:28:xx+02:00
- **Completed:** 2026-08-12T13:29:44+02:00
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments
- `handleNavigateToIssueFromMR` now calls `seedReleaseBreadcrumb()` instead of an unguarded inline `breadcrumbPush`, closing WR-01 (duplicate breadcrumb entry when a ticket key inside an MR sub-line is clicked after a peek was already opened)
- Deleted the dead `matchIssuesToMRs` export and its docstring from `releaseSummaries.ts` (WR-02), plus the now-unused `linkMRToTask` import — the superseded one-MR-per-issue rule can no longer be reintroduced by a future caller

## Task Commits

Each task was committed atomically:

1. **Task 1: Route handleNavigateToIssueFromMR through the idempotent breadcrumb seed (WR-01)** - `110c8912` (fix)
2. **Task 2: Delete the dead matchIssuesToMRs export (WR-02)** - `f710f8b5` (refactor)

_No plan-metadata commit — SUMMARY.md and REQUIREMENTS.md commit is handled separately per worktree-mode instructions._

## Files Created/Modified
- `taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` - `handleNavigateToIssueFromMR` body replaced with `seedReleaseBreadcrumb(); navigate(...)`, mirroring `openIssueFull`
- `taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts` - `matchIssuesToMRs` (and its docstring) and the `linkMRToTask` import removed; no other change

## Decisions Made
- None beyond what the plan specified — followed the plan exactly, including the "no replacement" instruction for the deleted matcher.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Both WR-01 and WR-02 are closed with no behavioural regression: full test suite (2506 tests) passes after each commit, `tsc --noEmit` clean, `npm run lint` flags no newly-listed file, and the release-detail suite (335 tests) is green. The breadcrumb sequence for the live UAT step referenced in plan 10 is unaffected structurally — `seedReleaseBreadcrumb()`'s existing idempotency guard now covers both entry paths (peek-open and MR-ticket-key click).

---
*Phase: 91.1-unified-release-detail-task-table*
*Completed: 2026-08-12*

## Self-Check: PASSED

All created/modified files verified present; all task commits (110c8912, f710f8b5) confirmed in git log.
