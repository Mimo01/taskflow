---
phase: 91.1-unified-release-detail-task-table
plan: 05
type: execute
wave: 4
depends_on: ["91.1-02", "91.1-03", "91.1-04"]
files_modified:
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
  - taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx
  - taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx
  - taskflow/src/routes/dashboard/release-detail/MrDriftSection.test.tsx
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx
  - taskflow/src/routes/dashboard/release-detail/driftDetection.ts
  - taskflow/src/routes/dashboard/release-detail/driftDetection.test.ts
autonomous: false
requirements: [DRIFT-01, DRIFT-02, DRIFT-03, DRIFT-04, DRIFT-05, DRIFT-06, DRIFT-07, DRIFT-08, MRFIX-01, MRFIX-02, MRFIX-03, MRFIX-04]
must_haves:
  truths:
    - "Release detail renders exactly one primary table — no standalone issues-only or drift-only section remains anywhere in the app"
    - "The Phase 89 held-order mechanism is gone from the codebase, not merely unused"
    - "Rows do not re-sort while the user fires corrective actions"
    - "The superseded wrong-milestone heuristic no longer exists as a parallel truth"
  artifacts:
    - path: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      provides: "One UnifiedTaskTable call site replacing two"
      contains: "UnifiedTaskTable"
  key_links:
    - from: "ReleaseDetailPage.tsx"
      to: "UnifiedTaskTable"
      via: "single render call with primaryRows/secondaryRows/flaggedMrCount"
      pattern: "<UnifiedTaskTable"
---

<objective>
Switch the page to the unified component, then remove everything it supersedes.

This plan resolves **RESEARCH Assumption A4 / Open Question 2** — whether
`key={versionId}` on the drift section serves any purpose beyond the now-dead held-order
mechanism — with an explicit verification task that runs **before** the deletion, not after.
The pre-planning read of `ReleaseDetailPage.tsx:251-257` shows its own comment claims two
purposes ("the D-11 held row order **and** the per-cell fix state would carry over"), and
`useMrFixMutation.ts:158-159` holds `status`/`errorMessage` in component-local `useState` —
so the remount plausibly does real work for sticky errors. Task 1 confirms that and applies
the split decision: **delete the `versionId` prop, keep `key={versionId}`**, with the comment
rewritten to state its one surviving purpose.

Purpose: criteria 1 and 6 land here, and the phase's "delete, don't leave dormant" rule
(D-18) is enforced by grep.
Output: one call site, four files deleted, dead derivations removed, human-verified UAT.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91.1-unified-release-detail-task-table/91.1-CONTEXT.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-PATTERNS.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-VALIDATION.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-03-SUMMARY.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the remount dependency, then collapse the two call sites into one</name>

  <files>taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>

  <read_first>
    - `taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` lines 34-130 and 225-280 (the hook destructure, the handlers `resolvedOnOpenIssue` / `openIssueFull` / `seedReleaseBreadcrumb` / `handleNavigateToIssueFromMR`, and both call sites including the comment block at :251-257 that documents the `key={versionId}` rationale)
    - `taskflow/src/routes/dashboard/release-detail/useMrFixMutation.ts` (specifically the `useState` declarations for `status` and `errorMessage`, and the `reset` contract — this is the A4 evidence)
    - `taskflow/src/routes/dashboard/release-detail/useMrFixMutation.test.tsx` (check for any test that depends on remount-on-version-change to clear state)
    - `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` (the props contract to satisfy)
  </read_first>

  <action>
**Step 1 — verify A4 before deleting anything.** Read `useMrFixMutation.ts` and confirm
whether `status`/`errorMessage` are component-local `useState` (per-hook-instance) or shared.
Grep `useMrFixMutation.test.tsx` for any test relying on a version change to clear state.
Record the finding in the plan summary in one sentence.

**Step 2 — apply the decision.** Unless the verification contradicts it, do exactly this:
  - **Delete** the `versionId={versionId}` prop. The new component has no `versionId` prop
    at all (plan 03 already omitted it), so passing it would be a type error.
  - **Keep** `key={versionId}` on `<UnifiedTaskTable>`. Its held-order purpose is gone, but
    its second documented purpose survives: the route has no key of its own, navigating
    between two already-cached releases skips the skeleton branch and remounts nothing, so
    without this key a sticky per-cell error on an MR present in both releases would outlive
    the release it described. The key costs nothing and closes that hole.
  - **Rewrite the comment block** at :251-257 to describe only the surviving purpose — one
    or two lines stating that the key remounts the section per release so per-cell fix
    state (`useMrFixMutation`'s local `status`/`errorMessage`) cannot carry over. Delete
    every mention of held order, D-11, and WR-09 from it; leaving them would read as if the
    mechanism still exists.
  If the verification DOES contradict the decision (state is not component-local, or a test
  proves the remount is load-bearing in some other way), stop and report rather than
  improvising.

**Step 3 — collapse the call sites.** Replace both the `<IssuesSection ... />` block
(:239-249) and the `<MrDriftSection ... />` block (:258-276) with a single
`<UnifiedTaskTable ... />` at the position `IssuesSection` occupies today, passing:
`key={versionId}`, `issueCounts`, `versionName={version.name}`,
`hasReleaseDate={!!version.releaseDate}`, `isLoadingIssues`, `isLoadingDrift`,
`hasMatchedMilestone`, `primaryRows`, `secondaryRows`, `flaggedMrCount`,
`onOpenIssue={resolvedOnOpenIssue}`, `onOpenIssueFull={openIssueFull}`,
`onSeedBreadcrumb={seedReleaseBreadcrumb}`,
`onNavigateToIssueFromMR={handleNavigateToIssueFromMR}`, and the `fix={{...}}` object
copied verbatim from the current `MrDriftSection` call (`projectId`, `baseUrl`, `token`,
`releaseBranchName`, `releaseBranchExists: branchState.kind === 'exists'`,
`matchedMilestone`). If plan 03's summary reports that `gitlabMatchType` had to be kept as a
prop, pass `gitlabMatchType={gitlabMatch.type}` too.

Update the import at :22 to `UnifiedTaskTable` and remove the `MrDriftSection` import.
Update the hook destructure at :56-91: add `primaryRows`, `secondaryRows`, `flaggedMrCount`;
remove `matchedRows`, `wrongMilestoneByKey`, `driftRows`, `driftFlaggedCount` — but only if
nothing else on the page still reads them (grep the file first; `driftRows` may feed another
section). Every other handler, prop and section on the page is untouched.
  </action>

  <verify>
    <automated>cd taskflow && npx tsc --noEmit && npx vitest run src/routes/dashboard/release-detail</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "<UnifiedTaskTable" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 1
    - `grep -c "IssuesSection\|MrDriftSection" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0
    - `grep -c "key={versionId}" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 1 and `grep -c "versionId={versionId}"` returns 0
    - The comment above the call site contains no occurrence of `held`, `D-11`, or `WR-09`
    - The summary states, in one sentence, whether `useMrFixMutation`'s status/error state is component-local and whether any test depended on the remount
    - `cd taskflow && npx tsc --noEmit` reports zero errors
    - `cd taskflow && npx vitest run src/routes/dashboard/release-detail` exits 0
  </acceptance_criteria>

  <done>The page renders one UnifiedTaskTable, the remount key survives with an accurate rationale, and the A4 question is answered in writing.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the superseded components, tests and derivations</name>

  <files>taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx, taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx, taskflow/src/routes/dashboard/release-detail/MrDriftSection.test.tsx, taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts, taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx, taskflow/src/routes/dashboard/release-detail/driftDetection.ts, taskflow/src/routes/dashboard/release-detail/driftDetection.test.ts</files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` lines 550-633 (the derivation block and the return object — the `union` / `buildIssueMrIndex` / `matchedRows` / `wrongMilestoneByKey` / `driftFlaggedCount` locals being removed)
    - `taskflow/src/routes/dashboard/release-detail/driftDetection.ts` (`countFlaggedMRs` :282-293 and `buildIssueMrIndex` :296-347 — the two exports being removed, and `unionMRs` :74 which must SURVIVE, since `buildDriftRows` calls it internally)
    - `taskflow/src/routes/dashboard/release-detail/driftDetection.test.ts` (the `describe('countFlaggedMRs')` :216-243 and `describe('buildIssueMrIndex')` :351+ blocks being removed)
    - `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` lines 396-464 (Test I's `driftFlaggedCount` assertions, which lose their subject)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-04-SUMMARY.md` (confirms the relocated coverage landed before `MrDriftSection.test.tsx` is deleted)
  </read_first>

  <action>
Delete, in this order, and only after confirming plan 04's summary reports the relocated
coverage green:

  1. `git rm taskflow/src/routes/dashboard/release-detail/MrDriftSection.test.tsx` — its
     corrective-action and title-rendering cases live in `UnifiedTaskTable.test.tsx`; its
     `applyHeldOrder` block dies with the mechanism (D-18, criterion 6).
  2. `git rm taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx` — this is what
     physically removes `applyHeldOrder`, `orderRef` and `heldForVersion` from the codebase.
     Confirm first that `matchTicketKeyInTitle` and `MrFixContext`, the two exports other
     modules may reference, are exported from `UnifiedTaskTable.tsx` and that nothing still
     imports them from here.
  3. `git rm taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx`. There is no
     `IssuesSection.test.tsx` in this directory (RESEARCH A5 — re-confirm with `ls` before
     assuming). Note that `IssuesSection` also names unrelated components under
     `routes/dashboard/issue-detail/` — do not touch those.
  4. In `useReleaseDetail.ts`: delete the `union` local (:565), the `buildIssueMrIndex` call
     and its `matchedRows` / `wrongMilestoneByKey` destructure (:567-571), the
     `driftFlaggedCount` local (:581), and those four names from the returned object. Remove
     `unionMRs`, `buildIssueMrIndex` and `countFlaggedMRs` from the module's import list —
     but keep `buildDriftRows`, `selectChannelA`, `buildTaskMrAttachment` and
     `countBrMsFlaggedMRs`. Also delete the now-stale D-05 comment block at :560-564 that
     explains why the second union existed. Everything else in the hook — all six queries,
     `isLoadingDrift`, `hasMatchedMilestone`, and every non-drift derivation — is untouched.
  5. In `driftDetection.ts`: delete `buildIssueMrIndex` and `countFlaggedMRs` with their
     JSDoc. `buildIssueMrIndex` is CONTEXT's explicitly superseded `wrongMilestoneByKey`
     heuristic and must not survive as a parallel truth; `countFlaggedMRs` is superseded by
     `countBrMsFlaggedMRs`. Keep `unionMRs`, every predicate, `buildDriftRows` (including
     its internal sort and its `row.flagged` field, which the sort still reads), and the
     two functions added in plan 01. Do NOT delete `matchIssuesToMRs` from
     `releaseSummaries.ts` — it has its own tests and is out of this phase's scope.
  6. In `driftDetection.test.ts`: delete the `describe('countFlaggedMRs')` and
     `describe('buildIssueMrIndex')` blocks, and any now-unused imports. Every other block,
     including plan 01's two new ones, stays.
  7. In `useReleaseDetail.test.tsx`: remove the `driftFlaggedCount` assertions from Test I,
     keeping the `flaggedMrCount` assertions plan 02 added. Update the comment above Test I
     so it names `flaggedMrCount`. Do not weaken the test — the 1 → 0 transition through
     `patchMrInChannelCaches` must still be asserted.

Then run the full gate: typecheck, full test suite, and Biome. For Biome, compare against
the documented baseline rather than an absolute count — the pass condition is **no NEW file
flagged** beyond the known pre-existing drift (`BacklogPage.tsx`, `BacklogRow.tsx`,
`components/ui/chart.tsx`, `MyTasksPage.tsx`, `MyTasksPage.test.tsx`, recorded at Phase 90
close). Any diagnostic in a release-detail file is a regression and must be fixed.
  </action>

  <verify>
    <automated>cd taskflow && npx tsc --noEmit && npm test && npx biome check src/routes/dashboard/release-detail src/routes/dashboard/ReleaseDetailPage.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `ls taskflow/src/routes/dashboard/release-detail/` shows no `IssuesSection.tsx`, `MrDriftSection.tsx` or `MrDriftSection.test.tsx`
    - `grep -rc "applyHeldOrder\|heldForVersion\|orderRef" taskflow/src` returns no matches anywhere in the tree
    - `grep -rc "wrongMilestoneByKey\|buildIssueMrIndex\|countFlaggedMRs" taskflow/src` returns no matches anywhere in the tree
    - `grep -rn "drift-task" taskflow/src` returns no matches
    - `grep -c "unionMRs" taskflow/src/routes/dashboard/release-detail/driftDetection.ts` is ≥ 2 (the export survives and `buildDriftRows` still calls it)
    - `grep -c "matchIssuesToMRs" taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts` is unchanged (out of scope, not deleted)
    - `cd taskflow && npx tsc --noEmit` reports zero errors
    - `cd taskflow && npm test` exits 0
    - `cd taskflow && npx biome check src/routes/dashboard/release-detail src/routes/dashboard/ReleaseDetailPage.tsx` reports zero diagnostics
  </acceptance_criteria>

  <done>The two replaced components, their test file, the held-order mechanism and the superseded wrong-milestone heuristic are gone from the codebase; typecheck, full suite and Biome are clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live UAT of the unified table</name>

  <action>
Start the app in dev mode, present the ten verification steps below to the developer
verbatim, and wait. Do not self-approve, do not infer an outcome from the automated suite,
and do not proceed to the summary until the developer responds. Record their response
literally in the plan summary — approvals as approvals, failures with the exact wording
they used, and any step they did not exercise as "not reported".
  </action>

  <what-built>
Release detail now renders one primary table keyed on Jira tasks in the fix version, with
every associated MR as an indented sub-line carrying the Phase 90 BR/MS fix cells, plus a
conditional secondary table for MRs no task covers. `IssuesSection`, `MrDriftSection`, the
TASK drift column and the D-11 held-order freeze are deleted. Automated coverage is green:
`driftDetection.test.ts` (attachment, partition invariant, BR-or-MS count),
`UnifiedTaskTable.test.tsx` (relocated corrective-action state machine plus D-04/D-05/D-06/
D-09/D-13/D-17), and the full suite.
  </what-built>

  <how-to-verify>
Run the app (`cd taskflow && npm run tauri dev`) and open a release with a real fix version.

1. **One table (criterion 1).** Confirm there is exactly one task table and no separate
   "MR Drift" section anywhere on the page.
2. **Stacked sub-lines (criterion 2).** Find a task with 2+ MRs. Confirm all of them are
   visible at once — `!iid`, truncated title with ticket keys in blue, author avatar+name,
   state badge — with no clicking or expanding.
3. **Streaming states (D-17, manual-only per 91.1-VALIDATION.md).** Hard-reload with a cold
   cache. Watch the MR slot under each task: it must go `loading merge requests…` →
   resolved. Confirm no task ever flashes an orange `⚠ No merge request` before the MR data
   arrives.
4. **Zero-MR states (criterion 4).** Find a task with no MRs on a release where the GitLab
   milestone DID match — it must show orange `⚠ No merge request`. Then open a release with
   no matched milestone: those lines must instead be neutral grey `— MR status unavailable`,
   with the "no milestone matched" tooltip, and the page must show exactly one orange
   degraded banner, not two.
5. **Fix cells (criterion 3).** Hover one MR sub-line: only that line's BR/MS icons should
   change to the action glyph — the sibling MR line above/below must stay showing its
   warning triangle. Click a flagged BR cell, confirm spinner → green check, and confirm the
   header flagged count decrements by one.
6. **Order stability (criterion 6, manual-only).** Pick an MR in the middle of a long table
   and fire its BR fix. Confirm no row moves before, during or after the mutation settles.
7. **Secondary table (criterion 5).** Confirm the "Not covered by tasks above" section lists
   only uncovered MRs, that a keyless one shows `—` and an out-of-scope one shows its key
   with a warning glyph and the "not in fix version" tooltip, and that its BR/MS cells work.
   Then open a healthy release with no uncovered MRs and confirm the entire section —
   heading included — is absent.
8. **Truncation (manual-only).** With mixed short and long MR titles, confirm each line
   truncates at its own width rather than all lines cutting at the same point.
9. **Task row click.** Click a task row body — the issue peek opens. Click the task key —
   the full issue page opens. Confirm the two do not both fire.
10. **Counts.** Confirm the header's flagged badge equals the number of distinct MRs showing
    an orange BR or MS glyph anywhere on the page, counting an MR that appears under two
    tasks only once.
  </how-to-verify>

  <resume-signal>Type "approved", or describe which numbered step failed and what you saw</resume-signal>

  <acceptance_criteria>
    - Developer explicitly approves, or reports per-step failures
    - Any reported failure is recorded verbatim in the plan summary; no step outcome is invented or inferred, and steps the developer did not exercise are recorded as "not reported"
  </acceptance_criteria>

  <done>Developer has exercised the unified table against live Jira/GitLab data and approved, or filed concrete defects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitLab/Jira API → page | The page composes remote data into the single table |
| UI → GitLab write API | Corrective actions fire from the only remaining call site |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91.1-06 | Tampering | stale per-cell fix state across a release switch | mitigate | `key={versionId}` is retained deliberately after verifying `useMrFixMutation` holds status/error in component-local `useState`, so a sticky error cannot describe a release the user has left |
| T-91.1-09 | Information disclosure | dead code paths retaining the superseded wrong-milestone heuristic | mitigate | `buildIssueMrIndex`/`wrongMilestoneByKey` are deleted and grep-asserted absent, so no second, divergent drift truth can be revived or rendered |
| T-91.1-07 | Elevation of privilege | `openUrl` to remote MR URLs | accept | Unchanged behaviour, single call site now instead of two |
| T-91.1-SC | Tampering | package installs | accept | No installs — the phase adds zero external packages |
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` clean
- `cd taskflow && npm test` green (phase gate per 91.1-VALIDATION.md)
- `cd taskflow && npx biome check src/routes/dashboard/release-detail src/routes/dashboard/ReleaseDetailPage.tsx` clean
- Grep sweeps for `applyHeldOrder`, `heldForVersion`, `orderRef`, `wrongMilestoneByKey`, `buildIssueMrIndex`, `countFlaggedMRs`, `drift-task` all return zero matches across `taskflow/src`
- Developer UAT approved
</verification>

<success_criteria>
Release detail renders exactly one primary table plus a conditional secondary table; the
held-order mechanism, the TASK column, both replaced components and the superseded
wrong-milestone heuristic are removed from the codebase; typecheck, full suite and Biome are
clean; the developer has verified the live behaviour.
</success_criteria>

<output>
Create `.planning/phases/91.1-unified-release-detail-task-table/91.1-05-SUMMARY.md` when done
</output>
