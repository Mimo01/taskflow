---
phase: 91.1-unified-release-detail-task-table
plan: 05
subsystem: ui
tags: [react, typescript, vitest, release-detail, drift-detection, cleanup]
status: checkpoint-pending

# Dependency graph
requires:
  - phase: 91.1-unified-release-detail-task-table
    plan: 02
    provides: primaryRows/secondaryRows/flaggedMrCount on useReleaseDetail()
  - phase: 91.1-unified-release-detail-task-table
    plan: 03
    provides: UnifiedTaskTable.tsx component
  - phase: 91.1-unified-release-detail-task-table
    plan: 04
    provides: UnifiedTaskTable.test.tsx (relocated coverage, safe to delete MrDriftSection.test.tsx)
provides:
  - ReleaseDetailPage.tsx renders exactly one UnifiedTaskTable call site
  - IssuesSection.tsx, MrDriftSection.tsx, MrDriftSection.test.tsx deleted
  - Held-order mechanism (applyHeldOrder/orderRef/heldForVersion) and the
    superseded wrong-milestone heuristic (buildIssueMrIndex/wrongMilestoneByKey)
    physically removed from the codebase
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A4 verification pattern: read the mutation hook's state storage (component-local useState vs shared) before deciding whether a remount key is load-bearing, rather than assuming from the old comment"

key-files:
  created: []
  modified:
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
    - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts
    - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx
    - taskflow/src/routes/dashboard/release-detail/driftDetection.ts
    - taskflow/src/routes/dashboard/release-detail/driftDetection.test.ts
  deleted:
    - taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx
    - taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx
    - taskflow/src/routes/dashboard/release-detail/MrDriftSection.test.tsx

key-decisions:
  - "A4 verified: useMrFixMutation's `status`/`errorMessage` are component-local useState (per-hook-instance, not shared), and useMrFixMutation.test.tsx has no test depending on a version-change remount to clear state. Applied the plan's pre-decided split: deleted the versionId prop, kept key={versionId}, rewrote the comment to state only the surviving purpose (guarding against stale per-cell fix state carrying over across a release switch) with no mention of held order, D-11, or WR-09."
  - "The D-15 test comparing countBrMsFlaggedMRs against the deleted countFlaggedMRs was rewritten to compute the naive row.flagged count inline (rows.filter(r => r.flagged).length) rather than deleting the test's assertion of BR-or-MS-total-strictly-less-than-naive-count — preserves the same regression coverage without the deleted export."
  - "One additional comment-only reference to the literal string `countFlaggedMRs` (in countBrMsFlaggedMRs's own JSDoc, explaining why it doesn't reuse the deleted function) was rewritten to avoid the literal substring, satisfying the plan's strict zero-match grep acceptance criterion for that name across taskflow/src."

requirements-completed: [DRIFT-01, DRIFT-02, DRIFT-03, DRIFT-04, DRIFT-05, DRIFT-06, DRIFT-07, DRIFT-08, MRFIX-01, MRFIX-02, MRFIX-03, MRFIX-04]

# Metrics
duration: 55min (Tasks 1-2; Task 3 is a live-UAT checkpoint, not yet run)
completed: 2026-08-12
---

# Phase 91.1 Plan 05: Page Wiring and Cleanup Summary

**Collapsed `ReleaseDetailPage.tsx`'s two call sites (`IssuesSection` + `MrDriftSection`) into a single `UnifiedTaskTable`, verified the `key={versionId}` remount dependency in writing before deleting anything, then physically removed both superseded components, their test file, the held-order mechanism, and the superseded wrong-milestone heuristic from the codebase.**

## Performance

- **Duration:** ~55 min for Tasks 1-2
- **Tasks:** 2 of 3 completed automatically; Task 3 (live UAT) is a `checkpoint:human-verify` gate — this plan is paused there, not yet complete
- **Files modified:** 5
- **Files deleted:** 3

## Accomplishments

- **Task 1 — A4 verification + collapse to one call site:** Read `useMrFixMutation.ts` and confirmed `status`/`errorMessage` live in component-local `useState`, one hook instance per (MR, action) cell — not shared across the page. Grepped `useMrFixMutation.test.tsx` for any remount-on-version-change dependency: none found. Applied the plan's pre-decided split exactly: deleted the `versionId` prop (the new component has none), kept `key={versionId}` on `<UnifiedTaskTable>`, and rewrote the rationale comment to state only the surviving purpose (stale per-cell fix state must not carry over when navigating between two already-cached releases) with zero occurrences of `held`, `D-11`, or `WR-09`. Replaced both the `IssuesSection` and `MrDriftSection` JSX blocks with one `UnifiedTaskTable` call wired to `primaryRows`/`secondaryRows`/`flaggedMrCount` from `useReleaseDetail`, updated the import list, and removed `matchedRows`/`wrongMilestoneByKey`/`driftRows`/`driftFlaggedCount` from the page's hook destructure (confirmed via grep that nothing else on the page read them).
- **Task 2 — Delete superseded components, tests and derivations:** Deleted `MrDriftSection.test.tsx`, `MrDriftSection.tsx` (physically removing `applyHeldOrder`/`orderRef`/`heldForVersion`), and `IssuesSection.tsx` (confirmed via `ls` that no `IssuesSection.test.tsx` existed in this directory, and that other `IssuesSection`-named components live under an unrelated `issue-detail/` path, untouched). In `useReleaseDetail.ts`: removed the `union`/`buildIssueMrIndex` derivation block and its `matchedRows`/`wrongMilestoneByKey`/`driftFlaggedCount` locals, removed those three fields plus `driftRows` from the returned object, and trimmed the `driftDetection` import to only what's still used (`buildDriftRows`, `buildTaskMrAttachment`, `countBrMsFlaggedMRs`, `selectChannelA`). In `driftDetection.ts`: deleted `buildIssueMrIndex` (the explicitly superseded wrong-milestone heuristic) and `countFlaggedMRs` (superseded by `countBrMsFlaggedMRs`), and removed the now-unused `matchIssuesToMRs` import (its module and tests in `releaseSummaries.ts` are untouched, out of scope). Updated both test files to match: `driftDetection.test.ts` lost the `describe('countFlaggedMRs')` and `describe('buildIssueMrIndex')` blocks and one comment-only literal reference; `useReleaseDetail.test.tsx`'s Test I now asserts the `flaggedMrCount` 1 → 0 transition through `patchMrInChannelCaches` (same regression coverage, new subject name).

## Task Commits

1. **Task 1: Verify the remount dependency, then collapse the two call sites into one** - `ea34ece5` (feat)
2. **Task 2: Delete the superseded components, tests and derivations** - `1fcd85a6` (feat)

## Files Created/Modified

- `taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` — one `UnifiedTaskTable` call site replacing two; `versionId` prop dropped, `key={versionId}` kept with rewritten rationale comment; hook destructure updated
- `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` — removed the superseded union/index derivation and four now-dead return fields; import list trimmed
- `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` — Test I retargeted from `driftFlaggedCount` to `flaggedMrCount`
- `taskflow/src/routes/dashboard/release-detail/driftDetection.ts` — `buildIssueMrIndex`/`countFlaggedMRs` deleted, unused import removed, one comment rewritten to drop a literal `countFlaggedMRs` reference
- `taskflow/src/routes/dashboard/release-detail/driftDetection.test.ts` — matching test blocks removed, one comparison test rewritten to compute the naive flagged count inline instead of calling the deleted export
- `taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx` — **deleted**
- `taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx` — **deleted** (physically removes `applyHeldOrder`/`orderRef`/`heldForVersion`)
- `taskflow/src/routes/dashboard/release-detail/MrDriftSection.test.tsx` — **deleted** (coverage relocated to `UnifiedTaskTable.test.tsx` in plan 04)

## Decisions Made

See `key-decisions` in frontmatter above (A4 verification outcome, the D-15 test rewrite, and the comment-literal fix).

## Deviations from Plan

**1. [Rule 1 - Bug] One extra comment-literal fix beyond the plan's explicit scope**
- **Found during:** Task 2 verification (running the plan's own `grep -rn "wrongMilestoneByKey\|buildIssueMrIndex\|countFlaggedMRs" taskflow/src` acceptance check)
- **Issue:** `countBrMsFlaggedMRs`'s own JSDoc in `driftDetection.ts` referenced the deleted `countFlaggedMRs` by name to explain why it isn't reused — a legitimate prose reference, but it violated the plan's strict zero-match acceptance criterion for that literal string
- **Fix:** Reworded the sentence to describe "a naive `row.flagged` count" instead of naming the deleted function
- **Files modified:** `taskflow/src/routes/dashboard/release-detail/driftDetection.ts`
- **Commit:** `1fcd85a6`

**2. [Rule 1 - Bug] Rewrote a test that called the deleted `countFlaggedMRs` directly**
- **Found during:** Task 2, first `tsc --noEmit` pass after deleting `countFlaggedMRs`
- **Issue:** `driftDetection.test.ts`'s `describe('countBrMsFlaggedMRs')` block had one test (`'a mixed list returns the BR-or-MS total, strictly less than countFlaggedMRs...'`) that called `countFlaggedMRs(rows)` directly to prove `countBrMsFlaggedMRs` returns a strictly smaller number when a task-only flag is present — not covered by the plan's listed `describe` blocks to delete (only `countFlaggedMRs` and `buildIssueMrIndex` top-level blocks were named)
- **Fix:** Computed the same "naive flagged count" inline (`rows.filter((r) => r.flagged).length`) instead of calling the deleted export, preserving the exact same regression assertion
- **Files modified:** `taskflow/src/routes/dashboard/release-detail/driftDetection.test.ts`
- **Commit:** `1fcd85a6`

No other deviations — plan executed as written, including the A4 verification and split decision, which the evidence confirmed rather than contradicted.

## Issues Encountered

**No `node_modules` in worktree (recurring, same as plans 01/02/03/04):** Symlinked `taskflow/node_modules` to the main checkout's `taskflow/node_modules` (gitignored, not committed) to run `tsc`/`vitest`/`biome` and to satisfy the pre-commit husky hook; removed the symlink immediately after each verification pass and before finishing each task's work, per standard practice for this worktree.

## User Setup Required

None so far — no external service configuration required for Tasks 1-2.

## Verification Status (Tasks 1-2)

- `cd taskflow && npx tsc --noEmit` — zero errors
- `cd taskflow && npm test` — 2495 passed / 2 skipped / 13 todo, zero failures
- `cd taskflow && npx biome check src/routes/dashboard/release-detail src/routes/dashboard/ReleaseDetailPage.tsx` — zero diagnostics
- `cd taskflow && npx biome check ./src` — baseline drift confirmed unchanged: only `BacklogPage.tsx`, `BacklogRow.tsx`, `components/ui/chart.tsx`, `MyTasksPage.tsx`, `MyTasksPage.test.tsx` flagged (the documented Phase 90 baseline); zero release-detail files, zero new files
- Grep sweeps all return zero matches across `taskflow/src`: `applyHeldOrder|heldForVersion|orderRef`, `wrongMilestoneByKey|buildIssueMrIndex|countFlaggedMRs`, `drift-task`
- `grep -c "unionMRs" driftDetection.ts` = 3 (export survives, still called internally)
- `grep -c "matchIssuesToMRs" releaseSummaries.ts` = 1 (unchanged, out of scope)
- `grep -c "<UnifiedTaskTable" ReleaseDetailPage.tsx` = 1; `grep -c "IssuesSection\|MrDriftSection"` = 0
- `grep -c "key={versionId}"` = 1; `grep -c "versionId={versionId}"` = 0
- Comment above the call site: zero occurrences of `held`, `D-11`, `WR-09`

## Next Phase Readiness — CHECKPOINT PENDING

**Task 3 (live UAT) has NOT run.** This plan is `autonomous: false` and paused at a `checkpoint:human-verify` gate per its own `gate="blocking"` designation. The developer must run `cd taskflow && npm run tauri dev`, open a release with a real fix version, and exercise the ten numbered verification steps in `91.1-05-PLAN.md`'s Task 3 (`<how-to-verify>`), then respond with "approved" or per-step failures. This SUMMARY will be updated with the literal UAT outcome once that response is received — no step outcome is invented here.

All automated work (Tasks 1-2) is committed and verified. Nothing further should be executed on this plan until the developer's UAT response arrives.

---
*Phase: 91.1-unified-release-detail-task-table*
*Tasks 1-2 completed: 2026-08-12*
*Task 3: awaiting human UAT*

## Self-Check: PASSED

- FOUND: taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
- FOUND: taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts
- MISSING (intentional deletion, confirmed via ls): taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx
- MISSING (intentional deletion, confirmed via ls): taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx
- MISSING (intentional deletion, confirmed via ls): taskflow/src/routes/dashboard/release-detail/MrDriftSection.test.tsx
- FOUND commit: ea34ece5 (feat: collapse IssuesSection/MrDriftSection into UnifiedTaskTable)
- FOUND commit: 1fcd85a6 (feat: delete superseded components, tests and derivations)
