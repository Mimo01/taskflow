---
phase: 91.1-unified-release-detail-task-table
plan: 02
type: execute
wave: 2
depends_on: ["91.1-01"]
files_modified:
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx
autonomous: true
requirements: [DRIFT-01, DRIFT-02, DRIFT-03, DRIFT-04]
must_haves:
  truths:
    - "The hook returns the task-keyed primary rows, the uncovered secondary rows, and the BR-or-MS flagged count"
    - "The flagged count still drops to zero the moment an optimistic cache patch clears the last BR/MS flag, with no refetch"
    - "The existing release-detail data layer keeps working — every current useReleaseDetail test stays green"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts"
      provides: "primaryRows / secondaryRows / flaggedMrCount on the hook return"
      contains: "buildTaskMrAttachment"
  key_links:
    - from: "useReleaseDetail.ts"
      to: "driftDetection.buildTaskMrAttachment"
      via: "call on driftRows + releaseIssues"
      pattern: "buildTaskMrAttachment\\(releaseIssues, driftRows\\)"
---

<objective>
Thread the new data-layer derivations through `useReleaseDetail` so the unified component
(plan 03) has props to consume, and prove the D-12 optimistic-badge path still works against
the redefined count.

This plan is deliberately **additive**: it adds `primaryRows`, `secondaryRows` and
`flaggedMrCount` to the hook return while leaving `matchedRows`, `wrongMilestoneByKey`,
`driftFlaggedCount` and the `union`/`buildIssueMrIndex` block in place. `ReleaseDetailPage`
still renders `IssuesSection` at this wave and would not compile if those were removed here.
Plan 05 does the removal, after the page has switched over.

Purpose: keeps the tree green at every wave boundary while unblocking plans 03/04.
Output: three new hook return fields plus regression coverage.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91.1-unified-release-detail-task-table/91.1-CONTEXT.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-PATTERNS.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive primaryRows, secondaryRows and flaggedMrCount in useReleaseDetail</name>

  <files>taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts</files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` lines 540-633 (the derivation block and the `return { ... } as const` object — this is both the site to edit and the pattern to imitate)
    - `taskflow/src/routes/dashboard/release-detail/driftDetection.ts` (the two functions added in plan 01 — their exact signatures and return shapes)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-PATTERNS.md` §"`useReleaseDetail.ts` (hook, request-response)"
  </read_first>

  <action>
In the derivation block immediately after the existing `buildDriftRows({...})` call
(currently `useReleaseDetail.ts:573-581`), add:

  - `const { primaryRows, secondaryRows } = buildTaskMrAttachment(releaseIssues, driftRows);`
  - `const flaggedMrCount = countBrMsFlaggedMRs(driftRows);`

Add both names to the existing `import { ... } from './driftDetection'` statement at the
top of the file.

Add `primaryRows`, `secondaryRows` and `flaggedMrCount` to the `return { ... } as const`
object (`:593-632`), placed next to the existing `driftRows` / `driftFlaggedCount` entries
so the drift-related fields stay grouped.

Pass `driftRows` — the full union — to `countBrMsFlaggedMRs`, not `secondaryRows` or a
concatenation. `driftRows` already covers both partitions and is already deduped by `mr.id`
at the `unionMRs` stage, which is precisely D-15's "distinct MRs across both tables,
counted once". Add a one-line comment stating that, so the argument is not "corrected"
to a concatenation later (which would double-count nothing but would suggest the dedup
requirement is unmet).

**Do not** in this plan: delete `union`, `buildIssueMrIndex`, `matchedRows`,
`wrongMilestoneByKey`, `driftFlaggedCount`, `countFlaggedMRs`, or their return entries;
and do not touch the six queries, `selectChannelA`, `isLoadingDrift` (`:582`) or
`hasMatchedMilestone` (`:583`). `isLoadingDrift` and `hasMatchedMilestone` are already
exactly what D-17's pending-vs-resolved-empty discriminant needs and are already returned —
thread them through unchanged, do not re-derive.
  </action>

  <verify>
    <automated>cd taskflow && npx tsc --noEmit && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "buildTaskMrAttachment(releaseIssues, driftRows)" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 1
    - `grep -c "countBrMsFlaggedMRs(driftRows)" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 1
    - `grep -c "matchedRows" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` is unchanged from before the edit (the old path is untouched)
    - The `return` object contains all of `primaryRows`, `secondaryRows`, `flaggedMrCount`, `driftRows`, `driftFlaggedCount`, `matchedRows`, `wrongMilestoneByKey`, `isLoadingDrift`, `hasMatchedMilestone`
    - `cd taskflow && npx tsc --noEmit` reports zero errors
    - `cd taskflow && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` exits 0 with no test modified yet
  </acceptance_criteria>

  <done>The hook exposes the three new derivations, the old ones still exist, and the whole existing useReleaseDetail suite is green unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression coverage for the new hook outputs</name>

  <files>taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx</files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` lines 380-465 (Test I, the D-12 optimistic-badge test — its mock setup and `patchMrInChannelCaches` call are the fixture to extend; note it currently asserts on `driftFlaggedCount`)
    - `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` (the return object just edited)
  </read_first>

  <behavior>
    - Test I (D-12 badge decrement) additionally asserts `flaggedMrCount` follows the same 1 → 0 path as `driftFlaggedCount`, driven purely by the optimistic `patchMrInChannelCaches` write with no refetch
    - With one fix-version issue `PROJ-1` and one MR titled `PROJ-1 fix thing`, `primaryRows` has length 1, `primaryRows[0].issue.key === 'PROJ-1'`, and `primaryRows[0].mrs` has length 1
    - `secondaryRows` is empty in that scenario
    - An MR whose title carries no fix-version key resolves into `secondaryRows` and appears under no `primaryRows` entry
  </behavior>

  <action>
Extend the existing `it('Test I: patching the cache clears a flagged count (D-12 badge
decrement path)')` to assert `flaggedMrCount` alongside `driftFlaggedCount` — both must go
1 → 0 through the same `act(() => patchMrInChannelCaches(...))` call. Keep the existing
`driftFlaggedCount` assertions in place; plan 05 removes them when the field goes away.
This is the load-bearing proof that swapping `countFlaggedMRs` for `countBrMsFlaggedMRs`
did not break the non-memoized recompute path (Phase 90 D-12).

Add one new `it` in the same `describe`, reusing Test I's `setupMocks()` + mock shape, that
asserts the attachment outputs: `primaryRows` length/keys/`mrs` length and empty
`secondaryRows` for the covered case. Then add a second `it` with the MR title changed to
carry no fix-version key (e.g. `'chore: bump deps'` with a `source_branch` that also
carries no key) asserting it lands in `secondaryRows` and that `primaryRows[0].mrs` is
empty.

Do not restructure or rename existing tests. Run mode only: `npx vitest run`.
  </action>

  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `cd taskflow && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` exits 0, with a test count at least 2 higher than before this task
    - `grep -c "flaggedMrCount" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` is ≥ 3
    - Test I asserts both `flaggedMrCount` and `driftFlaggedCount` transition 1 → 0
    - `cd taskflow && npm test` exits 0 (full suite, wave gate per 91.1-VALIDATION.md)
  </acceptance_criteria>

  <done>The new hook outputs have regression coverage and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitLab/Jira API → hook | All six queries return remote data; this plan only re-derives from already-fetched, already-validated results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91.1-03 | Tampering | flagged-count derivation | mitigate | `countBrMsFlaggedMRs` is fed the deduped `driftRows` union, asserted by the extended Test I so a stale/inflated badge cannot ship silently |
| T-91.1-04 | Denial of service | per-render derivation | accept | Both new functions are O(issues × rows) over lists bounded by one fix version; no memoization needed and none added (React Compiler is on; `useMemo` is not a stability guarantee in this codebase) |
| T-91.1-SC | Tampering | package installs | accept | No installs in this plan |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` green
- `cd taskflow && npm test` green (wave gate)
- `cd taskflow && npx tsc --noEmit` clean
</verification>

<success_criteria>
`useReleaseDetail` returns `primaryRows`, `secondaryRows` and `flaggedMrCount` alongside all
pre-existing fields; the D-12 optimistic-decrement path is proven for the new count; the
full test suite is green.
</success_criteria>

<output>
Create `.planning/phases/91.1-unified-release-detail-task-table/91.1-02-SUMMARY.md` when done
</output>
