---
phase: 12-backlog-view
plan: 04
type: execute
wave: 4
depends_on:
  - 12-03
files_modified:
  - taskflow/src/main.tsx
  - taskflow/src/components/app/Sidebar.tsx
autonomous: false
requirements:
  - BACK-01
  - BACK-02
  - BACK-03
  - BACK-04
  - BACK-05

must_haves:
  truths:
    - "User can navigate to /backlog from the sidebar in both developer and PM role sections"
    - "The /backlog route renders BacklogPage without a blank screen or console errors"
    - "User can verify all five BACK requirements working end-to-end against the Orange Jira instance"
  artifacts:
    - path: "taskflow/src/main.tsx"
      provides: "Route registration for /backlog path with eager import of BacklogPage"
      contains: "path: '/backlog'"
    - path: "taskflow/src/components/app/Sidebar.tsx"
      provides: "NavLink to /backlog in both developer and PM role sections"
      contains: "NavLink.*backlog"
  key_links:
    - from: "taskflow/src/components/app/Sidebar.tsx"
      to: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      via: "NavLink to='/backlog' → route renders BacklogPage"
      pattern: "/backlog"
    - from: "taskflow/src/main.tsx"
      to: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      via: "{ path: '/backlog', element: <BacklogPage /> }"
      pattern: "BacklogPage"
---

<objective>
Final wiring: register the /backlog route in main.tsx and add the Backlog NavLink to Sidebar.tsx in both developer and PM sections. Then a human verification checkpoint to confirm all five BACK requirements work against the live Orange Jira instance.

Purpose: Without the route and sidebar link, BacklogPage is unreachable. Human verification validates live JQL, move-to-sprint API, and futureSprints() availability — things unit tests cannot verify.
Output: Navigable /backlog route; human sign-off on all BACK requirements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-backlog-view/12-CONTEXT.md
@.planning/phases/12-backlog-view/12-RESEARCH.md
@.planning/phases/12-backlog-view/12-03-SUMMARY.md
</context>

<interfaces>
<!-- Current main.tsx router (lines 157-174): -->
```tsx
const router = createHashRouter([
  {
    element: <AppLayout />,
    errorElement: <ErrorPage />,
    children: [
      { path: '/', element: <Onboarding /> },
      { path: '/dashboard', element: <Dashboard /> },
      { path: '/settings', element: <Settings /> },
      { path: '/my-tasks', element: <MyTasksTab /> },
      { path: '/sprint-board', element: <SprintBoardTab /> },
      { path: '/mr-attention', element: <MrAttentionTab /> },
      { path: '/sprint-progress', element: <SprintProgressTab /> },
      { path: '/workload', element: <WorkloadTab /> },
      { path: '/releases', element: <ReleasesTab /> },
      { path: '/debug-logs', element: <DebugLogs /> },
    ],
  },
]);
```
Add: `{ path: '/backlog', element: <BacklogPage /> }` after /sprint-board.
Eager import (matching existing pattern): `import BacklogPage from './routes/dashboard/BacklogPage';`

<!-- Current Sidebar.tsx structure (from grep): -->
```tsx
// Developer section (lines ~84-95):
<NavLink to="/my-tasks" className={navLinkClass}>...</NavLink>
<NavLink to="/sprint-board" className={navLinkClass}>
  <span>Sprint Board</span>
</NavLink>
// ADD AFTER sprint-board: NavLink to="/backlog"
<NavLink to="/mr-attention" className={navLinkClass}>...</NavLink>

// PM section (lines ~107-118):
<NavLink to="/sprint-progress" className={navLinkClass}>...</NavLink>
<NavLink to="/workload" className={navLinkClass}>
  <span>Workload</span>
</NavLink>
// ADD AFTER workload: NavLink to="/backlog"
<NavLink to="/releases" className={navLinkClass}>...</NavLink>
```

<!-- NavLink pattern (Sidebar.tsx navLinkClass): -->
```tsx
const navLinkClass = ({ isActive }: { isActive: boolean }) =>
  cn('flex items-center gap-2 px-3 py-2 rounded-lg text-sm font-medium transition-colors',
     isActive ? 'bg-accent text-accent-foreground' : 'text-muted-foreground hover:bg-accent/50 hover:text-accent-foreground')

// Icon to use: List from lucide-react (backlog standard icon)
<NavLink to="/backlog" className={navLinkClass}>
  <List className="h-4 w-4 shrink-0" />
  <span className="hidden md:block">Backlog</span>
</NavLink>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Register /backlog route in main.tsx and add Sidebar NavLinks</name>
  <files>taskflow/src/main.tsx, taskflow/src/components/app/Sidebar.tsx</files>
  <action>
    **main.tsx changes:**
    1. Read main.tsx to see current imports and router.
    2. Add eager import: `import BacklogPage from './routes/dashboard/BacklogPage';` alongside other route imports at the top.
    3. Add route entry `{ path: '/backlog', element: <BacklogPage /> }` to the router children array, after the /sprint-board entry.

    **Sidebar.tsx changes:**
    1. Read Sidebar.tsx to see exact line numbers of Sprint Board and Workload NavLinks.
    2. Add `List` to the lucide-react import (alongside existing icons).
    3. In the developer role section: add the Backlog NavLink immediately after the Sprint Board NavLink and before the MR Attention NavLink.
    4. In the PM role section: add the Backlog NavLink immediately after the Workload NavLink and before the Releases NavLink.

    Both NavLinks use identical markup:
    ```tsx
    <NavLink to="/backlog" className={navLinkClass}>
      <List className="h-4 w-4 shrink-0" />
      <span className="hidden md:block">Backlog</span>
    </NavLink>
    ```
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>TypeScript compiles clean; full vitest suite green; /backlog route exists in router children; Sidebar has NavLink to /backlog in both dev and PM sections</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — all five BACK requirements on Orange Jira instance</name>
  <action>
    Run the app with `cd /Users/mimo/Desktop/Tasker/taskflow && npm run tauri dev` and perform the verification steps below against the live Orange Jira instance.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms all five BACK requirements (BACK-01 through BACK-05) pass on the Orange Jira instance and types "approved"</done>
  <what-built>
    Full Phase 12 backlog view: navigable /backlog route showing all issues not in any active or future sprint, with client-side filters (epic/label/assignee), bulk move-to-sprint with optimistic update, create story button, and row click opening the issue detail panel.
  </what-built>
  <how-to-verify>
    Start the app and connect to the Orange Jira instance. Then verify each requirement:

    **BACK-01 — Backlog list:**
    1. Click "Backlog" in the sidebar (appears in your role section)
    2. Confirm the page loads without a blank screen or console errors
    3. Confirm backlog issues appear as rows with: issue key, summary, story points, assignee avatar, epic badge
    4. Confirm no subtasks appear in the list (stories only)
    5. If you see a 400 error or "Backlog query unavailable" message: note it — this indicates futureSprints() is unavailable on Orange (see STATE.md concern). Report the exact error.

    **BACK-05 — Row click:**
    6. Click any issue row summary text
    7. Confirm the IssueDetailSheet slides open showing the full issue detail

    **BACK-04 — Filters:**
    8. Open the Epic filter dropdown — confirm epics from the backlog list appear as options
    9. Select an epic — confirm only issues matching that epic are shown; unmatched rows disappear
    10. Confirm an active epic chip appears in the filter bar with an X button
    11. Click the X on the epic chip — confirm all rows return
    12. Open the Assignee filter — select an assignee — confirm filtering narrows results
    13. Combine epic + assignee filters — confirm AND logic (only issues matching BOTH are shown)

    **BACK-02 — Move to sprint:**
    14. Check the checkbox on 1–2 backlog issues — confirm the sticky action bar appears at page bottom
    15. Confirm the bar shows "Move N issues to active sprint" and a "Deselect all" button
    16. If there IS an active sprint: click "Move X issues to active sprint"
        - Issues should disappear from the list immediately (optimistic removal)
        - Open Jira in a browser and verify those issues now appear in the active sprint
    17. If there is NO active sprint: confirm the move button is disabled and shows tooltip "No active sprint in this project"

    **BACK-03 — Create story:**
    18. Click "+ Create Story" in the page header (top right)
    19. Confirm the Create/Edit Issue modal opens with Issue Type pre-set to "Story"
    20. Fill in a summary and create the issue
    21. Confirm the modal closes and the new story appears in the backlog list

    Report: PASS or FAIL for each requirement, plus any console errors or unexpected behaviors.
  </how-to-verify>
  <resume-signal>Type "approved" if all five BACK requirements pass, or describe any failures with requirement ID (e.g., "BACK-01 fails: 400 error from futureSprints()")</resume-signal>
</task>

</tasks>

<verification>
- `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit` — clean
- `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run` — all tests green
- Human verification: all five BACK requirements confirmed working on Orange Jira instance
</verification>

<success_criteria>
- /backlog route registered; sidebar has NavLink in both developer and PM sections
- App navigates to BacklogPage when user clicks Backlog in sidebar
- Human confirms: backlog list loads, filters work, row click opens detail, move-to-sprint works, create story works
- All automated tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/12-backlog-view/12-04-SUMMARY.md`
</output>
