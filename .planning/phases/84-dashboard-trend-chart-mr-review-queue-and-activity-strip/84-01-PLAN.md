---
phase: 84-dashboard-trend-chart-mr-review-queue-and-activity-strip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/dashboardMetrics.ts
  - taskflow/src/routes/dashboard/dashboardMetrics.test.ts
autonomous: true
requirements: [DASH-04, DASH-05, DASH-06]
must_haves:
  truths:
    - "A worklog with dateStarted '2026-06-14' (normalized from '2026-06-14T23:00:00') buckets to the 2026-06-14 day, never 2026-06-15"
    - "buildWeekBuckets always returns 5 zero-filled Mon-Fri buckets even with no worklogs"
    - "mergeActivityEntries interleaves Jira activity + GitLab commits into one newest-first list capped at N"
    - "groupMrsByRole returns two non-overlapping groups: awaiting-review (reviewer not author) and my-open (author)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/dashboardMetrics.ts"
      provides: "buildWeekBuckets, mergeActivityEntries, groupMrsByRole pure functions"
      contains: "export function buildWeekBuckets"
    - path: "taskflow/src/routes/dashboard/dashboardMetrics.test.ts"
      provides: "timezone-safe bucketing test + grouping tests"
      contains: "2026-06-14T23:00:00"
  key_links:
    - from: "taskflow/src/routes/dashboard/dashboardMetrics.test.ts"
      to: "taskflow/src/routes/dashboard/dashboardMetrics.ts"
      via: "import buildWeekBuckets, mergeActivityEntries, groupMrsByRole"
      pattern: "from './dashboardMetrics'"
---

<objective>
Extend the existing pure-derivation module `dashboardMetrics.ts` with the three Wave-0 test seams Phase 84's components consume: `buildWeekBuckets` (timezone-safe weekly Tempo bucketing, DASH-04 criterion 1), `mergeActivityEntries` (newest-first Jira+commit interleave, DASH-05), and `groupMrsByRole` (reviewer-vs-author MR grouping, DASH-06). Add the mandated unit tests, including the safety-critical timezone test.

Purpose: These pure functions are the testable seams (Nyquist Wave 0). All three Phase 84 components import them and never re-derive inline, so the criterion-1 timezone bug and the MR-overlap bug are caught at the unit level before any component renders.

Output: Extended `dashboardMetrics.ts` (no React, no hooks) + extended `dashboardMetrics.test.ts`, all green.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-CONTEXT.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-RESEARCH.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-PATTERNS.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add buildWeekBuckets + groupMrsByRole pure functions to dashboardMetrics.ts</name>
  <files>taskflow/src/routes/dashboard/dashboardMetrics.ts</files>
  <read_first>
    - taskflow/src/routes/dashboard/dashboardMetrics.ts (current module — match its pure-function style, JSDoc form, and "NO React, NO hooks" contract; do NOT introduce any React import)
    - taskflow/src/services/tempo/types.ts (TempoWorklog shape: dateStarted: string already YYYY-MM-DD, timeSpentSeconds: number)
    - taskflow/src/services/tempo/worklogs.ts (line 53 — confirms dateStarted is pre-sliced to YYYY-MM-DD; bucketing must rely on this, never re-derive from a raw timestamp)
    - taskflow/src/services/gitlab.ts lines 243-258 (GitLabMR shape: author.id: number, reviewers: Array<{ id: number }>)
  </read_first>
  <behavior>
    - buildWeekBuckets([{ dateStarted:'2026-06-14', timeSpentSeconds:3600 }], '2026-06-09') → the bucket with day '2026-06-14' has hours === 1
    - buildWeekBuckets([], '2026-06-09') → exactly 5 buckets, labels ['Mon','Tue','Wed','Thu','Fri'], every hours === 0
    - buildWeekBuckets sums multiple worklogs on the same day (two 3600s logs on '2026-06-09' → Mon bucket hours === 2)
    - buildWeekBuckets ignores worklogs whose dateStarted falls outside the Mon-Fri window
    - groupMrsByRole(mrs, userId): awaitingReview = MRs where reviewers includes userId AND author.id !== userId; myOpen = MRs where author.id === userId; an MR where user is both author and reviewer appears ONLY in myOpen (no overlap)
  </behavior>
  <action>
    Export a `WeekBucket` interface `{ day: string; label: string; hours: number }` and a module-level constant `DAILY_TARGET_HOURS = 8`.

    Implement `export function buildWeekBuckets(worklogs: TempoWorklog[], weekStart: string): WeekBucket[]`. Build 5 buckets for labels `['Mon','Tue','Wed','Thu','Fri']`; each bucket's `day` is `weekStart` plus the index in days, computed via PURE YYYY-MM-DD string arithmetic — do NOT construct `new Date(...)` for bucket-day derivation and NEVER use `new Date(...).toISOString()` (per D-04, criterion 1, and the standup-date UTC-shift memory). Use a small helper that adds N days to a YYYY-MM-DD string by parsing the numeric Y/M/D parts and using `Date.UTC` only for the arithmetic, then formatting back via `.toISOString().slice(0,10)` on the UTC-constructed value — OR a manual day-count add; whichever you choose, add a one-line comment that the input/output are calendar dates with no timezone shift. Zero-fill all buckets (`hours: 0`). Then for each worklog, find the bucket where `bucket.day === worklog.dateStarted` (direct string equality on the pre-normalized field) and add `worklog.timeSpentSeconds / 3600` to `hours`. Worklogs with no matching bucket are skipped.

    Implement `export function groupMrsByRole(mrs: GitLabMR[], userId: number | undefined): { awaitingReview: GitLabMR[]; myOpen: GitLabMR[] }`. `awaitingReview = mrs.filter(mr => mr.reviewers.some(r => r.id === userId) && mr.author.id !== userId)`; `myOpen = mrs.filter(mr => mr.author.id === userId)`. Add a comment citing Pitfall 3 (RESEARCH): exclude self-authored from awaitingReview to prevent overlap. Import the `GitLabMR` type from `@/services/gitlab` and `TempoWorklog` from `@/services/tempo/types`.

    Implements DASH-04 (criterion 1 bucketing) and DASH-06 (criterion 3 grouping) per D-03, D-04, D-12.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/dashboardMetrics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/routes/dashboard/dashboardMetrics.ts` exports `buildWeekBuckets`, `groupMrsByRole`, `WeekBucket`, `DAILY_TARGET_HOURS`
    - `grep -n "toISOString\|toLocaleDateString" taskflow/src/routes/dashboard/dashboardMetrics.ts` shows no usage inside `buildWeekBuckets`'s bucket-matching logic (a UTC-only arithmetic helper is acceptable; raw-timestamp re-derivation of a worklog date is not)
    - File contains no `import ... 'react'` / no hooks (pure module preserved)
    - `cd taskflow && npx tsc --noEmit` passes for this file (no type errors)
  </acceptance_criteria>
  <done>buildWeekBuckets and groupMrsByRole exist as pure functions; module remains React-free; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add mergeActivityEntries + extend dashboardMetrics.test.ts with mandated tests</name>
  <files>taskflow/src/routes/dashboard/dashboardMetrics.ts, taskflow/src/routes/dashboard/dashboardMetrics.test.ts</files>
  <read_first>
    - taskflow/src/routes/dashboard/dashboardMetrics.ts (the functions added in Task 1; append mergeActivityEntries in the same style)
    - taskflow/src/routes/dashboard/dashboardMetrics.test.ts (existing tests — match describe/it structure and import style)
    - taskflow/src/services/jira.ts lines 778-795 (JiraActivityItem shape: issueKey, summary, issueType?, transitions: Array<{ fromStatus, toStatus, at }>, comments: Array<{ body, at }>)
    - taskflow/src/services/gitlab.ts lines 1265-1273 (GitLabCommit shape: id, short_id, title, message, author_name, authored_date: string ISO 8601, web_url)
    - .planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-PATTERNS.md (mergeActivityEntries reference shape, lines 484-498)
  </read_first>
  <behavior>
    - mergeActivityEntries with one Jira item (transition at '2026-06-14T10:00:00') and one commit (authored_date '2026-06-14T12:00:00'), cap 5 → returns 2 entries, the commit first (newest at top)
    - a Jira item with two transitions produces two entries (flatMap over transitions, each using transition.at as the timestamp)
    - cap is respected: 10 inputs with cap 5 → length 5
    - mergeActivityEntries([], [], 5) → []
    - timezone-safe bucketing (MANDATED): buildWeekBuckets([{ dateStarted:'2026-06-14', timeSpentSeconds:3600 }], '2026-06-09') → friday bucket hours === 1
    - groupMrsByRole non-overlap: an MR authored AND reviewed by userId appears only in myOpen
  </behavior>
  <action>
    In `dashboardMetrics.ts`, define an exported `ActivityEntry` discriminated union: `{ type: 'jira'; at: string; item: JiraActivityItem } | { type: 'commit'; at: string; item: GitLabCommit }`. Implement `export function mergeActivityEntries(jiraItems: JiraActivityItem[], commits: GitLabCommit[], cap: number): ActivityEntry[]`. Flatten each Jira item's `transitions` into `{ type:'jira', at: t.at, item }` entries; map each commit to `{ type:'commit', at: c.authored_date, item: c }`; concatenate; sort descending by `at` using `b.at.localeCompare(a.at)` (ISO strings sort correctly as strings — add a comment that no Date construction is needed); `.slice(0, cap)`. Import `JiraActivityItem` from `@/services/jira` and `GitLabCommit` from `@/services/gitlab` (type-only imports). Implements DASH-05 (criterion 2 interleave) per D-10.

    In `dashboardMetrics.test.ts`, add a new `describe('Phase 84 — buildWeekBuckets', ...)` block with: the MANDATED timezone-safe test exactly as specified in 84-PATTERNS.md lines 517-522 (worklog `dateStarted: '2026-06-14'` pre-normalized from `'2026-06-14T23:00:00'`, weekStart `'2026-06-09'`, assert friday bucket hours === 1) — include a comment quoting criterion 1; the future-days-zero test (empty array → 5 buckets all hours 0); a same-day-sum test. Add a `describe('Phase 84 — groupMrsByRole', ...)` block testing the non-overlap case (author+reviewer → only myOpen) and the basic split. Add a `describe('Phase 84 — mergeActivityEntries', ...)` block testing newest-first ordering, multi-transition flatMap, and cap. Build minimal typed fixtures with `as TempoWorklog[]` / `as GitLabMR[]` casts where only a subset of fields matters (mirror existing test fixture style in the file).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/dashboardMetrics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/routes/dashboard/dashboardMetrics.ts` exports `mergeActivityEntries` and `ActivityEntry`
    - Test file contains the literal string `2026-06-14T23:00:00` in a comment for the mandated test, and asserts the friday bucket hours === 1
    - `cd taskflow && npx vitest run src/routes/dashboard/dashboardMetrics.test.ts` reports all tests passing (existing + new)
    - `cd taskflow && npm run check` (biome + tsc) passes
  </acceptance_criteria>
  <done>All three pure functions exist and are unit-tested; the mandated timezone-safe test and the MR non-overlap test pass; check is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds only pure functions and unit tests — no I/O, no network, no DOM, no token handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Information Disclosure | pure helpers in dashboardMetrics.ts | accept | Functions operate on already-fetched, already-trusted in-memory data; no secrets, no token, no network. No new surface. |
| T-84-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH Package Legitimacy Audit: zero new packages). Nothing to verify. |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/dashboardMetrics.test.ts` — all green
- `cd taskflow && npm run check` — biome + tsc green
- Manual grep confirms no `toISOString`/`toLocaleDateString` inside the worklog-bucket-matching path
</verification>

<success_criteria>
- `buildWeekBuckets`, `mergeActivityEntries`, `groupMrsByRole` exported as pure functions from `dashboardMetrics.ts`
- The mandated timezone-safe bucketing test passes (DASH-04 criterion 1)
- The MR non-overlap grouping test passes (DASH-06)
- The newest-first interleave test passes (DASH-05)
- Module remains React-free; full `npm run check` green
</success_criteria>

<output>
Create `.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-01-SUMMARY.md` when done
</output>
