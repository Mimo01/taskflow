---
phase: 84-dashboard-trend-chart-mr-review-queue-and-activity-strip
plan: 03
type: execute
wave: 2
depends_on: ["84-01"]
files_modified:
  - taskflow/src/routes/dashboard/ActivityStrip.tsx
  - taskflow/src/routes/dashboard/ActivityStrip.test.tsx
autonomous: true
requirements: [DASH-05, DASH-07]
must_haves:
  truths:
    - "ActivityStrip uses byte-identical TanStack Query keys to StandupNotesPage's Yesterday Jira-activity and commits queries, so a warm Standup cache is shared (no duplicate request)"
    - "ActivityStrip fetches on a cold Dashboard load (not enabled:false) and shows the merged newest-first feed"
    - "The feed is capped at 5-7 items with a non-expanding '+N more' indicator for the remainder"
    - "Each source error is handled independently; the strip never goes fully blank from one source failing (DASH-07)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/ActivityStrip.tsx"
      provides: "Compact interleaved Jira+commits activity feed reusing Standup query keys"
      contains: "mergeActivityEntries"
  key_links:
    - from: "taskflow/src/routes/dashboard/ActivityStrip.tsx"
      to: "warm ['standup','jira',...] cache (StandupNotesPage)"
      via: "identical useQuery key"
      pattern: "'standup',\\s*'jira'"
    - from: "taskflow/src/routes/dashboard/ActivityStrip.tsx"
      to: "warm ['standup','commits',...] cache (StandupNotesPage)"
      via: "identical useQuery key, sixth element gitlabUsername || gitlabName || ''"
      pattern: "'standup',\\s*'commits'"
---

<objective>
Build the third new Dashboard section: `ActivityStrip` (DASH-05) — a compact, capped, newest-first feed merging Jira changelog/comment activity and GitLab commits. Its defining constraint is byte-identical TanStack Query keys to StandupNotesPage's Yesterday fetch, so any warm Standup cache is reused with zero duplicate network requests (criterion 2), while still fetching on a cold Dashboard load (D-09).

Purpose: This is the single most cache-fragile component in the phase — the wrong sixth element of the commits key fires a duplicate request and breaks criterion 2. The plan pins the exact key construction and adds a key-equality test. Shares no files with Plan 02; runs in the same wave.

Output: `ActivityStrip.tsx` + test.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-CONTEXT.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-RESEARCH.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-PATTERNS.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-UI-SPEC.md
@taskflow/src/routes/standup-notes/StandupNotesPage.tsx
@taskflow/src/routes/dashboard/dashboardMetrics.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActivityStrip.tsx — shared-key Jira+commits feed with independent degradation</name>
  <files>taskflow/src/routes/dashboard/ActivityStrip.tsx</files>
  <read_first>
    - taskflow/src/routes/standup-notes/StandupNotesPage.tsx lines 308-403 (THE source of truth for the two query keys: jiraActivityQuery key = ['standup','jira', jiraBaseUrl, activeJiraProject, yesterdayDate, jiraUsername ?? '']; commitsQuery key = ['standup','commits', gitlabBaseUrl, activeGitlabProject, yesterdayDate, resolvedAccountsKey || resolvedId.gitlabUsername || resolvedId.gitlabName || '']. For the Dashboard self-user case resolvedAccountsKey is '' so the sixth element is gitlabUsername || gitlabName || '')
    - taskflow/src/routes/dashboard/dashboardMetrics.ts (mergeActivityEntries, ActivityEntry — import; never re-derive merge/sort inline)
    - taskflow/src/services/jira.ts lines 778-833 (JiraActivityItem shape + fetchYesterdayJiraActivity signature)
    - taskflow/src/services/gitlab.ts (fetchUserCommits(baseUrl, token, projectId, date, usernames[], names[], emails[]) signature + GitLabCommit shape)
    - taskflow/src/routes/standup-notes/YesterdayColumn.tsx (compact-row rendering + per-source ErrorState reference)
    - taskflow/src/components/ui/{empty-state,error-state,skeleton}.tsx (state primitive props)
    - .planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-UI-SPEC.md (ActivityStrip visual + copywriting contract)
    - .planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-PATTERNS.md (ActivityStrip pattern lines 285-446; Pitfalls 1, 2, 5 on key sixth-element, yesterdayDate, token-in-key)
  </read_first>
  <behavior>
    - The Jira-activity useQuery key array is exactly ['standup', 'jira', jiraBaseUrl, activeJiraProject, yesterdayDate, jiraUsername ?? '']
    - The commits useQuery key array is exactly ['standup', 'commits', gitlabBaseUrl, activeGitlabProject, yesterdayDate, gitlabUsername || gitlabName || '']
    - Neither key contains a token string
    - Neither query uses enabled:false; both use the full enabled guard (cold Dashboard load fetches)
    - The rendered feed equals mergeActivityEntries(jiraData, commitsData, CAP) with CAP between 5 and 7
    - When total merged entries exceed CAP, a "+N more" button renders showing the remainder count
    - If the Jira query errors but commits succeed (or vice versa), the strip still renders the succeeding source plus an inline ErrorState for the failed one — never fully blank
  </behavior>
  <action>
    Create `ActivityStrip.tsx` with `'use no memo';` line 1. Props (props-only, loaded by index.tsx): `ActivityStripProps { jiraBaseUrl: string; jiraToken: string; activeJiraProject: string; jiraUsername: string | null; gitlabBaseUrl: string; gitlabToken: string; activeGitlabProject: number; gitlabUsername: string | null; gitlabName: string | null; gitlabEmail: string | null }`.

    Compute `yesterdayDate` with `new Date(Date.now() - 86_400_000).toLocaleDateString('en-CA')` (en-CA → YYYY-MM-DD local; NEVER toISOString — Pitfall 2). Define `const CAP = 6`.

    Jira activity query — key MUST be byte-identical to StandupNotesPage: `queryKey: ['standup', 'jira', jiraBaseUrl, activeJiraProject, yesterdayDate, jiraUsername ?? '']`. queryFn: `const token = await readSecret('jira-pat').catch(() => null); if (!token) throw new Error('No Jira token'); return fetchYesterdayJiraActivity(jiraBaseUrl ?? '', token, activeJiraProject ?? '', yesterdayDate, jiraUsername ?? '')`. `enabled: !!jiraBaseUrl && !!jiraToken && !!activeJiraProject && !!jiraUsername && !!yesterdayDate`. `staleTime: 5 * 60 * 1000`.

    Commits query — key MUST be byte-identical for the self-user case: `queryKey: ['standup', 'commits', gitlabBaseUrl, activeGitlabProject, yesterdayDate, gitlabUsername || gitlabName || '']` (NOT gitlabUserId — Pitfall 1; add the exact comment from 84-PATTERNS line 336-339 explaining the self-user path). queryFn: read 'gitlab-pat' via readSecret, throw if missing, then `fetchUserCommits(gitlabBaseUrl ?? '', token, activeGitlabProject ?? 0, yesterdayDate, [gitlabUsername ?? ''], [gitlabName ?? ''], [gitlabEmail ?? ''])` — match StandupNotesPage's self-user array argument shape (single-element arrays). `enabled: !!gitlabBaseUrl && !!gitlabToken && !!activeGitlabProject && !!yesterdayDate && (!!gitlabUsername || !!gitlabName)`. `staleTime: 5 * 60 * 1000`. DO NOT use enabled:false (D-09 — strip must fetch on cold load).

    Tokens MUST NOT enter either queryKey (T-62-06); use `!!jiraToken` / `!!gitlabToken` booleans only in the enabled guards. Add the verbatim comment that this matches StandupNotesPage for cache sharing (D-08, criterion 2).

    `const entries = useMemo(() => mergeActivityEntries(jiraActivityQuery.data ?? [], commitsQuery.data ?? [], CAP), [jiraActivityQuery.data, commitsQuery.data])`. Compute total merged count for the overflow: derive total via mergeActivityEntries with a large cap or by summing flatMapped jira transitions + commits length; `const overflow = Math.max(0, total - CAP)`. `const showSkeleton = useDelayedLoading(jiraActivityQuery.isLoading || commitsQuery.isLoading)`.

    Render a card `<div role="region" aria-label="Recent activity" className="rounded-lg border border-border bg-card p-4 flex flex-col gap-3">` with title "Recent activity". States: showSkeleton → 3 skeleton rows; both queries error → ErrorState ("Couldn't load activity", viewName="recent activity"); both empty & not loading → `<EmptyState icon={Activity} title="No recent activity" subtitle="Jira and GitLab activity from the last 24 hours will appear here." />`. Otherwise render the entries: each row `<div className="flex items-start gap-2 py-1">` with a size-4 icon (`Activity` text-sky-500 for jira, `GitCommitHorizontal` text-amber-500 for commit; aria-hidden), a `text-sm text-foreground truncate flex-1` description (jira: a short "moved {issueKey} to {toStatus}" / commit: commit title), and a `text-xs text-muted-foreground` relative timestamp. Render per-source inline ErrorState blocks independently (jira error and commits error each handled separately so one failure never blanks the other — DASH-07, Pitfall reference YesterdayColumn 796-820). If `overflow > 0`, render a non-expanding `<button type="button" className="text-xs text-muted-foreground hover:text-foreground mt-1 min-h-[32px]">+{overflow} more</button>` (no click handler this phase — D-11). Colors via tokens/tailwind tints per UI-SPEC; no hardcoded hex. Implements DASH-05 (D-07, D-08, D-09, D-10, D-11) and DASH-07 (D-17).
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit 2>&1 | grep -i ActivityStrip || echo "no ActivityStrip type errors"</automated>
  </verify>
  <acceptance_criteria>
    - File line 1 is exactly `'use no memo';`
    - The Jira key literal is exactly `'standup', 'jira', jiraBaseUrl, activeJiraProject, yesterdayDate, jiraUsername ?? ''`
    - The commits key sixth element is exactly `gitlabUsername || gitlabName || ''` (grep confirms; NOT gitlabUserId)
    - `grep -n "enabled: false\|enabled:false" taskflow/src/routes/dashboard/ActivityStrip.tsx` returns nothing
    - `grep -n "toISOString" taskflow/src/routes/dashboard/ActivityStrip.tsx` returns nothing
    - Neither queryKey array contains `jiraToken` or `gitlabToken`
    - `grep -n "mergeActivityEntries" taskflow/src/routes/dashboard/ActivityStrip.tsx` shows merge/sort delegated to dashboardMetrics
    - tsc reports no ActivityStrip type errors
  </acceptance_criteria>
  <done>ActivityStrip renders the merged capped feed using byte-identical Standup keys, fetches on cold load, caps with "+N more", and degrades per-source; types clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ActivityStrip.test.tsx — key-equality + interleave + cap + independent-degradation tests</name>
  <files>taskflow/src/routes/dashboard/ActivityStrip.test.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/ActivityStrip.tsx (the component under test)
    - taskflow/src/routes/standup-notes/StandupNotesPage.tsx lines 308-403 (the keys this test asserts equality against)
    - taskflow/src/routes/dashboard/MrHealthPanel.test.tsx OR SprintHealthSection.test.tsx (QueryClientProvider render harness + seeded-cache pattern to mirror)
    - .planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-VALIDATION.md (mandated criterion-2 verification + manual-only network observation)
  </read_first>
  <behavior>
    - A test seeds the QueryClient with data under exactly ['standup','jira', baseUrl, project, yesterdayDate, username] and ['standup','commits', glBaseUrl, glProject, yesterdayDate, glUsername]; rendering ActivityStrip with matching props consumes the seeded data and fires NO new fetch (queryFn spy not called)
    - The merged feed renders newest-first (commit at 12:00 before jira transition at 10:00)
    - With more than CAP entries seeded, a "+N more" indicator with the correct remainder renders
    - Forcing the Jira query into an error state while commits succeed still renders the commit rows (strip not blank)
  </behavior>
  <action>
    Create `ActivityStrip.test.tsx` mirroring the existing dashboard component test harness (QueryClientProvider + render, auth store set as needed). Write a `describe('ActivityStrip — shared-key cache reuse', ...)`:

    Test 1 (criterion 2 — no duplicate request): compute the same `yesterdayDate` the component uses, seed the QueryClient via `queryClient.setQueryData([...jira key...], jiraFixture)` and `setQueryData([...commits key...], commitsFixture)` using the EXACT key arrays from StandupNotesPage (jira sixth = username, commits sixth = gitlabUsername || gitlabName || ''). Spy on the network functions (mock `fetchYesterdayJiraActivity`/`fetchUserCommits` modules) and assert they are NOT called after render — proving the warm Standup cache is reused (the keys match byte-for-byte). If precise spy assertion is impractical in the harness, at minimum assert the seeded data renders without a loading state.

    Test 2 (interleave/ordering): seed one jira transition and one later commit; assert the commit row appears before the jira row in the DOM.

    Test 3 (cap): seed > CAP entries; assert "+N more" text with the correct remainder.

    Test 4 (independent degradation, DASH-07): drive the jira query to error (e.g. seed an error state or mock the queryFn to reject) while commits succeed; assert a commit row still renders and the strip is not fully blank.

    Use minimal typed fixtures (`as JiraActivityItem[]`, `as GitLabCommit[]`).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/ActivityStrip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test file seeds the cache using the exact StandupNotesPage key arrays (jira + commits)
    - A test asserts no duplicate fetch (spy not called) OR that seeded data renders without loading — proving key reuse
    - A test asserts newest-first ordering
    - A test asserts "+N more" with correct remainder
    - A test asserts independent degradation (one source error, other still renders)
    - `cd taskflow && npx vitest run src/routes/dashboard/ActivityStrip.test.tsx` green
    - `cd taskflow && npm run check` green
  </acceptance_criteria>
  <done>ActivityStrip tests prove key-identity cache reuse, newest-first ordering, cap overflow, and independent per-source degradation; check is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Jira (read) | ActivityStrip reuses the Standup Jira-activity query; queryFn reads the jira PAT via readSecret. Shared key = no new endpoint when warm; cold load fires the same read Standup already uses. |
| client → GitLab (read) | Reuses the Standup commits query; queryFn reads the gitlab PAT via readSecret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-02 | Information Disclosure | tokens in queryKey | mitigate | jira/gitlab tokens never appear in either queryKey (T-62-06); read inside queryFn via readSecret. Grep gate in acceptance criteria. |
| T-84-04 | Tampering | XSS via activity text / commit title | mitigate | All text rendered as JSX text nodes; React escapes. No dangerouslySetInnerHTML. |
| T-84-05 | Denial of Service | duplicate fetch load | mitigate | Byte-identical Standup keys ensure warm-cache reuse (criterion 2); cold load fires the same single read Standup would. No new polling interval. |
| T-84-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (RESEARCH Package Legitimacy Audit: zero new packages). |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/ActivityStrip.test.tsx` — green
- Grep gates: commits key sixth element is `gitlabUsername || gitlabName || ''`, no `enabled: false`, no `toISOString`, no token in key
- `cd taskflow && npm run check` — green
- (Manual, deferred to UAT in Plan 04) Visit Standup then Dashboard same session → zero new Jira-activity/commits requests
</verification>

<success_criteria>
- ActivityStrip uses byte-identical Standup keys → warm cache shared, no duplicate request (DASH-05 criterion 2)
- Fetches on cold Dashboard load (not enabled:false) (D-09)
- Merged newest-first feed, capped with "+N more" (D-10, D-11)
- Per-source independent degradation (DASH-07)
- Test file proves key identity, ordering, cap, and independent degradation; full check green
</success_criteria>

<output>
Create `.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-03-SUMMARY.md` when done
</output>
