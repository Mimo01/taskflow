---
phase: 84-dashboard-trend-chart-mr-review-queue-and-activity-strip
plan: 02
type: execute
wave: 2
depends_on: ["84-01"]
files_modified:
  - taskflow/src/routes/dashboard/WeeklyTrendChart.tsx
  - taskflow/src/routes/dashboard/WeeklyTrendChart.test.tsx
  - taskflow/src/routes/dashboard/MrReviewQueue.tsx
  - taskflow/src/routes/dashboard/MrReviewQueue.test.tsx
autonomous: true
requirements: [DASH-04, DASH-06, DASH-07]
must_haves:
  truths:
    - "WeeklyTrendChart renders a Mon-Fri bar chart of logged hours with an 8h reference line when tempoEnabled is true"
    - "WeeklyTrendChart renders a 'Tempo not connected' empty state (not an error) when tempoEnabled is false"
    - "MrReviewQueue lists two groups (Awaiting my review / My open MRs) derived client-side from the warm gitlab-mrs cache — no new fetch beyond the shared key"
    - "MrReviewQueue shows 'GitLab not connected' when no token resolves and 'No MRs awaiting review' when the queue is empty"
    - "Each component degrades independently with its own loading/error/empty state (DASH-07)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/WeeklyTrendChart.tsx"
      provides: "Weekly logged-hours BarChart in ChartWrapper with ReferenceLine"
      contains: "buildWeekBuckets"
    - path: "taskflow/src/routes/dashboard/MrReviewQueue.tsx"
      provides: "Two-group MR review queue from warm cache"
      contains: "groupMrsByRole"
  key_links:
    - from: "taskflow/src/routes/dashboard/WeeklyTrendChart.tsx"
      to: "fetchWorklogs via useQuery"
      via: "queryKey ['dashboard','tempo-week', jiraBaseUrl, weekStartDate, jiraUsername]"
      pattern: "'dashboard',\\s*'tempo-week'"
    - from: "taskflow/src/routes/dashboard/MrReviewQueue.tsx"
      to: "warm ['gitlab-mrs', gitlabBaseUrl, userId] cache"
      via: "useQuery same key as MrHealthPanel"
      pattern: "'gitlab-mrs'"
---

<objective>
Build two of the three new Dashboard sections: `WeeklyTrendChart` (DASH-04 — the ONE permitted new fetch, a Mon-Fri Tempo bar chart with a fixed 8h target line) and `MrReviewQueue` (DASH-06 — two-group MR queue derived from the warm `gitlab-mrs` cache, no new fetch). Both are props-only components mounted later by index.tsx, each with full independent degradation (DASH-07).

Purpose: These two sections share no files with Plan 03 (ActivityStrip) and run in the same wave. They consume the Plan 01 pure functions (`buildWeekBuckets`, `groupMrsByRole`) and follow the verified Phase 81 chart rules and the MrHealthPanel cache pattern exactly.

Output: `WeeklyTrendChart.tsx` + test, `MrReviewQueue.tsx` + test.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-CONTEXT.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-RESEARCH.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-PATTERNS.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-UI-SPEC.md
@taskflow/src/routes/dashboard/SprintHealthSection.tsx
@taskflow/src/routes/dashboard/MrHealthPanel.tsx
@taskflow/src/routes/dashboard/dashboardMetrics.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WeeklyTrendChart.tsx (Tempo bar chart + 8h ReferenceLine) + test</name>
  <files>taskflow/src/routes/dashboard/WeeklyTrendChart.tsx, taskflow/src/routes/dashboard/WeeklyTrendChart.test.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/SprintHealthSection.tsx (exact chart analog: 'use no memo' line 1, props-only auth, useQuery + staleTime + enabled guard, ChartWrapper + ChartContainer + responsive prop, isAnimationActive={false}, useDelayedLoading)
    - taskflow/src/components/chart-wrapper.tsx (ChartWrapper props: title, description, height, isLoading, error, isEmpty, onRetry, children — confirm exact prop names before use)
    - taskflow/src/routes/dashboard/dashboardMetrics.ts (buildWeekBuckets, DAILY_TARGET_HOURS, WeekBucket — import from './dashboardMetrics')
    - taskflow/src/services/tempo/worklogs.ts (fetchWorklogs(baseUrl, token, usernames[], from, to) signature)
    - taskflow/src/stores/settings.store.ts line 56 (tempoEnabled selector — but this component receives tempoEnabled as a PROP, not via store; confirm the value semantics)
    - .planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-UI-SPEC.md (WeeklyTrendChart copywriting + visual contract)
    - .planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-PATTERNS.md (WeeklyTrendChart pattern, lines 24-128; Pitfall 6 on all-zero weeks)
  </read_first>
  <behavior>
    - tempoEnabled=false → renders the "Tempo not connected" EmptyState (via ChartWrapper isEmpty), NOT an ErrorState
    - tempoEnabled=true with worklog data → renders a BarChart (bars present in DOM/SVG); the bucket data passed to BarChart comes from buildWeekBuckets
    - tempoEnabled=true with an empty worklogs array → renders the chart with all-zero bars (isEmpty stays false; an all-zero week is valid data, not empty — Pitfall 6)
    - query error (tempoEnabled=true) → ChartWrapper receives the error and renders ErrorState with retry
  </behavior>
  <action>
    Create `WeeklyTrendChart.tsx` with `'use no memo';` as line 1. Props interface `WeeklyTrendChartProps { jiraBaseUrl: string; jiraToken: string; jiraUsername: string; tempoEnabled: boolean }` — props-only, no useAuthStore/readSecret inside (SprintHealthSection precedent).

    Compute `weekStartDate` (Monday of current week, YYYY-MM-DD) and `todayDate` (en-CA local date, never toISOString) at the top. For Monday: derive via local calendar (`new Date()` getDay() to find Monday offset) then format the Monday with `toLocaleDateString('en-CA')` — this Date usage is for "which week am I in" only; the worklog bucketing itself is string-based inside buildWeekBuckets. Add a comment distinguishing the two.

    Fire the ONE permitted new fetch (D-05): `useQuery({ queryKey: ['dashboard','tempo-week', jiraBaseUrl, weekStartDate, jiraUsername], queryFn: () => fetchWorklogs(jiraBaseUrl, jiraToken, [jiraUsername], weekStartDate, todayDate), staleTime: 30_000, enabled: !!jiraBaseUrl && !!jiraToken && !!jiraUsername && tempoEnabled })`. Token MUST NOT appear in the queryKey (T-62-06); it is passed via the queryFn closure from the prop. `const showSkeleton = useDelayedLoading(isLoading)`.

    `const buckets = buildWeekBuckets(worklogs ?? [], weekStartDate)`. Define `chartConfig = { hours: { label: 'Hours logged', color: 'var(--chart-1)' } } satisfies ChartConfig`.

    Render `ChartWrapper` with `title="Hours logged this week"`, `description="Mon – Fri · 8 h/day target"`, `height={240}`, `isLoading={showSkeleton}`, `error={!tempoEnabled ? undefined : error}`, `isEmpty={!tempoEnabled}` (D-06: only Tempo-off is the empty state; all-zero weeks still render the chart — Pitfall 6), `onRetry={refetch}`. Inside: an explicit-height outer `<div style={{ height: 240 }} className="w-full">` wrapping `<ChartContainer config={chartConfig} className="h-full w-full" aria-label="Weekly logged hours bar chart">` containing `<BarChart data={buckets} responsive>` with `<XAxis dataKey="label" />`, `<YAxis domain={[0, 12]} tickFormatter={(v) => \`${v}h\`} />`, `<Bar dataKey="hours" fill="var(--chart-1)" isAnimationActive={false} />`, and `<ReferenceLine y={DAILY_TARGET_HOURS} stroke="var(--chart-2)" label={{ value: 'Target', position: 'right', fontSize: 11 }} />`.

    Wrap the whole thing in `role="region" aria-label="Weekly hours logged"` if not already provided by ChartWrapper's card. Colors are `var(--chart-N)` tokens only — no hex. Implements DASH-04 (D-01, D-02, D-03, D-05, D-06) and DASH-07 (D-17).

    Create `WeeklyTrendChart.test.tsx` mirroring `SprintHealthSection.test.tsx` setup (QueryClientProvider wrapper, render). Test: tempoEnabled=false renders the "Tempo not connected" copy and does NOT render an error; tempoEnabled=true with a mocked query returning [] still renders the chart container (not the empty state). Mock `fetchWorklogs` / use a QueryClient with seeded data as the existing dashboard tests do.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/WeeklyTrendChart.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File line 1 is exactly `'use no memo';`
    - `grep -n "ResponsiveContainer" taskflow/src/routes/dashboard/WeeklyTrendChart.tsx` returns nothing (uses `responsive` prop)
    - `grep -nE "#[0-9a-fA-F]{3,6}" taskflow/src/routes/dashboard/WeeklyTrendChart.tsx` returns no hardcoded hex colors
    - `grep -n "toISOString" taskflow/src/routes/dashboard/WeeklyTrendChart.tsx` returns nothing
    - queryKey contains `'dashboard', 'tempo-week'` and does NOT contain `jiraToken`
    - `isAnimationActive={false}` present on the Bar
    - Test asserts tempoEnabled=false → "Tempo not connected" empty state, not error
    - `cd taskflow && npx vitest run src/routes/dashboard/WeeklyTrendChart.test.tsx` green
  </acceptance_criteria>
  <done>WeeklyTrendChart renders the Tempo bar chart with 8h reference line, degrades to the Tempo-off empty state, follows all Phase 81 chart rules, and its test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MrReviewQueue.tsx (two-group MR queue from warm cache) + test</name>
  <files>taskflow/src/routes/dashboard/MrReviewQueue.tsx, taskflow/src/routes/dashboard/MrReviewQueue.test.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/MrHealthPanel.tsx (exact cache analog: ['gitlab-mrs', gitlabBaseUrl, userId] useQuery + queryFn deduping fetchAssignedMRs+fetchReviewerMRs into { filtered, merged }, gitlabUserId via useAuthStore, tokenLoading prop, per-MR ['mr-health', project_id, iid] via getQueryData)
    - taskflow/src/routes/dashboard/dashboardMetrics.ts (groupMrsByRole — import and use; do NOT re-derive grouping inline)
    - taskflow/src/services/gitlab.ts lines 243-258 (GitLabMR shape) + fetchAssignedMRs/fetchReviewerMRs signatures
    - taskflow/src/components/ui/empty-state.tsx (EmptyState props: icon, title, subtitle)
    - .planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-UI-SPEC.md (MrReviewQueue visual + copywriting contract, health badge labels)
    - .planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-PATTERNS.md (MrReviewQueue pattern lines 131-281: card shell, overlay-button row, health badge, empty states)
    - project memory: overlay button for nested interactive (absolute inset-0 button as SIBLING of the health badge span, not nested)
  </read_first>
  <behavior>
    - With a warm gitlab-mrs cache containing 1 reviewer-MR (not authored by me) and 1 authored-MR → renders both groups, each with its MR; the reviewer-MR appears only under "Awaiting my review", the authored-MR only under "My open MRs"
    - An MR I both authored and review appears only under "My open MRs" (groupMrsByRole non-overlap)
    - tokenLoading=true → skeleton rows, not the "GitLab not connected" empty state
    - tokenLoading=false and no gitlabBaseUrl/token → "GitLab not connected" EmptyState
    - cache present but both groups empty → "No MRs awaiting review" EmptyState
    - per-MR health badge reads ['mr-health', project_id, iid]: undefined → "Needs review", 'approved' → "Approved", 'changes_requested' → "Changes requested"
  </behavior>
  <action>
    Create `MrReviewQueue.tsx` (add `'use no memo';` line 1 — dashboard convention). Props `MrReviewQueueProps { gitlabBaseUrl: string; gitlabToken: string; tokenLoading?: boolean }`. Read `userId = useAuthStore((s) => s.gitlabUserId) ?? undefined`.

    Replicate MrHealthPanel's `useQuery` EXACTLY for cache sharing: `queryKey: ['gitlab-mrs', gitlabBaseUrl, userId]`, same queryFn (Promise.all of fetchAssignedMRs + fetchReviewerMRs, dedupe by iid into `{ filtered: merged, merged }`), `staleTime: 30_000`, `enabled: !!gitlabBaseUrl && !!gitlabToken && !!userId`. This makes MrReviewQueue and MrHealthPanel share the same cache entry (no duplicate fetch). `const showSkeleton = useDelayedLoading(isLoading)`.

    `const allMrs = mrQueryData?.filtered ?? []`. `const { awaitingReview, myOpen } = groupMrsByRole(allMrs, userId)` (from dashboardMetrics — never inline the filter). Read per-MR health with `queryClient.getQueryData<string>(['mr-health', mr.project_id, mr.iid])`; undefined → treat as needs_review.

    Render order: card shell `<div role="region" aria-label="MR review queue" className="rounded-lg border border-border bg-card p-4 flex flex-col gap-3 min-h-[160px]">` with a title "MR review queue". Branch: if (tokenLoading || showSkeleton) → 3 skeleton rows; else if no gitlabBaseUrl or no token (resolved) → `<EmptyState icon={Plugin} title="GitLab not connected" subtitle="Connect GitLab in Settings to see your MR queue." />`; else if awaitingReview.length === 0 && myOpen.length === 0 → `<EmptyState icon={GitMerge} title="No MRs awaiting review" subtitle="You're all caught up." />`; else render the two labelled groups.

    Each group: an uppercase label `<span className="text-xs font-medium text-muted-foreground uppercase tracking-wide">Awaiting my review</span>` / "My open MRs"; then each MR row using the OVERLAY BUTTON pattern (per memory): `<div className="relative flex items-center gap-2 py-2 hover:bg-muted/50 rounded cursor-pointer">` containing an `<button type="button" className="absolute inset-0 rounded" aria-label={mr.title} onClick={() => openUrl(mr.web_url)} />`, then a `<span className="text-sm truncate flex-1">{mr.title}</span>`, a `text-xs text-muted-foreground` project/author line, and a HealthBadge SIBLING (colored dot + text label: "Needs review"/"Approved"/"Changes requested", dot colors text-amber-500 / text-green-500 / text-destructive — never color alone). Import `openUrl` from `@tauri-apps/plugin-opener` (never window.open). Empty groups within a non-empty queue render their label with no rows (acceptable). Implements DASH-06 (D-12, D-13, D-14, D-15) and DASH-07.

    Create `MrReviewQueue.test.tsx` mirroring `MrHealthPanel.test.tsx`: seed the QueryClient with a `['gitlab-mrs', baseUrl, userId]` entry and `['mr-health', ...]` entries; render with auth-store userId set. Assert grouping (reviewer MR under group 1, authored MR under group 2), the GitLab-not-connected empty state (no token), and the empty-queue state.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/MrReviewQueue.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - queryKey is exactly `['gitlab-mrs', gitlabBaseUrl, userId]` (byte-matches MrHealthPanel so the cache is shared)
    - `grep -n "groupMrsByRole" taskflow/src/routes/dashboard/MrReviewQueue.tsx` shows the grouping is delegated to dashboardMetrics (no inline `.filter` on reviewers/author in the component)
    - `grep -n "window.open" taskflow/src/routes/dashboard/MrReviewQueue.tsx` returns nothing; `openUrl` is imported from `@tauri-apps/plugin-opener`
    - MR row uses the overlay-button-sibling pattern (no `<button>` nested inside another interactive element)
    - Health badge always pairs a dot with a text label
    - `cd taskflow && npx vitest run src/routes/dashboard/MrReviewQueue.test.tsx` green
  </acceptance_criteria>
  <done>MrReviewQueue renders two non-overlapping groups from the shared warm cache with no new fetch, context-aware empty states, health badges, and external-browser MR links; test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GitLab (read) | MrReviewQueue reads the warm gitlab-mrs cache; its queryFn (shared with MrHealthPanel) calls GitLab with a PAT loaded via readSecret. No new endpoint, no write. |
| client → external browser | MR row click opens `mr.web_url` via Tauri `openUrl`. |
| client → Tempo (read) | WeeklyTrendChart fires one new read-only fetchWorklogs call using the existing jira PAT. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-02 | Information Disclosure | tokens in queryKey | mitigate | jiraToken/gitlabToken NEVER appear in any queryKey (T-62-06); tokens live only in queryFn closures. Enforced by grep gate in acceptance criteria. |
| T-84-03 | Spoofing | open redirect via mr.web_url | accept | web_url originates from the configured GitLab instance API, not user input; opened via Tauri openUrl. Low risk per RESEARCH Security Domain (V5). |
| T-84-04 | Tampering | XSS via MR title / activity text | mitigate | All strings rendered as JSX text nodes, never dangerouslySetInnerHTML. React escapes by default. |
| T-84-05 | Denial of Service | extra Tempo/GitLab fetch load | accept | WeeklyTrendChart is the single sanctioned new fetch (D-05), gated by tempoEnabled + enabled guards; MrReviewQueue adds no new fetch (shares cache). |
| T-84-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (RESEARCH Package Legitimacy Audit: zero new packages). |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/WeeklyTrendChart.test.tsx src/routes/dashboard/MrReviewQueue.test.tsx` — green
- Grep gates in acceptance criteria (no ResponsiveContainer, no hex, no toISOString, no token-in-key, no window.open) all clean
- `cd taskflow && npm run check` — biome + tsc green
</verification>

<success_criteria>
- WeeklyTrendChart: Mon-Fri bars + 8h ReferenceLine when tempoEnabled; "Tempo not connected" empty state otherwise; all Phase 81 chart rules honored (DASH-04)
- MrReviewQueue: two non-overlapping groups from the shared warm cache, no new fetch, context-aware empty states, health badges, external MR links (DASH-06)
- Both components own their loading/error/empty states independently (DASH-07)
- Both test files green; full check green
</success_criteria>

<output>
Create `.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-02-SUMMARY.md` when done
</output>
