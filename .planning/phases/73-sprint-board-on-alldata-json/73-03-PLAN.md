---
phase: 73-sprint-board-on-alldata-json
plan: 03
type: execute
wave: 3
depends_on:
  - 73-02
files_modified:
  - taskflow/src/routes/dashboard/SprintBoardTab.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
  - taskflow/src/components/app/Sidebar.tsx
  - taskflow/src/components/app/Sidebar.test.tsx
  - taskflow/src/services/jira.ts
  - taskflow/src/services/jira/sprint-subtasks.ts
autonomous: true
requirements:
  - GH-BOARD-04
  - GH-CUT-01
must_haves:
  truths:
    - "The sprint-board toolbar shows ONE reload control labeled 'Reload board' (Phase 72's 'Reload workflow transitions' item is gone; the bare RefreshCw is gone; replaced by a single button per D-07a + UI-SPEC §2)"
    - "Clicking 'Reload board' invalidates all FIVE query keys per D-07 + R-01 + R-02: ['gh-all-data', boardId], ['gh-transitions', projectId], ['jira-statuses'], ['jira-board-quickfilters', boardId], ['jira-active-sprint', activeJiraProject, jiraBaseUrl]"
    - "On success the aria-live span reads 'Board reloaded'; on failure 'Failed to reload board'; auto-clears after 3 seconds"
    - "Sidebar prefetch for '/sprint-board' uses getGhAllData(boardId) instead of fetchSprintStories (D-08); skips silently if boardId is null (D-08a)"
    - "fetchSprintSubtasks source module + its re-export from jira.ts are deleted; no caller remains in src/ (D-09 + GH-CUT-01 hard cutover)"
    - "fetchBoardQuickFilters STAYS (R-01) and its caller in SprintBoardTab is unchanged"
  artifacts:
    - path: "taskflow/src/routes/dashboard/SprintBoardTab.tsx"
      provides: "Single 'Reload board' toolbar button; handleReloadBoard handler; Workflow icon import removed"
      contains: "handleReloadBoard"
    - path: "taskflow/src/components/app/Sidebar.tsx"
      provides: "Sprint-board prefetch swapped to getGhAllData via boardId async chain pattern"
      contains: "getGhAllData"
    - path: "taskflow/src/services/jira.ts"
      provides: "fetchSprintSubtasks re-export deleted"
  key_links:
    - from: "taskflow/src/routes/dashboard/SprintBoardTab.tsx"
      to: "invalidateGhAllData + invalidateGhTransitions + queryClient.invalidateQueries"
      via: "handleReloadBoard"
      pattern: "invalidateGhAllData"
    - from: "taskflow/src/components/app/Sidebar.tsx"
      to: "@/services/jira getGhAllData"
      via: "queryClient.fetchQuery(['jira-board-id', ...]).then(boardId => getGhAllData(...))"
      pattern: "getGhAllData\\("
---

<objective>
Close out Phase 73 by consolidating the sprint-board toolbar into a single "Reload board" action (replaces Phase 72's "Reload workflow transitions" per D-07a AND the bare RefreshCw button per UI-SPEC §2), swapping the Sidebar prefetch warm to `getGhAllData` (D-08 / D-08a), and deleting the now-dead `fetchSprintSubtasks` path (D-09; `fetchBoardQuickFilters` stays per R-01).

Purpose: Fulfills the write-side of GH-BOARD-04 (manual refresh action) and GH-CUT-01 (hard cutover — delete the legacy fetcher). Lands the cleanup Plan 02 deferred to keep the rewrite focused.

Output: SprintBoardTab toolbar collapsed to one button; Sidebar prefetch swapped; fetchSprintSubtasks module + re-export deleted; tests updated for the new toolbar + Sidebar prefetch.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-sprint-board-on-alldata-json/73-CONTEXT.md
@.planning/phases/73-sprint-board-on-alldata-json/73-RESEARCH.md
@.planning/phases/73-sprint-board-on-alldata-json/73-PATTERNS.md
@.planning/phases/73-sprint-board-on-alldata-json/73-UI-SPEC.md
@.planning/phases/73-sprint-board-on-alldata-json/73-01-SUMMARY.md
@.planning/phases/73-sprint-board-on-alldata-json/73-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse toolbar to single 'Reload board' action (D-07, D-07a, R-01, R-02, R-04, UI-SPEC §2)</name>
  <files>taskflow/src/routes/dashboard/SprintBoardTab.tsx, taskflow/src/routes/dashboard/SprintBoardTab.test.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/SprintBoardTab.tsx (lines 14-16 lucide import; 754-774 existing `handleReloadWorkflowTransitions` + `reloadTransitionsStatus` state + 3s timeout; 1130-1162 toolbar JSX — the two buttons to merge)
    - taskflow/src/services/jira/greenhopper/transitions.ts (invalidateGhTransitions signature — already imported in SprintBoardTab from Phase 72)
    - .planning/phases/73-sprint-board-on-alldata-json/73-UI-SPEC.md §"2. Reload board Toolbar Action" (markup + className + aria-live + 3s auto-clear + no toast)
    - .planning/phases/73-sprint-board-on-alldata-json/73-PATTERNS.md §"Reload board handler" (handler body + invalidation set)
    - .planning/phases/73-sprint-board-on-alldata-json/73-CONTEXT.md §"research_resolutions" Updated invalidation set for 'Reload board' (5 keys total)
    - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx (existing test for handleReloadWorkflowTransitions — needs replacement)
  </read_first>
  <behavior>
    - The toolbar renders exactly ONE reload `<button>` with aria-label="Reload board" (assert via testing-library getByRole('button', { name: 'Reload board' })); previous two buttons are gone
    - Clicking it calls invalidateGhAllData(qc, boardId), invalidateGhTransitions(qc, projectId), and queryClient.invalidateQueries with each of ['jira-statuses'], ['jira-board-quickfilters', boardId], ['jira-active-sprint', activeJiraProject, jiraBaseUrl] — total 5 invalidations
    - projectId sourced from `allData.issuesData.issues[0]?.projectId` (R-04), NOT from `localIssues[0].fields.project.id`
    - On success, the aria-live span reads "Board reloaded"
    - On thrown error, the aria-live span reads "Failed to reload board"
    - After 3 seconds the aria-live span clears (assert with vi.useFakeTimers + vi.advanceTimersByTime(3000))
  </behavior>
  <action>
Per D-07 + D-07a + R-04 + UI-SPEC §2:

(1) Rename state: `reloadTransitionsStatus` → `reloadBoardStatus`; corresponding setter and the 3s auto-clear `useEffect` rename. Reuse the exact pattern at lines ~754-758.

(2) Replace `handleReloadWorkflowTransitions` (lines ~761-774) with `handleReloadBoard`. Implementation per PATTERNS.md §"Reload board handler" — invalidate the FIVE keys from CONTEXT §"Updated invalidation set for 'Reload board'":
   - `invalidateGhAllData(queryClient, boardId)` (Plan 01 import)
   - `invalidateGhTransitions(queryClient, projectId)` (existing import)
   - `queryClient.invalidateQueries({ queryKey: ['jira-statuses'] })`
   - `queryClient.invalidateQueries({ queryKey: ['jira-board-quickfilters', boardId] })` (R-01)
   - `queryClient.invalidateQueries({ queryKey: ['jira-active-sprint', activeJiraProject, jiraBaseUrl] })` (R-02)

   Source `projectId` per R-04 from raw GH envelope: `const pid = allData?.issuesData.issues[0]?.projectId ?? 0;`. Source `activeSprintId` from existing `activeSprint?.id` if needed for the active-sprint key.

   try/catch: on success `setReloadBoardStatus('Board reloaded')`; on error `setReloadBoardStatus('Failed to reload board')`. No `sonner` / toast (UI-SPEC §2 — no toast library in project).

(3) Add import on line ~14-57 import block: `invalidateGhAllData` from `@/services/jira` (Plan 01 artifact).

(4) Remove `Workflow` from the `lucide-react` import (line 16). Verify with grep that `Workflow` has no other usage in the file before removal — `grep -F "Workflow" taskflow/src/routes/dashboard/SprintBoardTab.tsx` should return 0 after edit.

(5) Toolbar JSX (lines ~1130-1162): collapse the two `<button>` blocks into ONE per UI-SPEC §2:
   - Keep the existing `<span role="status" aria-live="polite">` at line ~1133 — bind its text to `reloadBoardStatus ?? ''`.
   - Replace both buttons with one `<button>` per UI-SPEC §2 markup (RefreshCw icon `size-3`, `animate-spin` while `isFetching`, `aria-label="Reload board"`, `title="Reload board"`, `disabled` while in-flight).

(6) Update `SprintBoardTab.test.tsx`: remove tests against the old "Reload workflow transitions" button; add new tests per the `<behavior>` block. Use `vi.mock` for `invalidateGhAllData`, `invalidateGhTransitions`, and the queryClient `invalidateQueries` spy. Use `vi.useFakeTimers()` for the 3s auto-clear assertion.

Do NOT introduce a toast library (UI-SPEC §2 explicit). Do NOT modify `lib/query-constants.ts` (D-06). Do NOT touch the filter bar, column rendering, or card chrome.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/routes/dashboard/SprintBoardTab.test.tsx --reporter=dot &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "handleReloadBoard" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns ≥2 matches (declaration + JSX onClick)
    - `grep -F "handleReloadWorkflowTransitions" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns 0 matches
    - `grep -F "Workflow" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns 0 matches (icon import removed)
    - `grep -F "invalidateGhAllData" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns ≥1 match
    - All FIVE invalidation calls present in handleReloadBoard body: grep matches for `invalidateGhAllData`, `invalidateGhTransitions`, `'jira-statuses'`, `'jira-board-quickfilters'`, `'jira-active-sprint'` — each ≥1
    - Exactly one toolbar reload button rendered (assert in test via `getAllByRole('button', { name: /reload/i }).length === 1`)
    - Tests for success + failure + 3s auto-clear all green
    - tsc + biome clean
  </acceptance_criteria>
  <done>Toolbar collapsed to single 'Reload board' action with all 5 invalidations wired; tests cover success/error/auto-clear; Workflow icon import removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swap Sidebar sprint-board prefetch to getGhAllData (D-08, D-08a)</name>
  <files>taskflow/src/components/app/Sidebar.tsx, taskflow/src/components/app/Sidebar.test.tsx</files>
  <read_first>
    - taskflow/src/components/app/Sidebar.tsx (lines 127-170 current `/sprint-board` + `/dashboard` prefetch block using fetchSprintStories; lines 207-214 canonical boardId async-chain pattern used for backlog; top-of-file imports for fetchBoardId)
    - taskflow/src/services/jira.ts (verify `fetchBoardId` and `getGhAllData` are exported from `@/services/jira`)
    - .planning/phases/73-sprint-board-on-alldata-json/73-PATTERNS.md §"Sidebar.tsx (component, prefetch swap)"
    - .planning/phases/73-sprint-board-on-alldata-json/73-CONTEXT.md §"D-08" + §"D-08a"
    - taskflow/src/components/app/Sidebar.test.tsx (existing prefetch test setup)
  </read_first>
  <behavior>
    - On prefetchForPath('/sprint-board') with known activeJiraProject + token + baseUrl: fetchSprintStories is NOT called; fetchBoardId is called; getGhAllData is called once with the resolved boardId
    - On prefetchForPath('/sprint-board') when fetchBoardId resolves null: getGhAllData is NOT called (D-08a — silent skip, no thrown error)
    - prefetchForPath('/dashboard') triggers the same allData prefetch (existing dashboard branch keeps the sprint-board warm)
    - The other three sprint-board prefetches (activeSprint, epicsBasic, projectStatuses) at lines 147-169 STAY (kept per D-09a)
  </behavior>
  <action>
Per D-08 / D-08a: replace the `fetchSprintStories` prefetch block at `Sidebar.tsx:127-146` with the boardId async-chain pattern from `Sidebar.tsx:207-214` (the backlog branch already demonstrates this pattern).

Concretely: inside the `if (path === '/sprint-board' || path === '/dashboard')` branch, replace the `queryClient.prefetchQuery({ queryKey: ['jira-sprint-stories', ...], queryFn: () => fetchSprintStories(...) })` call with `queryClient.fetchQuery({ queryKey: ['jira-board-id', activeJiraProject, jiraBaseUrl], queryFn: () => fetchBoardId(jiraBaseUrl, jiraToken, activeJiraProject), staleTime: Infinity }).then((boardId) => { if (boardId == null) return; return getGhAllData(queryClient, jiraBaseUrl, jiraToken, boardId); }).catch(() => {});`.

The `.catch(() => {})` at the end is required for parity with the existing backlog branch — prefetch is best-effort, should never throw to the user. D-08a is satisfied by the `if (boardId == null) return;` guard.

Keep the conditional inner `if (path === '/sprint-board') { ... }` block at lines 147-169 untouched — those prefetch the other three queries (activeSprint, epicsBasic, projectStatuses) which remain in SprintBoardTab per D-09a / R-02.

Add `getGhAllData` to the top-of-file import from `@/services/jira`. Verify `fetchBoardId` is already imported (used by the backlog branch at line 210); if not, add it. Remove the now-unused `fetchSprintStories` import from `@/services/jira` (in this file only; the fetcher itself stays for other callers per D-09a).

Update `Sidebar.test.tsx`: replace the existing prefetch assertion that mocks `fetchSprintStories` for the sprint-board path with assertions per the `<behavior>` block. Mock `fetchBoardId` and `getGhAllData` directly via `vi.mock('@/services/jira', ...)`. Cover both the success path (boardId resolved) and the silent-skip path (boardId null).

Do NOT modify the backlog branch (lines 207-214). Do NOT change useBoardId or fetchBoardId implementations.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/components/app/Sidebar.test.tsx --reporter=dot &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "getGhAllData(" taskflow/src/components/app/Sidebar.tsx` returns ≥1 match
    - `grep -F "fetchSprintStories" taskflow/src/components/app/Sidebar.tsx` returns 0 matches
    - `grep -F "fetchBoardId" taskflow/src/components/app/Sidebar.tsx` returns ≥1 match (within the sprint-board branch)
    - `grep -E "boardId == null|boardId === null" taskflow/src/components/app/Sidebar.tsx` returns ≥1 match (D-08a guard)
    - Both success-path and null-boardId silent-skip tests green
    - Existing Sidebar tests for other paths (backlog, dashboard non-prefetch) continue to pass
  </acceptance_criteria>
  <done>Sidebar prefetch for /sprint-board warms ['gh-all-data', boardId] via getGhAllData; silent-skip path covered; fetchSprintStories import removed from Sidebar.</done>
</task>

<task type="auto">
  <name>Task 3: Delete fetchSprintSubtasks + full-suite cutover gate (D-09, GH-CUT-01)</name>
  <files>taskflow/src/services/jira.ts, taskflow/src/services/jira/sprint-subtasks.ts</files>
  <read_first>
    - taskflow/src/services/jira/sprint-subtasks.ts (the source module — verify it's the only definition of fetchSprintSubtasks)
    - taskflow/src/services/jira.ts (locate the `fetchSprintSubtasks` re-export line(s))
    - .planning/phases/73-sprint-board-on-alldata-json/73-CONTEXT.md §"D-09" + §"research_resolutions" R-01 (DO NOT delete fetchBoardQuickFilters — quick filters source stays REST)
    - .planning/phases/73-sprint-board-on-alldata-json/73-PATTERNS.md §"jira.ts" Required changes (item 2)
  </read_first>
  <action>
Per D-09 + GH-CUT-01 hard cutover policy: delete `fetchSprintSubtasks` source + re-export now that Plan 02 removed the only caller (SprintBoardTab).

(1) Pre-check via grep that no caller remains anywhere in `taskflow/src/`:
    `grep -rn "fetchSprintSubtasks" taskflow/src/ --include="*.ts" --include="*.tsx" | grep -v sprint-subtasks.ts | grep -v jira.ts`
    Expected: empty. If non-empty, STOP — Plan 02 missed a caller and must be revisited before continuing.

(2) Delete the `fetchSprintSubtasks` function (and any colocated helper symbols only used by it) from `taskflow/src/services/jira/sprint-subtasks.ts`. If the file's entire purpose was `fetchSprintSubtasks`, delete the file outright; otherwise leave only the still-used exports.

(3) In `taskflow/src/services/jira.ts`: delete the `fetchSprintSubtasks` re-export line(s). If the file imported anything else from `sprint-subtasks.ts` solely to re-export, delete those too. Verify there are no other live re-exports from `sprint-subtasks.ts`; if the import itself becomes dead, remove it.

(4) DO NOT touch `fetchBoardQuickFilters` (R-01 — kept), `fetchAllData`, `fetchEpicsBasic`, `fetchActiveSprint`, `fetchProjectStatuses`, or `fetchSprintStories` (D-09a — kept; Task 2's Sidebar prefetch swap is the only swap, the fetcher itself stays for other callers).

(5) Run the full test suite + tsc + biome to confirm no regression. The cutover gate (GH-CUT-01) requires zero references to the deleted symbol across `src/`.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; COUNT=$(grep -rn "fetchSprintSubtasks" src/ --include="*.ts" --include="*.tsx" 2&gt;/dev/null | wc -l); test "$COUNT" -eq 0 &amp;&amp; npx tsc --noEmit &amp;&amp; npx biome check src/ &amp;&amp; npx vitest run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "fetchSprintSubtasks" taskflow/src/ --include="*.ts" --include="*.tsx" | wc -l` returns 0 (symbol fully purged from src/)
    - `taskflow/src/services/jira/sprint-subtasks.ts` either deleted OR contains no `fetchSprintSubtasks` definition
    - `grep -F "fetchBoardQuickFilters" taskflow/src/services/jira.ts` STILL returns ≥1 match (R-01 preservation gate — do not regress)
    - `npx tsc --noEmit` clean
    - `npx biome check src/` clean (0 errors, 0 warnings — memory `[[project_biome_state]]` preserved)
    - Full vitest suite green
  </acceptance_criteria>
  <done>fetchSprintSubtasks fully removed; hard cutover gate (GH-CUT-01) green; biome 0/0 baseline preserved; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click ↔ React Query cache invalidation | "Reload board" button is the only new user-input surface; no privileged action |
| Sidebar prefetch ↔ background network | Best-effort prefetch; failure must not propagate to the user (caught) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-07 | Denial of Service | Reload-board button spam-click | mitigate | Button `disabled` during in-flight refetch (UI-SPEC §2); React Query dedupes simultaneous invalidations |
| T-73-08 | Information Disclosure | Sidebar prefetch failure swallowed | accept | Prefetch is best-effort warm; failures surface on the real navigation via the standard error state — silent skip is the established Sidebar pattern (line 207-214 analog) |
| T-73-09 | Tampering | Deletion of `fetchSprintSubtasks` while a hidden caller exists | mitigate | Task 3 pre-check via grep across `src/` blocks the deletion if any caller remains |
</threat_model>

<verification>
- All three task verify commands green
- `npx tsc --noEmit` clean across taskflow/src
- `npx biome check src/` clean (0 errors, 0 warnings — memory `[[project_biome_state]]` baseline preserved)
- Full `npx vitest run` suite green (post-cutover regression gate per GH-CUT-01)
- Network-log spot-check (manual, end of phase): opening the sprint board issues exactly one `/rest/greenhopper/1.0/xboard/work/allData.json` GET; zero legacy `jira-sprint-stories` / `jira-sprint-subtasks` REST calls
</verification>

<success_criteria>
- GH-BOARD-04 (write side): single "Reload board" toolbar action invalidates all five query keys per D-07 + R-01 + R-02; Sidebar prefetch warms ['gh-all-data', boardId]
- GH-CUT-01: `fetchSprintSubtasks` source + re-export deleted; zero references in `src/`; full suite + tsc + biome green
- Memory `[[project_biome_state]]` 0/0 baseline preserved
- Memory `[[project_jira_ts_dual_file]]` honored — public surface from Plan 01 is the only import route used by SprintBoardTab and Sidebar
</success_criteria>

<output>
Create `.planning/phases/73-sprint-board-on-alldata-json/73-03-SUMMARY.md` when done.
</output>
