---
phase: 91.2-epics-page-redesign
plan: 01
subsystem: api
tags: [jira, epics, fetchers, tanstack-query, vitest]

# Dependency graph
requires: []
provides:
  - "fetchEpicsBasic orders epics by created ASC and returns the priority field on EpicEnriched"
  - "fetchEpicEnrichmentMap returns Map<string, EpicEnrichmentCounts> (total/done/inProgress/todo/points/donePoints)"
  - "fetchEpicEnrichmentMap fails closed — rejects on stories-fetch failure instead of resolving an empty map"
affects: [91.2-02, 91.2-03, 91.2-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Progressive-enrichment fetcher returns a typed aggregate (EpicEnrichmentCounts) instead of an inline shape, exported so the UI can import the type directly"
    - "Fail-closed fetcher: no .catch(() => []) swallow on the primary data path a page renders an error state from"

key-files:
  created: []
  modified:
    - taskflow/src/services/jira.ts
    - taskflow/src/services/jira.test.ts

key-decisions:
  - "Split the two fetcher edits into two atomic commits by temporarily reverting/reapplying the second block, since both tasks touch the same file with no other clean staging boundary"
  - "fetchEpicsWithEnrichment and fetchEpicStories intentionally left with their existing .catch(() => []) swallow — D-15's fail-closed fix scoped to fetchEpicEnrichmentMap only (the fetcher the redesigned page actually calls), per plan's smallest-safe-diff instruction"

patterns-established:
  - "EpicEnrichmentCounts: { total, done, inProgress, todo, points, donePoints } — exported type for progressive enrichment maps keyed by Jira issue key"

requirements-completed: [EPIC-01, EPIC-03, EPIC-04, EPIC-05, EPIC-06]

# Metrics
duration: 8min
completed: 2026-08-12
---

# Phase 91.2 Plan 01: Epic Fetcher Corrections Summary

**Corrected `fetchEpicsBasic` (creation-ascending order + priority field) and `fetchEpicEnrichmentMap` (per-status-category counts, donePoints, fail-closed error propagation) in `src/services/jira.ts`, with 6 new unit tests.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-08-12T23:39:00+02:00
- **Completed:** 2026-08-12T23:42:00+02:00
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments
- `fetchEpicsBasic` now issues `ORDER BY created ASC` (was `ORDER BY updated DESC`), requests the `priority` field, and maps it onto `EpicEnriched.priority` — `statusCategory != Done` left byte-identical (D-03)
- `fetchEpicEnrichmentMap` returns `Map<string, EpicEnrichmentCounts>` with `total`, `done`, `inProgress`, `todo`, `points`, and a new `donePoints` aggregate — and now rejects on a failed stories fetch instead of silently resolving an empty map (D-15 fail-closed correction)
- Added 6 new unit tests covering JQL ordering/priority field, priority mapping (present + null-when-absent), category aggregation + donePoints, unknown/missing statusCategory defaulting to `todo`, error propagation via `rejects.toThrow()`, and the zero-epic-keys no-fetch short circuit

## Task Commits

Each task was committed atomically:

1. **Task 1: fetchEpicsBasic — creation-ascending ordering, priority field, EpicEnriched.priority** - `badc5c23` (feat)
2. **Task 2: fetchEpicEnrichmentMap — category counts, donePoints, fail-closed errors** - `7e310b26` (feat)
3. **Task 3: Service-level unit tests for both fetchers** - `8e86232e` (test)

_Note: Tasks 1 and 2 touch the same file with no other clean git-staging boundary; each was isolated by temporarily reverting the other task's block, verifying, committing, then reapplying — both commits contain only their own task's diff._

## Files Created/Modified
- `taskflow/src/services/jira.ts` - `EpicEnriched.priority` field added; `fetchEpicsBasic` requests `priority` and orders `created ASC`; `EpicEnrichmentCounts` exported type; `fetchEpicEnrichmentMap` aggregates per-status-category counts + `donePoints` and no longer swallows fetch errors
- `taskflow/src/services/jira.test.ts` - New `describe('EPIC-01/EPIC-03: epics list + enrichment fetchers (Phase 91.2)', ...)` block with 6 tests

## Decisions Made
- Scoped the D-15 fail-closed fix to `fetchEpicEnrichmentMap` only, per the plan's explicit instruction — `fetchEpicsWithEnrichment` and `fetchEpicStories` keep their existing `.catch(() => [])` since they are separate callers and widening the blast radius was out of scope for this plan
- No new packages installed (RESEARCH.md's Package Legitimacy Audit confirmed); `npm install` was run in this worktree only to populate `node_modules` (not present at worktree creation), not to add a dependency

## Deviations from Plan

None - plan executed exactly as written. `taskflow/src/services/jira/epics.ts` (the confirmed-dead module) was not touched, verified via `grep -rn "from '@/services/jira/epics'" taskflow/src` returning no matches.

## Issues Encountered
- This worktree had no `node_modules` installed (fresh worktree checkout). Ran `npm install --no-audit --no-fund` inside `taskflow/` before verification could proceed — a one-time environment-setup step, not a plan deviation, and installed nothing beyond the project's existing locked dependency tree (package-lock.json unchanged).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `fetchEpicsBasic` and `fetchEpicEnrichmentMap` now have the exact data contract (`EpicEnriched.priority`, `EpicEnrichmentCounts`) the redesigned `EpicsPage.tsx` row/progress-cell components (plans 91.2-02/03) need — no further service-layer changes required for those plans to consume
- `EpicEnrichmentCounts` is exported and ready to import directly in the UI layer instead of re-declaring an inline shape
- `fetchEpicEnrichmentMap` now throws on failure; consuming `useQuery` calls in 91.2-02/03 must read `isError` to render the D-15 inline muted warning + retry state — do not add a new `.catch` at the call site

---
*Phase: 91.2-epics-page-redesign*
*Completed: 2026-08-12*
