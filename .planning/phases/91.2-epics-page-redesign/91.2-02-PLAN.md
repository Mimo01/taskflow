---
phase: 91.2-epics-page-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/EpicProgressCells.tsx
  - taskflow/src/routes/dashboard/EpicProgressCells.test.tsx
autonomous: true
requirements: [EPIC-03, EPIC-05, EPIC-06]

must_haves:
  truths:
    - "The progress visual is ONE three-segment bar (Done / In Progress / To Do by status category) with `done/total` beside it — not two columns (D-11, EPIC-05)"
    - "Per-status counts are revealed on hover via a native `title` attribute, since the app has no tooltip primitive (D-11)"
    - "While enrichment is pending both cells render skeleton shimmer at the settled column width, so nothing reflows when data lands (D-14, EPIC-03)"
    - "A successful-but-empty enrichment renders muted 'No stories' and no bar, visually distinct from both shimmer and failure (D-16, EPIC-03)"
    - "An enrichment failure renders a muted warning glyph that is a small precisely-bounded button calling stopPropagation, never a full-cell overlay (D-15, EPIC-03)"
    - "Story points render as `done/total SP` (D-13, EPIC-06)"
    - "No blocked/flagged segment, marker or count exists anywhere in this component (D-12)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/EpicProgressCells.tsx"
      provides: "EpicProgressCell + EpicPointsCell with all four enrichment states"
      min_lines: 60
      exports: ["EpicProgressCell", "EpicPointsCell", "EpicProgressCounts", "EnrichmentCellState"]
    - path: "taskflow/src/routes/dashboard/EpicProgressCells.test.tsx"
      provides: "Unit coverage of all four states, segment widths, hover title, and retry hit-testing"
  key_links:
    - from: "taskflow/src/routes/dashboard/EpicProgressCells.tsx"
      to: "taskflow/src/lib/statusStyles.ts"
      via: "statusCategoryDotClass for the three segment colours"
      pattern: "statusCategoryDotClass"
    - from: "EpicProgressCell error state"
      to: "onRetry prop"
      via: "button onClick with e.stopPropagation()"
      pattern: "stopPropagation"
---

<objective>
Build the two enrichment-driven cells of the redesigned epic row as a standalone, fully unit-tested presentational module: the three-segment progress bar with `done/total` (D-11) and the `done/total SP` points cell (D-13), each covering the four enrichment states — pending shimmer (D-14), ready, "No stories" (D-16), and failure with click-to-retry (D-15).

Purpose: isolating these cells lets every visual state be tested directly, without driving React Query through four scenarios, and lets this plan run in parallel with the service-layer plan.
Output: `src/routes/dashboard/EpicProgressCells.tsx` and its colocated test.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91.2-epics-page-redesign/91.2-CONTEXT.md
@.planning/phases/91.2-epics-page-redesign/91.2-UI-SPEC.md
@.planning/phases/91.2-epics-page-redesign/91.2-PATTERNS.md
@.planning/phases/91.2-epics-page-redesign/91.2-VALIDATION.md
</context>

<interface_contract>
This plan OWNS the contract that plan 91.2-03 consumes. Export exactly these names from `EpicProgressCells.tsx`:

- `export interface EpicProgressCounts { total: number; done: number; inProgress: number; todo: number; points: number; donePoints: number }`
  — declared locally, NOT imported from `@/services/jira`, so this plan has no dependency on plan 91.2-01. It is structurally identical to that plan's `EpicEnrichmentCounts`, so the service value assigns without a cast.
- `export type EnrichmentCellState = { kind: 'pending' } | { kind: 'error' } | { kind: 'ready'; counts: EpicProgressCounts }`
- `export function EpicProgressCell(props: { state: EnrichmentCellState; onRetry: () => void }): JSX.Element`
- `export function EpicPointsCell(props: { state: EnrichmentCellState; onRetry: () => void }): JSX.Element`
</interface_contract>

<tasks>

<task type="auto">
  <name>Task 1: EpicProgressCells.tsx — segmented bar, points cell, four states</name>
  <read_first>
    - taskflow/src/lib/statusStyles.ts (the `DOT_STYLES` map and `statusCategoryDotClass` — `'new'` → `bg-gray-400`, `'indeterminate'` → `bg-blue-500`, `'done'` → `bg-green-500`; these are the only sanctioned segment colours)
    - taskflow/src/components/ui/skeleton.tsx (the `Skeleton` primitive signature — a `div` taking `className`)
    - taskflow/src/routes/dashboard/BacklogRow.tsx (lines ~105-135 and ~166-180 — the two precedents for an inner interactive element inside a clickable row using `e.stopPropagation()`, and the `Skeleton className="h-4 w-14 rounded-full"` in-cell shimmer idiom)
    - taskflow/src/components/ui/progress.tsx (confirm for yourself that the base-ui `Progress` is single-fill and cannot express three segments — do not try to force it)
    - .planning/phases/91.2-epics-page-redesign/91.2-UI-SPEC.md (§ Component Contract, § Color, § Copywriting Contract — exact copy strings)
  </read_first>
  <files>taskflow/src/routes/dashboard/EpicProgressCells.tsx</files>
  <action>
Create `taskflow/src/routes/dashboard/EpicProgressCells.tsx` exporting exactly the four names in `<interface_contract>` above. Use `cn` from `@/lib/utils`, `Skeleton` from `@/components/ui/skeleton`, `statusCategoryDotClass` from `@/lib/statusStyles`, and `AlertTriangle` from `lucide-react`. No other imports; do NOT import `Progress` from `@/components/ui/progress` (single-fill only) and do NOT add a tooltip primitive.

`EpicProgressCell` renders by `state.kind`:

- `'pending'` (D-14) — a single `<Skeleton className="h-1.5 w-16 rounded-full" data-testid="epic-progress-pending" />` wrapped in a `flex items-center gap-1.5` container that also holds a `<Skeleton className="h-3 w-8 rounded" />` standing in for the `done/total` label. The two skeletons must occupy the same footprint as the ready state (64px track + gap + count label) so nothing reflows when data arrives.
- `'error'` (D-15) — a `<button type="button">` containing `<AlertTriangle className="w-3.5 h-3.5" />`, with `title="Failed to load — click to retry"` (exact string from UI-SPEC § Copywriting Contract), `aria-label="Retry loading epic progress"`, `data-testid="epic-progress-retry"`, and `className={cn('inline-flex items-center justify-center rounded p-0.5 text-muted-foreground hover:text-red-600 dark:hover:text-red-400 transition-colors')}`. Its `onClick` handler must call `e.stopPropagation()` before `onRetry()`. The button must wrap the glyph only — no `absolute inset-0`, no full-cell overlay, no padding beyond `p-0.5` (91.1 CR-05 precedent: an inner cell swallowed row clicks across ~190px of every row).
- `'ready'` with `counts.total === 0` (D-16) — `<span className="text-xs text-muted-foreground">No stories</span>` and no bar. Copy string is exactly `No stories`.
- `'ready'` with `counts.total > 0` — a `flex items-center gap-1.5` container holding:
  1. a track `<div className="flex h-1.5 w-16 overflow-hidden rounded-full bg-muted" title={...} data-testid="epic-progress-bar">` whose `title` is exactly `` `${done} Done · ${inProgress} In Progress · ${todo} To Do` `` (middle dot U+00B7 with single spaces, matching the CONTEXT.md row sketch) — this is the hover-reveal mechanism (D-11);
  2. inside the track, exactly three sibling `<div>`s in the order done → in progress → to do, each with `className={statusCategoryDotClass('done'|'indeterminate'|'new')}` and `style={{ width: `${(n / total) * 100}%` }}`. Give each a `data-testid` of `epic-segment-done`, `epic-segment-inprogress`, `epic-segment-todo`. Do not add borders, gaps or rounded corners to the individual segments — the track's `overflow-hidden rounded-full` supplies the shape;
  3. a `<span className="text-xs text-muted-foreground tabular-nums whitespace-nowrap">{done}/{total}</span>` label beside the track.

`EpicPointsCell` renders by the same `state.kind`, right-aligned:
- `'pending'` — `<Skeleton className="h-3 w-12 rounded" data-testid="epic-points-pending" />`
- `'error'` — the same small `<button>` shape as above with `title="Failed to load — click to retry"`, `aria-label="Retry loading epic story points"`, `data-testid="epic-points-retry"`, `stopPropagation` then `onRetry()`
- `'ready'` with `counts.total === 0` — `<span className="text-xs text-muted-foreground">—</span>` (em dash; "No stories" belongs to the progress cell only, so the row does not say it twice)
- `'ready'` with `counts.total > 0` — `<span className="text-xs text-muted-foreground tabular-nums whitespace-nowrap">{donePoints}/{points} SP</span>` (D-13)

Hard constraints for both cells:
- Every branch renders on ONE line — no wrapping, no stacked second line at any density (D-07). Use `whitespace-nowrap` on text spans; do not add `py-*`/`px-*` cell padding here, the row owns cell padding.
- No blocked/flagged segment, icon, count or prop anywhere (D-12).
- No new colour literals: segment colours come only from `statusCategoryDotClass`; the failure glyph uses the muted/red tokens named above (UI-SPEC § Color reserves red for this glyph alone).
- File-level JSDoc must state that these cells are the D-14/D-15/D-16 enrichment states of the epic row and that the retry is a whole-query refetch, not a per-epic fetch.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit -p tsconfig.json && npx biome check src/routes/dashboard/EpicProgressCells.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/routes/dashboard/EpicProgressCells.tsx` exists and exports `EpicProgressCell`, `EpicPointsCell`, `EpicProgressCounts`, `EnrichmentCellState`
    - `grep -n "statusCategoryDotClass" taskflow/src/routes/dashboard/EpicProgressCells.tsx` matches at least 3 times (one per segment)
    - `grep -c "stopPropagation" taskflow/src/routes/dashboard/EpicProgressCells.tsx` returns 2 (one per retry button)
    - `grep -in "flag\|blocked" taskflow/src/routes/dashboard/EpicProgressCells.tsx` returns no match (D-12)
    - `grep -n "from '@/components/ui/progress'" taskflow/src/routes/dashboard/EpicProgressCells.tsx` returns no match
    - The exact strings `No stories` and `Failed to load — click to retry` are present
    - `absolute inset-0` does not appear in the file
    - `npx tsc --noEmit` exits 0 and biome reports no new diagnostics for this file
  </acceptance_criteria>
  <done>Both cells exist with all four states, segment colours sourced from `statusCategoryDotClass`, and small precisely-bounded retry buttons.</done>
</task>

<task type="auto">
  <name>Task 2: EpicProgressCells.test.tsx — all four states, segment widths, hover title, retry hit-testing</name>
  <read_first>
    - taskflow/src/routes/dashboard/EpicProgressCells.tsx (the component just written — testids, copy strings, prop shapes)
    - taskflow/src/routes/dashboard/EpicsPage.test.tsx (the repo's RTL conventions: `render` from `@testing-library/react`, `describe`/`it`/`expect` from `vitest`)
    - .planning/phases/91.2-epics-page-redesign/91.2-VALIDATION.md (the behaviours this file must cover: "progress", "no stories", "retry", "points", "breakdown")
  </read_first>
  <files>taskflow/src/routes/dashboard/EpicProgressCells.test.tsx</files>
  <action>
Create `taskflow/src/routes/dashboard/EpicProgressCells.test.tsx` with a `describe('EpicProgressCells (Phase 91.2)')` block. These cells are pure presentational components — render them directly, no `QueryClientProvider` and no service mocks needed. Use `userEvent` or `fireEvent` consistent with whatever the neighbouring dashboard tests already use.

Test names must contain the words the validation map greps for (`progress`, `no stories`, `retry`, `points`, `breakdown`, `click`). Required cases:

1. `'EPIC-03: progress cell renders skeleton shimmer while enrichment is pending'` — `state={{ kind: 'pending' }}`; assert `epic-progress-pending` is in the document and neither `epic-progress-bar` nor the text `No stories` is.
2. `'EPIC-05: progress cell renders three segments sized by category share'` — counts `{ total: 10, done: 5, inProgress: 3, todo: 2, points: 20, donePoints: 8 }`; assert the three segment testids exist and their inline `style.width` values are `50%`, `30%`, `20%` respectively.
3. `'EPIC-05: breakdown title carries per-status counts on hover'` — same counts; assert the `epic-progress-bar` element's `title` attribute equals `5 Done · 3 In Progress · 2 To Do`.
4. `'EPIC-03: progress cell shows done/total beside the bar'` — assert the text `5/10` renders.
5. `'EPIC-03: progress cell shows "no stories" for a successful-but-empty enrichment'` — counts all zero; assert `No stories` renders and `epic-progress-bar` does not.
6. `'EPIC-03: progress cell shows a retry affordance on enrichment failure'` — `state={{ kind: 'error' }}` with a `vi.fn()` `onRetry`; assert `epic-progress-retry` exists with `title="Failed to load — click to retry"`, and that clicking it calls `onRetry` exactly once.
7. `'EPIC-07: retry click does not bubble to the row click handler'` — render the error-state cell inside a `<div onClick={rowSpy}>`; click the retry button; assert `onRetry` was called once and `rowSpy` was NOT called. This is the 91.1 CR-05 regression guard.
8. `'EPIC-06: points cell renders done/total SP'` — counts as in case 2; assert the text `8/20 SP` renders. Add sibling assertions for the pending skeleton (`epic-points-pending`), the zero-story em dash, and the error retry button.
9. `'EPIC-05: no blocked or flagged marker is rendered in any state'` — render each of the four states and assert `queryByText(/flag|blocked/i)` is null (D-12 negative guard).

Do not snapshot-test — assert on testids, text and attributes so the tests stay readable when the visual treatment is tuned.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/EpicProgressCells.test.tsx && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npx vitest run src/routes/dashboard/EpicProgressCells.test.tsx` passes with at least 9 tests
    - The segment-width test asserts exact `50%` / `30%` / `20%` strings, not a range or truthiness
    - The hit-testing test asserts the outer row spy was NOT called
    - `cd taskflow && npm test` full suite green
  </acceptance_criteria>
  <done>All four enrichment states, segment proportions, hover title, points formatting and retry hit-testing are covered by passing unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jira-sourced counts → DOM | Numeric aggregates derived from Jira story data are rendered as text and as a `title` attribute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91.2-04 | Tampering (XSS) | The `title` attribute carrying the per-status breakdown | accept | The `title` value is built only from numbers and hardcoded English category labels ("Done", "In Progress", "To Do") — no Jira-supplied string is interpolated, and React escapes attribute values regardless. No `dangerouslySetInnerHTML` anywhere in this file |
| T-91.2-05 | Denial of Service | The retry button re-fires the whole batched enrichment query | accept | Retry is strictly user-initiated (one click = one refetch), the callback is owned by the parent, and no auto-retry timer is introduced here |
| T-91.2-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero new packages; all imports (`lucide-react`, `Skeleton`, `cn`, `statusStyles`) are already in the repository |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/EpicProgressCells.test.tsx` — green
- `cd taskflow && npx tsc --noEmit -p tsconfig.json` — exits 0
- `cd taskflow && npm test` — full suite green
- `grep -in "flag\|blocked" taskflow/src/routes/dashboard/EpicProgressCells.tsx` — no match (D-12)
</verification>

<success_criteria>
- One three-segment bar with `done/total` beside it, colours from `statusCategoryDotClass` only (EPIC-05, D-11)
- Points render as `done/total SP` (EPIC-06, D-13)
- All four enrichment states render distinctly: shimmer, ready, "No stories", warning+retry (EPIC-03, D-14/D-15/D-16)
- Retry is a small bounded button that stops propagation, proven by a test asserting the row handler does not fire
- No flagged/blocked surface anywhere (D-12)
</success_criteria>

<output>
Create `.planning/phases/91.2-epics-page-redesign/91.2-02-SUMMARY.md` when done
</output>
