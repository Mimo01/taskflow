---
phase: 91.2
slug: epics-page-redesign
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-12
---

# Phase 91.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `91.2-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (jsdom environment) |
| **Config file** | `taskflow/vitest.config.ts` |
| **Quick run command** | `cd taskflow && npx vitest run src/routes/dashboard/EpicsPage.test.tsx` |
| **Full suite command** | `cd taskflow && npm test` |
| **Estimated runtime** | ~3s quick · full suite ~2586 tests |

---

## Sampling Rate

- **After every task commit:** Run `cd taskflow && npx vitest run src/routes/dashboard/EpicsPage.test.tsx`
- **After every plan wave:** Run `cd taskflow && npm test`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~5 seconds (quick run)

---

## Per-Task Verification Map

Task IDs are assigned by the planner; rows below are the required behavior coverage
each plan must map its tasks onto.

| Behavior | Requirement | Test Type | Automated Command | File Exists | Status |
|----------|-------------|-----------|-------------------|-------------|--------|
| Epics render in `created ASC` order; order unchanged after enrichment resolves | EPIC-01 | unit | `npx vitest run src/routes/dashboard/EpicsPage.test.tsx -t "order"` | ❌ W0 | ⬜ pending |
| `fetchEpicsBasic` JQL uses `ORDER BY created ASC` and requests the `priority` field | EPIC-01 / EPIC-04 | unit | `npx vitest run src/services/jira.test.ts -t "epics"` | ❌ W0 | ⬜ pending |
| Progress cell: skeleton while pending | EPIC-03 | unit | `... -t "progress"` | ❌ W0 | ⬜ pending |
| Progress cell: 3-segment bar + `done/total` on success | EPIC-03 / EPIC-05 | unit | `... -t "progress"` | ❌ W0 | ⬜ pending |
| Progress cell: "No stories" on successful-but-empty enrichment | EPIC-03 | unit | `... -t "no stories"` | ❌ W0 | ⬜ pending |
| Progress cell: warning glyph + retry on enrichment error | EPIC-03 | unit | `... -t "retry"` | ❌ W0 | ⬜ pending |
| `fetchEpicEnrichmentMap` propagates errors (no `.catch(() => [])`) | EPIC-03 | unit | `npx vitest run src/services/jira.test.ts -t "enrichment"` | ❌ W0 | ⬜ pending |
| `donePoints` aggregate present; points render as `done/total SP` | EPIC-06 | unit | `... -t "points"` | ❌ W0 | ⬜ pending |
| Priority icon renders with correct `iconUrl`/`title` from `epic.priority` | EPIC-04 | unit | `... -t "priority"` | ❌ W0 | ⬜ pending |
| Child-status breakdown: hover `title` carries To Do / In Progress / Done counts | EPIC-05 | unit | `... -t "breakdown"` | ❌ W0 | ⬜ pending |
| Row click still calls `onEpicClick`; retry glyph click does NOT (stopPropagation) | EPIC-07 | unit | `... -t "click"` | ❌ W0 | ⬜ pending |
| Column order is key → name → status → priority → progress → points → assignee | EPIC-02 | unit | `... -t "columns"` | ❌ W0 | ⬜ pending |
| Dead `src/services/jira/epics.ts` removed; suite still green | EPIC-07 | unit | `cd taskflow && npm test` | ✅ exists | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/routes/dashboard/EpicsPage.test.tsx` — extend `vi.mock('@/services/jira', ...)` with a `fetchEpicEnrichmentMap` stub (currently absent — the fetcher is unused by this page today)
- [ ] `src/routes/dashboard/EpicsPage.test.tsx` — update the 2 existing tests ("renders epic name and status badge", "shows empty state") for the new column order (key before name) and the new priority cell
- [ ] `src/services/jira.test.ts` — add/extend coverage for `fetchEpicsBasic` JQL ordering + `priority` field request, and `fetchEpicEnrichmentMap` error propagation + `donePoints`
- [ ] Framework install: none needed — Vitest already configured

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| No column collapses to zero width at any density | EPIC-02 | jsdom does not reproduce the WebKit/Tauri table-layout collapse | Launch the Tauri app, open `/epics`, cycle compact → default → comfortable density; confirm every column keeps width and each epic stays on exactly one line |
| Epic-colour treatment (D-09) reads as modern and keeps epic identity legible | EPIC-02 | Subjective visual judgement | Visual review at all 3 densities alongside Backlog epic chips |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags (`vitest run`, never `vitest`)
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
