---
phase: 91.2-epics-page-redesign
plan: 03
subsystem: ui
tags: [react, epics, tanstack-query, tailwind, vitest, react-testing-library]

# Dependency graph
requires: [91.2-01, 91.2-02]
provides:
  - "EpicsPage.tsx: rebuilt div+flex EpicRow with 7 columns (key, name, status, priority, progress, points, assignee) and the jira-epics-enrichment progressive-enrichment query"
  - "EpicsSkeleton.tsx: column-width-matched skeleton mirroring the settled row"
affects: [91.2-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Second useQuery keyed off the first query's derived key list (jira-epics-enrichment), merged by key inside epics.map(), never reordering or gating first paint"
    - "Per-row cell-state derivation function checking isError before data (91.1 CR-06 guard)"

key-files:
  created:
    - taskflow/src/routes/dashboard/EpicsSkeleton.test.tsx
  modified:
    - taskflow/src/routes/dashboard/EpicsPage.tsx
    - taskflow/src/routes/dashboard/EpicsSkeleton.tsx
    - taskflow/src/routes/dashboard/EpicsPage.test.tsx

key-decisions:
  - "Split the div+flex row rebuild (task 1), the enrichment query wiring (task 2), and the skeleton/test expansion (task 3) into three atomic commits per the plan's task boundaries"
  - "Added biome-ignore comments for lint/a11y/noStaticElementInteractions and lint/a11y/useKeyWithClickEvents on the row div — the plan explicitly forbids adding new a11y semantics (role=button/tabIndex) since the previous <tr onClick> had none either; biome flags div-based click handlers but not tr-based ones, so this is a lint-tool artifact, not a behaviour change"

patterns-established:
  - "EpicRow's div+flex cell idiom (CELL_PADDING constant + per-cell flex-none w-<n> / flex-1 min-w-0) is now the second app instance of the WebKit zero-width-column fix, alongside BacklogRow"

requirements-completed: [EPIC-01, EPIC-02, EPIC-03, EPIC-04, EPIC-05, EPIC-06, EPIC-07]

# Metrics
duration: 22min
completed: 2026-08-12
---

# Phase 91.2 Plan 03: EpicsPage Rebuild Summary

**Rebuilt `/epics` as a div+flex single-line row (key · name · status · priority · progress · points · assignee) with progress/points streaming in from a second `jira-epics-enrichment` query behind first paint, and a column-width-matched skeleton — 11 EpicsPage tests + 1 EpicsSkeleton test, all green.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-08-12T23:50:00Z
- **Completed:** 2026-08-12T00:12:00Z
- **Tasks:** 3 completed
- **Files modified:** 4 (1 new)

## Accomplishments
- `EpicRow` is now a div+flex row with 7 columns in the D-06 order, one `flex-1 min-w-0` name cell and explicit widths on every fixed cell — the `<table>`/`<colgroup>`/`<td>` markup and the 1px colour-bar cell are gone (D-09 single-carrier decision: the tinted name badge is now the sole colour carrier, `font-medium` → `font-normal`)
- Priority renders as an 18×18 `PriorityIcon` only, no visible label (D-10)
- Second `useQuery` (`jira-epics-enrichment`) derives its key list from the basic query's array only, is `enabled` only once keys exist, and never gates `showSkeleton`/`ErrorState`/`StaleDataBanner` — those stay driven exclusively by the basic query (D-04, D-14, EPIC-07)
- Per-row cell state checks `enrichmentIsError` before `enrichmentMap` (91.1 CR-06 guard), maps an absent epic key to zero-story "No stories" (D-16), and retry re-fires the whole batched query (documented in a code comment so it isn't "fixed" into a per-epic fetch later)
- `EpicsSkeleton` reshaped to 5 rows × 7 column-width-matched skeleton blocks mirroring `EpicRow` exactly, so nothing reflows when real data lands
- 11 `EpicsPage.test.tsx` tests cover ordering stability before/after enrichment, column order (key precedes name in DOM), priority icon rendering, all four enrichment states (pending/ready/no-stories/error), points, retry hit-testing (row click still fires, retry click does not), and the absence of any search input
- Stale top-of-file JSDoc ("deferred to EpicDetailSheet — no expensive bulk story query here") replaced with a description of the two-query progressive-enrichment shape

## Task Commits

Each task was committed atomically:

1. **Task 1: Rebuild EpicRow as a div+flex single-line row with the seven redesigned columns** - `cabcd90f` (feat)
2. **Task 2: Wire the progressive enrichment query and its four cell states** - `93f0e3d7` (feat)
3. **Task 3: Reshape EpicsSkeleton to the settled column widths and expand the page tests** - `e54c0c43` (test)

## Files Created/Modified
- `taskflow/src/routes/dashboard/EpicsPage.tsx` - `EpicRow` rebuilt as div+flex, 7 columns, `jira-epics-enrichment` query added, cell-state derivation, stale JSDoc replaced
- `taskflow/src/routes/dashboard/EpicsSkeleton.tsx` - reshaped from 5 generic bars to 5 rows × 7 column-width-matched skeleton cells
- `taskflow/src/routes/dashboard/EpicsSkeleton.test.tsx` (new) - render smoke test asserting 5 rows × 7 skeleton columns
- `taskflow/src/routes/dashboard/EpicsPage.test.tsx` - extended `vi.mock('@/services/jira', ...)` with `fetchEpicEnrichmentMap`, rewrote to 11 tests covering ordering, column order, priority, all four enrichment states, points, retry/click hit-testing, and the D-18 no-search negative guard

## Decisions Made
- Task boundaries matched the plan's three tasks exactly — no additional atomic splits needed
- Added biome-ignore comments on the row's click handler (see key-decisions above) rather than adding `role="button"`/`tabIndex` semantics that the plan explicitly said not to add, since the prior `<tr onClick>` had neither and biome's a11y rules only fire on `div`, not `tr`

## Deviations from Plan

None — plan executed exactly as written. The two a11y biome-ignore comments are a lint-tool compliance addition (Rule 3 — blocking issue: `npx biome check` failed without them), not a behavioural deviation; the plan itself explicitly instructed not to add new a11y semantics, and the ignore comments preserve that instruction while satisfying the lint gate in `<verification>`.

## Issues Encountered
- This worktree had no `node_modules`. Symlinked `taskflow/node_modules` to the main repo's `taskflow/node_modules` (gitignored, not committed), matching the approach used in plans 91.2-01/02.
- `npx biome check --write` auto-sorted the import block in `EpicsPage.tsx` (alphabetized the `@/services/jira` type-only import relative to the value import) — a formatting-only change, re-verified after with `npx tsc --noEmit` and the full test suite.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `/epics` now matches the full D-01…D-18 contract for this phase; no outstanding column, ordering, or enrichment-state gaps
- Plan 91.2-04 (if any polish/verification-only work remains) can build on this row shape and query wiring directly — no further structural changes anticipated

---
*Phase: 91.2-epics-page-redesign*
*Completed: 2026-08-12*
