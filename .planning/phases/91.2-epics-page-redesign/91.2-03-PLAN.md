---
phase: 91.2-epics-page-redesign
plan: 03
type: execute
wave: 2
depends_on: ["91.2-01", "91.2-02"]
files_modified:
  - taskflow/src/routes/dashboard/EpicsPage.tsx
  - taskflow/src/routes/dashboard/EpicsPage.test.tsx
  - taskflow/src/routes/dashboard/EpicsSkeleton.tsx
  - taskflow/src/routes/dashboard/EpicsSkeleton.test.tsx
autonomous: true
requirements: [EPIC-01, EPIC-02, EPIC-03, EPIC-04, EPIC-05, EPIC-06, EPIC-07]

must_haves:
  truths:
    - "Each epic renders as a single div+flex row with columns in order key → name → status → priority → progress → points → assignee (D-05, D-06, EPIC-02)"
    - "Every fixed cell carries an explicit width and exactly one cell is `flex-1 min-w-0`, so no column can collapse to zero width in WebKit (EPIC-02)"
    - "One epic occupies exactly one line at compact, default and comfortable density — density changes padding/font only (D-07, EPIC-02)"
    - "Priority renders as a `PriorityIcon` only, with the priority name in title/alt; no labels or components appear on the row (D-08, D-10, EPIC-04)"
    - "Rows render fully from `fetchEpicsBasic` at first paint while progress and points shimmer; enrichment merges by key and never reorders the list (D-04, D-14, D-17, EPIC-01, EPIC-03)"
    - "An enrichment failure shows the per-cell warning glyph and clicking it refetches the whole enrichment query — it never gates the page (D-15, EPIC-03)"
    - "Row click still calls onEpicClick to open EpicDetailSheet, and the skeleton / ErrorState / StaleDataBanner / '+ Create Epic' behaviours are unchanged (EPIC-07)"
    - "No search input, `/` binding, filter predicate, result count or no-match empty state is added (D-18, EPIC-07)"
    - "The skeleton mirrors the settled row's column widths so nothing reflows when data lands (D-14)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/EpicsPage.tsx"
      provides: "div+flex EpicRow with 7 columns and the second progressive-enrichment useQuery"
      contains: "jira-epics-enrichment"
    - path: "taskflow/src/routes/dashboard/EpicsSkeleton.tsx"
      provides: "Column-width-matched skeleton rows"
    - path: "taskflow/src/routes/dashboard/EpicsPage.test.tsx"
      provides: "Coverage for column order, ordering stability, priority, the four enrichment states, points and row-click hit-testing"
  key_links:
    - from: "taskflow/src/routes/dashboard/EpicsPage.tsx"
      to: "fetchEpicEnrichmentMap"
      via: "second useQuery keyed on the loaded epic keys, enabled only once keys exist"
      pattern: "jira-epics-enrichment"
    - from: "taskflow/src/routes/dashboard/EpicsPage.tsx EpicRow"
      to: "EpicProgressCell / EpicPointsCell"
      via: "enrichment cell state props"
      pattern: "EpicProgressCell"
    - from: "taskflow/src/routes/dashboard/EpicsPage.tsx EpicRow"
      to: "onEpicClick"
      via: "row-level onClick handler unchanged"
      pattern: "onEpicClick"
---

<objective>
Rebuild `/epics` as the redesigned portfolio surface: a single-line div+flex row carrying key · name · status · priority · progress · points · assignee (D-05/D-06/D-07), with progress and points streaming in behind first paint from a second enrichment query (D-04/D-14/D-15/D-16), and a skeleton that matches the settled column widths.

Purpose: this is the user-visible payload of the phase. Everything the previous two plans built is data and cells; this plan is the page.
Output: rewritten `EpicRow` + enrichment wiring in `EpicsPage.tsx`, reshaped `EpicsSkeleton.tsx`, and expanded tests.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91.2-epics-page-redesign/91.2-CONTEXT.md
@.planning/phases/91.2-epics-page-redesign/91.2-UI-SPEC.md
@.planning/phases/91.2-epics-page-redesign/91.2-PATTERNS.md
@.planning/phases/91.2-epics-page-redesign/91.2-VALIDATION.md
@.planning/phases/91.2-epics-page-redesign/91.2-01-SUMMARY.md
@.planning/phases/91.2-epics-page-redesign/91.2-02-SUMMARY.md
</context>

<inherited_interfaces>
From plan 91.2-01 (`taskflow/src/services/jira.ts`):
- `EpicEnriched` now carries `priority?: { name?: string; iconUrl?: string } | null`
- `fetchEpicsBasic(baseUrl, token, projectKey, epicNameFieldKey?, epicColorFieldKey?)` → `Promise<EpicEnriched[]>`, ordered `created ASC`
- `fetchEpicEnrichmentMap(baseUrl, token, epicKeys, storyPointsFieldKey?, epicLinkFieldKey?)` → `Promise<Map<string, EpicEnrichmentCounts>>`, REJECTS on fetch failure
- `EpicEnrichmentCounts = { total, done, inProgress, todo, points, donePoints }`

From plan 91.2-02 (`taskflow/src/routes/dashboard/EpicProgressCells.tsx`):
- `EnrichmentCellState = { kind: 'pending' } | { kind: 'error' } | { kind: 'ready'; counts: EpicProgressCounts }`
- `EpicProgressCell({ state, onRetry })`, `EpicPointsCell({ state, onRetry })`
`EpicProgressCounts` is structurally identical to `EpicEnrichmentCounts`, so map values assign without a cast.
</inherited_interfaces>

<design_decisions>
**D-09 epic-colour treatment — decided here, do not re-litigate.** UI-SPEC requires ONE primary colour carrier so the row stays calm next to the segmented bar. The decision: **keep the tinted epic-name badge as the sole colour carrier and DELETE the 1px left colour bar cell.** The badge keeps `epicColorToTailwind`'s className/style but drops `font-medium` in favour of `font-normal` (UI-SPEC § Typography: exactly two weights on this page). Epic identity stays legible and stays consistent with the Backlog epic chips, which use the same tinted-badge vocabulary.

**Column widths — fixed, explicit, WebKit-safe.** Left to right, on the row's direct children:
| Cell | Classes |
|------|---------|
| key | `flex-none w-24 pl-4 pr-2` |
| name | `flex-1 min-w-0 px-2 overflow-hidden` (the ONLY flex-1 cell) |
| status | `flex-none w-28 px-2` |
| priority | `flex-none w-8 px-2` |
| progress | `flex-none w-32 px-2` |
| points | `flex-none w-20 px-2 text-right` |
| assignee | `flex-none w-10 pl-2 pr-4` |
Every cell additionally carries the density padding triple `py-2 density-compact:py-1 density-comfortable:py-3` and `whitespace-nowrap` (except the name cell, which uses `truncate` on its inner span instead).
</design_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild EpicRow as a div+flex single-line row with the seven redesigned columns</name>
  <read_first>
    - taskflow/src/routes/dashboard/EpicsPage.tsx (the whole 224-line file — current `EpicRow`, the `<table>`/`<colgroup>` container at lines ~193-207, the page header, and the error/stale/empty wiring you must NOT change)
    - taskflow/src/routes/dashboard/BacklogRow.tsx (lines ~85-215, the `RowCells` function — the exact div+flex cell idiom, the 18×18 fixed icon wrapper for `PriorityIcon`, the density padding triple, and the tinted epic badge with `truncate` inside)
    - taskflow/src/lib/statusStyles.ts (the `statusPillClass` JSDoc — callers must NOT add geometry classes, and the pill needs a flex parent)
    - taskflow/src/lib/epicColors.ts (the `epicColorToTailwind` return shape `{ className, style }`)
    - taskflow/src/routes/dashboard/EpicProgressCells.tsx (from plan 91.2-02 — the props the two cells take)
    - .planning/phases/91.2-epics-page-redesign/91.2-UI-SPEC.md (§ Component Contract, § Typography, § Spacing)
  </read_first>
  <files>taskflow/src/routes/dashboard/EpicsPage.tsx</files>
  <action>
Replace `EpicRow` and its table container in `taskflow/src/routes/dashboard/EpicsPage.tsx`.

**Container.** Delete the `<table className="w-full text-sm">`, the entire `<colgroup>` (the `w-1`/`w-28`/`w-16` cols are the documented WebKit zero-width-collapse risk) and the `<tbody>`. Replace with `<div className="w-full text-sm">` wrapping the same `epics.map((epic) => <EpicRow ... />)` call. Keep `key={epic.key}` and keep rendering from the basic query's array in its returned order — never `.sort()` it (D-17, EPIC-01).

**Row wrapper.** `EpicRow` returns a `<div>` (not `<tr>`) with `className="flex w-full items-center border-b border-border hover:bg-muted/30 transition-colors cursor-pointer"`, `onClick={() => onEpicClick?.(epic.key)}`, plus `role="button"` and `tabIndex={0}` only if the current row already exposes them (it does not — do not add new a11y semantics that change existing behaviour; keep the click handler shape identical to today so EPIC-07 holds).

**Cells**, in this order (D-05, D-06), using the width/padding table in `<design_decisions>` above:
1. **Key** — `<span className="font-mono text-xs text-muted-foreground">{epic.key}</span>`. Plain span, not a button: unlike BacklogRow, the epic key has no separate navigation target here, and adding an inner button would create the 91.1 CR-05 click-swallowing risk for no benefit.
2. **Name** — the tinted badge, now the sole colour carrier (see `<design_decisions>`): `<span className={cn('inline-flex min-w-0 max-w-full items-center rounded border px-1.5 py-0.5 text-xs font-normal', colorResult.className)} style={colorResult.style} title={epic.epicName}>` with an inner `<span className="truncate">{epic.epicName}</span>`. Note `font-normal`, replacing today's `font-medium`.
3. **Status** — keep the existing `<div className="flex"><span className={statusPillClass(epic.status.statusCategory?.key)}>{epic.status.name}</span></div>` inner wrapper verbatim. The `flex` wrapper is mandatory: without it the pill's `min-w-[5.5rem]`/`text-center` silently collapse.
4. **Priority** (D-10, EPIC-04) — a fixed-size wrapper `<span className="flex items-center justify-center" style={{ width: 18, height: 18 }} aria-hidden={!epic.priority}>` containing `<PriorityIcon priority={epic.priority} />`. Icon only; no visible priority label text. `PriorityIcon` already returns null for a missing `iconUrl` and puts the name in `alt`/`title`.
5. **Progress** — `<EpicProgressCell state={enrichment} onRetry={onRetryEnrichment} />`.
6. **Points** — `<EpicPointsCell state={enrichment} onRetry={onRetryEnrichment} />`.
7. **Assignee** — the existing `CachedAvatar` block unchanged (`url={epic.assignee?.avatarUrls?.['48x48'] || null}`, `name={epic.assignee?.displayName || 'Unassigned'}`, `size={24}`).

**Deletions.** Remove the 1px colour-bar `<td>` and its `min-h-[3rem] density-compact:min-h-[2rem] density-comfortable:min-h-[3.5rem]` div entirely (D-09 single-carrier decision). Do NOT render labels, components, or any flag/blocked marker (D-08, D-12).

**Props.** Extend `EpicRowProps` to `{ epic: EpicEnriched; onEpicClick?: (key: string) => void; enrichment: EnrichmentCellState; onRetryEnrichment: () => void }`. For THIS task only, the page passes the literal `{ kind: 'pending' }` and a no-op `() => {}` — task 2 replaces both with the real query. Import `EnrichmentCellState`, `EpicProgressCell`, `EpicPointsCell` from `./EpicProgressCells`, `PriorityIcon` from `@/components/ui/priority-icon`, and `cn` from `@/lib/utils`.

**Do not touch** (EPIC-07, D-18): the page header markup and its "Epics" title / "+ Create Epic" button, `CreateEpicDialog`, `EmptyState`, `ErrorState`, `StaleDataBanner`, `useDelayedLoading`, the `['jira-epics-basic', ...]` query (its key is shared with `BacklogPage.tsx` and `SprintBoardTab`), or the `isRefreshing`/`bannerDismissed` state. Add no search input, no `/` keybinding, no filter predicate, no result count.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/EpicsPage.test.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<table\|<colgroup\|<tbody\|<td " taskflow/src/routes/dashboard/EpicsPage.tsx` returns 0
    - `grep -c "flex-1 min-w-0" taskflow/src/routes/dashboard/EpicsPage.tsx` returns 1 (exactly one growing cell)
    - Every non-name cell class string in `EpicRow` contains `flex-none` and an explicit `w-` value
    - `grep -n "PriorityIcon" taskflow/src/routes/dashboard/EpicsPage.tsx` matches, inside an 18×18 fixed wrapper span
    - `grep -n "statusPillClass" taskflow/src/routes/dashboard/EpicsPage.tsx` is still wrapped in a `<div className="flex">`
    - `grep -in "search\|labels\|components\|flagged" taskflow/src/routes/dashboard/EpicsPage.tsx` shows no search input, label rendering or flag marker (D-08, D-12, D-18)
    - `font-medium` no longer appears on the epic-name badge; `font-normal` does
    - `cd taskflow && npx tsc --noEmit` exits 0 and the existing two EpicsPage tests still pass (updated in task 3 if their assertions need column-order changes)
  </acceptance_criteria>
  <done>`EpicRow` is a div+flex single-line row with all seven columns in the D-06 order, explicit widths on every fixed cell, one `flex-1 min-w-0` name cell, and no table markup left.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the progressive enrichment query and its four cell states</name>
  <read_first>
    - taskflow/src/routes/dashboard/EpicsPage.tsx (as rewritten in task 1 — the existing `['jira-epics-basic', ...]` useQuery, the `token` state from `readSecret`, and the `useSettingsStore` destructuring)
    - taskflow/src/routes/dashboard/BacklogPage.tsx (lines ~505-525 and ~625-645 — the basic-query shape and the merge-by-key discipline that never reorders the base array)
    - taskflow/src/services/jira.ts (the `fetchEpicEnrichmentMap` signature and `EpicEnrichmentCounts` from plan 91.2-01)
    - taskflow/src/stores/settings.store.ts (confirm the exact names `storyPointsFieldKey` and `epicLinkFieldKey`)
    - .planning/phases/91.2-epics-page-redesign/91.2-CONTEXT.md (D-04, D-14, D-15, D-16)
  </read_first>
  <files>taskflow/src/routes/dashboard/EpicsPage.tsx</files>
  <action>
Add the second query to `EpicsPage` and replace task 1's placeholder props.

1. Destructure `storyPointsFieldKey` and `epicLinkFieldKey` from `useSettingsStore()` alongside the existing `epicNameFieldKey`/`epicColorFieldKey`.

2. Derive the key list from the basic query's data only: `const epicKeys = useMemo(() => epics.map((e) => e.key), [epics]);` — do not sort, filter or re-derive it from anything else.

3. Add the enrichment query below the existing one:
   - `queryKey: ['jira-epics-enrichment', activeJiraProject, jiraBaseUrl, epicKeys.join(',')]` — a NEW key, never reusing `['jira-epics-basic', ...]` (that key is shared with `BacklogPage` and `SprintBoardTab`)
   - `queryFn: () => fetchEpicEnrichmentMap(jiraBaseUrl ?? '', token ?? '', epicKeys, storyPointsFieldKey ?? undefined, epicLinkFieldKey ?? undefined)`
   - `enabled: !!jiraBaseUrl && !!token && epicKeys.length > 0`
   - destructure `data: enrichmentMap`, `isError: enrichmentIsError`, `refetch: refetchEnrichment`
   This query must NOT gate rendering: no `isLoading` of it may reach `showSkeleton`, `ErrorState` or `StaleDataBanner` (D-04, D-14). Those three remain driven exclusively by the basic query (EPIC-07).

4. Derive each row's cell state, in this precedence order:
   - `enrichmentIsError` → `{ kind: 'error' }` (D-15) — check error BEFORE data, so a stale-but-present map never masks a live failure (the 91.1 CR-06 unread-`isError` bug class)
   - else `enrichmentMap` is defined → `{ kind: 'ready', counts: enrichmentMap.get(epic.key) ?? { total: 0, done: 0, inProgress: 0, todo: 0, points: 0, donePoints: 0 } }` — an epic absent from the map genuinely has zero children, which renders "No stories" (D-16)
   - else → `{ kind: 'pending' }` (D-14)
   Compute this per row inside the `epics.map(...)` callback (or in a small helper taking the epic key) and pass it as the `enrichment` prop; pass `onRetryEnrichment={() => refetchEnrichment()}`.

5. Retry semantics are WHOLE-QUERY (locked at plan time): `fetchEpicEnrichmentMap` is a single batched JQL for all epic keys, so one click refetches enrichment for every row. Do NOT build a single-epic enrichment fetcher. Say so in a code comment next to `refetchEnrichment` so a future reader does not "fix" it.

6. Update the file's top JSDoc: the current text claiming story counts and progress are "deferred to EpicDetailSheet — no expensive bulk story query here" is now false. Replace it with a description of the two-query progressive-enrichment shape, the fixed `created ASC` ordering, and the fact that enrichment merges by key and never reorders rows.

Order stability is a hard constraint (D-17, EPIC-01): rows are rendered from the basic query's array and merged by key only. No `.sort()`, no `Array.from(enrichmentMap)` driving iteration, no reordering of any kind after enrichment lands.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/EpicsPage.test.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "jira-epics-enrichment" taskflow/src/routes/dashboard/EpicsPage.tsx` matches exactly one `queryKey`
    - `grep -c "\.sort(" taskflow/src/routes/dashboard/EpicsPage.tsx` returns 0
    - The enrichment query's `isError`/`isLoading` are not referenced by `showSkeleton`, `ErrorState` or `StaleDataBanner`
    - The cell-state derivation checks `enrichmentIsError` before `enrichmentMap`
    - `grep -n "deferred to EpicDetailSheet" taskflow/src/routes/dashboard/EpicsPage.tsx` returns no match (stale doc comment replaced)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Progress and points stream in behind first paint from a separate query, all four cell states are reachable, retry refetches the batched query, and row order comes only from the basic query.</done>
</task>

<task type="auto">
  <name>Task 3: Reshape EpicsSkeleton to the settled column widths and expand the page tests</name>
  <read_first>
    - taskflow/src/routes/dashboard/EpicsSkeleton.tsx (the current 11-line generic `h-10 w-full` shape)
    - taskflow/src/routes/dashboard/EpicsPage.tsx (as rewritten in tasks 1-2 — copy the exact cell widths so the skeleton cannot reflow)
    - taskflow/src/routes/dashboard/EpicsPage.test.tsx (the existing 2 tests, the `vi.mock('@/services/jira', ...)` block at lines 19-21, and the `QueryClientProvider` + `MemoryRouter` render harness to reuse)
    - .planning/phases/91.2-epics-page-redesign/91.2-VALIDATION.md (the per-task verification map — test names must contain `order`, `columns`, `priority`, `progress`, `no stories`, `retry`, `points`, `breakdown`, `click`)
  </read_first>
  <files>taskflow/src/routes/dashboard/EpicsSkeleton.tsx, taskflow/src/routes/dashboard/EpicsSkeleton.test.tsx, taskflow/src/routes/dashboard/EpicsPage.test.tsx</files>
  <action>
**A. `EpicsSkeleton.tsx`** — replace the five generic `<Skeleton className="h-10 w-full" />` bars with five `div className="flex w-full items-center border-b border-border"` rows whose children mirror the settled `EpicRow` cell widths exactly (`flex-none w-24 pl-4 pr-2`, `flex-1 min-w-0 px-2`, `flex-none w-28 px-2`, `flex-none w-8 px-2`, `flex-none w-32 px-2`, `flex-none w-20 px-2`, `flex-none w-10 pl-2 pr-4`), each carrying the same `py-2 density-compact:py-1 density-comfortable:py-3` padding and containing a single `<Skeleton>` sized to that cell's content (e.g. `h-3 w-16` for the key, `h-4 w-40` for the name badge, `h-4 w-20` for the status pill, `h-3.5 w-3.5` for priority, `h-1.5 w-16 rounded-full` for the bar, `h-3 w-12` for points, `size-6 rounded-full` for the avatar). Drop the outer `p-4 flex flex-col gap-2` wrapper's gap so skeleton rows sit flush like real rows. Keep the named export `EpicsSkeleton` and its zero-prop signature — `EpicsPage` renders it unchanged.

**B. `EpicsSkeleton.test.tsx`** (new) — a render smoke test asserting the component renders 5 rows and that the number of `[data-slot="skeleton"]` elements per row is 7 (one per column). Keep it lightweight; this file exists to catch a column being dropped from the skeleton, not to pin pixel values.

**C. `EpicsPage.test.tsx`** — extend `vi.mock('@/services/jira', ...)` to also stub `fetchEpicEnrichmentMap: vi.fn()`, and add `storyPointsFieldKey`/`epicLinkFieldKey` to the `useSettingsStore` mock if they are not already present (they are). Update the two existing tests for the new column order and add the new cases below. Every epic fixture needs the new `priority` field; keep `retry: false` on the test QueryClient so the error case resolves fast.

Required new/updated tests (names must contain the grep words):
1. `'EPIC-01: renders epics in the order returned by fetchEpicsBasic and the order is unchanged after enrichment resolves'` — three epics returned in a deliberate non-alphabetical order; assert the rendered key texts appear in DOM order matching the fixture order both before and after the enrichment promise resolves (query all key cells and compare the array).
2. `'EPIC-02: renders row columns in order key, name, status, priority, progress, points, assignee'` — assert the epic key element precedes the epic name element in DOM order (`compareDocumentPosition` or index within the row's children), and that all seven cells exist. This is the D-05 key-before-name regression guard, replacing today's name-first assertion.
3. `'EPIC-04: renders the priority icon with the priority name'` — fixture priority `{ name: 'Must', iconUrl: 'https://jira.example.com/icons/must.svg' }`; assert an `img` with `alt="Priority: Must"` and that `src` matches the fixture `iconUrl`.
4. `'EPIC-03: progress and points cells shimmer while the enrichment query is in flight'` — leave the `fetchEpicEnrichmentMap` mock pending; assert the pending testids from `EpicProgressCells` are present while the epic key and status are already rendered (proves first paint is not gated, D-04/D-14).
5. `'EPIC-03/EPIC-05: renders the segmented breakdown and done/total once enrichment resolves'` — resolve a Map with counts for the epic; assert the `done/total` text and the bar's hover `title` with per-status counts.
6. `'EPIC-06: renders story points as done/total SP'` — assert the `SP` text from the same resolved fixture.
7. `'EPIC-03: renders "no stories" for an epic missing from the enrichment map'` — resolve an empty Map; assert `No stories` renders (D-16).
8. `'EPIC-03: renders a retry affordance when the enrichment query fails'` — reject the `fetchEpicEnrichmentMap` mock; assert the retry button renders and that clicking it calls `fetchEpicEnrichmentMap` again (assert on the mock's call count, which proves the whole-query refetch wiring).
9. `'EPIC-07: row click still calls onEpicClick and the retry click does not'` — render with an `onEpicClick` spy supplied through the outlet context the page already reads; click the row body → spy called with the epic key; click the retry glyph → spy call count unchanged.
10. `'EPIC-07: renders no search input on the page'` — assert `queryByRole('textbox')` and `queryByPlaceholderText(/search/i)` are both null (D-18 negative guard), and that the "+ Create Epic" button is still present.

For test 9, supply `onEpicClick` the same way the page consumes it — via `useOutletContext`; if the existing harness renders `EpicsPage` as a bare route element, wrap it in a parent `Route` with an `Outlet context={{ onEpicClick }}` rather than mocking `react-router-dom`.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/EpicsPage.test.tsx src/routes/dashboard/EpicsSkeleton.test.tsx && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npx vitest run src/routes/dashboard/EpicsPage.test.tsx` passes with at least 10 tests, including the two updated originals
    - `cd taskflow && npx vitest run src/routes/dashboard/EpicsPage.test.tsx -t "order"` and `-t "columns"` and `-t "priority"` and `-t "progress"` and `-t "no stories"` and `-t "retry"` and `-t "points"` and `-t "breakdown"` and `-t "click"` each match at least one test
    - `grep -n "fetchEpicEnrichmentMap" taskflow/src/routes/dashboard/EpicsPage.test.tsx` shows it in the `vi.mock` factory
    - The ordering test compares an ARRAY of rendered keys against the fixture order, not a single `getByText`
    - `EpicsSkeleton.tsx` contains the same seven cell width classes as `EpicRow`
    - `cd taskflow && npm test` full suite green
  </acceptance_criteria>
  <done>The skeleton mirrors the settled column widths, and the page has automated coverage for ordering stability, column order, priority, all four enrichment states, points, row-click hit-testing and the absence of search.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jira-sourced epic/story data → rendered DOM | Epic names, keys, status names, priority `iconUrl` and assignee avatar URLs come from Jira and are rendered client-side |
| User click → Jira read request | The retry affordance triggers a refetch of the enrichment query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91.2-06 | Tampering (XSS) | `PriorityIcon` rendering a Jira-supplied `iconUrl` as an `<img src>` | accept | Pre-existing component used identically in `BacklogRow`; React escapes the attribute, the URL is same-origin-to-Jira and rendered as an image source only — no HTML injection path. No `dangerouslySetInnerHTML` is introduced on this page |
| T-91.2-07 | Information Disclosure | Enrichment failure surfacing raw error detail in the row | mitigate | The D-15 failure state renders only a fixed glyph and the fixed copy `Failed to load — click to retry`; the raw error object is never rendered into the row (page-level `ErrorState` keeps its existing, unchanged behaviour for the basic query) |
| T-91.2-08 | Denial of Service | The enrichment query re-firing on every epic-key change or retry click | mitigate | The query key is derived from the stable `created ASC` key list so it changes only when the epic set changes; `enabled` gates it on a non-empty key list; retry is user-initiated only, with no timer or auto-retry loop added |
| T-91.2-09 | Elevation of Privilege | New interactive element (retry button) inside a clickable row | mitigate | The retry button calls `e.stopPropagation()` and is bounded to the glyph; test 9 asserts the row's `onEpicClick` does not fire from it, closing the 91.1 CR-05 hit-testing regression |
| T-91.2-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero new packages in this phase (RESEARCH.md § Package Legitimacy Audit); no install task exists in this plan |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/EpicsPage.test.tsx src/routes/dashboard/EpicsSkeleton.test.tsx` — green
- `cd taskflow && npx tsc --noEmit -p tsconfig.json` — exits 0
- `cd taskflow && npm test` — full suite green
- `cd taskflow && npx biome check src/routes/dashboard/EpicsPage.tsx src/routes/dashboard/EpicsSkeleton.tsx` — no new files flagged versus the pre-task baseline
- `grep -c "<table\|<colgroup" taskflow/src/routes/dashboard/EpicsPage.tsx` — returns 0
</verification>

<success_criteria>
- Seven columns in the D-06 order, one line per epic, explicit widths on every fixed cell (EPIC-02)
- Rows render at first paint from the basic query while progress/points shimmer, then swap in place (EPIC-03, D-04/D-14)
- Enrichment failure shows the retry glyph per cell and never gates the page (EPIC-03, D-15)
- Missing-from-map epics show "No stories" (D-16)
- Priority icon renders from `epic.priority`; no labels/components/flags anywhere (EPIC-04, D-08, D-12)
- Row order matches `fetchEpicsBasic` before and after enrichment (EPIC-01, D-17)
- Row click, skeleton, ErrorState, StaleDataBanner, density variants and "+ Create Epic" behave exactly as before; no search UI exists (EPIC-07, D-18)
</success_criteria>

<output>
Create `.planning/phases/91.2-epics-page-redesign/91.2-03-SUMMARY.md` when done
</output>
