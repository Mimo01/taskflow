---
phase: 34-layout-customization
plan: 05
type: execute
wave: 4
depends_on: [34-02, 34-04]
files_modified: []
autonomous: false
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04, LAYOUT-05, LAYOUT-06, LAYOUT-07]

must_haves:
  truths:
    - "User can toggle sidebar items on/off in Settings > Appearance"
    - "User can drag-reorder sidebar items in Settings > Appearance"
    - "User can apply Dev or PM preset to restore defaults"
    - "Dashboard shows widgets from store in a draggable, resizable grid"
    - "User can add and remove dashboard widgets"
    - "Layout persists across page navigation"
    - "All 11 widget types render real data"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint for all Phase 34 features.

Purpose: Human confirms that sidebar customization and widget-based dashboard work correctly end-to-end before the phase is marked complete. Covers all LAYOUT requirements.
Output: User approval or list of issues to fix.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-layout-customization/34-CONTEXT.md
@.planning/phases/34-layout-customization/34-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify build</name>
  <files></files>
  <read_first>
    - taskflow/src/stores/settings.store.test.ts (verify tests exist for new fields)
  </read_first>
  <action>
Run the full test suite and build to verify no regressions:

```bash
cd taskflow && npx vitest run --reporter=verbose
cd taskflow && npm run build
```

Fix any test failures or build errors before proceeding to visual verification.

Also verify the preset application works correctly by checking that the settings store migration is in place:
- Confirm settings.store.ts has `version: 9`
- Confirm migration block handles `version < 9`
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with all tests passing
    - `npm run build` exits 0 with no errors
    - settings.store.ts contains `version: 9`
  </acceptance_criteria>
  <done>Full test suite passes, build succeeds, no regressions from Phase 34 changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 34 layout customization features</name>
  <files></files>
  <action>
Present the verification checklist to the user and wait for approval. Start the app with `cd taskflow && npm run tauri dev` if not already running.
  </action>
  <what-built>
Complete layout customization system:
1. Sidebar customization: data-driven sidebar rendering from sidebarItems[] store, Settings > Appearance toggle/reorder UI
2. Dashboard widget system: react-grid-layout grid with 11 widget types, add/remove/drag/resize
3. Preset system: Dev and PM presets restore both sidebar and dashboard layouts
4. Persistence: all layout state persists via Zustand + LazyStore
  </what-built>
  <how-to-verify>
**Start the app:** `cd taskflow && npm run tauri dev`

**1. Sidebar Customization (LAYOUT-01, LAYOUT-02):**
- Navigate to Settings > Appearance
- Scroll down to "Sidebar Items" section
- Toggle OFF one visible item (e.g., "Epics") -- verify it disappears from the sidebar immediately
- Toggle it back ON -- verify it reappears
- Drag an item to a different position in the list -- verify the sidebar order updates
- Verify Settings link stays pinned at the bottom regardless of changes

**2. Preset Application (LAYOUT-03, LAYOUT-07):**
- Click "Apply PM Preset" -- confirm the confirmation dialog appears
- Click Cancel -- verify nothing changes
- Click "Apply PM Preset" again and Confirm -- verify sidebar shows PM items (Sprint Progress, Workload visible; My Tasks, Sprint Board hidden)
- Navigate to Dashboard -- verify PM preset widgets appear (Sprint Health, Sprint Progress, Workload, Releases)
- Go back to Settings > Appearance, click "Apply Dev Preset" and Confirm -- verify Dev defaults restore

**3. Dashboard Widgets (LAYOUT-04, LAYOUT-05):**
- On Dashboard, verify widgets render in a grid with real data (My Subtasks, MR Health, Sprint Health for Dev preset)
- Drag a widget by its grip handle -- verify other widgets reflow smoothly
- Resize a widget from the bottom-right corner -- verify it respects min/max constraints
- Click "Add Widget" button -- verify picker dialog shows all 11 widget types with descriptions
- Add a widget (e.g., "Notifications") -- verify it appears in the grid
- Add the same widget type again -- verify duplicate is allowed (per D-08)
- Remove a widget via X button -- verify it disappears

**4. Persistence (LAYOUT-06):**
- Make some layout changes (move a widget, hide a sidebar item)
- Navigate to a different page and back to Dashboard -- verify layout persists
- Close and reopen the app -- verify layout persists

**5. Edge Cases:**
- Remove all widgets from dashboard -- verify empty state appears with "Your dashboard is empty" message and "Add Widget" button
- Toggle all sidebar items OFF -- verify sidebar is nearly empty (only Settings remains)
  </how-to-verify>
  <verify>Human approval</verify>
  <acceptance_criteria>
    - User confirms sidebar items can be toggled on/off from Settings > Appearance
    - User confirms sidebar items can be reordered via drag-and-drop
    - User confirms Dev/PM presets restore both sidebar and dashboard layouts
    - User confirms dashboard widgets can be added, removed, dragged, and resized
    - User confirms layout persists across navigation and app restart
  </acceptance_criteria>
  <done>All LAYOUT-01 through LAYOUT-07 requirements visually confirmed by user.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Full test suite passes
- Build succeeds
- All 7 LAYOUT requirements verified visually
</verification>

<success_criteria>
- All LAYOUT-01 through LAYOUT-07 requirements confirmed working by user
- No visual bugs or broken interactions
- Layout persists across navigation and app restart
</success_criteria>

<output>
After completion, create `.planning/phases/34-layout-customization/34-05-SUMMARY.md`
</output>
