---
phase: 44-loading-ux
plan: 03
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - taskflow/src/routes/dashboard/MyTasksTab.tsx
  - taskflow/src/routes/dashboard/MyTasksTab.test.tsx
  - taskflow/src/routes/dashboard/WorkloadTab.tsx
  - taskflow/src/routes/dashboard/WorkloadTab.test.tsx
  - taskflow/src/routes/dashboard/SprintProgressTab.tsx
  - taskflow/src/routes/dashboard/SprintProgressTab.test.tsx
  - taskflow/src/routes/dashboard/EpicsPage.tsx
  - taskflow/src/routes/dashboard/EpicsPage.test.tsx
  - taskflow/src/routes/dashboard/ReleasesTab.tsx
  - taskflow/src/routes/dashboard/ReleasesTab.test.tsx
  - taskflow/src/routes/dashboard/MrAttentionTab.tsx
  - taskflow/src/routes/dashboard/MrAttentionTab.test.tsx
autonomous: true
requirements: [LOAD-01, LOAD-05]

must_haves:
  truths:
    - "All 6 remaining views show layout-matched skeletons instead of raw bg-muted animate-pulse divs"
    - "No view shows skeleton flash when data loads within 200ms"
    - "Refresh buttons on all 6 views invalidate caches and show skeleton immediately"
    - "All existing tests pass after skeleton migration"
  artifacts:
    - path: "taskflow/src/routes/dashboard/MyTasksTab.tsx"
      provides: "My tasks with MyTasksSkeleton + useDelayedLoading + refresh invalidation"
      contains: "MyTasksSkeleton"
    - path: "taskflow/src/routes/dashboard/WorkloadTab.tsx"
      provides: "Workload with WorkloadSkeleton + useDelayedLoading + refresh invalidation"
      contains: "WorkloadSkeleton"
    - path: "taskflow/src/routes/dashboard/SprintProgressTab.tsx"
      provides: "Sprint progress with SprintProgressSkeleton + useDelayedLoading + refresh invalidation"
      contains: "SprintProgressSkeleton"
    - path: "taskflow/src/routes/dashboard/EpicsPage.tsx"
      provides: "Epics with EpicsSkeleton + useDelayedLoading + refresh invalidation"
      contains: "EpicsSkeleton"
    - path: "taskflow/src/routes/dashboard/ReleasesTab.tsx"
      provides: "Releases with ReleasesSkeleton + useDelayedLoading + refresh invalidation"
      contains: "ReleasesSkeleton"
    - path: "taskflow/src/routes/dashboard/MrAttentionTab.tsx"
      provides: "MR attention with MrAttentionSkeleton + useDelayedLoading + refresh invalidation"
      contains: "MrAttentionSkeleton"
  key_links:
    - from: "all 6 view files"
      to: "taskflow/src/hooks/useDelayedLoading.ts"
      via: "import { useDelayedLoading }"
      pattern: "useDelayedLoading"
    - from: "all 6 view files"
      to: "their co-located skeleton component"
      via: "import and render in loading branch"
      pattern: "import.*Skeleton.*from.*\\./"
---

<objective>
Wire skeletons, useDelayedLoading, and cache-invalidating refresh into the 6 remaining views: MyTasks, Workload, SprintProgress, Epics, Releases, MrAttention. These are straightforward replacements — no progressive loading needed (that's Plan 02's sprint board and backlog).

Purpose: Complete LOAD-01 coverage (all 8 major views have layout-matched skeletons) and LOAD-05 (all views use 200ms flicker prevention).
Output: 6 view files + 6 test files updated
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-loading-ux/44-CONTEXT.md
@.planning/phases/44-loading-ux/44-RESEARCH.md
@.planning/phases/44-loading-ux/44-UI-SPEC.md
@.planning/phases/44-loading-ux/44-01-SUMMARY.md

<interfaces>
From taskflow/src/hooks/useDelayedLoading.ts:
```typescript
export function useDelayedLoading(isPending: boolean, delayMs?: number): boolean;
```

From taskflow/src/routes/dashboard/MyTasksSkeleton.tsx:
```typescript
export function MyTasksSkeleton(): JSX.Element;
// 5 rows h-10 with data-testid="skeleton-row"
```

From taskflow/src/routes/dashboard/WorkloadSkeleton.tsx:
```typescript
export function WorkloadSkeleton(): JSX.Element;
// 5 rows h-8 with data-testid="skeleton-row"
```

From taskflow/src/routes/dashboard/SprintProgressSkeleton.tsx:
```typescript
export function SprintProgressSkeleton(): JSX.Element;
// 5 rows h-8 with data-testid="skeleton-row"
```

From taskflow/src/routes/dashboard/EpicsSkeleton.tsx:
```typescript
export function EpicsSkeleton(): JSX.Element;
// 5 rows h-10
```

From taskflow/src/routes/dashboard/ReleasesSkeleton.tsx:
```typescript
export function ReleasesSkeleton(): JSX.Element;
// 5 rows h-10 with data-testid="skeleton-row"
```

From taskflow/src/routes/dashboard/MrAttentionSkeleton.tsx:
```typescript
export function MrAttentionSkeleton(): JSX.Element;
// 5 rows h-10 with data-testid="skeleton-mr-row"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire skeletons into MyTasksTab, WorkloadTab, and SprintProgressTab</name>
  <files>
    taskflow/src/routes/dashboard/MyTasksTab.tsx,
    taskflow/src/routes/dashboard/MyTasksTab.test.tsx,
    taskflow/src/routes/dashboard/WorkloadTab.tsx,
    taskflow/src/routes/dashboard/WorkloadTab.test.tsx,
    taskflow/src/routes/dashboard/SprintProgressTab.tsx,
    taskflow/src/routes/dashboard/SprintProgressTab.test.tsx
  </files>
  <read_first>
    - taskflow/src/routes/dashboard/MyTasksTab.tsx (lines 85-100 for isLoading/refetch destructuring, lines 360-400 for refresh button and skeleton block)
    - taskflow/src/routes/dashboard/MyTasksTab.test.tsx (line 144-165 for skeleton test using findAllByTestId('skeleton-row'))
    - taskflow/src/routes/dashboard/WorkloadTab.tsx (lines 78-92 for isLoading/refetch, lines 325-350 for refresh button and skeleton block)
    - taskflow/src/routes/dashboard/WorkloadTab.test.tsx (check for skeleton assertions)
    - taskflow/src/routes/dashboard/SprintProgressTab.tsx (lines 50-60 for isLoading/refetch, lines 200-230 for refresh button and skeleton block)
    - taskflow/src/routes/dashboard/SprintProgressTab.test.tsx (check for skeleton assertions)
  </read_first>
  <action>
    Apply the same pattern to all 3 views. For EACH view (MyTasksTab, WorkloadTab, SprintProgressTab):

    **Step 1: Add imports**
    ```typescript
    import { useDelayedLoading } from '@/hooks/useDelayedLoading';
    import { XxxSkeleton } from './XxxSkeleton';  // MyTasksSkeleton, WorkloadSkeleton, SprintProgressSkeleton
    ```
    Also add `useQueryClient` to the `@tanstack/react-query` import if not already present, and `useEffect` to React imports if not already present.

    **Step 2: Add state and hook after the useQuery call**
    ```typescript
    const queryClient = useQueryClient();  // if not already present
    const [isRefreshing, setIsRefreshing] = useState(false);
    const showSkeleton = useDelayedLoading(isLoading) || isRefreshing;

    useEffect(() => {
      if (!isLoading) setIsRefreshing(false);
    }, [isLoading]);
    ```

    **Step 3: Replace inline skeleton block**
    Replace `{isLoading && (` block containing `bg-muted animate-pulse` divs with:
    ```typescript
    {showSkeleton && <XxxSkeleton />}
    ```

    **Step 4: Replace refresh button onClick**
    Replace `onClick={() => refetch()}` with cache invalidation:

    - **MyTasksTab**: `onClick={() => { setIsRefreshing(true); queryClient.invalidateQueries({ queryKey: ['jira-issues', 'my-tasks'] }); }}`
    - **WorkloadTab**: `onClick={() => { setIsRefreshing(true); queryClient.invalidateQueries({ queryKey: ['jira-issues', 'sprint-board'] }); }}`
    - **SprintProgressTab**: `onClick={() => { setIsRefreshing(true); queryClient.invalidateQueries({ queryKey: ['jira-issues', 'sprint-board'] }); }}`

    **Step 5: Update ErrorState onRetry props**
    Replace `onRetry={refetch}` with `onRetry={() => { setIsRefreshing(true); queryClient.invalidateQueries({ queryKey: ['...same key as above...'] }); }}`

    **Step 6: Update StaleDataBanner onRetry props**
    Same pattern as ErrorState.

    **Step 7: Update tests**
    - MyTasksTab.test.tsx uses `findAllByTestId('skeleton-row')` — the new MyTasksSkeleton component includes `data-testid="skeleton-row"` on each Skeleton row, so this test should still pass. Verify it does.
    - WorkloadTab.test.tsx and SprintProgressTab.test.tsx — check if they have similar skeleton assertions and verify they still pass.
    - If any test checks for `bg-muted` specifically (not `animate-pulse`), update the assertion.

    **Change `isLoading` guards to `showSkeleton`:**
    In each file, the conditional rendering currently uses `isLoading` to decide between skeleton and content. After changes, the skeleton branch uses `showSkeleton`, but the content guards (`!isLoading && !isError && data`) should remain unchanged — they gate on actual data availability, not skeleton display.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/MyTasksTab.test.tsx src/routes/dashboard/WorkloadTab.test.tsx src/routes/dashboard/SprintProgressTab.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - MyTasksTab.tsx contains `import { MyTasksSkeleton } from './MyTasksSkeleton'` and `useDelayedLoading`
    - MyTasksTab.tsx contains `invalidateQueries.*my-tasks`
    - MyTasksTab.tsx does NOT contain `bg-muted animate-pulse` (legacy skeleton removed)
    - WorkloadTab.tsx contains `import { WorkloadSkeleton } from './WorkloadSkeleton'` and `useDelayedLoading`
    - WorkloadTab.tsx contains `invalidateQueries.*sprint-board`
    - WorkloadTab.tsx does NOT contain inline skeleton with `bg-muted animate-pulse`
    - SprintProgressTab.tsx contains `import { SprintProgressSkeleton } from './SprintProgressSkeleton'` and `useDelayedLoading`
    - SprintProgressTab.tsx contains `invalidateQueries.*sprint-board`
    - SprintProgressTab.tsx does NOT contain inline skeleton with `bg-muted animate-pulse`
    - All 3 test suites pass with exit code 0
  </acceptance_criteria>
  <done>MyTasks, Workload, SprintProgress views use their skeleton components via useDelayedLoading, refresh buttons invalidate caches, no legacy skeleton markup remains, all tests green</done>
</task>

<task type="auto">
  <name>Task 2: Wire skeletons into EpicsPage, ReleasesTab, and MrAttentionTab</name>
  <files>
    taskflow/src/routes/dashboard/EpicsPage.tsx,
    taskflow/src/routes/dashboard/EpicsPage.test.tsx,
    taskflow/src/routes/dashboard/ReleasesTab.tsx,
    taskflow/src/routes/dashboard/ReleasesTab.test.tsx,
    taskflow/src/routes/dashboard/MrAttentionTab.tsx,
    taskflow/src/routes/dashboard/MrAttentionTab.test.tsx
  </files>
  <read_first>
    - taskflow/src/routes/dashboard/EpicsPage.tsx (lines 130-140 for isLoading/refetch, lines 170-200 for skeleton and content rendering)
    - taskflow/src/routes/dashboard/EpicsPage.test.tsx (check for skeleton/loading assertions)
    - taskflow/src/routes/dashboard/ReleasesTab.tsx (lines 125-140 for isLoading/refetch, lines 255-285 for refresh button and skeleton block)
    - taskflow/src/routes/dashboard/ReleasesTab.test.tsx (check for skeleton assertions)
    - taskflow/src/routes/dashboard/MrAttentionTab.tsx (lines 130-140 for isLoading/refetch, lines 335-365 for refresh button and skeleton block)
    - taskflow/src/routes/dashboard/MrAttentionTab.test.tsx (check for skeleton assertions)
  </read_first>
  <action>
    Apply the same pattern to all 3 views. For EACH view (EpicsPage, ReleasesTab, MrAttentionTab):

    **Step 1: Add imports**
    ```typescript
    import { useDelayedLoading } from '@/hooks/useDelayedLoading';
    import { XxxSkeleton } from './XxxSkeleton';  // EpicsSkeleton, ReleasesSkeleton, MrAttentionSkeleton
    ```
    Also add `useQueryClient` to the `@tanstack/react-query` import if not already present, and `useEffect`/`useState` to React imports if not already present.

    **Step 2: Add state and hook after the useQuery call**
    ```typescript
    const queryClient = useQueryClient();  // if not already present
    const [isRefreshing, setIsRefreshing] = useState(false);
    const showSkeleton = useDelayedLoading(isLoading) || isRefreshing;

    useEffect(() => {
      if (!isLoading) setIsRefreshing(false);
    }, [isLoading]);
    ```
    Note for ReleasesTab: `isLoading` is derived as `const isLoading = loadingVersions;` at line 238. Use that same variable.
    Note for MrAttentionTab: has both `gitlabTokenLoading` and `isLoading` — the skeleton guard currently checks `(gitlabTokenLoading || isLoading)`. Wire useDelayedLoading with `(gitlabTokenLoading || isLoading)` as the isPending input:
    ```typescript
    const showSkeleton = useDelayedLoading(gitlabTokenLoading || isLoading) || isRefreshing;
    ```

    **Step 3: Replace inline skeleton block**
    Replace `{isLoading && (` (or `{(gitlabTokenLoading || isLoading) && (` for MrAttentionTab) block with:
    ```typescript
    {showSkeleton && <XxxSkeleton />}
    ```

    **Step 4: Replace refresh button onClick**
    Replace `onClick={() => refetch()}` with:

    - **EpicsPage**: `onClick={() => { setIsRefreshing(true); queryClient.invalidateQueries({ queryKey: ['jira-epics-basic'] }); }}`
    - **ReleasesTab**: `onClick={() => { setIsRefreshing(true); queryClient.invalidateQueries({ queryKey: ['jira-fix-versions'] }); }}`
    - **MrAttentionTab**: `onClick={() => { setIsRefreshing(true); queryClient.invalidateQueries({ queryKey: ['jira-issues', 'my-tasks'] }); queryClient.invalidateQueries({ queryKey: ['gitlab-mrs'] }); }}`
      (MrAttentionTab has multiple queries — invalidate the primary ones that drive the main data)

    **Step 5: Update ErrorState and StaleDataBanner onRetry props**
    Same cache invalidation pattern as the refresh button for each view.

    **Step 6: Update tests**
    - Check each test file for skeleton/loading assertions. The new skeleton components preserve `data-testid` attributes:
      - ReleasesTab: `data-testid="skeleton-row"`
      - MrAttentionTab: `data-testid="skeleton-mr-row"`
    - If tests assert on `bg-muted` class, update to check for `animate-pulse` or `data-testid` instead.
    - EpicsPage test likely checks for `animate-pulse` — verify it still finds elements.

    **Step 7: Run full test suite for these 3 files**
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/EpicsPage.test.tsx src/routes/dashboard/ReleasesTab.test.tsx src/routes/dashboard/MrAttentionTab.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - EpicsPage.tsx contains `import { EpicsSkeleton } from './EpicsSkeleton'` and `useDelayedLoading`
    - EpicsPage.tsx contains `invalidateQueries.*jira-epics-basic`
    - EpicsPage.tsx does NOT contain inline `bg-muted animate-pulse` skeleton divs
    - ReleasesTab.tsx contains `import { ReleasesSkeleton } from './ReleasesSkeleton'` and `useDelayedLoading`
    - ReleasesTab.tsx contains `invalidateQueries.*jira-fix-versions`
    - ReleasesTab.tsx does NOT contain inline `bg-muted animate-pulse` skeleton divs
    - MrAttentionTab.tsx contains `import { MrAttentionSkeleton } from './MrAttentionSkeleton'` and `useDelayedLoading`
    - MrAttentionTab.tsx contains `invalidateQueries`
    - MrAttentionTab.tsx does NOT contain inline `bg-muted animate-pulse` skeleton divs
    - All 3 test suites pass with exit code 0
  </acceptance_criteria>
  <done>Epics, Releases, MrAttention views use their skeleton components via useDelayedLoading, refresh buttons invalidate caches, no legacy skeleton markup remains, all tests green</done>
</task>

</tasks>

<verification>
1. `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/MyTasksTab.test.tsx src/routes/dashboard/WorkloadTab.test.tsx src/routes/dashboard/SprintProgressTab.test.tsx src/routes/dashboard/EpicsPage.test.tsx src/routes/dashboard/ReleasesTab.test.tsx src/routes/dashboard/MrAttentionTab.test.tsx --reporter=verbose` — all 6 test suites pass
2. `grep -l "bg-muted animate-pulse" taskflow/src/routes/dashboard/MyTasksTab.tsx taskflow/src/routes/dashboard/WorkloadTab.tsx taskflow/src/routes/dashboard/SprintProgressTab.tsx taskflow/src/routes/dashboard/EpicsPage.tsx taskflow/src/routes/dashboard/ReleasesTab.tsx taskflow/src/routes/dashboard/MrAttentionTab.tsx` — returns nothing (all legacy skeletons removed)
3. `grep -l "useDelayedLoading" taskflow/src/routes/dashboard/MyTasksTab.tsx taskflow/src/routes/dashboard/WorkloadTab.tsx taskflow/src/routes/dashboard/SprintProgressTab.tsx taskflow/src/routes/dashboard/EpicsPage.tsx taskflow/src/routes/dashboard/ReleasesTab.tsx taskflow/src/routes/dashboard/MrAttentionTab.tsx` — returns all 6 files
4. `grep -l "invalidateQueries" taskflow/src/routes/dashboard/MyTasksTab.tsx taskflow/src/routes/dashboard/WorkloadTab.tsx taskflow/src/routes/dashboard/SprintProgressTab.tsx taskflow/src/routes/dashboard/EpicsPage.tsx taskflow/src/routes/dashboard/ReleasesTab.tsx taskflow/src/routes/dashboard/MrAttentionTab.tsx` — returns all 6 files
</verification>

<success_criteria>
- All 6 views use their dedicated skeleton component via useDelayedLoading
- All 6 views have refresh buttons that call invalidateQueries (not refetch) and show skeleton immediately via isRefreshing flag
- No bg-muted animate-pulse inline skeleton divs remain in any of the 6 view files
- All existing tests pass
- Combined with Plan 02, all 8 major views now have layout-matched skeletons (LOAD-01 complete)
</success_criteria>

<output>
After completion, create `.planning/phases/44-loading-ux/44-03-SUMMARY.md`
</output>
