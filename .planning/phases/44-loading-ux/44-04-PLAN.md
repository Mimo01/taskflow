---
phase: 44-loading-ux
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/SprintBoardTab.tsx
  - taskflow/src/routes/dashboard/BacklogPage.tsx
  - taskflow/src/routes/dashboard/MyTasksTab.tsx
  - taskflow/src/routes/dashboard/WorkloadTab.tsx
  - taskflow/src/routes/dashboard/SprintProgressTab.tsx
  - taskflow/src/routes/dashboard/EpicsPage.tsx
  - taskflow/src/routes/dashboard/ReleasesTab.tsx
  - taskflow/src/routes/dashboard/MrAttentionTab.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LOAD-01, LOAD-03, LOAD-04, LOAD-05]
gap_closure: true

must_haves:
  truths:
    - "npx tsc --noEmit exits with 0 errors"
    - "REQUIREMENTS.md LOAD-03 reflects deferred status with reason"
    - "REQUIREMENTS.md LOAD-04 reflects partial completion with reason"
    - "REQUIREMENTS.md traceability table matches checkbox states"
  artifacts:
    - path: "taskflow/src/routes/dashboard/SprintBoardTab.tsx"
      provides: "useQuery destructuring without refetch"
      contains: "const { data, isLoading, isError, error, dataUpdatedAt } = useQuery"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement tracking for LOAD-03 and LOAD-04"
      contains: "LOAD-03"
  key_links: []
---

<objective>
Close 3 verification gaps from 44-VERIFICATION.md: (1) remove unused `refetch` from 8 view files to fix TypeScript compilation, (2) update REQUIREMENTS.md to accurately reflect LOAD-03 deferred status and LOAD-04 partial completion.

Purpose: TypeScript compilation is broken (blocks CI), and REQUIREMENTS.md is inconsistent with what was actually built.
Output: Clean `npx tsc --noEmit`, accurate REQUIREMENTS.md tracking.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-loading-ux/44-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused refetch from all 8 view useQuery destructurings</name>
  <files>
    taskflow/src/routes/dashboard/SprintBoardTab.tsx,
    taskflow/src/routes/dashboard/BacklogPage.tsx,
    taskflow/src/routes/dashboard/MyTasksTab.tsx,
    taskflow/src/routes/dashboard/WorkloadTab.tsx,
    taskflow/src/routes/dashboard/SprintProgressTab.tsx,
    taskflow/src/routes/dashboard/EpicsPage.tsx,
    taskflow/src/routes/dashboard/ReleasesTab.tsx,
    taskflow/src/routes/dashboard/MrAttentionTab.tsx
  </files>
  <read_first>
    taskflow/src/routes/dashboard/SprintBoardTab.tsx,
    taskflow/src/routes/dashboard/BacklogPage.tsx,
    taskflow/src/routes/dashboard/MyTasksTab.tsx,
    taskflow/src/routes/dashboard/WorkloadTab.tsx,
    taskflow/src/routes/dashboard/SprintProgressTab.tsx,
    taskflow/src/routes/dashboard/EpicsPage.tsx,
    taskflow/src/routes/dashboard/ReleasesTab.tsx,
    taskflow/src/routes/dashboard/MrAttentionTab.tsx
  </read_first>
  <action>
In each of the 8 files, find the useQuery destructuring that includes `refetch` and remove it. The fix is identical in all files — remove `, refetch` from the destructuring pattern.

Specific locations (from VERIFICATION.md):
- SprintBoardTab.tsx line 496: `const { data, isLoading, isError, error, dataUpdatedAt, refetch } = useQuery({` → remove `, refetch`
- BacklogPage.tsx line 198: same pattern → remove `, refetch`
- MyTasksTab.tsx line 96: same pattern → remove `, refetch`
- WorkloadTab.tsx line 83: same pattern → remove `, refetch`
- SprintProgressTab.tsx line 54: same pattern → remove `, refetch`
- EpicsPage.tsx line 139: same pattern → remove `, refetch`
- ReleasesTab.tsx line 138: same pattern → remove `, refetch`
- MrAttentionTab.tsx line 139: same pattern → remove `, refetch`

After the edit, each line should read something like:
```typescript
const { data, isLoading, isError, error, dataUpdatedAt } = useQuery({
```

Do NOT change anything else in these files. This is a one-liner fix per file.

After editing all 8 files, run `cd taskflow && npx tsc --noEmit` to confirm zero errors.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SprintBoardTab.tsx does NOT contain the string ", refetch } = useQuery" or ", refetch}" anywhere
    - BacklogPage.tsx does NOT contain the string ", refetch } = useQuery" or ", refetch}" anywhere
    - MyTasksTab.tsx does NOT contain the string ", refetch" in a useQuery destructuring
    - WorkloadTab.tsx does NOT contain the string ", refetch" in a useQuery destructuring
    - SprintProgressTab.tsx does NOT contain the string ", refetch" in a useQuery destructuring
    - EpicsPage.tsx does NOT contain the string ", refetch" in a useQuery destructuring
    - ReleasesTab.tsx does NOT contain the string ", refetch" in a useQuery destructuring
    - MrAttentionTab.tsx does NOT contain the string ", refetch" in a useQuery destructuring
    - `cd taskflow && npx tsc --noEmit` exits with code 0 (zero errors)
    - `cd taskflow && npm test` still passes (no regressions)
  </acceptance_criteria>
  <done>All 8 TS6133 errors eliminated. TypeScript compiles cleanly. All existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md to reflect actual LOAD-03 and LOAD-04 status</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/44-loading-ux/44-VERIFICATION.md</read_first>
  <action>
Update REQUIREMENTS.md to accurately reflect what was built for LOAD-03 and LOAD-04.

**LOAD-03 (line 14):** Change from `- [ ] **LOAD-03**:` to:
```
- [ ] **LOAD-03**: User sees sprint board story headers immediately while subtasks load progressively beneath them — **Infra complete, deferred pending query split** (skeleton infrastructure exists in VirtualizedSwimlanes but subtasksLoading is always false because fetchSprintIssues returns stories+subtasks in a single query; requires Phase 45 query parallelization to activate)
```

**LOAD-04 (line 15):** Change from `- [ ] **LOAD-04**:` to:
```
- [~] **LOAD-04**: User sees backlog issue list immediately while epic metadata loads progressively — **Partial** (epic column header shows Skeleton while allEpics query is pending via separate jira-epics-basic query; per-row epic badges come from same query as rows so no per-row progressive loading)
```

**Traceability table (lines 69-70):** Update the status column:
- LOAD-03 row: change `Pending` to `Deferred — infra complete, blocked on query split`
- LOAD-04 row: change `Pending` to `Partial — header-level progressive loading implemented`

Do NOT change LOAD-01 or LOAD-05 — they are already correctly marked as Complete.
  </action>
  <verify>
    <automated>grep -n "LOAD-03\|LOAD-04" /Users/mimo/Desktop/Tasker/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md line containing LOAD-03 includes the text "Infra complete, deferred pending query split"
    - REQUIREMENTS.md line containing LOAD-04 includes the text "Partial"
    - REQUIREMENTS.md traceability table row for LOAD-03 contains "Deferred"
    - REQUIREMENTS.md traceability table row for LOAD-04 contains "Partial"
    - LOAD-01 line still shows `[x]` (unchanged)
    - LOAD-05 line still shows `[x]` (unchanged)
  </acceptance_criteria>
  <done>REQUIREMENTS.md accurately reflects LOAD-03 as deferred (infra complete, blocked on query split) and LOAD-04 as partial (header-level progressive loading works, per-row not possible with current data model). Traceability table matches checkbox states.</done>
</task>

</tasks>

<verification>
1. `cd taskflow && npx tsc --noEmit` exits 0 (no TypeScript errors)
2. `cd taskflow && npm test` all tests pass (no regressions from removing refetch)
3. `grep "refetch" taskflow/src/routes/dashboard/{SprintBoardTab,BacklogPage,MyTasksTab,WorkloadTab,SprintProgressTab,EpicsPage,ReleasesTab,MrAttentionTab}.tsx` returns no matches
4. REQUIREMENTS.md LOAD-03 and LOAD-04 entries reflect actual implementation state
</verification>

<success_criteria>
- TypeScript compiles with zero errors across the entire codebase
- All existing tests pass without modification
- REQUIREMENTS.md is the single source of truth for requirement status, matching what was actually built
</success_criteria>

<output>
After completion, create `.planning/phases/44-loading-ux/44-04-SUMMARY.md`
</output>
