---
phase: 62-tempo-worklog-viewer-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/components/app/sidebar-items.ts
  - taskflow/src/components/app/Sidebar.tsx
  - taskflow/src/components/app/Sidebar.test.tsx
  - taskflow/src/routes/routes.tsx
  - taskflow/src/routes/worklogs/WorklogsPage.tsx
autonomous: true
requirements: [TEMPO-01]
tags: [sidebar, routing, tempo, feature-gate]

must_haves:
  truths:
    - "A 'Worklogs' link appears in the sidebar tracking section when tempoEnabled=true"
    - "The 'Worklogs' link is hidden when tempoEnabled=false"
    - "Navigating to /worklogs renders the WorklogsPage stub without crashing"
    - "Sidebar.test.tsx covers both tempoEnabled=true and tempoEnabled=false cases"
  artifacts:
    - path: "taskflow/src/components/app/sidebar-items.ts"
      provides: "worklogs nav item definition in SIDEBAR_NAV_ITEMS + devVisible/pmVisible sets"
      contains: "id: 'worklogs'"
    - path: "taskflow/src/components/app/Sidebar.tsx"
      provides: "Clock icon in ICON_MAP + tempoEnabled selector + worklogs gate"
      contains: "tempoEnabled"
    - path: "taskflow/src/components/app/Sidebar.test.tsx"
      provides: "tempoEnabled gate describe block (D-06)"
      contains: "tempoEnabled gate"
    - path: "taskflow/src/routes/routes.tsx"
      provides: "/worklogs route registration with withLazy"
      contains: "/worklogs"
    - path: "taskflow/src/routes/worklogs/WorklogsPage.tsx"
      provides: "WorklogsPage stub component (replaced in Plan 02)"
      contains: "export default"
  key_links:
    - from: "taskflow/src/components/app/Sidebar.tsx"
      to: "useSettingsStore"
      via: "fine-grained selector for tempoEnabled"
      pattern: "useSettingsStore\\(\\(s\\) => s\\.tempoEnabled\\)"
    - from: "taskflow/src/components/app/Sidebar.tsx"
      to: "sidebar-items.ts SIDEBAR_NAV_ITEMS filter"
      via: "AND-clause `!(nav.id === 'worklogs' && !tempoEnabled)`"
      pattern: "nav\\.id === 'worklogs' && !tempoEnabled"
    - from: "taskflow/src/routes/routes.tsx"
      to: "taskflow/src/routes/worklogs/WorklogsPage.tsx"
      via: "lazy import + withLazy"
      pattern: "lazy\\(\\(\\) => import\\('./worklogs/WorklogsPage'\\)\\)"
---

<objective>
Wire the Tempo Worklogs viewer into the app shell: add the sidebar nav item gated by `tempoEnabled`, register the `/worklogs` route, and create a minimal `WorklogsPage` stub that Plan 02 will replace with the full implementation.

Purpose: Establishes navigation and routing so Plan 02 can focus entirely on page implementation without touching sidebar or routes files. Splitting this way also satisfies the D-06 gating contract (TEMPO-01 prerequisite) on its own commit boundary so the sidebar gate can be verified in isolation.

Output: Sidebar shows/hides "Worklogs" link based on `tempoEnabled`, `/worklogs` route resolves to a stub page, and Sidebar tests cover both gate states.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-tempo-worklog-viewer-ui/62-CONTEXT.md
@.planning/phases/62-tempo-worklog-viewer-ui/62-RESEARCH.md
@.planning/phases/62-tempo-worklog-viewer-ui/62-PATTERNS.md
@.planning/phases/62-tempo-worklog-viewer-ui/62-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From taskflow/src/components/app/sidebar-items.ts:
- `interface SidebarNavDef { id: string; label: string; path: string; iconName: string; section: string }`
- `SIDEBAR_NAV_ITEMS: SidebarNavDef[]` — array; insert worklogs entry after `releases` (current line 74) and before the Testing block (current line 75)
- `SIDEBAR_SECTIONS: { id: 'main' | 'planning' | 'code' | 'tracking' | 'testing'; label: string }[]`
- `getDefaultSidebarItems(preset: 'dev' | 'pm'): SidebarItem[]` — both `devVisible` and `pmVisible` Sets must include `'worklogs'` per RESEARCH A2

From taskflow/src/components/app/Sidebar.tsx:
- Lucide imports (alphabetical) — add `Clock` between `ChevronRight` and `FlaskConical`
- `ICON_MAP: Record<string, ComponentType<{ className?: string }>>` — add `Clock,` entry (no rename)
- Existing fine-grained selectors at lines 77-84 — add `const tempoEnabled = useSettingsStore((s) => s.tempoEnabled);` after `selectedAioProjectKey` (line 80)
- `sectionedItems` filter predicate at lines 285-289 — add new AND-clause: `!(nav.id === 'worklogs' && !tempoEnabled)`

From taskflow/src/routes/routes.tsx:
- `withLazy(Component: ComponentType): JSX.Element` — Suspense+ChunkErrorBoundary wrapper at lines 24-32
- Add `const WorklogsPage = lazy(() => import('./worklogs/WorklogsPage'));` after the existing `AioTestRunDetailPage` lazy declaration (line 22)
- Add route `{ path: '/worklogs', element: withLazy(WorklogsPage) }` after `/releases` entry (line 43) to mirror sidebar tracking-section order

From taskflow/src/stores/settings.store.ts:
- `tempoEnabled: boolean` exists at line 106 (added in Phase 61)
- `setTempoEnabled: (v: boolean) => void` exists at line 225

From taskflow/src/components/app/Sidebar.test.tsx:
- Existing mock-state file already declares `mockAioEnabled` and `mockSelectedAioProjectKey` module-level vars
- `vi.mock('@/stores/settings.store', ...)` factory at lines 63-92 already returns selector-aware state object
- Existing `sidebarItems` mock array at lines 69-80 includes `'workload'` (stale; was removed in Phase 59 but still in this test mock — leave alone, do NOT remove)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Clock icon + Worklogs nav definition + tempoEnabled gate to Sidebar (D-04, D-05, D-06)</name>
  <files>taskflow/src/components/app/sidebar-items.ts, taskflow/src/components/app/Sidebar.tsx</files>
  <read_first>
    - taskflow/src/components/app/sidebar-items.ts (full file, ~125 lines)
    - taskflow/src/components/app/Sidebar.tsx (lines 1-95 for imports + selectors, lines 282-292 for sectionedItems filter)
    - taskflow/src/stores/settings.store.ts (lines 100-115 and 220-230 to confirm tempoEnabled + setTempoEnabled exist)
    - .planning/phases/62-tempo-worklog-viewer-ui/62-CONTEXT.md (D-04 through D-08)
    - .planning/phases/62-tempo-worklog-viewer-ui/62-PATTERNS.md (sidebar-items.ts and Sidebar.tsx sections)
  </read_first>
  <behavior>
    - Sidebar renders "Worklogs" link in the Tracking section when `tempoEnabled=true`
    - Sidebar does NOT render "Worklogs" link when `tempoEnabled=false`
    - Worklogs link uses Lucide `Clock` icon and routes to `/worklogs`
    - Worklogs item is in both `devVisible` and `pmVisible` defaults so the gate is the single source of truth
    - Existing AIO gate behavior is unchanged
  </behavior>
  <action>
    In `taskflow/src/components/app/sidebar-items.ts`: insert the new nav item `{ id: 'worklogs', label: 'Worklogs', path: '/worklogs', iconName: 'Clock', section: 'tracking' }` into `SIDEBAR_NAV_ITEMS` immediately AFTER the existing `releases` entry and BEFORE the Testing (AIO) block (so it sits at the end of the Tracking section, per D-04 and D-05). Add the string `'worklogs'` to BOTH the `devVisible` and `pmVisible` Sets inside `getDefaultSidebarItems` so `DEV_SIDEBAR_PRESET` and `PM_SIDEBAR_PRESET` both show it by default (RESEARCH A2; the `tempoEnabled` gate is the user-facing control).

    In `taskflow/src/components/app/Sidebar.tsx`: (1) Add `Clock` to the existing `lucide-react` import block, keeping alphabetical order between `ChevronRight` and `FlaskConical`. (2) Add `Clock,` as a new entry in the `ICON_MAP` const (after `Tag,`); the key MUST match the `iconName` string exactly. (3) Add `const tempoEnabled = useSettingsStore((s) => s.tempoEnabled);` as a new fine-grained selector immediately after the `selectedAioProjectKey` selector — follow the IN-01 comment pattern already in place. (4) In the `sectionedItems` computation (currently lines 283-291), add a new AND-clause to the inner `.filter` predicate: `!(nav.id === 'worklogs' && !tempoEnabled)`. Place this on its own line directly after the existing AIO gate clause `!(nav.section === 'testing' && (!aioEnabled || !selectedAioProjectKey))`, mirroring the same negation-of-disable-condition shape per D-06. Do NOT modify any other selectors, the prefetch logic, or render JSX. Pitfall #2: do not skip the `tempoEnabled` selector — without it the filter clause evaluates against `undefined` and the gate silently fails open.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit 2>&1 | grep -E "(sidebar-items|Sidebar)\.tsx?" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id: 'worklogs'" taskflow/src/components/app/sidebar-items.ts` returns 1
    - `grep "iconName: 'Clock'" taskflow/src/components/app/sidebar-items.ts` matches the worklogs entry
    - `grep -c "'worklogs'" taskflow/src/components/app/sidebar-items.ts` returns >= 3 (1 in NAV_ITEMS + 1 in devVisible + 1 in pmVisible)
    - `grep -E "^\s*Clock,\s*$" taskflow/src/components/app/Sidebar.tsx | wc -l` returns 2 (one in import block, one in ICON_MAP)
    - `grep -c "tempoEnabled" taskflow/src/components/app/Sidebar.tsx` returns >= 2 (selector + gate clause)
    - `grep -c "nav.id === 'worklogs' && !tempoEnabled" taskflow/src/components/app/Sidebar.tsx` returns 1
    - `cd taskflow && npx tsc --noEmit` exits 0 (no new TypeScript errors introduced by this task)
    - Existing AIO gate clause `nav.section === 'testing' && (!aioEnabled || !selectedAioProjectKey)` is still present unchanged
  </acceptance_criteria>
  <done>Sidebar nav definition + Clock icon + tempoEnabled selector + gate clause are all in place; TypeScript compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend Sidebar.test.tsx with tempoEnabled gate tests (D-06)</name>
  <files>taskflow/src/components/app/Sidebar.test.tsx</files>
  <read_first>
    - taskflow/src/components/app/Sidebar.test.tsx (full file, ~177 lines)
    - .planning/phases/62-tempo-worklog-viewer-ui/62-PATTERNS.md (Sidebar.test.tsx section)
    - .planning/phases/62-tempo-worklog-viewer-ui/62-CONTEXT.md (D-06)
  </read_first>
  <behavior>
    - New test: when `tempoEnabled=true`, the sidebar renders an element with text "Worklogs"
    - New test: when `tempoEnabled=false`, the sidebar contains no element with text "Worklogs"
    - Existing `aioEnabled` tests still pass — no regressions
    - Mock `sidebarItems` array includes `{ id: 'worklogs', visible: true }` so the gate is the only thing hiding the link
  </behavior>
  <action>
    Add a new module-level `let mockTempoEnabled = false;` declaration alongside the existing `mockAioEnabled` and `mockSelectedAioProjectKey` (after line 8). Extend the state object inside the `vi.mock('@/stores/settings.store', ...)` factory: add `tempoEnabled: mockTempoEnabled,` to the returned state, and append `{ id: 'worklogs', visible: true },` to the `sidebarItems` array (Pitfall #4 — without this the gate tests fail because the item is filtered out by the `visibleIds` Set before the gate predicate runs). Add a new `describe('Sidebar — tempoEnabled gate', ...)` block AFTER the existing `describe('Sidebar — aioEnabled gate', ...)` block, containing exactly two `it()` blocks: (1) "D-06: shows Worklogs link when tempoEnabled=true" — sets `mockTempoEnabled = true`, dynamic-imports Sidebar, renders, asserts `getByText('Worklogs')` is defined; (2) "D-06: hides Worklogs link when tempoEnabled=false" — sets `mockTempoEnabled = false`, dynamic-imports Sidebar, renders, asserts `queryByText('Worklogs')` is null. Use the same `QueryClientProvider` + `MemoryRouter` wrapper structure as the existing AIO tests. Each `it()` MUST `await import('./Sidebar')` AFTER setting the mock variable so the hoisted factory closure sees the updated value (matches existing pattern). Do not refactor the existing `renderSidebar` helper — inline the `mockTempoEnabled` assignment per test to keep the change additive.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- Sidebar.test.tsx --run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mockTempoEnabled" taskflow/src/components/app/Sidebar.test.tsx` returns >= 4 (declaration + state field + 2 assignments in tests)
    - `grep -c "tempoEnabled gate" taskflow/src/components/app/Sidebar.test.tsx` returns 1
    - `grep "id: 'worklogs', visible: true" taskflow/src/components/app/Sidebar.test.tsx` returns 1 match
    - `grep -c "D-06" taskflow/src/components/app/Sidebar.test.tsx` returns 2
    - `cd taskflow && npm test -- Sidebar.test.tsx --run` exits 0 with all tests passing (existing aioEnabled tests + 2 new tempoEnabled tests)
    - Both new tests show "Worklogs" as the assertion target (not "tempo" or other strings)
  </acceptance_criteria>
  <done>Sidebar test suite passes with the new tempoEnabled gate describe block in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create WorklogsPage stub + register /worklogs route (TEMPO-01 wiring)</name>
  <files>taskflow/src/routes/worklogs/WorklogsPage.tsx, taskflow/src/routes/routes.tsx</files>
  <read_first>
    - taskflow/src/routes/routes.tsx (full file, ~56 lines)
    - .planning/phases/62-tempo-worklog-viewer-ui/62-PATTERNS.md (routes.tsx section)
    - .planning/phases/62-tempo-worklog-viewer-ui/62-CONTEXT.md (D-05)
  </read_first>
  <behavior>
    - `/worklogs` route resolves; navigating to it renders a non-empty page without errors
    - The stub renders a `<h1>Worklogs</h1>` heading so the route can be smoke-tested before Plan 02
    - The stub exports a default function component named `WorklogsPage`
    - Plan 02 will overwrite this file with the full implementation; the stub MUST be replaceable without touching `routes.tsx` again
  </behavior>
  <action>
    Create new file `taskflow/src/routes/worklogs/WorklogsPage.tsx` containing a single `export default function WorklogsPage()` that returns a minimal flex-column layout: a `<header class="px-6 py-4 border-b border-border">` containing `<h1 class="text-xl font-semibold">Worklogs</h1>` and a placeholder `<div class="flex-1 px-6 py-4 text-sm text-muted-foreground">Plan 62-02 will implement this view.</div>`. No imports beyond what's necessary (no useEffect, no TanStack Query, no service calls — this is a stub). Use a header comment block stating "Plan 62-01 stub — replaced by Plan 62-02 with the full data-table implementation." The component MUST be a default export so the existing `lazy(() => import('./worklogs/WorklogsPage'))` pattern in routes.tsx works without rewrites.

    In `taskflow/src/routes/routes.tsx`: add `const WorklogsPage = lazy(() => import('./worklogs/WorklogsPage'));` as a new lazy declaration after the `AioTestRunDetailPage` declaration (current line 22) — keep the visual grouping with the other tracking-section lazy components. Add route entry `{ path: '/worklogs', element: withLazy(WorklogsPage) },` to the `routes` array immediately after the `/releases` entry (current line 43), matching the sidebar tracking-section order so the route layout mirrors the nav layout. Do NOT modify any other routes or the `withLazy` helper.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit 2>&1 | grep -E "(WorklogsPage|routes\.tsx)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `taskflow/src/routes/worklogs/WorklogsPage.tsx` exists
    - `grep -c "export default function WorklogsPage" taskflow/src/routes/worklogs/WorklogsPage.tsx` returns 1
    - `grep -c "Plan 62-01 stub" taskflow/src/routes/worklogs/WorklogsPage.tsx` returns >= 1 (so Plan 02 executor can spot the stub immediately)
    - `grep -c "Worklogs" taskflow/src/routes/worklogs/WorklogsPage.tsx` returns >= 1 (the h1 text)
    - `grep "lazy.*WorklogsPage" taskflow/src/routes/routes.tsx` returns 1 match
    - `grep "path: '/worklogs'" taskflow/src/routes/routes.tsx` returns 1 match
    - `grep "withLazy(WorklogsPage)" taskflow/src/routes/routes.tsx` returns 1 match
    - `cd taskflow && npx tsc --noEmit` exits 0
    - `cd taskflow && npm run build 2>&1 | tail -5` does not show any "Cannot find module" errors for WorklogsPage
  </acceptance_criteria>
  <done>/worklogs route resolves to the stub page; Plan 02 can overwrite the stub file without touching routes.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings store → Sidebar render | Untrusted `tempoEnabled` value (could be undefined post-migration) determines whether the Worklogs link is visible |
| client → /worklogs route | Browser navigation to /worklogs must resolve to a real component (not a broken lazy import) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Tampering | Sidebar `tempoEnabled` gate | mitigate | Use fine-grained selector that returns `undefined` cleanly when field absent; gate clause `!(nav.id === 'worklogs' && !tempoEnabled)` treats `undefined` as falsy (link hidden — fail-closed) |
| T-62-02 | Denial of Service | `/worklogs` lazy chunk | accept | `ChunkErrorBoundary` already wraps `withLazy()`; failed chunk loads degrade gracefully to error UI (existing pattern, no new mitigation needed) |
| T-62-03 | Information Disclosure | Sidebar default visibility | accept | Adding 'worklogs' to `devVisible`/`pmVisible` is safe — the `tempoEnabled` gate is the authoritative control; users who never enable Tempo never see the link |
</threat_model>

<verification>
- `cd taskflow && npm test -- Sidebar.test.tsx --run` — all existing + 2 new tests pass
- `cd taskflow && npx tsc --noEmit` — exit 0 (no type errors)
- `cd taskflow && npm run build 2>&1 | tail -5` — build completes without errors
- Manual smoke: open dev build, toggle `tempoEnabled` in Settings → Integrations, confirm sidebar link appears/disappears, confirm `/worklogs` renders the stub heading
</verification>

<success_criteria>
1. `taskflow/src/components/app/sidebar-items.ts` declares the `worklogs` nav item with `iconName: 'Clock'` and section `'tracking'`, and both default presets include it
2. `taskflow/src/components/app/Sidebar.tsx` imports `Clock`, exposes it via `ICON_MAP`, declares a fine-grained `tempoEnabled` selector, and includes the gate clause `!(nav.id === 'worklogs' && !tempoEnabled)` in `sectionedItems`
3. `taskflow/src/components/app/Sidebar.test.tsx` runs the new `tempoEnabled gate` describe block green alongside the existing `aioEnabled gate` tests
4. `taskflow/src/routes/worklogs/WorklogsPage.tsx` exists as a stub with a clear "Plan 62-01 stub" marker
5. `taskflow/src/routes/routes.tsx` registers `/worklogs` via `withLazy(WorklogsPage)`
6. `npm run build` and `npm test -- Sidebar` both pass
</success_criteria>

<output>
Create `.planning/phases/62-tempo-worklog-viewer-ui/62-01-SUMMARY.md` when done. Include: what was wired (sidebar item, gate, route stub), explicit confirmation that the stub file is ready for Plan 02 to overwrite, and any deviations from the plan.
</output>
