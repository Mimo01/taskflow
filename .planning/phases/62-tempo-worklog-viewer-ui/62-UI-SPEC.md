---
phase: 62
slug: tempo-worklog-viewer-ui
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-05-21
---

# Phase 62 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova (style: base-nova, base: neutral, menuColor: default, menuAccent: subtle) |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react (Clock icon for Worklogs sidebar item) |
| Font | Geist Variable (--font-sans, already loaded via @fontsource-variable/geist) |

Source: `taskflow/components.json` + `npx shadcn info`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, cell inner horizontal padding |
| sm | 8px | Filter bar item spacing, pill gap |
| md | 16px | Page horizontal padding, filter bar padding, table cell vertical padding |
| lg | 24px | Page vertical padding (top of content area), section separation |
| xl | 32px | — |
| 2xl | 48px | — |
| 3xl | 64px | — |

Exceptions:
- Table column header cells: 12px vertical padding (between sm and md) to match data-dense table density
- Date preset pill height: 28px (7 × 4) to sit within the filter bar without dominating

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 12px | 500 (medium) | 1.4 |
| Heading (page title) | 20px | 600 (semibold) | 1.2 |
| Table cell / data | 13px | 400 (regular) | 1.4 |

Notes:
- Page title "Worklogs" uses 20px/600 — consistent with other page headings in the app (e.g. Sprint Board, Backlog)
- Table cell data (hours, names) uses 13px/400 to maximise information density without crowding
- Column headers use 12px/500 label size
- "Total" label in totals row/column uses 12px/600 (semibold) to distinguish from data cells
- All type uses Geist Variable via `font-sans` (inherited from `<html>`)

Source: existing page patterns in `taskflow/src/routes/dashboard/` + shadcn base-nova defaults

---

## Color

| Role | Token | Light mode | Usage |
|------|-------|------------|-------|
| Dominant (60%) | `--background` | oklch(1 0 0) — white | Page background, empty cells |
| Secondary (30%) | `--card` / `--sidebar` | oklch(1 0 0) / oklch(0.985 0 0) | Filter bar background, table header row background (use `--muted`: oklch(0.97 0 0)), totals row/column background |
| Accent (10%) | `--accent` | oklch(0.97 0 0) | Active date preset pill background, selected person chip background |
| Accent foreground | `--accent-foreground` | oklch(0.205 0 0) | Text on active preset pill, selected chip text |
| Muted foreground | `--muted-foreground` | oklch(0.556 0 0) | Column header text, empty state subtitle, date input placeholder |
| Border | `--border` | oklch(0.922 0 0) | Table cell borders, filter bar border |
| Destructive | `--destructive` | oklch(0.577 0.245 27.325) | Not used in this phase (no destructive actions) |

Accent reserved for:
1. The currently-active date preset pill (e.g. "This Week" selected)
2. The selected person chip in the people filter (when a person is active)
3. NavLink active state in the sidebar (existing app-wide pattern — bg-accent text-accent-foreground)

Dark mode: all tokens above have dark equivalents already defined in `src/index.css`. No new color variables needed.

Source: `taskflow/src/index.css` CSS variable definitions; CONTEXT.md D-08 (zero cells blank)

---

## Component Inventory

Components to use (all existing — no new shadcn installs required unless noted):

| Component | Source | Usage |
|-----------|--------|-------|
| `Button` (variant="ghost", size="sm") | `@/components/ui/button` | Date preset pills (inactive state) |
| `Button` (variant="secondary", size="sm") | `@/components/ui/button` | Date preset pills (active state — bg-accent) |
| `Input` | `@/components/ui/input` | People filter text input; custom date range `<input type="date">` fields |
| `Skeleton` | `@/components/ui/skeleton` | Loading skeleton for table cells during fetch |
| `EmptyState` | `@/components/ui/empty-state` | Zero-worklog state |
| `ErrorState` | `@/components/ui/error-state` | Fetch error state |
| `Badge` (variant="secondary") | `@/components/ui/badge` | Active person chip (dismissible) in filter bar |
| HTML `<table>` | native | Worklog grid — native table semantics; styled with Tailwind |

**shadcn installs for this phase:** none. All required components are already present.

Note on MultiFilterCombobox: The people filter reuses the autocomplete dropdown pattern from `BacklogFilterBar.tsx` but adapted to single-select. Implement inline in the WorklogsPage — do not extract to a shared component in this phase.

---

## Layout Structure

```
<WorklogsPage>
  ├── <header class="px-6 py-4 border-b border-border">
  │     ├── <h1>Worklogs</h1>          20px/600
  │     └── [optional subtitle / date span summary]
  │
  ├── <div class="flex items-center gap-2 px-6 py-3 border-b border-border bg-background">  ← Filter Bar
  │     ├── [Date preset pills: "This Week" | "Last Week" | "This Month" | "Last Month" | "Last Working Day" | "Custom"]
  │     │     └── When "Custom" active: <input type="date" /> from — <input type="date" /> to (inline, same row)
  │     └── [People filter combobox: text input + dropdown + active chip]
  │
  └── <div class="flex-1 overflow-auto px-6 py-4">  ← Table Area
        ├── [Loading: skeleton grid]
        ├── [Error: <ErrorState />]
        ├── [Empty: <EmptyState />]
        └── <table class="w-full text-sm border-collapse">
              ├── <thead> — sticky top-0, bg-muted
              │     └── <tr> Name | Day1 | Day2 | … | DayN | Total
              ├── <tbody>
              │     └── <tr> per person: displayName | h/blank | … | Total
              └── <tfoot> — sticky or appended last row
                    └── <tr class="bg-muted"> Total | sum/day | … | grand total
```

Source: CONTEXT.md layout decisions + existing dashboard page layout patterns

---

## Interaction Contracts

### Date Preset Bar

- Rendered as a horizontal row of pill buttons
- Default active on mount: "This Week"
- Only one preset active at a time; clicking a preset deactivates the previous
- Active pill: `bg-accent text-accent-foreground font-medium` border `border-border`
- Inactive pill: `ghost` button, `hover:bg-accent`
- When "Custom" is active: two `<input type="date">` fields appear inline to the right of the pills
  - Format: `YYYY-MM-DD` (native HTML date input)
  - Fetch fires when both `from` and `to` are filled; `to >= from` is required
  - Fields have a minimum width of 128px each

### People Filter

- Text input (ARIA role="combobox") with autocomplete dropdown below
- Placeholder: "Filter by person"
- Dropdown appears on focus; shows all names if query is empty
- Each dropdown item: displayName, 100% width, `hover:bg-accent` on hover, selects on click
- On selection: input clears, selected name appears as a dismissible chip (Badge variant="secondary") to the left of the input
- X button on chip clears the filter (resets to "all people shown")
- Only one person active at a time (single-select, CONTEXT.md D-01)
- When no person is selected: all rows shown; no chip displayed
- Dropdown closes on blur (150ms debounce, same as MultiFilterCombobox pattern)

### Table

- Column headers: sticky `top-0` with `bg-muted` background
- "Name" column: fixed min-width 160px, left-aligned
- Day columns: min-width 56px, right-aligned, numeric content
- "Total" column: rightmost, min-width 72px, right-aligned, `font-medium`
- Data cells: right-aligned numbers in `Xh Ym` format; blank (empty string) for zero hours (CONTEXT.md D-07, D-08)
- Totals row: last row, `bg-muted`, `font-medium` on all cells
- Row hover: `hover:bg-accent/50` (subtle, same as existing table patterns)
- Table is horizontally scrollable when date range produces more columns than viewport width (`overflow-x: auto` on wrapper)

### Loading State

- Skeleton grid: show 5 skeleton rows × number of columns in the selected date range (capped at 7 for initial estimate)
- Skeleton cells: `<Skeleton className="h-4 w-10" />` per cell, centered

### Empty State

- Icon: `Clock` (Lucide, 48px, `text-muted-foreground`)
- Title: "No worklogs found"
- Subtitle: "No hours were logged in the selected date range." (or "No hours logged for {displayName} in the selected period." when person filter is active)
- No action button

### Error State

- Use existing `<ErrorState>` component: `viewName="worklogs"`
- Auth error: shows "Session expired" with Reconnect button → `/settings`
- General error: shows "Couldn't load worklogs" with Retry button

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | none — page is read-only; no primary action |
| Page heading | "Worklogs" |
| Sidebar link label | "Worklogs" (CONTEXT.md D-05) |
| Date preset: This Week | "This Week" |
| Date preset: Last Week | "Last Week" |
| Date preset: This Month | "This Month" |
| Date preset: Last Month | "Last Month" |
| Date preset: Last Working Day | "Last Working Day" |
| Date preset: Custom | "Custom" |
| People filter placeholder | "Filter by person" |
| People filter label (sr-only) | "Filter by person" |
| Totals column header | "Total" |
| Totals row name cell | "Total" |
| Empty state heading | "No worklogs found" |
| Empty state body (no filter) | "No hours were logged in the selected date range." |
| Empty state body (person filter active) | "No hours logged for {displayName} in the selected period." |
| Error state view name | "worklogs" (feeds into "Couldn't load worklogs" in ErrorState) |
| Destructive confirmation | not applicable — no destructive actions in this phase |

Source: CONTEXT.md §Specific Ideas (Totals labels), REQUIREMENTS.md TEMPO-01 through TEMPO-03

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — no new installs this phase | not required |

No third-party registries declared. Safety gate not applicable.

Source: component inventory above — all components already installed in project

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
