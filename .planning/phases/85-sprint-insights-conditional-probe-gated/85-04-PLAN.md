---
phase: 85-sprint-insights-conditional-probe-gated
plan: 04
type: execute
wave: 2
depends_on: [85-01, 85-02]
files_modified:
  - taskflow/src/routes/dashboard/BurndownChart.tsx
  - taskflow/src/routes/dashboard/index.tsx
autonomous: true
requirements: [INSIGHT-02]

user_setup: []

must_haves:
  truths:
    - "User sees a Sprint Burndown chart for the active sprint at the bottom of the Dashboard"
    - "The burndown Y-axis is labelled in hours (h suffix), not story points"
    - "The burndown tooltip shows remaining time formatted as h+m via formatHoursMinutes"
    - "A burndown endpoint failure shows the burndown card's own error/retry state and never blanks velocity or other Dashboard sections"
    - "boardId is resolved via useBoardId (never the literal 6708) and passed as a prop"
  artifacts:
    - path: "taskflow/src/routes/dashboard/BurndownChart.tsx"
      provides: "Active-sprint burndown AreaChart via ChartWrapper (INSIGHT-02, D-07/D-08/D-09/D-10/D-11)"
      min_lines: 90
    - path: "taskflow/src/routes/dashboard/index.tsx"
      provides: "Sprint Insights row appended at bottom rendering VelocityChart + BurndownChart in a grid-cols-1 lg:grid-cols-2 gap-4 row, with boardId + activeSprintId props"
      contains: "BurndownChart"
  key_links:
    - from: "taskflow/src/routes/dashboard/BurndownChart.tsx"
      to: "greenhopperFetch (rapid-charts path, apiPath empty-string override)"
      via: "useQuery queryFn"
      pattern: "scopechangeburndownchart"
    - from: "taskflow/src/routes/dashboard/BurndownChart.tsx"
      to: "parseBurndownChanges + formatHoursMinutes (./dashboardMetrics, from 85-01)"
      via: "import"
      pattern: "parseBurndownChanges"
    - from: "taskflow/src/routes/dashboard/index.tsx"
      to: "BurndownChart + VelocityChart"
      via: "Sprint Insights section render with boardId/activeSprintId props"
      pattern: "grid-cols-1 lg:grid-cols-2 gap-4"
---

<objective>
Build the active-sprint Sprint Burndown chart (INSIGHT-02) and wire BOTH Sprint Insights
sections into the Dashboard.

Create BurndownChart.tsx — an AreaChart of hours-remaining-vs-ideal for the app's current
active sprint, fed by the GreenHopper scopechangeburndownchart endpoint. The unit is HOURS
(Probe C statisticField=timeestimate), NOT story points. The query uses a standard
staleTime of 30_000 (D-09 — active-sprint data changes mid-sprint) and degrades independently:
an endpoint failure shows this card's own error/retry state without unmounting velocity or any
other Dashboard section.

Then wire index.tsx: append a single Sprint Insights row at the BOTTOM of the Dashboard
(after Phase 84's Activity & Releases) rendering VelocityChart (from 85-03) and BurndownChart
side-by-side in a grid-cols-1 lg:grid-cols-2 gap-4 row. Resolve boardId via the existing
useBoardId hook (already available at index.tsx; never hardcode 6708) and resolve the active
sprint id via a fetchActiveSprint query that reuses the SAME cache key as SprintHealthSection
(so it dedups, zero extra network), passing both as props (D-16 props-only).

Purpose: complete INSIGHT-02 and make both probe-gated insight charts visible.
Output: BurndownChart.tsx; extended index.tsx with the Sprint Insights row.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/85-sprint-insights-conditional-probe-gated/85-CONTEXT.md
@.planning/phases/85-sprint-insights-conditional-probe-gated/85-UI-SPEC.md
@.planning/phases/85-sprint-insights-conditional-probe-gated/85-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BurndownChart.tsx (INSIGHT-02 — hours-remaining AreaChart)</name>
  <read_first>
    Target plus analogs (read each once, extract patterns in one pass):
    - taskflow/src/routes/dashboard/WeeklyTrendChart.tsx — EXACT analog: the 'use no memo' directive (line 1), props-only interface (no useAuthStore), useDelayedLoading for skeleton gating, token-NOT-in-queryKey invariant (T-84-02, lines ~95-106), the responsive prop on the chart (never ResponsiveContainer), isAnimationActive=false, explicit-height inner div (WebKit 0x0 guard), role="region" + aria-label wrapper, and the local formatHoursMinutes(hours) helper (lines 76-83 — NOTE: 85-01 extracts this to dashboardMetrics.ts; import it from ./dashboardMetrics, do not redefine).
    - taskflow/src/components/chart-wrapper.tsx — ChartWrapper API: state precedence error > isLoading > isEmpty > children; isEmpty prop, onRetry, height default 240, error prop.
    - taskflow/src/components/ui/chart.tsx — ChartContainer + ChartConfig type.
    - taskflow/src/services/jira/greenhopper/client.ts — greenhopperFetch(baseUrl, token, path, operation, apiPath?, init?); url = baseUrl + apiPath + path; passing apiPath as empty string yields baseUrl+path (correct). greenhopperFetch is imported DIRECTLY from @/services/jira/greenhopper/client (it is NOT re-exported from the jira.ts barrel).
    - taskflow/src/routes/dashboard/dashboardMetrics.ts — parseBurndownChanges, the BurndownPoint interface, the GreenHopperBurndown response type, and formatHoursMinutes ALL land here in 85-01; import them from ./dashboardMetrics. Confirm their exact exported names before importing.
    - .planning/phases/85-sprint-insights-conditional-probe-gated/85-UI-SPEC.md — the BurndownChart (INSIGHT-02) chart spec + Copywriting Contract (title "Sprint Burndown", description "Hours remaining · active sprint · time estimate", empty/error copy, legend "Remaining"/"Ideal") + Accessibility (role="region" aria-label "Sprint burndown chart"; ChartContainer aria-label "Sprint burndown area chart — hours remaining over sprint timeline").
    - taskflow/src/index.css — the --chart-3 and --muted-foreground tokens.
    - .planning/phases/85-sprint-insights-conditional-probe-gated/85-PATTERNS.md — the BurndownChart.tsx section for the full assembled render skeleton.
  </read_first>
  <action>
    Create taskflow/src/routes/dashboard/BurndownChart.tsx as a default-exported React component
    named BurndownChart. Begin the file with the 'use no memo' directive (Phase 81 D-02 — React
    Compiler conflict with the Recharts responsive prop). Add the mandatory D-10 probe-outcome
    comment near the top: "Probe C PASSED 2026-06-15: scopechangeburndownchart returns .changes +
    .workRateData. statisticField=timeestimate — Y-axis is hours remaining, NOT story points. Build
    is unconditional; error state handles runtime absence."

    Define a props-only interface BurndownChartProps with jiraBaseUrl (string), jiraToken (string),
    boardId (number | null), activeSprintId (number | null). No auth-store reads inside the component
    (D-16 props-only).

    Fetch the burndown with useQuery: queryKey set to the array of 'jira-burndown', boardId,
    activeSprintId (token absent from the key per T-84-02 — token lives in the queryFn closure only).
    In the async queryFn call greenhopperFetch with jiraBaseUrl, jiraToken, the rapid-charts path
    /rest/greenhopper/1.0/rapid/charts/scopechangeburndownchart with rapidViewId set to boardId and
    sprintId set to activeSprintId as query params, the operation label 'Load Sprint Burndown', and an
    empty string as the fifth apiPath-override argument (rapid-charts root differs from
    GREENHOPPER_API_PATH /xboard; per D-08 / Pitfall 2 any other value doubles the prefix and 404s).
    Throw on a non-ok response so TanStack Query surfaces it via error. Set staleTime to 30_000 (D-09 —
    explicitly NOT Infinity; the active-sprint burndown changes during the sprint) and enabled to the
    conjunction of a truthy jiraToken, boardId not null, and activeSprintId not null. Capture refetch
    for retry.

    Derive the series defensively (V5 input validation): compute burndownPoints by calling
    parseBurndownChanges with (burndownRaw.changes defaulted to an empty object) and
    burndownRaw.startTime when burndownRaw exists, else an empty array. parseBurndownChanges (from
    85-01) already clamps remaining to at least 0. Compute hasBurndownData as burndownPoints.length
    greater than 0.

    Render: outer div with role="region" and aria-label="Sprint burndown chart" wrapping a
    ChartWrapper with title="Sprint Burndown", description="Hours remaining · active sprint · time
    estimate", height=240, isLoading=showSkeleton, error=error, isEmpty set to the negation of
    hasBurndownData, and onRetry=refetch — where showSkeleton = useDelayedLoading(isLoading). Inside
    ChartWrapper place the explicit-height guard (a div with inline style height 240 and className
    "w-full") containing a ChartContainer (config=chartConfig, className "h-full w-full",
    aria-label="Sprint burndown area chart — hours remaining over sprint timeline") and an AreaChart
    with data=burndownPoints and the responsive prop (never ResponsiveContainer). AreaChart children:
    - XAxis with dataKey="t", a tickFormatter formatting the epoch-ms timestamp as en-GB short month +
      numeric day, tick fontSize 11, and interval 2 (show roughly every 3rd tick to avoid crowding).
    - YAxis with a tickFormatter that returns the numeric value followed by the literal lowercase h
      suffix, domain set to the tuple of 0 and 'auto', tick fontSize 11 — the h suffix is mandatory
      (HOURS, Probe C — never an SP/points label).
    - Tooltip with a formatter routing the value through formatHoursMinutes and a labelFormatter
      formatting the epoch-ms timestamp the same way as the XAxis. NOTE: if 85-01's parseBurndownChanges
      returns remaining in SECONDS, divide by 3600 inside the formatter; if it already returns HOURS,
      pass the value through directly. Confirm the unit from 85-01's BurndownPoint interface/function
      comment before finalizing this line and match it.
    - An Area: type "monotone", dataKey "remaining", name "Remaining", stroke and fill both
      var(--chart-3), fillOpacity 0.2, isAnimationActive false.
    - A Line: type "monotone", dataKey "ideal", name "Ideal", stroke var(--muted-foreground),
      strokeDasharray "4 4", dot false, isAnimationActive false — the dashed guideline uses
      --muted-foreground (NOT a chart token, keeps accent reserved for data series). The ideal field is
      the .workRateData-derived value on each point (provided by 85-01's parseBurndownChanges; if the
      field is named differently, match the interface).
    Declare chartConfig as an object mapping remaining to { label: 'Remaining', color: 'var(--chart-3)' }
    with the satisfies ChartConfig assertion. Use empty/error copy from the UI-SPEC Copywriting Contract
    (ChartWrapper handles both; pass viewName "Sprint Burndown" where the wrapper expects it).

    Write idiomatic TSX matching WeeklyTrendChart.tsx.
  </action>
  <acceptance_criteria>
    - File taskflow/src/routes/dashboard/BurndownChart.tsx exists, exports a BurndownChart component, and begins with the 'use no memo' directive.
    - The Y-axis tickFormatter output ends in a lowercase h (hours) and there is no SP/points axis label anywhere in the file.
    - Tooltip formatting routes through formatHoursMinutes (imported from ./dashboardMetrics).
    - The useQuery uses staleTime 30_000, NOT Infinity.
    - The chart uses the responsive prop; ResponsiveContainer is NOT imported.
    - Every Area/Line element sets isAnimationActive to false.
    - Colors use var(--chart-3) (remaining) and var(--muted-foreground) (ideal guideline) — no hardcoded hex.
    - greenhopperFetch is called with the full /rest/greenhopper/1.0/rapid/charts/scopechangeburndownchart path and an empty string as the apiPath override argument.
    - boardId/activeSprintId arrive as props; no literal 6708 and no useAuthStore/readSecret in the file.
    - The burndownRaw.changes empty-object fallback is present (V5); error surfaces via ChartWrapper error/onRetry (independent degradation).
    - The D-10 probe-outcome comment is present.
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && grep -q "use no memo" src/routes/dashboard/BurndownChart.tsx && grep -q "staleTime: 30_000" src/routes/dashboard/BurndownChart.tsx && ! grep -q "staleTime: Infinity" src/routes/dashboard/BurndownChart.tsx && ! grep -q "ResponsiveContainer" src/routes/dashboard/BurndownChart.tsx && grep -q "var(--chart-3)" src/routes/dashboard/BurndownChart.tsx && grep -q "var(--muted-foreground)" src/routes/dashboard/BurndownChart.tsx && grep -q "scopechangeburndownchart" src/routes/dashboard/BurndownChart.tsx && ! grep -q "6708" src/routes/dashboard/BurndownChart.tsx && grep -q "parseBurndownChanges" src/routes/dashboard/BurndownChart.tsx && grep -q "formatHoursMinutes" src/routes/dashboard/BurndownChart.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>BurndownChart.tsx renders an hours-remaining AreaChart from scopechangeburndownchart with a dashed ideal guideline, standard 30s staleTime, isolated error/retry, the locked Phase 81 chart stack, and the D-10 comment; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Sprint Insights row into Dashboard index.tsx</name>
  <read_first>
    - taskflow/src/routes/dashboard/index.tsx — read in full once. Key anchors: useBoardId is already called at ~line 82 yielding boardId. The additive two-column section rows are the "Sprint health + Weekly trend" row (~lines 219-236) and "Activity & Releases" row (~lines 238-261), both a div className="relative px-6 pb-6" wrapping a div className="grid grid-cols-1 lg:grid-cols-2 gap-4". The root element closes at ~line 262. The existing useQuery/useDelayedLoading/fetchActiveSprint imports and the auth-derived values (jiraBaseUrl, jiraToken, jiraUserDisplayName, storyPointsFieldKey, activeJiraProject) already exist in scope.
    - taskflow/src/routes/dashboard/SprintHealthSection.tsx — lines ~79-90: the active-sprint query uses queryKey of the array ['jira-active-sprint', activeJiraProject, jiraBaseUrl, boardId], queryFn fetchActiveSprint(jiraBaseUrl, jiraToken, activeJiraProject, boardId ?? undefined), staleTime 5 * 60_000, enabled the conjunction of truthy jiraBaseUrl, jiraToken, activeJiraProject and boardId not null. REUSE this exact key/queryFn/staleTime/enabled in index.tsx so the query dedups (zero extra network) and activeSprint.id becomes the burndown sprint id.
    - taskflow/src/services/jira.ts ~line 1341 — fetchActiveSprint(baseUrl, token, projectKey, boardId?) returns JiraActiveSprint or null; passing the resolved boardId skips re-discovery.
    - .planning/phases/85-sprint-insights-conditional-probe-gated/85-UI-SPEC.md — the Dashboard Layout section (the exact section markup + comment) and the VelocityChart prop list.
    - .planning/phases/85-sprint-insights-conditional-probe-gated/85-PATTERNS.md — the index.tsx section for the prop wiring and import-grouping guidance.
  </read_first>
  <action>
    Edit taskflow/src/routes/dashboard/index.tsx to append the Sprint Insights row and resolve the
    props it needs. Add imports for VelocityChart (default import from ./VelocityChart) and BurndownChart
    (default import from ./BurndownChart), following the existing local-component import grouping.
    VelocityChart.tsx is created by 85-03 in the same wave — import it by path; do not redefine it here,
    and do NOT create or modify VelocityChart.tsx in this plan.

    Resolve the active sprint id at the index level by adding a useQuery that reuses
    SprintHealthSection's cache identity exactly: queryKey the array ['jira-active-sprint',
    activeJiraProject, jiraBaseUrl, boardId], queryFn calling fetchActiveSprint with (jiraBaseUrl or
    empty string, jiraToken or empty string, activeJiraProject or empty string, boardId ?? undefined),
    staleTime 5 * 60_000, and enabled the conjunction of truthy jiraBaseUrl, jiraToken, activeJiraProject
    and boardId not null. Name the result something like activeSprintForBurndown and derive
    activeSprintId as activeSprintForBurndown.id ?? null. Add a short comment noting this shares the
    SprintHealthSection / Sidebar-prefetch key and so fires zero extra network calls. Ensure
    fetchActiveSprint is imported (add it to the existing @/services/jira import if not already present).

    Immediately after the closing div of the "Activity & Releases" row (and before the root element's
    closing div), insert a div className="relative px-6 pb-6" wrapping a div className="grid
    grid-cols-1 lg:grid-cols-2 gap-4", prefixed by the comment marking it as Sprint Insights —
    INSIGHT-01 / INSIGHT-02. Inside the grid render BOTH cards:
    - VelocityChart with jiraBaseUrl (jiraBaseUrl or empty string), jiraToken (jiraToken or empty
      string), jiraUserDisplayName (jiraUserDisplayName or empty string), boardId (boardId),
      storyPointsFieldKey (storyPointsFieldKey), and activeJiraProject (activeJiraProject or empty
      string). Match the prop names 85-03 defines on VelocityChartProps; if 85-03 omits activeJiraProject,
      drop it.
    - BurndownChart with jiraBaseUrl (jiraBaseUrl or empty string), jiraToken (jiraToken or empty
      string), boardId (boardId), and activeSprintId (activeSprintId).
    boardId MUST come from the existing useBoardId result (never the literal 6708). Each card owns its
    loading/error/empty state, so the two-column grid always renders and one card's failure never
    affects the other or the rest of the Dashboard (criterion 4 / D-09).
  </action>
  <acceptance_criteria>
    - index.tsx imports and renders both VelocityChart and BurndownChart.
    - The Sprint Insights div className="relative px-6 pb-6" block appears AFTER the Activity & Releases row and BEFORE the root closing div (it is the last section in the page body).
    - The two charts sit in a single grid grid-cols-1 lg:grid-cols-2 gap-4 row.
    - BurndownChart receives boardId from useBoardId (no literal 6708 anywhere in index.tsx) and activeSprintId derived from a fetchActiveSprint query that reuses queryKey ['jira-active-sprint', activeJiraProject, jiraBaseUrl, boardId] (cache-dedup with SprintHealthSection).
    - VelocityChart and BurndownChart are passed only props (no shared mutable state); a failure in one does not unmount the other.
    - tsc --noEmit is clean and the existing test suite still passes.
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && grep -q "from './BurndownChart'" src/routes/dashboard/index.tsx && grep -q "from './VelocityChart'" src/routes/dashboard/index.tsx && grep -q "Sprint Insights" src/routes/dashboard/index.tsx && grep -q "jira-active-sprint" src/routes/dashboard/index.tsx && ! grep -q "6708" src/routes/dashboard/index.tsx && npx tsc --noEmit -p tsconfig.json && npm test</automated>
  </verify>
  <done>index.tsx renders the Sprint Insights row (VelocityChart + BurndownChart) at the bottom in a two-column grid, with boardId from useBoardId and activeSprintId resolved via a cache-deduped fetchActiveSprint query; tsc clean and suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jira DC GreenHopper REST -> client | scopechangeburndownchart returns external `.changes`/`.workRateData`/`startTime` data parsed by the browser |
| index.tsx props -> chart components | boardId/activeSprintId flow from useBoardId + active-sprint cache into the burndown query URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-04-01 | Tampering | parseBurndownChanges input (`.changes`, `.startTime`) in BurndownChart.tsx | mitigate | V5 defensive parsing: default `burndownRaw.changes` to `{}` before parse; 85-01's parseBurndownChanges uses `?? 0` per-entry and Math.max(0, remaining) clamp so malformed/negative values cannot crash or render below zero |
| T-85-04-02 | Spoofing | burndown request URL (rapidViewId/sprintId) | mitigate | boardId derived from useBoardId, activeSprintId from the shared active-sprint cache — never hardcoded (no literal 6708); query disabled until both resolve (enabled guard) |
| T-85-04-03 | Denial of Service (section blanking) | burndown query failure mid-session | mitigate | standard staleTime 30_000 + ChartWrapper error/onRetry isolate the failure to this card; props-only design means a burndown error never unmounts VelocityChart or other Dashboard sections (D-09 / criterion 4) |
| T-85-04-04 | Information Disclosure | PAT token in TanStack Query cache/devtools | mitigate | token kept in the queryFn closure only, never in queryKey (T-84-02) |

No new package installs in this plan; no T-85-04-SC supply-chain checkpoint required. No high-severity unmitigated threats.
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit -p tsconfig.json` is clean after both tasks.
- `cd taskflow && npm test` passes (85-01's dashboardMetrics tests for parseBurndownChanges remain green; no regressions).
- Grep gates in each task's `<automated>` block confirm: hours Y-axis (no SP label), staleTime 30_000 not Infinity, responsive (no ResponsiveContainer), var(--chart-3)/var(--muted-foreground) colors, scopechangeburndownchart path, no literal 6708, parseBurndownChanges + formatHoursMinutes wired, both chart imports + Sprint Insights section + shared active-sprint queryKey present in index.tsx.
- Manual (UAT, out of scope for automated gate): with the burndown endpoint unreachable mid-session, the burndown card shows its own error/retry and VelocityChart + other Dashboard sections still render (independent degradation).
</verification>

<success_criteria>
- BurndownChart.tsx renders an active-sprint hours-remaining AreaChart (Y-axis in hours, dashed ideal guideline) via ChartWrapper using the locked Phase 81 chart stack, with standard 30s staleTime and isolated error/retry (INSIGHT-02, D-07/D-08/D-09/D-11).
- The burndown fetch uses greenhopperFetch with the full rapid-charts path and the apiPath empty-string override (D-08); input is defensively parsed (V5).
- The D-10 probe-outcome comment is present in BurndownChart.tsx.
- index.tsx renders both Sprint Insights cards at the bottom in a grid-cols-1 lg:grid-cols-2 gap-4 row, with boardId from useBoardId (no hardcoded 6708) and activeSprintId from a cache-deduped fetchActiveSprint query, props-only (D-16).
- tsc clean; test suite green.
</success_criteria>

<output>
Create `.planning/phases/85-sprint-insights-conditional-probe-gated/85-04-SUMMARY.md` when done
</output>
