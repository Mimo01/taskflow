---
phase: 85-sprint-insights-conditional-probe-gated
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/dashboardMetrics.ts
  - taskflow/src/routes/dashboard/dashboardMetrics.test.ts
  - taskflow/src/routes/dashboard/WeeklyTrendChart.tsx
autonomous: true
requirements: [INSIGHT-01, INSIGHT-02]

must_haves:
  truths:
    - "computePersonalVelocitySeries selects the LAST N sprints from an ascending list, never the first N (Probe A landmine guard)"
    - "Velocity SP sums exclude subtasks (parent 5 + 2 subtasks of 2 = 5, not 9) per D-04"
    - "Velocity SP sums exclude issues assigned to other users (displayName equality) per D-01"
    - "committed = sum of ALL my non-subtask SP; completed = sum of my DONE non-subtask SP per D-03"
    - "A sprint with 0 committed+completed is not 'qualifying' (drives the <3 guard) per D-06"
    - "parseBurndownChanges returns ascending-timestamp points anchored at startTime, clamped non-negative (V5 defensive parse) per INSIGHT-02"
    - "An hours formatter emits an 'h'/'m' suffix (never 'SP') for the burndown axis/tooltip per Probe C"
  artifacts:
    - path: "taskflow/src/routes/dashboard/dashboardMetrics.ts"
      provides: "computePersonalVelocitySeries, parseBurndownChanges, exported formatHoursMinutes; VelocityPoint + BurndownPoint interfaces"
      contains: "export function computePersonalVelocitySeries"
    - path: "taskflow/src/routes/dashboard/dashboardMetrics.test.ts"
      provides: "7 mandated Vitest unit tests across describe('computePersonalVelocitySeries') + describe('parseBurndownChanges') + hours-formatter"
      contains: "describe('computePersonalVelocitySeries'"
  key_links:
    - from: "taskflow/src/routes/dashboard/WeeklyTrendChart.tsx"
      to: "dashboardMetrics.formatHoursMinutes"
      via: "import (formatter extracted to single source of truth, re-used by burndown later)"
      pattern: "import.*formatHoursMinutes.*dashboardMetrics"
    - from: "taskflow/src/routes/dashboard/dashboardMetrics.test.ts"
      to: "dashboardMetrics.computePersonalVelocitySeries"
      via: "import + makeIssue factory"
      pattern: "computePersonalVelocitySeries"
---

<objective>
Add the two pure derivation functions that all Phase 85 UI will consume — `computePersonalVelocitySeries` (INSIGHT-01) and `parseBurndownChanges` (INSIGHT-02) — to `dashboardMetrics.ts`, and land ALL 7 mandated Vitest unit tests from the VALIDATION map. This is Wave 0: the test scaffold and pure logic must exist and be green before any chart component (85-03, 85-04) or barrel fetcher (85-02) consumes these functions.

Purpose: The closed-sprint ordering landmine (Probe A — ascending list) and the malformed-GreenHopper-input risk (V5) are both pure-logic concerns best locked down by unit tests before UI exists. Extracting `formatHoursMinutes` to `dashboardMetrics.ts` gives the burndown axis/tooltip a single tested source of truth (hours, not story points — Probe C `statisticField: timeestimate`).

Output: Extended `dashboardMetrics.ts` (2 new exported functions + 2 interfaces + 1 extracted/exported formatter), `WeeklyTrendChart.tsx` updated to import the extracted formatter, and `dashboardMetrics.test.ts` with the 7 mandated tests green.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/85-sprint-insights-conditional-probe-gated/85-CONTEXT.md
@.planning/phases/85-sprint-insights-conditional-probe-gated/85-RESEARCH.md
@.planning/phases/85-sprint-insights-conditional-probe-gated/85-PATTERNS.md
@.planning/phases/85-sprint-insights-conditional-probe-gated/85-VALIDATION.md

# The files being modified + their in-file analogs:
@taskflow/src/routes/dashboard/dashboardMetrics.ts
@taskflow/src/routes/dashboard/dashboardMetrics.test.ts
@taskflow/src/routes/dashboard/WeeklyTrendChart.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add computePersonalVelocitySeries + extract formatHoursMinutes (INSIGHT-01)</name>
  <read_first>
    - taskflow/src/routes/dashboard/dashboardMetrics.ts — the file being extended. Study its in-file analogs:
      `computePersonalTileCounts` (lines 64–85, the `!i.fields.issuetype.subtask && i.fields.assignee?.displayName === displayName` personal+subtask filter — D-01/D-04), `computeSpDone` (lines 36–40, the `statusCategory?.key === 'done'` + `(i.fields[spKey] ?? 0)` reduce), `computeSpTotal` (lines 46–51). The `JiraActiveSprint` type (id/name/state) and `JiraIssue` are imported from `@/services/jira`.
    - taskflow/src/routes/dashboard/WeeklyTrendChart.tsx (lines 74–83) — the private `formatHoursMinutes(hours: number): string` to extract verbatim ("1.5 → 1h 30m", "8 → 8h", "0.25 → 15m"); update its import site (used at lines ~116, ~197).
  </read_first>
  <behavior>
    - Tail-first: given the LAST 6 of an ascending sprint list, output sprintNames map 1:1 in order (the slicing happens in the 85-02 fetcher; this fn maps whatever sprints it is handed, so the test passes a pre-sliced tail and asserts ['Sprint 5'..'Sprint 10']).
    - Subtask exclusion: parent(5,done) + 2 subtasks(2,done each) → committed=5, completed=5 (not 9).
    - Personal filter: my issue(8) + Bob's issue(10) → committed=8, completed=8 (Bob excluded).
    - committed vs completed split: my done(3) + my in-progress(5) → committed=8, completed=3.
    - Qualifying: a sprint whose issues sum to 0 committed+completed yields `{committed:0, completed:0}` so callers can filter it out (drives the D-06 <3 guard).
  </behavior>
  <files>taskflow/src/routes/dashboard/dashboardMetrics.ts, taskflow/src/routes/dashboard/WeeklyTrendChart.tsx</files>
  <action>
    In `dashboardMetrics.ts`, append a Phase 85 section after `mergeActivityEntries` (line 243). Export `interface VelocityPoint { sprintName: string; committed: number; completed: number; }`.

    Export `computePersonalVelocitySeries(sprints: JiraActiveSprint[], issuesBySprint: Map<number, JiraIssue[]>, displayName: string, spKey: string): VelocityPoint[]`. For each sprint: look up `issuesBySprint.get(sprint.id) ?? []`; build `myNonSubtasks = issues.filter((i) => !i.fields.issuetype.subtask && i.fields.assignee?.displayName === displayName)` (lift the exact predicate from `computePersonalTileCounts` for D-01 + D-04). Compute `committed` as `myNonSubtasks.reduce((sum, i) => sum + ((i.fields[spKey] as number | null | undefined) ?? 0), 0)` (the `?? 0` is the null-SP guard from D-04). Compute `completed` by first `.filter((i) => i.fields.status.statusCategory?.key === 'done')` then the same reduce (mirror `computeSpDone`). Return `{ sprintName: sprint.name, committed, completed }`.

    Place this MANDATED D-03 inline comment immediately above the `committed` reduce, verbatim: `// "Committed" here = my final-assigned sprint scope, NOT start-of-sprint commitment — mid-sprint scope additions and assignee changes are not captured. Acceptable approximation for a personal trend (probe confirmed 2026-06-15).`

    Do NOT compute the <3 guard here — return one VelocityPoint per input sprint; callers filter `committed > 0 || completed > 0` to determine qualifying sprints. Do NOT slice/reorder sprints here — tail selection is the 85-02 fetcher's job (Probe A landmine); this fn must map sprints in the order received so the tail-ordering test is meaningful.

    Extract `formatHoursMinutes` from `WeeklyTrendChart.tsx` (lines 76–83) into `dashboardMetrics.ts` as an exported function (verbatim body), with a JSDoc noting it is the single source of truth for the Phase 85 burndown hours axis/tooltip. In `WeeklyTrendChart.tsx`, delete the local definition and add `formatHoursMinutes` to the existing `import { ... } from './dashboardMetrics'` (or add such an import); leave its two call sites unchanged. Reason: the burndown chart (85-04) must reuse one tested formatter, not re-hand-roll one (Probe C: unit is hours, never SP).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/dashboardMetrics.test.ts -t "computePersonalVelocitySeries" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "dashboardMetrics|WeeklyTrendChart" || echo "no type errors in touched files"</automated>
  </verify>
  <done>`computePersonalVelocitySeries` and exported `formatHoursMinutes` exist in dashboardMetrics.ts; the D-03 comment is present verbatim above the committed reduce; WeeklyTrendChart.tsx imports the extracted formatter (no local copy remains); all `computePersonalVelocitySeries` tests pass; no new tsc errors in the two touched files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add parseBurndownChanges with V5 defensive parsing (INSIGHT-02)</name>
  <read_first>
    - taskflow/src/routes/dashboard/dashboardMetrics.ts — append after Task 1's velocity section. Reuse the module's existing `(value ?? 0)` defensive idiom seen in `computeSpDone`/`computeDonutData`.
    - 85-RESEARCH.md Pattern 5 + Assumptions A2/A4 (the `.changes` entry shape `{ key, statC: { newValue, oldValue }, added }` is [ASSUMED]; values are seconds via `statisticField: timeestimate`). 85-VALIDATION.md Test 5 is the binding contract for this function's behavior.
    - 85-RESEARCH.md Security Domain (V5 Input Validation): `changes ?? {}`, `?? 0` on newValue/oldValue, `Math.max(0, remaining)`.
  </read_first>
  <behavior>
    - Ascending + anchor: `parseBurndownChanges({ '1000': [...], '2000': [...] }, 500)` → points[0].t === 500 (startTime anchor); `points.map(p=>p.t)` === [500, 1000, 2000].
    - Non-negative clamp: a change driving the running total below 0 yields `remaining >= 0` for every point (Math.max(0, …)).
    - Malformed defense: `parseBurndownChanges(undefined as any, 500)` and an entry missing `statC` do not throw — they return the anchor (and skip the bad entry) rather than crashing.
  </behavior>
  <files>taskflow/src/routes/dashboard/dashboardMetrics.ts</files>
  <action>
    Export `interface BurndownPoint { t: number; remaining: number; }` (`t` = epoch ms; `remaining` = seconds remaining per Probe C `timeestimate`; note the unit in a comment so the 85-04 tooltip divides by 3600).

    Export `parseBurndownChanges(changes: Record<string, Array<{ key: string; statC?: { newValue: number; oldValue: number }; added?: boolean }>>, startTime: number): BurndownPoint[]`.

    V5 defensive parse (mandated): start with `const safe = changes ?? {}` so a null/undefined `.changes` cannot throw. Derive `const timestamps = Object.keys(safe).map(Number).sort((a, b) => a - b)` (numeric ascending — string sort would misorder epochs). Seed `points = [{ t: startTime, remaining: 0 }]` and `let running = 0`. For each ts, for each entry in `safe[String(ts)] ?? []`: if `entry.statC` is present, `running += (entry.statC.newValue ?? 0) - (entry.statC.oldValue ?? 0)` (the `?? 0` guards malformed numeric fields); skip entries without `statC`. Push `{ t: ts, remaining: Math.max(0, running) }` (the clamp guards negative remaining — Tampering mitigation T-85-01). Return `points`.

    Do NOT inline a Recharts tickFormatter here — the `${v}h` axis formatter and the `formatHoursMinutes` tooltip live in the 85-04 component; this plan only owns the pure series + the extracted hours formatter from Task 1.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/dashboardMetrics.test.ts -t "parseBurndownChanges"</automated>
  </verify>
  <done>`parseBurndownChanges` exists with `changes ?? {}`, numeric-ascending sort, `?? 0` on newValue/oldValue, and `Math.max(0, running)` clamp; all `parseBurndownChanges` tests pass including the null-input no-throw case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add all 7 mandated Vitest tests (VALIDATION map)</name>
  <read_first>
    - taskflow/src/routes/dashboard/dashboardMetrics.test.ts — the existing `makeIssue(overrides)` factory (lines 26–63; supports `subtask`, `sp`, `statusCategory: 'new'|'indeterminate'|'done'`, `assignee`, `duedate`) and the existing `SP_KEY = 'customfield_10016'` constant (line 21). Reuse BOTH verbatim — do not add a new factory or a new SP key constant.
    - 85-VALIDATION.md "Per-Task Verification Map" (the 7 binding `-t` filter strings) and 85-RESEARCH.md Tests 1–5 (concrete assertions).
  </read_first>
  <behavior>
    - Each test name's substring must match the VALIDATION `-t` filter exactly so the per-task verify commands select them: "tail", "subtask exclusion", "personal velocity", "qualifying sprints", "committed vs completed", "parseBurndownChanges", "burndown hours".
  </behavior>
  <files>taskflow/src/routes/dashboard/dashboardMetrics.test.ts</files>
  <action>
    Add `computePersonalVelocitySeries`, `parseBurndownChanges`, and `formatHoursMinutes` to the existing top-of-file `import { ... } from './dashboardMetrics'`. Build sprint literals as `{ id, name, state: 'closed' as const }` matching `JiraActiveSprint`, and issue maps as `new Map([[sprintId, [makeIssue(...)]]])`.

    Append two `describe` blocks after `mergeActivityEntries` (after line 381), plus the hours-formatter test. Each test name MUST contain its VALIDATION filter substring:

    describe('computePersonalVelocitySeries'):
    - `it('selects last N sprints from ascending list (tail-first ordering)')` — build 10 sprints ids 1–10 ascending, slice(-6), pass the tail with an empty Map; assert `series.map(p=>p.sprintName)` === ['Sprint 5'..'Sprint 10'] (the "tail" filter). This encodes the Probe A guard: the chart must show the most-recent sprints, never the first page.
    - `it('excludes subtask SP from committed and completed velocity sums (subtask exclusion)')` — parent(5,done,Alice) + 2 subtasks(2,done,Alice); Map([[1,[…]]]); assert `series[0].committed===5` and `series[0].completed===5` (not 9). (the "subtask exclusion" filter)
    - `it('excludes other users from personal velocity sums')` — Alice(8,done) + Bob(10,done); assert committed===8, completed===8. (the "personal velocity" filter)
    - `it('qualifying sprints filter: a sprint with 0 committed+completed is not qualifying')` — 3 sprints, two with SP and one with sp:0; assert `series.filter(p=>p.committed>0||p.completed>0).length===2` (below the 3 threshold → D-06 hide). (the "qualifying sprints" filter)
    - `it('committed vs completed: committed counts all my issues, completed only done')` — Alice done(3) + Alice indeterminate(5); assert committed===8, completed===3. (the "committed vs completed" filter)

    describe('parseBurndownChanges'):
    - `it('parseBurndownChanges returns ascending-time points anchored at startTime, clamped non-negative')` — changes `{ '1000': [{ key:'PROJ-1', statC:{ newValue:28800, oldValue:0 }, added:true }], '2000': [{ key:'PROJ-1', statC:{ newValue:0, oldValue:28800 } }] }`, startTime 500; assert `points[0].t===500`, `points.map(p=>p.t)` === [500,1000,2000], every `remaining >= 0`. Add a sub-assertion that `parseBurndownChanges(undefined as never, 500)` returns `[{ t:500, remaining:0 }]` without throwing. (the "parseBurndownChanges" filter)

    Hours-formatter test (place in either describe or a small `describe('formatHoursMinutes — burndown hours suffix')`):
    - `it('burndown hours formatter emits an h/m suffix, never SP')` — assert `formatHoursMinutes(1.5)` === '1h 30m', `formatHoursMinutes(8)` === '8h', and `formatHoursMinutes(8)` does NOT contain 'SP' (`expect(formatHoursMinutes(8)).not.toMatch(/SP/)`). (the "burndown hours" filter)
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/dashboardMetrics.test.ts --reporter=verbose && for t in "tail" "subtask exclusion" "personal velocity" "qualifying sprints" "committed vs completed" "parseBurndownChanges" "burndown hours"; do npx vitest run src/routes/dashboard/dashboardMetrics.test.ts -t "$t" >/dev/null 2>&1 && echo "PASS: $t" || { echo "FAIL: $t"; exit 1; }; done</automated>
  </verify>
  <done>All 7 VALIDATION `-t` filters select a passing test; the full dashboardMetrics.test.ts suite is green; no pre-existing Phase 83/84 test regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GreenHopper `scopechangeburndownchart` response → `parseBurndownChanges` | External Jira DC API data (`.changes` map, 496 entries at probe) crosses into pure-function parsing. Shape is partially [ASSUMED] (RESEARCH A2/A4); values may be null/negative/malformed. |
| Jira `/sprint/{id}/issue` response → `computePersonalVelocitySeries` | External issue arrays; `assignee` may be null, SP field may be null/absent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01 | Tampering | `parseBurndownChanges` (malformed/null `.changes`, negative or null `statC` values) | mitigate | V5 Input Validation: `changes ?? {}` before iteration; `?? 0` on `newValue`/`oldValue`; `Math.max(0, running)` clamp on remaining. Unit-tested with a null-input no-throw case. |
| T-85-02 | Tampering | `computePersonalVelocitySeries` (null assignee / null SP field) | mitigate | `assignee?.displayName` optional chaining excludes null-assignee issues; `(i.fields[spKey] ?? 0)` treats missing SP as 0. Unit-tested via subtask/personal/qualifying cases. |
| T-85-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced in this plan (RESEARCH Package Legitimacy Audit: none). No install task; no legitimacy checkpoint required. |

No high-severity unmitigated threats. All external-data parsing in this plan is defended at the pure-function boundary.
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/dashboardMetrics.test.ts --reporter=verbose` — all tests (pre-existing + 7 new) green.
- `cd taskflow && npm test` — full suite green (no Phase 83/84 regression from the formatter extraction).
- `cd taskflow && npx tsc --noEmit` — no new type errors introduced by the new exports or the WeeklyTrendChart import change.
- Grep confirms the D-03 comment is present: `grep -F 'final-assigned sprint scope, NOT start-of-sprint commitment' taskflow/src/routes/dashboard/dashboardMetrics.ts`.
</verification>

<success_criteria>
- `computePersonalVelocitySeries` and `parseBurndownChanges` are exported from `dashboardMetrics.ts` with the documented signatures.
- The mandated D-03 inline comment appears verbatim above the committed-SP reduce.
- V5 defensive parsing (`changes ?? {}`, `?? 0`, `Math.max(0, …)`) is present in `parseBurndownChanges`.
- `formatHoursMinutes` is exported from `dashboardMetrics.ts` and consumed by `WeeklyTrendChart.tsx` (no duplicate local copy).
- All 7 VALIDATION `-t` filters select a passing test; full dashboardMetrics suite and `npm test` are green.
- No UI or barrel fetcher is added in this plan — it is the pure-logic + test foundation for 85-02/03/04.
</success_criteria>

<output>
Create `.planning/phases/85-sprint-insights-conditional-probe-gated/85-01-SUMMARY.md` when done.
</output>
