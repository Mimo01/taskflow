---
phase: 52-aio-navigation-project-pages
plan: "02"
type: execute
wave: 1
depends_on:
  - "52-00"
files_modified:
  - taskflow/src/components/app/sidebar-items.ts
  - taskflow/src/components/app/Sidebar.tsx
  - taskflow/src/stores/settings.store.ts
  - taskflow/src/routes/routes.tsx
autonomous: true
requirements:
  - AION-01
  - AION-02
  - AION-03

must_haves:
  truths:
    - "Sidebar.tsx imports FlaskConical from lucide-react and includes it in ICON_MAP"
    - "Sidebar.tsx excludes Testing section items when aioEnabled is false"
    - "Settings store is at version 16 with appendAioItemIfMissing migration guard"
    - "Both AIO routes are registered in routes.tsx as lazy-loaded with withLazy()"
    - "Sidebar.test.tsx stubs turn GREEN after this plan"
  artifacts:
    - path: "taskflow/src/components/app/sidebar-items.ts"
      provides: "Testing section + aio-projects nav item + both preset updates"
      contains: "id: 'testing'"
    - path: "taskflow/src/components/app/Sidebar.tsx"
      provides: "FlaskConical in ICON_MAP, aioEnabled gate in sectionedItems"
      contains: "FlaskConical"
    - path: "taskflow/src/stores/settings.store.ts"
      provides: "version 16 + appendAioItemIfMissing helper + migration guard"
      contains: "version < 16"
    - path: "taskflow/src/routes/routes.tsx"
      provides: "/aio-projects and /aio-project/:projectKey lazy routes"
      contains: "/aio-projects"
  key_links:
    - from: "taskflow/src/components/app/Sidebar.tsx"
      to: "taskflow/src/stores/settings.store.ts"
      via: "const { devToolsEnabled, sidebarItems, aioEnabled } = useSettingsStore()"
      pattern: "aioEnabled"
    - from: "taskflow/src/components/app/Sidebar.tsx"
      to: "taskflow/src/components/app/sidebar-items.ts"
      via: "SIDEBAR_NAV_ITEMS, SIDEBAR_SECTIONS"
      pattern: "SIDEBAR_SECTIONS"
    - from: "taskflow/src/routes/routes.tsx"
      to: "taskflow/src/routes/dashboard/AioProjectsPage.tsx"
      via: "lazy(() => import('./dashboard/AioProjectsPage'))"
      pattern: "AioProjectsPage"
---

<objective>
Wire the AIO navigation infrastructure: Testing sidebar section, aioEnabled gate, v15→v16 settings store migration, and two lazy routes. This plan touches only configuration and navigation files — no page component implementations.

Purpose: After this plan, the sidebar shows "Testing → AIO Projects" when aioEnabled is true, the store migrates existing users' sidebarItems correctly, and the router knows about both AIO routes. The Sidebar.test.tsx stubs from Plan 00 turn GREEN here.
Output: 4 modified files — sidebar-items.ts, Sidebar.tsx, settings.store.ts, routes.tsx.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/52-aio-navigation-project-pages/52-CONTEXT.md
@.planning/phases/52-aio-navigation-project-pages/52-PATTERNS.md
@.planning/phases/52-aio-navigation-project-pages/52-UI-SPEC.md

<interfaces>
<!-- Exact patterns from codebase (verified in 52-PATTERNS.md source read) -->

From taskflow/src/components/app/sidebar-items.ts (current structure):
- SIDEBAR_SECTIONS: { id: string; label: string }[] — add { id: 'testing', label: 'Testing' } after { id: 'tracking', label: 'Tracking' }
- SIDEBAR_NAV_ITEMS entries shape: { id, label, path, iconName, section }
- getDefaultSidebarItems uses devVisible and pmVisible Set<string> — both sets must include 'aio-projects'
- SidebarItem type (exported) — used by the sidebar UI components

From taskflow/src/components/app/Sidebar.tsx (current structure per PATTERNS.md):
- lucide-react named import block (lines 10–24) — add FlaskConical alphabetically
- ICON_MAP: Record<string, ComponentType<{ className?: string }>> (lines 45–55) — add FlaskConical entry
- useSettingsStore destructure (line 68): const { devToolsEnabled, sidebarItems } = useSettingsStore() — add aioEnabled
- sectionedItems derivation (lines 269–272): SIDEBAR_NAV_ITEMS.filter((nav) => nav.section === section.id && visibleIds.has(nav.id)) — add !(nav.section === 'testing' && !aioEnabled) guard

From taskflow/src/stores/settings.store.ts (current state — confirmed Phase 51 at v15):
- persist() options object contains version: 15 — change to version: 16
- migrate() callback has sequential if (version < N) guards — add if (version < 16) block after existing if (version < 15) block
- SidebarItem interface defined at line 21 (store-local, NOT imported from sidebar-items.ts)
- appendAioItemIfMissing helper: add as a module-level function before useSettingsStore

From taskflow/src/routes/routes.tsx (current lazy import block per PATTERNS.md):
- Existing lazy imports at lines 12–20: const EpicsPage = lazy(() => import('./dashboard/EpicsPage')), etc.
- withLazy() function (lines 22–30): wraps component in ChunkErrorBoundary + Suspense — do not modify
- Routes array (lines 32–48): flat objects { path, element } — add two entries
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Testing section and aio-projects item to sidebar-items.ts</name>
  <files>taskflow/src/components/app/sidebar-items.ts</files>
  <read_first>
    - taskflow/src/components/app/sidebar-items.ts — read the complete file to see exact SIDEBAR_SECTIONS array, SIDEBAR_NAV_ITEMS entries, and getDefaultSidebarItems preset sets before modifying
  </read_first>
  <action>
    Make three targeted modifications to sidebar-items.ts:

    1. SIDEBAR_SECTIONS array: append { id: 'testing', label: 'Testing' } as the last entry, after { id: 'tracking', label: 'Tracking' }. This matches D-01.

    2. SIDEBAR_NAV_ITEMS array: append a new entry at the end of the array (after the last existing item):
       { id: 'aio-projects', label: 'AIO Projects', path: '/aio-projects', iconName: 'FlaskConical', section: 'testing' }
       Add a comment line before it: // Testing (AIO). This matches D-02.

    3. getDefaultSidebarItems presets: add 'aio-projects' to both devVisible and pmVisible Set literals. The string must be appended inside the Set constructor arguments. Both presets get visible: true behavior through the Set — if the current implementation uses a Set.has check, no additional change is needed beyond adding the string. This matches D-03.

    Do not change any other entries, formatting, or exports.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/components/app/sidebar-items.ts contains: { id: 'testing', label: 'Testing' }
    - taskflow/src/components/app/sidebar-items.ts contains: id: 'aio-projects'
    - taskflow/src/components/app/sidebar-items.ts contains: iconName: 'FlaskConical'
    - taskflow/src/components/app/sidebar-items.ts contains: section: 'testing'
    - taskflow/src/components/app/sidebar-items.ts contains 'aio-projects' in devVisible set
    - taskflow/src/components/app/sidebar-items.ts contains 'aio-projects' in pmVisible set
    - npx vitest run passes with no new failures (sidebar-items.ts is config only — no test file)
  </acceptance_criteria>
  <done>sidebar-items.ts has Testing section, aio-projects nav item, and both presets updated</done>
</task>

<task type="auto">
  <name>Task 2: Update Sidebar.tsx — FlaskConical in ICON_MAP + aioEnabled gate</name>
  <files>taskflow/src/components/app/Sidebar.tsx</files>
  <read_first>
    - taskflow/src/components/app/Sidebar.tsx — read complete file; locate lucide-react import block, ICON_MAP object, useSettingsStore destructure line, sectionedItems derivation
    - taskflow/src/components/app/Sidebar.test.tsx — read the Wave 0 stub to understand exactly what the tests expect (aioEnabled=true shows Testing section, aioEnabled=false hides it)
  </read_first>
  <action>
    Make three targeted modifications to Sidebar.tsx:

    1. lucide-react import block: add FlaskConical to the named imports. Keep alphabetical order (FlaskConical comes after ChevronRight, before GitMerge). Per D-02 and PATTERNS.md pitfall — must be in BOTH the import AND the map.

    2. ICON_MAP object: add FlaskConical as a new entry. Key must be the string 'FlaskConical' to match iconName in sidebar-items.ts. Value is the imported FlaskConical component. Add it at the same position as in the import block (after the last existing entry is fine if alphabetical is not enforced in the map object).

    3. useSettingsStore destructure: add aioEnabled to the existing destructure:
       const { devToolsEnabled, sidebarItems, aioEnabled } = useSettingsStore();
       (was: const { devToolsEnabled, sidebarItems } = useSettingsStore();)

    4. sectionedItems filter: extend the SIDEBAR_NAV_ITEMS.filter predicate with:
       && !(nav.section === 'testing' && !aioEnabled)
       Result: items where section==='testing' are only included when aioEnabled===true. The .filter((section) => section.items.length > 0) that follows naturally collapses the empty Testing section from the DOM when aioEnabled is false. Per D-04.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run src/components/app/Sidebar.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/components/app/Sidebar.tsx contains: FlaskConical (in lucide-react import)
    - taskflow/src/components/app/Sidebar.tsx contains: FlaskConical, (in ICON_MAP object)
    - taskflow/src/components/app/Sidebar.tsx contains: aioEnabled (in useSettingsStore destructure)
    - taskflow/src/components/app/Sidebar.tsx contains: nav.section === 'testing' && !aioEnabled
    - npx vitest run src/components/app/Sidebar.test.tsx exits 0 (both stubs from Plan 00 now GREEN)
    - npx vitest run exits with no additional failures beyond pre-existing stub failures from Plans 00/01
  </acceptance_criteria>
  <done>FlaskConical in ICON_MAP, aioEnabled gate in sectionedItems, Sidebar.test.tsx 2/2 pass</done>
</task>

<task type="auto">
  <name>Task 3: settings.store.ts v15→v16 migration + routes.tsx lazy additions</name>
  <files>
    taskflow/src/stores/settings.store.ts
    taskflow/src/routes/routes.tsx
  </files>
  <read_first>
    - taskflow/src/stores/settings.store.ts — read complete file; find: (a) persist() options object with version: 15, (b) migrate() callback with if (version < 15) guard, (c) SidebarItem interface (store-local, ~line 21) to use for appendAioItemIfMissing param type, (d) location in file to add the helper function
    - taskflow/src/routes/routes.tsx — read complete file; find: (a) lazy import block, (b) routes array, (c) withLazy() function signature to confirm it takes ComponentType
  </read_first>
  <action>
    Modify taskflow/src/stores/settings.store.ts with 3 changes:

    1. Add appendAioItemIfMissing helper function. Place it as a module-level function before useSettingsStore (after imports block, before the store definition). Use the store-local SidebarItem type for the parameter — do NOT import from sidebar-items.ts:
       function appendAioItemIfMissing(items: SidebarItem[]): SidebarItem[] {
         if (items.some((i) => i.id === 'aio-projects')) return items;
         return [...items, { id: 'aio-projects', visible: true }];
       }

    2. Change version: 15 to version: 16 in the persist() options object. This is the version field in the second argument to persist(). Per D-03 — both the version field AND the migrate guard must be updated.

    3. Add migration guard: in the migrate() callback, append after the existing if (version < 15) block and before the return statement:
       if (version < 16) {
         if (Array.isArray(s.sidebarItems)) {
           s.sidebarItems = appendAioItemIfMissing(s.sidebarItems as SidebarItem[]);
         }
       }
       The Array.isArray guard protects against corrupt persisted state.

    Modify taskflow/src/routes/routes.tsx with 2 changes:

    1. Lazy imports block: add two new const declarations after the last existing lazy import (e.g., after MergeRequestDetailPage):
       const AioProjectsPage = lazy(() => import('./dashboard/AioProjectsPage'));
       const AioProjectOverviewPage = lazy(() => import('./dashboard/AioProjectOverviewPage'));

    2. Routes array: add two flat route entries after the last existing /mr/:projectId/:iid entry:
       { path: '/aio-projects', element: withLazy(AioProjectsPage) },
       { path: '/aio-project/:projectKey', element: withLazy(AioProjectOverviewPage) },
       No /aio-cycle route in Phase 52 — that is Phase 53's responsibility.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/stores/settings.store.ts contains: function appendAioItemIfMissing(
    - taskflow/src/stores/settings.store.ts contains: version: 16 (in persist options)
    - taskflow/src/stores/settings.store.ts contains: version < 16
    - taskflow/src/stores/settings.store.ts contains: appendAioItemIfMissing(s.sidebarItems
    - taskflow/src/stores/settings.store.ts does NOT contain: import.*sidebar-items (no cross-import)
    - taskflow/src/routes/routes.tsx contains: lazy(() => import('./dashboard/AioProjectsPage'))
    - taskflow/src/routes/routes.tsx contains: lazy(() => import('./dashboard/AioProjectOverviewPage'))
    - taskflow/src/routes/routes.tsx contains: path: '/aio-projects'
    - taskflow/src/routes/routes.tsx contains: path: '/aio-project/:projectKey'
    - npx vitest run exits with no additional failures beyond pre-existing stub failures
  </acceptance_criteria>
  <done>Settings store at v16 with migration guard. Routes registered. Full suite regression-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted store → migration | Zustand rehydrates from Tauri Store JSON; sidebarItems could be any shape after partial upgrades |
| URL params → route match | React Router matches :projectKey from URL before it reaches any page component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | projectKey URL param in routes.tsx | accept | Route registration only — no path manipulation here. encodeURIComponent applied downstream in cycles.ts (Plan 01). |
| T-52-02 | Information Disclosure | settings.store.ts migration — error branch | accept | Migration helper does not log or expose token data. If sidebarItems is not an array, the guard silently skips (Array.isArray check). |
| T-52-S1 | Tampering | appendAioItemIfMissing — sidebarItems shape | mitigate | Array.isArray(s.sidebarItems) guard prevents crash on corrupted persisted state. The idempotency check (items.some) prevents double-appending on repeated migration runs (D-03). |
</threat_model>

<verification>
- `grep "id: 'testing'" taskflow/src/components/app/sidebar-items.ts` — 1 match
- `grep "FlaskConical" taskflow/src/components/app/Sidebar.tsx | grep -v '^#' | wc -l` — at least 2 matches (import + ICON_MAP)
- `grep "aioEnabled" taskflow/src/components/app/Sidebar.tsx` — at least 2 matches (destructure + filter)
- `grep "version: 16" taskflow/src/stores/settings.store.ts` — 1 match
- `grep "version < 16" taskflow/src/stores/settings.store.ts` — 1 match
- `grep "appendAioItemIfMissing" taskflow/src/stores/settings.store.ts` — 2 matches (definition + call)
- `cd taskflow && npx vitest run src/components/app/Sidebar.test.tsx` — exits 0, 2 passed
- `cd taskflow && npx vitest run` — no regressions in existing 929 tests
</verification>

<success_criteria>
All 4 navigation infrastructure files updated. Sidebar shows Testing section when aioEnabled=true. Store migrates to v16 with idempotent guard. Both routes registered as lazy. Sidebar.test.tsx 2/2 green. Zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/52-aio-navigation-project-pages/52-02-SUMMARY.md`
</output>
