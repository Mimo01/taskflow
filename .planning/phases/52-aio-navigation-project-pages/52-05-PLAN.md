---
phase: 52-aio-navigation-project-pages
plan: "05"
type: execute
wave: 3
depends_on:
  - "52-04"
files_modified:
  - taskflow/src/services/aio/types.ts                                      # Path A: extend AioCycle with stats fields
  - taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx                # Path A: add Pass, Fail, Run Date columns
  - taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx           # Path A: mock stats fields, assert column rendering
  - .planning/ROADMAP.md                                                     # Path B: amend SC-3 to remove stats requirement
  - .planning/phases/52-aio-navigation-project-pages/52-CONTEXT.md          # Path B: update D-10 with resolution note
  - .planning/phases/52-aio-navigation-project-pages/52-VERIFICATION.md     # Path B: annotate Truth 9 as resolved via ROADMAP amendment
autonomous: false
requirements:
  - AION-03
gap_closure: true
gap_source: "VERIFICATION.md Truth 9: per-cycle summary stats absent — pending live API probe"

must_haves:
  truths:
    - "Live AIO testcycle endpoint has been probed and response fields from the first items[] entry are documented"
    - "Either: AioCycle type is extended with confirmed stats fields AND overview page renders Pass, Fail, Run Date columns, OR ROADMAP SC-3 and CONTEXT.md D-10 are amended to reflect API reality"
    - "All 13 Phase 52 tests pass after changes (whichever path is taken)"
    - "VERIFICATION.md Truth 9 gap is formally resolved: either implemented (Path A) or explicitly closed via ROADMAP amendment (Path B)"
  artifacts:
    - path: "taskflow/src/services/aio/types.ts"
      provides: "AioCycle interface — extended with stats (Path A) or unchanged (Path B)"
      exports: ["AioCycle"]
    - path: "taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx"
      provides: "Overview page — stats columns rendered (Path A) or Key/Name/Status unchanged (Path B)"
      exports: ["default AioProjectOverviewPage"]
    - path: ".planning/ROADMAP.md"
      provides: "Phase 52 SC-3 — stats requirement removed (Path B) or unchanged (Path A)"
      contains: "SC-3"
  key_links:
    - from: "curl probe output (items[0] field names)"
      to: "AioCycle interface in types.ts"
      via: "developer documents exact field names from JSON"
      pattern: "passedCount|failedCount|executedOn|totalPassed|totalFailed"
    - from: "AioCycle interface (Path A)"
      to: "AioProjectOverviewPage.tsx table columns"
      via: "cycle.passedCount, cycle.failedCount, cycle.executedOn"
      pattern: "passedCount|failedCount|executedOn"
---

<objective>
Close the single remaining Phase 52 gap: ROADMAP SC-3 requires per-cycle summary stats (pass/fail counts, run date) on the project overview page, but the AioCycle type and AioProjectOverviewPage currently provide only key/name/status. The resolution path depends on whether the live AIO testcycle endpoint actually returns stats fields.

Purpose: Definitively resolve Truth 9 from VERIFICATION.md. Either implement stats columns from real API data (Path A), or formally amend the ROADMAP to reflect that the API does not expose per-cycle stats at the list level (Path B). Phase 52 cannot be marked complete while ROADMAP SC-3 is unmet with no documented resolution.

Output (Path A): Extended AioCycle type, updated AioProjectOverviewPage with Pass/Fail/Run Date columns, updated tests.
Output (Path B): Amended ROADMAP SC-3, updated CONTEXT.md D-10, annotated VERIFICATION.md Truth 9.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/52-aio-navigation-project-pages/52-CONTEXT.md
@.planning/phases/52-aio-navigation-project-pages/52-VERIFICATION.md
@.planning/phases/51-aio-service-layer/51-CONTEXT.md

<interfaces>
<!-- Current state going into this plan. Executor must not explore — use as given. -->

From taskflow/src/services/aio/types.ts (current AioCycle — Path A extends this):
```typescript
export interface AioCycle {
  key: string;          // Cycle key, e.g. "PROJ-CY-2"
  name: string;         // Cycle display name
  status: string;       // Cycle status, e.g. "Active", "Closed"
  projectKey: string;   // Owning Jira project key
}
```

From taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx (current mock shape):
```typescript
{ key: 'PROJ-CY-2', name: 'Sprint 1 Cycle', status: 'Active', projectKey: 'PROJ' }
```

AIO probe base path (D-13 from 51-CONTEXT.md):
- Testcycle endpoint: GET /rest/aio-tcms-api/1.0/project/{projectKey}/testcycle
- Full URL pattern: $JIRA_BASE/rest/aio-tcms-api/1.0/project/{projectKey}/testcycle
- Auth: Authorization: Bearer $PAT (same Stronghold key 'jira-pat')

Candidate stats field names to look for in probe response (any of these would enable Path A):
- passedCount / totalPassed / passCount
- failedCount / totalFailed / failCount
- blockedCount / notExecutedCount / skippedCount
- executedOn / lastRun / completionDate / endDate / updatedAt (date string)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Probe the live AIO testcycle endpoint for stats fields</name>
  <files>.planning/phases/52-aio-navigation-project-pages/52-CONTEXT.md</files>
  <read_first>
    - .planning/phases/51-aio-service-layer/51-CONTEXT.md — D-13 confirms the working base path is /rest/aio-tcms-api/1.0 for testcycle; D-14 confirms Bearer PAT auth. Review before running so you have the correct JIRA_BASE and base path.
    - .planning/phases/52-aio-navigation-project-pages/52-CONTEXT.md — D-09 states the testcycle endpoint path pattern; D-10 is the decision to update if Path B applies.
    - .planning/phases/52-aio-navigation-project-pages/52-VERIFICATION.md — Gap section describes exactly what is missing and the two resolution paths.
  </read_first>
  <what-built>
    The executor has prepared the probe commands. You must run them manually against the live AIO instance because the instance is not reachable from the build environment.
  </what-built>
  <how-to-verify>
    Run the following in your terminal, substituting your values:

    ```
    export JIRA_BASE="https://your-jira-instance.example.com"
    export PAT="your-jira-personal-access-token"
    export PROJECT_KEY="PROJ"   # any real AIO project key on this instance
    ```

    Step 1 — Fetch the first page of test cycles and pretty-print:
    ```
    curl -s \
      -H "Authorization: Bearer $PAT" \
      "$JIRA_BASE/rest/aio-tcms-api/1.0/project/$PROJECT_KEY/testcycle?maxResults=5" \
      | python3 -m json.tool
    ```

    Step 2 — Inspect the first element in the items[] array. Look for any field beyond key/name/status/projectKey. Specifically look for:
      - numeric fields: passedCount, failedCount, blockedCount, totalPassed, totalFailed, passCount, failCount, notExecutedCount, skippedCount
      - date/string fields: executedOn, lastRun, completionDate, endDate, updatedAt, startDate

    Step 3 — If the items array is empty on that project, try another project key to get a cycle with actual test runs:
    ```
    curl -s \
      -H "Authorization: Bearer $PAT" \
      "$JIRA_BASE/rest/aio-tcms-api/1.0/project/$PROJECT_KEY/testcycle?maxResults=10" \
      | python3 -m json.tool
    ```

    Step 4 — If any cycles exist, optionally probe the detail endpoint for one cycle (different shape, richer data):
    ```
    export CYCLE_KEY="PROJ-CY-2"   # replace with a real cycle key from the list response
    curl -s \
      -H "Authorization: Bearer $PAT" \
      "$JIRA_BASE/rest/aio-tcms-api/1.0/project/$PROJECT_KEY/testcycle/$CYCLE_KEY/detail" \
      | python3 -m json.tool
    ```
    Note: the /detail endpoint is a Phase 53 concern but its shape informs whether list-level stats exist.
  </how-to-verify>
  <resume-signal>
    Type "probe done" and provide:
    1. The full JSON of the first items[] entry from the testcycle list response (paste the object, all fields)
    2. List any numeric or date fields you found beyond key/name/status/projectKey — or confirm "none found"
    3. Your chosen path: "Path A — stats fields exist, implement columns" or "Path B — no stats fields, amend ROADMAP"
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2 (Path A): Extend AioCycle and add stats columns to AioProjectOverviewPage</name>
  <files>
    taskflow/src/services/aio/types.ts,
    taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx,
    taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx
  </files>
  <read_first>
    - taskflow/src/services/aio/types.ts — current AioCycle interface; extend it with confirmed field names from probe
    - taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx — current 3-column table (Key/Name/Status); add Pass, Fail, Run Date columns after Status
    - taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx — current mock shape { key, name, status, projectKey }; add confirmed stats fields to mock data and assert column text
    - .planning/phases/51-aio-service-layer/51-CONTEXT.md — D-17 probe findings confirm AioPage<AioCycle> wrapper and field naming conventions for this AIO instance
  </read_first>
  <action>
    ONLY execute this task if the developer confirmed Path A (stats fields returned by the API).

    Replace the placeholder field names below with the exact field names confirmed by the probe in Task 1. The candidates in order of likelihood are:
    - Pass count field: passedCount (most likely) or totalPassed or passCount
    - Fail count field: failedCount (most likely) or totalFailed or failCount
    - Run date field: executedOn (most likely) or lastRun or completionDate

    Step 1 — Extend AioCycle in taskflow/src/services/aio/types.ts.
    Add optional fields to the AioCycle interface immediately after the projectKey field:
    ```
    passedCount?: number;    // <replace with exact probe field name> — confirmed by Phase 52 probe
    failedCount?: number;    // <replace with exact probe field name> — confirmed by Phase 52 probe
    executedOn?: string;     // <replace with exact probe field name> — ISO date string or null; confirmed by Phase 52 probe
    ```
    Use the exact field names returned by the API (not the candidate names above). Mark fields as optional (?) because cycles with no runs may omit them. Update the JSDoc comment on AioCycle to add "Optional stats fields (passedCount, failedCount, executedOn) present when cycles have been executed."

    Step 2 — Update AioProjectOverviewPage.tsx to add three columns after Status.
    Add three new <th> elements in the <thead> after the existing Status header:
    - "Pass" — w-16 px-3 py-2 text-right text-xs font-medium text-muted-foreground
    - "Fail" — w-16 px-3 py-2 text-right text-xs font-medium text-muted-foreground
    - "Run Date" — w-28 px-3 py-2 text-left text-xs font-medium text-muted-foreground

    Add three new <td> elements in each cycle row, matching the header widths:
    - Pass td: px-3 py-3 text-right text-xs tabular-nums — content: `{cycle.passedCount ?? '—'}`
    - Fail td: px-3 py-3 text-right text-xs tabular-nums — content: `{cycle.failedCount ?? '—'}`
    - Run Date td: px-3 py-3 text-xs text-muted-foreground — content: when cycle.executedOn is defined, render it as a formatted date string using `new Date(cycle.executedOn).toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' })`; when undefined render '—'

    Use the exact confirmed field names from the probe (not the candidate names). Do not import any additional libraries — `toLocaleDateString` is a native Date method.

    Step 3 — Update AioProjectOverviewPage.test.tsx.
    In the test "AION-03: renders a row for each cycle (key, name, status)", extend the mock return value to include the confirmed stats fields:
    ```typescript
    { key: 'PROJ-CY-2', name: 'Sprint 1 Cycle', status: 'Active', projectKey: 'PROJ',
      passedCount: 7, failedCount: 2, executedOn: '2026-04-15T10:30:00.000Z' }
    ```
    (Replace field names with exact probe names.) Add waitFor assertions for the new column values:
    ```typescript
    expect(screen.getByText('7')).toBeDefined();    // passedCount
    expect(screen.getByText('2')).toBeDefined();    // failedCount
    expect(screen.getByText('15 Apr 2026')).toBeDefined();   // formatted executedOn
    ```
    The EmptyState and ErrorState tests do not need changes (no stats in those paths).

    Do not add a date formatting utility file. The single `toLocaleDateString` call in the JSX is sufficient; no abstraction is needed.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run src/routes/dashboard/AioProjectOverviewPage.test.tsx src/services/aio 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/services/aio/types.ts AioCycle interface contains the three confirmed stats fields as optional properties (?, typed as number or string respectively)
    - The field names in types.ts exactly match the field names returned by the live API probe (not placeholder candidate names)
    - grep on AioProjectOverviewPage.tsx returns matches for the exact probe field names (e.g., "passedCount", "failedCount", "executedOn")
    - AioProjectOverviewPage.tsx table thead has exactly 6 <th> elements: Key, Name, Status, Pass, Fail, Run Date
    - AioProjectOverviewPage.tsx tbody td for Pass uses text-right tabular-nums and renders `{cycle.passedCount ?? '—'}`
    - AioProjectOverviewPage.tsx tbody td for Fail uses text-right tabular-nums and renders `{cycle.failedCount ?? '—'}`
    - AioProjectOverviewPage.tsx tbody td for Run Date renders `new Date(cycle.executedOn).toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' })` when defined, '—' when undefined
    - AioProjectOverviewPage.test.tsx mock data for the "renders a row" test includes all three stats fields with concrete values
    - AioProjectOverviewPage.test.tsx has assertions for the stats values (pass count, fail count, formatted date)
    - `npx vitest run src/routes/dashboard/AioProjectOverviewPage.test.tsx` exits 0, all 3 tests GREEN
    - `npx vitest run` (full suite) exits 0, all 13 Phase 52 tests GREEN, no regressions
  </acceptance_criteria>
  <done>AioCycle extended with confirmed stats fields. AioProjectOverviewPage renders 6-column table with Pass, Fail, Run Date. All 3 overview tests GREEN. Full 13 Phase 52 tests GREEN. ROADMAP SC-3 is satisfied.</done>
</task>

<task type="auto">
  <name>Task 3 (Path B): Amend ROADMAP SC-3, CONTEXT.md D-10, and VERIFICATION.md Truth 9</name>
  <files>
    .planning/ROADMAP.md,
    .planning/phases/52-aio-navigation-project-pages/52-CONTEXT.md,
    .planning/phases/52-aio-navigation-project-pages/52-VERIFICATION.md
  </files>
  <read_first>
    - .planning/ROADMAP.md — Phase 52 Success Criteria section; find SC-3 (item 3 under Phase 52 success criteria) — current text: "Clicking a project opens a project overview page listing all cycles with per-cycle summary stats (pass/fail counts, run date)"
    - .planning/phases/52-aio-navigation-project-pages/52-CONTEXT.md — D-10 decision text; current text: "Shows cycle name + status only. Pass/fail counts and run date are deferred to Phase 53. No N+1 fetches per cycle."
    - .planning/phases/52-aio-navigation-project-pages/52-VERIFICATION.md — Gap section Truth 9 entry; update the status annotation only (do not rewrite the verification report)
  </read_first>
  <action>
    ONLY execute this task if the developer confirmed Path B (no stats fields exist in the testcycle list response).

    Step 1 — Amend ROADMAP.md Phase 52 SC-3.
    Find the Phase 52 Success Criteria block. SC-3 is item 3:
    OLD: "Clicking a project opens a project overview page listing all cycles with per-cycle summary stats (pass/fail counts, run date)"
    NEW: "Clicking a project opens a project overview page listing all cycles with cycle key, name, and status (per-cycle summary stats deferred to Phase 53 cycle detail — AIO testcycle list endpoint does not return stats fields)"

    No other ROADMAP lines must change. Do not alter SC-1, SC-2, SC-4, or any other phase.

    Step 2 — Update CONTEXT.md D-10.
    Find the D-10 decision entry. Replace the existing text block for D-10 with:
    "Shows cycle name + status only. Pass/fail counts and run date deferred to Phase 53 — confirmed by Phase 52 probe: the AIO testcycle list endpoint (`GET /rest/aio-tcms-api/1.0/project/{projectKey}/testcycle`) does not return per-cycle stats fields (passedCount, failedCount, executedOn or equivalents). No N+1 fetches per cycle."

    Do not alter any other D-XX entries. The label "D-10:" must be preserved.

    Step 3 — Annotate VERIFICATION.md Truth 9.
    In the VERIFICATION.md frontmatter `gaps` section, change the status of the Truth 9 gap entry.
    Find the gaps block:
    ```yaml
    gaps:
      - truth: "Overview page shows per-cycle summary stats (pass/fail counts, run date) per ROADMAP SC-3"
        status: failed
    ```
    Change `status: failed` to `status: resolved_by_roadmap_amendment` and add a resolution note field:
    ```yaml
        resolution: "Phase 52 probe confirmed AIO testcycle list endpoint returns no stats fields. ROADMAP SC-3 amended to remove stats requirement. Stats remain in Phase 53 cycle detail scope."
    ```
    Also update the top-level frontmatter: change `status: gaps_found` to `status: complete` and `score: 8/9` to `score: 9/9`.

    No code changes are needed in this path. No TypeScript files are touched.
  </action>
  <verify>
    <automated>grep -n "per-cycle summary stats" /Users/mimo/Documents/Projects/taskflow/.planning/ROADMAP.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 52 SC-3 no longer contains the text "pass/fail counts, run date" as a requirement (it may reference them only as parenthetical explanation for deferral)
    - ROADMAP.md Phase 52 SC-3 contains the phrase "deferred to Phase 53" or "Phase 53 cycle detail"
    - ROADMAP.md Phase 52 SC-3 contains the rationale "AIO testcycle list endpoint does not return stats fields" (or equivalent exact wording from the action above)
    - grep "pass/fail counts, run date" .planning/ROADMAP.md returns 0 matches in the SC-3 line (the old requirement text is gone)
    - CONTEXT.md D-10 contains the phrase "confirmed by Phase 52 probe"
    - CONTEXT.md D-10 contains the text "does not return per-cycle stats fields"
    - VERIFICATION.md frontmatter status field is `complete`
    - VERIFICATION.md frontmatter score field is `9/9`
    - VERIFICATION.md gap entry for Truth 9 has `status: resolved_by_roadmap_amendment`
    - VERIFICATION.md gap entry for Truth 9 has a `resolution:` field with non-empty text
    - No TypeScript or test files are modified (grep git diff --name-only returns no .ts/.tsx files)
    - `cd taskflow && npx vitest run` exits 0 — all 13 Phase 52 tests remain GREEN (no regressions from doc-only changes)
  </acceptance_criteria>
  <done>ROADMAP SC-3 amended to reflect API reality. CONTEXT.md D-10 updated with probe resolution. VERIFICATION.md Truth 9 formally closed. Phase 52 score updated to 9/9. No code changes. Full Phase 52 test suite remains GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → live AIO instance | curl probe crosses this boundary; PAT travels over HTTPS |
| curl probe output → types.ts field names | Probe-confirmed field names are copied into the AioCycle interface — transcription error would cause runtime undefined values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52G-01 | Information Disclosure | PAT in curl probe command history | accept | Same exposure as Phase 51 probe. PAT stays in shell history only; never written to codebase files. |
| T-52G-02 | Tampering | Wrong field names transcribed from probe output into AioCycle interface (Path A) | mitigate | Acceptance criteria require field names in types.ts to exactly match probe output. Executor must paste probe JSON and copy field names character-for-character. |
| T-52G-03 | Denial of Service | N+1 per-cycle detail fetches if stats are implemented as detail calls | accept | Path A only uses fields already present in the list response — no additional API calls per cycle. If stats require /detail calls, Path B must be chosen instead. |
| T-52G-04 | Tampering | ROADMAP amendment removes more than SC-3 requirement (Path B) | mitigate | Acceptance criteria explicitly verify SC-1, SC-2, SC-4 are untouched. Executor must make a surgical single-line change to SC-3 only. |
</threat_model>

<verification>
After completing whichever path applies:

Path A verification:
- `grep -c "passedCount\|failedCount\|executedOn" taskflow/src/services/aio/types.ts` — returns 3 (one match per field in interface)
- `grep "Pass\|Fail\|Run Date" taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx` — returns 3 header matches
- `cd taskflow && npx vitest run src/routes/dashboard/AioProjectOverviewPage.test.tsx` — exits 0, 3 tests GREEN
- `cd taskflow && npx vitest run` — exits 0, all 13 Phase 52 tests GREEN

Path B verification:
- `grep -n "per-cycle summary stats" .planning/ROADMAP.md` — SC-3 line no longer contains this as a bare requirement
- `grep "confirmed by Phase 52 probe" .planning/phases/52-aio-navigation-project-pages/52-CONTEXT.md` — 1 match in D-10
- `grep "resolved_by_roadmap_amendment" .planning/phases/52-aio-navigation-project-pages/52-VERIFICATION.md` — 1 match
- `grep "^score:" .planning/phases/52-aio-navigation-project-pages/52-VERIFICATION.md` — returns "score: 9/9"
- `cd taskflow && npx vitest run` — exits 0, no regressions
</verification>

<success_criteria>
1. Developer has run the live API probe and documented whether stats fields exist in the testcycle list response
2. Path A: AioCycle has three confirmed stats fields (optional); AioProjectOverviewPage has 6-column table with Pass, Fail, Run Date; AioProjectOverviewPage.test.tsx has stats assertions; all 13 Phase 52 tests GREEN
3. Path B: ROADMAP SC-3 amended to remove stats as a requirement with probe-based rationale; CONTEXT.md D-10 updated with probe resolution; VERIFICATION.md Truth 9 marked resolved_by_roadmap_amendment with score 9/9; no TypeScript changes; all 13 Phase 52 tests GREEN
4. Either path leaves Phase 52 with zero open gaps — Truth 9 is definitively resolved
</success_criteria>

<output>
After completion, create `.planning/phases/52-aio-navigation-project-pages/52-05-SUMMARY.md`
</output>
