---
phase: 52-aio-navigation-project-pages
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - taskflow/src/services/aio/cycles.test.ts
  - taskflow/src/routes/dashboard/AioProjectsPage.test.tsx
  - taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx
  - taskflow/src/components/app/Sidebar.test.tsx
autonomous: true
requirements:
  - AION-01
  - AION-02
  - AION-03

must_haves:
  truths:
    - "All four Wave 0 test stub files exist and fail (RED) with no implementation"
    - "Test runner exits non-zero for each stub file individually"
    - "No production files are created or modified in this plan"
  artifacts:
    - path: "taskflow/src/services/aio/cycles.test.ts"
      provides: "Failing test stubs for fetchAioCycles (pagination, 401, 404, network)"
    - path: "taskflow/src/routes/dashboard/AioProjectsPage.test.tsx"
      provides: "Failing test stubs for AioProjectsPage render/empty/error states"
    - path: "taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx"
      provides: "Failing test stubs for AioProjectOverviewPage render/empty/error states"
    - path: "taskflow/src/components/app/Sidebar.test.tsx"
      provides: "Failing test stubs for aioEnabled gate on Testing section"
  key_links:
    - from: "cycles.test.ts"
      to: "src/services/aio/cycles.ts"
      via: "import { fetchAioCycles } from './cycles'"
      pattern: "fetchAioCycles"
    - from: "AioProjectsPage.test.tsx"
      to: "src/routes/dashboard/AioProjectsPage.tsx"
      via: "import AioProjectsPage from './AioProjectsPage'"
      pattern: "AioProjectsPage"
    - from: "Sidebar.test.tsx"
      to: "src/components/app/Sidebar.tsx"
      via: "import { Sidebar } from './Sidebar'"
      pattern: "aioEnabled"
---

<objective>
Create all four Wave 0 test stub files for Phase 52. These stubs contain the full test descriptions and import statements but no implementation — every `it()` body calls `expect(true).toBe(false)` to guarantee RED status. Later plans implement production code to turn these RED.

Purpose: Establish the Nyquist sampling contract before any implementation begins. Prevents "write code then write tests to match" regression.
Output: 4 test files that fail immediately on `npx vitest run`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/52-aio-navigation-project-pages/52-CONTEXT.md
@.planning/phases/52-aio-navigation-project-pages/52-VALIDATION.md
@.planning/phases/52-aio-navigation-project-pages/52-PATTERNS.md

<interfaces>
<!-- Key types and contracts — from Phase 51 deliverables (51-03-SUMMARY.md confirmed) -->

From taskflow/src/services/aio/types.ts (Phase 51):
- AioProject { id: number; projectKey: string; name: string }
- AioCycle { key: string; name: string; status: string; projectKey: string }
- AioPage<T> { items: T[]; startAt: number; maxResults: number; isLast: boolean }

From taskflow/src/services/aio/projects.ts (Phase 51):
- fetchAioProjects(baseUrl: string, token: string): Promise<AioProject[]>

From taskflow/src/services/aio/issue-runs.ts (Phase 51, pagination loop pattern):
- fetchAioTestRunsForCycle(baseUrl, token, projectKey, cycleKey): Promise<AioTestRun[]>
  — uses for(;;) loop, aioFetch, AioPage<T> unwrap with Array.isArray guard

Test mocking patterns (from taskflow/src/services/aio/projects.test.ts — verified):
- vi.mock('../../lib/apiFetch', () => ({ apiFetch: vi.fn() }))  placed BEFORE imports
- import { apiFetch } from '../../lib/apiFetch'; import { fetchAioCycles } from './cycles';
- const mockedApiFetch = vi.mocked(apiFetch);

Test mocking patterns (from taskflow/src/routes/dashboard/EpicsPage.test.tsx — verified):
- vi.mock('@/stores/settings.store', () => ({ useSettingsStore: () => ({ ... }) }))
- vi.mock('@/stores/auth.store', () => ({ useAuthStore: () => ({ jiraBaseUrl: 'https://jira.example.com' }) }))
- vi.mock('@/services/aio', () => ({ fetchAioProjects: vi.fn() }))
- vi.mock('@/services/stronghold', () => ({ readSecret: vi.fn().mockResolvedValue('test-jira-token') }))
- function makeClient() { return new QueryClient({ defaultOptions: { queries: { retry: false } } }); }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write cycles.test.ts stub (RED)</name>
  <files>taskflow/src/services/aio/cycles.test.ts</files>
  <read_first>
    - taskflow/src/services/aio/projects.test.ts — exact mock-before-import pattern, test structure, error cases
    - taskflow/src/services/aio/issue-runs.test.ts — pagination test pattern (what cycles.test.ts must also cover)
    - taskflow/src/services/aio/types.ts — AioCycle, AioPage<T> field names for stub data
  </read_first>
  <action>
    Create taskflow/src/services/aio/cycles.test.ts with all imports and vi.mock calls in place, but every it() body set to expect(true).toBe(false) so the file is immediately RED.

    Mock placement rule: vi.mock('../../lib/apiFetch', ...) must appear BEFORE any import of apiFetch or fetchAioCycles — mirrors projects.test.ts exactly.

    Describe block: fetchAioCycles. Test cases to stub (5 total):
    1. "returns AioCycle[] on 200 with paginated AioPage wrapper" — mock one page response, isLast: true, verify array length 1 and key field
    2. "accumulates items across multiple pages until isLast is true" — mock two page responses (isLast: false then isLast: true), verify length 2
    3. "throws ApiError with source 'jira' on 401" — mock 401 response, verify ApiError thrown
    4. "returns empty array on 404 (project not found or no cycles)" — mock 404, verify []
    5. "throws 'Cannot reach AIO' on network error (aioFetch throws)" — mock rejection, verify error message

    Constants at top: BASE = 'https://jira.example.com', TOKEN = 'test-token', PROJECT_KEY = 'PROJ'.

    Each it() body: expect(true).toBe(false) — no actual assertions yet.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run src/services/aio/cycles.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/services/aio/cycles.test.ts exists
    - File contains: vi.mock('../../lib/apiFetch'
    - File contains: describe('fetchAioCycles'
    - File contains 5 it( blocks
    - npx vitest run src/services/aio/cycles.test.ts exits non-zero (tests fail — correct for stub)
    - npx vitest run src/services/aio/cycles.test.ts output contains "5 failed"
  </acceptance_criteria>
  <done>cycles.test.ts exists, imports compile, 5 failing stubs, no production code written</done>
</task>

<task type="auto">
  <name>Task 2: Write AioProjectsPage.test.tsx and AioProjectOverviewPage.test.tsx stubs (RED)</name>
  <files>
    taskflow/src/routes/dashboard/AioProjectsPage.test.tsx
    taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx
  </files>
  <read_first>
    - taskflow/src/routes/dashboard/EpicsPage.test.tsx — full mock setup, makeClient(), MemoryRouter, render pattern; this is the exact analog
    - taskflow/src/services/aio/types.ts — AioProject and AioCycle field names for stub data shapes
  </read_first>
  <action>
    Create AioProjectsPage.test.tsx with:
    - All vi.mock calls: @/stores/settings.store, @/stores/auth.store, @/services/aio (mock fetchAioProjects), @/services/stronghold
    - makeClient() helper
    - describe('AioProjectsPage') with 3 it() stubs:
      1. "AION-02: renders a row for each AIO project (name and projectKey)"
      2. "AION-02: shows EmptyState when fetchAioProjects returns empty array"
      3. "AION-02: shows ErrorState when fetchAioProjects rejects"
    - Every it() body: expect(true).toBe(false)

    Create AioProjectOverviewPage.test.tsx with:
    - Same vi.mock pattern; mock fetchAioCycles from @/services/aio instead of fetchAioProjects
    - MemoryRouter initialEntries={['/aio-project/PROJ']} with Route path="/aio-project/:projectKey" so useParams resolves projectKey
    - describe('AioProjectOverviewPage') with 3 it() stubs:
      1. "AION-03: renders a row for each cycle (key, name, status)"
      2. "AION-03: shows EmptyState when fetchAioCycles returns empty array"
      3. "AION-03: shows ErrorState when fetchAioCycles rejects"
    - Every it() body: expect(true).toBe(false)

    Both files must import from the component path that will be created in Plan 03/04 — import will resolve at test run time (currently causes module-not-found but that counts as failing RED).
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run src/routes/dashboard/AioProjectsPage.test.tsx src/routes/dashboard/AioProjectOverviewPage.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/dashboard/AioProjectsPage.test.tsx exists
    - taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx exists
    - AioProjectsPage.test.tsx contains: describe('AioProjectsPage' and 3 it( blocks
    - AioProjectOverviewPage.test.tsx contains: describe('AioProjectOverviewPage' and 3 it( blocks
    - AioProjectOverviewPage.test.tsx contains: MemoryRouter and /aio-project/PROJ initialEntries
    - npx vitest run for both files exits non-zero (stubs fail — correct)
  </acceptance_criteria>
  <done>Both page test stub files exist, 3 stubs each, RED status confirmed</done>
</task>

<task type="auto">
  <name>Task 3: Write Sidebar.test.tsx stub (RED) for aioEnabled gate</name>
  <files>taskflow/src/components/app/Sidebar.test.tsx</files>
  <read_first>
    - taskflow/src/components/app/Sidebar.tsx — current imports, props/exports, useSettingsStore destructuring, what mocks are needed
    - taskflow/src/components/app/sidebar-items.ts — SIDEBAR_SECTIONS, SIDEBAR_NAV_ITEMS structure — to understand what the test needs to render
    - taskflow/src/components/app/CommandPalette.test.tsx — nearest app-component test for mock patterns in this directory
  </read_first>
  <action>
    Create taskflow/src/components/app/Sidebar.test.tsx.

    The test file must mock: useSettingsStore (from @/stores/settings.store) and useAuthStore (from @/stores/auth.store), react-router-dom (NavLink renders as anchor). The Sidebar component reads sidebarItems, aioEnabled, devToolsEnabled from useSettingsStore.

    describe('Sidebar — aioEnabled gate') with 2 it() stubs:
    1. "AION-01: Testing section is visible when aioEnabled is true" — when mock returns aioEnabled: true and sidebarItems includes aio-projects visible:true, expect Testing section in DOM
    2. "AION-01: Testing section is absent when aioEnabled is false" — when mock returns aioEnabled: false, expect Testing section absent from DOM

    Every it() body: expect(true).toBe(false).

    Mock useSettingsStore to return: { sidebarItems: [{ id: 'aio-projects', visible: true }], aioEnabled: true, devToolsEnabled: false } (or false for second test).
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run src/components/app/Sidebar.test.tsx 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/components/app/Sidebar.test.tsx exists
    - File contains: describe('Sidebar
    - File contains 2 it( blocks
    - File contains: aioEnabled
    - npx vitest run src/components/app/Sidebar.test.tsx exits non-zero (stubs fail — correct)
  </acceptance_criteria>
  <done>Sidebar.test.tsx exists with 2 failing stubs covering AION-01 aioEnabled gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → production module | Test imports production modules that do not exist yet — import resolution failures are expected RED state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | cycles.test.ts — T-52-01 stub must verify encodeURIComponent behavior | mitigate | Test case 1 "returns AioCycle[] on 200" implicitly validates path construction; a dedicated path-encoding assertion is added in Plan 01 when cycles.ts is implemented |
| T-52-02 | Information Disclosure | test stubs — token must not appear in assertion strings | accept | Stub bodies are expect(true).toBe(false); no real tokens used |
</threat_model>

<verification>
After all three tasks:
- `cd taskflow && npx vitest run src/services/aio/cycles.test.ts` — exits non-zero, 5 failed
- `cd taskflow && npx vitest run src/routes/dashboard/AioProjectsPage.test.tsx` — exits non-zero, 3 failed
- `cd taskflow && npx vitest run src/routes/dashboard/AioProjectOverviewPage.test.tsx` — exits non-zero, 3 failed
- `cd taskflow && npx vitest run src/components/app/Sidebar.test.tsx` — exits non-zero, 2 failed
- No production files were created or modified
- `cd taskflow && npx vitest run` — existing 929 tests still pass (regression check; 13 new stubs fail — expected)
</verification>

<success_criteria>
4 Wave 0 test stub files exist. All 13 stub tests fail (RED). Existing 929-test suite remains green. No production source files modified.
</success_criteria>

<output>
After completion, create `.planning/phases/52-aio-navigation-project-pages/52-00-SUMMARY.md`
</output>
