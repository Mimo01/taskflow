---
phase: 52-aio-navigation-project-pages
plan: "03"
type: execute
wave: 2
depends_on:
  - "52-01"
  - "52-02"
files_modified:
  - taskflow/src/routes/dashboard/AioProjectsPage.tsx
  - taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx
autonomous: true
requirements:
  - AION-02

must_haves:
  truths:
    - "AioProjectsPage renders a table row per AioProject with name and projectKey"
    - "Clicking a row navigates to /aio-project/:projectKey"
    - "Loading state shows AioProjectsSkeleton after 200ms delay"
    - "Empty list shows EmptyState with FlaskConical icon"
    - "Fetch error shows ErrorState with retry that invalidates query"
    - "AioProjectsPage.test.tsx stubs from Plan 00 turn GREEN"
  artifacts:
    - path: "taskflow/src/routes/dashboard/AioProjectsPage.tsx"
      provides: "AIO projects list page with full loading/error/empty/data states"
      exports: ["default AioProjectsPage"]
    - path: "taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx"
      provides: "5-row skeleton with no internal p-4 padding"
      exports: ["AioProjectsSkeleton"]
  key_links:
    - from: "taskflow/src/routes/dashboard/AioProjectsPage.tsx"
      to: "taskflow/src/services/aio"
      via: "import { fetchAioProjects } from '@/services/aio'"
      pattern: "fetchAioProjects"
    - from: "taskflow/src/routes/dashboard/AioProjectsPage.tsx"
      to: "taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx"
      via: "import { AioProjectsSkeleton } from './AioProjectsSkeleton'"
      pattern: "AioProjectsSkeleton"
    - from: "taskflow/src/routes/dashboard/AioProjectsPage.tsx"
      to: "React Query"
      via: "useQuery queryKey: ['aio', jiraBaseUrl, 'projects']"
      pattern: "aio.*jiraBaseUrl.*projects"
---

<objective>
Build AioProjectsPage and AioProjectsSkeleton — the AIO projects list route. This is a direct mirror of EpicsPage.tsx with AIO-specific data, query key, and navigation target.

Purpose: After this plan, navigating to /aio-projects renders the full page with table rows for each AIO test project. AioProjectsPage.test.tsx stubs (3 cases) turn GREEN.
Output: AioProjectsPage.tsx (default export), AioProjectsSkeleton.tsx (named export).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/52-aio-navigation-project-pages/52-CONTEXT.md
@.planning/phases/52-aio-navigation-project-pages/52-PATTERNS.md
@.planning/phases/52-aio-navigation-project-pages/52-UI-SPEC.md

<interfaces>
<!-- Key imports and contracts this page component uses. Executor must not explore these — use as given. -->

From taskflow/src/services/aio (index barrel, via Plan 01):
- fetchAioProjects(baseUrl: string, token: string): Promise<AioProject[]>
- AioProject { id: number; projectKey: string; name: string }

From taskflow/src/stores/auth.store.ts:
- useAuthStore() returns { jiraBaseUrl: string | null, ... }

From taskflow/src/services/stronghold:
- readSecret(key: string): Promise<string>
- key for Jira PAT: 'jira-pat'

From @tanstack/react-query:
- useQuery<T>({ queryKey, queryFn, enabled })
- useQueryClient() — for invalidateQueries on retry

From taskflow/src/hooks/useDelayedLoading.ts:
- useDelayedLoading(isLoading: boolean): boolean — returns true after 200ms, preventing skeleton flash

From taskflow/src/components/ui/empty-state.tsx:
- EmptyState({ icon, title, subtitle }) — icon is a ComponentType (e.g. FlaskConical)

From taskflow/src/components/ui/error-state.tsx:
- ErrorState({ error, onRetry, viewName }) — viewName used in error copy

Query key (locked in CONTEXT.md):
- ['aio', jiraBaseUrl, 'projects'] — used for both queryKey and invalidateQueries

Navigation target on row click (D-06):
- navigate(`/aio-project/${project.projectKey}`) — use useNavigate() hook

UI-SPEC page layout:
- Outer: flex flex-col h-full
- Header: px-6 py-4 border-b border-border flex-shrink-0 / h1 text-xl font-semibold "AIO Projects"
- Content: flex-1 overflow-auto
- Skeleton wrapper: p-4 around AioProjectsSkeleton (skeleton itself has NO internal p-4)
- Table: w-full text-sm / thead border-b bg-muted/10 / tbody rows
- Row: border-b border-border hover:bg-muted/30 transition-colors cursor-pointer
- Name column: px-4 py-3 / Key column: px-3 py-3 text-xs text-muted-foreground font-mono

UI-SPEC table headers (Copywriting Contract):
- Name column header: "Name"
- Key column header: "Key"

UI-SPEC empty state copy:
- title: "No test projects found"
- subtitle: "AIO test projects will appear here once configured in your Jira instance"
- icon: FlaskConical

UI-SPEC error state: viewName="AIO projects"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AioProjectsSkeleton.tsx (no internal padding)</name>
  <files>taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/EpicsSkeleton.tsx — read the actual file; note that EpicsSkeleton has internal p-4 (confirmed pitfall from RESEARCH.md); AioProjectsSkeleton must NOT replicate this — the page wrapper provides p-4 exclusively
    - taskflow/src/components/ui/skeleton.tsx — confirm Skeleton import path
  </read_first>
  <action>
    Create taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx as a named export component.

    Import: import { Skeleton } from '@/components/ui/skeleton'

    Function: export function AioProjectsSkeleton()

    Return: a div with className="flex flex-col gap-2" (NO p-4 — the page wrapper provides padding)
    containing 5 Skeleton elements: {[0, 1, 2, 3, 4].map((i) => (<Skeleton key={i} className="h-10 w-full" />))}

    The absence of p-4 is intentional and must not be "corrected" — it matches the UI-SPEC skeleton padding rule and avoids the EpicsSkeleton double-padding pitfall.
  </action>
  <verify>
    <automated>grep -n "p-4" /Users/mimo/Documents/Projects/taskflow/taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx exists
    - File contains: export function AioProjectsSkeleton()
    - File contains: className="flex flex-col gap-2" (no p-4)
    - File contains: Skeleton key={i} className="h-10 w-full"
    - grep "p-4" on the file returns empty (no internal padding — critical)
  </acceptance_criteria>
  <done>AioProjectsSkeleton.tsx exists, 5 rows h-10 w-full, no internal p-4</done>
</task>

<task type="auto">
  <name>Task 2: Create AioProjectsPage.tsx and pass AioProjectsPage.test.tsx</name>
  <files>taskflow/src/routes/dashboard/AioProjectsPage.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/EpicsPage.tsx — read the complete file; this is the direct analog. Extract: import block, useAuthStore usage, readSecret useEffect pattern, useQuery invocation, useDelayedLoading, error/skeleton/empty/data render structure
    - taskflow/src/routes/dashboard/AioProjectsPage.test.tsx — read the Wave 0 stub to understand exactly what assertions the tests will make (which text strings, which component queries, which mock return values)
    - taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx — confirm the import path for the skeleton component created in Task 1
  </read_first>
  <action>
    Create taskflow/src/routes/dashboard/AioProjectsPage.tsx as a default export function component.

    Imports (in order, using @/ alias):
    - useQuery, useQueryClient from @tanstack/react-query
    - FlaskConical from lucide-react
    - useEffect, useState from react
    - useNavigate from react-router-dom
    - EmptyState from @/components/ui/empty-state
    - ErrorState from @/components/ui/error-state
    - useDelayedLoading from @/hooks/useDelayedLoading
    - fetchAioProjects, and type AioProject from @/services/aio
    - readSecret from @/services/stronghold
    - useAuthStore from @/stores/auth.store
    - AioProjectsSkeleton from ./AioProjectsSkeleton

    Component body:
    1. const { jiraBaseUrl } = useAuthStore()
    2. const [token, setToken] = useState<string | null>(null)
    3. useEffect: readSecret('jira-pat').then(setToken).catch(() => setToken(null)) — dependency array []
    4. const queryClient = useQueryClient()
    5. const navigate = useNavigate()
    6. useQuery<AioProject[]> with queryKey: ['aio', jiraBaseUrl, 'projects'], queryFn: () => fetchAioProjects(jiraBaseUrl!, token!), enabled: !!jiraBaseUrl && !!token
    7. const showSkeleton = useDelayedLoading(isLoading)

    Render:
    - Outer div: className="flex flex-col h-full"
    - Page header div: className="flex items-center justify-between px-6 py-4 border-b border-border flex-shrink-0"
      containing h1 className="text-xl font-semibold" with text "AIO Projects"
    - Content div: className="flex-1 overflow-auto"
      States in this order:
      a. Error (no cached data): {isError && !data && <div className="p-4"><ErrorState error={error} onRetry={() => queryClient.invalidateQueries({ queryKey: ['aio', jiraBaseUrl, 'projects'] })} viewName="AIO projects" /></div>}
      b. Skeleton: {showSkeleton ? <div className="p-4"><AioProjectsSkeleton /></div> : ...}
      c. After skeleton gate — data table or empty state:
         Table: className="w-full text-sm" with thead className="border-b bg-muted/10"
         Headers: "Name" (auto width, px-4 py-2) + "Key" (w-28, px-3 py-2) — both text-left text-xs font-medium text-muted-foreground
         Rows: map over data ?? [] — tr key={project.id} className="border-b border-border hover:bg-muted/30 transition-colors cursor-pointer" onClick={() => navigate(`/aio-project/${project.projectKey}`)}
         Name td: px-4 py-3, text content project.name
         Key td: px-3 py-3, inner span className="font-mono text-xs text-muted-foreground" with project.projectKey
         Empty state: when data !== undefined && data.length === 0, render EmptyState icon={FlaskConical} title="No test projects found" subtitle="AIO test projects will appear here once configured in your Jira instance"
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run src/routes/dashboard/AioProjectsPage.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/dashboard/AioProjectsPage.tsx exists
    - File contains: export default function AioProjectsPage()
    - File contains: queryKey: ['aio', jiraBaseUrl, 'projects']
    - File contains: fetchAioProjects(jiraBaseUrl!, token!)
    - File contains: enabled: !!jiraBaseUrl && !!token
    - File contains: navigate(`/aio-project/${project.projectKey}`)
    - File contains: viewName="AIO projects"
    - File contains: title="No test projects found"
    - File contains: AioProjectsSkeleton
    - npx vitest run src/routes/dashboard/AioProjectsPage.test.tsx exits 0 (all 3 stubs from Plan 00 GREEN)
    - npx vitest run exits with no additional regressions
  </acceptance_criteria>
  <done>AioProjectsPage.tsx complete. AioProjectsPage.test.tsx 3/3 green. No regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL navigation | navigate() called with project.projectKey from API response — not from user input |
| React Query cache | AioProject[] from AIO API; typed but not validated beyond TypeScript cast |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | projectKey used in navigate() target | accept | projectKey comes from AIO API response (trusted, authenticated source), not from URL params on this page. encodeURIComponent is not needed on navigate() since React Router handles encoding. The actual URL path injection threat is in cycles.ts (mitigated in Plan 01). |
| T-52-02 | Information Disclosure | ErrorState viewName in error display | accept | viewName="AIO projects" is a static string — no credentials or tokens in error output. ErrorState component confirmed to not expose internals. |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/AioProjectsPage.test.tsx` — exits 0, 3 passed
- `grep -n "p-4" taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx` — empty (no internal padding)
- `grep "queryKey.*aio.*jiraBaseUrl.*projects" taskflow/src/routes/dashboard/AioProjectsPage.tsx` — 1 match
- `cd taskflow && npx vitest run` — all existing tests plus 3 new tests green; Sidebar (2) + cycles (5) also green
</verification>

<success_criteria>
AioProjectsPage renders project list correctly. AioProjectsSkeleton has no double-padding. AioProjectsPage.test.tsx 3/3 green. Full suite regression-free.
</success_criteria>

<output>
After completion, create `.planning/phases/52-aio-navigation-project-pages/52-03-SUMMARY.md`
</output>
