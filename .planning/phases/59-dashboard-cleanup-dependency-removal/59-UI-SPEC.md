---
phase: 59
slug: dashboard-cleanup-dependency-removal
status: approved
reviewed_at: 2026-05-20T00:00:00Z
shadcn_initialized: true
preset: base-nova
created: 2026-05-20
---

# Phase 59 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Phase Character

Phase 59 is a deletion phase. It produces no new UI surfaces. The only UI output is a one-line stub component (`export default function Dashboard() { return <div />; }`) that holds the `/dashboard` route open for Phase 60 to overwrite. All design contract sections below document what is being removed and confirm the surviving design system state.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova (source: components.json) |
| Component library | Radix UI (via shadcn base-nova) |
| Icon library | lucide-react (source: components.json `iconLibrary`) |
| Font | Geist Variable (source: index.css `@fontsource-variable/geist`) |
| Tailwind version | v4 (source: npx shadcn info) |
| CSS variable mode | oklch tokens via `@theme inline` in index.css |

No changes to design system in this phase. shadcn registration is `registries: {}` — no third-party registries declared.

---

## Spacing Scale

Declared values (must be multiples of 4). This phase makes no spacing changes; existing scale documented for executor reference.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none. The dashboard stub (`<div />`) has no spacing requirements.

---

## Typography

Existing typography tokens preserved. No new typographic elements introduced in this phase.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 12px | 400 | 1.4 |
| Heading | 16px | 600 | 1.2 |
| Display | 20px | 600 | 1.2 |

Source: inferred from existing dashboard component patterns (`text-xl font-semibold` = 20px/600, `text-xs text-muted-foreground` = 12px/400, body default = 14px/400). No new sizes added in this phase.

---

## Color

Existing color tokens preserved unchanged. No new color usage introduced. The empty stub produces no rendered color.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--background` oklch(1 0 0) light / oklch(0.145 0 0) dark | Page background, surfaces |
| Secondary (30%) | `--card` oklch(1 0 0) light / oklch(0.205 0 0) dark | Cards, sidebar, nav |
| Accent (10%) | `--primary` oklch(0.205 0 0) light / oklch(0.922 0 0) dark | Active sidebar items, primary buttons |
| Destructive | `--destructive` oklch(0.577 0.245 27.325) light / oklch(0.704 0.191 22.216) dark | Destructive actions only |

Accent reserved for: active sidebar navigation item highlight, primary action buttons. Not used for informational UI.

Source: index.css `@layer base` — all values pre-existing.

---

## Copywriting Contract

Phase 59 has no new user-visible copy. The only output is a blank `<div />` stub. No text, no headings, no CTAs, no empty states, no errors.

| Element | Copy |
|---------|------|
| Primary CTA | none — this phase introduces no CTAs |
| Empty state heading | none — stub is `<div />` with no content (source: CONTEXT.md D-01, specifics: "No 'Dashboard coming soon' text in the stub") |
| Empty state body | none |
| Error state | none — no async operations introduced |
| Destructive confirmation | none — no destructive user actions in this phase |

---

## Components Being Removed

The following components are deleted in this phase. Listed here so the executor and auditor have a complete removal inventory.

| Component | File | Status |
|-----------|------|--------|
| WidgetGrid | `src/routes/dashboard/WidgetGrid.tsx` | DELETE |
| WidgetCard | `src/routes/dashboard/WidgetCard.tsx` | DELETE |
| WidgetPicker | `src/routes/dashboard/WidgetPicker.tsx` | DELETE |
| WorkloadTab | `src/routes/dashboard/WorkloadTab.tsx` | DELETE |
| WorkloadSkeleton | `src/routes/dashboard/WorkloadSkeleton.tsx` | DELETE |
| WorkloadTab test | `src/routes/dashboard/WorkloadTab.test.tsx` | DELETE |
| widgets/ folder | `src/routes/dashboard/widgets/` (11 files + registry.ts) | DELETE |
| Dashboard index | `src/routes/dashboard/index.tsx` | REPLACE with `<div />` stub |

---

## Stub Component Contract

The only UI artifact produced by this phase:

```tsx
export default function Dashboard() {
  return <div />;
}
```

- No imports
- No props
- No logic
- No styling
- Renders nothing visible to the user
- Phase 60 overwrites this file entirely

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none added in this phase | not required |
| third-party | none declared | not applicable |

No new components are added in this phase. `components.json` `registries` field is empty (`{}`). Registry vetting gate: not triggered.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS — no new copy; stub has no text per explicit decision
- [ ] Dimension 2 Visuals: PASS — no new visuals; deletion phase
- [ ] Dimension 3 Color: PASS — existing color system preserved unchanged
- [ ] Dimension 4 Typography: PASS — existing type system preserved unchanged
- [ ] Dimension 5 Spacing: PASS — no spacing changes; stub produces no layout
- [ ] Dimension 6 Registry Safety: PASS — no third-party registries; vetting gate not triggered

**Approval:** pending
