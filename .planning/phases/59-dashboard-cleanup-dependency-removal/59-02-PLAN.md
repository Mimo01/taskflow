---
phase: 59-dashboard-cleanup-dependency-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/routes.tsx
  - taskflow/src/components/app/sidebar-items.ts
  - taskflow/src/main.tsx
  - taskflow/src/routes/dashboard/WikiRenderer.tsx
  - taskflow/src/routes/dashboard/DiscussionThreads.tsx
autonomous: true
requirements:
  - REMOVE-01

must_haves:
  truths:
    - "D-02: All workload references are scrubbed in Phase 59 — not deferred. The five consumers (routes.tsx, sidebar-items.ts, main.tsx routeLabel, WikiRenderer.tsx staticLabels, DiscussionThreads.tsx staticLabels) are surgically edited in this plan."
    - "Navigating to /workload no longer matches any route entry in routes.tsx (per D-02)"
    - "The sidebar does not render a Workload nav item (per D-02)"
    - "The header / breadcrumb label resolver in main.tsx no longer maps /workload to 'Workload' (per D-02)"
    - "The WikiRenderer and DiscussionThreads staticLabels records no longer map /workload to 'Workload' (per D-02)"
    - "TypeScript compiles after these edits"
  artifacts:
    - path: "taskflow/src/routes/routes.tsx"
      provides: "Route table without /workload (D-02)"
      contains: "withLazy"
    - path: "taskflow/src/components/app/sidebar-items.ts"
      provides: "Sidebar inventory without workload (D-02)"
      contains: "SIDEBAR_NAV_ITEMS"
  key_links:
    - from: "taskflow/src/routes/routes.tsx"
      to: "./dashboard/WorkloadTab"
      via: "lazy import (MUST NOT EXIST after this plan)"
      pattern: "WorkloadTab"
    - from: "taskflow/src/components/app/sidebar-items.ts"
      to: "/workload path"
      via: "SIDEBAR_NAV_ITEMS entry (MUST NOT EXIST after this plan)"
      pattern: "'workload'|'/workload'"
---

<objective>
Scrub all five non-store, non-widget references to `/workload` and `Workload` across the routing, sidebar inventory, header label resolver, and the two `staticLabels` records in WikiRenderer/DiscussionThreads. After this plan there is no functional or label-mapping wiring left that points to the deleted Workload page.

Purpose: Plan 01 deletes the WorkloadTab and WorkloadSkeleton source files; this plan removes every remaining consumer so the deleted page is unreachable and unmentioned. Per CONTEXT.md D-02, all workload references are scrubbed in Phase 59 — not deferred to Phase 63. The five touched files have zero overlap with Plan 01, so this plan runs in parallel in Wave 1.

Output: Five surgical edits removing the workload route entry, sidebar item, header label branch, and two `staticLabels` entries (all per D-02).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-dashboard-cleanup-dependency-removal/59-CONTEXT.md
@.planning/phases/59-dashboard-cleanup-dependency-removal/59-PATTERNS.md
@taskflow/src/routes/routes.tsx
@taskflow/src/components/app/sidebar-items.ts
@taskflow/src/main.tsx
@taskflow/src/routes/dashboard/WikiRenderer.tsx
@taskflow/src/routes/dashboard/DiscussionThreads.tsx

<interfaces>
<!-- Verified line numbers from grep at planning time: -->

<!-- routes.tsx -->
<!-- Line 16: const WorkloadTab = lazy(() => import('./dashboard/WorkloadTab')); -->
<!-- Line 44: { path: '/workload', element: withLazy(WorkloadTab) }, -->
<!-- Both lines must be deleted. The /dashboard route entry around line 37 stays unchanged. -->

<!-- sidebar-items.ts -->
<!-- Line 74:  { id: 'workload', label: 'Workload', path: '/workload', iconName: 'Users', section: 'tracking' }, -->
<!-- Line 111: 'workload', — entry inside the pmVisible Set constructor -->
<!-- devVisible (lines ~94-101) does NOT contain 'workload' — leave it untouched. -->

<!-- main.tsx -->
<!-- Line 292: if (pathname.startsWith('/workload')) return 'Workload'; — single-line deletion inside routeLabel() -->

<!-- WikiRenderer.tsx -->
<!-- Line 869 (inside staticLabels: Record<string, string> object spanning ~lines 862-873): -->
<!--   '/workload': 'Workload', -->
<!-- Single-line deletion; rest of record unchanged. -->

<!-- DiscussionThreads.tsx -->
<!-- Line 60 (inside staticLabels: Record<string, string> object spanning ~lines 53-64): -->
<!--   '/workload': 'Workload', -->
<!-- Same pattern as WikiRenderer — single-line deletion. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove /workload route entry and lazy import (D-02)</name>
  <files>taskflow/src/routes/routes.tsx</files>
  <read_first>taskflow/src/routes/routes.tsx</read_first>
  <action>
    In `taskflow/src/routes/routes.tsx` (per D-02: workload reference removal):
    - Delete the line `const WorkloadTab = lazy(() =&gt; import('./dashboard/WorkloadTab'));` (currently line 16).
    - Delete the line `{ path: '/workload', element: withLazy(WorkloadTab) },` from the routes array (currently line 44).
    - Do not touch any other lazy import or route entry. The `/dashboard` route (currently around line 37) stays — its element resolves to the Phase 59 stub from Plan 01.
    After the edits, `grep -nE 'WorkloadTab|/workload' taskflow/src/routes/routes.tsx` must return zero matches.
  </action>
  <verify>
    <automated>! grep -qE 'WorkloadTab|/workload' taskflow/src/routes/routes.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'WorkloadTab|/workload' taskflow/src/routes/routes.tsx` returns 0
    - `grep -c "path: '/dashboard'" taskflow/src/routes/routes.tsx` still returns at least 1 (the dashboard route survives)
    - `cd taskflow &amp;&amp; npx tsc --noEmit` exits 0 (no leaked references to the deleted import)
  </acceptance_criteria>
  <done>routes.tsx no longer imports WorkloadTab or maps `/workload`; the dashboard route is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Workload sidebar nav item and pmVisible reference (D-02)</name>
  <files>taskflow/src/components/app/sidebar-items.ts</files>
  <read_first>taskflow/src/components/app/sidebar-items.ts</read_first>
  <action>
    In `taskflow/src/components/app/sidebar-items.ts` (per D-02):
    - Delete the SIDEBAR_NAV_ITEMS entry `{ id: 'workload', label: 'Workload', path: '/workload', iconName: 'Users', section: 'tracking' },` (currently line 74).
    - Delete the string `'workload',` from the `pmVisible` Set constructor argument list (currently line 111). The surviving entries listed in PATTERNS.md "5. sidebar-items.ts" must remain present and in their existing order: `'dashboard', 'my-tasks', 'sprint-board', 'backlog', 'epics', 'merge-requests', 'sprint-progress', 'releases', 'aio-projects'`.
    - Do not touch the `devVisible` set — verification confirmed it never contained `'workload'`.
    After the edits, `grep -nE "workload|Workload" taskflow/src/components/app/sidebar-items.ts` must return zero matches.
  </action>
  <verify>
    <automated>! grep -qiE 'workload' taskflow/src/components/app/sidebar-items.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ciE 'workload' taskflow/src/components/app/sidebar-items.ts` returns 0
    - `grep -c "'aio-projects'" taskflow/src/components/app/sidebar-items.ts` still returns at least 1 (the rest of pmVisible is intact)
    - `cd taskflow &amp;&amp; npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>sidebar-items.ts contains no workload references; pmVisible retains its 9 surviving entries.</done>
</task>

<task type="auto">
  <name>Task 3: Remove /workload branch from main.tsx routeLabel and from both staticLabels records (D-02)</name>
  <files>
    taskflow/src/main.tsx,
    taskflow/src/routes/dashboard/WikiRenderer.tsx,
    taskflow/src/routes/dashboard/DiscussionThreads.tsx
  </files>
  <read_first>
    taskflow/src/main.tsx,
    taskflow/src/routes/dashboard/WikiRenderer.tsx,
    taskflow/src/routes/dashboard/DiscussionThreads.tsx
  </read_first>
  <action>
    Apply three identical-in-spirit single-line deletions (per D-02). These three files have unrelated logic but share the same removed pair `'/workload' → 'Workload'`.

    1. `taskflow/src/main.tsx` — inside the `routeLabel(pathname: string): string` function (currently around lines 284-299), delete the single line `if (pathname.startsWith('/workload')) return 'Workload';` (currently line 292). All surrounding `if (pathname.startsWith(...))` branches for sprint-board, backlog, my-tasks, epics, dashboard, sprint-progress, releases, issue/, merge-requests, mr/, release/ stay unchanged.

    2. `taskflow/src/routes/dashboard/WikiRenderer.tsx` — inside the `staticLabels: Record&lt;string, string&gt;` object literal (currently lines 862-873), delete the single entry `'/workload': 'Workload',` (currently line 869). Surrounding entries for `/sprint-board`, `/backlog`, `/my-tasks`, `/epics`, `/dashboard`, `/sprint-progress`, `/releases`, `/merge-requests` remain.

    3. `taskflow/src/routes/dashboard/DiscussionThreads.tsx` — inside the analogous `staticLabels: Record&lt;string, string&gt;` object literal (currently lines 53-64), delete the same `'/workload': 'Workload',` entry (currently line 60). Same surviving entries as WikiRenderer.

    After all three edits, `grep -rnE "/workload|'Workload'" taskflow/src/main.tsx taskflow/src/routes/dashboard/WikiRenderer.tsx taskflow/src/routes/dashboard/DiscussionThreads.tsx` must return zero matches.
  </action>
  <verify>
    <automated>! grep -qE "/workload|'Workload'" taskflow/src/main.tsx taskflow/src/routes/dashboard/WikiRenderer.tsx taskflow/src/routes/dashboard/DiscussionThreads.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "/workload" taskflow/src/main.tsx` returns 0
    - `grep -cE "/workload" taskflow/src/routes/dashboard/WikiRenderer.tsx` returns 0
    - `grep -cE "/workload" taskflow/src/routes/dashboard/DiscussionThreads.tsx` returns 0
    - `grep -c "/sprint-board" taskflow/src/routes/dashboard/WikiRenderer.tsx` returns at least 1 (other staticLabels entries preserved)
    - `grep -c "/sprint-board" taskflow/src/routes/dashboard/DiscussionThreads.tsx` returns at least 1
    - `grep -c "if (pathname.startsWith('/sprint-board'))" taskflow/src/main.tsx` returns at least 1 (other routeLabel branches preserved)
    - `cd taskflow &amp;&amp; npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All three files no longer mention `/workload` or render the `Workload` label; surrounding logic untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → route resolver | A user navigating to `/workload` crosses into the React Router matcher. After this plan, no route entry matches that path. |
| URL → breadcrumb / label | The `routeLabel` function and two `staticLabels` records translate a URL pathname into a user-visible string. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-06 | Denial of Service | /workload navigation post-removal | accept | React Router falls through to the existing not-found / catch-all handler; no crash. ROADMAP success criterion 1 explicitly accepts this behavior. |
| T-59-07 | Information Disclosure | breadcrumb label leakage | mitigate | All three label sources (`main.tsx routeLabel`, `WikiRenderer staticLabels`, `DiscussionThreads staticLabels`) are scrubbed in the same plan, preventing one source from rendering a stale "Workload" string while the route is gone. |
| T-59-08 | Tampering | route table mutation ordering | accept | This plan touches five disjoint files with no shared state; ordering inside the plan is irrelevant. |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `grep -rciE 'workload' taskflow/src/routes/routes.tsx taskflow/src/components/app/sidebar-items.ts taskflow/src/main.tsx taskflow/src/routes/dashboard/WikiRenderer.tsx taskflow/src/routes/dashboard/DiscussionThreads.tsx` returns 0 across all five files.
- `cd taskflow && npx tsc --noEmit` exits 0.
- Existing tests under `taskflow/src/components/app/` and `taskflow/src/routes/dashboard/WikiRenderer.test.tsx` still pass: `cd taskflow && npm test -- --run src/routes/dashboard/WikiRenderer.test.tsx` exits 0.
</verification>

<success_criteria>
- All five files compile and have zero remaining references to `/workload` or `Workload` (D-02 satisfied)
- Other route entries, sidebar items, label branches, and staticLabels entries remain untouched
- Phase 59 success criteria 1 and 2 (no `/workload` route, no Workload sidebar entry) are satisfied
</success_criteria>

<output>
Create `.planning/phases/59-dashboard-cleanup-dependency-removal/59-02-SUMMARY.md` when done.
</output>
