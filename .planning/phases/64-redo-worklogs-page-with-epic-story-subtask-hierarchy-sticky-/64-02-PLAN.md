---
phase: 64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-
plan: 02
type: execute
wave: 2
depends_on:
  - 64-01
files_modified:
  - taskflow/src/routes/worklogs/WorklogCellPopover.tsx
  - taskflow/src/routes/worklogs/WorklogEntryRow.tsx
  - taskflow/src/routes/worklogs/EditWorklogForm.tsx
  - taskflow/src/routes/worklogs/WorklogsPage.tsx
  - taskflow/src/routes/worklogs/WorklogsPage.test.tsx
autonomous: false
requirements:
  - TEMPO-EDIT-01
user_setup: []
tags:
  - worklogs
  - cell-popover
  - jira-worklog-crud
  - mutations
  - cache-invalidation

must_haves:
  truths:
    - "Clicking any non-zero data cell opens a popover listing the individual Tempo worklog entries for that issue+day"
    - "Each entry row in the popover shows time spent, author display name, optional comment, plus pencil (edit) and trash (delete) icons"
    - "Clicking the trash icon deletes the worklog via deleteWorklog and the table refetches immediately"
    - "Clicking the pencil icon swaps the entry row in place with an EditWorklogForm (duration + date + comment + Save/Discard)"
    - "Saving the edit calls updateWorklog with the existing worklog ID and refetches the table"
    - "An 'Add entry' button at the bottom of the popover opens LogWorkPopover prefilled with the cell's issueKey, and on successful create the Tempo table refetches"
    - "Mutation pending states keep the popover open until onSuccess; the popover does not close on the mutate() call"
    - "Zero-hour cells render blank with cursor-default and have no popover trigger"
  artifacts:
    - path: "taskflow/src/routes/worklogs/WorklogCellPopover.tsx"
      provides: "Cell drill-down popover with entry list + add-entry section"
      contains: "Popover, PopoverContent, PopoverTrigger, queryClient.invalidateQueries(['tempo', 'worklogs'])"
    - path: "taskflow/src/routes/worklogs/WorklogEntryRow.tsx"
      provides: "Single Tempo worklog entry row with edit/delete affordances"
      contains: "deleteWorklog, Pencil, Trash2, EditWorklogForm"
    - path: "taskflow/src/routes/worklogs/EditWorklogForm.tsx"
      provides: "Inline edit form for an existing worklog (duration + date + comment)"
      contains: "updateWorklog, parseDuration, DurationInput"
  key_links:
    - from: "taskflow/src/routes/worklogs/WorklogsPage.tsx"
      to: "WorklogCellPopover"
      via: "non-zero data cell button trigger"
      pattern: "WorklogCellPopover"
    - from: "taskflow/src/routes/worklogs/WorklogEntryRow.tsx"
      to: "jira/worklogs.deleteWorklog"
      via: "useMutation"
      pattern: "deleteWorklog\\("
    - from: "taskflow/src/routes/worklogs/EditWorklogForm.tsx"
      to: "jira/worklogs.updateWorklog"
      via: "useMutation"
      pattern: "updateWorklog\\("
    - from: "all mutation onSuccess callbacks"
      to: "TanStack Query cache"
      via: "invalidateQueries"
      pattern: "queryKey: \\['tempo', 'worklogs'\\]"
---

<objective>
Add cell drill-down editing on top of the Phase 64 hierarchy table (Plan 01). Wire a `WorklogCellPopover` to every non-zero data cell that shows the individual Tempo worklog entries for the cell's `(issueKey, date)` pair with inline edit/delete affordances and an "Add entry" section that reuses `LogWorkPopover` from `issue-detail/`.

Purpose: Delivers the "log entry editing" half of Phase 64's goal and the v2-derived TEMPO-09 cell drill-down. Uses the existing Jira worklog CRUD (`createWorklog`, `updateWorklog`, `deleteWorklog`) and the established `IssueDetailPage` mutation+invalidation pattern; no new Tempo write API is introduced.
Output: Three new sibling components (`WorklogCellPopover.tsx`, `WorklogEntryRow.tsx`, `EditWorklogForm.tsx`) + cell-button trigger wiring in `WorklogsPage.tsx` + new test coverage + a final human-verify checkpoint for the interactive behavior.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@.planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-CONTEXT.md
@.planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-RESEARCH.md
@.planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-PATTERNS.md
@.planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-UI-SPEC.md
@.planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-01-SUMMARY.md

<interfaces>
<!-- Existing exports the new components MUST use. Verified by codebase inspection. -->

From taskflow/src/services/jira/worklogs.ts:
```typescript
export async function createWorklog(baseUrl: string, token: string, issueKey: string,
  params: { timeSpentSeconds: number; started: string; comment?: string }): Promise<void>;

export async function updateWorklog(baseUrl: string, token: string, issueKey: string,
  worklogId: string,
  params: { timeSpentSeconds: number; started: string; comment?: string }): Promise<void>;

export async function deleteWorklog(baseUrl: string, token: string, issueKey: string,
  worklogId: string): Promise<void>;
```

From taskflow/src/routes/dashboard/issue-detail/LogWorkPopover.tsx (line 20–24):
```typescript
interface LogWorkPopoverProps {
  issueKey: string;
  jiraBaseUrl: string;
  onSuccess?: () => void;
}
export function LogWorkPopover(props: LogWorkPopoverProps): JSX.Element;
```

From taskflow/src/routes/dashboard/issue-detail/DurationInput.tsx (line 12–19):
```typescript
interface DurationInputProps {
  value: string;
  onChange: (v: string) => void;
  error?: string | null;
  autoFocus?: boolean;
}
export function DurationInput(props: DurationInputProps): JSX.Element;
```

From taskflow/src/services/jira/duration:
```typescript
export function parseDuration(input: string): { seconds: number } | null;
```

From taskflow/src/services/stronghold:
```typescript
export function readSecret(name: 'jira-pat'): Promise<string>;
```

From taskflow/src/services/tempo/types.ts:
```typescript
// (see Plan 01 — same TempoWorklog interface; jiraWorklogId is the field used for CRUD)
```

NEW component shapes (this plan creates them):
```typescript
// WorklogCellPopover.tsx
interface WorklogCellPopoverProps {
  issueKey: string;
  date: string; // YYYY-MM-DD
  entries: TempoWorklog[]; // worklog entries for this (issueKey, date) cell
  jiraBaseUrl: string;
  totalSeconds: number; // pre-aggregated cell total, used as trigger label
  dayColClassName?: string; // schedule-day background class from parent
  children?: never;
}
export function WorklogCellPopover(props: WorklogCellPopoverProps): JSX.Element;

// WorklogEntryRow.tsx
interface WorklogEntryRowProps {
  entry: TempoWorklog;
  issueKey: string;
  jiraBaseUrl: string;
  onMutationSuccess: () => void;
}
export function WorklogEntryRow(props: WorklogEntryRowProps): JSX.Element;

// EditWorklogForm.tsx
interface EditWorklogFormProps {
  entry: TempoWorklog;
  issueKey: string;
  jiraBaseUrl: string;
  onDiscard: () => void;
  onSuccess: () => void;
}
export function EditWorklogForm(props: EditWorklogFormProps): JSX.Element;
```

Cache invalidation key (broad prefix — catches all `[jiraBaseUrl, from, to, username]` suffixes):
```typescript
queryClient.invalidateQueries({ queryKey: ['tempo', 'worklogs'] });
```

Jira worklog API requires `+0000` offset, not `Z` (CRITICAL — already proven in LogWorkPopover.tsx line 75):
```typescript
const started = new Date(`${date}T12:00:00`).toISOString().replace('Z', '+0000');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create WorklogEntryRow + EditWorklogForm sibling components</name>
  <files>taskflow/src/routes/worklogs/WorklogEntryRow.tsx, taskflow/src/routes/worklogs/EditWorklogForm.tsx, taskflow/src/routes/worklogs/WorklogsPage.test.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/IssueDetailPage.tsx lines 250–340 (reference: `worklogEditMutation`, `worklogDeleteMutation`, `handleWorklogEdit` — direct model for the mutation handlers; copy the pattern, not the file)
    - taskflow/src/routes/dashboard/issue-detail/LogWorkPopover.tsx lines 66–138 (reference: form layout — Label, DurationInput, Input type=date, Textarea, Button Save + Discard)
    - taskflow/src/routes/dashboard/issue-detail/DurationInput.tsx (entire 90-ish lines — confirm prop shape used by EditWorklogForm)
    - taskflow/src/services/jira/worklogs.ts (confirm updateWorklog/deleteWorklog signatures and `started` format)
    - taskflow/src/services/tempo/types.ts (confirm `entry.jiraWorklogId` and `entry.tempoWorklogId` are both optional `number`)
    - .planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-UI-SPEC.md (§WorklogEntryRow Layout; §EditWorklogForm; §Copywriting — Save Changes / Discard Changes / Saving…)
    - .planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-PATTERNS.md (§WorklogEntryRow.tsx, §EditWorklogForm.tsx)
  </read_first>
  <behavior>
    - WorklogEntryRow renders a horizontal row: `[time spent in bold] [author displayName] [optional truncated comment max-w-[100px]] [Pencil button (size-3.5)] [Trash2 button (size-3.5)]`.
    - Trash button click triggers a `deleteMutation`: resolves Jira PAT via `readSecret('jira-pat')`, throws `'No token'` if missing, computes `worklogId = entry.jiraWorklogId?.toString() ?? entry.tempoWorklogId?.toString()`, throws `'No worklog ID'` if both are missing, calls `deleteWorklog(jiraBaseUrl, token, issueKey, worklogId)`. On success calls `onMutationSuccess()`. Trash button is disabled while `deleteMutation.isPending`.
    - Pencil button click sets internal `editing` state to true; while editing, the row renders `<EditWorklogForm entry={entry} issueKey={issueKey} jiraBaseUrl={jiraBaseUrl} onDiscard={() => setEditing(false)} onSuccess={() => { setEditing(false); onMutationSuccess(); }} />` in place of the normal row layout.
    - EditWorklogForm initializes `duration` from `formatSeconds(entry.timeSpentSeconds)` (e.g. `'2h 30m'`), `date` from `entry.dateStarted` (already YYYY-MM-DD), `comment` from `entry.comment ?? ''`.
    - On Save click: validate `parseDuration(duration)` → if null, show inline error `"Invalid duration"` and stop. Otherwise compute `started = new Date(date + 'T12:00:00').toISOString().replace('Z', '+0000')`, then call `updateWorklog(jiraBaseUrl, token, issueKey, worklogId, { timeSpentSeconds: parsed.seconds, started, comment: comment.trim() || undefined })`. On success calls `onSuccess()`.
    - Save button copy: `"Save Changes"` normally, `"Saving…"` while pending; disabled while pending. Discard button: variant `ghost`, label `"Discard Changes"`, click calls `onDiscard()`.
    - All buttons have `type="button"` to prevent accidental form submission. Trash and Pencil buttons have `aria-label="Delete worklog entry"` and `"Edit worklog entry"` respectively.
    - Components are pure presentational + their own mutations — they receive `jiraBaseUrl` and `issueKey` via props and call `readSecret` internally (matching LogWorkPopover.tsx pattern).
  </behavior>
  <action>Create `taskflow/src/routes/worklogs/EditWorklogForm.tsx` exporting `EditWorklogForm(props: EditWorklogFormProps)` with the shape declared in `<interfaces>`. Implementation mirrors `IssueDetailPage.tsx` lines 256–285 for the `editMutation` and `LogWorkPopover.tsx` lines 96–134 for the form JSX, adapted: pre-populate `duration` via a small `formatSecondsForInput(secs)` helper that emits `'2h 30m'` style strings (it's fine to reuse the existing `formatSeconds` shape but it must NEVER return empty string when seconds > 0 — guard against that case by using `'0m'` for sub-minute totals). Apply `started = new Date(date + 'T12:00:00').toISOString().replace('Z', '+0000')`. Show inline `<p className="text-xs text-destructive">{error}</p>` for both `parseDuration` failures and mutation errors. Create `taskflow/src/routes/worklogs/WorklogEntryRow.tsx` exporting `WorklogEntryRow(props: WorklogEntryRowProps)`. Local state: `const [editing, setEditing] = useState(false)`. The `deleteMutation` follows IssueDetailPage.tsx lines 287–296 exactly (substituting prop-supplied `jiraBaseUrl`/`issueKey`). Render the row layout per UI-SPEC §WorklogEntryRow Layout (text-xs, gap-2, icons size-3.5). On `editing === true`, render `<EditWorklogForm ... />` instead of the row layout. In `WorklogsPage.test.tsx`, add four new tests scoped to these components (mount via a thin test harness that renders each component in isolation; queryClient via `QueryClientProvider` already present in the file's existing helpers — reuse, do not re-create): (a) "WorklogEntryRow shows time, author, and comment fields" — assert all three text nodes render; (b) "WorklogEntryRow trash button calls deleteWorklog with jiraWorklogId" — click trash; assert the `deleteWorklog` mock was called with `(jiraBaseUrl, token, issueKey, jiraWorklogId.toString())`; (c) "WorklogEntryRow pencil button swaps in EditWorklogForm" — click pencil; assert `Save Changes` button is now present; (d) "EditWorklogForm save calls updateWorklog with parsed seconds and +0000 started" — fill form, click Save, assert `updateWorklog` mock called with `{ timeSpentSeconds, started: ${matches /\+0000$/}, comment }`.</action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/worklogs/WorklogsPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `taskflow/src/routes/worklogs/EditWorklogForm.tsx` exists and exports `EditWorklogForm`
    - File `taskflow/src/routes/worklogs/WorklogEntryRow.tsx` exists and exports `WorklogEntryRow`
    - `EditWorklogForm.tsx` contains the literal `.replace('Z', '+0000')`
    - `EditWorklogForm.tsx` imports `parseDuration` from `@/services/jira/duration` and `updateWorklog` from `@/services/jira/worklogs`
    - `EditWorklogForm.tsx` contains `Save Changes` and `Discard Changes` string literals (per UI-SPEC Copywriting Contract)
    - `EditWorklogForm.tsx` contains `Saving…` string literal (the ellipsis is U+2026, matching `LogWorkPopover.tsx` style — confirm or use three ASCII dots if codebase convention differs; check `grep "Logging\.\.\." taskflow/src/routes/dashboard/issue-detail/LogWorkPopover.tsx` to align)
    - `WorklogEntryRow.tsx` imports `deleteWorklog` from `@/services/jira/worklogs` and uses `Pencil` and `Trash2` from `lucide-react`
    - `WorklogEntryRow.tsx` contains `aria-label="Edit worklog entry"` and `aria-label="Delete worklog entry"`
    - `cd taskflow && npx vitest run src/routes/worklogs/WorklogsPage.test.tsx` exits 0
    - The four new tests (`WorklogEntryRow shows time`, `WorklogEntryRow trash button calls deleteWorklog`, `WorklogEntryRow pencil button swaps in EditWorklogForm`, `EditWorklogForm save calls updateWorklog`) pass — verify by `grep -c "WorklogEntryRow shows time\|trash button calls deleteWorklog\|pencil button swaps in\|save calls updateWorklog" taskflow/src/routes/worklogs/WorklogsPage.test.tsx` ≥ 4
    - `cd taskflow && npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>Two sibling components implementing the per-entry view + edit interactions exist, have unit tests, and compile with the existing TypeScript config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create WorklogCellPopover + wire it into non-zero cells in WorklogsPage.tsx</name>
  <files>taskflow/src/routes/worklogs/WorklogCellPopover.tsx, taskflow/src/routes/worklogs/WorklogsPage.tsx, taskflow/src/routes/worklogs/WorklogsPage.test.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/issue-detail/LogWorkPopover.tsx (entire 140 lines — analog for the popover shell)
    - taskflow/src/routes/worklogs/WorklogsPage.tsx (post-Plan-01 state — find the hierarchy `<td>` data cells in story/subtask rows; this is the integration point)
    - taskflow/src/routes/worklogs/WorklogEntryRow.tsx (just created in Task 1 — confirm prop shape)
    - taskflow/src/components/ui/popover.tsx (confirm shadcn Popover/PopoverContent/PopoverTrigger exports)
    - .planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-UI-SPEC.md (§Cell Click Popover; §Interaction Contracts — Cell Click → Popover, Delete Worklog Entry; §Copywriting — "Add entry")
    - .planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-PATTERNS.md (§WorklogCellPopover.tsx, §Pitfall 4 — nested Radix Popover warning)
    - .planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-RESEARCH.md (§Pitfall 4 — nested popover handling; §Open Question 1 — LogWorkPopover nesting recommendation)
  </read_first>
  <behavior>
    - WorklogCellPopover wraps a `<button type="button">` trigger (the non-zero data cell button) inside a shadcn `<Popover>`. The trigger renders `formatSeconds(totalSeconds)`, has `cursor-pointer`, `hover:bg-accent/60`, and `aria-label="View worklogs for ${issueKey} on ${date}"`.
    - PopoverContent layout (`w-72 p-4`):
      1. Header: `text-xs font-semibold mb-2` reading `${issueKey} · ${formatDayHeader(date)}` (reuse `formatDayHeader` exported from `WorklogsPage.tsx` — promote it to a named export if not already).
      2. Scrollable entry list: `<div className="max-h-48 overflow-y-auto space-y-1">` containing one `<WorklogEntryRow>` per entry, keyed by `entry.jiraWorklogId ?? entry.tempoWorklogId ?? index`. Each row receives `onMutationSuccess={() => queryClient.invalidateQueries({ queryKey: ['tempo', 'worklogs'] })}`.
      3. Separator: `<div className="border-t border-border mt-2 pt-2">`
      4. Add-entry section: `<LogWorkPopover issueKey={issueKey} jiraBaseUrl={jiraBaseUrl} onSuccess={() => queryClient.invalidateQueries({ queryKey: ['tempo', 'worklogs'] })} />` — uses the existing component as a nested popover (acceptable per RESEARCH §Open Question 1 recommendation: nest the popover; if focus/dismiss conflicts manifest during the human-verify checkpoint, fall back to extracting the inline form).
    - The popover does NOT close on mutate; it closes only on `onSuccess` (which removes the row from the list as the parent refetches and re-renders new entries) or on click-away.
    - Zero-hour cells remain unchanged: blank `<td>` with `cursor-default`, no popover trigger.
    - In `WorklogsPage.tsx`, every data-row `<td>` for story and subtask rows that currently renders `formatSeconds(node.dayMap.get(day) ?? 0)` is replaced with a conditional: if `secs > 0`, render `<WorklogCellPopover issueKey={key} date={day} entries={cellEntries} jiraBaseUrl={jiraBaseUrl!} totalSeconds={secs} dayColClassName={dayColClass(dayTypeMap.get(day))} />`; otherwise render the existing blank cell. `cellEntries` is computed as `(data ?? []).filter((w) => w.issue.key === key && w.dateStarted === day)`. For epic rows, only the epic's direct-entry cells (`epic.entries`) are clickable in the same way; subtask-aggregated cells on an epic row should ALSO open a popover that shows ALL underlying entries for the epic-rolled-up amount on that day, BUT the issueKey passed to WorklogCellPopover must remain the epic key — and the entries list scopes to entries whose `issue.key === epicKey` only (i.e., direct epic worklogs, never the rolled-up subtask entries). Document this in a code comment so a future reviewer understands why the epic row popover doesn't show subtask entries.
  </behavior>
  <action>Create `taskflow/src/routes/worklogs/WorklogCellPopover.tsx` exporting `WorklogCellPopover(props: WorklogCellPopoverProps)` with the shape declared in `<interfaces>`. Inside: `const queryClient = useQueryClient()`, `const [open, setOpen] = useState(false)`. Render `<Popover open={open} onOpenChange={setOpen}>` containing `<PopoverTrigger asChild>` wrapping a `<button type="button" className="w-full text-right cursor-pointer hover:bg-accent/60" aria-label={`View worklogs for ${issueKey} on ${date}`}>{formatSeconds(totalSeconds)}</button>`, and `<PopoverContent className="w-72 p-4">` with the four-section layout listed in `<behavior>`. Import `LogWorkPopover` from `@/routes/dashboard/issue-detail/LogWorkPopover` and `formatSeconds` + `formatDayHeader` from `./WorklogsPage`. If `formatSeconds`/`formatDayHeader` are not already exported, promote both to named exports at the top of `WorklogsPage.tsx` (`export function formatSeconds(...)` / `export function formatDayHeader(...)`). In `WorklogsPage.tsx`: import `WorklogCellPopover` from `./WorklogCellPopover`. In the hierarchy `<tbody>` render, for every story-row and subtask-row data cell, wrap the value in a conditional that uses `WorklogCellPopover` when `secs > 0`. The data cell `<td>` itself keeps `className="text-right px-4 py-3 border border-border ${dayColClass(dayTypeMap.get(day))}"`; the cell's inner content is the popover when `secs > 0` or blank otherwise. For epic-direct cells, scope `cellEntries` to entries whose `w.issue.key === epicKey` so the popover doesn't display rolled-up subtask entries (add a code comment explaining this). Add four new tests in `WorklogsPage.test.tsx`: (a) "clicking a non-zero data cell opens WorklogCellPopover" — render hierarchy with one worklog of 2h; click the cell button; assert the popover content (e.g., the entry's time text) is visible; (b) "zero-hour cells do NOT render WorklogCellPopover trigger" — render hierarchy with a day-cell that has 0 seconds; assert no `button[aria-label*="View worklogs"]` in that cell; (c) "popover shows individual entries from raw worklog data" — feed two distinct entries on the same (issueKey, date); open popover; assert both entries' authors render; (d) "trash icon click invokes delete and invalidates tempo cache" — click trash inside open popover; assert `deleteWorklog` mock called once and `queryClient.invalidateQueries` was called with `{ queryKey: ['tempo', 'worklogs'] }` (use a spy on the test's queryClient or assert refetch happened by mocking `apiFetch` for `/rest/tempo-timesheets/3/worklogs` and counting calls).</action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/worklogs/WorklogsPage.test.tsx && cd taskflow && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - File `taskflow/src/routes/worklogs/WorklogCellPopover.tsx` exists and exports `WorklogCellPopover`
    - `WorklogCellPopover.tsx` imports `Popover, PopoverContent, PopoverTrigger` from `@/components/ui/popover`
    - `WorklogCellPopover.tsx` imports `LogWorkPopover` from `@/routes/dashboard/issue-detail/LogWorkPopover`
    - `WorklogCellPopover.tsx` contains `queryKey: ['tempo', 'worklogs']` in an `invalidateQueries` call
    - `WorklogsPage.tsx` imports and references `WorklogCellPopover` (regex `import.*WorklogCellPopover`)
    - `WorklogsPage.tsx` exports `formatSeconds` and `formatDayHeader` (`grep -c "export function formatSeconds\|export function formatDayHeader" taskflow/src/routes/worklogs/WorklogsPage.tsx | grep -v '^#'` ≥ 2)
    - `WorklogsPage.tsx` contains a comment near the epic-row popover wiring explaining why epic-direct cells scope entries to `w.issue.key === epicKey` (search for "epic" and "direct" in proximity)
    - All 4 new tests pass (`grep -c "non-zero data cell opens WorklogCellPopover\|zero-hour cells do NOT render WorklogCellPopover\|popover shows individual entries\|trash icon click invokes delete" taskflow/src/routes/worklogs/WorklogsPage.test.tsx` ≥ 4)
    - `cd taskflow && npx vitest run src/routes/worklogs/WorklogsPage.test.tsx` exits 0
    - `cd taskflow && npx vitest run` exits 0 (full suite green)
    - `cd taskflow && npx tsc --noEmit -p tsconfig.json` exits 0
    - `cd taskflow && npm run build` exits 0 (sanity check — confirms no module resolution issues with the new sibling files)
  </acceptance_criteria>
  <done>WorklogCellPopover exists and is wired into every non-zero story/subtask data cell (and epic-direct cells, with scoped entries). Clicking a cell shows the entry list with edit/delete and an Add-entry section that opens the existing LogWorkPopover. Mutations invalidate the Tempo cache. Full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — hierarchy table + sticky behavior + cell popover interactions</name>
  <files>taskflow/src/routes/worklogs/WorklogsPage.tsx, taskflow/src/routes/worklogs/WorklogCellPopover.tsx, taskflow/src/routes/worklogs/WorklogEntryRow.tsx, taskflow/src/routes/worklogs/EditWorklogForm.tsx</files>
  <read_first>
    - .planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-UI-SPEC.md (full document — interaction contracts, copywriting, sticky CSS expectations)
    - .planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-CONTEXT.md (D-01 through D-15 — the locked decisions you are verifying against)
  </read_first>
  <action>Run the application locally and manually verify every numbered behavior in <how-to-verify> against the locked decisions D-01..D-15. Capture any failures (with item number + description) and return them to the executor for fix-up before re-running the checkpoint. Do not modify code in this task — only verify.</action>
  <verify>
    <human-check>Operator reports `approved` after completing all 10 numbered checks below.</human-check>
  </verify>
  <done>Operator typed `approved` after all 10 checks; or, all reported failures from a previous run have been fixed and re-verified.</done>
  <what-built>Phase 64 worklogs page redesign (Plans 01 + 02 combined): epic/story/subtask hierarchy, sticky date header + first column, leaf-row navigation to `/issue/:key`, cell drill-down popover with per-entry edit / delete / add.</what-built>
  <acceptance_criteria>
    - Operator typed `approved` in the resume signal
    - If any item failed, executor logged the failure list and created a follow-up task before resuming
  </acceptance_criteria>
  <how-to-verify>
    Run `cd taskflow && npm run tauri dev` (or `npm run dev` if developing in the browser). Sign in if needed so Jira PAT is in Stronghold and Tempo is enabled (Settings → Integrations).

    Navigate to **/worklogs** (or click the Tempo sidebar entry).

    Verify each of the following — type `approved` only after every item is checked.

    1. **Hierarchy renders.** With the default "This Week" preset and "me" person filter:
       - Each row corresponds to an issue (epic, story, or subtask), NOT a person.
       - Epic rows show a `Layers` (purple) icon and bold label with a slightly darker background (`bg-muted/40`).
       - Story rows show a `BookOpen` (blue) icon, normal weight, label indented `pl-4`.
       - Subtask rows show a `GitBranch` (muted) icon, normal weight + muted text, label indented `pl-8`.
       - If no stories have a parent epic, a non-clickable "No Epic" header row appears.

    2. **Sticky behavior.** Resize the window narrow enough that the table horizontally scrolls; scroll the table both vertically and horizontally:
       - Date header row (`Mon 19`, `Tue 20`, …, `Total`) stays at the top.
       - Issue-name column on the left stays in view.
       - Top-left corner cell shows "Issue" and stays in both axes (`z-30`).
       - No scrolled content bleeds through the sticky cells (cells have an opaque `bg-background` fill).

    3. **Unresolvable issue handling.** If any worklog has a key not in Jira (or hide an issue type from project permissions to simulate), that row renders the key as `KEY-XXX` with muted strike-through, but its hours still appear in the per-issue total and the day totals row.

    4. **Leaf-row navigation.** Click a subtask row's name area:
       - The app navigates to `/issue/<subtask-key>` (issue detail page renders).
       - The top-of-app navigation trail shows `Worklogs > ISSUE-KEY` (or equivalent).
       - Use the back button or click "Worklogs" in the trail to return.
       - Repeat with a story row and an epic row — both should also navigate.
       - "No Epic" header row should NOT respond to clicks.

    5. **Cell popover — read.** Click any non-zero data cell:
       - A popover opens showing one row per individual Tempo worklog entry for that (issue, day) pair.
       - Each row shows time spent, author display name, optional comment (truncated past ~100px), and pencil + trash icons.
       - Zero-hour cells do NOT respond to clicks (no popover, no `cursor-pointer`).

    6. **Cell popover — delete.** Click the trash icon on one entry:
       - The entry is deleted immediately (no confirmation dialog).
       - The table refetches; the cell's total updates accordingly.
       - The popover stays open until the success arrives; once the entry vanishes from data the popover content updates.

    7. **Cell popover — edit.** Click the pencil icon on a different entry:
       - The row swaps in place with a form (Duration + Date + Comment + Save Changes / Discard Changes).
       - Edit the duration (e.g. change `2h` to `3h 15m`), click `Save Changes`.
       - Pending state shows `Saving…`; button is disabled.
       - On success, the form collapses back to a row and the cell total reflects the new duration.
       - Click `Discard Changes` on another entry — form closes without mutation.

    8. **Cell popover — add.** Click `Log Work` (the existing LogWorkPopover's button) inside the cell popover:
       - The Log Work form appears (Time Spent / Date / Comment).
       - Enter `30m`, leave date default, submit.
       - On success the new entry appears in the cell's entry list and the cell total increases.
       - The cell popover does NOT close prematurely from clicks inside the nested Log Work popover.

    9. **Enrichment failure alert (optional).** If you can simulate a Jira error (e.g. revoke PAT briefly): the inline Alert "Some issues could not be loaded. Hours are still shown." appears below the filter bar. (Skip if you can't simulate — note it in the response.)

    10. **No regressions.** Filter bar still works:
        - Switching date preset still updates the table.
        - The person combobox still filters to that author.
        - Saved filters (TEMPO-04/05) still load, save, rename, delete via the existing pills + context menu.
        - Weekend/holiday columns are still shaded.
  </how-to-verify>
  <resume-signal>Type `approved` if every numbered item works as described. Otherwise list which items failed (by number) with details so the executor can fix and re-present.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Jira API (write) | Duration string and comment text from EditWorklogForm and LogWorkPopover cross into a write endpoint (`PUT /worklog/{id}`, `POST /worklog`) |
| User click → DOM | Trash button click triggers a destructive operation (delete worklog) without confirmation |
| Tempo data → React state | `entry.jiraWorklogId` flows from Tempo API response into URL path segment of `updateWorklog`/`deleteWorklog` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-06 | Input validation | EditWorklogForm duration field | mitigate | All duration strings parsed via `parseDuration` (existing, tested validator in `services/jira/duration`); invalid input rejected with inline error and `mutate` never called |
| T-64-07 | Tampering | `started` ISO string sent to Jira PUT | mitigate | Constructed server-safe via `new Date(date + 'T12:00:00').toISOString().replace('Z', '+0000')` — date comes from `<input type="date">` (browser-validated YYYY-MM-DD) |
| T-64-08 | Elevation of privilege | Delete/update worklog owned by another user | accept | Jira REST enforces server-side permissions; 401/403 → `ApiError` thrown and surfaced via mutation `onError` → inline error text. No client-side authority check needed |
| T-64-09 | Repudiation | Immediate delete with no confirmation dialog | accept | Per UI-SPEC §Delete Worklog Entry: matches IssueDetailPage; entries are granular and recoverable by re-logging. Acceptable risk for solo-developer usage |
| T-64-10 | Information disclosure | Worklog comment rendered to DOM | accept | React renders comment as text node — no `dangerouslySetInnerHTML`. Already accepted in IssueDetailPage |
| T-64-SC | Tampering | No new package installs | accept | This plan installs zero new npm packages (verified — RESEARCH §Package Legitimacy Audit). Package legitimacy gate not triggered |

ASVS L1 mapping: V5 Input Validation — `parseDuration` covers free-form duration input. No other untrusted input crosses the trust boundary in this plan.
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/worklogs/WorklogsPage.test.tsx` exits 0
- `cd taskflow && npx vitest run` exits 0
- `cd taskflow && npx tsc --noEmit -p tsconfig.json` exits 0
- `cd taskflow && npm run build` exits 0
- Human verification checkpoint (Task 3) PASSED — operator explicitly typed `approved`.
</verification>

<success_criteria>
- Three new sibling components exist under `taskflow/src/routes/worklogs/`: `WorklogCellPopover.tsx`, `WorklogEntryRow.tsx`, `EditWorklogForm.tsx`
- Every non-zero data cell in the hierarchy table is a `WorklogCellPopover` trigger
- Clicking a cell opens a popover listing the underlying Tempo worklog entries with edit/delete/add affordances
- Pencil → inline EditWorklogForm with Save Changes / Discard Changes; Save calls `updateWorklog` and refetches; Discard reverts
- Trash → immediate `deleteWorklog` + cache invalidation; no confirmation dialog
- "Add entry" → reuses `LogWorkPopover` from `issue-detail/` with cell's `issueKey` and refreshes the table on success
- The popover does not auto-close on mutate; closes only on `onSuccess` removal or click-away
- All mutations invalidate cache key prefix `['tempo', 'worklogs']`
- Cell entries scoped to `(issueKey, date)` matching `w.issue.key === issueKey && w.dateStarted === date`
- 4 new unit tests for Task 1 (entry row + edit form) and 4 new unit tests for Task 2 (cell popover wiring) pass
- Human-verify checkpoint passed
</success_criteria>

<output>
Create `.planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-02-SUMMARY.md` when done.
</output>
