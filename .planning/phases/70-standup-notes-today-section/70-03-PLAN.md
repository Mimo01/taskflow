---
phase: 70-standup-notes-today-section
plan: 03
type: execute
wave: 3
depends_on: [70-02]
files_modified:
  - taskflow/src/routes/standup-notes/StandupNotesPage.tsx
autonomous: false
requirements: [STAND-07, STAND-08, STAND-09]
must_haves:
  truths:
    - "Opening /standup-notes renders the real Today column (not the placeholder) in the right 50%"
    - "TodayColumn receives onIssueClick from the page's outlet context"
    - "Clicking an In Progress / Up Next / Pinned-Jira row opens the standard issue detail; clicking a pinned AIO cycle row opens the AIO cycle detail"
    - "Clicking Log Work on a row opens the popover pre-filled with today's date and the row's issue key without navigating; the logged-time chip refreshes after a successful log"
    - "Full vitest suite is green and `npm run build` produces zero errors"
  artifacts:
    - path: "taskflow/src/routes/standup-notes/StandupNotesPage.tsx"
      provides: "Page wired to render <TodayColumn onIssueClick={onIssueClick} /> in place of the placeholder"
      contains: "TodayColumn"
  key_links:
    - from: "StandupNotesPage.tsx"
      to: "TodayColumn.tsx"
      via: "default import + render in right column"
      pattern: "import TodayColumn from './TodayColumn'"
    - from: "StandupNotesPage.tsx right column"
      to: "onIssueClick (outlet context)"
      via: "prop pass-through"
      pattern: "<TodayColumn onIssueClick={onIssueClick}"
---

<objective>
Wire the Plan 02 `TodayColumn` into `StandupNotesPage.tsx`, replacing the `TodayColumnPlaceholder` in the right 50%, then verify the whole phase end-to-end: full test suite + production build green, followed by a human-verify checkpoint covering the manual-only behaviors (Log Work prefill, chip refresh, AIO cycle navigation, real sprint split) that automated tests cannot observe.

Purpose: This is the single integration point (RESEARCH.md StandupNotesPage Integration — lines 321-324) and the phase's final gate. Isolating it in its own plan keeps file ownership clean (only this plan touches the page) and places the human checkpoint where it belongs — after all automation is complete.
Output: Modified `StandupNotesPage.tsx`; green full suite + build; approved human-verify.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-standup-notes-today-section/70-CONTEXT.md
@.planning/phases/70-standup-notes-today-section/70-RESEARCH.md
@.planning/phases/70-standup-notes-today-section/70-PATTERNS.md
@.planning/phases/70-standup-notes-today-section/70-UI-SPEC.md
@.planning/phases/70-standup-notes-today-section/70-VALIDATION.md

<interfaces>
<!-- Exact integration point — verified from StandupNotesPage.tsx. -->

Line 30 currently:   import TodayColumnPlaceholder from './TodayColumnPlaceholder';
Lines 322-325 currently:
  {/* Right column — Today placeholder (50%) */}
  <div className="w-1/2 overflow-auto">
    <TodayColumnPlaceholder />
  </div>

`onIssueClick` is already destructured from useOutletContext at line ~95 in the page.
TodayColumn (Plan 02) is a default export: `function TodayColumn({ onIssueClick })`.
TodayColumn reads auth/settings/pinned stores internally — only `onIssueClick` is passed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace TodayColumnPlaceholder with TodayColumn in StandupNotesPage</name>
  <files>taskflow/src/routes/standup-notes/StandupNotesPage.tsx</files>
  <read_first>
    - taskflow/src/routes/standup-notes/StandupNotesPage.tsx (line 30 import; line ~95 onIssueClick from useOutletContext; lines 322-325 placeholder render)
    - taskflow/src/routes/standup-notes/TodayColumn.tsx (confirm default export + props shape — created Plan 02)
    - .planning/phases/70-standup-notes-today-section/70-PATTERNS.md (StandupNotesPage.tsx modify section — exact swap + import changes)
    - .planning/phases/70-standup-notes-today-section/70-RESEARCH.md (StandupNotesPage Integration Point)
  </read_first>
  <action>
    At line 30, replace the default import of `TodayColumnPlaceholder` with `import TodayColumn from './TodayColumn';`. At lines 322-325, replace `<TodayColumnPlaceholder />` with `<TodayColumn onIssueClick={onIssueClick} />` inside the existing `<div className="w-1/2 overflow-auto">` wrapper (do NOT change the wrapper or the comment beyond updating it to reference Today, not "placeholder"). `onIssueClick` is already in scope from the page's `useOutletContext` destructure — do not re-derive it. Do not delete `TodayColumnPlaceholder.tsx` (leave it in the tree per CONTEXT canonical_refs "delete or keep for reference"); just stop importing it. Make no other changes to the page — the left column, header, and all Phase 69 logic are out of scope (UI-SPEC Scope Boundary).
  </action>
  <verify>
    <automated>cd taskflow && grep -n "TodayColumn" src/routes/standup-notes/StandupNotesPage.tsx && grep -c "TodayColumnPlaceholder" src/routes/standup-notes/StandupNotesPage.tsx && npx tsc --noEmit 2>&1 | grep -iE "StandupNotesPage" ; echo "tsc-page-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `StandupNotesPage.tsx` contains `import TodayColumn from './TodayColumn'` and renders `<TodayColumn onIssueClick={onIssueClick} />`
    - Source assertion: `grep -c TodayColumnPlaceholder src/routes/standup-notes/StandupNotesPage.tsx` returns 0 (no remaining import or usage)
    - Source assertion: the right-column wrapper `<div className="w-1/2 overflow-auto">` is unchanged
    - tsc reports no errors referencing StandupNotesPage
  </acceptance_criteria>
  <done>StandupNotesPage renders TodayColumn with onIssueClick; no TodayColumnPlaceholder references remain; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite + production-build phase gate</name>
  <files>taskflow/src/routes/standup-notes/StandupNotesPage.tsx</files>
  <read_first>
    - .planning/phases/70-standup-notes-today-section/70-VALIDATION.md (Sampling Rate — per-wave full suite; before verify-work: full suite + npm run build)
    - .planning/STATE.md (Phase 59 standing rule: verify with `npm run build`, not just tsc)
  </read_first>
  <action>
    Run the full Vitest suite and the production build as the phase gate. If any test fails, diagnose and fix the root cause (do not delete or skip tests; do not add `.only`/watch flags). If the build fails, fix the underlying type/import error. Re-run both until both are green. This task fixes integration regressions only — it does not add new features. Capture the final pass/fail counts for the SUMMARY.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run 2>&1 | tail -15 && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: `npx vitest run` exits 0 with zero failing tests (full suite, not just standup-notes scope)
    - CLI output: `npm run build` exits 0 with zero errors
    - Behavior assertion: no test was skipped/deleted/`.only`-ed to achieve green (git diff shows no test removals)
  </acceptance_criteria>
  <done>Full vitest suite green and `npm run build` produces zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The real Today column on /standup-notes: four sections (In Progress, Up Next, MRs Awaiting You, Pinned) wired to live Jira/Tempo/GitLab data, with per-row Log Work, read-only pinned Jira issues + AIO cycles, and per-section loading/error/empty states. Automated tests cover the filter logic, Log Work presence + no-navigation, pinned discrimination + read-only, and MRs-hidden-when-disconnected. The following behaviors are verifiable only against live data and are listed in VALIDATION.md as manual-only.
  </what-built>
  <how-to-verify>
    1. Launch the app (`cd taskflow && npm run tauri dev`) and open the Standup Notes page from the sidebar.
    2. Confirm the right column shows "Today" + today's date, then sections in order: IN PROGRESS, UP NEXT, MRS AWAITING YOU, PINNED. Empty sections are hidden; if everything is empty you should see the single "Nothing planned for today" empty state.
    3. STAND-07: With an active sprint, confirm only YOUR leaf items appear, split correctly — in-progress items under IN PROGRESS, to-do items under UP NEXT, and Done items absent. Parent stories with subtasks should NOT appear.
    4. STAND-09: Click "Log Work" on an In Progress row — the popover must open pre-filled with today's date and that row's issue key, and the row must NOT navigate. Submit a worklog and confirm the logged-time chip on that row refreshes without a manual page refresh.
    5. STAND-08: Confirm pinned items show with no pin/unpin/remove controls. Click a pinned Jira issue → standard issue detail opens. Pin an AIO cycle, return here, click it → the AIO cycle detail page opens (/aio-cycle/{projectKey}/{key}).
    6. MRs (scope add): If GitLab is connected and you are a reviewer on open MRs, confirm they list under MRS AWAITING YOU with an "awaiting review" label. If GitLab is disconnected, the section must be absent.
    7. Confirm no console errors and that disabling an integration (e.g. Tempo) degrades gracefully (chips absent / section hidden) rather than erroring.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (inherits Plan 02 boundaries) | This plan only wires the existing TodayColumn into the page and runs verification. It adds no new API calls, tokens, or write paths beyond what Plan 02 already audited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-06 | Tampering | StandupNotesPage integration edit | accept | The change is a one-line component swap passing an already-in-scope callback; it introduces no new data flow, no new untrusted input, and no new persisted state. React escapes all rendered text in the child column (covered by T-70-03). |
| T-70-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH.md Package Legitimacy Audit confirms zero new/flagged packages. No legitimacy checkpoint required. |

**Rationale:** This plan is pure wiring + verification of components whose threats were enumerated in Plan 02. No new threat surface is introduced; nothing rises to HIGH. The human-verify checkpoint here is a functional/UAT gate (manual-only behaviors), not a security gate.
</threat_model>

<verification>
- `npx vitest run` (full suite) green.
- `npm run build` zero errors.
- `grep -c TodayColumnPlaceholder src/routes/standup-notes/StandupNotesPage.tsx` returns 0.
- Human-verify checkpoint approved (STAND-07/08/09 manual behaviors confirmed against live data).
</verification>

<success_criteria>
/standup-notes is fully usable for daily standups: the Today column renders live data with correct STAND-07 filtering, STAND-08 read-only pinned Jira + AIO navigation, and STAND-09 per-row Log Work with chip refresh. Full test suite and production build are green, and the human operator has confirmed the manual-only behaviors. Phase 70 is complete.
</success_criteria>

<output>
Create `.planning/phases/70-standup-notes-today-section/70-03-SUMMARY.md` when done.
</output>
