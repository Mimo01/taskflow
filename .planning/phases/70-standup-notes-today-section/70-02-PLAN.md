---
phase: 70-standup-notes-today-section
plan: 02
type: execute
wave: 2
depends_on: [70-01]
files_modified:
  - taskflow/src/routes/standup-notes/TodayColumn.tsx
  - taskflow/src/routes/standup-notes/TodayInProgressSection.tsx
  - taskflow/src/routes/standup-notes/TodayUpNextSection.tsx
  - taskflow/src/routes/standup-notes/TodayMrsSection.tsx
  - taskflow/src/routes/standup-notes/TodayPinnedSection.tsx
  - taskflow/src/routes/standup-notes/TodayColumn.test.tsx
  - taskflow/src/routes/standup-notes/TodayPinnedSection.test.tsx
  - taskflow/src/routes/standup-notes/TodayMrsSection.test.tsx
autonomous: true
requirements: [STAND-07, STAND-08, STAND-09]
must_haves:
  truths:
    - "TodayColumn renders four sections in fixed order: In Progress, Up Next, MRs Awaiting You, Pinned (D-01)"
    - "In Progress + Up Next rows show issue-type icon, key, summary, SP badge, and (In Progress only) a logged-time chip; each carries a Log Work trigger (STAND-07, STAND-09)"
    - "Clicking a row navigates via onIssueClick(key); clicking Log Work opens LogWorkPopover WITHOUT navigating (stopPropagation) (D-07, STAND-09)"
    - "Pinned section shows Jira issues (via fetchIssueMeta) and AIO cycles (from pinnedCycleMeta) read-only with no pin/unpin controls; AIO rows navigate via useNavigate to /aio-cycle/{projectKey}/{key} (STAND-08, D-08, D-09)"
    - "MRs Awaiting You lists open reviewer MRs from fetchReviewerMRs with a static 'awaiting review' label; section hidden when GitLab not connected (D-02, D-10)"
    - "Each section degrades independently: skeleton while loading, inline ErrorState+retry on failure, hidden when 0 items; full-column empty state only when all sections empty (D-03)"
  artifacts:
    - path: "taskflow/src/routes/standup-notes/TodayColumn.tsx"
      provides: "Top-level Today column; owns 4 useQuery calls (sprint-mine, today-tempo, reviewer-mrs, pinned-meta), section ordering, full-column empty state, LogWork onSuccess invalidation"
      exports: ["default"]
      min_lines: 120
    - path: "taskflow/src/routes/standup-notes/TodayInProgressSection.tsx"
      provides: "In Progress rows with SP + logged-time chips + Log Work popover"
    - path: "taskflow/src/routes/standup-notes/TodayUpNextSection.tsx"
      provides: "Up Next rows with SP chip + Log Work popover (no logged-time chip)"
    - path: "taskflow/src/routes/standup-notes/TodayMrsSection.tsx"
      provides: "MRs Awaiting You rows (non-interactive) with awaiting-review label"
    - path: "taskflow/src/routes/standup-notes/TodayPinnedSection.tsx"
      provides: "Pinned rows — Jira issues + AIO cycles, read-only"
    - path: "taskflow/src/routes/standup-notes/TodayColumn.test.tsx"
      provides: "Render/interaction tests: Log Work present + stopPropagation, MRs hidden when no GitLab (STAND-09, MRs scope)"
    - path: "taskflow/src/routes/standup-notes/TodayPinnedSection.test.tsx"
      provides: "Pinned discrimination + read-only assertion (STAND-08)"
    - path: "taskflow/src/routes/standup-notes/TodayMrsSection.test.tsx"
      provides: "Review-state label class assertions (MRs scope)"
  key_links:
    - from: "TodayColumn.tsx"
      to: "filterSprintItems.ts"
      via: "import { filterSprintItems }"
      pattern: "from './filterSprintItems'"
    - from: "TodayColumn.tsx"
      to: "['jira-issues', 'sprint-board-mine', ...]"
      via: "fetchSprintIssues(..., true, ...) with distinct cache key"
      pattern: "sprint-board-mine"
    - from: "TodayColumn.tsx"
      to: "['standup', 'today-tempo', ...]"
      via: "fetchWorklogs(.., todayStr, todayStr) + invalidate on LogWork success"
      pattern: "today-tempo"
    - from: "TodayInProgressSection.tsx"
      to: "LogWorkPopover"
      via: "named import wrapped in stopPropagation span"
      pattern: "stopPropagation"
    - from: "TodayPinnedSection.tsx"
      to: "/aio-cycle/{projectKey}/{key}"
      via: "useNavigate"
      pattern: "aio-cycle"
---

<objective>
Build the real Today column: a `TodayColumn` component (default export) that owns four independent TanStack queries and renders four sections in fixed order (In Progress, Up Next, MRs Awaiting You, Pinned), plus the four section subcomponents and their render/interaction tests. The column consumes the verified `filterSprintItems` helper from Plan 01 to split my leaf sprint work by status category, attaches a `LogWorkPopover` to every In Progress / Up Next row, lists open reviewer MRs, and shows read-only pinned Jira issues + AIO cycles.

Purpose: This is the core of STAND-07/08/09 plus the user-decided MRs Awaiting You scope addition (D-02). It does NOT touch `StandupNotesPage.tsx` — wiring is Plan 03 — so this plan creates only new files (zero overlap with the page), enabling clean wave separation.
Output: `TodayColumn.tsx` + 4 section components + 3 test files.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-standup-notes-today-section/70-CONTEXT.md
@.planning/phases/70-standup-notes-today-section/70-RESEARCH.md
@.planning/phases/70-standup-notes-today-section/70-PATTERNS.md
@.planning/phases/70-standup-notes-today-section/70-UI-SPEC.md
@.planning/phases/70-standup-notes-today-section/70-VALIDATION.md

<interfaces>
<!-- Verified contracts. Use directly — no codebase exploration needed. -->

filterSprintItems(issues: JiraIssue[], jiraUserDisplayName: string): { inProgress: JiraIssue[]; upNext: JiraIssue[] }
  — from './filterSprintItems' (Plan 01).

fetchSprintIssues(baseUrl, token, projectKey, assignedToMe=true, storyPointsFieldKey='customfield_10016', epicLinkFieldKey?): Promise<JiraIssue[]>
  — from '@/services/jira'. Returns flat array of parents + subtasks.

fetchIssueMeta(baseUrl, token, keys: string[]): Promise<Record<string, StandupIssueMeta>>
  — from '@/services/jira'. Returns {} on failure / empty keys. StandupIssueMeta: { type?, isSubtask?, summary?, parentKey?, parentSummary?, parentType? }.

fetchReviewerMRs(baseUrl, token, userId: number): Promise<GitLabMR[]>
  — from '@/services/gitlab'. GitLabMR (line 186) has NO review_state field. Has iid, title, state, reviewers[]{id,name,username}.

fetchWorklogs(baseUrl, token, usernames: string[], fromDate: string, toDate: string)
  — from '@/services/tempo'. Each worklog has .issue.key and .timeSpentSeconds.

formatDuration(totalSeconds: number): string  — from '@/services/jira/duration'.

LogWorkPopover — NAMED export from '@/routes/dashboard/issue-detail/LogWorkPopover'.
  Props: { issueKey: string; jiraBaseUrl: string; onSuccess?: () => void; initialDate?: string }.
  Renders its OWN trigger (PopoverTrigger styled as outline/sm Button, Clock icon + "Log Work"). Do not re-skin.

usePinnedTabsStore() — { pinnedKeys: string[]; pinnedCycleMeta: Record<string,{name,projectKey}>; isPinned }.
useAuthStore — fields: jiraBaseUrl, gitlabBaseUrl, activeJiraProject, jiraUsername, gitlabUserId (number|null), jiraUserDisplayName.
useSettingsStore — selectors: (s)=>s.tempoEnabled, (s)=>s.storyPointsFieldKey.
readSecret('jira-pat') / readSecret('gitlab-pat') — from '@/services/stronghold'.

AIO cycle navigation (main.tsx 524-526): navigate(`/aio-cycle/${meta.projectKey}/${key}`). Detect cycle via key in pinnedCycleMeta.

Shared UI primitives: IssueTypeIcon '@/components/ui/issue-type-icon'; EmptyState '@/components/ui/empty-state';
  ErrorState '@/components/ui/error-state' (props error/onRetry/viewName); Skeleton '@/components/ui/skeleton';
  useDelayedLoading '@/hooks/useDelayedLoading' (isPending, delayMs=200).
lucide icons: Clock, GitBranch, ListChecks.

Exact UI classes, copy strings, section labels, and row anatomy are SPECIFIED in 70-UI-SPEC.md
(Row Anatomy, Section Anatomy, Color, Typography, Copywriting Contract, Icon Table). Follow them verbatim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TodayColumn shell — four queries, section ordering, empty state</name>
  <files>taskflow/src/routes/standup-notes/TodayColumn.tsx</files>
  <read_first>
    - taskflow/src/routes/standup-notes/StandupNotesPage.tsx (lines 15-31 imports; 79-115 auth/settings selectors + token loading; 235-244 issueMetaQuery pattern; existing 'standup' query keys)
    - taskflow/src/routes/standup-notes/YesterdayColumn.tsx (per-section loading/error/empty pattern; full-column empty state lines ~434-452; LoadingSkeletons)
    - taskflow/src/routes/dashboard/DashboardInProgressCard.tsx (shared cache key + useDelayedLoading usage line 51)
    - .planning/phases/70-standup-notes-today-section/70-PATTERNS.md (TodayColumn.tsx section — all four query blocks, todayLoggedByIssue memo, navigation, empty-state)
    - .planning/phases/70-standup-notes-today-section/70-RESEARCH.md (Pitfalls 1,2,3,6; Logged-Time Chip Data Path; Pinned Resolution Pattern; the three critical corrections)
    - .planning/phases/70-standup-notes-today-section/70-UI-SPEC.md (Column Structure, Loading State, Full-column empty state, Interaction Contracts)
  </read_first>
  <behavior>
    - Renders heading "Today" + today's date sub-label, then sections in fixed order: In Progress, Up Next, MRs Awaiting You, Pinned
    - Sprint query uses key ['jira-issues', 'sprint-board-mine', activeJiraProject, storyPointsFieldKey] with assignedToMe=true (NOT the shared 'sprint-board' key)
    - Today-tempo query uses key ['standup', 'today-tempo', jiraBaseUrl, todayStr, jiraUsername ?? ''] calling fetchWorklogs(.., todayStr, todayStr); enabled only when tempoEnabled
    - Reviewer-MRs query enabled only when gitlabBaseUrl + gitlabToken + gitlabUserId present
    - Pinned-meta query enabled only when sortedJiraKeys.length > 0
    - LogWork onSuccess invalidates the today-tempo key
    - Full-column empty state ("Nothing planned for today") fires only when all sections resolved with 0 items and none loading/erroring
  </behavior>
  <action>
    Create `TodayColumn` as a `default export function` taking props `{ onIssueClick: (key: string) => void }`. Read auth via `useAuthStore` (jiraBaseUrl, gitlabBaseUrl, activeJiraProject, jiraUsername, gitlabUserId) and `jiraUserDisplayName` via a separate fine-grained selector. Read `tempoEnabled` and `storyPointsFieldKey` via per-field `useSettingsStore` selectors (Phase 68 rule). Replicate StandupNotesPage's local token-state loading (`jiraToken`, `gitlabToken` via useEffect + readSecret) for `enabled` guards. Define a local TZ-safe `todayString()` (NOT toLocaleDateString — Phase 62 rule) and memoize `todayStr`.

    Declare four `useQuery` calls exactly per the interfaces/PATTERNS keys: (1) sprint issues with key `['jira-issues', 'sprint-board-mine', activeJiraProject, storyPointsFieldKey]`, queryFn `fetchSprintIssues(jiraBaseUrl!, jiraToken!, activeJiraProject!, true, storyPointsFieldKey)`, enabled on jiraBaseUrl+jiraToken+activeJiraProject (RESEARCH Pitfall 1 — never reuse the shared 'sprint-board' key); (2) today-tempo (key + fetchWorklogs as in interfaces, enabled also requires tempoEnabled + jiraUsername); (3) reviewer-mrs key `['standup', 'reviewer-mrs', gitlabBaseUrl, gitlabUserId]`, queryFn reads gitlab-pat via readSecret inside the closure and calls `fetchReviewerMRs(gitlabBaseUrl!, token, gitlabUserId!)`, enabled on gitlabBaseUrl+gitlabToken+`!!gitlabUserId`; (4) pinned-meta key `['standup', 'pinned-meta', jiraBaseUrl, sortedJiraKeys]` reading jira-pat in closure. Tokens NEVER appear in any queryKey (T-62-06).

    Derive `{ inProgress, upNext }` by calling `filterSprintItems(sprintQuery.data ?? [], jiraUserDisplayName ?? '')`. Build `todayLoggedByIssue: Map<string, number>` via useMemo summing `timeSpentSeconds` per `issue.key` from the today-tempo data. Split pinned keys into `pinnedJiraKeys` (not in pinnedCycleMeta) and `pinnedCycleKeys` (in pinnedCycleMeta); `sortedJiraKeys = useMemo(() => [...pinnedJiraKeys].sort())`. Get `useNavigate()` and `useQueryClient()`. Define `handleLogWorkSuccess` invalidating the today-tempo key, and `handleCycleClick(key)` navigating to `/aio-cycle/${pinnedCycleMeta[key].projectKey}/${key}`.

    Render the root `<div className="flex flex-col h-full px-6 py-4">` with the heading block (UI-SPEC Column Structure), then the four section subcomponents IN FIXED ORDER passing each its filtered data + loading/error/onRetry + the shared callbacks. Render the full-column EmptyState (icon Clock, exact copy from UI-SPEC Copywriting Contract) only when all four section data sets are empty AND no query is loading/erroring. Follow UI-SPEC classes/copy verbatim. No fenced code in this plan — implement per the PATTERNS excerpts.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit 2>&1 | grep -iE "TodayColumn|standup-notes/Today" ; echo "tsc-today-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: grep 'sprint-board-mine' in TodayColumn.tsx returns a match AND grep "'sprint-board'," (the shared key) returns NO match (no cache contamination — RESEARCH Pitfall 1)
    - Source assertion: grep 'today-tempo' returns a match; grep for the LogWork onSuccess invalidation references the today-tempo key (not yesterdayDate)
    - Source assertion: no token string appears inside any queryKey array (grep verifies 'jira-pat'/'gitlab-pat' appear only inside queryFn/useEffect, never adjacent to queryKey)
    - Source assertion: grep -c 'toLocaleDateString' returns 0 (Phase 62 rule)
    - Source assertion: TodayColumn imports filterSprintItems from './filterSprintItems'
    - Behavior assertion: reviewer-mrs query `enabled` includes `!!gitlabUserId`; pinned-meta query `enabled` includes `sortedJiraKeys.length > 0`
    - tsc reports no errors referencing TodayColumn (section components may not exist yet in Task 1; if so, gate this on Task 2 completion — see Done)
  </acceptance_criteria>
  <done>TodayColumn.tsx exists as a default-export component owning the four correctly-keyed queries, filterSprintItems integration, today-logged map, navigation + invalidation handlers, and the full-column empty state. Type-checks clean once Task 2's sections exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Four section components (In Progress, Up Next, MRs, Pinned)</name>
  <files>taskflow/src/routes/standup-notes/TodayInProgressSection.tsx, taskflow/src/routes/standup-notes/TodayUpNextSection.tsx, taskflow/src/routes/standup-notes/TodayMrsSection.tsx, taskflow/src/routes/standup-notes/TodayPinnedSection.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/DashboardInProgressCard.tsx (row button structure lines 127-137)
    - taskflow/src/routes/dashboard/issue-detail/LogWorkPopover.tsx (named export, built-in trigger, props)
    - taskflow/src/routes/standup-notes/YesterdayColumn.tsx (LoadingSkeletons, ErrorState usage, hidden-when-empty rule)
    - .planning/phases/70-standup-notes-today-section/70-PATTERNS.md (each section's pattern + props interfaces; Shared Patterns; Section Hidden-When-Empty Rule; Jira Not Connected Inline Notice)
    - .planning/phases/70-standup-notes-today-section/70-UI-SPEC.md (Row Anatomy for each row type, Section Anatomy, Icon Table, exact viewName strings, review-state label classes)
    - .planning/phases/70-standup-notes-today-section/70-RESEARCH.md (Pitfall 4 stopPropagation; Option A MR review state; Pitfall 6 AIO navigation)
  </read_first>
  <behavior>
    - In Progress row: issue-type icon + key + truncated summary + SP badge (when populated) + logged-time chip (when today's logged seconds > 0) + Log Work trigger; whole row click → onIssueClick(key)
    - Up Next row: identical to In Progress MINUS the logged-time chip; Log Work trigger still present (all open sprint work loggable — D-06)
    - Log Work trigger wrapped in a span with onClick stopPropagation so it never triggers row navigation (Pitfall 4)
    - MRs row: non-interactive div (no button), GitBranch icon + !iid + truncated title + right-aligned "awaiting review" muted label (Option A — no per-MR API call); section hidden entirely when GitLab not connected
    - Pinned Jira row: IssueTypeIcon + key + summary, click → onIssueClick(key); no pin/unpin controls
    - Pinned AIO cycle row: ListChecks icon + projectKey + cycle name, click → onCycleClick(key); no pin/unpin controls
    - Every section: skeleton while loading (useDelayedLoading), ErrorState+retry on error, returns null when 0 items + not loading + not erroring
  </behavior>
  <action>
    Create the four section components, each typed with the props interface defined in 70-PATTERNS.md (TodayInProgressSectionProps, TodayUpNextSectionProps, TodayMrsSectionProps, TodayPinnedSectionProps — copy them verbatim). Each section receives already-filtered data + isLoading/isError/error/onRetry from TodayColumn (sections do NOT own queries). Implement the shared three-state pattern from YesterdayColumn: `useDelayedLoading(isLoading)` → render the three-bar `LoadingSkeletons`; `isError` → render `<ErrorState error={error} onRetry={onRetry} viewName="..." />` with the exact per-section viewName from the UI-SPEC Copywriting Contract; `items.length === 0 && !loading && !error` → `return null` (hidden, per D-03). When a section has rows, wrap them in `<div className="divide-y divide-border">`.

    In Progress / Up Next rows: build the `<button>` per UI-SPEC Row Anatomy classes; render `IssueTypeIcon`, the mono key, the truncated summary, the SP badge from `issue.fields[storyPointsFieldKey] as number | null` (omit when null), and — In Progress only — a logged-time chip from `formatDuration(todayLoggedByIssue.get(issue.key) ?? 0)` shown only when seconds > 0. Append the `LogWorkPopover` (named import) with `issueKey`, `jiraBaseUrl`, `initialDate={todayStr}`, `onSuccess={onLogWorkSuccess}`, wrapped in `<span onClick={(e) => e.stopPropagation()}>` (Pitfall 4). Up Next is identical minus the logged-time chip and the `todayLoggedByIssue` prop.

    MRs section: render each MR as a non-interactive `<div>` (NOT a button — UI-SPEC) with GitBranch icon, `!{mr.iid}`, truncated title, and a static `"awaiting review"` label using the muted class. Implement the `review_state`-conditional label as a code-comment-documented future path only (Option A — RESEARCH correction #2: GitLabMR has no review_state; do NOT require it). The amber `"changes requested"` branch may be present behind a guard but must default to "awaiting review" for all returned MRs.

    Pinned section: render two row variants per UI-SPEC Pinned Row Anatomy — Jira keys (`pinnedJiraKeys`, IssueTypeIcon + key + `pinnedMeta[key]?.summary ?? key`, click → onIssueClick) and AIO cycles (`pinnedCycleKeys`, ListChecks + `pinnedCycleMeta[key].projectKey` + `.name`, click → onCycleClick). No pin/unpin/remove controls anywhere (D-08). Follow all UI-SPEC classes/copy verbatim. No fenced code in this plan.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit 2>&1 | grep -iE "standup-notes/Today" ; echo "tsc-sections-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: TodayInProgressSection.tsx AND TodayUpNextSection.tsx each contain `LogWorkPopover` wrapped by a `stopPropagation` handler (grep both: 'stopPropagation' returns a match in each)
    - Source assertion: TodayUpNextSection.tsx does NOT reference `todayLoggedByIssue` (grep -c returns 0 — no logged-time chip on Up Next)
    - Source assertion: TodayMrsSection.tsx contains the literal `awaiting review` and uses no field named `review_state` as a required input (RESEARCH correction #2)
    - Source assertion: TodayPinnedSection.tsx contains no pin/unpin handler (grep -ci 'unpin\|togglePin\|removePin' returns 0 — D-08 read-only)
    - Source assertion: TodayPinnedSection.tsx routes AIO cycle clicks through `onCycleClick` (not onIssueClick) and AIO rows use `ListChecks`
    - Each section uses the exact `viewName` string from UI-SPEC ("In Progress items", "Up Next items", "MRs awaiting you", "Pinned items")
    - `npx tsc --noEmit` reports no errors under src/routes/standup-notes/Today*
  </acceptance_criteria>
  <done>All four section components exist, type-check clean, follow UI-SPEC row/section anatomy, implement the three-state degradation, and honor D-06/D-07/D-08/D-10 + RESEARCH correction #2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render/interaction tests — Log Work, MRs hidden, pinned read-only, review label</name>
  <files>taskflow/src/routes/standup-notes/TodayColumn.test.tsx, taskflow/src/routes/standup-notes/TodayPinnedSection.test.tsx, taskflow/src/routes/standup-notes/TodayMrsSection.test.tsx</files>
  <read_first>
    - taskflow/src/routes/standup-notes/YesterdayColumn.tempo-disabled.test.tsx (render-test structure: mocking TanStack Query + router + stores)
    - taskflow/src/routes/standup-notes/TodayColumn.tsx (component under test — created Task 1)
    - taskflow/src/routes/standup-notes/TodayPinnedSection.tsx, TodayMrsSection.tsx (created Task 2)
    - .planning/phases/70-standup-notes-today-section/70-VALIDATION.md (Per-Task Verification Map rows for STAND-08, STAND-09, MRs scope; Wave 0 Requirements)
    - .planning/phases/70-standup-notes-today-section/70-RESEARCH.md (Validation Architecture — What Is Realistically Unit-Testable; the manual-only items NOT to automate)
  </read_first>
  <behavior>
    - TodayColumn.test.tsx: Log Work trigger present on rendered In Progress rows (STAND-09)
    - TodayColumn.test.tsx: clicking the Log Work trigger does NOT invoke the row's onIssueClick (stopPropagation — STAND-09)
    - TodayColumn.test.tsx: MRs Awaiting You section absent from the DOM when GitLab not connected (MRs scope)
    - TodayPinnedSection.test.tsx: a key present in pinnedCycleMeta renders an AIO cycle row; a key absent renders a Jira issue row (STAND-08 discrimination)
    - TodayPinnedSection.test.tsx: no pin/unpin/remove control elements rendered (STAND-08 read-only)
    - TodayMrsSection.test.tsx: "awaiting review" label uses the muted class; the "changes requested" branch (if rendered) uses the amber class
  </behavior>
  <action>
    Mirror `YesterdayColumn.tempo-disabled.test.tsx` setup: mock `@tanstack/react-query` query hooks (or wrap in a `QueryClientProvider`), mock `react-router-dom` `useNavigate`, and mock the auth/settings/pinned stores so the component renders deterministically with fixture data. Use `@testing-library/react` `render` + `screen` and `vitest`.

    In `TodayColumn.test.tsx`: render with a fixture sprint result containing at least one In Progress leaf item assigned to the mocked `jiraUserDisplayName`; assert a Log Work trigger is present (query by the "Log Work" accessible name from LogWorkPopover's built-in trigger). Provide an `onIssueClick` spy; fire a click on the Log Work trigger (or its stopPropagation wrapper) and assert `onIssueClick` was NOT called (do not assert popover internal date state — that is manual-only per VALIDATION.md). Render once with `gitlabBaseUrl` null/absent and assert the "MRS AWAITING YOU" header is not in the document.

    In `TodayPinnedSection.test.tsx`: render with `pinnedJiraKeys=['PROJ-1']`, `pinnedCycleKeys=['PROJ-CY-9']`, matching `pinnedCycleMeta` + `pinnedMeta`; assert the cycle row shows the cycle name and the Jira row shows the issue summary, and assert no element with a pin/unpin/remove role or label exists. In `TodayMrsSection.test.tsx`: render with one MR and assert the "awaiting review" label carries the muted class; if the amber branch is implemented, render a fixture exercising it and assert the amber class string is applied. No watch flags, no `.only`.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/standup-notes/TodayColumn.test.tsx src/routes/standup-notes/TodayPinnedSection.test.tsx src/routes/standup-notes/TodayMrsSection.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Test command exits 0; all three test files pass
    - Behavior assertion (STAND-09): test proves clicking Log Work does NOT call the row's onIssueClick spy
    - Behavior assertion (STAND-09): test proves a Log Work trigger renders on In Progress rows
    - Behavior assertion (MRs scope): test proves "MRS AWAITING YOU" header is absent when GitLab disconnected
    - Behavior assertion (STAND-08): pinned test proves cycle-vs-Jira discrimination by pinnedCycleMeta membership AND absence of pin/unpin controls
    - No `.only` / no watch flag in any test file (grep -c '\.only' returns 0)
    - Full scoped run `npx vitest run src/routes/standup-notes/` is green (does not regress Plan 01 or Phase 69 tests)
  </acceptance_criteria>
  <done>The three test files exist and pass; STAND-08 discrimination + read-only, STAND-09 Log Work present + no-navigation, and MRs-hidden-when-disconnected are all proven by automated tests; scoped standup-notes suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Jira REST API | Sprint issues, issue meta, and worklog reads cross here (read-only). Token sourced from Stronghold. |
| client → GitLab REST API | Reviewer-MR reads cross here (read-only). Token sourced from Stronghold. |
| client → Tempo REST API | Today worklog reads cross here (read-only). |
| LogWorkPopover → Jira REST API | The one write path (create worklog) — already audited; this plan only passes issueKey + initialDate and reads back via invalidation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-02 | Information Disclosure | Query keys for sprint/tempo/MR/pinned queries | mitigate | Per T-62-06, tokens are read via `readSecret('jira-pat')`/`readSecret('gitlab-pat')` inside the queryFn closure or held in local state for `enabled` guards — NEVER placed in any queryKey segment. Acceptance criteria grep-assert this. |
| T-70-03 | Tampering / XSS | Rendering issue summaries, MR titles, cycle names | accept | All untrusted text (issue summary, MR title, pinned cycle name) is rendered as React text children, which React escapes by default. No `dangerouslySetInnerHTML`, no `href` built from untrusted input. Navigation targets are app-internal route templates with interpolated keys, not free-form URLs. |
| T-70-04 | Information Disclosure | Reviewer-MR list / today worklog data | accept | Read-only fetches of the current user's own review/worklog data in a local Tauri app; no broadening of existing exposure. Queries are gated on the relevant integration being connected + enabled. |
| T-70-05 | Elevation / unintended write | LogWorkPopover invocation from rows | mitigate | The Log Work path reuses the already-audited `LogWorkPopover` write component unchanged; this plan adds no new write logic. The stopPropagation wrapper prevents accidental navigation but introduces no privilege change. |
| T-70-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH.md Package Legitimacy Audit confirms zero new/flagged packages. No legitimacy checkpoint required. |

**Rationale:** The realistic surface is small — read-only API reads plus reuse of one already-audited write popover, all inside a local Tauri app. The one genuine, plan-specific risk worth enforcing is token-in-queryKey leakage (T-70-02), which is mitigated by the T-62-06 pattern and grep-asserted in acceptance criteria. No threat rises to HIGH; no blocking security checkpoint is warranted.
</threat_model>

<verification>
- `npx vitest run src/routes/standup-notes/` green (Plan 01 + Plan 02 tests + Phase 69 tests, no regressions).
- `npx tsc --noEmit` reports no errors under src/routes/standup-notes/Today*.
- grep gates from acceptance criteria pass: `sprint-board-mine` present, shared `'sprint-board',` absent, `today-tempo` present, no token in any queryKey, no `toLocaleDateString`.
</verification>

<success_criteria>
The Today column and its four sections exist as standalone, type-checking, tested components that honor every locked decision (D-01..D-10) and the three RESEARCH corrections (new sprint-board-mine cache key, no review_state dependency, new today-tempo query). STAND-07 (filtered sprint work + Log Work targets), STAND-08 (read-only pinned Jira + AIO), and STAND-09 (per-row Log Work, no row navigation) are implemented and proven by automated tests. The column is NOT yet wired into the page — that is Plan 03.
</success_criteria>

<output>
Create `.planning/phases/70-standup-notes-today-section/70-02-SUMMARY.md` when done.
</output>
