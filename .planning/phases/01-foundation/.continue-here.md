---
phase: 01-foundation
task: UAT in progress
status: in_progress
last_updated: 2026-03-11T10:51:29.402Z
---

<current_state>
Running UAT for Phase 1 (verify-work). On Test 2 (Onboarding Wizard: Full Flow) — currently retrying after a series of fixes made during UAT. Test 2 has not yet passed; waiting for user to confirm after latest fix.

UAT session file: .planning/phases/01-foundation/01-UAT.md (status: testing)
</current_state>

<completed_work>

- Test 1: Cold Start Smoke Test — PASS (after fixing Tailwind v3→v4 migration)
- Test 2: First attempt — ISSUE (project selector too narrow + GitLab TLS error)

Fixes applied during this UAT session (all committed):
1. `fix(css): migrate to Tailwind v4` (a7c8463) — shadcn@4.0.5 generates v4 CSS but project had v3; installed @tailwindcss/vite, replaced @tailwind directives with @import "tailwindcss", added @theme inline block
2. `fix(tauri): correct http plugin scope format` (1f9ab9b) — scope was top-level key (ignored); moved inline on permission entry; fixed wildcard from `**` to `*:*`
3. `fix(uat): accept invalid TLS certs and fix selector width` (f8a63ce) — added danger.acceptInvalidCerts/Hostnames to jira.ts and gitlab.ts; added w-full to all SelectTrigger elements
4. `fix(http): add acceptInvalidHostnames` (5b74a53) — Rust deserializer required both fields
5. `fix(tauri): enable dangerous-settings feature` (5d534d7) — Cargo feature flag required for danger settings to work
</completed_work>

<remaining_work>

UAT Tests still to run (Tests 2–15):
- Test 2: Onboarding Wizard: Full Flow — retrying (Rust recompile triggered by last fix)
- Test 3: Jira PAT Validation — Success & Project Dropdown
- Test 4: Jira PAT Validation — Error Messages
- Test 5: Back Navigation Preserves Values
- Test 6: GitLab PAT Validation
- Test 7: Role Picker Step
- Test 8: Done Step → Dashboard Navigation
- Test 9: Sidebar Visibility
- Test 10: Settings: Masked Token Display & Eye-Toggle Reveal
- Test 11: Settings: Update Token
- Test 12: Settings: Role Switch
- Test 13: Settings: Theme Toggle (Light/Dark/System)
- Test 14: Settings: Jira Project Select
- Test 15: Re-Auth Banner
</remaining_work>

<decisions_made>

- Tailwind v4 migration: shadcn@4.0.5 uses Tailwind v4 syntax (@theme inline, @utility, @custom-variant) — must use @tailwindcss/vite plugin, not postcss plugin
- @theme inline block needed to map CSS variables (--border, --background, etc.) to Tailwind color utilities (border-border, bg-background, etc.)
- Tauri http scope must be inline on permission entry object: `{ "identifier": "http:default", "allow": ["https://*:*", "http://*:*"] }` — top-level scope key is ignored
- danger settings require both acceptInvalidCerts AND acceptInvalidHostnames (Rust requires both fields), AND the Cargo feature `dangerous-settings` enabled
</decisions_made>

<blockers>

None currently — last fix (dangerous-settings Cargo feature) triggers Rust recompile. User needs to wait for compile then retry Test 2.
</blockers>

<context>
We're mid-UAT on Phase 1. The phase was already verified (24/24 automated checks passed) but there were UAT issues (CSS not loading, CORS). A fresh UAT session was started. Test 1 passed after the Tailwind v4 migration. Test 2 is being retried after multiple fixes to Tauri http plugin scope and TLS settings.

The app is a Tauri 2 desktop app (taskflow/) with React 18 + TypeScript. The PAT onboarding wizard connects to Jira and GitLab via tauri-plugin-http. Internal instances use self-signed TLS certs.
</context>

<next_action>
Resume with: /gsd:resume-work

On resume, immediately ask the user if Test 2 passed after the Rust recompile, then continue with Test 3 and onwards. The UAT file is at .planning/phases/01-foundation/01-UAT.md — read it to see current state.

Present the next pending test using the checkpoint box format from verify-work workflow.
</next_action>
