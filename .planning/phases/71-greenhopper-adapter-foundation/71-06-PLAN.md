---
phase: 71-greenhopper-adapter-foundation
plan: 06
type: execute
wave: 4
depends_on:
  - 71-02
  - 71-03
  - 71-04
  - 71-05
files_modified:
  - taskflow/src/services/jira/greenhopper/index.ts
  - taskflow/src/services/jira.ts
autonomous: true
requirements:
  - GH-ADAPT-01
  - GH-ADAPT-02
  - GH-ADAPT-03
must_haves:
  truths:
    - "`src/services/jira/greenhopper/index.ts` re-exports adapter/allData/data/details/entityMaps/transitions/types BUT NOT client (D-06)"
    - "`src/services/jira.ts` (legacy dual-file) re-exports fetchAllData, fetchBacklogData, fetchIssueDetails, fetchGhTransitions, adaptIssue, buildEntityMaps, resolveStatus, resolvePriority, resolveType, resolveEpic, resolveParent (D-05) — Phases 72-75 consumers import from `services/jira`, NOT `services/jira/greenhopper`"
    - "GhIssue, GhBoardIssue, GhAllDataResponse, GhBacklogResponse, GhDetailsResponse, GhTransitionsResponse, GhTransition, EntityMaps are re-exported as types from services/jira.ts (D-05)"
    - "Full vitest suite is green across the new greenhopper module; biome lint is clean (baseline: 0 errors, 0 warnings per memory `project_biome_state.md`)"
  artifacts:
    - path: "taskflow/src/services/jira/greenhopper/index.ts"
      provides: "Public barrel — re-exports everything in greenhopper/ except client"
    - path: "taskflow/src/services/jira.ts"
      provides: "Modified — appends value + type re-export blocks pointing at './jira/greenhopper'"
      contains: "from './jira/greenhopper'"
  key_links:
    - from: "taskflow/src/services/jira.ts"
      to: "taskflow/src/services/jira/greenhopper/index.ts"
      via: "two `from './jira/greenhopper'` re-export blocks"
      pattern: "from '\\./jira/greenhopper'"
---

<objective>
Stitch the greenhopper module into the project's import graph. This plan ships the public barrel (`index.ts`) honoring D-06 (no client re-export), then appends the value + type re-export blocks to `src/services/jira.ts` (the legacy dual-file) per D-05 so Phases 72-75 import via the established `services/jira` convention (the 60-imports-via-jira.ts memory note).

Purpose: Without this plan, the work shipped in 71-02..71-05 is unreachable to downstream consumers. This is the integration that makes GH-ADAPT-01/02/03 collectively usable.

Output: One new barrel + one modified legacy file + green full vitest suite + clean biome lint.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-greenhopper-adapter-foundation/71-CONTEXT.md
@.planning/phases/71-greenhopper-adapter-foundation/71-RESEARCH.md
@.planning/phases/71-greenhopper-adapter-foundation/71-PATTERNS.md
@taskflow/src/services/aio/index.ts
@taskflow/src/services/jira.ts
@taskflow/src/services/jira/greenhopper/client.ts
@taskflow/src/services/jira/greenhopper/types.ts
@taskflow/src/services/jira/greenhopper/allData.ts
@taskflow/src/services/jira/greenhopper/data.ts
@taskflow/src/services/jira/greenhopper/details.ts
@taskflow/src/services/jira/greenhopper/transitions.ts
@taskflow/src/services/jira/greenhopper/entityMaps.ts
@taskflow/src/services/jira/greenhopper/adapter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create greenhopper/index.ts barrel (D-06 — client NOT re-exported)</name>
  <files>taskflow/src/services/jira/greenhopper/index.ts</files>
  <read_first>
    - taskflow/src/services/aio/index.ts (entire file — verbatim analog)
    - .planning/phases/71-greenhopper-adapter-foundation/71-CONTEXT.md D-06 (client.ts is internal)
    - .planning/phases/71-greenhopper-adapter-foundation/71-PATTERNS.md §"greenhopper/index.ts" (full block to copy)
  </read_first>
  <action>
    Create `taskflow/src/services/jira/greenhopper/index.ts`:
    - Module doc-comment (2-3 lines): "GreenHopper service submodules barrel export. client.ts is intentionally NOT exported — it is internal to greenhopper/ (D-06). Domain modules import greenhopperFetch directly from './client'."
    - Seven `export * from './<file>'` statements covering: adapter, allData, data, details, entityMaps, transitions, types
    - No export for `./client`

    Order alphabetical (matches aio/index.ts style). Do NOT add a default export. Do NOT re-export anything from `./__fixtures__`.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; test $(grep -c "from './client'" src/services/jira/greenhopper/index.ts) -eq 0 &amp;&amp; test $(grep -cE "^export \* from '\\./(adapter|allData|data|details|entityMaps|transitions|types)'" src/services/jira/greenhopper/index.ts) -eq 7 &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "from './client'" taskflow/src/services/jira/greenhopper/index.ts` == 0 (D-06)
    - Source assertion: exactly 7 `export * from './<X>'` lines covering adapter, allData, data, details, entityMaps, transitions, types
    - Type-check: `cd taskflow && npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Public barrel shipped, client.ts not exported, type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Append D-05 re-export blocks to services/jira.ts (value + type) and run full lint + test</name>
  <files>taskflow/src/services/jira.ts</files>
  <read_first>
    - taskflow/src/services/jira.ts (entire existing re-export region; line numbers ~23-28 hold the current pattern: `export { addIssuesToSprint } from './jira/sprints'`, `export * from './jira-changelog'`)
    - .planning/phases/71-greenhopper-adapter-foundation/71-RESEARCH.md §"services/jira.ts re-export style" (lines ~474-506 — exact block to paste)
    - .planning/phases/71-greenhopper-adapter-foundation/71-PATTERNS.md §"src/services/jira.ts" (placement note: append at the bottom is acceptable; near other re-export blocks at the top is also acceptable — match what most recent re-export additions did)
    - .planning/phases/71-greenhopper-adapter-foundation/71-CONTEXT.md D-05 (locked surface list)
    - Memory note: `project_jira_ts_dual_file.md` — 60 imports use this legacy file; this is the right place
  </read_first>
  <action>
    Modify `taskflow/src/services/jira.ts` by appending two re-export blocks. Choose placement: prefer the bottom of the file (lower-risk for diff conflicts than weaving into the existing top block). Both blocks reference `'./jira/greenhopper'` (the barrel from Task 1).

    Block 1 — value re-exports (functions):
    - `export { fetchAllData, fetchBacklogData, fetchIssueDetails, fetchGhTransitions, adaptIssue, createAdapter, buildEntityMaps, resolveStatus, resolvePriority, resolveType, resolveEpic, resolveParent } from './jira/greenhopper';`

    Block 2 — type-only re-exports:
    - `export type { GhIssue, GhBoardIssue, GhAllDataResponse, GhBacklogResponse, GhDetailsResponse, GhTransitionsResponse, GhTransition, GhStatusEntity, GhPriorityEntity, GhTypeEntity, GhEpicEntity, EntityMaps } from './jira/greenhopper';`

    Add a 1-line comment header above the blocks: `// GreenHopper (Phase 71) — re-exported here per D-05 (legacy dual-file convention; consumers import from 'services/jira').`

    Do NOT modify any existing import or export in `jira.ts`. Do NOT alter `JiraIssue`/`JiraTransition` definitions. Do NOT add the re-exports to `src/services/jira/index.ts` (the modular barrel — explicitly excluded per Pitfall 1).

    After edit, run from `taskflow/`:
    - `npx biome check src/services/jira.ts src/services/jira/greenhopper` and fix any new biome errors/warnings to maintain the 0/0 baseline (memory `project_biome_state.md`).
    - `npx vitest run` (full suite) — must be green; this confirms no regression in the 60 existing jira.ts consumers.
    - `npx tsc --noEmit` — clean.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; grep -c "from './jira/greenhopper'" src/services/jira.ts &amp;&amp; test $(grep -c "from './jira/greenhopper'" src/services/jira.ts) -ge 2 &amp;&amp; npx biome check src/services/jira.ts src/services/jira/greenhopper &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "from './jira/greenhopper'" taskflow/src/services/jira.ts` ≥ 2 (one value block + one type block; Pitfall 1 verification grep from RESEARCH)
    - Source assertion: each named export appears exactly once across the two blocks (no duplicates with existing exports — verify by `grep -c "export {.*fetchAllData" taskflow/src/services/jira.ts` == 1)
    - Lint: `cd taskflow && npx biome check src/services/jira.ts src/services/jira/greenhopper` reports 0 errors and 0 warnings
    - Type-check: `cd taskflow && npx tsc --noEmit` clean
    - Full suite: `cd taskflow && npx vitest run` is green end-to-end (proves no regression in the 60 existing jira.ts consumers)
  </acceptance_criteria>
  <done>jira.ts re-exports the entire D-05 surface (functions + types); biome clean; full vitest suite green; GH-ADAPT-01/02/03 collectively reachable from `services/jira`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import graph integrity | Adding re-exports to a 60-import file risks a name collision or breaks a tree-shaking assumption — full vitest + biome run is the mitigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-15 | Tampering | Accidental client.ts re-export through barrel | mitigate | Task 1 acceptance grep on `from './client'` == 0 |
| T-71-16 | Spoofing | Name collision between GH re-export and existing jira.ts symbol | mitigate | Task 2 verify pipeline runs full tsc + vitest; any duplicate-export error halts the task |
| T-71-17 | Denial of Service | Lint regression breaks CI for downstream phases | mitigate | Task 2 runs biome check as part of the verify pipeline; baseline 0/0 must be preserved |
| T-71-18 | Information Disclosure | Wrong barrel (services/jira/index.ts) exports surface, bypassing the 60-import convention | mitigate | Pitfall 1 — re-exports go to services/jira.ts (legacy dual-file) ONLY; verified by grep on the specific file |
| T-71-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` clean
- `cd taskflow && npx vitest run` full suite green
- `cd taskflow && npx biome check src/services/jira.ts src/services/jira/greenhopper` 0 errors / 0 warnings
- `grep -c "from './jira/greenhopper'" taskflow/src/services/jira.ts` ≥ 2
- `grep -c "from './client'" taskflow/src/services/jira/greenhopper/index.ts` == 0
</verification>

<success_criteria>
- greenhopper/index.ts barrel ships (D-06 honored)
- services/jira.ts re-exports the full D-05 surface (value + type)
- Full vitest suite green; biome clean
- GH-ADAPT-01, GH-ADAPT-02, GH-ADAPT-03 collectively reachable via `import { ... } from 'services/jira'`
</success_criteria>

<output>
Create `.planning/phases/71-greenhopper-adapter-foundation/71-06-SUMMARY.md` when done summarizing: barrel contents, jira.ts re-export blocks (function + type names), final test/lint/tsc status, any deviations from D-05/D-06.
</output>
