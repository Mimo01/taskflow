---
phase: 27-refactoring-type-safety
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/issue-detail/index.ts
  - taskflow/src/routes/dashboard/issue-detail/IssueDetailSidebar.tsx
  - taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx
  - taskflow/src/routes/dashboard/issue-detail/DescriptionSection.tsx
  - taskflow/src/routes/dashboard/issue-detail/SubtasksSection.tsx
  - taskflow/src/routes/dashboard/issue-detail/LinkedIssuesSection.tsx
  - taskflow/src/routes/dashboard/issue-detail/MergeRequestsSection.tsx
  - taskflow/src/routes/dashboard/issue-detail/useFieldMutation.ts
  - taskflow/src/routes/dashboard/issue-detail/utils.ts
  - taskflow/src/routes/dashboard/IssueDetailSidebar.tsx
autonomous: true
requirements: [REFAC-03]

must_haves:
  truths:
    - "IssueDetailSidebar still renders all sections: fields, description, subtasks, linked issues, MRs"
    - "Field mutations (status, assignee, priority, sprint, epic) still work"
    - "Main orchestrator file is under 200 lines"
    - "All existing tests still pass after decomposition"
  artifacts:
    - path: "taskflow/src/routes/dashboard/issue-detail/IssueDetailSidebar.tsx"
      provides: "Slim layout orchestrator (~150 lines)"
    - path: "taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx"
      provides: "Status, assignee, priority, sprint, epic field editors"
    - path: "taskflow/src/routes/dashboard/issue-detail/useFieldMutation.ts"
      provides: "Shared field update hook with debounce"
      exports: ["useFieldMutation"]
    - path: "taskflow/src/routes/dashboard/issue-detail/index.ts"
      provides: "Barrel re-export of IssueDetailSidebar"
  key_links:
    - from: "taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx"
      to: "taskflow/src/routes/dashboard/issue-detail/useFieldMutation.ts"
      via: "import useFieldMutation"
      pattern: "import.*useFieldMutation"
    - from: "taskflow/src/routes/dashboard/issue-detail/IssueDetailSidebar.tsx"
      to: "taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx"
      via: "import FieldsSection"
      pattern: "import.*FieldsSection"
---

<objective>
Decompose IssueDetailSidebar.tsx (725 lines) into focused section sub-components with shared field mutation hook.

Purpose: Make the sidebar maintainable by extracting each visual section into its own component.
Output: issue-detail/ subdirectory with orchestrator + 5 section components + shared hook + utilities.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-refactoring-type-safety/27-CONTEXT.md
@.planning/phases/27-refactoring-type-safety/27-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared hook, utilities, and section sub-components</name>
  <files>taskflow/src/routes/dashboard/issue-detail/useFieldMutation.ts, taskflow/src/routes/dashboard/issue-detail/utils.ts, taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx, taskflow/src/routes/dashboard/issue-detail/DescriptionSection.tsx, taskflow/src/routes/dashboard/issue-detail/SubtasksSection.tsx, taskflow/src/routes/dashboard/issue-detail/LinkedIssuesSection.tsx, taskflow/src/routes/dashboard/issue-detail/MergeRequestsSection.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/IssueDetailSidebar.tsx (full file -- understand all sections, utility functions, field mutation patterns, props interface)
  </read_first>
  <action>
1. Create `taskflow/src/routes/dashboard/issue-detail/` directory.

2. Create `taskflow/src/routes/dashboard/issue-detail/utils.ts`:
   - Extract utility functions from IssueDetailSidebar.tsx. Look for functions like `extractSprintName`, `statusDot`, `mrStateClasses` or similar helper functions defined at the top of the file or outside the component.
   - Only extract functions that are used by multiple sub-components. If a utility is only used by one section, keep it in that section's file.
   - Check if any of these utilities are used outside of IssueDetailSidebar (in other dashboard components). If so, consider placing them in a more shared location -- but for now, keep in issue-detail/utils.ts.

3. Create `taskflow/src/routes/dashboard/issue-detail/useFieldMutation.ts`:
   - Extract the field update pattern from IssueDetailSidebar. This is the logic that calls `updateIssueField` from `@/services/jira` and handles optimistic updates or refetches.
   - If there is a debounce pattern for description or other text fields, include `useDebounce` in this file.
   - The hook should accept the issue key and return a mutation function.
   - Use TanStack Query's `useMutation` if the current code uses it, or replicate the exact pattern used.

4. Create the 5 section sub-components by identifying the JSX sections in IssueDetailSidebar:

   **`FieldsSection.tsx`**: The section rendering status, assignee, priority, sprint, and epic fields with their edit controls (dropdowns, popovers, etc.). This is likely the largest section.
   - Props: issue detail data, field mutation handler, available options (statuses, users, sprints, epics)
   - Uses useFieldMutation hook

   **`DescriptionSection.tsx`**: The markdown description display section.
   - Props: description string, issue key (for edit if applicable)
   - May include edit-in-place functionality if present

   **`SubtasksSection.tsx`**: The subtask list section.
   - Props: subtasks array, navigation handler

   **`LinkedIssuesSection.tsx`**: The linked issues display section.
   - Props: linked issues array, navigation handler

   **`MergeRequestsSection.tsx`**: The merge request list display section.
   - Props: merge requests array, any state classes helper

5. Each sub-component:
   - Receives data via props (prop threading, no React context -- per project convention)
   - Preserves ALL existing Tailwind classes exactly (no visual changes)
   - Has the same DOM output as the original inline JSX
   - Imports shared utilities from `./utils` as needed

6. **Do NOT create the orchestrator or barrel yet** -- that is Task 2.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/dashboard/issue-detail/useFieldMutation.ts exists and contains `export function useFieldMutation` or `export const useFieldMutation`
    - taskflow/src/routes/dashboard/issue-detail/utils.ts exists (even if small)
    - taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx exists and contains `export function FieldsSection` or `export default function FieldsSection`
    - taskflow/src/routes/dashboard/issue-detail/DescriptionSection.tsx exists
    - taskflow/src/routes/dashboard/issue-detail/SubtasksSection.tsx exists
    - taskflow/src/routes/dashboard/issue-detail/LinkedIssuesSection.tsx exists
    - taskflow/src/routes/dashboard/issue-detail/MergeRequestsSection.tsx exists
    - TypeScript compiles without errors (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>All 5 section sub-components, shared hook, and utilities created. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create orchestrator, barrel export, update original file</name>
  <files>taskflow/src/routes/dashboard/issue-detail/IssueDetailSidebar.tsx, taskflow/src/routes/dashboard/issue-detail/index.ts, taskflow/src/routes/dashboard/IssueDetailSidebar.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/IssueDetailSidebar.tsx (understand the component's props interface and how it's imported by consumers)
    - taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx (from Task 1)
    - taskflow/src/routes/dashboard/issue-detail/DescriptionSection.tsx (from Task 1)
    - taskflow/src/routes/dashboard/issue-detail/SubtasksSection.tsx (from Task 1)
    - taskflow/src/routes/dashboard/issue-detail/LinkedIssuesSection.tsx (from Task 1)
    - taskflow/src/routes/dashboard/issue-detail/MergeRequestsSection.tsx (from Task 1)
    - taskflow/src/routes/dashboard/IssueDetailPage.tsx (consumer -- check how IssueDetailSidebar is imported)
    - taskflow/src/routes/dashboard/IssueDetailSheet.tsx (consumer -- check import path)
  </read_first>
  <action>
1. Create `taskflow/src/routes/dashboard/issue-detail/IssueDetailSidebar.tsx`:
   - Import all 5 section sub-components
   - Import useFieldMutation hook
   - Keep any TanStack Query data-fetching hooks that load issue detail data in the orchestrator
   - Wire sections together: pass issue data as props to each section
   - Maintain the EXACT same props interface as the original component
   - Target: ~150 lines (under 200 absolute max)
   - The orchestrator handles layout (section ordering, conditional rendering) but delegates rendering to sub-components

2. Create `taskflow/src/routes/dashboard/issue-detail/index.ts`:
   ```typescript
   export { IssueDetailSidebar } from './IssueDetailSidebar';
   ```
   Also re-export any types that consumers need.

3. Update the ORIGINAL `taskflow/src/routes/dashboard/IssueDetailSidebar.tsx`:
   - Replace its content with a re-export: `export { IssueDetailSidebar } from './issue-detail';`
   - This ensures any direct imports of the old path still work.
   - Check all consumer imports: if they use `from './IssueDetailSidebar'` or `from '@/routes/dashboard/IssueDetailSidebar'`, the re-export shim handles it.

4. Run full test suite: `cd taskflow && npx vitest --run`
5. Run Biome check: `cd taskflow && npx biome check src/routes/dashboard/issue-detail/`
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/dashboard/issue-detail/IssueDetailSidebar.tsx exists
    - `wc -l taskflow/src/routes/dashboard/issue-detail/IssueDetailSidebar.tsx` is under 200
    - taskflow/src/routes/dashboard/issue-detail/index.ts exists and contains `export { IssueDetailSidebar }`
    - The orchestrator imports all 5 section components (grep for FieldsSection, DescriptionSection, SubtasksSection, LinkedIssuesSection, MergeRequestsSection)
    - The original IssueDetailSidebar.tsx re-exports from issue-detail directory
    - All tests pass (npx vitest --run exits 0)
    - No import errors across the codebase (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>IssueDetailSidebar decomposed into orchestrator (~150 lines) + 5 section components + shared hook + utilities. All tests pass.</done>
</task>

</tasks>

<verification>
- `cd taskflow && npx vitest --run` — all tests pass
- `cd taskflow && npx tsc --noEmit` — no TypeScript errors
- `wc -l taskflow/src/routes/dashboard/issue-detail/IssueDetailSidebar.tsx` — under 200 lines
- `wc -l taskflow/src/routes/dashboard/issue-detail/*.tsx taskflow/src/routes/dashboard/issue-detail/*.ts` — each file under 200 lines
</verification>

<success_criteria>
- IssueDetailSidebar (725 lines) decomposed into ~9 focused files
- Orchestrator under 200 lines
- Each section component is a focused, self-contained unit
- Shared field mutation hook eliminates duplicated update patterns
- All existing imports and tests continue to work
- Visual output identical to original
</success_criteria>

<output>
After completion, create `.planning/phases/27-refactoring-type-safety/27-04-SUMMARY.md`
</output>
