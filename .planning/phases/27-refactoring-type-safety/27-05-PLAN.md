---
phase: 27-refactoring-type-safety
plan: 05
type: execute
wave: 2
depends_on: [27-01, 27-02, 27-03, 27-04]
files_modified:
  - taskflow/src/stores/settings.store.ts
  - taskflow/src/stores/recent-items.store.ts
  - taskflow/src/stores/pinned-tabs.store.ts
  - taskflow/src/routes/notifications/NotificationRow.tsx
  - taskflow/src/services/gitlab.ts
  - taskflow/biome.json
autonomous: true
requirements: [TYPE-01, TYPE-02, REFAC-06]

must_haves:
  truths:
    - "Zero `as unknown as` double-casts remain in production source files"
    - "Zero `any` types in production source files (TYPE-02 confirmed or fixed)"
    - "Biome noExplicitAny rule is enabled as error for production code"
    - "All existing tests still pass"
    - "REFAC-06 is documented as satisfied"
  artifacts:
    - path: "taskflow/biome.json"
      provides: "Updated Biome config with noExplicitAny enabled"
      contains: "noExplicitAny.*error"
  key_links:
    - from: "taskflow/biome.json"
      to: "taskflow/src/"
      via: "Biome lint enforcement"
      pattern: "noExplicitAny.*error"
---

<objective>
Fix all remaining double-casts (TYPE-01), verify no `any` types exist (TYPE-02), enable Biome strict type rules, and document REFAC-06 as satisfied.

Purpose: Eliminate all unsafe type patterns and enable Biome rules to prevent future regressions.
Output: Zero double-casts, Biome noExplicitAny enabled, all type safety requirements met.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-refactoring-type-safety/27-CONTEXT.md
@.planning/phases/27-refactoring-type-safety/27-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all remaining double-casts across 5 files</name>
  <files>taskflow/src/stores/settings.store.ts, taskflow/src/stores/recent-items.store.ts, taskflow/src/stores/pinned-tabs.store.ts, taskflow/src/routes/notifications/NotificationRow.tsx, taskflow/src/services/gitlab.ts</files>
  <read_first>
    - taskflow/src/stores/settings.store.ts (line 252: `return s as unknown as SettingsState`)
    - taskflow/src/stores/recent-items.store.ts (line 57: `persisted as unknown as RecentItemsState`)
    - taskflow/src/stores/pinned-tabs.store.ts (line 57: `persisted as unknown as PinnedTabsState`)
    - taskflow/src/routes/notifications/NotificationRow.tsx (line 161: `e as unknown as React.MouseEvent`)
    - taskflow/src/services/gitlab.ts (lines 754 and 803: `data as unknown as GitLabMR[]`)
  </read_first>
  <action>
NOTE: The 3 double-casts in jira.ts have already been fixed in Plan 02 (replaced with `isResponseLikeError` type guard in jira/client.ts). This task fixes the remaining 6 double-casts in other files.

**Fix 1 -- settings.store.ts (line 252):**
Current: `return s as unknown as SettingsState;`
Fix: The `migrate` function builds up state `s` through version checks. The variable `s` is typed as `Record<string, unknown>` or similar. After all migration guards, a single assertion is valid:
```typescript
return s as SettingsState;
```
Remove the `unknown` intermediate. The `migrate` function's purpose IS to assert the shape after migration, so a single `as SettingsState` is semantically correct.

If TypeScript still complains about the single cast (because the source type is too different), type the `persisted` parameter of the migrate function:
```typescript
migrate: (persisted: Record<string, unknown>, version: number) => {
  const s = persisted as Record<string, unknown>;
  // ... existing version checks ...
  return s as SettingsState;
},
```

**Fix 2 -- recent-items.store.ts (line 57):**
Current: `migrate: (persisted, _version) => persisted as unknown as RecentItemsState,`
Fix: Type the parameter and use single assertion:
```typescript
migrate: (persisted: Record<string, unknown>, _version: number) => persisted as RecentItemsState,
```

**Fix 3 -- pinned-tabs.store.ts (line 57):**
Current: `migrate: (persisted, _version) => persisted as unknown as PinnedTabsState,`
Fix: Same pattern:
```typescript
migrate: (persisted: Record<string, unknown>, _version: number) => persisted as PinnedTabsState,
```

**Fix 4 -- NotificationRow.tsx (line 161):**
Current: `onClick(e as unknown as React.MouseEvent);` inside a `onKeyDown` handler.
Fix: Change the `onClick` prop type on the `ActionIcon` component from:
```typescript
onClick: (e: React.MouseEvent) => void;
```
to:
```typescript
onClick: (e: React.MouseEvent | React.KeyboardEvent) => void;
```
Then the call becomes simply `onClick(e)` with no cast needed. The handler likely only uses `e.stopPropagation()` which exists on both event types.

Also check all callers of ActionIcon to ensure they accept the wider type. If callers destructure mouse-specific properties from the event, a different approach is needed -- but based on the codebase, the event is used only for stopPropagation.

**Fix 5 -- gitlab.ts (line 754):**
Current: `const data = (await response.json()) as Array<Record<string, unknown>>;` followed by label enrichment, then `return data as unknown as GitLabMR[];`
Fix: Type the initial `response.json()` result directly:
```typescript
const data = (await response.json()) as GitLabMR[];
```
Then the label enrichment loop mutates `data` in place (which works since we're asserting the shape), and the return becomes simply `return data;` with no cast.

**Fix 6 -- gitlab.ts (line 803):**
Same pattern as Fix 5. In the `searchMergeRequests` function:
```typescript
const data = (await response.json()) as GitLabMR[];
```
And return `data` directly.

After all fixes, verify: `grep -rn "as unknown as" taskflow/src/ --include="*.ts" --include="*.tsx" | grep -v test | grep -v node_modules` should return ZERO results.

Run: `cd taskflow && npx vitest --run`
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && grep -rn "as unknown as" src/ --include="*.ts" --include="*.tsx" | grep -v test | grep -v node_modules | grep -v ".d.ts" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "as unknown as" taskflow/src/ --include="*.ts" --include="*.tsx" | grep -v test | grep -v node_modules | grep -v ".d.ts"` returns 0 lines
    - taskflow/src/stores/settings.store.ts contains `as SettingsState` but NOT `as unknown as SettingsState`
    - taskflow/src/stores/recent-items.store.ts contains `as RecentItemsState` but NOT `as unknown as RecentItemsState`
    - taskflow/src/stores/pinned-tabs.store.ts contains `as PinnedTabsState` but NOT `as unknown as PinnedTabsState`
    - taskflow/src/routes/notifications/NotificationRow.tsx does NOT contain `as unknown as React.MouseEvent`
    - taskflow/src/routes/notifications/NotificationRow.tsx contains `React.MouseEvent | React.KeyboardEvent` in the onClick prop type
    - taskflow/src/services/gitlab.ts does NOT contain `as unknown as GitLabMR[]`
    - All tests pass (npx vitest --run exits 0)
  </acceptance_criteria>
  <done>All 9 double-casts eliminated from production code. Zero `as unknown as` patterns remain. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify TYPE-02, enable Biome noExplicitAny, run final checks</name>
  <files>taskflow/biome.json</files>
  <read_first>
    - taskflow/biome.json (current rule configuration, lines 30-90)
  </read_first>
  <action>
1. **Verify TYPE-02 (no `any` types in production):**
   Run: `grep -rn ": any" taskflow/src/ --include="*.ts" --include="*.tsx" | grep -v test | grep -v node_modules | grep -v ".d.ts"`

   The research phase found 0 occurrences. Confirm this is still true after all refactoring. If any new `any` types were introduced during Plans 01-04, fix them now with proper types.

2. **Update biome.json to enable noExplicitAny as "error" for production code:**

   Change line 35 from:
   ```json
   "noExplicitAny": "off",
   ```
   to:
   ```json
   "noExplicitAny": "error",
   ```

   The test file override at lines 78-88 already keeps `noExplicitAny` as `"off"` for test files -- this is correct and should remain.

3. **Run Biome check to ensure no violations:**
   `cd taskflow && npx biome check src/`

   If any violations are found (production files using explicit `any`), fix them with proper types before proceeding.

4. **Run full test suite one final time:**
   `cd taskflow && npx vitest --run`

5. **Run TypeScript compiler check:**
   `cd taskflow && npx tsc --noEmit`

6. **Document REFAC-06 status:** The notifications store split was assessed during Plan 01 Task 1 and found to be already satisfied by the existing `partialize()` pattern. No code changes were made. This should be noted in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx biome check src/ 2>&1 | tail -5 && npx vitest --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/biome.json line in suspicious rules contains `"noExplicitAny": "error"` (not "off")
    - taskflow/biome.json test override still contains `"noExplicitAny": "off"` for test files
    - `cd taskflow && npx biome check src/` exits 0 (no errors)
    - `grep -rn ": any\b" taskflow/src/ --include="*.ts" --include="*.tsx" | grep -v test | grep -v node_modules | grep -v ".d.ts"` returns 0 lines (TYPE-02 confirmed)
    - `cd taskflow && npx vitest --run` exits 0 (all tests pass)
    - `cd taskflow && npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>TYPE-02 verified (0 `any` types). Biome noExplicitAny enabled as error. All type safety requirements complete. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `cd taskflow && grep -rn "as unknown as" src/ --include="*.ts" --include="*.tsx" | grep -v test | grep -v node_modules | grep -v ".d.ts" | wc -l` — returns 0
- `cd taskflow && npx biome check src/` — exits 0
- `cd taskflow && npx vitest --run` — all tests pass
- `cd taskflow && npx tsc --noEmit` — exits 0
- `grep "noExplicitAny" taskflow/biome.json` — shows "error" in main rules, "off" in test override
</verification>

<success_criteria>
- All 9 `as unknown as` double-casts eliminated (TYPE-01 complete)
- Zero `any` types in production code confirmed (TYPE-02 complete)
- Biome noExplicitAny enabled as "error" for production code
- REFAC-06 documented as satisfied by existing partialize() pattern
- All 489+ tests pass
- Biome and TypeScript checks clean
</success_criteria>

<output>
After completion, create `.planning/phases/27-refactoring-type-safety/27-05-SUMMARY.md`
</output>
