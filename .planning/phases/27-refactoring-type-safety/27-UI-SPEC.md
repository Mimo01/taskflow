---
phase: 27
slug: refactoring-type-safety
status: draft
shadcn_initialized: true
preset: existing (shadcn + Tailwind v4 + Geist font)
created: 2026-03-19
---

# Phase 27 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Phase classification: NON-VISUAL (pure refactoring)**

Phase 27 is a code-structure-only refactoring phase. It decomposes large modules, extracts shared utilities, fixes type safety issues, and enables Biome strict rules. There are zero new UI components, zero visual changes, and zero interaction changes. All existing visual behavior must be preserved identically after refactoring.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized) |
| Preset | existing project preset (Tailwind v4 + shadcn) |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react (existing) |
| Font | Geist Variable (@fontsource-variable/geist) |

**Phase 27 impact:** No design system changes. All existing tokens, components, and styles remain unchanged.

---

## Spacing Scale

Existing project spacing scale (Tailwind v4 defaults, multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none

**Phase 27 impact:** No spacing changes. Extracted sub-components must preserve identical spacing from the original monolithic components.

---

## Typography

Existing project typography (no changes in this phase):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 12px | 500 (medium) | 1.4 |
| Heading | 20px | 600 (semibold) | 1.2 |
| Display | 28px | 600 (semibold) | 1.2 |

**Phase 27 impact:** No typography changes. All extracted components inherit parent typography exactly.

---

## Color

Existing project color system (CSS custom properties via shadcn, no changes in this phase):

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | var(--background) | Background, surfaces |
| Secondary (30%) | var(--card), var(--muted) | Cards, sidebar, nav |
| Accent (10%) | var(--primary) | Primary buttons, active states, links |
| Destructive | var(--destructive) | Delete actions, error states |

Accent reserved for: primary CTA buttons, active navigation indicators, focused input borders, link text

**Phase 27 impact:** No color changes. REFAC-08 replaces one inline gradient style in SprintBoardTab with equivalent Tailwind classes -- the visual output must be pixel-identical.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | Not applicable -- no new UI elements |
| Empty state heading | Not applicable -- no new states |
| Empty state body | Not applicable -- no new states |
| Error state | Not applicable -- no new error handling UI |
| Destructive confirmation | Not applicable -- no new destructive actions |

**Phase 27 impact:** No copywriting changes. All existing user-facing text remains identical. The refactoring must not alter any rendered strings, labels, tooltips, or error messages.

---

## Visual Regression Contract

Since this phase restructures component code without changing behavior, the following visual regression rules apply:

| Rule | Enforcement |
|------|-------------|
| Extracted sub-components produce identical DOM output | Manual visual inspection + existing test suite (489 tests) |
| No CSS class changes in extracted components | Preserve all existing Tailwind classes exactly |
| No prop interface changes on public components | TypeScript compilation catches signature mismatches |
| REFAC-08 inline style replacement | SprintBoardTab gradient must render identically with Tailwind classes |
| useReducer migration preserves form behavior | All form states, resets, and validations behave identically |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | No new blocks | not required |

**Phase 27 impact:** No new shadcn components installed. No registry operations needed.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS (not applicable -- non-visual phase)
- [ ] Dimension 2 Visuals: PASS (no visual changes -- regression contract defined)
- [ ] Dimension 3 Color: PASS (not applicable -- no color changes)
- [ ] Dimension 4 Typography: PASS (not applicable -- no typography changes)
- [ ] Dimension 5 Spacing: PASS (not applicable -- no spacing changes)
- [ ] Dimension 6 Registry Safety: PASS (no new registry blocks)

**Approval:** pending
