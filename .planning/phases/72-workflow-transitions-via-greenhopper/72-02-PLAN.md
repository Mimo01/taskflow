---
phase: 72
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - taskflow/src/routes/dashboard/StatusPopover.tsx
  - taskflow/src/routes/dashboard/StatusPopover.test.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
  - taskflow/src/routes/dashboard/BulkActionBar.tsx
  - taskflow/src/routes/dashboard/BulkActionBar.test.tsx
  - taskflow/src/routes/dashboard/QuickCreateInput.tsx
  - taskflow/src/routes/dashboard/QuickCreateInput.test.tsx
autonomous: true
requirements: [GH-TRANS-02, GH-TRANS-03]
tags: [greenhopper, react-query, sprint-board, status-popover, bulk-action, quick-create, transitions, jira]

must_haves:
  truths:
    - "StatusPopover reads transitions via useGhTransitions(Number(issue.fields.project.id), issue.fields.issuetype.id) — no fetchTransitions call (GH-TRANS-02)"
    - "SprintBoardTab issues exactly one /rest/greenhopper/1.0/xboard/work/transitions.json per project per session via a single useGhTransitions call; per-issue prefetch loop is gone (GH-TRANS-02)"
    - "BulkActionBar resolves per-issue transitions through getGhTransitions(queryClient, baseUrl, token, projectId, issueTypeId) — no fetchTransitions call (GH-TRANS-02)"
    - "QuickCreateInput resolves transitions through getGhTransitions(queryClient, baseUrl, token, projectId, issueTypeId) — no fetchTransitions call (GH-TRANS-02)"
    - "Sprint-board toolbar has a 'Reload workflow transitions' control that invalidates ['gh-transitions-envelope', projectId] + ['gh-transitions', projectId] + ['jira-statuses'] and triggers refetch; feedback uses the existing inline lastRefreshed label idiom (no toast dependency added) (GH-TRANS-03, D-07)"
    - "projectId for the toolbar refresh action is derived from the already-fetched ['project-statuses', activeJiraProject] query data (no new fetch) per planner decision Q3"
  artifacts:
    - path: taskflow/src/routes/dashboard/StatusPopover.tsx
      provides: "Component reads transitions through useGhTransitions"
      contains: "useGhTransitions"
    - path: taskflow/src/routes/dashboard/SprintBoardTab.tsx
      provides: "Project-level envelope warm + toolbar 'Reload workflow transitions' action"
      contains: "invalidateGhTransitions"
    - path: taskflow/src/routes/dashboard/BulkActionBar.tsx
      provides: "Per-issue transition lookup via getGhTransitions"
      contains: "getGhTransitions"
    - path: taskflow/src/routes/dashboard/QuickCreateInput.tsx
      provides: "Post-create transition lookup via getGhTransitions; projectId+issueTypeId received as props"
      contains: "getGhTransitions"
  key_links:
    - from: taskflow/src/routes/dashboard/StatusPopover.tsx
      to: "@/services/jira (useGhTransitions)"
      via: "named import"
      pattern: "useGhTransitions"
    - from: taskflow/src/routes/dashboard/SprintBoardTab.tsx
      to: "@/services/jira (useGhTransitions, invalidateGhTransitions)"
      via: "named import"
      pattern: "invalidateGhTransitions"
    - from: taskflow/src/routes/dashboard/BulkActionBar.tsx
      to: "@/services/jira (getGhTransitions)"
      via: "named import"
      pattern: "getGhTransitions"
    - from: taskflow/src/routes/dashboard/QuickCreateInput.tsx
      to: "@/services/jira (getGhTransitions)"
      via: "named import"
      pattern: "getGhTransitions"
---

<objective>
Swap the four legacy `fetchTransitions` call sites to the new Plan 01 cache APIs (`useGhTransitions` / `getGhTransitions` / `invalidateGhTransitions`) and add the sprint-board toolbar "Reload workflow transitions" action per D-07. No legacy code is removed in this plan — `fetchTransitions` still exists in `services/jira.ts` and `services/jira/transitions.ts`; Plan 03 deletes it after this plan demonstrates the four call sites work entirely through the new cache.

Purpose: This is the GH-TRANS-02 + GH-TRANS-03 plan. After this plan ships, opening the sprint board issues exactly one `/rest/greenhopper/1.0/xboard/work/transitions.json` per project per session and zero `/rest/api/2/issue/*/transitions` calls; the toolbar action invalidates the envelope + status map and refetches. Feedback uses the existing inline `lastRefreshed` label idiom at `SprintBoardTab.tsx:1103` per planner decision Q1 (no toast library installed; RESEARCH Pitfall 5). The exact success/error strings come straight from D-07: "Workflow transitions reloaded" / "Failed to reload workflow".

Output:
- Four call-site components migrated to the new API surface
- Tests at each call site updated to mock the new APIs (and verify no `fetchTransitions` mock is invoked)
- Sprint-board toolbar action with inline aria-live feedback, no new dependency
- Full taskflow test suite green
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-workflow-transitions-via-greenhopper/72-CONTEXT.md
@.planning/phases/72-workflow-transitions-via-greenhopper/72-RESEARCH.md
@.planning/phases/72-workflow-transitions-via-greenhopper/72-PATTERNS.md
@.planning/phases/72-workflow-transitions-via-greenhopper/72-01-SUMMARY.md

@taskflow/src/routes/dashboard/StatusPopover.tsx
@taskflow/src/routes/dashboard/SprintBoardTab.tsx
@taskflow/src/routes/dashboard/BulkActionBar.tsx
@taskflow/src/routes/dashboard/QuickCreateInput.tsx
@taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
@taskflow/src/routes/dashboard/QuickCreateInput.test.tsx
@taskflow/src/services/jira/greenhopper/transitions.ts
@taskflow/src/services/jira.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate StatusPopover + SprintBoardTab (hook consumers + toolbar refresh action)</name>
  <files>
    taskflow/src/routes/dashboard/StatusPopover.tsx,
    taskflow/src/routes/dashboard/StatusPopover.test.tsx,
    taskflow/src/routes/dashboard/SprintBoardTab.tsx,
    taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
  </files>
  <read_first>
    taskflow/src/routes/dashboard/StatusPopover.tsx (full file — current useQuery(['transitions', issueKey]) at lines 42-55, imports at 17-18, prop surface used by callers);
    taskflow/src/routes/dashboard/SprintBoardTab.tsx (full read of lines 1-50 imports, 500-750 for activeJiraProject + workflowStatuses query + prefetch loop, 1090-1130 for toolbar host; PATTERNS analogs §SprintBoardTab.tsx);
    taskflow/src/routes/dashboard/SprintBoardTab.test.tsx (full file — observe existing mock conventions for `@/services/jira` so the swap can mirror them);
    taskflow/src/services/jira/greenhopper/transitions.ts (Plan 01 output — useGhTransitions / invalidateGhTransitions signatures);
    .planning/phases/72-workflow-transitions-via-greenhopper/72-PATTERNS.md (§ StatusPopover.tsx + § SprintBoardTab.tsx — line-by-line swap guidance and toolbar analog at 1101-1117)
  </read_first>
  <behavior>
    StatusPopover:
    - Component renders the same DOM/API as today; the data source flips
    - When opened against issue X with `fields.project.id="10042"` and `fields.issuetype.id="3"`, the hook is called as `useGhTransitions(10042, "3")`
    - No mock of `fetchTransitions` is invoked during a render that previously fetched transitions
    - On `isLoading` / `isError` / data-present states the UI shows the same affordances it shows today (loading row, error row, transition list)

    SprintBoardTab:
    - On board mount with N issues across the same project, exactly ONE call to mocked `fetchGhTransitions` (or one envelope cache write) occurs — not N
    - The legacy per-issue prefetch loop (current lines ~729-742 using `queryClient.fetchQuery({queryKey: ['transitions', issue.key], ...})`) is removed
    - The local `getTransitions(issueKey)` helper is renamed/retyped to accept a `JiraIssue` (signature: `function getTransitions(issue: JiraIssue): JiraTransition[] | undefined`) and reads the per-type cache `['gh-transitions', Number(issue.fields.project.id), issue.fields.issuetype.id]`. Callers of the helper inside `SprintBoardTab.tsx` are updated to pass the full issue object. This shape is committed per PATTERNS §SprintBoardTab.tsx Analog #2 — no inline-rewrite alternative.
    - Toolbar has a new button sibling to the existing RefreshCw at lines ~1101-1117 with `aria-label="Reload workflow transitions"`, `title="Reload workflow transitions"`, lucide `Workflow` icon, same className/sizing
    - Click handler:
      • Reads projectId from the already-fetched `['project-statuses', activeJiraProject, jiraBaseUrl]` query data (response carries the project's numeric id) per planner decision Q3 — do NOT add a new fetch
      • Calls `invalidateGhTransitions(queryClient, projectId)` AND `queryClient.invalidateQueries({ queryKey: ['jira-statuses'] })`
      • Inline feedback: updates an `aria-live="polite"` label co-located with the existing `lastRefreshed` span (line 1103) — text reads exactly `"Workflow transitions reloaded"` on success and exactly `"Failed to reload workflow"` on error (these strings are quoted directly from CONTEXT D-07)
    - Action is visible whenever a sprint board is shown (D-07a); no keyboard shortcut (D-07b)
  </behavior>
  <action>
    STATUS POPOVER:
    Read the existing prop surface of `StatusPopover.tsx`. Today it accepts `issueKey` (and pulls project/typeId from somewhere — likely the same parent renders it next to a `JiraIssue`). Update the component props: replace `issueKey: string` with `projectId: number` and `issueTypeId: string` (or accept the `issue: JiraIssue` object and derive both inline — CONTEXT D-05 + Claude's-Discretion area allows either; pick the IDs-direct form to minimise the prop surface). Replace the existing `useQuery({queryKey: ['transitions', issueKey], queryFn: ... fetchTransitions(...)})` block (lines 42-55) with `const { data: transitions, isLoading, isError, refetch } = useGhTransitions(projectId, issueTypeId);`. Remove `enabled: false` + manual `refetch()` on open — the envelope cache is project-scoped and the per-type adapt is cheap. Update imports: drop `fetchTransitions`; add `useGhTransitions`. Remove `readSecret` import if it's only used by the deleted fetcher branch. Update the parent renderers of `StatusPopover` (grep `<StatusPopover` to find them) to pass the new props.

    Create `taskflow/src/routes/dashboard/StatusPopover.test.tsx` (if absent — RESEARCH Wave 0 says it doesn't exist). Mock `@/services/jira`'s `useGhTransitions` returning `{ data: [...], isLoading: false, isError: false, refetch: vi.fn() }`. Cases: (1) renders transition list from the hook return; (2) loading state when `isLoading: true`; (3) error state when `isError: true`; (4) no call to `fetchTransitions` mock — assert via `vi.spyOn` that the legacy name is NOT in the mock map / is not called.

    SPRINT BOARD TAB:
    Replace the prefetch loop at lines ~729-742 with a single `useGhTransitions(projectId, sentinelTypeId)` call that warms the project envelope. The `projectId` is derived from `workflowStatuses` query result (the `['project-statuses', activeJiraProject, jiraBaseUrl]` data already fetched at lines 709-716 — it carries the numeric project id; read its shape from the existing code and pick the field, typically `workflowStatuses?.[0]?.projectId` or similar — verify during implementation). `sentinelTypeId` can be the first issue's `issuetype.id`. Document the choice in a code comment.

    Update the `getTransitions(...)` helper (current line ~744) to read from the per-type cache. Commit to the issue-object form per PATTERNS §SprintBoardTab.tsx Analog #2: change signature from `(issueKey: string)` to `(issue: JiraIssue)` and body to `queryClient.getQueryData<JiraTransition[]>(['gh-transitions', Number(issue.fields.project.id), issue.fields.issuetype.id])`. Audit and update its callers in the same file to pass the full issue object.

    Add the toolbar refresh button as a sibling to the existing RefreshCw at lines ~1101-1117, inside the same flex container. Use the same className. `aria-label="Reload workflow transitions"`. Icon: `Workflow` from `lucide-react` (add to existing lucide import). Click handler per the behavior block. Add a small `useState<string | null>(null)` for the inline status text and clear it after ~3s with a `setTimeout` cleaned up on unmount (mirror the existing transient-feedback patterns in this file if any; if none, document the timeout cleanup). Render the status text in an `aria-live="polite"` `<span>` placed next to the existing `lastRefreshed` span (line ~1103). The two literal strings to surface (verbatim from CONTEXT D-07) are `"Workflow transitions reloaded"` on success and `"Failed to reload workflow"` on error — write them as inline string literals so source-grep gates can verify their presence.

    Update imports in `SprintBoardTab.tsx`: drop `fetchTransitions` from the `@/services/jira` import; add `useGhTransitions, invalidateGhTransitions`. Keep `JiraTransition` type import.

    Update `taskflow/src/routes/dashboard/SprintBoardTab.test.tsx`: replace any `fetchTransitions` mock with mocks for `useGhTransitions` and `invalidateGhTransitions`. Add a test "issues a single transitions envelope fetch per project on mount" — render the board with multiple issues, assert mocked `fetchGhTransitions` (or the hook's underlying fetcher mock) is called exactly once. Add a test "toolbar 'Reload workflow transitions' action invalidates envelope + jira-statuses" — render the board, click the new button by `aria-label`, assert `invalidateGhTransitions` mock called once with `(queryClient, projectId)` AND that `queryClient.invalidateQueries` is called with the exact argument `{ queryKey: ['jira-statuses'] }` (use `expect(invalidateSpy).toHaveBeenCalledWith({ queryKey: ['jira-statuses'] })`). Add a test that after a successful click, an element with role `status` (or selected via `getByText`) renders the exact text `"Workflow transitions reloaded"`. (Promote this aria-live assertion from prose to an explicit test — it is the user-visible contract for D-07.)

    Verify the existing RefreshCw button at lines 1101-1117 is NOT changed (Phase 73 may unify them; Phase 72 ships a focused sibling action per D-07a).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/StatusPopover.test.tsx src/routes/dashboard/SprintBoardTab.test.tsx && npx tsc --noEmit -p .</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (StatusPopover no longer imports fetchTransitions): `grep -cE "fetchTransitions" taskflow/src/routes/dashboard/StatusPopover.tsx` returns `0`
    - Source assertion (StatusPopover uses useGhTransitions): `grep -c "useGhTransitions" taskflow/src/routes/dashboard/StatusPopover.tsx` returns at least `1`
    - Source assertion (SprintBoardTab no longer imports fetchTransitions): `grep -cE "fetchTransitions" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns `0`
    - Source assertion (SprintBoardTab uses invalidateGhTransitions): `grep -c "invalidateGhTransitions" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns at least `1`
    - Source assertion (legacy queryKey gone): `grep -cE "queryKey:\s*\[\s*'transitions'" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns `0` and same for StatusPopover.tsx returns `0`
    - Source assertion (toolbar aria-label): `grep -c "Reload workflow transitions" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns at least `2` (aria-label + title; also possibly inline status text)
    - Source assertion (aria-live for feedback): `grep -c "aria-live" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns at least `1`
    - **B-01: inline success string present in source:** `grep -c "Workflow transitions reloaded" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns at least `1`
    - **B-01: inline error string present in source:** `grep -c "Failed to reload workflow" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns at least `1`
    - **B-01: inline success string asserted in test:** `grep -c "Workflow transitions reloaded" taskflow/src/routes/dashboard/SprintBoardTab.test.tsx` returns at least `1` (the test renders the board, clicks the new button, and asserts the aria-live span text reads `"Workflow transitions reloaded"`)
    - **B-02: jira-statuses queryKey invalidation in source:** `grep -cE "queryKey:.*['\"]jira-statuses['\"]" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns at least `1`
    - **B-02: jira-statuses invalidation asserted in test:** `grep -cE "queryKey:.*['\"]jira-statuses['\"]" taskflow/src/routes/dashboard/SprintBoardTab.test.tsx` returns at least `1` (the test asserts `queryClient.invalidateQueries` was called with `{ queryKey: ['jira-statuses'] }`)
    - **W-02: getTransitions helper takes a JiraIssue (issue-object form, no `(issueKey: string)` alternative):** `grep -cE "function getTransitions\(issue" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns at least `1`
    - Test command exits 0; new tests above all green
    - No toast dependency added: `git diff package.json` shows no new entries; `grep -cE "sonner|react-hot-toast|react-toastify|@radix-ui/react-toast" taskflow/package.json` returns `0`
  </acceptance_criteria>
  <done>
    StatusPopover + SprintBoardTab read from the new cache; sprint board issues one transitions fetch per project session; toolbar action with inline aria-live feedback shipped using the exact D-07 strings; tsc + the two test files green; no toast lib added.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate BulkActionBar + QuickCreateInput (imperative consumers via getGhTransitions)</name>
  <files>
    taskflow/src/routes/dashboard/BulkActionBar.tsx,
    taskflow/src/routes/dashboard/BulkActionBar.test.tsx,
    taskflow/src/routes/dashboard/QuickCreateInput.tsx,
    taskflow/src/routes/dashboard/QuickCreateInput.test.tsx
  </files>
  <read_first>
    taskflow/src/routes/dashboard/BulkActionBar.tsx (lines 1-30 imports, 8-22 props, 156-200 the parallelBatch transition loop);
    taskflow/src/routes/dashboard/QuickCreateInput.tsx (full file — handleSubmit at lines 41-65, prop surface 1-40, type picker state holding issueTypeId);
    taskflow/src/routes/dashboard/QuickCreateInput.test.tsx (existing — observe mock conventions);
    taskflow/src/services/jira/greenhopper/transitions.ts (Plan 01 output — getGhTransitions signature);
    .planning/phases/72-workflow-transitions-via-greenhopper/72-PATTERNS.md (§ BulkActionBar.tsx + § QuickCreateInput.tsx — line-by-line analog);
    .planning/phases/72-workflow-transitions-via-greenhopper/72-RESEARCH.md (Pitfall 6 — QuickCreateInput issueTypeId source)
  </read_first>
  <behavior>
    BulkActionBar:
    - For a bulk status change across keys `["PROJ-1", "PROJ-2"]` from projectId 10042 with issuetype id "3", the inner async handler calls `getGhTransitions(queryClient, jiraBaseUrl, jiraToken, 10042, "3")` per key — but the envelope is fetched ONCE (RQ ensureQueryData dedupe)
    - No mocked `fetchTransitions` call
    - If an issue in `keys` is missing from the `issues` prop, the handler throws a descriptive error containing the key

    QuickCreateInput:
    - Receives `projectId: number` and `issueTypeId: string` as new props (planner decision Q2: derive from the type-picker state already held in this component per RESEARCH §"call site #4" — pass through from parent SprintBoardTab where both are known per the column context)
    - On submit, after `createIssue(...)`, calls `getGhTransitions(queryClient, jiraBaseUrl, jiraToken, projectId, issueTypeId)` and applies the transition if a matching `to.id === statusId` is found
    - No mocked `fetchTransitions` call
    - `createIssue`'s signature is NOT widened — issueTypeId comes from the prop (planner decision Q2)
  </behavior>
  <action>
    BULK ACTION BAR:
    Replace the inner `fetchTransitions(jiraBaseUrl, jiraToken, key)` at line ~161 with `getGhTransitions(queryClient, jiraBaseUrl, jiraToken, projectId, issueTypeId)`. To get `projectId` and `issueTypeId` per key, look up the issue object from the component's `issues: JiraIssue[]` prop (already received per `BulkActionBar.tsx:8-22`):

    Inside the parallelBatch handler:
    - Find `const issue = issues.find((i) => i.key === key);`
    - If not found: `throw new Error(\`Issue ${key} not in selection\`);`
    - Derive `const projectId = Number(issue.fields.project.id);` and `const issueTypeId = issue.fields.issuetype.id;`
    - Call `const transitions = await getGhTransitions(queryClient, jiraBaseUrl, jiraToken, projectId, issueTypeId);`
    - Existing `transition = transitions.find((t) => t.to.name.toLowerCase() === targetStatus.toLowerCase())` line stays
    - Existing `postTransition(...)` call stays unchanged (D-08: POST stays on REST)

    Add `const queryClient = useQueryClient();` near the top of the component (after other hooks). Update imports: drop `fetchTransitions`; add `getGhTransitions`; add `useQueryClient` from `@tanstack/react-query` if not already imported. Keep `postTransition`.

    Update or create `taskflow/src/routes/dashboard/BulkActionBar.test.tsx` (RESEARCH Wave 0 flags it as "check; if absent → Wave 0"). Mock `@/services/jira`'s `getGhTransitions` and `postTransition`. Cases:
    (1) Bulk status change across 2 keys same project — assert `getGhTransitions` called twice (one per key) but the underlying fetcher mock called once if exercised via a real envelope, OR (simpler) assert `getGhTransitions` called with the expected `(qc, baseUrl, token, 10042, "3")` for each key.
    (2) Throws if an issue key in `keys` is missing from `issues` prop.
    (3) `fetchTransitions` is NOT in the mocked module — `vi.mocked(...)` access raises if attempted, OR assert via the import-graph that the deleted name is absent.

    QUICK CREATE INPUT:
    Per RESEARCH Pitfall 6 + planner decision Q2: add `projectId: number` and `issueTypeId: string` to `QuickCreateInputProps`. These come from the parent (`SprintBoardTab.tsx` already knows both from the column/type-picker context — verify by reading where `<QuickCreateInput` is rendered in `SprintBoardTab.tsx` and thread the props through). Do NOT widen `createIssue`'s return shape.

    Replace `fetchTransitions(jiraBaseUrl, jiraToken, newKey)` at line ~51 with `getGhTransitions(queryClient, jiraBaseUrl, jiraToken, projectId, issueTypeId)`. The existing `transitions.find((tr) => tr.to.id === statusId)` line stays. Add `const queryClient = useQueryClient();` near the top. Update imports: drop `fetchTransitions`; add `getGhTransitions, useQueryClient`. Keep `createIssue, postTransition`.

    Update parent renderer in `SprintBoardTab.tsx` to pass the new `projectId` and `issueTypeId` props to `<QuickCreateInput .../>`. Source the values from the same `localIssues[0].fields.project.id` / column-context issue type as Task 1 uses for the envelope warm.

    Update `taskflow/src/routes/dashboard/QuickCreateInput.test.tsx`: replace the `fetchTransitions` mock with `getGhTransitions`. Update fixtures to pass the new `projectId` + `issueTypeId` props. Add a test "uses GH cache for post-create transition" that asserts `getGhTransitions` was called with the new prop values, and `fetchTransitions` mock was NOT called.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/BulkActionBar.test.tsx src/routes/dashboard/QuickCreateInput.test.tsx src/routes/dashboard/SprintBoardTab.test.tsx && npx tsc --noEmit -p .</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (BulkActionBar): `grep -cE "fetchTransitions" taskflow/src/routes/dashboard/BulkActionBar.tsx` returns `0`
    - Source assertion (BulkActionBar uses getGhTransitions): `grep -c "getGhTransitions" taskflow/src/routes/dashboard/BulkActionBar.tsx` returns at least `1`
    - Source assertion (QuickCreateInput): `grep -cE "fetchTransitions" taskflow/src/routes/dashboard/QuickCreateInput.tsx` returns `0`
    - Source assertion (QuickCreateInput uses getGhTransitions): `grep -c "getGhTransitions" taskflow/src/routes/dashboard/QuickCreateInput.tsx` returns at least `1`
    - Source assertion (QuickCreateInput prop additions): `grep -cE "projectId\s*:\s*number" taskflow/src/routes/dashboard/QuickCreateInput.tsx` returns at least `1` and `grep -cE "issueTypeId\s*:\s*string" taskflow/src/routes/dashboard/QuickCreateInput.tsx` returns at least `1`
    - Source assertion (SprintBoardTab passes new props): `grep -cE "<QuickCreateInput[\s\S]*projectId" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns at least `1` (the JSX element passes projectId)
    - All four call-site test files pass + SprintBoardTab.test.tsx still passes; tsc clean
    - No call site retains the legacy `['transitions', issueKey]` queryKey: `grep -rE "queryKey:\s*\[\s*'transitions'\s*," taskflow/src/routes/dashboard/ | grep -v '^#' | wc -l` returns `0`
  </acceptance_criteria>
  <done>
    BulkActionBar + QuickCreateInput migrated to `getGhTransitions`; new props on QuickCreateInput threaded from SprintBoardTab; all four call sites route through the new cache; legacy `fetchTransitions` still exists in services/jira* but has zero consumers in `src/routes/dashboard/`; tests green; tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (click/drag/quick-create text) → call-site handler | Bounded: drag/click events trigger cache reads; QuickCreateInput passes user-typed summary text through to the existing `createIssue` (unchanged surface) |
| Call-site handler → `getGhTransitions` / `useGhTransitions` | Auth-bearing read; reuses the Stronghold PAT pipeline established in Plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-07 | Spoofing | Per-issue project/typeId derived from `issue.fields.project.id` / `issuetype.id` at call sites | accept | Values originate from the Jira server's own issue payload; no client-supplied override path. `Number(...)` coercion is total. |
| T-72-08 | Tampering | Toolbar refresh action invalidates queries from a click handler | accept | Action only invalidates RQ caches; no server mutation. Worst case: extra GET to `transitions.json` and `/rest/api/2/status` (idempotent reads). |
| T-72-09 | Information Disclosure | 401/403 propagation across the four call sites | mitigate | `useGhTransitions` / `getGhTransitions` propagate `ApiError` to existing `useQuery.error` / try-catch paths; existing components already render error states for those failure modes (audit during implementation; do not silently swallow). |
| T-72-10 | Denial of Service | Per-issue prefetch loop firing on every mount | mitigate | Loop deleted in Task 1; replaced by one envelope warm. Test asserts mocked fetcher called exactly once per project mount. |
| T-72-11 | Elevation of Privilege | Status-id spoof via fallback `{key: 'indeterminate'}` letting a user trigger a destructive transition | mitigate | Call sites match transition by `to.name`/`to.id`, not by `statusCategory.key`. Synthesized fallback's `to.name === 'Status N'` cannot match a real target like 'Done', so no privilege gain. |
| T-72-12 | Repudiation | Toolbar refresh action has no audit | accept | Read-only action; no audit needed beyond existing apiFetch operation labels ('Load Statuses', existing 'Load Transitions' from Phase 71). |
| T-72-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs zero packages (no toast lib — per RESEARCH Pitfall 5). Verified by `package.json` diff acceptance criterion. |
</threat_model>

<verification>
- Per-task automated test commands above all exit 0
- Plan-wide full suite quick run: `cd taskflow && npx vitest run` exits 0
- biome lint clean on all four migrated files: `cd taskflow && npx biome check src/routes/dashboard/StatusPopover.tsx src/routes/dashboard/SprintBoardTab.tsx src/routes/dashboard/BulkActionBar.tsx src/routes/dashboard/QuickCreateInput.tsx`
- `grep -rE "fetchTransitions" taskflow/src/routes/dashboard/ | grep -v '^#' | wc -l` returns `0` (legacy name fully gone from call sites; the symbol still exists in services/jira until Plan 03)
- `grep -c "Reload workflow transitions" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns at least `2`
- Manual UAT (deferred to phase verification, listed in 72-VALIDATION.md `Manual-Only Verifications`): open the sprint board, DevTools → Network → filter `transitions`, drag an issue between columns, confirm zero `/rest/api/2/issue/*/transitions` hits and exactly one `/rest/greenhopper/1.0/xboard/work/transitions.json?projectId=N` per fresh project (or zero on cache hit). Confirm "Reload workflow transitions" button triggers the inline label update.
</verification>

<success_criteria>
Maps to ROADMAP Phase 72 success criteria 2 and 3:

2. (full) Sprint-board drag-to-transition reads available transitions from the cache (no per-issue REST `/transitions` call in the network log) — delivered by SprintBoardTab + BulkActionBar migration.
3. (full) Issue-detail status change reads from the same cache — delivered by StatusPopover migration.
4. (toolbar half) Manual refresh action surfaced on the sprint-board toolbar.
</success_criteria>

<output>
Create `.planning/phases/72-workflow-transitions-via-greenhopper/72-02-SUMMARY.md` when done, summarising: each of the four call-site swaps (before/after pointers), the toolbar refresh action (anchor file/line + inline feedback decision), the QuickCreateInput prop additions and where they are threaded from, the prop change to StatusPopover (`issueKey` → `projectId + issueTypeId`), confirmation that `package.json` is unchanged, and confirmation that the legacy `fetchTransitions` definition is still present in `services/jira.ts` and `services/jira/transitions.ts` (Plan 03's job).
</output>
</output>
