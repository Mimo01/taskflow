---
phase: 72
plan: 03
type: execute
wave: 3
depends_on: ["72-02"]
files_modified:
  - taskflow/src/services/jira.ts
  - taskflow/src/services/jira.test.ts
  - taskflow/src/services/jira/transitions.ts
  - taskflow/src/services/jira/transitions.test.ts
autonomous: true
requirements: [GH-TRANS-02, GH-TRANS-03]
tags: [greenhopper, cutover, deletion, jira, transitions, gh-cut-01]

must_haves:
  truths:
    - "Legacy fetchTransitions GET is deleted from src/services/jira.ts (the ~40-line block at the former lines 670-711, including JSDoc) per D-08 and GH-CUT-01"
    - "Legacy fetchTransitions GET is deleted from src/services/jira/transitions.ts (the function defined at lines 12-45) per D-08"
    - "postTransition (the POST path) remains in src/services/jira/transitions.ts unchanged (D-08)"
    - "No production source file under src/ imports fetchTransitions — zero hits via the grep gate"
    - "Tests for the deleted GET are removed (the describe('fetchTransitions',...) block in src/services/jira/transitions.test.ts and the corresponding cases in src/services/jira.test.ts); the describe('postTransition',...) cases remain"
    - "Full vitest suite + biome lint + tsc --noEmit all green after the deletion"
  artifacts:
    - path: taskflow/src/services/jira.ts
      provides: "Re-export surface; legacy fetchTransitions definition removed"
      contains: "useGhTransitions"
    - path: taskflow/src/services/jira/transitions.ts
      provides: "postTransition only (the GET is gone)"
      contains: "postTransition"
  key_links:
    - from: taskflow/src/services/jira/transitions.ts
      to: "(no external symbol — fetchTransitions deleted)"
      via: "deletion"
      pattern: "postTransition"
---

<objective>
Execute the hard cutover specified by D-08 and GH-CUT-01: delete both legacy copies of fetchTransitions (the GET path) and their tests, leaving postTransition and the rest of the file intact. Plan 02 already migrated all four call sites; this plan removes the dead code and proves no production consumer remains.

Purpose: Phase 72's cutover policy is "hard cutover per surface — each phase replaces its REST path in place; the old REST board/backlog/detail/transitions paths are deleted as their replacements ship" (ROADMAP). After this plan, the codebase has zero references to the legacy per-issue transitions GET, the ['transitions', issueKey] queryKey, and the supporting test mocks. The full test suite is green and biome reports no regressions.

Output:
- services/jira.ts — legacy fetchTransitions function (current ~lines 670-711) deleted; JiraTransition interface (~lines 183-191) RETAINED (consumed by the new GH adapter)
- services/jira/transitions.ts — fetchTransitions (current ~lines 12-45) deleted; postTransition retained; top-level JSDoc updated to reflect POST-only; unused JiraTransition type import removed if no longer needed
- services/jira.test.ts — fetchTransitions test cases removed (RESEARCH §"Wave 0 Gaps" notes lines ~213-225)
- services/jira/transitions.test.ts — describe('fetchTransitions',...) block removed; describe('postTransition',...) retained
- All tests green, tsc --noEmit clean, biome lint clean
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-workflow-transitions-via-greenhopper/72-CONTEXT.md
@.planning/phases/72-workflow-transitions-via-greenhopper/72-RESEARCH.md
@.planning/phases/72-workflow-transitions-via-greenhopper/72-PATTERNS.md
@.planning/phases/72-workflow-transitions-via-greenhopper/72-01-SUMMARY.md
@.planning/phases/72-workflow-transitions-via-greenhopper/72-02-SUMMARY.md

@taskflow/src/services/jira.ts
@taskflow/src/services/jira/transitions.ts
@taskflow/src/services/jira/transitions.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy fetchTransitions (both copies) plus their tests; verify full suite green</name>
  <files>
    taskflow/src/services/jira.ts,
    taskflow/src/services/jira.test.ts,
    taskflow/src/services/jira/transitions.ts,
    taskflow/src/services/jira/transitions.test.ts
  </files>
  <read_first>
    taskflow/src/services/jira.ts (locate fetchTransitions — RESEARCH says lines 670-711; verify current line range via grep);
    taskflow/src/services/jira/transitions.ts (full file — verify current lines for fetchTransitions vs postTransition and the JiraTransition import at the top);
    taskflow/src/services/jira/transitions.test.ts (locate the fetchTransitions describe block);
    taskflow/src/services/jira.test.ts (locate the legacy fetchTransitions cases per RESEARCH §"Wave 0 Gaps");
    .planning/phases/72-workflow-transitions-via-greenhopper/72-PATTERNS.md (§ src/services/jira.ts + § src/services/jira/transitions.ts — deletion targets and postTransition keep-region)
  </read_first>
  <action>
    Step 1 — Verify Plan 02 fully landed before deleting:
    Run `grep -rn 'fetchTransitions' taskflow/src/routes/dashboard/ --include='*.ts' --include='*.tsx' | grep -v '^#' | wc -l`. The result MUST be 0. If non-zero, stop and report — Plan 02 left a consumer behind and Plan 03 must not proceed until that is fixed.

    Step 2 — Delete the legacy fetchTransitions definition in services/jira.ts:
    Run `grep -n 'export async function fetchTransitions' taskflow/src/services/jira.ts` to confirm the current line of the function. Read the surrounding region (typically the JSDoc starts a handful of lines above the function header) and delete the entire block: the JSDoc comment, the function signature, the body, and the closing brace. Approximate range per RESEARCH/PATTERNS: lines 670-711. Do NOT touch the JiraTransition interface at lines ~183-191 — it is the adapter return type and is consumed by Plan 01/02 code.

    Step 3 — Delete the legacy fetchTransitions in services/jira/transitions.ts:
    Delete the function block (PATTERNS lines 12-45) including its JSDoc. Verify postTransition (PATTERNS lines 50-83) is untouched. Update the file's top-level JSDoc/comment so it reads "Jira issue transition POST operation" — the phrase "GET" must no longer appear referring to this file's role. After the delete, run `grep -c 'JiraTransition' taskflow/src/services/jira/transitions.ts`. If the result is 0, also remove the now-unused `import type { JiraTransition } from './types';` line (per PATTERNS line 7). If the result is greater than 0 (e.g., used in postTransition's typing), leave the import in place.

    Step 4 — Remove the legacy tests in services/jira.test.ts:
    Run `grep -n 'fetchTransitions' taskflow/src/services/jira.test.ts` to locate the cases (RESEARCH notes lines ~213-225). Delete either the entire describe('fetchTransitions',...) block or the individual it(...) cases that reference fetchTransitions if they live at the top level. Do not touch unrelated tests. If a `vi.mock(...)` or import becomes unused after the delete, prune it.

    Step 5 — Remove the legacy describe block in services/jira/transitions.test.ts:
    Delete the describe('fetchTransitions',...) block. Leave the describe('postTransition',...) block intact along with any shared imports/beforeEach it needs. If a module-level import (e.g., `import type { JiraTransition }`) becomes unused after the delete, remove it too.

    Step 6 — Global call-site grep gate:
    Run `grep -rn 'fetchTransitions' taskflow/src --include='*.ts' --include='*.tsx' | grep -v '^#'`. The expected output is empty (zero lines). This is the hard contract for D-08 + GH-CUT-01.

    Step 7 — Run the full verification stack:
    `cd taskflow && npx tsc --noEmit -p . && npx vitest run && npx biome check src/services/jira.ts src/services/jira/transitions.ts src/services/jira.test.ts src/services/jira/transitions.test.ts`

    All three (tsc, vitest, biome) must exit 0. If any test that was not in the deletion set now fails, investigate — likely a mock leak from the deleted blocks or a stale import.
  </action>
  <verify>
    <automated>cd taskflow && test "$(grep -rn 'fetchTransitions' src --include='*.ts' --include='*.tsx' | grep -v '^#' | wc -l | tr -d ' ')" = "0" && npx tsc --noEmit -p . && npx vitest run && npx biome check src/services/jira.ts src/services/jira/transitions.ts src/services/jira.test.ts src/services/jira/transitions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (grep gate, the hard D-08 contract): `cd taskflow && grep -rn 'fetchTransitions' src --include='*.ts' --include='*.tsx' | grep -v '^#' | wc -l` returns `0`
    - Source assertion (services/jira.ts legacy GET gone): `grep -c 'export async function fetchTransitions' taskflow/src/services/jira.ts` returns `0`
    - Source assertion (services/jira/transitions.ts legacy GET gone): `grep -c 'export async function fetchTransitions' taskflow/src/services/jira/transitions.ts` returns `0`
    - Source assertion (postTransition retained): `grep -c 'export async function postTransition' taskflow/src/services/jira/transitions.ts` returns `1`
    - Source assertion (JiraTransition interface still defined for adapter consumers): `grep -c 'export interface JiraTransition' taskflow/src/services/jira.ts` returns `1`
    - Source assertion (Plan 01 re-exports still present): `grep -c 'useGhTransitions' taskflow/src/services/jira.ts` returns at least `1`, `grep -c 'getGhTransitions' taskflow/src/services/jira.ts` returns at least `1`, `grep -c 'invalidateGhTransitions' taskflow/src/services/jira.ts` returns at least `1`, `grep -c 'fetchAllJiraStatuses' taskflow/src/services/jira.ts` returns at least `1`
    - tsc, vitest, biome all exit 0 in the combined verify command above
  </acceptance_criteria>
  <done>
    Both copies of the legacy fetchTransitions GET and their tests are deleted; the grep gate is at zero hits; postTransition and JiraTransition retained; full suite + lint + typecheck green; D-08 and GH-CUT-01 satisfied for the transitions surface.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tree integrity → CI / runtime | A deletion plan introduces no new trust boundaries, but a botched delete can leave dead imports that crash at build/test time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-13 | Tampering | Accidental deletion of postTransition or the JiraTransition interface | mitigate | Acceptance criteria explicitly assert `postTransition` is retained (grep count = 1) and `JiraTransition` interface is retained (grep count = 1). Plan reviewer can verify before merging. |
| T-72-14 | Denial of Service | Plan 02 not fully completed (a call site still imports fetchTransitions) | mitigate | Step 1 of the action runs the call-site grep gate before any deletion; the plan stops and reports if a consumer remains. |
| T-72-15 | Information Disclosure | Stale mock or import referencing fetchTransitions surfaces in a future grep audit | mitigate | The hard grep gate (acceptance criteria #1) covers the entire `src/` tree across both `.ts` and `.tsx` files. |
| T-72-16 | Repudiation | Cutover commit author/timestamp | accept | Standard git history; no additional audit required. |
| T-72-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs zero packages. Pure-deletion plan. |
</threat_model>

<verification>
- Combined automated gate in Task 1's verify (grep + tsc + vitest + biome) exits 0
- Manual smoke (deferred to phase-level verification per 72-VALIDATION.md Manual-Only Verifications): open the sprint board, drag an issue between columns, confirm zero `/rest/api/2/issue/*/transitions` requests in the network log (the GET path is physically gone from the build, so this becomes a regression-proof gate)
- Plan 02's behaviors remain green: the four call sites still function via `useGhTransitions` / `getGhTransitions`; the toolbar action still invalidates and refetches
</verification>

<success_criteria>
Maps to ROADMAP Phase 72 success criterion 4 (the deletion half) and the milestone-level GH-CUT-01:

4. (deletion half) "the old per-issue /transitions REST path is deleted" — delivered by this plan.
GH-CUT-01: "Hard cutover per surface — each phase replaces its REST path in place. No coexistence flag; old REST paths for board/backlog/detail/transitions are deleted as their replacements ship." — satisfied for the transitions surface.
</success_criteria>

<output>
Create `.planning/phases/72-workflow-transitions-via-greenhopper/72-03-SUMMARY.md` when done, summarising: exact line ranges deleted in `services/jira.ts` and `services/jira/transitions.ts`, the JSDoc comment update in `services/jira/transitions.ts`, the test blocks removed, the final grep-gate result (must be 0 hits across `src/`), and the full-suite/biome/tsc outcome. Note that `postTransition` and the `JiraTransition` interface are preserved.
</output>
