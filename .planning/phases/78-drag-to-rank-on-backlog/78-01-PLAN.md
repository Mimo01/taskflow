---
phase: 78-drag-to-rank-on-backlog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/test/package-deps.guard.test.ts
  - taskflow/package.json
  - taskflow/package-lock.json
  - taskflow/src/components/ui/confirm-sprint-move-dialog.tsx
  - taskflow/src/services/jira/rank-api.test.ts
  - taskflow/src/routes/dashboard/__tests__/BacklogPage.rank.test.ts
autonomous: true
requirements: [RANK-03, RANK-04, RANK-05]
must_haves:
  truths:
    - "@dnd-kit/core, /sortable, /modifiers, /utilities are installed and importable"
    - "The @dnd-kit absence guard no longer fails the test suite"
    - "ConfirmSprintMoveDialog renders a caller-supplied cancel label, defaulting to 'Cancel'"
    - "Wave-0 test scaffolds exist (rank-api.test.ts, BacklogPage.rank.test.ts) and define the RANK-03/04/05 expectations"
  artifacts:
    - path: "taskflow/src/components/ui/confirm-sprint-move-dialog.tsx"
      provides: "cancelLabel prop with 'Cancel' default"
      contains: "cancelLabel"
    - path: "taskflow/src/services/jira/rank-api.test.ts"
      provides: "Unit test scaffold asserting integer rankCustomFieldId in PUT body"
      contains: "rankCustomFieldId"
    - path: "taskflow/src/routes/dashboard/__tests__/BacklogPage.rank.test.ts"
      provides: "Optimistic rollback / cancelQueries / error-banner test scaffold"
      contains: "cancelQueries"
  key_links:
    - from: "taskflow/package.json"
      to: "@dnd-kit/core"
      via: "dependencies entry"
      pattern: "@dnd-kit/core"
---

<objective>
Lay the Phase 78 foundation: remove the `@dnd-kit` absence guard (D-12), install the four `@dnd-kit` packages, extend `ConfirmSprintMoveDialog` with a `cancelLabel` prop (Pitfall 4 / UI-SPEC "Keep Position"), and create the two Wave-0 test scaffolds (RANK-03 service test, RANK-04/RANK-05 BacklogPage mutation test) so downstream plans implement against an existing test contract.

Purpose: The drag-to-rank feature cannot import `@dnd-kit` while the absence guard test forbids it, and downstream service/UI plans need a failing test to drive (Nyquist). This plan establishes the dependency + test surface without touching `rank.ts` or `BacklogPage.tsx`, so it runs in parallel with the rank.ts fix (Plan 02).
Output: Installed packages, a `cancelLabel`-capable dialog, two new test files defining RANK-03/04/05 behavior.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-drag-to-rank-on-backlog/78-CONTEXT.md
@.planning/phases/78-drag-to-rank-on-backlog/78-RESEARCH.md
@.planning/phases/78-drag-to-rank-on-backlog/78-PATTERNS.md
@.planning/phases/78-drag-to-rank-on-backlog/78-UI-SPEC.md
@.planning/phases/78-drag-to-rank-on-backlog/78-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove @dnd-kit absence guard and install the four packages (D-12)</name>
  <files>taskflow/src/test/package-deps.guard.test.ts, taskflow/package.json, taskflow/package-lock.json</files>
  <read_first>
    - taskflow/src/test/package-deps.guard.test.ts (current guard — the react-grid-layout describe must stay; the @dnd-kit describe must go)
    - .planning/phases/78-drag-to-rank-on-backlog/78-RESEARCH.md § Standard Stack + Package Legitimacy Audit (verified versions, all four npm-VERIFIED, no postinstall)
    - .planning/phases/78-drag-to-rank-on-backlog/78-PATTERNS.md § package-deps.guard.test.ts (remove describe block lines 52-82; file ends at the react-grid-layout block close)
  </read_first>
  <action>
    Delete the entire second `describe('package.json — @dnd-kit absence guard (Phase 67 / SETUI-02)', ...)` block from `taskflow/src/test/package-deps.guard.test.ts` (the block iterating `DND_KIT_PACKAGES`). Leave the first `react-grid-layout absence guard` describe block untouched. The file must end with the closing `});` of the react-grid-layout describe.
    Then install the four packages from the `taskflow/` directory at the RESEARCH-verified versions: `@dnd-kit/core@6.3.1`, `@dnd-kit/sortable@10.0.0`, `@dnd-kit/modifiers@9.0.0`, `@dnd-kit/utilities@3.2.2` (run `npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/modifiers @dnd-kit/utilities` inside `taskflow/`). These four are npm-VERIFIED in the Package Legitimacy Audit (Approved disposition, no postinstall scripts) — no legitimacy checkpoint required. Remove the guard BEFORE installing so the suite never goes red on the install.
  </action>
  <verify>
    <automated>cd taskflow && npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/modifiers @dnd-kit/utilities && node -e "require.resolve('@dnd-kit/core'); require.resolve('@dnd-kit/sortable'); require.resolve('@dnd-kit/modifiers'); require.resolve('@dnd-kit/utilities'); console.log('all resolved')"</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/test/package-deps.guard.test.ts` contains NO occurrence of the string `@dnd-kit absence guard` (the describe block is gone): `grep -c "@dnd-kit absence guard" taskflow/src/test/package-deps.guard.test.ts` returns `0`.
    - The react-grid-layout absence guard describe block is still present: `grep -c "react-grid-layout absence guard" taskflow/src/test/package-deps.guard.test.ts` returns `1`.
    - `taskflow/package.json` `dependencies` includes all four `@dnd-kit/*` keys: `cd taskflow && node -e "const p=require('./package.json').dependencies; ['@dnd-kit/core','@dnd-kit/sortable','@dnd-kit/modifiers','@dnd-kit/utilities'].forEach(k=>{if(!p[k])throw new Error('missing '+k)}); console.log('ok')"` prints `ok`.
    - `npm test -- --run src/test/package-deps.guard.test.ts` passes (only the react-grid-layout cases remain).
  </acceptance_criteria>
  <done>The @dnd-kit guard block is deleted, all four packages are in package.json dependencies and importable, and the guard test passes with only react-grid-layout assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Add cancelLabel prop to ConfirmSprintMoveDialog (Pitfall 4 / UI-SPEC)</name>
  <files>taskflow/src/components/ui/confirm-sprint-move-dialog.tsx</files>
  <read_first>
    - taskflow/src/components/ui/confirm-sprint-move-dialog.tsx (full current component — interface lines 12-20, DialogClose render line 44)
    - .planning/phases/78-drag-to-rank-on-backlog/78-PATTERNS.md § confirm-sprint-move-dialog.tsx (three concrete changes)
    - .planning/phases/78-drag-to-rank-on-backlog/78-UI-SPEC.md § Copywriting Contract (dialog cancel button = "Keep Position")
    - taskflow/src/routes/dashboard/BacklogPage.tsx lines 886-905 (the two existing ConfirmSprintMoveDialog usages — they must keep rendering "Cancel" since they omit the new prop)
  </read_first>
  <action>
    Add an optional `cancelLabel?: string` field to `ConfirmSprintMoveDialogProps` (after `isPending?`). Destructure it in the component signature with a default of `'Cancel'`. Replace the hardcoded `Cancel` text inside the `DialogClose render={<Button variant="outline" />}` element with `{cancelLabel}`. Do NOT change anything else — the two existing usages in BacklogPage omit the prop and therefore continue rendering "Cancel"; the Phase 78 drag-triggered usage (added in Plan 04) will pass `cancelLabel="Keep Position"`.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "confirm-sprint-move-dialog" || echo "no type errors in dialog"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cancelLabel" taskflow/src/components/ui/confirm-sprint-move-dialog.tsx` returns at least `3` (interface field, destructure default, render usage).
    - The default is `'Cancel'`: `grep -c "cancelLabel = 'Cancel'" taskflow/src/components/ui/confirm-sprint-move-dialog.tsx` returns `1`.
    - The literal string `>Cancel<` is no longer hardcoded inside `DialogClose` (replaced by `{cancelLabel}`): `grep -c "{cancelLabel}" taskflow/src/components/ui/confirm-sprint-move-dialog.tsx` returns `1`.
    - `npx tsc --noEmit` reports no new errors in this file.
  </acceptance_criteria>
  <done>ConfirmSprintMoveDialog accepts `cancelLabel`, defaults to "Cancel", existing callers unaffected, type-checks clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Wave-0 test scaffolds for rank-api and BacklogPage rank mutation (RANK-03/04/05)</name>
  <files>taskflow/src/services/jira/rank-api.test.ts, taskflow/src/routes/dashboard/__tests__/BacklogPage.rank.test.ts</files>
  <read_first>
    - taskflow/src/services/jira/sprints.test.ts (analog: vi.mock('../../lib/apiFetch') + apiFetch assertion shape)
    - taskflow/src/routes/dashboard/__tests__/BacklogPage.network.test.tsx (analog: QueryClient + vi.mock setup for the BacklogPage test)
    - .planning/phases/78-drag-to-rank-on-backlog/78-PATTERNS.md § rank-api.test.ts and § BacklogPage.rank.test.ts (full scaffold bodies)
    - .planning/phases/78-drag-to-rank-on-backlog/78-VALIDATION.md § Wave 0 Requirements and Per-Task Verification Map
  </read_first>
  <behavior>
    - rank-api.test.ts: `rankIssueApi` calls `apiFetch('jira', `${BASE}/rest/agile/1.0/issue/rank`, { method:'PUT', body:... }, 'Rank Issue')`; body is `{ issues:[key], rankCustomFieldId, ...position }`; `rankCustomFieldId` is serialized as a JSON number (typeof === 'number', value 10105 from fixture), NEVER a string; 401 → throws ApiError; 500 → throws generic Error with status.
    - BacklogPage.rank.test.ts: RANK-03 the rank mutation passes the fixture integer `rankCustomFieldId` (10105) read from cached GhBacklogResponse, not a hardcoded constant; RANK-04 a rejected `rankIssueApi` rolls the local order back to the previous order and sets the inline `rankError` banner text "Couldn't save new order — reverted"; RANK-05 `queryClient.cancelQueries({ queryKey: ['gh-backlog', boardId] })` is invoked in `onMutate`.
  </behavior>
  <action>
    Create `taskflow/src/services/jira/rank-api.test.ts` mirroring `sprints.test.ts`: mock `../../lib/apiFetch`, import the (not-yet-existing) `rankIssueApi` from `./rank-api`, and write the cases from PATTERNS.md (correct PUT body incl. integer `rankCustomFieldId` fixture value `10105`; `rankBeforeIssue` top-of-list case; 401 → ApiError; 500 → Error; explicit `typeof body.rankCustomFieldId === 'number'` assertion). These tests MUST fail at creation because `./rank-api` does not yet exist — that is the Wave-0 RED state Plan 03 turns GREEN.
    Create `taskflow/src/routes/dashboard/__tests__/BacklogPage.rank.test.ts` mirroring `BacklogPage.network.test.tsx`'s mock + QueryClient setup, with the three behavior cases above. Mock `../../../services/jira/rank-api` so `rankIssueApi` is a `vi.fn()`. Use `BOARD_ID` and `RANK_FIELD_ID = 10105` constants and the query key `['gh-backlog', BOARD_ID]`. These tests will fail until Plan 04 wires the mutation — Wave-0 RED. Write the scaffolds so they reference the real implementation surface (mutation trigger, `rankError` state, `cancelQueries`) rather than being placeholder `it.todo`s; where the exact trigger requires Plan 04's wiring, drive through the rendered component or the exported handler as the analog test does, but keep the assertions concrete (see PATTERNS.md bodies).
    Do NOT create `rank-api.ts` or modify BacklogPage here — those are Plan 03 and Plan 04. This task only establishes the failing contract.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --run src/services/jira/rank-api.test.ts 2>&1 | grep -E "Cannot find module|failed|FAIL|No test files" || echo "scaffold present (expected RED until Plan 03)"</automated>
  </verify>
  <acceptance_criteria>
    - File `taskflow/src/services/jira/rank-api.test.ts` exists and imports `rankIssueApi` from `./rank-api`: `grep -c "from './rank-api'" taskflow/src/services/jira/rank-api.test.ts` returns `1`.
    - rank-api.test.ts contains the integer-type assertion: `grep -c "typeof body.rankCustomFieldId" taskflow/src/services/jira/rank-api.test.ts` returns at least `1`, and the fixture value 10105 is referenced.
    - File `taskflow/src/routes/dashboard/__tests__/BacklogPage.rank.test.ts` exists and asserts `cancelQueries` with the gh-backlog key: `grep -c "cancelQueries" taskflow/src/routes/dashboard/__tests__/BacklogPage.rank.test.ts` returns at least `1`.
    - BacklogPage.rank.test.ts references the rollback banner copy: `grep -c "Couldn't save new order" taskflow/src/routes/dashboard/__tests__/BacklogPage.rank.test.ts` returns at least `1`.
    - Both files are syntactically valid (vitest collects them): running `npm test -- --run src/services/jira/rank-api.test.ts` reports a module-not-found / failing-assertion RED state, not a parse/collection error.
  </acceptance_criteria>
  <done>Both Wave-0 test files exist with concrete RANK-03/04/05 assertions and are in the expected RED state (rank-api module absent, BacklogPage mutation not yet wired).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → npm registry | Package install pulls four `@dnd-kit/*` packages over the network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-SC | Tampering | npm install of `@dnd-kit/core`, `/sortable`, `/modifiers`, `/utilities` | accept | All four npm-VERIFIED in RESEARCH § Package Legitimacy Audit (Approved, ~4yr-old, github.com/clauderic/dnd-kit, no postinstall scripts). slopcheck's SLOP verdict was a false positive from running against PyPI not npm. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no blocking-human legitimacy checkpoint required. Pin to RESEARCH-verified versions. |
| T-78-01 | Tampering | confirm-sprint-move-dialog.tsx cancelLabel | accept | `cancelLabel` is a presentational string with a hardcoded default; no user/network data flows into it; no injection surface. |

No high-severity threats in this plan. No new secrets, tokens, or network write paths are introduced — this plan only installs vetted packages, edits a presentational prop, and creates test files.
</threat_model>

<verification>
- `npm test -- --run src/test/package-deps.guard.test.ts` passes (react-grid-layout guard only).
- All four `@dnd-kit/*` packages resolve via `require.resolve`.
- `npx tsc --noEmit` reports no new errors in `confirm-sprint-move-dialog.tsx`.
- `rank-api.test.ts` and `BacklogPage.rank.test.ts` exist and are in the expected RED state.
</verification>

<success_criteria>
- @dnd-kit absence guard removed; four packages installed and importable.
- ConfirmSprintMoveDialog accepts `cancelLabel` (default "Cancel"); existing usages unchanged.
- Wave-0 test scaffolds define RANK-03/04/05 expectations and fail for the right reasons.
</success_criteria>

<output>
Create `.planning/phases/78-drag-to-rank-on-backlog/78-01-SUMMARY.md` when done.
</output>
