---
phase: 78-drag-to-rank-on-backlog
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/jira/rank.ts
  - taskflow/src/services/jira/rank.test.ts
autonomous: true
requirements: [RANK-02]
must_haves:
  truths:
    - "rankIssue(before, after) returns a rank strictly between before and after for cross-bucket neighbours (CR-01)"
    - "rankIssue preserves precision for value strings longer than 11 base-36 chars (CR-02)"
    - "rank.test.ts asserts rankLt(before, result) && rankLt(result, after) on every applicable case using a bucket-aware rankLt"
    - "The ⚠️ KNOWN-BROKEN header is removed from rank.ts"
  artifacts:
    - path: "taskflow/src/services/jira/rank.ts"
      provides: "Fixed LexoRank midpoint (cross-bucket + BigInt digit-by-digit base-36)"
      contains: "parseBase36"
    - path: "taskflow/src/services/jira/rank.test.ts"
      provides: "Strengthened strict-ordering tests incl. E10/E11/E12 and bucket-aware rankLt"
      contains: "E12"
  key_links:
    - from: "taskflow/src/services/jira/rank.ts"
      to: "rankIssue cross-bucket branch"
      via: "beforeBucket !== afterBucket guard"
      pattern: "beforeBucket !== afterBucket"
---

<objective>
Fix the known-broken `rank.ts` LexoRank calculator (D-10): correct cross-bucket midpoint handling (CR-01) and replace `parseInt`-based base-36 conversion with arbitrary-precision digit-by-digit BigInt math (CR-02). Strengthen `rank.test.ts` so every case asserts strict ordering with a bucket-aware `rankLt`, and add E10/E11/E12. Remove the ⚠️ KNOWN-BROKEN header.

Purpose: `rankIssue` is a hard prerequisite — every drag mutation computes a rank between two neighbours via this function. The current implementation returns ranks that sort *before* `before` across buckets and silently collapses long ranks to the same value. This must be correct and provably so before any mutation consumes it. Runs in parallel with Plan 01 (no shared files).
Output: A correct, fully-tested `rankIssue` with no KNOWN-BROKEN header.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-drag-to-rank-on-backlog/78-CONTEXT.md
@.planning/phases/78-drag-to-rank-on-backlog/78-RESEARCH.md
@.planning/phases/78-drag-to-rank-on-backlog/78-PATTERNS.md
@.planning/phases/78-drag-to-rank-on-backlog/78-VALIDATION.md
@.planning/todos/pending/rank-ts-blockers-phase78-prereq.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strengthen rank.test.ts (bucket-aware rankLt, strict ordering everywhere, E7 replacement, E10/E11/E12)</name>
  <files>taskflow/src/services/jira/rank.test.ts</files>
  <read_first>
    - taskflow/src/services/jira/rank.test.ts (current tests — value-only rankLt lines 13-21; E7 prefix-only assertion lines 66-71)
    - .planning/phases/78-drag-to-rank-on-backlog/78-PATTERNS.md § rank.test.ts (fixed rankLt, E7 replacement, E10/E11/E12 bodies)
    - .planning/phases/78-drag-to-rank-on-backlog/78-RESEARCH.md § Pattern 3 + § Strengthening rank.test.ts (D-10)
    - .planning/todos/pending/rank-ts-blockers-phase78-prereq.md (why E7's prefix check let CI stay green)
  </read_first>
  <behavior>
    - Bucket-aware `rankLt(a,b)`: parse bucket (int, default 0) and value portion; if buckets differ compare buckets numerically, else compare value strings lexically. So `rankLt('0|zzzzzzg:', '1|000000:')` === true.
    - E7 replaced: for before=`'0|zzzzzz:'`, after=`'1|000000:'`, assert BOTH `rankLt(before, result)` and `rankLt(result, after)` (not a `startsWith('0|')` check).
    - E10 (CR-02): before=`'0|aaaaaaaaaaaa:'`, after=`'0|zzzzzzzzzzzz:'` (12 chars) → strictly between.
    - E11 (CR-01): before=`'0|zzzzzz:'`, after=`'1|000000:'` → strictly between.
    - E12 (repeated midpoint): insert 5 successive midpoints between `'0|aaaaaa:'` and `'0|bbbbbb:'`; all inserted results strictly increasing.
    - Existing E1–E9 keep asserting strict ordering with the new `rankLt` where they already do (E1,E2,E4,E5,E6,E8,E9).
  </behavior>
  <action>
    Replace the value-only `rankLt` helper in `rank.test.ts` with the bucket-aware version from PATTERNS.md (parse `{ bucket: parseInt(bkt,10), val }`; compare bucket first, then val). Replace the E7 test (currently `expect(result.startsWith('0|')).toBe(true)`) with the strict-ordering assertion `rankLt(before, result) && rankLt(result, after)` for before=`'0|zzzzzz:'` / after=`'1|000000:'`. Append E10, E11, E12 exactly as specified in the behavior block. Do NOT modify `rank.ts` in this task — these tests should go RED against the current broken implementation (E7/E11 cross-bucket and E10 precision will fail), proving the bugs, then turn GREEN after Task 2.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --run src/services/jira/rank.test.ts 2>&1 | grep -E "E7|E10|E11|fail|FAIL" || echo "tests present"</automated>
  </verify>
  <acceptance_criteria>
    - `rankLt` is bucket-aware: `grep -c "ra.bucket" taskflow/src/services/jira/rank.test.ts` returns at least `1` and `grep -c "ra.bucket < rb.bucket" taskflow/src/services/jira/rank.test.ts` returns `1`.
    - E7 no longer uses the prefix check: `grep -c "startsWith('0|')" taskflow/src/services/jira/rank.test.ts` returns `0`.
    - E10, E11, E12 exist: `grep -c "E10\|E11\|E12" taskflow/src/services/jira/rank.test.ts` returns at least `3`.
    - Against the UNFIXED `rank.ts`, the suite is RED on the cross-bucket / precision cases (this is the expected pre-Task-2 state): `npm test -- --run src/services/jira/rank.test.ts` reports failing tests.
  </acceptance_criteria>
  <done>rank.test.ts uses a bucket-aware rankLt, asserts strict ordering on every applicable case, includes E10/E11/E12, and is RED against the current broken rank.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix rank.ts CR-01 (cross-bucket) + CR-02 (BigInt base-36) and remove KNOWN-BROKEN header</name>
  <files>taskflow/src/services/jira/rank.ts</files>
  <read_first>
    - taskflow/src/services/jira/rank.ts (current implementation — rankIssue lines 33-37, midpoint lines 57-79, KNOWN-BROKEN header lines 7-17)
    - .planning/phases/78-drag-to-rank-on-backlog/78-PATTERNS.md § rank.ts (concrete CR-01 branch + parseBase36/toBase36/midpoint replacements)
    - .planning/phases/78-drag-to-rank-on-backlog/78-RESEARCH.md § Pattern 1, Pattern 2, § Full rank.ts Fix (verified via node -e)
  </read_first>
  <action>
    Edit `rankIssue` to add a cross-bucket branch (CR-01): when `before !== null && after !== null` and `bucket(before) !== bucket(after)`, return `${beforeBucket}|${beforeVal}${midChar}:` where `midChar = ALPHABET[Math.floor(ALPHABET.length / 2)]` (the alphabet-midpoint character 'i') — this stays in the lower bucket and extends `before`'s value, producing a rank strictly > before (same bucket, longer value) and < after (higher bucket). Keep the existing same-bucket path (`targetBucket = beforeBucket || afterBucket || '0'`; `effectiveAfterVal = afterVal || pad(beforeVal, beforeVal.length + 1)`; `midpoint(beforeVal, effectiveAfterVal)`).
    Replace the `midpoint` function's `BigInt(parseInt(s, 36))` conversions (CR-02) with digit-by-digit BigInt parsing: add `parseBase36(s)` (`result = result * 36n + BigInt(ALPHABET.indexOf(c))` per char) and `toBase36(n, minLen)` (repeated `% 36n` / `/ 36n`, left-pad to minLen). `midpoint` computes `(parseBase36(pa) + parseBase36(pb)) / 2n`, renders via `toBase36(mid, len)`, and keeps the existing adjacent-gap guard (if `result === pa`, append `ALPHABET[Math.floor(ALPHABET.length / 2)]`).
    Remove the entire `⚠️ KNOWN-BROKEN` JSDoc block (the bullet list referencing CR-01/CR-02 and "MUST fix") from the file header. Leave the remaining accurate doc comments.
    Do not change the public signature `rankIssue(before: string | null, after: string | null): string` or the exported surface.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --run src/services/jira/rank.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/services/jira/rank.test.ts` passes ALL cases (E1–E12) GREEN.
    - Cross-bucket branch present: `grep -c "beforeBucket !== afterBucket" taskflow/src/services/jira/rank.ts` returns `1`.
    - Digit-by-digit parse present and no `parseInt(...,36)` remains: `grep -c "parseBase36" taskflow/src/services/jira/rank.ts` returns at least `2` AND `grep -c "parseInt(pa\|parseInt(pb\|parseInt(.*36)" taskflow/src/services/jira/rank.ts` returns `0`.
    - KNOWN-BROKEN header removed: `grep -c "KNOWN-BROKEN" taskflow/src/services/jira/rank.ts` returns `0`.
    - Behavioral spot-check passes: `cd taskflow && node -e "const {rankIssue}=require('./src/services/jira/rank.ts'); " 2>/dev/null || npm test -- --run src/services/jira/rank.test.ts` (the vitest run is the authoritative check; E11 proves `rankIssue('0|zzzzzz:','1|000000:')` is strictly between).
  </acceptance_criteria>
  <done>rank.ts handles cross-bucket neighbours correctly, uses arbitrary-precision BigInt base-36 math, carries no KNOWN-BROKEN header, and rank.test.ts is fully GREEN (E1–E12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | `rank.ts` is a pure function with no I/O, no network, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-02 | Tampering | rankIssue output consumed by later rank PUT | mitigate | Strict-ordering unit tests (E1–E12) prove the produced rank sorts strictly between neighbours; a malformed rank cannot reach the API because the value is computed from server-sourced neighbour ranks, not user input. |

No high-severity threats. `rank.ts` is a side-effect-free pure function; no secrets, no network, no user-controlled input (neighbour ranks come from the cached server response). Correctness — not a security boundary — is the risk this plan retires.
</threat_model>

<verification>
- `npm test -- --run src/services/jira/rank.test.ts` is fully GREEN (E1–E12).
- No `parseInt(..., 36)` remains in rank.ts; `parseBase36` present.
- No `KNOWN-BROKEN` text remains in rank.ts.
</verification>

<success_criteria>
- Cross-bucket midpoint (CR-01) and arbitrary-precision base-36 (CR-02) fixed.
- Every test asserts strict ordering with a bucket-aware rankLt; E10/E11/E12 added.
- KNOWN-BROKEN header removed; rank.ts ready to be consumed by the rank API and mutation.
</success_criteria>

<output>
Create `.planning/phases/78-drag-to-rank-on-backlog/78-02-SUMMARY.md` when done.
</output>
