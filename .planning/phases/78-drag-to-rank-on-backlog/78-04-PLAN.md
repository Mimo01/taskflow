---
phase: 78-drag-to-rank-on-backlog
plan: 04
type: execute
wave: 3
depends_on: [78-01, 78-02, 78-03]
autonomous: false
files_modified:
  - taskflow/src/routes/dashboard/BacklogRow.tsx
  - taskflow/src/routes/dashboard/BacklogPage.tsx
  - taskflow/src/routes/dashboard/__tests__/BacklogPage.rank.test.ts
requirements: [RANK-01, RANK-02, RANK-04, RANK-05]
user_setup: []
must_haves:
  truths:
    - "Each Backlog section (active sprint, every future sprint, unassigned backlog) is an independent dnd-kit SortableContext under one DndContext (D-01)"
    - "A whole row is draggable via PointerSensor { delay: 150, tolerance: 5 }; the justDragged ref guard suppresses the click-to-peek that fires after drop (D-06)"
    - "Intra-section drop fires the rank mutation immediately and optimistically; a DragOverlay ghost + insertion line show during drag (D-02, D-07)"
    - "A failed rank/cross-section move rolls back to the pre-drag order and shows the inline banner 'Couldn't save new order — reverted' (D-09, RANK-04)"
    - "cancelQueries(['gh-backlog', boardId]) runs in onMutate and an isDraggingRef-gated local order state is the rendered source of truth during drag, so a focus refetch cannot snap the list back (D-08, RANK-05)"
    - "A cross-section drop opens ConfirmSprintMoveDialog with cancelLabel='Keep Position'; Confirm fires sprint-membership + rank, Keep Position rolls back (D-03, D-04)"
    - "The list renders sections in Jira rank order on initial load (RANK-01)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/BacklogRow.tsx"
      provides: "useSortable-wrapped draggable row with justDragged-guarded click"
      contains: "useSortable"
    - path: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      provides: "DndContext + per-section SortableContext + rank mutation + flicker gate + error banner + cross-section confirm"
      contains: "DndContext"
  key_links:
    - from: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      to: "rankIssueApi"
      via: "rankMutation.mutationFn"
      pattern: "rankIssueApi"
    - from: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      to: "['gh-backlog', boardId] cache"
      via: "cancelQueries in onMutate"
      pattern: "cancelQueries"
    - from: "taskflow/src/routes/dashboard/BacklogRow.tsx"
      to: "DndContext sortable state"
      via: "useSortable({ id: issue.key })"
      pattern: "useSortable"
---

<objective>
Wire drag-to-rank into the Backlog UI: make `BacklogRow` a dnd-kit sortable item, wrap `BacklogPage`'s sections in a single `DndContext` with one `SortableContext` per section, add the optimistic rank mutation (consuming `rankIssueApi` + `rankIssue` from earlier waves), the flicker-mitigation gate (D-08), the inline rollback banner (D-09), the DragOverlay ghost + insertion line (D-07), section highlight (D-05), and cross-section confirmation via `ConfirmSprintMoveDialog` with `cancelLabel="Keep Position"` (D-03/D-04). Turn the Plan-01 `BacklogPage.rank.test.ts` GREEN, then a human verifies the drag interaction end-to-end.

Purpose: This is the user-facing payoff — drag a row, it reorders and persists, survives the background poll, and rolls back with a clear message on failure. It is last because it consumes the fixed `rankIssue` (Plan 02), the `rankIssueApi` service (Plan 03), and the `cancelLabel` dialog + test scaffold (Plan 01).
Output: Draggable, persisting, flicker-free Backlog reorder across all sections with gated cross-section moves.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-drag-to-rank-on-backlog/78-CONTEXT.md
@.planning/phases/78-drag-to-rank-on-backlog/78-RESEARCH.md
@.planning/phases/78-drag-to-rank-on-backlog/78-PATTERNS.md
@.planning/phases/78-drag-to-rank-on-backlog/78-UI-SPEC.md
@.planning/phases/78-drag-to-rank-on-backlog/78-VALIDATION.md
@.planning/phases/78-drag-to-rank-on-backlog/78-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make BacklogRow a dnd-kit sortable item with justDragged-guarded click (D-06, D-07)</name>
  <files>taskflow/src/routes/dashboard/BacklogRow.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/BacklogRow.tsx (current row — BacklogRowProps interface; the ContextMenuTrigger render-prop `<tr>` and the no-context-menu `<tr>`; existing onClick → (onOpenIssue ?? onIssueClick)(issue.key))
    - .planning/phases/78-drag-to-rank-on-backlog/78-PATTERNS.md § BacklogRow.tsx (useSortable hook, dragStyle, applying setNodeRef/attributes/listeners to BOTH `<tr>` paths, Pitfall 6 render-prop note)
    - .planning/phases/78-drag-to-rank-on-backlog/78-UI-SPEC.md § Drag Initiation + § Drag Ghost + § Interaction Contract (cursor-grab / data-dragging, opacity-60 ghost)
    - .planning/phases/78-drag-to-rank-on-backlog/78-RESEARCH.md § Pattern 6 + Pitfall 6 (ContextMenuTrigger render-prop conflict; 150ms delay mitigation)
  </read_first>
  <behavior>
    - A `BacklogRow` with a given `issue.key` exposes drag attributes/listeners on its `<tr>` and applies the dnd-kit transform via `CSS.Transform.toString(transform)`.
    - When `isDragging` is true the original row is hidden (`opacity: 0`) so only the DragOverlay ghost shows; `data-dragging="true"` is set on the `<tr>` for cursor styling.
    - With `isOverlay` true, `useSortable` is disabled (the overlay clone must not register as a sortable item).
    - The `onClick` handler returns early when the passed `justDragged` ref is `current === true`, so a drop does not also open the peek.
  </behavior>
  <action>
    Import `useSortable` from `@dnd-kit/sortable` and `CSS` from `@dnd-kit/utilities`. Add two props to `BacklogRowProps`: `isOverlay?: boolean` and `justDragged?: React.MutableRefObject<boolean>`. Call `useSortable({ id: issue.key, disabled: isOverlay })` at the top of the component, destructuring `attributes, listeners, setNodeRef, transform, transition, isDragging`. Build `dragStyle: React.CSSProperties = { transform: CSS.Transform.toString(transform), transition, opacity: isDragging ? 0 : 1, cursor: isDragging ? 'grabbing' : 'grab' }`. Apply `ref={setNodeRef}`, `style={dragStyle}` (merge with any existing style), `data-dragging={isDragging ? 'true' : undefined}`, `{...attributes}`, and `{...listeners}` to BOTH `<tr>` rendering paths — the one rendered via `ContextMenuTrigger render={<tr .../>}` AND the no-context-menu `<tr>`. In each path's `onClick`, add a leading guard `if (justDragged?.current) return;` before the existing `(onOpenIssue ?? onIssueClick)(issue.key)` call. Do NOT wrap the `<tr>` in a new element — the listeners go on the same `<tr>` that is the ContextMenuTrigger element (Pitfall 6); the 150ms PointerSensor delay (set in Task 2) prevents contextmenu/drag conflicts. Keep all existing cell rendering and props unchanged.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "BacklogRow" || echo "BacklogRow type-clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useSortable" taskflow/src/routes/dashboard/BacklogRow.tsx` returns at least `1`.
    - `grep -c "CSS.Transform.toString" taskflow/src/routes/dashboard/BacklogRow.tsx` returns at least `1`.
    - The justDragged guard is present in the click path: `grep -c "justDragged?.current" taskflow/src/routes/dashboard/BacklogRow.tsx` returns at least `1`.
    - `data-dragging` and `setNodeRef` are wired: `grep -c "setNodeRef" taskflow/src/routes/dashboard/BacklogRow.tsx` returns at least `1` (both `<tr>` paths) and `grep -c "data-dragging" taskflow/src/routes/dashboard/BacklogRow.tsx` returns at least `1`.
    - `isOverlay` disables the hook: `grep -c "disabled: isOverlay" taskflow/src/routes/dashboard/BacklogRow.tsx` returns `1`.
    - `npx tsc --noEmit` reports no new errors in BacklogRow.tsx.
  </acceptance_criteria>
  <done>BacklogRow is a sortable item on both render paths, hides itself while dragging, exposes cursor/data-dragging styling, disables sortable when used as the overlay clone, and guards its click against the post-drop event.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire DndContext + per-section SortableContext + optimistic rank mutation + flicker gate + error banner + cross-section confirm into BacklogPage (D-01..D-09, RANK-01/02/04/05)</name>
  <files>taskflow/src/routes/dashboard/BacklogPage.tsx, taskflow/src/routes/dashboard/__tests__/BacklogPage.rank.test.ts</files>
  <read_first>
    - taskflow/src/routes/dashboard/BacklogPage.tsx (current page — useGhBacklogData at ~253, rankFieldKey effect 259-266, confirmMoveToSprint 670-703 + confirmMoveToBacklog as the optimistic-mutation analog, bannerDismissed 424, the two ConfirmSprintMoveDialog usages 886-905, the sprint-section vs backlog-bucket render, invalidateGhBacklogData usage, readSecret('jira-pat'))
    - taskflow/src/routes/dashboard/__tests__/BacklogPage.rank.test.ts (Plan-01 scaffold — RANK-03 integer fixture, RANK-04 rollback+banner, RANK-05 cancelQueries: the contract this task turns GREEN)
    - taskflow/src/routes/dashboard/issue-detail/useFieldMutation.ts (canonical onMutate/onError/onSettled optimistic pattern)
    - .planning/phases/78-drag-to-rank-on-backlog/78-PATTERNS.md § BacklogPage.tsx (imports, sensor setup, state additions, rankMutation, handleDragStart/handleDragEnd incl. getRankPosition rule, DndContext/SortableContext/DragOverlay render, error banner) and § Shared Patterns
    - .planning/phases/78-drag-to-rank-on-backlog/78-UI-SPEC.md § Interaction Contract + § Copywriting Contract (banner copy, dialog "Keep Position", section highlight bg-accent/10 ring-1)
    - .planning/phases/78-drag-to-rank-on-backlog/78-RESEARCH.md § Pattern 5/7/8, Pitfall 1/3/5, § Determining rankBefore/AfterIssue
  </read_first>
  <behavior>
    - RANK-01: each section renders its issues in server rank order on load (the SortableContext `items` are the section's issue keys in `GhBacklogResponse` order, unless a `localOrder` override is set during drag).
    - RANK-02: dragging a row within a section and dropping reorders it (via `arrayMove`) and fires `rankMutation`.
    - RANK-04: when `rankIssueApi` rejects, `onError` restores `context.snapshot` to `['gh-backlog', boardId]`, restores `localOrder` to `previousOrder`, and sets `rankError = "Couldn't save new order — reverted"`.
    - RANK-05: `onMutate` calls `queryClient.cancelQueries({ queryKey: ['gh-backlog', boardId] })` and sets `isDraggingRef.current = true`; while `isDraggingRef.current` is true the rendered section order comes from `localOrder`, not refetched server data.
    - D-03/D-04: a drop whose `over` container differs from the `active` source container opens `ConfirmSprintMoveDialog` (cancelLabel="Keep Position"); Confirm runs the sprint-membership call (`addIssuesToSprint` / `moveIssuesToBacklog`) plus the rank PUT; Keep Position rolls back local order with no API call.
  </behavior>
  <action>
    Add imports: `DndContext, DragOverlay, PointerSensor, closestCenter, useSensor, useSensors` from `@dnd-kit/core`; `restrictToVerticalAxis` from `@dnd-kit/modifiers`; `SortableContext, arrayMove, verticalListSortingStrategy` from `@dnd-kit/sortable`; add `rankIssueApi` to the existing `@/services/jira` import; ensure `useMutation, useQueryClient` are imported. Configure `const sensors = useSensors(useSensor(PointerSensor, { activationConstraint: { delay: 150, tolerance: 5 } }))` (D-06).
    Add state/refs: `isDraggingRef` (useRef false), `justDragged` (useRef false), `activeId` (useState string|null), `localOrder` (useState Map<string,string[]>), `rankError` (useState string|null).
    Add `rankMutation = useMutation({...})` following the confirmMoveToSprint/useFieldMutation pattern: `mutationFn` reads the PAT via `readSecret('jira-pat')` and calls `rankIssueApi(jiraBaseUrl ?? '', token, issueKey, rankCustomFieldId, position)` where `rankCustomFieldId` is `backlog?.rankCustomFieldId` (the INTEGER from the cached response — never the `customfield_*` string); `onMutate` awaits `cancelQueries({ queryKey: ['gh-backlog', boardId] })`, snapshots `getQueryData(['gh-backlog', boardId])`, sets `isDraggingRef.current = true`, applies `setLocalOrder(prev => new Map(prev).set(sectionId, newOrder))`, returns `{ snapshot, sectionId, previousOrder }`; `onError` restores the snapshot via `setQueryData`, restores `localOrder` to `previousOrder`, sets `rankError = "Couldn't save new order — reverted"`, clears `isDraggingRef`; `onSettled` clears `isDraggingRef` and calls `invalidateGhBacklogData(queryClient, boardId)`.
    Add `handleDragStart({active})` (set `activeId`, `isDraggingRef.current = true`, clear `rankError`) and `handleDragEnd({active, over})`: clear `isDraggingRef`/`activeId`, set `justDragged.current = true` then `setTimeout(() => { justDragged.current = false; }, 50)`; read `sourceContainer = active.data.current?.sortable?.containerId` and `targetContainer = over?.data.current?.sortable?.containerId ?? over?.id`; if same container → `arrayMove` the section keys, compute position via the rule `above = newOrder[newIndex-1]` → `{ rankAfterIssue: above }` else `below = newOrder[newIndex+1]` → `{ rankBeforeIssue: below }` else `{}`, then `rankMutation.mutate({ issueKey, sectionId, newOrder, previousOrder, rankCustomFieldId: backlog?.rankCustomFieldId ?? 0, position })`; if different container → set a `pendingDragMove` state ({ issueKey, fromSectionId, toSectionId, fromSprintName, toSprintName, newOrder, previousOrder }) and open the drag confirm dialog instead of mutating immediately.
    Render: wrap the existing sprint-sections + backlog-bucket render in `<DndContext sensors={sensors} collisionDetection={closestCenter} modifiers={[restrictToVerticalAxis]} onDragStart onDragEnd onDragOver>`. Give each section its own `<SortableContext id={...} items={sectionIssueKeys} strategy={verticalListSortingStrategy}>` with a STABLE explicit `id` per section (`sprint-${sprint.id}` for sprint sections, `backlog` for the unassigned bucket — Pitfall 5). The rendered key order for a section is `localOrder.get(sectionId) ?? serverOrder` so the drag window is gated by local state (D-08). Pass `justDragged` to each `BacklogRow`. Add a `<DragOverlay>` rendering a `BacklogRow` clone (`isOverlay`, `opacity-60`/`shadow-lg`) for `activeId`. On `onDragOver`, when the pointer is over a different section, apply the section-highlight wrapper class `ring-1 ring-border bg-accent/10` to that section's `<tbody>` (D-05); remove on drag end/return.
    Add the inline `rankError` banner above the list following the existing `bannerDismissed` idiom (reuse the muted banner markup; dismiss button clears `rankError`; copy exactly "Couldn't save new order — reverted"). 
    Cross-section: add a `ConfirmSprintMoveDialog` instance bound to `pendingDragMove` with `cancelLabel="Keep Position"`; its `onConfirm` runs the sprint-membership change (`addIssuesToSprint(...)` when moving into a sprint, `moveIssuesToBacklog(...)` when moving to the backlog bucket) followed by the rank mutation against the target section; its cancel/`onOpenChange(false)` rolls the local order back to `previousOrder` with no API call. On membership-or-rank failure, roll back and set the cross-section banner copy "Couldn't move issue — reverted". Do NOT alter the two existing `ConfirmSprintMoveDialog` usages (they keep default "Cancel"). Do NOT re-enable `useVirtual` (leave `const useVirtual = false` — Pitfall: position:absolute on `<tr>`).
    Finally, complete the Plan-01 `BacklogPage.rank.test.ts` so its RANK-03/04/05 assertions exercise the real mutation surface and pass (wire the test's mutation trigger to the actual handler/exported seam as the analog network test does); the file must end GREEN.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --run src/routes/dashboard/__tests__/BacklogPage.rank.test.ts && npm test -- --run src/routes/dashboard/__tests__/BacklogPage.network.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/routes/dashboard/__tests__/BacklogPage.rank.test.ts` is fully GREEN (RANK-03 integer fixture, RANK-04 rollback+banner, RANK-05 cancelQueries).
    - `npm test -- --run src/routes/dashboard/__tests__/BacklogPage.network.test.tsx` still passes (RANK-01 rank-order render unbroken).
    - DndContext + per-section SortableContext present: `grep -c "DndContext" taskflow/src/routes/dashboard/BacklogPage.tsx` ≥ 1 and `grep -c "SortableContext" taskflow/src/routes/dashboard/BacklogPage.tsx` ≥ 1.
    - Flicker gate wired: `grep -c "cancelQueries" taskflow/src/routes/dashboard/BacklogPage.tsx` ≥ 1 and `grep -c "isDraggingRef" taskflow/src/routes/dashboard/BacklogPage.tsx` ≥ 2.
    - Mutation consumes the service and integer field id: `grep -c "rankIssueApi" taskflow/src/routes/dashboard/BacklogPage.tsx` ≥ 1 and `grep -c "backlog?.rankCustomFieldId" taskflow/src/routes/dashboard/BacklogPage.tsx` ≥ 1 (no hardcoded `10105` in the mutation: `grep -c "10105" taskflow/src/routes/dashboard/BacklogPage.tsx` returns `0`).
    - Rollback banner copy present: `grep -c "Couldn't save new order — reverted" taskflow/src/routes/dashboard/BacklogPage.tsx` returns `1`.
    - Cross-section dialog uses Keep Position: `grep -c 'cancelLabel="Keep Position"' taskflow/src/routes/dashboard/BacklogPage.tsx` returns `1`.
    - PointerSensor activation constraint present: `grep -c "delay: 150" taskflow/src/routes/dashboard/BacklogPage.tsx` returns `1`.
    - `npm test` (full suite) is GREEN and `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>BacklogPage drags reorder within every section optimistically, persist via rankIssueApi with the cached integer rankCustomFieldId, survive a focus refetch via cancelQueries + isDraggingRef gate, roll back with the inline banner on failure, and gate cross-section moves behind ConfirmSprintMoveDialog with "Keep Position"; BacklogPage.rank.test.ts and the full suite are GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — drag-to-rank interaction end-to-end</name>
  <action>Pause for human verification after the automated suite is GREEN. The reviewer exercises the five manual behaviors below (drag/ghost/insertion line, click-vs-drag, no-flicker-during-refetch, cross-section confirm, failure rollback) in the running app, since dnd-kit pointer drag and DragOverlay rendering are not exercisable in jsdom (VALIDATION.md Manual-Only Verifications).</action>
  <what-built>
    Drag-to-rank is fully wired into the Backlog page across all sections (active sprint, future sprints, unassigned backlog), with optimistic persistence, flicker mitigation, inline rollback banner, and gated cross-section moves. The automated suite (rank.test.ts, rank-api.test.ts, BacklogPage.rank.test.ts, BacklogPage.network.test.tsx, full suite) is GREEN — but the dnd-kit pointer drag, DragOverlay ghost, insertion line, section highlight, and no-flicker-during-refetch behavior cannot be exercised in jsdom (per VALIDATION.md Manual-Only Verifications) and need human eyes.
  </what-built>
  <how-to-verify>
    Run the app (`cd taskflow && npm run tauri dev`, or the project's standard dev command) and open the Backlog page for a board with an active sprint plus at least one future sprint and some backlog items.
    1. Intra-section reorder (RANK-02/D-07): Press-and-hold a row ~150ms and drag it within the active-sprint section. Confirm a semi-transparent ghost (opacity ~60%, shadow) follows the cursor, a primary-colored insertion line marks the drop position, and on drop the row stays in its new position (no snap-back). Reload the page — the new order persists (RANK-03).
    2. Click vs drag disambiguation (D-06): A quick click on a row still opens the peek; a click on the issue key still navigates full-page; a drag does NOT open the peek on release.
    3. No-flicker during refetch (RANK-05/D-08): Begin a drag, alt-tab away and back (or otherwise trigger a window-focus refetch) mid-drag; confirm the list order does not jump or revert during the drag.
    4. Cross-section move (D-03/D-04/D-05): Drag a row from the active sprint into a future-sprint section. Confirm the target section is highlighted while hovering, a dialog titled "Move Issue" appears reading "Move {KEY} from {Source} to {Target}?" with buttons "Keep Position" and "Confirm". Click "Keep Position" → row returns to original position, no change. Repeat and click "Confirm" → row moves to the target section and order persists after reload.
    5. Failure rollback (RANK-04/D-09): If you can induce a failure (e.g. revoke network briefly, or use a board where the rank write 403s), confirm the list rolls back to the pre-drag order and an inline banner reads "Couldn't save new order — reverted" (or "Couldn't move issue — reverted" for a cross-section move). The banner is dismissible.
  </how-to-verify>
  <resume-signal>Type "approved" if all five behaviors work, or describe the specific issue(s) observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Jira API | BacklogPage triggers authenticated rank PUT + sprint-membership writes via the existing apiFetch path |
| browser pointer events → drag state | dnd-kit pointer input drives reorder; no untrusted external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-04A | Tampering | rankCustomFieldId in the mutation body | mitigate | Read `backlog?.rankCustomFieldId` (integer) straight from the cached `GhBacklogResponse`; never hardcoded and never user-typed. Acceptance test asserts no `10105` literal in BacklogPage and the rank-api test asserts a numeric field id from the fixture. |
| T-78-04B | Spoofing / EoP | rank + sprint-membership writes | mitigate | Reuses the established authenticated `apiFetch('jira', ...)` path with the Stronghold PAT; a PAT lacking permission yields 401/403 → ApiError → onError rollback + inline banner (no silent success). No new auth path or secret introduced. |
| T-78-04C | Denial of Service (UX) | mid-drag background refetch overwriting optimistic order | mitigate | `cancelQueries` in `onMutate` + `isDraggingRef`-gated local order state hold the optimistic order during the drag window (D-08); `onSettled` re-invalidates only after the write settles. |
| T-78-04D | Information Disclosure | issue keys / sprint names rendered in the confirm dialog | accept | Keys and sprint names are already-rendered server data the user is authorized to see; the dialog reflects existing UI state, no new disclosure. |

No high-severity threats. All writes reuse the existing authenticated Jira `apiFetch` path; no new secrets or tokens; the rank field id is sourced from cached server data, not user input.
</threat_model>

<verification>
- `npm test -- --run src/routes/dashboard/__tests__/BacklogPage.rank.test.ts` GREEN (RANK-03/04/05).
- `npm test -- --run src/routes/dashboard/__tests__/BacklogPage.network.test.tsx` GREEN (RANK-01).
- `npm test` full suite GREEN; `npx tsc --noEmit` clean.
- Human checkpoint approves the five manual behaviors (drag/ghost, click-vs-drag, no-flicker, cross-section confirm, failure rollback).
</verification>

<success_criteria>
- Rows are draggable in every section; reorders persist via rankIssueApi with the cached integer rankCustomFieldId.
- Optimistic order holds through a focus refetch (no flicker); failures roll back with an inline banner.
- Cross-section moves are gated behind ConfirmSprintMoveDialog ("Keep Position"/"Confirm") firing sprint-membership + rank.
- All automated tests GREEN; human verification approved.
</success_criteria>

<output>
Create `.planning/phases/78-drag-to-rank-on-backlog/78-04-SUMMARY.md` when done.
</output>
