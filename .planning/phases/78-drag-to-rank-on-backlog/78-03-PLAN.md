---
phase: 78-drag-to-rank-on-backlog
plan: 03
type: execute
wave: 2
depends_on: [78-01, 78-02]
files_modified:
  - taskflow/src/services/jira/rank-api.ts
  - taskflow/src/services/jira.ts
  - taskflow/src/services/jira/rank-api.test.ts
autonomous: true
requirements: [RANK-03]
must_haves:
  truths:
    - "rankIssueApi issues PUT /rest/agile/1.0/issue/rank with an integer rankCustomFieldId and the issue key"
    - "rankIssueApi follows the addIssuesToSprint apiFetch + 204 + ApiError(401/403) convention"
    - "rankIssueApi is re-exported from the services/jira.ts barrel"
    - "rank-api.test.ts (created in Plan 01) is fully GREEN"
  artifacts:
    - path: "taskflow/src/services/jira/rank-api.ts"
      provides: "rankIssueApi service function (PUT rank)"
      exports: ["rankIssueApi"]
    - path: "taskflow/src/services/jira.ts"
      provides: "Barrel re-export of rankIssueApi"
      contains: "rank-api"
  key_links:
    - from: "taskflow/src/services/jira/rank-api.ts"
      to: "PUT /rest/agile/1.0/issue/rank"
      via: "apiFetch"
      pattern: "rest/agile/1.0/issue/rank"
    - from: "taskflow/src/services/jira.ts"
      to: "rankIssueApi"
      via: "barrel export"
      pattern: "rankIssueApi"
---

<objective>
Implement the `rankIssueApi` service function (RANK-03): a `PUT /rest/agile/1.0/issue/rank` call that carries the issue key, the **integer** `rankCustomFieldId` (read from the cached `GhBacklogResponse`, never hardcoded), and a `rankBeforeIssue`/`rankAfterIssue` position. Re-export it from the `services/jira.ts` barrel. Turn the Plan-01 Wave-0 test `rank-api.test.ts` GREEN.

Purpose: This is the network write that persists a reorder to Jira. It is the seam between the UI drag (Plan 04) and Jira. Isolating it as a tested pure-ish service function (mirroring `addIssuesToSprint`) keeps the BacklogPage mutation thin and lets us assert the integer-vs-string `rankCustomFieldId` contract (Pitfall: A1).
Output: `rank-api.ts` + barrel export; `rank-api.test.ts` GREEN.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-drag-to-rank-on-backlog/78-CONTEXT.md
@.planning/phases/78-drag-to-rank-on-backlog/78-RESEARCH.md
@.planning/phases/78-drag-to-rank-on-backlog/78-PATTERNS.md
@.planning/phases/78-drag-to-rank-on-backlog/78-VALIDATION.md
@.planning/phases/78-drag-to-rank-on-backlog/78-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement rankIssueApi and barrel-export it (RANK-03)</name>
  <files>taskflow/src/services/jira/rank-api.ts, taskflow/src/services/jira.ts, taskflow/src/services/jira/rank-api.test.ts</files>
  <read_first>
    - taskflow/src/services/jira/sprints.ts lines 206-267 (moveIssuesToBacklog + addIssuesToSprint — exact apiFetch(service,url,init,opName) shape, 204 handling, ApiError on 401/403)
    - taskflow/src/services/jira/rank-api.test.ts (the Plan-01 scaffold — the contract this implementation must satisfy: PUT url, body shape, integer rankCustomFieldId, 401→ApiError, 500→Error)
    - taskflow/src/services/jira.ts lines 26-28 (barrel export pattern, e.g. `export { addIssuesToSprint } from './jira/sprints';`)
    - taskflow/src/lib/api-error.ts and taskflow/src/lib/apiFetch.ts (ApiError signature `(message, status, service)`; apiFetch signature)
    - .planning/phases/78-drag-to-rank-on-backlog/78-PATTERNS.md § rank-api.ts (full implementation + barrel line) and § rankCustomFieldId integer note
  </read_first>
  <action>
    Create `taskflow/src/services/jira/rank-api.ts` exporting `async function rankIssueApi(baseUrl: string, token: string, issueKey: string, rankCustomFieldId: number, position: { rankBeforeIssue: string } | { rankAfterIssue: string } | Record<string, never>): Promise<void>`. Build `url = \`${baseUrl.replace(/\/$/, '')}/rest/agile/1.0/issue/rank\``. Build `body = { issues: [issueKey], rankCustomFieldId, ...position }` — `rankCustomFieldId` is the **integer** (typed `number`), NEVER the `customfield_10105` string. Call `apiFetch('jira', url, { method: 'PUT', headers: { Authorization: \`Bearer ${token}\`, 'Content-Type': 'application/json' }, body: JSON.stringify(body) }, 'Rank Issue')`. On `!response.ok && response.status !== 204`: if status is 401 or 403 throw `new ApiError('Failed to rank issue', response.status, 'jira')`, else throw `new Error(\`Failed to rank issue: ${response.status}\`)`. Import `ApiError` from `../../lib/api-error` and `apiFetch` from `../../lib/apiFetch` (match sprints.ts import paths).
    Add `export { rankIssueApi } from './jira/rank-api';` to `taskflow/src/services/jira.ts` adjacent to the existing `export { addIssuesToSprint } from './jira/sprints';` line.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --run src/services/jira/rank-api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/services/jira/rank-api.test.ts` is fully GREEN (all Plan-01 cases pass, including `typeof body.rankCustomFieldId === 'number'` and value 10105).
    - The PUT endpoint is exact: `grep -c "rest/agile/1.0/issue/rank" taskflow/src/services/jira/rank-api.ts` returns `1`.
    - The operation name matches the test: `grep -c "'Rank Issue'" taskflow/src/services/jira/rank-api.ts` returns `1`.
    - 401/403 path throws ApiError: `grep -c "ApiError" taskflow/src/services/jira/rank-api.ts` returns at least `1`.
    - Barrel export present: `grep -c "rankIssueApi" taskflow/src/services/jira.ts` returns at least `1` and `grep -c "rank-api" taskflow/src/services/jira.ts` returns `1`.
    - `rankCustomFieldId` parameter is typed `number` (not string): `grep -c "rankCustomFieldId: number" taskflow/src/services/jira/rank-api.ts` returns `1`.
  </acceptance_criteria>
  <done>rankIssueApi exists, follows the apiFetch/ApiError convention, sends an integer rankCustomFieldId to PUT /rest/agile/1.0/issue/rank, is re-exported from the barrel, and rank-api.test.ts is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Jira API | `rankIssueApi` issues an authenticated write (`PUT /rest/agile/1.0/issue/rank`) carrying the user's Jira PAT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-03A | Information Disclosure | PAT in Authorization header | mitigate | Reuses the existing `apiFetch('jira', ...)` auth path with `Authorization: Bearer ${token}`; token is read from Tauri Stronghold by the caller, never logged in this function. No new secret storage introduced. |
| T-78-03B | Tampering | request body (issueKey, rankCustomFieldId, position) | accept | `issueKey` and neighbour keys come from the cached server `GhBacklogResponse` (server-sourced, not user free-text); `rankCustomFieldId` is the integer read from that same cached response, not user-controllable injection. No sanitization needed beyond JSON serialization. |
| T-78-03C | Elevation of Privilege | rank write authorization | mitigate | Authorization is delegated to Jira: a PAT lacking rank-write permission yields 401/403, which `rankIssueApi` surfaces as `ApiError` (caught by the Plan-04 onError rollback) — no silent success, no client-side privilege assumption. |

No high-severity threats. The write reuses the established authenticated `apiFetch` path; no new secrets/tokens are introduced; `rankCustomFieldId` is sourced from cached server data, not user input.
</threat_model>

<verification>
- `npm test -- --run src/services/jira/rank-api.test.ts` GREEN.
- `npx tsc --noEmit` clean for the new file and barrel.
- Barrel re-export resolves (`rankIssueApi` importable from `@/services/jira`).
</verification>

<success_criteria>
- rankIssueApi implemented per the addIssuesToSprint convention with integer rankCustomFieldId.
- Re-exported from services/jira.ts barrel.
- Plan-01 rank-api.test.ts fully GREEN.
</success_criteria>

<output>
Create `.planning/phases/78-drag-to-rank-on-backlog/78-03-SUMMARY.md` when done.
</output>
