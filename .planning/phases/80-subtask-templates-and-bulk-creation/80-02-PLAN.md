---
phase: 80-subtask-templates-and-bulk-creation
plan: 02
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - taskflow/src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx
autonomous: true
requirements: [SUBTPL-03, SUBTPL-05, SUBTPL-08]
must_haves:
  truths:
    - "A single SubtaskTemplateRow component renders the curated core inline fields + Advanced expand"
    - "The same row component works in both Settings (edit) mode and bulk-modal (preview) mode"
    - "Placeholder assignees render as colored chips with resolved-value hints, e.g. @inherit → Alice (D-11)"
    - "Per-row status icons (pending/creating/created/failed) render in preview mode"
  artifacts:
    - path: "taskflow/src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx"
      provides: "Shared row component for Settings editor and bulk preview"
      exports: ["SubtaskTemplateRow"]
      min_lines: 120
  key_links:
    - from: "taskflow/src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx"
      to: "resolveRowPlaceholders.ts"
      via: "resolveAssignee for chip display hint"
      pattern: "resolveAssignee"
    - from: "taskflow/src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx"
      to: "SubtaskTemplateRow type from store"
      via: "row prop typing"
      pattern: "SubtaskTemplateRow"
---

<objective>
Build the shared `SubtaskTemplateRow.tsx` component — the single row UI consumed by both the Settings template editor (edit mode) and the bulk-create modal preview (preview mode). It renders the curated core inline fields (title, assignee, priority, labels, due date, estimate, story points), an Advanced expand (components + custom fields), placeholder chips with resolved hints, and per-row status icons.

Purpose: Defining this row as one reusable component (D-07, recommended in CONTEXT.md Discretion) prevents divergence between Settings and the modal and makes Plans 03 and 04 independent consumers. It is the interface contract both downstream surfaces wire against.
Output: `SubtaskTemplateRow.tsx` with a `mode: 'settings' | 'preview'` prop and optional `rowState` for preview status.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-subtask-templates-and-bulk-creation/80-CONTEXT.md
@.planning/phases/80-subtask-templates-and-bulk-creation/80-PATTERNS.md
@.planning/phases/80-subtask-templates-and-bulk-creation/80-UI-SPEC.md
@taskflow/src/stores/subtask-templates.store.ts
@taskflow/src/routes/dashboard/resolveRowPlaceholders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SubtaskTemplateRow inline core fields + chips + status icons</name>
  <files>taskflow/src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/create-edit-issue/CustomFieldsSection.tsx (analog — field rendering, three-branch select/autocomplete/input switch)
    - taskflow/.planning/phases/80-subtask-templates-and-bulk-creation/80-PATTERNS.md (SubtaskTemplateRow.tsx section — imports, row layout, chip pattern, status-icon pattern, props interface)
    - taskflow/.planning/phases/80-subtask-templates-and-bulk-creation/80-UI-SPEC.md (Component Inventory §2 — exact classNames, field widths, accessibility)
    - taskflow/src/stores/subtask-templates.store.ts (SubtaskTemplateRow type)
    - taskflow/src/routes/dashboard/resolveRowPlaceholders.ts (resolveAssignee for chip hint)
    - taskflow/src/components/ui/input.tsx, taskflow/src/components/ui/select.tsx, taskflow/src/components/ui/button.tsx (primitives)
  </read_first>
  <action>
    Create `SubtaskTemplateRow.tsx` exporting `SubtaskTemplateRow`. Props: `row: SubtaskTemplateRow` (store type), `mode: 'settings' | 'preview'`, `onChange: (patch: Partial<SubtaskTemplateRow>) => void`, `onRemove: () => void`, `creatmetaFields?: CreatemetaField[]` (for Advanced custom fields), `assignees?: JiraUser[]` (for assignee picker), `placeholderCtx?: PlaceholderContext` (for chip hints in preview), `rowState?: { status: 'pending' | 'creating' | 'created' | 'failed'; error?: string }` (preview only), `dragHandleProps?` (settings drag handle).
    Inline core fields left-to-right per UI-SPEC §2 with the exact widths: Title `<Input className="flex-1 min-w-0" placeholder="Subtask title" />` (required — empty shows `border-destructive` ring), Assignee selector `w-32` (renders PlaceholderChip when value is `@inherit`/`@current`/`@unassigned`, else resolved display name), Priority select `w-28`, Labels multi-select `w-32`, Due date input `w-32`, Estimate text input `w-20` placeholder `"e.g. 2h"`, Story points number input `w-16`, Advanced toggle (`ChevronRight`/`ChevronDown`, `variant="ghost" size="icon-sm"`, `aria-expanded`, `aria-controls="{row.id}-advanced"`).
    Placeholder chips per UI-SPEC Color section: `@inherit` = `bg-blue-100 text-blue-800 dark:bg-blue-900/40 dark:text-blue-300`; `@current` = `bg-violet-100 text-violet-800 dark:bg-violet-900/40 dark:text-violet-300`; `@unassigned` = `bg-muted text-muted-foreground`. Chip anatomy `inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-xs font-normal`, hint suffix in `text-muted-foreground`, computed from `resolveAssignee(row.assignee, placeholderCtx)` displayHint. `aria-label="@inherit placeholder, resolved to {value}"`.
    Far-right control is mode-dependent: settings mode → Remove `X` ghost icon button (`text-muted-foreground hover:text-destructive`, `aria-label="Remove row"`); preview mode → status icon: pending none, creating `<Loader2 className="size-4 animate-spin text-muted-foreground" />` + row `opacity-70`, created `<CheckCircle2 className="size-4 text-green-600 dark:text-green-400" role="img" aria-label="Created" />`, failed `<AlertCircle className="size-4 text-destructive" role="img" aria-label="Failed" />` + row wrapper `bg-destructive/5`. Row container `flex items-center gap-2 min-h-[44px] px-2 py-1 rounded-md hover:bg-muted/50`. Disable all field inputs when `rowState?.status === 'creating' || rowState?.status === 'created'` (Pitfall 6 — no edits mid-creation).
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "SubtaskTemplateRow" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `SubtaskTemplateRow.tsx` exports `SubtaskTemplateRow` and accepts a `mode: 'settings' | 'preview'` prop: `grep -E "mode.*'settings'.*'preview'|'settings' \| 'preview'" taskflow/src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx` matches
    - All three chip color classes present: `grep -c "bg-blue-100\|bg-violet-100\|bg-muted text-muted-foreground" taskflow/src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx` ≥ 3
    - Status icons gated on rowState: `grep -c "Loader2\|CheckCircle2\|AlertCircle" taskflow/src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx` ≥ 3
    - `aria-label="Remove row"` and `aria-controls` present
    - `cd taskflow && npx biome check src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx` passes
  </acceptance_criteria>
  <done>Row renders core inline fields, placeholder chips with hints, and mode-dependent far-right control; typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add Advanced expand (components + custom fields) to SubtaskTemplateRow</name>
  <files>taskflow/src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/create-edit-issue/CustomFieldsSection.tsx (three-branch field rendering: allowedValues→Select, hasAutocomplete→Input+dropdown, plain→Input)
    - taskflow/.planning/phases/80-subtask-templates-and-bulk-creation/80-UI-SPEC.md (Component Inventory §2 Advanced expand classNames)
    - taskflow/src/services/jira.ts (CreatemetaField type, wrapCustomFieldValue)
  </read_first>
  <action>
    Add the Advanced expand region to `SubtaskTemplateRow.tsx`, rendered below the row when the Advanced toggle is open. Container `flex flex-col gap-3 pl-4 pt-2 pb-3 rounded-b-md bg-muted/30 border-l-2 border-border` with `id="{row.id}-advanced"`. Subsection label `"Advanced fields"` in `text-xs font-semibold text-muted-foreground uppercase tracking-wide` (D-07/D-08). Contains: a Components multi-select (writes `row.components` string[] of component IDs) and the createmeta-derived custom fields rendered with the three-branch pattern from `CustomFieldsSection.tsx` (allowedValues → Select, autocomplete → Input, plain → Input), writing into `row.customFieldValues[fieldId]` via `onChange`. Only render custom fields present in `creatmetaFields` (the long tail per SUBTPL-03). Inputs disabled when `rowState?.status` is `creating`/`created`.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "SubtaskTemplateRow" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - Advanced container with `id="{row.id}-advanced"`-style template present: `grep -c "advanced" taskflow/src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx` ≥ 1
    - `"Advanced fields"` label string present
    - Custom fields iterate over `creatmetaFields`: `grep -c "creatmetaFields" taskflow/src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx` ≥ 1
    - `cd taskflow && npx biome check src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx` passes
  </acceptance_criteria>
  <done>Advanced expand exposes components + all createmeta custom fields; full SUBTPL-03 field set reachable; typechecks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → row data model | freeform field values entered in the row UI become create payload data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-03 | Input Validation | SubtaskTemplateRow title field | mitigate | Required title enforced with border-destructive ring; empty title blocks downstream Save/Create (per UI-SPEC interaction contract) |
| T-80-04 | Tampering | SubtaskTemplateRow field values | accept | Values rendered as React text/controlled inputs (no dangerouslySetInnerHTML); Jira DC server validates on submit (RESEARCH V5 note) |
| T-80-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase |
</threat_model>

<verification>
- `cd taskflow && npm run check` (biome + tsc) clean after both tasks
- Component compiles and is importable by Plans 03 and 04
</verification>

<success_criteria>
- One shared row component serves both Settings and bulk-modal surfaces (D-07)
- Placeholder chips render with resolved hints (SUBTPL-05, D-11)
- Advanced expand exposes the full optional field set (SUBTPL-03, D-08)
- Per-row status icons render in preview mode (SUBTPL-06 readiness)
</success_criteria>

<output>
Create `.planning/phases/80-subtask-templates-and-bulk-creation/80-02-SUMMARY.md` when done
</output>
