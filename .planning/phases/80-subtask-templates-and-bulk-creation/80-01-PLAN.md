---
phase: 80-subtask-templates-and-bulk-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/stores/subtask-templates.store.ts
  - taskflow/src/stores/subtask-templates.store.test.ts
  - taskflow/src/routes/dashboard/resolveTemplateFields.ts
  - taskflow/src/routes/dashboard/resolveTemplateFields.test.ts
  - taskflow/src/routes/dashboard/resolveRowPlaceholders.ts
  - taskflow/src/routes/dashboard/resolveRowPlaceholders.test.ts
  - taskflow/src/routes/dashboard/BulkCreateSubtasksModal.test.ts
  - taskflow/src/services/jira.ts
  - taskflow/src/routes/dashboard/BulkProgressIndicator.tsx
autonomous: true
requirements: [SUBTPL-02, SUBTPL-03, SUBTPL-05, SUBTPL-06, SUBTPL-07, SUBTPL-08]
must_haves:
  truths:
    - "Subtask templates persist across app restarts via createTauriStorage('subtask-templates.json')"
    - "resolveTemplateFields computes the exact 'N fields skipped' count for a template+createmeta combo; unsupported fields are dropped (D-02), never blocked even when project-required (D-03)"
    - "resolveRowPlaceholders resolves the three placeholders @inherit/@current/@unassigned at create time (D-09)"
    - "@current resolves to jiraUsername (DC name field), never jiraUserDisplayName"
    - "@inherit on a parent field with no value resolves to empty, never an error"
    - "JiraIssueDetail.fields includes components so @inherit can read parent components"
    - "BulkProgressIndicator can render 'Creating N subtasks...' via actionVerb/noun props"
  artifacts:
    - path: "taskflow/src/stores/subtask-templates.store.ts"
      provides: "Persistent template store mirroring tempo-filters.store.ts"
      contains: "createTauriStorage('subtask-templates.json')"
    - path: "taskflow/src/routes/dashboard/resolveTemplateFields.ts"
      provides: "Pure field-drop computation"
      exports: ["resolveTemplateFields"]
    - path: "taskflow/src/routes/dashboard/resolveRowPlaceholders.ts"
      provides: "Pure placeholder resolver"
      exports: ["resolveAssignee", "resolveRowForCreate"]
    - path: "taskflow/src/stores/subtask-templates.store.test.ts"
      provides: "SUBTPL-02 store persistence tests"
    - path: "taskflow/src/routes/dashboard/resolveTemplateFields.test.ts"
      provides: "SUBTPL-03/05 field-drop tests"
    - path: "taskflow/src/routes/dashboard/resolveRowPlaceholders.test.ts"
      provides: "SUBTPL-08 placeholder tests"
    - path: "taskflow/src/routes/dashboard/BulkCreateSubtasksModal.test.ts"
      provides: "SUBTPL-06/07 retry+ordering test stub"
  key_links:
    - from: "taskflow/src/stores/subtask-templates.store.ts"
      to: "taskflow/src/lib/tauri-storage.ts"
      via: "createTauriStorage('subtask-templates.json')"
      pattern: "createTauriStorage\\('subtask-templates.json'\\)"
    - from: "taskflow/src/routes/dashboard/resolveRowPlaceholders.ts"
      to: "JiraIssueDetail.fields"
      via: "parentIssue.fields.assignee/priority/labels/duedate/components reads"
      pattern: "parentIssue\\.fields\\."
---

<objective>
Build the pure-data foundation for Phase 80: the persistent template store, the two pure resolver utilities (field-drop + placeholder resolution), the four Wave-0 test files, the additive `components` field on `JiraIssueDetail`, and the `actionVerb`/`noun` props on `BulkProgressIndicator`.

Purpose: Every downstream surface (Settings section, shared row, bulk modal) consumes these contracts. Extracting the resolvers as pure functions and defining the store shape first prevents a scavenger hunt and makes the riskiest behaviors (retry-no-duplicate, placeholder resolution, field-drop count) unit-testable in isolation.
Output: `subtask-templates.store.ts`, `resolveTemplateFields.ts`, `resolveRowPlaceholders.ts`, four test files, `components` typed on `JiraIssueDetail` + requested in `fetchIssueDetail`, and `BulkProgressIndicator` text props.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-subtask-templates-and-bulk-creation/80-CONTEXT.md
@.planning/phases/80-subtask-templates-and-bulk-creation/80-RESEARCH.md
@.planning/phases/80-subtask-templates-and-bulk-creation/80-PATTERNS.md
@.planning/phases/80-subtask-templates-and-bulk-creation/80-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create subtask-templates store + persistence test</name>
  <files>taskflow/src/stores/subtask-templates.store.ts, taskflow/src/stores/subtask-templates.store.test.ts</files>
  <read_first>
    - taskflow/src/stores/tempo-filters.store.ts (EXACT analog — mirror structure line-for-line)
    - taskflow/src/lib/tauri-storage.ts (createTauriStorage adapter)
    - taskflow/src/stores/recent-items.store.test.ts (store test pattern with createTauriStorage-backed store)
    - taskflow/.planning/phases/80-subtask-templates-and-bulk-creation/80-RESEARCH.md (Pattern 1 — full store shape)
  </read_first>
  <action>
    Create `useSubtaskTemplatesStore` mirroring `tempo-filters.store.ts` exactly per D-01/D-02. Export interfaces `SubtaskTemplateRow` and `SubtaskTemplate` in this file. `SubtaskTemplateRow` fields: `id: string`, `title: string`, `assignee: string` (carrying placeholder sentinels `'@inherit' | '@current' | '@unassigned'` or a concrete username), `priority: string | null`, `labels: string[]`, `duedate: string | null`, `timeEstimate: string`, `storyPoints: number | null`, `components: string[]`, `customFieldValues: Record<string, string>`. `SubtaskTemplate` fields: `id`, `name`, `subtaskIssueTypeId`, `subtaskIssueTypeName`, `rows: SubtaskTemplateRow[]`. State actions: `addTemplate(t)`, `removeTemplate(id)`, `renameTemplate(id, name)`, `updateTemplate(id, patch: Partial<SubtaskTemplate>)`, `moveTemplate(id, direction: 'up' | 'down' | 'front' | 'back')` (vertical list — use `'up'/'down'` not tempo's `'left'/'right'`). persist config: `name: 'subtask-templates-store'`, `storage: createTauriStorage('subtask-templates.json')`, `version: 1`, `migrate: (persisted, _version) => persisted as SubtaskTemplatesState` (Pitfall 3 — never omit version/migrate). For store-integrity hardening (threat T-80-01): in `migrate`, coerce a non-array `templates` to `[]` and drop any template entry whose `rows` is not an array, so an untrusted/corrupt persisted JSON shape cannot crash consumers.
    Write `subtask-templates.store.test.ts` covering SUBTPL-02: add a template then assert it is in `templates`; remove by id; rename by id; updateTemplate patches a field; moveTemplate 'up'/'down'/'front'/'back' reorders correctly; migrate({templates: 'garbage'}) returns `{templates: []}`. Reset store state between tests via `useSubtaskTemplatesStore.setState({ templates: [] })`.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- src/stores/subtask-templates.store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createTauriStorage('subtask-templates.json')" taskflow/src/stores/subtask-templates.store.ts` returns 1
    - `grep -c "version: 1" taskflow/src/stores/subtask-templates.store.ts` returns 1 and a `migrate:` key is present
    - Store test file asserts add/remove/rename/update/move AND the garbage-migrate coercion; `npm run test -- src/stores/subtask-templates.store.test.ts` passes
  </acceptance_criteria>
  <done>Store persists templates to subtask-templates.json with version/migrate guards; all store tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract resolveTemplateFields + resolveRowPlaceholders pure functions with tests</name>
  <files>taskflow/src/routes/dashboard/resolveTemplateFields.ts, taskflow/src/routes/dashboard/resolveTemplateFields.test.ts, taskflow/src/routes/dashboard/resolveRowPlaceholders.ts, taskflow/src/routes/dashboard/resolveRowPlaceholders.test.ts</files>
  <read_first>
    - taskflow/.planning/phases/80-subtask-templates-and-bulk-creation/80-PATTERNS.md (resolveTemplateFields.ts and resolveRowPlaceholders.ts sections — full signatures and logic)
    - taskflow/.planning/phases/80-subtask-templates-and-bulk-creation/80-RESEARCH.md (Pattern 4 field-drop, Pattern 5 placeholder resolution)
    - taskflow/src/routes/dashboard/create-edit-issue/useCreateEditQueries.ts (CORE_FIELD_IDS set pattern)
    - taskflow/src/services/jira.ts (CreatemetaField type, JiraIssueDetail type ~L1206)
    - taskflow/src/stores/subtask-templates.store.ts (SubtaskTemplateRow type from Task 1)
  </read_first>
  <behavior>
    - resolveTemplateFields: a custom fieldId present in createmeta is kept; a custom fieldId absent from createmeta is counted in skippedFieldIds and removed from the cleaned row
    - resolveTemplateFields: core fields (summary, assignee, priority, labels, duedate, timetracking, parent, storyPointsFieldKey) are NEVER counted as skipped regardless of createmeta
    - resolveTemplateFields: a row with components set AND 'components' absent from createmeta counts components once and clears it; if 'components' is in createmeta it is kept
    - resolveTemplateFields: totalSkipped equals the sum of per-row skippedFieldIds lengths
    - resolveAssignee('@unassigned') → { payloadName: null, displayHint: '@unassigned' }
    - resolveAssignee('@current', ctx) → payloadName = ctx.jiraUsername; displayHint uses jiraUserDisplayName
    - resolveAssignee('@inherit', ctx) → payloadName = parent assignee name or null; displayHint '@inherit → {displayName|(none)}'
    - resolveRowForCreate: @inherit on priority/labels/duedate with parent value missing resolves to empty (no error, D-12)
  </behavior>
  <action>
    Create `resolveTemplateFields.ts` exporting `resolveTemplateFields(rows, creatmetaFields, storyPointsFieldKey)` returning `{ resolvedRows: { row, skippedFieldIds }[], totalSkipped }` per the PATTERNS.md signature. ALWAYS_ALLOWED set: `summary, assignee, priority, labels, duedate, timetracking, parent` plus `storyPointsFieldKey` when non-null. Only `customFieldValues` keys and `components` are eligible to be skipped; never the curated core fields.
    Create `resolveRowPlaceholders.ts` exporting `resolveAssignee(assignee, ctx)` and `resolveRowForCreate(row, ctx)` per the PATTERNS.md signature. `PlaceholderContext` = `{ jiraUsername: string | null; jiraUserDisplayName: string | null; parentIssue: JiraIssueDetail }`. `@current` payload uses `ctx.jiraUsername` (DC name), hint uses `jiraUserDisplayName`. `@inherit` reads `parentIssue.fields.assignee/priority/labels/duedate`; missing values resolve to empty per D-12. Treat all resolved values as data — never interpolate template strings into other field values (threat T-80-02). Both files are pure (no hooks, no imports from React/zustand).
    Write both `.test.ts` files asserting every bullet in <behavior>. For placeholder tests, construct a minimal mock `JiraIssueDetail` with and without each field.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- src/routes/dashboard/resolveTemplateFields.test.ts src/routes/dashboard/resolveRowPlaceholders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `resolveTemplateFields.ts` exports `resolveTemplateFields`; `resolveRowPlaceholders.ts` exports `resolveAssignee` and `resolveRowForCreate`
    - Neither util imports `react`, `zustand`, or `@tanstack/react-query`: `grep -E "from 'react'|from 'zustand'|react-query" taskflow/src/routes/dashboard/resolveTemplateFields.ts taskflow/src/routes/dashboard/resolveRowPlaceholders.ts` returns nothing
    - Both test files pass with the @current-uses-jiraUsername and @inherit-empty-on-missing assertions present
  </acceptance_criteria>
  <done>Both pure resolvers exist, are import-clean, and their unit tests pass green.</done>
</task>

<task type="auto">
  <name>Task 3: Add components to JiraIssueDetail, adapt BulkProgressIndicator text, scaffold modal test</name>
  <files>taskflow/src/services/jira.ts, taskflow/src/routes/dashboard/BulkProgressIndicator.tsx, taskflow/src/routes/dashboard/BulkCreateSubtasksModal.test.ts</files>
  <read_first>
    - taskflow/src/services/jira.ts (JiraIssueDetail.fields type ~L1209-1246; fetchIssueDetail fields list ~L1355-1380)
    - taskflow/src/routes/dashboard/BulkProgressIndicator.tsx (props interface ~L13-21; statusText derivation ~L44-57)
    - taskflow/.planning/phases/80-subtask-templates-and-bulk-creation/80-PATTERNS.md (BulkProgressIndicator adapt section; Modification Map)
    - taskflow/.planning/phases/80-subtask-templates-and-bulk-creation/80-UI-SPEC.md (Copywriting Contract — exact progress strings)
  </read_first>
  <action>
    In `jira.ts` (Pitfall 1, additive — no breaking callers): add `components?: Array<{ id: string; name: string }>` to the `JiraIssueDetail.fields` object type (alongside `duedate`/`labels`), and add the literal string `'components'` to the `fields` array inside `fetchIssueDetail` (the array starting `const fields = [ 'summary', ... ]` ~L1356). Do not change the function signature.
    In `BulkProgressIndicator.tsx`: add optional props `actionVerb?: string` (default `'Updating'`) and `noun?: string` (default `'issues'`) to `BulkProgressIndicatorProps`, and rewire the `statusText` derivation so the in-progress/all-success/partial/all-failed strings use the verb+noun. Match the UI-SPEC copy contract exactly: in-progress `"Creating {N} subtasks..."`, all-success `"{N} subtask{s} created"`, partial `"{N} created, {M} failed"`, all-failed `"All {N} subtasks failed"` when passed `actionVerb="Creating" noun="subtasks"`. Keep existing default text identical for current callers (regression-safe). Do not alter the `aria-live="polite"` status region.
    Create `BulkCreateSubtasksModal.test.ts` as a Wave-0 stub that already encodes the SUBTPL-06/07 contract against the to-be-extracted creation loop: it MUST mock `createIssue` and assert (a) sequential ordering — createIssue is called with row titles in array order (index 0 before 1 before 2); (b) retry-no-duplicate — given a partial-failure run, a retry pass never re-calls createIssue for a row whose status is 'created'. If the modal's creation loop is not yet importable, structure the test against a small exported `createAllRows` helper that Plan 04 will implement, and mark the not-yet-implemented assertions with `it.todo` so the file runs green now but documents the contract.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- src/routes/dashboard/BulkCreateSubtasksModal.test.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "jira.ts\|BulkProgressIndicator" || echo "no type errors in touched files"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "components" taskflow/src/services/jira.ts` shows the field added; `'components'` literal appears in the fetchIssueDetail fields array (verify with `grep -n "'components'" taskflow/src/services/jira.ts`)
    - `grep -c "actionVerb" taskflow/src/routes/dashboard/BulkProgressIndicator.tsx` ≥ 2 (interface + usage); existing default text unchanged for no-prop callers
    - `BulkCreateSubtasksModal.test.ts` exists and runs green (passing assertions or it.todo for deferred loop)
    - `cd taskflow && npx biome check src/services/jira.ts src/routes/dashboard/BulkProgressIndicator.tsx` passes
  </acceptance_criteria>
  <done>components is typed+requested on issue detail; BulkProgressIndicator renders subtask copy via props with no regression; modal test contract scaffolded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted JSON → store load | subtask-templates.json on disk is reloaded into app state; an externally-edited/corrupt file is untrusted input |
| template field values → createIssue payload | user-authored row values flow into authenticated Jira DC create calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering | subtask-templates.store.ts migrate/load | mitigate | migrate() coerces non-array `templates` to `[]` and drops entries with non-array `rows`, so a corrupt persisted shape cannot crash consumers |
| T-80-02 | Injection | resolveRowPlaceholders.ts / resolveTemplateFields.ts | mitigate | resolved placeholder/field values are passed as discrete data fields to createIssue; never string-interpolated into other field values |
| T-80-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH Package Legitimacy Audit confirms zero installs) |
</threat_model>

<verification>
- `cd taskflow && npm run test` (full suite) green after this plan's tasks
- `cd taskflow && npm run check` (biome + tsc) clean
- All three Wave-0 pure-function/store test files exist and pass; BulkCreateSubtasksModal.test.ts exists
</verification>

<success_criteria>
- Persistent store mirrors tempo-filters pattern with version/migrate integrity guard (SUBTPL-02)
- resolveTemplateFields computes exact skip counts; resolveRowPlaceholders resolves all three placeholders correctly (SUBTPL-03, SUBTPL-05, SUBTPL-08)
- JiraIssueDetail exposes components for @inherit (SUBTPL-08)
- BulkProgressIndicator renders 'Creating N subtasks...' via props (SUBTPL-06)
- Modal creation-loop contract (ordering + retry-no-duplicate) scaffolded (SUBTPL-06, SUBTPL-07)
</success_criteria>

<output>
Create `.planning/phases/80-subtask-templates-and-bulk-creation/80-01-SUMMARY.md` when done
</output>
