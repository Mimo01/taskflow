---
phase: 80-subtask-templates-and-bulk-creation
plan: 03
type: execute
wave: 3
depends_on: ["80-01", "80-02"]
files_modified:
  - taskflow/src/routes/settings/SubtaskTemplatesSection.tsx
  - taskflow/src/routes/settings/Settings.tsx
autonomous: true
requirements: [SUBTPL-01, SUBTPL-02, SUBTPL-03]
must_haves:
  truths:
    - "Settings has a 'Subtask Templates' section reachable from the settings nav"
    - "User can create, rename, reorder, and delete named templates"
    - "User can edit a template's rows (add/remove/reorder) with the shared row component"
    - "Templates persist across app restart (via the Wave-1 store)"
  artifacts:
    - path: "taskflow/src/routes/settings/SubtaskTemplatesSection.tsx"
      provides: "Settings UI for template CRUD + row editing"
      exports: ["default"]
      min_lines: 80
    - path: "taskflow/src/routes/settings/Settings.tsx"
      provides: "Section registration (type union, SECTIONS, render block)"
      contains: "subtask-templates"
  key_links:
    - from: "taskflow/src/routes/settings/SubtaskTemplatesSection.tsx"
      to: "useSubtaskTemplatesStore"
      via: "store actions add/remove/rename/move/update"
      pattern: "useSubtaskTemplatesStore"
    - from: "taskflow/src/routes/settings/Settings.tsx"
      to: "SubtaskTemplatesSection"
      via: "conditional render on activeSection === 'subtask-templates'"
      pattern: "subtask-templates"
---

<objective>
Build the Settings "Subtask Templates" section (SUBTPL-01) and register it in `Settings.tsx`. Users create, rename, reorder, and delete named templates, and edit each template's rows (add/remove/reorder) using the shared `SubtaskTemplateRow` from Plan 02. All state is backed by the Wave-1 persistent store, so templates survive restart (SUBTPL-02).

Purpose: This is the management surface for templates — the first half of the phase user story. It consumes the store contract (Plan 01) and the row component (Plan 02) without modifying either.
Output: `SubtaskTemplatesSection.tsx` + three surgical insertions in `Settings.tsx`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-subtask-templates-and-bulk-creation/80-CONTEXT.md
@.planning/phases/80-subtask-templates-and-bulk-creation/80-PATTERNS.md
@.planning/phases/80-subtask-templates-and-bulk-creation/80-UI-SPEC.md
@taskflow/src/stores/subtask-templates.store.ts
@taskflow/src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SubtaskTemplatesSection (template list, CRUD, empty state)</name>
  <files>taskflow/src/routes/settings/SubtaskTemplatesSection.tsx</files>
  <read_first>
    - taskflow/src/routes/settings/WorkflowSection.tsx (EXACT shell analog — outer div, h2, h3, store consumption)
    - taskflow/.planning/phases/80-subtask-templates-and-bulk-creation/80-PATTERNS.md (SubtaskTemplatesSection.tsx section — card row layout, empty state, dnd note)
    - taskflow/.planning/phases/80-subtask-templates-and-bulk-creation/80-UI-SPEC.md (Component Inventory §1, Copywriting Contract, Interaction Contracts → Settings Template Editor)
    - taskflow/src/stores/subtask-templates.store.ts (store actions + types)
    - taskflow/src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx (row component, settings mode)
  </read_first>
  <action>
    Create `SubtaskTemplatesSection.tsx` (default export) mirroring `WorkflowSection.tsx` shell: outer `<div data-testid="section-subtask-templates" className="flex flex-col gap-8">` with `<h2 className="text-lg font-semibold">Subtask Templates</h2>`. Consume `useSubtaskTemplatesStore` for `templates, addTemplate, removeTemplate, renameTemplate, moveTemplate, updateTemplate`.
    Template list: each template is a card row `flex items-center gap-2 rounded-lg border bg-background px-3 py-2` with GripVertical drag handle (`text-muted-foreground`, cursor-grab), inline name `<input>` (`border-none bg-transparent text-sm font-normal focus:outline-none focus:ring-1 focus:ring-ring rounded px-1`; blur non-empty saves via renameTemplate, blur empty restores), an `"Edit Rows"` ghost button (toggles inline row editor, collapsing any other open one), and a `Trash2` ghost icon button (`text-destructive hover:text-destructive`, `aria-label="Delete template {name}"`, removeTemplate immediately — no confirm dialog per Copywriting Contract).
    `"New Template"` button above the list (`variant="outline" size="sm"`, right-aligned, `Plus` icon) → addTemplate with name `"Untitled Template"`, focused for rename. Empty state per UI-SPEC §1: `flex flex-col items-center gap-3 py-12 text-center` with heading `"No templates yet"`, body `"Create a template to bulk-add subtasks from any issue."`, outline `"New Template"` CTA.
    Template reorder via `@dnd-kit/core` + `@dnd-kit/sortable` using the P78/P79 fixed-height DragOverlay pattern (Pitfall 5 — never a live-rendered clone; `onDragEnd` only, no `onDragOver` reorder); on drop call moveTemplate to persist.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "SubtaskTemplatesSection" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - Default export present; `data-testid="section-subtask-templates"` present
    - All store actions wired: `grep -c "addTemplate\|removeTemplate\|renameTemplate\|moveTemplate" taskflow/src/routes/settings/SubtaskTemplatesSection.tsx` ≥ 4
    - Exact copy present: `grep -c "No templates yet\|Untitled Template\|New Template" taskflow/src/routes/settings/SubtaskTemplatesSection.tsx` ≥ 3
    - dnd uses onDragEnd only: `grep -c "onDragOver" taskflow/src/routes/settings/SubtaskTemplatesSection.tsx` returns 0
    - `cd taskflow && npx biome check src/routes/settings/SubtaskTemplatesSection.tsx` passes
  </acceptance_criteria>
  <done>Settings section lists templates with CRUD + reorder + empty state; persists via store; typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Inline row editor + register section in Settings.tsx</name>
  <files>taskflow/src/routes/settings/SubtaskTemplatesSection.tsx, taskflow/src/routes/settings/Settings.tsx</files>
  <read_first>
    - taskflow/src/routes/settings/Settings.tsx (SettingsSection union ~L43, SECTIONS array ~L45-54, render block ~L84-96, section imports)
    - taskflow/.planning/phases/80-subtask-templates-and-bulk-creation/80-PATTERNS.md (Settings.tsx modify section — exact 4 insertion points; SubtaskTemplatesSection row editor)
    - taskflow/.planning/phases/80-subtask-templates-and-bulk-creation/80-UI-SPEC.md (Component Inventory §1 row editor; §2 row component; subtask type selector)
    - taskflow/src/routes/dashboard/create-edit-issue/SubtaskTemplateRow.tsx (row component, settings mode)
    - taskflow/src/routes/dashboard/create-edit-issue/useCreateEditQueries.ts (createmeta issue-type + field query chain, subtask filter)
  </read_first>
  <action>
    In `SubtaskTemplatesSection.tsx`: add the inline row editor shown below a template when "Edit Rows" is active. Container `flex flex-col gap-4 rounded-lg border bg-muted/30 px-4 py-4 mt-1`, subsection header `"ROWS"` in `text-xs font-semibold text-muted-foreground uppercase tracking-wide`. Render each template row via `<SubtaskTemplateRow mode="settings" ... />` writing back through `updateTemplate(template.id, { rows })`. A subtask-type `<Select>` at the top of the editor sets the template's `subtaskIssueTypeId`/`subtaskIssueTypeName` (filter the project's issue types by `issuetype.subtask === true` per D-04/D-05 — never name comparison); changing it re-derives the editable createmeta fields. Fetch createmeta via the `['createmeta-issuetypes', projectKey]` → `['createmeta', projectKey, typeId, 'Subtask']` chain (reuse useCreateEditQueries pattern). Footer `"+ Add row"` ghost button with `Plus` icon appends a blank row with assignee defaulted to `@inherit` (D-10). Row reorder via the same dnd-kit overlay pattern. Empty title shows `border-destructive` ring (Save disabled state per Interaction Contract).
    In `Settings.tsx` (4 surgical insertions per PATTERNS.md): (1) add `'subtask-templates'` to the `SettingsSection` union; (2) add `{ id: 'subtask-templates', label: 'Subtask Templates', icon: <LayoutTemplate className="h-4 w-4" /> }` to SECTIONS after the `'workflow'` entry, importing `LayoutTemplate` from lucide-react; (3) `import SubtaskTemplatesSection from './SubtaskTemplatesSection'`; (4) add render block `{activeSection === 'subtask-templates' && <SubtaskTemplatesSection />}` matching the existing pattern.
  </action>
  <verify>
    <automated>cd taskflow && npm run check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Settings.tsx` contains `'subtask-templates'` in union, SECTIONS, import, and render block: `grep -c "subtask-templates\|SubtaskTemplatesSection\|LayoutTemplate" taskflow/src/routes/settings/Settings.tsx` ≥ 4
    - Subtask type filter uses the subtask flag: `grep -c "\.subtask" taskflow/src/routes/settings/SubtaskTemplatesSection.tsx` ≥ 1; no `issuetype.name ===` comparison for subtask detection
    - `"+ Add row"` and `"ROWS"` strings present; new ad-hoc row defaults assignee to `@inherit`
    - `cd taskflow && npm run check` (biome + tsc) is clean
  </acceptance_criteria>
  <done>Inline row editor edits template rows with shared component + subtask-type selection; section registered and reachable in Settings; full suite/typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → persisted template store | template names/rows entered in Settings are written to subtask-templates.json |
| stored subtaskIssueTypeId → createmeta query | a template's stored type id is used to fetch editable fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-05 | Input Validation | template name + row title fields | mitigate | Empty template name restores previous; empty row title shows destructive ring and blocks save (UI-SPEC Interaction Contract) |
| T-80-06 | DoS | stored subtaskIssueTypeId stale after admin change | mitigate | If stored type id absent from current project's subtask types, fall back to first available subtask type (Pitfall 4) — never hard-fail the editor |
| T-80-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase |
</threat_model>

<verification>
- `cd taskflow && npm run check` clean
- Manual (Wave-merge): create 2 templates, reorder, rename, restart app — order/names persist (SUBTPL-01/02 per VALIDATION manual-only)
</verification>

<success_criteria>
- Settings section supports full template CRUD + reorder (SUBTPL-01)
- Templates persist across restart (SUBTPL-02)
- Row editor exposes required title + createmeta-derived optional fields per subtask type (SUBTPL-03)
- Section registered and reachable from settings nav
</success_criteria>

<output>
Create `.planning/phases/80-subtask-templates-and-bulk-creation/80-03-SUMMARY.md` when done
</output>
