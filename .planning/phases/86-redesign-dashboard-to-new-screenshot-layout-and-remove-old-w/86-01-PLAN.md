---
phase: 86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/MyIssuesCard.tsx
  - taskflow/src/routes/dashboard/MyIssuesCard.test.tsx
  - taskflow/src/routes/dashboard/UpcomingReleasesTimeline.tsx
  - taskflow/src/routes/dashboard/UpcomingReleasesTimeline.test.tsx
autonomous: true
requirements: [D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-14]
must_haves:
  truths:
    - "MyIssuesCard shows {done} of {total} done as issue counts (not story points), bucketed by statusCategory"
    - "MyIssuesCard segments always sum to the total (toDo + inProgress + done === total)"
    - "MyIssuesCard shows an empty state (not an error) when 0 issues are assigned to me"
    - "UpcomingReleasesTimeline shows up to 3 unreleased versions WITH a releaseDate, soonest-first"
    - "UpcomingReleasesTimeline shows only what exists when fewer than 3 releases, and 'Tomorrow' for daysUntil===1"
    - "Both cards reuse existing Jira cache keys and add zero new network calls"
  artifacts:
    - path: "taskflow/src/routes/dashboard/MyIssuesCard.tsx"
      provides: "My Issues segmented-bar card (props-only, self-fetching via shared sprint-board cache)"
      contains: "statusCategory"
    - path: "taskflow/src/routes/dashboard/UpcomingReleasesTimeline.tsx"
      provides: "Up-to-3-dot release timeline (props-only, reuses fix-versions cache)"
      contains: "getReleaseTimingLabel"
    - path: "taskflow/src/routes/dashboard/MyIssuesCard.test.tsx"
      provides: "D-03 sum invariant + D-05 empty-state coverage"
    - path: "taskflow/src/routes/dashboard/UpcomingReleasesTimeline.test.tsx"
      provides: "D-06/D-08 fewer-than-3 + empty-state + Tomorrow coverage"
  key_links:
    - from: "MyIssuesCard.tsx"
      to: "['jira-issues','sprint-board',activeJiraProject,storyPointsFieldKey]"
      via: "useQuery queryKey (verbatim reuse)"
      pattern: "jira-issues.*sprint-board"
    - from: "UpcomingReleasesTimeline.tsx"
      to: "['jira-fix-versions', activeJiraProject]"
      via: "useQuery queryKey (verbatim reuse)"
      pattern: "jira-fix-versions"
---

<objective>
Build the two top-row card components of the redesigned dashboard — MyIssuesCard (left) and
UpcomingReleasesTimeline (right) — plus their Wave-0 test scaffolds. Both are props-only (D-16),
self-fetch via existing TanStack Query cache keys (zero new network calls), and own their
loading/error/empty degradation independently (DASH-07).

Purpose: deliver the personal-progress and release-readiness regions of the screenshot layout,
reusing verified data patterns lifted from the components being deleted in Plan 03.

Output: 4 files — 2 components, 2 tests. These compile and test GREEN standalone (not yet wired
into index.tsx; wiring + old-widget deletion happen in Plan 03).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-CONTEXT.md
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-RESEARCH.md
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-PATTERNS.md
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MyIssuesCard — segmented sprint-progress card</name>
  <read_first>
    - taskflow/src/routes/dashboard/SprintHealthSection.tsx (closest analog: props-only shape, shared sprint-board cache key, useDelayedLoading + degradation structure)
    - taskflow/src/routes/dashboard/dashboardMetrics.ts (lift the `filterNonSubtasks` survivor + the statusCategory bucketing pattern at lines 64-84 — copy the derivation inline; do NOT import any non-survivor export)
    - taskflow/src/routes/dashboard/SprintHealthSection.test.tsx (test analog: warm-cache seed via queryClient.setQueryData + makeIssue factory)
    - 86-PATTERNS.md §MyIssuesCard.tsx (exact imports, props interface, derivation, card shell, segmented-bar, legend markup)
    - 86-UI-SPEC.md §Region 2a (typography roles, segment colors, legend, a11y, copy)
  </read_first>
  <behavior>
    - Test (unit): for a mixed-status fixture, toDo + inProgress + done === myNonSubtasks.length (D-03 invariant).
    - Test (render): issue assigned to a different displayName → myNonSubtasks empty → "No issues assigned" empty state, NOT an ErrorState (D-05).
    - Test (render): seeding the SPRINT_BOARD_KEY cache with my issues renders the big "{done}" number and "of {total} done".
  </behavior>
  <action>
    Create `taskflow/src/routes/dashboard/MyIssuesCard.tsx` as a props-only component (D-16): no
    useAuthStore, no readSecret. Props: `jiraBaseUrl`, `jiraToken`, `activeJiraProject`,
    `storyPointsFieldKey`, `jiraUserDisplayName` (all strings). First line `'use no memo';`.

    Fetch with `useQuery` reusing the VERBATIM cache key
    `['jira-issues','sprint-board',activeJiraProject,storyPointsFieldKey]`, queryFn
    `fetchSprintIssues(jiraBaseUrl, jiraToken, activeJiraProject, false, storyPointsFieldKey)` (pass
    `assignedToMe=false` and filter personally in the component), `staleTime: 30_000`,
    `enabled: !!jiraBaseUrl && !!jiraToken && !!activeJiraProject`. Token stays in the queryFn
    closure — NEVER in queryKey. Gate the skeleton through `useDelayedLoading(isLoading)`.

    Derive counts per D-02/D-03/D-04: `filterNonSubtasks(sprintIssues)` (the survivor import from
    `./dashboardMetrics`) then `.filter(i => i.fields.assignee?.displayName === jiraUserDisplayName)`;
    bucket by `i.fields.status.statusCategory?.key` — `'new'`→toDo, `'indeterminate'`→inProgress,
    `'done'`→done; `total = myNonSubtasks.length`. Counts are ISSUE COUNTS, never story points.

    Render the card per UI-SPEC Region 2a: Card with `role="region" aria-label="My issues this sprint"`,
    CardHeader label "MY ISSUES" (`text-xs font-normal text-muted-foreground uppercase tracking-wide`)
    with a `CheckSquare` size-4 `text-chart-1` icon, CardDescription "this sprint". Big-number row:
    `done` in `text-4xl font-semibold text-foreground`, " of {total} done" in
    `text-sm font-normal text-muted-foreground`. Segmented bar: `h-2 rounded-full overflow-hidden
    bg-muted flex` with three proportional flex segments (To Do `bg-muted-foreground/40`, In Progress
    `bg-chart-1`, Done `bg-chart-2`) only rendered when `total > 0`; segment width
    `${(count/total)*100}%`; give the bar `role="img"` with
    `aria-label="Sprint progress: {toDo} to do, {inProgress} in progress, {done} done"`. Legend row:
    gap-4, per item an 8px rounded-sm swatch + "{label} {count}" in Label-role text.

    Degradation (DASH-07): `showSkeleton` → 3 Skeleton lines with `aria-busy="true"`; else `error` →
    `<ErrorState error={error} onRetry={refetch} viewName="My Issues" />`; else `total === 0` →
    `<EmptyState icon={CheckSquare} title="No issues assigned" subtitle="You have no issues assigned in the current sprint." />`;
    else the data view. Empty state is NEVER an error (D-05).
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- src/routes/dashboard/MyIssuesCard</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npm run test -- src/routes/dashboard/MyIssuesCard` exits 0.
    - MyIssuesCard.test.tsx asserts `toDo + inProgress + done === total` for a mixed fixture (D-03).
    - MyIssuesCard.test.tsx asserts "No issues assigned" renders (and no ErrorState) when no issues match my displayName (D-05).
    - `grep -n "statusCategory" taskflow/src/routes/dashboard/MyIssuesCard.tsx` shows the three-bucket mapping; file contains NO `storyPoints`/`computeSp` references.
    - `grep -c "jira-issues.*sprint-board" taskflow/src/routes/dashboard/MyIssuesCard.tsx` returns >=1 (verbatim cache-key reuse).
    - First line of MyIssuesCard.tsx is `'use no memo';`.
  </acceptance_criteria>
  <done>MyIssuesCard renders done/total counts, a 3-segment bar that sums to total, legend, and per-section loading/error/empty states; its test (D-03 invariant + D-05 empty) passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UpcomingReleasesTimeline — up-to-3-dot release timeline</name>
  <read_first>
    - taskflow/src/routes/dashboard/DashboardReleaseCard.tsx (closest analog: lift `getReleaseTimingLabel` VERBATIM, fix-versions + release-issues queries, donePct logic, ascending sort)
    - taskflow/src/routes/dashboard/DashboardReleaseCard.test.tsx (test analog: useQuery/useQueries mock + makeFixVersion factory)
    - 86-PATTERNS.md §UpcomingReleasesTimeline.tsx (exact imports, sort+slice(0,3), useQueries pattern, formatTimingLabel with "Tomorrow" case, timeline markup)
    - 86-UI-SPEC.md §Region 2b (dot/readiness-bar styling, timing-label copy, a11y, empty copy)
    - 86-RESEARCH.md §fetchFixVersions/fetchReleaseIssues (cache keys, donePct, "Tomorrow" render note)
  </read_first>
  <behavior>
    - Test (render): 2 upcoming versions → exactly 2 dots, no placeholder third dot (D-08).
    - Test (render): 0 unreleased versions with a releaseDate → "No upcoming releases" empty state (D-06/D-08).
    - Test (render/unit): a version due tomorrow (daysUntil===1) renders the literal "Tomorrow" label (D-08).
  </behavior>
  <action>
    Create `taskflow/src/routes/dashboard/UpcomingReleasesTimeline.tsx`, props-only (D-16): props
    `jiraBaseUrl`, `jiraToken`, `activeJiraProject`. First line `'use no memo';`.

    Lift `getReleaseTimingLabel(releaseDate, released)` VERBATIM from DashboardReleaseCard (returns
    `'overdue' | 'due-today' | { daysUntil } | null`) — do NOT modify the function. Fetch fix versions
    with `useQuery` reusing the VERBATIM key `['jira-fix-versions', activeJiraProject]`, queryFn
    `fetchFixVersions(jiraBaseUrl, jiraToken, activeJiraProject)`, `staleTime: 5*60_000`,
    `enabled: !!jiraBaseUrl && !!jiraToken && !!activeJiraProject`.

    Compute `upcomingVersions` per D-06: `.filter(v => !v.released && !!v.releaseDate)` then
    `.sort((a,b) => (a.releaseDate ?? '').localeCompare(b.releaseDate ?? ''))` (ASCENDING — soonest
    first, opposite of ReleasesTab) then `.slice(0, 3)`. Fetch per-release issues via `useQueries`,
    one query per upcoming version reusing the VERBATIM key
    `['jira-release-issues', activeJiraProject, v.name]`, queryFn
    `fetchReleaseIssues(jiraBaseUrl, jiraToken, activeJiraProject, v.name)`.

    Per release compute `donePct` per D-07: `doneCount = issues.filter(i =>
    i.fields.status.statusCategory?.key === 'done').length`, `donePct = totalCount > 0 ?
    Math.min(100, Math.round((doneCount/totalCount)*100)) : 0` (the `Math.min(100, …)` clamp is the
    STRIDE mitigation T-86-02). Render the relative-due label via a `formatTimingLabel` render helper
    that maps: `due-today`→"Today", `overdue`→"overdue" (amber `text-amber-600 dark:text-amber-400`),
    `{daysUntil:1}`→"Tomorrow", `{daysUntil:n}`→"in {n} days" (D-08).

    Render per UI-SPEC Region 2b: Card `role="region" aria-label="Upcoming releases"`, CardHeader
    "UPCOMING RELEASES" with a `Calendar` size-4 `text-chart-1` icon, CardDescription
    "next 3 with due dates". A horizontal timeline: an absolutely-positioned 1px `bg-border` track line
    behind up-to-3 dots laid out with `justify-between`. Each dot is a 10px circle (`size-2.5
    ring-2 ring-card`), `bg-chart-2` when donePct>=80 else `bg-muted-foreground`; below it stack the
    truncated release name (`text-xs text-foreground truncate max-w-[96px]`), the relative-due label,
    a `bg-muted` track with a `bg-chart-2` fill at `{donePct}%` width (height `h-1`, width 80px), and
    "{donePct}% ready".

    Degradation (DASH-07): `showSkeleton = useDelayedLoading(isLoading)` → 3 pulse rows; else `error`
    → `<ErrorState error={error} onRetry={refetch} viewName="Upcoming Releases" />`; else
    `upcomingVersions.length === 0` →
    `<EmptyState icon={Calendar} title="No upcoming releases" subtitle="No unreleased versions with a due date were found." />`;
    else the timeline.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- src/routes/dashboard/UpcomingReleasesTimeline</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npm run test -- src/routes/dashboard/UpcomingReleasesTimeline` exits 0.
    - Test asserts exactly 2 dots render for a 2-version fixture and no placeholder (D-08).
    - Test asserts "No upcoming releases" renders for an empty/no-due-date fixture (D-06/D-08).
    - Test asserts the literal text "Tomorrow" renders for a daysUntil===1 fixture (D-08).
    - `grep -c "jira-fix-versions" taskflow/src/routes/dashboard/UpcomingReleasesTimeline.tsx` returns >=1.
    - `grep -n "localeCompare" taskflow/src/routes/dashboard/UpcomingReleasesTimeline.tsx` shows the ascending sort; `slice(0, 3)` present.
    - First line is `'use no memo';`.
  </acceptance_criteria>
  <done>UpcomingReleasesTimeline renders up to 3 soonest-due unreleased versions with readiness bars and correct relative-due labels (including "Tomorrow"), with loading/error/empty states; its test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jira API → React render | Untrusted strings (version names, status keys, issue counts) cross into JSX. Read-only; no new endpoint, no mutation, no auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Information Disclosure (XSS on render) | MyIssuesCard / UpcomingReleasesTimeline | mitigate | All Jira strings (version name, counts, labels) render as plain text children in JSX — NO `dangerouslySetInnerHTML`, no raw HTML. Acceptance: `grep -c dangerouslySetInnerHTML` on both new files returns 0. |
| T-86-02 | Tampering (out-of-range donePct from bad API data) | UpcomingReleasesTimeline | mitigate | `donePct = totalCount > 0 ? Math.min(100, Math.round(...)) : 0` clamps to 0–100 (division-by-zero guarded). |
| T-86-03 | Tampering (unknown statusCategory.key) | MyIssuesCard | accept | Unknown keys fall through all three buckets (counted in none); `?.key` optional-chains so render never throws. Low risk — read-only display. |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | Phase 86 installs ZERO new packages (RESEARCH §Package Legitimacy Audit: SKIPPED). No supply-chain surface. |

No `high` severity threats — read-only dashboard, no new API surface, no new secrets.
</threat_model>

<verification>
- `cd taskflow && npm run test -- src/routes/dashboard/MyIssuesCard src/routes/dashboard/UpcomingReleasesTimeline` GREEN.
- `cd taskflow && npm run check` GREEN (biome + tsc) — new files compile with no unreferenced exports introduced.
- `grep -c dangerouslySetInnerHTML taskflow/src/routes/dashboard/MyIssuesCard.tsx taskflow/src/routes/dashboard/UpcomingReleasesTimeline.tsx` returns 0 for both (T-86-01).
</verification>

<success_criteria>
- MyIssuesCard: issue-count buckets sum to total (D-03), 0-issues empty state (D-05), shared sprint-board cache key (D-05).
- UpcomingReleasesTimeline: up-to-3 soonest-due versions with due dates (D-06), donePct readiness (D-07), fewer-than-3 + Tomorrow + empty edges (D-08), shared fix-versions cache key.
- Both files start with `'use no memo';`, are props-only, and add zero new network calls.
- npm run check GREEN; both test files pass.
</success_criteria>

<output>
Create `.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-01-SUMMARY.md` when done
</output>
