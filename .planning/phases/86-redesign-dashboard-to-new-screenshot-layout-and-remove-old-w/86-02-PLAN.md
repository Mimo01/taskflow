---
phase: 86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/HoursCommitsChart.tsx
  - taskflow/src/routes/dashboard/HoursCommitsChart.test.tsx
autonomous: true
requirements: [D-09, D-10, D-11, D-12, D-14]
must_haves:
  truths:
    - "HoursCommitsChart renders a rolling 7 calendar days ending today (weekday labels, today highlighted)"
    - "Hours (blue, left axis) and commits (green, right axis) render as grouped dual-axis bars"
    - "Hours come from Tempo fetchWorklogs, commits from GitLab fetchUserCommits — no new endpoint"
    - "An all-zero connected week renders 7 flat bars with '0h'/'0' labels, NOT an empty state"
    - "Tempo-off renders a graceful empty state (not an error); commits stay 0 when GitLab unconfigured"
    - "Date bucketing uses local-calendar (en-CA) strings, never toISOString UTC shift"
  artifacts:
    - path: "taskflow/src/routes/dashboard/HoursCommitsChart.tsx"
      provides: "Full-width dual-axis ComposedChart (props-only; Tempo + 7×GitLab queries)"
      contains: "ComposedChart"
    - path: "taskflow/src/routes/dashboard/HoursCommitsChart.test.tsx"
      provides: "D-12 all-zero-week + Tempo-off coverage"
  key_links:
    - from: "HoursCommitsChart.tsx"
      to: "fetchWorklogs"
      via: "useQuery key ['dashboard','tempo-7day',jiraBaseUrl,todayDate,jiraUsername]"
      pattern: "tempo-7day"
    - from: "HoursCommitsChart.tsx"
      to: "fetchUserCommits"
      via: "useQueries × 7, key ['standup','commits',...,day,...]"
      pattern: "standup.*commits"
---

<objective>
Build the full-width bottom chart of the redesigned dashboard: HoursCommitsChart — a dual-Y-axis
grouped-bar ComposedChart showing hours logged (blue, Tempo) and commits (green, GitLab) for a
rolling 7 calendar days ending today. Props-only (D-16); owns its Tempo query + 7 parallel GitLab
commit queries; follows the locked Phase 81 charting contract (D-14).

Purpose: deliver the activity-trend region of the screenshot layout with honest zero-states, no new
API surface, and the net-new dual-axis Recharts shape this codebase has no prior analog for.

Output: 2 files — the chart component + its test. Compiles and tests GREEN standalone; wired into
index.tsx in Plan 03. Visual correctness in real Tauri WebKit is verified separately in Plan 04.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-CONTEXT.md
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-RESEARCH.md
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-PATTERNS.md
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HoursCommitsChart data layer — rolling-7 buckets + dual queries</name>
  <read_first>
    - taskflow/src/routes/dashboard/WeeklyTrendChart.tsx (primary analog: fetchWorklogs query, local-date helpers, tempo-off empty state, buildWeekBuckets bucketing)
    - taskflow/src/routes/dashboard/ActivityStrip.tsx (secondary analog: fetchUserCommits per-day query + the exact ['standup','commits',...] cache key + enabled guard)
    - taskflow/src/routes/dashboard/dashboardMetrics.ts (copy `addDays` (lines 159-163) and reuse the `formatHoursMinutes` survivor; DO NOT import non-survivor exports — `buildWeekBuckets` is being deleted)
    - taskflow/src/services/tempo/worklogs.ts + taskflow/src/services/tempo/types.ts (fetchWorklogs signature; TempoWorklog.dateStarted is pre-sliced YYYY-MM-DD)
    - taskflow/src/services/gitlab.ts (fetchUserCommits signature — single-date param, internal UTC conversion)
    - 86-PATTERNS.md §HoursCommitsChart.tsx (imports, props interface, getTodayDate/addDays/getRolling7Days, both queries, buildRolling7Buckets helper)
    - 86-RESEARCH.md §Rolling-7 Bucket Logic + §fetchUserCommits (per-day call pattern, UTC-shift trap)
  </read_first>
  <behavior>
    - Test (unit): buildRolling7Buckets returns 7 buckets, last bucket isToday===true, bucket days are the 7 local-calendar dates ending todayDate (D-09).
    - Test (unit): worklogs are bucketed by string equality on dateStarted (a worklog on day N adds its hours to bucket N only); commitsByDay map populates the commits field (D-11).
    - Test (unit): an all-zero input (no worklogs, empty commits map) yields 7 buckets all with hours:0 commits:0 (feeds D-12).
  </behavior>
  <action>
    Create `taskflow/src/routes/dashboard/HoursCommitsChart.tsx`, props-only (D-16). First line
    `'use no memo';`. Props interface per PATTERNS: `jiraBaseUrl`, `jiraToken`, `jiraUsername`,
    `tempoEnabled`, `gitlabBaseUrl`, `gitlabToken`, `activeGitlabProject`, `gitlabUsername`,
    `gitlabName`, `gitlabEmail`.

    Add local-date helpers (copy verbatim from PATTERNS / WeeklyTrendChart): `getTodayDate()` returns
    `new Date().toLocaleDateString('en-CA')` (local YYYY-MM-DD — NEVER toISOString, the UTC-shift trap
    per D-11); `addDays(dateStr, n)` via `Date.UTC` arithmetic (copy from dashboardMetrics.ts:159-163,
    inline here since that helper is not a survivor export); `getRolling7Days(todayDate)` →
    `Array.from({length:7}, (_,i) => addDays(todayDate, i-6))`.

    Add the net-new `buildRolling7Buckets(worklogs, commitsByDay: Map<string,number>, todayDate)`
    helper (adapts buildWeekBuckets): build 7 DayBucket objects `{day, label, isToday, hours, commits}`
    where `day = addDays(todayDate, i-6)`, `label` = `new Date(`${day}T12:00:00`).toLocaleDateString('en-US',{weekday:'short'})`
    (noon to dodge DST), `isToday = day === todayDate`, `commits = commitsByDay.get(day) ?? 0`; then
    sum hours by `worklogs.find(b.day === wl.dateStarted)` string equality, `hours +=
    wl.timeSpentSeconds/3600`.

    Wire the Tempo query: `useQuery` key `['dashboard','tempo-7day',jiraBaseUrl,todayDate,jiraUsername]`
    (todayDate anchor auto-rotates at midnight), queryFn `fetchWorklogs(jiraBaseUrl, jiraToken,
    [jiraUsername], addDays(todayDate,-6), todayDate)`, `enabled: ... && tempoEnabled`. Wire commits via
    `useQueries` over `getRolling7Days(todayDate)`, each query reusing the VERBATIM ActivityStrip key
    `['standup','commits',gitlabBaseUrl,activeGitlabProject,day,gitlabUsername||gitlabName||'']`,
    queryFn calling `fetchUserCommits(gitlabBaseUrl, gitlabToken, activeGitlabProject, day,
    [gitlabUsername??''], [gitlabName??''], [gitlabEmail??''])` (pass `day` as the single-date param —
    fetchUserCommits does the UTC conversion internally; do NOT bucket by authored_date),
    `enabled: !!gitlabBaseUrl && !!gitlabToken && !!activeGitlabProject && (!!gitlabUsername || !!gitlabName)`.
    Tokens stay in queryFn closures, NEVER in queryKey. Build `commitsByDay` Map from the 7 results
    (`result.data?.length ?? 0` per day), then `dayBuckets = buildRolling7Buckets(...)`.

    Export `buildRolling7Buckets` (named) so the test can unit-test it directly. Render is added in
    Task 2.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- src/routes/dashboard/HoursCommitsChart</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npm run test -- src/routes/dashboard/HoursCommitsChart` exits 0 (unit tests for buildRolling7Buckets present and passing).
    - Test asserts 7 buckets, last bucket isToday===true, and an all-zero input yields all hours:0/commits:0 (feeds D-12).
    - `grep -c "tempo-7day" taskflow/src/routes/dashboard/HoursCommitsChart.tsx` returns >=1.
    - `grep -c "standup" taskflow/src/routes/dashboard/HoursCommitsChart.tsx` returns >=1 (reused commits cache key).
    - `grep -c "toISOString" taskflow/src/routes/dashboard/HoursCommitsChart.tsx` returns 0 inside the date-bucketing helpers (en-CA only; D-11). (`addDays` may use toISOString purely to format a Date.UTC result, matching dashboardMetrics.ts — that is acceptable; bucketing of worklogs/commits must use en-CA day strings.)
    - First line is `'use no memo';`.
  </acceptance_criteria>
  <done>The data layer fetches Tempo hours + 7 days of GitLab commits via existing cache keys and produces 7 local-calendar day buckets ending today; buildRolling7Buckets unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HoursCommitsChart render — dual-axis ComposedChart + states</name>
  <read_first>
    - taskflow/src/routes/dashboard/WeeklyTrendChart.tsx (Cell today-stroke highlight, LabelList pattern, ChartContainer + responsive, explicit-height div, tempo-off EmptyState lines 106-123)
    - taskflow/src/components/chart-wrapper.tsx + taskflow/src/components/ui/chart.tsx (ChartContainer/ChartConfig API)
    - taskflow/src/routes/dashboard/WeeklyTrendChart.test.tsx (test analog for chart render assertions in jsdom)
    - 86-PATTERNS.md §HoursCommitsChart.tsx (ComposedChart body, TodayAwareTick foreignObject, header-right totals, chartConfig)
    - 86-RESEARCH.md §Recharts v3 Dual-Axis Grouped Bars (yAxisId pitfalls, minPointSize on commits, ReferenceLine, foreignObject A2 fallback)
    - 86-UI-SPEC.md §Region 3 (axes, reference line, bar labels, today pill, header totals, all states, a11y, copy)
  </read_first>
  <behavior>
    - Test (render): tempoEnabled=false → "Tempo not connected" EmptyState, no chart (D-12).
    - Test (render): all-zero connected week (tempoEnabled=true, no worklogs, no commits) → ChartContainer present, NO EmptyState ("zero is valid data", D-12).
    - Test (render): chart renders both Bar series (hours + commits) with their dataKeys present.
  </behavior>
  <action>
    Add the render to `HoursCommitsChart.tsx` per UI-SPEC Region 3. `chartConfig = { hours: {label:'Hours logged', color:'var(--chart-1)'}, commits: {label:'Commits', color:'var(--chart-2)'} } satisfies ChartConfig`.

    Tempo-off branch (lift WeeklyTrendChart 106-123): when `!tempoEnabled`, return the Card with an
    `<EmptyState icon={Timer} title="Tempo not connected" subtitle="Connect Tempo in Settings to see logged hours." />`
    inside an explicit-height div (D-12: empty state for tempo-off only — NEVER for an all-zero
    connected week). Loading via `useDelayedLoading` → Skeleton; error → `<ErrorState ... onRetry={refetch} viewName="Hours & Commits" />`.

    Card shell: `role="region" aria-label="Past 7 days hours and commits"`. CardHeader uses a
    `flex items-center justify-between` row: CardTitle "PAST 7 DAYS · HOURS & COMMITS PER DAY"
    (`text-xs font-normal text-muted-foreground uppercase tracking-wide`) on the left; header-right
    totals on the right — `{formatHoursMinutes(totalHours)} h logged` colored `var(--chart-1)` and
    `{totalCommits} commits` colored `var(--chart-2)` (Body role).

    Chart body: outer `<div style={{ height: 280 }} className="w-full">` (explicit-height WebKit 0×0
    guard, D-14). `<ChartContainer config={chartConfig} className="h-full w-full" aria-label="Hours and commits per day bar chart">`
    wrapping `<ComposedChart data={dayBuckets} responsive margin={{top:24,right:40,left:0,bottom:0}}>`
    (the `responsive` prop replaces ResponsiveContainer per D-14; verify it works on ComposedChart —
    Assumption A1). Axes: `<XAxis dataKey="label" tick={<TodayAwareTick todayLabel={todayLabel} />} />`,
    `<YAxis yAxisId="hours" orientation="left" tickFormatter={v => `${v}h`} />`,
    `<YAxis yAxisId="commits" orientation="right" />`. `<ReferenceLine yAxisId="hours" y={maxHours}
    strokeDasharray="4 4" stroke="var(--muted-foreground)" />` where `maxHours = Math.max(...dayBuckets.map(b=>b.hours))`.

    Hours `<Bar yAxisId="hours" dataKey="hours" fill="var(--chart-1)" radius={[4,4,0,0]}
    isAnimationActive={false}>` with per-bucket `<Cell>` carrying `stroke="var(--foreground)"
    strokeWidth={2}` when `b.isToday` and a `<LabelList dataKey="hours" position="top">` whose
    formatter returns `formatHoursMinutes(n)` for n>0 else `'0h'` (D-12). Commits `<Bar yAxisId="commits"
    dataKey="commits" fill="var(--chart-2)" radius={[4,4,0,0]} isAnimationActive={false}
    minPointSize={1}>` (minPointSize so 0/flat bars stay visible) with matching today `<Cell>` strokes
    and a `<LabelList dataKey="commits" position="bottom">` formatter returning `String(n)` (D-12 "0").
    BOTH `<Bar>` and BOTH `<YAxis>` MUST carry matching `yAxisId` strings (RESEARCH pitfall) and
    `isAnimationActive={false}` (D-14).

    `TodayAwareTick`: render the today weekday label as a pill via `<foreignObject>` (`bg-foreground
    text-background rounded-full px-2 py-0.5 text-xs`); other ticks as plain `<text fill="var(--muted-foreground)">`.
    Note in a code comment that this is Assumption A2 (Tauri WebKit foreignObject) verified in Plan 04;
    SVG `<text>`+`<rect>` is the documented fallback.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- src/routes/dashboard/HoursCommitsChart</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npm run test -- src/routes/dashboard/HoursCommitsChart` exits 0.
    - Test asserts tempoEnabled=false renders "Tempo not connected" and no chart (D-12).
    - Test asserts an all-zero connected week renders the chart container and NO EmptyState (D-12).
    - `grep -c "ComposedChart" taskflow/src/routes/dashboard/HoursCommitsChart.tsx` returns >=1.
    - `grep -c "isAnimationActive={false}" taskflow/src/routes/dashboard/HoursCommitsChart.tsx` returns >=2 (both bars; D-14).
    - `grep -c "yAxisId" taskflow/src/routes/dashboard/HoursCommitsChart.tsx` returns >=4 (2 YAxis + 2 Bar + ReferenceLine).
    - `cd taskflow && npm run check` exits 0.
  </acceptance_criteria>
  <done>HoursCommitsChart renders dual-axis grouped bars (hours left/blue, commits right/green), today highlight + pill, dashed max gridline, header totals, and correct all-zero vs tempo-off states; tests pass; npm run check GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tempo/GitLab API → React render | Untrusted strings (commit counts, worklog hours) cross into chart labels. Read-only; reuses existing authenticated queries; no new endpoint or secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04 | Information Disclosure (XSS on render) | HoursCommitsChart | mitigate | All values render as plain text in SVG `<text>`/`<LabelList>`/JSX children — no `dangerouslySetInnerHTML`, no raw HTML in the foreignObject pill. Acceptance: `grep -c dangerouslySetInnerHTML` returns 0. |
| T-86-05 | Information Disclosure (secrets in cache key) | HoursCommitsChart | mitigate | PAT tokens live only inside queryFn closures; queryKeys contain baseUrl/project/day/username only — never a token. Acceptance: no `token`/`pat`/`secret` substring inside any queryKey array. |
| T-86-06 | Denial of Service (7 parallel GitLab calls rate-limit) | HoursCommitsChart | accept | 7 bounded parallel useQueries, each warm-cache-shared with ActivityStrip's daily entry (RESEARCH Open Q1). Monitored at Plan 04 UAT; range-query fallback documented if triggered. Read-only, low risk. |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH §Package Legitimacy Audit: SKIPPED). |

No `high` severity threats — read-only, reuses existing authenticated queries, no new API surface.
</threat_model>

<verification>
- `cd taskflow && npm run test -- src/routes/dashboard/HoursCommitsChart` GREEN.
- `cd taskflow && npm run check` GREEN (biome + tsc).
- `grep -c dangerouslySetInnerHTML taskflow/src/routes/dashboard/HoursCommitsChart.tsx` returns 0 (T-86-04).
- No token/pat/secret substring appears inside any `queryKey: [...]` array in the file (T-86-05).
</verification>

<success_criteria>
- Rolling 7 calendar days ending today, weekday labels, today highlighted (D-09).
- Dual Y-axis grouped bars: hours left/blue, commits right/green, header totals, dashed max gridline (D-10).
- Hours from fetchWorklogs, commits from 7× fetchUserCommits — no new endpoint (D-11).
- All-zero connected week → flat 0-bars with "0h"/"0" labels, NOT empty state; tempo-off → graceful empty state (D-12).
- Charting contract honored: 'use no memo', responsive prop, explicit-height div, isAnimationActive={false}, var(--chart-N) (D-14).
- npm run check GREEN; tests pass.
</success_criteria>

<output>
Create `.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-02-SUMMARY.md` when done
</output>
