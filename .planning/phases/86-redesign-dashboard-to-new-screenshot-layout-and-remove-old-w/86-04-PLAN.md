---
phase: 86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w
plan: 04
type: execute
wave: 3
depends_on: ["86-03"]
files_modified: []
autonomous: false
requirements: [D-10, D-14]
must_haves:
  truths:
    - "The dual-axis ComposedChart renders correctly in the real Tauri WebKit runtime (not just jsdom)"
    - "The redesigned dashboard visually matches the approved screenshots across all 3 regions"
  artifacts: []
  key_links:
    - from: "HoursCommitsChart.tsx (foreignObject today pill, responsive ComposedChart)"
      to: "Tauri WebKit render"
      via: "live app launch — Assumptions A1/A2 verification"
      pattern: "ComposedChart"
---

<objective>
Human-verify the parts of the redesign that jsdom cannot assert: the net-new dual-axis grouped-bar
chart and the foreignObject today-pill rendering in the real Tauri WebKit runtime, plus overall visual
fidelity to the approved screenshots. This closes the manual-only items in 86-VALIDATION.md and
resolves Recharts Assumptions A1 (`responsive` on ComposedChart) and A2 (foreignObject in WebKit).

Purpose: D-10/D-14 acceptance cannot be proven in unit tests — Recharts + WebKit rendering and pixel
fidelity require a human looking at the running app.

Output: human sign-off (or a defect list to feed gap-closure).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-DESIGN-INTENT.md
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-UI-SPEC.md
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT — dual-axis chart + visual fidelity in Tauri WebKit</name>
  <action>HUMAN CHECKPOINT — no code changes. Present the running app and the verification steps below to the user; do not auto-approve (this is a blocking human-verify gate, D-10/D-14 + Assumptions A1/A2). Record the user's verdict in the SUMMARY.</action>
  <what-built>
    The redesigned dashboard (3 regions: hero greeting + sprint-day subline, MY ISSUES segmented-bar
    card, UPCOMING RELEASES up-to-3-dot timeline, full-width PAST 7 DAYS dual-axis hours/commits chart),
    all old widgets removed. Automated tests + npm run check are already GREEN; this verifies the
    WebKit-only rendering and screenshot fidelity that jsdom cannot.
  </what-built>
  <how-to-verify>
    1. Launch the desktop app in dev: `cd taskflow && npm run tauri dev` (wait for the window).
    2. Open the Dashboard. Confirm the hero: time-of-day greeting + first name, and a subline reading
       "{Weekday}, {D} {Month} {YYYY} · Sprint day {N} of {M}" (the sprint clause is absent if there is
       no active sprint — that is correct, not a bug).
    3. MY ISSUES card: a big "{done}" number with " of {total} done"; a 3-segment bar (To Do / In
       Progress / Done) whose widths look proportional and sum to full; a legend with matching counts.
    4. UPCOMING RELEASES card: up to 3 dots on a horizontal track, each with name, relative-due label
       ("Today"/"Tomorrow"/"in N days"/"overdue"), a thin readiness bar, and "{n}% ready".
    5. PAST 7 DAYS chart (the critical WebKit check):
       - Blue hours bars on the LEFT axis and green commits bars on the RIGHT axis render SIDE-BY-SIDE
         per day (bars are NOT missing/collapsed — Assumption A1: `responsive` on ComposedChart works).
       - The X-axis shows 7 weekday labels (Tue…Mon) and TODAY is shown as a pill
         (Assumption A2: foreignObject renders in WebKit; if the pill is broken/blank, that is a defect
         to report — fallback is SVG text+rect).
       - Today's column bars carry the highlight stroke.
       - A dashed gridline sits at the max hours value.
       - Days with 0 hours / 0 commits render as flat bars WITH "0h" / "0" labels (not omitted).
       - Header-right totals show "{h} h logged" (blue) and "{n} commits" (green).
       - The chart does NOT collapse to 0×0 height (explicit-height guard holds).
    6. Toggle a degraded path if convenient: with Tempo disabled the chart shows "Tempo not connected"
       (not an error); other cards keep working independently.
    7. Compare the full layout against the two approved screenshots referenced in 86-DESIGN-INTENT.md.
  </how-to-verify>
  <verify><human-check>User confirms WebKit chart render + screenshot fidelity (blocking).</human-check></verify>
  <done>User has typed "approved" (or a defect list has been captured to feed gap-closure).</done>
  <resume-signal>Type "approved" if the chart renders correctly in WebKit and the layout matches the screenshots, or describe the defects (broken today pill, missing/overlapping bars, 0×0 collapse, label clipping, layout mismatch) to feed gap-closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No code changes in this plan — verification only. The trust boundaries and mitigations established in
Plans 01–03 (read-only Jira/Tempo/GitLab render, plain-text rendering, no new API surface, no new
secrets) are unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-10 | (verification only) | dashboard render | accept | No new code; confirms the Plan 01–03 mitigations hold in the live runtime. No new threat introduced. |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages — verification only. |

No `high` severity threats — manual verification, no code changes.
</threat_model>

<verification>
- Human confirms (or rejects) the chart renders correctly in Tauri WebKit and the layout matches the screenshots.
- If rejected, the defect list feeds `/gsd-plan-phase 86 --gaps`.
</verification>

<success_criteria>
- Dual-axis grouped bars render correctly side-by-side in WebKit (D-10, Assumption A1 resolved).
- Today pill renders in WebKit or is replaced by the documented fallback (D-14, Assumption A2 resolved).
- 0-value days show flat bars with "0h"/"0" labels; dashed max gridline present; no 0×0 collapse.
- Layout matches the approved screenshots; human sign-off recorded.
</success_criteria>

<output>
Create `.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-04-SUMMARY.md` when done
</output>
