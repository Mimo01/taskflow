---
phase: 86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w
plan: 03
type: execute
wave: 2
depends_on: ["86-01", "86-02"]
files_modified:
  - taskflow/src/routes/dashboard/index.tsx
  - taskflow/src/routes/dashboard/index.test.tsx
  - taskflow/src/routes/dashboard/dashboardMetrics.ts
  - taskflow/src/routes/dashboard/dashboardMetrics.test.ts
  - taskflow/src/routes/dashboard/widget-removal.guard.test.ts
  - taskflow/src/routes/dashboard/StatTile.tsx
  - taskflow/src/routes/dashboard/StatTile.test.tsx
  - taskflow/src/routes/dashboard/SprintHealthSection.tsx
  - taskflow/src/routes/dashboard/SprintHealthSection.test.tsx
  - taskflow/src/routes/dashboard/WeeklyTrendChart.tsx
  - taskflow/src/routes/dashboard/WeeklyTrendChart.test.tsx
  - taskflow/src/routes/dashboard/ActivityStrip.tsx
  - taskflow/src/routes/dashboard/ActivityStrip.test.tsx
  - taskflow/src/routes/dashboard/DashboardReleaseCard.tsx
  - taskflow/src/routes/dashboard/DashboardReleaseCard.test.tsx
  - taskflow/src/routes/dashboard/VelocityChart.tsx
  - taskflow/src/routes/dashboard/BurndownChart.tsx
  - taskflow/src/services/jira.ts
  - taskflow/src/services/jira/greenhopper/burndown.ts
  - taskflow/src/lib/concurrency.ts
  - taskflow/src/components/app/Sidebar.tsx
autonomous: true
requirements: [D-01, D-13, D-14]
must_haves:
  truths:
    - "The Dashboard renders exactly 3 regions: hero greeting, top-row (MyIssues + UpcomingReleases), full-width HoursCommitsChart — nothing else"
    - "The hero subline shows date + 'Sprint day {elapsed} of {total}', and hides the sprint clause when there is no active sprint"
    - "All 7 old widget files (StatTile, SprintHealthSection, WeeklyTrendChart, ActivityStrip, DashboardReleaseCard, VelocityChart, BurndownChart) plus their tests no longer exist"
    - "Orphaned service helpers (fetchClosedSprints, fetchSprintIssuesBySprintId, fetchBurndown, getVelocityLimit) are deleted; dashboardMetrics keeps only filterNonSubtasks + formatHoursMinutes"
    - "npm run check is GREEN (biome + tsc, no unreferenced exports) and widget-removal.guard passes"
  artifacts:
    - path: "taskflow/src/routes/dashboard/index.tsx"
      provides: "3-region dashboard composition + hero + sprint-day subline"
      contains: "HoursCommitsChart"
    - path: "taskflow/src/routes/dashboard/widget-removal.guard.test.ts"
      provides: "Phase 86 absence assertions for 11 deleted files + import-absence on index.tsx"
      contains: "Phase 86"
    - path: "taskflow/src/routes/dashboard/dashboardMetrics.ts"
      provides: "Slimmed module — only filterNonSubtasks + formatHoursMinutes survive"
  key_links:
    - from: "index.tsx"
      to: "MyIssuesCard / UpcomingReleasesTimeline / HoursCommitsChart"
      via: "JSX composition in the 3-region layout"
      pattern: "MyIssuesCard|UpcomingReleasesTimeline|HoursCommitsChart"
    - from: "index.tsx"
      to: "activeSprint.startDate/endDate"
      via: "sprint-day subline calc (hidden when null)"
      pattern: "Sprint day"
---

<objective>
Land the redesign coherently in one wave: rewrite `index.tsx` to compose ONLY the 3 new components
(hero + MyIssues + UpcomingReleases + HoursCommitsChart) with the D-13 sprint-day subline; delete all
7 old widget files + their tests; remove the now-orphaned service helpers; slim `dashboardMetrics.ts`
to its 2 survivors; prune `dashboardMetrics.test.ts`; extend `widget-removal.guard.test.ts`; and
update the Sidebar prefetch comment. This is sequenced so `npm run check` is GREEN at the end — the
rewrite removes the old imports in the same commit the files are deleted, so there is never a dangling
import or unreferenced export.

Purpose: D-01 full clean slate + D-13 hero, with zero orphaned code, verified two ways (guard test +
npm run check).

Output: the live 3-region dashboard, a slimmed metrics module, an extended deletion guard, and a
GREEN check.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-CONTEXT.md
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-RESEARCH.md
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-PATTERNS.md
@.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-UI-SPEC.md
@taskflow/src/routes/dashboard/86-01-SUMMARY.md
@taskflow/src/routes/dashboard/86-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite index.tsx to the 3-region layout + sprint-day subline</name>
  <read_first>
    - taskflow/src/routes/dashboard/index.tsx (the file being rewritten — KEEP getTimeGreeting, first-name parser, token-load effects, sprint-board query, active-sprint query, hero shell; REMOVE all old widget imports + their JSX + tile/SP derivations)
    - taskflow/src/routes/dashboard/MyIssuesCard.tsx (Plan 01 — its props interface to feed)
    - taskflow/src/routes/dashboard/UpcomingReleasesTimeline.tsx (Plan 01 — props)
    - taskflow/src/routes/dashboard/HoursCommitsChart.tsx (Plan 02 — props)
    - taskflow/src/routes/dashboard/index.test.tsx (rewrite to test the new 3-region layout + D-13 subline)
    - 86-PATTERNS.md §index.tsx (retain-verbatim blocks, sprint-day subline calc, hero shell text-4xl, 3-region composition, the exact REMOVE list)
    - 86-UI-SPEC.md §Region 1 (hero typography, subline format/states, copy)
    - 86-RESEARCH.md §fetchActiveSprint (sprint-day calc, hide-when-null)
  </read_first>
  <behavior>
    - Test (render): with an active sprint (startDate/endDate set) the hero subline contains "Sprint day {n} of {m}" (D-13).
    - Test (render): with activeSprint=null the subline shows the date and NO "Sprint day" clause (D-13).
    - Test (render): the dashboard renders MyIssuesCard, UpcomingReleasesTimeline, and HoursCommitsChart regions and none of the 7 old widgets.
  </behavior>
  <action>
    Rewrite `taskflow/src/routes/dashboard/index.tsx` (D-01). KEEP verbatim: `'use no memo';`,
    `getTimeGreeting()`, the first-name parser, both `readSecret` token-load effects (jira-pat,
    gitlab-pat), the sprint-board `useQuery`
    (`['jira-issues','sprint-board',activeJiraProject,storyPointsFieldKey]`), and the active-sprint
    `useQuery` (`['jira-active-sprint',activeJiraProject,jiraBaseUrl,boardId]`). REMOVE every import and
    JSX usage of `StatTile`, `SprintHealthSection`, `WeeklyTrendChart`, `ActivityStrip`,
    `DashboardReleaseCard`, `VelocityChart`, `BurndownChart`, plus `computePersonalTileCounts`,
    `computeSpDone`, and the now-unused lucide icons `Activity`, `CheckCircle2`, `Clock`, `Zap`, and the
    tile/SP derivations (`tileCounts`, `spDone`, `showTileSkeleton`).

    Add the D-13 sprint-day subline: `today = new Date().toLocaleDateString('en-CA')`; if
    `activeSprint?.startDate && activeSprint?.endDate`, compute `elapsed` and `total` via `Date.UTC`
    day arithmetic on the `.slice(0,10)` split dates (`+1` inclusive, per RESEARCH/UI-SPEC), producing
    `sprintClause = " · Sprint day {elapsed} of {total}"`; otherwise `sprintClause = ''` (hide clause —
    NEVER render "Sprint day" when no active sprint). Hero shell: `<h1 className="text-4xl font-semibold
    text-foreground">{timeGreeting} {firstName ?? 'there'}</h1>` and a subline `<p className="text-xs
    text-muted-foreground mt-1">` showing
    `new Date().toLocaleDateString('en-GB',{weekday:'long',day:'numeric',month:'long',year:'numeric'}){sprintClause}`.

    Compose the body (replacing the old grid entirely): a `grid grid-cols-1 lg:grid-cols-2 gap-4` top
    row with `<MyIssuesCard .../>` left and `<UpcomingReleasesTimeline .../>` right, then a full-width
    `<HoursCommitsChart .../>` below. Feed each component the props it declared in Plans 01/02 from the
    values index.tsx already loads (jiraBaseUrl, jiraToken, activeJiraProject, storyPointsFieldKey,
    jiraUserDisplayName, jiraUsername, tempoEnabled, gitlab* values). Keep the index.tsx-owned single PAT
    load (D-16) — the cards receive tokens as props.

    Rewrite `index.test.tsx` to cover the 3 behaviors above (mock the 3 child components or seed caches;
    assert subline with/without active sprint).
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- src/routes/dashboard/index</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npm run test -- src/routes/dashboard/index` exits 0.
    - index.test.tsx asserts "Sprint day" appears with an active sprint and is ABSENT when activeSprint=null (D-13).
    - `grep -E "MyIssuesCard|UpcomingReleasesTimeline|HoursCommitsChart" taskflow/src/routes/dashboard/index.tsx` matches all three.
    - `cat taskflow/src/routes/dashboard/index.tsx | grep -v '^\s*//' | grep -v '^\s*\*' | grep -Ec "StatTile|SprintHealthSection|WeeklyTrendChart|ActivityStrip|DashboardReleaseCard|VelocityChart|BurndownChart"` returns 0 (no old widget references in non-comment lines).
    - First line is `'use no memo';`.
  </acceptance_criteria>
  <done>index.tsx renders only the 3 new regions with a correct sprint-day subline (hidden when no sprint) and imports none of the 7 old widgets; its rewritten test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete old widgets + orphaned helpers + slim dashboardMetrics</name>
  <read_first>
    - 86-RESEARCH.md §Removal Map (the exact DELETE list: 7 components + their tests, 4 service helpers with their files/line refs and verified sole-consumers, dashboardMetrics survivor map)
    - taskflow/src/routes/dashboard/dashboardMetrics.ts (delete all non-survivors; KEEP only filterNonSubtasks + formatHoursMinutes + any private helper they still need)
    - taskflow/src/routes/dashboard/dashboardMetrics.test.ts (prune to only the 2 survivors' tests)
    - taskflow/src/services/jira.ts (remove fetchClosedSprints, fetchSprintIssuesBySprintId, and the fetchBurndown re-export — confirm no remaining import refs first)
    - taskflow/src/services/jira/greenhopper/burndown.ts (delete fetchBurndown source)
    - taskflow/src/lib/concurrency.ts (remove getVelocityLimit — sole consumer was VelocityChart)
    - taskflow/src/components/app/Sidebar.tsx (lines 140-156: KEEP the ['jira-active-sprint',...] prefetch; only update the stale comment that references SprintHealthSection)
  </read_first>
  <action>
    Per the RESEARCH Removal Map (D-01): before each deletion, grep the repo to CONFIRM zero remaining
    consumers outside the dashboard widget cluster (the research verified these, but confirm again so no
    orphan is created). Then delete the 7 component files and their tests that exist:
    `StatTile.tsx`, `StatTile.test.tsx`, `SprintHealthSection.tsx`, `SprintHealthSection.test.tsx`,
    `WeeklyTrendChart.tsx`, `WeeklyTrendChart.test.tsx`, `ActivityStrip.tsx`, `ActivityStrip.test.tsx`,
    `DashboardReleaseCard.tsx`, `DashboardReleaseCard.test.tsx`, `VelocityChart.tsx`, `BurndownChart.tsx`
    (note: VelocityChart and BurndownChart have NO dedicated test files — do not invent paths).

    Remove the orphaned service helpers (sole consumers were the deleted charts, verified): delete
    `fetchClosedSprints` and `fetchSprintIssuesBySprintId` from `services/jira.ts`; delete `fetchBurndown`
    from `services/jira/greenhopper/burndown.ts` and its re-export line in `jira.ts`; delete
    `getVelocityLimit` from `lib/concurrency.ts`. (The `concurrency.ts:43` JSDoc mention of
    fetchSprintIssuesBySprintId is a comment only — update/remove the comment too.)

    Slim `dashboardMetrics.ts` to keep ONLY `filterNonSubtasks` and `formatHoursMinutes` (and any private
    helper those two still require) per the survivor map; delete `computePersonalTileCounts`,
    `computeSpDone`, `computeSpTotal`, `computeDonutData`, `getDaysRemaining`, `WeekBucket`,
    `DAILY_TARGET_HOURS`, `buildWeekBuckets`, `ActivityEntry`, `mergeActivityEntries`, `VelocityPoint`,
    `computePersonalVelocitySeries`, `BurndownPoint`, `parseBurndownChanges`, `buildIdealGuideline`, and
    the now-unused private `addDays`. Prune `dashboardMetrics.test.ts` to keep only tests for the 2
    survivors. Update the Sidebar comment to drop the SprintHealthSection reference (keep the prefetch).
  </action>
  <verify>
    <automated>cd taskflow && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npm run check` exits 0 (biome + tsc — proves zero dangling imports / unreferenced exports).
    - `node -e "['StatTile.tsx','StatTile.test.tsx','SprintHealthSection.tsx','SprintHealthSection.test.tsx','WeeklyTrendChart.tsx','WeeklyTrendChart.test.tsx','ActivityStrip.tsx','ActivityStrip.test.tsx','DashboardReleaseCard.tsx','DashboardReleaseCard.test.tsx','VelocityChart.tsx','BurndownChart.tsx'].forEach(f=>{if(require('fs').existsSync('taskflow/src/routes/dashboard/'+f)){console.error('STILL EXISTS '+f);process.exit(1)}})"` exits 0.
    - `grep -Ec "fetchClosedSprints|fetchSprintIssuesBySprintId|fetchBurndown|getVelocityLimit" taskflow/src/services/jira.ts taskflow/src/lib/concurrency.ts` returns 0 (excluding deleted burndown.ts).
    - `grep -Ec "computePersonalTileCounts|computeSpDone|buildWeekBuckets|computePersonalVelocitySeries|parseBurndownChanges" taskflow/src/routes/dashboard/dashboardMetrics.ts` returns 0; `filterNonSubtasks` and `formatHoursMinutes` still present.
    - Sidebar `['jira-active-sprint'` prefetch still present; its comment no longer names SprintHealthSection.
  </acceptance_criteria>
  <done>All 7 old widgets + their tests are gone, orphaned service helpers removed, dashboardMetrics slimmed to 2 survivors, Sidebar prefetch retained with updated comment, and npm run check is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Extend widget-removal.guard.test.ts for Phase 86</name>
  <read_first>
    - taskflow/src/routes/dashboard/widget-removal.guard.test.ts (existing pattern: fs.existsSync absence + comment-stripped source-string import-absence; add a new describe block after the existing ones)
    - 86-PATTERNS.md §widget-removal.guard.test.ts (exact new describe block with 11 file-absence assertions + 1 index-import-absence assertion; note ActivityStrip.test.tsx DOES exist)
    - 86-RESEARCH.md §widget-removal.guard.test.ts (assertion pattern + Phase 86 additions)
  </read_first>
  <behavior>
    - Test (fs): each of the 11 deleted files (7 components + their existing tests: StatTile, StatTile.test, SprintHealthSection, SprintHealthSection.test, WeeklyTrendChart, WeeklyTrendChart.test, ActivityStrip, ActivityStrip.test, DashboardReleaseCard, DashboardReleaseCard.test, VelocityChart, BurndownChart) asserts fs.existsSync === false.
    - Test (source): index.tsx (comment-stripped) matches none of the 7 old widget names.
  </behavior>
  <action>
    Append a new `describe('dashboard subtree — Phase 86 widget removal guard', ...)` block to
    `taskflow/src/routes/dashboard/widget-removal.guard.test.ts`, copying the existing fs.existsSync and
    comment-stripped source-string patterns. Add one `it(... does not exist)` per deleted file:
    StatTile.tsx, StatTile.test.tsx, SprintHealthSection.tsx, SprintHealthSection.test.tsx,
    WeeklyTrendChart.tsx, WeeklyTrendChart.test.tsx, ActivityStrip.tsx, ActivityStrip.test.tsx (this
    test file DID exist — assert its absence), DashboardReleaseCard.tsx, DashboardReleaseCard.test.tsx,
    VelocityChart.tsx, BurndownChart.tsx. Add one `it('index.tsx does not import old Phase 83-85
    widgets')` that reads index.tsx, strips comment lines (lines starting with `//` or `*`), and asserts
    the joined non-comment source does NOT match `/StatTile/`, `/SprintHealthSection/`,
    `/WeeklyTrendChart/`, `/ActivityStrip/`, `/DashboardReleaseCard/`, `/VelocityChart/`, `/BurndownChart/`.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- src/routes/dashboard/widget-removal.guard</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npm run test -- src/routes/dashboard/widget-removal.guard` exits 0.
    - `grep -c "Phase 86" taskflow/src/routes/dashboard/widget-removal.guard.test.ts` returns >=1.
    - The new describe block contains 12 fs-absence `it(`s (7 components + 5 existing tests: ActivityStrip.test, StatTile.test, SprintHealthSection.test, WeeklyTrendChart.test, DashboardReleaseCard.test) plus 1 import-absence `it(`.
    - `cd taskflow && npm run test` (full suite) exits 0.
  </acceptance_criteria>
  <done>The deletion guard enforces Phase 86 zero-dead-code two ways (fs absence + index import absence); the full test suite is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jira active-sprint API → hero render | Untrusted startDate/endDate strings drive the sprint-day calc rendered as plain text. Read-only; reuses the existing prefetched cache key; no new endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-07 | Information Disclosure (XSS on render) | index.tsx hero | mitigate | Greeting, date, and sprint-day clause render as plain text JSX children — no `dangerouslySetInnerHTML`. Acceptance: `grep -c dangerouslySetInnerHTML taskflow/src/routes/dashboard/index.tsx` returns 0. |
| T-86-08 | Tampering (malformed sprint dates → NaN subline) | index.tsx | mitigate | Sprint clause only computed when both startDate and endDate present; otherwise hidden (D-13). Date math via Date.UTC on split integers; no clause rendered on missing data. |
| T-86-09 | Tampering (dead code path left importable) | dashboard subtree | mitigate | Two-way enforcement: widget-removal.guard fs+import assertions AND `npm run check` (no unreferenced exports). A re-introduced widget fails both gates. |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages. Deletions only. |

No `high` severity threats — read-only dashboard, deletions + composition, no new API surface.
</threat_model>

<verification>
- `cd taskflow && npm run test` (full suite) GREEN — includes index, widget-removal.guard, dashboardMetrics, and the Plan 01/02 component tests.
- `cd taskflow && npm run check` GREEN (biome + tsc — no unreferenced exports; the definitive zero-dead-code gate).
- `grep -c dangerouslySetInnerHTML taskflow/src/routes/dashboard/index.tsx` returns 0 (T-86-07).
</verification>

<success_criteria>
- Dashboard renders exactly the 3 screenshot regions; nothing below (D-01).
- Hero subline shows date + sprint-day, hidden when no active sprint (D-13).
- All 7 old widgets + their tests deleted; orphaned helpers removed; dashboardMetrics slimmed to 2 survivors (D-01).
- Charting contract intact on the wired chart (D-14, verified via Plan 02 + npm run check).
- widget-removal.guard passes; npm run check GREEN; full suite GREEN.
</success_criteria>

<output>
Create `.planning/phases/86-redesign-dashboard-to-new-screenshot-layout-and-remove-old-w/86-03-SUMMARY.md` when done
</output>
