---
phase: 91-post-release-merge-back-verification
plan: 06
type: execute
wave: 2
depends_on: ["91-05"]
gap_closure: true
files_modified:
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx
  - taskflow/src/routes/dashboard/release-detail/MetaRow.tsx
  - taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx
autonomous: true
requirements: [MERGE-01, MERGE-03]

must_haves:
  truths:
    - "The 'Merged back' row resolves to a terminal state for realistic release/milestone configurations — a failed gitlab-project fetch and an unparseable milestone title both produce a non-loading verdict end to end through the hook"
    - "The hook's test suite can actually fail on the CR-01..CR-04 bug states rather than asserting a tautology"
    - "The D-12 'no interactive control in the Merged back row' regression lock is anchored to a stable test hook that a styling change cannot silently disable"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts"
      provides: "isError threading for the gitlab-project query and the tracking-MR-unavailable signal"
      contains: "defaultBranchCheckFailed"
    - path: "taskflow/src/routes/dashboard/release-detail/MetaRow.tsx"
      provides: "Stable data-testid hook for row-scoped assertions"
      contains: "data-testid"
    - path: "taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx"
      provides: "Non-tautological verdict assertions for the CR-03/CR-04 bug states"
  key_links:
    - from: "useReleaseDetail.ts gitlab-project useQuery"
      to: "resolveMergeBackVerdict"
      via: "isError destructured and passed as defaultBranchCheckFailed"
      pattern: "defaultBranchCheckFailed"
    - from: "useReleaseDetail.ts releaseBranchName"
      to: "resolveMergeBackVerdict"
      via: "releaseBranchName === null passed as trackingMRsUnavailable"
      pattern: "trackingMRsUnavailable"
---

<objective>
Close the hook half of VERIFICATION gap 3 (CR-04's missing `isError` capture and
CR-03's missing "cannot be attempted" signal) and gap 4's test-quality warnings
WR-03 and WR-05.

Plan 91-05 gives `resolveMergeBackVerdict` the two optional params; this plan
supplies them from real query state, then replaces the hook test's tautology
(`expect(mergeBackVerdict).toHaveProperty('kind')` — always true for a
discriminated union) with assertions that fail against the bug states, and
re-anchors the D-12 "no buttons in the row" lock from a Tailwind class selector
to a stable `data-testid`.

Purpose: without this wiring, 91-05's terminal branches are unreachable in the
running app.
Output: a row that terminates for realistic inputs, plus tests that can actually
detect a regression.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-post-release-merge-back-verification/91-VERIFICATION.md
@.planning/phases/91-post-release-merge-back-verification/91-CONTEXT.md
@.planning/phases/91-post-release-merge-back-verification/91-05-SUMMARY.md
@taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts
@taskflow/src/routes/dashboard/release-detail/mergeBackVerification.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread the project-fetch error and the tracking-MR-unavailable signal into the resolver (CR-03, CR-04)</name>
  <files>taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts</files>
  <read_first>
    - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts — the `gitlab-project` query (~L150-157), `releaseBranchName` derivation (~L147), the branch query's `isError: branchCheckFailed` destructuring (~L159-176) as the in-file precedent, the tracking-MR query and its `enabled` gate (~L243-256), and the `resolveMergeBackVerdict(...)` call site (~L274-284)
    - taskflow/src/routes/dashboard/release-detail/mergeBackVerification.ts — the params type of `resolveMergeBackVerdict` AS MODIFIED BY PLAN 91-05, to use the exact param names `defaultBranchCheckFailed` and `trackingMRsUnavailable`
    - .planning/phases/91-post-release-merge-back-verification/91-05-SUMMARY.md — confirm the two param names and their semantics before wiring
  </read_first>
  <action>
    Destructure `isError` from the `gitlab-project` `useQuery` as
    `isError: defaultBranchCheckFailed`, matching the branch query's existing
    `isError: branchCheckFailed` naming convention two blocks below. Leave the
    query's `queryKey`, `queryFn`, `enabled` gate and `staleTime` untouched —
    `activeGitlabProject` granularity and the 5-minute staleTime are a cache
    contract (D-15 / P88 CR-02).

    Compute `const trackingMRsUnavailable = releasedVersion && releaseBranchName === null;`
    immediately above the `resolveMergeBackVerdict` call, with a comment citing
    CR-03: the tracking-MR query's `enabled` gate includes
    `releaseBranchName !== null`, so an unparseable milestone title leaves
    `trackingMRs` permanently `undefined` with `isError` permanently `false` —
    indistinguishable from in-flight without this signal, the same defect class
    `releaseBranch.ts`'s `unresolvable` kind already models.

    Pass both new values into the existing `resolveMergeBackVerdict({...})` call
    as `defaultBranchCheckFailed` and `trackingMRsUnavailable`. Change nothing
    else in the call. Add no new query, no new invalidation, no store write, and
    no override/dismiss state (D-12, D-15 — this phase is read-only).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isError: defaultBranchCheckFailed" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns exactly 1 match, inside the `gitlab-project` query block
    - `grep -n "trackingMRsUnavailable" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns at least 2 matches (declaration and resolver argument)
    - The `resolveMergeBackVerdict({...})` call site passes both `defaultBranchCheckFailed` and `trackingMRsUnavailable`
    - The `gitlab-project` query's `queryKey`, `enabled` expression and `staleTime` are byte-identical to before the change
    - No `useMutation`, no `queryClient.setQueryData`, and no Zustand/Tauri store write is added by this task
    - `cd taskflow && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` exits 0
    - `cd taskflow && npx tsc --noEmit` exits 0 with no output
  </acceptance_criteria>
  <done>Both terminal signals reach the resolver from live query state; the query cache contract and the read-only posture are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the hook test's tautology with bug-state assertions (WR-03)</name>
  <files>taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx — the whole file, especially `setupMocks` (~L79-140) including the `fetchProject` / `fetchProjectMilestones` / `fetchSourceBranchMRs` / `compareRefs` / `searchProjectTags` mocks, the `makeMilestone` factory and its `'33.5.0 (21.07.2026)'` title format, and the final `it('exposes mergeBackVerdict with a kind property')` case (~L509-517) that is being replaced
    - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts — as modified by Task 1, to know which mock rejections map to which verdict
    - taskflow/src/routes/dashboard/release-detail/mergeBackVerification.ts — the `MergeBackVerdict` union, so assertions target real discriminants
  </read_first>
  <action>
    Delete the `it('exposes mergeBackVerdict with a kind property')` case —
    `toHaveProperty('kind')` is true for every member of a discriminated union
    and can never fail (WR-03). Extend `setupMocks` with optional overrides so
    each new case can control the relevant mock: at minimum a
    `fetchProjectImpl`, a `milestoneTitle`, a `fetchSourceBranchMRsImpl`, and a
    `compareRefsImpl` (follow the existing `fetchBranchImpl` override pattern
    already in the signature). Keep every existing default so all current cases
    remain unchanged and passing.

    Add cases, each on a released version (`released: true`) with
    `retry: false` on the QueryClient so rejections settle promptly:
    - CR-04: `fetchProject` rejects. Wait for the verdict to settle and assert
      `result.current.mergeBackVerdict.kind` is `'couldnt-verify'` and
      explicitly `not.toBe('loading')`.
    - CR-03: the matched milestone's title is unparseable for a version
      (e.g. `'Sprint planning (21.07.2026)'` — keep a date that still matches
      `RELEASE_DATE` so the milestone matches but no version/branch is
      derivable). Assert `result.current.mergeBackVerdict.kind` is
      `'couldnt-verify'` and explicitly `not.toBe('loading')`.
    - CR-01 end to end: `fetchSourceBranchMRs` resolves to one MR with
      `state: 'merged'` and `target_branch: 'master'` while `fetchProject`
      returns `default_branch: 'develop'`, with `searchProjectTags` returning a
      matching `v33.5.0` tag and `compareRefs` returning
      `{ diffCount: 3, commitCount: 12, timedOut: false }`. Assert the verdict
      `kind` is `'likely-not-merged'`, explicitly not `'merged'`.
    - Happy path preserved: same setup but `target_branch: 'develop'` → verdict
      `kind` is `'merged'` with `via: 'tracking-mr'`.

    Use `await waitFor(...)` on the verdict discriminant itself rather than on a
    mock-call count, so a case cannot pass while the verdict is still settling.
    Do not weaken or delete any existing case, especially the D-05 "zero extra
    GitLab calls when unreleased" assertions.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "toHaveProperty('kind')" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` returns 0 matches
    - A case exists in which `fetchProject` rejects and asserts `mergeBackVerdict.kind` is `'couldnt-verify'` and not `'loading'`
    - A case exists with an unparseable milestone title asserting `mergeBackVerdict.kind` is `'couldnt-verify'` and not `'loading'`
    - A case exists with a merged MR targeting `'master'` against `default_branch: 'develop'` asserting `mergeBackVerdict.kind` is `'likely-not-merged'`
    - A case exists with a merged MR targeting `'develop'` asserting `mergeBackVerdict.kind` is `'merged'` and `via` is `'tracking-mr'`
    - Every pre-existing case in the file still passes, including the unreleased-version zero-extra-calls assertions
    - `cd taskflow && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` exits 0
    - `cd taskflow && npx tsc --noEmit` exits 0 with no output
  </acceptance_criteria>
  <done>The hook suite fails against the CR-01/CR-03/CR-04 bug states instead of passing regardless.</done>
</task>

<task type="auto">
  <name>Task 3: Re-anchor the D-12 no-control regression lock to a stable test hook (WR-05, MERGE-03)</name>
  <files>taskflow/src/routes/dashboard/release-detail/MetaRow.tsx, taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/release-detail/MetaRow.tsx — the whole 8-line component; note the outer `div.flex.items-start.gap-2` the test currently selects on
    - taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx — the `renders the likely-not-merged verdict, with no button in the row (D-12 lock)` case (~L244-260) and the surrounding `data-testid` conventions (`merge-back-merged`, `branch-status-released`, etc.)
    - taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx — the `MetaRow label="Merged back"` block (~L282+) confirming there is no interactive control inside it today
    - .planning/phases/91-post-release-merge-back-verification/91-CONTEXT.md — D-12, to confirm this task adds a lock and never an override affordance
  </read_first>
  <action>
    Give `MetaRow` a stable, label-derived test hook on its outer `div`:
    `data-testid={\`meta-row-${label}\`}` normalized to a kebab-case slug
    (lowercase, spaces to hyphens) so the "Merged back" row is addressable as
    `meta-row-merged-back` regardless of className changes. Add the attribute
    only — do not change `MetaRow`'s props signature, layout classes, or DOM
    structure, since every Details row in the sidebar renders through it.

    In `ReleaseDetailSidebar.test.tsx`, rewrite the D-12 lock case to scope via
    `screen.getByTestId('meta-row-merged-back')` instead of
    `el.closest('.flex.items-start.gap-2')`, keeping the
    `within(row).queryAllByRole('button')` length-0 assertion. Strengthen the
    lock by also asserting the row contains no element matching
    `input, select, textarea, [role="checkbox"], [role="switch"], [role="menuitem"]`
    — D-12 forbids an override control in ANY form, not only a `<button>`.

    Add a second D-12 lock case asserting the same emptiness for the
    `couldnt-verify` verdict (the state most likely to tempt a future
    "confirm it merged" affordance), and assert the row's text content contains
    no case-insensitive match for `override`, `dismiss`, `acknowledge`, or
    `confirm`.

    Add no interactive control to any source file. This task exists to lock the
    recorded MERGE-03 descope (D-12), which is satisfied by absence, not by
    implementation.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "data-testid" taskflow/src/routes/dashboard/release-detail/MetaRow.tsx` returns 1 match on the outer `div`
    - `grep -n "closest('.flex.items-start.gap-2')" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx` returns 0 matches
    - `screen.getByTestId('meta-row-merged-back')` resolves in the likely-not-merged render
    - The likely-not-merged row asserts 0 elements with `role="button"` AND 0 matches for `input, select, textarea, [role="checkbox"], [role="switch"], [role="menuitem"]`
    - A second lock case makes the same assertions for the `couldnt-verify` verdict
    - The row's text content matches none of `/override|dismiss|acknowledge|confirm/i`
    - Every pre-existing case in `ReleaseDetailSidebar.test.tsx` still passes, including the branch-row D-08 wording cases
    - `grep -rn "onClick" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx` shows no new handler inside the `MetaRow label="Merged back"` block
    - `cd taskflow && npx vitest run src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx` exits 0
    - `cd taskflow && npx tsc --noEmit` exits 0 with no output
  </acceptance_criteria>
  <done>The D-12 lock survives arbitrary MetaRow styling changes and covers non-button controls; no override affordance is introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitLab API → hook query state | Failure modes (401/403/500/timeout) cross here and must become an honest verdict rather than an indefinite spinner |
| Hook → presentational sidebar | Only plain verdict data crosses; no handler, no mutation, no store write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-06-01 | Denial of Service (of information) | `gitlab-project` query error handling | mitigate | Task 1 captures `isError` and routes it to `couldnt-verify`, so a 500/timeout on `fetchProject` can no longer suppress the verdict indefinitely |
| T-91-06-02 | Repudiation | hook test suite | mitigate | Task 2 replaces an always-true assertion with bug-state assertions so a regression to the CR-01/CR-03/CR-04 behavior fails CI instead of passing silently |
| T-91-06-03 | Elevation of Privilege | "Merged back" row | mitigate | Task 3's strengthened lock asserts no button, input, select, textarea, checkbox, switch or menuitem exists in the row — the advisory verdict cannot acquire a state-changing control (D-12) |
| T-91-06-04 | Information Disclosure | token handling | accept | This plan adds no new API call and no new error-message construction; the existing `apiFetch('gitlab', …)` redaction and the prior phase's no-token-leak property are unchanged |
| T-91-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies are installed by this plan; nothing to audit |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/services/gitlab.test.ts src/routes/dashboard/release-detail/mergeBackVerification.test.ts src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` — all 4 files green, test count >= 205
- `cd taskflow && npx tsc --noEmit` — exit 0
- `grep -n "defaultBranchCheckFailed\|trackingMRsUnavailable" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` — both present
</verification>

<success_criteria>
- A failed `fetchProject` and an unparseable milestone title each produce a non-`loading` verdict through the real hook
- No hook test asserts a tautology; the suite fails on the CR-01/CR-03/CR-04 bug states
- The D-12 lock is anchored to `data-testid`, not a Tailwind class, and covers non-button controls
- MERGE-03 remains satisfied via the recorded descope D-12 — regression lock only, no override control and no persistence introduced
- Full targeted suite and typecheck stay green
</success_criteria>

<output>
Create `.planning/phases/91-post-release-merge-back-verification/91-06-SUMMARY.md` when done
</output>
