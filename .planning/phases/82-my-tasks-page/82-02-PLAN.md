---
phase: 82-my-tasks-page
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - taskflow/src/stores/my-tasks.store.ts
  - taskflow/src/stores/my-tasks.store.test.ts
autonomous: true
requirements: [MYTASK-08]
must_haves:
  truths:
    - "groupingMode and scope persist across sessions via Tauri Store my-tasks.json"
    - "Default groupingMode is my-day and default scope is current-sprint (D-09)"
    - "The transient filter state is NOT in the store (D-01/D-10)"
  artifacts:
    - path: "taskflow/src/stores/my-tasks.store.ts"
      provides: "useMyTasksStore (Zustand + persist + createTauriStorage('my-tasks.json'))"
      exports: ["useMyTasksStore"]
      contains: "createTauriStorage('my-tasks.json')"
    - path: "taskflow/src/stores/my-tasks.store.test.ts"
      provides: "persist/restore tests + filter-not-persisted assertion"
  key_links:
    - from: "taskflow/src/stores/my-tasks.store.ts"
      to: "createTauriStorage"
      via: "persist middleware storage option"
      pattern: "createTauriStorage\\('my-tasks.json'\\)"
---

<objective>
Create the persisted Zustand store that survives app restarts for the My Tasks page's grouping mode and scope (MYTASK-08, D-10). Mirrors the established `pinned-tabs.store.ts` pattern exactly. Wave 0 — no UI dependency, fully unit-testable against the global Tauri plugin-store mock.

Purpose: Per criterion 7 only grouping mode and scope persist; the transient filter must never enter the store (D-01). Isolating the store as its own Wave 0 plan lets the persistence contract be proven before the page consumes it, and keeps file ownership disjoint from the sort lib and service plans for parallel execution.
Output: `src/stores/my-tasks.store.ts` + co-located test.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-my-tasks-page/82-RESEARCH.md
@.planning/phases/82-my-tasks-page/82-PATTERNS.md
@.planning/phases/82-my-tasks-page/82-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: my-tasks.store.ts persisted Zustand store + tests</name>
  <files>taskflow/src/stores/my-tasks.store.ts, taskflow/src/stores/my-tasks.store.test.ts</files>
  <read_first>
    - taskflow/src/stores/my-tasks.store.ts (the file being created)
    - taskflow/src/stores/my-tasks.store.test.ts (the file being created — write tests first, RED)
    - taskflow/src/stores/pinned-tabs.store.ts (PATTERNS.md exact analog — full create/persist/version/migrate pattern)
    - taskflow/src/stores/pinned-tabs.store.test.ts (PATTERNS.md exact analog — LazyStore vi.mock, beforeEach setState reset, act() mutation pattern)
    - taskflow/src/lib/tauri-storage.ts (createTauriStorage signature — the ONLY persistence backend)
    - .planning/phases/82-my-tasks-page/82-PATTERNS.md (§my-tasks.store.ts — full target source)
  </read_first>
  <behavior>
    - Default state: groupingMode 'my-day', scope 'current-sprint' (D-09)
    - setGroupingMode('by-status') updates groupingMode to 'by-status'
    - setScope('all-assigned') updates scope to 'all-assigned'
    - After setState mutation, the persisted value is restored (test re-reads getState)
    - `'activeFilter' in useMyTasksStore.getState()` is false — filter is never in the store (D-01/D-10)
  </behavior>
  <action>
    Write the test first (RED): vi.mock('@tauri-apps/plugin-store') with the LazyStore stub (get/set/save/delete as resolved vi.fn) exactly as pinned-tabs.store.test.ts. In beforeEach, reset via `useMyTasksStore.setState({ groupingMode: 'my-day', scope: 'current-sprint' })` inside `act()`. Add the behavior-bullet tests, including the explicit `expect('activeFilter' in useMyTasksStore.getState()).toBe(false)` assertion.

    Then implement `my-tasks.store.ts`: define `GroupingMode = 'my-day' | 'by-status' | 'by-sprint-parent'` and `Scope = 'current-sprint' | 'all-assigned'`; `MyTasksState` with groupingMode, scope, setGroupingMode, setScope. Wrap `create<MyTasksState>()(persist((set) => ({...}), { name: 'my-tasks-store', storage: createTauriStorage('my-tasks.json'), version: 0, migrate: (persisted, _v) => persisted as MyTasksState }))`. Import `createTauriStorage` from '../lib/tauri-storage'. Do NOT add activeFilter to state (MYTASK-08, D-10). Never call LazyStore directly.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- --run src/stores/my-tasks.store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npm run test -- --run src/stores/my-tasks.store.test.ts` passes
    - Source assertion: `grep -q "createTauriStorage('my-tasks.json')" taskflow/src/stores/my-tasks.store.ts` succeeds
    - Source assertion: `grep -qv activeFilter taskflow/src/stores/my-tasks.store.ts` — no `activeFilter` token anywhere in the store source
    - Behavior: the `'activeFilter' in getState()` test is present and asserts `false`
    - Source assertion: default `groupingMode: 'my-day'` and `scope: 'current-sprint'` present (D-09)
  </acceptance_criteria>
  <done>The persisted store exists mirroring pinned-tabs.store.ts, persists only groupingMode + scope, excludes the filter, and the test suite is green.</done>
</task>

</tasks>

<threat_model>
Read-only personal-task viewer over already-authenticated data. No new auth or write endpoints. This plan's only persistence is `my-tasks.json` holding UI preferences (groupingMode, scope) — never issue data, never credentials.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Tauri Store (my-tasks.json) | UI-preference strings cross into local on-disk JSON. No credential or issue content crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-02 | Information Disclosure | my-tasks.json persisted store | mitigate | Store schema is restricted to two enum string fields (groupingMode, scope); no issue data, PII, or tokens are persisted — enforced by the `activeFilter`-absent test and the typed `MyTasksState` shape. |
| T-82-03 | Tampering | my-tasks.json on-disk file | accept | Local file; an attacker with disk access has broader compromise. Only valid effect of tampering is an invalid enum → store falls back via typed defaults; no security impact. |
| T-82-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd taskflow && npm run test -- --run src/stores/my-tasks.store.test.ts` green
- `git diff taskflow/package.json` empty (no new packages)
</verification>

<success_criteria>
MYTASK-08 satisfied at the unit level: grouping mode and scope persist via Tauri Store, the filter is provably excluded, and defaults match D-09.
</success_criteria>

<output>
Create `.planning/phases/82-my-tasks-page/82-02-SUMMARY.md` when done.
</output>
