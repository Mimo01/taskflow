---
phase: 67-settings-ui-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/settings/SidebarItemsList.tsx
  - taskflow/src/routes/settings/SidebarItemsList.test.tsx
  - taskflow/src/routes/settings/Settings.test.tsx
  - taskflow/src/stores/settings.store.ts
  - taskflow/package.json
  - taskflow/package-lock.json
autonomous: true
requirements: [SETUI-01, SETUI-02, SETUI-03]
must_haves:
  truths:
    - "Settings → Sidebar renders a plain checkbox-per-item visibility list with no drag handles"
    - "Toggling a sidebar item checkbox calls setSidebarItemVisible with the item id and new boolean"
    - "Section headers (Main, Planning, Code, Tracking, Testing) still group the items"
    - "No file under taskflow/src imports any @dnd-kit/* package"
    - "reorderSidebarItem no longer exists on the settings store type or implementation"
    - "npm run build completes green after dnd-kit packages are uninstalled"
  artifacts:
    - path: "taskflow/src/routes/settings/SidebarItemsList.tsx"
      provides: "Visibility-only sidebar item list (no dnd-kit)"
      max_lines: 70
    - path: "taskflow/src/stores/settings.store.ts"
      provides: "Settings store with reorderSidebarItem removed"
      contains: "setSidebarItemVisible"
    - path: "taskflow/src/routes/settings/SidebarItemsList.test.tsx"
      provides: "Tests for visibility-only list (drag tests removed)"
    - path: "taskflow/package.json"
      provides: "Dependency list with no @dnd-kit/* entries"
  key_links:
    - from: "taskflow/src/routes/settings/SidebarItemsList.tsx"
      to: "useSettingsStore.setSidebarItemVisible"
      via: "checkbox onChange handler"
      pattern: "setSidebarItemVisible\\(nav\\.id"
---

<objective>
Strip drag-and-drop reordering from the Settings → Sidebar items list, leaving a
visibility-only checkbox list. Remove the now-orphaned `reorderSidebarItem` store
action and uninstall the four `@dnd-kit/*` packages.

Purpose: SETUI-02 — sidebar configuration should be a single, simple
visibility-only surface. SETUI-01 (sidebar panel removed from Appearance) and
SETUI-03 (all-visible default via v22 migration) were already satisfied in Phase 66;
this plan verifies those two and implements the remaining SETUI-02 work.

Output:
- `SidebarItemsList.tsx` reduced from ~180 LOC to ~40–50 LOC, no dnd-kit imports
- `settings.store.ts` with `reorderSidebarItem` type + implementation removed
- `SidebarItemsList.test.tsx` + `Settings.test.tsx` updated
- `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/modifiers`, `@dnd-kit/utilities` uninstalled
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-settings-ui-cleanup/67-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. Use directly — no exploration needed. -->

From taskflow/src/components/app/sidebar-items.ts:
- `export const SIDEBAR_SECTIONS: { id: string; label: string }[]` — sections in order: Main, Planning, Code, Tracking, Testing
- `export const SIDEBAR_NAV_ITEMS: SidebarNavDef[]` — each has `{ id, label, section, ... }`
- `export interface SidebarItem { id: string; visible: boolean }`
- `export function getDefaultSidebarItems(): SidebarItem[]` — no-arg, all-visible after Phase 66

From taskflow/src/stores/settings.store.ts (current, before this plan):
- `sidebarItems: SidebarItem[]`
- `setSidebarItems: (items: SidebarItem[]) => void`  ← KEEP (used by migration, D-05)
- `setSidebarItemVisible: (id: string, visible: boolean) => void`  ← KEEP (D-03)
- `reorderSidebarItem: (fromIndex: number, toIndex: number) => void`  ← REMOVE (D-04): type at line 158, impl at lines 307–313
- Store `version: 22` — DO NOT bump (D-04: removing an in-memory action does not affect persisted data)

Existing settings-row styling to reuse (D-01, code_context):
- Row container: `flex items-center gap-2 px-2 py-1.5 rounded-md hover:bg-accent`
- Checkbox: `h-4 w-4 rounded border-border accent-primary`
- Section header: `text-xs font-semibold uppercase tracking-wider text-muted-foreground px-1`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite SidebarItemsList as visibility-only list; remove reorderSidebarItem store action; update both test files</name>
  <files>
    taskflow/src/routes/settings/SidebarItemsList.tsx,
    taskflow/src/routes/settings/SidebarItemsList.test.tsx,
    taskflow/src/routes/settings/Settings.test.tsx,
    taskflow/src/stores/settings.store.ts
  </files>
  <read_first>
    - taskflow/src/routes/settings/SidebarItemsList.tsx (current dnd-kit implementation being replaced)
    - taskflow/src/routes/settings/SidebarItemsList.test.tsx (current drag-handle tests being rewritten)
    - taskflow/src/routes/settings/Settings.test.tsx (mock object containing reorderSidebarItem at line 133)
    - taskflow/src/stores/settings.store.ts (reorderSidebarItem type line 158, impl lines 307–313; keep setSidebarItems + setSidebarItemVisible)
    - taskflow/src/components/app/sidebar-items.ts (SIDEBAR_SECTIONS, SIDEBAR_NAV_ITEMS, getDefaultSidebarItems contracts)
  </read_first>
  <behavior>
    SidebarItemsList component (after rewrite):
    - Renders one row per SIDEBAR_NAV_ITEMS entry, grouped under SIDEBAR_SECTIONS headers (Main, Planning, Code, Tracking, Testing).
    - Each row = `[checkbox] Label` only — NO drag-handle button, NO GripVertical icon, NO data-sortable-item attribute.
    - Checkbox checked state = item visibility from useSettingsStore().sidebarItems (default true when id missing from map).
    - Toggling a checkbox calls setSidebarItemVisible(navId, newChecked).
    - Section with zero matching items renders nothing.
    settings.store.ts (after edit):
    - reorderSidebarItem removed from both the SettingsState type and the store implementation.
    - setSidebarItems and setSidebarItemVisible still present and unchanged.
    Tests after update — SidebarItemsList.test.tsx:
    - Test 'renders section headers: Main, Planning, Code, Tracking, Testing' PASSES (kept unchanged).
    - Test 'checkbox toggles call setSidebarItemVisible ...' PASSES (kept unchanged); first checkbox click calls setSidebarItemVisible('dashboard', false).
    - Drag-handle test ('renders drag handles ...') DELETED.
    - Old row-structure test ('each item row contains drag handle, checkbox, and label text') DELETED.
    - New row-structure test asserts: a row contains a checkbox and label text in DOM order (checkbox before label), with no data-sortable-item attribute and no button[aria-label="Drag to reorder"].
    Tests after update — Settings.test.tsx:
    - mock store object no longer has a reorderSidebarItem entry.
  </behavior>
  <action>
    Rewrite taskflow/src/routes/settings/SidebarItemsList.tsx to a visibility-only list per D-01/D-02/D-03. Remove ALL `@dnd-kit/*` imports, the `GripVertical` import, `useState`, the `SortableItem` component, `DndContext`, `SortableContext`, `DragOverlay`, `useSortable`, `useSensor`/`useSensors`/`PointerSensor`, `closestCenter`, `restrictToVerticalAxis`, `verticalListSortingStrategy`, `CSS.Transform`, `handleDragStart`, `handleDragEnd`, and `activeId` state. Destructure only `sidebarItems` and `setSidebarItemVisible` from `useSettingsStore()` (do NOT reference `reorderSidebarItem`). Keep the section-grouping render: outer `div` className `flex flex-col gap-4`, map `SIDEBAR_SECTIONS`, per section render a header `span` with className `text-xs font-semibold uppercase tracking-wider text-muted-foreground px-1` and a `flex flex-col gap-1` container. Since reorder is gone, iterate items via `SIDEBAR_NAV_ITEMS.filter((nav) => nav.section === section.id)` (static registry order is correct now that ordering is fixed). Build a visibility lookup `new Map(sidebarItems.map((item) => [item.id, item.visible]))` and default to `true` when missing. Each row is a `div` with className `flex items-center gap-2 px-2 py-1.5 rounded-md hover:bg-accent` containing a checkbox `input type="checkbox"` (className `h-4 w-4 rounded border-border accent-primary`, `checked={isVisible}`, `onChange={(e) => setSidebarItemVisible(nav.id, e.target.checked)}`) followed by a `span` className `text-sm` with the label. Use `key={nav.id}` on rows and `key={section.id}` on section containers. Do NOT render a drag-handle button, a GripVertical icon, or a `data-sortable-item` attribute. Replace the stale header doc-comment ("Sortable checkbox list … @dnd-kit/sortable for drag-and-drop reordering …") with an accurate one-liner describing a visibility-only checkbox list grouped by section (Claude's Discretion per CONTEXT).

    In taskflow/src/stores/settings.store.ts remove the `reorderSidebarItem` member from the SettingsState type (line ~158) and its implementation block (lines ~307–313). Leave `setSidebarItems` and `setSidebarItemVisible` untouched. Do NOT change `version: 22` (D-04).

    In taskflow/src/routes/settings/SidebarItemsList.test.tsx (per D-07): delete the `const reorderSidebarItem = vi.fn();` declaration, the `reorderSidebarItem.mockClear();` call, and the `reorderSidebarItem,` entry in the `useSettingsStore.setState({...})` mock. Delete the test `'renders drag handles with aria-label "Drag to reorder" for each sidebar nav item'`. Delete the test `'each item row contains drag handle, checkbox, and label text'`. Add a replacement row-structure test (e.g. `'each item row contains a checkbox and label text with no drag handle'`) that renders SidebarItemsList, finds the 'Dashboard' label, gets its closest row via `dashboardLabel.parentElement` (NOT `[data-sortable-item]` — that attribute is gone), asserts the row has an `input[type="checkbox"]`, asserts the row has NO `button[aria-label="Drag to reorder"]` and NO `[data-sortable-item]`, and asserts checkbox DOM index is less than label index. Keep the `'checkbox toggles call setSidebarItemVisible ...'` and `'renders section headers ...'` tests unchanged. Update the file's top doc-comment to drop the "drag-reorder" framing.

    In taskflow/src/routes/settings/Settings.test.tsx remove the `reorderSidebarItem: vi.fn(),` line (line ~133) from the mock store object (D-08).
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit && npx vitest run src/routes/settings/SidebarItemsList.test.tsx src/routes/settings/Settings.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@dnd-kit' taskflow/src/routes/settings/SidebarItemsList.tsx` returns 0
    - `grep -c 'data-sortable-item' taskflow/src/routes/settings/SidebarItemsList.tsx` returns 0
    - `grep -c 'GripVertical' taskflow/src/routes/settings/SidebarItemsList.tsx` returns 0
    - `SidebarItemsList.tsx` contains `setSidebarItemVisible(nav.id,` in an onChange handler
    - `grep -rc 'reorderSidebarItem' taskflow/src/` returns 0 across all files (store, both tests, component)
    - `taskflow/src/stores/settings.store.ts` still contains `setSidebarItems:` and `setSidebarItemVisible:`
    - `taskflow/src/stores/settings.store.ts` still contains `version: 22`
    - SidebarItemsList.test.tsx no longer contains the strings `'Drag to reorder'` or `'drag handle'` in a kept test name; `'renders section headers'` and `'checkbox toggles call setSidebarItemVisible'` tests still present
    - `cd taskflow && npx tsc --noEmit` exits 0
    - `cd taskflow && npx vitest run src/routes/settings/SidebarItemsList.test.tsx src/routes/settings/Settings.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    SidebarItemsList renders visibility-only rows grouped by section with no drag UI; reorderSidebarItem is gone from store + tests; tsc clean; both targeted test files pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Uninstall the four @dnd-kit packages and verify full production build</name>
  <files>
    taskflow/package.json,
    taskflow/package-lock.json
  </files>
  <read_first>
    - taskflow/package.json (dependencies block, @dnd-kit/* lines 26–29)
    - taskflow/src/routes/settings/SidebarItemsList.tsx (confirm no dnd-kit imports remain — Task 1 output)
  </read_first>
  <action>
    From the `taskflow/` workspace directory run a single `npm uninstall` for all four packages: `@dnd-kit/core @dnd-kit/sortable @dnd-kit/modifiers @dnd-kit/utilities` (D-06). This removes them from `package.json` dependencies and updates `package-lock.json`. Do NOT run uninstall from the repo root — run it inside `taskflow/`. After uninstall, run `npm run build` from `taskflow/` to confirm no orphaned dnd-kit imports remain — `npm run build` is required here (not just `tsc`) because Vite catches import/CSS resolution failures that the TypeScript compiler misses (Phase 59 standing rule). If the build surfaces a missed dnd-kit import, fix the offending import in `SidebarItemsList.tsx` and rebuild.
  </action>
  <verify>
    <automated>cd taskflow && grep -c '@dnd-kit' package.json; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@dnd-kit' taskflow/package.json` returns 0
    - `grep -c '@dnd-kit' taskflow/package-lock.json` returns 0 (or the lockfile no longer lists @dnd-kit packages as installed entries)
    - `cd taskflow && npm run build` exits 0
    - No build error references `@dnd-kit` or `Cannot find module '@dnd-kit/...'`
  </acceptance_criteria>
  <done>
    All four @dnd-kit packages are removed from package.json and package-lock.json; `npm run build` is green with no dnd-kit resolution errors.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (run from `taskflow/`):
1. SETUI-02: `grep -rc '@dnd-kit' src/` returns 0; `grep -c 'data-sortable-item\|GripVertical\|DndContext' src/routes/settings/SidebarItemsList.tsx` returns 0; component is ~40–70 LOC.
2. SETUI-02 store: `grep -rc 'reorderSidebarItem' src/` returns 0.
3. SETUI-01 (pre-satisfied Phase 66 commit da30013b): `grep -c 'SidebarItemsList' src/routes/settings/AppearanceSection.tsx` returns 0 — confirm Appearance no longer renders the panel.
4. SETUI-03 (pre-satisfied Phase 66): `grep -c 'version: 22' src/stores/settings.store.ts` returns 1 and the v22 migration resets sidebarItems to all-visible (unchanged by this phase).
5. `npx tsc --noEmit` exits 0; `npm run build` exits 0; `npx vitest run src/routes/settings/` passes.
</verification>

<success_criteria>
1. Settings → Sidebar shows visibility toggles only — no drag handles, no dnd-kit sortable, no reorder handlers (SETUI-02).
2. The only sidebar configuration surface is Settings → Sidebar; Appearance no longer renders SidebarItemsList (SETUI-01, verified pre-satisfied).
3. Fresh-install and post-migration users see all sidebar nav items visible by default via the unchanged v22 migration (SETUI-03, verified pre-satisfied).
4. No `@dnd-kit/*` package remains in package.json/package-lock.json; `npm run build` green.
</success_criteria>

<output>
Create `.planning/phases/67-settings-ui-cleanup/67-01-SUMMARY.md` when done.
</output>
