---
phase: 55-aio-project-selection-in-settings
plan: 02
type: execute
wave: 2
depends_on: [55-01]
files_modified:
  - taskflow/src/routes/settings/IntegrationsSection.tsx
  - taskflow/src/routes/settings/IntegrationsSection.test.tsx
autonomous: true
requirements: []
requirements_addressed: []
tags: [settings-ui, picker, shadcn-select, react-query, aio]

must_haves:
  truths:
    - "User opens Settings → Integrations and sees the existing aioEnabled toggle (unchanged)"
    - "When aioEnabled === true, a project picker renders below the toggle inside the same AIO Test Management subsection (D-01, D-02)"
    - "When aioEnabled === false, the picker is NOT rendered (D-02)"
    - "Picker fetches projects via fetchAioProjects(jiraBaseUrl, token) with cache key ['aio', jiraBaseUrl, 'projects'] (D-04)"
    - "Picker loading state shows inline Loader2 spinner + 'Loading projects…' text (D-05, UI-SPEC color matrix)"
    - "Picker error state shows inline XCircle icon + 'Couldn't load AIO projects.' + inline Retry button bound to refetch() (D-05, UI-SPEC)"
    - "Picker empty state (projects.length === 0) shows a disabled Select with placeholder 'No AIO projects available' (D-05, UI-SPEC)"
    - "Picker trigger displays the project name (not projectKey) when selectedAioProjectKey is set, looked up via projects.find(p => p.projectKey === selectedAioProjectKey) (Pitfall 3)"
    - "Selecting an item calls setSelectedAioProjectKey(projectKey) — store update is silent (no toast, no redirect, no banner per D-14)"
  artifacts:
    - path: "taskflow/src/routes/settings/IntegrationsSection.tsx"
      provides: "AIO project picker block gated on aioEnabled === true"
      contains: "selectedAioProjectKey"
    - path: "taskflow/src/routes/settings/IntegrationsSection.test.tsx"
      provides: "Component tests covering gating, render, change, loading, error, empty states"
      contains: "AIO project picker"
  key_links:
    - from: "taskflow/src/routes/settings/IntegrationsSection.tsx"
      to: "useSettingsStore"
      via: "destructure { aioEnabled, setAioEnabled, selectedAioProjectKey, setSelectedAioProjectKey }"
      pattern: "selectedAioProjectKey, setSelectedAioProjectKey"
    - from: "taskflow/src/routes/settings/IntegrationsSection.tsx"
      to: "fetchAioProjects in @/services/aio"
      via: "useQuery({ queryKey: ['aio', jiraBaseUrl, 'projects'], queryFn: () => fetchAioProjects(jiraBaseUrl!, token!), enabled: !!jiraBaseUrl && !!token })"
      pattern: "'aio', jiraBaseUrl, 'projects'"
    - from: "taskflow/src/routes/settings/IntegrationsSection.tsx"
      to: "Stronghold via @/services/stronghold readSecret('jira-pat')"
      via: "useEffect on mount with [jiraBaseUrl] dependency"
      pattern: "readSecret\\('jira-pat'\\)"
---

<objective>
Add a shadcn `<Select>` AIO project picker inside `IntegrationsSection.tsx`, gated on `aioEnabled === true` (D-02), wired to `selectedAioProjectKey` from Plan 01, fetching projects via the existing `fetchAioProjects` service with cache key `['aio', jiraBaseUrl, 'projects']`. Mirror the credential-loader + `useQuery` pattern from the (about-to-be-deleted) `AioProjectsPage.tsx` and the shadcn Select shape from `GitLabStep.tsx:107-136`. Render the loading / error / empty / loaded states exactly as the UI-SPEC color matrix dictates. Extend `IntegrationsSection.test.tsx` to cover all picker behaviors.

Purpose: This plan delivers the actual UI surface that subsumes the deleted AIO projects list page (Phase 52). After this plan ships, a user can enable AIO, see all 80 AIO projects in a Select, pick one, and have the choice persisted (via Plan 01's store field). The sidebar gate (Plan 03) reads the persisted value to compute its deep-link.
Output: Updated `IntegrationsSection.tsx` (toggle unchanged + picker block added with loading/error/empty/loaded states), updated `IntegrationsSection.test.tsx` with picker tests.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-aio-project-selection-in-settings/55-CONTEXT.md
@.planning/phases/55-aio-project-selection-in-settings/55-RESEARCH.md
@.planning/phases/55-aio-project-selection-in-settings/55-PATTERNS.md
@.planning/phases/55-aio-project-selection-in-settings/55-UI-SPEC.md
@.planning/phases/55-aio-project-selection-in-settings/55-01-PLAN.md
@taskflow/src/routes/settings/IntegrationsSection.tsx
@taskflow/src/routes/settings/IntegrationsSection.test.tsx
@taskflow/src/routes/dashboard/AioProjectsPage.tsx
@taskflow/src/routes/onboarding/GitLabStep.tsx
@taskflow/src/routes/settings/ConnectionsSection.tsx
@taskflow/src/services/aio/projects.ts
@taskflow/src/services/aio/types.ts
@taskflow/src/components/ui/select.tsx
@taskflow/src/components/ui/label.tsx
@taskflow/src/components/ui/skeleton.tsx

<interfaces>
<!-- Verified anchors (2026-05-14): -->
<!-- IntegrationsSection.tsx (current full file, ~31 lines): destructures `{ aioEnabled, setAioEnabled }` and renders an `<h2>Integrations</h2>` + a subsection `<div className="flex flex-col gap-4">` containing `<h3>AIO Test Management</h3>` + a `<label>` with the checkbox toggle. The toggle is unchanged by this plan. -->
<!-- AioProjectsPage.tsx lines 1-31: imports `useQuery`, `readSecret`, `useAuthStore`, calls `useEffect(() => { readSecret('jira-pat').then(setToken).catch(() => setToken(null)); }, []);`, then `useQuery({ queryKey: ['aio', jiraBaseUrl, 'projects'], queryFn: () => fetchAioProjects(jiraBaseUrl!, token!), enabled: !!jiraBaseUrl && !!token })`. -->
<!-- GitLabStep.tsx lines 107-136: the canonical Select picker shape; selected project name lookup via `projects.find(p => p.id === selectedProjectId)` for the trigger label; placeholder text `Choose a project...`. -->
<!-- ConnectionsSection.tsx lines 136-153: inline loading row pattern `<div className="flex items-center gap-1.5 text-sm text-muted-foreground"><Loader2 className="h-4 w-4 animate-spin" /><span>...</span></div>` and inline error row `<div className="flex items-center gap-1.5 text-sm text-destructive" role="alert"><XCircle className="h-4 w-4" /><span>...</span></div>`. -->
<!-- services/aio/projects.ts: exports `fetchAioProjects(baseUrl: string, token: string): Promise<AioProject[]>`. -->
<!-- services/aio/types.ts: `interface AioProject { id: number; projectKey: string; name: string; }`. -->
<!-- services/aio/index.ts barrel exports `fetchAioProjects` (verified by AioProjectsPage.tsx import path `@/services/aio`). -->
<!-- components/ui/select.tsx: exports `Select`, `SelectTrigger`, `SelectContent`, `SelectItem` (Radix shadcn). `SelectTrigger` supports `id`, `className`, `disabled`. -->
<!-- IntegrationsSection.test.tsx existing mock (lines 5-12): `const mockStore = { aioEnabled: false, setAioEnabled: vi.fn() }; vi.mock('../../stores/settings.store', () => ({ useSettingsStore: () => mockStore }))`. Mock object must be extended with `selectedAioProjectKey` + `setSelectedAioProjectKey` (Pitfall 4). -->
<!-- AioProjectsPage.test.tsx pattern (lines 5-30): `vi.mock('@/services/stronghold', () => ({ readSecret: vi.fn().mockResolvedValue('test-jira-token') })); vi.mock('@/services/aio', () => ({ fetchAioProjects: vi.fn() })); vi.mock('@/stores/auth.store', () => ({ useAuthStore: () => ({ jiraBaseUrl: 'https://jira.example.com' }) }));` + `QueryClient` wrapper. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AIO project picker block to IntegrationsSection.tsx</name>
  <files>taskflow/src/routes/settings/IntegrationsSection.tsx</files>
  <read_first>
    - taskflow/src/routes/settings/IntegrationsSection.tsx (full file — confirm the existing toggle structure to preserve)
    - taskflow/src/routes/dashboard/AioProjectsPage.tsx (lines 1-31 — credential loader + useQuery shape to mirror)
    - taskflow/src/routes/onboarding/GitLabStep.tsx (lines 107-136 — Select picker shape, selected-name lookup, placeholder string)
    - taskflow/src/routes/settings/ConnectionsSection.tsx (lines 125-160 — inline Loader2 + XCircle status row patterns)
    - taskflow/src/services/aio/projects.ts (confirm fetchAioProjects signature: `(baseUrl: string, token: string) =&gt; Promise&lt;AioProject[]&gt;`)
    - taskflow/src/services/aio/types.ts (confirm AioProject shape: `{ id, projectKey, name }`)
    - taskflow/src/components/ui/select.tsx (confirm Select/SelectTrigger/SelectContent/SelectItem exports and that SelectTrigger accepts `id`, `className`, `disabled`)
    - taskflow/src/components/ui/label.tsx (confirm Label accepts `htmlFor`)
    - .planning/phases/55-aio-project-selection-in-settings/55-UI-SPEC.md (Color matrix lines 102-110; Copywriting lines 116-155; Spacing rules + E-01 exception lines 39-64; Interaction contract lines 186-200)
    - .planning/phases/55-aio-project-selection-in-settings/55-CONTEXT.md (D-01 through D-05, D-14)
    - .planning/phases/55-aio-project-selection-in-settings/55-PATTERNS.md (lines 88-258 — exact import block, credential loader, useQuery shape, Select picker shape, inline loading/error/empty rows, gate pattern, subsection spacing rule)
    - .planning/phases/55-aio-project-selection-in-settings/55-RESEARCH.md (Pitfalls 1, 3; Open Question 1 on missing-credentials UX — apply default of perpetual loading state to match AioProjectsPage behavior; do NOT add a cross-section "configure Jira first" message in this plan)
  </read_first>
  <behavior>
    - When `aioEnabled` is `false`, the picker block (label, Select, inline state rows) is NOT in the DOM.
    - When `aioEnabled` is `true`:
      - Credential loader runs once on mount (dependency `[jiraBaseUrl]`) — `readSecret('jira-pat')` → `setToken`; on error → `setToken(null)`.
      - `useQuery` fires with `queryKey: ['aio', jiraBaseUrl, 'projects']`, `queryFn: () => fetchAioProjects(jiraBaseUrl!, token!)`, `enabled: !!jiraBaseUrl && !!token` (Pitfall 1).
      - While `isLoading`, render the inline loading row: `<Loader2 className="h-4 w-4 animate-spin" />` + `<span>Loading projects…</span>` inside `<div className="flex items-center gap-1.5 text-sm text-muted-foreground">` (UI-SPEC color matrix).
      - On `isError`, render the inline error row: `<XCircle className="h-4 w-4" />` + `<span>Couldn't load AIO projects.</span>` + inline `<button type="button" onClick={() => refetch()}>` with `Retry` text, wrapped in `<div className="flex items-center gap-1.5 text-sm text-destructive" role="alert">` (UI-SPEC color matrix + copywriting).
      - When the query resolves with `projects.length === 0`, render the picker as a `<Select disabled value="">` with trigger text `<span className="...text-muted-foreground">No AIO projects available</span>` and empty `<SelectContent />`.
      - When the query resolves with projects, render the enabled `<Select value={selectedAioProjectKey ?? ''} onValueChange={setSelectedAioProjectKey}>` with `<SelectTrigger id="aio-project" className="w-full">`. The trigger renders `selectedProject ? selectedProject.name : <span className="text-muted-foreground">Choose a project...</span>` where `selectedProject = projects.find(p => p.projectKey === selectedAioProjectKey)` (Pitfall 3). The `<SelectContent>` maps each project to `<SelectItem key={p.projectKey} value={p.projectKey}>{p.name}</SelectItem>`.
      - Above the Select, render `<Label htmlFor="aio-project">AIO Project</Label>` (UI-SPEC copywriting). Below the Select, render `<p className="text-xs text-muted-foreground">Pick the AIO Test Management project this app shows.</p>` (UI-SPEC copywriting).
      - The picker block wraps in `<div className="flex flex-col gap-1.5">` (UI-SPEC E-01 spacing exception for label-control vertical gap).
      - The picker block sits INSIDE the existing `<div className="flex flex-col gap-4">` subsection that already contains the toggle (UI-SPEC spacing rule 1 — toggle on top, picker below, gap-4 between them).
    - Selecting an item calls `setSelectedAioProjectKey(projectKey)`. The component does NOT trigger any toast, redirect, banner, useEffect-on-value-change, or useNavigate (D-14 silent persist).
  </behavior>
  <action>
    Modify `taskflow/src/routes/settings/IntegrationsSection.tsx` to add the picker block. The existing toggle row stays intact — only ADD new code.

    1. Imports — add the following named imports (preserve the existing `import { useSettingsStore } from '../../stores/settings.store';`):
       - `import { useQuery } from '@tanstack/react-query';`
       - `import { Loader2, XCircle } from 'lucide-react';`
       - `import { useEffect, useState } from 'react';`
       - `import { Label } from '@/components/ui/label';`
       - `import { Select, SelectContent, SelectItem, SelectTrigger } from '@/components/ui/select';`
       - `import { fetchAioProjects } from '@/services/aio';`
       - `import { readSecret } from '@/services/stronghold';`
       - `import { useAuthStore } from '@/stores/auth.store';`
       Note: `Skeleton` is NOT used (the UI-SPEC color matrix specifies `Loader2` inline spinner for the loading state, not a Skeleton block — keep the picker compact, consistent with `ConnectionsSection.tsx`). Do NOT import `Skeleton`.

    2. Inside the component, EXTEND the existing destructure from `useSettingsStore()` to also pull `selectedAioProjectKey` and `setSelectedAioProjectKey`:
       `const { aioEnabled, setAioEnabled, selectedAioProjectKey, setSelectedAioProjectKey } = useSettingsStore();`

    3. Below the destructure, add the credential loader + query (only relevant when `aioEnabled === true`, but the hooks must run unconditionally per React Rules of Hooks — the rendered output gates on `aioEnabled`):
       - `const { jiraBaseUrl } = useAuthStore();`
       - `const [token, setToken] = useState<string | null>(null);`
       - `useEffect(() => { readSecret('jira-pat').then(setToken).catch(() => setToken(null)); }, [jiraBaseUrl]);` (dependency is `[jiraBaseUrl]` per RESEARCH.md — user can change the URL in ConnectionsSection mid-session)
       - `const { data: projects, isLoading, isError, refetch } = useQuery({ queryKey: ['aio', jiraBaseUrl, 'projects'], queryFn: () => fetchAioProjects(jiraBaseUrl!, token!), enabled: !!jiraBaseUrl && !!token });` (the `enabled` guard is mandatory per Pitfall 1)
       - `const selectedProject = projects?.find((p) => p.projectKey === selectedAioProjectKey);` (Pitfall 3 — required for trigger label lookup)

    4. In the JSX, INSIDE the existing `<div className="flex flex-col gap-4">` subsection (the one containing `<h3>AIO Test Management</h3>` and the toggle `<label>`), AFTER the toggle `<label>` and as a sibling element, add a conditional block: `{aioEnabled && (...)}` containing the picker UI.

       The picker UI structure (exact JSX):
       - Outer wrapper: `<div className="flex flex-col gap-1.5">` (E-01 exception)
       - First child: `<Label htmlFor="aio-project">AIO Project</Label>`
       - Second child: branch on query state:
         - If `isLoading`: render `<div className="flex items-center gap-1.5 text-sm text-muted-foreground"><Loader2 className="h-4 w-4 animate-spin" /><span>Loading projects…</span></div>`
         - Else if `isError`: render `<div className="flex items-center gap-1.5 text-sm text-destructive" role="alert"><XCircle className="h-4 w-4" /><span>Couldn't load AIO projects. <button type="button" onClick={() => refetch()} className="underline hover:no-underline">Retry</button></span></div>`
         - Else if `projects && projects.length === 0`: render `<Select disabled value=""><SelectTrigger id="aio-project" className="w-full"><span className="flex flex-1 text-left text-sm text-muted-foreground">No AIO projects available</span></SelectTrigger><SelectContent /></Select>`
         - Else (data loaded with at least one project): render the full enabled `<Select value={selectedAioProjectKey ?? ''} onValueChange={setSelectedAioProjectKey}><SelectTrigger id="aio-project" className="w-full"><span className="flex flex-1 text-left text-sm">{selectedProject ? selectedProject.name : <span className="text-muted-foreground">Choose a project...</span>}</span></SelectTrigger><SelectContent>{projects.map((p) => <SelectItem key={p.projectKey} value={p.projectKey}>{p.name}</SelectItem>)}</SelectContent></Select>`
       - Third child (always rendered, below the Select / state row): `<p className="text-xs text-muted-foreground">Pick the AIO Test Management project this app shows.</p>`

    5. Do NOT introduce any of the following:
       - No `useNavigate`, no `useNavigation`, no router calls.
       - No `useEffect` watching `selectedAioProjectKey` (D-14 silent persist — selection only writes to the store).
       - No toast / banner / dialog.
       - No new component files.
       - No fenced code blocks or comments referencing TODO / FIXME / "v1".
       - No `aria-live` regions (the existing `role="alert"` on the error row is sufficient — matches `GitLabStep.tsx:102`).
       - No project key suffix in the trigger or dropdown items (UI-SPEC copywriting: name-only by default).

    Constraints (UI-SPEC + RESEARCH):
    - Class names MUST match the patterns above verbatim (`gap-1.5` for E-01 cases; `gap-4` is the toggle/picker container that already exists).
    - The `id="aio-project"` on `SelectTrigger` MUST match `<Label htmlFor="aio-project">` for a11y.
    - The error message string is exactly `Couldn't load AIO projects.` (UI-SPEC copywriting); the loading string is exactly `Loading projects…`; the placeholder string is exactly `Choose a project...`; the helper string is exactly `Pick the AIO Test Management project this app shows.`; the empty string is exactly `No AIO projects available`. The label is exactly `AIO Project`.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npm test -- --run src/routes/settings/IntegrationsSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fetchAioProjects" taskflow/src/routes/settings/IntegrationsSection.tsx` returns >= 2 (import + call inside `queryFn`).
    - `grep -c "readSecret('jira-pat')" taskflow/src/routes/settings/IntegrationsSection.tsx` returns 1.
    - `grep -c "'aio', jiraBaseUrl, 'projects'" taskflow/src/routes/settings/IntegrationsSection.tsx` returns 1.
    - `grep -c "enabled: !!jiraBaseUrl &amp;&amp; !!token" taskflow/src/routes/settings/IntegrationsSection.tsx` returns 1 (Pitfall 1).
    - `grep -c "setSelectedAioProjectKey" taskflow/src/routes/settings/IntegrationsSection.tsx` returns >= 2 (destructure + onValueChange).
    - `grep -c "selectedProject" taskflow/src/routes/settings/IntegrationsSection.tsx` returns >= 1 (Pitfall 3 lookup).
    - `grep -c 'AIO Project' taskflow/src/routes/settings/IntegrationsSection.tsx` returns >= 1 (Label text — note: also matches `AIO Test Management` heading, accept >=1).
    - `grep -F "Pick the AIO Test Management project this app shows." taskflow/src/routes/settings/IntegrationsSection.tsx | wc -l` returns 1 (helper text verbatim).
    - `grep -F "Choose a project..." taskflow/src/routes/settings/IntegrationsSection.tsx | wc -l` returns 1.
    - `grep -F "Loading projects" taskflow/src/routes/settings/IntegrationsSection.tsx | wc -l` returns 1.
    - `grep -F "Couldn't load AIO projects." taskflow/src/routes/settings/IntegrationsSection.tsx | wc -l` returns 1.
    - `grep -F "No AIO projects available" taskflow/src/routes/settings/IntegrationsSection.tsx | wc -l` returns 1.
    - `grep -c 'htmlFor="aio-project"' taskflow/src/routes/settings/IntegrationsSection.tsx` returns 1.
    - `grep -c 'id="aio-project"' taskflow/src/routes/settings/IntegrationsSection.tsx` returns >= 1 (SelectTrigger id; appears in both empty-state and loaded-state branches — accept 1 or 2).
    - `grep -c "useNavigate\\|useNavigation" taskflow/src/routes/settings/IntegrationsSection.tsx` returns 0 (D-14 silent persist — no redirect).
    - `grep -c "toast" taskflow/src/routes/settings/IntegrationsSection.tsx` returns 0 (D-14 silent persist).
    - `grep -c "role=\"alert\"" taskflow/src/routes/settings/IntegrationsSection.tsx` returns 1.
    - `cd taskflow &amp;&amp; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `IntegrationsSection.tsx` renders the existing toggle unchanged AND, when `aioEnabled === true`, a fully-wired AIO project picker with loading / error / empty / loaded states matching the UI-SPEC color matrix and copywriting verbatim. Picker is hidden when `aioEnabled === false`. Selection persists silently to the store (D-14). All strings, class names, and ARIA attributes match the UI-SPEC. TypeScript compiles.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend IntegrationsSection.test.tsx with picker tests</name>
  <files>taskflow/src/routes/settings/IntegrationsSection.test.tsx</files>
  <read_first>
    - taskflow/src/routes/settings/IntegrationsSection.test.tsx (full file — confirm existing `mockStore` shape lines 5-12 and existing toggle tests lines 31-47)
    - taskflow/src/routes/dashboard/AioProjectsPage.test.tsx (full file — confirm mock factory patterns for `@/services/stronghold`, `@/services/aio`, `@/stores/auth.store`, and `QueryClient` wrapper)
    - .planning/phases/55-aio-project-selection-in-settings/55-PATTERNS.md (lines 260-329 — exact mock-extension shape and test cases)
    - .planning/phases/55-aio-project-selection-in-settings/55-CONTEXT.md (D-01, D-02, D-03, D-05, D-14 — behaviors to test)
    - .planning/phases/55-aio-project-selection-in-settings/55-RESEARCH.md (Pitfall 4 — mock objects must include selectedAioProjectKey + setSelectedAioProjectKey)
  </read_first>
  <behavior>
    - Test 1 (D-02 gate hidden): When `mockStore.aioEnabled = false`, the picker label `AIO Project` is NOT in the DOM (use `queryByText` / `queryByLabelText`).
    - Test 2 (D-03 render): When `mockStore.aioEnabled = true`, `fetchAioProjects` returns 3 projects, picker renders 3 `<SelectItem>` entries (the SelectContent items are only mounted when the dropdown opens — assert via opening the dropdown with `user.click` on the trigger, then `findByRole('option')` or similar; alternatively assert against the projects in the data via the trigger label when a project is selected).
    - Test 3 (D-03 change handler): When `mockStore.aioEnabled = true` and projects load, clicking the trigger and selecting an option invokes `mockStore.setSelectedAioProjectKey` with the chosen `projectKey`. Use Testing Library `userEvent` for the click; assert `vi.mocked(mockStore.setSelectedAioProjectKey)` was called with the expected value.
    - Test 4 (D-05 loading): When `fetchAioProjects` is left pending (return a never-resolving Promise), the loading row text `Loading projects…` is visible.
    - Test 5 (D-05 error): When `fetchAioProjects` rejects, the error text `Couldn't load AIO projects.` and the `Retry` button are visible.
    - Test 6 (D-05 empty): When `fetchAioProjects` resolves to `[]`, the trigger renders `No AIO projects available` and the Select is disabled (assert via `data-disabled` or `aria-disabled` attribute on the trigger button).
    - Test 7 (D-14 silent persist — negative assertion): Selecting an item does NOT call any `vi.fn()` for `useNavigate`, no `toast` is dispatched. Simplest expression: spy on `setSelectedAioProjectKey` and assert it was called; no `useNavigate` mock is imported in the test (so this is implicitly satisfied — note in a comment).
  </behavior>
  <action>
    Modify `taskflow/src/routes/settings/IntegrationsSection.test.tsx` as follows:

    1. Extend `mockStore` (currently lines 5-12) to include `selectedAioProjectKey: null` (mutable default) and `setSelectedAioProjectKey: vi.fn()`. Keep the existing `aioEnabled: false, setAioEnabled: vi.fn()` properties intact.

    2. Add three new `vi.mock` factory calls at the top of the file (after the existing settings store mock):
       - `vi.mock('@/services/stronghold', () => ({ readSecret: vi.fn().mockResolvedValue('test-jira-token') }));`
       - `vi.mock('@/services/aio', () => ({ fetchAioProjects: vi.fn() }));`
       - `vi.mock('@/stores/auth.store', () => ({ useAuthStore: () => ({ jiraBaseUrl: 'https://jira.example.com' }) }));`

    3. Add imports for `QueryClient`, `QueryClientProvider` from `@tanstack/react-query`, `userEvent` from `@testing-library/user-event`, and `fetchAioProjects` from `@/services/aio` (for typed `vi.mocked(...)` access). Add `import { waitFor, screen, within } from '@testing-library/react'` if not already imported.

    4. Add a `makeClient()` helper that returns `new QueryClient({ defaultOptions: { queries: { retry: false } } })` (matches `AioProjectsPage.test.tsx`).

    5. Add a `renderWithClient(ui)` helper that wraps the component in `<QueryClientProvider client={makeClient()}>` before passing to `render`.

    6. Add a new `describe('IntegrationsSection — AIO project picker', ...)` block AFTER the existing toggle describe block. Inside, add a `beforeEach` that:
       - Calls `vi.clearAllMocks()`.
       - Resets `mockStore.aioEnabled = false; mockStore.selectedAioProjectKey = null;` (mutate the shared object directly).
       - Resets `vi.mocked(fetchAioProjects).mockReset()`.

    7. Add the 6 picker tests covering the behaviors in `<behavior>` above:
       - "hides the picker when aioEnabled is false"
       - "renders the project list when aioEnabled is true and the query resolves"
       - "calls setSelectedAioProjectKey with the projectKey when an option is selected"
       - "shows the loading row while the query is pending"
       - "shows the error row and a Retry button when the query rejects"
       - "shows a disabled Select with 'No AIO projects available' when projects=[]"

       For each test that requires `aioEnabled=true`: set `mockStore.aioEnabled = true` BEFORE rendering. For tests with project data: configure `vi.mocked(fetchAioProjects).mockResolvedValue([{ id: 1, projectKey: 'PROJ1', name: 'Project One' }, { id: 2, projectKey: 'PROJ2', name: 'Project Two' }, { id: 3, projectKey: 'PROJ3', name: 'Project Three' }])`. Use `await waitFor(() => expect(...).toBeInTheDocument())` for async assertions.

       For the change-handler test (Test 3): use `userEvent.click` on the SelectTrigger, then `userEvent.click` on the desired `SelectItem` (locate by role `option` and accessible name). Assert `vi.mocked(mockStore.setSelectedAioProjectKey)` was called with the projectKey. Note: shadcn Radix Select renders `<SelectItem>` lazily into a portal — use `screen.findByRole('option', { name: 'Project Two' })` to wait for portal mount.

       For the empty test (Test 6): the trigger button is rendered but disabled. Assert the trigger has `aria-disabled="true"` or equivalent via `screen.getByText('No AIO projects available')` and the surrounding trigger has the disabled attribute.

    8. Do NOT modify any existing toggle test (`describe('IntegrationsSection — aioEnabled toggle', ...)` or whatever its current name is). Do NOT modify the existing `vi.mock` for `useSettingsStore` other than extending the `mockStore` literal.

    Constraints:
    - Tests MUST use the `userEvent.setup()` API for clicks (matches modern React Testing Library convention). If the repo's existing pattern uses `fireEvent`, fall back to that — read the existing test file to confirm.
    - Do NOT import the real `useSettingsStore` (it's already mocked) or the real `fetchAioProjects`.
    - Do NOT include `aioEnabled` test changes that would alter existing assertions; the existing tests remain valid because `mockStore.aioEnabled = false` is reset in the new picker block's `beforeEach`.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npm test -- --run src/routes/settings/IntegrationsSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AIO project picker" taskflow/src/routes/settings/IntegrationsSection.test.tsx` returns >= 1.
    - `grep -c "fetchAioProjects" taskflow/src/routes/settings/IntegrationsSection.test.tsx` returns >= 2 (vi.mock + at least one mockResolvedValue/mockRejectedValue).
    - `grep -c "setSelectedAioProjectKey: vi.fn()" taskflow/src/routes/settings/IntegrationsSection.test.tsx` returns 1.
    - `grep -c "QueryClientProvider" taskflow/src/routes/settings/IntegrationsSection.test.tsx` returns >= 1.
    - `grep -c "Couldn't load AIO projects" taskflow/src/routes/settings/IntegrationsSection.test.tsx` returns >= 1.
    - `grep -c "Loading projects" taskflow/src/routes/settings/IntegrationsSection.test.tsx` returns >= 1.
    - `grep -c "No AIO projects available" taskflow/src/routes/settings/IntegrationsSection.test.tsx` returns >= 1.
    - `cd taskflow &amp;&amp; npm test -- --run src/routes/settings/IntegrationsSection.test.tsx` reports all tests passing (existing toggle tests + 6 new picker tests).
  </acceptance_criteria>
  <done>
    `IntegrationsSection.test.tsx` has the extended `mockStore`, three new service/store mocks, a `QueryClient` wrapper helper, and a new `'IntegrationsSection — AIO project picker'` describe block with 6 passing tests covering gate-hidden, render, change-handler, loading, error, and empty states. All existing toggle tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live AIO REST endpoint (`/rest/aio-tcms/1.0/project`) | Authenticated via Bearer PAT; responses are trusted enough to drive `<SelectItem>` keys and the persisted `selectedAioProjectKey` value. |
| Stronghold credential store (`readSecret('jira-pat')`) | Read-only at component mount; existing trust boundary inherited from Phase 51 / `apiFetch` infrastructure. |
| User input via `<Select>` | The user can only pick a `projectKey` value that was rendered from the authenticated AIO response — no free-text input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-04 | Tampering | `selectedAioProjectKey` value flowing into URL construction in Plan 03 | mitigate | The picker's `onValueChange` callback receives a `projectKey` string sourced exclusively from a `<SelectItem value={p.projectKey}>` rendered from the authenticated `fetchAioProjects` response. No free-text input. Sidebar.tsx (Plan 03) constructs `/aio-project/${selectedAioProjectKey}` using this value; React Router's `to` prop URL-encodes path segments natively, and Tauri's same-origin context prevents external redirection. |
| T-55-05 | Information Disclosure | `readSecret('jira-pat')` in client component | accept | This is the existing established pattern from `AioProjectsPage.tsx` and `ConnectionsSection.tsx`. PAT lives in Stronghold; the in-memory `token` state is short-lived (component lifetime). Picker does NOT log or render the token. |
| T-55-06 | Denial of Service | `useQuery` firing on toggle | accept | `enabled: !!jiraBaseUrl && !!token` guard prevents the query from firing with null credentials (Pitfall 1). Standard React Query retry policy (`retry: 1` per `main.tsx`) caps retries. `gcTime: Infinity` keeps the cache warm per session — no thundering-herd refetch on tab focus. |
| T-55-07 | Repudiation | Silent persist (D-14) | accept | No audit log requirement for a single-user desktop preference. The action is reversible by changing the selection in Settings. |
</threat_model>

<verification>
After both tasks complete:

1. `cd taskflow && npm test -- --run src/routes/settings/IntegrationsSection.test.tsx` → all tests green (existing toggle + 6 new picker tests).
2. `cd taskflow && npx tsc --noEmit` → no TypeScript errors.
3. `grep -n "selectedAioProjectKey\\|fetchAioProjects" taskflow/src/routes/settings/IntegrationsSection.tsx` → at least 4 matches (destructure, query, lookup, change handler indirectly).
4. Full test suite spot-check: `cd taskflow && npm test -- --run` → no unrelated regressions.
</verification>

<success_criteria>
- Picker is hidden when `aioEnabled === false` and visible when `aioEnabled === true` (D-02).
- Picker renders projects via `fetchAioProjects` with cache key `['aio', jiraBaseUrl, 'projects']` (D-04).
- Picker has the four UI-SPEC states (loading, error, empty, loaded) with the exact copywriting strings.
- Selection writes silently to the store via `setSelectedAioProjectKey(projectKey)` — no navigate, no toast (D-14).
- Trigger displays the project `name` for the currently-stored `projectKey` via the `find` lookup (Pitfall 3).
- 6 new picker tests pass alongside the existing toggle tests.
</success_criteria>

<output>
After completion, create `.planning/phases/55-aio-project-selection-in-settings/55-02-SUMMARY.md`. Note: this plan converts the deleted list page's selection surface into the Settings picker — record the import additions and the exact JSX structure for Plan 04 (route/file deletion) to cross-reference.
</output>
