---
phase: 55-aio-project-selection-in-settings
plan: 04
type: execute
wave: 3
depends_on: [55-02, 55-03]
files_modified:
  - taskflow/src/routes/routes.tsx
  - taskflow/src/routes/dashboard/AioProjectsPage.tsx
  - taskflow/src/routes/dashboard/AioProjectsPage.test.tsx
  - taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [AION-02]
requirements_addressed: [AION-02]
tags: [destructive-cleanup, route-removal, traceability, aion-02, aio]

must_haves:
  truths:
    - "The /aio-projects route no longer exists in routes.tsx (D-12)"
    - "The lazy import of AioProjectsPage no longer exists in routes.tsx (D-12)"
    - "AioProjectsPage.tsx, AioProjectsPage.test.tsx, and AioProjectsSkeleton.tsx no longer exist on disk (D-11)"
    - "REQUIREMENTS.md AION-02 traceability row points at Phase 55 instead of Phase 52 (D-13)"
    - "REQUIREMENTS.md footer 'Last updated' timestamp is refreshed (D-13)"
    - "The /aio-project/:projectKey, /aio-cycle/:projectKey/:cycleKey, and /aio-cycle/:projectKey/:cycleKey/run/:runId routes STAY untouched (they are the deep-link target after Phase 55)"
    - "No production import of AioProjectsPage, AioProjectsSkeleton remains anywhere in src/ after deletion"
  artifacts:
    - path: "taskflow/src/routes/routes.tsx"
      provides: "Cleaned route table without /aio-projects entry or AioProjectsPage lazy import"
      contains: "/aio-project/:projectKey"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated AION-02 traceability row pointing to Phase 55"
      contains: "| AION-02 | Phase 55 |"
  key_links:
    - from: "Phase 55 picker (Plan 02) inside IntegrationsSection.tsx"
      to: "AION-02 traceability"
      via: "REQUIREMENTS.md table row update + the picker dropdown surfaces all AIO projects (subsuming the deleted list page)"
      pattern: "AION-02 \\| Phase 55"
    - from: "Phase 55 sidebar deep-link (Plan 03)"
      to: "/aio-project/:projectKey route"
      via: "NavLink to= /aio-project/${selectedAioProjectKey} resolves to the existing Phase 52 route which STAYS"
      pattern: "/aio-project/:projectKey"
---

<objective>
Destructive cleanup wave: delete the now-redundant `AioProjectsPage.tsx`, `AioProjectsPage.test.tsx`, and `AioProjectsSkeleton.tsx` files (D-11); remove the `/aio-projects` route entry and `AioProjectsPage` lazy import from `routes.tsx` (D-12); re-point the AION-02 traceability row in `.planning/REQUIREMENTS.md` from Phase 52 to Phase 55 and refresh the footer "Last updated" timestamp (D-13).

Purpose: This plan closes the loop on Phase 55's refactor. The picker (Plan 02) now surfaces every AIO project a user can pick, subsuming the original "list of all AIO test projects" requirement. The sidebar entry (Plan 03) deep-links to a single project's overview — the dedicated multi-project list page is no longer reachable, no longer needed, and no longer documented in the requirement traceability as a Phase 52 surface. Removing the dead code prevents the "stale `/aio-projects` route" pitfall (RESEARCH.md Pitfall 5) and keeps `routes.tsx` clean.

Why this plan is Wave 3 (depends on Plans 02 AND 03): if Plan 04 ran before Plan 02 or Plan 03 completed, there would be a window where the user could not reach AIO projects at all — the picker would not yet exist (no Settings surface), the sidebar would still link to a deleted route, and the user would be stranded. Wave-3 ordering guarantees the new selection surfaces are functional before the old one is removed.

Output: 3 deleted files, 2 lines removed from `routes.tsx` (lazy import + route entry), 1 row updated + 1 timestamp refreshed in `REQUIREMENTS.md`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-aio-project-selection-in-settings/55-CONTEXT.md
@.planning/phases/55-aio-project-selection-in-settings/55-RESEARCH.md
@.planning/phases/55-aio-project-selection-in-settings/55-PATTERNS.md
@.planning/phases/55-aio-project-selection-in-settings/55-02-PLAN.md
@.planning/phases/55-aio-project-selection-in-settings/55-03-PLAN.md
@taskflow/src/routes/routes.tsx

<interfaces>
<!-- Verified anchors (2026-05-14): -->
<!-- routes.tsx line 21 (per PATTERNS.md): `const AioProjectsPage = lazy(() => import('./dashboard/AioProjectsPage'));` -->
<!-- routes.tsx line 52: `{ path: '/aio-projects', element: withLazy(AioProjectsPage) },` -->
<!-- routes.tsx lines 53-58 STAY (do NOT touch):
       `{ path: '/aio-project/:projectKey', element: withLazy(AioProjectOverviewPage) },`
       `{ path: '/aio-cycle/:projectKey/:cycleKey', element: withLazy(AioCycleDetailPage) },`
       `{ path: '/aio-cycle/:projectKey/:cycleKey/run/:runId', element: withLazy(AioTestRunDetailPage) }` -->
<!-- REQUIREMENTS.md line 66: `| AION-02 | Phase 52 | Pending |` — change Phase 52 → Phase 55 -->
<!-- REQUIREMENTS.md line 87 (footer): `*Last updated: 2026-05-12 — traceability populated after roadmap creation (Phases 51-54)*` — refresh date and note -->
<!-- RESEARCH.md confirms AION-02 requirement TEXT does not need to change: "User can view a list of all AIO test projects" is technically still satisfied because the picker dropdown shows all 80 projects in a scrollable list. -->
<!-- RESEARCH.md Phase 54 Cross-Impact Analysis: `AioTestRunsSection.tsx` calls `fetchAioProjects` internally (line 429) — does NOT depend on the list page. Deleting the list page is safe wrt Phase 54. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the three AIO list page files</name>
  <files>
    taskflow/src/routes/dashboard/AioProjectsPage.tsx,
    taskflow/src/routes/dashboard/AioProjectsPage.test.tsx,
    taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx
  </files>
  <read_first>
    - taskflow/src/routes/dashboard/AioProjectsPage.tsx (full file — confirm no exports are imported elsewhere besides routes.tsx line 21)
    - taskflow/src/routes/dashboard/AioProjectsPage.test.tsx (full file — confirm it tests only the page itself, no shared utilities exported)
    - taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx (full file — confirm not imported anywhere outside AioProjectsPage.tsx)
    - .planning/phases/55-aio-project-selection-in-settings/55-CONTEXT.md (D-11 — file list to delete)
    - .planning/phases/55-aio-project-selection-in-settings/55-RESEARCH.md (Phase 54 Cross-Impact — confirms Phase 54 has no dependency on these files)
  </read_first>
  <action>
    Delete the following three files exactly. Use `git rm` so the deletions are staged for commit and tracked in version control:

    1. `taskflow/src/routes/dashboard/AioProjectsPage.tsx`
    2. `taskflow/src/routes/dashboard/AioProjectsPage.test.tsx`
    3. `taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx`

    BEFORE deleting, run a project-wide grep to confirm NO other file imports any export from these three modules. The command to run from the project root:
    `grep -rn "AioProjectsPage\\|AioProjectsSkeleton" taskflow/src/ --include='*.ts' --include='*.tsx' | grep -v "taskflow/src/routes/dashboard/AioProjectsPage" | grep -v "taskflow/src/routes/dashboard/AioProjectsSkeleton"`
    The expected output is exactly one line: `taskflow/src/routes/routes.tsx` referencing `AioProjectsPage` in the lazy import (line 21). If ANY other file matches, STOP and surface the file to the planner — there is an undocumented import that must be addressed before deletion.

    After the grep returns the expected single match, perform the deletions:
    - `git rm taskflow/src/routes/dashboard/AioProjectsPage.tsx`
    - `git rm taskflow/src/routes/dashboard/AioProjectsPage.test.tsx`
    - `git rm taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx`

    Do NOT delete or modify any of these neighbor files (all stay):
    - `taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx` (Phase 52 — deep-link target)
    - `taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx` (Phase 52)
    - `taskflow/src/routes/dashboard/AioCycleDetailPage.tsx` (Phase 53)
    - `taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx` (Phase 54)
    - `taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx` (Phase 54 — uses `fetchAioProjects` directly, independent of this page)

    Do NOT touch `taskflow/src/services/aio/projects.ts` — the picker (Plan 02) and the Phase 54 issue-detail section both consume `fetchAioProjects`.
  </action>
  <verify>
    <automated>test ! -f taskflow/src/routes/dashboard/AioProjectsPage.tsx &amp;&amp; test ! -f taskflow/src/routes/dashboard/AioProjectsPage.test.tsx &amp;&amp; test ! -f taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f taskflow/src/routes/dashboard/AioProjectsPage.tsx` exits non-zero (file does not exist).
    - `test -f taskflow/src/routes/dashboard/AioProjectsPage.test.tsx` exits non-zero.
    - `test -f taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx` exits non-zero.
    - `git status --porcelain taskflow/src/routes/dashboard/AioProjectsPage.tsx taskflow/src/routes/dashboard/AioProjectsPage.test.tsx taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx` shows all three as deleted (lines start with `D`).
    - `grep -rn "AioProjectsSkeleton" taskflow/src/ --include='*.ts' --include='*.tsx'` returns 0 matches (the skeleton class name was only used by the deleted page).
    - `grep -rn "AioProjectsPage" taskflow/src/ --include='*.ts' --include='*.tsx'` returns 1 line — `routes.tsx` lazy import (which Task 2 then removes). After Task 2, this grep returns 0 matches.
  </acceptance_criteria>
  <done>
    Three files deleted via `git rm`. No undocumented imports exist before deletion. Phase 52's `AioProjectOverviewPage.tsx` and the rest of the AIO surface remain intact. The deletions are staged for commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove /aio-projects route entry and AioProjectsPage lazy import from routes.tsx</name>
  <files>taskflow/src/routes/routes.tsx</files>
  <read_first>
    - taskflow/src/routes/routes.tsx (full file — confirm line 21 lazy import and line 52 route entry; confirm lines 53-58 STAY)
    - .planning/phases/55-aio-project-selection-in-settings/55-CONTEXT.md (D-12)
    - .planning/phases/55-aio-project-selection-in-settings/55-PATTERNS.md (lines 553-577 — exact diff lines)
  </read_first>
  <action>
    Modify `taskflow/src/routes/routes.tsx` to remove exactly two lines:

    1. The lazy import declaration at line 21: `const AioProjectsPage = lazy(() => import('./dashboard/AioProjectsPage'));` — DELETE.

    2. The route table entry at line 52: `{ path: '/aio-projects', element: withLazy(AioProjectsPage) },` — DELETE.

    Preserve every other lazy import, every other route entry, the `withLazy` import, the `lazy` import, the routes array structure, and especially the three downstream AIO routes (lines 53-58 of the original file):
    - `{ path: '/aio-project/:projectKey', element: withLazy(AioProjectOverviewPage) },` — STAYS (sidebar deep-link target per Plan 03)
    - `{ path: '/aio-cycle/:projectKey/:cycleKey', element: withLazy(AioCycleDetailPage) },` — STAYS (Phase 53)
    - `{ path: '/aio-cycle/:projectKey/:cycleKey/run/:runId', element: withLazy(AioTestRunDetailPage) },` — STAYS (Phase 54)

    Do NOT introduce a redirect (e.g., `{ path: '/aio-projects', element: <Navigate to="/settings" /> }`). The CONTEXT.md / UI-SPEC explicitly accepts that paste-in deep-links to `/aio-projects` will hit the existing router fallback — no migration banner, no toast (UI-SPEC "Destructive actions / confirmations" table).

    Do NOT change line ordering of the remaining routes. Do NOT change `withLazy` semantics. Do NOT remove unrelated lazy imports.

    After saving, the file should have exactly two fewer lines than before (one fewer lazy import declaration, one fewer route entry).
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AioProjectsPage" taskflow/src/routes/routes.tsx` returns 0.
    - `grep -c "/aio-projects" taskflow/src/routes/routes.tsx` returns 0 (the literal `/aio-projects` no longer appears anywhere in this file).
    - `grep -c "/aio-project/:projectKey" taskflow/src/routes/routes.tsx` returns 1 (downstream route STAYS).
    - `grep -c "/aio-cycle/:projectKey/:cycleKey" taskflow/src/routes/routes.tsx` returns >= 1 (Phase 53 routes STAY).
    - `cd taskflow &amp;&amp; npx tsc --noEmit` exits 0 (deletion does not leave dangling references).
    - `cd taskflow &amp;&amp; npm test -- --run` reports the full test suite green (no regressions from the deletions — Phase 52 `AioProjectOverviewPage`, Phase 53 cycle pages, Phase 54 issue detail, Phase 55 picker + sidebar tests all still pass).
  </acceptance_criteria>
  <done>
    `routes.tsx` no longer contains the `AioProjectsPage` lazy import or the `/aio-projects` route. The three downstream AIO routes (`/aio-project/:projectKey`, `/aio-cycle/:projectKey/:cycleKey`, `/aio-cycle/:projectKey/:cycleKey/run/:runId`) are intact. TypeScript compiles. Full test suite green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-point AION-02 traceability to Phase 55 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — confirm AION-02 row at line 66 and footer timestamp at line 87)
    - .planning/phases/55-aio-project-selection-in-settings/55-CONTEXT.md (D-13 — traceability update; RESEARCH.md AION-02 Traceability Update Mechanics)
    - .planning/phases/55-aio-project-selection-in-settings/55-RESEARCH.md (AION-02 Traceability Update Mechanics section — confirms requirement TEXT does NOT need to change; only the phase number in the traceability row and the footer timestamp)
  </read_first>
  <action>
    Modify `.planning/REQUIREMENTS.md` exactly as follows:

    1. In the traceability table (line ~66), change the AION-02 row from:
       `| AION-02 | Phase 52 | Pending |`
       to:
       `| AION-02 | Phase 55 | Pending |`

       Do NOT add additional columns, do NOT change `Pending`, do NOT split the row into multiple phase references. The convention everywhere else in the table is single-phase-per-row, and AION-02 is now wholly delivered by Phase 55 (the picker in Settings is the new "list of all AIO test projects" surface).

    2. Do NOT modify the AION-02 requirement text in the `### AIO Navigation` section (line 13): `- [ ] **AION-02**: User can view a list of all AIO test projects` — the requirement is technically still satisfied (the picker dropdown shows all 80 projects in a scrollable list). Wording change is out of scope per RESEARCH.md.

    3. Update the footer timestamp (line ~87) from:
       `*Last updated: 2026-05-12 — traceability populated after roadmap creation (Phases 51-54)*`
       to:
       `*Last updated: 2026-05-14 — AION-02 traceability re-pointed to Phase 55 (picker in Settings subsumes the deleted list page surface)*`

       Use today's date `2026-05-14` (project current date per session context). If the executor runs on a later date, substitute that date in ISO format.

    4. Do NOT change any other requirement row, do NOT change `Coverage` counts (the totals remain 14 mapped / 0 unmapped — Phase 55 takes over coverage from Phase 52 for AION-02, no new unmapped requirements).
  </action>
  <verify>
    <automated>grep -c "^| AION-02 | Phase 55 | Pending |" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "| AION-02 | Phase 55 | Pending |" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "| AION-02 | Phase 52 | Pending |" .planning/REQUIREMENTS.md` returns 0 (old row replaced).
    - `grep -c "AION-02 traceability re-pointed to Phase 55" .planning/REQUIREMENTS.md` returns 1 (footer note recorded).
    - `grep -c "Unmapped: 0" .planning/REQUIREMENTS.md` returns 1 (coverage block unchanged).
    - The AION-02 requirement text `**AION-02**: User can view a list of all AIO test projects` is unchanged in `### AIO Navigation`.
  </acceptance_criteria>
  <done>
    AION-02 traceability row points at Phase 55, the footer timestamp is refreshed with a note describing the change, the requirement text and coverage counts are unchanged. Closes D-13.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem deletions | `git rm` is a tracked destructive operation; reversible via `git restore` until the commit lands. |
| Route table mutation | `routes.tsx` is the single source of URL → component mapping; a stale entry pointing at a deleted module would crash on lazy import. Removing both the route entry AND the lazy import in lockstep is the only safe sequence. |
| Documentation drift | `REQUIREMENTS.md` traceability supports verifier and downstream-phase auditors; a stale Phase 52 reference would mis-report the surface that delivers AION-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-11 | Tampering | Lazy import dangling after route removal | mitigate | Task 2 removes BOTH the lazy import (line 21) AND the route entry (line 52) in the same commit. The acceptance criteria asserts both grep counts are 0; the automated TS check (`npx tsc --noEmit`) catches dangling references. |
| T-55-12 | Denial of Service | User pastes `/aio-projects` deep-link | accept | Per CONTEXT.md / UI-SPEC, paste-in deep-links to `/aio-projects` will hit the existing route fallback (404 / not-found). Bookmarks are unlikely (Phase 52 shipped 2026-05-13, two days before this phase). No redirect requirement; documenting the behavior in the UI-SPEC is sufficient. |
| T-55-13 | Repudiation | Traceability row update | mitigate | The footer timestamp records the change date and reason. Git history of `REQUIREMENTS.md` provides the audit trail. |
| T-55-14 | Tampering | Accidental deletion of Phase 52/53/54 files | mitigate | Task 1 explicitly enumerates the three files to delete and lists by name every neighbor file that must NOT be touched. Task 1 also includes a project-wide grep to confirm no undocumented imports exist before deletion. |
</threat_model>

<verification>
After all three tasks complete:

1. `cd taskflow && npx tsc --noEmit` → 0 errors (no dangling imports).
2. `cd taskflow && npm test -- --run` → full suite green; specifically:
   - Plan 01 tests (settings store) pass.
   - Plan 02 tests (IntegrationsSection picker) pass.
   - Plan 03 tests (Sidebar gate + dynamic href) pass.
   - Phase 52 `AioProjectOverviewPage` tests still pass (it has no dependency on the deleted list page).
   - Phase 54 `AioTestRunsSection` tests still pass (it calls `fetchAioProjects` directly, independent of the list page).
3. `grep -rn "AioProjectsPage\\|AioProjectsSkeleton" taskflow/src/ --include='*.ts' --include='*.tsx'` returns 0 matches.
4. `grep -c "| AION-02 | Phase 55 | Pending |" .planning/REQUIREMENTS.md` returns 1.
5. `git status --porcelain` shows 3 deletions (AioProjectsPage, .test, Skeleton) + 2 modifications (routes.tsx, REQUIREMENTS.md).
</verification>

<success_criteria>
- Three files deleted: `AioProjectsPage.tsx`, `AioProjectsPage.test.tsx`, `AioProjectsSkeleton.tsx` (D-11).
- `routes.tsx` no longer references `AioProjectsPage` or the `/aio-projects` path; the three downstream AIO routes are intact (D-12).
- `REQUIREMENTS.md` AION-02 row points at Phase 55 and footer is timestamped 2026-05-14 with a one-line rationale (D-13).
- TypeScript compiles and the full test suite passes (no regressions).
- No production import of the deleted modules remains anywhere in `taskflow/src/`.
</success_criteria>

<output>
After completion, create `.planning/phases/55-aio-project-selection-in-settings/55-04-SUMMARY.md`. This SUMMARY is the verifier's primary reference for confirming Phase 55 success criteria: the picker (Plan 02) + sidebar (Plan 03) replace the list page (deleted here); AION-02 is delivered by the Settings picker, not by a dedicated page. Record file deletion paths, the exact `routes.tsx` line numbers removed (pre-edit), and the new AION-02 traceability row text.
</output>
