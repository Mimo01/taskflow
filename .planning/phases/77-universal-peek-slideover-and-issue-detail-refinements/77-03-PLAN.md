---
phase: 77-universal-peek-slideover-and-issue-detail-refinements
plan: 03
type: execute
wave: 3
depends_on: ["77-01", "77-02"]
files_modified:
  - taskflow/src/components/app/PeekPanel.tsx
  - taskflow/src/main.tsx
  - taskflow/src/components/app/CommandPalette.tsx
  - taskflow/src/routes/notifications/NotificationPopover.tsx
  - taskflow/src/components/app/PeekPanel.test.tsx
autonomous: false
requirements: [PEEK-01, PEEK-02, PEEK-03, PEEK-04, PEEK-05, PEEK-06, PEEK-07]
must_haves:
  truths:
    - "PeekPanel renders the full IssueDetailView in single-column with a header bar (key left; Open full page + X right) and a left-edge resize divider (default 480, min 360, max 720, persisted)"
    - "PeekPanel mounts at app/layout level as a flex-row sibling of <main>; underlying view is NOT covered by a backdrop and has no role=dialog (PEEK-03)"
    - "Opening peek = onOpenIssue(key) sets peekIssueKey; clicking another issue swaps the key without closing (PEEK-04); route change closes the peek (D-07)"
    - "Escape and the X both close the peek; Open full page navigates to /issue/:key (PEEK-06/07); Escape does not fire while the command palette is open (Pitfall 6)"
    - "CommandPalette and NotificationPopover receive an onOpenIssue prop so body selection opens the peek and key/full-page still navigates"
  artifacts:
    - path: "taskflow/src/components/app/PeekPanel.tsx"
      provides: "Squeeze layout panel: resize handle + header + IssueDetailView single-column"
      min_lines: 60
      contains: "IssueDetailView"
    - path: "taskflow/src/main.tsx"
      provides: "peekIssueKey/peekPanelWidth state, handleOpenPeek, route-change close, PeekPanel mount, onOpenIssue in outlet context + CommandPalette/NotificationPopover props"
      contains: "PeekPanel"
  key_links:
    - from: "main.tsx outlet context"
      to: "all surfaces (Plan 04)"
      via: "onOpenIssue: handleOpenPeek"
      pattern: "onOpenIssue: handleOpenPeek"
    - from: "PeekPanel onOpenIssue"
      to: "setPeekIssueKey (swap)"
      via: "D-13 swap-in-peek"
      pattern: "setPeekIssueKey"
    - from: "PeekPanel"
      to: "IssueDetailView layout single-column"
      via: "render"
      pattern: "layout=\"single-column\""
---

<objective>
Build the universal non-blocking peek: a CSS squeeze/push layout panel (NOT a Dialog — D-01) mounted at the AppLayout level as a flex-row sibling of `<main>`. PeekPanel renders the shared `IssueDetailView` (from Plan 02) in single-column with a header bar and a persisted, resizable left-edge divider. Wire `peekIssueKey`/`peekPanelWidth` state, `handleOpenPeek`, route-change close, and the `onOpenIssue` outlet-context handler in main.tsx. Add an `onOpenIssue` prop to CommandPalette and NotificationPopover (they receive handlers as direct props, not via outlet context).

Purpose: This delivers the peek mechanism (PEEK-01..07) and the universal seam (`onOpenIssue` in context) that Plan 04's surfaces consume. D-13 swap-in-peek works for free because PeekPanel passes onOpenIssue=setPeekIssueKey to IssueDetailView.
Output: PeekPanel.tsx, modified main.tsx, CommandPalette.tsx, NotificationPopover.tsx; PeekPanel.test.tsx cases activated.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-CONTEXT.md
@.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-RESEARCH.md
@.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-PATTERNS.md
@.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-UI-SPEC.md
@.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-02-SUMMARY.md

> EXECUTOR NOTE (PATTERNS.md analog override — D-05): DISREGARD the 77-PATTERNS.md mapping of PeekPanel.tsx to `IssueDetailSheet.tsx` / `IssueDetailBody`. Per D-05 this phase uses the EXTRACTED `IssueDetailView` component (built in Plan 02, see 77-02-SUMMARY) as the peek body — NOT the legacy IssueDetailSheet/IssueDetailBody. Where PATTERNS.md says "copy IssueDetailBody", read `IssueDetailView` (its layout + onOpenIssue prop) instead. The IssueDetailSheet excerpts in PATTERNS are illustrative of the query/skeleton shape only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PeekPanel (squeeze layout + header + resize + IssueDetailView single-column)</name>
  <read_first>
    - taskflow/src/routes/dashboard/IssueDetailView.tsx (Plan 02 — its prop interface incl. layout + onOpenIssue)
    - taskflow/src/routes/dashboard/IssueDetailPage.tsx (resize handle DOM ~657-664; useResizable call ~319-329)
    - taskflow/src/hooks/useResizable.ts (direction:'left', min/max, onCommit, store-hydration sync)
    - taskflow/src/components/app/CommandPalette.tsx (useHotkeys('escape', ...) pattern ~line 77)
    - taskflow/src/components/ui/button.tsx (Button variants ghost/icon/sm for the header)
    - taskflow/.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-UI-SPEC.md (§ Peek Header Bar, § Peek Panel Structural Rules)
    - taskflow/.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-PATTERNS.md (§ PeekPanel.tsx — outer wrapper, header, hotkey, layout container)
  </read_first>
  <action>
    Create taskflow/src/components/app/PeekPanel.tsx exporting `PeekPanel`. Props: `{ issueKey: string; width: number | null; onWidthChange: (w: number) => void; onClose: () => void; onOpenIssue: (key: string) => void; onNavigateFull: (key: string) => void; paletteOpen: boolean; }`.
    Resize: call useResizable with `initialWidth: width ?? 480`, `min: 360`, `max: 720`, `onCommit: onWidthChange`, `direction: 'left'` (per D-03 / PATTERNS § useResizable). Memoize initialWidth on the width prop only (Pitfall 2 — store hydration sync is handled inside the hook).
    Outer wrapper: `<div className={'relative border-l border-border bg-card overflow-hidden flex flex-col shrink-0' + (isDragging ? '' : ' transition-all duration-200')} style={{ width }}>` (D-04 left border + card surface, no dimming/backdrop; UI-SPEC Color § secondary uses --card). Use the width from useResizable for the style.
    Drag handle: copy the absolute-positioned handle DOM from IssueDetailPage:657-664 but use `cursor-col-resize` (UI-SPEC § Interaction) and aria-label "Resize preview panel". Wire onMouseDown={handleMouseDown}.
    Header bar (UI-SPEC § Peek Header Bar): `<div className="flex items-center justify-between h-10 px-4 border-b border-border shrink-0">` — left: `<span className="text-xs font-mono text-muted-foreground">{issueKey}</span>`; right: a `<Button variant="ghost" size="sm" onClick={() => onNavigateFull(issueKey)}>` with `<ExternalLink className="size-3.5 mr-1" />` and label "Open full page", then a `<Button variant="ghost" size="icon" aria-label="Close preview" onClick={onClose}>` with `<X className="size-4" />`.
    Body: `<div className="flex-1 overflow-hidden"><IssueDetailView issueKey={issueKey} layout="single-column" onOpenIssue={onOpenIssue} /></div>`. Per D-13, onOpenIssue here is the swap handler passed from main.tsx (setPeekIssueKey), so parent/subtask/linked clicks swap the peek.
    Escape: `useHotkeys('escape', onClose, { enableOnFormTags: true, enabled: !!issueKey && !paletteOpen })` (Pitfall 6 / A3 — do not double-dismiss with CommandPalette).
    Import X, ExternalLink from lucide-react; Button from @/components/ui/button; IssueDetailView from @/routes/dashboard/IssueDetailView; useResizable; useHotkeys.
    Do NOT use any @base-ui Dialog/Sheet/portal/backdrop (D-01/D-08 anti-patterns).
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit 2>&1 | grep -iE "PeekPanel" || echo "PeekPanel typecheck clean"</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/components/app/PeekPanel.tsx exists, exports PeekPanel, >= 60 lines
    - `grep -q 'layout="single-column"' taskflow/src/components/app/PeekPanel.tsx` and `grep -q "IssueDetailView" taskflow/src/components/app/PeekPanel.tsx`
    - `grep -q "min: 360" taskflow/src/components/app/PeekPanel.tsx` and `grep -q "max: 720" taskflow/src/components/app/PeekPanel.tsx` and `grep -q "direction: 'left'" taskflow/src/components/app/PeekPanel.tsx`
    - `grep -q "!paletteOpen" taskflow/src/components/app/PeekPanel.tsx` (Escape scope guard)
    - No Dialog/Sheet import: `grep -Ec "from '@/components/ui/sheet'|@base-ui/react.*Dialog" taskflow/src/components/app/PeekPanel.tsx` == 0
    - tsc clean
  </acceptance_criteria>
  <done>PeekPanel renders single-column IssueDetailView with header + resizable divider; Escape guarded against palette; no Dialog/backdrop.</done>
</task>

<task type="auto">
  <name>Task 2: Wire peek state + mount in main.tsx; add onOpenIssue to context + CommandPalette + NotificationPopover</name>
  <read_first>
    - taskflow/src/main.tsx (AppLayout return ~505-595; outlet context ~553-564; handleIssueClick ~336-349; route-change effect ~290-299; CommandPalette mount ~568-578; locate the NotificationPopover mount via `grep -n "NotificationPopover" src/main.tsx`)
    - taskflow/src/components/app/CommandPalette.tsx (CommandPaletteProps ~41-48; handleIssueSelect ~164-168)
    - taskflow/src/routes/notifications/NotificationPopover.tsx (props ~216-220; handleRowClick ~269-284) and NotificationRow.tsx (single button ~189-194)
    - taskflow/src/components/app/PeekPanel.tsx (its prop signature)
    - taskflow/.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-PATTERNS.md (§ main.tsx — state, handler, route-close effect, outlet extension, mount JSX)
  </read_first>
  <action>
    main.tsx:
    (1) State: `const [peekIssueKey, setPeekIssueKey] = useState<string | null>(null);` and read `const peekPanelWidth = useSettingsStore((s) => s.peekPanelWidth); const setPeekPanelWidth = useSettingsStore((s) => s.setPeekPanelWidth);` (Plan 01 added these).
    (2) Handler: `const handleOpenPeek = (issueKey: string) => setPeekIssueKey(issueKey);` near handleIssueClick.
    (3) Route-change close (D-07): add a `prevPathRef = useRef(location.pathname)` and a useEffect on `location.pathname` that calls `setPeekIssueKey(null)` and updates the ref when pathname changes. Do NOT couple this to the breadcrumb-reset effect's startsWith guards — the peek closes on ANY pathname change. Swapping a peeked issue does not change pathname (Pitfall 3), so swaps stay open.
    (4) Layout: wrap the existing `<main className="flex-1 overflow-auto">...<Outlet/>...</main>` in a new `<div className="flex flex-row flex-1 overflow-hidden min-h-0">` (A5 — min-h-0 required). Change `<main>` to `className="flex-1 overflow-auto min-w-0"`. After `</main>`, render `{peekIssueKey && (<PeekPanel issueKey={peekIssueKey} width={peekPanelWidth} onWidthChange={setPeekPanelWidth} onClose={() => setPeekIssueKey(null)} onOpenIssue={(key) => setPeekIssueKey(key)} onNavigateFull={(key) => { setPeekIssueKey(null); handleIssueClick(key, true); }} paletteOpen={paletteOpen} />)}`. onNavigateFull closes then navigates (the route change would also close it, but closing first avoids any race).
    (5) Outlet context: add `onOpenIssue: handleOpenPeek,` to the context object (~line 555-563).
    (6) CommandPalette mount (~568-578): add prop `onOpenIssue={(key) => { handleOpenPeek(key); setPaletteOpen(false); }}`.
    (7) NotificationPopover mount: add prop `onOpenIssue={(key) => { handleOpenPeek(key); /* close popover as the existing onIssueClick path does */ }}` — match the existing close behavior of that mount.
    CommandPalette.tsx: add `onOpenIssue: (issueKey: string) => void;` to CommandPaletteProps. In handleIssueSelect (body/Enter selection), dispatch to `onOpenIssue(issueKey)` instead of onIssueClick; keep onIssueClick for the explicit key-element click that Plan 04 Task 3 adds (it renders the result-row issue key as a distinct inner clickable with stopPropagation → onIssueClick). For THIS plan: body selection → onOpenIssue (peek) only; do not add the inner key clickable here. Document in SUMMARY that CommandPalette body-select now opens the peek and that the key split is delivered in Plan 04 Task 3.
    NotificationPopover.tsx: add `onOpenIssue: (key: string) => void;` to its props. In handleRowClick, change the issue-key branch from `onIssueClick(issueKey)` to `onOpenIssue(issueKey)` (body row click → peek); keep onIssueClick available for the key-element click that NotificationRow's key split calls — that split (the inner key button + onIssueKeyClick wiring in NotificationRow.tsx) is delivered in Plan 04 Task 3, whose files_modified includes NotificationPopover.tsx and NotificationRow.tsx. For THIS plan: only the body row click is rerouted to onOpenIssue (peek).
    Import PeekPanel into main.tsx.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit 2>&1 | grep -iE "main.tsx|CommandPalette|NotificationPopover|PeekPanel" || echo "main + palette + notif typecheck clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "onOpenIssue: handleOpenPeek" taskflow/src/main.tsx` (universal context seam)
    - `grep -q "PeekPanel" taskflow/src/main.tsx` and `grep -q "flex flex-row flex-1 overflow-hidden min-h-0" taskflow/src/main.tsx`
    - `grep -q "setPeekIssueKey(null)" taskflow/src/main.tsx` appears in a useEffect keyed on location.pathname (route-change close)
    - `grep -q "onOpenIssue" taskflow/src/components/app/CommandPalette.tsx` and `grep -q "onOpenIssue" taskflow/src/routes/notifications/NotificationPopover.tsx`
    - tsc clean across all four files
  </acceptance_criteria>
  <done>Peek state lives at AppLayout level; PeekPanel mounts as a flex-row sibling; onOpenIssue is in outlet context and on CommandPalette/NotificationPopover; route change closes the peek.</done>
</task>

<task type="auto">
  <name>Task 3: Activate PeekPanel.test.tsx cases</name>
  <read_first>
    - taskflow/src/components/app/PeekPanel.test.tsx (Plan 01 stub)
    - taskflow/src/components/app/PeekPanel.tsx (just built)
    - taskflow/.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-PATTERNS.md (§ Test files — PeekPanel assertions)
  </read_first>
  <action>
    Convert the it.todo cases in PeekPanel.test.tsx to real assertions. Mock the IssueDetailView module (or mock the underlying useQuery) so PeekPanel renders without live network. Assert:
    - PEEK-02: PeekPanel renders the IssueDetailView body for a given issueKey (assert presence of a body testid or mocked IssueDetailView marker).
    - PEEK-03: rendered DOM contains NO element with `role="dialog"` (querch `screen.queryByRole('dialog')` is null) — proves CSS panel, not Dialog.
    - PEEK-06: clicking the "Open full page" button calls onNavigateFull with the issueKey.
    - PEEK-07: firing Escape keydown calls onClose; clicking the "Close preview" button calls onClose.
    - PEEK-04: re-rendering PeekPanel with a different issueKey shows the new key in the header (swap without crash).
    Leave PEEK-01 (handler sets state, owned by main.tsx) and PEEK-05 (key click, owned by TaskCard) as todo here — they are covered in Plan 04 / TaskCard.test.tsx.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/components/app/PeekPanel.test.tsx --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - PeekPanel.test.tsx PEEK-02/03/04/06/07 cases are it() (not todo) and pass
    - `grep -q "queryByRole('dialog')" taskflow/src/components/app/PeekPanel.test.tsx` (PEEK-03 backdrop/dialog absence assertion)
    - `npx vitest run src/components/app/PeekPanel.test.tsx` exits 0
  </acceptance_criteria>
  <done>PeekPanel unit tests for PEEK-02/03/04/06/07 pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The universal peek slideover: clicking an issue body (try CommandPalette result and a notification for now — card surfaces land in Plan 04) opens a right-edge panel showing the FULL issue detail (fields stacked on top, then description, then comments + comment composer) per D-05/D-06. The underlying view stays live and undimmed. The panel has a header (key, Open full page, X), a draggable left divider (default 480, min 360, max 720, persisted), Escape/X dismissal, and closes on route navigation. Also verify the full-page /issue/:key detail still renders identically after the IssueDetailView extraction (Plan 02).
  </what-built>
  <how-to-verify>
    1. Run `cd taskflow && npm run dev` (or the project's dev/Tauri command).
    2. Open Command Palette (Cmd/Ctrl-K), search an issue, select it (Enter/click body) → a right-edge peek opens; the list behind stays bright and scrollable. Confirm NO dark backdrop.
    3. In the peek, confirm: parent breadcrumb above title for a subtask; stacked fields (status/assignee/priority) ABOVE description; comments list AND a comment composer are present (D-05 full editability).
    4. Drag the left divider — width changes, clamps at ~360 and ~720. Restart the app, reopen a peek — width persisted.
    5. Click a subtask/parent/linked-issue link INSIDE the peek → the peek SWAPS to that issue (does not navigate, does not close) — D-13.
    6. Press Escape → peek closes. Reopen, click X → closes. Reopen, click "Open full page" → navigates to /issue/:key and peek closes.
    7. Open a peek, then click a sidebar nav item (route change) → peek closes (D-07).
    8. Navigate to any /issue/:key full page directly → confirm comments, activity timeline, composer, worklogs, AIO test runs all render exactly as before the refactor.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. backdrop appears, comments missing in peek, width not persisting, full-page regressed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click → peekIssueKey | Issue key derived from in-DOM keys already rendered; no new free-text input |
| Jira REST via IssueDetailView | Existing PAT-secured queries, unchanged |

## STRIDE Threat Register

Per RESEARCH § Security Domain, no new auth/session/access-control/crypto/validation surfaces. peekIssueKey is a click-derived existing key.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-03 | Information Disclosure | Peek renders full detail same as full page | accept | Same data + same auth as the full-page detail the user can already open; no new exposure. |
| T-77-04 | Denial of Service | Stored peekPanelWidth out of range causing broken layout | mitigate | useResizable clamps to min 360 / max 720 at read (Plan 03 Task 1). |
| T-77-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase. |
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` clean
- `cd taskflow && npx vitest run src/components/app/PeekPanel.test.tsx` green
- `cd taskflow && npx vitest run` full suite green
- Human-verify checkpoint approved
</verification>

<success_criteria>
- PEEK-01..07 behaviors live: open from body, full-detail single-column, non-blocking (no backdrop/role=dialog), swap on issue click, key→full-page seam ready, Open full page + X + Escape dismiss
- onOpenIssue universal context seam in place for Plan 04
- Full-page detail unchanged after extraction
</success_criteria>

<output>
Create `.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-03-SUMMARY.md` when done
</output>
