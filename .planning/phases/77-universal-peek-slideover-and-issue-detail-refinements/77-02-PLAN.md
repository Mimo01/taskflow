---
phase: 77-universal-peek-slideover-and-issue-detail-refinements
plan: 02
type: execute
wave: 2
depends_on: ["77-01"]
files_modified:
  - taskflow/src/routes/dashboard/IssueDetailView.tsx
  - taskflow/src/routes/dashboard/IssueDetailContent.test.tsx
  - taskflow/src/routes/dashboard/IssueDetailPage.tsx
  - taskflow/src/routes/dashboard/IssueDetailContent.tsx
  - taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx
autonomous: true
requirements: [PEEK-02, DETAIL-01, DETAIL-02]
must_haves:
  truths:
    - "The full-page issue detail (/issue/:key) still renders identically: two-column, comments, activity timeline, comment composer, worklogs, AIO test runs"
    - "A shared IssueDetailView component renders the FULL detail (all independent queries + comment/worklog mutations) and accepts layout='two-column' | 'single-column' and onOpenIssue per D-05/D-06/D-13"
    - "On a subtask detail, the parent shows as an ArrowUpRight breadcrumb above the title in main content (DETAIL-01); the sidebar parent MetaRow is gone"
    - "Subtask rows, epic-story rows, and the Add-subtask button in IssueDetailContent carry cursor-pointer; subtask row vertical padding is py-2 (DETAIL-02 + UI-SPEC spacing)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/IssueDetailView.tsx"
      provides: "Shared full-detail body with layout + onOpenIssue props (D-05 override target for the peek)"
      min_lines: 200
    - path: "taskflow/src/routes/dashboard/IssueDetailContent.tsx"
      provides: "Parent breadcrumb above title + cursor-pointer sweep"
      contains: "ArrowUpRight"
    - path: "taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx"
      provides: "Parent MetaRow removed"
  key_links:
    - from: "IssueDetailPage.tsx"
      to: "IssueDetailView"
      via: "render with layout='two-column' onOpenIssue={onIssueClick}"
      pattern: "IssueDetailView"
    - from: "IssueDetailContent.tsx parent breadcrumb"
      to: "onOpenIssue prop"
      via: "onClick swap-or-navigate (D-13)"
      pattern: "onOpenIssue\\?\\.\\(.*parent"
---

<objective>
Extract the FULL issue-detail body — every independent query (comments, subtask enrichment, changelog, worklogs, epic stories), the comment and worklog mutations, the activity timeline, the comment composer, and the AIO test-runs section — out of `IssueDetailPage` into a reusable `IssueDetailView` component that takes `layout: 'two-column' | 'single-column'` and `onOpenIssue`. This is the D-05 override: the peek must render the full interactive detail (NOT the thin `IssueDetailBody` in IssueDetailSheet.tsx, which lacks comments/timeline/composer). `IssueDetailPage` becomes a thin wrapper (route params + breadcrumb header) rendering `IssueDetailView layout="two-column"`. Also land DETAIL-01 (parent breadcrumb relocation) and DETAIL-02 (cursor sweep) in IssueDetailContent + FieldsSection, since those files are touched by this extraction.

Purpose: Plan 03's PeekPanel renders this exact composition in single-column so the peek is fully editable with comments per D-05. Doing the extraction once, here, prevents two divergent detail bodies.
Output: IssueDetailView.tsx (shared), slimmed IssueDetailPage.tsx, IssueDetailContent.tsx (breadcrumb + cursors), FieldsSection.tsx (parent removed).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-CONTEXT.md
@.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-RESEARCH.md
@.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-PATTERNS.md
@.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared IssueDetailView (full detail + layout prop)</name>
  <read_first>
    - taskflow/src/routes/dashboard/IssueDetailPage.tsx (FULL file — this is the source of the extraction: queries lines 93-220, mutations 265-456, two-column JSX 460-680, IssueDetailSkeleton 824-841, CommentCard 682-820)
    - taskflow/src/routes/dashboard/IssueDetailSheet.tsx (the thin IssueDetailBody — note what it is MISSING vs IssueDetailPage: no comments, no timeline, no composer, no worklogs, no enrichment; this is why we extract from IssueDetailPage, NOT reuse IssueDetailBody — per planning-context D-05 override)
    - taskflow/src/routes/dashboard/IssueDetailContent.tsx (consumer of comments/enrichedSubtasks/onOpenIssue props)
    - taskflow/src/routes/dashboard/IssueDetailSidebar.tsx (consumer of onOpenIssue)
  </read_first>
  <behavior>
    - Rendering IssueDetailView with layout="two-column" for a story key produces the same DOM structure the current IssueDetailPage body produces (left content column + right resizable sidebar, activity timeline, comment composer present).
    - Rendering IssueDetailView with layout="single-column" stacks: sidebar fields block first, then content (description/comments/subtasks) below, in a flex-col container — no two-column split, no internal resize handle.
    - onOpenIssue passed to IssueDetailView flows unchanged to IssueDetailContent and IssueDetailSidebar (D-13 seam).
    - For an issue with no subtasks, TTI/skeleton gates behave as before (subtasksSettled logic preserved).
  </behavior>
  <action>
    Create taskflow/src/routes/dashboard/IssueDetailView.tsx. Move from IssueDetailPage into IssueDetailView ALL of: the base `jira-issue-detail` query, `jira-epic-stories` query, the three independent section queries (`jira-issue-comments`, `jira-subtask-enrichment`, `jira-issue-changelog`), the `jira-worklogs` query, the comment edit/delete mutations and their refs/handlers, the worklog edit/delete mutations and handlers, the useDelayedLoading gates, useMentionUserMap/userMap derivation, attachmentMap, the recent-item push effect, and the perf marks (TTFMP/TTI) — keep them intact and behaviorally identical. Move CommentCard and IssueDetailSkeleton into IssueDetailView.tsx (or a shared module IssueDetailView imports). Export `IssueDetailView`.
    Props interface: `{ issueKey: string; layout: 'two-column' | 'single-column'; onOpenIssue?: (key: string) => void; onEdit?: (vals: EditInitialValues) => void; onClone?: (vals: EditInitialValues) => void; onAddSubtask?: (parentKey: string) => void; isPinned?: boolean; onTogglePin?: (key: string) => void; }`. The component reads auth/settings stores itself (as IssueDetailPage does today). Get pinned state (usePinnedTabsStore) and recentItems inside IssueDetailView OR accept isPinned/onTogglePin as props — choose props so PeekPanel/IssueDetailPage can pass them; default to reading the stores internally if simpler, document the choice in SUMMARY.
    Layout branching:
    - layout="two-column": reproduce the existing JSX at IssueDetailPage.tsx:511-676 verbatim — `<div ref={containerRef} className="flex flex-1 overflow-hidden">` with left `flex-1 overflow-auto min-w-0` column (IssueDetailContent + AIO + activity timeline + sticky CommentComposer) and the right `border-l ... shrink-0` resizable sidebar using useResizable(min 240, max 50% container, direction 'left', onCommit setIssueDetailPanelWidth). Keep the resize handle DOM (lines 657-664) ONLY in two-column mode.
    - layout="single-column": render `<div className="flex flex-col h-full overflow-auto">` with IssueDetailSidebar in a top block (`p-4 border-b`) FIRST, then a block containing IssueDetailContent + AIO test runs + activity timeline + CommentComposer (`p-4`). NO useResizable, NO resize handle (the peek's outer width/resize is owned by PeekPanel in Plan 03). Per D-06.
    The not-found/error and skeleton states (IssueDetailPage.tsx:494-509) move into IssueDetailView and render in both layouts.
    DO NOT change query keys, staleTime, enabled conditions, or invalidation targets — they must match exactly so peek and full-page share cache (RESEARCH Pitfall 4).
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit 2>&1 | grep -iE "IssueDetailView|IssueDetailPage" || echo "IssueDetailView/Page typecheck clean"</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/dashboard/IssueDetailView.tsx exists, exports IssueDetailView, >= 200 lines
    - `grep -q "layout: 'two-column' | 'single-column'\|layout?: 'two-column'\|'single-column'" taskflow/src/routes/dashboard/IssueDetailView.tsx` (a layout prop with both variants is declared)
    - `grep -c "useQuery" taskflow/src/routes/dashboard/IssueDetailView.tsx` >= 5 (base, epic-stories, comments, enrichment, changelog, worklogs all moved)
    - `grep -q "CommentComposer" taskflow/src/routes/dashboard/IssueDetailView.tsx` (composer lives in the shared body, satisfying D-05 full editability)
    - tsc clean for both files
  </acceptance_criteria>
  <done>IssueDetailView renders the full interactive detail with a layout prop; query keys/mutations/invalidations unchanged; single-column variant stacks sidebar-first per D-06.</done>
</task>

<task type="auto">
  <name>Task 2: Slim IssueDetailPage to a wrapper around IssueDetailView</name>
  <read_first>
    - taskflow/src/routes/dashboard/IssueDetailPage.tsx (the wrapper that remains: route params lines 53-71, breadcrumb header 460-491)
    - taskflow/src/routes/dashboard/IssueDetailView.tsx (just created — its prop interface)
  </read_first>
  <action>
    Reduce IssueDetailPage to: read `key` from useParams; read `onIssueClick, openEdit, openClone, openAddSubtask` from useOutletContext; keep the breadcrumb-trail header JSX (lines 460-491) and handleBack; render `<IssueDetailView issueKey={issueKey} layout="two-column" onOpenIssue={onIssueClick} onEdit={openEdit} onClone={openClone} onAddSubtask={openAddSubtask} />`. Delete all the query/mutation/handler code now living in IssueDetailView. Per D-13, IssueDetailPage passes `onOpenIssue={onIssueClick}` so in-detail links navigate full-page (existing behavior). Keep the outer `<div className="flex flex-col h-full overflow-hidden">` wrapper so the breadcrumb header sits above the view.
    Remove now-unused imports from IssueDetailPage.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit 2>&1 | grep -iE "IssueDetailPage" || echo "IssueDetailPage typecheck clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "IssueDetailView" taskflow/src/routes/dashboard/IssueDetailPage.tsx` and `grep -q 'layout="two-column"' taskflow/src/routes/dashboard/IssueDetailPage.tsx`
    - `grep -q "onOpenIssue={onIssueClick}" taskflow/src/routes/dashboard/IssueDetailPage.tsx` (D-13 navigate context preserved)
    - IssueDetailPage no longer declares the comments/changelog/worklog mutations (grep -c "useMutation" taskflow/src/routes/dashboard/IssueDetailPage.tsx == 0)
    - tsc clean
  </acceptance_criteria>
  <done>IssueDetailPage is a thin wrapper; full-page detail renders via IssueDetailView two-column with onOpenIssue=navigate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DETAIL-01 parent breadcrumb + DETAIL-02 cursor sweep</name>
  <read_first>
    - taskflow/src/routes/dashboard/IssueDetailContent.tsx (title block ~217-223; subtask row buttons ~102-105; epic story rows ~257-284; Add-subtask button ~310-313)
    - taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx (parent MetaRow ~641-653 — the source-of-truth styling for the new breadcrumb)
    - taskflow/src/routes/dashboard/IssueDetailContent.test.tsx (Plan 01 stub — convert its DETAIL-01/02 it.todo to it())
    - taskflow/.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-UI-SPEC.md (§ DETAIL-01, § DETAIL-02)
  </read_first>
  <behavior>
    - For a subtask issue whose fields.parent is set, IssueDetailContent renders a clickable row ABOVE the h2 title: ArrowUpRight icon (size-3), parent key (text-xs font-mono text-muted-foreground), em-dash, parent summary (text-sm text-muted-foreground); the row has cursor-pointer and hover:underline; clicking it calls onOpenIssue(parentKey).
    - For a non-subtask or a subtask without parent, no breadcrumb renders.
    - The sidebar (FieldsSection) no longer renders a Parent MetaRow.
    - Subtask row buttons in IssueDetailContent carry cursor-pointer and py-2.
  </behavior>
  <action>
    IssueDetailContent.tsx: Insert the parent breadcrumb immediately before the title `<div>` at line 217, guarded by `issue.fields.issuetype.subtask && issue.fields.parent` (per D-11). Use the styling from UI-SPEC § DETAIL-01: a `<div className="flex items-center gap-1 mb-1 cursor-pointer hover:underline" onClick={() => onOpenIssue?.(issue.fields.parent!.key)}>` containing `<ArrowUpRight className="size-3 text-muted-foreground" />`, `<span className="font-mono text-xs text-muted-foreground">{issue.fields.parent.key}</span>`, `<span className="text-sm text-muted-foreground">— {issue.fields.parent.fields.summary}</span>`. Import ArrowUpRight from lucide-react. The onClick uses the existing onOpenIssue prop so it swaps in peek / navigates on full page (D-13) with zero extra wiring.
    DETAIL-02 cursor sweep in IssueDetailContent: on the subtask row button (~line 105) change `py-1.5` to `py-2` (UI-SPEC spacing, Pitfall 5) AND add `cursor-pointer` to its className; add `cursor-pointer` to the epic story row buttons (~257-284) and the Add-subtask button (~310-313). Leave the shadcn Pin <Button> alone (its base style already has cursor-pointer).
    FieldsSection.tsx: delete the entire parent MetaRow block at ~641-653 (the `{isSubtask && f.parent && (<MetaRow label="Parent">...)}`). Remove any now-unused local that only fed it. Do not touch the other field rows (they already have cursor-pointer per the audit).
    Convert the DETAIL-01 and DETAIL-02 it.todo cases in IssueDetailContent.test.tsx into real it() assertions: render with a subtask fixture (fields.issuetype.subtask=true, fields.parent set) and assert the ArrowUpRight breadcrumb + parent key appear above the h2; assert a subtask-row button has the cursor-pointer class.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/IssueDetailContent.test.tsx --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ArrowUpRight" taskflow/src/routes/dashboard/IssueDetailContent.tsx` and the breadcrumb onClick calls onOpenIssue with the parent key (`grep -E "onOpenIssue\?\.\(.*parent" taskflow/src/routes/dashboard/IssueDetailContent.tsx`)
    - `grep -cv '^\s*//' taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx | xargs true; grep -c 'label="Parent"' taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx` returns 0 (MetaRow removed)
    - `grep -q "py-2" taskflow/src/routes/dashboard/IssueDetailContent.tsx` near the subtask row and no `py-1.5` remains on that subtask button
    - IssueDetailContent.test.tsx DETAIL-01/DETAIL-02 cases pass (no longer todo)
  </acceptance_criteria>
  <done>Parent breadcrumb renders above the title and calls onOpenIssue (D-13); sidebar parent MetaRow removed; subtask/epic/add-subtask rows have cursor-pointer; subtask row is py-2; DETAIL tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jira REST (fetchIssueDetail/comments/changelog/worklogs) | Already-secured PAT-auth queries; unchanged by this refactor |

## STRIDE Threat Register

Pure component refactor + display change. No new data surfaces, no new endpoints, no input validation. All data flows through existing secured queries (RESEARCH § Security Domain).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-02 | Tampering | Refactor regressions (lost query/mutation) breaking detail behavior | mitigate | tsc + IssueDetailContent.test.tsx + manual full-page parity check (Plan 03 human-verify covers peek); query keys/invalidations preserved verbatim per Task 1 action. |
| T-77-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase. |
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` clean
- `cd taskflow && npx vitest run src/routes/dashboard/IssueDetailContent.test.tsx` green
- Manual smoke (deferred to Plan 03 human-verify): /issue/:key full page renders comments + composer + timeline as before
</verification>

<success_criteria>
- Shared IssueDetailView renders full detail (comments/timeline/composer) with layout + onOpenIssue props
- IssueDetailPage is a thin wrapper, full-page behavior unchanged
- DETAIL-01 parent breadcrumb in main content; sidebar parent removed
- DETAIL-02 cursor-pointer on all IssueDetailContent clickable rows; subtask row py-2
</success_criteria>

<output>
Create `.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-02-SUMMARY.md` when done
</output>
