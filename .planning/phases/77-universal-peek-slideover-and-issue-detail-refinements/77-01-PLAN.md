---
phase: 77-universal-peek-slideover-and-issue-detail-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/stores/settings.store.ts
  - taskflow/src/components/app/PeekPanel.test.tsx
  - taskflow/src/routes/dashboard/TaskCard.test.tsx
  - taskflow/src/routes/dashboard/IssueDetailContent.test.tsx
autonomous: true
requirements: [PEEK-01, PEEK-02, PEEK-03, PEEK-04, PEEK-05, PEEK-06, PEEK-07, DETAIL-01, DETAIL-02]
must_haves:
  truths:
    - "Settings store persists peekPanelWidth across restart (default 480, store version 26)"
    - "Three Nyquist test files exist and run (red/skipped placeholders allowed until implementation lands)"
    - "JiraIssueDetail.fields.parent confirmed present (no type change needed) — recorded in SUMMARY"
  artifacts:
    - path: "taskflow/src/stores/settings.store.ts"
      provides: "peekPanelWidth field + setter + v26 migration"
      contains: "peekPanelWidth"
    - path: "taskflow/src/components/app/PeekPanel.test.tsx"
      provides: "Wave 0 test stubs for PEEK-01..04,06,07"
    - path: "taskflow/src/routes/dashboard/TaskCard.test.tsx"
      provides: "Wave 0 test stub for PEEK-05"
    - path: "taskflow/src/routes/dashboard/IssueDetailContent.test.tsx"
      provides: "Wave 0 test stubs for DETAIL-01, DETAIL-02"
  key_links:
    - from: "settings.store.ts"
      to: "PeekPanel (Plan 03) useResizable onCommit"
      via: "setPeekPanelWidth / peekPanelWidth"
      pattern: "peekPanelWidth"
---

<objective>
Lay the Wave 0 foundation for Phase 77: create the three Nyquist test files named in VALIDATION.md so downstream plans satisfy Dimension 8, add the persisted `peekPanelWidth` setting (store version 26) that PeekPanel's resize divider commits to, and confirm the `JiraIssueDetail.fields.parent` type that DETAIL-01's parent breadcrumb depends on (Assumption A1).

Purpose: Tests must exist before the components they cover (Nyquist). The settings field and type confirmation are leaf dependencies of Plans 02 and 03 — landing them first unblocks both with zero file conflict.
Output: settings.store.ts v26 + three test stub files.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-CONTEXT.md
@.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-RESEARCH.md
@.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-PATTERNS.md
@.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add peekPanelWidth to settings store (v26) + confirm parent type</name>
  <read_first>
    - taskflow/src/stores/settings.store.ts (the file being modified — read initialSettings ~line 51-58, SettingsState interface ~line 96/140, actions block ~line 246, version ~line 346, migrate chain ~line 440-449)
    - taskflow/src/services/jira/types.ts (confirm-only — parent field on JiraIssueDetail at line 152)
  </read_first>
  <action>
    In settings.store.ts, mirror the existing nullable `issueDetailPanelWidth` field (NOT a plain number) per D-03 persistence:
    (1) initialSettings: add `peekPanelWidth: null as number | null,` next to `issueDetailPanelWidth` (~line 51).
    (2) SettingsState interface: add `peekPanelWidth: number | null;` and `setPeekPanelWidth: (w: number) => void;` next to the `issueDetailPanelWidth` declarations (~line 140).
    (3) actions in create(): add `setPeekPanelWidth: (w) => set({ peekPanelWidth: w }),` next to `setIssueDetailPanelWidth` (~line 246).
    (4) bump `version: 25` to `version: 26` (~line 346).
    (5) migrate(): after the `if (version < 25)` block (~line 448), add `if (version < 26) { if (s.peekPanelWidth === undefined) s.peekPanelWidth = null; }`.
    The store has no restrictive partialize, so no partialize edit is needed; do NOT add peekPanelWidth to the resetSettings 'preferences' keep-list (a UI preference, resets with preferences).
    Then read src/services/jira/types.ts line 152 to confirm `parent?: { id: string; key: string; fields: { summary: string } }` is declared on JiraIssueDetail.fields. It IS present per A1 — record "A1 confirmed, no type change" in the SUMMARY. Do not add or modify the type.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit 2>&1 | grep -i "settings.store" || echo "settings.store typechecks clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "peekPanelWidth" taskflow/src/stores/settings.store.ts` returns >= 4 (initialSettings, interface field, setter, migration)
    - `grep -q "version: 26" taskflow/src/stores/settings.store.ts` succeeds
    - `grep -q "version < 26" taskflow/src/stores/settings.store.ts` succeeds
    - SUMMARY records: "A1 confirmed — JiraIssueDetail.fields.parent present at types.ts:152, no type change"
  </acceptance_criteria>
  <done>settings.store.ts exports peekPanelWidth (nullable, default null), setPeekPanelWidth, version 26 with a v26 migration block; tsc clean for that file; parent type confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 Nyquist test stubs</name>
  <read_first>
    - taskflow/vitest.config.ts (test env, setup, globals)
    - taskflow/.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-VALIDATION.md (per-req test map)
    - taskflow/.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-PATTERNS.md (§ Test files — assertions per file)
    - An existing Vitest + RTL test in taskflow/src for the project's render/mock idioms (e.g. any *.test.tsx under src — find one with `grep -rl "@testing-library/react" taskflow/src`)
  </read_first>
  <action>
    Create three test files. Each test that targets not-yet-built behavior MUST be a real assertion wrapped so the file runs green-or-skipped on Wave 0 (use `it.skip(...)` with the intended assertion in the body as a TODO comment, OR `it.todo('...')`). Do NOT use empty `it()` bodies — every case names the exact behavior it will assert.
    - taskflow/src/components/app/PeekPanel.test.tsx — cases: PEEK-01 "body-open handler sets peekIssueKey" (it.todo until Plan 03/04 wire it), PEEK-02 "PeekPanel renders issue-detail-body (data-testid) for story/subtask/bug/epic fixtures", PEEK-03 "no element with role='dialog' is rendered (CSS panel, not Dialog)", PEEK-04 "changing issueKey prop swaps content without unmount flash", PEEK-06 "Open full page button calls onNavigateFull with the issue key", PEEK-07 "Escape keydown and X button each call onClose". Mark cases whose component does not yet exist as it.todo; convert to it()/it.skip when PeekPanel lands in Plan 03.
    - taskflow/src/routes/dashboard/TaskCard.test.tsx — case PEEK-05 "clicking the issue key element calls onIssueClick/navigate to /issue/:key and stopPropagation prevents body onOpenIssue"; case "clicking the card body calls onOpenIssue with the key, not navigate". Mock react-router navigate. it.todo until Plan 04 splits the click target.
    - taskflow/src/routes/dashboard/IssueDetailContent.test.tsx — case DETAIL-01 "for a subtask fixture with fields.parent, an ArrowUpRight breadcrumb with the parent key renders ABOVE the h2 title"; case DETAIL-01 "parent MetaRow no longer renders in the sidebar path"; case DETAIL-02 "subtask row buttons carry the cursor-pointer class". it.todo until Plan 02 lands the breadcrumb/cursor changes.
    Place a header comment in each file: `// Phase 77 Nyquist stubs — see 77-VALIDATION.md. Convert it.todo → it() as the covered plan lands.`
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/components/app/PeekPanel.test.tsx src/routes/dashboard/TaskCard.test.tsx src/routes/dashboard/IssueDetailContent.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist and `npx vitest run` on them exits 0 (todo/skip cases do not fail the run)
    - Each file's test names reference the requirement ID it covers (grep `PEEK-0` / `DETAIL-0` in each file body, excluding comment lines: `grep -v '^//' <file> | grep -c "PEEK-\|DETAIL-"` > 0)
    - No `it()` body is empty — every non-todo case has at least one expect(...)
  </acceptance_criteria>
  <done>Three Nyquist test files exist, run green (todo/skip allowed), and each names the requirement IDs it covers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri Store (settings.json) | Persisted UI preference (peekPanelWidth); local-only, no remote input |

## STRIDE Threat Register

Per RESEARCH § Security Domain: this phase introduces NO new auth/session/access-control/crypto/input-validation surfaces. `peekPanelWidth` is a numeric UI preference written by the user's own drag action. No new threats.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Tampering | settings.json peekPanelWidth value | accept | Local-only preference; clamped 360–720 by useResizable in PeekPanel (Plan 03). Out-of-range stored value is clamped at read, not trusted raw. |
| T-77-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` clean for settings.store.ts
- `cd taskflow && npx vitest run src/components/app/PeekPanel.test.tsx src/routes/dashboard/TaskCard.test.tsx src/routes/dashboard/IssueDetailContent.test.tsx` exits 0
</verification>

<success_criteria>
- peekPanelWidth persisted (v26 migration), nullable default null mirroring issueDetailPanelWidth
- Three Nyquist test files exist and run green
- A1 confirmed (parent type present, no change)
</success_criteria>

<output>
Create `.planning/phases/77-universal-peek-slideover-and-issue-detail-refinements/77-01-SUMMARY.md` when done
</output>
