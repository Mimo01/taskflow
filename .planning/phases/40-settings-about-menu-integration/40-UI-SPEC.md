---
phase: 40
slug: settings-about-menu-integration
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-03-25
---

# Phase 40 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova |
| Component library | Radix UI (via shadcn) |
| Icon library | Lucide (lucide-react) |
| Font | Geist Variable (sans-serif) |

Source: `taskflow/components.json`, `taskflow/src/index.css`, `npx shadcn info`.

No third-party registries. `components.json` field `"registries": {}` confirms only the official shadcn registry is in use.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions:
- Settings content area: `px-6 py-8` (24px/32px) — matches existing Settings.tsx pattern. Do not change.
- Settings sidebar nav items: `px-3 py-2` (12px/8px) — matches existing nav button pattern. Do not change.
- About dialog content: `gap-4` (16px) between rows, `gap-2` (8px) between label and value within a row.
- Version history rows: `py-3` (12px) vertical padding per row for comfortable tap/click target.

Source: `Settings.tsx` line 49 (`px-2 py-6`), line 58 (`px-3 py-2`), line 69 (`py-8 px-6`); `WorkflowSection.tsx` line 23 (`gap-8`).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (text-sm) | 400 (normal) | 1.5 |
| Label | 14px (text-sm) | 500 (medium) | 1.4 |
| Heading | 18px (text-lg) | 600 (semibold) | 1.2 |
| Caption | 12px (text-xs) | 400 (normal) | 1.4 |

Application to this phase:
- About dialog title ("Taskflow"): `text-lg font-semibold` — 18px / 600
- About dialog metadata labels ("Version", "Build Date", etc.): `text-sm font-medium` — 14px / 500
- About dialog metadata values: `text-sm` — 14px / 400
- Updates section heading: `text-lg font-semibold` — matches WorkflowSection heading pattern
- Subsection headings (e.g., "Version History"): `text-sm font-semibold text-muted-foreground uppercase tracking-wide` — matches WorkflowSection subsection pattern
- Control labels: `text-sm font-medium` — 14px / 500
- Helper/description text: `text-xs text-muted-foreground` — 12px / 400
- Version history release titles: `text-sm font-medium` — 14px / 500
- Version history dates: `text-xs text-muted-foreground` — 12px / 400

Source: `WorkflowSection.tsx` lines 24, 29, 33, 36; `EmptyState.tsx` lines 14-17.

---

## Color

All values use CSS custom properties (OKLCH). Light and dark mode both defined in `src/index.css`.

| Role | CSS Variable | Light Value | Dark Value | Usage |
|------|-------------|-------------|------------|-------|
| Dominant (60%) | `--background` | oklch(1 0 0) = white | oklch(0.145 0 0) = near-black | App background, dialog backdrop |
| Secondary (30%) | `--card` / `--muted` | oklch(0.97 0 0) = off-white | oklch(0.205 0 0) / oklch(0.269 0 0) | Settings content area, version history rows |
| Accent (10%) | `--accent` | oklch(0.97 0 0) | oklch(0.269 0 0) | Active Settings nav item, hover states |
| Destructive | `--destructive` | oklch(0.577 0.245 27.325) = red | oklch(0.704 0.191 22.216) = lighter red | Not used in this phase |
| Muted text | `--muted-foreground` | oklch(0.556 0 0) | oklch(0.708 0 0) | Metadata values, helper text, dates |
| Border | `--border` | oklch(0.922 0 0) | oklch(1 0 0 / 10%) | Dividers between version history rows |

Accent reserved for:
- Active Settings sidebar nav item background (`bg-accent text-accent-foreground`)
- Hover state on Settings sidebar nav items
- No other uses in this phase

Status indicators (not accent):
- "Up to date" status: `text-green-500` with `CheckCircle` icon (Lucide)
- "Update available" status: `text-yellow-500` with `ArrowUpCircle` icon (Lucide)
- "Checking..." state: `text-muted-foreground` with animated `Loader2` icon (Lucide)

Source: `src/index.css` lines 79-148; `Settings.tsx` lines 57-62.

---

## Component Inventory

Existing components used in this phase (no new installs needed):

| Component | Path | Used For |
|-----------|------|----------|
| Dialog, DialogContent, DialogHeader, DialogFooter, DialogTitle, DialogDescription | `@/components/ui/dialog` | About dialog shell |
| Button | `@/components/ui/button` | "Close" in About dialog, "Check Now" in Updates section, "Retry" in version history error state |
| Select | `@/components/ui/select` | Check frequency dropdown in Updates section |
| Badge | `@/components/ui/badge` | "(current)" badge on current version in version history |
| Skeleton | `@/components/ui/skeleton` | Loading state for version history list |
| EmptyState | `@/components/ui/empty-state` | Version history when GitHub API unreachable |

No new shadcn components need to be installed. All required primitives are already present.

---

## Interaction Contracts

### About Dialog

- Trigger: macOS app menu "About Taskflow" item (Rust emits `menu-about` event; React `listen()` catches it and opens dialog)
- Trigger (Windows/Linux): Help menu "About Taskflow" item (same event)
- Open behavior: Dialog opens centered, overlaid on current view
- Content layout: App icon (top, centered) → "Taskflow" heading → metadata rows (label + value pairs) → update status row → "Close" button
- Metadata rows: Version, Build Date, Commit SHA, Platform/Arch, Update Status
- Update Status: reads live from `useUpdateStore` — renders "Up to date" with `CheckCircle` (green) or "Update available (x.y.z)" with `ArrowUpCircle` (yellow)
- Dismiss: single "Close" button (`variant="outline"`) — no click-outside-to-close (use `DialogContent` with `onPointerDownOutside` prevented, or default shadcn dismiss)
- Width: `max-w-sm` — compact, focused

### Updates Settings Section

- Navigation: "Updates" appears in the Settings sidebar after "Workflow" and before "Advanced". Icon: `RefreshCw` (Lucide, 16x16).
- Section heading: "Updates" (`text-lg font-semibold`)
- Version display: "Current version: x.y.z" as a `text-sm` row — reads from `buildInfo.version`
- Frequency dropdown: Label "Check for updates" with `Select` component showing options: Every hour / Every 6 hours / Every 12 hours / Daily / Manual only. Reads/writes `updateCheckInterval` in settings store.
- Last checked: `text-xs text-muted-foreground` below the frequency dropdown — "Last checked: 2 hours ago" (relative time). Hidden when never checked.
- "Check Now" button: `variant="outline"` size `sm`. States:
  - Default: "Check Now"
  - Checking: `Loader2` spinner (animated, `animate-spin`) + "Checking..." — button disabled
  - Result (5s then resets): "Up to date" or "Update available (x.y.z)" as inline text next to button (not inside button)
  - If update found: result text is a `Button variant="link"` that opens UpdateDialog (Phase 39)

### Version History List

- Positioned below the update controls, separated by a `gap-8` spacer and a subsection heading "Release History"
- Loading state: 3 `Skeleton` rows, each `h-10 w-full rounded-md`
- Loaded state: Scrollable list of release rows. Each row:
  - Left: version tag (`text-sm font-medium`) + "(current)" `Badge variant="secondary"` if matches `buildInfo.version`
  - Right: published date (`text-xs text-muted-foreground`, formatted as "Mar 24, 2026")
  - Click to expand: chevron (`ChevronDown` / `ChevronUp`, Lucide) rotates 180deg on open; changelog renders below as `ReactMarkdown` with `remarkGfm` inside a `prose prose-sm` container (already uses `@tailwindcss/typography`)
  - Only one row expanded at a time (accordion behavior)
  - Border between rows: `border-b border-border`
- Error state: `EmptyState` component with `WifiOff` (Lucide) icon, title "Unable to load release history", subtitle "Check your internet connection and try again.", action `<Button variant="outline" size="sm" onClick={retry}>Retry</Button>`
- Empty state (0 releases returned): `EmptyState` with `PackageOpen` (Lucide) icon, title "No release history", subtitle "Release history will appear here once the first version is published."
- Page size: fetch latest 20 releases from GitHub Releases API

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Settings sidebar nav item | Updates |
| About dialog title | Taskflow |
| About dialog subtitle | none — title only |
| About dialog close button | Close |
| Updates section heading | Updates |
| Release history subsection heading | Release History |
| Check frequency label | Check for updates |
| Check frequency options | Every hour / Every 6 hours / Every 12 hours / Daily / Manual only |
| Last checked label | Last checked: {relative time} (e.g., "Last checked: 2 hours ago") |
| Check Now button (default) | Check Now |
| Check Now button (checking) | Checking... |
| Check Now inline result (up to date) | Up to date |
| Check Now inline result (update available) | Update available ({version}) |
| Update available link label | View update |
| Current version badge | current |
| Version history loading | (skeleton rows, no text) |
| Version history error heading | Unable to load release history |
| Version history error body | Check your internet connection and try again. |
| Version history error action | Retry |
| Version history empty heading | No release history |
| Version history empty body | Release history will appear here once the first version is published. |
| macOS menu item | About Taskflow |
| Windows/Linux menu item | About Taskflow |

No destructive actions in this phase. No confirmation dialogs needed.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | dialog, button, select, badge, skeleton (all pre-existing) | not required |

No third-party registries. `components.json` confirms `"registries": {}`. No new component installs planned for this phase — all required components already exist in `taskflow/src/components/ui/`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
