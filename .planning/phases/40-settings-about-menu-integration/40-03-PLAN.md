---
phase: 40-settings-about-menu-integration
plan: 03
type: execute
wave: 2
depends_on: [40-01, 40-02]
files_modified:
  - taskflow/src/components/about/AboutDialog.test.tsx
  - taskflow/src/routes/settings/UpdatesSection.test.tsx
  - taskflow/src/routes/settings/Settings.test.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "AboutDialog tests verify all metadata fields render correctly"
    - "AboutDialog tests verify update status displays for both idle and available states"
    - "UpdatesSection tests verify frequency dropdown, Check Now flow, and version history states"
    - "Settings.test.tsx updated to expect 7 nav buttons including Updates"
  artifacts:
    - path: "taskflow/src/components/about/AboutDialog.test.tsx"
      provides: "Unit tests for About dialog"
      min_lines: 40
    - path: "taskflow/src/routes/settings/UpdatesSection.test.tsx"
      provides: "Unit tests for Updates section and version history"
      min_lines: 60
    - path: "taskflow/src/routes/settings/Settings.test.tsx"
      provides: "Updated nav count assertion (6 -> 7) and Updates label"
      contains: "Updates"
  key_links:
    - from: "taskflow/src/components/about/AboutDialog.test.tsx"
      to: "taskflow/src/components/about/AboutDialog.tsx"
      via: "render + assertions"
      pattern: "AboutDialog"
    - from: "taskflow/src/routes/settings/UpdatesSection.test.tsx"
      to: "taskflow/src/routes/settings/UpdatesSection.tsx"
      via: "render + assertions"
      pattern: "UpdatesSection"
---

<objective>
Write tests for AboutDialog, UpdatesSection, and update the existing Settings test to cover the new Updates nav item.

Purpose: Ensure all new UI components have automated test coverage and existing tests don't break (UI-01 through UI-04).
Output: AboutDialog.test.tsx, UpdatesSection.test.tsx, updated Settings.test.tsx.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-settings-about-menu-integration/40-CONTEXT.md
@.planning/phases/40-settings-about-menu-integration/40-RESEARCH.md

<interfaces>
<!-- Executor reads the actual source files from Plan 01 and 02 outputs. Key shapes: -->

From taskflow/src/components/about/AboutDialog.tsx (created in Plan 01):
```typescript
export function AboutDialog({ open, onClose }: { open: boolean; onClose: () => void }): JSX.Element;
// Renders: buildInfo.version, buildInfo.commitSha, buildInfo.buildDate, navigator.platform
// Reads: useUpdateStore() for status + availableVersion
```

From taskflow/src/routes/settings/UpdatesSection.tsx (created in Plan 02):
```typescript
export default function UpdatesSection(): JSX.Element;
// data-testid="section-updates"
// Contains: frequency Select, "Check Now" button, version history with useQuery
// Reads: useSettingsStore for updateCheckInterval, lastChecked
// Reads: useUpdateStore for status, availableVersion
// Calls: updaterService.check() on "Check Now"
// Fetches: RELEASES_API_URL via useQuery
```

From taskflow/src/components/update/UpdateDialog.test.tsx (mock patterns):
```typescript
vi.mock('@tauri-apps/plugin-updater', () => ({ check: vi.fn() }));
vi.mock('@/services/updater', () => ({ updaterService: { check: vi.fn(), downloadAndInstall: vi.fn() } }));
```

From taskflow/src/routes/settings/Settings.test.tsx (existing structure):
```typescript
// Line 129-134: Tests 6 nav buttons with regex /Connections|Appearance|...|Advanced/i
// mockSettingsStore needs updateCheckInterval: 6 for UpdatesSection
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write AboutDialog tests</name>
  <files>
    taskflow/src/components/about/AboutDialog.test.tsx
  </files>
  <read_first>
    taskflow/src/components/about/AboutDialog.tsx,
    taskflow/src/components/update/UpdateDialog.test.tsx,
    taskflow/src/stores/update.store.ts,
    taskflow/src/lib/build-info.ts,
    taskflow/src/test/setup.ts
  </read_first>
  <action>
Create `taskflow/src/components/about/AboutDialog.test.tsx` with these tests:

**Mock setup:**
```typescript
import { render, screen } from '@testing-library/react';
import { describe, expect, it, vi, beforeEach } from 'vitest';
import { AboutDialog } from './AboutDialog';

// Mock build-info with known values
vi.mock('@/lib/build-info', () => ({
  buildInfo: { version: '1.6.0', commitSha: 'abc1234', buildDate: '2026-03-24' },
}));

// Mock update store — default to idle status
const mockUpdateStore = { status: 'idle' as const, availableVersion: null };
vi.mock('@/stores/update.store', () => ({
  useUpdateStore: (selector?: (s: typeof mockUpdateStore) => unknown) =>
    selector ? selector(mockUpdateStore) : mockUpdateStore,
}));
```

**Test cases:**

1. `it('renders version from buildInfo')` — render with `open={true}`, assert `screen.getByText('1.6.0')` exists
2. `it('renders commit SHA from buildInfo')` — assert `screen.getByText('abc1234')` exists
3. `it('renders build date from buildInfo')` — assert `screen.getByText('2026-03-24')` exists
4. `it('renders platform info')` — assert `screen.getByText(/mac|win|linux/i)` exists (navigator.platform returns a string in jsdom)
5. `it('renders Taskflow title')` — assert `screen.getByText('Taskflow')` exists
6. `it('renders app icon')` — assert `screen.getByAltText('Taskflow')` exists (the img tag)
7. `it('shows "Up to date" when status is idle')` — assert `screen.getByText(/up to date/i)` exists
8. `it('shows "Update available" when status is available')` — before render, set `mockUpdateStore.status = 'available'; mockUpdateStore.availableVersion = '1.7.0';`, render, assert `screen.getByText(/update available.*1\.7\.0/i)` exists. Reset in afterEach/beforeEach.
9. `it('renders Close button')` — assert `screen.getByRole('button', { name: /close/i })` exists
10. `it('does not render when open is false')` — render with `open={false}`, assert dialog content is NOT in document

**Reset mock state in `beforeEach`:**
```typescript
beforeEach(() => {
  mockUpdateStore.status = 'idle';
  mockUpdateStore.availableVersion = null;
});
```
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/components/about/AboutDialog.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - AboutDialog.test.tsx exists and contains at least 8 test cases (`it(` appears >= 8 times)
    - AboutDialog.test.tsx mocks `@/lib/build-info` with version '1.6.0', commitSha 'abc1234', buildDate '2026-03-24'
    - AboutDialog.test.tsx mocks `@/stores/update.store` with status and availableVersion
    - AboutDialog.test.tsx tests both 'idle' (Up to date) and 'available' (Update available) status display
    - AboutDialog.test.tsx tests Close button existence
    - AboutDialog.test.tsx tests open={false} does not render
    - All tests pass (vitest exit code 0)
  </acceptance_criteria>
  <done>AboutDialog has comprehensive test coverage for all metadata fields, both update status states, and open/close behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Write UpdatesSection tests and update Settings.test.tsx</name>
  <files>
    taskflow/src/routes/settings/UpdatesSection.test.tsx,
    taskflow/src/routes/settings/Settings.test.tsx
  </files>
  <read_first>
    taskflow/src/routes/settings/UpdatesSection.tsx,
    taskflow/src/routes/settings/Settings.tsx,
    taskflow/src/routes/settings/Settings.test.tsx,
    taskflow/src/stores/settings.store.ts,
    taskflow/src/stores/update.store.ts,
    taskflow/src/services/updater.ts,
    taskflow/src/test/setup.ts
  </read_first>
  <action>
**1. Create `taskflow/src/routes/settings/UpdatesSection.test.tsx`:**

**Mock setup:**
```typescript
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { describe, expect, it, vi, beforeEach } from 'vitest';
import UpdatesSection from './UpdatesSection';

function renderWithQuery(ui: React.ReactElement) {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return render(<QueryClientProvider client={qc}>{ui}</QueryClientProvider>);
}

// Mock build-info
vi.mock('@/lib/build-info', () => ({
  buildInfo: { version: '1.6.0', commitSha: 'abc1234', buildDate: '2026-03-24' },
}));

// Mock settings store
const mockSettingsStore = {
  updateCheckInterval: 6 as 1 | 6 | 12 | 24 | 'manual',
  setUpdateCheckInterval: vi.fn(),
  lastChecked: null as string | null,
  setLastChecked: vi.fn(),
};
vi.mock('@/stores/settings.store', () => ({
  useSettingsStore: Object.assign(
    (selector?: (s: typeof mockSettingsStore) => unknown) =>
      selector ? selector(mockSettingsStore) : mockSettingsStore,
    { getState: () => mockSettingsStore },
  ),
}));

// Mock update store
const mockUpdateStore = {
  status: 'idle' as const,
  availableVersion: null as string | null,
  setChecking: vi.fn(),
  setAvailable: vi.fn(),
  setError: vi.fn(),
  resetToIdle: vi.fn(),
};
vi.mock('@/stores/update.store', () => ({
  useUpdateStore: Object.assign(
    (selector?: (s: typeof mockUpdateStore) => unknown) =>
      selector ? selector(mockUpdateStore) : mockUpdateStore,
    { getState: () => mockUpdateStore },
  ),
}));

// Mock updater service
const mockCheck = vi.fn().mockResolvedValue(null);
vi.mock('@/services/updater', () => ({
  updaterService: { check: (...args: unknown[]) => mockCheck(...args) },
}));

// Mock global fetch for version history
const mockFetch = vi.fn();
vi.stubGlobal('fetch', mockFetch);
```

**Reset in `beforeEach`:**
```typescript
beforeEach(() => {
  vi.clearAllMocks();
  mockSettingsStore.updateCheckInterval = 6;
  mockSettingsStore.lastChecked = null;
  mockUpdateStore.status = 'idle';
  mockUpdateStore.availableVersion = null;
  mockFetch.mockResolvedValue({
    ok: true,
    json: () => Promise.resolve([
      { tag_name: 'v1.6.0', name: 'v1.6.0', published_at: '2026-03-24T12:00:00Z', body: '## Changes\n- Feature A', prerelease: false, draft: false },
      { tag_name: 'v1.5.0', name: 'v1.5.0', published_at: '2026-03-20T12:00:00Z', body: '## Changes\n- Feature B', prerelease: false, draft: false },
    ]),
  });
});
```

**Test cases (UI-03):**

1. `it('renders section-updates testid')` — `screen.getByTestId('section-updates')`
2. `it('renders current version display')` — `screen.getByText(/1\.6\.0/)`
3. `it('renders Updates heading')` — `screen.getByRole('heading', { name: /updates/i })`
4. `it('renders Check Now button')` — `screen.getByRole('button', { name: /check now/i })`
5. `it('renders check frequency dropdown')` — look for the Select trigger showing "Every 6 hours"
6. `it('hides last checked when never checked')` — assert `screen.queryByText(/last checked/i)` is null when `mockSettingsStore.lastChecked = null`
7. `it('shows last checked when timestamp exists')` — set `mockSettingsStore.lastChecked = new Date().toISOString()`, render, assert `screen.getByText(/last checked/i)` exists
8. `it('Check Now button shows checking state')` — click Check Now, assert `screen.getByText(/checking/i)` appears

**Test cases (UI-04):**

9. `it('renders version history loading skeletons')` — mock fetch to return a pending promise, render, assert 3 skeleton elements exist (query by class or role)
10. `it('renders version history release rows when loaded')` — render, waitFor `screen.getByText('v1.6.0')` and `screen.getByText('v1.5.0')`
11. `it('shows (current) badge for matching version')` — render, waitFor `screen.getByText('current')` (Badge text)
12. `it('shows error state with Retry when fetch fails')` — mock fetch to reject, render, waitFor `screen.getByText(/unable to load release history/i)` and `screen.getByRole('button', { name: /retry/i })`
13. `it('expands release row to show changelog on click')` — render, waitFor release row, click v1.5.0 row, assert `screen.getByText(/feature b/i)` appears (from markdown body)

**2. Update `taskflow/src/routes/settings/Settings.test.tsx`:**

a) In the `mockSettingsStore` object, add:
```typescript
updateCheckInterval: 6,
setUpdateCheckInterval: vi.fn(),
lastChecked: null,
setLastChecked: vi.fn(),
```

b) Update the first test (`'renders 7 sidebar nav buttons'`):
- Change the regex in `getAllByRole('button', { name: /.../ })` to include `Updates`:
```typescript
const navButtons = screen.getAllByRole('button', {
  name: /Connections|Appearance|Sidebar|Notifications|Workflow|Updates|Advanced/i,
});
expect(navButtons.length).toBe(7);
```

c) Update the `'renders sidebar buttons with correct labels'` test — add:
```typescript
expect(screen.getByRole('button', { name: /updates/i })).toBeInTheDocument();
```

d) Add mock for `@/stores/update.store` at top level (UpdatesSection reads it):
```typescript
vi.mock('@/stores/update.store', () => ({
  useUpdateStore: Object.assign(
    (selector?: (s: Record<string, unknown>) => unknown) =>
      selector ? selector({ status: 'idle', availableVersion: null }) : { status: 'idle', availableVersion: null },
    { getState: () => ({ status: 'idle', availableVersion: null, setChecking: vi.fn(), setAvailable: vi.fn(), setError: vi.fn(), resetToIdle: vi.fn() }) },
  ),
}));
```

e) Add mock for `@/services/updater`:
```typescript
vi.mock('@/services/updater', () => ({
  updaterService: { check: vi.fn().mockResolvedValue(null) },
}));
```

f) Add mock for `@/lib/build-info`:
```typescript
vi.mock('@/lib/build-info', () => ({
  buildInfo: { version: '1.6.0', commitSha: 'abc1234', buildDate: '2026-03-24' },
}));
```

g) Stub global fetch for version history query:
```typescript
vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
  ok: true,
  json: () => Promise.resolve([]),
}));
```
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/settings/UpdatesSection.test.tsx src/routes/settings/Settings.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - UpdatesSection.test.tsx exists and contains at least 10 test cases (`it(` appears >= 10 times)
    - UpdatesSection.test.tsx mocks `@/stores/settings.store`, `@/stores/update.store`, `@/services/updater`, and global `fetch`
    - UpdatesSection.test.tsx tests Check Now button state changes
    - UpdatesSection.test.tsx tests version history loading, loaded, and error states
    - UpdatesSection.test.tsx tests (current) badge on matching version
    - UpdatesSection.test.tsx tests expandable changelog
    - Settings.test.tsx contains `expect(navButtons.length).toBe(7)` (updated from 6)
    - Settings.test.tsx name regex includes `Updates`
    - Settings.test.tsx has mock for `@/stores/update.store`
    - Settings.test.tsx has mock for `@/services/updater`
    - Settings.test.tsx has mock for `@/lib/build-info`
    - All tests pass (vitest exit code 0)
  </acceptance_criteria>
  <done>Full test coverage for UpdatesSection (controls + version history) and Settings nav updated to 7 items. All new and existing tests pass.</done>
</task>

</tasks>

<verification>
- `cd taskflow && npx vitest run src/components/about/AboutDialog.test.tsx src/routes/settings/UpdatesSection.test.tsx src/routes/settings/Settings.test.tsx` — all pass
- `cd taskflow && npm test` — full suite passes (no regressions)
</verification>

<success_criteria>
- AboutDialog has test coverage for all metadata fields and both update status states (UI-01)
- UpdatesSection has test coverage for controls, version history states, and expandable changelogs (UI-03, UI-04)
- Settings.test.tsx updated with 7 nav buttons including Updates (UI-02, UI-03)
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/40-settings-about-menu-integration/40-03-SUMMARY.md`
</output>
