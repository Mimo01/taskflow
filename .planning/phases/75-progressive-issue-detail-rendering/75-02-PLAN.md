---
phase: 75-progressive-issue-detail-rendering
plan: 02
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - taskflow/src/routes/dashboard/IssueDetailPage.tsx
  - taskflow/src/routes/dashboard/IssueDetailContent.tsx
  - taskflow/src/routes/dashboard/issue-detail/ActivityTimeline.tsx
autonomous: true
requirements: [PERF-DETAIL-01, PERF-DETAIL-02, GH-CUT-01, GH-CUT-02]
user_setup: []

must_haves:
  truths:
    - "Header (title/key/status/assignee) renders as soon as the base issue fetch resolves, not gated on comments/subtasks/changelog"
    - "Comments, subtasks, and changelog each show a localized skeleton (200ms-gated) while their own query is pending"
    - "Each independent section shows an inline ErrorState with retry on its own query error, without blanking the panel"
    - "Only base-fetch failure blanks the panel (D-08)"
    - "performance.mark/measure instrumentation records TTFMP (header paint) and TTI (last section resolved)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/IssueDetailPage.tsx"
      provides: "Three independent section queries + global-gate removal + perf marks"
      contains: "jira-issue-comments"
    - path: "taskflow/src/routes/dashboard/IssueDetailContent.tsx"
      provides: "Subtask skeleton + error slot"
      contains: "SubtasksSkeleton"
    - path: "taskflow/src/routes/dashboard/issue-detail/ActivityTimeline.tsx"
      provides: "Reachable changelog skeleton via undefined prop"
      contains: "changelog === undefined"
  key_links:
    - from: "taskflow/src/routes/dashboard/IssueDetailPage.tsx"
      to: "fetchComments / fetchEnrichedSubtasks / fetchIssueChangelog"
      via: "three useQuery calls with dedicated keys"
      pattern: "jira-issue-(comments|changelog)|jira-subtask-enrichment"
    - from: "taskflow/src/routes/dashboard/IssueDetailPage.tsx (comments derivation)"
      to: "commentsQuery.data"
      via: "replace issue?.fields.comment?.comments derivation"
      pattern: "commentsQuery\\.data"
---

<objective>
The crux of the phase. Remove the single global `isLoading || !issue` gate at `IssueDetailPage.tsx:377` and push loading state down into three new independent `useQuery` calls (comments, enriched subtasks, changelog), each gated through `useDelayedLoading(isPending, 200)` with its own inline `ErrorState` retry. Rewire the `comments` derivation and the `ActivityTimeline` changelog prop to consume the new queries instead of the base response. Add `performance.mark`/`measure` instrumentation for TTFMP and TTI (GH-CUT-02 measurement baked in, not bolted on).

Per D-08: only base-fetch failure may blank the panel; every other section degrades independently (D-07, mirroring Phase 69 standup). Per GH-CUT-01: all queries stay on REST v2. Per D-09: this applies to the canonical `IssueDetailPage` only (the Epic `isEpic` branch benefits automatically via shared `IssueDetailContent`); the legacy dead `IssueDetailSheet` is NOT touched.

Purpose: Deliver progressive rendering — header paints first (PERF-DETAIL-01), each section gates its own skeleton (PERF-DETAIL-02).
Output: Rewritten `IssueDetailPage`, subtask skeleton/error in `IssueDetailContent`, reachable changelog skeleton in `ActivityTimeline`, perf instrumentation. The 75-01 progressive test scaffold turns GREEN.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-progressive-issue-detail-rendering/75-CONTEXT.md
@.planning/phases/75-progressive-issue-detail-rendering/75-RESEARCH.md
@.planning/phases/75-progressive-issue-detail-rendering/75-PATTERNS.md
@.planning/phases/75-progressive-issue-detail-rendering/75-UI-SPEC.md
@.planning/phases/75-progressive-issue-detail-rendering/75-VALIDATION.md
@.planning/phases/75-progressive-issue-detail-rendering/75-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three independent section queries + delayed-loading gates + perf instrumentation to IssueDetailPage</name>
  <files>taskflow/src/routes/dashboard/IssueDetailPage.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/IssueDetailPage.tsx (base query 77-92; comments derivation 127; useMentionUserMap/commentTexts 154-155; initialUserMap memo 137-152; worklogs query 247-256; ActivityTimeline call ~412-413)
    - taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx (lines 624-684 — useQuery + useDelayedLoading + ErrorState canonical shape)
    - taskflow/src/hooks/useDelayedLoading.ts (full file — input is isPending)
    - taskflow/src/services/jira/comments.ts (fetchComments signature) and taskflow/src/services/jira/changelog.ts (fetchIssueChangelog — from 75-01)
    - .planning/phases/75-progressive-issue-detail-rendering/75-PATTERNS.md (IssueDetailPage 3-query block + gates + derivation/prop changes, lines 193-311)
    - .planning/phases/75-progressive-issue-detail-rendering/75-RESEARCH.md (§Common Pitfalls 1,2,4,5,6 lines 437-476; §Verification measurement lines 388-431)
  </read_first>
  <action>
    After the existing worklogs query, add three `useQuery` calls, each using the token-outside-key convention (read `readSecret('jira-pat')` inside `queryFn`), `staleTime: 30_000`, and `enabled: !!issueKey && !!jiraBaseUrl && !!jiraConnected`:
    - `commentsQuery` key `['jira-issue-comments', issueKey, jiraBaseUrl]` calling `fetchComments(jiraBaseUrl, token, issueKey ?? '')`.
    - `subtaskEnrichmentQuery` key `['jira-subtask-enrichment', issueKey, jiraBaseUrl]` calling `fetchEnrichedSubtasks(jiraBaseUrl, token, issue?.fields.subtasks ?? [])`, with the additional enable condition `&& (issue?.fields.subtasks?.length ?? 0) > 0` (read subtask keys from base cache inside queryFn; key stays `issueKey`-based per Pitfall 4).
    - `changelogQuery` key `['jira-issue-changelog', issueKey, jiraBaseUrl]` calling `fetchIssueChangelog(jiraBaseUrl, token, issueKey ?? '')`.

    Add delayed-loading gates: `const showCommentsSkeleton = useDelayedLoading(commentsQuery.isPending)`, `showSubtasksSkeleton = useDelayedLoading(subtaskEnrichmentQuery.isPending)`, `showChangelogSkeleton = useDelayedLoading(changelogQuery.isPending)`. Use `isPending` (TanStack v5 canonical), NOT isLoading.

    Replace the `comments` derivation (line 127) from `issue?.fields.comment?.comments ?? []` to `commentsQuery.data ?? []` (Pitfall 1 — `commentTexts`, `useMentionUserMap`, `initialUserMap` all keep working with `[]` on initial load).

    Add perf instrumentation per RESEARCH §Verification: `performance.mark('issue-detail-start')` on mount; a `useEffect` watching `issue` that on first non-undefined fires `performance.mark('issue-detail-header-paint')` + `performance.measure('TTFMP', 'issue-detail-start', 'issue-detail-header-paint')` guarded by a ref so it runs once; a `useEffect` watching base+comments+changelog+subtasks resolution that fires `performance.mark('issue-detail-fully-loaded')` + `performance.measure('TTI', ...)`. Log via `console.table(performance.getEntriesByType('measure'))` so the 75-04 measurement checkpoint can read it.

    Do NOT touch any mutation/invalidation site in this task — that is 75-03.
  </action>
  <verify>
    <automated>cd taskflow && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Three `useQuery` calls with keys `['jira-issue-comments', ...]`, `['jira-subtask-enrichment', ...]`, `['jira-issue-changelog', ...]` (grep all three present).
    - `comments` derivation reads `commentsQuery.data ?? []` (grep `commentsQuery.data`); the old `issue?.fields.comment?.comments` derivation is gone.
    - `useDelayedLoading(commentsQuery.isPending)` etc. present (grep `useDelayedLoading`).
    - `performance.mark('issue-detail-start')` and `performance.measure('TTFMP'` and `'TTI'` present.
    - Token read inside queryFn, never in any query key (grep query keys contain no `token`/`pat`).
    - `cd taskflow && npm run build` passes.
  </acceptance_criteria>
  <done>Three section queries fire in parallel with delayed skeletons; comments come from the new query; TTFMP/TTI instrumented; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove global gate, wire ActivityTimeline changelog prop, add per-section inline errors</name>
  <files>taskflow/src/routes/dashboard/IssueDetailPage.tsx, taskflow/src/routes/dashboard/issue-detail/ActivityTimeline.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/IssueDetailPage.tsx (global gate line 377; ActivityTimeline call 412-413; full-panel IssueDetailSkeleton 626-643)
    - taskflow/src/routes/dashboard/issue-detail/ActivityTimeline.tsx (lines 122-131 — existing `changelog === undefined` skeleton guard; props interface for the changelog prop)
    - taskflow/src/components/ui/error-state.tsx (ErrorState props: error, onRetry, viewName — "Couldn't load {viewName}" + Retry)
    - .planning/phases/75-progressive-issue-detail-rendering/75-PATTERNS.md (global-gate removal + ActivityTimeline prop change + inline error pattern, lines 264-311 and 348-369)
    - .planning/phases/75-progressive-issue-detail-rendering/75-UI-SPEC.md (Interaction Contract + Copywriting Contract, lines 122-164)
    - .planning/phases/75-progressive-issue-detail-rendering/75-RESEARCH.md (Pitfall 2 — pass raw possibly-undefined changelog, lines 445-451)
  </read_first>
  <action>
    Change the global gate at line 377 from `{isLoading || !issue ? <IssueDetailSkeleton/> : ...}` to gate only on base fetch: `{!issue ? (isLoading ? <IssueDetailSkeleton/> : <full-panel ErrorState error onRetry=refetchBase viewName="issue"/>) : (...)}`. Per D-08 this is the ONLY case that may blank the panel; sections handle their own pending state below.

    Update the `ActivityTimeline` prop interface: change `changelog` from `ChangelogHistory[]` to `ChangelogHistory[] | undefined`. The existing `changelog === undefined` skeleton guard (lines 122-131) becomes reachable. In `IssueDetailPage`, change the prop from `changelog={issue.changelog?.histories ?? []}` to `changelog={showChangelogSkeleton ? undefined : changelogQuery.data}` (Pitfall 2 — pass raw/undefined to trigger the built-in skeleton; the 200ms gate suppresses flash). Add a changelog `ErrorState` (viewName "activity") wrapping the ActivityTimeline area when `changelogQuery.isError`.

    Add inline `ErrorState` for comments: when `commentsQuery.isError`, render `<div className="p-4"><ErrorState error={commentsQuery.error} onRetry={() => void queryClient.invalidateQueries({ queryKey: ['jira-issue-comments', issueKey, jiraBaseUrl] })} viewName="comments" /></div>` in the comments slot, otherwise pass through to the existing comments render. Use viewName strings "comments" / "activity" exactly (Copywriting Contract → "Couldn't load comments" / "Couldn't load activity").

    Pass `enrichedSubtasks` (from `subtaskEnrichmentQuery.data`), `showSubtasksSkeleton`, and the subtask query error/refetch down to `IssueDetailContent` as props (consumed in Task 3).
  </action>
  <verify>
    <automated>cd taskflow && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The gate at ~377 no longer contains `isLoading || !issue`; it gates on `!issue` only (grep `isLoading || !issue` returns 0 hits).
    - `ActivityTimeline` changelog prop type is `ChangelogHistory[] | undefined`; the call passes `changelogQuery.data` (raw), not `?? []`.
    - `ErrorState` with `viewName="comments"` and `viewName="activity"` present in IssueDetailPage.
    - `cd taskflow && npm run build` passes.
  </acceptance_criteria>
  <done>Global gate removed; only base failure blanks the panel; comments + activity show inline errors; changelog skeleton reachable.</done>
</task>

<task type="auto">
  <name>Task 3: Subtask skeleton + error slot in IssueDetailContent; turn the progressive test green</name>
  <files>taskflow/src/routes/dashboard/IssueDetailContent.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/IssueDetailContent.tsx (subtask section block ~186-230; props interface; isEpic/isSubtask flags; jiraBaseUrlFromStore at line 63)
    - taskflow/src/routes/dashboard/issue-detail/SubtasksSkeleton.tsx (from 75-01)
    - taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx (lines 666-684 — skeleton-then-error-then-content gating order)
    - taskflow/src/routes/dashboard/IssueDetailPage.progressive.test.tsx (from 75-01 — the RED tests this task turns GREEN)
    - .planning/phases/75-progressive-issue-detail-rendering/75-PATTERNS.md (IssueDetailContent subtask skeleton + error, lines 315-344)
  </read_first>
  <action>
    Extend `IssueDetailContent`'s props to accept `enrichedSubtasks` (the merged subtasks array, possibly undefined while pending), `showSubtasksSkeleton`, and subtask error + refetch (passed from 75-02 Task 2). Before the existing subtask `<section>` render, add the gating order (matching AioTestRunsSection): if `!isEpic && !isSubtask && enrichedSubtasks === undefined && showSubtasksSkeleton` → render `<SubtasksSkeleton />`; else if subtask query isError → render `<div className="p-4"><ErrorState error onRetry={subtaskRefetch} viewName="subtasks" /></div>`; else render the existing subtask list using `enrichedSubtasks` (fall back to base `subtasks` when enrichment is empty/disabled). Preserve existing empty-state copy "No subtasks." unchanged (PERF-DETAIL-03).

    Import `SubtasksSkeleton` from `./issue-detail/SubtasksSkeleton`. Do NOT add `font-semibold` to any new element.

    After wiring, run the 75-01 progressive test — it should now pass (header renders independently, comments-skeleton/subtasks-skeleton testids present, changelog skeleton reachable). If any assertion still fails, fix the wiring (not the test).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/IssueDetailPage.progressive.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `IssueDetailContent.tsx` imports and renders `SubtasksSkeleton` under the `enrichedSubtasks === undefined && showSubtasksSkeleton` condition.
    - Subtask `ErrorState` with `viewName="subtasks"` present.
    - `IssueDetailPage.progressive.test.tsx` is now GREEN (all `it` blocks pass).
    - Existing "No subtasks." empty-state copy unchanged.
    - `cd taskflow && npm run build` passes and `cd taskflow && npm run test` is green for the progressive file.
  </acceptance_criteria>
  <done>Subtasks gate their own skeleton/error; the Wave 0 progressive test turns green; PERF-DETAIL-01/02 behaviors verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView → Jira REST v2 | Same authenticated read boundary; this plan only changes client-side query orchestration and rendering |

## STRIDE Threat Register

Pure client-side rendering refactor. No new endpoints, inputs, or auth. Comments/changelog/subtasks were already fetched and rendered before this phase — only the fetch orchestration and gating change.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04 | Information Disclosure | New query keys | accept | Keys are `[prefix, issueKey, jiraBaseUrl]` only — no PAT in keys (token read inside queryFn) |
| T-75-05 | Denial of Service | Three parallel section queries instead of one | accept | TanStack Query dedups + caches per key; same total data volume as the prior single call, just split — no amplification |
| T-75-02 | Tampering | Comment/changelog/subtask rendering (XSS) | accept | Rendering path (wiki renderer, sanitization) unchanged; only data source moves |
| T-75-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase |

No NEW high-severity threats. Per-section error isolation (D-07/D-08) is a resilience improvement, not a new attack surface.
</threat_model>

<verification>
- `cd taskflow && npm run build` green.
- `IssueDetailPage.progressive.test.tsx` GREEN (was RED after 75-01).
- Global `isLoading || !issue` gate removed; only base failure blanks the panel.
- Three section queries on REST v2 keys; no GreenHopper endpoint (GH-CUT-01).
- TTFMP/TTI perf marks present for the 75-04 measurement checkpoint (GH-CUT-02 instrumentation).
</verification>

<success_criteria>
- Header paints on base resolve, independently of comments/subtasks/changelog (PERF-DETAIL-01).
- Each section shows a 200ms-gated localized skeleton; no global spinner (PERF-DETAIL-02).
- Per-section inline errors with retry; only base failure blanks panel (D-07/D-08).
</success_criteria>

<output>
Create `.planning/phases/75-progressive-issue-detail-rendering/75-02-SUMMARY.md` when done.
</output>
