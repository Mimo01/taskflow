---
phase: 75-progressive-issue-detail-rendering
plan: 03
type: execute
wave: 3
depends_on: ["75-02"]
files_modified:
  - taskflow/src/routes/dashboard/IssueDetailPage.tsx
  - taskflow/src/routes/dashboard/CommentComposer.tsx
  - taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx
  - taskflow/src/routes/dashboard/IssueDetailContent.tsx
  - taskflow/src/routes/dashboard/IssueDetailPage.progressive.test.tsx
autonomous: true
requirements: [PERF-DETAIL-03]
user_setup: []

must_haves:
  truths:
    - "Posting, editing, or deleting a comment invalidates ['jira-issue-comments', issueKey, jiraBaseUrl]"
    - "A status transition invalidates ['jira-issue-changelog', issueKey, jiraBaseUrl] so the activity section refreshes"
    - "The pre-existing wrong-key attachment-delete invalidation at IssueDetailContent.tsx:68 is fixed to ['jira-issue-detail', issueKey, jiraBaseUrl]"
    - "Field edit optimistic update still targets the base ['jira-issue-detail', ...] key after the split"
    - "Comment + changelog invalidation is asserted by automated tests"
  artifacts:
    - path: "taskflow/src/routes/dashboard/CommentComposer.tsx"
      provides: "Post-comment invalidates the comments key"
      contains: "jira-issue-comments"
    - path: "taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx"
      provides: "Status change invalidates the changelog key"
      contains: "jira-issue-changelog"
    - path: "taskflow/src/routes/dashboard/IssueDetailPage.progressive.test.tsx"
      provides: "Invalidation assertions for new keys"
      contains: "jira-issue-comments"
  key_links:
    - from: "comment mutations (post/edit/delete)"
      to: "['jira-issue-comments', issueKey, jiraBaseUrl]"
      via: "queryClient.invalidateQueries"
      pattern: "jira-issue-comments"
    - from: "status transition (FieldsSection)"
      to: "['jira-issue-changelog', issueKey, jiraBaseUrl]"
      via: "queryClient.invalidateQueries onSettled"
      pattern: "jira-issue-changelog"
---

<objective>
The highest-risk area of the phase (PERF-DETAIL-03). After 75-02 split comments and changelog into their own cache keys, every mutation that previously refreshed those sections by invalidating only `['jira-issue-detail', ...]` is now stale. Fan out invalidation to the correct new keys at every affected call site, fix the pre-existing wrong-key attachment-delete bug at `IssueDetailContent.tsx:68`, and assert the fan-out with automated tests.

Per the RESEARCH §Mutation/Invalidation Impact map (exhaustive 10-site grep): comment post/edit/delete must add the comments key; status transition must add the changelog key; attachment upload/delete and field edits stay on the base key (unchanged); BacklogPage prefix invalidation and WatcherToggle need no change.

Purpose: Guarantee all existing detail-panel features (post comment, edit fields, status change, attachment, watcher, clone, pin, deep link) work unchanged against the split keys (PERF-DETAIL-03).
Output: Updated invalidation at CommentComposer, comment edit/delete in IssueDetailPage, FieldsSection status change, the IssueDetailContent:68 key fix, and invalidation assertions in the progressive test.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-progressive-issue-detail-rendering/75-CONTEXT.md
@.planning/phases/75-progressive-issue-detail-rendering/75-RESEARCH.md
@.planning/phases/75-progressive-issue-detail-rendering/75-PATTERNS.md
@.planning/phases/75-progressive-issue-detail-rendering/75-VALIDATION.md
@.planning/phases/75-progressive-issue-detail-rendering/75-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fan out comment + changelog invalidation; fix the IssueDetailContent:68 key bug</name>
  <files>taskflow/src/routes/dashboard/CommentComposer.tsx, taskflow/src/routes/dashboard/IssueDetailPage.tsx, taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx, taskflow/src/routes/dashboard/IssueDetailContent.tsx</files>
  <read_first>
    - .planning/phases/75-progressive-issue-detail-rendering/75-RESEARCH.md (§Mutation/Invalidation Impact — the full 10-site current-state + required-state tables, lines 288-328)
    - .planning/phases/75-progressive-issue-detail-rendering/75-PATTERNS.md (CommentComposer/comment-mutation/FieldsSection/IssueDetailContent invalidation diffs, lines 373-426)
    - taskflow/src/routes/dashboard/CommentComposer.tsx (invalidation at line 89)
    - taskflow/src/routes/dashboard/IssueDetailPage.tsx (comment edit invalidation line 174; comment delete line 189)
    - taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx (status change onSettled invalidation block lines 260-271)
    - taskflow/src/routes/dashboard/IssueDetailContent.tsx (wrong-key attachment-delete invalidation line 68; jiraBaseUrlFromStore at line 63)
  </read_first>
  <action>
    CommentComposer.tsx (~line 89): after the existing `invalidateQueries({ queryKey: ['jira-issue-detail', issueKey, jiraBaseUrl] })`, add `invalidateQueries({ queryKey: ['jira-issue-comments', issueKey, jiraBaseUrl] })`.

    IssueDetailPage.tsx comment edit (~line 174) and comment delete (~line 189): in each `onSuccess`, add the same `['jira-issue-comments', issueKey, jiraBaseUrl]` invalidation alongside the existing base-key invalidation.

    FieldsSection.tsx status change `onSettled` (~lines 260-271): add `invalidateQueries({ queryKey: ['jira-issue-changelog', issueKey, jiraBaseUrl] })` after the existing invalidations (a status transition writes a changelog entry, so the activity section must refresh). Leave the existing board/sprint/epics/version invalidations untouched.

    IssueDetailContent.tsx (line 68): change the wrong/dead key `['issue-detail', issueKey]` to `['jira-issue-detail', issueKey, jiraBaseUrlFromStore]` (`jiraBaseUrlFromStore` already in scope at line 63). This is the pre-existing bug noted in CONTEXT/RESEARCH — fix as part of this phase.

    DO NOT change: attachment upload (AttachmentsSection:54 / AttachmentUpload:33 — stay base-only, attachments are on base), field-edit optimistic target in useFieldMutation (stays base key — field values on base), BacklogPage prefix invalidation (sprint move doesn't change comments/changelog — acceptable per RESEARCH), WatcherToggle (already isolated). Confirm these are untouched.
  </action>
  <verify>
    <automated>cd taskflow && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `CommentComposer.tsx` contains `jira-issue-comments` invalidation (grep).
    - IssueDetailPage comment edit AND delete `onSuccess` each invalidate `jira-issue-comments` (grep count ≥ 2 across the two handlers).
    - `FieldsSection.tsx` status-change onSettled invalidates `jira-issue-changelog` (grep).
    - `IssueDetailContent.tsx` no longer contains the bare `['issue-detail', issueKey]` key (grep `'issue-detail', issueKey` filtered to exclude `'jira-issue-detail'` returns 0 hits); it uses `['jira-issue-detail', issueKey, jiraBaseUrlFromStore]`.
    - `AttachmentsSection.tsx`/`AttachmentUpload.tsx`/`WatcherToggle.tsx`/`BacklogPage.tsx` invalidation lines are unchanged.
    - `cd taskflow && npm run build` passes.
  </acceptance_criteria>
  <done>Comment mutations refresh the comments cache; status change refreshes the changelog cache; the dead attachment-delete key is fixed; no over-invalidation introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert invalidation fan-out in the progressive test</name>
  <files>taskflow/src/routes/dashboard/IssueDetailPage.progressive.test.tsx</files>
  <behavior>
    - PERF-DETAIL-03: triggering a comment post/edit/delete calls `queryClient.invalidateQueries` with a key matching `['jira-issue-comments', issueKey, jiraBaseUrl]`.
    - PERF-DETAIL-03: triggering a status transition invalidates `['jira-issue-changelog', issueKey, jiraBaseUrl]`.
    - These assertions pass GREEN after Task 1's wiring.
  </behavior>
  <read_first>
    - taskflow/src/routes/dashboard/IssueDetailPage.progressive.test.tsx (current scaffold from 75-01/75-02)
    - taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx (spy-on-invalidateQueries pattern via QueryClient)
    - taskflow/src/routes/dashboard/issue-detail/FieldsSection.test.tsx (existing pattern asserting optimistic update / invalidation if present — reuse mocking approach)
    - .planning/phases/75-progressive-issue-detail-rendering/75-VALIDATION.md (Per-Task Verification Map PERF-DETAIL-03 rows; Wave 0 comment-mutation invalidation requirement)
  </read_first>
  <action>
    Add a `describe('invalidation fan-out')` block to the progressive test. Spy on `queryClient.invalidateQueries` (via `vi.spyOn` on the test QueryClient instance returned by `makeQueryClient`). Mock the comment post/edit/delete service calls to resolve, trigger the mutation through the rendered component (or directly invoke the mutation handler if the component surface is awkward), and assert `invalidateQueries` was called with an object whose `queryKey` deep-equals `['jira-issue-comments', 'PROJ-123', <mockBaseUrl>]`. Add an analogous assertion for the status-transition path invalidating `['jira-issue-changelog', ...]`.

    If asserting through the full component render is brittle, it is acceptable to assert at the handler level by importing the invalidation helper, but prefer the rendered-component path to match VALIDATION's "unit" test type. Keep `retry:false` on the test QueryClient.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/IssueDetailPage.progressive.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains assertions matching `jira-issue-comments` and `jira-issue-changelog` invalidation.
    - The full progressive test file is GREEN.
    - `cd taskflow && npm run test` is green for the file.
  </acceptance_criteria>
  <done>Invalidation fan-out for the new keys is automatically asserted; PERF-DETAIL-03 covered by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate</name>
  <files>taskflow/src/routes/dashboard/IssueDetailPage.tsx</files>
  <read_first>
    - .planning/phases/75-progressive-issue-detail-rendering/75-VALIDATION.md (Sampling Rate + Validation Sign-Off — full suite green before verify)
    - taskflow/src/routes/dashboard/issue-detail/FieldsSection.test.tsx (existing test that must still pass — optimistic update against base key)
  </read_first>
  <action>
    Run the full Vitest suite from `taskflow/` and confirm green, with special attention to the existing `FieldsSection.test.tsx` (field-edit optimistic update must still target the base `['jira-issue-detail', ...]` key after the split — RESEARCH confirms no change needed) and any comment/attachment/worklog tests. If any pre-existing test regressed because of the comments/changelog split (e.g. a test that read `issue.fields.comment`), update that test to the new query-based source — but do NOT weaken behavioral assertions. Then run `npm run build` to confirm tsc + vite (CSS imports) are clean.

    This task makes no production change unless a regression is found; its purpose is the phase regression gate before verification.
  </action>
  <verify>
    <automated>cd taskflow && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npm run test` exits 0 (full suite green, including FieldsSection.test.tsx).
    - `cd taskflow && npm run build` exits 0.
    - Any test touched is updated to the new query source without weakening assertions (note each in the SUMMARY).
  </acceptance_criteria>
  <done>Full suite + build green; all existing detail-panel features verified working against split keys (PERF-DETAIL-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView → Jira REST v2 write paths | Existing mutation endpoints (comment post/edit/delete, field/status edit, attachment); unchanged by this plan |

## STRIDE Threat Register

This plan only adjusts client-side cache-invalidation targets after the read split. No write path, endpoint, input, or auth changes. The IssueDetailContent:68 fix repairs a pre-existing dead invalidation (security-neutral correctness fix).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-06 | Tampering | Stale cache after mutation | mitigate | Explicit invalidation fan-out to the correct new keys at every affected call site (RESEARCH 10-site map), asserted by automated tests in Task 2 |
| T-75-07 | Repudiation | Changelog not refreshing after status change | mitigate | Status transition explicitly invalidates `jira-issue-changelog` so the activity audit trail stays current |
| T-75-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase |

No NEW high-severity threats. Write paths stay on existing REST v2 (PERF-DETAIL-03); only cache orchestration changes.
</threat_model>

<verification>
- `cd taskflow && npm run test` full suite green; `npm run build` green.
- Comment mutations invalidate `jira-issue-comments`; status change invalidates `jira-issue-changelog` (asserted by tests).
- IssueDetailContent:68 key fixed to the canonical base key.
- Attachment/field/watcher/backlog invalidation unchanged (no over-invalidation).
</verification>

<success_criteria>
- All existing detail-panel features work unchanged against the split query keys (PERF-DETAIL-03).
- Invalidation fan-out asserted by automated tests; full suite + build green.
</success_criteria>

<output>
Create `.planning/phases/75-progressive-issue-detail-rendering/75-03-SUMMARY.md` when done. Note any pre-existing tests updated for the new comment source.
</output>
