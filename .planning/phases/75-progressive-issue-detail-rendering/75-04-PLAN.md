---
phase: 75-progressive-issue-detail-rendering
plan: 04
type: execute
wave: 4
depends_on: ["75-02", "75-03"]
files_modified:
  - taskflow/docs/perf/75-issue-detail-progressive.md
autonomous: false
requirements: [GH-CUT-02, PERF-DETAIL-01, PERF-DETAIL-02]
user_setup: []

must_haves:
  truths:
    - "A verification artifact records before/after time-to-first-meaningful-paint (header visible)"
    - "The artifact records before/after time-to-fully-interactive (last section resolved)"
    - "The artifact records per-section latencies (comments, subtasks, changelog, worklogs)"
    - "The artifact documents which section gates 'fully loaded'"
    - "A human confirms progressive rendering and per-section error isolation in the running app"
  artifacts:
    - path: "taskflow/docs/perf/75-issue-detail-progressive.md"
      provides: "GH-CUT-02 perf verification artifact"
      contains: "time-to-first-meaningful-paint"
  key_links:
    - from: "performance.measure('TTFMP'/'TTI')"
      to: "taskflow/docs/perf/75-issue-detail-progressive.md"
      via: "console.table capture transcribed into the artifact"
      pattern: "TTFMP|TTI"
---

<objective>
Record the GH-CUT-02 performance verification artifact: before/after time-to-first-meaningful-paint (header visible) and time-to-fully-interactive (last section resolved), per-section latencies, and which section gates "fully loaded." Because the app is a Tauri desktop app reading a live Jira instance, real-network timing can only be captured by a human running the app with the instrumentation added in 75-02 — this plan is a human-verify checkpoint, not an autonomous test.

Per CONTEXT §specifics and RESEARCH §Verification: the instrumentation (`performance.mark`/`measure` for TTFMP/TTI + `console.table`) was baked into 75-02, so this plan reads those numbers and transcribes them, plus visually confirms progressive rendering + per-section error isolation (PERF-DETAIL-01/02, D-07/D-08).

Purpose: Satisfy success criterion 4 / GH-CUT-02 — the perf-verification artifact of the final phase.
Output: `taskflow/docs/perf/75-issue-detail-progressive.md`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-progressive-issue-detail-rendering/75-CONTEXT.md
@.planning/phases/75-progressive-issue-detail-rendering/75-RESEARCH.md
@.planning/phases/75-progressive-issue-detail-rendering/75-VALIDATION.md
@.planning/phases/75-progressive-issue-detail-rendering/75-02-SUMMARY.md
@.planning/phases/75-progressive-issue-detail-rendering/75-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the perf verification artifact</name>
  <files>taskflow/docs/perf/75-issue-detail-progressive.md</files>
  <read_first>
    - .planning/phases/75-progressive-issue-detail-rendering/75-RESEARCH.md (§Verification/Measurement lines 388-431 — what to measure, the measurement approach, expected gating section)
    - .planning/phases/75-progressive-issue-detail-rendering/75-VALIDATION.md (Manual-Only Verifications table — the two manual checks)
    - .planning/phases/75-progressive-issue-detail-rendering/75-CONTEXT.md (§specifics lines 88-93)
  </read_first>
  <action>
    Create `taskflow/docs/perf/75-issue-detail-progressive.md` with the measurement structure ready for the human to fill: a "Before (single-gate)" section and an "After (progressive)" section, each with rows for time-to-first-meaningful-paint (header visible), time-to-fully-interactive (last section resolved), and per-section latencies (base, comments, enriched subtasks, changelog, worklogs). Include a "Which section gates 'fully loaded'" line (RESEARCH expects changelog as the heaviest payload — confirm during capture). Include a "How captured" note: open issue detail with DevTools console open, read the `console.table(performance.getEntriesByType('measure'))` output produced by the 75-02 instrumentation, and the per-section query timings from the React Query devtools / network panel. Add a "Test issue" field (the issue key used) and "Jira instance / network conditions" note (per Assumption A3 — numbers are instance-dependent).

    Leave the numeric cells as `TODO — fill from live capture`; the human checkpoint (Task 2) fills them.
  </action>
  <verify>
    <automated>test -f taskflow/docs/perf/75-issue-detail-progressive.md && grep -q "time-to-first-meaningful-paint" taskflow/docs/perf/75-issue-detail-progressive.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `taskflow/docs/perf/75-issue-detail-progressive.md`.
    - Contains before/after sections, TTFMP + TTI rows, per-section latency rows (base/comments/subtasks/changelog/worklogs), and a "gating section" line.
    - Documents the capture method referencing the 75-02 `performance.measure` marks.
  </acceptance_criteria>
  <done>Artifact scaffold ready for the human to fill with live numbers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture live perf numbers + verify progressive rendering and error isolation</name>
  <what-built>
    Progressive issue-detail rendering: header paints on base resolve, each section (comments, subtasks, changelog) shows a 200ms-gated localized skeleton then content, per-section inline errors with retry, and `performance.mark`/`measure` instrumentation (TTFMP + TTI) logged to the console. The perf artifact scaffold awaits live numbers.
  </what-built>
  <how-to-verify>
    1. Run the app: `cd taskflow && npm run dev` (or the project's tauri dev command), connected to the live Jira instance.
    2. Open DevTools console. Navigate to an issue with a non-trivial comment thread + subtasks + active changelog (record the issue key).
    3. Observe progressive rendering: the header (title/key/status/assignee) appears first; comments / subtasks / activity each show a skeleton briefly then fill — confirm NO global blocking spinner gates the whole panel (PERF-DETAIL-01/02). Cache-hit reopen should show no skeleton flash (200ms gate, D-06).
    4. Read the console `console.table(...measure...)` output: record TTFMP and TTI. Note per-section latencies from the network panel / React Query devtools. Identify which section resolves last ("gates fully loaded" — expected: changelog).
    5. For before/after: if a pre-split build is not readily available, record the "After" numbers and describe the qualitative before-state (panel blank until slowest call) — note this in the artifact. (The 75-02 SUMMARY may already capture pre-instrumentation observations.)
    6. Error isolation: simulate a single-section failure (e.g. block the `/comment` request via devtools, or temporarily point it at a bad path) and confirm comments shows an inline "Couldn't load comments" + Retry while the rest of the panel stays fully functional (D-07); confirm Retry refetches.
    7. Fill all `TODO` cells in `taskflow/docs/perf/75-issue-detail-progressive.md` with the captured numbers and the gating section, and commit the artifact.
  </how-to-verify>
  <acceptance_criteria>
    - `taskflow/docs/perf/75-issue-detail-progressive.md` has all numeric cells filled (TTFMP, TTI, per-section latencies) and the gating section named.
    - Human confirms: header paints first, per-section skeletons (no global spinner), no flash on cache hit, per-section error isolation + working retry.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the artifact is filled and progressive rendering + error isolation are confirmed, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView → Jira REST v2 | Read-only verification; no code behavior change in this plan |

## STRIDE Threat Register

This plan only adds a documentation artifact and runs a human verification pass. No code paths, endpoints, inputs, or auth are introduced.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-08 | Information Disclosure | Perf artifact contents | accept | Artifact records timing numbers + an issue key; no secrets, no PAT, no PII beyond an issue identifier — same data already visible in the app |
| T-75-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase |

No NEW high-severity threats. Documentation + verification only.
</threat_model>

<verification>
- `taskflow/docs/perf/75-issue-detail-progressive.md` exists and is fully filled (GH-CUT-02).
- Human-confirmed: header-first paint, per-section skeletons, no global spinner, per-section error isolation + retry (PERF-DETAIL-01/02, D-07/D-08).
</verification>

<success_criteria>
- Perf verification artifact records before/after TTFMP + TTI, per-section latencies, and the gating section (GH-CUT-02 / success criterion 4).
- Progressive rendering and independent error degradation visually confirmed in the running app.
</success_criteria>

<output>
Create `.planning/phases/75-progressive-issue-detail-rendering/75-04-SUMMARY.md` when done.
</output>
