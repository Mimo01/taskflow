---
phase: 66-roles-removal
plan: 02
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - taskflow/src/routes/settings/SidebarSection.tsx
  - taskflow/src/routes/settings/AppearanceSection.tsx
  - taskflow/src/routes/settings/PresetButtons.tsx
  - taskflow/src/routes/settings/RoleSection.tsx
  - taskflow/src/components/app/OnboardingWizard.tsx
  - taskflow/src/routes/onboarding/RoleStep.tsx
  - taskflow/src/routes/onboarding/RoleStep.test.tsx
  - taskflow/src/stores/onboarding.store.ts
  - taskflow/src/stores/onboarding.store.test.ts
  - taskflow/src/lib/tauri-storage.test.ts
  - taskflow/src/routes/settings/SidebarItemsList.test.tsx
  - taskflow/src/routes/settings/Settings.test.tsx
  - taskflow/src/routes/settings/ConnectionsSection.test.tsx
autonomous: true
requirements: [ROLES-01, ROLES-02, ROLES-03, ROLES-05]
must_haves:
  truths:
    - "The onboarding wizard renders exactly 4 steps: Welcome, Jira, GitLab, Done"
    - "No PresetButtons appear anywhere in Settings (Sidebar or Appearance sections)"
    - "RoleStep.tsx, RoleSection.tsx, PresetButtons.tsx, and RoleStep.test.tsx no longer exist"
    - "useOnboardingStore has no role field"
    - "grep for role-gated conditionals across components/ and routes/ returns no results"
    - "The full vitest suite passes and npm run build succeeds"
  artifacts:
    - path: "taskflow/src/components/app/OnboardingWizard.tsx"
      provides: "4-step wizard with no RoleStep"
      contains: "['Welcome', 'Jira', 'GitLab', 'Done']"
    - path: "taskflow/src/routes/settings/SidebarSection.tsx"
      provides: "Sidebar section with no PresetButtons"
  key_links:
    - from: "taskflow/src/components/app/OnboardingWizard.tsx"
      to: "taskflow/src/stores/onboarding.store.ts"
      via: "useOnboardingStore destructure with no role"
      pattern: "const \\{ step, jiraValidated, gitlabValidated \\}"
---

<objective>
Remove every role-related UI surface and dead-code file, strip the `role` field from the in-memory onboarding store, and update all test files that referenced the now-deleted role symbols. After this plan, the codebase contains no role step, no role section, no preset buttons, no `role` field anywhere, and the full test suite plus production build pass.

Purpose: This is the cleanup wave. It depends on Plan 01 having already removed `setRole`/`applyPreset`/`DEV_SIDEBAR_PRESET`/`PM_SIDEBAR_PRESET` from the store and sidebar-items source, so the UI files and tests that consumed those symbols can be removed/updated without compile errors at any intermediate step.

Output: Updated SidebarSection, AppearanceSection, OnboardingWizard, onboarding.store; four deleted files (RoleStep.tsx, RoleStep.test.tsx, RoleSection.tsx, PresetButtons.tsx); five updated test files (onboarding.store.test.ts, tauri-storage.test.ts, SidebarItemsList.test.tsx, Settings.test.tsx, ConnectionsSection.test.tsx); a green full suite and a passing `npm run build`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-roles-removal/66-CONTEXT.md
@.planning/phases/66-roles-removal/66-RESEARCH.md
@.planning/phases/66-roles-removal/66-PATTERNS.md
@.planning/phases/66-roles-removal/66-UI-SPEC.md

<interfaces>
<!-- Current contracts the executor must change/remove. Extracted from codebase. -->

From taskflow/src/components/app/OnboardingWizard.tsx (CURRENT):
- import line 18: `import RoleStep from '@/routes/onboarding/RoleStep';`
- line 23: `const STEP_LABELS = ['Welcome', 'Jira', 'GitLab', 'Role', 'Done'];`
- line 25: `const STEP_COMPONENTS = [WelcomeStep, JiraStep, GitLabStep, RoleStep, DoneStep];`
- line 28: `const { step, jiraValidated, gitlabValidated, role } = useOnboardingStore();`
- line 33: `if (role) completedSteps.push(3);`
- The doc comment (lines 1-13) describes a "5-step wizard" with RoleStep at index 3

From taskflow/src/stores/onboarding.store.ts (CURRENT):
- interface line 22: `role: 'developer' | 'pm' | 'tech-lead' | null;`
- initial state line 40: `role: null,`
- line 25 `set` Omit type — `role` is NOT in the Omit list (no change needed there)

From taskflow/src/routes/settings/SidebarSection.tsx (CURRENT):
- import line 8: `import PresetButtons from './PresetButtons';`
- usage line 21: `<PresetButtons />`
- doc comment lines 4-5 mention "plus preset buttons for quick layout resets"

From taskflow/src/routes/settings/AppearanceSection.tsx (CURRENT):
- import line 14: `import PresetButtons from './PresetButtons';`
- usage line 65: `<PresetButtons />` (inside the "Sidebar Items section" div, after `<SidebarItemsList />`)

Test mock locations (CURRENT):
- Settings.test.tsx: `role:` line 92, `setRole: vi.fn(),` line 113, `applyPreset: vi.fn(),` line 136 (all inside `mockSettingsStore`)
- ConnectionsSection.test.tsx: `role:` line 43, `setRole: vi.fn(),` line 58 (inside `mockSettingsStore`)
- onboarding.store.test.ts: `role: null,` in beforeEach setState; `role: 'developer'` set + `expect(state.role).toBe('developer')` assertion in the "set updates partial state" test
- tauri-storage.test.ts: `role: 'developer'` fixture field + `expect(parsed.state.role).toBe('developer')` assertion in the first test
- SidebarItemsList.test.tsx: `import { DEV_SIDEBAR_PRESET, SIDEBAR_NAV_ITEMS }` line 26; `DEV_SIDEBAR_PRESET.map(...)` in beforeEach
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove PresetButtons from SidebarSection + AppearanceSection, then delete PresetButtons.tsx and RoleSection.tsx</name>
  <files>taskflow/src/routes/settings/SidebarSection.tsx, taskflow/src/routes/settings/AppearanceSection.tsx, taskflow/src/routes/settings/PresetButtons.tsx, taskflow/src/routes/settings/RoleSection.tsx</files>
  <read_first>
    - taskflow/src/routes/settings/SidebarSection.tsx (import line 8, usage line 21, doc comment lines 4-5)
    - taskflow/src/routes/settings/AppearanceSection.tsx (import line 14, usage line 65)
    - taskflow/src/routes/settings/PresetButtons.tsx (the file being deleted — confirm it is only imported by the two sections above)
    - .planning/phases/66-roles-removal/66-UI-SPEC.md (sections "SidebarSection" and "AppearanceSection" — post-deletion contract: heading + helper text + SidebarItemsList only)
    - .planning/phases/66-roles-removal/66-CONTEXT.md (D-05, D-06, D-07)
  </read_first>
  <action>
    Implements ROLES-02 (D-05) and ROLES-03 (D-06, D-07). Edit order to keep TypeScript compiling:
    (1) In `SidebarSection.tsx`: remove the `import PresetButtons from './PresetButtons';` line (line 8) and the `<PresetButtons />` JSX usage (line 21). Keep the `<SidebarItemsList />` and the heading/helper block. Update the file doc comment (lines 4-5) to drop the "plus preset buttons for quick layout resets" clause. The section's `gap-6` container and `data-testid="section-sidebar"` stay unchanged.
    (2) In `AppearanceSection.tsx`: remove the `import PresetButtons from './PresetButtons';` line (line 14) and the `<PresetButtons />` JSX usage (line 65, inside the "Sidebar Items section" div). Keep `<SidebarItemsList />` and the `<label>Sidebar Items</label>` and everything else unchanged.
    (3) Delete the file `taskflow/src/routes/settings/PresetButtons.tsx` entirely (no remaining importers after steps 1-2).
    (4) Delete the file `taskflow/src/routes/settings/RoleSection.tsx` entirely — it is confirmed dead code (not imported in Settings.tsx; D-05). Verify with grep that nothing imports `RoleSection` before deleting.
  </action>
  <verify>
    <automated>cd taskflow && test ! -f src/routes/settings/PresetButtons.tsx && test ! -f src/routes/settings/RoleSection.tsx && grep -rc "PresetButtons\|RoleSection" src/ | grep -v ':0$' || echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/routes/settings/PresetButtons.tsx` does not exist
    - `taskflow/src/routes/settings/RoleSection.tsx` does not exist
    - `grep -rn "PresetButtons" taskflow/src` returns no matches
    - `grep -rn "RoleSection" taskflow/src` returns no matches
    - `SidebarSection.tsx` still contains `<SidebarItemsList />` and `data-testid="section-sidebar"`
    - `AppearanceSection.tsx` still contains `<SidebarItemsList />` and the `Sidebar Items` label
  </acceptance_criteria>
  <done>PresetButtons usage removed from both sections; PresetButtons.tsx and RoleSection.tsx deleted; no dangling references.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse wizard to 4 steps, remove RoleStep, strip role from onboarding store, delete RoleStep files</name>
  <files>taskflow/src/components/app/OnboardingWizard.tsx, taskflow/src/stores/onboarding.store.ts, taskflow/src/routes/onboarding/RoleStep.tsx, taskflow/src/routes/onboarding/RoleStep.test.tsx</files>
  <read_first>
    - taskflow/src/components/app/OnboardingWizard.tsx (import line 18, STEP_LABELS line 23, STEP_COMPONENTS line 25, destructure line 28, completedSteps push line 33, doc comment lines 1-13)
    - taskflow/src/stores/onboarding.store.ts (interface line 22, initial state line 40)
    - taskflow/src/routes/onboarding/RoleStep.tsx (the file being deleted — confirm it is only referenced by OnboardingWizard.tsx)
    - .planning/phases/66-roles-removal/66-UI-SPEC.md (section "OnboardingWizard" — 4-step contract: StepIndicator renders exactly 4 labels, Done shifts to index 3, completedSteps tracks only jiraValidated→1 and gitlabValidated→2)
    - .planning/phases/66-roles-removal/66-CONTEXT.md (D-01, D-02, D-03, D-04)
    - .planning/phases/66-roles-removal/66-RESEARCH.md (Pitfall 6 — RoleStep.test.tsx is todos-only and must be deleted with RoleStep.tsx)
  </read_first>
  <action>
    Implements ROLES-01 (D-01, D-02, D-03, D-04). Edit order to keep TypeScript compiling:
    (1) In `OnboardingWizard.tsx`: remove the `import RoleStep from '@/routes/onboarding/RoleStep';` line (line 18). Change `STEP_LABELS` to `['Welcome', 'Jira', 'GitLab', 'Done']` (remove `'Role'`). Change `STEP_COMPONENTS` to `[WelcomeStep, JiraStep, GitLabStep, DoneStep]` (remove `RoleStep`). Change the store destructure to `const { step, jiraValidated, gitlabValidated } = useOnboardingStore();` (remove `role`). Remove the `if (role) completedSteps.push(3);` line entirely (D-03 — do not add any Done-step completion tracking). Update the doc comment (lines 1-13) to describe a 4-step wizard (Welcome 0, Jira 1, GitLab 2, Done 3) — remove the RoleStep index-3 line and renumber Done to index 3.
    (2) In `onboarding.store.ts`: remove the `role: 'developer' | 'pm' | 'tech-lead' | null;` line from the `OnboardingState` interface (line 22) and the `role: null,` line from the `create()` initial state (line 40). Do NOT change the `set` Omit type on line 25 (role is not in the Omit list). No migration is needed — this store is in-memory only (D-02).
    (3) Delete `taskflow/src/routes/onboarding/RoleStep.tsx` entirely (no remaining importers after step 1).
    (4) Delete `taskflow/src/routes/onboarding/RoleStep.test.tsx` entirely (todos-only orphan per Pitfall 6).
  </action>
  <verify>
    <automated>cd taskflow && test ! -f src/routes/onboarding/RoleStep.tsx && test ! -f src/routes/onboarding/RoleStep.test.tsx && grep -q "\['Welcome', 'Jira', 'GitLab', 'Done'\]" src/components/app/OnboardingWizard.tsx && grep -c "RoleStep" src/components/app/OnboardingWizard.tsx | grep -q '^0$' && grep -c "role" src/stores/onboarding.store.ts | grep -q '^0$' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/routes/onboarding/RoleStep.tsx` does not exist
    - `taskflow/src/routes/onboarding/RoleStep.test.tsx` does not exist
    - `OnboardingWizard.tsx` contains `const STEP_LABELS = ['Welcome', 'Jira', 'GitLab', 'Done'];`
    - `OnboardingWizard.tsx` contains no occurrence of `RoleStep` or `role`
    - `OnboardingWizard.tsx` contains no `completedSteps.push(3)`
    - `onboarding.store.ts` contains no occurrence of `role`
  </acceptance_criteria>
  <done>Wizard renders 4 steps; RoleStep import/usage removed; onboarding store has no role field; RoleStep.tsx and RoleStep.test.tsx deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Update remaining test files, run full suite, run production build, verify ROLES-05 grep</name>
  <files>taskflow/src/stores/onboarding.store.test.ts, taskflow/src/lib/tauri-storage.test.ts, taskflow/src/routes/settings/SidebarItemsList.test.tsx, taskflow/src/routes/settings/Settings.test.tsx, taskflow/src/routes/settings/ConnectionsSection.test.tsx</files>
  <read_first>
    - taskflow/src/stores/onboarding.store.test.ts (beforeEach setState with `role: null`; the "set updates partial state" test setting/asserting `role: 'developer'`)
    - taskflow/src/lib/tauri-storage.test.ts (first test fixture `role: 'developer'` + assertion `expect(parsed.state.role).toBe('developer')`; theme assertion already present)
    - taskflow/src/routes/settings/SidebarItemsList.test.tsx (import line 26 `DEV_SIDEBAR_PRESET`; beforeEach `DEV_SIDEBAR_PRESET.map(...)`; dashboard-checkbox comment line ~62)
    - taskflow/src/routes/settings/Settings.test.tsx (mockSettingsStore: `role:` line 92, `setRole: vi.fn(),` line 113, `applyPreset: vi.fn(),` line 136)
    - taskflow/src/routes/settings/ConnectionsSection.test.tsx (mockSettingsStore: `role:` line 43, `setRole: vi.fn(),` line 58)
    - taskflow/src/components/app/sidebar-items.ts (confirm exported names after Plan 01: getDefaultSidebarItems, SIDEBAR_NAV_ITEMS — no preset constants)
    - .planning/phases/66-roles-removal/66-RESEARCH.md (Pitfalls 3, 4, 5; "Updated SidebarItemsList.test.tsx beforeEach" example)
    - .planning/phases/66-roles-removal/66-PATTERNS.md (test sections for exact line edits)
  </read_first>
  <action>
    Updates supporting tests for ROLES-01/ROLES-03/ROLES-04 and verifies ROLES-05. Make these edits:
    (1) `onboarding.store.test.ts`: remove `role: null,` from the beforeEach setState. In the "set updates partial state" test, remove `role: 'developer',` from the `set({ ... })` call and remove the `expect(state.role).toBe('developer');` assertion. Keep the jiraUrl set + assertion.
    (2) `tauri-storage.test.ts` (Pitfall 4): in the first test's fixture, remove the `role: 'developer'` field from `state` (keep `theme: 'system'` and the other fields) and remove the `expect(parsed.state.role).toBe('developer');` assertion line. The existing `expect(parsed.state.theme).toBe('system');` and `expect(parsed.version).toBe(21);` assertions stay — they preserve the test's intent (unrelated fields survive a patch).
    (3) `SidebarItemsList.test.tsx` (Pitfall 3): change the import on line 26 from `import { DEV_SIDEBAR_PRESET, SIDEBAR_NAV_ITEMS } from '@/components/app/sidebar-items';` to `import { SIDEBAR_NAV_ITEMS, getDefaultSidebarItems } from '@/components/app/sidebar-items';`. In the beforeEach, replace `DEV_SIDEBAR_PRESET.map((item) => ({ ...item }))` with `getDefaultSidebarItems().map((item) => ({ ...item }))`. Update the dashboard-checkbox comment that references `DEV_SIDEBAR_PRESET` to reference `getDefaultSidebarItems()` instead. Do NOT change the drag-handle count assertion — it already targets `SIDEBAR_NAV_ITEMS.length` which is correct.
    (4) `Settings.test.tsx` (Pitfall 5): remove `role: 'developer' as ...` (line 92), `setRole: vi.fn(),` (line 113), and `applyPreset: vi.fn(),` (line 136) from `mockSettingsStore`. No assertions in this file exercise those fields.
    (5) `ConnectionsSection.test.tsx` (Pitfall 5): remove `role: 'developer' as ...` (line 43) and `setRole: vi.fn(),` (line 58) from `mockSettingsStore`.
    Then run the full test suite and production build. Finally run the ROLES-05 verification grep across components and routes — it must return zero role-gated conditionals (D-11 confirmed this is pre-satisfied; the grep is the success-criterion proof).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run 2>&1 | tail -8 && npm run build 2>&1 | tail -5 && (grep -rn "\.role\b" src/components src/routes | grep -v -E "(payload\.role|user\.role|author\.role|aria-role)" || echo "ROLES-05-GREP-CLEAN")</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (full suite green)
    - `npm run build` exits 0 (production build succeeds — required per Phase 59 precedent; tsc alone is insufficient)
    - `onboarding.store.test.ts`, `tauri-storage.test.ts`, `Settings.test.tsx`, `ConnectionsSection.test.tsx` contain no `role:`, `setRole`, or `applyPreset` mock entries
    - `SidebarItemsList.test.tsx` contains no `DEV_SIDEBAR_PRESET`; it imports and uses `getDefaultSidebarItems`
    - `grep -rn "\.role" taskflow/src/components taskflow/src/routes` returns no role-gated conditional rendering (ROLES-05 success criterion)
  </acceptance_criteria>
  <done>All five test files updated; full vitest suite passes; npm run build succeeds; ROLES-05 grep confirms no role conditionals remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan deletes UI files and test fixtures and removes an in-memory store field. No data crosses any trust boundary; the onboarding store is in-memory only with no persistence and no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-02 | Information Disclosure | onboarding.store.ts role field removal | accept | The `role` field was a UI preference held in memory only; it never gated data access or API calls. Removing it discloses nothing. |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. RESEARCH.md confirms zero new dependencies. |

Note (per security_context): This phase performs no authentication, session management, or access-control changes. ROLES-05 confirms (D-11) there are zero role-gated conditionals anywhere in components/routes — the `role` value never controlled access to any feature, route, or data. ASVS L1 categories are Not Applicable to this deletion-only plan.
</threat_model>

<verification>
- `cd taskflow && npx vitest run` — full suite exits 0
- `cd taskflow && npm run build` — exits 0
- File deletions confirmed: `RoleStep.tsx`, `RoleStep.test.tsx`, `RoleSection.tsx`, `PresetButtons.tsx` do not exist
- `grep -rn "PresetButtons\|RoleSection\|RoleStep" taskflow/src` — no matches
- `grep -rn "\.role" taskflow/src/components taskflow/src/routes` — no role-gated conditionals (ROLES-05)
</verification>

<success_criteria>
- Onboarding wizard shows exactly 4 steps: Welcome, Jira, GitLab, Done (ROLES-01)
- Settings exposes no role toggle and no Dev/PM preset buttons in Sidebar or Appearance (ROLES-02, ROLES-03)
- `RoleStep.tsx`, `RoleStep.test.tsx`, `RoleSection.tsx`, `PresetButtons.tsx` deleted
- `useOnboardingStore` has no `role` field (ROLES-01)
- `grep -r` for role-gated conditionals across components/ and routes/ returns no results (ROLES-05)
- Every sidebar nav item is accessible to all users by default (ROLES-06 — completed end-to-end with Plan 01's all-visible default + v22 migration)
- Full vitest suite green and `npm run build` succeeds
</success_criteria>

<output>
Create `.planning/phases/66-roles-removal/66-02-SUMMARY.md` when done.
</output>
