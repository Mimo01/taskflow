---
phase: 63
slug: tempo-saved-filters-test-pass
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-05-21
---

# Phase 63 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react |
| Font | Geist Variable (sans-serif) |

Source: `taskflow/components.json` (style: "base-nova", iconLibrary: "lucide"), `taskflow/src/index.css` (`--font-sans: 'Geist Variable', sans-serif`).

No new shadcn components are required for this phase. All UI is built on top of existing WorklogsPage primitives: inline `<button>`, `<input>`, and `Badge` (already installed).

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, inline × button padding |
| sm | 8px | Gap between pills within a row, between badge and × button |
| md | 16px | Horizontal padding on filter bar rows (`px-6` → 24px; pill `px-3` → 12px) |
| lg | 24px | Filter bar horizontal padding (matches existing `px-6`) |
| xl | 32px | Not used in this phase |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Saved filter pill height: `h-7` (28px) — matches existing preset pill height from WorklogsPage.
- Save-filter inline input height: `h-7` (28px) — same row as pills, consistent height.
- × delete button touch target: minimum 24×24px (`w-6 h-6`) with `p-1` (4px) inner padding.

Source: existing WorklogsPage filter bar uses `gap-2`, `px-6 py-3`, pill `px-3 h-7 text-xs` — this phase adds the saved-filters row directly above using the same spacing.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body / table cell | 12px (text-xs) | 400 (regular) | 1.5 |
| Label / pill text | 12px (text-xs) | 400 (regular) | 1 (single line, vertically centred) |
| Filter row heading | 12px (text-xs) | 600 (semibold) | 1 |
| Page heading | 20px (text-xl) | 600 (semibold) | 1.2 |

Source: WorklogsPage uses `text-xs` throughout the filter bar and table; `text-xl font-semibold` for the `<h1>`. No new type sizes introduced in this phase.

---

## Color

All values are CSS custom properties from `taskflow/src/index.css`.

| Role | CSS Variable | Usage |
|------|-------------|-------|
| Dominant (60%) | `--background` | Page background, filter bar background |
| Secondary (30%) | `--muted` / `--secondary` | Table header (`bg-muted`), tfoot row, inactive pill hover state |
| Accent (10%) | `--accent` / `--accent-foreground` | Active saved filter pill, active preset pill, combobox option hover |
| Destructive | `--destructive` | × delete button on saved filter pill (hover state only) |

Accent reserved for:
1. The currently **active** saved filter pill background (`bg-accent text-accent-foreground font-semibold border border-border`).
2. The currently **active** preset pill background (already established in Phase 62, unchanged).
3. Combobox option hover (`hover:bg-accent`) — existing pattern, unchanged.

Destructive reserved for:
- × delete button on a saved filter pill — `hover:text-destructive transition-colors` on the × icon button. Matches the existing person-filter chip pattern in WorklogsPage line 341.

Source: WorklogsPage active pill uses `bg-accent text-accent-foreground`; × button uses `hover:text-destructive` — this phase mirrors both patterns exactly.

**Focal point:** Visual anchor is the active saved filter pill when one is set; otherwise the preset pills row.

---

## Component Inventory

No new shadcn components are installed. Phase 63 reuses:

| Component | Source | Usage in this phase |
|-----------|--------|---------------------|
| `Badge` (variant="secondary") | `@/components/ui/badge` | Active person chip — already in WorklogsPage |
| `Skeleton` | `@/components/ui/skeleton` | Loading state already in WorklogsPage — no change |
| `EmptyState` | `@/components/ui/empty-state` | Unchanged empty state |
| `ErrorState` | `@/components/ui/error-state` | Unchanged error state |
| Inline `<button>` + `<input>` | HTML | Save-filter button, rename input, saved filter pills |

---

## Interaction Contract

### Saved Filters Row

- **Location:** Directly above the existing preset pills row. Rendered only when `savedFilters.length > 0`. No empty row is shown.
- **Layout:** `flex items-center gap-2 px-6 py-2 border-b border-border bg-background flex-wrap` — one tier above the main filter bar row.
- **Pill shape:** Same border-radius as preset pills (`rounded-md`), height `h-7`, `text-xs`.
- **Inactive pill:** `border border-border rounded-md px-3 h-7 text-xs hover:bg-accent cursor-pointer`.
- **Active pill** (after load): `bg-accent text-accent-foreground font-semibold border border-border rounded-md px-3 h-7 text-xs` — mirrors active preset pill style.
- **× delete button:** Appears on hover of the pill (`group-hover:opacity-100`). Renders as `ml-1 w-6 h-6 p-1 hover:text-destructive transition-colors` inline with the label. No confirmation dialog — inline delete (matches v1.5 Jira saved filter pattern from `saved-filter.store.ts`).
- **Double-click rename:** Double-clicking the pill label replaces it with an `<input type="text">` pre-filled with the filter name, `h-5 text-xs border border-ring rounded px-1 focus:outline-none`. Blur or Enter commits the rename; Escape cancels.

### Save Filter Button + Inline Input

- **Button:** Positioned at the end of the main filter bar row, after the people combobox. Label: `Save filter` (text, no icon). Style: `text-xs text-muted-foreground hover:text-foreground px-2 h-7 rounded-md hover:bg-accent transition-colors` — visually subdued, not a primary CTA.
- **Toggle behaviour:** Clicking "Save filter" shows an inline `<input type="text" placeholder="Filter name" />` + two icon buttons (confirm: `Check`, 16px; cancel: `X`, 16px). The input appears in the same row, after the button.
- **Input style:** `rounded border border-border bg-background px-2 py-1 text-xs text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-ring w-36` — matches the people filter input style in WorklogsPage line 366.
- **Confirm button:** `lucide Check` icon, 16px, `aria-label="Confirm save"`, `hover:text-primary`, commits save if input non-empty.
- **Cancel button:** `lucide X` icon, 16px, `aria-label="Cancel"`, `hover:text-muted-foreground`, dismisses the input without saving.
- **Empty name guard:** If the input is empty on confirm, do nothing (no save, no error copy needed — the field is self-explanatory).

### Loading a Saved Filter

- Clicking a pill applies `preset` and `selectedUsername`/`selectedDisplayName` to WorklogsPage state immediately. No loading indicator on the pill itself. The existing table skeleton handles the re-fetch state.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Save filter" |
| Save input placeholder | "Filter name" |
| Saved filters row aria-label | "Saved filters" |
| Delete button aria-label | "Delete {name} filter" |
| Confirm save button aria-label | "Confirm save" |
| Cancel button aria-label | "Cancel" |
| Rename input aria-label | "Rename filter" |
| Empty state heading (no worklogs) | "No worklogs found" — unchanged from Phase 62 |
| Empty state body (person selected) | "No hours logged for {name} in the selected period." — unchanged |
| Empty state body (no person) | "No hours were logged in the selected date range." — unchanged |
| Error state | Delegated to `<ErrorState>` component — unchanged |
| Destructive action | Delete saved filter: inline × on hover, no confirmation dialog (filter is re-saveable). |

No modal confirmation for delete. The delete is immediate with the × on hover. This matches the v1.5 Jira inline-delete pattern and the existing person-chip × in WorklogsPage.

Source: all non-saved-filter copy pre-populated from WorklogsPage.tsx lines 429–435; save/delete copy from CONTEXT.md D-04, D-05.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | badge, skeleton, progress (pre-existing) | not required |
| third-party | none | not applicable |

No new registry components are added in this phase. `components.json` registries field is `{}`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
