---
phase: 68-startup-wizard-integrations-step
plan: 03
type: execute
wave: 3
depends_on: ["68-02"]
files_modified:
  - taskflow/src/components/app/OnboardingWizard.tsx
autonomous: false
requirements: [WIZ-01]
must_haves:
  truths:
    - "Wizard renders 5 steps in order: Welcome, Jira, GitLab, Integrations, Done"
    - "IntegrationsStep is rendered at step index 3, between GitLabStep and DoneStep"
    - "StepIndicator shows the Integrations label and checkmarks step 3 once integrationsVisited is true"
    - "Production build (npm run build) succeeds and full test suite is green"
  artifacts:
    - path: "taskflow/src/components/app/OnboardingWizard.tsx"
      provides: "5-step wizard shell with IntegrationsStep wired in at index 3"
      contains: "IntegrationsStep"
  key_links:
    - from: "taskflow/src/components/app/OnboardingWizard.tsx"
      to: "taskflow/src/routes/onboarding/IntegrationsStep.tsx"
      via: "default import + STEP_COMPONENTS array entry at index 3"
      pattern: "import IntegrationsStep from"
    - from: "taskflow/src/components/app/OnboardingWizard.tsx"
      to: "useOnboardingStore.integrationsVisited"
      via: "completedSteps.push(3) when integrationsVisited"
      pattern: "integrationsVisited"
---

<objective>
Wire the new `IntegrationsStep` into the wizard shell: add it to `STEP_LABELS` and `STEP_COMPONENTS` at index 3 (between GitLab and Done), update the `completedSteps` derivation to checkmark step 3 when `integrationsVisited` is true, then verify the full production build and test suite are green. This is the final wiring that satisfies WIZ-01 (the Integrations step appears in the wizard between Connections and Done).

Purpose: Without this wiring the step exists but is unreachable. This plan also runs the phase-gate build verification (`npm run build`, per the Phase 59 standing rule that `tsc` alone misses CSS import failures) and ends with a human-verify checkpoint to confirm the wizard flow visually.

Output: Modified `OnboardingWizard.tsx`; green build + full test suite.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-startup-wizard-integrations-step/68-CONTEXT.md
@.planning/phases/68-startup-wizard-integrations-step/68-RESEARCH.md
@.planning/phases/68-startup-wizard-integrations-step/68-PATTERNS.md
@.planning/phases/68-startup-wizard-integrations-step/68-UI-SPEC.md
@.planning/phases/68-startup-wizard-integrations-step/68-02-SUMMARY.md

<interfaces>
<!-- Produced by Plans 01 + 02. -->
From taskflow/src/routes/onboarding/IntegrationsStep.tsx:
- export default function IntegrationsStep(): JSX.Element

From taskflow/src/stores/onboarding.store.ts (post Plan 01):
- useOnboardingStore() => { step: number; jiraValidated: boolean; gitlabValidated: boolean; integrationsVisited: boolean; ... }

Current OnboardingWizard.tsx (to modify):
- STEP_LABELS = ['Welcome', 'Jira', 'GitLab', 'Done']  (line 21)
- STEP_COMPONENTS = [WelcomeStep, JiraStep, GitLabStep, DoneStep]  (line 23)
- completedSteps derivation lines 26-30 (jiraValidated->1, gitlabValidated->2)
- StepIndicator usage line 38 (already accepts dynamic steps[]/currentStep/completedSteps)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire IntegrationsStep into OnboardingWizard at index 3</name>
  <files>taskflow/src/components/app/OnboardingWizard.tsx</files>
  <read_first>
    - taskflow/src/components/app/OnboardingWizard.tsx (current full file — STEP_LABELS line 21, STEP_COMPONENTS line 23, completedSteps lines 26-30, header docblock lines 1-12)
    - taskflow/src/routes/onboarding/IntegrationsStep.tsx (the component being imported — from Plan 02)
    - .planning/phases/68-startup-wizard-integrations-step/68-PATTERNS.md (OnboardingWizard.tsx section — exact updated arrays + completedSteps pattern)
  </read_first>
  <action>
    Add `import IntegrationsStep from '@/routes/onboarding/IntegrationsStep';` to the imports (alongside the other step imports). Change `STEP_LABELS` to `['Welcome', 'Jira', 'GitLab', 'Integrations', 'Done']`. Change `STEP_COMPONENTS` to `[WelcomeStep, JiraStep, GitLabStep, IntegrationsStep, DoneStep]` (IntegrationsStep at index 3, DoneStep moves to index 4). Update the destructure on the `useOnboardingStore()` call to also pull `integrationsVisited`. After the existing `if (gitlabValidated) completedSteps.push(2);`, add `if (integrationsVisited) completedSteps.push(3);`. Update the file header docblock step list (lines 4-8) to reflect 5 steps: 0 Welcome, 1 Jira, 2 GitLab, 3 Integrations, 4 Done, and change "4-step wizard shell" to "5-step wizard shell". The `CurrentStep` fallback (`?? DoneStep`) and StepIndicator usage need no change (StepIndicator already consumes dynamic arrays).
  </action>
  <verify>
    <automated>cd taskflow && npm test -- OnboardingWizard 2>&amp;1 | tail -5; cd taskflow &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -i "OnboardingWizard\|IntegrationsStep" || echo "no type errors in wizard files"</automated>
  </verify>
  <acceptance_criteria>
    - OnboardingWizard.tsx contains `import IntegrationsStep from '@/routes/onboarding/IntegrationsStep'`
    - STEP_LABELS equals `['Welcome', 'Jira', 'GitLab', 'Integrations', 'Done']`
    - STEP_COMPONENTS has IntegrationsStep at index 3 and DoneStep at index 4
    - completedSteps derivation contains `if (integrationsVisited) completedSteps.push(3)`
    - File header docblock describes 5 steps with Integrations at index 3
    - `npx tsc --noEmit` reports no errors in OnboardingWizard.tsx or IntegrationsStep.tsx
  </acceptance_criteria>
  <done>The wizard renders 5 steps with Integrations at index 3; StepIndicator checkmarks step 3 once integrationsVisited is true; no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-gate build + full test suite verification</name>
  <files>taskflow/ (no source edits — verification only)</files>
  <read_first>
    - .planning/STATE.md (Phase 59 standing rule: verify with `npm run build`, not just `tsc` — react-grid-layout CSS imports fail silently in TypeScript)
    - .planning/phases/68-startup-wizard-integrations-step/68-VALIDATION.md (full suite must be green before verify-work; per-task verification map)
  </read_first>
  <action>
    Run the full test suite (`cd taskflow && npm test`) and confirm green — this covers onboarding.store, AioBlock, IntegrationsStep, IntegrationsSection, and OnboardingWizard tests from all three plans. Then run the production build (`cd taskflow && npm run build`) and confirm it exits 0 — this is the Phase 59 standing rule that catches CSS/import failures `tsc` misses. If either fails, fix the failure (do not amend prior commits; create a new fix commit) and re-run both. Do NOT skip the build step in favor of `tsc` alone.
  </action>
  <verify>
    <automated>cd taskflow && npm test 2>&amp;1 | tail -15 &amp;&amp; cd taskflow &amp;&amp; npm run build 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npm test` exits 0 (full suite green)
    - `cd taskflow && npm run build` exits 0 (production build succeeds)
    - No test files for AioBlock, IntegrationsStep, onboarding.store, IntegrationsSection, or OnboardingWizard are red
  </acceptance_criteria>
  <done>Full test suite green and production build succeeds — phase gate satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the live wizard Integrations step flow</name>
  <what-built>
    The onboarding wizard now has a 5th step. Run the app and walk the wizard to the new Integrations step (Welcome → Jira → GitLab → Integrations → Done). The Integrations step shows the AIO toggle (with project picker when enabled) and the Tempo toggle, with Back/Continue navigation gated per D-01..D-04.
  </what-built>
  <how-to-verify>
    1. Start the app: `cd taskflow && npm run tauri dev` (or the project's normal dev command).
    2. If onboarding is already complete, reset it (clear the persisted settings store / use a fresh profile) so the wizard shows.
    3. Advance: Welcome → Jira (connect) → GitLab (connect) → you should land on a step labeled "Integrations" in the StepIndicator, between GitLab and Done.
    4. On the Integrations step, confirm: heading "Set up Integrations"; an "AIO Test Management" toggle; a "Tempo Timesheets" toggle; Back and Continue buttons.
    5. Toggle AIO ON with no project selected → Continue should be DISABLED. Pick a project → Continue ENABLED. Toggle AIO OFF → Continue ENABLED regardless.
    6. Spacing/weight check: the AIO toggle label should be regular weight (font-normal), and the picker sub-elements should use 8px gaps (gap-2), per the UI-SPEC.
    7. Toggle Tempo ON, click Continue to reach Done, then click Back twice to return — confirm AIO/Tempo selections persisted (no reset). Confirm the Integrations step shows a checkmark in the StepIndicator after you've passed it.
    8. Confirm completing the wizard wrote aioEnabled/tempoEnabled/selectedAioProjectKey into Settings (open Settings → Integrations and verify the same values).
  </how-to-verify>
  <resume-signal>Type "approved" if the wizard flow, gating, persistence, and spacing are correct, or describe the issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only wires an existing step into the wizard shell array and runs verification. No data crosses any new boundary. |

This phase introduces NO new attack surface. OnboardingWizard wiring is a static array + a boolean push into completedSteps; the build/verification task touches no source. Confirmed by RESEARCH.md Security Domain (all ASVS categories N/A).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-05 | Tampering | STEP_COMPONENTS array indexing | accept | Static array literal; `CurrentStep = STEP_COMPONENTS[step] ?? DoneStep` already guards out-of-range indices. No external input controls the index beyond the clamped store step. |
| T-68-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: zero new packages). No legitimacy checkpoint required. |
</threat_model>

<verification>
- OnboardingWizard renders IntegrationsStep at index 3; STEP_LABELS includes "Integrations" (WIZ-01)
- completedSteps checkmarks step 3 when integrationsVisited
- `cd taskflow && npm test` full suite green
- `cd taskflow && npm run build` succeeds (Phase 59 standing rule)
- Human verification confirms the wizard flow, Continue gating, persistence, and UI-SPEC spacing
</verification>

<success_criteria>
- WIZ-01 satisfied: the Integrations step appears in the wizard between Connections (GitLab) and Done
- Full test suite green and production build succeeds (phase gate)
- Human approval of the live wizard flow
</success_criteria>

<output>
Create `.planning/phases/68-startup-wizard-integrations-step/68-03-SUMMARY.md` when done
</output>
