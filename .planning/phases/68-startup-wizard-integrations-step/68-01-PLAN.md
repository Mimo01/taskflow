---
phase: 68-startup-wizard-integrations-step
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/stores/onboarding.store.ts
  - taskflow/src/stores/onboarding.store.test.ts
  - taskflow/src/components/integrations/AioBlock.tsx
  - taskflow/src/components/integrations/AioBlock.test.tsx
  - taskflow/src/routes/settings/IntegrationsSection.tsx
autonomous: true
requirements: [WIZ-01, WIZ-02]
must_haves:
  truths:
    - "onboarding store goNext advances through step 4 (Integrations) and clamps there"
    - "onboarding store tracks integrationsVisited (false by default, settable via set)"
    - "AioBlock renders the AIO toggle and all four picker states (loading/error/empty/loaded) when enabled"
    - "AioBlock reads and writes aioEnabled and selectedAioProjectKey directly from useSettingsStore"
    - "Settings -> Integrations still renders the full AIO picker (now via the extracted AioBlock)"
  artifacts:
    - path: "taskflow/src/components/integrations/AioBlock.tsx"
      provides: "Self-contained extracted AIO toggle + project picker component"
      exports: ["default"]
      contains: "export default function AioBlock"
    - path: "taskflow/src/components/integrations/AioBlock.test.tsx"
      provides: "Unit tests for AioBlock four picker states + stale-key + selection wiring"
    - path: "taskflow/src/stores/onboarding.store.ts"
      provides: "Step limit bumped to 4 + integrationsVisited flag"
      contains: "integrationsVisited"
    - path: "taskflow/src/routes/settings/IntegrationsSection.tsx"
      provides: "Settings Integrations section now importing AioBlock instead of inline AIO block"
      contains: "AioBlock"
  key_links:
    - from: "taskflow/src/routes/settings/IntegrationsSection.tsx"
      to: "taskflow/src/components/integrations/AioBlock.tsx"
      via: "default import + mount"
      pattern: "import AioBlock from"
    - from: "taskflow/src/components/integrations/AioBlock.tsx"
      to: "useSettingsStore"
      via: "fine-grained selectors for aioEnabled/selectedAioProjectKey"
      pattern: "useSettingsStore\\(\\(s\\) => s\\.(aioEnabled|selectedAioProjectKey)"
---

<objective>
Build the foundation layer for the wizard Integrations step: bump the onboarding store step limit, add the `integrationsVisited` flag, extract the AIO toggle + picker block from `IntegrationsSection.tsx` into a self-contained shared `AioBlock` component (applying the UI-SPEC `gap-2`/`font-normal` corrections), and update `IntegrationsSection` to consume the extracted component.

Purpose: This is the shared substrate that both Settings → Integrations and the new wizard `IntegrationsStep` (Plan 02) mount. Extracting once prevents the AIO picker logic from diverging across two surfaces (D-05). Bumping the store limit unblocks navigation to the new step (WIZ-01).

Output: Modified `onboarding.store.ts` + `onboarding.store.test.ts`, new `AioBlock.tsx` + `AioBlock.test.tsx`, modified `IntegrationsSection.tsx` (net LOC decreases).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-startup-wizard-integrations-step/68-CONTEXT.md
@.planning/phases/68-startup-wizard-integrations-step/68-RESEARCH.md
@.planning/phases/68-startup-wizard-integrations-step/68-PATTERNS.md
@.planning/phases/68-startup-wizard-integrations-step/68-UI-SPEC.md
@.planning/phases/68-startup-wizard-integrations-step/68-VALIDATION.md

<interfaces>
<!-- useSettingsStore fields the AioBlock binds to (confirmed in settings.store.ts lines 97-104, 217-222). -->
From taskflow/src/stores/settings.store.ts:
- aioEnabled: boolean; setAioEnabled: (v: boolean) => void
- tempoEnabled: boolean; setTempoEnabled: (v: boolean) => void
- selectedAioProjectKey: string | null; setSelectedAioProjectKey: (key: string | null) => void

From taskflow/src/services/aio:
- fetchAioProjects(jiraBaseUrl: string, token: string): Promise<{ id: number; projectKey: string; name: string }[]>

From taskflow/src/services/stronghold:
- readSecret(key: string): Promise<string>  // key 'jira-pat'

From taskflow/src/stores/auth.store:
- useAuthStore() => { jiraBaseUrl: string; ... }

From taskflow/src/stores/onboarding.store.ts (current):
- OnboardingState has jiraValidated, gitlabValidated, set(partial), goNext(), goBack()
- goNext currently clamps at Math.min(3, ...) — line 42
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump onboarding store step limit to 4 and add integrationsVisited flag</name>
  <files>taskflow/src/stores/onboarding.store.ts, taskflow/src/stores/onboarding.store.test.ts</files>
  <read_first>
    - taskflow/src/stores/onboarding.store.ts (current interface + create() — goNext at line 42 uses Math.min(3, ...))
    - taskflow/src/stores/onboarding.store.test.ts (current beforeEach reset state lines 7-21, existing goNext/goBack/set tests)
  </read_first>
  <behavior>
    - Test: goNext called 5 times from step 0 clamps at step 4 (was 3)
    - Test: integrationsVisited defaults to false
    - Test: set({ integrationsVisited: true }) updates integrationsVisited to true
    - Existing tests (goNext increments, goBack decrements, goBack clamps at 0, set updates partial) still pass
  </behavior>
  <action>
    First update `onboarding.store.test.ts` (RED): add `integrationsVisited: false` to the `beforeEach` reset state object (alongside `jiraValidated: false`, `gitlabValidated: false`). Add two new test cases per the behavior block: "goNext clamps at step 4" (call goNext 5 times, assert step === 4) and "set updates integrationsVisited" (call set({ integrationsVisited: true }), assert integrationsVisited === true). Then update `onboarding.store.ts` (GREEN): add `integrationsVisited: boolean;` to the `OnboardingState` interface immediately after `gitlabValidated: boolean;`; add `integrationsVisited: false,` to the `create()` initial state immediately after `gitlabValidated: false,`; change `goNext: () => set({ step: Math.min(3, get().step + 1) })` to use `Math.min(4, ...)`. Do NOT change `goBack` (its clamp at 0 stays).
  </action>
  <verify>
    <automated>cd taskflow && npm test -- onboarding.store</automated>
  </verify>
  <acceptance_criteria>
    - onboarding.store.ts contains `integrationsVisited: boolean;` in the OnboardingState interface
    - onboarding.store.ts contains `integrationsVisited: false,` in create() initial state
    - onboarding.store.ts goNext uses `Math.min(4,` (not `Math.min(3,`)
    - onboarding.store.test.ts beforeEach reset object contains `integrationsVisited: false`
    - onboarding.store.test.ts contains a test asserting goNext clamps at step 4
    - `cd taskflow && npm test -- onboarding.store` exits 0
  </acceptance_criteria>
  <done>goNext advances through and clamps at step 4; integrationsVisited flag exists, defaults false, and is settable; all onboarding.store tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract AioBlock component with UI-SPEC corrections + tests</name>
  <files>taskflow/src/components/integrations/AioBlock.tsx, taskflow/src/components/integrations/AioBlock.test.tsx</files>
  <read_first>
    - taskflow/src/routes/settings/IntegrationsSection.tsx (extraction source — AIO block is lines 11-146; note line 70 uses font-medium and lines 84/87/92 use gap-1.5, all WRONG per UI-SPEC)
    - taskflow/src/routes/settings/IntegrationsSection.test.tsx (mock template — copy the Select mock lines 48-103 verbatim, the renderWithClient helper lines 105-111, and the AIO picker test cases lines 156-326)
    - .planning/phases/68-startup-wizard-integrations-step/68-PATTERNS.md (CSS Correction Reference table near end + AioBlock pattern section)
    - .planning/phases/68-startup-wizard-integrations-step/68-UI-SPEC.md (Interaction States + Copywriting Contract — verbatim copy strings)
  </read_first>
  <behavior>
    - Test: hides picker (no "AIO Project" label) when aioEnabled=false
    - Test: shows loading row "Loading projects…" while query pending (aioEnabled=true)
    - Test: shows error row with role="alert" + Retry button when query rejects
    - Test: shows disabled Select with "No AIO projects available" when projects=[]
    - Test: renders project list sorted alphabetically case-insensitive when resolved
    - Test: calls setSelectedAioProjectKey with projectKey on option selection
    - Test: shows stale-key destructive warning when selectedAioProjectKey not in fetched projects
    - Test: no stale-key warning when selectedAioProjectKey resolves to a known project
  </behavior>
  <action>
    First create `AioBlock.test.tsx` (RED): adapt from `IntegrationsSection.test.tsx`. Copy the `vi.mock('@/components/ui/select', ...)` block (lines 48-103), the `makeClient`/`renderWithClient` helpers (lines 105-111), and the auth/stronghold/aio service mocks verbatim. Mock the settings store with ONLY the four AIO fields (aioEnabled, setAioEnabled, selectedAioProjectKey, setSelectedAioProjectKey) — Tempo fields are NOT in AioBlock (D-06). Import `AioBlock from './AioBlock'`. Port the "AIO project picker" describe-block test cases (lines 156-325) plus the toggle render/check tests (lines 130-153), rendering `<AioBlock />` instead of `<IntegrationsSection />`. Then create `AioBlock.tsx` (GREEN): `export default function AioBlock()` with no props. Use the imports listed in PATTERNS.md (useQuery, Loader2, XCircle, useEffect/useMemo/useState, Label, Select family, fetchAioProjects, readSecret, useAuthStore, useSettingsStore). Use fine-grained selectors for aioEnabled/setAioEnabled/selectedAioProjectKey/setSelectedAioProjectKey. Replicate the token-fetch useEffect, the useQuery with queryKey `['aio', jiraBaseUrl, 'projects']`, the sortedProjects useMemo (localeCompare with sensitivity 'base'), the selectedProject lookup, and the selectedKeyIsStale derivation exactly as in the source. Render the AIO subheading h3, the toggle label row, and the conditional picker covering all four states + stale warning + helper text. APPLY THE UI-SPEC CORRECTIONS: the toggle label `<p>` MUST be `text-sm font-normal` (NOT `font-medium`); the picker container, loading row, and error row MUST use `gap-2` (NOT `gap-1.5`). All copy strings are verbatim from the UI-SPEC Copywriting Contract.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- AioBlock</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/components/integrations/AioBlock.tsx contains `export default function AioBlock`
    - AioBlock.tsx contains the toggle label as `text-sm font-normal` and contains NO occurrence of `font-medium`
    - AioBlock.tsx contains NO occurrence of `gap-1.5` (grep -v '^//' on the file returns zero matches for `gap-1.5`)
    - AioBlock.tsx contains queryKey `['aio', jiraBaseUrl, 'projects']`
    - AioBlock.tsx binds aioEnabled and selectedAioProjectKey via `useSettingsStore((s) => ...)` selectors
    - AioBlock.test.tsx exists and covers all eight behaviors in the behavior block
    - `cd taskflow && npm test -- AioBlock` exits 0
  </acceptance_criteria>
  <done>Self-contained AioBlock renders the AIO toggle and all four picker states with UI-SPEC-correct spacing/weight; binds directly to useSettingsStore; all AioBlock tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Update IntegrationsSection to import AioBlock</name>
  <files>taskflow/src/routes/settings/IntegrationsSection.tsx</files>
  <read_first>
    - taskflow/src/routes/settings/IntegrationsSection.tsx (current full file — AIO block at lines 64-146, Tempo block at lines 148-168, store reads at lines 14-19, query logic at lines 21-59)
    - taskflow/src/components/integrations/AioBlock.tsx (the extracted component this file now mounts — created in Task 2)
    - taskflow/src/routes/settings/IntegrationsSection.test.tsx (existing tests that must still pass — the AIO picker assertions move to AioBlock.test.tsx; the section's own tests assert the section heading + Tempo toggle still work)
  </read_first>
  <action>
    Remove the inline AIO block from `IntegrationsSection.tsx`: delete the AIO store selectors (aioEnabled, setAioEnabled, selectedAioProjectKey, setSelectedAioProjectKey at lines 14-17), the jiraBaseUrl/token/useEffect/useQuery/sortedProjects/selectedProject/selectedKeyIsStale logic (lines 21-59), and the entire AIO `<div className="flex flex-col gap-4">...</div>` JSX block (lines 64-146). Replace the AIO JSX block with a single `<AioBlock />` mount. Add `import AioBlock from '@/components/integrations/AioBlock';` to the imports. Remove now-unused imports: `useQuery`, `Loader2`, `XCircle`, `useEffect`, `useMemo`, `useState`, `Label`, `Select`/`SelectContent`/`SelectItem`/`SelectTrigger`, `fetchAioProjects`, `readSecret`, `useAuthStore`. KEEP the `useSettingsStore` import and the Tempo selectors (tempoEnabled, setTempoEnabled) and the entire Tempo block (lines 148-168) and the outer `<div data-testid="section-integrations" className="flex flex-col gap-8">` wrapper with its `<h2>Integrations</h2>` heading. The "Tempo Timesheets" toggle label keeps `font-semibold` (it was never part of the UI-SPEC AIO correction). Net file LOC must decrease.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- IntegrationsSection &amp;&amp; cd taskflow &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -i IntegrationsSection || true</automated>
  </verify>
  <acceptance_criteria>
    - IntegrationsSection.tsx contains `import AioBlock from '@/components/integrations/AioBlock'`
    - IntegrationsSection.tsx contains `<AioBlock />`
    - IntegrationsSection.tsx no longer contains `fetchAioProjects`, `useQuery`, or the inline AIO `<Select` picker JSX
    - IntegrationsSection.tsx still contains `data-testid="section-integrations"` and the Tempo toggle (`Enable Tempo Timesheets`)
    - `cd taskflow && npm test -- IntegrationsSection` exits 0
    - IntegrationsSection.tsx has fewer lines than before extraction
  </acceptance_criteria>
  <done>Settings → Integrations renders the AIO picker through the extracted AioBlock; the Tempo block and section wrapper are unchanged; IntegrationsSection tests green; net LOC decreased.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Jira/AIO API | AioBlock fetches the AIO project list using a PAT read from Stronghold. This boundary already exists and is unchanged by extraction. |

This phase introduces NO new attack surface. The work is component extraction + store wiring. There is no user-entered free text on this step (only a toggle and a project picker bound to a fetched list), no new auth flow, and the Jira PAT continues to be read via the established `readSecret('jira-pat')` Stronghold IPC (never stored in Zustand). Confirmed by RESEARCH.md Security Domain section (all ASVS categories N/A).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Information Disclosure | Jira PAT read in AioBlock | accept | PAT read via existing `readSecret('jira-pat')` Stronghold abstraction; never placed in Zustand or onboarding store; identical to the pre-extraction IntegrationsSection behavior. No new exposure. |
| T-68-02 | Tampering | AIO project list response | accept | Project list rendered as text in a Select; values are projectKey/name only, no HTML injection (React escapes). Low value, read-only. |
| T-68-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: zero new packages). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd taskflow && npm test -- onboarding.store` green
- `cd taskflow && npm test -- AioBlock` green
- `cd taskflow && npm test -- IntegrationsSection` green
- AioBlock.tsx contains no `font-medium` and no `gap-1.5` (UI-SPEC corrections applied)
- IntegrationsSection.tsx imports and mounts AioBlock (no inline AIO picker remains)
</verification>

<success_criteria>
- Onboarding store advances through step 4 and tracks integrationsVisited (WIZ-01 substrate)
- AioBlock is self-contained, binds to useSettingsStore, renders all four picker states with corrected spacing/weight (WIZ-02 substrate)
- Settings → Integrations consumes AioBlock with net LOC reduction (D-05, no divergence)
- All three test files green
</success_criteria>

<output>
Create `.planning/phases/68-startup-wizard-integrations-step/68-01-SUMMARY.md` when done
</output>
