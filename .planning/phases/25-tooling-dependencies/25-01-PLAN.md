---
phase: 25-tooling-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/biome.json
  - taskflow/package.json
  - taskflow/src/**/*.ts
  - taskflow/src/**/*.tsx
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02

must_haves:
  truths:
    - "Running `npx biome check ./src` in taskflow/ exits 0 with zero errors"
    - "Running `npm run check` in taskflow/ exits 0 (biome check + tsc --noEmit)"
    - "Running `npm run lint` runs biome lint on ./src"
    - "Running `npm run format` reformats source files in place"
    - "Running `npm run format:check` checks format without modifying files"
    - "All 162 source files have consistent single-quote, 2-space indent, semicolons style"
  artifacts:
    - path: "taskflow/biome.json"
      provides: "Biome linter/formatter configuration"
      contains: "biomejs.dev/schemas"
    - path: "taskflow/package.json"
      provides: "npm scripts for lint, format, format:check, check, fix"
      contains: "biome check"
  key_links:
    - from: "taskflow/package.json scripts.check"
      to: "biome check + tsc --noEmit"
      via: "npm run check"
      pattern: "biome check.*&&.*tsc --noEmit"
    - from: "taskflow/biome.json"
      to: "taskflow/src/**"
      via: "biome check --write ./src"
      pattern: "organizeImports"
---

<objective>
Install and configure Biome as the project's linter and formatter, add CI-ready npm scripts, and auto-fix the entire codebase to pass all Biome checks with zero errors.

Purpose: Establish consistent automated code quality tooling for the codebase, replacing the need for ESLint/Prettier.
Output: biome.json config, new npm scripts (lint, format, format:check, check, fix), all source files passing biome check.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-tooling-dependencies/25-CONTEXT.md
@.planning/phases/25-tooling-dependencies/25-RESEARCH.md

<interfaces>
<!-- Current package.json scripts section (will be extended) -->
From taskflow/package.json:
```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  }
}
```

From taskflow/tsconfig.json:
```json
{
  "compilerOptions": {
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "baseUrl": ".",
    "paths": { "@/*": ["./src/*"] }
  },
  "include": ["src"]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Biome, create config, add npm scripts</name>
  <files>taskflow/biome.json, taskflow/package.json</files>
  <read_first>
    - taskflow/package.json (current scripts and dependencies)
    - taskflow/tsconfig.json (understand existing TS config)
    - taskflow/vite.config.ts (understand build setup)
  </read_first>
  <action>
1. Install Biome:
   ```bash
   cd taskflow && npm install --save-dev @biomejs/biome
   ```

2. Create `taskflow/biome.json` with this exact configuration:
```json
{
  "$schema": "https://biomejs.dev/schemas/2.4.8/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true,
    "defaultBranch": "main"
  },
  "files": {
    "ignoreUnknown": true
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100,
    "lineEnding": "lf"
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "single",
      "jsxQuoteStyle": "double",
      "semicolons": "always",
      "trailingCommas": "all",
      "arrowParentheses": "always"
    },
    "globals": []
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true,
      "suspicious": {
        "noExplicitAny": "off",
        "noArrayIndexKey": "error"
      },
      "correctness": {
        "useExhaustiveDependencies": "warn"
      },
      "a11y": {
        "recommended": true
      },
      "style": {
        "useNamingConvention": {
          "level": "warn",
          "options": {
            "strictCase": false,
            "conventions": [
              { "selector": { "kind": "variable" }, "formats": ["camelCase", "PascalCase", "CONSTANT_CASE"] },
              { "selector": { "kind": "function" }, "formats": ["camelCase", "PascalCase"] },
              { "selector": { "kind": "typeLike" }, "formats": ["PascalCase"] }
            ]
          }
        }
      }
    }
  },
  "assist": {
    "actions": {
      "source": {
        "organizeImports": {
          "level": "error"
        }
      }
    }
  },
  "overrides": [
    {
      "includes": ["**/*.test.*", "**/*.spec.*", "**/test/**"],
      "linter": {
        "rules": {
          "suspicious": {
            "noExplicitAny": "off"
          }
        }
      }
    }
  ]
}
```

Key config decisions:
- `noExplicitAny: "off"` per user decision (Phase 27 will enable)
- `a11y.recommended: true` per user decision (surfaces issues for Phase 28)
- `useNamingConvention: "warn"` with camelCase/PascalCase/CONSTANT_CASE for variables, camelCase/PascalCase for functions, PascalCase for types
- `organizeImports: "error"` in assist actions for auto-sorting imports
- `lineWidth: 100` to avoid excessive line wrapping in JSX-heavy code
- Single quotes, 2-space indent, semicolons to match existing codebase conventions (93% single quotes, extensive semicolons)
- `vcs.useIgnoreFile: true` excludes node_modules, dist, src-tauri/target via .gitignore

3. Add these npm scripts to package.json (preserve existing scripts, add after "tauri"):
```json
{
  "lint": "biome lint ./src",
  "format": "biome format --write ./src",
  "format:check": "biome format ./src",
  "check": "biome check ./src && tsc --noEmit",
  "fix": "biome check --write ./src"
}
```

The final scripts section should be:
```json
"scripts": {
  "dev": "vite",
  "build": "tsc && vite build",
  "preview": "vite preview",
  "tauri": "tauri",
  "lint": "biome lint ./src",
  "format": "biome format --write ./src",
  "format:check": "biome format ./src",
  "check": "biome check ./src && tsc --noEmit",
  "fix": "biome check --write ./src"
}
```
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && test -f biome.json && grep -q "biomejs.dev/schemas" biome.json && grep -q '"check"' package.json && grep -q '"lint"' package.json && grep -q '"format"' package.json && grep -q '"format:check"' package.json && grep -q '"fix"' package.json && echo "PASS: Biome config and scripts exist"</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/biome.json exists and contains `"$schema": "https://biomejs.dev/schemas/2.4.8/schema.json"`
    - taskflow/biome.json contains `"noExplicitAny": "off"`
    - taskflow/biome.json contains `"organizeImports"`
    - taskflow/biome.json contains `"quoteStyle": "single"`
    - taskflow/biome.json contains `"useNamingConvention"`
    - taskflow/biome.json contains `"a11y"` with `"recommended": true`
    - taskflow/package.json contains `"lint": "biome lint ./src"`
    - taskflow/package.json contains `"format": "biome format --write ./src"`
    - taskflow/package.json contains `"format:check": "biome format ./src"`
    - taskflow/package.json contains `"check": "biome check ./src && tsc --noEmit"`
    - taskflow/package.json contains `"fix": "biome check --write ./src"`
    - taskflow/package.json devDependencies contains `@biomejs/biome`
  </acceptance_criteria>
  <done>Biome installed as devDependency, biome.json configured with all required rules (a11y, naming conventions, import sorting, noExplicitAny suppressed), and all 5 npm scripts (lint, format, format:check, check, fix) added to package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-fix entire codebase and resolve remaining Biome violations</name>
  <files>taskflow/src/**/*.ts, taskflow/src/**/*.tsx</files>
  <read_first>
    - taskflow/biome.json (just created in Task 1)
    - taskflow/package.json (verify scripts exist)
  </read_first>
  <action>
1. Run the auto-fix command to format and lint-fix the entire codebase:
   ```bash
   cd taskflow && npx biome check --write ./src
   ```
   This will: reformat all files (single quotes, 2-space indent, semicolons, line width 100), sort and organize imports, remove unused imports, and auto-fix all auto-fixable lint violations.

2. Check what remains after auto-fix:
   ```bash
   cd taskflow && npx biome check ./src 2>&1
   ```

3. For any remaining non-auto-fixable violations:
   - **Naming convention warnings** from external API field names (e.g., `customfield_10016`, Jira API property names): Add `// biome-ignore lint/style/useNamingConvention: external API field name` comments on the specific lines, OR if widespread in service files, add an override in biome.json for service file patterns:
     ```json
     {
       "includes": ["**/services/**"],
       "linter": {
         "rules": {
           "style": {
             "useNamingConvention": "off"
           }
         }
       }
     }
     ```
   - **Accessibility warnings** (a11y rules): Leave as warnings -- these are intentionally surfaced for Phase 28. Do NOT suppress them.
   - **useExhaustiveDependencies warnings**: Leave as warnings -- these are informational. Fix only if trivially correct (add missing dep to array). Do NOT suppress.
   - **Other lint errors that are NOT auto-fixable and NOT in Phase 27 scope (type-related)**: Fix manually. These are real bugs or code quality issues.

4. Verify clean state:
   ```bash
   cd taskflow && npx biome check ./src
   ```
   Must exit 0 with zero errors. Warnings are acceptable (a11y, naming conventions, exhaustive deps).

5. Verify TypeScript still compiles:
   ```bash
   cd taskflow && npx tsc --noEmit
   ```

6. Run full check script:
   ```bash
   cd taskflow && npm run check
   ```
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx biome check ./src && npm run check && echo "PASS: All Biome checks pass, check script passes"</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npx biome check ./src` exits with code 0
    - `cd taskflow && npm run check` exits with code 0 (biome check + tsc --noEmit both pass)
    - `cd taskflow && npm run lint` exits with code 0
    - `cd taskflow && npm run format:check` exits with code 0
    - No `// biome-ignore` comments suppressing a11y rules (those must stay as warnings, not suppressed)
    - No `// biome-ignore` comments suppressing noExplicitAny (rule is already off globally)
  </acceptance_criteria>
  <done>`npx biome check ./src` exits 0 with zero errors. `npm run check` (biome + tsc) exits 0. All source files have consistent formatting. Import sorting applied across all files.</done>
</task>

</tasks>

<verification>
1. `cd taskflow && npx biome check ./src` -- exits 0, no errors
2. `cd taskflow && npm run check` -- exits 0 (biome check + tsc --noEmit)
3. `cd taskflow && npm run lint` -- exits 0
4. `cd taskflow && npm run format:check` -- exits 0
5. `cd taskflow && npx vitest run` -- existing tests still pass (no regressions from formatting changes)
</verification>

<success_criteria>
- biome.json exists at taskflow/biome.json with full linter + formatter config
- 5 new npm scripts in package.json: lint, format, format:check, check, fix
- `npm run check` is a CI-ready gate combining biome check and tsc --noEmit
- All source files pass biome check with zero errors
- Existing test suite still passes
</success_criteria>

<output>
After completion, create `.planning/phases/25-tooling-dependencies/25-01-SUMMARY.md`
</output>
