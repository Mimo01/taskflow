---
phase: 26-test-regression-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/test/setup.ts
  - taskflow/package.json
  - taskflow/src/services/jira.ts
autonomous: true
requirements: [TEST-04]

must_haves:
  truths:
    - "LazyStore unhandled rejections no longer appear in test output"
    - "npm test script exists and runs the test suite from taskflow directory"
    - "tsc --noEmit no longer reports unused variable in jira.ts"
  artifacts:
    - path: "taskflow/src/test/setup.ts"
      provides: "Global LazyStore mock preventing Tauri IPC errors in jsdom"
      contains: "vi.mock('@tauri-apps/plugin-store'"
    - path: "taskflow/package.json"
      provides: "npm test script"
      contains: '"test": "vitest run"'
    - path: "taskflow/src/services/jira.ts"
      provides: "Cleaned up unused variable"
  key_links:
    - from: "taskflow/src/test/setup.ts"
      to: "taskflow/vitest.config.ts"
      via: "setupFiles config"
      pattern: "setupFiles.*setup\\.ts"
---

<objective>
Add global LazyStore mock to test setup, add npm test script, and fix the jira.ts unused variable.

Purpose: Eliminate all 47 LazyStore unhandled rejection errors that pollute test output and affect 8 test files. Provide the `npm test` script required by success criterion #4. Remove the 1 production TS error (unused variable) that is trivial and unblocks clean `tsc --noEmit`.
Output: Clean test infrastructure -- no unhandled rejections, runnable via `npm test`, one fewer TS error.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-test-regression-fixes/26-CONTEXT.md
@.planning/phases/26-test-regression-fixes/26-RESEARCH.md

<interfaces>
<!-- From taskflow/vitest.config.ts: setup file is registered as -->
setupFiles: ['./src/test/setup.ts']

<!-- From taskflow/src/test/setup.ts: current content -->
import '@testing-library/jest-dom';
import { randomFillSync } from 'node:crypto';
Object.defineProperty(window, 'crypto', { ... });

<!-- From taskflow/package.json scripts (current): -->
"dev": "vite"
"build": "tsc && vite build"
"preview": "vite preview"
"tauri": "tauri"
"lint": "biome lint ./src"
<!-- No "test" script exists -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add global LazyStore mock to setup.ts and npm test script</name>
  <files>taskflow/src/test/setup.ts, taskflow/package.json</files>
  <read_first>
    taskflow/src/test/setup.ts
    taskflow/package.json
    taskflow/vitest.config.ts
  </read_first>
  <action>
**1. Update `taskflow/src/test/setup.ts`** -- append global LazyStore mock AFTER the existing crypto polyfill:

```typescript
// Mock @tauri-apps/plugin-store globally -- LazyStore needs Tauri IPC which
// is unavailable in jsdom. Provide a synchronous in-memory implementation.
vi.mock('@tauri-apps/plugin-store', () => {
  class LazyStore {
    private data = new Map<string, unknown>();
    async get<T>(key: string): Promise<T | undefined> {
      return this.data.get(key) as T | undefined;
    }
    async set(key: string, value: unknown): Promise<void> {
      this.data.set(key, value);
    }
    async delete(key: string): Promise<void> {
      this.data.delete(key);
    }
    async save(): Promise<void> {}
    async load(): Promise<void> {}
  }
  return { LazyStore };
});
```

Keep the existing `@testing-library/jest-dom` import and crypto polyfill intact. The `vi` global is available because `vitest.config.ts` has `globals: true`.

**2. Update `taskflow/package.json`** -- add `"test": "vitest run"` to the scripts section. Place it after the `"lint"` script. Also add `"test:watch": "vitest"` for convenience (but this is optional -- the key requirement is `"test": "vitest run"`).

Also add `"check": "biome check ./src"` if it doesn't already exist (Phase 25 may have added it -- check first). The required addition is only `"test": "vitest run"`.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && grep -c "vi.mock.*plugin-store" src/test/setup.ts && grep -c '"test"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/test/setup.ts` contains `vi.mock('@tauri-apps/plugin-store'`
    - `taskflow/src/test/setup.ts` contains `class LazyStore`
    - `taskflow/src/test/setup.ts` contains `private data = new Map`
    - `taskflow/src/test/setup.ts` still contains `@testing-library/jest-dom`
    - `taskflow/src/test/setup.ts` still contains `randomFillSync`
    - `taskflow/package.json` contains `"test": "vitest run"`
    - Running `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run 2>&1 | grep -c "Unhandled"` returns 0
  </acceptance_criteria>
  <done>Global LazyStore mock is in setup.ts, npm test script exists, and running the suite produces zero "Unhandled Rejection" errors</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused _sprintIdsWithIssues variable from jira.ts</name>
  <files>taskflow/src/services/jira.ts</files>
  <read_first>
    taskflow/src/services/jira.ts
  </read_first>
  <action>
Find the `_sprintIdsWithIssues` variable declaration in `taskflow/src/services/jira.ts` and remove it. This is a declared-but-never-read variable (TS6133). It may be a `const` or `let` assignment -- remove the entire declaration line (or block if multi-line).

Do NOT change any other code in jira.ts. This is the only production code fix in this phase.

Verify with: `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | grep "_sprintIdsWithIssues"` -- should return nothing.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | grep -c "_sprintIdsWithIssues"</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/services/jira.ts` does NOT contain `_sprintIdsWithIssues`
    - `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | grep "_sprintIdsWithIssues"` returns empty (0 matches)
    - No other changes to jira.ts beyond removing the unused variable
  </acceptance_criteria>
  <done>The TS6133 unused variable error for _sprintIdsWithIssues is eliminated</done>
</task>

</tasks>

<verification>
Run from taskflow directory:
1. `npm test 2>&1 | grep "Unhandled"` -- zero matches (LazyStore rejections gone)
2. `npm test` -- script exists and runs (may still have 57 test failures -- those are fixed in Plan 02)
3. `npx tsc --noEmit 2>&1 | grep "jira.ts"` -- zero matches (unused var gone)
</verification>

<success_criteria>
- Zero "Unhandled Rejection" errors in test output (was 47)
- `npm test` script exists and runs vitest
- jira.ts unused variable TS error eliminated
- All previously passing tests still pass (no regressions from the mock)
</success_criteria>

<output>
After completion, create `.planning/phases/26-test-regression-fixes/26-01-SUMMARY.md`
</output>
