---
phase: 79-drag-to-transition-on-sprint-board
plan: 02
type: tdd
wave: 2
depends_on: ["79-01"]
files_modified:
  - taskflow/src/routes/dashboard/sprintBoardDragHelpers.ts
  - taskflow/src/routes/dashboard/sprintBoardDragHelpers.test.ts
autonomous: true
requirements: [TRAN-01, TRAN-02, TRAN-03]
must_haves:
  truths:
    - "filterDroppableTransitions returns only transitions reachable from the current status AND with neither hasScreen nor hasValidators (D-05 + D-07)"
    - "buildDropModel produces kind:'split' for a column with >=2 reachable transitions (D-01)"
    - "buildDropModel produces kind:'single' for a column with exactly 1 reachable transition (D-02)"
    - "buildDropModel produces kind:'invalid' (or omits) for a column with 0 reachable transitions (D-06)"
    - "split zones are labelled by transition NAME, not destination status name (D-03)"
    - "resolveDropTransitionId returns the transitionId for a valid zone/single-column over.id and null for invalid/own/no-zone (snap-back) (TRAN-01)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/sprintBoardDragHelpers.ts"
      provides: "Pure drop-model + filter + drop-resolution helpers (jsdom-testable seam)"
      exports: ["buildDropModel", "filterDroppableTransitions", "resolveDropTransitionId"]
      min_lines: 40
    - path: "taskflow/src/routes/dashboard/sprintBoardDragHelpers.test.ts"
      provides: "Unit tests for all three helpers incl. D-07 regression"
      contains: "describe('buildDropModel'"
  key_links:
    - from: "taskflow/src/routes/dashboard/sprintBoardDragHelpers.ts"
      to: "filterTransitionsForStatus (greenhopper transitions service)"
      via: "filterDroppableTransitions wraps it then applies the D-07 hasScreen/hasValidators exclusion"
      pattern: "filterTransitionsForStatus"
    - from: "buildDropModel"
      to: "droppable id scheme (zone:<transitionId> / col:<categoryKey>)"
      via: "resolveDropTransitionId decodes over.id against the dropModel"
      pattern: "resolveDropTransitionId"
---

<objective>
Create the pure-function test seam `sprintBoardDragHelpers.ts` (mirroring `backlogDragHelpers.ts`)
that holds ALL drop logic so it can be unit-tested without a real dnd-kit pointer drag (jsdom
cannot drive one). Three exports: `filterDroppableTransitions` (D-05 reachability + D-07
screen/validator exclusion), `buildDropModel` (transitions → per-column split/single/invalid model,
D-01/D-02/D-06), and `resolveDropTransitionId` (over.id → transitionId | null for snap-back, TRAN-01).

Purpose: This is the primary test seam for the whole phase (Wave 0 gap). Extracting drop logic into
pure functions is the Phase 78 precedent (`backlogDragHelpers`) — jsdom cannot simulate the drag, so
the logic must be assertable in isolation. Plan 03 wires these into the live board.

Output: One new helper module + its full unit test file, both green.

Depends on Plan 01: `filterDroppableTransitions` and the dropModel read `t.hasScreen`/
`t.hasValidators` on `JiraTransition`, which only exist after the D-08 type change (Plan 01).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-drag-to-transition-on-sprint-board/79-CONTEXT.md
@.planning/phases/79-drag-to-transition-on-sprint-board/79-RESEARCH.md
@.planning/phases/79-drag-to-transition-on-sprint-board/79-PATTERNS.md
@.planning/phases/79-drag-to-transition-on-sprint-board/79-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for filterDroppableTransitions, buildDropModel, resolveDropTransitionId (RED)</name>
  <files>taskflow/src/routes/dashboard/sprintBoardDragHelpers.test.ts</files>
  <read_first>
    - taskflow/src/routes/dashboard/__tests__/backlogDragHelpers.test.ts (imports block, one-describe-per-function structure, fixture factory pattern, REGRESSION: comment-prefix convention)
    - taskflow/src/routes/dashboard/backlogDragHelpers.ts (export shape — named pure functions + interfaces, NO default export, minimal imports)
    - taskflow/src/services/jira/types.ts (JiraTransition shape incl. the new hasScreen?/hasValidators? from Plan 01, and the `to.statusCategory.key` path used for bucketing)
    - taskflow/src/services/jira/greenhopper/transitions.ts (`filterTransitionsForStatus` signature — what filterDroppableTransitions wraps)
    - .planning/phases/79-drag-to-transition-on-sprint-board/79-PATTERNS.md (§ sprintBoardDragHelpers.ts and § sprintBoardDragHelpers.test.ts — the exact export types `CategoryKey`/`DropZone`/`ColumnDropModel`/`DropModel` and the `makeTransition` fixture factory)
    - .planning/phases/79-drag-to-transition-on-sprint-board/79-VALIDATION.md (Per-Task Verification Map rows for dropModel/transition-filter/drop-resolution)
  </read_first>
  <action>
    Create `sprintBoardDragHelpers.test.ts` importing from `./sprintBoardDragHelpers`. Use a
    `makeTransition(id, name, toStatusCategoryKey, opts?)` fixture factory (opts: `hasScreen?`,
    `hasValidators?`, `fromStatusId?`) returning a `JiraTransition` with
    `to.statusCategory.key = toStatusCategoryKey`, mirroring the PATTERNS.md factory.

    Write three `describe` blocks (one per function), each red against the not-yet-implemented module:

    `describe('filterDroppableTransitions')`: (a) keeps transitions reachable from the given
    currentStatusId; (b) REGRESSION: drops a transition with `hasScreen:true` (D-07); (c) REGRESSION:
    drops a transition with `hasValidators:true` (D-07); (d) keeps a transition where both flags are
    false/undefined; (e) returns [] when input is empty.

    `describe('buildDropModel')`: (a) a category with >=2 transitions yields `{kind:'split', zones:[...]}`
    where each zone has `transitionId` + `transitionName` and the zone label equals the transition NAME
    not the status name (D-03); (b) a category with exactly 1 transition yields `{kind:'single', zone}`
    (D-02); (c) a category with 0 transitions yields `{kind:'invalid'}` (or is absent from the map —
    assert whichever the implementation contract chooses; pick `{kind:'invalid'}` per PATTERNS types);
    (d) transitions are bucketed by `to.statusCategory.key` into the three CATEGORY keys
    `'new'|'indeterminate'|'done'`.

    `describe('resolveDropTransitionId')`: (a) a split-zone over.id `zone:<transitionId>` resolves to
    that transitionId; (b) a single-column over.id `col:<categoryKey>` resolves to that column's single
    transitionId; (c) an over.id for an invalid column resolves to null (snap-back, D-06); (d) a null
    over.id resolves to null; (e) an unknown over.id resolves to null.

    These tests MUST fail (module does not exist yet) — this is the RED step.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/sprintBoardDragHelpers.test.ts 2>&1 | grep -qiE "fail|error|cannot find" && echo RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `sprintBoardDragHelpers.test.ts` exists with exactly three top-level `describe` blocks named `filterDroppableTransitions`, `buildDropModel`, `resolveDropTransitionId`
    - At least two `it` cases carry the `REGRESSION:` prefix for the D-07 hasScreen/hasValidators exclusion
    - Running the test fails (RED) because `./sprintBoardDragHelpers` is not yet implemented
  </acceptance_criteria>
  <done>Test file exists, covers all three helpers incl. D-07 regression and snap-back null cases, and fails because the module is unimplemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sprintBoardDragHelpers.ts to pass (GREEN)</name>
  <files>taskflow/src/routes/dashboard/sprintBoardDragHelpers.ts</files>
  <read_first>
    - taskflow/src/routes/dashboard/backlogDragHelpers.ts (module docblock style, named-export-only shape, no React/dnd-kit imports)
    - taskflow/src/routes/dashboard/sprintBoardDragHelpers.test.ts (the contract just written in Task 1 — implement to satisfy it exactly)
    - taskflow/src/services/jira/greenhopper/transitions.ts (`filterTransitionsForStatus` to wrap)
    - .planning/phases/79-drag-to-transition-on-sprint-board/79-PATTERNS.md (§ sprintBoardDragHelpers.ts — exact exported type definitions: `CategoryKey`, `DropZone`, `ColumnDropModel`, `DropModel`)
  </read_first>
  <behavior>
    - filterDroppableTransitions(all, currentStatusId): filterTransitionsForStatus(all, currentStatusId) then .filter(t => !t.hasScreen && !t.hasValidators)
    - buildDropModel(transitions): Map<CategoryKey, ColumnDropModel>; bucket by to.statusCategory.key; >=2 → split, ==1 → single, ==0 → invalid
    - resolveDropTransitionId(overId, dropModel): zone:<id> → id; col:<key> → that column's single zone transitionId; invalid/null/unknown → null
  </behavior>
  <action>
    Create `sprintBoardDragHelpers.ts` with a module docblock mirroring `backlogDragHelpers.ts`
    (purpose: drop-model seam extracted so jsdom can unit-test drag logic). Import ONLY the
    `JiraTransition` type and `filterTransitionsForStatus` — no React, no dnd-kit, no query client
    (mirror the minimal-import constraint).

    Export the types from PATTERNS.md verbatim: `type CategoryKey = 'new' | 'indeterminate' | 'done'`,
    `interface DropZone { transitionId: string; transitionName: string }`, the discriminated union
    `type ColumnDropModel = {kind:'split'; zones: DropZone[]} | {kind:'single'; zone: DropZone} |
    {kind:'invalid'}`, and `type DropModel = Map<CategoryKey, ColumnDropModel>`.

    Implement `filterDroppableTransitions(all, currentStatusId)` = `filterTransitionsForStatus(all,
    currentStatusId)` then `.filter((t) => !t.hasScreen && !t.hasValidators)` (D-05 + D-07).

    Implement `buildDropModel(transitions)`: group transitions by `to.statusCategory?.key` (cast to
    `CategoryKey`); for each of the three category keys present, emit `{kind:'split', zones}` when the
    bucket length >=2 (each zone `{transitionId: t.id, transitionName: t.name}` — D-03 uses the
    transition NAME), `{kind:'single', zone}` when length ==1, and `{kind:'invalid'}` when 0. Build the
    droppable id scheme used by resolveDropTransitionId: split zones keyed `zone:<transitionId>`, single
    columns reachable via `col:<categoryKey>`.

    Implement `resolveDropTransitionId(overId, dropModel)`: null overId → null; `zone:<transitionId>`
    → return the transitionId; `col:<categoryKey>` → look up the column model, return its single zone's
    transitionId for `kind:'single'`, else null (invalid/split-without-zone-hit/own-column); unknown id
    → null (silent snap-back per D-06).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/sprintBoardDragHelpers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `sprintBoardDragHelpers.ts` exports `buildDropModel`, `filterDroppableTransitions`, `resolveDropTransitionId` (named, no default)
    - `grep -c "from '@dnd-kit" taskflow/src/routes/dashboard/sprintBoardDragHelpers.ts` returns 0 (no dnd-kit import — pure seam)
    - `grep -c "from 'react'" taskflow/src/routes/dashboard/sprintBoardDragHelpers.ts` returns 0
    - `cd taskflow && npx vitest run src/routes/dashboard/sprintBoardDragHelpers.test.ts` exits 0 (all GREEN)
    - `cd taskflow && npx tsc --noEmit` reports no errors for the new module
  </acceptance_criteria>
  <done>All three helpers implemented as pure functions; the full Task-1 test suite is green; module imports no React/dnd-kit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server transition list → drop-target model | `filterDroppableTransitions`/`buildDropModel` decide which transitions become drop targets purely from the server-provided `JiraTransition[]`; this is where screen/validator transitions are excluded before any drop can fire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-02 | Tampering | `filterDroppableTransitions` D-07 exclusion | mitigate | The filter excludes any transition with `hasScreen` or `hasValidators` from the drop model, so a screen/validator transition can never become a silent drop target (TRAN-03). Unit REGRESSION tests assert both flags are excluded — locking the behavior against regressions. |
| T-79-03 | Spoofing | `resolveDropTransitionId` over.id decoding | mitigate | Drop resolution only returns a transitionId that exists in the freshly-built dropModel (sourced from the server transition list); an unknown/forged `over.id` resolves to null → silent snap-back, never an arbitrary transition. |
| T-79-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (RESEARCH Package Legitimacy Audit). No supply-chain surface. |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/sprintBoardDragHelpers.test.ts` green
- `cd taskflow && npx tsc --noEmit` no new errors
- Module imports neither React nor dnd-kit (pure seam preserved)
</verification>

<success_criteria>
- Three pure helpers (`filterDroppableTransitions`, `buildDropModel`, `resolveDropTransitionId`) exist and are unit-tested
- D-01/D-02/D-06 split/single/invalid logic covered; D-03 transition-name labelling covered; D-05/D-07 reachability + screen/validator exclusion covered (incl. regression tests); TRAN-01 drop-resolution + null snap-back covered
- All tests green; seam is jsdom-assertable per the Phase 78 precedent
</success_criteria>

<output>
Create `.planning/phases/79-drag-to-transition-on-sprint-board/79-02-SUMMARY.md` when done
</output>
