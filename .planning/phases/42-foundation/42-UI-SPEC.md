---
phase: 42
slug: foundation
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-03-29
---

# Phase 42 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova (neutral, cssVariables: true) |
| Component library | radix-ui (via shadcn) |
| Icon library | lucide-react |
| Font | Geist Variable (`@fontsource-variable/geist`) |

Source: `taskflow/components.json`, `taskflow/src/index.css`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none — this phase adds no new layout surfaces; all spacing follows the existing scale already in use.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 12px | 400 | 1.4 |
| Heading | 24px | 600 | 1.2 |
| Display | 28px | 600 | 1.2 |

Weights: 400 (normal) for Body and Label; 600 (semibold) for Heading and Display. The 12px size is sufficient to distinguish Label from Body without a third weight.

Source: Inferred from `ErrorPage.tsx` — `text-2xl font-semibold` (h1), `text-sm text-muted-foreground` (body), standard shadcn base-nova scale. No new type sizes introduced in this phase.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `oklch(1 0 0)` light / `oklch(0.145 0 0)` dark | Background, page surfaces (`--background`) |
| Secondary (30%) | `oklch(0.985 0 0)` light / `oklch(0.205 0 0)` dark | Cards, sidebar, nav (`--card`, `--sidebar`) |
| Accent (10%) | `oklch(0.97 0 0)` light / `oklch(0.269 0 0)` dark | Hover states, muted containers (`--accent`, `--muted`) |
| Destructive | `oklch(0.577 0.245 27.325)` light / `oklch(0.704 0.191 22.216)` dark | Chunk-load error boundary icon only (`--destructive`) |

Accent reserved for: hover/focus backgrounds on the Retry Loading button ghost variant in the chunk-load error boundary; no new interactive elements are introduced in this phase.

Source: `taskflow/src/index.css` CSS custom properties.

---

## Copywriting Contract

### Suspense Fallback (lazy route chunk loading)

| Element | Copy |
|---------|------|
| Primary CTA | n/a — spinner only, no CTA present during chunk load |
| Accessible label | `aria-label="Loading page"` on the spinner container |

Decision D-04 (42-CONTEXT.md): simple centered spinner, no text label visible on screen.

### Chunk-Load Error Boundary

| Element | Copy |
|---------|------|
| Error heading | "Something went wrong loading this page" |
| Error body | "The page failed to load. Check your connection and try again." |
| Primary CTA | "Retry Loading" |
| Secondary CTA | "Go to Dashboard" |
| Destructive confirmation | none — no destructive actions in this phase |

Source: Decision D-05 (42-CONTEXT.md) mandates reuse of `ErrorPage` styling. Copy derived from `ErrorPage.tsx` existing pattern and adapted for chunk-load context.

### Bundle Analysis (developer-facing only, no end-user UI)

No user-facing copy required. The `ANALYZE=true vite build` report is a developer tool — decision D-09 (42-CONTEXT.md).

---

## Component Inventory

New components introduced in this phase:

| Component | Path | Description |
|-----------|------|-------------|
| `RouteSpinner` | `src/components/ui/route-spinner.tsx` | Centered `Loader2` spinner used as Suspense fallback for all 6 lazy routes. Reuses `Loader2` from lucide-react + `animate-spin`. |
| `ChunkErrorBoundary` | `src/components/ChunkErrorBoundary.tsx` | React class error boundary wrapping each lazy route. Renders error state derived from `ErrorPage` styling on chunk failure. Exposes `onRetry` (reloads chunk) and "Go to Dashboard" link. |

Existing components reused:

| Component | Path | Usage |
|-----------|------|-------|
| `ErrorPage` | `src/routes/error/ErrorPage.tsx` | Styling reference and layout pattern for `ChunkErrorBoundary` |
| `Button` | `src/components/ui/button.tsx` | "Retry Loading" (variant: ghost, size: lg) and "Go to Dashboard" (variant: default, size: lg) in error boundary |

---

## Interaction States

### `RouteSpinner` (Suspense fallback)
- Renders: `Loader2` icon at `size-8`, `animate-spin`, `text-muted-foreground`, vertically and horizontally centered in `min-h-screen`
- No hover, focus, or active states — purely informational
- Screen reader: `<div role="status" aria-label="Loading page">` wrapping the spinner

### `ChunkErrorBoundary` (chunk-load failure)
- Renders: `AlertTriangle` icon in `bg-destructive/10 text-destructive` circle (56px, matches `ErrorPage`)
- Heading: `text-2xl font-semibold tracking-tight`
- Body: `text-sm text-muted-foreground`
- Retry Loading button: `variant="ghost" size="lg"` with `RefreshCw` icon — calls `window.location.reload()`
- Dashboard button: `variant="default" size="lg"` with `Home` icon — navigates to `/dashboard`
- Layout: `min-h-screen flex items-center justify-center bg-background px-4` (identical to `ErrorPage`)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Skeleton (existing), ui primitives | not required |
| third-party | none | not applicable |

No third-party registries declared. `components.json` confirms `"registries": {}`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
