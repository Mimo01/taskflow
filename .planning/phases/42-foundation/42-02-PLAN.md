---
phase: 42-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/vite.config.ts
  - taskflow/package.json
  - taskflow/src/components/app/Sidebar.tsx
  - taskflow/src/components/app/PinnedTabStrip.tsx
  - taskflow/src/components/UnifiedFilterBar.tsx
  - taskflow/src/routes/notifications/NotificationPopover.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.tsx
  - taskflow/src/routes/dashboard/BacklogPage.tsx
  - taskflow/src/routes/dashboard/BacklogFilterBar.tsx
  - taskflow/src/routes/dashboard/BulkActionBar.tsx
  - taskflow/src/routes/dashboard/EpicsPage.tsx
  - taskflow/src/routes/dashboard/ImageLightbox.tsx
  - taskflow/src/routes/dashboard/InlineComment.tsx
  - taskflow/src/routes/dashboard/IssueLinkRow.tsx
  - taskflow/src/routes/dashboard/IssueDetailPage.tsx
  - taskflow/src/routes/dashboard/IssueDetailContent.tsx
  - taskflow/src/routes/dashboard/MergeRequestDetailPage.tsx
  - taskflow/src/routes/dashboard/MrAttentionTab.tsx
  - taskflow/src/routes/dashboard/MyTasksTab.tsx
  - taskflow/src/routes/dashboard/QuickFilterChipRow.tsx
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
  - taskflow/src/routes/dashboard/ReleasesTab.tsx
  - taskflow/src/routes/dashboard/SprintProgressTab.tsx
  - taskflow/src/routes/dashboard/WidgetGrid.tsx
  - taskflow/src/routes/dashboard/WikiRenderer.tsx
  - taskflow/src/routes/dashboard/WorkloadTab.tsx
  - taskflow/src/routes/dashboard/issue-detail/ActivityTimeline.tsx
  - taskflow/src/routes/dashboard/issue-detail/AttachmentLightbox.tsx
  - taskflow/src/routes/dashboard/issue-detail/AttachmentUpload.tsx
  - taskflow/src/routes/dashboard/issue-detail/AttachmentsSection.tsx
  - taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx
  - taskflow/src/routes/dashboard/issue-detail/IssueDetailSidebar.tsx
  - taskflow/src/routes/dashboard/issue-detail/LinkedIssuesSection.tsx
  - taskflow/src/routes/dashboard/issue-detail/useFieldMutation.ts
  - taskflow/src/routes/dashboard/widgets/ReleasesWidget.tsx
  - taskflow/src/routes/dashboard/widgets/SprintProgressWidget.tsx
  - taskflow/src/routes/dashboard/widgets/WorkloadWidget.tsx
autonomous: true
requirements:
  - ROUT-04

must_haves:
  truths:
    - "React Compiler is active at build time and auto-memoizes components across the codebase"
    - "No manual React.memo, useMemo, or useCallback calls remain in the codebase"
  artifacts:
    - path: "taskflow/vite.config.ts"
      provides: "React Compiler via @rolldown/plugin-babel with reactCompilerPreset"
      contains: "reactCompilerPreset"
    - path: "taskflow/package.json"
      provides: "babel-plugin-react-compiler@1.0.0 and @rolldown/plugin-babel dev dependencies"
      contains: "babel-plugin-react-compiler"
  key_links:
    - from: "taskflow/vite.config.ts"
      to: "babel-plugin-react-compiler"
      via: "@rolldown/plugin-babel with reactCompilerPreset"
      pattern: "babel.*reactCompilerPreset"
---

<objective>
Enable React Compiler for automatic memoization and remove all manual memo/useMemo/useCallback calls.

Purpose: Eliminate manual memoization overhead — the compiler handles it more precisely at the IR level, and keeping both is noise (per D-08).
Output: Updated vite.config.ts with React Compiler, 34 files cleaned of manual memoization calls.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-foundation/42-CONTEXT.md
@.planning/phases/42-foundation/42-RESEARCH.md

@taskflow/vite.config.ts
@taskflow/package.json

<interfaces>
From taskflow/vite.config.ts:
```typescript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";
import path from "path";
import { execSync } from "child_process";

export default defineConfig(async () => ({
  plugins: [tailwindcss(), react()],
  // ... resolve, define, server config
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React Compiler dependencies and update vite.config.ts</name>
  <files>taskflow/vite.config.ts, taskflow/package.json</files>
  <read_first>
    taskflow/vite.config.ts,
    taskflow/package.json
  </read_first>
  <action>
**Step 1: Install dependencies** (per D-07 for exact pin):
```bash
cd /Users/mimo/Desktop/Tasker/taskflow
npm install --save-dev --save-exact babel-plugin-react-compiler@1.0.0
npm install --save-dev @rolldown/plugin-babel
```

**Step 2: Update vite.config.ts**

Add two new imports at the top:
```typescript
import { reactCompilerPreset } from "@vitejs/plugin-react";
import babel from "@rolldown/plugin-babel";
```

Note: `reactCompilerPreset` is a named export from `@vitejs/plugin-react` (already imported as `react`). Update the existing import line:
```typescript
import react, { reactCompilerPreset } from "@vitejs/plugin-react";
```

Update the plugins array from:
```typescript
plugins: [tailwindcss(), react()],
```
to:
```typescript
plugins: [
  tailwindcss(),
  react(),
  babel({ presets: [reactCompilerPreset()] }),
],
```

CRITICAL: Use `@rolldown/plugin-babel` NOT `rollup-plugin-babel` or `vite-plugin-babel` — those are incompatible with Vite 8 / Rolldown.

CRITICAL: `babel-plugin-react-compiler` must be pinned to exact `1.0.0` (no ^ or ~ prefix) per D-07.

After editing, run `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit` to confirm types are clean.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/vite.config.ts contains `import babel from "@rolldown/plugin-babel"`
    - taskflow/vite.config.ts contains `reactCompilerPreset` (imported from @vitejs/plugin-react)
    - taskflow/vite.config.ts contains `babel({ presets: [reactCompilerPreset()] })`
    - taskflow/package.json contains `"babel-plugin-react-compiler": "1.0.0"` (exact, no caret)
    - taskflow/package.json contains `"@rolldown/plugin-babel"`
    - `tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>React Compiler is active at build time via @rolldown/plugin-babel + reactCompilerPreset. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove manual memo/useMemo/useCallback from components and notifications</name>
  <files>
    taskflow/src/components/app/Sidebar.tsx,
    taskflow/src/components/app/PinnedTabStrip.tsx,
    taskflow/src/components/UnifiedFilterBar.tsx,
    taskflow/src/routes/notifications/NotificationPopover.tsx,
    taskflow/src/routes/dashboard/SprintBoardTab.tsx,
    taskflow/src/routes/dashboard/BacklogPage.tsx,
    taskflow/src/routes/dashboard/BacklogFilterBar.tsx,
    taskflow/src/routes/dashboard/BulkActionBar.tsx,
    taskflow/src/routes/dashboard/EpicsPage.tsx,
    taskflow/src/routes/dashboard/ImageLightbox.tsx,
    taskflow/src/routes/dashboard/InlineComment.tsx,
    taskflow/src/routes/dashboard/IssueLinkRow.tsx
  </files>
  <read_first>
    taskflow/src/components/app/Sidebar.tsx,
    taskflow/src/components/app/PinnedTabStrip.tsx,
    taskflow/src/components/UnifiedFilterBar.tsx,
    taskflow/src/routes/notifications/NotificationPopover.tsx,
    taskflow/src/routes/dashboard/SprintBoardTab.tsx,
    taskflow/src/routes/dashboard/BacklogPage.tsx,
    taskflow/src/routes/dashboard/BacklogFilterBar.tsx,
    taskflow/src/routes/dashboard/BulkActionBar.tsx,
    taskflow/src/routes/dashboard/EpicsPage.tsx,
    taskflow/src/routes/dashboard/ImageLightbox.tsx,
    taskflow/src/routes/dashboard/InlineComment.tsx,
    taskflow/src/routes/dashboard/IssueLinkRow.tsx
  </read_first>
  <action>
Per D-08: Remove ALL manual `React.memo()`, `useMemo()`, and `useCallback()` calls from these 12 files.

**For each file, apply these transformations:**

1. **`useMemo(() => expr, [deps])`** — Replace with just `expr` (the computed value inline, or assign to a `const`). If the expression is complex (multi-line), extract into a plain `const` variable.

2. **`useCallback(fn, [deps])`** — Replace with just `fn` (the function directly). If it was `const handler = useCallback(() => { ... }, [deps])`, change to `const handler = () => { ... }`.

3. **`React.memo(Component)`** or `memo(Component)` — Unwrap. If the component was `export default React.memo(MyComponent)`, change to `export default MyComponent`. If defined inline as `const MyComponent = memo(...)`, unwrap the inner function.

4. **Clean up unused imports:** After removing calls, remove `useMemo`, `useCallback`, `memo` from React import statements. Do NOT remove the `react` import entirely if other items are still imported from it (e.g., `useState`, `useEffect`, `useRef`).

READ EACH FILE before modifying. Understand what the useMemo/useCallback computes, then inline/simplify.

After all removals in this batch, run:
1. `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit` — expect 0 errors
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - None of the 12 files import `useMemo`, `useCallback`, or `memo` from 'react'
    - None of the 12 files contain `React.memo(`, `useMemo(`, or `useCallback(`
    - `tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>12 component/notification files cleaned of manual memoization. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 3: Remove manual memo/useMemo/useCallback from dashboard root and issue-detail</name>
  <files>
    taskflow/src/routes/dashboard/IssueDetailPage.tsx,
    taskflow/src/routes/dashboard/IssueDetailContent.tsx,
    taskflow/src/routes/dashboard/MergeRequestDetailPage.tsx,
    taskflow/src/routes/dashboard/MrAttentionTab.tsx,
    taskflow/src/routes/dashboard/MyTasksTab.tsx,
    taskflow/src/routes/dashboard/QuickFilterChipRow.tsx,
    taskflow/src/routes/dashboard/ReleaseDetailPage.tsx,
    taskflow/src/routes/dashboard/ReleasesTab.tsx,
    taskflow/src/routes/dashboard/SprintProgressTab.tsx,
    taskflow/src/routes/dashboard/WidgetGrid.tsx,
    taskflow/src/routes/dashboard/WikiRenderer.tsx,
    taskflow/src/routes/dashboard/WorkloadTab.tsx,
    taskflow/src/routes/dashboard/issue-detail/ActivityTimeline.tsx,
    taskflow/src/routes/dashboard/issue-detail/AttachmentLightbox.tsx,
    taskflow/src/routes/dashboard/issue-detail/AttachmentUpload.tsx,
    taskflow/src/routes/dashboard/issue-detail/AttachmentsSection.tsx,
    taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx,
    taskflow/src/routes/dashboard/issue-detail/IssueDetailSidebar.tsx,
    taskflow/src/routes/dashboard/issue-detail/LinkedIssuesSection.tsx,
    taskflow/src/routes/dashboard/issue-detail/useFieldMutation.ts
  </files>
  <read_first>
    taskflow/src/routes/dashboard/IssueDetailPage.tsx,
    taskflow/src/routes/dashboard/IssueDetailContent.tsx,
    taskflow/src/routes/dashboard/MergeRequestDetailPage.tsx,
    taskflow/src/routes/dashboard/MyTasksTab.tsx,
    taskflow/src/routes/dashboard/WorkloadTab.tsx,
    taskflow/src/routes/dashboard/issue-detail/ActivityTimeline.tsx,
    taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx
  </read_first>
  <action>
Per D-08: Remove ALL manual `React.memo()`, `useMemo()`, and `useCallback()` calls from these 20 files.

**Apply the same transformations as Task 2:**

1. **`useMemo(() => expr, [deps])`** — Replace with just `expr` inline or as a plain `const`.
2. **`useCallback(fn, [deps])`** — Replace with just `fn` directly.
3. **`React.memo(Component)`** or `memo(Component)` — Unwrap the inner function.
4. **Clean up unused imports:** Remove `useMemo`, `useCallback`, `memo` from React imports.

READ EACH FILE before modifying. Understand what the useMemo/useCallback computes, then inline/simplify.

After all removals in this batch, run:
1. `grep -rn "useMemo\|useCallback\|React\.memo\b" taskflow/src/ --include="*.tsx" --include="*.ts"` — expect 0 matches
2. `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit` — expect 0 errors
3. `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run` — expect all tests pass
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && grep -rcn "useMemo\|useCallback\|React\.memo" src/ --include="*.tsx" --include="*.ts" | grep -v ":0$" | wc -l && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "useMemo\|useCallback\|React\.memo" taskflow/src/ --include="*.tsx" --include="*.ts"` returns 0 matches
    - No file in taskflow/src/ imports `useMemo`, `useCallback`, or `memo` from 'react'
    - `tsc --noEmit` exits 0
    - `vitest run` exits 0
  </acceptance_criteria>
  <done>All 34 files cleaned of manual memoization. Zero useMemo/useCallback/React.memo calls remain in codebase. TypeScript clean, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Remove manual memo/useMemo/useCallback from widgets</name>
  <files>
    taskflow/src/routes/dashboard/widgets/ReleasesWidget.tsx,
    taskflow/src/routes/dashboard/widgets/SprintProgressWidget.tsx,
    taskflow/src/routes/dashboard/widgets/WorkloadWidget.tsx
  </files>
  <read_first>
    taskflow/src/routes/dashboard/widgets/ReleasesWidget.tsx,
    taskflow/src/routes/dashboard/widgets/SprintProgressWidget.tsx,
    taskflow/src/routes/dashboard/widgets/WorkloadWidget.tsx
  </read_first>
  <action>
Per D-08: Remove ALL manual `React.memo()`, `useMemo()`, and `useCallback()` calls from these 3 widget files.

**Apply the same transformations as Tasks 2 and 3:**

1. **`useMemo(() => expr, [deps])`** — Replace with just `expr` inline or as a plain `const`.
2. **`useCallback(fn, [deps])`** — Replace with just `fn` directly.
3. **`React.memo(Component)`** or `memo(Component)` — Unwrap the inner function.
4. **Clean up unused imports:** Remove `useMemo`, `useCallback`, `memo` from React imports.

READ EACH FILE before modifying.

After all removals, run the full verification:
1. `grep -rn "useMemo\|useCallback\|React\.memo\b" taskflow/src/ --include="*.tsx" --include="*.ts"` — expect 0 matches
2. `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit` — expect 0 errors
3. `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run` — expect all tests pass
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && grep -rcn "useMemo\|useCallback\|React\.memo" src/ --include="*.tsx" --include="*.ts" | grep -v ":0$" | wc -l && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "useMemo\|useCallback\|React\.memo" taskflow/src/ --include="*.tsx" --include="*.ts"` returns 0 matches across entire codebase
    - No file in taskflow/src/ imports `useMemo`, `useCallback`, or `memo` from 'react'
    - `tsc --noEmit` exits 0
    - `vitest run` exits 0
  </acceptance_criteria>
  <done>Zero manual memoization calls remain in codebase. React Compiler handles all memoization automatically. TypeScript clean, all tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -rn "useMemo\|useCallback\|React\.memo" taskflow/src/ --include="*.tsx" --include="*.ts"` — 0 matches (D-08 compliance)
2. `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit` — 0 errors
3. `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run` — all tests pass
4. `cd /Users/mimo/Desktop/Tasker/taskflow && npx vite build` — build succeeds with React Compiler active
</verification>

<success_criteria>
- React Compiler active at build time via @rolldown/plugin-babel + reactCompilerPreset
- babel-plugin-react-compiler pinned to exact 1.0.0
- Zero useMemo/useCallback/React.memo calls remain in src/
- Build, TypeScript, and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/42-foundation/42-02-SUMMARY.md`
</output>
