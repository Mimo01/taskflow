---
phase: 89-three-channel-drift-detection
plan: 04
type: execute
wave: 3
depends_on: ["89-01", "89-02"]
files_modified:
  - taskflow/src/routes/dashboard/ReleasesTab.tsx
  - taskflow/src/routes/dashboard/ReleasesTab.test.tsx
autonomous: true
requirements: [DRIFT-09]

must_haves:
  truths:
    - "A release row shows an aggregate drift count beside the existing branch and milestone indicators"
    - "The count derives from exactly ONE project-wide open-MR request regardless of how many rows are displayed"
    - "Hovering the indicator explains that it covers branch and milestone only, so a higher detail-page count is not a bug"
    - "The existing row-branch-present / row-missing-branch / row-missing-milestone indicators still render unchanged"
  artifacts:
    - path: "taskflow/src/routes/dashboard/ReleasesTab.tsx"
      provides: "one fully-paginated open-MR query plus the per-row drift count and its indicator"
      contains: "gitlab-open-mrs"
    - path: "taskflow/src/routes/dashboard/ReleasesTab.test.tsx"
      provides: "assertions for the row-drift-count testid and the fetch-once guarantee"
      contains: "row-drift-count"
  key_links:
    - from: "ReleasesTab.tsx"
      to: "fetchOpenProjectMRs"
      via: "single useQuery keyed ['gitlab-open-mrs', activeGitlabProject]"
      pattern: "gitlab-open-mrs"
    - from: "ReleasesTab.tsx"
      to: "driftDetection.computeRowDriftCount"
      via: "per-row local derivation inside toMatched"
      pattern: "computeRowDriftCount"
---

<objective>
Add the aggregate drift count to each Releases-list row (DRIFT-09, D-14, D-15), derived from a single project-wide open-MR fetch and filtered locally per row — the same fetch-once pattern Phase 88 established for release branches.

Purpose: The list page is where a user notices drift before opening anything. It must cost one request, not one per row.
Output: One new query in `ReleasesTab.tsx`, a fourth field on the per-row derivation, and an indicator filling the slot the Phase 88 comment already reserves.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-three-channel-drift-detection/89-CONTEXT.md
@.planning/phases/89-three-channel-drift-detection/89-UI-SPEC.md
@.planning/phases/89-three-channel-drift-detection/89-PATTERNS.md
@.planning/phases/89-three-channel-drift-detection/89-01-SUMMARY.md
@.planning/phases/89-three-channel-drift-detection/89-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch the project's open MRs once and derive a per-row drift count</name>
  <files>taskflow/src/routes/dashboard/ReleasesTab.tsx, taskflow/src/routes/dashboard/ReleasesTab.test.tsx</files>

  <read_first>
    - `taskflow/src/routes/dashboard/ReleasesTab.tsx` lines 186-212 (the `releaseBranches` fetch-once query plus its D-18 comment — the exact shape and rationale to copy) and lines 224-298 (the `toMatched` builder where `branchMissing` / `branchPresent` / `milestoneMissing` are derived locally per row, and the `MatchedVersion` object that gains a fourth field)
    - `taskflow/src/routes/dashboard/release-detail/driftDetection.ts` (`computeRowDriftCount` signature from plan 89-02, and its JSDoc note that TASK is deliberately excluded)
    - `taskflow/src/services/gitlab.ts` (`fetchOpenProjectMRs` signature from plan 89-01)
    - `taskflow/src/routes/dashboard/ReleasesTab.tsx` — grep for `MatchedVersion` to find the type declaration that must gain `driftCount: number`
  </read_first>

  <action>
    Add a single `useQuery` to `ReleasesTab.tsx` immediately after the `releaseBranches` query, calling `fetchOpenProjectMRs(gitlabBaseUrl ?? '', gitlabToken ?? '', activeGitlabProject ?? 0)` with query key exactly `['gitlab-open-mrs', activeGitlabProject]`, `enabled: !!gitlabBaseUrl && !!activeGitlabProject && !!gitlabToken`, and `staleTime: 5 * 60_000` — byte-for-byte the same guard and staleTime as the `releaseBranches` query above it. Destructure `data: openMrs` and `isSuccess: openMrsLoaded`.

    Copy the D-18 fetch-once comment style from lines 187-191 and adapt it for D-14: the entire project's open MR set is fetched in exactly ONE fully-paginated request regardless of row count, then evaluated locally per row. Never a per-row query hook — that pattern exists a few lines below only because Jira issue counts have no batch endpoint.

    Inside `toMatched`, add a fourth derived field to the returned `MatchedVersion` object:
    `driftCount: openMrsLoaded && !version.released ? computeRowDriftCount(openMrs ?? [], derived, matchedMilestoneId) : 0`
    where `derived` is the already-computed release branch name for the row and `matchedMilestoneId` is the id of the milestone that produced `bestMatch` (resolve it from the milestone list the candidates were built from — extend the `candidates` mapping at line 229 to carry `id` alongside `date` / `name` / `url`, and carry the matched candidate's id through `bestMatch` selection).

    Gate on `!version.released`, mirroring the documented CR-01 / WR-04 rationale already in this file at lines 258-265: release branches are deleted once merged, so historical releases would otherwise report drift forever and bury the signal for the unreleased versions this indicator exists to police. **Planner discretion recorded:** D-14 did not state a released-version rule; this follows the established precedent for the sibling indicators in the same row.

    Gate on `openMrsLoaded` for the same reason `branchMissing` gates on `branchesLoaded` — a count derived from an in-flight or errored fetch reads as real drift.

    Add `driftCount` to the `MatchedVersion` type declaration.
  </action>

  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx tsc --noEmit && npx vitest run src/routes/dashboard/ReleasesTab.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "'gitlab-open-mrs'" taskflow/src/routes/dashboard/ReleasesTab.tsx` returns 1
    - `grep -c 'useQueries' taskflow/src/routes/dashboard/ReleasesTab.tsx` is unchanged from before this task — no new per-row query hook was introduced
    - `grep -c 'computeRowDriftCount' taskflow/src/routes/dashboard/ReleasesTab.tsx` returns 1
    - The `MatchedVersion` type declares `driftCount: number`
    - The drift count expression contains both `openMrsLoaded` and `!version.released`
    - `npx tsc --noEmit` exits 0 and `npx vitest run src/routes/dashboard/ReleasesTab.test.tsx` exits 0 (the `vi.mock('@/services/gitlab', ...)` factory in that test file lists functions explicitly and must be extended with `fetchOpenProjectMRs: vi.fn().mockResolvedValue([])`, or the import resolves to undefined)
  </acceptance_criteria>

  <done>One project-wide open-MR fetch feeds a per-row branch-and-milestone drift count.</done>
</task>

<task type="auto">
  <name>Task 2: Render the drift indicator in the reserved slot and assert it in tests</name>
  <files>taskflow/src/routes/dashboard/ReleasesTab.tsx, taskflow/src/routes/dashboard/ReleasesTab.test.tsx</files>

  <read_first>
    - `taskflow/src/routes/dashboard/ReleasesTab.tsx` lines 556-587 (the reserved-slot comment at 558-563, the `branchPresent` and `branchMissing` indicator spans, and the task-count span the new badge must sit before)
    - `.planning/phases/89-three-channel-drift-detection/89-UI-SPEC.md` § Copywriting Contract, "Releases-list aggregate drift indicator" row (exact label and tooltip copy) and § Color (orange is reserved for drift)
    - `taskflow/src/routes/dashboard/ReleasesTab.test.tsx` lines 414-520 (the `release-row drift indicators (D-17/D-18/D-19)` describe block — the setup pattern for mocking milestones, `matchGitLabToFixVersion` and branch fetches, and the `findByTestId` assertion style to copy)
  </read_first>

  <action>
    Insert the D-15 indicator between the `branchMissing` block and the `{/* Task count */}` comment — exactly where the reserved-slot comment says a Phase 89 aggregate drift count should append. Do not restructure the row or move the existing indicators.

    Render it only when `driftCount > 0`. Follow the existing `<span title={...} data-testid={...}>` shape used by the sibling indicators. Use `data-testid="row-drift-count"`. Visible label per the UI-SPEC copywriting contract: the count followed by the word `drift` (for example `3 drift`), styled with the existing orange warning tone (`text-orange-600 dark:text-orange-400`) and `text-xs tabular-nums`, with an `AlertTriangle` at `size-3 shrink-0` and `aria-hidden="true"` matching the `branchMissing` span. Include a `<span className="sr-only">` describing the count for screen readers, mirroring the sibling indicators.

    The `title` attribute is mandatory (D-14) and must use the UI-SPEC copy: the count of MRs needing branch or milestone attention, plus the instruction to open the release for the full check including task links. This is what stops the legitimately-higher detail-page count from reading as a bug. Use a native `title` attribute — there is no Tooltip primitive in this codebase (UI-SPEC § Design System).

    Update the existing `release-row drift indicators` describe block in `ReleasesTab.test.tsx` additively — do not modify the existing `row-missing-branch` / `row-branch-present` / `row-missing-milestone` tests, which are named in CONTEXT.md's must-not-regress list. Add tests that:
    1. Given one open MR with a wrong target branch and no release milestone, `findByTestId('row-drift-count')` resolves and its text contains `1`.
    2. Given no drifting MRs, `queryByTestId('row-drift-count')` is null.
    3. `fetchOpenProjectMRs` is called exactly once even when several fix versions render (the fetch-once guarantee, mirroring the existing `expect(fetchProjectBranches).toHaveBeenCalledTimes(1)` assertion at line 437).
    4. The indicator's `title` attribute is present and mentions branch or milestone (the D-14 discrepancy explanation).
  </action>

  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run src/routes/dashboard/ReleasesTab.test.tsx && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'row-drift-count' taskflow/src/routes/dashboard/ReleasesTab.tsx` returns 1 and `grep -c 'row-drift-count' taskflow/src/routes/dashboard/ReleasesTab.test.tsx` is at least 2
    - `grep -c 'row-missing-branch\|row-branch-present\|row-missing-milestone' taskflow/src/routes/dashboard/ReleasesTab.tsx` is unchanged from before this task
    - The `row-drift-count` span appears in the source AFTER the `branchMissing` block and BEFORE the `{/* Task count */}` comment
    - The indicator carries a non-empty `title` attribute mentioning both branch and milestone coverage
    - A test asserts `fetchOpenProjectMRs` is called exactly once across a multi-row render
    - `npx vitest run src/routes/dashboard/ReleasesTab.test.tsx` exits 0 with the pre-existing D-17/D-18/D-19 tests still passing
  </acceptance_criteria>

  <done>The Releases list shows a per-row drift count in the reserved slot, with a tooltip that pre-empts the detail-page discrepancy, and the Phase 88 indicators are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri app → GitLab REST API | One additional project-wide authenticated GET on the Releases list mount |
| GitLab MR data → row rendering | MR milestone titles and branch names flow into a rendered count and `title` attribute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-13 | Denial of Service | Per-row MR queries fanning out across a long release list | mitigate | Exactly one `useQuery` keyed `['gitlab-open-mrs', activeGitlabProject]`; the per-row work is a pure local filter via `computeRowDriftCount`. Enforced by the Task 2 test asserting `fetchOpenProjectMRs` is called once across a multi-row render (D-14, P88 D-18). |
| T-89-14 | Elevation of Privilege | `activeGitlabProject ?? 0` reaching the URL | mitigate | `enabled` guard includes `!!activeGitlabProject`, so the `?? 0` fallback is unreachable — the WR-10 pattern already used by the sibling `releaseBranches` query. |
| T-89-15 | Information Disclosure | Rendering GitLab-sourced strings into a `title` attribute | mitigate | The `title` is a fixed template with an interpolated integer count only; no MR title or branch name is interpolated into it. React escapes attribute values regardless. |
| T-89-16 | Spoofing | A count derived from an in-flight or errored fetch reading as confirmed drift | mitigate | The count is gated on `openMrsLoaded` (query `isSuccess`), mirroring the CR-01 rationale already documented for `branchMissing` at `ReleasesTab.tsx:258-265`. |
| T-89-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed in this phase. |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/ReleasesTab.test.tsx` exits 0
- `cd taskflow && npx tsc --noEmit` exits 0
- `cd taskflow && npm run check` shows no new errors beyond the 2-error biome baseline
- Manual (deferred to phase UAT): open the Releases list against the live instance and confirm the badge appears only on unreleased rows with real drift
</verification>

<success_criteria>
- DRIFT-09: each qualifying release row shows an aggregate drift count
- The count comes from one request regardless of row count (D-14, D-18 from Phase 88)
- The indicator sits in the reserved slot beside the existing branch and milestone icons (D-15), with the existing testids untouched
- The tooltip states the branch-and-milestone-only coverage so a higher detail-page count is not read as a bug (D-14)
</success_criteria>

<output>
Create `.planning/phases/89-three-channel-drift-detection/89-04-SUMMARY.md` when done
</output>
