---
phase: 89-three-channel-drift-detection
plan: 03
type: execute
wave: 3
depends_on: ["89-01", "89-02"]
files_modified:
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx
  - taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts
  - taskflow/src/services/gitlab.ts
  - taskflow/src/services/gitlab.test.ts
autonomous: true
requirements: [DRIFT-01, DRIFT-02, DRIFT-03, DRIFT-04]

must_haves:
  truths:
    - "The release detail page fires three eager, independently-scoped channel queries on mount"
    - "Channel A uses a NEW project-scoped query key, so no stale page-capped cache entry can be served"
    - "Channel C is disabled when no release branch name can be derived (no matched milestone)"
    - "The Issues table's Wrong-milestone / Missing-MR data comes from the three-channel union, not a capped 100-MR fetch"
    - "`fetchRecentProjectMRs` and `buildWrongMilestoneMap` no longer exist anywhere in the codebase"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts"
      provides: "Channel A/B/C queries, driftRows, driftFlaggedCount, re-sourced matchedRows and wrongMilestoneByKey"
      contains: "gitlab-all-project-mrs"
  key_links:
    - from: "useReleaseDetail.ts"
      to: "driftDetection.buildDriftRows"
      via: "derived call over the three channel query results"
      pattern: "buildDriftRows"
    - from: "useReleaseDetail.ts"
      to: "fetchBranchTargetedMRs"
      via: "Channel C query keyed ['gitlab-branch-mrs', projectId, releaseBranchName]"
      pattern: "gitlab-branch-mrs"
---

<objective>
Wire the three discovery channels into `useReleaseDetail.ts` — the single data-layer hook — and delete the superseded page-capped heuristic in the same change, so the codebase never carries two competing sources of MR truth.

Purpose: D-05 traded zero-regression coexistence for one source of truth. The deletion has to land with the replacement, not after it.
Output: A hook exposing `driftRows`, `driftFlaggedCount` and re-sourced `matchedRows` / `wrongMilestoneByKey`; `fetchRecentProjectMRs` and `buildWrongMilestoneMap` removed along with their tests.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-three-channel-drift-detection/89-CONTEXT.md
@.planning/phases/89-three-channel-drift-detection/89-PATTERNS.md
@.planning/phases/89-three-channel-drift-detection/89-01-SUMMARY.md
@.planning/phases/89-three-channel-drift-detection/89-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the three channel queries to useReleaseDetail and derive the drift rows</name>
  <files>taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts, taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx</files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` (full file, 374 lines — the existing query shapes, the `enabled` guard conventions at lines 144-147, the `allProjectMilestones` project-scoped-key comment at lines 93-104, the CR-02 invalidation-granularity comment at lines 237-246, and the WR-10 never-`?? 0`-a-project-id lesson at lines 196-199)
    - `taskflow/src/routes/dashboard/release-detail/driftDetection.ts` (the exported contract from plan 89-02: `selectChannelA`, `buildDriftRows`, `countFlaggedMRs`, `buildIssueMrIndex`, `unionMRs`, `DriftRow`)
    - `taskflow/src/services/gitlab.ts` (the three new fetchers from plan 89-01 and their exact signatures)
    - `.planning/phases/89-three-channel-drift-detection/89-RESEARCH.md` § Common Pitfalls 4 and 5 (Channel A must be project-scoped, must not reuse the deleted query key)
  </read_first>

  <action>
    Add three query blocks to `useReleaseDetail.ts`, each modelled on the existing `milestoneMRs` query at lines 284-296 (same `staleTime: 5 * 60_000`, same `enabled`-guard style, eager on mount per D-16 — never lazy, never click-to-load).

    Channel A (DRIFT-01) — `fetchAllProjectMRs`, query key exactly `['gitlab-all-project-mrs', activeGitlabProject]`. Project-scoped only, NO `versionId` in the key: the fetch itself does not depend on which release is open, only the filter applied afterwards does (Pitfall 4; same reasoning as the `allProjectMilestones` comment already in this file). Do NOT reuse `['gitlab-recent-project-mrs', activeGitlabProject]` — a stale entry under the old key would serve page-capped data (Pitfall 5). Add a comment at the query citing D-16, Pitfall 4 and Pitfall 5.
    `enabled: !!gitlabBaseUrl && !!activeGitlabProject && !!gitlabToken`.

    Channel B (DRIFT-02) — the existing `milestoneMRs` query, unchanged. Do not alter its key `['gitlab-milestone-mrs', activeGitlabProject, gitlabMatch.candidateName]`; it is a cross-component cache contract.

    Channel C (DRIFT-03) — `fetchBranchTargetedMRs`, query key exactly `['gitlab-branch-mrs', activeGitlabProject, releaseBranchName]` (version-scoped through the derived branch name, matching the existing `gitlab-branch` query at line 136). Its `enabled` guard must include `releaseBranchName !== null`, copying the shape at lines 144-146 — this is D-18's degraded state: no matched milestone means no derivable branch name means nothing to query.

    Then derive, below the queries:
    - `const fixVersionIssueKeys = new Set(releaseIssues.map((i) => i.key));`
    - `const channelA = selectChannelA(allProjectMRs ?? [], fixVersionIssueKeys);`
    - `const driftRows = buildDriftRows({ channelA, channelB: milestoneMRs ?? [], channelC: branchTargetedMRs ?? [], releaseBranchName, matchedMilestoneId: matchedMilestone?.id ?? null, fixVersionIssueKeys });`
    - `const driftFlaggedCount = countFlaggedMRs(driftRows);`
    - `const isLoadingDrift` — true while any of the three channel queries is still fetching for the first time (use each `useQuery`'s `isLoading`), so the section can show a loading state instead of a false "No merge requests found".

    Add all of `driftRows`, `driftFlaggedCount`, `isLoadingDrift` and `hasMatchedMilestone` (`matchedMilestone !== null`) to the hook's returned object. Leave the existing `unmatchedMRs` return in place for now — plan 89-05 removes it together with its last consumer.

    Do not import or call any GitLab function outside `services/gitlab.ts` (D-19), and do not add a second hook — every query for this page lives here (P87 D-07).
  </action>

  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx tsc --noEmit && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "'gitlab-all-project-mrs'" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 1
    - `grep -c "'gitlab-branch-mrs'" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 1
    - `grep -c "activeGitlabProject, versionId\]" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 0 for the Channel A key — the key must be exactly two elements
    - The Channel C query's `enabled` expression contains `releaseBranchName !== null`
    - `grep -c 'buildDriftRows\|countFlaggedMRs\|selectChannelA' taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` is at least 3
    - The hook's returned object includes `driftRows`, `driftFlaggedCount`, `isLoadingDrift`, `hasMatchedMilestone`
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` exits 0 (the gitlab mock factory in that file must be extended with the new fetchers or the imports resolve to undefined)
  </acceptance_criteria>

  <done>All three channels fetch eagerly with correct scoping, and the hook exposes a sorted drift row list plus its flagged count.</done>
</task>

<task type="auto">
  <name>Task 2: Re-source the Issues table data from the union and delete buildWrongMilestoneMap</name>
  <files>taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts, taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts, taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx</files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` (current state after Task 1 — specifically lines 298-327: `matchIssuesToMRs`, the GGX-WARN-01 comment block, the `recentProjectMRs` query and the `wrongMilestoneByKey` derivation)
    - `taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts` lines 107-168 (`matchIssuesToMRs` stays; `buildWrongMilestoneMap` at 137-168 including its JSDoc is deleted)
    - `taskflow/src/routes/dashboard/release-detail/driftDetection.ts` (`buildIssueMrIndex` signature and return shape from plan 89-02)
    - `taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx` lines 12-24 (the consumer prop types that must keep compiling unchanged)
    - `taskflow/src/routes/dashboard/release-detail/releaseSummaries.test.ts` (check for any `buildWrongMilestoneMap` coverage before deleting)
  </read_first>

  <action>
    Replace the GGX-WARN-01 block in `useReleaseDetail.ts` (the `missingRows` derivation, the `recentProjectMRs` query and the `buildWrongMilestoneMap` call, roughly lines 303-327) with a single call to `buildIssueMrIndex(union, releaseIssues, matchedMilestone?.id ?? null)`, destructuring `matchedRows` and `wrongMilestoneByKey` from it. Reuse the same `unionMRs` result that `buildDriftRows` consumed rather than unioning twice — hoist `const union = unionMRs(channelA, milestoneMRs ?? [], branchTargetedMRs ?? [])` above both calls and pass it into `buildDriftRows` if plan 89-02's signature accepts a prebuilt union; if it does not, call `unionMRs` once and keep `buildDriftRows` taking the three arrays, accepting the second cheap union.

    Remove the now-dead `matchIssuesToMRs` call at line 301 and the `missingRows` variable, and drop `missingRows` from the hook's returned object (verified unused: no consumer destructures it). Keep `unmatchedMRs` in the return — plan 89-05 removes it. Source `unmatchedMRs` from the drift rows instead of the deleted `matchIssuesToMRs` call: the union MRs whose linked key is absent from the fix-version issue set.

    Delete the GGX-WARN-01 explanatory comment block (lines 303-308) — it documents an approach that no longer exists. Replace it with a short comment citing D-05: the three-channel union supersedes the capped recent-MR heuristic; MRs older than the latest 100 are no longer silently missed.

    Delete `buildWrongMilestoneMap` and its JSDoc entirely from `releaseSummaries.ts` (lines 137-168) and remove it from `useReleaseDetail.ts`'s import list. Remove `fetchRecentProjectMRs` from `useReleaseDetail.ts`'s import list. Keep `matchIssuesToMRs` — it is still called from `buildIssueMrIndex`.

    Update `useReleaseDetail.test.tsx`: remove `fetchRecentProjectMRs: vi.fn()` from the `vi.mock('@/services/gitlab', ...)` factory and the `vi.mocked(gitlab.fetchRecentProjectMRs).mockResolvedValue([])` line in `setupMocks`; add `fetchAllProjectMRs`, `fetchBranchTargetedMRs` to the factory with `mockResolvedValue([])` defaults in `setupMocks`. Add a test asserting Channel A's query fires with the key `['gitlab-all-project-mrs', 42]` and that `fetchBranchTargetedMRs` is NOT called when `releaseBranchName` is null.
  </action>

  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx tsc --noEmit && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx src/routes/dashboard/release-detail/releaseSummaries.test.ts src/routes/dashboard/release-detail/driftDetection.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rc 'buildWrongMilestoneMap' taskflow/src` returns 0 across every file
    - `grep -c 'GGX-WARN-01' taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 0
    - `grep -c 'buildIssueMrIndex' taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 1
    - `grep -c 'matchIssuesToMRs' taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts` is at least 1 — that function survives
    - `useReleaseDetail.test.tsx` no longer references `fetchRecentProjectMRs` and mocks `fetchAllProjectMRs` and `fetchBranchTargetedMRs`
    - A new test asserts `fetchBranchTargetedMRs` is not called when no milestone matched (Channel C disabled, D-18)
    - `npx tsc --noEmit` exits 0 and the three named test files pass
  </acceptance_criteria>

  <done>The Issues table's data comes from the three-channel union, and the wrong-milestone heuristic is gone from the codebase.</done>
</task>

<task type="auto">
  <name>Task 3: Delete fetchRecentProjectMRs and its test block</name>
  <files>taskflow/src/services/gitlab.ts, taskflow/src/services/gitlab.test.ts</files>

  <read_first>
    - `taskflow/src/services/gitlab.ts` lines 1532-1589 (`fetchRecentProjectMRs` and its JSDoc — the whole block is deleted)
    - `taskflow/src/services/gitlab.test.ts` lines 454-547 (`describe('fetchRecentProjectMRs (GGX-WARN-01)')` — deleted) and line 24 (its import — deleted)
    - The output of `grep -rn 'fetchRecentProjectMRs' taskflow/src` — run this first and confirm the only remaining references are the two files above
  </read_first>

  <action>
    Confirm with `grep -rn 'fetchRecentProjectMRs' taskflow/src` that after Task 2 the only remaining references are the definition in `gitlab.ts` and the test block plus import in `gitlab.test.ts`. If any other consumer appears, stop and report rather than deleting.

    Delete the entire `fetchRecentProjectMRs` function and its JSDoc block from `taskflow/src/services/gitlab.ts` (D-05). Do not leave a deprecation stub, a re-export, or a comment pointing at it — it must not be resurrectable as a fallback.

    Delete the `describe('fetchRecentProjectMRs (GGX-WARN-01)')` block from `taskflow/src/services/gitlab.test.ts` and remove `fetchRecentProjectMRs` from that file's import list at line 24. Do not delete or modify the neighbouring describe blocks.

    Run the full suite afterwards and confirm the "Consumers that must not regress" set is green: `useReleaseDetail.test.tsx`, `ReleaseDetailSidebar.test.tsx`, `releaseSummaries.test.ts`, `ReleasesTab.test.tsx`.
  </action>

  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && test "$(grep -rc 'fetchRecentProjectMRs' src | grep -v ':0' | wc -l | tr -d ' ')" = "0" && npx tsc --noEmit && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn 'fetchRecentProjectMRs' taskflow/src` produces no output
    - `grep -c 'GGX-WARN-01' taskflow/src/services/gitlab.test.ts` returns 0
    - `npx vitest run` (full suite) exits 0
    - `npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx src/routes/dashboard/release-detail/releaseSummaries.test.ts src/routes/dashboard/ReleasesTab.test.tsx` exits 0
    - `npm run check` reports no new errors beyond the 2-error `BacklogPage.tsx` / `BacklogRow.tsx` biome baseline
  </acceptance_criteria>

  <done>The page-capped fetch and its tests are gone; the full suite is green on the union-sourced data path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TanStack Query cache → hook consumers | Cached MR data may outlive a deploy; a key collision could serve data fetched under different semantics |
| Tauri app → GitLab REST API | Three new outbound authenticated GET requests fire eagerly on every release detail mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-09 | Tampering | Channel A query key colliding with the deleted `['gitlab-recent-project-mrs', ...]` entry | mitigate | Channel A uses a NEW key `['gitlab-all-project-mrs', activeGitlabProject]`; the old key is never referenced again after Task 3. Asserted by grep in Task 1 acceptance criteria (Pitfall 5). |
| T-89-10 | Denial of Service | Three eager fully-paginated fetches on every release detail mount (RESEARCH A4 — project-wide MR history could be large) | accept | D-16 is an explicit user decision choosing independent scoped queries over a shared fetch. `staleTime: 5 * 60_000` plus the project-scoped Channel A key means repeat navigations within 5 minutes reuse the cache. If the probe's total-MR count proves large, record it in the summary rather than silently capping — a cap would reintroduce the GGX-WARN-01 bug class. |
| T-89-11 | Elevation of Privilege | `activeGitlabProject ?? 0` reaching a URL and querying an unintended project | mitigate | Every new `queryFn` keeps `activeGitlabProject` in its `enabled` guard so the `?? 0` fallback is unreachable — the WR-10 pattern already documented at `useReleaseDetail.ts:196-199`. |
| T-89-12 | Information Disclosure | GitLab PAT in three additional request paths | mitigate | All three go through `apiFetch('gitlab', ...)`, which redacts credentials in devtools instrumentation and marks disconnect on 401 (D-19). No raw `fetch`. |
| T-89-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed in this phase. |
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` exits 0
- `cd taskflow && npx vitest run` (full suite) exits 0
- `grep -rn 'fetchRecentProjectMRs\|buildWrongMilestoneMap' taskflow/src` produces no output
- `cd taskflow && npm run check` shows no new errors beyond the 2-error biome baseline
</verification>

<success_criteria>
- Three channel queries fire eagerly and independently on mount (D-16), each fully paginated (D-17)
- Channel A is project-scoped under a new query key; Channel C is disabled when no branch name derives (D-18)
- `driftRows`, `driftFlaggedCount`, `isLoadingDrift`, `hasMatchedMilestone` are exposed by the hook
- The Issues table's `matchedRows` and `wrongMilestoneByKey` are re-sourced from the union with unchanged prop shapes (D-05, D-06)
- `fetchRecentProjectMRs` and `buildWrongMilestoneMap` are deleted, with no stub or fallback left behind
</success_criteria>

<output>
Create `.planning/phases/89-three-channel-drift-detection/89-03-SUMMARY.md` when done
</output>
