---
phase: 58
plan: 04
type: execute
wave: 2
depends_on: [58-02, 58-03]
files_modified:
  - .planning/phases/58-redesign-data-fetch-of-aio-cycle-detail-executions-list-and-/58-UAT.md
autonomous: false
requirements: [AIO58-01, AIO58-02, AIO58-03, AIO58-04]
tags: [aio, uat, cycle-detail]

must_haves:
  truths:
    - "Developer opens a real cycle (≥50 runs, ideally ≥200) and observes the progress bar appearing before the runs table"
    - "Network panel during initial load shows ≤2 AIO requests to populate the progress bar (jiraProjectId + cycle-summaries)"
    - "Defect resolution is visibly cache-deduped: opening the Defects tab shows N unique Jira lookups equal to the count of unique defect IDs across all runs (not N × runs)"
    - "No 401 error flash on first navigation to the page after a fresh app launch (credential gate verified)"
    - "Filter chips still toggle correctly, Pin/Unpin still works, breadcrumb still navigates back"
  artifacts:
    - path: ".planning/phases/58-redesign-data-fetch-of-aio-cycle-detail-executions-list-and-/58-UAT.md"
      provides: "Live UAT checklist with verdicts and any inline-fix notes"
      contains: "## UAT Verdict"
  key_links:
    - from: "58-UAT.md UAT Verdict"
      to: "Wave 2 closure (Phase 58 SHIPPED)"
      via: "Developer types 'approved'"
      pattern: "UAT_VERDICT: PASS"
---

<objective>
End-to-end human verification of the Phase 58 refactor against the live AIO instance. Captures perceived-performance gains (progress bar appears before runs table) that automated tests cannot measure, regressions in interactive behaviours (filter chips, Pin button, breadcrumb), and any cache-dedup observations that confirm Plan 02's defect-resolution refactor works as intended.

Purpose: Phase 53 shipped this page; Phase 58 redesigns its data fetch. Live UAT against a production-scale cycle (target: 200+ runs, multiple defects) is the only way to validate the perceived-performance improvements and confirm there are no regressions in the cycle detail interactive behaviours.

Output: `58-UAT.md` with check results and a `UAT_VERDICT:` line.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-redesign-data-fetch-of-aio-cycle-detail-executions-list-and-/58-RESEARCH.md
@.planning/phases/58-redesign-data-fetch-of-aio-cycle-detail-executions-list-and-/58-PROBE-FINDINGS.md
@.planning/phases/58-redesign-data-fetch-of-aio-cycle-detail-executions-list-and-/58-02-SUMMARY.md
@.planning/phases/58-redesign-data-fetch-of-aio-cycle-detail-executions-list-and-/58-03-SUMMARY.md
@taskflow/src/routes/dashboard/AioCycleDetailPage.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Live UAT against production AIO instance — record verdict in 58-UAT.md</name>
  <files>.planning/phases/58-redesign-data-fetch-of-aio-cycle-detail-executions-list-and-/58-UAT.md</files>
  <read_first>
    - .planning/phases/58-redesign-data-fetch-of-aio-cycle-detail-executions-list-and-/58-RESEARCH.md (Pitfalls and Common Pitfalls sections — verifier should mentally check each)
    - .planning/phases/58-redesign-data-fetch-of-aio-cycle-detail-executions-list-and-/58-02-SUMMARY.md (what Plan 02 actually shipped — Branch A vs B)
    - .planning/phases/58-redesign-data-fetch-of-aio-cycle-detail-executions-list-and-/58-03-SUMMARY.md (what Plan 03 actually shipped — including which CYCLE_NUMERIC_ID_DECISION branch was implemented)
  </read_first>
  <what-built>
    Plans 02 and 03 are now merged. The cycle detail page should:
    - Render the progress bar from the cycle summary endpoint (1 fast POST) instead of waiting on the full run enumeration
    - Pass raw jiraDefectIDs (stringified) to DefectRow, which resolves them via component-level useQuery (TanStack cache dedup)
    - Gate every useQuery on `!tokenLoading` (no 401 flash on first load)
    - Optionally fetch runs via a new fast endpoint (Branch B from Plan 02) instead of the legacy paginated path
  </what-built>
  <how-to-verify>
    Open Taskflow, navigate to a cycle in a real AIO project. Target cycle properties:
    - At least 50 runs (200+ ideal — that's where the old slow path was most painful)
    - At least 1 run with a defect link (Phase 56-05 cycles work well — find a cycle with FAIL runs that have linked Jira bugs)
    - Mix of statuses (Pass / Fail / Blocked / Not Run) so the progress bar exercises all colours
    - Both an "active" cycle and a closed cycle — try both

    For each check below, mark ✅ / ❌ / ⚠️ in 58-UAT.md and add notes for any ❌/⚠️.

    Performance & data-fetch behaviour:
    1. ✅ / ❌ Progress bar appears BEFORE the runs table. With DevTools Network throttling set to "Fast 3G", the progress bar should still appear within ~1–2s while the runs table takes longer.
    2. ✅ / ❌ Initial page load fires the right requests in this order (Network panel filter on `testcycle` and `issue`):
       - `GET /rest/api/2/project/{projectKey}` (or whatever `fetchJiraProjectNumericId` resolves to)
       - `GET /rest/aio-tcms-api/1.0/project/{projectKey}/testcycle/{cycleKey}/detail`
       - (If Branch RESOLVE-VIA-PAGED-ENDPOINT) `POST /rest/aio-tcms/1.0/project/{numId}/testcycle/paged?…`
       - `POST /rest/aio-tcms/1.0/project/{numId}/testcycle/summary/paged?…` (body is `[cycleNumericId]`)
       - Runs request (legacy `aio-tcms-api/1.0` if Branch A; new endpoint if Branch B)
    3. ✅ / ❌ Progress bar segment colours match status counts (visually compare to AioProjectOverviewPage's bar for the same cycle — they should match exactly).
    4. ✅ / ❌ Progress bar counts row shows In Progress (status 54) when the cycle has runs in that state. (NEW from Plan 03 step 4.)

    Defect resolution & cache dedup:
    5. ✅ / ❌ Click the Defects tab. For a cycle with K unique defect Jira keys across N runs, the Network panel shows exactly K calls to `/rest/api/2/issue/{numericId}` — NOT N. (Stronger check: if two FAIL runs share the same linked bug ID, only ONE Jira fetch fires.)
    6. ✅ / ❌ Defects tab Triggered-By column shows the test case keys correctly (e.g. `PROJ-TC-1, PROJ-TC-5`).
    7. ✅ / ❌ Clicking a defect key in the Defects tab navigates to `/issue/{key}` (the existing Jira issue detail route).

    Credential gate (no 401 flash):
    8. ✅ / ❌ Hard-reload the app (Cmd+R or full quit + relaunch). Navigate directly to the cycle detail URL. There must be NO red toast and NO 401 error banner during initial render.
    9. ✅ / ❌ Open Network panel BEFORE the page renders — confirm zero requests fire with `Authorization: Bearer null` or missing auth header.

    Regression checks (must still work):
    10. ✅ / ❌ Filter chips: clicking "Pass" hides PASS rows; ArrowRight on a focused chip moves focus to the next chip; Enter/Space toggles a chip.
    11. ✅ / ❌ Pin button: clicking "Pin" adds the cycle to the header tab strip; refresh the app; the pin persists. Click "Unpin" removes it.
    12. ✅ / ❌ Breadcrumb: navigating to this page from AioProjectOverviewPage shows a working back arrow + clickable parent breadcrumb segment.
    13. ✅ / ❌ Row click: clicking a run row navigates to `/aio-cycle/{projectKey}/{cycleKey}/run/{runId}`; keyboard Enter on a focused row does the same; breadcrumb pushes the cycle name.
    14. ✅ / ❌ Empty state: navigating to a cycle with zero runs shows the "No test runs found" empty state, not a crash.

    Graceful degradation:
    15. ✅ / ❌ In DevTools, set the summary request to "Block request URL" temporarily and reload. The progress bar should still render — falling back to counts derived from the runs query (Plan 03 step 4 graceful-degradation path).

    Create `58-UAT.md` with the 15 rows above as a markdown table:

    | # | Check | Verdict | Notes |
    |---|-------|---------|-------|
    | 1 | Progress bar appears before runs table | ✅/❌/⚠️ | … |
    | … | … | … | … |

    Final lines:

    ```
    ## UAT Verdict

    UAT_VERDICT: <PASS | PASS-WITH-CONCERNS | FAIL>
    Cycle tested: <PROJ-CY-X (N runs, K defects)>
    Closed cycle tested: <PROJ-CY-Y (N runs)>
    Date: <ISO date>
    ```
  </how-to-verify>
  <action>
    Developer runs the 15 live checks above against the production AIO instance, records verdicts in `58-UAT.md`, and writes the `## UAT Verdict` block at the bottom. No source code is modified in this task. If any check fails, do NOT close this checkpoint — file gap-closure plan 58-05 instead.
  </action>
  <verify>
    <automated>grep -E '^UAT_VERDICT: (PASS|PASS-WITH-CONCERNS|FAIL)$' .planning/phases/58-redesign-data-fetch-of-aio-cycle-detail-executions-list-and-/58-UAT.md</automated>
    <human-check>All 15 check rows have a ✅/❌/⚠️ mark; the UAT_VERDICT line matches the worst row's outcome (any ❌ → FAIL).</human-check>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/58-redesign-data-fetch-of-aio-cycle-detail-executions-list-and-/58-UAT.md` exists
    - File contains all 15 check rows
    - File ends with a `## UAT Verdict` heading
    - File contains the literal pattern `UAT_VERDICT: PASS` or `UAT_VERDICT: PASS-WITH-CONCERNS`
    - `grep -E '^UAT_VERDICT: (PASS|PASS-WITH-CONCERNS|FAIL)$' 58-UAT.md` exits 0
    - If `UAT_VERDICT: FAIL`: do NOT close this checkpoint — file a gap-closure plan (58-05) instead
    - File is committed to git
  </acceptance_criteria>
  <done>58-UAT.md committed with verdict PASS or PASS-WITH-CONCERNS, all 15 check rows populated, developer typed "approved".</done>
  <resume-signal>Type "approved" when 58-UAT.md shows PASS or PASS-WITH-CONCERNS; type "fail" with a one-paragraph reason if any ❌ is observed and gap-closure is needed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → live AIO instance | UAT exercises authenticated requests against production data |
| browser DevTools → developer eyes | Verdict relies on developer observation accuracy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04-01 | Information Disclosure | 58-UAT.md captures real cycle keys / defect IDs | mitigate | Use project-scoped IDs only (e.g. `PROJ-CY-2`, `PROJ-1234`); do not paste run summaries / bug titles verbatim |
| T-58-04-02 | Repudiation | UAT verdict signed only by developer eyes | accept | Solo developer workflow; verdict file commits to git with a timestamp — sufficient audit trail |
| T-58-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
Wave 2 is closed when:
- 58-UAT.md exists, committed, with verdict PASS or PASS-WITH-CONCERNS
- All 15 check rows have a verdict mark
- Developer types "approved"
</verification>

<success_criteria>
- Live UAT verdict committed to 58-UAT.md
- Phase 58 ROADMAP entry can be marked complete after verdict is PASS
</success_criteria>

<output>
Create `.planning/phases/58-redesign-data-fetch-of-aio-cycle-detail-executions-list-and-/58-04-SUMMARY.md` when done.
</output>
