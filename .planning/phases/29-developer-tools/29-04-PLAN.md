---
phase: 29-developer-tools
plan: 04
type: execute
wave: 1
depends_on: ["29-03"]
files_modified:
  - taskflow/src/main.tsx
  - taskflow/src-tauri/src/lib.rs
autonomous: true
gap_closure: true
requirements:
  - DEVT-01
  - DEVT-02
  - DEVT-03
  - DEVT-04
  - DEVT-05

must_haves:
  truths:
    - "Native menu 'Developer Tools' item navigates to /dev-tools (not /debug-logs)"
    - "No references to 'debug-logs' remain in main.tsx or lib.rs menu handling"
  artifacts:
    - path: "taskflow/src/main.tsx"
      provides: "Frontend listener for native menu navigation to /dev-tools"
      contains: "menu-dev-tools"
    - path: "taskflow/src-tauri/src/lib.rs"
      provides: "Native menu item with correct ID and label"
      contains: "menu-dev-tools"
  key_links:
    - from: "taskflow/src-tauri/src/lib.rs"
      to: "taskflow/src/main.tsx"
      via: "Tauri event emit/listen with ID 'menu-dev-tools'"
      pattern: "menu-dev-tools"
---

<objective>
Fix stale native menu listener that still points at the removed `/debug-logs` route.

Purpose: Close the single verification gap blocking full DEVT-04 satisfaction. The Tauri native "Debug" menu and its frontend listener were not updated when Plan 03 replaced `/debug-logs` with `/dev-tools`.
Output: Both the Rust menu definition and the TypeScript listener use `menu-dev-tools` pointing at `/dev-tools`.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-developer-tools/29-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Tauri native menu item and event handler in lib.rs</name>
  <files>taskflow/src-tauri/src/lib.rs</files>
  <read_first>taskflow/src-tauri/src/lib.rs</read_first>
  <action>
In `taskflow/src-tauri/src/lib.rs`, make three changes:

1. **Line 105-106** — Rename the menu item label and ID:
   - Change `MenuItemBuilder::new("Debug Logs")` to `MenuItemBuilder::new("Developer Tools")`
   - Change `.id("menu-debug-logs")` to `.id("menu-dev-tools")`

2. **Line 108-110** — Rename the submenu:
   - Change `"Debug"` to `"Dev Tools"` in the `Submenu::with_items` call
   - Rename the variable from `debug_logs_item` to `dev_tools_item` and update `&[&debug_logs_item]` to `&[&dev_tools_item]`

3. **Line 158** — Update the match arm in `on_menu_event`:
   - Change `| "menu-debug-logs" =>` to `| "menu-dev-tools" =>`

Leave DebugMenuState struct name and toggle_debug_menu command name unchanged — they are internal Rust identifiers with no user-facing impact and the frontend already calls `toggle_debug_menu` correctly.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && grep -c "menu-dev-tools" src-tauri/src/lib.rs && grep -c "menu-debug-logs" src-tauri/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "menu-dev-tools" src-tauri/src/lib.rs` returns 2 matches (MenuItemBuilder .id and match arm)
    - `grep "menu-debug-logs" src-tauri/src/lib.rs` returns 0 matches
    - `grep "Developer Tools" src-tauri/src/lib.rs` returns 1 match (menu item label)
    - `grep '"Dev Tools"' src-tauri/src/lib.rs` returns 1 match (submenu label)
  </acceptance_criteria>
  <done>Tauri native menu emits "menu-dev-tools" event with "Developer Tools" label. No references to "menu-debug-logs" or "Debug Logs" remain in lib.rs.</done>
</task>

<task type="auto">
  <name>Task 2: Update frontend listener in main.tsx to navigate to /dev-tools</name>
  <files>taskflow/src/main.tsx</files>
  <read_first>taskflow/src/main.tsx</read_first>
  <action>
In `taskflow/src/main.tsx`, line 196:

Change:
```
listen('menu-debug-logs', () => navigate('/debug-logs')),
```

To:
```
listen('menu-dev-tools', () => navigate('/dev-tools')),
```

This is the only change needed in this file. The `toggle_debug_menu` invoke on line 207 and `devToolsEnabled` subscription on line 187 are already correct.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && grep -c "menu-dev-tools" src/main.tsx && grep -c "menu-debug-logs" src/main.tsx && grep -c "debug-logs" src/main.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "menu-dev-tools" src/main.tsx` returns 1 match (the listen call)
    - `grep "menu-debug-logs" src/main.tsx` returns 0 matches
    - `grep "debug-logs" src/main.tsx` returns 0 matches
    - `grep "/dev-tools" src/main.tsx` returns 1 match (navigate target)
  </acceptance_criteria>
  <done>Frontend listener receives "menu-dev-tools" event and navigates to /dev-tools. No references to "debug-logs" remain in main.tsx.</done>
</task>

</tasks>

<verification>
1. `grep -r "menu-debug-logs" taskflow/src/ taskflow/src-tauri/` returns zero results
2. `grep -r "debug-logs" taskflow/src/main.tsx` returns zero results
3. `grep "menu-dev-tools" taskflow/src/main.tsx taskflow/src-tauri/src/lib.rs` returns matches in both files
4. `cd taskflow && npx vitest run` — all existing tests still pass (no regressions)
</verification>

<success_criteria>
- The stale `menu-debug-logs` listener is replaced with `menu-dev-tools` navigating to `/dev-tools`
- The Tauri native menu item matches: label "Developer Tools", ID "menu-dev-tools"
- Zero references to `menu-debug-logs` or `/debug-logs` remain in main.tsx or lib.rs
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/29-developer-tools/29-04-SUMMARY.md`
</output>
