---
phase: 66-roles-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/components/app/sidebar-items.ts
  - taskflow/src/components/app/sidebar-items.test.ts
  - taskflow/src/stores/settings.store.ts
autonomous: true
requirements: [ROLES-04, ROLES-06]
must_haves:
  truths:
    - "getDefaultSidebarItems() takes no arguments and returns all 9 nav items with visible: true"
    - "DEV_SIDEBAR_PRESET and PM_SIDEBAR_PRESET no longer exist"
    - "useSettingsStore has no role field, no setRole action, no applyPreset action"
    - "useSettingsStore is at persist version 22 with a migration that drops role and resets sidebarItems to all-visible"
    - "tsc compiles with no errors after the store + sidebar-items changes"
  artifacts:
    - path: "taskflow/src/components/app/sidebar-items.ts"
      provides: "No-arg getDefaultSidebarItems all-visible default"
      contains: "export function getDefaultSidebarItems(): SidebarItem[]"
    - path: "taskflow/src/stores/settings.store.ts"
      provides: "Role-free settings store at version 22"
      contains: "version: 22"
  key_links:
    - from: "taskflow/src/stores/settings.store.ts"
      to: "taskflow/src/components/app/sidebar-items.ts"
      via: "getDefaultSidebarItems() no-arg call in initial state, v9 block, and v22 block"
      pattern: "getDefaultSidebarItems\\(\\)"
---

<objective>
Simplify the sidebar-items data source and the settings store so the Developer/PM role concept is structurally removed at its foundation: `getDefaultSidebarItems` becomes a no-arg all-visible function, the preset constants are deleted, and `useSettingsStore` drops `role`/`setRole`/`applyPreset` with a v22 migration that resets every user to all-visible.

Purpose: This is the foundation wave. Every UI file and test that references role symbols (Wave 2) depends on these signature and store-shape changes being in place first. Doing the data layer first means Wave 2's deletions of UI callers compile cleanly.

Output: Updated `sidebar-items.ts` (no-arg function, no preset exports), updated `sidebar-items.test.ts` (all-visible test replaces preset tests), updated `settings.store.ts` (role-free interface + initial state, v22 migration, version bump, fixed v9 block).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-roles-removal/66-CONTEXT.md
@.planning/phases/66-roles-removal/66-RESEARCH.md
@.planning/phases/66-roles-removal/66-PATTERNS.md

<interfaces>
<!-- Current contracts the executor must change. Extracted from codebase. -->

From taskflow/src/components/app/sidebar-items.ts (CURRENT — to be changed):
- `export function getDefaultSidebarItems(preset: 'dev' | 'pm'): SidebarItem[]` (lines 93-123) — uses devVisible/pmVisible Sets
- `export const DEV_SIDEBAR_PRESET = getDefaultSidebarItems('dev')` (line 126)
- `export const PM_SIDEBAR_PRESET = getDefaultSidebarItems('pm')` (line 127)
- `export const SIDEBAR_NAV_ITEMS: SidebarNavDef[]` — 9 items: dashboard, my-tasks, sprint-board, backlog, epics, merge-requests, sprint-progress, releases, worklogs, aio-projects (NOTE: that is 10 ids listed but the array has these entries; executor must confirm SIDEBAR_NAV_ITEMS.length at runtime — the test asserts against SIDEBAR_NAV_ITEMS.length, not a hardcoded number)
- `export interface SidebarItem { id: string; visible: boolean }`

From taskflow/src/stores/settings.store.ts (CURRENT — to be changed):
- interface `SettingsState` line 25: `role: 'developer' | 'pm' | 'tech-lead' | null;`
- interface line 146: `setRole: (role: 'developer' | 'pm' | 'tech-lead') => void;`
- interface line 161 doc comment: `/** Sidebar item visibility and order. Default: DEV_SIDEBAR_PRESET. */`
- interface line 166: `applyPreset: (preset: 'dev' | 'pm') => void;`
- initial state line 182: `role: null,`
- action line 294: `setRole: (role) => set({ role }),`
- initial state line 309: `sidebarItems: getDefaultSidebarItems('dev'),`
- action lines 324-327: `applyPreset: (preset) => set({ sidebarItems: getDefaultSidebarItems(preset) }),`
- persist version line 332: `version: 21,`
- v9 migration block lines 375-379: reads `s.role`, computes preset, calls `getDefaultSidebarItems(preset)`
- v21 migration block lines 420-423 (last existing block before `return persisted as SettingsState`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify getDefaultSidebarItems to no-arg all-visible and remove preset constants</name>
  <files>taskflow/src/components/app/sidebar-items.ts</files>
  <read_first>
    - taskflow/src/components/app/sidebar-items.ts (the file being modified — see current getDefaultSidebarItems at lines 93-123 and the DEV_SIDEBAR_PRESET/PM_SIDEBAR_PRESET exports at lines 126-127)
    - .planning/phases/66-roles-removal/66-PATTERNS.md (section "taskflow/src/components/app/sidebar-items.ts" — exact before/after target)
  </read_first>
  <action>
    Implements ROLES-06 (D-12, D-13). Change `getDefaultSidebarItems` to take no parameters and return `SIDEBAR_NAV_ITEMS.map((item) => ({ id: item.id, visible: true }))`. Remove the `preset: 'dev' | 'pm'` parameter, the `devVisible` Set, the `pmVisible` Set, and the `visibleSet` conditional. Update the JSDoc above the function to describe it as returning all nav items visible by default (remove the preset description). Delete the two exported constants `DEV_SIDEBAR_PRESET` and `PM_SIDEBAR_PRESET` (lines 125-127 including the `/** Pre-computed preset constants for tests and comparisons. */` comment). Do NOT change `SIDEBAR_NAV_ITEMS`, `SIDEBAR_SECTIONS`, `SidebarNavDef`, or `SidebarItem` — those stay exactly as-is.
  </action>
  <verify>
    <automated>cd taskflow && grep -c "getDefaultSidebarItems(preset" src/components/app/sidebar-items.ts | grep -q '^0$' && grep -c "DEV_SIDEBAR_PRESET\|PM_SIDEBAR_PRESET" src/components/app/sidebar-items.ts | grep -q '^0$' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `sidebar-items.ts` contains `export function getDefaultSidebarItems(): SidebarItem[]` (no parameter)
    - `sidebar-items.ts` contains no occurrence of `DEV_SIDEBAR_PRESET` or `PM_SIDEBAR_PRESET`
    - `sidebar-items.ts` contains no occurrence of `devVisible`, `pmVisible`, or `visibleSet`
    - The function body returns `SIDEBAR_NAV_ITEMS.map(...)` with `visible: true` for every item
    - `SIDEBAR_NAV_ITEMS` array is unchanged
  </acceptance_criteria>
  <done>getDefaultSidebarItems takes no arguments and returns all items visible; preset constants removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update sidebar-items.test.ts to assert all-visible default</name>
  <files>taskflow/src/components/app/sidebar-items.test.ts</files>
  <read_first>
    - taskflow/src/components/app/sidebar-items.test.ts (the file being modified — note the two preset tests calling getDefaultSidebarItems('pm') and getDefaultSidebarItems('dev') around lines 29-39, and the Phase 59 workload-absence guard tests at lines 19-28 and 41-51)
    - taskflow/src/components/app/sidebar-items.ts (the just-changed source — confirm exported names: getDefaultSidebarItems, SIDEBAR_NAV_ITEMS)
    - .planning/phases/66-roles-removal/66-RESEARCH.md (Pitfall 1 and the "Updated sidebar-items.test.ts" code example)
  </read_first>
  <behavior>
    - Test: getDefaultSidebarItems() returns an array of length SIDEBAR_NAV_ITEMS.length
    - Test: every returned item has visible === true
    - Test: returned item ids equal SIDEBAR_NAV_ITEMS ids in order
    - Preserved: the existing "workload absence guard" tests must continue to pass unchanged
  </behavior>
  <action>
    Replace the two preset-specific `it(...)` blocks that call `getDefaultSidebarItems('pm')` and `getDefaultSidebarItems('dev')` with a single `it('getDefaultSidebarItems returns all SIDEBAR_NAV_ITEMS with visible: true', ...)` test that asserts: `getDefaultSidebarItems()` has length `SIDEBAR_NAV_ITEMS.length`, every item's `visible` is `true`, and the mapped ids equal `SIDEBAR_NAV_ITEMS.map((i) => i.id)`. Do NOT alter the surrounding `describe` block or the Phase 59 workload-absence guard tests — those reference `workload` (which is not in SIDEBAR_NAV_ITEMS) and remain valid. Remove any now-unused preset-only imports if present (the file imports getDefaultSidebarItems and SIDEBAR_NAV_ITEMS — keep both).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/components/app/sidebar-items.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/app/sidebar-items.test.ts` exits 0
    - `sidebar-items.test.ts` contains no call to `getDefaultSidebarItems('dev')` or `getDefaultSidebarItems('pm')`
    - `sidebar-items.test.ts` contains an assertion `.every((item) => item.visible)` or equivalent proving all items visible
    - The workload-absence guard test(s) still execute (grep for `workload` still returns matches in the file)
  </acceptance_criteria>
  <done>sidebar-items.test.ts passes with the all-visible test; no preset-arg calls remain.</done>
</task>

<task type="auto">
  <name>Task 3: Strip role/setRole/applyPreset from settings store, bump to v22, add migration</name>
  <files>taskflow/src/stores/settings.store.ts</files>
  <read_first>
    - taskflow/src/stores/settings.store.ts (the file being modified — interface lines 24-167, initial state + actions lines 181-328, persist options lines 329-427 including version line 332, v9 block lines 375-379, v21 block lines 420-423)
    - taskflow/src/components/app/sidebar-items.ts (the just-changed source — confirm getDefaultSidebarItems now takes no args)
    - .planning/phases/66-roles-removal/66-PATTERNS.md (section "taskflow/src/stores/settings.store.ts" — enumerates all 7 edits with exact lines)
    - .planning/phases/66-roles-removal/66-RESEARCH.md (Pitfall 2 — v9 block fix; "v22 Migration Block" code example)
  </read_first>
  <action>
    Implements ROLES-04 (D-08, D-09, D-10) and the call-site half of ROLES-06 (D-14). Make exactly these edits:
    (1) Delete the `role` field from the `SettingsState` interface (line 25: `role: 'developer' | 'pm' | 'tech-lead' | null;`).
    (2) Delete the `setRole` declaration from the interface (line 146: `setRole: (role: 'developer' | 'pm' | 'tech-lead') => void;`).
    (3) Delete the `applyPreset` declaration from the interface (line 166: `applyPreset: (preset: 'dev' | 'pm') => void;`).
    (4) Update the interface doc comment on line 161 from `Default: DEV_SIDEBAR_PRESET.` to describe the default as all items visible (e.g. `Default: all items visible.`).
    (5) Delete `role: null,` from the initial state (line 182).
    (6) Delete the `setRole: (role) => set({ role }),` action (line 294).
    (7) Change the initial state call site from `sidebarItems: getDefaultSidebarItems('dev'),` to `sidebarItems: getDefaultSidebarItems(),` (line 309).
    (8) Delete the entire `applyPreset` action (lines 324-327).
    (9) Fix the v9 migration block (lines 375-379) — replace the `const role`/`const preset`/`getDefaultSidebarItems(preset)` body with `s.sidebarItems = getDefaultSidebarItems();` (no-arg). Keep the `if (version < 9) {` guard. This is the only edit to a historic block and it is required to keep TypeScript compiling after the signature change (per Pitfall 2; v22 resets sidebarItems anyway).
    (10) Bump the persist version on line 332 from `version: 21,` to `version: 22,`.
    (11) Add a new migration block immediately after the existing `if (version < 21) { ... }` block (after line 423, before `return persisted as SettingsState`): `if (version < 22) { delete (s as Record<string, unknown>).role; s.sidebarItems = getDefaultSidebarItems(); }`.
    Do NOT alter any other migration block (v1 through v8, v10 through v21) and do NOT remove the `import { getDefaultSidebarItems }` line — it is still used.
  </action>
  <verify>
    <automated>cd taskflow && grep -c "applyPreset\|setRole" src/stores/settings.store.ts | grep -q '^0$' && grep -q "version: 22," src/stores/settings.store.ts && grep -q "if (version < 22)" src/stores/settings.store.ts && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `settings.store.ts` contains no occurrence of `setRole` or `applyPreset`
    - `settings.store.ts` contains no `role:` field in the interface or initial state (grep `role:` returns no SettingsState field — the historic v9 block no longer reads `s.role`)
    - `settings.store.ts` contains `version: 22,`
    - `settings.store.ts` contains the block `if (version < 22)` that calls `delete (s as Record<string, unknown>).role` and `getDefaultSidebarItems()`
    - The v9 block calls `getDefaultSidebarItems()` with no argument
    - `npx tsc --noEmit` reports no errors originating from settings.store.ts or sidebar-items.ts
  </acceptance_criteria>
  <done>Settings store is role-free, at version 22, with a v22 migration that drops role and resets sidebarItems; tsc compiles clean for the changed files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted settings.json → in-memory store | The Zustand persist `migrate` function reads previously-persisted data and reshapes it on app start. This is the only data-crossing point in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Tampering | settings.store.ts v22 migration | accept | Migration only deletes the `role` key and resets `sidebarItems` to a code-defined all-visible array; no user input crosses the boundary, no external data is trusted. Local-only desktop preference store. |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. RESEARCH.md confirms zero new dependencies. |

Note (per security_context): This phase performs no authentication, session management, or access-control changes. The `role` field was a UI preference only — it never gated any API call, data access, or privileged operation (confirmed in RESEARCH.md Security Domain). ASVS L1 categories are Not Applicable to this deletion-only plan.
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` — no errors
- `cd taskflow && npx vitest run src/components/app/sidebar-items.test.ts` — exits 0
- `grep -rn "DEV_SIDEBAR_PRESET\|PM_SIDEBAR_PRESET\|applyPreset\|setRole" taskflow/src/components/app/sidebar-items.ts taskflow/src/stores/settings.store.ts` — no matches
</verification>

<success_criteria>
- `getDefaultSidebarItems()` is a no-arg function returning all SIDEBAR_NAV_ITEMS with `visible: true` (ROLES-06 data layer)
- `DEV_SIDEBAR_PRESET` and `PM_SIDEBAR_PRESET` no longer exist
- `useSettingsStore` has no `role` field, no `setRole`, no `applyPreset`, and is at persist version 22 (ROLES-04)
- The v22 migration drops `role` and resets `sidebarItems` to all-visible; the v9 historic block compiles with the no-arg signature
- `tsc --noEmit` passes for the two changed files (downstream UI files in Wave 2 may still reference removed symbols until Plan 02 runs — that is expected and isolated to Wave 2 files)
</success_criteria>

<output>
Create `.planning/phases/66-roles-removal/66-01-SUMMARY.md` when done.
</output>
