---
phase: 65-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/aio/cycles.ts
  - taskflow/src/lib/aioUtils.ts
  - taskflow/src/lib/aioUtils.test.ts
  - taskflow/src/routes/dashboard/AioCycleDetailPage.tsx
  - taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx
autonomous: true
requirements: [CLEAN-06, CLEAN-07]
must_haves:
  truths:
    - "An AIO in-progress run (testRunStatusID 52) renders as IN_PROGRESS in the Executions tab, not NOT_EXECUTED"
    - "The AIO status map is built at runtime from the live /config endpoint when AIO activates; there is no hardcoded ID→status constant"
    - "When /config is unavailable or fails, normalizeStatusById returns 'notRun' for every ID — no crash, no hardcoded entries"
    - "AioCycleDetailPage resolves status IDs via normalizeStatusById, not via a static AIO_STATUS_MAP import"
  artifacts:
    - path: "taskflow/src/services/aio/cycles.ts"
      provides: "TESTCASE_STATUS_MAP with IDs 51 (NOT_EXECUTED) and 52 (IN_PROGRESS)"
      contains: "52: 'IN_PROGRESS'"
    - path: "taskflow/src/lib/aioUtils.ts"
      provides: "Runtime AIO status map cache + initializeAioStatusMap init function; no static AIO_STATUS_MAP"
      contains: "export async function initializeAioStatusMap"
    - path: "taskflow/src/lib/aioUtils.test.ts"
      provides: "Tests for initializeAioStatusMap + normalizeStatusById against runtime map (mocked fetch)"
      contains: "initializeAioStatusMap"
    - path: "taskflow/src/routes/dashboard/AioCycleDetailPage.tsx"
      provides: "Status resolution via normalizeStatusById"
      contains: "normalizeStatusById(Number(idStr))"
  key_links:
    - from: "taskflow/src/lib/aioUtils.ts"
      to: "taskflow/src/services/aio/cycles.ts"
      via: "import { fetchAioProjectConfig }"
      pattern: "fetchAioProjectConfig"
    - from: "taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx"
      to: "taskflow/src/lib/aioUtils.ts"
      via: "initializeAioStatusMap side-effect on configQuery.data"
      pattern: "initializeAioStatusMap"
    - from: "taskflow/src/routes/dashboard/AioCycleDetailPage.tsx"
      to: "taskflow/src/lib/aioUtils.ts"
      via: "normalizeStatusById import + call"
      pattern: "normalizeStatusById"
---

<objective>
Fix AIO test-execution status rendering. CLEAN-06 adds the missing 51/52 entries to the test-case status map so in-progress runs stop bucketing as NOT_EXECUTED. CLEAN-07 removes the hardcoded `AIO_STATUS_MAP` constant entirely and replaces it with a runtime map built from the live AIO `/config` endpoint at integration-activation time, so non-standard AIO instances that use different numeric IDs render correctly.

Purpose: Hardcoded status IDs are brittle across AIO plugin versions/instances; `statusType` strings are stable. CLEAN-06 makes existing fallback behavior explicit and correct for ID 52; CLEAN-07 makes the whole map version-independent.
Output: `TESTCASE_STATUS_MAP` with 51/52 entries; a dynamic `initializeAioStatusMap` + module cache in `aioUtils.ts`; migrated consumer in `AioCycleDetailPage.tsx`; an activation call site in `AioProjectOverviewPage.tsx`; updated `aioUtils.test.ts`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-tech-debt-cleanup/65-CONTEXT.md
@.planning/phases/65-tech-debt-cleanup/65-RESEARCH.md
@.planning/phases/65-tech-debt-cleanup/65-PATTERNS.md

<interfaces>
<!-- Current state extracted from codebase. -->

cycles.ts line 335 — TESTCASE_STATUS_MAP (file-private, no export):
```
const TESTCASE_STATUS_MAP: Record<number, string> = {
  53: 'PASS', 901: 'PASS', 54: 'FAIL', 55: 'BLOCKED',
};
// chipStatusFromId (line 342) falls back to 'NOT_EXECUTED' for unknown IDs
```

cycles.ts line 435 — fetch function to REUSE (already exported; do not duplicate):
```
export async function fetchAioProjectConfig(
  baseUrl: string, token: string, jiraProjectId: number,
): Promise<AioTestRunStatusConfig[]>
// returns [] on 404, throws ApiError on 401, throws Error otherwise
```

aioUtils.ts lines 55-74 — static map (to REMOVE) + normalizeStatusById (to rewire):
```
export const AIO_STATUS_MAP: Record<number, 'pass'|'fail'|'blocked'|'notRun'|'inProgress'> = {
  51:'notRun', 52:'inProgress', 53:'pass', 54:'fail', 55:'blocked', 901:'pass',
};
export function normalizeStatusById(id: number): ... { return AIO_STATUS_MAP[id] ?? 'notRun'; }
```

AioCycleDetailPage.tsx line 26 import + line 454 usage (CONSUMER — must migrate):
```
import { AIO_STATUS_MAP, normalizeStatus, normalizeStatusLabel } from '@/lib/aioUtils';
const statusKey = AIO_STATUS_MAP[Number(idStr)] ?? 'notRun';
```

AioProjectOverviewPage.tsx — activation call site (already has useEffect imported line 3; jiraBaseUrl, token, jiraProjectId, configQuery all in scope around lines 267-301; uses its OWN local buildStatusMap — does NOT import AIO_STATUS_MAP):
```
const configQuery = useQuery({ queryKey: ['aio', jiraBaseUrl, 'project-config', projectKey],
  queryFn: () => fetchAioProjectConfig(jiraBaseUrl!, token!, jiraProjectId!), ... });
const statusMap = useMemo(() => buildStatusMap(configQuery.data ?? []), [configQuery.data]);
```

types.ts line 213 — AioTestRunStatusConfig (has ID + statusType fields):
```
export interface AioTestRunStatusConfig { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NOT_EXECUTED (51) + IN_PROGRESS (52) to TESTCASE_STATUS_MAP (CLEAN-06)</name>
  <files>taskflow/src/services/aio/cycles.ts</files>
  <read_first>
    - taskflow/src/services/aio/cycles.ts (lines 335-345 — the `TESTCASE_STATUS_MAP` constant and the `chipStatusFromId` consumer at line 342)
    - .planning/phases/65-tech-debt-cleanup/65-PATTERNS.md (CLEAN-06 section — before/after map; Pitfall 6 distinguishing TESTCASE_STATUS_MAP from AIO_STATUS_MAP)
  </read_first>
  <action>
    Add two entries to the file-private `TESTCASE_STATUS_MAP` (`Record<number, string>`) at line 335 of `cycles.ts`: `51: 'NOT_EXECUTED'` and `52: 'IN_PROGRESS'`. Place them at the top of the object so the final map is `{ 51: 'NOT_EXECUTED', 52: 'IN_PROGRESS', 53: 'PASS', 901: 'PASS', 54: 'FAIL', 55: 'BLOCKED' }`. These are RAW string labels matching `chipStatusFromId`'s return convention (NOT the canonical lowercase keys used in `aioUtils.ts`). Before this change, ID 52 fell through `chipStatusFromId`'s `?? 'NOT_EXECUTED'` fallback (line 343) and rendered as NOT_EXECUTED; adding `52: 'IN_PROGRESS'` makes in-progress runs render correctly. Do not change `chipStatusFromId` or any other code in the file.
  </action>
  <verify>
    <automated>cd taskflow && grep -n "51: 'NOT_EXECUTED'\|52: 'IN_PROGRESS'" src/services/aio/cycles.ts && npm test -- cycles 2>/dev/null; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "52: 'IN_PROGRESS'" taskflow/src/services/aio/cycles.ts` returns 1
    - `grep -c "51: 'NOT_EXECUTED'" taskflow/src/services/aio/cycles.ts` returns 1
    - `chipStatusFromId` body is unchanged (still `TESTCASE_STATUS_MAP[id ?? 0] ?? 'NOT_EXECUTED'`)
    - `cd taskflow && npm run build` exits 0
  </acceptance_criteria>
  <done>`TESTCASE_STATUS_MAP` contains IDs 51 and 52 with correct raw labels; testRunStatusID 52 resolves to IN_PROGRESS via `chipStatusFromId`; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace static AIO_STATUS_MAP with runtime initializeAioStatusMap + update tests (CLEAN-07)</name>
  <files>taskflow/src/lib/aioUtils.ts, taskflow/src/lib/aioUtils.test.ts</files>
  <read_first>
    - taskflow/src/lib/aioUtils.ts (the full file — lines 50-74 hold the static `AIO_STATUS_MAP` and `normalizeStatusById`)
    - taskflow/src/lib/aioUtils.test.ts (lines 1-7 imports, 63-87 `AIO_STATUS_MAP` describe block, 89-121 `normalizeStatusById` describe block — both will break and must be rewritten)
    - taskflow/src/services/aio/cycles.ts (line 435 `fetchAioProjectConfig` signature — the function to reuse, do not duplicate)
    - taskflow/src/services/aio/types.ts (line 213 `AioTestRunStatusConfig` — fields `ID` and `statusType`)
    - taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx (lines 32-41 `STATUS_TYPE_MAP` + `buildStatusMap` — the analog pattern to mirror)
    - .planning/phases/65-tech-debt-cleanup/65-PATTERNS.md (aioUtils.ts CLEAN-07 section + aioUtils.test.ts CLEAN-07 section)
    - .planning/phases/65-tech-debt-cleanup/65-RESEARCH.md (Pattern 4, Pitfall 2 — test rewrite guidance; D-02 through D-06)
  </read_first>
  <behavior>
    - After `initializeAioStatusMap` succeeds with a config containing IDs 53→PASSED, 54→FAILED, 55→BLOCKED, 51→NOT_RUN, 52→IN_PROGRESS, `normalizeStatusById` returns 'pass', 'fail', 'blocked', 'notRun', 'inProgress' respectively.
    - `normalizeStatusById` returns 'notRun' for any ID not present in the runtime map (e.g. 999).
    - When `fetchAioProjectConfig` rejects, `initializeAioStatusMap` does not throw; the runtime map becomes empty and `normalizeStatusById` returns 'notRun' for every ID.
  </behavior>
  <action>
    Implement the fully dynamic AIO status map per D-02 through D-06 (no static seed map anywhere).

    In `aioUtils.ts`:
    1. Remove the entire `export const AIO_STATUS_MAP = { ... }` constant (lines 55-63) — no static entries remain (D-02).
    2. Add `import { fetchAioProjectConfig } from '@/services/aio/cycles';` and `import type { AioTestRunStatusConfig } from '@/services/aio/types';` (use the type for the function's array element if needed).
    3. Add a module-level cache `let runtimeAioStatusMap: Record<number, 'pass' | 'fail' | 'blocked' | 'notRun' | 'inProgress'> = {};` (D-04).
    4. Add a module-level `STATUS_TYPE_MAP` mapping statusType strings to canonical keys: PASSED→'pass', FAILED→'fail', BLOCKED→'blocked', NOT_RUN→'notRun', IN_PROGRESS→'inProgress' — mirroring the analog in AioProjectOverviewPage.tsx lines 32-41.
    5. Add `export async function initializeAioStatusMap(baseUrl: string, token: string, jiraProjectId: number): Promise<void>` that calls `fetchAioProjectConfig(baseUrl, token, jiraProjectId)`, then sets `runtimeAioStatusMap = Object.fromEntries(statuses.map((s) => [s.ID, STATUS_TYPE_MAP[s.statusType] ?? 'notRun']))`. Wrap in try/catch; on catch set `runtimeAioStatusMap = {}` and do not rethrow (D-05). Do not duplicate the HTTP call — reuse `fetchAioProjectConfig` (D-06).
    6. Change `normalizeStatusById` to `return runtimeAioStatusMap[id] ?? 'notRun';`. Keep its signature and the unknown-ID fallback.
    7. Leave `normalizeStatus` and `normalizeStatusLabel` untouched.

    In `aioUtils.test.ts` (per 65-RESEARCH.md Pitfall 2):
    1. Update the top imports: remove `AIO_STATUS_MAP`, add `initializeAioStatusMap` and `vi` from vitest; keep `normalizeStatus`, `normalizeStatusById`, `normalizeStatusLabel`.
    2. Add `vi.mock('@/services/aio/cycles', () => ({ fetchAioProjectConfig: vi.fn() }))` and import the mocked `fetchAioProjectConfig`.
    3. Delete the `describe('AIO_STATUS_MAP', ...)` block (lines 63-87) — the constant no longer exists.
    4. Rewrite the `describe('normalizeStatusById', ...)` block (lines 89-121) into a `describe('initializeAioStatusMap + normalizeStatusById', ...)` block per 65-PATTERNS.md: one test mocking a full config and asserting 53→pass / 54→fail / 55→blocked / 51→notRun / 52→inProgress after `await initializeAioStatusMap(...)`; one test asserting unknown ID (999) → 'notRun'; one test where `fetchAioProjectConfig` rejects and after init all IDs → 'notRun'. Because `runtimeAioStatusMap` is shared module state, re-initialize (or reset via the reject path) inside each test so cross-test ordering is deterministic.
    5. Keep the `normalizeStatus` and `normalizeStatusLabel` describe blocks unchanged.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- aioUtils</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const AIO_STATUS_MAP" taskflow/src/lib/aioUtils.ts` returns 0
    - `grep -c "export async function initializeAioStatusMap" taskflow/src/lib/aioUtils.ts` returns 1
    - `grep -c "runtimeAioStatusMap\[id\] ?? 'notRun'" taskflow/src/lib/aioUtils.ts` returns 1 (normalizeStatusById reads the runtime cache)
    - `grep -c "fetchAioProjectConfig" taskflow/src/lib/aioUtils.ts` returns at least 1 (reuses, not duplicates, the HTTP call)
    - `grep -c "AIO_STATUS_MAP" taskflow/src/lib/aioUtils.test.ts` returns 0
    - `cd taskflow && npm test -- aioUtils` exits 0, including a test that asserts ID 52 → 'inProgress' after init and a test that asserts all IDs → 'notRun' when fetch rejects
  </acceptance_criteria>
  <done>No static `AIO_STATUS_MAP`; `initializeAioStatusMap` populates a module cache from `/config` via the reused fetch function; `normalizeStatusById` reads the cache; tests cover success, unknown-ID fallback, and fetch-failure fallback; aioUtils tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate AioCycleDetailPage consumer + wire activation call site (CLEAN-07)</name>
  <files>taskflow/src/routes/dashboard/AioCycleDetailPage.tsx, taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/AioCycleDetailPage.tsx (line 26 import, line 454 `AIO_STATUS_MAP[Number(idStr)] ?? 'notRun'` usage)
    - taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx (line 3 imports, lines 16/294-301 `configQuery` + `statusMap`, lines 267-291 where `jiraBaseUrl`/`token`/`jiraProjectId` are in scope)
    - taskflow/src/lib/aioUtils.ts (the new `initializeAioStatusMap` + `normalizeStatusById` exports from Task 2)
    - .planning/phases/65-tech-debt-cleanup/65-PATTERNS.md (aioUtils.ts CLEAN-07 "Call site" + "consumer migration" subsections; "React Query + useEffect side-effect" shared pattern)
    - .planning/phases/65-tech-debt-cleanup/65-RESEARCH.md (Pitfall 1 — missed AioCycleDetailPage usages break the build; Open Question 1 — call site recommendation)
  </read_first>
  <action>
    Wire the dynamic map into its one consumer and add the activation call site. Depends on Task 2's new exports.

    In `AioCycleDetailPage.tsx` (consumer migration — required or the build breaks, per Pitfall 1):
    1. Line 26: change `import { AIO_STATUS_MAP, normalizeStatus, normalizeStatusLabel } from '@/lib/aioUtils';` to `import { normalizeStatus, normalizeStatusLabel, normalizeStatusById } from '@/lib/aioUtils';` (drop `AIO_STATUS_MAP`, add `normalizeStatusById`).
    2. Line 454: change `const statusKey = AIO_STATUS_MAP[Number(idStr)] ?? 'notRun';` to `const statusKey = normalizeStatusById(Number(idStr));` (the `?? 'notRun'` is now internal to `normalizeStatusById`).

    In `AioProjectOverviewPage.tsx` (activation call site — per Open Question 1 recommendation, avoids a duplicate HTTP call because React Query dedups the `/config` query):
    1. Add `import { initializeAioStatusMap } from '@/lib/aioUtils';` to the imports.
    2. Add a `useEffect` immediately after the `statusMap` memo (after line 301) that, when `configQuery.data && jiraBaseUrl && token && jiraProjectId` are all truthy, calls `void initializeAioStatusMap(jiraBaseUrl, token, jiraProjectId);`. Dependency array: `[configQuery.data, jiraBaseUrl, token, jiraProjectId]`. `useEffect` is already imported (line 3); do not change the existing `configQuery` or local `buildStatusMap`/`statusMap` — they continue serving this page's own rendering.

    After both edits, `AIO_STATUS_MAP` must not be referenced anywhere in `src/` (it was removed in Task 2).
  </action>
  <verify>
    <automated>cd taskflow && grep -rn "AIO_STATUS_MAP" src/ ; npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "AIO_STATUS_MAP" taskflow/src/` returns no results
    - `grep -c "normalizeStatusById(Number(idStr))" taskflow/src/routes/dashboard/AioCycleDetailPage.tsx` returns 1
    - `grep -c "initializeAioStatusMap" taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx` returns at least 2 (import + useEffect call)
    - `cd taskflow && npm run build` exits 0 (no "has no exported member 'AIO_STATUS_MAP'" error)
    - `cd taskflow && npm test` exits 0 (full suite)
  </acceptance_criteria>
  <done>`AioCycleDetailPage` resolves status via `normalizeStatusById`; `AioProjectOverviewPage` calls `initializeAioStatusMap` on config resolution; no `AIO_STATUS_MAP` reference remains; full build + test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Taskflow client → AIO `/config` endpoint | `initializeAioStatusMap` consumes the AIO `/config` response (statusType strings, numeric IDs) over the existing authenticated `aioFetch` (Bearer PAT). Response shapes the runtime status map. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-03 | Tampering | `/config` response → `runtimeAioStatusMap` | mitigate | `STATUS_TYPE_MAP[s.statusType] ?? 'notRun'` whitelists statusType strings to a fixed five-value canonical union; any unexpected/injected statusType collapses to 'notRun' — no arbitrary value enters the rendered status |
| T-65-04 | Denial of Service | `initializeAioStatusMap` on `/config` failure | mitigate | try/catch sets the map to `{}` and never rethrows (D-05); a failed/unreachable `/config` degrades to all-'notRun' rendering, not a crash |
| T-65-05 | Information Disclosure | reuse of existing `fetchAioProjectConfig` (Bearer PAT) | accept | No new credential handling — reuses the existing authenticated AIO fetch path established in v1.8; no new secret surface introduced |
| T-65-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH.md: "No new packages required"). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `cd taskflow && npm test -- aioUtils` exits 0 (init success / unknown-ID / fetch-failure cases)
- `cd taskflow && npm test` exits 0 (full suite — confirms no consumer was missed)
- `cd taskflow && npm run build` exits 0 (catches the removed `AIO_STATUS_MAP` export across all consumers)
- `grep -rn "AIO_STATUS_MAP" taskflow/src/` returns no results
- `grep -c "52: 'IN_PROGRESS'" taskflow/src/services/aio/cycles.ts` returns 1
</verification>

<success_criteria>
- CLEAN-06: `TESTCASE_STATUS_MAP` has 51/52 entries; in-progress runs (ID 52) render IN_PROGRESS in the Executions tab.
- CLEAN-07: No hardcoded `AIO_STATUS_MAP`; runtime map built from `/config` via reused `fetchAioProjectConfig`; empty-map fallback on failure; `AioCycleDetailPage` migrated to `normalizeStatusById`; activation wired in `AioProjectOverviewPage`; tests + build green.
</success_criteria>

<output>
Create `.planning/phases/65-tech-debt-cleanup/65-02-SUMMARY.md` when done.
</output>
