---
phase: 65-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/worklogs/WorklogsPage.tsx
  - taskflow/src/services/tempo/types.ts
  - taskflow/src/stores/tempo-filters.store.ts
  - taskflow/src/components/app/Sidebar.test.tsx
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-04, CLEAN-05]
must_haves:
  truths:
    - "WorklogsPage clears its combobox close timer when the component unmounts (no setTimeout fires on an unmounted component)"
    - "WorklogsPage renders ErrorState whenever a worklog fetch errors, even if a previous successful-empty result is cached"
    - "The worklog hierarchy table renders without React fragment-key warnings when switching saved filters"
    - "DatePreset is exported from the tempo service types layer and the store imports it from there, not from a route component"
    - "Sidebar.test.tsx contains no 'workload' sidebar mock entry and the existing sidebar test count is unchanged"
  artifacts:
    - path: "taskflow/src/services/tempo/types.ts"
      provides: "DatePreset union type export"
      contains: "export type DatePreset"
    - path: "taskflow/src/routes/worklogs/WorklogsPage.tsx"
      provides: "Timer cleanup effect, single-condition error state, keyed hierarchy fragments, DatePreset import"
      contains: "import type { DatePreset }"
    - path: "taskflow/src/stores/tempo-filters.store.ts"
      provides: "DatePreset import from service types"
      contains: "from '../services/tempo/types'"
    - path: "taskflow/src/components/app/Sidebar.test.tsx"
      provides: "Sidebar mock without stale workload entry"
  key_links:
    - from: "taskflow/src/stores/tempo-filters.store.ts"
      to: "taskflow/src/services/tempo/types.ts"
      via: "import type { DatePreset }"
      pattern: "import type \\{ DatePreset \\} from '\\.\\./services/tempo/types'"
    - from: "taskflow/src/routes/worklogs/WorklogsPage.tsx"
      to: "taskflow/src/services/tempo/types.ts"
      via: "import type { DatePreset }"
      pattern: "import type \\{ DatePreset \\} from '@/services/tempo/types'"
---

<objective>
Pay down five carried React + type-architecture debt items in the worklogs surface and one stale sidebar test mock: timer cleanup (CLEAN-01), error-state visibility (CLEAN-02), keyed hierarchy fragments (CLEAN-03), the `DatePreset` type-architecture inversion (CLEAN-04), and the stale `workload` sidebar test entry (CLEAN-05).

Purpose: These are correctness bugs (memory/leak risk, suppressed error UI, React key warnings) and an architectural inversion where `stores/` imports from a route component. All are line-addressable surgical edits.
Output: A corrected `WorklogsPage.tsx`, a `DatePreset` export in the tempo service types layer, an updated store import, and a cleaned sidebar test mock.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-tech-debt-cleanup/65-CONTEXT.md
@.planning/phases/65-tech-debt-cleanup/65-RESEARCH.md
@.planning/phases/65-tech-debt-cleanup/65-PATTERNS.md

<interfaces>
<!-- Current state extracted from codebase — executor should rely on these, then read_first files for full context. -->

WorklogsPage.tsx line 29 import (NO React namespace import — must add `React` for React.Fragment):
```
import { useEffect, useMemo, useRef, useState } from 'react';
```

WorklogsPage.tsx line 53 — DatePreset to move out:
```
export type DatePreset =
  | 'this-week' | 'last-week' | 'this-month'
  | 'last-month' | 'last-working-day' | 'custom';
```

WorklogsPage.tsx line 306 — timer ref:
```
const closeTimer = useRef<ReturnType<typeof setTimeout> | null>(null);
```

WorklogsPage.tsx line 951 — error condition:
```
{isError && !data ? (
  <ErrorState error={error} onRetry={refetch} viewName="worklogs" />
```

tempo-filters.store.ts line 4 (inversion to fix):
```
import type { DatePreset } from '../routes/worklogs/WorklogsPage';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix WorklogsPage timer cleanup, error state, and keyed fragments (CLEAN-01, CLEAN-02, CLEAN-03)</name>
  <files>taskflow/src/routes/worklogs/WorklogsPage.tsx</files>
  <read_first>
    - taskflow/src/routes/worklogs/WorklogsPage.tsx (the full file — see current state of lines 29, 306, 658-667, 951, 1050, 1129, 1238, 1241)
    - taskflow/src/routes/worklogs/WorklogsPage.test.tsx (existing test patterns — 41 tests; understand render/mock setup before adding assertions)
    - .planning/phases/65-tech-debt-cleanup/65-PATTERNS.md (sections CLEAN-01, CLEAN-02, CLEAN-03 — exact before/after for each site)
  </read_first>
  <behavior>
    - CLEAN-01: After mount and unmount, any pending `closeTimer.current` setTimeout is cleared — no `setOpen(false)` fires on the unmounted component (no act warning / no late state update).
    - CLEAN-02: When the worklogs query is in `isError` state, `ErrorState` renders even when `data` is a cached empty array (`[]`). Previously `isError && !data` suppressed the error UI because `[]` is truthy.
    - CLEAN-03: The Epic/Story/Subtask hierarchy table renders with no React "each child in a list should have a unique key" / fragment-key console warnings.
  </behavior>
  <action>
    Apply three surgical edits to `WorklogsPage.tsx`, all per the exact before/after blocks in 65-PATTERNS.md.

    CLEAN-01: Add a cleanup `useEffect` with empty dependency array that, on unmount, calls `clearTimeout(closeTimer.current)` guarded by an `if (closeTimer.current)` check. Insert it alongside the other `useEffect` blocks (near line 327, after the existing effects). Do not change `handleComboboxFocus` (line 658) or `handleComboboxBlur` (line 667).

    CLEAN-02: At line 951, change the conditional from `isError && !data` to `isError` alone (per D-01). The `ErrorState` element, props (`error={error} onRetry={refetch} viewName="worklogs"`), and the rest of the ternary are unchanged. This shows the error view whenever the fetch fails regardless of React Query cache contents; no separate error banner.

    CLEAN-03: Convert the three shorthand fragments returned by `.map()` callbacks in the hierarchy table to `React.Fragment` with a `key`. Site 1 (line ~1050, epic iterator outer return): `<>` → `<React.Fragment key={epicKey}>`, closing `</>` (line ~1241) → `</React.Fragment>`. Site 2 (line ~1129, story iterator return inside the epic map): `<>` → `<React.Fragment key={storyKey}>`, closing `</>` (line ~1238) → `</React.Fragment>`. The third closing fragment in the pair is the story-level close — key is `storyKey` (NOT subtaskKey), per 65-PATTERNS.md CLEAN-03 Site 3 note. Move the existing `key={`epic-${epicKey}`}` / `key={`story-${storyKey}`}` props that currently sit on the inner `<tr>` elements up onto the new `React.Fragment` wrappers as shown in 65-PATTERNS.md (the inner `<tr>` no longer needs its own list key once the fragment is keyed; leave any non-list `<tr>` keys as-is if removing them would orphan a sibling). Do NOT touch the `<>` fragments at lines 804/834 and 874/888 — those are not `.map()` callbacks and need no key.

    `React.Fragment` requires the `React` namespace in scope. The line 29 import is `import { useEffect, useMemo, useRef, useState } from 'react';` with no default import. Change it to `import React, { useEffect, useMemo, useRef, useState } from 'react';`.

    Then add/extend tests in `WorklogsPage.test.tsx`: a test that unmounts the component with a pending close timer and asserts no post-unmount state update warning (use fake timers + unmount); a test that renders with the query forced into `isError` while `data` is `[]` and asserts `ErrorState` is present; and rely on React's render-time key warning surfacing in the existing hierarchy render test (assert no `console.error` calls matching /key/ during a hierarchy render with multiple epics/stories).
  </action>
  <verify>
    <automated>cd taskflow && npm test -- WorklogsPage</automated>
  </verify>
  <acceptance_criteria>
    - `WorklogsPage.tsx` line 29 import contains `import React, {`
    - `grep -n "clearTimeout(closeTimer.current)" taskflow/src/routes/worklogs/WorklogsPage.tsx` returns a match inside a `useEffect` cleanup return
    - `grep -c "isError && !data" taskflow/src/routes/worklogs/WorklogsPage.tsx` returns 0
    - `grep -c "<React.Fragment key=" taskflow/src/routes/worklogs/WorklogsPage.tsx` returns at least 2
    - `cd taskflow && npm test -- WorklogsPage` exits 0
    - New test asserting ErrorState renders when `isError` is true and `data` is `[]` passes
  </acceptance_criteria>
  <done>Timer cleanup effect present; error condition is `isError` only; hierarchy fragments keyed with `React` in scope; WorklogsPage tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Move DatePreset to tempo service types and fix store import (CLEAN-04)</name>
  <files>taskflow/src/services/tempo/types.ts, taskflow/src/routes/worklogs/WorklogsPage.tsx, taskflow/src/stores/tempo-filters.store.ts</files>
  <read_first>
    - taskflow/src/services/tempo/types.ts (existing export pattern — the `TempoWorklog` interface sets the JSDoc + export convention)
    - taskflow/src/routes/worklogs/WorklogsPage.tsx (lines 45-53 — current `export type DatePreset` block and existing `@/services/tempo` imports)
    - taskflow/src/stores/tempo-filters.store.ts (lines 1-9 — the import to fix and the `preset: DatePreset` usage)
    - .planning/phases/65-tech-debt-cleanup/65-PATTERNS.md (CLEAN-04 section + "Store import from service types" shared pattern)
  </read_first>
  <action>
    Move the `DatePreset` type out of the route component into the tempo service types layer, eliminating the `stores/` → `routes/` import inversion. Four coordinated edits (all four required — see 65-RESEARCH.md Pitfall 5):

    1. In `taskflow/src/services/tempo/types.ts`: append an exported `DatePreset` union with the exact six members from the current declaration: `'this-week' | 'last-week' | 'this-month' | 'last-month' | 'last-working-day' | 'custom'`. Add a JSDoc comment following the file's existing `TempoWorklog` JSDoc style, noting it was moved from WorklogsPage per CLEAN-04.

    2. In `taskflow/src/routes/worklogs/WorklogsPage.tsx`: delete the local `export type DatePreset = ...` block (lines 53-59) and add `import type { DatePreset } from '@/services/tempo/types';` to the import group. `WorklogsPage.tsx` still uses `DatePreset` locally, so the import must remain.

    3. In `taskflow/src/stores/tempo-filters.store.ts`: change line 4 from `import type { DatePreset } from '../routes/worklogs/WorklogsPage';` to `import type { DatePreset } from '../services/tempo/types';`. No other store changes.

    4. Confirm no other file imports `DatePreset` from `WorklogsPage` (grep below). If any exist, update them to `@/services/tempo/types` as well.
  </action>
  <verify>
    <automated>cd taskflow && grep -rn "DatePreset.*from.*WorklogsPage\|DatePreset.*from.*routes/worklogs" src/ ; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export type DatePreset" taskflow/src/services/tempo/types.ts` returns 1
    - `grep -c "export type DatePreset" taskflow/src/routes/worklogs/WorklogsPage.tsx` returns 0
    - `grep -c "import type { DatePreset } from '@/services/tempo/types'" taskflow/src/routes/worklogs/WorklogsPage.tsx` returns 1
    - `grep -c "from '../services/tempo/types'" taskflow/src/stores/tempo-filters.store.ts` returns at least 1 (the DatePreset import)
    - `grep -rn "DatePreset.*from.*routes/worklogs" taskflow/src/` returns no results
    - `cd taskflow && npm run build` exits 0 (tsc + vite build both green)
  </acceptance_criteria>
  <done>`DatePreset` exported from `services/tempo/types.ts`; WorklogsPage and the store both import it from there; no store→route inversion remains; `npm run build` green.</done>
</task>

<task type="auto">
  <name>Task 3: Remove stale workload sidebar test mock (CLEAN-05)</name>
  <files>taskflow/src/components/app/Sidebar.test.tsx</files>
  <read_first>
    - taskflow/src/components/app/Sidebar.test.tsx (lines 71-83 — the `sidebarItems` mock array; line 79 is the `{ id: 'workload', visible: true }` entry to remove)
    - .planning/phases/65-tech-debt-cleanup/65-PATTERNS.md (CLEAN-05 section — before/after mock array)
  </read_first>
  <action>
    Delete the single stale mock entry `{ id: 'workload', visible: true },` at line 79 of the `sidebarItems` array in `Sidebar.test.tsx`. The `workload` sidebar item was removed from the app in v1.9 (Phase 59 dashboard cleanup); this mock entry is dead. After removal the array has 10 items (dashboard, my-tasks, sprint-board, backlog, epics, merge-requests, sprint-progress, releases, worklogs, aio-projects). Do NOT add, rename, or reorder any other entry. Do NOT change any `it(...)`/`test(...)` block — the existing sidebar test count (7) must remain unchanged.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- Sidebar</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "workload" taskflow/src/components/app/Sidebar.test.tsx` returns 0
    - `grep -c "it(\|test(" taskflow/src/components/app/Sidebar.test.tsx` returns 7 (unchanged)
    - `grep -c "id: '" taskflow/src/components/app/Sidebar.test.tsx` reflects 10 sidebarItems entries (one fewer than before)
    - `cd taskflow && npm test -- Sidebar` exits 0
  </acceptance_criteria>
  <done>No `workload` reference in `Sidebar.test.tsx`; sidebar test count unchanged at 7; Sidebar tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan touches only client-side React component lifecycle, a type definition, a store import path, and a test mock. No new untrusted-input crossing, no network/auth/storage surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Denial of Service | `closeTimer` setTimeout in WorklogsPage | mitigate | CLEAN-01 adds explicit unmount cleanup so a post-unmount timer cannot fire a state update / leak a handle (the bug being fixed) |
| T-65-02 | Information Disclosure | ErrorState rendering on worklog fetch failure | accept | CLEAN-02 surfaces fetch errors via the existing `ErrorState` component which already governs what error detail is shown; no new error detail is exposed by changing the gate condition |
| T-65-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH.md: "No new packages required"). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `cd taskflow && npm test -- WorklogsPage` exits 0
- `cd taskflow && npm test -- Sidebar` exits 0
- `cd taskflow && npm run build` exits 0 (catches the CLEAN-04 import wiring across tsc + vite)
- `grep -rn "DatePreset.*from.*routes/worklogs" taskflow/src/` returns no results
- `grep -c "isError && !data" taskflow/src/routes/worklogs/WorklogsPage.tsx` returns 0
- `grep -c "workload" taskflow/src/components/app/Sidebar.test.tsx` returns 0
</verification>

<success_criteria>
- CLEAN-01: A cleanup `useEffect` clears `closeTimer.current` on unmount; covered by a test that unmounts with a pending timer.
- CLEAN-02: Error condition at line 951 is `isError` alone; `ErrorState` renders on fetch error even with cached `[]` data.
- CLEAN-03: All three hierarchy `.map()` fragments use `React.Fragment` with a key; no fragment-key warnings; `React` is in scope.
- CLEAN-04: `DatePreset` lives in `services/tempo/types.ts`; WorklogsPage + store import from there; `npm run build` green; no store→route inversion.
- CLEAN-05: `Sidebar.test.tsx` has no `workload` entry; test count unchanged at 7.
</success_criteria>

<output>
Create `.planning/phases/65-tech-debt-cleanup/65-01-SUMMARY.md` when done.
</output>
