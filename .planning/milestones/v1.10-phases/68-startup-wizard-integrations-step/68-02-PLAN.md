---
phase: 68-startup-wizard-integrations-step
plan: 02
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - taskflow/src/routes/onboarding/IntegrationsStep.tsx
  - taskflow/src/routes/onboarding/IntegrationsStep.test.tsx
autonomous: true
requirements: [WIZ-02, WIZ-03, WIZ-04]
must_haves:
  truths:
    - "IntegrationsStep renders the AioBlock, an inline Tempo toggle, and Back/Continue navigation"
    - "Continue is disabled when aioEnabled and (no project selected OR loading OR error OR empty list); enabled when aioEnabled is false"
    - "Tempo toggle reads/writes tempoEnabled directly from useSettingsStore"
    - "Continue click sets integrationsVisited=true and calls goNext; Back calls goBack"
    - "No wizard-local state for aioEnabled/tempoEnabled/selectedAioProjectKey — all bound to useSettingsStore"
  artifacts:
    - path: "taskflow/src/routes/onboarding/IntegrationsStep.tsx"
      provides: "Wizard Integrations step with AioBlock + inline Tempo toggle + gated navigation"
      exports: ["default"]
      contains: "export default function IntegrationsStep"
    - path: "taskflow/src/routes/onboarding/IntegrationsStep.test.tsx"
      provides: "Unit tests for Continue gating (D-01..D-04), Tempo toggle, store binding, navigation"
  key_links:
    - from: "taskflow/src/routes/onboarding/IntegrationsStep.tsx"
      to: "taskflow/src/components/integrations/AioBlock.tsx"
      via: "default import + mount"
      pattern: "import AioBlock from"
    - from: "taskflow/src/routes/onboarding/IntegrationsStep.tsx"
      to: "useOnboardingStore"
      via: "goBack/goNext + set({ integrationsVisited: true })"
      pattern: "integrationsVisited: true"
---

<objective>
Create the new `IntegrationsStep` wizard route component. It mounts the extracted `AioBlock` (from Plan 01), adds an inline Tempo toggle (D-06 — not extracted), and renders Back/Continue navigation with the locked Continue-gating expression (D-01..D-04). All settings bind directly to `useSettingsStore` (D-10); Continue uses Option A — a duplicate `useQuery` with the same key as AioBlock so TanStack Query deduplicates to a single network call, giving the step access to `isLoading`/`isError` for gating.

Purpose: This is the actual wizard step that satisfies WIZ-02 (AIO toggle + picker), WIZ-03 (Tempo toggle), and WIZ-04 (direct settings-store writes, no wizard-local state).

Output: New `IntegrationsStep.tsx` + `IntegrationsStep.test.tsx`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-startup-wizard-integrations-step/68-CONTEXT.md
@.planning/phases/68-startup-wizard-integrations-step/68-RESEARCH.md
@.planning/phases/68-startup-wizard-integrations-step/68-PATTERNS.md
@.planning/phases/68-startup-wizard-integrations-step/68-UI-SPEC.md
@.planning/phases/68-startup-wizard-integrations-step/68-VALIDATION.md
@.planning/phases/68-startup-wizard-integrations-step/68-01-SUMMARY.md

<interfaces>
<!-- Produced by Plan 01 (Wave 1). -->
From taskflow/src/components/integrations/AioBlock.tsx:
- export default function AioBlock(): JSX.Element  // self-contained, no props

From taskflow/src/stores/onboarding.store.ts (post Plan 01):
- useOnboardingStore() => { goBack(): void; goNext(): void; set(partial): void; integrationsVisited: boolean; ... }

From taskflow/src/stores/settings.store.ts:
- aioEnabled, selectedAioProjectKey (read for gating); tempoEnabled, setTempoEnabled (Tempo toggle)

From taskflow/src/services/aio:
- fetchAioProjects(jiraBaseUrl, token) — used in the duplicate gating query (Option A, same queryKey ['aio', jiraBaseUrl, 'projects'])

From taskflow/src/services/stronghold:
- readSecret('jira-pat')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write IntegrationsStep tests covering gating, Tempo toggle, store binding, navigation</name>
  <files>taskflow/src/routes/onboarding/IntegrationsStep.test.tsx</files>
  <read_first>
    - taskflow/src/routes/settings/IntegrationsSection.test.tsx (mock structure template — settings store mock, fetchAioProjects mock, readSecret mock, renderWithClient helper, QueryClientProvider wrap)
    - taskflow/src/routes/onboarding/JiraStep.test.tsx (wizard step test + onboarding store mock pattern, navigation assertions)
    - .planning/phases/68-startup-wizard-integrations-step/68-PATTERNS.md (IntegrationsStep.test.tsx section — lists exact mocks to add: mock useOnboardingStore, mock AioBlock as a stub div)
    - .planning/phases/68-startup-wizard-integrations-step/68-CONTEXT.md (D-01..D-04 gating rules, D-10 single-source-of-truth)
  </read_first>
  <behavior>
    - Test: Continue disabled when aioEnabled=true and query isLoading (D-02)
    - Test: Continue disabled when aioEnabled=true and query isError (D-03)
    - Test: Continue disabled when aioEnabled=true and projects=[] (D-04)
    - Test: Continue disabled when aioEnabled=true and selectedAioProjectKey=null with projects loaded (D-01)
    - Test: Continue enabled when aioEnabled=false regardless of project state (D-01)
    - Test: Continue enabled when aioEnabled=true and a project is selected and projects loaded (D-01)
    - Test: Tempo toggle renders and clicking it calls setTempoEnabled(true) (WIZ-03)
    - Test: clicking Continue calls set({ integrationsVisited: true }) and goNext (WIZ-04 + completedSteps)
    - Test: clicking Back calls goBack
  </behavior>
  <action>
    Create `IntegrationsStep.test.tsx` (RED — IntegrationsStep.tsx does not exist yet, so the import fails until Task 2). Mock the settings store exposing aioEnabled, selectedAioProjectKey, tempoEnabled, setTempoEnabled (selector-aware, same shape as IntegrationsSection.test.tsx mockStore). Mock `@/stores/onboarding.store` with `useOnboardingStore: () => ({ goBack: vi.fn(), goNext: vi.fn(), set: vi.fn() })` — keep the spy references on a module-level `mockOnboardingStore` object so assertions can read them. Mock `@/components/integrations/AioBlock` as `{ default: () => <div data-testid="aio-block" /> }` to isolate IntegrationsStep navigation/gating logic. Mock `@/services/aio` (fetchAioProjects), `@/services/stronghold` (readSecret resolving 'test-jira-token'), and `@/stores/auth.store` (jiraBaseUrl) so the duplicate gating query can run. Use the `renderWithClient` QueryClientProvider helper (retry:false). Drive `isLoading`/`isError`/`projects` states by configuring `fetchAioProjects` mock per test (never-resolving promise for loading, rejection for error, `[]` for empty, populated array for loaded) and set `mockStore.aioEnabled` / `mockStore.selectedAioProjectKey` per the behavior block. Assert Continue button disabled state via `screen.getByRole('button', { name: /continue/i })` and `.toBeDisabled()/.toBeEnabled()`, waiting for query resolution with `waitFor` where needed. Assert navigation by clicking buttons and checking `mockOnboardingStore.goNext`/`goBack`/`set` calls.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- IntegrationsStep</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/onboarding/IntegrationsStep.test.tsx exists
    - Test file covers all nine behaviors in the behavior block (6 gating + Tempo + Continue + Back)
    - Test mocks AioBlock as a stub (`data-testid="aio-block"`) and mocks useOnboardingStore with goBack/goNext/set spies
    - After Task 2 completes, `cd taskflow && npm test -- IntegrationsStep` exits 0
  </acceptance_criteria>
  <done>Test file exists and exercises Continue gating for all D-01..D-04 states, the Tempo toggle write, store-bound navigation (goNext + integrationsVisited), and Back navigation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement IntegrationsStep component</name>
  <files>taskflow/src/routes/onboarding/IntegrationsStep.tsx</files>
  <read_first>
    - taskflow/src/routes/onboarding/GitLabStep.tsx (layout analog — outer container `flex flex-col gap-6 max-w-lg mx-auto py-8` at line 67, heading block lines 68-73, navigation row lines 139-163)
    - taskflow/src/routes/settings/IntegrationsSection.tsx (Tempo block source — lines 148-168, the inline toggle markup to replicate)
    - taskflow/src/components/integrations/AioBlock.tsx (the component to mount — from Plan 01)
    - .planning/phases/68-startup-wizard-integrations-step/68-PATTERNS.md (IntegrationsStep.tsx section — imports, layout, navigation, Continue gating, Tempo inline patterns)
    - .planning/phases/68-startup-wizard-integrations-step/68-UI-SPEC.md (Layout Contract, Copywriting Contract — step heading/subtitle copy, Tempo verbatim strings)
    - taskflow/src/routes/onboarding/IntegrationsStep.test.tsx (the tests this implementation must pass — from Task 1)
  </read_first>
  <behavior>
    - Renders heading "Set up Integrations" and subtitle "Enable optional plugins to see test execution and worklog data."
    - Mounts <AioBlock />
    - Renders inline Tempo toggle bound to tempoEnabled/setTempoEnabled
    - Continue disabled per continueDisabled expression; Back always enabled
    - handleContinue sets integrationsVisited=true then goNext; Back calls goBack
  </behavior>
  <action>
    Create `IntegrationsStep.tsx` (GREEN — make Task 1 tests pass). `export default function IntegrationsStep()`. Imports: Button, AioBlock (default from `@/components/integrations/AioBlock`), useSettingsStore, useOnboardingStore, useQuery, fetchAioProjects, readSecret, useAuthStore, useEffect/useState. Read `goBack`/`goNext` from useOnboardingStore (and `set` for integrationsVisited). Read `aioEnabled` and `selectedAioProjectKey` from useSettingsStore via fine-grained selectors; read `tempoEnabled`/`setTempoEnabled` likewise. Implement Option A gating query: token via readSecret('jira-pat') in a useEffect guarded on jiraBaseUrl, then `useQuery({ queryKey: ['aio', jiraBaseUrl, 'projects'], queryFn: () => fetchAioProjects(jiraBaseUrl!, token!), enabled: !!jiraBaseUrl && !!token })` — same key as AioBlock (intentional dedup; add a code comment noting this). Compute `const continueDisabled = aioEnabled && (!selectedAioProjectKey || isLoading || isError || (Array.isArray(projects) && projects.length === 0));`. `handleContinue` calls `set({ integrationsVisited: true })` then `goNext()`. Render the outer container `<div className="flex flex-col gap-6 max-w-lg mx-auto py-8">`, the heading/subtitle block (h2 `text-xl font-semibold` "Set up Integrations" + p `text-sm text-muted-foreground mt-1` subtitle), `<AioBlock />`, the inline Tempo block (subheading h3 `text-sm font-semibold text-muted-foreground uppercase tracking-wide` "Tempo Timesheets" + the toggle label row replicated verbatim from IntegrationsSection lines 152-167 with `aria-label="Enable Tempo Timesheets"`), and the navigation row `<div className="flex gap-3">` with `<Button variant="ghost" onClick={goBack}>Back</Button>` + `<Button onClick={handleContinue} disabled={continueDisabled}>Continue</Button>`. Do NOT introduce any useState for aioEnabled/tempoEnabled/selectedAioProjectKey (D-10).
  </action>
  <verify>
    <automated>cd taskflow && npm test -- IntegrationsStep</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/onboarding/IntegrationsStep.tsx contains `export default function IntegrationsStep`
    - File contains `import AioBlock from '@/components/integrations/AioBlock'` and `<AioBlock />`
    - File contains queryKey `['aio', jiraBaseUrl, 'projects']` (Option A dedup) with a comment marking it intentional
    - File contains the continueDisabled expression gating on aioEnabled + (!selectedAioProjectKey || isLoading || isError || empty)
    - File contains `set({ integrationsVisited: true })` in handleContinue followed by goNext
    - File contains the inline Tempo toggle with `aria-label="Enable Tempo Timesheets"` bound to setTempoEnabled
    - File contains NO useState for aioEnabled/tempoEnabled/selectedAioProjectKey
    - `cd taskflow && npm test -- IntegrationsStep` exits 0
  </acceptance_criteria>
  <done>IntegrationsStep renders AioBlock + inline Tempo toggle + gated Back/Continue; all settings bind to useSettingsStore; Continue gating matches D-01..D-04; all IntegrationsStep tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Jira/AIO API | The duplicate gating `useQuery` in IntegrationsStep fetches the AIO project list with the Stronghold-read PAT. Same boundary as AioBlock; TanStack Query deduplicates to a single request. Unchanged surface. |

This phase introduces NO new attack surface. IntegrationsStep is a wizard view composed of an extracted toggle/picker (AioBlock) plus a Tempo checkbox. No free-text input, no new auth flow. The gating query reuses the established `readSecret('jira-pat')` + `fetchAioProjects` path; the PAT is never written to the onboarding store or any persisted wizard state. Confirmed by RESEARCH.md Security Domain (all ASVS categories N/A).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-03 | Information Disclosure | PAT used in IntegrationsStep gating query | accept | PAT read via `readSecret('jira-pat')` Stronghold IPC, held only in component-local state for the query lifetime; never persisted to onboarding/settings store. Identical pattern to AioBlock and pre-existing IntegrationsSection. |
| T-68-04 | Tampering | Settings store writes (aioEnabled/tempoEnabled/selectedAioProjectKey) | accept | Writes go through existing typed store setters; values are booleans / a projectKey chosen from a fetched list. No injection path. |
| T-68-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: zero new packages). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd taskflow && npm test -- IntegrationsStep` green
- Continue gating verified for all four AIO states + the aioEnabled=false short-circuit
- Tempo toggle write verified (setTempoEnabled)
- Continue sets integrationsVisited=true and calls goNext; Back calls goBack
- No wizard-local useState for the three settings fields (D-10)
</verification>

<success_criteria>
- IntegrationsStep satisfies WIZ-02 (AIO toggle + picker via AioBlock, Continue gated), WIZ-03 (Tempo toggle), WIZ-04 (direct settings-store writes, no separate wizard state)
- All IntegrationsStep tests green
</success_criteria>

<output>
Create `.planning/phases/68-startup-wizard-integrations-step/68-02-SUMMARY.md` when done
</output>
