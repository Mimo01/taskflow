---
phase: 70-standup-notes-today-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/standup-notes/filterSprintItems.ts
  - taskflow/src/routes/standup-notes/filterSprintItems.test.ts
autonomous: true
requirements: [STAND-07]
must_haves:
  truths:
    - "Given a flat sprint-issue list, filterSprintItems returns only the current user's leaf items (D-04: leaf-level work items assigned to me; subtasks + childless tasks/stories/bugs, parents excluded)"
    - "In Progress = statusCategory.key === 'indeterminate'; Up Next = statusCategory.key === 'new'; Done excluded from both (D-05: status split by Jira status category)"
    - "Subtasks are always leaf; childless tasks/stories/bugs are leaf; tasks-with-subtasks (parents) are excluded (D-04)"
    - "Items not assigned to me (assignee.displayName !== jiraUserDisplayName) are excluded (D-04 assignee match)"
  artifacts:
    - path: "taskflow/src/routes/standup-notes/filterSprintItems.ts"
      provides: "Pure filterSprintItems(issues, jiraUserDisplayName) => { inProgress, upNext } helper"
      exports: ["filterSprintItems", "FilteredSprintItems"]
      min_lines: 25
    - path: "taskflow/src/routes/standup-notes/filterSprintItems.test.ts"
      provides: "Unit tests for leaf detection, status split, assignee match (STAND-07)"
      contains: "filterSprintItems"
  key_links:
    - from: "filterSprintItems.test.ts"
      to: "filterSprintItems.ts"
      via: "import { filterSprintItems }"
      pattern: "from './filterSprintItems'"
---

<objective>
Extract the In Progress / Up Next filter logic from `DashboardInProgressCard` into a pure, testable helper `filterSprintItems(issues, jiraUserDisplayName) => { inProgress, upNext }`, extended per D-04 (leaf-item scope) and D-05 (status-category split). Ship it with a full unit-test suite — this is the highest-value test target in the phase because the logic is pure and trivially testable without mocking TanStack Query or the router.

Purpose: STAND-07 (my open sprint work) hinges entirely on this filter being correct. Isolating it as a pure function lets Wave 2's `TodayColumn` consume a verified contract instead of re-deriving the filter inline.
Output: `filterSprintItems.ts` (pure helper) + `filterSprintItems.test.ts` (Vitest unit suite).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-standup-notes-today-section/70-CONTEXT.md
@.planning/phases/70-standup-notes-today-section/70-RESEARCH.md
@.planning/phases/70-standup-notes-today-section/70-PATTERNS.md
@.planning/phases/70-standup-notes-today-section/70-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase + RESEARCH.md. No exploration required. -->

JiraIssue type — import from '@/services/jira' (taskflow/src/services/jira.ts line 139).
Relevant fields used by the filter:
  issue.fields.issuetype.subtask        : boolean
  issue.fields.status.statusCategory?.key : 'new' | 'indeterminate' | 'done'
  issue.fields.assignee?.displayName    : string | undefined
  issue.fields.subtasks                 : array | undefined (present on parent issues; undefined on subtask rows)

Canonical analog filter — DashboardInProgressCard.tsx lines 57-62:
  issue.fields.issuetype.subtask
    && issue.fields.status.statusCategory?.key === 'indeterminate'
    && issue.fields.assignee?.displayName === jiraUserDisplayName

D-04 leaf rule: a subtask is ALWAYS leaf (issuetype.subtask === true short-circuits any
subtasks.length check). A non-subtask is leaf only when (subtasks?.length ?? 0) === 0.
D-05 split: indeterminate → inProgress, new → upNext, done → excluded from both.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write filterSprintItems pure helper</name>
  <files>taskflow/src/routes/standup-notes/filterSprintItems.ts</files>
  <read_first>
    - taskflow/src/routes/dashboard/DashboardInProgressCard.tsx (lines 57-62 — canonical filter to lift)
    - taskflow/src/services/jira.ts (line 139 — JiraIssue interface; confirm field paths)
    - .planning/phases/70-standup-notes-today-section/70-PATTERNS.md (filterSprintItems.ts section — full pattern + the note on subtasks-field availability)
    - .planning/phases/70-standup-notes-today-section/70-RESEARCH.md ("Leaf Item Filter" + Pitfall 7)
  </read_first>
  <behavior>
    - Subtask with statusCategory.key 'indeterminate' assigned to me → in inProgress
    - Subtask with statusCategory.key 'new' assigned to me → in upNext
    - Childless task (issuetype.subtask false, subtasks length 0) with 'new' → in upNext
    - Task with subtasks (subtasks length > 0) → excluded from both lists even if assigned to me + indeterminate
    - Item with statusCategory.key 'done' → excluded from both lists
    - Item assigned to someone else (displayName mismatch) → excluded from both lists
    - Item with no assignee (assignee undefined) → excluded from both lists
  </behavior>
  <action>
    Create the module exporting interface `FilteredSprintItems { inProgress: JiraIssue[]; upNext: JiraIssue[] }` and `export function filterSprintItems(issues: JiraIssue[], jiraUserDisplayName: string): FilteredSprintItems`. Import the `JiraIssue` type from `@/services/jira` with `import type`. Implement two local predicates per D-04/D-05: `isLeaf` returns true when `issue.fields.issuetype.subtask` is true OR (`!issue.fields.issuetype.subtask` AND `(issue.fields.subtasks?.length ?? 0) === 0`); `isAssignedToMe` returns `issue.fields.assignee?.displayName === jiraUserDisplayName`. Build `inProgress` by filtering for `isLeaf && isAssignedToMe && status.statusCategory?.key === 'indeterminate'`; build `upNext` identically but `=== 'new'`. Do NOT include any `'done'` items. Add a JSDoc block documenting the leaf rule and the D-04/D-05 references. No fenced React, no query code — this file is pure data transform only.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "filterSprintItems" ; echo "tsc-filter-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `filterSprintItems.ts` exports both `filterSprintItems` and `FilteredSprintItems` (grep: `export function filterSprintItems` and `export interface FilteredSprintItems` each return a match)
    - Source assertion: file contains no `useQuery`, no JSX (grep -c 'useQuery' returns 0; grep -c 'return (' for JSX returns 0)
    - Behavior assertion: the leaf predicate short-circuits on `issuetype.subtask === true` before reading `subtasks.length` (no undefined access on subtask rows)
    - tsc reports no type errors referencing filterSprintItems
  </acceptance_criteria>
  <done>filterSprintItems.ts exists, exports the pure helper + type, type-checks clean, contains no query/JSX code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write filterSprintItems unit test suite</name>
  <files>taskflow/src/routes/standup-notes/filterSprintItems.test.ts</files>
  <read_first>
    - taskflow/src/routes/standup-notes/filterSprintItems.ts (the helper under test — created in Task 1)
    - taskflow/src/routes/standup-notes/YesterdayColumn.test.ts (mirror this pure-unit-test structure + inline fixture-builder pattern)
    - .planning/phases/70-standup-notes-today-section/70-VALIDATION.md (Per-Task Verification Map rows for STAND-07: filter helper, in-progress split, up-next split, leaf detection, assignee match)
  </read_first>
  <behavior>
    - "includes subtask with indeterminate status assigned to me in inProgress"
    - "includes childless task with new status in upNext"
    - "excludes task-with-subtasks (parent) from both lists"
    - "excludes done items from both lists"
    - "excludes items not assigned to me"
    - "excludes items with no assignee"
  </behavior>
  <action>
    Create a Vitest suite (`describe('filterSprintItems', ...)`) importing `{ filterSprintItems }` from `./filterSprintItems` and `{ describe, expect, it }` from `vitest`. Add a local `makeIssue(overrides: { key: string; statusKey: 'new' | 'indeterminate' | 'done'; isSubtask: boolean; subtasksLen?: number; displayName?: string })` factory that returns a minimal `JiraIssue`-shaped object (cast `as JiraIssue`) populating only `fields.issuetype.subtask`, `fields.status.statusCategory.key`, `fields.assignee` (set to `{ displayName }` or `undefined` when displayName omitted), and `fields.subtasks` (array of length `subtasksLen ?? 0`). Write the six tests named per `<behavior>`, asserting membership/absence by issue key in the `inProgress` and `upNext` arrays. Use a fixed `jiraUserDisplayName = 'Me Myself'` and set fixture `displayName` to match (or mismatch) it. No watch flags.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/standup-notes/filterSprintItems.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test command `npx vitest run src/routes/standup-notes/filterSprintItems.test.ts` exits 0 with all six tests passing
    - Behavior assertion: a task-with-subtasks fixture (subtasksLen > 0, indeterminate, assigned to me) appears in NEITHER inProgress NOR upNext
    - Behavior assertion: a 'done' fixture assigned to me appears in NEITHER list
    - Behavior assertion: a fixture with mismatched displayName appears in NEITHER list
    - No `vitest --watch` / no `.only` left in the file (grep -c '\.only' returns 0)
  </acceptance_criteria>
  <done>filterSprintItems.test.ts exists with the six named tests; `npx vitest run src/routes/standup-notes/filterSprintItems.test.ts` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Pure in-memory data transform on already-fetched JiraIssue objects. No I/O, no token handling, no network, no markup rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Information Disclosure | filterSprintItems helper | accept | Operates on data already fetched and held in client memory by an existing query; introduces no new data exposure. No PII leaves the function. |

**Rationale:** This plan adds no network calls, no token handling, no DOM injection, and no new persisted state. The only input is an in-memory `JiraIssue[]` already present in the app. No threat rises to HIGH; no `<task type="checkpoint:human-verify">` security gate is warranted. No package installs (T-70-SC not applicable — see RESEARCH.md Package Legitimacy Audit: zero new packages).
</threat_model>

<verification>
- `npx vitest run src/routes/standup-notes/filterSprintItems.test.ts` green (6/6).
- `npx tsc --noEmit` shows no errors referencing filterSprintItems.
- filterSprintItems.ts contains no `useQuery` and no JSX.
</verification>

<success_criteria>
STAND-07 filter contract is implemented as a pure function and proven by unit tests: leaf detection (D-04), status-category split (D-05), and assignee match all verified green. Wave 2's TodayColumn can import `filterSprintItems` as a trusted contract.
</success_criteria>

<output>
Create `.planning/phases/70-standup-notes-today-section/70-01-SUMMARY.md` when done.
</output>
