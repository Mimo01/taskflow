---
phase: 88-release-branch-milestone-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/release-detail/releaseBranch.ts
  - taskflow/src/routes/dashboard/release-detail/releaseBranch.test.ts
  - taskflow/src/routes/dashboard/release-detail/releaseMilestone.ts
  - taskflow/src/routes/dashboard/release-detail/releaseMilestone.test.ts
autonomous: true
requirements: [RELBR-01, RELBR-05, RELMS-03, RELMS-04]

must_haves:
  truths:
    - "D-09: milestone title `33.5.0 (21.07.2026)` derives branch name `release/33.5.0` (version component only, date suffix stripped)"
    - "D-11: a milestone title with no leading `X.Y.Z` version token derives no branch name (null), never a sanitized guess"
    - "RELBR-05 / D-12: a branch name violating git-ref rules (space, `..`, trailing `.lock`, leading `/`, control char, `~^:?*[\\`, segment starting with `.`) is rejected before any write"
    - "D-01: milestone title format `X.Y.Z (DD.MM.YYYY)` is enforced by one shared regex constant; `1.1.0` bare-semver is NOT valid"
    - "D-04: a Jira ISO release date `2026-07-21` formats to the title date component `21.07.2026`"
    - "D-06/D-07: inherited group (ancestor) milestones are excluded from the duplicate check and the reference list by `project_id` match"
    - "RELMS-04: a candidate title that already exists (whitespace- and case-normalized comparison) resolves to the colliding milestone, blocking submit"
    - "D-10/D-11: branch UI state resolves to a discriminated union covering blocked-no-milestone, unresolvable, invalid-ref, loading, exists, missing"
    - "D-12/D-21: both modules are React-free (no imports from react, no hooks, no store reads) and unit-tested"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/releaseBranch.ts"
      provides: "Pure version extraction, branch-name derivation, git-ref validation, branch-state resolution"
      exports: ["RELEASE_BRANCH_PREFIX", "extractVersionFromMilestoneTitle", "deriveReleaseBranchName", "isValidGitRefName", "resolveBranchState"]
    - path: "taskflow/src/routes/dashboard/release-detail/releaseBranch.test.ts"
      provides: "Unit coverage for RELBR-01 and RELBR-05"
      contains: "deriveReleaseBranchName"
    - path: "taskflow/src/routes/dashboard/release-detail/releaseMilestone.ts"
      provides: "Milestone title format constant/validator, due-date formatting, ancestor filtering, duplicate detection"
      exports: ["MILESTONE_TITLE_FORMAT_RE", "isValidMilestoneTitle", "formatMilestoneDueDate", "buildMilestoneTitle", "ownProjectMilestones", "normalizeMilestoneTitle", "findDuplicateMilestone"]
    - path: "taskflow/src/routes/dashboard/release-detail/releaseMilestone.test.ts"
      provides: "Unit coverage for RELMS-03 title format and RELMS-04 duplicate detection"
      contains: "findDuplicateMilestone"
  key_links:
    - from: "taskflow/src/routes/dashboard/release-detail/releaseBranch.ts"
      to: "no runtime imports"
      via: "pure module — React-free per D-12/D-21"
      pattern: "^import"
    - from: "taskflow/src/routes/dashboard/release-detail/releaseMilestone.ts"
      to: "structural milestone type (not @/services/gitlab runtime import)"
      via: "local `MilestoneLike` structural type so the module has zero service coupling"
      pattern: "MilestoneLike"
---

<objective>
Create the two React-free pure modules this phase's UI and hook layers depend on: `releaseBranch.ts` (D-09 version-only branch derivation, D-11 unresolvable gate, RELBR-05 git-ref validation, branch-state resolution) and `releaseMilestone.ts` (D-01 title format, D-04 due-date formatting, D-06/D-07 ancestor filtering, RELMS-04 duplicate detection), each with a co-located Vitest suite.

Purpose: This is the phase's Wave 0 — every downstream plan (hook wiring, sidebar rows, both dialogs, the list-row indicator) consumes these functions, and this is the only place the phase's domain rules are unit-testable. It closes all four Wave 0 test gaps listed in 88-VALIDATION.md.
Output: `releaseBranch.ts`, `releaseBranch.test.ts`, `releaseMilestone.ts`, `releaseMilestone.test.ts` — all four new, in `taskflow/src/routes/dashboard/release-detail/`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88-release-branch-milestone-creation/88-CONTEXT.md
@.planning/phases/88-release-branch-milestone-creation/88-RESEARCH.md
@.planning/phases/88-release-branch-milestone-creation/88-PATTERNS.md
@taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts
@taskflow/src/routes/dashboard/release-detail/releaseSummaries.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 88-01-T1: Pure releaseBranch module — version extraction, branch derivation, git-ref validation, branch-state resolution</name>

  <files>
    taskflow/src/routes/dashboard/release-detail/releaseBranch.ts (new),
    taskflow/src/routes/dashboard/release-detail/releaseBranch.test.ts (new)
  </files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts` — the exact module-header JSDoc convention and function-per-concern shape to mirror (lines 1-50: `computeMilestoneWindow`)
    - `taskflow/src/routes/dashboard/release-detail/releaseSummaries.test.ts` — the `describe`-per-exported-function, edge-case-first `it` naming convention to mirror (lines 1-12 imports, lines 170-180 `computeMilestoneWindow` block)
    - `.planning/phases/88-release-branch-milestone-creation/88-RESEARCH.md` § "Pattern 3" — the exact bodies for `extractVersionFromMilestoneTitle`, `deriveReleaseBranchName`, `isValidGitRefName`, written against this codebase's conventions
    - `.planning/phases/88-release-branch-milestone-creation/88-CONTEXT.md` — D-09 (version-only derivation), D-11 (unresolvable → disable, never guess), D-10 (milestone is a hard prerequisite), D-12 (pure module + tests)
  </read_first>

  <behavior>
    `deriveReleaseBranchName` (RELBR-01, D-09):
    - `"33.5.0 (21.07.2026)"` → `"release/33.5.0"`
    - `"33.5.0"` → `"release/33.5.0"` (bare version, no date suffix, still resolves — D-11 only blocks when NO version token exists)
    - `"  1.0.0 (01.01.2026)  "` → `"release/1.0.0"` (leading/trailing whitespace trimmed)
    - `"v33.5.0 (21.07.2026)"` → `null` (version token must be leading, not prefixed)
    - `"Sprint 42"` → `null`
    - `"33.5 (21.07.2026)"` → `null` (two-part version is not `X.Y.Z`)
    - `null` / `undefined` / `""` → `null`

    `extractVersionFromMilestoneTitle`: same inputs, returns the bare version (`"33.5.0"`) or `null`.

    `isValidGitRefName` (RELBR-05, D-12) — `true` only for `"release/33.5.0"`-shaped names; `false` for each of:
    - `""` (empty), a 256-character name (length > 255)
    - `"release/33.5.0 (21.07.2026)"` (contains a space)
    - `"release/33..5.0"` (contains `..`)
    - `"release/33.5.0.lock"` (trailing `.lock`)
    - `"release/33.5.0."` (trailing `.`)
    - `"/release/33.5.0"` (leading `/`), `"release/33.5.0/"` (trailing `/`), `"release//33.5.0"` (double slash)
    - `"release/@{33.5.0"` (contains `@{`), `"@"` (literally `@`)
    - `"release/33.5.0"` (ASCII control char), `"release/33.5.0"` (DEL)
    - `"release/33.5.0~1"`, `"release/33.5.0^"`, `"release/33.5.0:x"`, `"release/33.5.0?"`, `"release/33.5.0*"`, `"release/33.5.0[x"`, `"release/33.5.0\\x"` (each disallowed char)
    - `"release/.hidden"` (path segment starting with `.`)

    `resolveBranchState` (RELBR-02/RELBR-03 state machine, D-10/D-11):
    - `{ hasMatchedMilestone: false, ... }` → `{ kind: 'blocked-no-milestone' }` (D-10 — milestone is a hard prerequisite)
    - `{ hasMatchedMilestone: true, milestoneTitle: 'Sprint 42', ... }` → `{ kind: 'unresolvable' }` (D-11)
    - a milestone title that derives a branch name failing `isValidGitRefName` → `{ kind: 'invalid-ref', branchName }` (RELBR-05 pre-flight block)
    - valid name, `branchExists: undefined` → `{ kind: 'loading', branchName }`
    - valid name, `branchExists: true` → `{ kind: 'exists', branchName }`
    - valid name, `branchExists: false` → `{ kind: 'missing', branchName }`
  </behavior>

  <action>
Create `taskflow/src/routes/dashboard/release-detail/releaseBranch.ts` as a React-free pure module (no `react` import, no hooks, no store reads, no service imports) mirroring `releaseSummaries.ts`'s module-header JSDoc style. Explain in the header: D-09 version-only derivation (real milestone titles are `X.Y.Z (DD.MM.YYYY)`, e.g. `33.5.0 (21.07.2026)`; the ` (DD.MM.YYYY)` suffix is stripped because spaces and parentheses are illegal git refs), D-11's never-sanitize-and-guess rule, and D-10's milestone-prerequisite gate. Give every exported function its own `@param`/`@returns` JSDoc block.

Export exactly these five symbols:

1. `RELEASE_BRANCH_PREFIX` — the string constant `'release/'` (hardcoded per RELBR-01; a configurable prefix is an explicitly deferred idea, do not add a parameter for it).
2. `extractVersionFromMilestoneTitle(title: string | null | undefined): string | null` — trim the input, match the anchored leading pattern `^(\d+\.\d+\.\d+)`, return capture group 1 or `null`. Do NOT require the `(DD.MM.YYYY)` suffix to be present.
3. `deriveReleaseBranchName(milestoneTitle: string | null | undefined): string | null` — returns `` `${RELEASE_BRANCH_PREFIX}${version}` `` when a version is extracted, otherwise `null`. Never fall back to slugifying the raw title (D-11).
4. `isValidGitRefName(name: string): boolean` — the practical `git-check-ref-format(1)` subset from 88-RESEARCH.md Pattern 3. Reuse that body verbatim including its `biome-ignore lint/suspicious/noControlCharactersInRegex` comment for the control-character regex.
5. `resolveBranchState(params: { hasMatchedMilestone: boolean; milestoneTitle: string | null | undefined; branchExists: boolean | undefined }): BranchState` — also export the `BranchState` discriminated union type with variants `{ kind: 'blocked-no-milestone' }`, `{ kind: 'unresolvable' }`, `{ kind: 'invalid-ref'; branchName: string }`, `{ kind: 'loading'; branchName: string }`, `{ kind: 'exists'; branchName: string }`, `{ kind: 'missing'; branchName: string }`. Evaluate in that precedence order: no matched milestone first (D-10), then null derivation (D-11), then ref validity (RELBR-05), then `branchExists` tri-state.

Create `taskflow/src/routes/dashboard/release-detail/releaseBranch.test.ts` with `import { describe, expect, it } from 'vitest';` and a named import of the four functions plus the `BranchState` type from `'./releaseBranch'`. One `describe` block per exported function; name each `it` after the specific rule under test (e.g. `it('rejects a trailing .lock suffix', ...)`), matching `releaseSummaries.test.ts`'s edge-case-first naming. Cover every case enumerated in `<behavior>` above — a suite that only tests "has a space" and "is empty" for `isValidGitRefName` does not satisfy this task.
  </action>

  <verify>
    <automated>cd taskflow &amp;&amp; npm run test -- src/routes/dashboard/release-detail/releaseBranch.test.ts</automated>
    <automated>cd taskflow &amp;&amp; npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `npm run test -- src/routes/dashboard/release-detail/releaseBranch.test.ts` exits 0 with zero failing tests
    - `grep -c "^import" taskflow/src/routes/dashboard/release-detail/releaseBranch.ts` returns `0` — the module has no imports at all (React-free per D-12/D-21)
    - `grep -n "release/" taskflow/src/routes/dashboard/release-detail/releaseBranch.ts` shows the literal `'release/'` assigned to `RELEASE_BRANCH_PREFIX` and no second hardcoded occurrence
    - `npm run test -- src/routes/dashboard/release-detail/releaseBranch.test.ts -t "deriveReleaseBranchName"` matches and passes at least 6 assertions
    - `npm run test -- src/routes/dashboard/release-detail/releaseBranch.test.ts -t "isValidGitRefName"` matches and passes; the suite includes explicit assertions for `..`, `.lock`, leading `/`, and a control character
    - `npx tsc --noEmit` reports no errors originating from either new file
    - `deriveReleaseBranchName('Sprint 42')` returns `null` (asserted in the suite) — no sanitized-guess branch name is ever produced
  </acceptance_criteria>

  <done>
    `releaseBranch.ts` exports `RELEASE_BRANCH_PREFIX`, `extractVersionFromMilestoneTitle`, `deriveReleaseBranchName`, `isValidGitRefName`, `resolveBranchState` and the `BranchState` type; `releaseBranch.test.ts` covers every case in `<behavior>` and passes; the module has zero imports.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 88-01-T2: Pure releaseMilestone module — title format, due-date formatting, ancestor filtering, duplicate detection</name>

  <files>
    taskflow/src/routes/dashboard/release-detail/releaseMilestone.ts (new),
    taskflow/src/routes/dashboard/release-detail/releaseMilestone.test.ts (new)
  </files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts` — module-header + JSDoc conventions to mirror
    - `taskflow/src/routes/dashboard/release-detail/releaseSummaries.test.ts` — test-suite conventions to mirror
    - `.planning/phases/88-release-branch-milestone-creation/88-CONTEXT.md` — D-01 (real title format `X.Y.Z (DD.MM.YYYY)`, NOT `1.1.0`), D-02 (format is enforced, off-format cannot submit), D-04 (due_date from Jira release date is mandatory), D-06 (exclude inherited group milestones), D-07 (`project_id` local filter, no second request), D-08 (client duplicate check is best-effort, GitLab is authority)
    - `.planning/phases/88-release-branch-milestone-creation/88-RESEARCH.md` § "Assumptions Log" A3 and § "Open Questions" #1 — the unverified `project_id`/`group_id` presence assumption this task must degrade safely against
    - `taskflow/src/services/gitlab.ts` lines 226-235 — the current `GitLabMilestone` shape (read for field names only; do NOT import it here)
  </read_first>

  <behavior>
    `MILESTONE_TITLE_FORMAT_RE` / `isValidMilestoneTitle` (RELMS-03, D-01/D-02):
    - `"33.5.0 (21.07.2026)"` → valid
    - `"1.1.0"` → INVALID (bare semver is the documented-but-wrong REQUIREMENTS.md format; D-01 corrects it)
    - `"33.5.0 (2026-07-21)"` → invalid (ISO date, wrong date format)
    - `"33.5.0(21.07.2026)"` → invalid (missing the single space before the paren)
    - `"33.5.0 (21.07.2026) "` → invalid (trailing whitespace; the regex is anchored `^...$`)
    - `"Release 33.5.0 (21.07.2026)"` → invalid (leading text)

    `formatMilestoneDueDate` (D-04):
    - `"2026-07-21"` → `"21.07.2026"`
    - `"2026-01-05"` → `"05.01.2026"` (zero-padded)
    - `null` / `undefined` / `"not-a-date"` → `null`

    `buildMilestoneTitle` (D-02 prefill):
    - `("33.5.0", "2026-07-21")` → `"33.5.0 (21.07.2026)"`
    - `("33.5.0", null)` → `null` (D-04: a dateless milestone would be created and still render as unmatched, so it must not be buildable)

    `ownProjectMilestones` (D-06/D-07, defensive against A3):
    - list with mixed `project_id: 7` and `group_id: 3, project_id: null` entries, `projectId: 7` → only the `project_id === 7` entries
    - list where NO entry carries a numeric `project_id` (field absent entirely on this GitLab instance) → the list is returned UNFILTERED rather than emptied, so a missing field degrades to "no ancestor filtering" instead of silently hiding every milestone

    `normalizeMilestoneTitle` (RELMS-04 comparison normalization):
    - `"  33.5.0   (21.07.2026)  "` → `"33.5.0 (21.07.2026)"` (trimmed, internal whitespace runs collapsed to one space, lowercased)

    `findDuplicateMilestone` (RELMS-04):
    - candidate `"33.5.0 (21.07.2026)"` against a list containing `"33.5.0 (21.07.2026)"` → returns that milestone
    - candidate `"33.5.0 (21.07.2026)"` against a list containing `" 33.5.0  (21.07.2026) "` → returns that milestone (normalized comparison catches whitespace variants)
    - candidate `"33.6.0 (28.07.2026)"` against a list of other titles → `null`
    - a colliding title that belongs to an inherited group milestone (`group_id` set, `project_id` mismatched) → `null` (D-06: a group milestone is not a project-level conflict)
  </behavior>

  <action>
Create `taskflow/src/routes/dashboard/release-detail/releaseMilestone.ts` as a React-free pure module with the same header/JSDoc conventions as `releaseSummaries.ts`. The header must state D-01 explicitly: the team's real GitLab milestone titles are `X.Y.Z (DD.MM.YYYY)` (e.g. `33.5.0 (21.07.2026)`); `REQUIREMENTS.md` RELMS-03's `1.1.0` example is a documentation error and must not be implemented.

Declare a local structural type `MilestoneLike` = `{ title: string; project_id?: number | null; group_id?: number | null }` and type the list-taking functions generically as `<T extends MilestoneLike>`. Do NOT import `GitLabMilestone` from `@/services/gitlab` — the structural type keeps this module free of service coupling and lets it compile independently of Plan 88-02's interface change.

Export exactly these seven symbols:

1. `MILESTONE_TITLE_FORMAT_RE` — the single source-of-truth regex, anchored: `/^\d+\.\d+\.\d+ \(\d{2}\.\d{2}\.\d{4}\)$/`. Both the dialog's live validation and any duplicate-check normalization import this constant; do not re-declare the pattern anywhere else.
2. `isValidMilestoneTitle(title: string): boolean` — `MILESTONE_TITLE_FORMAT_RE.test(title)`. Because the regex is not `/g`, it is stateless and safe to reuse.
3. `formatMilestoneDueDate(isoDate: string | null | undefined): string | null` — converts a Jira `YYYY-MM-DD` release date to the `DD.MM.YYYY` title component. Validate the input against `/^\d{4}-\d{2}-\d{2}$/` and return `null` on anything else; build the output by string slicing, not `new Date()`, so no timezone shift can occur.
4. `buildMilestoneTitle(version: string, isoReleaseDate: string | null | undefined): string | null` — returns `` `${version} (${formatted})` `` or `null` when the date cannot be formatted (D-04 makes the date mandatory).
5. `ownProjectMilestones<T extends MilestoneLike>(milestones: readonly T[], projectId: number): T[]` — D-06/D-07 ancestor filter on `project_id === projectId`. Guard against RESEARCH assumption A3: if no element in the input has a `typeof project_id === 'number'`, return a copy of the input unfiltered and note in a code comment that the field is absent on this GitLab instance so ancestor filtering degrades to a no-op rather than emptying the list (the D-07 fallback of a dialog-scoped `include_ancestors=false` fetch is out of scope for this phase).
6. `normalizeMilestoneTitle(title: string): string` — `title.trim().replace(/\s+/g, ' ').toLowerCase()`. Comparison-only: callers must still send the user's exact unmodified input to GitLab on write.
7. `findDuplicateMilestone<T extends MilestoneLike>(milestones: readonly T[], candidateTitle: string, projectId: number): T | null` — runs `ownProjectMilestones` first, then returns the first element whose `normalizeMilestoneTitle(el.title)` equals `normalizeMilestoneTitle(candidateTitle)`, else `null`. Add a JSDoc note recording D-08: this check is best-effort because the source list is the ±7-day windowed milestone query, so a milestone dated outside the window is invisible here and GitLab's server-side validation remains the authority.

Also add a JSDoc note on `findDuplicateMilestone` recording the known open item: `.planning/phases/88-release-branch-milestone-creation/probe.sh` has not been run against the live instance, so exact-vs-fuzzy matching is unconfirmed; the normalized-comparison / exact-write split implemented here is the defensive default.

Create `taskflow/src/routes/dashboard/release-detail/releaseMilestone.test.ts` with `import { describe, expect, it } from 'vitest';`, a small inline fixture builder for `MilestoneLike` objects (mirroring `releaseSummaries.test.ts`'s inline-fixture style), one `describe` per exported function, and every case enumerated in `<behavior>`. Name the format-regex describe block exactly `MILESTONE_TITLE_FORMAT` so `npm run test -t "MILESTONE_TITLE_FORMAT"` selects it.
  </action>

  <verify>
    <automated>cd taskflow &amp;&amp; npm run test -- src/routes/dashboard/release-detail/releaseMilestone.test.ts</automated>
    <automated>cd taskflow &amp;&amp; npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `npm run test -- src/routes/dashboard/release-detail/releaseMilestone.test.ts` exits 0 with zero failing tests
    - `npm run test -- src/routes/dashboard/release-detail/releaseMilestone.test.ts -t "MILESTONE_TITLE_FORMAT"` matches at least one describe block and passes
    - `npm run test -- src/routes/dashboard/release-detail/releaseMilestone.test.ts -t "findDuplicateMilestone"` matches and passes, including an assertion that an inherited group milestone (`group_id` set, mismatched `project_id`) with a colliding title returns `null`
    - The suite asserts `isValidMilestoneTitle('1.1.0') === false` and `isValidMilestoneTitle('33.5.0 (21.07.2026)') === true`
    - The suite asserts `ownProjectMilestones` returns the input unfiltered when no element carries a numeric `project_id`
    - `grep -c "@/services/gitlab" taskflow/src/routes/dashboard/release-detail/releaseMilestone.ts` returns `0` — no service import
    - `grep -c "from 'react'" taskflow/src/routes/dashboard/release-detail/releaseMilestone.ts` returns `0` — React-free per D-12/D-21
    - `grep -c "MILESTONE_TITLE_FORMAT_RE =" taskflow/src/routes/dashboard/release-detail/releaseMilestone.ts` returns `1` — the title-format pattern is declared exactly once (single source of truth per "Don't Hand-Roll"), and no other file in `taskflow/src` re-declares an equivalent `DD.MM.YYYY` regex
    - `npx tsc --noEmit` reports no errors originating from either new file
  </acceptance_criteria>

  <done>
    `releaseMilestone.ts` exports `MILESTONE_TITLE_FORMAT_RE`, `isValidMilestoneTitle`, `formatMilestoneDueDate`, `buildMilestoneTitle`, `ownProjectMilestones`, `normalizeMilestoneTitle`, `findDuplicateMilestone`; `releaseMilestone.test.ts` covers every case in `<behavior>` and passes; the module imports nothing from `@/services/gitlab` or `react`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| derived string → GitLab URL path segment | Branch names derived from milestone titles (which originate in GitLab, editable by any project member) are later interpolated into REST URL path segments by Plan 88-02 |
| derived string → GitLab write body | Milestone titles typed by the user are later sent as a POST body field by Plan 88-06 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-V5 | Tampering | `isValidGitRefName` / `MILESTONE_TITLE_FORMAT_RE` (`releaseBranch.ts`, `releaseMilestone.ts`) | mitigate | ASVS V5 input validation as defense-in-depth: git-ref rules and the anchored `^...$` title regex reject malformed values before any write call is issued. Explicitly NOT the authority — GitLab's server-side validation is (D-08). Both validators are unit-tested against the full rule set in this plan. |
| T-88-INJ-PRE | Tampering | `deriveReleaseBranchName` output | mitigate | The derived name is constrained to `release/` + a `\d+\.\d+\.\d+` capture, so no path-traversal (`..`, `/`) or query-injection characters can survive derivation. `isValidGitRefName` is a second gate. Path-segment `encodeURIComponent` is enforced downstream in Plan 88-02. |
| T-88-CONF | Information Disclosure | `normalizeMilestoneTitle` | accept | Milestone titles are non-sensitive project metadata already visible to every project member; lowercasing for comparison discloses nothing. The user's exact input is preserved for the write. |
| T-88-SC | Tampering | npm installs | accept | This plan installs zero packages (88-RESEARCH.md § Package Legitimacy Audit: not applicable). No `[ASSUMED]`/`[SUS]` packages exist, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `cd taskflow && npm run test -- src/routes/dashboard/release-detail/` — both new suites plus the existing `releaseSummaries.test.ts` pass
- `cd taskflow && npm run check` — biome + tsc show ZERO NEW errors relative to the documented 2-error `BacklogPage.tsx`/`BacklogRow.tsx` formatting baseline. Do not "fix" the Backlog files; do not treat a non-clean run as failure unless a third error appears or an error names one of this plan's files.
</verification>

<success_criteria>
- Four new files exist in `taskflow/src/routes/dashboard/release-detail/`: `releaseBranch.ts`, `releaseBranch.test.ts`, `releaseMilestone.ts`, `releaseMilestone.test.ts`
- Both new suites pass under `vitest run`
- Neither module imports `react`, a store, or `@/services/gitlab`
- All four 88-VALIDATION.md Wave 0 gaps are closed (RELBR-01, RELBR-05, RELMS-03 format, RELMS-04 duplicate)
- `npm run check` shows no new errors vs the 2-error baseline
</success_criteria>

<output>
Create `.planning/phases/88-release-branch-milestone-creation/88-01-SUMMARY.md` when done.
</output>
