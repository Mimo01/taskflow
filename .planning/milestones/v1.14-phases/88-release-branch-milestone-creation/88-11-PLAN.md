---
phase: 88-release-branch-milestone-creation
plan: 11
type: execute
wave: 4
depends_on: ["88-07", "88-08", "88-09", "88-10"]
gap_closure: true
files_modified:
  - .planning/phases/88-release-branch-milestone-creation/88-RESEARCH.md
autonomous: false
requirements: [RELBR-03, RELBR-04, RELMS-02, RELMS-04]
user_setup:
  - service: gitlab
    why: "The create-branch and create-milestone write paths have never executed against a real GitLab instance — both live checkpoints (88-05-T3, 88-06-T3) were waived during execution. Coverage to date is mocked-fetch unit tests only. Separately, 88-RESEARCH.md Open Question #1 records that `probe.sh` (the RELMS-04 whitespace/case-variant collision scan and the D-07/A3 `project_id` presence check) has never been run against the live instance, so `findDuplicateMilestone`'s normalization remains probe-gated."
    env_vars:
      - name: gitlab-pat (stronghold secret, already configured in the app)
        source: "GitLab -> User Settings -> Access Tokens. Tasks 2 and 3 need `api` scope; Task 4 needs a SECOND, deliberately scope-restricted token (e.g. `read_user` only) to exercise the 403 path."
      - name: GITLAB_BASE_URL / GITLAB_PAT / PROJECT_ID (shell env, Task 5 only)
        source: "Same GitLab instance and project as the app's settings. `probe.sh` is read-only (all GETs) and cannot read the app's stronghold-stored PAT, so the token must be supplied to the shell directly."
    dashboard_config:
      - task: "Identify a throwaway GitLab project (or a safe branch namespace in the real one) where creating a `release/<version>` branch and an `X.Y.Z (DD.MM.YYYY)` milestone is acceptable and can be deleted afterwards"
        location: "GitLab -> the project configured as activeGitlabProject in TaskFlow settings"
      - task: "Ensure a Jira fix version with a release date exists and is visible in the Releases tab, so a milestone can be date-matched to it"
        location: "Jira -> project -> Releases"
      - task: "Task 5 needs the REAL production project id (not the throwaway one) — the point of the collision scan is to inspect the team's actual milestone titles"
        location: "GitLab -> the project configured as activeGitlabProject in TaskFlow settings"

must_haves:
  truths:
    - "A release branch created from the release detail view against real GitLab appears at the correct ref, and the sidebar flips from missing to exists without a manual reload"
    - "A milestone created from the release detail view against real GitLab appears in the detail sidebar AND on the Releases list without a manual refresh"
    - "A scope-restricted PAT produces GitLab's own explanatory message inside the create dialog"
    - "A failed branch fetch on the Releases tab reads as 'GitLab unavailable', visibly distinct from a confirmed missing branch"
    - "The `search=release/` server-side filter returns the release branch set as D-18 assumes"
    - "findDuplicateMilestone's whitespace/case normalization is confirmed against the team's real milestone titles, closing 88-RESEARCH.md Open Question #1"
  artifacts:
    - path: ".planning/phases/88-release-branch-milestone-creation/88-11-SUMMARY.md"
      provides: "Recorded live-GitLab checkpoint outcomes, replacing the two waived checkpoints, plus the probe.sh output"
      contains: "88-05-T3"
    - path: ".planning/phases/88-release-branch-milestone-creation/88-RESEARCH.md"
      provides: "Open Question #1 marked resolved with the live probe outcome"
      contains: "Open Questions"
  key_links:
    - from: "createBranch / createMilestone"
      to: "a real GitLab instance"
      via: "human-executed write against a live project"
      pattern: "live"
    - from: "probe.sh Probe B/C output"
      to: "findDuplicateMilestone + ownProjectMilestones normalization"
      via: "human comparison of real titles against the client-side normalization rules"
      pattern: "normalizeMilestoneTitle"
---

<objective>
Un-waive and actually run the live-GitLab verification that Plans 88-05 and 88-06 skipped, and
close the last unresolved research open question.

88-VERIFICATION.md records that both write-path checkpoints (88-05-T3 create-branch, 88-06-T3
create-milestone) were explicitly waived by the user during execution. `createBranch` and
`createMilestone` have never executed against a real GitLab instance; all confidence to date
comes from mocked-fetch unit tests. CR-03 specifically affects the error paths those checkpoints
would have exercised, so the recommendation is to run them now that the fixes have landed.

D-18's assumption that `GET /projects/:id/repository/branches?search=release/` performs a
server-side prefix filter is also unconfirmed against a real API — a partial or differently
scoped result set would produce exactly the false-positive drift signal Plan 88-07 just fixed.

88-RESEARCH.md `## Open Questions` #1 is still open: `probe.sh`'s whitespace/case-variant
milestone-title collision scan (RELMS-04) and its `project_id`/`group_id` presence check
(assumption A3, decision D-07) have never run live. 88-01-SUMMARY.md confirms
`findDuplicateMilestone` "remains probe-gated". Task 3 below exercises the *exact-title*
duplicate path, which is a different and weaker check than the normalization scan the open
question calls for — so Task 5 runs the probe.

Purpose: convert "unit-tested" into "observed working", against the real instance, with the real
token, including the permission-failure path and the normalization assumptions.
Output: recorded checkpoint outcomes in the SUMMARY, and a resolved Open Question #1.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-release-branch-milestone-creation/88-CONTEXT.md
@.planning/phases/88-release-branch-milestone-creation/88-VERIFICATION.md
@.planning/phases/88-release-branch-milestone-creation/88-05-SUMMARY.md
@.planning/phases/88-release-branch-milestone-creation/88-06-SUMMARY.md
@.planning/phases/88-release-branch-milestone-creation/88-07-SUMMARY.md
@.planning/phases/88-release-branch-milestone-creation/88-09-SUMMARY.md
@.planning/phases/88-release-branch-milestone-creation/88-10-SUMMARY.md
</context>

<out_of_scope>
- **Open Question #2** (exact GitLab CE/EE version of the team's instance). Left open
  deliberately: RESEARCH.md itself classifies it as non-blocking, and its own recommendation is
  "if `probe.sh` surfaces an unexpected response shape, escalate then". Task 5 runs that probe,
  so this question is discharged by escalation-if-needed rather than by a dedicated check. If
  Task 5's Probe A/B output shows any shape deviation from the documented schema, record it in
  the SUMMARY and escalate — do not absorb it silently.
- **Acting on a Probe C finding.** Task 5 *detects* whether the team's real milestone titles
  contain whitespace-only or case-only collisions that `normalizeMilestoneTitle` would treat
  differently from GitLab's server-side check. If it finds one, this plan records and escalates
  it; changing the normalization rules is a separate, scoped change with its own tests, not
  something to improvise inside a verification plan.
</out_of_scope>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite gate and launch the app for live verification</name>
  <files>(none — verification only; no source files are modified by this task)</files>
  <read_first>
    - .planning/phases/88-release-branch-milestone-creation/88-07-SUMMARY.md,
      88-08-SUMMARY.md, 88-09-SUMMARY.md, 88-10-SUMMARY.md — confirm every fix plan completed and
      note any recorded deviation that changes what the checkpoints below should expect.
    - .planning/phases/88-release-branch-milestone-creation/88-CONTEXT.md §Established Patterns —
      the quality gate is `npm run check`, and the project baseline is **2 pre-existing biome
      formatting errors** in `BacklogPage.tsx` / `BacklogRow.tsx`. Gate on zero NEW errors
      relative to that baseline; do not fix the Backlog files here.
  </read_first>
  <action>
    Run the full test suite and the type/lint gate across the whole app, then start the dev build
    so the human checkpoints below have a running application to drive:
    - `cd taskflow && npx vitest run` — record the total pass count and compare with the 187/187
      recorded in 88-VERIFICATION.md; the new count must be higher (this gap-closure round added
      roughly 20 regression tests) with zero failures.
    - `cd taskflow && npx tsc --noEmit` — must be clean.
    - `cd taskflow && npx biome check src` — must show exactly the 2 known baseline errors in
      `BacklogPage.tsx` / `BacklogRow.tsx` and no others.
    - Start the app (`npm run tauri dev` from `taskflow/`, or the project's usual dev command) and
      leave it running for the checkpoints.
    If any gate fails, STOP and report rather than proceeding to the live writes — a live write
    against real GitLab from a broken build is not recoverable by re-running.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npx vitest run` exits 0, with a total test count strictly greater than 187
      and zero failures.
    - `cd taskflow && npx tsc --noEmit` exits 0 with no output.
    - Every error reported by `cd taskflow && npx biome check src` is in `BacklogPage.tsx` or
      `BacklogRow.tsx`; no error is reported in any file touched by Plans 88-07 through 88-10.
    - The app is running and the Releases tab renders against the configured live GitLab project.
  </acceptance_criteria>
  <done>All automated gates pass at or above the pre-gap-closure baseline, and a live app instance is available for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live create-branch against real GitLab, including the D-18 branch-set filter (RELBR-04, un-waives 88-05-T3)</name>
  <files>(none — live verification against GitLab; no source files modified)</files>
  <read_first>
    - .planning/phases/88-release-branch-milestone-creation/88-05-SUMMARY.md — what the waived
      88-05-T3 checkpoint was supposed to cover.
    - .planning/phases/88-release-branch-milestone-creation/88-CONTEXT.md D-14, D-15, D-18.
    - taskflow/src/services/gitlab.ts `createBranch` and `fetchProjectBranches` — the exact
      request shape and query parameters being validated live.
  </read_first>
  <action>
    Present this checkpoint to the user and BLOCK until they respond. Do not perform the GitLab
    writes yourself and do not mark the checkpoint resolved on the user's behalf. Print the
    verification script below verbatim, wait for the resume signal, then record the outcome —
    including any step that deviated — in the plan SUMMARY. If the user declines, record it as
    `waived — not performed`, never as verified.
  </action>
  <what-built>
    `createBranch` posts to `POST /api/v4/projects/:id/repository/branches` with `ref` taken from
    the project's fetched `default_branch` (never a hardcoded `main`). The sidebar's Release
    Branch row now distinguishes exists / missing / loading / check-failed / unresolvable /
    blocked, and invalidates both `['gitlab-branch', …]` and
    `['gitlab-release-branches', activeGitlabProject]` on success. Plan 88-07 gated the Releases
    list indicator on `isSuccess`. None of this has ever run against a real GitLab instance.
  </what-built>
  <how-to-verify>
    1. Open the Releases tab. Pick an unreleased fix version that has a release date and a
       matched GitLab milestone whose title is `X.Y.Z (DD.MM.YYYY)`, and for which the
       corresponding `release/X.Y.Z` branch does NOT yet exist in GitLab.
    2. Confirm the row shows the orange missing-branch triangle with tooltip "No release branch".
       Reload the tab and watch closely during the initial load: the triangle must NOT flash in
       before the branch query resolves (this is CR-01).
    3. Open that release's detail view. The sidebar "Release Branch" row must read
       "No release branch" with an enabled "Create branch" button.
    4. Click "Create branch". The confirm dialog must name the exact branch (`release/X.Y.Z`) and
       the exact ref it will branch from. Verify that ref equals the project's real default
       branch in GitLab (GitLab -> Settings -> Repository -> Default branch). Confirm.
    5. Expected: the dialog closes, and within a few seconds the sidebar row flips to a green
       check showing `release/X.Y.Z` — with no manual page reload.
    6. In GitLab, open the project's Branches page. The new branch must exist and its head commit
       must equal the head of the default branch at creation time.
    7. Navigate back to the Releases tab. The missing-branch triangle for that row must be gone
       without a manual refresh.
    8. **D-18 filter check.** In a browser or terminal, call
       `GET <gitlab>/api/v4/projects/<id>/repository/branches?search=release/&per_page=100`
       with your PAT and compare the returned names against the project's full branch list.
       Confirm that (a) every branch whose name starts with `release/` is present, and (b) the
       filter is a real server-side prefix/substring filter, not a no-op returning everything.
       If the project has more than 100 `release/*` branches, confirm the app's paginated fetch
       returned all of them (the branch set is fully walked with no page cap).
    9. Delete the test branch in GitLab afterwards if it was created only for this check.
  </how-to-verify>
  <verify>
    <human-check>Steps 2, 5, 6, 7 and 8 all behaved as described, confirmed by the user.</human-check>
  </verify>
  <resume-signal>Type "approved" if steps 2, 5, 6, 7 and 8 all behaved as described, or describe exactly which step deviated and what you observed instead.</resume-signal>
  <acceptance_criteria>
    - The branch exists in GitLab at the project's real default-branch ref.
    - No missing-branch triangle flashes during the Releases-tab initial load (CR-01 confirmed
      fixed in live conditions).
    - The sidebar flips missing -> exists with no manual reload.
    - The Releases-list row clears without a manual refresh.
    - The `search=release/` response is confirmed to be a genuine server-side filter returning
      the complete `release/*` set.
  </acceptance_criteria>
  <done>RELBR-04's write path is confirmed working against a real GitLab instance, and D-18's fetch strategy is confirmed against the real API.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live create-milestone and cross-view propagation (RELMS-02, closes CR-02, un-waives 88-06-T3)</name>
  <files>(none — live verification against GitLab; no source files modified)</files>
  <read_first>
    - .planning/phases/88-release-branch-milestone-creation/88-06-SUMMARY.md — the waived
      88-06-T3 checkpoint, and the correction commit that recorded it as waived rather than done.
    - .planning/phases/88-release-branch-milestone-creation/88-09-SUMMARY.md — the CR-02
      invalidation-granularity fix whose real-world effect step 8 validates.
    - .planning/phases/88-release-branch-milestone-creation/88-CONTEXT.md D-03, D-04, D-06.
  </read_first>
  <action>
    Present this checkpoint to the user and BLOCK until they respond. Do not perform the GitLab
    write yourself. Print the verification script below verbatim, wait for the resume signal,
    then record the outcome in the plan SUMMARY. Step 8 is the decisive one: if the Releases list
    still shows "No GitLab link" after navigating back, CR-02 is NOT closed — record that as a
    failed checkpoint and stop rather than rationalising it as cache staleness. Note that step 5
    exercises only the EXACT-title duplicate path; the whitespace/case-variant normalization
    question is Task 5's job and is not discharged here.
  </action>
  <what-built>
    `createMilestone` posts `{ title, due_date }` to `POST /api/v4/projects/:id/milestones`
    (D-04: the due date is mandatory because `resolveGitLabMatch` matches by date). Plan 88-09
    changed `onSuccess` to invalidate the project-granular key
    `['gitlab-milestones', activeGitlabProject]` instead of the detail page's windowed key, so
    the Releases list — which caches the same prefix under a different window — is finally
    refreshed by the write. Plan 88-10 made the dialog prefill a valid, submittable title.
    This write has never executed against a real GitLab instance.
  </what-built>
  <how-to-verify>
    1. In the Releases tab, pick an unreleased fix version with a release date that currently has
       NO matching GitLab milestone (row shows "No GitLab link" and the missing-milestone
       triangle). Note the version name and its release date.
    2. Open its detail view and click "Create milestone".
    3. Expected (WR-01): the title field opens PREFILLED with a valid `X.Y.Z (DD.MM.YYYY)` value
       derived from the Jira version name and release date — no leading space, no red format
       error, and the "Create milestone" button enabled on first render.
    4. Confirm the "Recent milestones" reference list shows real project milestones and does NOT
       include inherited group/ancestor milestones (D-06).
    5. Type an existing milestone's exact title. Expected: the duplicate message appears and
       submit is blocked (RELMS-04) — confirming duplicate detection is live, not silently
       disabled. Restore the prefilled title.
    6. Confirm creation. Expected: the dialog closes; the detail sidebar's GitLab milestone row
       now shows the match, and the Release Branch row transitions out of
       "Create the milestone first".
    7. In GitLab, open the project's Milestones page. The milestone must exist with the exact
       typed title AND a `due_date` equal to the Jira release date (not blank, not shifted by a
       day — a timezone shift here would be a real defect).
    8. **CR-02, the critical step.** Navigate back to the Releases tab WITHOUT reloading the app
       and WITHOUT waiting five minutes. The row must now show the GitLab link and the
       missing-milestone triangle must be gone. If it still shows "No GitLab link", CR-02 is not
       actually closed — report that immediately.
    9. Delete the test milestone in GitLab afterwards if it was created only for this check.
  </how-to-verify>
  <verify>
    <human-check>Steps 3, 5, 6, 7 and especially 8 behaved as described, confirmed by the user.</human-check>
  </verify>
  <resume-signal>Type "approved" if steps 3, 5, 6, 7 and especially 8 behaved as described, or describe exactly which step deviated. Step 8 failing means CR-02 is still open.</resume-signal>
  <acceptance_criteria>
    - The milestone exists in GitLab with the exact title and a `due_date` matching the Jira
      release date exactly (no off-by-one).
    - The dialog opened with a valid, submittable prefilled title.
    - Typing an existing title blocked submit with a duplicate message.
    - The Releases list reflects the new milestone on navigation back, with no app reload and no
      five-minute wait.
  </acceptance_criteria>
  <done>RELMS-02's write path is confirmed against real GitLab and CR-02's cross-view propagation is confirmed in the exact scenario the verifier predicted would fail.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Restricted-PAT error surfacing and branch-fetch-error distinctness (WR-11, CR-01, CR-03)</name>
  <files>(none — live verification against GitLab; no source files modified)</files>
  <read_first>
    - .planning/phases/88-release-branch-milestone-creation/88-10-SUMMARY.md — the WR-11
      body-first error classification and the WR-03 dismissal lock being validated here.
    - .planning/phases/88-release-branch-milestone-creation/88-07-SUMMARY.md and 88-09-SUMMARY.md
      — the "GitLab unavailable" chip and the `check-failed` branch row.
    - taskflow/src/services/api.ts (or wherever `apiFetch` and its PAT redaction live) — confirm
      what the devtools instrumentation is expected to show before asking the user to inspect it.
  </read_first>
  <action>
    Present this checkpoint to the user and BLOCK until they respond. Instruct the user to create
    a SECOND, deliberately scope-restricted GitLab PAT for this test and to restore the real
    token afterwards; do not create or handle tokens yourself. Print the verification script
    below verbatim, wait for the resume signal, then record the outcome in the plan SUMMARY —
    including the two verbatim GitLab error strings the user reports from steps 1 and 2, which
    are the direct evidence that WR-11's fix works against a real instance.
  </action>
  <what-built>
    Plan 88-10 made `createBranch` and `createMilestone` read GitLab's response body BEFORE
    classifying 401/403, so the dialog shows GitLab's own explanation instead of
    "Failed to create branch". Plan 88-07 added a distinct "GitLab unavailable" chip for a failed
    branch fetch so it no longer reads as drift. Plan 88-09 made a failed branch-existence check
    render as `check-failed` with a Retry instead of a permanent "Loading...". All three are
    error paths that mocked fetch cannot validate for behavioural confidence.
  </what-built>
  <how-to-verify>
    Create a SECOND GitLab personal access token with deliberately insufficient scope (e.g.
    `read_user` only, no `api`, no `read_repository`). Swap it into TaskFlow's GitLab PAT setting
    for this test, then restore the real token afterwards.

    1. **Write-path 403 (WR-11).** With the restricted token, open a release detail view and
       attempt "Create branch". Expected: the dialog STAYS OPEN and shows GitLab's actual message
       text (something like "403 Forbidden" or "insufficient_scope", not the bare literal
       "Failed to create branch"). Record the exact string shown.
    2. Repeat for "Create milestone" and record the exact string shown.
    3. **Dismissal lock (WR-03).** While a create request is in flight, try pressing Escape and
       clicking Cancel. Expected: neither dismisses the dialog until the request settles.
       (This may be hard to hit on a fast local instance — if the request settles too quickly to
       test, say so rather than guessing.)
    4. **No secret leakage.** Confirm the error text shown in the dialog does NOT contain the
       token value or the string `PRIVATE-TOKEN`. Also check the devtools network/console panel:
       `apiFetch`'s instrumentation must still redact the PAT.
    5. **Branch-check failure (CR-03).** Still on the restricted token, open a release detail
       view whose milestone matches and whose branch name derives. Expected: the "Release Branch"
       row shows the orange "Couldn't check the release branch" error with a "Retry" button —
       NOT a permanent "Loading...", and NOT a "No release branch" claim. Click "Retry" and
       confirm it re-attempts (it will fail again with this token; that is fine — the point is
       the affordance works and the state is honest).
    6. **List-level distinctness (CR-01).** Go to the Releases tab with the restricted token.
       Expected: the header shows a "GitLab unavailable" chip, and NO row shows the
       missing-branch triangle. A failed fetch must read as unavailability, not as drift.
       Confirm you can tell this state apart at a glance from Task 2 step 2's genuine
       missing-branch state.
    7. Restore the real PAT and confirm normal operation resumes (branch states resolve, no
       unavailable chip). Revoke the restricted token in GitLab.
  </how-to-verify>
  <verify>
    <human-check>Steps 1, 2, 4, 5, 6 and 7 behaved as described, with the two verbatim error strings supplied by the user.</human-check>
  </verify>
  <resume-signal>Type "approved" if steps 1, 2, 4, 5, 6 and 7 behaved as described — and paste the two exact error strings you recorded in steps 1 and 2 so they can go in the SUMMARY. Otherwise describe what you saw.</resume-signal>
  <acceptance_criteria>
    - Both create dialogs display GitLab's own message text on 403, not the generic fallback;
      the exact strings are recorded in the SUMMARY.
    - No displayed error string and no devtools log line contains the PAT value or
      `PRIVATE-TOKEN`.
    - The Release Branch row shows "Couldn't check the release branch" with a working Retry,
      never a permanent "Loading..." and never a false "No release branch".
    - The Releases tab shows the "GitLab unavailable" chip with zero missing-branch triangles.
    - Normal operation resumes on restoring the real PAT.
  </acceptance_criteria>
  <done>The three error paths that mocked tests cannot validate are confirmed to be honest, actionable, and free of credential leakage under a real restricted token.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Run probe.sh's live milestone-title collision scan and resolve 88-RESEARCH.md Open Question #1 (RELMS-04, D-07/A3)</name>
  <files>.planning/phases/88-release-branch-milestone-creation/88-RESEARCH.md</files>
  <read_first>
    - .planning/phases/88-release-branch-milestone-creation/probe.sh — the whole script. It is
      read-only (all GETs). Probe A paginates
      `GET /projects/:id/milestones?include_ancestors=true`; Probe B reports `project_id` /
      `group_id` presence and prints an explicit PASS/FAIL verdict line for D-07; Probe C prints
      the own-project titles, a `group_by(title | ascii_downcase | trim)` collision list, all
      titles failing the `X.Y.Z (DD.MM.YYYY)` format regex, and the active/closed state
      distribution. Probe C's own verdict line says "manual review required".
    - .planning/phases/88-release-branch-milestone-creation/88-RESEARCH.md `## Open Questions`
      (around line 459) — question #1's exact wording, and `## Assumptions Log` A3, which Probe B
      exists to settle.
    - .planning/phases/88-release-branch-milestone-creation/88-01-SUMMARY.md — the record that
      `findDuplicateMilestone` "remains probe-gated".
    - taskflow/src/routes/dashboard/release-detail/releaseMilestone.ts — the three functions this
      probe validates: `normalizeMilestoneTitle` (trim, collapse internal whitespace runs to a
      single space, lowercase), `findDuplicateMilestone`, and `ownProjectMilestones` (which
      returns the list UNFILTERED when NO element carries a numeric `project_id`, and filters on
      `project_id === projectId` otherwise). Probe B's verdict decides which of those two
      behaviours the real instance triggers.
    - .planning/phases/88-release-branch-milestone-creation/88-CONTEXT.md D-05, D-06, D-07, D-08.
  </read_first>
  <action>
    Present this checkpoint to the user and BLOCK until they respond. Do not attempt to run
    `probe.sh` yourself — it needs a PAT that lives in the app's Tauri stronghold and is not
    readable from the shell; the user must supply `GITLAB_BASE_URL`, `GITLAB_PAT` and
    `PROJECT_ID` themselves. Print the instructions below verbatim, wait for the pasted probe
    output, then:

    1. Read the pasted output and evaluate it against the five acceptance criteria below.
    2. If all five pass, edit
       `.planning/phases/88-release-branch-milestone-creation/88-RESEARCH.md`: rename the heading
       `## Open Questions` to `## Open Questions (RESOLVED)`, and replace question #1's
       `Recommendation:` bullet with a `Resolved:` bullet recording the date, the Probe B verdict
       line verbatim, the number of Probe C collisions found, and the number of off-format titles
       found. Leave question #2's content untouched apart from the heading rename, and add one
       line under it noting it is discharged by escalation-if-needed per this plan's
       `<out_of_scope>`.
    3. Record the probe output in the plan SUMMARY — the full block if short, otherwise Probe B's
       verdict line plus Probe C's three lists.
    4. **Anti-rationalization guard, identical to Task 3's.** If the user declines to run the
       probe, record it in the SUMMARY as `declined — not performed` and leave the RESEARCH.md
       heading unchanged. If the probe runs but the output is ambiguous, truncated, or the user
       is unsure (e.g. Probe C prints collisions and it is unclear whether they matter), record
       it as `inconclusive: <the specific ambiguity>` and leave the heading unchanged. Never mark
       Open Question #1 resolved on a partial or inferred result, and never soften a collision
       finding into a pass.
    5. If Probe C reports any COLLISION line, or Probe B reports FAIL, do NOT change the
       normalization or the D-07 filter here — that is out of scope for this plan. Record the
       finding, mark the question `resolved — action required`, and escalate with a concrete
       description of which real titles collide and what the client-side normalization would do
       with them.
  </action>
  <what-built>
    `normalizeMilestoneTitle` (trim + collapse internal whitespace + lowercase) and
    `findDuplicateMilestone` were implemented against fixtures only. `ownProjectMilestones`
    implements the D-07 ancestor filter with a defensive fallback for assumption A3: if no
    milestone carries a numeric `project_id`, it returns the list unfiltered rather than empty.
    Which of those two paths the team's real GitLab instance actually takes has never been
    observed, and whether the team's real milestone titles contain whitespace-only or case-only
    variants that would confuse the comparison is unknown. `probe.sh` was written during research
    specifically to answer both, and has never been run.
  </what-built>
  <how-to-verify>
    1. From the repo root, run the probe against the REAL production project (not the throwaway
       one used in Tasks 2-3 — the point is to inspect the team's actual titles):

       `GITLAB_BASE_URL="https://<your-gitlab>" GITLAB_PAT="<pat with read access>" PROJECT_ID="<activeGitlabProject>" bash .planning/phases/88-release-branch-milestone-creation/probe.sh`

       It is read-only (all GETs). Prefix the command with a space so the PAT does not land in
       shell history. If `jq` is missing, install it or say so — do not hand-edit the script.
    2. Paste the entire block between `==== PHASE 88 PROBE RESULTS ====` and `==== END ====`
       back into the chat.
    3. While pasting, answer three questions explicitly:
       a. **Probe B verdict** — does the last Probe B line say PASS (project_id present, D-07
          local filter viable) or FAIL (project_id absent, D-07 fallback needed)?
       b. **Probe C collisions** — did the "trimmed-duplicate check" section print any
          `COLLISION:` lines? If yes, paste them and say whether the colliding titles are
          genuinely the same release or two different releases that merely normalize alike.
       c. **Off-format titles** — how many titles appear under "titles NOT matching the
          D-01/D-02 real format"? Are any of them titles the team still considers active?
    4. Sanity-check the app against the probe: open the create-milestone dialog for any release
       and confirm the "Recent milestones" reference list contains the same own-project titles
       Probe C printed for the overlapping date window, and does NOT contain any title Probe B
       classified as inherited/ancestor. A mismatch means the D-07 filter behaves differently in
       the app than the probe predicts — report it rather than assuming they agree.
    5. Delete `/tmp/phase88-titles.json`, which the probe writes.
  </how-to-verify>
  <verify>
    <human-check>Probe output pasted; Probe B verdict, Probe C collision count and off-format title count all reported; the app's reference list matches Probe C's own-project titles.</human-check>
  </verify>
  <resume-signal>Paste the probe output block and answer 3a/3b/3c, then type "approved". Type "declined" if you will not run it, or "inconclusive: [what was ambiguous]" if the output does not settle the question.</resume-signal>
  <acceptance_criteria>
    - **Probe B (A3 / D-07):** the verdict line is reported verbatim. PASS means `project_id` is
      present, so `ownProjectMilestones` takes its filtering path and D-07 is confirmed as
      implemented. FAIL means the field is absent, so `ownProjectMilestones` silently returns the
      list unfiltered and inherited group milestones would appear in both the reference list and
      the duplicate check — a valid probe result, but NOT a pass for this criterion: record it as
      `resolved — action required` and escalate.
    - **Probe C collisions (RELMS-04):** the "trimmed-duplicate check" section prints ZERO
      `COLLISION:` lines. Any collision means two distinct real milestones normalize to the same
      string under `normalizeMilestoneTitle`, so `findDuplicateMilestone` would block a
      legitimate title — record the exact colliding titles, mark `resolved — action required`,
      escalate, and do not change the normalization here.
    - **Probe C off-format titles:** the count is reported. A non-zero count is acceptable
      (legacy titles are expected, and D-11 already handles unparseable ones), but every one must
      be listed in the SUMMARY so the branch-derivation implications are visible. Flag any
      off-format title belonging to a currently active release.
    - **App / probe agreement:** the create-milestone dialog's "Recent milestones" list contains
      the same own-project titles Probe C printed for the overlapping date window, with no
      inherited/ancestor titles present.
    - **Documentation:** on a full pass,
      `grep -c "## Open Questions (RESOLVED)" .planning/phases/88-release-branch-milestone-creation/88-RESEARCH.md`
      returns 1, and question #1 contains a `Resolved:` bullet naming the date and the Probe B
      verdict. On a decline or an inconclusive run that grep returns 0 and the SUMMARY records
      `declined — not performed` or `inconclusive: <reason>` respectively.
  </acceptance_criteria>
  <done>88-RESEARCH.md Open Question #1 is settled by real data: `findDuplicateMilestone`'s normalization and the D-07 ancestor filter are either confirmed against the team's actual milestone titles, or the specific mismatch is documented and escalated — with a decline or an ambiguous run recorded as exactly that, never as verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human tester → live GitLab project | This plan deliberately performs real, persistent writes against a production-adjacent GitLab project |
| Restricted PAT → app error surfaces | A second, low-scope token is introduced into the app's secret store for the duration of Task 4 |
| GitLab error body → dialog copy and devtools logs | Real server messages flow to the UI and the instrumentation layer for the first time |
| Shell-supplied PAT → probe.sh and pasted output | Task 5 requires a PAT in a shell environment variable and pastes API output into the chat transcript |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-11-01 | Tampering | Live GitLab project state | mitigate | Tasks 2 and 3 create real, persistent objects. Both checkpoints require choosing a throwaway project or a safe namespace up front (`user_setup`), and both end with an explicit cleanup step. Task 1 blocks the live writes behind a green full-suite gate so a broken build never writes. Task 5 is strictly read-only (`probe.sh` issues only GETs). |
| T-88-11-02 | Information disclosure | PAT in error copy or devtools | mitigate | Task 4 step 4 is an explicit, blocking check that neither the dialog text nor the devtools instrumentation contains the token value or `PRIVATE-TOKEN`. Plan 88-10 asserted this in source; this checkpoint confirms it in the running app. |
| T-88-11-03 | Information disclosure | Second restricted PAT lifecycle | mitigate | The restricted token is explicitly scoped (`read_user` only, no `api`), is swapped in only for Task 4, and Task 4 step 7 requires restoring the real PAT and revoking the restricted one in GitLab. |
| T-88-11-04 | Elevation of privilege | Writing with an over-scoped token | accept | Tasks 2 and 3 require an `api`-scoped PAT — that is the app's normal operating scope (per 88-CONTEXT.md, permission gating is explicitly out of scope for this phase; the team is all Developer+). No new privilege is introduced. |
| T-88-11-05 | Repudiation | Unrecorded checkpoint outcome | mitigate | This plan exists precisely because two earlier checkpoints were waived without evidence. Each checkpoint's `resume-signal` demands specific observations (two verbatim error strings, the probe output block), and each `<action>` instructs the executor to record a decline as `waived — not performed` / `declined — not performed`, and an ambiguous Task 5 run as `inconclusive`, never as verified. |
| T-88-11-06 | Information disclosure | `probe.sh` PAT handling and pasted output (Task 5) | mitigate | The probe reads its token from `GITLAB_PAT` and sends it only in a `PRIVATE-TOKEN` header; it never echoes the token — its output carries milestone ids, titles, states and due dates only. The instructions require prefixing the command with a space (keeping the PAT out of shell history), confirming before pasting that the output contains no token material, and deleting `/tmp/phase88-titles.json` afterwards. |
| T-88-11-07 | Tampering | `probe.sh` integrity | mitigate | The script is committed in this repo and is reviewed as part of `<read_first>`. The executor must run it as-is and must not hand-edit it to make a check pass; a missing `jq` is reported, not worked around. |
| T-88-11-SC | Tampering | npm installs | mitigate | No dependencies are added. The only file this plan modifies is `88-RESEARCH.md` (documentation), and only on a full Task 5 pass. |
</threat_model>

<verification>
- `cd taskflow && npx vitest run` — zero failures, total count above the 187 baseline.
- `cd taskflow && npx tsc --noEmit` — clean.
- `cd taskflow && npx biome check src` — only the 2 known baseline errors in
  `BacklogPage.tsx` / `BacklogRow.tsx`.
- All four human checkpoints resolved with an explicit "approved" plus the recorded observations
  each one demands. If the user waives or declines a checkpoint, the SUMMARY MUST record it as
  `waived — not performed` / `declined — not performed` (following the precedent set by 88-06's
  corrected SUMMARY), never as verified, so the next verification pass sees the truth.
- On a Task 5 pass:
  `grep -c "## Open Questions (RESOLVED)" .planning/phases/88-release-branch-milestone-creation/88-RESEARCH.md`
  returns 1. On a decline or inconclusive run it returns 0 and the SUMMARY records why.
</verification>

<success_criteria>
- `createBranch` has executed successfully against a real GitLab instance, off the real
  `default_branch`, with the result visible in GitLab and reflected in both views.
- `createMilestone` has executed successfully against a real GitLab instance with a correct
  `due_date`, and CR-02's cross-view propagation is confirmed in the exact failing scenario.
- D-18's `search=release/` server-side filter semantics are confirmed against the real API.
- A restricted PAT produces GitLab's own actionable message in both dialogs, with no credential
  leakage, and both the list-level and detail-level error states read as unavailability rather
  than as drift.
- `probe.sh` has run against the real project; Probe B's D-07/A3 verdict and Probe C's collision
  and off-format counts are recorded; `findDuplicateMilestone` is no longer probe-gated.
- 88-RESEARCH.md Open Question #1 is marked resolved — or explicitly recorded as declined /
  inconclusive / resolved-action-required, with the reason.
- The two previously waived checkpoints (88-05-T3, 88-06-T3) are explicitly discharged by name
  in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/88-release-branch-milestone-creation/88-11-SUMMARY.md` when done.
It MUST explicitly state, by name, the disposition of 88-05-T3 and 88-06-T3 (discharged by
Task 2 / Task 3, or waived again), record the two verbatim GitLab error strings captured in
Task 4, and record Task 5's probe outcome (Probe B verdict line, Probe C collision count,
off-format title count) together with the resulting status of 88-RESEARCH.md Open Question #1.
</output>
