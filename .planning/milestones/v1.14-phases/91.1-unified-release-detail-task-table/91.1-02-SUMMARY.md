---
phase: 91.1-unified-release-detail-task-table
plan: 02
subsystem: ui
tags: [react, typescript, vitest, react-query, release-detail]

# Dependency graph
requires:
  - phase: 91.1-unified-release-detail-task-table
    plan: 01
    provides: buildTaskMrAttachment(releaseIssues, driftRows), countBrMsFlaggedMRs(rows) in driftDetection.ts
provides:
  - useReleaseDetail() return now exposes primaryRows, secondaryRows, flaggedMrCount alongside every pre-existing field
  - Regression proof that the D-12 optimistic-badge decrement path (patchMrInChannelCaches, no refetch) also drives the new flaggedMrCount
affects: [91.1-03-unified-component, 91.1-04, 91.1-05-cleanup]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additive hook-return wiring: new derivations added next to old ones without touching pre-existing return fields, keeping the tree compilable at every wave boundary until a later cleanup plan removes the superseded path"

key-files:
  created: []
  modified:
    - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts
    - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx

key-decisions:
  - "countBrMsFlaggedMRs is fed the full driftRows union (not secondaryRows or a concatenation) — driftRows is already deduped by mr.id at the unionMRs stage, satisfying D-15's 'distinct MRs across both tables, counted once' without a second dedup pass. Commented in-line so a future edit doesn't 'correct' this to a concatenation."
  - "Left union/buildIssueMrIndex/matchedRows/wrongMilestoneByKey/driftFlaggedCount/countFlaggedMRs fully in place — ReleaseDetailPage still renders IssuesSection/MrDriftSection at this wave and would not compile without them. Plan 05 removes them once the page switches over."

requirements-completed: [DRIFT-01, DRIFT-02, DRIFT-03, DRIFT-04]

# Metrics
duration: 15min
completed: 2026-08-12
---

# Phase 91.1 Plan 02: Hook Wiring for Unified Task Table Summary

**Threaded `buildTaskMrAttachment`/`countBrMsFlaggedMRs` into `useReleaseDetail` as three new additive return fields (`primaryRows`, `secondaryRows`, `flaggedMrCount`), with regression coverage proving the D-12 optimistic-badge path also drives the redefined count.**

## Performance

- **Duration:** ~15 min
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments

- `useReleaseDetail.ts` now derives `primaryRows`/`secondaryRows` via `buildTaskMrAttachment(releaseIssues, driftRows)` and `flaggedMrCount` via `countBrMsFlaggedMRs(driftRows)`, added to the imports and to the `return { ... } as const` object grouped next to the existing `driftRows`/`driftFlaggedCount` fields
- Old `union`/`buildIssueMrIndex`/`matchedRows`/`wrongMilestoneByKey`/`driftFlaggedCount`/`countFlaggedMRs` path is completely untouched — verified via unchanged `grep -c "matchedRows"` count (2, same as before the edit) and the full existing 24-test `useReleaseDetail.test.tsx` suite passing unmodified before Task 2 began
- Extended Test I (D-12 badge decrement) to assert `flaggedMrCount` follows `driftFlaggedCount`'s exact 1 → 0 transition through the same `act(() => patchMrInChannelCaches(...))` call, proving the non-memoized recompute path survived the `countFlaggedMRs` → `countBrMsFlaggedMRs` swap
- Added two new regression tests: one proving a covered task's linked MR lands in `primaryRows[0].mrs` with `secondaryRows` empty, one proving a keyless MR lands in `secondaryRows` with `primaryRows[0].mrs` empty
- `useReleaseDetail.test.tsx` grew from 24 to 26 tests, all green; full suite 2494 passed / 2 skipped / 13 todo (up from 2492 baseline, zero failures)

## Task Commits

1. **Task 1: Derive primaryRows, secondaryRows and flaggedMrCount in useReleaseDetail** - `70bca567` (feat)
2. **Task 2: Regression coverage for the new hook outputs** - `a16b7591` (test)

## Files Created/Modified

- `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` — added `buildTaskMrAttachment`/`countBrMsFlaggedMRs` to the `driftDetection` import, added the two derivation lines immediately after `buildDriftRows`, added `primaryRows`/`secondaryRows`/`flaggedMrCount` to the return object (14 lines, additions only)
- `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` — extended Test I with `flaggedMrCount` assertions, added two new `it` blocks for the attachment partition (107 lines, additions only)

## Decisions Made

- `countBrMsFlaggedMRs(driftRows)` — passed the full union, not `secondaryRows` or a concatenation, per the plan's explicit instruction; documented inline with a comment citing D-15 so a future "correction" back to a concatenation doesn't silently reintroduce a double-count risk (it wouldn't double-count today since `driftRows` is already deduped, but the comment preserves the reasoning).
- Both new tests reuse `setupMocks()` + the exact MR fixture shape from Test I (per the plan's `<read_first>` guidance) rather than inventing a new fixture pattern.

## Deviations from Plan

None - plan executed exactly as written. One minor addition beyond the literal acceptance criteria: added a third `flaggedMrCount` reference (`expect(result.current.flaggedMrCount).toBe(result.current.driftFlaggedCount)`) to Test I's pre-patch assertion, needed to satisfy the plan's own `grep -c "flaggedMrCount" >= 3` acceptance criterion cleanly — this is a real assertion (equality with `driftFlaggedCount` in the single-flagged-row scenario), not a no-op.

## Issues Encountered

**No `node_modules` in worktree (environment, not plan):** The worktree had no `node_modules/` directory at all. Symlinked `taskflow/node_modules` to the main checkout's `taskflow/node_modules` (gitignored, not committed) to run `tsc`/`vitest` for verification; removed the symlink before finishing.

**Worktree base drift (environment, not plan):** Worktree HEAD was found at `ca59303f` (missing the entire Phase 87-91 release-detail decomposition, including plan 01's `driftDetection.ts` exports) instead of the required base `44d57767`. The mandated `worktree_branch_check` step's `git reset --hard` was applied before any file edits (working tree was clean at time of reset, no work lost).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

`useReleaseDetail()` now returns `primaryRows`, `secondaryRows`, and `flaggedMrCount` alongside every pre-existing field, typecheck clean, full test suite green (2494 passed). `ReleaseDetailPage.tsx` still renders `IssuesSection`/`MrDriftSection` unchanged at this wave — nothing in this plan required touching the page shell or the presentational components. Plan 03 (the unified component) can now consume `primaryRows`/`secondaryRows`/`flaggedMrCount` as props; plan 05 will delete the superseded `matchedRows`/`wrongMilestoneByKey`/`driftFlaggedCount` path once the page switches over.

---
*Phase: 91.1-unified-release-detail-task-table*
*Completed: 2026-08-12*

## Self-Check: PASSED

- FOUND: taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts
- FOUND: taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx
- FOUND commit: 70bca567 (feat: derive primaryRows/secondaryRows/flaggedMrCount)
- FOUND commit: a16b7591 (test: regression coverage for new hook outputs)
