---
status: resolved
phase: 91.1-unified-release-detail-task-table
source: [91.1-VERIFICATION.md]
started: 2026-08-12
updated: 2026-08-12T00:00:00Z
---

## Current Test

[testing complete]

<!-- Context: these four items have automated coverage but were never walked live. The
developer approved the phase overall on 2026-08-12 without reporting per-step outcomes, so
they are recorded as pending rather than inferred as passing. Two are worth an actual look
because the click model changed *after* the last live walkthrough: the gap-closure fix pass
(CR-05) rewrote which parts of a task row are clickable. -->

## Tests

### 1. Badge filtering end-to-end
expected: clicking `⚠ N wrong branch` / `⚠ N no milestone` filters the table with a
"Showing only…" notice and a `Clear filter` button; clicking again toggles it off; fixing a
flagged MR from inside the filtered view decrements the count and drops the row out of the
filter without re-sorting the table
result: pass

### 2. WR-01 breadcrumb regression via the changed click surface
expected: open a task peek and close it, then click a Jira key inside an MR title in the
secondary (uncovered MRs) table — the breadcrumb shows the release name once, not twice.
Note: ticket keys are now linkified only in the secondary table, since the primary table no
longer renders MR titles.
result: issue
reported: "clicking to open peek version of the task appends the same release into breadcrumbs. it shouldnt do anything. other cases work"
severity: major

### 3. CR-01/CR-02 reload-timing flash
expected: reload a real release detail page — no "No merge request" or "not covered by tasks
above" text flashes before the data arrives
result: pass

### 4. Row click surface after the CR-05 fix
expected: clicking anywhere on a task row that is not the MR link, the `+N` marker or an
actionable BR/MS cell opens the issue. Before the fix, the ~190px MR column silently swallowed
row clicks. The MR link still opens GitLab, and the `+N` marker reveals the hidden MRs on hover.
result: pass

### 5. Jira key clickability in the secondary "not covered by tasks above" table
expected: the Jira issue in the secondary table is clickable the same way as in the primary
task table
result: issue
reported: "there is a problem in the 'not covered by tasks above' table. The jira issue should be clickable the same way as in the above table"
severity: major
note: reported ad hoc during the test-4 walkthrough, not part of the original 4-item list

## Summary

total: 5
passed: 3
issues: 2
pending: 0
skipped: 0
blocked: 0

## Gaps

Both gaps were diagnosed and fixed inline on 2026-08-12 (developer chose the inline route over
planner ceremony — the diagnoses were line-precise). Gap 1 was fixed with the "filter at render"
shape, explicitly chosen over "don't seed on the peek path": the seed still runs, and
`ReleaseDetailPage` drops the self-referential entry before rendering, so the WR-01 full-page
paths are untouched. Regression coverage added in `ReleaseHeader.test.tsx` (new, 3 tests) and
`UnifiedTaskTable.test.tsx` (1 test). Full suite green: 2574 passed.

- truth: "Opening a task peek from the release detail page leaves the breadcrumb unchanged — the release name appears once"
  status: fixed
  reason: "User reported: clicking to open peek version of the task appends the same release into breadcrumbs. it shouldnt do anything. other cases work"
  severity: major
  test: 2
  root_cause: "`TaskRow` calls `onSeedBreadcrumb()` unconditionally before `onOpenIssue()` (UnifiedTaskTable.tsx:842-845). The peek path does not change the pathname, so the seeded `{path: /release/:id, label: version.name}` entry stays on the trail while the user is still on the release page. `ReleaseBreadcrumbHeader` renders every trail entry and then appends a hardcoded `/ {versionName}` (ReleaseHeader.tsx:28-41), so the release name shows twice — and since the header is hidden when the trail is empty (line 17), it also appears out of nowhere on peek open."
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx"
      issue: "TaskRow overlay seeds the breadcrumb on the peek path, which never navigates"
    - path: "taskflow/src/routes/dashboard/release-detail/ReleaseHeader.tsx"
      issue: "Renders the trail plus a static trailing versionName, so a self-referential trail entry duplicates the release name"
  missing:
    - "Suppress the release's own entry when rendering the header (filter trail entries whose path equals the current /release/:versionId), OR stop seeding on the peek path and seed instead at the peek's navigate-full moment"
    - "Keep the seed for the full-page paths — openIssueFull and handleNavigateToIssueFromMR still need it (WR-01)"
  debug_session: ""

- truth: "The Jira issue in the secondary 'not covered by tasks above' table is clickable the same way as in the primary task table"
  status: fixed
  reason: "User reported: there is a problem in the 'not covered by tasks above' table. The jira issue should be clickable the same way as in the above table"
  severity: major
  test: 5
  root_cause: "In the secondary table all three `keyCell` branches render the extracted Jira key as an inert `<span>` (UnifiedTaskTable.tsx:1120-1153: `secondary-key-flagged`, `secondary-key-none`, `secondary-key-unevaluated`). Only keys embedded in the MR *title* get linkified, via `titleContent`'s buttons in `MrSubLine`. The primary table's key cell is a real button (line 847-856). `onNavigateToIssueFromMR` is already in scope at the call site (line 1159) but is never wired to the key cell."
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx"
      issue: "Secondary-table keyCell renders keys[0] as plain text in the flagged and unevaluated branches"
  missing:
    - "Wrap `keys[0]` in a button calling `onNavigateToIssueFromMR(keys[0])` in the flagged and unevaluated branches (the keys.length === 0 branch has no key to link)"
    - "Preserve the existing styling cues — orange for flagged, muted for unevaluated — and keep the `+N` suffix and tooltip non-clickable"
  debug_session: ""
