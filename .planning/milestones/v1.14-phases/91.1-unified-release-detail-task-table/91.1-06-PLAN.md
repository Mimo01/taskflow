---
phase: 91.1-unified-release-detail-task-table
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx
  - taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx
  - taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
autonomous: true
requirements: [DRIFT-04, DRIFT-07]
must_haves:
  truths:
    - "While the Jira fix-version issue query is in flight, no secondary table row is rendered and no 'not in fix version' claim is made"
    - "When any of the three MR channel queries fails, every task shows a neutral 'couldn't check merge requests' line instead of the orange 'No merge request' warning"
    - "While the GitLab milestone query is in flight, no task shows 'MR status unavailable' and no task shows 'No merge request'"
    - "The flagged-count badge is absent until both the issue query and the drift queries have settled"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts"
      provides: "driftUnavailable flag from the three MR channel isError states; isLoadingDrift including the milestone query"
      contains: "driftUnavailable"
    - path: "taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx"
      provides: "Settled-state gating of the secondary section, the flagged badge, and a failed-check MR-slot state"
      contains: "mr-slot-failed"
  key_links:
    - from: "useReleaseDetail.ts"
      to: "UnifiedTaskTable driftUnavailable prop"
      via: "ReleaseDetailPage prop threading"
      pattern: "driftUnavailable"
    - from: "UnifiedTaskTable.tsx secondary section"
      to: "isLoadingIssues"
      via: "render guard"
      pattern: "!isLoadingIssues && secondaryRows.length > 0"
---

<objective>
Close the two BLOCKING verification gaps (roadmap success criteria 4 and 5) recorded in
`91.1-VERIFICATION.md`: the unified table currently makes three affirmative negative claims
("not in fix version", "No merge request", "MR status unavailable") from data that has not
arrived or never will.

This plan is deliberately narrow — settled-state derivation only. It carries none of the
UAT visual redesign work (plans 09/10) and none of the review cleanup (plans 07/08), so it
can land and flip `91.1-VERIFICATION.md` to `passed` independently.

Purpose: a check that did not run must never terminate as a settled negative — the same
CR-03/WR-04 rule `useReleaseDetail.ts` already enforces for the tag and tracking-MR
channels, applied to the three MR channels feeding the new table.

Output: `driftUnavailable` on the hook return, `isLoadingDrift` widened to include the
milestone query, a new `mr-slot-failed` neutral state, an `isLoadingIssues`-gated secondary
section and flagged badge, plus regression tests for each.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-VERIFICATION.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-REVIEW.md
@.planning/phases/91.1-unified-release-detail-task-table/91.1-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive driftUnavailable and a milestone-inclusive isLoadingDrift in useReleaseDetail</name>
  <files>taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts, taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx</files>
  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` lines 128-160 (the
      `gitlab-milestones` query at line 136 whose loading state is currently discarded) and
      lines 494-620 (the three channel queries at 503/518/536, `isLoadingDrift` at 576, and
      the return object at 587-620)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-REVIEW.md` section CR-02
      (contains the exact intended destructuring shape)
    - `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` lines 798-900
      (the existing `WR-04: a rejected gitlab-pat terminates the verdict` and
      `a rejecting searchProjectTags resolves to couldnt-verify` tests — reuse their
      rejecting-service mock setup rather than inventing a new one)
  </read_first>
  <behavior>
    - Test 1: when `fetchAllProjectMRs` rejects, the hook returns `driftUnavailable: true`
      and `isLoadingDrift: false`
    - Test 2: when `fetchMilestoneMRs` rejects, the hook returns `driftUnavailable: true`
    - Test 3: when `fetchBranchTargetedMRs` rejects, the hook returns `driftUnavailable: true`
    - Test 4: with all three channels resolving normally, `driftUnavailable` is `false`
    - Test 5: while `fetchProjectMilestones` is still pending (never-resolving promise) and
      the three channels have resolved, `isLoadingDrift` is `true` (previously `false`)
  </behavior>
  <action>
    In `useReleaseDetail.ts`:
    1. Destructure `isError` from all three MR channel `useQuery` calls at lines 503, 518
       and 536, aliased as `channelAFailed`, `channelBFailed`, `channelCFailed`.
    2. Destructure `isLoading: isLoadingMilestones` from the `['gitlab-milestones',
       activeGitlabProject, 'all']` query at line 136 (currently only `data:
       allProjectMilestones` is taken).
    3. Change `isLoadingDrift` (line 576) to
       `isLoadingChannelA || isLoadingChannelB || isLoadingChannelC || isLoadingMilestones`.
    4. Add `const driftUnavailable = channelAFailed || channelBFailed || channelCFailed;`
       immediately after it, with a comment citing the CR-03/WR-04 rule already stated in
       this file: a channel that cannot be queried at all is a check that did not happen, so
       it must not terminate as a settled negative.
    5. Add `driftUnavailable` to the hook's return object alongside `isLoadingDrift`.
    Do NOT fold `driftUnavailable` into `isLoadingDrift` — a failed channel is a terminal
    state, not a pending one; conflating them would spin the pending line forever.
    Add the five behavior tests to `useReleaseDetail.test.tsx` using the existing rejecting-mock
    pattern from the merge-back describe block.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*[/*]' taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts | grep -c 'isError:'` returns 3
    - `grep -n 'isLoadingMilestones' taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` matches both the destructuring and the `isLoadingDrift` expression
    - `grep -n 'driftUnavailable' taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` matches both the derivation and the return object
    - `npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` passes, including the 5 new cases
  </acceptance_criteria>
  <done>The hook exposes `driftUnavailable`, and `isLoadingDrift` stays true until the milestone query settles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate the secondary section, the flagged badge, and add the mr-slot-failed state</name>
  <files>taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx, taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx, taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` lines 503-564
      (`MrSlot`'s four-state ladder) and lines 566-694 (props interface, header badge at
      591-598, secondary section guard at 656)
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-REVIEW.md` sections
      CR-01, CR-02 and IN-03
    - `.planning/phases/91.1-unified-release-detail-task-table/91.1-UI-SPEC.md` section
      "State line variants inside the MR-slot" — states 2 and 3 must never render while
      state 1's condition holds; the new failed state joins that mutual exclusion
    - `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx` lines 115-172
      (`defaultProps`/`renderSection`/`renderWithRows` helpers — extend `defaultProps` with
      the new prop rather than rewriting call sites) and lines 595-637 (the D-17 four-state
      suite the new state must slot into)
    - `taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` lines 244-262 (the single
      `<UnifiedTaskTable>` call site)
  </read_first>
  <behavior>
    - Test 1: `isLoadingIssues: true` with a non-empty `secondaryRows` renders no
      `secondary-section` and no `secondary-key-flagged`
    - Test 2: `isLoadingIssues: false` with the same non-empty `secondaryRows` still renders
      `secondary-section` (no regression of D-13)
    - Test 3: `driftUnavailable: true`, `mrs: []`, `hasMatchedMilestone: true` renders
      `mr-slot-failed` and neither `mr-slot-none` nor `mr-slot-unavailable` nor
      `mr-slot-pending`
    - Test 4: `mr-slot-failed` contains no `.text-orange-600` element (neutral, not a warning)
    - Test 5: `isLoadingDrift: true` still wins over `driftUnavailable: true`
      (`mr-slot-pending` renders, `mr-slot-failed` does not)
    - Test 6: `isLoadingDrift: true` hides `flagged-count-badge`; `isLoadingIssues: true`
      hides it; both false renders it
  </behavior>
  <action>
    In `UnifiedTaskTable.tsx`:
    1. Add `driftUnavailable: boolean` to `UnifiedTaskTableProps`, thread it into `MrSlot`.
    2. In `MrSlot`, insert a new branch AFTER the `isLoadingDrift` branch and BEFORE the
       `mrs.length === 0 && hasMatchedMilestone` branch: when `driftUnavailable && mrs.length === 0`,
       render `data-testid="mr-slot-failed"` with copy `— couldn't check merge requests`,
       `title="GitLab merge request lookup failed — this task's MR status is unknown"`, using
       the same neutral `text-muted-foreground` treatment as the existing `mr-slot-unavailable`
       branch. Never `text-orange-*` — this is an unknown, not a verified absence. Order
       matters: pending beats failed beats verified-empty.
    3. Change the secondary section guard at line 656 from `secondaryRows.length > 0` to
       `!isLoadingIssues && secondaryRows.length > 0` (CR-01).
    4. Wrap the flagged-count badge (lines 591-598) in `!isLoadingDrift && !isLoadingIssues &&`
       (IN-03). This plan owns IN-03 in full; plan 09 replaces the badge's presentation but
       must preserve this gate.
    In `ReleaseDetailPage.tsx`: destructure `driftUnavailable` from `useReleaseDetail` and
    pass it to `<UnifiedTaskTable>` at the existing call site. No other change here.
    Add the six behavior tests, placing 3-5 inside the existing `D-17` describe block
    (renamed to reflect five mutually exclusive states) and 1-2 inside `D-13: secondary table`.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '!isLoadingIssues && secondaryRows.length > 0' taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns exactly 1 match
    - `grep -n 'mr-slot-failed' taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` returns 1 match, and the surrounding JSX contains no `text-orange`
    - `grep -c 'driftUnavailable' taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` is at least 2 (destructure + prop)
    - `npx vitest run src/routes/dashboard/release-detail/` passes with 109 prior tests plus the new cases, zero failures
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>No affirmative negative claim renders from an unsettled or failed query; the whole release-detail suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitLab API → rendered UI | Channel query failures (401 after PAT rotation, 5xx) reach the render layer as absent data |
| Jira API → rendered UI | Fix-version issue list latency reaches the render layer as an empty array |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91.1-09 | Repudiation | `MrSlot` asserting "No merge request" from a failed fetch | mitigate | New `mr-slot-failed` terminal state driven by `driftUnavailable`, ordered after pending and before verified-empty; asserted by tests 3-5 |
| T-91.1-10 | Repudiation | Secondary table asserting "not in fix version" from an unfetched fix version | mitigate | Section render gated on `!isLoadingIssues`; asserted by test 1 |
| T-91.1-11 | Information disclosure | Error text from a rejected GitLab query surfaced to the UI | accept | The new failed line renders fixed copy only; no `error.message` is interpolated |
| T-91.1-SC | Tampering | package installs | accept | No installs in this plan |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/release-detail/` — all green
- `cd taskflow && npx tsc --noEmit` — exits 0
- `cd taskflow && npm run lint` — no NEW files flagged relative to the pre-change baseline
</verification>

<success_criteria>
Roadmap success criteria 4 and 5 hold under every query state: pending, failed, and settled.
Every `missing:` item in `91.1-VERIFICATION.md`'s `gaps:` block has a corresponding code
change and at least one test that fails without it.
</success_criteria>

<output>
Create `.planning/phases/91.1-unified-release-detail-task-table/91.1-06-SUMMARY.md` when done
</output>
