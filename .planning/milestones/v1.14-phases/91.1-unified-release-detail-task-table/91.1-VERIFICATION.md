---
phase: 91.1-unified-release-detail-task-table
verified: 2026-08-12T13:11:45Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/6
  gaps_closed:
    - "CR-05: MR cell and evaluated BR/MS slots no longer swallow the full-row click"
    - "CR-06: a failed GitLab-milestone fetch no longer terminates as a verified 'no milestone matched'"
    - "CR-07: DriftActionCell keyed to mr.id so sticky write state cannot rebind to a different MR"
    - "CR-08: the +N marker is hit-testable and its tooltip (plus sr-only text) actually renders"
    - "WR-06: secondary table has its own header strip matching its four real columns"
    - "WR-07: degraded MR slots explain themselves in visible text, not an unreachable tooltip"
    - "WR-08: +N marker counts from the task's unfiltered MR list, not the filtered view"
    - "WR-09: selectDisplayMr prefers an evaluated MR over an unevaluated higher-iid one"
    - "WR-10: a partial channel failure is surfaced page-wide via drift-partial-banner"
    - "Original truth 'zero-MR task still surfaces Missing MR / degraded-milestone state, secondary table gated on isLoadingIssues': UnifiedTaskTable.tsx:1111 now gates secondary section on !isLoadingIssues; driftUnavailable folds in milestonesFailed"
    - "UAT-91.1-A: table decluttered, then fully consolidated to one row per task per developer's live request"
    - "UAT-91.1-B: single flagged count replaced with two clickable, filtering warning badges"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Badge filtering end-to-end: click '⚠ N wrong branch', confirm table restricts to matching rows, fix an MR from inside the filtered view, confirm the count decrements and the row leaves the filter without the table re-sorting around the user; repeat for '⚠ N no milestone'."
    expected: "Filter applies, fix-inside-filter works, badge count and filtered set both update correctly."
    why_human: "UAT-91.1-B round 2 was approved by the developer without a step-by-step walkthrough (91.1-UAT.md test 12: 'not walked step-by-step'); automated coverage exists but the interactive filtering flow was not observed live."
  - test: "WR-01 regression: open a task peek from the primary table, close it, then click a Jira key inside a secondary-table MR title (the only place ticket keys are still linkified inline), and confirm the release breadcrumb entry appears exactly once."
    expected: "Exactly one 'release' breadcrumb entry, no duplicate."
    why_human: "91.1-UAT.md records this as not walked in the live session; the click surface changed (MR titles no longer render inline in the primary table) so the pre-consolidation walkthrough doesn't map 1:1 onto the shipped table. A unit-level regression test exists but the live breadcrumb-store interaction was not observed."
  - test: "CR-01/CR-02 load-flash regression: hard-reload the release detail page on a real release and watch for a flash of 'No merge request' or 'not covered by tasks above' content before data settles."
    expected: "No flash of unsettled-state content before the real data renders."
    why_human: "Recorded as 'approved-without-detailed-walkthrough' in 91.1-UAT.md; gating logic exists in code (`!isLoadingDrift && !isLoadingIssues` guards) and is unit-tested, but a live reload-timing observation was not performed."
---

# Phase 91.1: Unified Release Detail Task Table Verification Report

**Phase Goal:** Release detail presents ONE primary table — Jira tasks in this fix version, each
with its merge requests rendered inline in the row and every per-MR drift check and corrective
action available there — plus a single secondary table for merge requests the release's tasks do
not cover. The separate "Issues" and "MR Drift" tables are replaced, not merely restyled.

**Verified:** 2026-08-12T13:11:45Z
**Status:** human_needed
**Re-verification:** Yes — the prior verification (`status: gaps_found`, 4/6) predates
gap-closure plans 91.1-06 through 91.1-10 and the second code-review pass (`91.1-REVIEW.md`).
This report re-checks the original gaps plus the eight new blockers/warnings the second review
found (CR-05..CR-08, WR-06..WR-10), verifies success criteria 2 and 3 against the criteria as
**superseded at live UAT on 2026-08-12** (not the struck-through original), and independently
confirms the claimed fix commits (`0c8e9a1d`…`7fef0195`) actually landed in source rather than
trusting SUMMARY.md narration.

## Goal Achievement

### Observable Truths

Truths below are the ROADMAP success criteria for Phase 91.1, with criteria 2 and 3 evaluated
against their **live-UAT-superseded replacement text** per the task instructions, not the
struck-through original (stacked per-MR sub-lines were built, shown to the developer on real
data, and explicitly rejected in favor of one consolidated line per task).

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Release detail renders exactly one primary table, keyed on Jira tasks (key/summary/assignee/status), no standalone issues-only or drift-only table remains | ✓ VERIFIED | `ReleaseDetailPage.tsx:246` renders only `<UnifiedTaskTable>`; a filesystem search for `IssuesSection*`/`MrDriftSection*` returns nothing — both deleted. `TaskRow` (`UnifiedTaskTable.tsx:809-894`) renders Key/Summary/Assignee/Status/MR/BR/MS in one row. |
| 2 (superseded) | Each task renders on exactly one line, carrying the pre-v1.14 MR cell (GitMerge icon + state-coloured `!iid` + outline state badge); a multi-MR task shows its most relevant MR (flagged > evaluated > highest iid) plus a hoverable `+N` marker naming the rest; no MR is dropped from the data layer | ✓ VERIFIED | `TaskMrCell` (`UnifiedTaskTable.tsx:602-807`) renders one `GitMerge`+`!iid`+outline-badge cell per task (`:718-746`). `selectDisplayMr` (`:549-555`) implements flagged→evaluated→highest-iid precedence (WR-09 fix, commit `1b38ca93`, confirmed landed). `+N` marker (`:758-767`) is `pointer-events-auto relative z-10 cursor-help` with a real `title` tooltip AND sr-only text (CR-08 fix, commit `358a8f23` — confirmed landed; the review's flagged `pointer-events-none` decoy is gone from current source). `allMrs` (unfiltered) feeds the count, not the filtered `mrs` (WR-08 fix, `3830aa2d`). Live UAT re-run confirmed: developer said "way better" after consolidation (`91.1-UAT.md` Round 2), with one follow-up defect (key wrapping) fixed in `c9720c90`. |
| 3 (amended) | The BR/MS drift marks and click-to-fix actions operate on the task row's displayed MR, with the same pending/error/retry and independent-lock behaviour | ✓ VERIFIED | `TaskMrCell` renders `DriftActionCell` for BR/MS when `selected.evaluated` (`:781-800`), reusing the same component `MrSubLine` uses. `DriftCellSlot interactive` is now derived from `isMrFixActionable`, not hardcoded (CR-05 fix, `0c8e9a1d`) — confirmed at `:781,791`. `DriftActionCell` is now `key`ed by `mr.id` (`:783,793`) plus a render-phase guard that resets on `mr.id` change (`:227-232`), so write state cannot bind to the wrong MR after a filter/cache change (CR-07 fix, `419fa2ae`) — confirmed present in current source, not just claimed in the review. |
| 4 | The TASK drift check is dropped from the primary table; a task with zero MRs still surfaces "Missing MR"/degraded-milestone state | ✓ VERIFIED | No TASK column/cell exists in `TaskRow`/`TaskMrCell`; `UnifiedTaskTable.test.tsx:16-19` documents the TASK-cell assertions as an intentional loss. `TaskMrCell` renders `mr-slot-none` ("No merge request", orange, `:665-678`), `mr-slot-unavailable` ("no milestone, not checked", `:680-693`) and `mr-slot-failed` ("MR status unknown", `:650-663`) for the zero-MR cases, each with a distinct `data-testid`. The original verifier's gap (secondary section not gated on `isLoadingIssues`) is closed: `UnifiedTaskTable.tsx:1111` now reads `!isLoadingIssues && filteredSecondaryRows.length > 0`. |
| 5 | A secondary table lists only MRs not covered by the primary table — both no-Jira-key MRs and out-of-fix-version-key MRs, the latter flagged | ✓ VERIFIED | `buildTaskMrAttachment` (`driftDetection.ts:395-415`) computes `secondaryRows` as MRs whose `taskKeys` don't intersect `coveredKeys`. `UnifiedTaskTable.tsx:1111-1166` renders it with its own `SecondaryHeaderStrip` (WR-06 fix, `db157395`) and three key-cell variants (`secondary-key-flagged`/`-none`/`-unevaluated`) driven by `row.taskReason`, not a re-derivation (WR-04/WR-03 fixes, plan 08). |
| 6 | Primary-table rows keep Jira order and never re-sort while the user acts; Phase 89 D-11 frozen-order workaround is unnecessary | ✓ VERIFIED | `buildTaskMrAttachment` maps `primaryRows` directly over `releaseIssues` (`driftDetection.ts:404-410`) with no re-sort by drift status; `useReleaseDetail.ts` has no `.sort()` on `primaryRows`. `UnifiedTaskTable.test.tsx:14-15` documents the frozen-order mechanism as deliberately removed ("row order is now the component's contract"). |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx` | Unified primary+secondary table, presentational | ✓ VERIFIED | 1169 lines; `TaskRow`, `TaskMrCell`, `MrSubLine`, `ColumnHeaderStrip`, `SecondaryHeaderStrip` all present, wired, substantive (not stub) |
| `taskflow/src/routes/dashboard/release-detail/driftDetection.ts` | Channel A/B/C union, drift evaluation, task/MR partition | ✓ VERIFIED | `selectChannelA`, `evaluateBranchDrift`, `evaluateMilestoneDrift`, `evaluateTaskDrift`, `buildDriftRows`, `buildTaskMrAttachment`, `countBranchFlaggedMRs`, `countMilestoneFlaggedMRs`, `countBrMsFlaggedMRs` all exported and exercised by tests |
| `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` | Threads primaryRows/secondaryRows/driftUnavailable/milestonesFailed | ✓ VERIFIED | `milestonesFailed` destructured (`:146`), `driftUnavailable` computed from four channels including it (`:622`), both returned (`:660,662`) |
| `taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` | Single call site to `UnifiedTaskTable`, no `IssuesSection`/`MrDriftSection` | ✓ VERIFIED | `:246` single render; repo-wide grep for old component names returns nothing |
| `taskflow/src/routes/dashboard/release-detail/IssuesSection.tsx`, `MrDriftSection.tsx` | Deleted | ✓ VERIFIED | Files do not exist on disk |
| `taskflow/src/routes/dashboard/release-detail/useMrFixMutation.ts` | Re-hosted (not rewritten) corrective-action mutation hook | ✓ VERIFIED | File exists, imported by `UnifiedTaskTable.tsx:13`, exercised via the real hook (not mocked) in `UnifiedTaskTable.test.tsx` per its own header comment |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `useReleaseDetail.ts` (driftUnavailable, milestonesFailed) | `UnifiedTaskTable` props | `ReleaseDetailPage.tsx:253,255` prop threading | ✓ WIRED | `driftUnavailable={driftUnavailable}`, `milestoneLookupFailed={milestonesFailed}` |
| `UnifiedTaskTable` MR cell / BR / MS cells | Row overlay click (D-04 full-row click) | `pointer-events-none` vs `relative z-10` sibling-overlay convention | ✓ WIRED | MR cell wrapper is `pointer-events-none relative` (`:727`); `DriftCellSlot interactive` derived from `isMrFixActionable` (`:781,791`), not hardcoded — CR-05 confirmed fixed, not merely claimed |
| `DriftActionCell` | Its own MR identity | `key={`br-${mr.id}`}` / `key={`ms-${mr.id}`}` + render-phase `lastMrIdRef` guard | ✓ WIRED | `:783,793` (key) and `:227-232` (guard) both present — CR-07 confirmed fixed |
| `mr-extra-count` marker | Hover tooltip | `pointer-events-auto relative z-10 cursor-help` + `title` | ✓ WIRED | `:758-767` — CR-08 confirmed fixed (was `pointer-events-none` per the review; current source is hit-testable) |
| Badge click (`flagged-br-badge`/`flagged-ms-badge`) | Table filtering | `activeFilter` state → `filteredPrimaryRows`/`filteredSecondaryRows` | ✓ WIRED | `:918-938` |

### Requirements Coverage

| Requirement | Source Plan(s) | Status | Evidence |
|-------------|-----------------|--------|----------|
| DRIFT-01 | 91.1-02 | ✓ SATISFIED | `selectChannelA` (`driftDetection.ts:111`) |
| DRIFT-02 | 91.1-02 | ✓ SATISFIED | Channel B implemented via GitLab milestone query, unioned in `buildDriftRows` |
| DRIFT-03 | 91.1-02 | ✓ SATISFIED | Channel C (target-branch), paginated per `useReleaseDetail.ts` |
| DRIFT-04 | 91.1-01, 91.1-02, 91.1-06, 91.1-07 | ✓ SATISFIED | `unionMRs` (`driftDetection.ts:77`) retains per-channel provenance (`row.channels`) |
| DRIFT-05 | 91.1-03, 91.1-04, 91.1-09, 91.1-10 | ✓ SATISFIED | `evaluateBranchDrift` (`:128`) |
| DRIFT-06 | 91.1-03, 91.1-04, 91.1-09, 91.1-10 | ✓ SATISFIED | `evaluateMilestoneDrift` (`:145`) |
| DRIFT-07 | 91.1-01, 91.1-03, 91.1-06, 91.1-08 | ✓ SATISFIED | `evaluateTaskDrift` (`:177`), secondary table's `taskReason === 'not-in-fix-version'` flagging |
| DRIFT-08 | 91.1-01, 91.1-03, 91.1-04, 91.1-08, 91.1-10 | ✓ SATISFIED | `classifyMrState` (`:199`) |
| DRIFT-09 | — | Descoped (documented) | REQUIREMENTS.md: "DESCOPED 2026-08-11 by user at UAT" — not a gap, a pre-existing decision unrelated to this verification |
| MRFIX-01 | 91.1-03, 91.1-04, 91.1-05 | ✓ SATISFIED | `DriftActionCell action="retarget"`, `useMrFixMutation` |
| MRFIX-02 | 91.1-03, 91.1-04, 91.1-05 | ✓ SATISFIED | `DriftActionCell action="assign-milestone"` |
| MRFIX-03 | 91.1-03, 91.1-04, 91.1-05 | ✓ SATISFIED | Per-cell independent `status`/`errorMessage`/`fire`/`reset`, now correctly MR-keyed after CR-07 |
| MRFIX-04 | 91.1-03, 91.1-04, 91.1-05 | ✓ SATISFIED | `mrFixReadiness` gates `retarget` on `fix.releaseBranchExists` (`:157-161`) |

All 12 requirement IDs declared for this phase are accounted for. No orphaned requirements found.
REQUIREMENTS.md maps DRIFT-01..08/MRFIX-01..04 to "Phase 89"/"Phase 90" as their point of origin,
but this phase's own PLAN frontmatter and each SUMMARY's `requirements-completed` field confirm
the intentional re-hosting per the phase's stated scope ("re-hosted onto the unified table, not
re-implemented").

### Gate Results (independently reproduced by this verifier)

| Gate | Command | Result |
|------|---------|--------|
| TypeScript | `npx tsc --noEmit` | exit 0 |
| Unit tests (release-detail scope) | `npx vitest run src/routes/dashboard/release-detail/` | 12 files, 399 passed |
| Lint | `npx biome check` on the 5 files touched by the gap-closure fix pass | Checked 5 files, no diagnostics |
| Debt markers | `grep -n "TBD\|FIXME\|XXX"` on `UnifiedTaskTable.tsx`, `useReleaseDetail.ts`, `ReleaseDetailPage.tsx`, `driftDetection.ts` | none found |

### Anti-Patterns Found

None blocking. All eight prior review blockers/warnings (CR-05..CR-08, WR-06..WR-10) confirmed
fixed in code, each traceable to a dedicated commit (`0c8e9a1d`, `cddec970`, `419fa2ae`,
`358a8f23`, `db157395`, `3b6eb202`, `3830aa2d`, `1b38ca93`, `7fef0195`) and a dedicated test
(`UnifiedTaskTable.test.tsx` describe blocks named after each finding ID).

### Human Verification Required

Three items, all explicitly recorded in `91.1-UAT.md` as "approved-without-detailed-walkthrough"
rather than individually confirmed. Automated coverage exists for all three at the unit level, but
the interactive/live-timing behaviour was not observed by the developer step-by-step. Full detail
in the `human_verification` frontmatter above:

1. Badge filtering + fix-inside-filter end-to-end flow (UAT-91.1-B steps not walked).
2. WR-01 breadcrumb regression via the changed click surface (MR titles no longer inline in the
   primary table).
3. CR-01/CR-02 reload-timing flash check.

### Gaps Summary

No blocking gaps. All ROADMAP success criteria — including criteria 2 and 3 evaluated against
their live-UAT-superseded replacement text — are verified in the codebase, not merely claimed in
SUMMARY.md. Both original gaps from the prior `gaps_found` verification are closed (secondary
section is now gated on `!isLoadingIssues`; `driftUnavailable` now folds in all four channels
including the milestone query). All eight blockers/warnings from the second code-review pass
(`91.1-REVIEW.md`) were independently re-checked against current source — not assumed from the
SUMMARY — and confirmed fixed. `MrSubLine` correctly survives only for the secondary
(uncovered-MRs) table, as intended: its absence from the primary table is the developer's
explicit, UAT-confirmed choice, not a gap.

Status is `human_needed` rather than `passed` solely because three UAT steps were recorded as
approved-without-detailed-walkthrough — per the task instructions, these are surfaced for human
confirmation rather than treated as verified.

---

_Verified: 2026-08-12T13:11:45Z_
_Verifier: Claude (gsd-verifier)_
