---
phase: 91.1
slug: unified-release-detail-task-table
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-12
audited: 2026-08-13
---

# Phase 91.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.0.18 (+ @testing-library/react) |
| **Config file** | `taskflow/vitest.config.ts` |
| **Quick run command** | `cd taskflow && npx vitest run src/routes/dashboard/release-detail/<file>.test.tsx` |
| **Full suite command** | `cd taskflow && npm test` |
| **Estimated runtime** | ~60 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run the targeted `npx vitest run <changed test file>`
- **After every plan wave:** Run `cd taskflow && npm test`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

This phase modifies shared data-layer code (`useReleaseDetail.ts`, `driftDetection.ts`) that
other release-detail tests depend on, so the per-wave full-suite run is load-bearing, not optional.

---

## Per-Task Verification Map

> Reconciled 2026-08-13 against the 35 executed tasks across plans 91.1-01…91.1-10 (the planner
> left this table as `TBD` at plan time). Mapped at plan granularity.
>
> **Threat Ref / Secure Behavior:** N/A for every row — this phase re-hosts existing drift/fix
> behaviour in a new table; the write surface is unchanged from Phase 90.

| Plan | Tasks | Requirement | Test Type | Automated Command | File Exists | Status |
|------|-------|-------------|-----------|-------------------|-------------|--------|
| 91.1-01 | 3 | DRIFT-04, DRIFT-07, DRIFT-08 | unit | `npx vitest run src/routes/dashboard/release-detail/driftDetection.test.ts` | ✅ | ✅ green |
| 91.1-02 | 3 | DRIFT-01..04 | unit + hook | `npx vitest run src/routes/dashboard/release-detail/driftDetection.test.ts src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` | ✅ | ✅ green |
| 91.1-03 | 4 | DRIFT-05..08, MRFIX-01..04 | component | `npx vitest run src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx` | ✅ | ✅ green |
| 91.1-04 | 3 | MRFIX-01..04, DRIFT-05, DRIFT-06, DRIFT-08 | component + unit | `npx vitest run src/routes/dashboard/release-detail/` | ✅ | ✅ green |
| 91.1-05 | 4 | DRIFT-01..08, MRFIX-01..04 | component + integration | same | ✅ | ✅ green |
| 91.1-06 | 3 | DRIFT-04, DRIFT-07 | unit | `npx vitest run src/routes/dashboard/release-detail/driftDetection.test.ts` | ✅ | ✅ green |
| 91.1-07 | 3 | DRIFT-04 | unit | same | ✅ | ✅ green |
| 91.1-08 | 4 | DRIFT-07, DRIFT-08 | unit + component (CR/WR review fixes) | `npx vitest run src/routes/dashboard/release-detail/` | ✅ | ✅ green |
| 91.1-09 | 4 | DRIFT-05, DRIFT-06 | component (UAT-91.1-B badges) | `npx vitest run src/routes/dashboard/release-detail/UnifiedTaskTable.test.tsx` | ✅ | ✅ green |
| 91.1-10 | 4 | DRIFT-05, DRIFT-06, DRIFT-08 | component + unit | `npx vitest run src/routes/dashboard/release-detail/` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] **Delivered** — `src/routes/dashboard/release-detail/driftDetection.test.ts` `buildTaskMrAttachment` describe; cases for the task↔MR attachment function: single MR/task, zero MR/task, one MR duplicated under two tasks (D-09), keyless MR → secondary table, out-of-scope-key MR → secondary table flagged with reason
- [x] **Delivered** — `driftDetection.test.ts` `countBrMsFlaggedMRs` + `countBranchFlaggedMRs / countMilestoneFlaggedMRs` describes; cases for the BR-or-MS-only flagged count, deduped by `mr.id`, including secondary-table rows (D-15)
- [x] **Delivered as `UnifiedTaskTable.test.tsx`** (100 tests, 23 describes) — successor to `MrDriftSection.test.tsx` — overlay-button click-anywhere (D-04), hover-reveal scoped to the MR sub-line rather than the whole task block (D-05), the four mutually-exclusive MR-slot states (D-17), secondary-table hide-when-empty (D-13), and the relocated DriftActionCell state machine (pending/error/retry, independent per-MR locks)
- [x] No framework install needed — Vitest + `@testing-library/react` already configured

**Coverage-preservation rule:** `MrDriftSection.test.tsx` currently carries the DriftActionCell
state-machine coverage. That coverage must be relocated, not dropped, when the file is renamed
or split. Tests for `applyHeldOrder` are the exception — that mechanism is deliberately deleted
(D-11 / success criterion 6), so its tests are removed intentionally.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Streaming states as the three channel queries resolve at different times | D-17 | Reliable assertion needs a fake-timer harness around three independent query `isLoading` flags; cheaper and more faithful to check in the running app | Open a release detail page with a cold cache; confirm each MR slot passes through pending → resolved without flashing a wrong state, and that a task with zero MRs shows "Missing MR" rather than a spinner-forever |
| Rows do not re-sort while the user is clicking fix actions | Success criterion 6 | Ordering stability under live mutation is an interaction property | Trigger a BR fix on a mid-table MR; confirm no row moves before, during, or after the mutation settles |
| Per-row truncation behaves per-row (no table-layout truncation sync) | UI-SPEC layout | Visual property | Render a release with mixed short/long MR titles; confirm each line truncates at its own width |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (35 tasks / 10 plans)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references — all 3 items delivered
- [x] No watch-mode flags (`vitest run`, never `vitest`/`--watch`)
- [x] Feedback latency < 60s (full suite 12.6s)
- [x] `nyquist_compliant: true` set in frontmatter
- [x] **Coverage-preservation rule honoured** — DriftActionCell state-machine coverage relocated into
      `UnifiedTaskTable.test.tsx` (`per-MR corrective actions` + `CR-07: per-cell write state follows MR
      identity, not row position`), not dropped. `applyHeldOrder` tests correctly removed with the
      mechanism itself (D-11 / success criterion 6).

**Approval:** approved 2026-08-13 (retroactive audit via `/gsd-validate-phase`)

---

## Validation Audit 2026-08-13

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

All three Wave 0 deliverables exist and pass. `UnifiedTaskTable.test.tsx` alone carries 100 tests across
23 describes, explicitly covering D-04, D-05, D-06, D-09, D-13, D-14/D-15, D-16, D-17 and the CR-01,
CR-05, CR-06, CR-07, CR-08, WR-05, WR-06, WR-07, WR-10 review findings by name.

The `nyquist_compliant: false` flag was **stale bookkeeping** — this file was written at plan time and
never updated after execution. No test was missing.

**Note on the three Manual-Only rows:** one of them (badge filtering, listed as a 91.1-VERIFICATION
human-verification item) *does* now have automated coverage — `UnifiedTaskTable.test.tsx`'s
`UAT-91.1-B: filtering warning badges` describe. It is retained as manual-only because the automated test
covers the filter logic, not the live interaction property the developer wanted to observe (that the table
does not re-sort around the user mid-fix). The other two rows (streaming states across three independently
resolving queries, per-row truncation) remain correctly manual — both are visual/timing properties.
