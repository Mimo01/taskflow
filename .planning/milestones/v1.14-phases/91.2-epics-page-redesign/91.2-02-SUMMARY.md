---
phase: 91.2-epics-page-redesign
plan: 02
subsystem: ui
tags: [react, epics, progress-bar, tailwind, vitest, react-testing-library]

# Dependency graph
requires: []
provides:
  - "EpicProgressCells.tsx: EpicProgressCell + EpicPointsCell presentational components with a standalone EpicProgressCounts/EnrichmentCellState contract"
  - "Full unit coverage of all four enrichment states (pending/ready/no-stories/error) for both cells"
affects: [91.2-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Locally-declared EpicProgressCounts/EnrichmentCellState interface (not imported from services/jira) so this plan has zero dependency on the service-layer plan; structurally identical to that plan's EpicEnrichmentCounts so the service value assigns without a cast"
    - "Three-segment proportional bar built from sibling divs inside an overflow-hidden rounded-full track, colours sourced only from statusCategoryDotClass — no bespoke palette"
    - "Native title attribute as the hover-reveal mechanism (no tooltip primitive in the codebase)"
    - "Small precisely-bounded <button> retry glyph with e.stopPropagation() before invoking onRetry — never a full-cell overlay (91.1 CR-05 regression guard)"

key-files:
  created:
    - taskflow/src/routes/dashboard/EpicProgressCells.tsx
    - taskflow/src/routes/dashboard/EpicProgressCells.test.tsx
  modified: []

key-decisions:
  - "Duplicated the retry-button JSX in both cells instead of factoring a shared RetryButton component, to satisfy the plan's literal acceptance check `grep -c stopPropagation == 2` (one call site per button)"

patterns-established:
  - "Enrichment cell states as a closed EnrichmentCellState union (pending/error/ready) — future enrichment-driven cells in this codebase should reuse this shape rather than inventing ad hoc loading/error booleans"

requirements-completed: [EPIC-03, EPIC-05, EPIC-06]

# Metrics
duration: 12min
completed: 2026-08-12
---

# Phase 91.2 Plan 02: EpicProgressCells Summary

**Standalone `EpicProgressCells.tsx` module shipping the three-segment Done/In-Progress/To-Do progress bar (`done/total`) and the `done/total SP` points cell, both fully covering pending/ready/empty/error enrichment states with 12 passing unit tests.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-08-12T21:28:00Z
- **Completed:** 2026-08-12T21:40:40Z
- **Tasks:** 2 completed
- **Files modified:** 2 (both new)

## Accomplishments
- `EpicProgressCell` renders the single three-segment bar (D-11) with a `done/total` label, colours sourced exclusively from `statusCategoryDotClass`
- `EpicPointsCell` renders `done/total SP` (D-13)
- Both cells cover all four enrichment states: pending shimmer sized to the settled column width (D-14), ready, "No stories" for a successful-but-empty result (D-16), and a small bounded retry glyph on failure (D-15)
- Per-status breakdown revealed via native `title` hover text — no tooltip primitive introduced
- Retry buttons call `e.stopPropagation()` before `onRetry()`, proven by a dedicated hit-testing test that asserts an outer row click spy is never invoked (91.1 CR-05 regression guard)
- Zero blocked/flagged surface anywhere in the file (D-12), verified by both a static grep and a runtime negative-assertion test across all four states

## Task Commits

Each task was committed atomically:

1. **Task 1: EpicProgressCells.tsx — segmented bar, points cell, four states** - `eab4bdd1` (feat)
2. **Task 2: EpicProgressCells.test.tsx — all four states, segment widths, hover title, retry hit-testing** - `32f134b1` (test)

**Plan metadata:** pending (this commit)

## Files Created/Modified
- `taskflow/src/routes/dashboard/EpicProgressCells.tsx` - `EpicProgressCell` + `EpicPointsCell`, plus the exported `EpicProgressCounts`/`EnrichmentCellState` contract consumed by plan 91.2-03
- `taskflow/src/routes/dashboard/EpicProgressCells.test.tsx` - 12 unit tests covering all four states, exact segment-width percentages, the hover title string, retry click behaviour, and the D-12 negative guard

## Decisions Made
- Initially factored a shared `RetryButton` sub-component for both cells (DRY), but the plan's acceptance criteria requires `grep -c "stopPropagation" EpicProgressCells.tsx` to return exactly 2 (one literal call site per retry button). Inlined both buttons instead so the literal grep-based acceptance check passes while keeping identical runtime behaviour.
- Used `fireEvent` (not `userEvent`) in the test file, matching the simpler synchronous-click convention already used elsewhere in `src/routes/dashboard/*.test.tsx` (e.g. `AioProjectOverviewPage.test.tsx`) for components with no async state.

## Deviations from Plan

None - plan executed exactly as written (the RetryButton→inline change above is a compliance adjustment to match the plan's own literal acceptance criteria, not a deviation from the plan's intent).

## Issues Encountered
- The worktree had no `node_modules` (fresh worktree checkout). Symlinked `taskflow/node_modules` to the main repo's `taskflow/node_modules` (identical `package.json`, confirmed via diff) to run `tsc`, `biome`, and `vitest` without a redundant `npm install`. The symlink is gitignored and does not appear in `git status`.
- `npx biome check --write` reformatted the test file's multi-line `render(...)` calls and a type-annotation line break on first run; re-verified after the auto-format and all 12 tests still passed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plan 91.2-03 (the `EpicsPage.tsx`/`EpicsSkeleton.tsx` row rebuild) can now import `EpicProgressCell`, `EpicPointsCell`, `EpicProgressCounts`, and `EnrichmentCellState` directly from this file and wire them into the new div+flex row, assigning the service-layer plan's `EpicEnrichmentCounts` values without a cast (structurally identical shape).
- No blockers.

---
*Phase: 91.2-epics-page-redesign*
*Completed: 2026-08-12*

## Self-Check: PASSED

- FOUND: taskflow/src/routes/dashboard/EpicProgressCells.tsx
- FOUND: taskflow/src/routes/dashboard/EpicProgressCells.test.tsx
- FOUND: commit eab4bdd1 (Task 1)
- FOUND: commit 32f134b1 (Task 2)
