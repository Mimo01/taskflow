---
phase: 91.2-epics-page-redesign
plan: 04
type: execute
wave: 3
depends_on: ["91.2-03"]
files_modified:
  - taskflow/src/services/jira/epics.ts
  - taskflow/src/services/jira/epics.test.ts
  - .planning/PROJECT.md
autonomous: false
requirements: [EPIC-02, EPIC-07]

must_haves:
  truths:
    - "The dead `src/services/jira/epics.ts` module and its colocated test are gone, and the full suite is still green (EPIC-07)"
    - "PROJECT.md no longer records the superseded 'EpicsPage uses fetchEpicsBasic, enrichment too slow' decision as current (D-04)"
    - "A human has confirmed in the real Tauri/WebKit webview that no column collapses to zero width and every epic stays on one line at compact, default and comfortable density (EPIC-02, D-07)"
    - "A human has confirmed the D-09 colour treatment reads as modern and keeps epic identity legible next to the segmented bar"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Corrected Key Decisions row reflecting progressive enrichment"
      contains: "progressive"
  key_links:
    - from: "taskflow/src/services/jira/index.ts"
      to: "removed epics module"
      via: "barrel never re-exported it — deletion is inert"
      pattern: "export \\* from"
---

<objective>
Close out the phase: remove the dead `services/jira/epics.ts` duplicate that made this phase's file targeting ambiguous, correct the now-false PROJECT.md decision row, and put the two manual-only verifications (WebKit zero-width columns across densities, D-09 colour review) in front of a human.

Purpose: the dead duplicate is the exact trap RESEARCH.md flagged as the single most important finding; leaving it invites the next phase to edit the wrong file. The manual checks cover the two behaviours jsdom structurally cannot reproduce.
Output: two deleted files, one corrected decision row, and a signed-off visual check.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91.2-epics-page-redesign/91.2-CONTEXT.md
@.planning/phases/91.2-epics-page-redesign/91.2-RESEARCH.md
@.planning/phases/91.2-epics-page-redesign/91.2-VALIDATION.md
@.planning/phases/91.2-epics-page-redesign/91.2-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the dead services/jira/epics.ts module and its test</name>
  <read_first>
    - taskflow/src/services/jira/epics.ts (confirm for yourself it is the duplicate of the functions now living in `src/services/jira.ts`)
    - taskflow/src/services/jira/index.ts (confirm the barrel re-exports only `attachments`, `duration`, `types`, `users`, `worklogs` — never `./epics`)
    - taskflow/src/services/jira/types.ts (line ~264, the duplicate `EpicEnriched` interface — check whether anything other than the deleted module referenced it)
    - .planning/phases/91.2-epics-page-redesign/91.2-RESEARCH.md (§ Summary and § Pitfall 1 — the dual-file finding and the grep that proves the module is dead)
  </read_first>
  <files>taskflow/src/services/jira/epics.ts, taskflow/src/services/jira/epics.test.ts</files>
  <action>
Prove-then-delete, in this order — do not delete first.

1. Run `grep -rn "services/jira/epics\|from './epics'" taskflow/src`. The only expected match is `taskflow/src/services/jira/epics.test.ts` importing its own subject. If ANY other file matches, stop and report instead of deleting — the dead-code premise would be false.

2. Delete `taskflow/src/services/jira/epics.ts` and `taskflow/src/services/jira/epics.test.ts` with `git rm`.

3. `taskflow/src/services/jira/types.ts` keeps its `EpicEnriched` interface ONLY if something still references it. Run `grep -rn "EpicEnriched" taskflow/src` after the deletion: the live definition and all real consumers are in `taskflow/src/services/jira.ts`. If the `types.ts` copy has zero remaining references, delete just that interface block from `types.ts` (leave every other export in that file untouched — it is re-exported by the barrel and widely consumed). If it still has references, leave it alone and note why in the summary.

4. Run the full suite. The deleted test file removes its own cases; no other test may change count or status. If any unrelated test breaks, the deletion premise was wrong — restore and report.
  </action>
  <verify>
    <automated>cd taskflow && npm test && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f taskflow/src/services/jira/epics.ts && test ! -f taskflow/src/services/jira/epics.test.ts` succeeds
    - `grep -rn "services/jira/epics\|from './epics'" taskflow/src` returns no matches
    - `git status --porcelain` shows both files as deletions (`D`), staged via `git rm`
    - `cd taskflow && npx tsc --noEmit` exits 0
    - `cd taskflow && npm test` is green with no failures introduced
  </acceptance_criteria>
  <done>The dead epic module and its test are removed, nothing imports them, typecheck and full suite are green.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the superseded PROJECT.md Key Decisions row</name>
  <read_first>
    - .planning/PROJECT.md (line ~300, the row reading "EpicsPage uses fetchEpicsBasic (not fetchEpicsWithEnrichment) | Enrichment requires N story-count queries — too slow for list view per user preference | ✓ Good — user accepted trade-off; detail available on click", plus the surrounding table's column format)
    - .planning/phases/91.2-epics-page-redesign/91.2-CONTEXT.md (§ Integration Points — the note that roadmap D-04 supersedes this decision)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
Rewrite that single Key Decisions row so it records the current, post-91.2 truth instead of the superseded one. Keep the table's three-column shape and the surrounding rows byte-identical.

New decision text: `EpicsPage renders fetchEpicsBasic first, then merges fetchEpicEnrichmentMap progressively`. Rationale column: note that Phase 91.2 (roadmap D-04) accepted the second batched query on the condition it never blocks first paint, and that it is one batched JQL for all epics — not N per-epic queries, which is what the original decision was avoiding. Outcome column: mark it as superseding the earlier "enrichment too slow for list view" decision, with the reference `Phase 91.2`.

Change nothing else in PROJECT.md — no other rows, no other sections.
  </action>
  <verify>
    <automated>grep -c "enrichment too slow for list view" .planning/PROJECT.md; grep -c "fetchEpicEnrichmentMap" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fetchEpicEnrichmentMap" .planning/PROJECT.md` matches the rewritten row
    - `grep -n "too slow for list view" .planning/PROJECT.md` no longer matches as a live/current decision
    - `git diff --stat .planning/PROJECT.md` shows a one-row change (roughly 1 insertion / 1 deletion), not a table rewrite
  </acceptance_criteria>
  <done>PROJECT.md records progressive enrichment as the current decision and marks the old one superseded by Phase 91.2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual Tauri/WebKit verification — zero-width columns, one-line rows, colour treatment</name>
  <read_first>
    - .planning/phases/91.2-epics-page-redesign/91.2-VALIDATION.md (§ Manual-Only Verifications — the two justified manual items)
    - .planning/phases/91.2-epics-page-redesign/91.2-UI-SPEC.md (§ Component Contract, § Visual Focal Point)
  </read_first>
  <action>
Pause autonomous execution and hand the running app to the user. Launch the Tauri dev build first (`cd taskflow && npm run tauri dev`) so the webview is already open, then present the verification steps below. Do not mark the phase complete until the user responds. Record their verdict — approval or the specific issues found (density, column, state) — verbatim in the plan summary; a rejection becomes the input to a follow-up gap-closure plan, not a silent pass.
  </action>
  <what-built>
    `/epics` rebuilt as a div+flex single-line row with columns key · name · status · priority · progress (three-segment bar + `done/total`) · points (`done/total SP`) · assignee, ordered `created ASC`, with progress and points streaming in behind first paint. The `<table>`/`<colgroup>` structure and the 1px left colour bar are gone; the tinted epic-name badge is now the sole epic-colour carrier.
  </what-built>
  <how-to-verify>
    1. Run the desktop app (`cd taskflow && npm run tauri dev`) — this must be the real Tauri/WebKit webview, not a browser tab and not jsdom. jsdom does not reproduce the WebKit column-collapse bug this check exists for.
    2. Navigate to `/epics` on a project with at least ~10 epics, including at least one epic with no child stories.
    3. Cycle the density setting through compact → default → comfortable. At each density confirm:
       - Every one of the seven columns is visible with non-zero width — especially priority (18px) and the progress bar
       - Every epic occupies exactly one line: no wrapped cell, no stacked second line, no clipped row
       - Long epic names truncate with an ellipsis inside the badge rather than pushing other columns
    4. Confirm the ordering: the first row is the oldest epic (lowest key number), and the order does not re-shuffle when the progress bars fill in.
    5. Confirm the four enrichment states: shimmer on first load, filled bars afterwards, `No stories` on the childless epic. Hover a bar and confirm the per-status breakdown appears in the native tooltip.
    6. Click a row → `EpicDetailSheet` opens as before. Click "+ Create Epic" → the dialog opens as before. Confirm there is no search box anywhere on the page.
    7. Visual judgement (D-09): does the tinted epic-name badge still read as epic identity alongside the Backlog epic chips, without competing with the segmented progress bar for attention? Compare side by side with the Backlog view.
  </how-to-verify>
  <verify>
    <human-check>All three densities keep every column at non-zero width with one line per epic; the four enrichment states render distinctly; row click and "+ Create Epic" behave as before; the colour treatment reads as modern and legible</human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms no zero-width column at compact, default or comfortable density (EPIC-02, ROADMAP criterion 8)
    - Human confirms exactly one line per epic at all three densities (D-07)
    - Human confirms row click still opens `EpicDetailSheet` and no search UI exists (EPIC-07, D-18)
    - Human approves or rejects the D-09 colour treatment; a rejection is recorded with the specific change requested
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the issues found (which density, which column, which state)</resume-signal>
  <done>A human has verified the two manual-only items and signed off, or filed specific issues for a follow-up plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository source tree | Files are deleted from the codebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91.2-10 | Denial of Service (self-inflicted) | Deleting `src/services/jira/epics.ts` while a consumer still imports it | mitigate | Task 1 requires a grep proof of zero external importers BEFORE deletion, plus a `tsc --noEmit` and full-suite gate after; a non-empty grep aborts the deletion |
| T-91.2-11 | Repudiation | Losing the deleted module's history | accept | Deletion uses `git rm`, so the file and its test remain fully recoverable from git history |
| T-91.2-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — no packages are installed in this phase (RESEARCH.md § Package Legitimacy Audit) |
</threat_model>

<verification>
- `cd taskflow && npm test` — full suite green after the deletion
- `cd taskflow && npx tsc --noEmit -p tsconfig.json` — exits 0
- `grep -rn "services/jira/epics" taskflow/src` — no matches
- Human sign-off recorded on the checkpoint
</verification>

<success_criteria>
- The dead duplicate epic module and its test are deleted with no import breakage (EPIC-07)
- PROJECT.md's superseded enrichment decision is corrected
- A human has verified no zero-width columns and one line per epic at all three densities in the real Tauri webview (EPIC-02)
- A human has approved the D-09 colour treatment or filed a specific change request
</success_criteria>

<output>
Create `.planning/phases/91.2-epics-page-redesign/91.2-04-SUMMARY.md` when done
</output>
