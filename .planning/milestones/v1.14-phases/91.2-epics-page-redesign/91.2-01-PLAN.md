---
phase: 91.2-epics-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/jira.ts
  - taskflow/src/services/jira.test.ts
autonomous: true
requirements: [EPIC-01, EPIC-03, EPIC-04, EPIC-05, EPIC-06]

must_haves:
  truths:
    - "The epics list JQL orders by `created ASC`, so rows arrive in creation/key order (D-17, EPIC-01)"
    - "`fetchEpicsBasic` returns each epic's Jira `priority` object so the row can render PriorityIcon (D-10, EPIC-04)"
    - "`fetchEpicEnrichmentMap` returns per-status-category counts (done / inProgress / todo) for every epic key (D-11, EPIC-05)"
    - "`fetchEpicEnrichmentMap` returns a `donePoints` aggregate alongside `points` (D-13, EPIC-06)"
    - "A failed stories fetch propagates as a rejected promise instead of resolving to an empty map, so the UI can show the D-15 error state (EPIC-03)"
    - "Done epics stay excluded — the `statusCategory != Done` clause is untouched (D-03)"
  artifacts:
    - path: "taskflow/src/services/jira.ts"
      provides: "fetchEpicsBasic ordering + priority field; fetchEpicEnrichmentMap category counts, donePoints, fail-closed error propagation"
      contains: "ORDER BY created ASC"
    - path: "taskflow/src/services/jira.test.ts"
      provides: "Unit coverage for the JQL ordering, priority field request, aggregation shape and error propagation"
      contains: "fetchEpicEnrichmentMap"
  key_links:
    - from: "taskflow/src/services/jira.ts fetchEpicsBasic"
      to: "Jira /rest/api/2/search"
      via: "JQL string with ORDER BY created ASC and a fields list containing 'priority'"
      pattern: "ORDER BY created ASC"
    - from: "taskflow/src/services/jira.ts fetchEpicEnrichmentMap"
      to: "fetchAllSearchPages"
      via: "un-caught await so rejections reach the caller"
      pattern: "fetchAllSearchPages\\("
---

<objective>
Correct the two live epic fetchers in `src/services/jira.ts` so the redesigned `/epics` page has the data it needs: creation-ascending ordering (D-17), the `priority` field (D-10), per-status-category child counts (D-11), a `donePoints` aggregate (D-13), and fail-closed error propagation (D-15).

Purpose: every downstream UI plan reads from these two functions. Getting the data contract right first means the row and the progress cell are pure presentation.
Output: edited `fetchEpicsBasic` / `fetchEpicEnrichmentMap` / `EpicEnriched` in `src/services/jira.ts`, plus new unit tests in `src/services/jira.test.ts`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91.2-epics-page-redesign/91.2-CONTEXT.md
@.planning/phases/91.2-epics-page-redesign/91.2-RESEARCH.md
@.planning/phases/91.2-epics-page-redesign/91.2-PATTERNS.md
@.planning/phases/91.2-epics-page-redesign/91.2-VALIDATION.md
</context>

<critical_landmine>
**Dual-file trap.** `@/services/jira` resolves to the FILE `taskflow/src/services/jira.ts`, not the directory `taskflow/src/services/jira/`. `taskflow/src/services/jira/epics.ts` contains a near-identical copy of these functions and is DEAD CODE — nothing imports it outside its own colocated test (verified: `grep -rn "from '@/services/jira/epics'\|from './epics'" src` returns only `src/services/jira/epics.test.ts:7`). Editing it has zero runtime effect. Every edit in this plan goes in `taskflow/src/services/jira.ts` (epic section starts at the `// Phase 13 — Epic Management` banner, ~line 2407). Do NOT touch `src/services/jira/epics.ts` — plan 91.2-04 deletes it.
</critical_landmine>

<tasks>

<task type="auto">
  <name>Task 1: fetchEpicsBasic — creation-ascending ordering, priority field, EpicEnriched.priority</name>
  <read_first>
    - taskflow/src/services/jira.ts (read the epic section from the `// Phase 13 — Epic Management` banner through `fetchEpicsWithEnrichment`, ~lines 2407-2580 — you need the current `EpicEnriched` interface, `fetchEpicsBasic`, and `fetchEpicsWithEnrichment`'s field list which already includes `'priority'` and is the in-file analog to port from)
    - .planning/phases/91.2-epics-page-redesign/91.2-PATTERNS.md (section `taskflow/src/services/jira.ts — fetchEpicsBasic / fetchEpicEnrichmentMap`, which quotes the exact current and required strings)
  </read_first>
  <files>taskflow/src/services/jira.ts</files>
  <action>
Three edits inside `taskflow/src/services/jira.ts`, all within the `// Phase 13 — Epic Management` section.

1. `EpicEnriched` interface: add an optional `priority?: { name?: string; iconUrl?: string } | null;` member after `assignee`. Do not remove or rename any existing member (`key`, `epicName`, `summary`, `status`, `assignee`, `totalStories`, `doneStories`, `totalPoints`, `color`) — `BacklogPage.tsx` and `SprintBoardTab` consume this same type through the shared `['jira-epics-basic', ...]` query key.

2. `fetchEpicsBasic` field list: the current `epicFields` is built as `[...new Set(['summary', 'status', 'assignee', epicNameFieldKey, epicColorFieldKey])].join(',')`. Add the literal `'priority'` to that Set so the search request asks Jira for the priority field. `'priority'` is a Jira system field (not a customfield) — `fetchEpicsWithEnrichment` in this same file already requests it exactly this way, so port from there rather than inventing a key.

3. `fetchEpicsBasic` JQL and mapping, per D-17 (EPIC-01) and D-10 (EPIC-04):
   - Change the trailing clause of the JQL template from `ORDER BY updated DESC` to `ORDER BY created ASC`. Leave `project = ${projectKey} AND issuetype = Epic AND statusCategory != Done` byte-identical — D-03 keeps Done epics hidden, and changing that clause is out of scope.
   - In the `epicIssues.map(...)` return object, add `priority: epic.fields.priority ?? null,` after `assignee`. Keep the existing `totalStories: 0 / doneStories: 0 / totalPoints: 0` placeholders — the enrichment query, not this one, supplies real counts.

Also update the function's JSDoc to say the list is returned in creation order (oldest first). Do not touch `fetchEpicsWithEnrichment` or `fetchEpicStories` — they are separate callers and stay on their current ordering/fields (smallest safe diff).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/services/jira.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ORDER BY created ASC" taskflow/src/services/jira.ts` matches inside `fetchEpicsBasic`
    - `grep -c "ORDER BY updated DESC" taskflow/src/services/jira.ts` no longer matches within the `fetchEpicsBasic` body (other functions elsewhere in the file may legitimately still use it)
    - `grep -n "statusCategory != Done" taskflow/src/services/jira.ts` still matches inside `fetchEpicsBasic` (D-03 unchanged)
    - The `EpicEnriched` interface declares `priority?: { name?: string; iconUrl?: string } | null`
    - `fetchEpicsBasic`'s `epicFields` Set literal contains `'priority'`
    - `fetchEpicsBasic`'s mapped return object contains a `priority:` key
    - `npx tsc --noEmit` exits 0 (no existing `EpicEnriched` consumer breaks)
  </acceptance_criteria>
  <done>`fetchEpicsBasic` requests `priority`, orders by `created ASC`, returns `priority` on every epic, and typechecks against all existing consumers.</done>
</task>

<task type="auto">
  <name>Task 2: fetchEpicEnrichmentMap — category counts, donePoints, fail-closed errors</name>
  <read_first>
    - taskflow/src/services/jira.ts (the `fetchEpicEnrichmentMap` body, ~lines 2458-2495 — the current `storyFields` list, the `.catch(() => [] as JiraIssue[])`, and the `countMap` aggregation loop)
    - .planning/phases/91.2-epics-page-redesign/91.2-RESEARCH.md (`## Code Examples` — the verified target aggregation loop and the error-propagation before/after)
    - .planning/phases/91.2-epics-page-redesign/91.2-CONTEXT.md (D-11, D-12, D-13, D-15)
  </read_first>
  <files>taskflow/src/services/jira.ts</files>
  <action>
Rework `fetchEpicEnrichmentMap` in `taskflow/src/services/jira.ts`.

1. Export a named type for the aggregate so the UI can import it instead of re-declaring an inline shape:
   `export interface EpicEnrichmentCounts { total: number; done: number; inProgress: number; todo: number; points: number; donePoints: number; }`
   Place it directly above `fetchEpicEnrichmentMap`. Change the function's return type to `Promise<Map<string, EpicEnrichmentCounts>>`.

2. Fail-closed correction (D-15, EPIC-03): delete the `.catch(() => [] as JiraIssue[])` suffix on the `fetchAllSearchPages(...)` await so a failed stories fetch rejects and reaches `useQuery`'s `isError`. This is the same bug class as `searchProjectTags` in 91-07 and 88-CR-01 — a swallowed error currently renders as verified-negative "0 stories". Leave the identical `.catch(() => [])` in `fetchEpicsWithEnrichment` and `fetchEpicStories` untouched: they are other callers, and widening the blast radius is out of scope for this phase.

3. Per-status-category aggregation (D-11, EPIC-05) and `donePoints` (D-13, EPIC-06): widen the `countMap` entry default from `{ total: 0, done: 0, points: 0 }` to `{ total: 0, done: 0, inProgress: 0, todo: 0, points: 0, donePoints: 0 }`. In the loop body, read `story.fields.status.statusCategory?.key` once into a local, read the story points once into a local (`(story.fields[storyPointsFieldKey] as number | null) ?? 0`), always add points to `entry.points`, then branch: `'done'` increments `entry.done` and adds the points to `entry.donePoints`; `'indeterminate'` increments `entry.inProgress`; every other value (including `'new'` and `undefined`) increments `entry.todo`. Keep the existing `if (!ek) continue;` guard on the epic-link field and keep the early `if (epicKeys.length === 0) return new Map();` return.

4. Do NOT add `flaggedFieldKey` or any flag-related field to `storyFields` — D-12 states the Jira Flagged field is never fetched by this page. `storyFields` keeps exactly its current members (`'status'`, `storyPointsFieldKey`, `epicLinkFieldKey`, `'customfield_10016'`).

5. Do not change the JQL (`"Epic Link" in (${epicKeys.join(',')}) AND issuetype != Sub-task`) or the function signature/parameter defaults — the epic keys interpolated into the JQL come from a prior trusted Jira response, and widening that interpolation surface is explicitly out of scope (see `<threat_model>` T-91.2-01).

Update the JSDoc to state that the function now throws on fetch failure (callers must render an error state) and lists the returned aggregate members.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/services/jira.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "EpicEnrichmentCounts" taskflow/src/services/jira.ts` shows an exported interface with all six members: `total`, `done`, `inProgress`, `todo`, `points`, `donePoints`
    - The `fetchEpicEnrichmentMap` body contains no `.catch(` on its `fetchAllSearchPages` await
    - `grep -n "flagged" taskflow/src/services/jira.ts` shows no flag field added to `fetchEpicEnrichmentMap`'s `storyFields`
    - The JQL string in `fetchEpicEnrichmentMap` is byte-identical to before this task
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>`fetchEpicEnrichmentMap` returns `Map<string, EpicEnrichmentCounts>` with category counts and `donePoints`, and rejects on fetch failure instead of returning an empty map.</done>
</task>

<task type="auto">
  <name>Task 3: Service-level unit tests for both fetchers</name>
  <read_first>
    - taskflow/src/services/jira.test.ts (read the `describe('EPIC-01: fetchEpicsWithEnrichment', ...)` block, ~lines 1323-1430 — reuse its `makeEpicIssue` / `makeStoryIssue` / `makeJsonResponse` helper style and its `vi.mocked(mockFetch).mockResolvedValueOnce(...)` sequencing; also read the file's import block at the top to see how epic fetchers are imported)
    - taskflow/src/services/jira.ts (the two functions edited in tasks 1-2, as the source of truth for exact behaviour)
    - .planning/phases/91.2-epics-page-redesign/91.2-VALIDATION.md (rows targeting `npx vitest run src/services/jira.test.ts -t "epics"` and `-t "enrichment"`)
  </read_first>
  <files>taskflow/src/services/jira.test.ts</files>
  <action>
Add a new `describe('EPIC-01/EPIC-03: epics list + enrichment fetchers (Phase 91.2)', ...)` block to `taskflow/src/services/jira.test.ts`, importing `fetchEpicsBasic` and `fetchEpicEnrichmentMap` from `./jira` alongside the existing epic imports. Reuse the local `makeEpicIssue` / `makeStoryIssue` / `makeJsonResponse` helper pattern from the existing `fetchEpicsWithEnrichment` describe block (duplicate or hoist them — do not refactor unrelated existing tests).

Test names must contain the words the validation map greps for. Required cases:

1. `'epics list JQL orders by created ASC and requests the priority field'` — call `fetchEpicsBasic`, then assert on the URL passed to the mocked fetch: `decodeURIComponent(url)` contains `ORDER BY created ASC`, does not contain `ORDER BY updated DESC`, still contains `statusCategory != Done`, and the `fields=` segment contains `priority`.
2. `'epics list maps the priority object onto each epic'` — resolve an epic fixture whose `fields.priority` is `{ name: 'Must', iconUrl: 'https://jira.example.com/images/icons/priorities/must.svg' }`; assert the returned epic's `priority.name === 'Must'` and `priority.iconUrl` matches. Add a second epic fixture with `priority` absent and assert its mapped `priority` is `null`.
3. `'enrichment aggregates per status category and donePoints'` — stories across two epics covering all three category keys (`'done'`, `'indeterminate'`, `'new'`) with distinct point values; assert the returned map entry has exact `total`, `done`, `inProgress`, `todo`, `points` and `donePoints` numbers, and that `donePoints` only sums points from `done`-category stories.
4. `'enrichment treats an unknown or missing statusCategory as todo'` — a story whose `status.statusCategory` is undefined increments `todo`, not `done`/`inProgress`.
5. `'enrichment propagates fetch errors instead of returning an empty map'` — make the mocked fetch reject (or resolve non-ok so `fetchAllSearchPages` throws); assert `await expect(fetchEpicEnrichmentMap(...)).rejects.toThrow()`. This is the regression guard for the D-15 fail-closed correction — it must fail if anyone re-adds a `.catch`.
6. `'enrichment returns an empty map without fetching for zero epic keys'` — assert the mocked fetch was not called.

Follow the file's existing mock-fetch setup (`mockFetch` / `vi.mocked`) rather than introducing a new HTTP mocking approach.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/services/jira.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npx vitest run src/services/jira.test.ts` passes with 6 new tests added and zero previously-passing tests broken
    - `cd taskflow && npx vitest run src/services/jira.test.ts -t "enrichment"` matches at least the aggregation, error-propagation and empty-keys tests
    - The error-propagation test uses `rejects.toThrow()` (not a truthiness check on an empty map)
    - `cd taskflow && npm test` full suite is green
  </acceptance_criteria>
  <done>Both fetchers have unit coverage asserting ordering, the priority field, the six-member aggregate, and error propagation; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Taskflow client → Jira REST API | Epic keys and field keys are interpolated into a JQL query string sent to Jira over HTTPS with a Bearer PAT |
| Jira REST response → client aggregation | Untyped `fields[...]` values (story points, epic link, status category) are read and summed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91.2-01 | Tampering | `fetchEpicEnrichmentMap` JQL interpolation of `epicKeys` into `"Epic Link" in (...)` | accept | Pre-existing surface, not introduced or widened here. Epic keys originate from a prior trusted Jira search response (Jira-issued key format), never from user free text. Task 2 explicitly forbids adding any new interpolated value to this JQL, and D-18 removed the only planned user-input source on this page |
| T-91.2-02 | Information Disclosure | Bearer PAT in the `Authorization` header built inside these fetchers | accept | Unchanged from current code — the token is read via `readSecret('jira-pat')` (Stronghold) and passed as a parameter; this plan adds no logging of headers, URLs or token values |
| T-91.2-03 | Denial of Service | Removing `.catch(() => [])` means enrichment failures now reject and React Query may retry | mitigate | Retry is bounded by React Query's default retry policy and by the manual, user-initiated retry affordance built in plan 91.2-02/03 — no unbounded auto-retry loop is introduced |
| T-91.2-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this phase installs zero new packages (RESEARCH.md § Package Legitimacy Audit); no install task exists in this plan |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/services/jira.test.ts` — green
- `cd taskflow && npx tsc --noEmit -p tsconfig.json` — exits 0
- `cd taskflow && npm test` — full suite green
- `grep -rn "from '@/services/jira/epics'" taskflow/src` — still returns nothing outside `epics.test.ts` (confirms no edit landed in the dead module)
</verification>

<success_criteria>
- `fetchEpicsBasic` issues `ORDER BY created ASC`, requests `priority`, and returns it on every epic (EPIC-01, EPIC-04)
- `fetchEpicEnrichmentMap` returns `Map<string, EpicEnrichmentCounts>` with `total`, `done`, `inProgress`, `todo`, `points`, `donePoints` (EPIC-05, EPIC-06)
- A failed stories fetch rejects rather than resolving to an empty map (EPIC-03, D-15)
- Six new unit tests cover all of the above and the full suite is green
- `src/services/jira/epics.ts` is untouched
</success_criteria>

<output>
Create `.planning/phases/91.2-epics-page-redesign/91.2-01-SUMMARY.md` when done
</output>
