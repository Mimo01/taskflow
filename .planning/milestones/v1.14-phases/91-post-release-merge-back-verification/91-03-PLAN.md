---
phase: 91-post-release-merge-back-verification
plan: 03
type: execute
wave: 2
depends_on: ["91-01", "91-02"]
files_modified:
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts
  - taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
  - taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx
autonomous: true
requirements: [MERGE-01, MERGE-02, MERGE-03]
must_haves:
  truths:
    - "On a released fix version's detail page, a 'Merged back' row states whether the release landed in the fetched default branch"
    - "The row is hidden entirely for an unreleased version or when no milestone matched"
    - "An unreleased version fires zero extra GitLab calls"
    - "The 'Release Branch' released-state row no longer asserts an unverified merge"
    - "No confirm, dismiss, acknowledge, or override control exists anywhere near the verdict, and nothing persists"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts"
      provides: "two gated queries + resolveMergeBackVerdict call + mergeBackVerdict in the return object"
      contains: "resolveMergeBackVerdict"
    - path: "taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx"
      provides: "the 'Merged back' MetaRow and the softened released-branch wording"
      contains: "Merged back"
    - path: "taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx"
      provides: "coverage of all five verdict renderings plus the hidden case and the D-08 wording change"
      contains: "merge-back"
  key_links:
    - from: "taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts"
      to: "services/gitlab.ts fetchSourceBranchMRs + compareRefs"
      via: "two useQuery calls gated on releasedVersion"
      pattern: "fetchSourceBranchMRs|compareRefs"
    - from: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      to: "ReleaseDetailSidebar"
      via: "mergeBackVerdict prop"
      pattern: "mergeBackVerdict=\\{mergeBackVerdict\\}"
---

<objective>
Wire the merge-back verdict end to end: two gated queries and the resolver call in
`useReleaseDetail.ts`, the new "Merged back" `MetaRow` in `ReleaseDetailSidebar.tsx` with D-10's
locked copy, the D-08 softening of the existing released-branch wording, prop threading through
`ReleaseDetailPage.tsx`, and component + hook test coverage.

Purpose: this plan is where MERGE-01 becomes visible — "once the fix version is marked released,
the user sees whether `release/[tag]` has been merged back into the project default branch" — and
where D-08 removes the existing unverified "was merged and deleted" claim that MERGE-01 exists to
replace with evidence.

**MERGE-03 is DESCOPED by D-12 and this plan implements no part of it.** The user's decision, given
twice ("I dont want to store anything", then "no override control at all" after the MERGE-03
consequence was stated explicitly with a session-only alternative on the table), removes any manual
override / confirm / dismiss / acknowledge control and any persistence (no Zustand, no Tauri store,
no Jira, no GitLab). MERGE-03 appears in this plan's `requirements` for coverage traceability only.
It must be recorded as an intentional descope — the same handling as DASH-06 (P84 UAT) and DRIFT-09
(P89 UAT) — and its absence is NOT a gap. D-13 keeps the advisory tone in the word "Likely" and in
the tooltip naming exactly what was compared, which is most of what an override would have bought.
Do not invent an affordance to close MERGE-03.

Output: a working, advisory, read-only merge-back verdict row.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-post-release-merge-back-verification/91-CONTEXT.md
@.planning/phases/91-post-release-merge-back-verification/91-RESEARCH.md
@.planning/phases/91-post-release-merge-back-verification/91-PATTERNS.md
@.planning/phases/91-post-release-merge-back-verification/91-UI-SPEC.md
@.planning/phases/91-post-release-merge-back-verification/91-01-SUMMARY.md
@.planning/phases/91-post-release-merge-back-verification/91-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two gated queries and the resolver call in useReleaseDetail.ts</name>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` L1-46 (import block style),
      L140-215 (the project/`defaultBranch` query, the branch query + `branchCheckFailed`, the
      CR-03 comment block, `releasedVersion`, `matchedVersionNumber`, `needsTagLookup`, the
      `gitlab-release-tags` query, and the `resolveBranchState` call site), and the hook's return
      object around L452 where `branchState` is exposed
    - `taskflow/src/services/gitlab.ts` — `fetchSourceBranchMRs` and `compareRefs` signatures from Plan 01
    - `taskflow/src/routes/dashboard/release-detail/mergeBackVerification.ts` — `resolveMergeBackVerdict`
      parameter names and the `MergeBackVerdict` union from Plan 02
    - `.planning/phases/91-post-release-merge-back-verification/91-RESEARCH.md` §Common Pitfalls 3 and 4
    - `.planning/phases/91-post-release-merge-back-verification/91-PATTERNS.md`
      §`useReleaseDetail.ts` and §`?? 0` project-id guard (WR-10)
  </read_first>

  <files>taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts</files>

  <action>
Modify `useReleaseDetail.ts` only. Do not touch the sidebar or the page in this task.

1. **Imports.** Add `compareRefs` and `fetchSourceBranchMRs` to the existing `@/services/gitlab`
   import list, keeping its alphabetical ordering. Add a new import line beside the existing
   `./releaseBranch` import for `resolveMergeBackVerdict` and a `import type { MergeBackVerdict }`
   from `./mergeBackVerification`.

2. **Extract the tag name once.** The `resolveBranchState` call at L199-209 currently computes
   `findReleaseTag((releaseTags ?? []).map((t) => t.name), matchedVersionNumber)` inline. Hoist that
   expression to `const mergeBackTagName` declared above the `resolveBranchState` call, and pass
   `releaseTagName: mergeBackTagName` there instead. Both the branch row and the new verdict read
   the same resolved tag — do not call `findReleaseTag` twice.

3. **Widen `needsTagLookup` (D-01 / 91-RESEARCH Pitfall 4).** Change it from
   `releasedVersion && branchResult?.exists === false && !!matchedVersionNumber` to
   `releasedVersion && !!matchedVersionNumber` — drop the `branchResult?.exists === false` clause.
   Update the comment block above it (currently L174-184): the tag is now needed whenever a version
   is released, independent of branch state, because D-01's content-comparison fallback triggers
   whenever no merged tracking MR is found, which is determined independently of (and often before)
   the branch-existence check. Note in the comment that the redundant fetch for a released version
   whose branch still exists is accepted: the tag query is a single search-scoped call and
   `searchProjectTags` returns `[]` on failure rather than throwing.

4. **Tracking-MR query.** Add a `useQuery` returning `{ data: trackingMRs, isError: trackingMRsCheckFailed }`:
   - `queryKey: ['gitlab-mr-source-branch', activeGitlabProject, releaseBranchName]`
   - `queryFn: () => fetchSourceBranchMRs(gitlabBaseUrl ?? '', gitlabToken ?? '', activeGitlabProject ?? 0, releaseBranchName ?? '')`
   - `enabled: !!gitlabBaseUrl && !!activeGitlabProject && !!gitlabToken && releasedVersion && releaseBranchName !== null`
   - `staleTime: 5 * 60_000`
   The `releasedVersion` clause is D-05's gate: an unreleased version must fire ZERO extra GitLab
   calls. The `?? ''`/`?? 0` fallbacks are only safe because `enabled` independently gates on
   `!!activeGitlabProject`/`!!gitlabToken`/`!!gitlabBaseUrl` (WR-10) — comment that pairing.

5. **Compare query.** Add a `useQuery` returning `{ data: compareResult, isError: compareCheckFailed }`:
   - `queryKey: ['gitlab-compare', activeGitlabProject, defaultBranch, mergeBackTagName]`
   - `queryFn: () => compareRefs(gitlabBaseUrl ?? '', gitlabToken ?? '', activeGitlabProject ?? 0, defaultBranch ?? '', mergeBackTagName ?? '')`
   - `enabled: !!gitlabBaseUrl && !!activeGitlabProject && !!gitlabToken && releasedVersion && defaultBranch !== null && mergeBackTagName !== null`
   - `staleTime: 5 * 60_000`
   Per 91-RESEARCH Pitfall 3, the `enabled` gate depends on the tag query's RESOLVED result
   (`mergeBackTagName !== null`), not merely on `needsTagLookup` — comment that, or the query fires
   with an empty `to` ref or never fires at all.

6. **Resolver call.** Below the `resolveBranchState` call add:
   `const mergeBackVerdict: MergeBackVerdict = resolveMergeBackVerdict({ releasedVersion, hasMatchedMilestone: matchedMilestone !== null, defaultBranch, trackingMRs, trackingMRsCheckFailed, tagName: mergeBackTagName, expectedTagName: matchedVersionNumber ? \`v${matchedVersionNumber}\` : null, compareResult, compareCheckFailed })`.
   `expectedTagName` is the tag the app WOULD have looked for — it feeds the `couldn't verify`
   tooltip so the user learns which tag was missing (UI-SPEC: "substitute the actual expected tag
   name"). Pass only already-resolved plain values, never live query objects.

7. **Return.** Add `mergeBackVerdict` to the hook's returned object next to `branchState`.

Do not add any mutation, any `queryClient.invalidateQueries` call, any store write, or any
persistence — this phase is read-only (D-15). Do not add `useMemo`/`useCallback` (React Compiler is
on; the single existing `useMemo` at L317 is a deliberate query-key-stability exception, not a
precedent).
  </action>

  <verify>
    <automated>cd taskflow && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "gitlab-mr-source-branch" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 1 match
    - `grep -n "'gitlab-compare'" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 1 match
    - `grep -n "resolveMergeBackVerdict" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 2 matches (import + call)
    - `grep -n "mergeBackVerdict" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` includes an occurrence inside the hook's return object
    - `grep -n "needsTagLookup =" -A2 taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` shows `releasedVersion && !!matchedVersionNumber` with no `branchResult?.exists` clause
    - Both new `enabled` expressions contain `releasedVersion` (D-05 gate)
    - The compare query's `enabled` contains `mergeBackTagName !== null` and `defaultBranch !== null`
    - `grep -c "findReleaseTag(" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` is 2 (import + one call site)
    - The diff introduces no `useMutation`, no `invalidateQueries`, and no store write
    - `cd taskflow && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>The hook fires both new queries only for released versions, resolves the verdict from plain data, and returns `mergeBackVerdict` — with the tag gate widened per D-01 and no writes anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: The "Merged back" MetaRow, the D-08 wording softening, and prop threading</name>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx` L1-20 (icon imports),
      L26-56 (`RowAction`, `RowUnavailable`), L58-108 (`ReleaseDetailSidebarProps` + destructure),
      L206-276 (the full "Release Branch" `MetaRow`, including the `check-failed`/`loading`/`exists`
      blocks to copy shape from and **L253-266, the `released` block whose wording D-08 requires
      softening**), L278+ (the "MR Labels" row the new row must precede)
    - `taskflow/src/routes/dashboard/release-detail/MetaRow.tsx` — the row primitive
    - `taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` L55-70 (hook destructure) and L320-335
      (the `<ReleaseDetailSidebar …>` prop list)
    - `taskflow/src/routes/dashboard/release-detail/mergeBackVerification.ts` — the union's field
      names and the two date formatters
    - `.planning/phases/91-post-release-merge-back-verification/91-UI-SPEC.md` §Copywriting Contract,
      §Color, §Spacing Scale, §Interaction Contract — this contract is APPROVED; reproduce its
      strings exactly and do not redesign
  </read_first>

  <files>taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx, taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>

  <action>
**A. Soften the existing released-branch wording (D-08 — required, not polish).**

In `ReleaseDetailSidebar.tsx` L253-266, the `branchState.kind === 'released'` block currently renders
a `title` of `"{branchName} was merged and deleted; tagged {tagName}"` (and a longer no-tag variant
also containing "was merged and deleted"). That asserts a merge with zero evidence — exactly the
claim MERGE-01 exists to verify. Replace both `title` variants so they state only what is known:
  - with a tag: `` `${branchState.branchName} deleted · tagged ${branchState.tagName}` ``
  - without a tag: `` `${branchState.branchName} deleted. No matching tag found — tags are an incomplete record, so this is not evidence the release did not ship.` ``
The word "merged" must not appear anywhere in this block. Keep `data-testid="branch-status-released"`,
the visible `Released` text, the `size-3` icon, and the tag `font-mono` span unchanged. Replace the
`Check` icon in THIS block with `GitBranch` (already imported) so the affirmative green-implying
check no longer stands in for an unverified merge claim; the `Check` icon now belongs exclusively to
the new row's evidence-backed `merged` verdict.

**B. Add the "Merged back" row.**

Add `mergeBackVerdict: MergeBackVerdict` to `ReleaseDetailSidebarProps` immediately after
`defaultBranch: string | null` (L69) and to the component's destructured parameter list, with a
`import type { MergeBackVerdict }` plus a value import of `formatEvidenceDate` and
`formatVerdictDate` from `./mergeBackVerification`.

Insert a new `MetaRow` between the "Release Branch" `MetaRow`'s closing tag (currently L276) and the
"MR Labels" `MetaRow` (L278), wrapped in a JSX `&&` guard so the ENTIRE row disappears when
`mergeBackVerdict.kind === 'hidden'` — matching the existing conditional-row convention used by the
"MRs"/"Issues"/"Story points" rows. Never render a placeholder `—` (D-11).

Inside `<MetaRow label="Merged back">`, branch on `mergeBackVerdict.kind` using the same ternary
chain style as the Release Branch row. Copy is LOCKED by D-10 / UI-SPEC — reproduce exactly:

  - `loading` → `<span className="text-muted-foreground text-xs">Loading...</span>` with
    `data-testid="merge-back-loading"`. No spinner, no skeleton (UI-SPEC Interaction Contract).
  - `merged`, `via === 'tracking-mr'` → an inline-flex span, `className="inline-flex items-center gap-1 text-green-600 text-xs dark:text-green-400"`,
    `data-testid="merge-back-merged"`, containing a `<Check className="size-3 shrink-0" />` and the
    text `Merged into {defaultBranch}` followed by ` · {formatVerdictDate(mergedAt)}` ONLY when that
    formatter returns a non-null value (P-03: `merged_at` is optional on the list endpoint — omit
    the separator and date rather than rendering an empty or invented one).
    `title` = `` `via !${mrIid}, merged ${formatEvidenceDate(mergedAt)}` `` when the date is
    available, otherwise `` `via !${mrIid}` ``.
  - `merged`, `via === 'content-compare'` → identical icon, colour, testid, and main-line shape, but
    the text is `Merged into {defaultBranch}` with NO date, and
    `title` = `` `no diff between ${tagName} and ${defaultBranch}` `` (**planner call P-02**, closing
    91-RESEARCH Open Question 1: D-10 specifies only the MR-sourced tooltip; this path has no MR and
    no merge date, so it names the evidence it actually has rather than inventing an MR reference or
    passing off the tag's creation date as a merge date).
  - `likely-not-merged` → `className="inline-flex items-center gap-1 text-orange-600 text-xs dark:text-orange-400"`,
    `data-testid="merge-back-likely-not-merged"`, `<AlertTriangle className="size-3 shrink-0" />`,
    text `Likely not merged into {defaultBranch}`,
    `title` = `` `${tagName} has ${commitsNotInDefault} commits not in ${defaultBranch}` ``.
  - `couldnt-verify` → `className="inline-flex items-center gap-1 text-muted-foreground text-xs"`,
    `data-testid="merge-back-couldnt-verify"`, no icon-colour emphasis, text `Couldn't verify`,
    `title` = `` `no tracking MR and no ${expectedTagName} tag found` `` when `expectedTagName` is
    non-null and `reason === 'no-mr-no-tag'`; when `expectedTagName` is null use
    `no tracking MR and no release tag found`; when `reason === 'check-failed'` use
    `the merge-back check could not be completed`.

Hard constraints for this row (D-12 / D-13 / UI-SPEC): NO `RowAction`, no `Button`, no link, no
checkbox, no dismiss/confirm/acknowledge control, no `onClick`, no banner, no toast. It is text +
icon + native `title` only. Do not copy the `check-failed` row's `Retry` `RowAction`. Use the native
`title` attribute, never a shadcn `Tooltip`/`Popover`. Every icon is `size-3 shrink-0`; introduce no
new gap, icon size, or hex colour.

**C. Thread the prop.** In `ReleaseDetailPage.tsx`, add `mergeBackVerdict` to the `useReleaseDetail`
destructure alongside `branchState`, and pass `mergeBackVerdict={mergeBackVerdict}` to
`<ReleaseDetailSidebar>` next to `branchState={branchState}`. Change nothing else in that file.
  </action>

  <verify>
    <automated>cd taskflow && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "was merged and deleted" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx` is 0
    - `grep -n "deleted · tagged" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx` returns 1 match
    - The `branchState.kind === 'released'` block contains no `merged` substring and no `<Check` element
    - `grep -n 'label="Merged back"' taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx` returns 1 match, positioned after the Release Branch `MetaRow` and before `label="MR Labels"`
    - `grep -c "merge-back-merged\|merge-back-likely-not-merged\|merge-back-couldnt-verify\|merge-back-loading" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx` is at least 4
    - The new row's JSX contains no `RowAction`, no `<Button`, no `onClick`, and no `href`
    - `grep -n "no diff between" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx` returns 1 match (P-02 tooltip)
    - `grep -n "Likely not merged into" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx` returns 1 match
    - The new row uses `text-green-600 dark:text-green-400`, `text-orange-600 dark:text-orange-400`, and `text-muted-foreground` only — no new hex value, no new icon size besides `size-3`
    - `grep -n "mergeBackVerdict=\{mergeBackVerdict\}" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 1 match
    - `cd taskflow && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>The sidebar renders a hidden-or-five-state "Merged back" row with D-10's exact copy and no interactive control, the released-branch row no longer claims a merge, and the page threads the verdict through.</done>
</task>

<task type="auto">
  <name>Task 3: Component and hook test coverage</name>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx` L1-60 (the
      `renderSidebar` harness and its full default prop list — it must gain a
      `mergeBackVerdict: { kind: 'hidden' }` default) and L106-136 (the two existing
      `branch-status-released` assertions that D-08's wording change lands on)
    - `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` L1-80 (the
      `vi.mock('@/services/gitlab', …)` factory — it must gain `fetchSourceBranchMRs` and
      `compareRefs` entries — plus `makeMilestone`, `makeWrapper`, and `setupMocks`)
    - `taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx` — the row from Task 2
    - `.planning/phases/91-post-release-merge-back-verification/91-VALIDATION.md`
      §Requirement → Observable Signal Map (the MERGE-01 `component`/`hook` rows and the D-08 row)
    - `.planning/phases/91-post-release-merge-back-verification/91-RESEARCH.md` §Pitfall 5
      (the hidden-row coverage gap this task must not repeat)
  </read_first>

  <files>taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx, taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx</files>

  <action>
**A. `ReleaseDetailSidebar.test.tsx`.**

Add `mergeBackVerdict={{ kind: 'hidden' }}` to the `renderSidebar` default prop list so every
existing test keeps passing unchanged, and import `type { MergeBackVerdict }`.

Update the two existing released-state tests (L108-127) for D-08: keep the `Released` and `v33.6.0`
text assertions, and ADD an assertion that the element's `title` attribute does not match `/merged/i`
and does match `/deleted/`. This is the regression lock on the wording change — the row must never
regain a merge claim.

Add `describe('ReleaseDetailSidebar — Merged back row (MERGE-01)')` with:
  - `hidden` → `expect(screen.queryByText('Merged back')).toBeNull()` and all four verdict testids
    absent (91-RESEARCH Pitfall 5 — the absence case is the one most often skipped)
  - `loading` → `merge-back-loading` present with text `Loading...`
  - `merged` via `tracking-mr` with a `mergedAt` → `merge-back-merged` has text content
    `Merged into develop` and `21 Jul`, and its `title` is `via !4821, merged 21.07.2026`
  - `merged` via `tracking-mr` with `mergedAt: null` → text contains `Merged into develop`, contains
    no `·` separator, and `title` is `via !4821` (P-03)
  - `merged` via `content-compare` → text `Merged into develop`, `title`
    `no diff between v33.7.0 and develop` (P-02)
  - `likely-not-merged` → `merge-back-likely-not-merged` text `Likely not merged into develop`,
    `title` `v33.7.0 has 12 commits not in develop`
  - `couldnt-verify` / `no-mr-no-tag` → `merge-back-couldnt-verify` text `Couldn't verify`, `title`
    `no tracking MR and no v33.7.0 tag found`
  - `couldnt-verify` / `check-failed` → `title` `the merge-back check could not be completed`
  - **D-12 lock:** for the `likely-not-merged` verdict assert the row contains no `button` element —
    query within the row and assert `queryAllByRole('button')` is empty — proving no override,
    confirm, dismiss, or retry affordance was introduced

**B. `useReleaseDetail.test.tsx`.**

Add `fetchSourceBranchMRs: vi.fn()` and `compareRefs: vi.fn()` to the existing
`vi.mock('@/services/gitlab', …)` factory (the factory is exhaustive — omitting them breaks the
import). Add `describe('useReleaseDetail — merge-back queries (D-05 gating)')` with:
  - **unreleased fires zero extra calls:** render with `version.released === false` and a matched
    milestone, `waitFor` the existing branch query to settle, then assert
    `fetchSourceBranchMRs` and `compareRefs` were each called 0 times. This is D-05's
    "unreleased versions fire ZERO extra GitLab calls" contract.
  - **released fires the tracking-MR query:** with `released: true` and a matched milestone, assert
    `fetchSourceBranchMRs` was called with the derived release branch name.
  - **released + a resolved tag fires the compare query:** stub `searchProjectTags` to return the
    matching tag and `fetchProject` to return `default_branch: 'develop'`, then assert `compareRefs`
    was called with `'develop'` as `from` and the tag name as `to` (91-RESEARCH Pitfall 3 — the
    compare query depends on the tag query's RESOLVED result).
  - **tag lookup widened (D-01 / Pitfall 4):** with `released: true` and the branch-existence query
    still reporting the branch as EXISTING, assert `searchProjectTags` was still called — proving
    the `branchResult?.exists === false` clause was dropped from `needsTagLookup`.
  - **verdict is exposed:** assert the hook's result object has a `mergeBackVerdict` with a `kind`
    property.

Follow the file's existing `setupMocks`/`waitFor` conventions; add no new mocking library.
  </action>

  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx src/routes/dashboard/release-detail/useReleaseDetail.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` exits 0
    - A sidebar test asserts `screen.queryByText('Merged back')` is null for the `hidden` verdict
    - Sidebar tests assert the exact tooltip strings `via !4821, merged 21.07.2026`,
      `no diff between v33.7.0 and develop`, `v33.7.0 has 12 commits not in develop`, and
      `no tracking MR and no v33.7.0 tag found`
    - A sidebar test asserts the released-branch row's `title` does not match `/merged/i`
    - A sidebar test asserts zero `button` roles inside the `likely-not-merged` row (D-12 lock)
    - A hook test asserts `fetchSourceBranchMRs` and `compareRefs` were called 0 times for an
      unreleased version
    - A hook test asserts `searchProjectTags` is called for a released version whose branch exists
    - `grep -c "fetchSourceBranchMRs\|compareRefs" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` is at least 6
    - No `.only`, no bare `vitest` watch invocation, no new mocking dependency
  </acceptance_criteria>

  <done>All five verdict renderings, the hidden case, the D-08 wording change, the D-12 no-control lock, and D-05's zero-extra-calls gate have green automated coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitLab REST API → React Query cache → sidebar row | Branch names, tag names, MR ids and URLs, and diff/commit counts flow from an external API into rendered row text and native `title` tooltips |
| Hook query gating → GitLab REST API | `activeGitlabProject`, `gitlabToken`, `gitlabBaseUrl`, and the derived refs decide which project is queried |
| Query keys → cross-component cache | `['gitlab-mr-source-branch', …]` and `['gitlab-compare', …]` are a cross-component cache contract (P87 D-11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-10 | Tampering / unintended access | the two new `useQuery` calls in `useReleaseDetail.ts` | mitigate | WR-10 — the `?? 0` / `?? ''` argument fallbacks are paired with an `enabled` gate that independently requires `!!activeGitlabProject && !!gitlabToken && !!gitlabBaseUrl`, so a request can never be issued against project `0` or with an empty token |
| T-91-11 | Information Disclosure | the verdict row's text and native `title` tooltip | mitigate | The row renders only union fields (branch name, tag name, MR iid, formatted date, integer counts). It never renders `error.message`, a response body, a raw URL, or a token — a failed check collapses to the fixed literal `the merge-back check could not be completed` |
| T-91-12 | Information Disclosure | cross-component query-key collision | mitigate | Both new keys are new, project-scoped prefixes (`gitlab-mr-source-branch`, `gitlab-compare`) that collide with no existing key, so no other page's cached data can be mistakenly rendered into this row |
| T-91-13 | Tampering | absent state persistence | mitigate | D-12/D-15 — this plan adds no mutation, no `invalidateQueries`, no Zustand write, no Tauri store write, and no Jira/GitLab write. There is no client-controlled state that could be tampered with to force a favourable verdict; the verdict is recomputed from server data on every render |
| T-91-14 | Denial of Service | extra GitLab calls per page load | mitigate | D-05 gates both queries on `releasedVersion`, so the common unreleased case fires zero extra calls; `staleTime: 5 * 60_000` bounds refetch frequency. Asserted by the zero-calls hook test |
| T-91-15 | Repudiation | — | accept | Read-only advisory display; there is no action to attribute or audit. No log/audit requirement applies (ASVS L1) |
| T-91-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase (91-RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `cd taskflow && npm run test` — full suite green, zero regressions in the release-detail folder
- `cd taskflow && npm run check` — `tsc --noEmit` clean; biome flags NO NEW files beyond the
  documented pre-existing baseline drift (BacklogPage.tsx, BacklogRow.tsx, components/ui/chart.tsx,
  MyTasksPage.tsx, MyTasksPage.test.tsx). Do not hardcode a diagnostic count; do not fix unrelated
  pre-existing files.
- `grep -rn "was merged and deleted" taskflow/src` returns nothing (D-08 complete)
- `grep -rniE "override|acknowledge|dismiss|confirmed this myself" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx` returns nothing related to the merge-back row (D-12 lock)
</verification>

<success_criteria>
- MERGE-01: a released fix version's detail page shows a "Merged back" row naming the FETCHED
  default branch, in one of `merged` / `likely not merged` / `couldn't verify` / `Loading...`, and
  the row is absent entirely for an unreleased version or an unmatched milestone
- MERGE-02: the verdict prefers a merged tracking MR and falls back to the tag-vs-default-branch
  content comparison, wired through the Plan 01 fetchers and the Plan 02 resolver
- MERGE-03: DESCOPED by D-12 — no override, confirm, dismiss, or acknowledge control exists and
  nothing persists. Recorded as an intentional descope (same handling as DASH-06 / DRIFT-09);
  its absence is not a gap and must not be closed by inventing an affordance
- D-08: the released-branch row no longer contains the word "merged" anywhere
- Full suite green; typecheck clean; no NEW biome files flagged
</success_criteria>

<output>
Create `.planning/phases/91-post-release-merge-back-verification/91-03-SUMMARY.md` when done
</output>
