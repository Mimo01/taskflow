---
phase: 91-post-release-merge-back-verification
plan: 08
type: execute
wave: 2
depends_on: ["91-07"]
gap_closure: true
files_modified:
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx
  - taskflow/src/routes/dashboard/release-detail/mergeBackVerification.test.ts
  - taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx
autonomous: true
requirements: [MERGE-01, MERGE-02, MERGE-03]

must_haves:
  truths:
    - "On a released version whose tracking-MR query resolves before the tag query, the 'Merged back' row shows Loading — never a terminal 'no tracking MR and no vX.Y.Z tag found' claim — until the tag query settles"
    - "On a genuine tag-fetch failure the row reports couldnt-verify with reason 'check-failed', not 'no-mr-no-tag'"
    - "A tag query that never runs (no derivable version number) is not reported as pending and still terminates"
    - "MERGE-03 stays satisfied by recorded descope D-12 — no override, confirm, dismiss or acknowledge control and no persistence is introduced anywhere"
    - "The four files this phase newly flagged in `biome check` are clean again, with no previously-clean file newly flagged"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts"
      provides: "isError capture and the derived pending signal for the gitlab-release-tags query, threaded into resolveMergeBackVerdict"
      contains: "tagCheckFailed"
    - path: "taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx"
      provides: "Slow-resolving and rejecting searchProjectTags hook cases asserting loading vs check-failed"
  key_links:
    - from: "useReleaseDetail.ts gitlab-release-tags useQuery"
      to: "resolveMergeBackVerdict"
      via: "isError destructured as tagCheckFailed and a derived tagLookupPending, both passed as arguments"
      pattern: "tagLookupPending"
    - from: "searchProjectTags rejection (91-07)"
      to: "verdict reason 'check-failed'"
      via: "React Query isError on the tag query"
      pattern: "isError: tagCheckFailed"
---

<objective>
Wire the tag channel's in-flight and failure signals — created in plan 91-07 —
from live React Query state into `resolveMergeBackVerdict`, closing the hook
half of 91-VERIFICATION's truth-5 gap. Without this, 91-07's step-4.5 branch is
unreachable in the running app and the user still sees a false terminal
"Couldn't verify — no tracking MR and no vX.Y.Z tag found" claim during the
ordinary page-load window where the tracking-MR query settles first.

Also closes 91-REVIEW WR-02: `biome check` newly flags four files touched by the
previous gap-closure round (all four clean at `HEAD~8`). Per the project's
drifted-baseline convention the gate is "no NEWLY flagged files", never an
absolute diagnostic count — all four current diagnostics are formatter-only.

Deferred deliberately, not dropped: 91-REVIEW WR-04 (the five-branch nested
ternary at `ReleaseDetailSidebar.tsx:285-340` with duplicated date formatting
and `couldnt-verify` as an implicit `else`). It is a maintainability warning
with no user-visible defect, and refactoring the row's render while the D-12
no-control lock and the verdict copy are the phase's acceptance surface adds
risk with no goal-backward payoff. Record it as accepted technical debt in the
SUMMARY; do not silently drop it and do not fix it here.

Purpose: make the resolver's fourth-channel guard actually fire in the app.
Output: a row that stays honest for the whole page-load window and reports the
real reason on a tag-fetch failure, plus a clean biome surface.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-post-release-merge-back-verification/91-VERIFICATION.md
@.planning/phases/91-post-release-merge-back-verification/91-CONTEXT.md
@.planning/phases/91-post-release-merge-back-verification/91-07-SUMMARY.md
@taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts
@taskflow/src/routes/dashboard/release-detail/mergeBackVerification.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread the tag query's failure and in-flight signals into the resolver (useReleaseDetail.ts:188-298)</name>
  <files>taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts</files>
  <read_first>
    - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts L144-298 — the whole merge-back region: the `gitlab-project` query's `isError: defaultBranchCheckFailed` destructuring (L151-159) and the branch query's `isError: branchCheckFailed` (L163-179) as the in-file naming precedent; `needsTagLookup` (L188-198) and the comment clause at L197 asserting `searchProjectTags` returns `[]` on failure, which plan 91-07 made false; the `gitlab-release-tags` query (L200-211); `mergeBackTagName` (L216-219) and its second consumer `branchState` (L221-228); the tracking-MR query (L235-251); the compare query and its `mergeBackTagName !== null` enable gate (L253-275); the `trackingMRsUnavailable` derivation and its CR-03 comment (L277-284); and the `resolveMergeBackVerdict({...})` call (L286-298)
    - taskflow/src/routes/dashboard/release-detail/mergeBackVerification.ts AS MODIFIED BY PLAN 91-07 — the params object, to use the exact names `tagLookupPending` and `tagCheckFailed` and to confirm the step-4.5 semantics before wiring
    - .planning/phases/91-post-release-merge-back-verification/91-07-SUMMARY.md — confirm the two param names, their defaults, and that `searchProjectTags` now rejects
    - .planning/phases/91-post-release-merge-back-verification/91-CONTEXT.md — D-05 (an unreleased version fires ZERO extra GitLab calls), D-15 (this phase is read-only; project-granular cache keys, never a windowed key)
  </read_first>
  <action>
    Destructure `isError: tagCheckFailed` from the `gitlab-release-tags`
    `useQuery`, matching the `isError: defaultBranchCheckFailed` and
    `isError: branchCheckFailed` naming convention already used twice above.
    Leave that query's `queryKey`, `queryFn`, `enabled` expression and
    `staleTime` byte-identical — the `activeGitlabProject`-granular key and the
    5-minute staleTime are a cache contract (D-15 / P88 CR-02).

    Immediately below the query, derive
    `const tagLookupPending = needsTagLookup && releaseTags === undefined && !tagCheckFailed;`
    Do NOT use React Query's `isPending` or `isLoading` for this: `isPending` is
    also true for a disabled query, which would pin the row at Loading forever
    for the case where `matchedVersionNumber` is null and `needsTagLookup` is
    false — the exact CR-03 defect class this phase already fixed twice.
    Deriving from `needsTagLookup` makes "the query will never run" explicitly
    NOT pending. Comment it with that reasoning and cite 91-VERIFICATION truth 5.

    Pass `tagLookupPending` and `tagCheckFailed` into the existing
    `resolveMergeBackVerdict({...})` call alongside the other channel signals.
    Change nothing else in that call.

    Correct the now-false clause in the L190-198 comment block ("that redundant
    fetch is accepted because it is a single search-scoped call and
    `searchProjectTags` returns `[]` on failure rather than throwing"): state
    instead that `searchProjectTags` now rejects on failure (91-07) so the tag
    channel surfaces `isError` like the other three, and that the branch row is
    unaffected because it reads only the resolved `mergeBackTagName`, which
    stays `null` on failure exactly as it did when the service swallowed the
    error — a tag outage still does not escalate into a branch-row error.

    Add no new query, no `refetch` wiring, no `useMutation`, no
    `queryClient.setQueryData`, no Zustand/Tauri store write, and no
    override/confirm/dismiss/acknowledge state or control (D-12, D-15 — this
    phase is read-only and MERGE-03 is satisfied by absence).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isError: tagCheckFailed" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns exactly 1 match, inside the `gitlab-release-tags` query block
    - `grep -n "tagLookupPending" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns at least 2 matches (declaration and resolver argument)
    - The `tagLookupPending` expression contains `needsTagLookup` and does NOT contain `isPending` or `isLoading`
    - The `resolveMergeBackVerdict({...})` call passes all six channel signals: `defaultBranchCheckFailed`, `trackingMRsCheckFailed`, `trackingMRsUnavailable`, `tagLookupPending`, `tagCheckFailed`, `compareCheckFailed`
    - The `gitlab-release-tags` query's `queryKey`, `enabled` expression and `staleTime` are byte-identical to before the change
    - `grep -n "returns \`\[\]\` on failure" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 0 matches
    - `grep -c "useMutation\|setQueryData" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` is unchanged from before the edit (no new write path)
    - `grep -Ein "override|dismiss|acknowledge" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 0 matches
    - `cd taskflow && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` exits 0
    - `cd taskflow && npx tsc --noEmit` exits 0 with no output
  </acceptance_criteria>
  <done>Both tag-channel signals reach the resolver from live query state, and a never-running tag query is not misreported as pending.</done>
</task>

<task type="auto">
  <name>Task 2: Hook tests for a slow-resolving and a rejecting searchProjectTags (91-VERIFICATION missing item 3)</name>
  <files>taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx — the whole file: the `setupMocks` overrides object and its `fetchProjectImpl` / `fetchBranchImpl` / `fetchSourceBranchMRsImpl` / `compareRefsImpl` / `milestoneTitle` / `released` keys (~L90-155), the `searchProjectTags` default mock (~L148), the `makeMilestone` factory and its `'33.5.0 (21.07.2026)'` title format, the QueryClient wrapper and its retry configuration, and the four verdict cases added by plan 91-06 (`CR-04` ~L527, `CR-03` ~L545, `CR-01` ~L563, happy path ~L595) as the structural template
    - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts AS MODIFIED BY TASK 1 — to know that a rejecting `searchProjectTags` sets `tagCheckFailed` and an unsettled one sets `tagLookupPending`
    - taskflow/src/routes/dashboard/release-detail/mergeBackVerification.ts AS MODIFIED BY PLAN 91-07 — the `MergeBackVerdict` union and the `reason` values, so assertions target real discriminants
  </read_first>
  <action>
    Add a `searchProjectTagsImpl` override key to `setupMocks`, following the
    exact shape of the existing `fetchSourceBranchMRsImpl` / `compareRefsImpl`
    keys, defaulting to the current `() => Promise.resolve([])` so every
    existing case keeps its present behaviour with no edits.

    Ensure the QueryClient used by the new cases has `retry: false` so a
    rejection settles promptly (reuse the existing wrapper if it already does;
    do not weaken retry settings for existing cases).

    Add these cases, all on a released version (`released: true`) with a
    matched, parseable milestone title:

    - Pending-tag case (the reported failure sequence): `fetchSourceBranchMRs`
      resolves immediately to `[]` while `searchProjectTags` returns a promise
      that does not settle for the duration of the assertion (hold a
      deferred/never-resolving promise; resolve it in a `finally`/cleanup so the
      test does not leak). Assert `result.current.mergeBackVerdict.kind` is
      `'loading'`, and explicitly `.not.toBe('couldnt-verify')`. Then settle the
      tag promise with `[]` and `waitFor` the verdict to become
      `'couldnt-verify'` with `reason` `'no-mr-no-tag'` — proving the row only
      makes the terminal claim after the channel actually settles, which is the
      whole gap.
    - Rejecting-tag case: `searchProjectTags` rejects (e.g. with
      `new Error('Failed to load release tags: status 500')`, matching 91-07's
      message shape). `waitFor` the verdict and assert `kind` is
      `'couldnt-verify'`, `reason` is `'check-failed'`, and explicitly
      `reason` `.not.toBe('no-mr-no-tag')` and `kind` `.not.toBe('loading')`.
    - Merged-MR-outranks-tag-channel case: `fetchSourceBranchMRs` resolves to
      one MR with `state: 'merged'` and `target_branch: 'develop'` (matching the
      default `fetchProject` `default_branch: 'develop'`) while
      `searchProjectTags` rejects. Assert `kind` is `'merged'` with `via`
      `'tracking-mr'` — a definitive positive signal must not be downgraded by
      an unrelated channel failing (D-02).
    - Never-running-tag-query case: an unparseable milestone title (no derivable
      version number, so `needsTagLookup` is false and the tag query never
      fires). Assert `searchProjectTags` was never called AND the verdict
      settles to a non-`'loading'` kind — the CR-03 regression lock for the new
      `tagLookupPending` derivation.

    Every assertion must `waitFor` the verdict discriminant itself, never a
    mock-call count, so a case cannot pass while the verdict is still settling.
    Do not weaken or delete any existing case, especially the D-05 "an
    unreleased version fires zero extra GitLab calls" assertions and the four
    CR-01/CR-03/CR-04/happy-path cases from plan 91-06.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "searchProjectTagsImpl" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` returns at least 3 matches (override type, default wiring, and at least one case)
    - A case exists in which the tag promise is unsettled and asserts `mergeBackVerdict.kind` is `'loading'` and `.not.toBe('couldnt-verify')`, then asserts the post-settle verdict is `'couldnt-verify'` / `'no-mr-no-tag'`
    - A case exists in which `searchProjectTags` rejects and asserts `reason` is `'check-failed'` and `.not.toBe('no-mr-no-tag')`
    - A case exists in which `searchProjectTags` rejects but a merged `develop`-targeting MR is present, asserting `kind` `'merged'` and `via` `'tracking-mr'`
    - A case exists with an unparseable milestone title asserting `searchProjectTags` was not called and the verdict `kind` is not `'loading'`
    - No new case asserts on a mock-call count in place of the verdict discriminant
    - Every pre-existing case in the file still passes with no edits to its assertions
    - The full targeted suite test count is strictly greater than the 224 recorded in 91-VERIFICATION
    - `cd taskflow && npx vitest run src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` exits 0
    - `cd taskflow && npx tsc --noEmit` exits 0 with no output
  </acceptance_criteria>
  <done>The suite fails against the reported bug state — a terminal "no tag found" claim rendered while the tag query is still in flight, and a `no-mr-no-tag` reason on a tag-fetch failure.</done>
</task>

<task type="auto">
  <name>Task 3: Restore the four newly-flagged files to a clean biome check (WR-02)</name>
  <files>taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts, taskflow/src/routes/dashboard/release-detail/useReleaseDetail.test.tsx, taskflow/src/routes/dashboard/release-detail/mergeBackVerification.test.ts, taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx</files>
  <read_first>
    - .planning/phases/91-post-release-merge-back-verification/91-REVIEW.md — WR-02, confirming these four files were clean at `HEAD~8` and are newly flagged by this phase
    - .planning/phases/91-post-release-merge-back-verification/91-VERIFICATION.md — the `biome check` row in Behavioral Spot-Checks (4 formatter errors, all four files newly flagged, cosmetic not functional)
    - The current `npx biome check` output for these four files, to confirm every remaining diagnostic is formatter-only before applying a write
  </read_first>
  <action>
    Run `npx biome check` on the four files plus every file this phase's plans
    91-07 and 91-08 touched (`src/services/gitlab.ts`,
    `src/services/gitlab.test.ts`, `mergeBackVerification.ts`). Confirm from the
    output that all remaining diagnostics are `format` category before doing
    anything.

    Apply `npx biome check --write` to exactly those files. If any diagnostic is
    a lint rule rather than a formatter diff, do NOT auto-fix it blindly — read
    the rule, fix it by hand in the smallest way that preserves behaviour, and
    record the rule name in the SUMMARY.

    Do not reformat any file this phase did not touch — the repository's biome
    baseline has drifted (~16 diagnostics across 5 unrelated files), and the
    project convention gates on "no NEWLY flagged files", never on an absolute
    diagnostic count. Widening the blast radius here would make the gate
    unreadable.

    Re-run the full targeted suite afterwards: formatting a test file must not
    change any assertion.
  </action>
  <verify>
    <automated>cd taskflow && npx biome check src/services/gitlab.ts src/services/gitlab.test.ts src/routes/dashboard/release-detail/useReleaseDetail.ts src/routes/dashboard/release-detail/useReleaseDetail.test.tsx src/routes/dashboard/release-detail/mergeBackVerification.ts src/routes/dashboard/release-detail/mergeBackVerification.test.ts src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx && npx vitest run src/services/gitlab.test.ts src/routes/dashboard/release-detail/mergeBackVerification.test.ts src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx src/routes/dashboard/release-detail/useReleaseDetail.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npx biome check` over all seven phase-touched files exits 0 with zero diagnostics
    - `git diff --stat` for this task shows changes confined to the phase-touched files — no unrelated file is reformatted
    - No assertion text, `it(` title, or expected value changed as a result of formatting (verify by reading the diff, not by trusting the tool)
    - The full targeted suite still exits 0 with the same test count as after Task 2
    - `cd taskflow && npx tsc --noEmit` exits 0 with no output
  </acceptance_criteria>
  <done>The phase newly flags zero files in `biome check`, and the pre-existing unrelated baseline drift is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitLab API failure → React Query `isError` → verdict reason text | The only path by which an API-side condition becomes user-facing copy |
| Hook → presentational `ReleaseDetailSidebar` | Only plain verdict data crosses; no handler, no mutation, no store write (D-15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-08-01 | Repudiation | "Merged back" row copy | mitigate | Task 1 routes a tag-fetch failure to `reason: 'check-failed'` so the row stops asserting "no tag found" — a claim it cannot support — and Task 2 locks that with a rejecting-mock test |
| T-91-08-02 | Denial of Service (of information) | `tagLookupPending` derivation | mitigate | Derived from `needsTagLookup` rather than React Query `isPending`, so a permanently disabled tag query terminates instead of pinning the row at Loading forever (the CR-03 defect class); Task 2's never-running-query case is the regression lock |
| T-91-08-03 | Information Disclosure | verdict reason surface | mitigate | The resolver emits only the two enumerated reason values; no API error message, status code, URL or token reaches the UI — Task 1 adds no error-string plumbing |
| T-91-08-04 | Elevation of Privilege | advisory verdict row | mitigate | Acceptance criteria assert zero `override|dismiss|acknowledge` tokens in the hook and no new mutation or cache-write path; MERGE-03 stays satisfied by the recorded D-12 descope, locked by the existing `meta-row-merged-back` no-control tests from plan 91-06 which must keep passing |
| T-91-08-05 | Tampering | `biome check --write` over test files | mitigate | Task 3 requires reading the diff to confirm no assertion text changed, and re-running the full suite after formatting |
| T-91-08-SC | Tampering | npm/pip/cargo installs | mitigate | No dependency is added or upgraded by this plan; nothing to audit |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/services/gitlab.test.ts src/routes/dashboard/release-detail/mergeBackVerification.test.ts src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx src/routes/dashboard/release-detail/useReleaseDetail.test.tsx` — 4 files green, test count strictly greater than 224
- `cd taskflow && npx tsc --noEmit` — exit 0, no output
- `cd taskflow && npx biome check` over the seven phase-touched files — exit 0
- `grep -n "tagLookupPending\|isError: tagCheckFailed" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` — both present
- `grep -Ein "override|dismiss|acknowledge" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` — 0 matches outside test-lock assertions (MERGE-03 / D-12)
</verification>

<success_criteria>
- A released version whose tracking-MR query settles first shows `loading`, not a terminal `couldnt-verify`, until the tag query settles
- A rejecting `searchProjectTags` produces `couldnt-verify` / `check-failed` through the real hook, never `no-mr-no-tag` and never a permanent spinner
- A merged default-branch tracking MR still resolves to `merged` even when the tag channel fails
- A tag query that never runs still terminates
- MERGE-03 remains satisfied via recorded descope D-12 — no override control and no persistence introduced; the existing no-control locks still pass
- WR-04 is explicitly recorded as deferred technical debt in the SUMMARY, with the reason
- Zero files newly flagged by `biome check`; full targeted suite and typecheck green
</success_criteria>

<output>
Create `.planning/phases/91-post-release-merge-back-verification/91-08-SUMMARY.md` when done
</output>
