---
phase: 90-per-mr-corrective-actions
plan: 03
type: execute
wave: 3
depends_on: [90-02]
files_modified:
  - taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx
  - taskflow/src/routes/dashboard/release-detail/MrDriftSection.test.tsx
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
autonomous: true
requirements: [MRFIX-01, MRFIX-02, MRFIX-03, MRFIX-04]

must_haves:
  truths:
    - "Row order is captured on first non-empty render and held for the life of the mounted list, so a cell that turns `✓` stays in place instead of jumping to the clean partition (D-11)"
    - "Hovering anywhere on a row swaps every actionable flagged cell on that row from `⚠` to its action icon (`GitBranch` in BR, `Milestone` in MS); the cell under the pointer additionally gets a neutral background/ring (D-01, D-02, D-03)"
    - "Each action is a real focus-reachable `<button>` inside the unchanged 28px cell, with an `aria-label` mirroring its `title` verbatim, and no row width is added (D-01, D-04)"
    - "Only flagged cells are actionable — `ok`, `na`, non-evaluated rows and the TASK column have no click handler and no hover swap (D-05, P89 D-11)"
    - "A firing cell shows a `Loader2` spinner until the PUT settles and only then resolves to `✓` — the glyph is pessimistic even though the cache write is optimistic (D-06)"
    - "A failed action renders `⚠` in red with the flattened GitLab message in its tooltip, stays that way through background refetches, renders nothing outside the row, and retries on click (D-07, D-08, D-10)"
    - "BR and MS on one row lock independently: one can spin while the other is clicked (D-09)"
    - "When the release branch does not exist the BR cell stays orange `⚠` and inert, with a tooltip pointing at the create-branch control above (D-14, MRFIX-04)"
    - "With no matched milestone the degraded banner and `—` cells are unchanged and carry no actions (D-15)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx"
      provides: "applyHeldOrder + DriftActionCell + the MrFixContext prop"
      contains: "function DriftActionCell"
    - path: "taskflow/src/routes/dashboard/release-detail/MrDriftSection.test.tsx"
      provides: "pending / success / sticky failure / independent / unavailable / held sort order coverage"
      contains: "held sort order"
    - path: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      provides: "the fix context wired from useReleaseDetail into MrDriftSection"
      contains: "releaseBranchExists"
  key_links:
    - from: "DriftActionCell"
      to: "useMrFixMutation"
      via: "one hook instance per (MR, action) cell"
      pattern: "useMrFixMutation\\("
    - from: "MrDriftSection row div"
      to: "the action icon reveal"
      via: "Tailwind named group on the row"
      pattern: "group/row"
    - from: "ReleaseDetailPage"
      to: "MrDriftSection fix prop"
      via: "branchState.kind === 'exists' + matchedMilestone + gitlab config"
      pattern: "branchState.kind === 'exists'"
---

<objective>
Make the BR and MS drift cells conditionally interactive, inside the existing 28px footprint.

Everything visual here is already locked by `90-UI-SPEC.md` and D-01…D-15 — this plan implements that contract, it does not re-decide it. Two constraints deserve highlighting because they read against instinct:

- **D-06:** the spinner is deliberate. The cache write is optimistic, but the glyph must NOT claim `✓` until the PUT returns. Do not "fix" this to an instant checkmark.
- **D-11:** the flagged-first sort freezes on mount. Wrong-branch and missing-milestone co-occur constantly, so a live re-sort would move the row out from under the pointer between the user's two fixes. This must be a `useRef` snapshot — React Compiler is on and a `useMemo` is not a stability guarantee.

Purpose: this is the surface all four requirements are judged on; the mutation layer from Plan 02 is invisible without it.
Output: `applyHeldOrder` + `DriftActionCell` in `MrDriftSection.tsx`, the new `fix` prop wired from `ReleaseDetailPage.tsx`, and the interaction test suite VALIDATION.md requires.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-per-mr-corrective-actions/90-CONTEXT.md
@.planning/phases/90-per-mr-corrective-actions/90-UI-SPEC.md
@.planning/phases/90-per-mr-corrective-actions/90-PATTERNS.md
@.planning/phases/90-per-mr-corrective-actions/90-VALIDATION.md
@.planning/phases/90-per-mr-corrective-actions/90-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the row order for the life of the mounted list (D-11)</name>
  <files>taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx, taskflow/src/routes/dashboard/release-detail/MrDriftSection.test.tsx</files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx` (whole file — current props, the `rows.map` at L151, and `matchTicketKeyInTitle` as the precedent for exporting a pure helper from this component file for testing)
    - `taskflow/src/routes/dashboard/release-detail/MrDriftSection.test.tsx` L15-77 (the `makeMR`/`makeRow`/`renderSection` fixture builders to reuse, and the existing `renders rows in the given order without re-sorting` test that must stay green)
    - `taskflow/src/routes/dashboard/release-detail/driftDetection.ts` L270-280 (the flagged-first, iid-descending comparator this freeze is layered on top of — `buildDriftRows` keeps re-sorting on every render; the freeze lives in the view)
    - `taskflow/src/components/app/PinnedTabStrip.tsx` L65-125 (`didDragRef` — the codebase idiom for a component-local ref that gates behaviour across renders without a memo)
    - `.planning/phases/90-per-mr-corrective-actions/90-RESEARCH.md` Pitfall 3 (why this is a `useRef` snapshot and never a `useMemo`)
  </read_first>

  <behavior>
    `applyHeldOrder(rows: DriftRow[], heldIds: number[]): DriftRow[]`
    - `heldIds = [1, 2, 3]`, rows arriving as ids `[3, 1, 2]` -> returned order is ids `[1, 2, 3]`
    - a row whose id is NOT in `heldIds` is appended after all held rows, preserving the incoming relative order of such rows
    - a held id with no matching row is skipped (no hole, no throw)
    - an empty `heldIds` returns the incoming rows unchanged (same order, same length)
    - never mutates the input array

    `MrDriftSection` (D-11 integration)
    - first non-empty render captures the incoming order; a re-render with the SAME rows re-sorted (e.g. a fixed row now flagged-first-demoted) renders them in the originally captured DOM order
    - a re-render that adds a brand-new row still shows the original rows first, with the new one last
    - a first render with `rows: []` (loading) does not freeze an empty order — the first real row list is what gets captured
  </behavior>

  <action>
    In `MrDriftSection.tsx`, add `export function applyHeldOrder(rows: DriftRow[], heldIds: number[]): DriftRow[]` implementing the behavior above (index map from `heldIds`, stable partition of held vs unknown ids, no mutation). Export it for direct unit testing, mirroring how `matchTicketKeyInTitle` is already exported from this file.

    Inside the component add the freeze exactly as RESEARCH Pitfall 3 specifies: `const orderRef = useRef<number[] | null>(null);` then, before rendering the list, `if (orderRef.current === null && rows.length > 0) orderRef.current = rows.map((r) => r.mr.id);` and map over `applyHeldOrder(rows, orderRef.current ?? [])` instead of `rows`. Do not wrap any of this in `useMemo`/`useCallback` — React Compiler is on and a memo is not a freeze guarantee. Add a comment naming D-11 and the reason (wrong-branch and missing-milestone co-occur, so a live re-sort moves the row out from under the pointer between the user's two fixes) plus the explicit note that re-sorting resumes on the next mount/navigation.

    In `MrDriftSection.test.tsx`, add a `describe` whose name contains the literal `held sort order` covering the integration bullets (use `rerender` from the `render` result with a re-sorted array) plus direct unit tests of `applyHeldOrder`. Leave every existing test untouched and green.
  </action>

  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/routes/dashboard/release-detail/MrDriftSection.test.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "export function applyHeldOrder" MrDriftSection.tsx` returns 1
    - `grep -c "useRef" MrDriftSection.tsx` returns >= 1 and `grep -cE "useMemo|useCallback|React\.memo" MrDriftSection.tsx` returns 0
    - `npx vitest run src/routes/dashboard/release-detail/MrDriftSection.test.tsx -t "held sort order"` passes
    - The integration test asserts DOM order via `screen.getAllByTestId('drift-row')` text content after a `rerender` with a deliberately re-sorted array — not just the helper's return value
    - The existing `renders rows in the given order without re-sorting` test still passes unmodified
    - All 200 lines' worth of pre-existing tests in the file still pass (`npx vitest run …MrDriftSection.test.tsx` fully green)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>Row order is captured once per mount and a fixed row turns `✓` in place instead of jumping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build DriftActionCell and wire the fix context from the page</name>
  <files>taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx, taskflow/src/routes/dashboard/ReleaseDetailPage.tsx, taskflow/src/routes/dashboard/release-detail/MrDriftSection.test.tsx</files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx` L54-78 (`DriftMarkCell` — the glyph/testid/28px contract to preserve exactly) and L162-250 (the row div and the three cell call sites)
    - `taskflow/src/routes/dashboard/release-detail/useMrFixMutation.ts` (Plan 02 — the hook signature, `MrFixAction`, and the `{ status, errorMessage, fire }` return)
    - `taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` L57-90 (the `useReleaseDetail` destructure) and L250-257 (the current `<MrDriftSection …>` call site)
    - `taskflow/src/routes/dashboard/release-detail/releaseBranch.ts` L90-105 (the `branchState.kind` union — `'exists'` is the ONLY kind that means the branch is there; `'released'`, `'missing'`, `'check-failed'`, `'loading'`, `'invalid-ref'`, `'unresolvable'`, `'blocked-no-milestone'` all mean retarget is unavailable)
    - `.planning/phases/90-per-mr-corrective-actions/90-UI-SPEC.md` § "Interaction Contract" and § "Copywriting Contract" (the locked per-state visuals and the exact tooltip strings)
    - `taskflow/src/routes/standup-notes/WatchedPersonPicker.tsx` L85-95 (existing named-group hover-reveal idiom in this codebase: `group-hover/watched:opacity-60`)
  </read_first>

  <behavior>
    - A flagged BR cell with a resolved, existing release branch renders a `<button>` with `aria-label` and `title` both equal to `Retarget to release/33.5.0`, containing an `AlertTriangle` (rest) and a `GitBranch` (revealed) icon
    - A flagged MS cell with a matched milestone renders a `<button>` with `aria-label`/`title` `Assign milestone 33.5.0 (21.07.2026)`, containing `AlertTriangle` + `Milestone` icons
    - Clicking the BR button calls `useMrFixMutation`'s `fire` for `action: 'retarget'`; clicking MS fires `action: 'assign-milestone'`
    - While `status === 'pending'` the cell renders a spinner with `animate-spin` and further clicks do not re-fire (D-06, D-09)
    - When the mutation settles and the cache-derived `mark` becomes `ok`, the cell renders the green `Check` and is no longer a button (D-05, D-06)
    - When `status === 'error'` the cell renders `AlertTriangle` with a red class (`text-red-600`/`dark:text-red-400`), `title` equal to the hook's `errorMessage`, remains clickable, and renders nothing outside the cell (D-07)
    - BR cell with `mark === 'flag'` and `releaseBranchExists === false` renders a NON-button `span`, orange `AlertTriangle`, `title` `Release branch doesn't exist yet — create it above to enable retargeting` (D-14, MRFIX-04)
    - `mark === 'ok'`, `mark === 'na'`, non-evaluated rows, and the TASK cell never render a button and never swap glyphs (D-05, P89 D-11)
    - A cell whose gitlab config is incomplete (`projectId`/`baseUrl`/`token` null) renders as inert
    - The cell root keeps `data-testid` (`drift-br`/`drift-ms`/`drift-task`) and the classes `flex-none w-[28px]` in every state, button or not (P89 D-20 — narrow cells collapse to 0 in this WebKit/Tauri webview)
  </behavior>

  <action>
    In `MrDriftSection.tsx`:

    1. Add an exported `MrFixContext` interface with fields `projectId: number | null`, `baseUrl: string | null`, `token: string | null`, `releaseBranchName: string | null`, `releaseBranchExists: boolean`, `matchedMilestone: { id: number; title: string } | null`, and add a required `fix: MrFixContext` prop to `MrDriftSectionProps`. The section stays presentational and props-driven (P87 D-08): it fetches nothing and reads no store — every write input arrives through this one prop.

    2. Add `function DriftActionCell({ mr, action, mark, testId, fix }: …)` rendering the SAME outer geometry as `DriftMarkCell` (`data-testid={testId}` and `className="flex-none w-[28px] flex items-center justify-center"` on the root, whether the root is a `<span>` or a `<button type="button">`). It calls `useMrFixMutation` unconditionally (Rules of Hooks) with `action`, `mr`, the `fix` fields, `targetBranch: fix.releaseBranchName`, `milestone: fix.matchedMilestone`.

       Compute `actionable` as: `mark === 'flag'` AND `fix.projectId != null && fix.baseUrl != null && fix.token != null` AND (`action === 'retarget' ? fix.releaseBranchExists && fix.releaseBranchName != null : fix.matchedMilestone != null`).

       Render precedence, in this order: `status === 'pending'` -> `Loader2` with `size-3.5 animate-spin text-muted-foreground` and no click; `status === 'error'` -> `AlertTriangle` with `size-3.5 text-red-600 dark:text-red-400`, `title`/`aria-label` = `errorMessage ?? 'Update failed — click to retry'`, still a button that calls `fire` (retry, D-07); `mark === 'ok'` -> green `Check` in a plain span; `mark === 'na'` -> the muted em dash in a plain span; `mark === 'flag' && actionable` -> the reveal button described below; `mark === 'flag' && !actionable` -> plain span with orange `AlertTriangle` and, for the BR action with a missing branch, `title` `Release branch doesn't exist yet — create it above to enable retargeting` (D-14).

       Tooltip/label strings, verbatim from the UI-SPEC copy contract: BR -> `Retarget to ${fix.releaseBranchName}` (`releaseBranchName` already carries the `release/` prefix, so this reads `Retarget to release/33.5.0`); MS -> `Assign milestone ${fix.matchedMilestone.title}`. Set BOTH `title` and `aria-label` to the same string — `title` alone is not a reliable accessible name for an icon-only control (UI-SPEC checker recommendation D2).

       The reveal button (D-01/D-02/D-03/D-04): `type="button"`, `className` includes the 28px geometry plus `group/fix rounded hover:bg-accent hover:ring-1 hover:ring-border focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring`. Inside it, two icons in the same 28px box so nothing shifts: the rest `AlertTriangle` (`size-3.5 text-orange-600 dark:text-orange-400 group-hover/row:hidden group-focus-visible/fix:hidden`) and the action icon — `GitBranch` for retarget, `Milestone` for assign-milestone (`size-3.5 hidden group-hover/row:block group-focus-visible/fix:block`). Import both from `lucide-react` alongside the existing `AlertTriangle, Check, Loader2`. Row hover reveals every actionable cell at once; the pointer-specific cell gets the background/ring from its own `hover:` classes. If the `group-focus-visible/fix:` variant does not emit CSS under this project's Tailwind v4 setup, implement the focus half with local `onFocus`/`onBlur` state toggling a `data-revealed="true"` attribute on the button and key the icons off `data-[revealed=true]:` — do not silently drop the D-04 focus reveal.

    3. Add `group/row` to the row `<div>` at L163-167 (keep every existing class). Replace the two evaluated-row call sites `<DriftMarkCell mark={row.br} testId="drift-br" />` and `<DriftMarkCell mark={row.ms} testId="drift-ms" />` with `<DriftActionCell mr={mr} action="retarget" mark={row.br} testId="drift-br" fix={fix} />` and the `assign-milestone` equivalent. Leave the TASK cell and both non-evaluated branches on `DriftMarkCell` untouched — the TASK column is never actionable (P89 D-11) and the degraded `—` cells carry no actions (D-15).

    In `ReleaseDetailPage.tsx`: pass `fix={{ projectId: activeGitlabProject ?? null, baseUrl: gitlabBaseUrl ?? null, token: gitlabToken, releaseBranchName, releaseBranchExists: branchState.kind === 'exists', matchedMilestone: matchedMilestone ? { id: matchedMilestone.id, title: matchedMilestone.title } : null }}` to `<MrDriftSection>`, adding `activeGitlabProject`, `gitlabBaseUrl`, `gitlabToken` to the existing `useReleaseDetail` destructure if they are not already there. Narrow `matchedMilestone` to `{ id, title }` explicitly rather than passing the full `GitLabMilestone` — the MR-embedded milestone is the narrow shape and the optimistic patch must match it exactly (driftDetection Pitfall 1).

    In `MrDriftSection.test.tsx`: `renderSection` must now wrap in a `QueryClientProvider` (borrow the `makeWrapper` shape from `useReleaseDetail.test.tsx` L69-73), supply a default `fix` (project 42, base `https://gitlab.example.com`, token `t`, `releaseBranchName: 'release/33.5.0'`, `releaseBranchExists: true`, `matchedMilestone: { id: 1, title: '33.5.0 (21.07.2026)' }`), and `vi.mock('@/services/gitlab', …)` with `updateMergeRequest: vi.fn()`. All pre-existing tests must pass with only that harness change — no assertion edits.
  </action>

  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/routes/dashboard/release-detail/MrDriftSection.test.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "function DriftActionCell" MrDriftSection.tsx` returns 1 and `grep -c "useMrFixMutation" MrDriftSection.tsx` returns >= 1
    - `grep -c "group/row" MrDriftSection.tsx` returns >= 1 (row-level reveal) and the action icon classes contain both `group-hover/row:` and a focus reveal (`group-focus-visible/fix:` or the `data-[revealed=true]:` fallback)
    - `grep -c 'w-\[28px\]' MrDriftSection.tsx` is >= its pre-task count — no cell lost its explicit px width when it became a button
    - `grep -cE "GitBranch|Milestone" MrDriftSection.tsx` >= 2 (both action icons imported and used)
    - The BR button's `aria-label` and `title` are the identical string; same for MS (asserted by a test comparing the two attributes)
    - `grep -c "text-red-600" MrDriftSection.tsx` >= 1 (D-07 failure colour) and no `--destructive` CSS variable is introduced into this file
    - `grep -c "Release branch doesn't exist yet" MrDriftSection.tsx` returns 1 (D-14 copy, verbatim)
    - No toast, no alert, no error element rendered outside the cell: `grep -cE "toast|Alert\b|role=\"alert\"" MrDriftSection.tsx` returns 0
    - The TASK cell call site still uses `DriftMarkCell`, not `DriftActionCell`
    - `ReleaseDetailPage.tsx` contains `branchState.kind === 'exists'` and passes a narrowed `{ id, title }` milestone
    - `npx vitest run src/routes/dashboard/release-detail/MrDriftSection.test.tsx` fully green (all pre-existing tests plus Task 1's)
    - `npx tsc --noEmit` exits 0 and `npx biome check ./src` flags only the `BacklogPage.tsx`/`BacklogRow.tsx` baseline
  </acceptance_criteria>

  <done>The two flagged cells are focus-reachable hover-revealed buttons inside the unchanged 28px footprint, wired to the per-cell mutation hook, with the D-14 inert state and no page-level error surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Interaction test suite — pending, success, sticky failure, independence, unavailable</name>
  <files>taskflow/src/routes/dashboard/release-detail/MrDriftSection.test.tsx</files>

  <read_first>
    - `taskflow/src/routes/dashboard/release-detail/MrDriftSection.test.tsx` (current state after Tasks 1-2 — the `renderSection` harness, `QueryClientProvider` wrapper, `makeMR`/`makeRow` builders, and the `getByTestId('drift-br').querySelector('svg')` / `toHaveAttribute('title', …)` assertion idiom to keep using)
    - `taskflow/src/routes/dashboard/release-detail/MrDriftSection.tsx` (the states to assert, after Task 2)
    - `taskflow/src/routes/dashboard/release-detail/useMrFixMutation.ts` (what the component consumes — mock `@/services/gitlab`'s `updateMergeRequest`, not the hook, so the real cache/status logic is exercised)
    - `.planning/phases/90-per-mr-corrective-actions/90-VALIDATION.md` § "Per-Task Verification Map" (the required `-t` filter strings: `pending`, `independent`, `sticky failure`, `unavailable`)
    - `.planning/phases/90-per-mr-corrective-actions/90-UI-SPEC.md` § "Interaction Contract" (the per-state expectations these tests encode)
  </read_first>

  <behavior>
    - `-t "pending"`: clicking a flagged BR cell whose `updateMergeRequest` never resolves leaves a spinner (`.animate-spin`) in the cell; a second click does not increase `updateMergeRequest`'s call count (D-06, D-09)
    - success: with `updateMergeRequest` resolving and the row re-rendered with `br: 'ok'` (the cache-derived mark), the cell renders the green check and is no longer a `button` (D-06)
    - `-t "sticky failure"`: with `updateMergeRequest` rejecting `new Error("target_branch can't be blank")`, the cell ends up with that exact `title`, a `text-red-600` class, and stays that way after `queryClient.invalidateQueries()` plus a settled refetch; the string `[object Object]` appears nowhere in the DOM; nothing is rendered outside the row (`drift-row` remains the only container, no `role="alert"`) (D-07, D-08, D-10)
    - retry: clicking the red cell calls `updateMergeRequest` a second time and the red state clears while pending (D-07)
    - `-t "independent"`: on one row with both `br: 'flag'` and `ms: 'flag'`, clicking BR (never-resolving) then MS issues two `updateMergeRequest` calls with disjoint bodies (`{ target_branch }` and `{ milestone_id }`), and failing only MS leaves BR spinning and BR's cell not red (D-09, MRFIX-03)
    - `-t "unavailable"`: with `fix.releaseBranchExists: false` and `br: 'flag'`, `drift-br` is not a `button`, clicking it calls nothing, and its `title` is `Release branch doesn't exist yet — create it above to enable retargeting`; the MS cell on the same row is still actionable (MRFIX-04, D-14)
    - inert cells: `ok`, `na`, non-evaluated rows and `drift-task` (even when `task: 'flag'`) contain no `button` element (D-05, P89 D-11)
    - degraded: with `hasMatchedMilestone: false` the banner renders and neither BR nor MS is a button (D-15)
  </behavior>

  <action>
    Extend `MrDriftSection.test.tsx` with a `describe('per-MR corrective actions', …)` block covering every bullet above. Mock `@/services/gitlab`'s `updateMergeRequest` (a `vi.fn()` you control per test with `mockResolvedValue` / `mockRejectedValue` / a never-resolving `new Promise(() => {})`) rather than mocking `useMrFixMutation` — the point is to exercise the real status machine and cache patching end to end from a click.

    Use `fireEvent.click` / `userEvent` consistent with whatever the file already uses, `await waitFor` for status transitions, and assert on `screen.getByTestId('drift-br')` / `('drift-ms')` exactly as the existing tests do. For the "nothing outside the row" assertion, check that the rendered container has no `role="alert"` element and that the error text appears only inside the `drift-row` element. For the sticky-failure refetch simulation, call `queryClient.invalidateQueries()` on the test's own client inside `act` and `await waitFor` before re-asserting the red state.

    Test names must contain the literal substrings `pending`, `sticky failure`, `independent`, and `unavailable` so VALIDATION.md's `-t` filters resolve. Do not modify or weaken any pre-existing assertion in the file.
  </action>

  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/routes/dashboard/release-detail/MrDriftSection.test.tsx &amp;&amp; npx vitest run src/routes/dashboard/release-detail/</automated>
  </verify>

  <acceptance_criteria>
    - Each of these passes and matches at least one test: `-t "pending"`, `-t "sticky failure"`, `-t "independent"`, `-t "unavailable"`, `-t "held sort order"`
    - The independence test asserts the two recorded `updateMergeRequest` bodies are disjoint — one carries only `target_branch`, the other only `milestone_id`
    - The sticky-failure test asserts the tooltip text equals the thrown message verbatim AND that `document.body.textContent` does not contain `[object Object]`
    - The unavailable test asserts `drift-br` `tagName !== 'BUTTON'` and that `updateMergeRequest` was not called after a click
    - An inert-cells test asserts `screen.getByTestId('drift-task').querySelector('button')` is null even when `task: 'flag'`
    - `npx vitest run src/routes/dashboard/release-detail/` is fully green (every file in the directory)
    - No pre-existing assertion in `MrDriftSection.test.tsx` was changed or deleted
  </acceptance_criteria>

  <done>Every locked interaction state (pending, success, sticky failure, retry, independence, unavailable, inert, degraded) is covered by an automated test using the real mutation machinery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Click -> write intent | The UI decides only WHICH of two pre-resolved values is written; it never composes a target |
| GitLab error text -> DOM | Server-controlled string rendered as a `title`/`aria-label` on a cell |
| Missing prerequisite -> reachable control | A branch that does not exist must not be a clickable target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-11 | Tampering | `DriftActionCell` write scope | mitigate | The cell passes no branch/milestone of its own: `targetBranch` is `fix.releaseBranchName` and `milestone` is `fix.matchedMilestone`, both resolved upstream by `useReleaseDetail`. There is no text input, no dropdown, and no "change it anyway" affordance on `ok`/`na` cells (D-05), so no arbitrary target is reachable from the UI. |
| T-90-12 | Tampering | unavailable-state write | mitigate | `actionable` requires `fix.releaseBranchExists && fix.releaseBranchName != null` for retarget, and the non-actionable branch renders a `<span>` with no handler rather than a disabled button — the write path is structurally absent, not merely blocked (D-14/MRFIX-04, asserted by the `unavailable` test). |
| T-90-13 | Information Disclosure | error tooltip | mitigate | The tooltip renders only the hook's `errorMessage`, which originates from Plan 01's `flattenGitLabError`-composed message (no token, header, or URL). The `sticky failure` test additionally asserts no `[object Object]` leaks, and nothing is rendered outside the row (no toast/summary surface that could persist a message elsewhere). |
| T-90-14 | Denial of Service | double-click / retry storm | mitigate | The pending branch renders a spinner with no click handler and `fire()` self-locks (Plan 02), asserted by the `pending` test's call-count check. Retries are strictly user-initiated; no timer exists. |
| T-90-15 | Spoofing | icon-only control with no accessible name | mitigate | Both `title` and `aria-label` are set to the same explicit string naming the exact change (UI-SPEC D2), so the control cannot be actuated without its meaning being announced. |
| T-90-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies — `lucide-react` and `@tanstack/react-query` are pre-existing at pinned versions; UI-SPEC § Registry Safety confirms zero new shadcn pulls. Any install attempt must stop the plan. |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/release-detail/` — all release-detail tests green
- `cd taskflow && npx tsc --noEmit` — exits 0 (proves the new required `fix` prop is wired at the single call site)
- `cd taskflow && npx biome check ./src` — diagnostics confined to the `BacklogPage.tsx`/`BacklogRow.tsx` baseline
- `grep -c 'w-\[28px\]'` on `MrDriftSection.tsx` shows no cell lost its explicit width (P89 D-20 regression guard)
</verification>

<success_criteria>
- All four requirements are observable in tests: retarget (MRFIX-01), assign-milestone (MRFIX-02), independent per-cell status and retry (MRFIX-03), unavailable retarget without a branch (MRFIX-04)
- Row width is unchanged and the frozen sort holds through a success transition
- No error surface exists outside the cell
</success_criteria>

<output>
Create `.planning/phases/90-per-mr-corrective-actions/90-03-SUMMARY.md` when done. Record which focus-reveal mechanism was used (`group-focus-visible/fix:` vs the `data-revealed` fallback) — Plan 04's UAT checks it by keyboard.

Also record this planned deviation under a `## Deviations` heading (D-11 requires deviations to be recorded): the held sort order (`orderRef` / `applyHeldOrder`) lives in `MrDriftSection.tsx`, not in `useReleaseDetail.ts` where 90-PATTERNS.md grouped the pattern. Rationale: "life of the mounted list" is the list component itself, and 90-VALIDATION.md places the `held sort order` test in `MrDriftSection.test.tsx`.
</output>
