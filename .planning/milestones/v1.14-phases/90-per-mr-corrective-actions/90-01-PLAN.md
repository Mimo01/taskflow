---
phase: 90-per-mr-corrective-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/90-per-mr-corrective-actions/probe.sh
  - .planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md
  - taskflow/src/services/gitlab.ts
  - taskflow/src/services/gitlab.test.ts
autonomous: false
requirements: [MRFIX-01, MRFIX-02]

must_haves:
  truths:
    - "The roadmap-mandated probe is answered as documentation only: `reset_approvals_on_push`, a live MR's `approved_by`, and the project's protected branches are recorded verbatim in 90-PROBE-RESULTS.md, and no UI friction is added as a result (D-16)"
    - "One real failing-PUT error body from `PUT /merge_requests/:iid` is captured verbatim, resolving RESEARCH Open Question A1 for the error flattener"
    - "`flattenGitLabError` turns all three GitLab error-body shapes (string, string[], field-keyed object) into readable text — never `[object Object]` (D-10, closes 88-REVIEW WR-01 for this phase)"
    - "`updateMergeRequest` PUTs only `target_branch` and/or `milestone_id`, through `apiFetch('gitlab', ...)`, and throws on every non-2xx with a flattened message that contains no token, header, or request URL"
  artifacts:
    - path: ".planning/phases/90-per-mr-corrective-actions/probe.sh"
      provides: "read-only approvals/protected-branch probe + one deliberately-invalid scratch PUT for error-shape ground truth"
      contains: "protected_branches"
    - path: ".planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md"
      provides: "verbatim probe output + the D-16 no-UI-change ruling + the captured error body"
      contains: "D-16"
    - path: "taskflow/src/services/gitlab.ts"
      provides: "flattenGitLabError + updateMergeRequest (the phase's only new write endpoint)"
      contains: "export async function updateMergeRequest"
    - path: "taskflow/src/services/gitlab.test.ts"
      provides: "updateMergeRequest describe block covering PUT body, all three error shapes, and 401/403 -> ApiError"
      contains: "describe('updateMergeRequest'"
  key_links:
    - from: "updateMergeRequest"
      to: "apiFetch('gitlab', ...)"
      via: "the PUT call (never raw fetch — inherits 15s timeout, 401 markDisconnected, PRIVATE-TOKEN redaction)"
      pattern: "apiFetch\\(\\s*'gitlab'"
    - from: "updateMergeRequest error branch"
      to: "flattenGitLabError"
      via: "message extraction before throwing"
      pattern: "flattenGitLabError\\(body\\)"
---

<objective>
Establish the phase's service-layer foundation and answer the roadmap probe.

Two things must be true before any mutation or UI work starts: (1) the one new GitLab write endpoint `PUT /projects/:id/merge_requests/:iid` exists in `gitlab.ts`, restricted to `target_branch` and `milestone_id`, and (2) its error messages are actually readable — D-10 makes the carried-forward WR-01 `[object Object]` defect blocking here, because D-07 puts the raw message straight into a cell tooltip with no other error surface on the page.

The probe (ROADMAP §Phase 90) is documentation only: per **D-16** (hard user decision, already survived a direct challenge) the approval-reset answer changes **no** UI — no confirm dialog, no warning, no tooltip line, regardless of outcome. Its second job is capturing one real failing-PUT body as ground truth for RESEARCH Open Question A1.

Purpose: every later plan in this phase calls `updateMergeRequest`; a narrow error extractor here would make the whole failure affordance worthless.
Output: `probe.sh`, `90-PROBE-RESULTS.md`, `flattenGitLabError` + `updateMergeRequest` in `gitlab.ts`, and a `describe('updateMergeRequest')` block in `gitlab.test.ts`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-per-mr-corrective-actions/90-CONTEXT.md
@.planning/phases/90-per-mr-corrective-actions/90-RESEARCH.md
@.planning/phases/90-per-mr-corrective-actions/90-PATTERNS.md
@.planning/phases/90-per-mr-corrective-actions/90-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the read-only approvals / protected-branch probe script</name>
  <files>.planning/phases/90-per-mr-corrective-actions/probe.sh</files>

  <read_first>
    - `.planning/phases/89-three-channel-drift-detection/probe.sh` (the invocation contract to mirror exactly: `set -uo pipefail`, `: "${GITLAB_BASE_URL:?...}"` env guards, `AUTH=(-H "PRIVATE-TOKEN: ${GITLAB_PAT}" ...)`, `curl -sS --max-time 30`, banner `==================== PHASE NN PROBE RESULTS ====================` and closing `==================== END — paste everything above ====================`)
    - `.planning/phases/90-per-mr-corrective-actions/90-RESEARCH.md` § "Probe: MR-approval / protected-branch rules" (the four concrete steps) and § "Open Questions" (A1)
    - `.planning/phases/90-per-mr-corrective-actions/90-CONTEXT.md` § "The approval-reset side effect" (D-16 — the probe's outcome is a fact to record, never a dialog to add)
  </read_first>

  <action>
    Create `.planning/phases/90-per-mr-corrective-actions/probe.sh`, modelled byte-for-byte on Phase 89's `probe.sh` structure (same env-var guard idiom, same `AUTH` array, same banner/END framing so the developer's paste is unambiguous). Required env vars: `GITLAB_BASE_URL`, `GITLAB_PAT`, `PROJECT_ID`. Optional env vars: `SAMPLE_MR_IID` (an MR that already has at least one approval) and `SCRATCH_MR_IID` (a throwaway MR used only for the invalid-PUT capture).

    Four probes, printed under `----- PROBE A/B/C/D -----` headings:

    - PROBE A — `GET /api/v4/projects/${PROJECT_ID}/approvals`. Print the raw JSON, then a single derived line `reset_approvals_on_push: <true|false|ABSENT>` extracted with `jq -r '.reset_approvals_on_push'`.
    - PROBE B — `GET /api/v4/projects/${PROJECT_ID}/merge_requests/${SAMPLE_MR_IID}/approvals`. Skip with the literal line `PROBE B: SKIPPED (SAMPLE_MR_IID unset)` when the var is unset. Otherwise print raw JSON plus derived lines `approvals_required: …`, `approvals_left: …`, `approved_by_count: <jq '.approved_by | length'>`.
    - PROBE C — fully paginated `GET /api/v4/projects/${PROJECT_ID}/protected_branches?per_page=100&page=N` (same `while :; do … [ "${COUNT}" -lt 100 ] && break` loop shape Phase 89 used for branches). Print every protected-branch `name`, and a derived line `release_pattern_protected: <YES|NO>` set to YES when any name equals `release/*` or begins with `release/`.
    - PROBE D — the single non-read-only call in this phase's probe, and the only reason it is safe: a **deliberately invalid** `PUT /api/v4/projects/${PROJECT_ID}/merge_requests/${SCRATCH_MR_IID}` with body `{"target_branch":"__taskflow-probe-nonexistent-branch__"}`. Because that branch cannot exist, GitLab must reject it — the MR is never modified. Use `curl -sS --max-time 30 -o /tmp/phase90-put-body.txt -w '%{http_code}'` so both status and verbatim body are printed under `http_status:` and `response_body:` labels. Skip with the literal line `PROBE D: SKIPPED (SCRATCH_MR_IID unset)` when the var is unset. Add an inline comment naming this as the RESEARCH A1 ground-truth capture and stating that no valid write is ever issued by this script.

    Do not read, echo, or log `GITLAB_PAT` anywhere in the output. Do not add any probe that issues a *valid* write.
  </action>

  <acceptance_criteria>
    - `bash -n .planning/phases/90-per-mr-corrective-actions/probe.sh` exits 0 (syntax valid)
    - The script greps clean for all four endpoints: `/approvals`, `/merge_requests/${SAMPLE_MR_IID}/approvals`, `/protected_branches`, and the PUT to `/merge_requests/${SCRATCH_MR_IID}`
    - `grep -c '__taskflow-probe-nonexistent-branch__' probe.sh` returns at least 1 (the invalid target proves PROBE D cannot mutate the MR)
    - The only `-X PUT` / `--request PUT` occurrence in the file is PROBE D; there is no POST, PATCH or DELETE anywhere
    - `grep -E 'echo .*GITLAB_PAT' probe.sh` returns nothing (token never printed)
    - Both optional-var skip paths print their literal `SKIPPED` lines rather than issuing a call with an empty iid
  </acceptance_criteria>

  <verify>
    <automated>bash -n .planning/phases/90-per-mr-corrective-actions/probe.sh &amp;&amp; grep -q 'protected_branches' .planning/phases/90-per-mr-corrective-actions/probe.sh &amp;&amp; grep -q '__taskflow-probe-nonexistent-branch__' .planning/phases/90-per-mr-corrective-actions/probe.sh</automated>
  </verify>

  <done>A syntactically valid, token-safe probe script exists covering the three read-only endpoints plus the one deliberately-failing PUT capture.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the probe against live GitLab and record 90-PROBE-RESULTS.md</name>
  <files>.planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md</files>

  <read_first>
    - `.planning/phases/90-per-mr-corrective-actions/probe.sh` (written in Task 1 — the script the developer runs)
    - `.planning/phases/89-three-channel-drift-detection/89-PROBE-RESULTS.md` (the file shape to mirror: `## Raw output` verbatim block plus explicit derived-verdict lines)
    - `.planning/phases/90-per-mr-corrective-actions/90-CONTEXT.md` § D-16 (the ruling this document must restate)
  </read_first>

  <what-built>
    `probe.sh` from Task 1. It cannot be run by Claude: the GitLab PAT lives in the Tauri Stronghold vault and is not readable here. Prior phases used instance `https://git.devel.sun.orange.sk` and project `455`. Never fabricate probe output.
  </what-built>

  <how-to-verify>
    1. `chmod +x .planning/phases/90-per-mr-corrective-actions/probe.sh`
    2. From the repo root run (fill in a real approved MR iid and a throwaway MR iid):
       `GITLAB_BASE_URL="https://git.devel.sun.orange.sk" GITLAB_PAT="<your PAT>" PROJECT_ID="455" SAMPLE_MR_IID="<an MR with >=1 approval>" SCRATCH_MR_IID="<a throwaway MR>" bash .planning/phases/90-per-mr-corrective-actions/probe.sh`
    3. Paste everything between the `PHASE 90 PROBE RESULTS` banner and the `END` line back into the chat.
    4. PROBE D issues one PUT with target branch `__taskflow-probe-nonexistent-branch__`. It is expected to FAIL with a 4xx — that failure body is the point. If it somehow returns 2xx, say so: the MR's target branch was changed and must be reverted in GitLab manually.
  </how-to-verify>

  <action>
    After the developer pastes the output, write `.planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md` containing, in this order:
    1. `## Raw output` — the pasted block verbatim, not paraphrased.
    2. `## Findings` — one line each: `reset_approvals_on_push: <value>`, `approved_by_count: <value|SKIPPED>`, `release_pattern_protected: <YES|NO|SKIPPED>`.
    3. `## A1 resolution` — the verbatim PROBE D `http_status` and `response_body`, followed by exactly one of `A1: field-keyed object shape confirmed`, `A1: flat string shape confirmed`, `A1: array shape confirmed`, or `A1: UNRESOLVED (probe D skipped)`. Note that all three shapes are handled defensively by `flattenGitLabError` regardless, so no outcome blocks Task 3.
    4. `## D-16 ruling` — a paragraph stating that per D-16 this result changes no UI: no confirm dialog, no inline warning, no extra tooltip line on retarget, whatever the approval/protected-branch answer turned out to be. State explicitly that downstream agents must not reintroduce friction citing this evidence.

    Do not include the PAT or any `PRIVATE-TOKEN` header value in the recorded output — redact it if the paste contains one.
  </action>

  <acceptance_criteria>
    - `.planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md` exists with all four headings: `## Raw output`, `## Findings`, `## A1 resolution`, `## D-16 ruling`
    - `grep -E 'A1: (field-keyed object shape confirmed|flat string shape confirmed|array shape confirmed|UNRESOLVED)' 90-PROBE-RESULTS.md` matches exactly one line
    - The `## Findings` section contains a literal `reset_approvals_on_push:` line
    - The `## D-16 ruling` section contains the words `no confirm dialog` and `no warning`
    - `grep -iE 'private-token: [A-Za-z0-9_-]{8,}' 90-PROBE-RESULTS.md` returns nothing (no leaked credential)
    - No source file under `taskflow/src` is modified by this task
  </acceptance_criteria>

  <verify>
    <automated>test -f .planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md &amp;&amp; grep -qE 'A1: (field-keyed object shape confirmed|flat string shape confirmed|array shape confirmed|UNRESOLVED)' .planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md &amp;&amp; grep -q 'reset_approvals_on_push:' .planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md &amp;&amp; ! grep -qiE 'private-token: [A-Za-z0-9_-]{8,}' .planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md</automated>
  </verify>

  <done>The roadmap probe is recorded as documentation, RESEARCH A1 has a verdict, and the D-16 no-UI-change ruling is written down for downstream agents.</done>

  <resume-signal>Paste the probe output, then type "approved" (or report if PROBE D unexpectedly succeeded)</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add flattenGitLabError and updateMergeRequest to gitlab.ts</name>
  <files>taskflow/src/services/gitlab.ts, taskflow/src/services/gitlab.test.ts</files>

  <read_first>
    - `taskflow/src/services/gitlab.ts` L997-1036 (`updateMilestone` — the PUT + URL + `apiFetch` label shape to copy; also the exact `body?.message ?? status` line that IS the WR-01 defect)
    - `taskflow/src/services/gitlab.ts` L1106-1157 (`createBranch` — the already-widened `string | string[]` error handling and the WR-11 comment about composing messages ONLY from `body.message` or a fixed literal, never the token/header/URL)
    - `taskflow/src/services/gitlab.ts` L425-450 (`GitLabMR` interface — the return type; note `milestone: { id: number; title: string } | null`)
    - `taskflow/src/services/gitlab.test.ts` L1991-2090 (`describe('createBranch')` — the `mockFetch` harness, `calledOptions.method`/`JSON.parse(calledOptions.body)` assertions, and the `ApiError` `toMatchObject({ status, source: 'gitlab' })` idiom)
    - `.planning/phases/90-per-mr-corrective-actions/90-RESEARCH.md` Pattern 1 + Pattern 2 (the three-branch flattener and the target function shape) and § "Verified GitLab parameter table" (`milestone_id` is the GLOBAL id, not iid)
    - `.planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md` (Task 2 output — the real error body shape observed)
  </read_first>

  <behavior>
    `flattenGitLabError(body: unknown): string | undefined`
    - `{ message: 'Target branch is invalid' }` -> `'Target branch is invalid'`
    - `{ message: ['a', 'b'] }` -> `'a, b'`
    - `{ message: { target_branch: ["can't be blank"] } }` -> `"target_branch can't be blank"` (D-10 — the shape neither existing analog handles)
    - `{ message: { target_branch: ['x'], milestone_id: ['y'] } }` -> `'target_branch x; milestone_id y'`
    - `{ error: 'insufficient_scope' }` (no `message` key) -> `undefined`
    - `null`, `undefined`, `'a string'`, `42` -> `undefined`
    - Never returns a string containing `[object Object]` for any of the above

    `updateMergeRequest(baseUrl, token, projectId, mrIid, fields)`
    - `{ target_branch: 'release/33.7.0' }` -> PUTs to `<base>/api/v4/projects/42/merge_requests/7` with method `PUT` and body exactly `{"target_branch":"release/33.7.0"}`; resolves the parsed `GitLabMR`
    - `{ milestone_id: 5 }` -> body exactly `{"milestone_id":5}`
    - `{}` (neither field) -> throws before any fetch; `apiFetch` is never called
    - extra/unknown keys passed at runtime are dropped from the body — only `target_branch` and `milestone_id` are ever sent
    - non-2xx with a field-keyed body -> throws `Error` whose message contains the flattened text and does NOT contain `[object Object]`
    - 401 and 403 -> throws `ApiError` matching `{ status, source: 'gitlab' }` with the flattened message (or the literal fallback `Failed to update merge request`)
    - network rejection from `apiFetch` -> throws `Error('Cannot reach <baseUrl> — check the base URL')`
    - no thrown message ever contains the token, the `PRIVATE-TOKEN` header, or the request URL
  </behavior>

  <action>
    In `taskflow/src/services/gitlab.ts`, add a module-level exported `flattenGitLabError(body: unknown): string | undefined` implementing exactly the three-branch normalisation in RESEARCH Pattern 1 (string -> as-is; array -> `.join(', ')`; object -> `Object.entries(...).map(([field, errs]) => \`${field} ${Array.isArray(errs) ? errs.join(', ') : String(errs)}\`).join('; ')`). Give it a doc comment naming D-10 and 88-REVIEW WR-01, and stating that `updateMilestone`/`createBranch`/`createMilestone` each independently reinvented a narrower widening — this helper is the single place the object-keyed shape is handled, so a fourth reinvention is not to be added. Do **not** modify the three existing write functions in this plan (back-porting is explicitly a Deferred Idea, out of scope).

    Add `export async function updateMergeRequest(baseUrl: string, token: string, projectId: number, mrIid: number, fields: { target_branch?: string; milestone_id?: number }): Promise<GitLabMR>` following `updateMilestone`'s skeleton: `const base = baseUrl.replace(/\/$/, '')`, URL `${base}/api/v4/projects/${projectId}/merge_requests/${mrIid}`, `apiFetch('gitlab', url, { method: 'PUT', headers: { 'PRIVATE-TOKEN': token, 'Content-Type': 'application/json' }, body }, 'Update Merge Request')` inside try/catch whose catch throws `Cannot reach ${baseUrl} — check the base URL`.

    Two security requirements that are not in either analog (threats T-90-01 and T-90-02 in this plan's threat model):
    - Build the request body by explicitly picking `fields.target_branch` and `fields.milestone_id` into a fresh object, never `JSON.stringify(fields)` and never a spread of the argument. Any other key a caller passes at runtime must be dropped. Throw `new Error('updateMergeRequest requires target_branch or milestone_id')` when both are undefined (GitLab's own "must include at least one non-required attribute" rule), before issuing the request.
    - Error messages compose only from `flattenGitLabError(body)` or a fixed literal — carry `createBranch`'s WR-11 comment forward verbatim in spirit. `401`/`403` throw `new ApiError(msg ?? 'Failed to update merge request', response.status, 'gitlab')` so `apiFetch`'s `markDisconnected` behaviour is preserved; every other non-2xx throws `new Error(\`Failed to update merge request: ${msg ?? \`status ${response.status}\`}\`)`.

    Document in the JSDoc that `milestone_id` is the milestone's GLOBAL `id` (matching `GitLabMR.milestone.id` and `GitLabMilestone.id`), not the project-scoped `iid`, and that `0`/empty unassigns — mirroring the `id`-vs-`iid` note `updateMilestone` already carries.

    In `taskflow/src/services/gitlab.test.ts`, add a `describe('updateMergeRequest', ...)` block modelled on `describe('createBranch')`, plus a `describe('flattenGitLabError', ...)` block covering the pure-function cases above. Reuse the file's existing `mockFetch` harness — do not introduce a new mocking style. Include one assertion asserting the thrown message does not match `/\[object Object\]/` for the field-keyed case, and one asserting the thrown message does not contain the token string used in the test.
  </action>

  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/services/gitlab.test.ts -t "updateMergeRequest" &amp;&amp; npx vitest run src/services/gitlab.test.ts -t "flattenGitLabError" &amp;&amp; npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "export async function updateMergeRequest" taskflow/src/services/gitlab.ts` returns 1
    - `grep -c "export function flattenGitLabError" taskflow/src/services/gitlab.ts` returns 1
    - `updateMergeRequest`'s HTTP call goes through `apiFetch('gitlab', …, 'Update Merge Request')` — `grep -v '^\s*\*' taskflow/src/services/gitlab.ts | grep -c "Update Merge Request"` is at least 1, and there is no bare `fetch(` inside the function
    - The function body contains no `JSON.stringify(fields)`; the body object is assembled from the two allowed keys only
    - `npx vitest run src/services/gitlab.test.ts -t "updateMergeRequest"` passes with at least 7 tests (happy PUT for each field, empty-fields throw, unknown-key drop, field-keyed error flattening, 401 ApiError, 403 ApiError, network failure)
    - `npx vitest run src/services/gitlab.test.ts -t "flattenGitLabError"` passes with at least 6 cases covering string / array / field-keyed object / multi-field object / no-message / non-object
    - `npx tsc --noEmit` exits 0
    - `npx biome check ./src` reports diagnostics only for `BacklogPage.tsx` / `BacklogRow.tsx` (the known 2-error baseline) — zero new files flagged
    - `updateMilestone`, `createBranch`, and `createMilestone` are byte-unchanged (back-port is out of scope)
  </acceptance_criteria>

  <done>`updateMergeRequest` exists, sends only the two whitelisted fields through `apiFetch`, and every failure path produces a readable, credential-free message via `flattenGitLabError`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App -> GitLab REST (authenticated write) | First MR-mutating request the app has ever issued; the PAT crosses here in a `PRIVATE-TOKEN` header |
| GitLab error body -> app UI | Server-controlled text that D-07 renders directly in a cell tooltip |
| Developer shell -> live GitLab (probe) | One deliberately-invalid PUT issued outside the app, by hand, with a real PAT in the environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Tampering | `updateMergeRequest` request body | mitigate | Body assembled by explicitly picking `target_branch`/`milestone_id`; never `JSON.stringify(fields)` or a spread, so no caller-supplied key (e.g. `state_event: 'close'`, `assignee_id`) can reach GitLab. Throws when both are absent. Asserted by the "unknown-key drop" test. |
| T-90-02 | Information Disclosure | thrown error messages | mitigate | Messages composed only from `flattenGitLabError(body)` or the fixed literal `Failed to update merge request` — never the token, `PRIVATE-TOKEN` header, or request URL (carries `createBranch`'s WR-11 rule). Asserted by a test that the thrown message excludes the test token. |
| T-90-03 | Information Disclosure | probe output recorded into `.planning/` | mitigate | `probe.sh` never echoes `GITLAB_PAT`; Task 2's acceptance criteria grep the recorded file for a leaked `PRIVATE-TOKEN` value and redact before commit. |
| T-90-04 | Tampering | PROBE D live PUT | mitigate | The probe's only write targets `__taskflow-probe-nonexistent-branch__`, which cannot exist, so GitLab must reject it and the MR cannot be modified; run against a developer-nominated throwaway MR, behind a blocking human checkpoint. Unexpected 2xx is called out in the resume instructions as requiring manual revert. |
| T-90-05 | Denial of Service | `apiFetch` timeout | accept | Inherited 15s AbortController timeout and single-shot mutation (no retry loop) bound the blast radius; no new control needed. |
| T-90-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable this phase — zero new dependencies (RESEARCH § "Package Legitimacy Audit": not applicable). Any executor that finds itself running `npm install` must stop; slopcheck + a blocking human checkpoint would be required for any `[ASSUMED]`/`[SUS]` package. |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/services/gitlab.test.ts` — full gitlab service suite green (existing tests plus the two new describe blocks)
- `cd taskflow && npx tsc --noEmit` — exits 0
- `cd taskflow && npx biome check ./src` — diagnostics confined to the known `BacklogPage.tsx`/`BacklogRow.tsx` baseline
- `.planning/phases/90-per-mr-corrective-actions/90-PROBE-RESULTS.md` records the probe verbatim plus the D-16 ruling
</verification>

<success_criteria>
- `updateMergeRequest` is callable with `{ target_branch }` or `{ milestone_id }` and cannot be coerced into sending any other MR field
- No GitLab error body shape produces `[object Object]` (D-10 satisfied for this phase's write path)
- The roadmap probe is answered in writing, with an explicit restatement that D-16 keeps the UI friction-free either way
</success_criteria>

<output>
Create `.planning/phases/90-per-mr-corrective-actions/90-01-SUMMARY.md` when done. Record: the probe findings in one line each, the A1 verdict, and any deviation from RESEARCH Pattern 1/2.
</output>
