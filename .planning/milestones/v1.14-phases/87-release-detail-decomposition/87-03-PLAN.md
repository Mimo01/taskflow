---
phase: 87-release-detail-decomposition
plan: 03
type: execute
wave: 3
depends_on: ["87-02"]
files_modified:
  - taskflow/src/routes/dashboard/release-detail/MetaRow.tsx
  - taskflow/src/routes/dashboard/release-detail/ReleaseDetailSkeleton.tsx
  - taskflow/src/routes/dashboard/release-detail/ReleaseHeader.tsx
  - taskflow/src/routes/dashboard/release-detail/DescriptionsSection.tsx
  - taskflow/src/routes/dashboard/release-detail/LabelSummarySection.tsx
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
autonomous: true
requirements: [FOUND-01]

must_haves:
  truths:
    - "The breadcrumb header still renders during the loading skeleton state, exactly as before"
    - "The Descriptions block still collapses to a single 'No description' section when a milestone is matched and neither side has text"
    - "The Labels block still renders only when milestone MRs have loaded AND at least one label exists"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/MetaRow.tsx"
      provides: "Private release-detail MetaRow copy preserving min-w-0 (D-13)"
      contains: "flex-1 min-w-0"
    - path: "taskflow/src/routes/dashboard/release-detail/ReleaseDetailSkeleton.tsx"
      provides: "Loading skeleton, moved verbatim"
      contains: "release-detail-skeleton"
    - path: "taskflow/src/routes/dashboard/release-detail/ReleaseHeader.tsx"
      provides: "Breadcrumb header + title heading as two separate named exports"
      exports: ["ReleaseBreadcrumbHeader", "ReleaseTitleHeading"]
    - path: "taskflow/src/routes/dashboard/release-detail/DescriptionsSection.tsx"
      provides: "Jira + GitLab description blocks incl. the collapsed-empty branch"
    - path: "taskflow/src/routes/dashboard/release-detail/LabelSummarySection.tsx"
      provides: "Label summary chips from milestone MRs"
  key_links:
    - from: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      to: "./release-detail/ReleaseHeader"
      via: "direct-path import (D-05, no barrel)"
      pattern: "from './release-detail/ReleaseHeader'"
---

<objective>
Extract the five low-risk leaf pieces of `ReleaseDetailPage.tsx`: `MetaRow`, `ReleaseDetailSkeleton`, the breadcrumb/title header, the descriptions block, and the label summary block.

Purpose: D-01's per-section split, starting with the sections that have no nested-structure trap and no handler webs. Every one is a verbatim JSX move behind a props interface.

Output: five new files in `release-detail/`, each imported by direct relative path from the page shell.

Hard constraint: zero user-visible change. Every extracted JSX subtree must render byte-identical markup — same tags, same `className` strings, same conditional gates, same icon components.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-release-detail-decomposition/87-CONTEXT.md
@.planning/phases/87-release-detail-decomposition/87-RESEARCH.md
@.planning/phases/87-release-detail-decomposition/87-PATTERNS.md
@.planning/phases/87-release-detail-decomposition/87-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract MetaRow.tsx and ReleaseDetailSkeleton.tsx</name>
  <files>taskflow/src/routes/dashboard/release-detail/MetaRow.tsx, taskflow/src/routes/dashboard/release-detail/ReleaseDetailSkeleton.tsx, taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx (the two local functions at the file tail: `MetaRow` and `ReleaseDetailSkeleton`, pre-refactor L1488-1518)
    - taskflow/src/routes/dashboard/issue-detail/MetaRow.tsx (the near-duplicate — read it to confirm the delta, do NOT import it)
    - taskflow/src/routes/dashboard/ReleasesSkeleton.tsx (file-placement/export convention reference only, NOT content)
    - .planning/phases/87-release-detail-decomposition/87-PATTERNS.md (§MetaRow.tsx — the verbatim target body)
  </read_first>
  <action>
    Create `release-detail/MetaRow.tsx` containing the local `MetaRow` body verbatim, with `function` changed to `export function`. It MUST keep `className="flex-1 min-w-0"` on the value `<span>`. `issue-detail/MetaRow.tsx` has `flex-1` WITHOUT `min-w-0`; that one-class delta changes truncation behavior, which is why D-13 mandates a private copy. Do NOT import from `issue-detail/MetaRow`. Do NOT convert the inline destructured prop type `{ label, children }: { label: string; children: React.ReactNode }` into an `interface MetaRowProps` — this is the documented exception to the folder's interface convention; both the source and the analog use the inline form.

    Create `release-detail/ReleaseDetailSkeleton.tsx` containing the local `ReleaseDetailSkeleton` body verbatim, with `function` changed to `export function`. Keep `data-testid="release-detail-skeleton"`, the `className="flex h-full p-6 gap-6"` wrapper, all six left-column `Skeleton` elements with their exact width classes, and the right column's inline `style={{ width: 288 }}`. Import only `Skeleton` from `@/components/ui/skeleton`.

    In `ReleaseDetailPage.tsx`: delete both local function declarations from the file tail and add direct-path imports `from './release-detail/MetaRow'` and `from './release-detail/ReleaseDetailSkeleton'`. Do not create or use a `release-detail/index.ts` barrel (D-05 — the page shell imports by direct path, matching how `IssueDetailContent.tsx` imports its sections). Remove the `Skeleton` import from the page if it becomes unused.
  </action>
  <acceptance_criteria>
    - `grep -c "flex-1 min-w-0" taskflow/src/routes/dashboard/release-detail/MetaRow.tsx` returns 1
    - `grep -c "interface MetaRowProps" taskflow/src/routes/dashboard/release-detail/MetaRow.tsx` returns 0
    - `grep -c "issue-detail/MetaRow" taskflow/src/routes/dashboard/release-detail/MetaRow.tsx` returns 0
    - `grep -c "release-detail-skeleton" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSkeleton.tsx` returns 1
    - `grep -c "width: 288" taskflow/src/routes/dashboard/release-detail/ReleaseDetailSkeleton.tsx` returns 1
    - `grep -c "^function MetaRow\|^function ReleaseDetailSkeleton" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0
    - `grep -c "from './release-detail/MetaRow'" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 1
    - `ls taskflow/src/routes/dashboard/release-detail/index.ts` does not exist
    - `cd taskflow && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit</automated>
  </verify>
  <done>`MetaRow.tsx` (with `min-w-0`) and `ReleaseDetailSkeleton.tsx` exist as named exports in `release-detail/`, the page imports them by direct path, and the page no longer declares either locally.</done>
</task>

<task type="auto">
  <name>Task 2: Extract ReleaseHeader.tsx (breadcrumb header + title heading as two exports)</name>
  <files>taskflow/src/routes/dashboard/release-detail/ReleaseHeader.tsx, taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx (the breadcrumb block `{trail.length > 0 && (...)}` and the Rocket/`v{version.id}`/`<h2>` heading block inside the left column)
    - taskflow/src/routes/dashboard/issue-detail/MergeRequestsSection.tsx (the component/prop-typing convention: `interface XProps` above a named export, props destructured in the signature)
    - .planning/phases/87-release-detail-decomposition/87-RESEARCH.md (§2 ReleaseHeader — the nullable-`version` hazard)
  </read_first>
  <action>
    PLANNER DEVIATION FROM D-01 (record it in the SUMMARY): D-01 describes one `ReleaseHeader.tsx` covering "back/breadcrumb header + name/status heading". RESEARCH §1 verified these are NOT adjacent in the JSX tree — the breadcrumb renders unconditionally at the top level (outside the `isLoading || !version` ternary) while the title heading renders inside the left column, inside the else-branch. Merging them into one component would move the heading out of the left column and change the DOM. Therefore `ReleaseHeader.tsx` exports TWO named components rendered at their existing positions.

    Create `release-detail/ReleaseHeader.tsx` with:

    `export function ReleaseBreadcrumbHeader({ trail, versionName, onBack, onBreadcrumbClick }: ReleaseBreadcrumbHeaderProps)` — the entire `{trail.length > 0 && (<div className="px-6 py-3 border-b flex items-center gap-2 text-sm flex-shrink-0">...)}` block. Props: `trail: BreadcrumbEntry[]` (use the trail element type from `@/stores/breadcrumb.store`), `versionName: string | undefined` (the shell passes `version?.name`), `onBack: () => void`, `onBreadcrumbClick: (index: number, path: string) => void`. Keep the `trail.length > 0` guard INSIDE this component and return `null` otherwise, so the shell renders it unconditionally. Keep the trailing `<span className="font-medium">{versionName ?? 'Release'}</span>` fallback — this component must render correctly while `version` is still null during loading. The per-entry onClick body (`useBreadcrumbStore.setState({ trail: trail.slice(0, i) })` + `navigate(entry.path, { replace: true })`) moves into a shell-level closure passed as `onBreadcrumbClick`; do NOT import `useNavigate` or `useBreadcrumbStore` inside this file (D-08, hazard 4).

    `export function ReleaseTitleHeading({ versionId, versionName }: ReleaseTitleHeadingProps)` — the `<div>` containing the `Rocket` icon, `<p className="text-xs font-mono text-muted-foreground">v{version.id}</p>` and `<h2 className="text-xl font-semibold leading-snug">{version.name}</h2>`. Props: `versionId: string`, `versionName: string`. Both non-nullable — it renders only inside the loaded branch.

    Each component gets its own `interface XxxProps` declared immediately above it. Named exports only.

    In `ReleaseDetailPage.tsx`: replace the two JSX blocks with `<ReleaseBreadcrumbHeader ... />` and `<ReleaseTitleHeading versionId={version.id} versionName={version.name} />` at their EXACT current positions in the tree — the breadcrumb stays a direct child of the outer `<div className="flex flex-col h-full overflow-hidden">` above the `isLoading || !version` ternary; the title heading stays the first child inside `<div className="p-6 space-y-6">`. Define `handleBreadcrumbClick` in the shell. Drop `ArrowLeft` / `Rocket` imports from the page if they become unused.
  </action>
  <acceptance_criteria>
    - `grep -c "export function ReleaseBreadcrumbHeader\|export function ReleaseTitleHeading" taskflow/src/routes/dashboard/release-detail/ReleaseHeader.tsx` returns 2
    - `grep -c "interface ReleaseBreadcrumbHeaderProps\|interface ReleaseTitleHeadingProps" taskflow/src/routes/dashboard/release-detail/ReleaseHeader.tsx` returns 2
    - `grep -nE "useNavigate|useBreadcrumbStore" taskflow/src/routes/dashboard/release-detail/ReleaseHeader.tsx` returns no match (presentational only, D-08)
    - `ReleaseHeader.tsx` contains the literal strings `px-6 py-3 border-b flex items-center gap-2 text-sm flex-shrink-0`, `aria-label="Go back"`, `?? 'Release'`, `text-xl font-semibold leading-snug`, and `text-xs font-mono text-muted-foreground`
    - `grep -c "export default" taskflow/src/routes/dashboard/release-detail/ReleaseHeader.tsx` returns 0
    - In `ReleaseDetailPage.tsx`, `<ReleaseBreadcrumbHeader` appears before (above) the `isLoading || !version ?` ternary — confirm by line numbers from `grep -n`
    - `grep -c "aria-label=\"Go back\"" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0
    - `cd taskflow && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit && npm run check</automated>
  </verify>
  <done>Breadcrumb header and title heading render from `ReleaseHeader.tsx` at their original tree positions; the breadcrumb still renders during loading; neither component imports routing or store hooks.</done>
</task>

<task type="auto">
  <name>Task 3: Extract DescriptionsSection.tsx and LabelSummarySection.tsx</name>
  <files>taskflow/src/routes/dashboard/release-detail/DescriptionsSection.tsx, taskflow/src/routes/dashboard/release-detail/LabelSummarySection.tsx, taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx (the `{/* Description(s) ... */}` ternary block and the `{/* Label summary from milestone MRs */}` block in the left column)
    - taskflow/src/routes/dashboard/issue-detail/MergeRequestsSection.tsx (component/prop convention)
    - .planning/phases/87-release-detail-decomposition/87-RESEARCH.md (§2 DescriptionsSection / LabelSummarySection props contracts)
  </read_first>
  <action>
    Create `release-detail/DescriptionsSection.tsx` with `interface DescriptionsSectionProps { gitlabMatchType: ReleaseMatch['type']; matchedMilestone: GitLabMilestone | null; versionDescription: string | null | undefined }` and `export function DescriptionsSection(...)`.

    Move the ENTIRE description ternary verbatim — including the three-way structure:
    - collapsed branch when `gitlabMatchType !== 'none' && matchedMilestone && !versionDescription && !matchedMilestone.description` → one `<section>` with the `FileText` icon heading "Description" and the italic "No description" paragraph;
    - otherwise the `<>` fragment containing the Jira `<section>` (heading text switches between `'Jira Description'` and `'Description'` based on `gitlabMatchType !== 'none' && matchedMilestone`) and the conditional GitLab `<section>` with its `ReactMarkdown remarkPlugins={[remarkGfm]}` block and the exact `text-sm prose prose-sm dark:prose-invert max-w-none [&_p]:my-1 [&_ul]:my-1 [&_ul]:pl-4 [&_li]:my-0` class string.
    Keep the outer `<>...</>` fragment — the two sections are siblings inside the parent's `space-y-6` flow and must not gain a wrapping element. Move the `ReactMarkdown`, `remarkGfm`, and `FileText` imports into this file.

    Create `release-detail/LabelSummarySection.tsx` with `interface LabelSummarySectionProps { milestoneMRsLoaded: boolean; labelSummary: Array<{ label: { name: string; color: string; text_color: string }; count: number }> }` and `export function LabelSummarySection(...)`. Move the label block verbatim: the gate becomes `if (!milestoneMRsLoaded || labelSummary.length === 0) return null;` (semantically identical to the current `{milestoneMRs && labelSummary.length > 0 && ...}` — note an EMPTY loaded array is truthy today, so the shell must pass `milestoneMRsLoaded={!!milestoneMRs}`, not `milestoneMRs.length > 0`). Keep the `Tag` icon heading "Labels", the `flex flex-wrap gap-1.5` container, and each chip's inline `style` with `backgroundColor: l.label.color`, `color: l.label.text_color`, `borderColor: \`${l.label.color}80\`` and the `{l.label.name} ({l.count})` text.

    In `ReleaseDetailPage.tsx`: replace both blocks with `<DescriptionsSection gitlabMatchType={gitlabMatch.type} matchedMilestone={matchedMilestone} versionDescription={version.description} />` and `<LabelSummarySection milestoneMRsLoaded={!!milestoneMRs} labelSummary={labelSummary} />`, in their exact current positions inside `<div className="p-6 space-y-6">`. Drop the page's now-unused `ReactMarkdown`, `remarkGfm`, and `Tag` imports (keep `FileText` only if still referenced elsewhere in the page).
  </action>
  <acceptance_criteria>
    - `grep -c "export function DescriptionsSection" taskflow/src/routes/dashboard/release-detail/DescriptionsSection.tsx` returns 1
    - `DescriptionsSection.tsx` contains all three of: `'Jira Description'`, `'GitLab Description'`, `dark:prose-invert max-w-none [&_p]:my-1`
    - `grep -c "ReactMarkdown" taskflow/src/routes/dashboard/release-detail/DescriptionsSection.tsx` is at least 2 (import + usage)
    - `grep -c "ReactMarkdown\|remarkGfm" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0
    - `LabelSummarySection.tsx` contains `milestoneMRsLoaded` and the literal `` `${l.label.color}80` ``
    - `grep -c "milestoneMRsLoaded={!!milestoneMRs}" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 1
    - `grep -c "text_color" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0
    - `grep -nE "useQuery|useNavigate|useAuthStore" taskflow/src/routes/dashboard/release-detail/DescriptionsSection.tsx taskflow/src/routes/dashboard/release-detail/LabelSummarySection.tsx` returns no matches
    - `cd taskflow && npx tsc --noEmit` exits 0 and `cd taskflow && npm run check` shows no more than the 2 baseline errors
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && npm run check && npm test -- ReleasesTab.test.tsx UpcomingReleasesTimeline.test.tsx releaseSummaries.test.ts</automated>
  </verify>
  <done>Descriptions and Labels render from their own presentational files with identical markup and identical conditional gates; the page no longer references `ReactMarkdown` or label chip styling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote content → DOM | GitLab milestone `description` is remote markdown rendered into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-06 | Tampering (XSS) | `DescriptionsSection` rendering `matchedMilestone.description` via ReactMarkdown | mitigate | Move the `ReactMarkdown remarkPlugins={[remarkGfm]}` usage verbatim; do NOT add `rehype-raw` or `dangerouslySetInnerHTML` — the existing default (raw HTML disabled) is the control and must be preserved unchanged |
| T-87-07 | Tampering | GitLab label `color` / `text_color` interpolated into an inline `style` object in `LabelSummarySection` | accept | Pre-existing behavior, values are React style-object properties (not a raw CSS string), and this phase makes no change to it |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `cd taskflow && npm run check` shows zero NEW errors beyond the 2-error `BacklogPage.tsx`/`BacklogRow.tsx` baseline
- `cd taskflow && npm test -- ReleasesTab.test.tsx UpcomingReleasesTimeline.test.tsx releaseSummaries.test.ts` exits 0
- No file in `release-detail/` other than `useReleaseDetail.ts` contains `useQuery`, `useNavigate`, `useAuthStore`, or `useBreadcrumbStore`
</verification>

<success_criteria>
- Five new files exist in `release-detail/`, all named exports, all with `interface XxxProps` above the component (except `MetaRow`, which keeps its inline prop type)
- `MetaRow.tsx` preserves `min-w-0` and does not import from `issue-detail/`
- The breadcrumb header still renders while the skeleton is showing
- No `release-detail/index.ts` barrel is created
- Rendered markup for all five extracted blocks is unchanged
</success_criteria>

<output>
Create `.planning/phases/87-release-detail-decomposition/87-03-SUMMARY.md` when done. Record the D-01 deviation: `ReleaseHeader.tsx` exports two components (`ReleaseBreadcrumbHeader`, `ReleaseTitleHeading`) rather than one, because the breadcrumb and title occupy structurally separate positions in the JSX tree.
</output>
