---
phase: 87-release-detail-decomposition
plan: 02
type: execute
wave: 2
depends_on: ["87-01"]
files_modified:
  - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
autonomous: true
requirements: [FOUND-01]

must_haves:
  truths:
    - "All 6 useQuery calls execute from useReleaseDetail.ts, not from the page component"
    - "Query keys, staleTime and enabled guards are byte-identical to their pre-refactor form, preserving cache sharing with ReleasesTab and UpcomingReleasesTimeline"
    - "The page renders the same data it did before, sourced from the hook's returned object"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts"
      provides: "Single data-layer hook: 6 queries + gitlab token effect + derived values (D-07)"
      contains: "export function useReleaseDetail"
  key_links:
    - from: "taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts"
      to: "./releaseSummaries"
      via: "direct relative import of the pure computations"
      pattern: "from '\\./releaseSummaries'"
    - from: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      to: "./release-detail/useReleaseDetail"
      via: "single hook call replacing all inline queries"
      pattern: "useReleaseDetail\\("
    - from: "taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts"
      to: "@/services/jira"
      via: "imports fetchVersionIssueCounts / fetchFixVersionIssues moved in Plan 01"
      pattern: "fetchVersionIssueCounts"
---

<objective>
Move the entire data layer out of `ReleaseDetailPage.tsx` into `release-detail/useReleaseDetail.ts`: the 6 `useQuery` calls, the GitLab-token `useEffect`, and the derived values now computed by `releaseSummaries.ts` (Plan 01). Delete the page's now-dead local fetchers and inline computations.

Purpose: D-07 — one obvious place for release data. This is the largest single behavior-risk step in the phase because query keys are a silent cross-component cache contract (D-11).

Output: `useReleaseDetail.ts` plus a page shell that fetches nothing itself.

Scope note: the page's JSX is NOT restructured in this plan. Sections stay inline; only their data sources change from local `const`s to destructured hook fields. Edit-modal state (`editing`, `edit*`, `handleSave`, `startEditing`, `cancelEditing`, `buildJiraDiff`, `buildGitlabDiff`) STAYS in the page shell — it moves in Plan 05.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-release-detail-decomposition/87-CONTEXT.md
@.planning/phases/87-release-detail-decomposition/87-RESEARCH.md
@.planning/phases/87-release-detail-decomposition/87-PATTERNS.md
@.planning/phases/87-release-detail-decomposition/87-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create release-detail/useReleaseDetail.ts with all 6 queries carried over verbatim</name>
  <files>taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts</files>
  <read_first>
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx (L142-200 store reads + token effect, L200-260 queries 1-3, L262-392 match/derive/queries 4-6, L394-475 remaining derived values)
    - taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts (the exported signatures created in Plan 01)
    - taskflow/src/routes/dashboard/issue-detail/useLinkedMRs.ts (the co-located feature-hook analog: named export, store destructure, useQuery, return one flat object)
    - .planning/phases/87-release-detail-decomposition/87-RESEARCH.md (§3 — the verbatim query inventory and cross-consumer key cross-checks)
  </read_first>
  <action>
    Create `taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` — co-located beside its section files, NOT in `src/hooks/` (D-10; `src/hooks/` is reserved for cross-feature hooks like `useResizable`) — with a single named export `export function useReleaseDetail(versionId: string | undefined)`. No default export.

    Move into the hook, in this exact order (order matters — query 5 depends on `gitlabMatch`, query 6 depends on `missingRows` which derives from query 4):

    1. Store reads: `const { jiraBaseUrl, activeJiraProject, gitlabBaseUrl, activeGitlabProject } = useAuthStore()` and `const storyPointsFieldKey = useSettingsStore((s) => s.storyPointsFieldKey)`.
    2. `const [gitlabToken, setGitlabToken] = useState<string | null>(null)` plus the `useEffect` on `[gitlabBaseUrl]` that calls `readSecret('gitlab-pat')`. Keep it a `useEffect` — do NOT convert it to a `useQuery` (hazard 8; that would change caching/loading semantics).
    3. Query 1 `fixVersions` — key `['jira-fix-versions', activeJiraProject]`, `staleTime: 5 * 60_000`, `enabled: !!jiraBaseUrl && !!activeJiraProject`. Also capture `isLoading`.
    4. `const version = fixVersions?.find((v) => v.id === versionId) ?? null`.
    5. Query 2 `issueCounts` — key `['jira-version-counts', versionId]`.
    6. `const milestoneWindow = computeMilestoneWindow(version?.releaseDate)`.
    7. Query 3 `milestones` — key `['gitlab-milestones', activeGitlabProject, milestoneWindow?.from, milestoneWindow?.to]`, `enabled: ... && milestoneWindow !== null`.
    8. `const { gitlabMatch, matchedMilestone } = resolveGitLabMatch(version?.releaseDate, milestones)`.
    9. Query 4 `fixVersionIssues` — key `['jira-fixversion-issues', versionId, storyPointsFieldKey]`. Also capture `isLoading: isLoadingIssues`.
    10. Query 5 `milestoneMRs` — key `['gitlab-milestone-mrs', activeGitlabProject, gitlabMatch.candidateName]`.
    11. `const releaseIssues = fixVersionIssues ?? []`, `const releaseMrs = milestoneMRs ?? []`, then `const { matchedRows, unmatchedMRs } = matchIssuesToMRs(releaseIssues, releaseMrs)` and `const missingRows = matchedRows.filter((r) => r.mr === null)`.
    12. Query 6 `recentProjectMRs` — key `['gitlab-recent-project-mrs', activeGitlabProject]`, `enabled: !!gitlabBaseUrl && !!activeGitlabProject && !!gitlabToken && gitlabMatch.type !== 'none' && missingRows.length > 0`.
    13. `const wrongMilestoneByKey = buildWrongMilestoneMap(matchedMilestone, recentProjectMRs, missingRows)`.
    14. The remaining derived values, each a direct call into `releaseSummaries.ts`: `labelSummary`, `labelCoverage`, `mrStateCounts`, `issueStatusCounts`, `storyPoints` (via `computeStoryPoints(releaseIssues, storyPointsFieldKey)`), `hasStoryPoints` (via `computeHasStoryPoints(...)`).

    D-11 IS A HARD CONSTRAINT with no compile-time or test-time signal. Copy every `queryKey` array, `staleTime`, `enabled` expression, and `queryFn` body character-for-character from the page — including each query's own duplicated `await readSecret('jira-pat').catch(() => null)` + `throw new Error('No credentials')` preamble. Do NOT hoist those `readSecret` calls into a shared helper (hazard 10, D-16). Do NOT reorder key members. Do NOT change `5 * 60_000` to `300_000`. `jira-fix-versions`, `jira-version-counts` and `gitlab-milestones` are live shared-cache keys read by `ReleasesTab.tsx` and `UpcomingReleasesTimeline.tsx`.

    Import `fetchVersionIssueCounts` and `fetchFixVersionIssues` from `@/services/jira` (Plan 01 landed them there), alongside the existing `fetchFixVersions`. Do NOT import `fetch` from `@tauri-apps/plugin-http` into this file.

    Do NOT add `useMemo` anywhere. None of these values are memoized today; every one recomputes on each render and must continue to.

    Return one flat object containing: `version`, `isLoading`, `issueCounts`, `milestones`, `milestoneWindow`, `gitlabMatch`, `matchedMilestone`, `fixVersionIssues`, `isLoadingIssues`, `milestoneMRs`, `releaseIssues`, `releaseMrs`, `matchedRows`, `unmatchedMRs`, `missingRows`, `wrongMilestoneByKey`, `labelSummary`, `labelCoverage`, `mrStateCounts`, `issueStatusCounts`, `storyPoints`, `hasStoryPoints`, `gitlabToken`, `jiraBaseUrl`, `activeJiraProject`, `gitlabBaseUrl`, `activeGitlabProject`, `storyPointsFieldKey`.

    Do NOT edit `ReleaseDetailPage.tsx` in this task.
  </action>
  <acceptance_criteria>
    - `grep -c "export function useReleaseDetail" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 1
    - `grep -c "useQuery(" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 6
    - Each of these exact key literals appears once in the hook file: `'jira-fix-versions'`, `'jira-version-counts'`, `'gitlab-milestones'`, `'jira-fixversion-issues'`, `'gitlab-milestone-mrs'`, `'gitlab-recent-project-mrs'`
    - `grep -c "5 \* 60_000" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 6
    - `grep -c "readSecret('jira-pat')" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 3 (one per Jira query — NOT consolidated)
    - `grep -c "useMemo" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns 0
    - `grep -nE "from '@tauri-apps/plugin-http'" taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts` returns no match
    - Hook file contains `readSecret('gitlab-pat')` inside a `useEffect` with dependency array `[gitlabBaseUrl]`
    - `cd taskflow && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit</automated>
  </verify>
  <done>`useReleaseDetail.ts` runs all 6 queries with verbatim keys/staleTime/enabled, owns the GitLab token effect, calls into `releaseSummaries.ts` for every derived value, and typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire ReleaseDetailPage.tsx onto useReleaseDetail and delete the inline data layer</name>
  <files>taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx (full file — you are deleting from and rewiring it)
    - taskflow/src/routes/dashboard/release-detail/useReleaseDetail.ts (the hook created in Task 1 — its exact return shape)
    - .planning/phases/87-release-detail-decomposition/87-RESEARCH.md (§9 hazards 7, 8, 9, 10, 11)
  </read_first>
  <action>
    Delete from `ReleaseDetailPage.tsx`:
    - The module-level `interface VersionIssueCounts` and both local fetcher functions (`fetchVersionIssueCounts` L68-96, `fetchFixVersionIssues` L100-138) plus the `// ---- Issue count fetching ...` and `// ---- Fetch Jira issues for a fix version ----` banner comments.
    - The `import { fetch } from '@tauri-apps/plugin-http'` line, now unused.
    - All 6 `useQuery` calls, the `gitlabToken` state + its `useEffect`, and every derived `const`/IIFE from L226-475 (`MILESTONE_LEEWAY_DAYS`, `milestoneWindow`, `gitlabMatch`/`matchedMilestone`, `releaseIssues`, `releaseMrs`, `releaseIssueKeySet`, `releaseMrByIssue`, `releaseUnmatched`, `matchedRows`, `unmatchedMRs`, `missingRows`, `wrongMilestoneByKey`, `labelMap`, `labelSummary`, `labelCoverage`, `mrStateCounts`, `issueStatusCounts`, `issueStoryPoints`, `storyPoints`, `hasStoryPoints`).
    - Any import that becomes unused as a result (`fetchProjectMilestonesInRange`, `fetchRecentProjectMRs`, `fetchMilestoneMRs`, `fetchFixVersions`, `matchGitLabToFixVersion`, `linkMRToTask`, `readSecret` — but ONLY those still unreferenced after the edit; `readSecret` is still used inside `handleSave`, and `updateFixVersion`/`updateMilestone`/`extractTicketKeys` remain in use).

    Replace them with a single call: destructure everything the JSX and `handleSave`/`startEditing` need from `useReleaseDetail(versionId)`.

    KEEP in the shell, unchanged:
    - `useParams`, `useNavigate`, `useQueryClient`, `useOutletContext`, `useBreadcrumbStore` reads, `usePinnedTabsStore`'s five selector reads, `useSettingsStore`'s `releaseDetailPanelWidth`/`setReleaseDetailPanelWidth`.
    - `containerRef` + the `useResizable({ ... max: () => (containerRef.current?.offsetWidth ?? 800) * 0.5 ... })` call and `handleHovered` state — `containerRef` is attached to the outer flex wrapper that spans BOTH columns (hazard 7), so this hook cannot move into `useReleaseDetail`.
    - All edit-modal state and handlers (`editing`, the six `edit*` states, `jiraError`, `gitlabError`, `isSaving`, `startEditing`, `cancelEditing`, `buildJiraDiff`, `buildGitlabDiff`, `isEditDirty`, `isMilestoneTitleInvalid`, `handleSave` and its four `queryClient.invalidateQueries` calls). These move in Plan 05, not now.
    - `seedReleaseBreadcrumb`, `openIssueFull`, `handleBack`, and every JSX block exactly as-is.

    HOOK-ORDER SAFETY (hazard 9): `useReleaseDetail(versionId)` must be called unconditionally near the top of the component, above every `useState`/`useEffect`/`useResizable` or in the same unconditional block. The `if (!versionId) return null;` guard at L649 must remain BELOW all hook calls. Never place a hook call after a conditional return.

    Do NOT change any JSX. Do not rename any variable the JSX reads — if the hook returns `matchedRows`, destructure it as `matchedRows`, not `rows`. The diff inside the `return (...)` block should be empty except where a deleted local is now a destructured hook field with the identical name.
  </action>
  <acceptance_criteria>
    - `grep -c "useQuery(" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0
    - `grep -c "useReleaseDetail(" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 1
    - `grep -c "async function fetchVersionIssueCounts\|async function fetchFixVersionIssues" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0
    - `grep -nE "from '@tauri-apps/plugin-http'" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns no match
    - `grep -c "matchGitLabToFixVersion\|linkMRToTask" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0
    - `grep -c "MILESTONE_LEEWAY_DAYS" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0
    - `wc -l taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` is under 1150 (down from 1518)
    - `grep -c "useResizable(" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 1 (stayed in the shell)
    - `grep -c "handleSave" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` is at least 2 (edit logic still in the shell — not moved in this plan)
    - `git diff -U0 taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` shows no additions or deletions of JSX tags (only imports, hook wiring, and deleted logic change)
    - `cd taskflow && npx tsc --noEmit` exits 0
    - `cd taskflow && npm test -- ReleasesTab.test.tsx UpcomingReleasesTimeline.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit && npm test -- ReleasesTab.test.tsx UpcomingReleasesTimeline.test.tsx releaseSummaries.test.ts</automated>
  </verify>
  <done>The page fetches nothing itself; all data arrives from a single `useReleaseDetail(versionId)` call. JSX is unchanged. Typecheck and the two consumer suites pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Jira/GitLab REST | PAT/token reads via `readSecret` cross into query functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-04 | Information Disclosure | `readSecret('jira-pat')` / `readSecret('gitlab-pat')` relocated into `useReleaseDetail.ts` | mitigate | Tokens stay in function-local scope and the existing `gitlabToken` `useState`; do not add them to any query key, log, or the hook's returned object beyond the pre-existing `gitlabToken` field |
| T-87-05 | Tampering | React Query cache keys shared with ReleasesTab/UpcomingReleasesTimeline | mitigate | Verbatim key copy enforced by grep gates in Task 1 acceptance criteria; manual cache-sharing check recorded in 87-VALIDATION.md Manual-Only Verifications |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` exits 0
- `cd taskflow && npm test -- ReleasesTab.test.tsx UpcomingReleasesTimeline.test.tsx releaseSummaries.test.ts` exits 0
- `cd taskflow && npm run check` shows zero NEW errors beyond the 2-error `BacklogPage.tsx`/`BacklogRow.tsx` baseline
- `grep -c "useQuery(" taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` returns 0
</verification>

<success_criteria>
- All 6 queries live in `useReleaseDetail.ts` with verbatim keys, staleTime and enabled guards
- The page component contains zero `useQuery` calls and zero raw fetch plumbing
- `useResizable`/`containerRef` and edit-modal state remain in the shell
- JSX output is unchanged
</success_criteria>

<output>
Create `.planning/phases/87-release-detail-decomposition/87-02-SUMMARY.md` when done. Record the hook's exact return-object field list so Plans 03-06 can wire section props without re-reading the hook.
</output>
