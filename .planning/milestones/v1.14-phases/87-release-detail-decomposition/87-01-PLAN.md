---
phase: 87-release-detail-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts
  - taskflow/src/routes/dashboard/release-detail/releaseSummaries.test.ts
  - taskflow/src/services/jira.ts
autonomous: true
requirements: [FOUND-01]

must_haves:
  truths:
    - "The 11 derived release computations exist as React-free exported functions callable outside a component"
    - "releaseSummaries.test.ts asserts the six non-obvious edge cases enumerated in 87-VALIDATION.md Wave 0"
    - "fetchVersionIssueCounts and fetchFixVersionIssues are importable from @/services/jira"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts"
      provides: "Pure derived-computation module (D-09)"
      contains: "export function computeLabelCoverage"
    - path: "taskflow/src/routes/dashboard/release-detail/releaseSummaries.test.ts"
      provides: "Wave 0 unit coverage (D-14)"
      contains: "describe("
    - path: "taskflow/src/services/jira.ts"
      provides: "fetchVersionIssueCounts + fetchFixVersionIssues on the service layer (D-12)"
      contains: "export async function fetchVersionIssueCounts"
  key_links:
    - from: "taskflow/src/routes/dashboard/release-detail/releaseSummaries.test.ts"
      to: "./releaseSummaries"
      via: "direct relative import, no render(), no mocks"
      pattern: "from '\\./releaseSummaries'"
    - from: "taskflow/src/services/jira.ts"
      to: "@/lib/apiFetch"
      via: "apiFetch('jira', ...) in both moved fetchers (D-12a)"
      pattern: "apiFetch\\('jira'"
---

<objective>
Land the three artifacts that do NOT require editing `ReleaseDetailPage.tsx`: the pure derived-logic module, its unit tests, and the two Jira fetchers on the service layer.

Purpose: this is Wave 0/Wave 1 groundwork. `releaseSummaries.test.ts` is the phase's only new automated coverage (D-14) and must exist before the extraction plans that depend on it. Doing this without touching the page file means Plan 02 starts from an unmodified monolith.

Output: `release-detail/releaseSummaries.ts`, `release-detail/releaseSummaries.test.ts`, and two new exported functions in `services/jira.ts`.

Scope note (D-16): this plan ADDS code only. `ReleaseDetailPage.tsx` is NOT edited — its inline copies of these computations and fetchers stay in place and remain the live code path until Plan 02 deletes them. Temporary duplication between this plan and Plan 02 is intended and must not be "cleaned up" here.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-release-detail-decomposition/87-CONTEXT.md
@.planning/phases/87-release-detail-decomposition/87-RESEARCH.md
@.planning/phases/87-release-detail-decomposition/87-PATTERNS.md
@.planning/phases/87-release-detail-decomposition/87-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create release-detail/releaseSummaries.ts as a React-free pure module</name>
  <files>taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts</files>
  <read_first>
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx (read L1-60 for imports/types and L226-475 for every computation body being moved)
    - taskflow/src/services/releaseLinker.ts (the exact analog per 87-PATTERNS.md: module doc comment, named exports, JSDoc @param/@returns, zero React imports)
    - .planning/phases/87-release-detail-decomposition/87-RESEARCH.md (§4 — the verified 9-computation inventory with source line ranges)
  </read_first>
  <action>
    Create the new folder `taskflow/src/routes/dashboard/release-detail/` and add `releaseSummaries.ts`.

    Copy each computation body VERBATIM from `ReleaseDetailPage.tsx` — same branching, same comments, same tie-breaks. The only permitted change is converting closure reads into explicit function parameters. Do NOT add `useMemo`, do NOT simplify, do NOT reorder branches, do NOT rename local variables inside the bodies.

    Export exactly these symbols with these exact names and signatures:

    - `MILESTONE_LEEWAY_DAYS` — const `7` (currently page L227)
    - `computeMilestoneWindow(releaseDate: string | null | undefined): { from: string; to: string } | null` — from page L228-240, including the inner `addDays` helper using `new Date(d)` / `setDate` / `toISOString().slice(0, 10)`
    - `resolveGitLabMatch(releaseDate: string | null | undefined, milestones: GitLabMilestone[] | undefined): { gitlabMatch: ReleaseMatch; matchedMilestone: GitLabMilestone | null }` — from page L265-308, preserving the `noMatch` sentinel `{ type: 'none', candidateName: '', candidateUrl: '' }`, the `break` on first exact match, and the closest-`due_date` fuzzy tie-break using `Number.POSITIVE_INFINITY` for unparseable dates
    - `matchIssuesToMRs(releaseIssues: JiraIssue[], releaseMrs: GitLabMR[]): { matchedRows: Array<{ issue: JiraIssue; mr: GitLabMR | null }>; unmatchedMRs: GitLabMR[] }` — from page L337-355, keeping the `releaseIssueKeySet` / `releaseMrByIssue` / `releaseUnmatched` intermediates and the `linkMRToTask` call
    - `buildWrongMilestoneMap(matchedMilestone: GitLabMilestone | null, recentProjectMRs: GitLabMR[] | undefined, missingRows: Array<{ issue: JiraIssue; mr: GitLabMR | null }>): Map<string, GitLabMR>` — from page L377-392, preserving the `matchedMilestone && recentProjectMRs` guard (returns an empty Map otherwise), the per-row single-key `Set`, and the `mr.milestone == null || mr.milestone.id !== matchedMilestone.id` predicate
    - `computeLabelSummary(releaseMrs: GitLabMR[]): Array<{ label: { name: string; color: string; text_color: string }; count: number }>` — from page L394-412, preserving the count-desc then `localeCompare` alphabetical tie-break
    - `export interface LabelCoverage { total: number; labeled: number; unlabeled: GitLabMR[]; allLabeled: boolean }` and `computeLabelCoverage(releaseMrs: GitLabMR[]): LabelCoverage | null` — from page L415-424, preserving the `releaseMrs.length === 0` → `null` early return
    - `computeMrStateCounts(releaseMrs: GitLabMR[]): { merged: number; opened: number; closed: number }` — from page L427-438, preserving the else-bucket that folds `closed` and `locked` into `closed`
    - `computeIssueStatusCounts(releaseIssues: JiraIssue[]): { new: number; indeterminate: number; done: number }` — from page L441-451, preserving the `else` fallback to `new` for unknown `statusCategory.key`
    - `issueStoryPoints(issue: JiraIssue, storyPointsFieldKey: string): number | null` — from page L453-456; `storyPointsFieldKey` becomes the second parameter instead of a closure read (hazard 11)
    - `computeStoryPoints(releaseIssues: JiraIssue[], storyPointsFieldKey: string): { total: number; completed: number }` — from page L457-470
    - `computeHasStoryPoints(releaseIssues: JiraIssue[], storyPointsFieldKey: string): boolean` — from page L471-475, preserving `sp !== null && sp > 0` (a `0` does NOT count)

    Imports allowed: `import type { GitLabMilestone, GitLabMR } from '@/services/gitlab'`, `import type { JiraIssue } from '@/services/jira'`, `import type { ReleaseMatch } from '@/services/releaseLinker'`, `import { matchGitLabToFixVersion } from '@/services/releaseLinker'`, `import { linkMRToTask } from '@/services/linkEngine'`. NOTHING from `react`, `@tanstack/react-query`, or `@/stores/*`.

    Add a file-top doc comment block (mirroring `releaseLinker.ts` L1-16) that documents the two non-obvious rules flagged in RESEARCH §4/§8: `computeHasStoryPoints` requires `sp > 0` (not `sp !== null`), and `computeIssueStatusCounts` folds an unknown `statusCategory.key` into the `new` bucket. Give each exported function a JSDoc `@param`/`@returns` block in `releaseLinker.ts` style.
  </action>
  <acceptance_criteria>
    - File `taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts` exists
    - `grep -c "^export " taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts` returns at least 12
    - Each of these strings appears in the file: `computeMilestoneWindow`, `resolveGitLabMatch`, `matchIssuesToMRs`, `buildWrongMilestoneMap`, `computeLabelSummary`, `computeLabelCoverage`, `computeMrStateCounts`, `computeIssueStatusCounts`, `issueStoryPoints`, `computeStoryPoints`, `computeHasStoryPoints`, `MILESTONE_LEEWAY_DAYS`
    - `grep -nE "from 'react'|react-query|@/stores/" taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts` returns no matches (React-free, D-09)
    - `grep -c "useMemo" taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts` returns 0
    - File contains `sp !== null && sp > 0` and `releaseMrs.length === 0` (verbatim preservation of the two guard semantics)
    - `taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` is unchanged by this task — `git diff --name-only` does not list it
    - `cd taskflow && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit</automated>
  </verify>
  <done>`releaseSummaries.ts` exports all 11 computations plus `MILESTONE_LEEWAY_DAYS` and `LabelCoverage`, imports nothing from React/stores/react-query, and typechecks clean. `ReleaseDetailPage.tsx` untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write releaseSummaries.test.ts covering the Wave 0 edge cases</name>
  <files>taskflow/src/routes/dashboard/release-detail/releaseSummaries.test.ts</files>
  <read_first>
    - taskflow/src/routes/dashboard/release-detail/releaseSummaries.ts (created in Task 1 — the module under test)
    - taskflow/src/services/releaseLinker.test.ts (the exact style analog: plain describe/it/expect from 'vitest', no render(), no QueryClientProvider, no mocks)
    - .planning/phases/87-release-detail-decomposition/87-VALIDATION.md (§Wave 0 Requirements — the authoritative edge-case list)
  </read_first>
  <behavior>
    - `computeLabelSummary([])` returns `[]`
    - `computeLabelSummary` omits MRs whose `labels` array is empty from the result map
    - `computeLabelSummary` breaks equal counts alphabetically by `label.name` (e.g. counts 2/2 for `zebra`/`alpha` → `alpha` first)
    - `computeLabelCoverage([])` returns `null` (not an object with `total: 0`)
    - `computeLabelCoverage` with MRs where none carry labels returns `allLabeled: false` and `unlabeled.length === total`
    - `computeMrStateCounts` buckets `merged` → merged, `opened` → opened, and both `closed` AND `locked` → closed
    - `computeStoryPoints` excludes issues whose SP field is `null`, `undefined`, or a non-number from `total`
    - `computeStoryPoints` adds to `completed` only when `statusCategory.key === 'done'`
    - `computeHasStoryPoints` returns `false` when every issue's SP is `0` (proves `> 0`, not `!== null`)
    - `computeIssueStatusCounts` counts an unknown `statusCategory.key` (e.g. `'wat'`) and a missing `statusCategory` in the `new` bucket
    - `computeMilestoneWindow(undefined)` and `computeMilestoneWindow(null)` return `null`
    - `computeMilestoneWindow('2026-03-05')` rolls back across the month boundary to `'2026-02-26'` for `from` and `'2026-03-12'` for `to`
  </behavior>
  <action>
    Create `releaseSummaries.test.ts` beside the module. Follow `releaseLinker.test.ts` exactly: `import { describe, expect, it } from 'vitest'`, direct relative import `from './releaseSummaries'`, one top-level `describe` per exported function under test, `it()` names written as plain-English behavior statements. No `render()`, no `QueryClientProvider`, no `vi.mock` — the module is pure.

    Build minimal inline object literals cast to the needed types (e.g. `as unknown as GitLabMR`) rather than importing fixtures; only the fields each function actually reads need to be present (`labels`, `state`, `milestone` for MRs; `key`, `fields.status.statusCategory.key`, and the story-point field for issues).

    Cover every bullet in the `<behavior>` block above. Verify the month-boundary expectation by computing it, not by asserting whatever the implementation happens to return.

    Do NOT write tests for `resolveGitLabMatch`, `matchIssuesToMRs`, or `buildWrongMilestoneMap` — those wrap `matchGitLabToFixVersion`/`linkMRToTask`, which are already covered by `releaseLinker.test.ts`, and adding coverage there is beyond D-14's stated scope.
  </action>
  <acceptance_criteria>
    - `cd taskflow && npm test -- releaseSummaries.test.ts` exits 0 with at least 12 passing assertions-bearing `it` blocks
    - `grep -c "render(" taskflow/src/routes/dashboard/release-detail/releaseSummaries.test.ts` returns 0
    - `grep -c "vi.mock" taskflow/src/routes/dashboard/release-detail/releaseSummaries.test.ts` returns 0
    - Test file contains a case asserting `computeLabelCoverage([])` is `null` (an assertion for `toBeNull` on that call)
    - Test file contains a `'locked'` MR state case landing in the `closed` bucket
    - Test file contains a case where every issue's story points is `0` and `computeHasStoryPoints` is expected `false`
    - Test file asserts `computeMilestoneWindow('2026-03-05')` yields `from: '2026-02-26'`
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && npm test -- releaseSummaries.test.ts</automated>
  </verify>
  <done>`npm test -- releaseSummaries.test.ts` is green and covers every Wave 0 edge case listed in 87-VALIDATION.md.</done>
</task>

<task type="auto">
  <name>Task 3: Move the two Jira fetchers into services/jira.ts using apiFetch</name>
  <files>taskflow/src/services/jira.ts</files>
  <read_first>
    - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx (L60-138 — the two fetcher bodies and the `VersionIssueCounts` interface being moved)
    - taskflow/src/services/jira.ts (read L1080-1140 for `fetchFixVersions`, the target-pattern neighbour, and the file's import block)
    - taskflow/src/lib/apiFetch.ts (understand the 15s AbortController timeout and 401 markDisconnected behavior being adopted)
    - .planning/phases/87-release-detail-decomposition/87-PATTERNS.md (§`src/services/jira.ts` (MODIFIED) — the explicit "do NOT copy the ApiError shape wholesale" constraints)
  </read_first>
  <action>
    Add `fetchVersionIssueCounts` and `fetchFixVersionIssues` to `taskflow/src/services/jira.ts` (the LEGACY single file — NOT `services/jira/`; all ~60 imports point at `jira.ts`). Place both immediately after `fetchFixVersions` for discoverability, with `fetchFixVersions`-style JSDoc `@param`/`@returns` blocks.

    Also add and export `export interface VersionIssueCounts { issuesFixed: number; issuesTotal: number }` (moved from `ReleaseDetailPage.tsx` L62-65).

    Copy both function bodies verbatim from the page, with EXACTLY these changes and no others:

    `fetchVersionIssueCounts(baseUrl: string, token: string, versionId: string): Promise<VersionIssueCounts>`
    - Replace the two raw `fetch(totalUrl, { headers })` / `fetch(doneUrl, { headers })` calls with `apiFetch('jira', totalUrl, { headers }, 'Load Release Issue Counts')` and `apiFetch('jira', doneUrl, { headers }, 'Load Release Issue Counts')`.
    - KEEP the `Promise.allSettled` wrapper, KEEP the per-request `r.ok ? r.json() : { total: 0 }` ternary, KEEP the `?? 0` fallbacks. This function must retain its current contract: it never throws on an HTTP failure, it always resolves to a counts object.
    - Do NOT add an `ApiError` branch, do NOT add a `response.ok` throw, do NOT add a try/catch around `apiFetch`.

    `fetchFixVersionIssues(baseUrl: string, token: string, versionId: string, storyPointsFieldKey: string): Promise<JiraIssue[]>`
    - Replace the single raw `fetch(url, { headers })` with `apiFetch('jira', url, { headers }, 'Load Release Issues')`.
    - KEEP the `while (true)` pagination loop, `maxResults = 200`, the `startAt` advance, the `allIssues.length >= data.total || allIssues.length === before` break condition, and the exact error string `` `Failed to fetch issues: status ${resp.status}` `` thrown as a plain `Error`.
    - Do NOT convert that error to `ApiError`, do NOT add 401/403 special-casing.

    Both functions: preserve `/^\d+$/.test(versionId)` guard and its `` `Invalid versionId: ${versionId}` `` error verbatim; preserve the `new Set([...])` story-point-field dedup and the field list `['summary', 'status', 'assignee', 'issuetype', ...]` verbatim; preserve the `baseUrl.replace(/\/$/, '')` normalization.

    Do NOT import `fetch` from `@tauri-apps/plugin-http` into `jira.ts` — all 33 existing functions in the file use `apiFetch` and zero use raw fetch; keep that invariant.

    DELIBERATE BEHAVIOR CHANGE (D-12a, user-approved — state this in the SUMMARY, do not hide it): both functions gain a hard 15-second request timeout they did not have before, and both now trigger `markDisconnected('jira')` on a 401 response. This is the only intentional behavior delta in Phase 87.

    Do NOT edit `ReleaseDetailPage.tsx` in this task. Its local copies remain the live code path until Plan 02.
  </action>
  <acceptance_criteria>
    - `grep -c "export async function fetchVersionIssueCounts" taskflow/src/services/jira.ts` returns 1
    - `grep -c "export async function fetchFixVersionIssues" taskflow/src/services/jira.ts` returns 1
    - `grep -c "export interface VersionIssueCounts" taskflow/src/services/jira.ts` returns 1
    - `grep -c "apiFetch('jira'" taskflow/src/services/jira.ts` returns 36 (was 33; +2 in fetchVersionIssueCounts, +1 in fetchFixVersionIssues)
    - `grep -nE "from '@tauri-apps/plugin-http'" taskflow/src/services/jira.ts` returns no match
    - `jira.ts` contains the literal strings `Failed to fetch issues: status`, `/^\d+$/.test(versionId)`, and `Promise.allSettled`
    - `jira.ts` does NOT contain `ApiError` inside either new function body (verify by reading the two function bodies)
    - `taskflow/src/routes/dashboard/ReleaseDetailPage.tsx` is not listed in `git diff --name-only`
    - `cd taskflow && npx tsc --noEmit` exits 0
    - `cd taskflow && npm test -- ReleasesTab.test.tsx` exits 0 (its `@tauri-apps/plugin-http` fetch mock still satisfies apiFetch — RESEARCH §8)
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit && npm test -- ReleasesTab.test.tsx UpcomingReleasesTimeline.test.tsx</automated>
  </verify>
  <done>Both fetchers live in `services/jira.ts` behind `apiFetch('jira', ...)`, keep their original fallback/error contracts, typecheck clean, and the two consumer suites still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Jira REST API | PAT Bearer token crosses here; `versionId` is interpolated into a JQL string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Tampering | `versionId` interpolated into JQL in both moved fetchers | mitigate | Preserve the `/^\d+$/.test(versionId)` guard verbatim (Task 3); it is the existing injection control and must not be relaxed during the move |
| T-87-02 | Information Disclosure | PAT in `Authorization` header, devtools capture | mitigate | Adopting `apiFetch('jira', ...)` routes both calls through the wrapper that redacts `Authorization`/`PRIVATE-TOKEN` in devtools instrumentation — a net improvement over the raw `fetch` calls |
| T-87-03 | Denial of Service | Unbounded `fetchFixVersionIssues` pagination against a hung Jira | mitigate | `apiFetch`'s 15s `AbortController` timeout now bounds each page request (D-12a, deliberate) |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — no new dependencies are added by any task |
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` exits 0
- `cd taskflow && npm test -- releaseSummaries.test.ts ReleasesTab.test.tsx UpcomingReleasesTimeline.test.tsx` exits 0
- `cd taskflow && npm run check` reports no more than the 2 pre-existing baseline errors in `BacklogPage.tsx` / `BacklogRow.tsx` — zero NEW errors
- `git diff --name-only` lists only `releaseSummaries.ts`, `releaseSummaries.test.ts`, and `services/jira.ts`
</verification>

<success_criteria>
- `release-detail/releaseSummaries.ts` exports 11 pure functions + `MILESTONE_LEEWAY_DAYS` + `LabelCoverage`, with zero React/store/react-query imports
- `releaseSummaries.test.ts` is green and covers all six Wave 0 edge-case groups from 87-VALIDATION.md
- `fetchVersionIssueCounts`, `fetchFixVersionIssues`, and `VersionIssueCounts` are exported from `@/services/jira` and use `apiFetch('jira', ...)`
- `ReleaseDetailPage.tsx` is byte-unchanged after this plan
- `npm run check` shows zero new errors relative to the 2-error baseline
</success_criteria>

<output>
Create `.planning/phases/87-release-detail-decomposition/87-01-SUMMARY.md` when done. Record explicitly: the D-12a behavior delta (15s timeout + 401 disconnect-marking now applied to both fetchers), and the exported symbol list of `releaseSummaries.ts` so Plan 02 can consume it without re-reading the file.
</output>
