---
phase: 10-sprint-board-redesign
plan: 04
type: execute
wave: 4
depends_on:
  - 10-03
files_modified: []
autonomous: false
requirements:
  - BOARD-01
  - BOARD-02
  - BOARD-03
  - BOARD-04
  - BOARD-05

must_haves:
  truths:
    - "Sprint board shows workflow-API columns (including empty ones) sorted by status category"
    - "All team members' cards are visible grouped under story headers"
    - "Dragging a card between columns transitions it immediately (optimistic) and reflects the change in Jira"
    - "Dragging to an invalid target column snaps the card back with an inline error"
    - "Clicking any card opens the issue detail sheet without the board losing state"
    - "The + Add button in a column creates a new Story in that column's status"
  artifacts:
    - path: "taskflow/src/routes/dashboard/SprintBoardTab.tsx"
      provides: "Fully rebuilt board — verified by human"
    - path: "taskflow/src/routes/dashboard/DraggableCard.tsx"
      provides: "Drag + click co-existence — verified by human"
    - path: "taskflow/src/routes/dashboard/QuickCreateInput.tsx"
      provides: "Inline create — verified by human"
  key_links:
    - from: "human tester"
      to: "sprint board"
      via: "manual drag interaction"
      pattern: "visual verification"
---

<objective>
Human verification of the completed sprint board redesign. All automated tests pass — this checkpoint confirms the visual and interactive behavior works correctly in the live app.

Purpose: Drag-and-drop, animated overlays, and Jira API transitions cannot be fully verified by jsdom tests. A human must confirm the board looks and feels correct with real data.
Output: Human sign-off on all five BOARD requirements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-sprint-board-redesign/10-CONTEXT.md
@.planning/phases/10-sprint-board-redesign/10-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Sprint Board Redesign:
    - Columns sourced from Jira workflow statuses API (not derived from cards); empty columns visible
    - Subtasks grouped as kanban cards under story header rows; a story appears in each column where it has subtasks
    - Stories with no subtasks appear as standalone draggable cards
    - All team members' work visible (not filtered to current user)
    - Drag-to-move status transitions with optimistic update and rollback on failure
    - Invalid drop targets visually disabled during drag
    - Inline "+ Add" creates a new Story in the clicked column
    - Clicking any card opens the issue detail sheet (board stays mounted)
  </what-built>
  <how-to-verify>
    1. Open the app and navigate to the Sprint Board tab
    2. BOARD-01: Confirm columns match the Jira workflow (not just statuses of current sprint cards). Check that empty columns are shown. Confirm subtasks appear as cards grouped under their parent story headers. Confirm a story with subtasks in two columns shows its header in both columns.
    3. BOARD-02: Confirm other team members' cards are visible (not just your own issues).
    4. BOARD-03: Drag a subtask card from one column to another. Confirm it moves immediately. Switch to Jira and confirm the transition was recorded. (Optional: if you can trigger a rejected transition, confirm the card snaps back with a brief error.)
    5. BOARD-04: Click "+ Add" at the bottom of a column. Type a short summary and press Enter. Confirm a new card appears (may need a moment for the board to re-fetch). Check Jira to confirm the issue was created.
    6. BOARD-05: Click any card on the board. Confirm the issue detail sheet slides open with the correct issue data. Confirm the board columns remain visible in the background (sheet doesn't navigate away). Close the sheet and confirm the board is still intact.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All five BOARD requirements confirmed working by human tester:
- BOARD-01: Grouped kanban with workflow API columns
- BOARD-02: All team members visible
- BOARD-03: Drag transitions with rollback
- BOARD-04: Inline quick-create
- BOARD-05: Issue detail sheet from board cards
</verification>

<success_criteria>
Human tester types "approved" after verifying all five BOARD behaviors in the live app.
</success_criteria>

<output>
After completion, create `.planning/phases/10-sprint-board-redesign/10-04-SUMMARY.md`
</output>
