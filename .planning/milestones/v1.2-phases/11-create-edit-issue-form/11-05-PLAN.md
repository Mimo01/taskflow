---
phase: 11-create-edit-issue-form
plan: 05
type: execute
wave: 5
depends_on: [11-04]
files_modified: []
autonomous: false

requirements: [CREATE-01, CREATE-02, CREATE-03, CREATE-04]

must_haves:
  truths:
    - "User can open Create Issue modal from the sidebar and successfully create a Story with summary, description, assignee, story points, priority, and epic link — issue appears on sprint board after creation"
    - "User can open Create Issue modal from the sidebar and create a Subtask — Parent field visible, Epic Link hidden"
    - "User can open Create Issue modal from IssueDetailContent '+ Add subtask' — modal opens pre-filled with Subtask type and correct parent key"
    - "Custom fields (including Account) appear in the create form after the skeleton resolves — Account field is rendered based on its schema type (not hardcoded)"
    - "User can open the Edit modal from any open issue (via Edit button in IssueDetailContent) and update summary, description, assignee, story points, priority, and epic link — changes reflected immediately in the detail panel"
    - "User can add one or more issue links (relates to / blocks / is blocked by) — link type dropdown shows Jira-discovered names; issue search finds real issues; links appear on the created/edited issue when viewed in the detail panel"
  artifacts:
    - path: "src/routes/dashboard/CreateEditIssueModal.tsx"
      provides: "Verified working create and edit flows"
    - path: "src/routes/dashboard/IssueLinkRow.tsx"
      provides: "Verified working link type dropdown and issue search"
  key_links:
    - from: "Sidebar Create Issue button"
      to: "Jira issue created"
      via: "Modal submit → createIssue() → POST /rest/api/2/issue → cache invalidation → sprint board refresh"
    - from: "Edit button"
      to: "Jira issue updated"
      via: "Modal submit → bulkUpdateIssue() → PUT /rest/api/2/issue/{key} → ['jira-issue-detail', key] invalidation"
---

<objective>
Human verification that all four CREATE requirements work correctly against the live Orange Jira instance.

Purpose: The automated tests cover unit behavior, but create/edit flows require a live Jira instance to confirm createmeta resolves real fields, the Account custom field appears correctly, and issue links are actually created on the server.
Output: Human approval that the feature works end-to-end.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-create-edit-issue-form/11-CONTEXT.md
@.planning/phases/11-create-edit-issue-form/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification automated check</name>
  <files></files>
  <action>
    Run the full test suite to confirm everything is green before human verification:
    ```bash
    cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run
    cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit
    ```
    If any tests fail or TypeScript errors exist, fix them before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <done>All tests pass, zero TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — all CREATE requirements</name>
  <action>Start the app with `cd /Users/mimo/Desktop/Tasker/taskflow && npm run tauri dev` and verify all CREATE requirements manually against the live Orange Jira instance using the steps in how-to-verify.</action>
  <what-built>
    Phase 11 complete implementation:
    - CreateEditIssueModal (Dialog, type switcher, dynamic createmeta fields, DescriptionEditor, create/edit mutations)
    - IssueLinkRow (link type discovery, issue search, multiple rows)
    - Sidebar "Create Issue" button (second nav item, opens modal)
    - IssueDetailContent "Edit" button (opens edit modal pre-filled)
    - IssueDetailContent "+ Add subtask" button (opens create modal with Subtask type + parent)
    - AppLayout owns all modal state
  </what-built>
  <how-to-verify>
    Run the app: `cd /Users/mimo/Desktop/Tasker/taskflow && npm run tauri dev`

    **CREATE-01: Create a new Story**
    1. Click "Create Issue" in the sidebar (second nav item, after Dashboard)
    2. Verify modal opens centered (not slide-over)
    3. Type switcher at top shows Story/Subtask/Bug — confirm Story selected by default
    4. Fill: Summary = "Phase 11 verification story", Description = "*bold test*", Priority = Medium, Story Points = 3
    5. Set an assignee using the search (type a teammate's name, select from dropdown)
    6. Select an epic from the Epic Link dropdown
    7. Click Submit — confirm modal closes
    8. Open Sprint Board — confirm new issue appears in the correct column

    **CREATE-01: Create a Subtask**
    1. Click "Create Issue" in sidebar, switch type to Subtask
    2. Confirm: Parent field appears (required), Epic Link field disappears
    3. Enter a parent issue key (e.g. PROJ-100), fill Summary
    4. Submit — confirm Subtask created (check in Jira or issue detail)

    **CREATE-02: Custom fields (Account field)**
    1. Open Create Issue modal, select Story
    2. After a brief loading skeleton, confirm "Account" field appears in the form
    3. The Account field should be a dropdown or searchable input (based on its schema type)
    4. Confirm submit is blocked if Account is required and empty; unblocked after filling it

    **CREATE-03: Edit an existing issue**
    1. Open any issue from Sprint Board or My Tasks (click card to open IssueDetailSheet)
    2. Confirm "Edit" button visible in the issue header (alongside "Open in Jira")
    3. Click Edit — modal opens pre-filled with: summary, description (in Edit tab), assignee, priority, story points, epic link
    4. Change the summary to add " [edited]"
    5. Click Submit — confirm modal closes, issue detail panel refreshes with new summary

    **CREATE-01: Add Subtask from issue detail**
    1. Open any Story in IssueDetailSheet
    2. Scroll to Subtasks section — confirm "+ Add subtask" button visible
    3. Click it — modal opens in create mode, type=Subtask, parent pre-filled with the parent story key
    4. Fill summary, submit — confirm subtask appears in the subtask list

    **CREATE-04: Issue links**
    1. Open Create Issue modal, fill required fields
    2. Click "Add link" button — confirm a link row appears with type dropdown + search input
    3. Select a link type from the dropdown (e.g. "relates to") — confirm names come from Jira (not hardcoded)
    4. Type an issue key in the search input — confirm dropdown shows matching issues
    5. Select an issue — confirm the link row shows the selected issue key
    6. Add a second link row with a different link type and issue
    7. Submit — confirm both links appear when you open the new issue's detail panel
  </how-to-verify>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; automated verification not applicable</automated>
  </verify>
  <done>Human approved all CREATE requirements against live Jira instance.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All four CREATE requirements verified working against live Orange Jira instance.
</verification>

<success_criteria>
- CREATE-01: User can create story, subtask, and bug with all specified fields
- CREATE-02: Account field and other required custom fields appear from createmeta — dynamically discovered
- CREATE-03: Edit modal pre-fills and saves all six editable fields
- CREATE-04: Issue links added, link types from discovered list, appear in issue detail
- Human verification: approved
</success_criteria>

<output>
After completion, create `.planning/phases/11-create-edit-issue-form/11-05-SUMMARY.md`
</output>
