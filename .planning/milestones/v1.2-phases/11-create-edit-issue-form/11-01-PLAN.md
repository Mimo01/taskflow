---
phase: 11-create-edit-issue-form
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/dashboard/CreateEditIssueModal.test.tsx
  - src/services/jira.test.ts
  - src/services/jira.ts
autonomous: true
requirements: [CREATE-01, CREATE-02, CREATE-03, CREATE-04]

must_haves:
  truths:
    - "createIssue() accepts full field set (issuetype, description, assignee, priority, parent, epic link, story points, custom fields)"
    - "fetchCreatemeta() returns required fields array — tries new 8.4+ endpoint first, falls back to legacy flat endpoint on 404"
    - "bulkUpdateIssue() sends all changed fields in a single PUT to /rest/api/2/issue/{key}"
    - "fetchIssueLinkTypes() returns discovered link type array from /rest/api/2/issueLinkType"
    - "createIssueLink() posts to /rest/api/2/issueLink with correct body (type.id, inwardIssue.key, outwardIssue.key)"
    - "All Wave 0 test stubs exist as failing RED tests — test file for CreateEditIssueModal.test.tsx created, jira.test.ts extended"
  artifacts:
    - path: "src/routes/dashboard/CreateEditIssueModal.test.tsx"
      provides: "Wave 0 stubs for CREATE-01,02,03,04 component behavior"
    - path: "src/services/jira.test.ts"
      provides: "Extended with stubs for fetchCreatemeta, bulkUpdateIssue, fetchIssueLinkTypes, createIssueLink"
    - path: "src/services/jira.ts"
      provides: "Extended createIssue, fetchCreatemeta, bulkUpdateIssue, fetchIssueLinkTypes, createIssueLink"
  key_links:
    - from: "createIssue()"
      to: "POST /rest/api/2/issue"
      via: "apiFetch — body only includes defined fields (never undefined)"
      pattern: "Object\\.entries.*filter.*undefined"
    - from: "fetchCreatemeta()"
      to: "GET /rest/api/2/issue/createmeta/{project}/issuetypes/{id}"
      via: "apiFetch — 404 triggers legacy fallback"
      pattern: "resp\\.ok.*fallback\\|404"
    - from: "bulkUpdateIssue()"
      to: "PUT /rest/api/2/issue/{issueKey}"
      via: "apiFetch — accepts Record<string, unknown>"
      pattern: "method.*PUT.*fields"
---

<objective>
Create Wave 0 test stubs (RED) for all CREATE-01..04 behaviors and extend jira.ts with the five new service functions that the CreateEditIssueModal needs.

Purpose: Establish the RED baseline before any UI implementation; give the executor clean service contracts to code against in plans 02 and 03.
Output: CreateEditIssueModal.test.tsx (Wave 0 stubs), extended jira.test.ts (Wave 0 stubs for new jira functions), extended jira.ts (5 new exports).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-create-edit-issue-form/11-CONTEXT.md
@.planning/phases/11-create-edit-issue-form/11-RESEARCH.md
@.planning/phases/11-create-edit-issue-form/11-VALIDATION.md

<interfaces>
<!-- Key existing jira.ts exports the executor extends. -->

From src/services/jira.ts:
```typescript
// apiFetch pattern (established helper):
// apiFetch(service: 'jira' | 'gitlab', url: string, init: RequestInit): Promise<Response>

// Existing createIssue (to be REPLACED with extended version):
export async function createIssue(
  baseUrl: string,
  token: string,
  projectKey: string,
  summary: string,
): Promise<{ id: string; key: string }>

// Existing updateIssueField (single-field, NOT replaced — stays for Phase 9 inline edits):
export async function updateIssueField(
  baseUrl: string, token: string, issueKey: string, fieldKey: string, value: unknown
): Promise<void>
```

From src/stores/settings.store.ts:
```typescript
interface SettingsState {
  epicLinkFieldKey: string      // e.g. 'customfield_10014'
  storyPointsFieldKey: string   // e.g. 'customfield_10016'
  accountFieldKey: string | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED test stubs</name>
  <files>src/routes/dashboard/CreateEditIssueModal.test.tsx, src/services/jira.test.ts</files>
  <behavior>
    CreateEditIssueModal.test.tsx stubs (all should skip/todo — no implementation yet):
    - Stub: renders type switcher (Story / Subtask / Bug) as first field
    - Stub: switching to Subtask shows Parent field, hides Epic Link
    - Stub: switching to Story shows Epic Link, hides Parent
    - Stub: submit button disabled when required custom field is empty
    - Stub: edit mode pre-fills summary, description, assignee, priority, story points, epic link from props
    - Stub: link row visible after clicking "Add link"; has link type dropdown and issue search input
    - Stub: multiple link rows can be added

    jira.test.ts stubs (failing RED tests — import the functions that do not yet exist):
    - fetchCreatemeta: returns required fields from new endpoint (mock 200 returning {values: [...]})
    - fetchCreatemeta: falls back to legacy endpoint on new-endpoint 404
    - bulkUpdateIssue: sends PUT with correct fields body
    - fetchIssueLinkTypes: returns array from mocked issueLinkType response
    - createIssueLink: sends POST with type.id, inwardIssue.key, outwardIssue.key
    - createIssue (extended): sends issuetype, description, assignee, priority in body when provided
    - createIssue (extended): does NOT include undefined fields in body (never send undefined to Jira)
  </behavior>
  <action>
    Create src/routes/dashboard/CreateEditIssueModal.test.tsx with vi.todo() or test.skip() stubs for
    every component behavior listed above. Import CreateEditIssueModal from './CreateEditIssueModal' —
    the file does not yet exist, so the test file will fail to compile (RED).

    Extend src/services/jira.test.ts with failing tests (not stubs) for the five new jira functions:
    fetchCreatemeta, bulkUpdateIssue, fetchIssueLinkTypes, createIssueLink, and the extended createIssue.
    Import each function name — they do not yet exist, so imports will cause RED failures.
    Use vi.stubGlobal / vi.fn() mocks for fetch consistent with the existing jira.test.ts patterns
    (look at how updateIssueField and other functions are tested in the file before writing).

    Do NOT use watch mode. Do NOT implement production code yet.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/CreateEditIssueModal.test.tsx src/services/jira.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist. CreateEditIssueModal.test.tsx fails (import error — file not created yet). jira.test.ts fails on the new function imports/assertions. Existing jira.test.ts tests remain green (no regressions).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend jira.ts with five new service functions</name>
  <files>src/services/jira.ts</files>
  <behavior>
    - createIssue extended: backward-compatible new signature accepts optional `options` object with issuetype, description, assignee ({name}), priority ({name}), parent ({key}), [fieldKey: string]: unknown for dynamic custom fields. Body is filtered — Object.entries(options).filter(([,v]) => v !== undefined) — never send undefined fields to avoid Jira "field not on screen" 400.
    - fetchCreatemeta: tries GET /rest/api/2/issue/createmeta/{projectKey}/issuetypes/{issueTypeId}?maxResults=50 first; on non-ok (including 404), falls back to GET /rest/api/2/issue/createmeta?projectKeys={key}&issuetypeNames={typeName}&expand=projects.issuetypes.fields. Returns CreatemetaField[] = { fieldId: string; name: string; required: boolean; schema: { type: string; custom?: string; allowedValues?: { id: string; value: string }[] } }[]. Export this type too.
    - fetchIssueLinkTypes: GET /rest/api/2/issueLinkType. Returns IssueLinkType[] = { id: string; name: string; inward: string; outward: string }[]. Export this type.
    - createIssueLink: POST /rest/api/2/issueLink with body { type: { id: linkTypeId }, inwardIssue: { key: inwardKey }, outwardIssue: { key: outwardKey } }. Throws on non-201/200. Export function.
    - bulkUpdateIssue: PUT /rest/api/2/issue/{issueKey} with body { fields: Record<string, unknown> }. 204 and 200 both treated as success (Jira DC returns 204 on update). Throws with Jira error message on failure. Export function.
  </behavior>
  <action>
    In src/services/jira.ts:

    1. REPLACE the existing `createIssue` export with the extended backward-compatible version from RESEARCH.md Pattern 3. The existing QuickCreateInput.tsx only passes (baseUrl, token, projectKey, summary) — confirm the new optional `options` param does not break that call site.

    2. ADD `export interface CreatemetaField` and `export async function fetchCreatemeta` (Pattern 2 from RESEARCH.md — dual-endpoint with 404 fallback). The function needs issueTypeId AND issueTypeName as separate params to support both endpoint strategies. Signature:
       ```typescript
       export async function fetchCreatemeta(
         baseUrl: string, token: string, projectKey: string,
         issueTypeId: string, issueTypeName: string
       ): Promise<CreatemetaField[]>
       ```

    3. ADD `export interface IssueLinkType` and `export async function fetchIssueLinkTypes` (Pattern 6 from RESEARCH.md).

    4. ADD `export async function createIssueLink` (Pattern 5 from RESEARCH.md).

    5. ADD `export async function bulkUpdateIssue` (Pattern 4 from RESEARCH.md).

    CRITICAL: Never send ADF for description — function signatures accept plain string (wiki markup).
    CRITICAL: Assignee uses DC format { name: username }, NOT { accountId }.
    CRITICAL: Do not re-discover custom fields here — that is discoverCustomFields()'s job.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/services/jira.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All new jira.test.ts tests pass GREEN. Existing tests still pass. TypeScript compiles without errors on jira.ts (run `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | grep jira.ts`).</done>
</task>

</tasks>

<verification>
cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/services/jira.test.ts
cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | grep -E "error TS"
</verification>

<success_criteria>
- CreateEditIssueModal.test.tsx exists with Wave 0 stubs (fails to compile — component not yet created)
- jira.test.ts extended tests pass GREEN for all five new functions
- createIssue(), fetchCreatemeta(), bulkUpdateIssue(), fetchIssueLinkTypes(), createIssueLink() all exported from jira.ts
- No TypeScript errors in jira.ts
- No regressions in existing jira.test.ts tests
</success_criteria>

<output>
After completion, create `.planning/phases/11-create-edit-issue-form/11-01-SUMMARY.md`
</output>
