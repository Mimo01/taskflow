---
phase: 11-create-edit-issue-form
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - src/routes/dashboard/IssueLinkRow.tsx
  - src/routes/dashboard/CreateEditIssueModal.tsx
autonomous: true
requirements: [CREATE-04]

must_haves:
  truths:
    - "An 'Add link' button in the modal form adds a new link row (link type dropdown + issue search input)"
    - "Link type dropdown is populated from fetchIssueLinkTypes() — never hardcoded"
    - "Issue search input is type-to-search (debounced 300ms) using searchJira(), showing a dropdown of matching issues with key + summary"
    - "Multiple link rows can be added before submitting"
    - "After createIssue() succeeds, createIssueLink() is called for each link row — errors on individual links do not fail the whole submit"
    - "Issue link rows render as compact removable rows in the form"
  artifacts:
    - path: "src/routes/dashboard/IssueLinkRow.tsx"
      provides: "Single link row with type dropdown + issue search + remove button"
      exports: ["IssueLinkRow", "IssueLinkRowValue"]
    - path: "src/routes/dashboard/CreateEditIssueModal.tsx"
      provides: "Updated to include link rows section + Add link button + post-create link creation"
  key_links:
    - from: "IssueLinkRow type dropdown"
      to: "fetchIssueLinkTypes()"
      via: "useQuery(['jira-link-types', jiraBaseUrl]) in parent modal — passed as linkTypes prop"
      pattern: "useQuery.*link-types\\|issueLinkType"
    - from: "IssueLinkRow issue search"
      to: "searchJira()"
      via: "debounced useQuery(['jira-search', query]) — same pattern as SearchOverlay"
      pattern: "searchJira.*query\\|debounce"
    - from: "CreateEditIssueModal submit"
      to: "createIssueLink()"
      via: "for..of loop after createIssue() resolves — catch per-link errors silently"
      pattern: "for.*link.*createIssueLink.*catch"
---

<objective>
Build IssueLinkRow component and wire issue links into the CreateEditIssueModal — link type discovery, type-to-search issue picker, multiple rows, and post-create link API calls.

Purpose: Fulfills CREATE-04. Issue links are added in the same form submission flow — links are created via separate API calls after the main issue is created (Jira DC constraint).
Output: IssueLinkRow.tsx and updated CreateEditIssueModal.tsx.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-create-edit-issue-form/11-CONTEXT.md
@.planning/phases/11-create-edit-issue-form/11-RESEARCH.md
@.planning/phases/11-create-edit-issue-form/11-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor uses in this plan. -->

From src/services/jira.ts (plan 11-01):
```typescript
export interface IssueLinkType {
  id: string
  name: string
  inward: string
  outward: string
}

export async function fetchIssueLinkTypes(
  baseUrl: string, token: string
): Promise<IssueLinkType[]>

export async function createIssueLink(
  baseUrl: string, token: string,
  linkTypeId: string, inwardKey: string, outwardKey: string
): Promise<void>

// Existing searchJira (for issue picker):
export async function searchJira(
  baseUrl: string, token: string, query: string
): Promise<JiraIssue[]>
```

From src/routes/dashboard/CreateEditIssueModal.tsx (plan 11-02):
```typescript
// linkRows state already declared as empty array — plan 11-03 fills it in
// IssueLinkRowValue interface to define here:
export interface IssueLinkRowValue {
  id: string        // uuid for stable React key (use crypto.randomUUID())
  linkTypeId: string
  issueKey: string
}
```

From src/components/ui/select.tsx:
```typescript
// shadcn-style Select wrapper
export { Select, SelectTrigger, SelectValue, SelectContent, SelectItem }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IssueLinkRow component</name>
  <files>src/routes/dashboard/IssueLinkRow.tsx</files>
  <behavior>
    - Props: linkTypes: IssueLinkType[], value: IssueLinkRowValue, onChange: (v: IssueLinkRowValue) => void, onRemove: () => void
    - Link type Select dropdown: options from linkTypes prop. Display `type.outward` as label (e.g. "blocks"). Value is type.id (never hardcoded type name).
    - Issue search input: text input with debounced query (300ms). On each keystroke after debounce, calls searchJira() via useQuery. Shows dropdown of results (key + summary). On selection, sets issueKey in the row value and clears the search input display.
    - Remove button (X icon from lucide-react) triggers onRemove.
    - Compact horizontal layout: [type select | search input (flex-1) | remove button]
    - Export named: `IssueLinkRow` and `IssueLinkRowValue`.
  </behavior>
  <action>
    Create src/routes/dashboard/IssueLinkRow.tsx.

    Export interface IssueLinkRowValue:
    ```typescript
    export interface IssueLinkRowValue {
      id: string
      linkTypeId: string
      issueKey: string
    }
    ```

    For the debounced search, follow the same pattern used in IssueDetailSidebar.tsx (doSearch + useDebounce or setTimeout). Use searchJira() from jira.ts. Read token via `readSecret('jira-pat')` and jiraBaseUrl from useAuthStore() — same pattern as CommentComposer and IssueDetailSidebar.

    Issue search results dropdown: render as absolute-positioned list below the input (simple div with bg-background border rounded shadow, max-h-48 overflow-y-auto). Each result: `{issue.key}: {issue.fields.summary}`. On click, set issueKey on the row value and close dropdown.

    Use Select from @/components/ui/select for the link type dropdown.
    Use Input from @/components/ui/input for the search input.
    Use X icon from lucide-react for the remove button.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | grep IssueLinkRow</automated>
  </verify>
  <done>IssueLinkRow.tsx compiles without TypeScript errors. IssueLinkRowValue interface exported. Import chain resolves (jira.ts, auth.store, stronghold, UI components).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire issue links into CreateEditIssueModal</name>
  <files>src/routes/dashboard/CreateEditIssueModal.tsx</files>
  <behavior>
    - Add useQuery(['jira-link-types', jiraBaseUrl]) calling fetchIssueLinkTypes() — runs when jiraBaseUrl is available.
    - Add "Add link" button below the core fields section (above the submit button). Clicking it appends a new IssueLinkRowValue with crypto.randomUUID() id, empty linkTypeId and issueKey to linkRows state.
    - Render each linkRows entry as an IssueLinkRow component. Pass linkTypes from the query, value, onChange (update that row by id), and onRemove (filter that row out by id).
    - In the submit mutation (after createIssue() resolves with the new issue key): iterate linkRows, for each row with non-empty linkTypeId and issueKey call createIssueLink(). Wrap each call in try/catch — individual link failures do not fail the overall submit (log to console, do not set apiError for link failures).
    - In edit mode: the same link rows UI is shown. After bulkUpdateIssue() resolves, iterate linkRows and call createIssueLink() for each row (same pattern). Existing links on the issue are NOT removed — this form only adds new links (matching Jira's own behavior).
    - If fetchIssueLinkTypes query is loading, disable the "Add link" button.
  </behavior>
  <action>
    Update src/routes/dashboard/CreateEditIssueModal.tsx:

    1. Add import for IssueLinkRow and IssueLinkRowValue from './IssueLinkRow'.
    2. Add import for fetchIssueLinkTypes from '@/services/jira'.
    3. Replace the `linkRows: IssueLinkRowValue[]` stub (empty array from plan 11-02) with proper useState<IssueLinkRowValue[]>([]).
    4. Add useQuery for link types: `useQuery({ queryKey: ['jira-link-types', jiraBaseUrl], queryFn: async () => { ... } })` — reads token via readSecret, returns fetchIssueLinkTypes result.
    5. Add "Add link" button in JSX — use Plus icon from lucide-react.
    6. Render link rows section: map over linkRows, render IssueLinkRow for each.
    7. Update submit mutation to call createIssueLink() for each link row after main create/update succeeds.

    CRITICAL: issue links CANNOT be sent in the createIssue body — they must be separate calls AFTER the issue is created (RESEARCH.md Pitfall 3 and Pattern 5).
    CRITICAL: link type names are dynamic — always use type.id, never hardcode "Blocks", "Relates To" etc.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/CreateEditIssueModal.test.tsx src/services/jira.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All CreateEditIssueModal and jira tests pass. TypeScript compiles without errors. Link rows render in the form. Issue links created after main issue create/update.</done>
</task>

</tasks>

<verification>
cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/CreateEditIssueModal.test.tsx src/services/jira.test.ts
cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | grep -E "error TS"
</verification>

<success_criteria>
- IssueLinkRow.tsx renders type dropdown (from discovered link types) + issue search + remove button
- Multiple link rows can be added before submitting
- Link types populated from fetchIssueLinkTypes() — no hardcoded names
- Issue search uses searchJira() with 300ms debounce
- createIssueLink() called per row after issue create/update; per-link failures are silent
- All tests pass, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-create-edit-issue-form/11-03-SUMMARY.md`
</output>
