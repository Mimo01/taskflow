---
phase: 41-ci-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src-tauri/tauri.conf.json
  - taskflow/src/hooks/useVersionPolicyCheck.ts
  - taskflow/src/routes/settings/UpdatesSection.tsx
  - .github/workflows/release.yml
autonomous: true
requirements:
  - CI-01
  - CI-02

must_haves:
  truths:
    - "Updater endpoint URL points to Mimo01/taskflow-releases"
    - "Version policy URL points to Mimo01/taskflow-releases"
    - "GitHub Releases API URL points to Mimo01/taskflow-releases"
    - "A release.yml workflow exists that triggers on semver tag push"
    - "The workflow builds macOS universal, Windows x86_64, and Linux x86_64"
    - "The workflow publishes to taskflow-releases via PAT"
    - "Existing tests remain green after URL changes"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "CI pipeline workflow"
      min_lines: 80
    - path: "taskflow/src-tauri/tauri.conf.json"
      provides: "Updater endpoint config with real URL"
      contains: "Mimo01/taskflow-releases"
    - path: "taskflow/src/hooks/useVersionPolicyCheck.ts"
      provides: "Version policy check with real URL"
      contains: "Mimo01/taskflow-releases"
    - path: "taskflow/src/routes/settings/UpdatesSection.tsx"
      provides: "Version history with real API URL"
      contains: "Mimo01/taskflow-releases"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "taskflow/scripts/inject-version.cjs"
      via: "node scripts/inject-version.cjs >> $GITHUB_ENV"
      pattern: "inject-version"
    - from: ".github/workflows/release.yml"
      to: "Mimo01/taskflow-releases"
      via: "tauri-action owner/repo inputs"
      pattern: "owner: Mimo01"
    - from: "taskflow/src-tauri/tauri.conf.json"
      to: "Mimo01/taskflow-releases"
      via: "updater endpoints array"
      pattern: "Mimo01/taskflow-releases"
---

<objective>
Create the GitHub Actions release workflow and replace all placeholder URLs so the app points to the real public releases repo.

Purpose: This is the entire CI pipeline implementation -- the workflow file that builds cross-platform artifacts on tag push and publishes to the public repo, plus wiring the app to the correct release endpoints.
Output: `.github/workflows/release.yml` workflow file + 3 source files with real URLs
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-ci-pipeline/41-CONTEXT.md
@.planning/phases/41-ci-pipeline/41-RESEARCH.md

<interfaces>
<!-- Key files the executor needs to understand -->

From taskflow/scripts/inject-version.cjs (line 28 - stdout format):
```javascript
process.stdout.write(`APP_VERSION=${version}\nAPP_COMMIT_SHA=${sha}\nAPP_BUILD_DATE=${buildDate}\n`);
```
This output format is KEY: redirecting to $GITHUB_ENV makes these env vars available in subsequent steps.

From taskflow/src-tauri/tauri.conf.json (current state):
```json
{
  "version": "0.0.0-dev",
  "bundle": { "createUpdaterArtifacts": true },
  "plugins": {
    "updater": {
      "endpoints": [
        "https://github.com/OWNER/RELEASES_REPO/releases/latest/download/latest.json"
      ]
    }
  }
}
```
Note: no `pubkey` field exists yet in plugins.updater -- this must NOT be added by this plan (requires key generation which is a human action).

From taskflow/src/hooks/useVersionPolicyCheck.ts (line 14-15):
```typescript
const VERSION_POLICY_URL =
  'https://raw.githubusercontent.com/OWNER/RELEASES_REPO/main/version-policy.json';
```

From taskflow/src/routes/settings/UpdatesSection.tsx (line 41-42):
```typescript
const RELEASES_API_URL =
  'https://api.github.com/repos/PLACEHOLDER/PLACEHOLDER/releases?per_page=20';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all placeholder URLs with real Mimo01/taskflow-releases URLs</name>
  <files>
    taskflow/src-tauri/tauri.conf.json,
    taskflow/src/hooks/useVersionPolicyCheck.ts,
    taskflow/src/routes/settings/UpdatesSection.tsx
  </files>
  <read_first>
    taskflow/src-tauri/tauri.conf.json,
    taskflow/src/hooks/useVersionPolicyCheck.ts,
    taskflow/src/routes/settings/UpdatesSection.tsx
  </read_first>
  <action>
Replace placeholder URLs in three files. Exact replacements:

**File 1: `taskflow/src-tauri/tauri.conf.json`**
- Line 39: Replace `https://github.com/OWNER/RELEASES_REPO/releases/latest/download/latest.json`
- With: `https://github.com/Mimo01/taskflow-releases/releases/latest/download/latest.json`
- Per D-02.

**File 2: `taskflow/src/hooks/useVersionPolicyCheck.ts`**
- Lines 14-15: Replace `https://raw.githubusercontent.com/OWNER/RELEASES_REPO/main/version-policy.json`
- With: `https://raw.githubusercontent.com/Mimo01/taskflow-releases/main/version-policy.json`
- Per D-03. Also remove or update the comment on line 13 from "Placeholder URL -- will resolve to public releases repo in Phase 41" to "Public releases repo for version policy" since it is no longer a placeholder.

**File 3: `taskflow/src/routes/settings/UpdatesSection.tsx`**
- Lines 40-42: Replace `https://api.github.com/repos/PLACEHOLDER/PLACEHOLDER/releases?per_page=20`
- With: `https://api.github.com/repos/Mimo01/taskflow-releases/releases?per_page=20`
- Per D-04. Also remove the TODO comment on line 40 (`// TODO(Phase-41): set real repo path when public repo is created`) since this is now resolved.

Do NOT modify any other code in these files. Only the URL strings and their associated TODO/placeholder comments.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker && grep -c "Mimo01/taskflow-releases" taskflow/src-tauri/tauri.conf.json taskflow/src/hooks/useVersionPolicyCheck.ts taskflow/src/routes/settings/UpdatesSection.tsx && grep -c "PLACEHOLDER\|OWNER/RELEASES_REPO" taskflow/src-tauri/tauri.conf.json taskflow/src/hooks/useVersionPolicyCheck.ts taskflow/src/routes/settings/UpdatesSection.tsx; cd taskflow && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tauri.conf.json` contains `https://github.com/Mimo01/taskflow-releases/releases/latest/download/latest.json`
    - `useVersionPolicyCheck.ts` contains `https://raw.githubusercontent.com/Mimo01/taskflow-releases/main/version-policy.json`
    - `UpdatesSection.tsx` contains `https://api.github.com/repos/Mimo01/taskflow-releases/releases?per_page=20`
    - No file in `taskflow/src` or `taskflow/src-tauri` contains `OWNER/RELEASES_REPO` or `PLACEHOLDER/PLACEHOLDER`
    - `npx vitest run` exits 0 (all existing tests pass)
  </acceptance_criteria>
  <done>All three placeholder URLs replaced with real Mimo01/taskflow-releases URLs. No placeholder strings remain. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions release workflow</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    taskflow/scripts/inject-version.cjs,
    taskflow/src-tauri/tauri.conf.json
  </read_first>
  <action>
Create `.github/workflows/release.yml` with the following exact content structure. This is the complete CI pipeline per CI-01 and CI-02.

**Workflow name:** `Release`

**Trigger:** `on.push.tags` matching `'v[0-9]*'` (per D-07)

**Concurrency:** group `release-${{ github.ref }}`, cancel-in-progress true

**Job: `release`**
- `permissions.contents: write`
- `strategy.fail-fast: false` (prevent one platform failure from canceling others)
- Matrix with three entries (per D-09, D-10, D-11):
  1. `platform: macos-latest`, `args: --target universal-apple-darwin`, `rust_targets: aarch64-apple-darwin,x86_64-apple-darwin`
  2. `platform: windows-latest`, `args: --target x86_64-pc-windows-msvc`, `rust_targets: x86_64-pc-windows-msvc`
  3. `platform: ubuntu-22.04`, `args: ''`, `rust_targets: ''`
- `runs-on: ${{ matrix.platform }}`

**Steps (in order):**

1. **Checkout:** `actions/checkout@v4` with `fetch-depth: 0` (needed for git tag access and inject-version.cjs git describe)

2. **Install Linux dependencies** (conditional: `if: matrix.platform == 'ubuntu-22.04'`):
   ```
   sudo apt-get update
   sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
   ```

3. **Setup Node.js:** `actions/setup-node@v4` with `node-version: lts/*`, `cache: npm`, `cache-dependency-path: taskflow/package-lock.json`

4. **Install Rust stable:** `dtolnay/rust-toolchain@stable` with `targets: ${{ matrix.rust_targets }}`

5. **Rust cache:** `swatinem/rust-cache@v2` with `workspaces: taskflow/src-tauri -> target`

6. **Install frontend dependencies:** `working-directory: taskflow`, `run: npm ci`

7. **Run tests:** `working-directory: taskflow`, `run: npx vitest run` (run tests before building to catch regressions early)

8. **Extract tag annotation body** (id: `tag_body`, shell: bash):
   ```bash
   BODY=$(git tag -l --format='%(contents:body)' "$GITHUB_REF_NAME")
   echo "body<<EOF" >> $GITHUB_OUTPUT
   echo "$BODY" >> $GITHUB_OUTPUT
   echo "EOF" >> $GITHUB_OUTPUT
   ```

9. **Inject version** (shell: bash, working-directory: taskflow):
   ```bash
   node scripts/inject-version.cjs >> $GITHUB_ENV
   ```

10. **Build and release:** `tauri-apps/tauri-action@v0`
    - env:
      - `GITHUB_TOKEN: ${{ secrets.RELEASES_REPO_TOKEN }}` (fine-grained PAT with contents:write on taskflow-releases)
      - `TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}`
      - `TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}`
      - `APP_VERSION: ${{ env.APP_VERSION }}`
      - `APP_COMMIT_SHA: ${{ env.APP_COMMIT_SHA }}`
      - `APP_BUILD_DATE: ${{ env.APP_BUILD_DATE }}`
    - with:
      - `projectPath: taskflow`
      - `tagName: v__VERSION__`
      - `releaseName: Taskflow v__VERSION__`
      - `releaseBody: ${{ steps.tag_body.outputs.body }}`
      - `releaseDraft: false` (per D-08: fully automatic, no manual approval)
      - `prerelease: false`
      - `owner: Mimo01`
      - `repo: taskflow-releases`
      - `args: ${{ matrix.args }}`

**Important implementation notes:**
- Use `shell: bash` explicitly on both the tag_body and inject-version steps (cross-platform compatibility, avoids PowerShell eval issue on Windows)
- `fetch-depth: 0` on checkout is critical -- without it, `git describe --tags` in inject-version.cjs fails and version falls back to `0.0.0-dev`
- The `targets` field for dtolnay/rust-toolchain handles the empty string case for Linux correctly (no extra targets installed)
- Per D-12: tauri-action with `createUpdaterArtifacts: true` in tauri.conf.json generates the updater artifacts automatically (.tar.gz/.zip + signatures + latest.json)
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker && test -f .github/workflows/release.yml && echo "FILE EXISTS" && grep -c "v\[0-9\]\*" .github/workflows/release.yml && grep -c "universal-apple-darwin" .github/workflows/release.yml && grep -c "RELEASES_REPO_TOKEN" .github/workflows/release.yml && grep -c "tauri-apps/tauri-action@v0" .github/workflows/release.yml && grep -c "owner: Mimo01" .github/workflows/release.yml && grep -c "repo: taskflow-releases" .github/workflows/release.yml && grep -c "inject-version" .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/release.yml` exists
    - Workflow triggers on `push.tags` matching `v[0-9]*`
    - Matrix has exactly 3 entries: `macos-latest`, `windows-latest`, `ubuntu-22.04`
    - macOS entry has `args: --target universal-apple-darwin` and `rust_targets: aarch64-apple-darwin,x86_64-apple-darwin`
    - Workflow uses `actions/checkout@v4` with `fetch-depth: 0`
    - Workflow uses `dtolnay/rust-toolchain@stable` with `targets: ${{ matrix.rust_targets }}`
    - Workflow uses `swatinem/rust-cache@v2` with `workspaces: taskflow/src-tauri -> target`
    - Workflow runs `npx vitest run` before building
    - Workflow has `node scripts/inject-version.cjs >> $GITHUB_ENV` step with `shell: bash`
    - Workflow has tag body extraction step with id `tag_body`
    - Workflow uses `tauri-apps/tauri-action@v0` with `owner: Mimo01` and `repo: taskflow-releases`
    - Workflow sets `GITHUB_TOKEN: ${{ secrets.RELEASES_REPO_TOKEN }}`
    - Workflow sets `TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}`
    - Workflow sets `releaseDraft: false` and `prerelease: false`
    - Workflow has `fail-fast: false` in strategy
    - Workflow installs Linux dependencies conditionally for `ubuntu-22.04` only
    - File has `concurrency` block with `cancel-in-progress: true`
  </acceptance_criteria>
  <done>Complete release.yml workflow exists that triggers on semver tag push, builds macOS universal + Windows x86_64 + Linux x86_64, and publishes to Mimo01/taskflow-releases via PAT.</done>
</task>

</tasks>

<verification>
1. All placeholder URLs replaced: `grep -r "PLACEHOLDER\|OWNER/RELEASES_REPO" taskflow/src taskflow/src-tauri` returns no results
2. All real URLs present: `grep -r "Mimo01/taskflow-releases" taskflow/src taskflow/src-tauri .github/workflows/` returns matches in all 4 files
3. Workflow file is valid YAML: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"`
4. Existing tests pass: `cd taskflow && npx vitest run` exits 0
</verification>

<success_criteria>
- `.github/workflows/release.yml` exists with complete 3-platform build matrix
- All placeholder URLs in app source replaced with `Mimo01/taskflow-releases` equivalents
- All 83+ existing vitest tests pass
- No `PLACEHOLDER` or `OWNER/RELEASES_REPO` strings remain in source
</success_criteria>

<output>
After completion, create `.planning/phases/41-ci-pipeline/41-01-SUMMARY.md`
</output>
