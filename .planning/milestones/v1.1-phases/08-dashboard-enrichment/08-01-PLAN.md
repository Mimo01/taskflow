---
phase: 08-dashboard-enrichment
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/SubtasksPanel.test.tsx
  - taskflow/src/routes/dashboard/MrHealthPanel.test.tsx
  - taskflow/src/routes/dashboard/SprintHealthPanel.test.tsx
  - taskflow/src/routes/dashboard/NotificationsPanel.test.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "Four test files exist covering DASH-01 through DASH-04 with failing stubs"
    - "Running npx vitest run src/routes/dashboard reports all 4 new test files"
    - "Each test file has at minimum 2 describe blocks and failing (vi.todo or pending) stubs"
  artifacts:
    - path: "taskflow/src/routes/dashboard/SubtasksPanel.test.tsx"
      provides: "Test stubs for DASH-01 behaviors"
    - path: "taskflow/src/routes/dashboard/MrHealthPanel.test.tsx"
      provides: "Test stubs for DASH-02 behaviors"
    - path: "taskflow/src/routes/dashboard/SprintHealthPanel.test.tsx"
      provides: "Test stubs for DASH-03 behaviors"
    - path: "taskflow/src/routes/dashboard/NotificationsPanel.test.tsx"
      provides: "Test stubs for DASH-04 behaviors"
  key_links:
    - from: "08-02-PLAN.md"
      to: "SubtasksPanel.test.tsx"
      via: "Wave 0 tests must exist before Wave 1 implementation"
    - from: "08-03-PLAN.md"
      to: "MrHealthPanel.test.tsx, SprintHealthPanel.test.tsx"
      via: "Wave 0 tests must exist before Wave 1 implementation"
    - from: "08-04-PLAN.md"
      to: "NotificationsPanel.test.tsx"
      via: "Wave 0 tests must exist before Wave 1 implementation"
---

<objective>
Create failing test stubs for all four dashboard panel components before any implementation begins. This is the Nyquist Wave 0 requirement — tests must exist (RED state) before panels are written.

Purpose: Ensures every panel component has verified automated coverage; provides a contract that implementation plans must satisfy.
Output: Four test files in RED state, runnable immediately via vitest.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-dashboard-enrichment/08-CONTEXT.md
@.planning/phases/08-dashboard-enrichment/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test stubs for SubtasksPanel and MrHealthPanel</name>
  <files>taskflow/src/routes/dashboard/SubtasksPanel.test.tsx, taskflow/src/routes/dashboard/MrHealthPanel.test.tsx</files>
  <behavior>
    SubtasksPanel (DASH-01):
    - Test: renders subtask row with key, title, status badge, parent story name
    - Test: hides orphan subtasks whose parent.key is not in the sprint issue set
    - Test: shows "No open subtasks in the current sprint" empty state when no subtasks
    - Test: limits display to 5 subtasks, shows "View all in My Tasks" link when more exist
    - Test: clicking a subtask row calls window.open with the Jira browse URL

    MrHealthPanel (DASH-02):
    - Test: shows correct Needs Review / Approved / Changes Requested counts from mr-health cache
    - Test: shows "No open MRs" empty state when assigned MRs list is empty
  </behavior>
  <action>
    Create both test files with vi.mock for @tanstack/react-query (useQuery, useQueryClient), @/stores/auth.store, @/stores/settings.store. Use vitest `it.todo()` stubs (or minimal failing `expect(true).toBe(false)` placeholder) for each behavior listed above. The component files do not exist yet — import them with the path they will have when created (e.g., `import SubtasksPanel from './SubtasksPanel'`). Tests will fail at import resolution — that is expected RED state. Do not implement the components.

    Use the existing test pattern from MrAttentionTab.test.tsx or MyTasksTab.test.tsx in the same directory for mock setup (vi.mock, beforeEach, render from @testing-library/react).
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/SubtasksPanel.test.tsx src/routes/dashboard/MrHealthPanel.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist on disk. Running vitest reports them (even if they fail due to missing component files — RED state is correct). No syntax errors in test files themselves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write test stubs for SprintHealthPanel and NotificationsPanel</name>
  <files>taskflow/src/routes/dashboard/SprintHealthPanel.test.tsx, taskflow/src/routes/dashboard/NotificationsPanel.test.tsx</files>
  <behavior>
    SprintHealthPanel (DASH-03):
    - Test: shows "% done" computed from done-points / total-points (guard: 0% when no points)
    - Test: shows "N days left" when activeSprint.endDate is present
    - Test: hides "days left" segment gracefully when activeSprint is null or endDate absent
    - Test: lists at-risk items (in-progress stories with timeSpentSeconds == 0) by title below summary
    - Test: shows no at-risk list when all in-progress items have time logged

    NotificationsPanel (DASH-04):
    - Test: shows last 3 unread notifications sorted newest-first (items 4+ hidden)
    - Test: shows "No unread notifications" empty state when all notifications are read or none exist
    - Test: clicking a notification row opens inline detail (NotificationDetail rendered), not navigation
    - Test: "View all notifications" link renders pointing to /notifications route
  </behavior>
  <action>
    Create both test files with same vi.mock pattern as Task 1. For NotificationsPanel, also mock useNotificationsStore from @/stores/notifications.store. For SprintHealthPanel, mock useQuery (for active sprint), useSettingsStore (storyPointsFieldKey), useAuthStore. Use it.todo() stubs for each behavior. Component files will not exist yet — RED state is expected.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/SprintHealthPanel.test.tsx src/routes/dashboard/NotificationsPanel.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist on disk. Running vitest reports them in RED state (missing component is expected). No syntax errors.</done>
</task>

</tasks>

<verification>
All four test files exist on disk. `npx vitest run src/routes/dashboard` picks them up and reports them (failing is correct — RED state). No new production code created in this plan.
</verification>

<success_criteria>
Four *.test.tsx files exist covering all DASH-01 through DASH-04 behaviors as stubs. Wave 1 plans can now reference these files in their `<automated>` verify commands.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-enrichment/08-01-SUMMARY.md`
</output>
