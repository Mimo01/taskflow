---
phase: 07-story-subtask-hierarchy-mr-subtask-filter
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/MrAttentionTab.tsx
  - taskflow/src/routes/dashboard/MrAttentionTab.test.tsx
  - taskflow/src/services/gitlab.ts
autonomous: true
requirements: [MRAT-01, MRAT-02]
gap_closure: true

must_haves:
  truths:
    - "MR Attention tab fetches reviewer MRs after userId resolves (not before)"
    - "Stale gitlab-mrs cache is invalidated when userId changes"
    - "gitlab.ts has no duplicate fetchProjectMilestonesInRange identifier"
  artifacts:
    - path: "taskflow/src/routes/dashboard/MrAttentionTab.tsx"
      provides: "Fixed query key and enabled guard for gitlab-mrs query"
      contains: "userId"
    - path: "taskflow/src/services/gitlab.ts"
      provides: "Single definition of fetchProjectMilestonesInRange"
  key_links:
    - from: "MrAttentionTab.tsx"
      to: "fetchReviewerMRs"
      via: "queryKey includes userId; enabled requires !!userId"
      pattern: "queryKey.*userId"
---

<objective>
Fix the MR Attention tab returning an empty reviewer MR list due to a userId race condition, and remove the duplicate function in gitlab.ts that would cause a TypeScript error.

Purpose: UAT test 6 failed — "mr matching seems to be broken. When fetching mr, it returns empty array from gitlab." Root cause: the `gitlab-mrs` query fires before `validateGitLab` resolves, `userId` is `undefined`, `fetchReviewerMRs` is never called, and the stale empty cache is never busted when `userId` arrives (because `userId` is not in the query key).
Output: Fixed MrAttentionTab query with `userId` in key/guard; clean gitlab.ts with no duplicate.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-story-subtask-hierarchy-mr-subtask-filter/07-03-SUMMARY.md
</context>

<interfaces>
<!-- From MrAttentionTab.tsx — current broken query (lines 97-140) -->
```typescript
// BROKEN — userId not in key, not in enabled guard
const { data: mrQueryData, ... } = useQuery({
  queryKey: ['gitlab-mrs', gitlabBaseUrl],          // ← missing userId
  queryFn: async () => {
    const [assigned, reviewer] = await Promise.all([
      fetchAssignedMRs(gitlabBaseUrl!, token),
      userId ? fetchReviewerMRs(gitlabBaseUrl!, token, userId) : Promise.resolve([]),
    ])
    // ...
  },
  enabled: !!gitlabBaseUrl && !!gitlabToken,        // ← missing !!userId
})

const userId = currentUser?.id  // resolves async after validateGitLab
```

<!-- From gitlab.ts — duplicate function that must be removed -->
// fetchProjectMilestonesInRange already exists at line 469
// Uncommitted diff re-adds an identical copy — that copy must NOT be committed
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix gitlab-mrs query key and enabled guard in MrAttentionTab</name>
  <files>taskflow/src/routes/dashboard/MrAttentionTab.tsx, taskflow/src/routes/dashboard/MrAttentionTab.test.tsx</files>
  <behavior>
    - Test: reviewer MRs appear after userId resolves (renderWithQueryAndUser pre-seeds cache → MR must show up)
    - Test: existing stale-badge test still passes when using renderWithQueryAndUser (no regression)
    - Test: linking test still passes when using renderWithQueryAndUser (no regression)
  </behavior>
  <action>
**Step 1 — RED: update tests first.**

In `MrAttentionTab.test.tsx`, switch the two existing tests that use `renderWithQuery` (stale-badge test and linking test) to use `renderWithQueryAndUser` instead. This is the correct helper as both tests involve reviewer MR paths. Run `npx vitest run MrAttentionTab` — these tests should now fail (RED) because the production code still has the broken query.

The MRAT-02 tests already use `renderWithQueryAndUser` and already pass — do not change them.

**Step 2 — GREEN: fix MrAttentionTab.tsx.**

Locate the `gitlab-mrs` useQuery block (currently around line 97-140). Make two changes:

1. **Query key** — add `userId` as the fourth element:
   ```typescript
   queryKey: ['gitlab-mrs', gitlabBaseUrl, userId],
   ```

2. **Enabled guard** — add `!!userId`:
   ```typescript
   enabled: !!gitlabBaseUrl && !!gitlabToken && !!userId,
   ```

These two changes ensure:
- The query does not fire until `validateGitLab` has resolved and `userId` is available.
- When `userId` changes from `undefined` to a real value, TanStack Query treats it as a new query key and fires a fresh fetch — the stale empty-array cache is never used for the real userId.
- `fetchReviewerMRs` is always called with a real `userId` (the `userId ?` ternary inside the queryFn can remain as a safety guard but should never be false now).

No other changes to the component logic.

**Step 3 — verify GREEN:** run `npx vitest run MrAttentionTab` — all tests including stale-badge, linking, and all four MRAT-02 tests must pass.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run MrAttentionTab 2>&1 | tail -20</automated>
  </verify>
  <done>All MrAttentionTab tests pass; queryKey contains userId; enabled guard includes !!userId</done>
</task>

<task type="auto">
  <name>Task 2: Revert duplicate fetchProjectMilestonesInRange in gitlab.ts</name>
  <files>taskflow/src/services/gitlab.ts</files>
  <action>
The uncommitted diff in `taskflow/src/services/gitlab.ts` re-adds `fetchProjectMilestonesInRange`, which already exists at line 469 in the committed file. Committing this diff would create a duplicate export and cause a TypeScript error.

Discard the uncommitted changes to this file:

```bash
git checkout -- taskflow/src/services/gitlab.ts
```

After this, `git diff taskflow/src/services/gitlab.ts` should be empty and `grep -c "fetchProjectMilestonesInRange" taskflow/src/services/gitlab.ts` should return `1`.

Do NOT make any other changes to gitlab.ts.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker && git diff taskflow/src/services/gitlab.ts | wc -l</automated>
  </verify>
  <done>git diff for gitlab.ts is empty (0 lines); only one definition of fetchProjectMilestonesInRange exists in the file</done>
</task>

</tasks>

<verification>
1. `npx vitest run MrAttentionTab` — all tests green
2. `npx tsc --noEmit` — no errors in MrAttentionTab.tsx or gitlab.ts
3. `git diff taskflow/src/services/gitlab.ts` — empty (no uncommitted changes)
4. `grep -c "fetchProjectMilestonesInRange" taskflow/src/services/gitlab.ts` — returns `1`
</verification>

<success_criteria>
- MrAttentionTab `gitlab-mrs` query key is `['gitlab-mrs', gitlabBaseUrl, userId]`
- MrAttentionTab `gitlab-mrs` enabled guard includes `!!userId`
- All MrAttentionTab tests pass (stale-badge, no-stale, linking, 4x MRAT-02)
- gitlab.ts has exactly one `fetchProjectMilestonesInRange` definition and no uncommitted diff
</success_criteria>

<output>
After completion, create `.planning/phases/07-story-subtask-hierarchy-mr-subtask-filter/07-05-SUMMARY.md`
</output>
