---
phase: 75-progressive-issue-detail-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/jira/changelog.ts
  - taskflow/src/services/jira.ts
  - taskflow/src/routes/dashboard/issue-detail/CommentsSkeleton.tsx
  - taskflow/src/routes/dashboard/issue-detail/SubtasksSkeleton.tsx
  - taskflow/src/routes/dashboard/IssueDetailPage.progressive.test.tsx
autonomous: true
requirements: [PERF-DETAIL-01, PERF-DETAIL-02, PERF-DETAIL-03, GH-CUT-01]
user_setup: []

must_haves:
  truths:
    - "fetchIssueDetail no longer requests comments or changelog and no longer runs the inline subtask-enrichment JQL"
    - "fetchIssueChangelog returns the changelog histories array via REST v2 expand=changelog (no GreenHopper endpoint)"
    - "fetchEnrichedSubtasks returns subtasks with assignees merged in, returning unenriched on failure"
    - "CommentsSkeleton and SubtasksSkeleton render Skeleton primitives at the UI-SPEC dimensions"
    - "A failing progressive test scaffold exists asserting per-section independent rendering (PERF-DETAIL-01/02)"
  artifacts:
    - path: "taskflow/src/services/jira/changelog.ts"
      provides: "fetchIssueChangelog REST v2 changelog fetcher"
      exports: ["fetchIssueChangelog"]
    - path: "taskflow/src/routes/dashboard/issue-detail/CommentsSkeleton.tsx"
      provides: "Comments section skeleton"
      exports: ["CommentsSkeleton"]
    - path: "taskflow/src/routes/dashboard/issue-detail/SubtasksSkeleton.tsx"
      provides: "Subtasks section skeleton"
      exports: ["SubtasksSkeleton"]
    - path: "taskflow/src/routes/dashboard/IssueDetailPage.progressive.test.tsx"
      provides: "Wave 0 failing progressive-rendering test scaffold"
      contains: "fetchIssueComments"
  key_links:
    - from: "taskflow/src/services/jira.ts (fetchIssueDetail)"
      to: "taskflow/src/services/jira.ts (fetchEnrichedSubtasks)"
      via: "extracted standalone function"
      pattern: "export async function fetchEnrichedSubtasks"
    - from: "taskflow/src/services/jira/changelog.ts"
      to: "/rest/api/2/issue/{key}?expand=changelog"
      via: "apiFetch REST v2 call"
      pattern: "expand=changelog"
---

<objective>
Build the data-layer foundation for progressive issue detail rendering: extract the three slow paths out of the single `fetchIssueDetail` call into independent service functions, create the two new per-section skeletons, and lay down the failing Wave 0 progressive test scaffold. No consumer wiring happens here — this plan produces the pieces the page rewrite (75-02) and invalidation pass (75-03) consume.

Per D-01/D-02: the base `/issue/{key}` fetch must stop carrying comments (`comment` field), changelog (`expand=changelog`), and the sequential subtask-enrichment JQL. Per GH-CUT-01/PERF-DETAIL-03: every new fetcher stays on Jira REST v2 — no GreenHopper endpoints. Per D-04: do NOT over-split — description / fields / attachments / links stay on the base fetch. Per D-03: worklogs / epic stories / AIO test runs are already independent and are left untouched.

Purpose: Decompose the monolithic fetch so each slow path can be queried and gated independently (PERF-DETAIL-01/02).
Output: `fetchIssueChangelog` (new file), `fetchEnrichedSubtasks` (extracted), slimmed `fetchIssueDetail`, `CommentsSkeleton`, `SubtasksSkeleton`, and a failing `IssueDetailPage.progressive.test.tsx`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-progressive-issue-detail-rendering/75-CONTEXT.md
@.planning/phases/75-progressive-issue-detail-rendering/75-RESEARCH.md
@.planning/phases/75-progressive-issue-detail-rendering/75-PATTERNS.md
@.planning/phases/75-progressive-issue-detail-rendering/75-UI-SPEC.md
@.planning/phases/75-progressive-issue-detail-rendering/75-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose fetchIssueDetail — slim base fetch + fetchEnrichedSubtasks + fetchIssueChangelog</name>
  <files>taskflow/src/services/jira/changelog.ts, taskflow/src/services/jira.ts</files>
  <read_first>
    - taskflow/src/services/jira.ts (lines 1486-1577 — current fetchIssueDetail: field list at ~1509, expand=changelog at ~1528, subtask-enrichment block at 1545-1574)
    - taskflow/src/services/jira/comments.ts (lines 1-36 — exact import + apiFetch + ApiError + response.ok pattern to mirror in changelog.ts)
    - taskflow/src/services/jira/worklogs.ts (fetchFullWorklogs — the "return [] on failure" non-critical-enrichment pattern, contrasted with comments.ts which throws)
    - .planning/phases/75-progressive-issue-detail-rendering/75-RESEARCH.md (§Query Decomposition lines 234-285; §Code Examples fetchIssueChangelog + fetchEnrichedSubtasks)
    - .planning/phases/75-progressive-issue-detail-rendering/75-PATTERNS.md (changelog.ts analog + fetchEnrichedSubtasks extraction shape, lines 29-127)
  </read_first>
  <action>
    Create `taskflow/src/services/jira/changelog.ts` exporting `fetchIssueChangelog(baseUrl, token, issueKey): Promise<ChangelogHistory[]>`. Mirror `comments.ts` exactly: import `ApiError` from `../../lib/api-error` and `apiFetch` from `../../lib/apiFetch`; build URL `${baseUrl.replace(/\/$/, '')}/rest/api/2/issue/${issueKey}?expand=changelog&fields=summary` (Option A per RESEARCH — proven on-prem, do NOT use the `/changelog` sub-resource); wrap apiFetch in try/catch for network errors; on `!response.ok` throw `ApiError` for 401/403 else plain `Error`; parse `data.changelog?.histories ?? []`. Use operation name `'Load Issue Detail'`. It must THROW on error (it is a primary section, not optional enrichment) — unlike worklogs which returns []. Resolve the `ChangelogHistory` type name from the existing `JiraIssueDetail` shape in jira.ts; reuse the existing exported type, do not redefine it.

    In `taskflow/src/services/jira.ts`: add exported `fetchEnrichedSubtasks(baseUrl, token, subtasks): Promise<typeof subtasks>` extracted verbatim from the inline block at lines 1545-1574 — build JQL `key in (${subtaskKeys})`, GET `/rest/api/2/search?jql=...&fields=assignee&maxResults=${subtasks.length}`, on `!ok` return `subtasks` unenriched (non-critical), else build an assignee Map keyed by issue key and merge assignees back. Then modify `fetchIssueDetail`: remove `'comment'` from the fields array, remove `&expand=changelog` from the URL, and delete the entire `if (issue.fields.subtasks?.length > 0)` enrichment block (1545-1574). The `subtasks` field STAYS in the base fields list (it provides keys + count for the skeleton). Keep `fetchIssueDetail`'s return type unchanged otherwise.

    Do not touch any consumer of fetchIssueDetail in this task — that is 75-02. After this change the base response will no longer include `fields.comment` or `changelog`; that is expected and handled by 75-02.
  </action>
  <verify>
    <automated>cd taskflow && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/services/jira/changelog.ts` exports `fetchIssueChangelog`; grep `expand=changelog` present, grep for `greenhopper` ABSENT in the file.
    - `taskflow/src/services/jira.ts` exports `fetchEnrichedSubtasks` (grep `export async function fetchEnrichedSubtasks`).
    - In `fetchIssueDetail`'s field list, `'comment'` is no longer present and the URL no longer contains `expand=changelog` (grep `expand=changelog` returns 0 hits inside fetchIssueDetail body); the `subtasks` field IS still present.
    - The inline subtask-enrichment `if (issue.fields.subtasks?.length > 0)` block is removed from `fetchIssueDetail`.
    - `cd taskflow && npm run build` passes (tsc + vite build — CSS imports validated, not just tsc).
  </acceptance_criteria>
  <done>Three independent REST v2 fetch paths exist; base fetch is slimmed to header/description/fields/attachments/links/subtasks only; build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Create CommentsSkeleton and SubtasksSkeleton</name>
  <files>taskflow/src/routes/dashboard/issue-detail/CommentsSkeleton.tsx, taskflow/src/routes/dashboard/issue-detail/SubtasksSkeleton.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/issue-detail/AioTestRunsSkeleton.tsx (full file — exact import + structure to copy, including data-testid convention)
    - taskflow/src/components/ui/skeleton.tsx (the Skeleton primitive)
    - .planning/phases/75-progressive-issue-detail-rendering/75-UI-SPEC.md (Skeleton Dimensions Per Section table, lines 105-118; Typography contract — no font-semibold on new elements, lines 64-67)
    - .planning/phases/75-progressive-issue-detail-rendering/75-PATTERNS.md (CommentsSkeleton + SubtasksSkeleton target shapes, lines 131-189)
  </read_first>
  <action>
    Create `CommentsSkeleton.tsx` exporting `CommentsSkeleton`: import `Skeleton` from `@/components/ui/skeleton`; render a wrapper `div className="space-y-3" data-testid="comments-skeleton"` containing `Skeleton className="h-6 w-32"` (heading) then three `Skeleton className="h-10 w-full"` rows. Match the UI-SPEC Comments row pattern exactly.

    Create `SubtasksSkeleton.tsx` exporting `SubtasksSkeleton`: wrapper `div className="space-y-2" data-testid="subtasks-skeleton"` containing `Skeleton className="h-6 w-40"` (heading) then two `Skeleton className="h-8 w-full"` rows (compact, per UI-SPEC).

    Do NOT add `font-semibold` or any weight beyond what `Skeleton` carries (UI-SPEC 2-weight contract governs net-new elements). No new shadcn installs — `Skeleton` is pre-existing.
  </action>
  <verify>
    <automated>cd taskflow && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `CommentsSkeleton.tsx` exports `CommentsSkeleton`, contains `data-testid="comments-skeleton"`, one `h-6 w-32` and three `h-10 w-full` Skeletons.
    - `SubtasksSkeleton.tsx` exports `SubtasksSkeleton`, contains `data-testid="subtasks-skeleton"`, one `h-6 w-40` and two `h-8 w-full` Skeletons.
    - Neither file contains `font-semibold`.
    - `cd taskflow && npm run build` passes.
  </acceptance_criteria>
  <done>Both per-section skeletons exist at UI-SPEC dimensions, reserving final-size slots (D-05); build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 — failing progressive-rendering test scaffold</name>
  <files>taskflow/src/routes/dashboard/IssueDetailPage.progressive.test.tsx</files>
  <behavior>
    - PERF-DETAIL-01: when the base issue query resolves but the comments query is still pending, the header (title/key/status/assignee) is in the DOM AND a comments skeleton (or null) is shown — header does not wait on comments.
    - PERF-DETAIL-02: when the comments query isPending and useDelayedLoading returns true, `comments-skeleton` testid is rendered; when the subtask enrichment query isPending, `subtasks-skeleton` testid is rendered.
    - PERF-DETAIL-02: when changelog query data is undefined, ActivityTimeline shows its built-in skeleton.
    - These tests MUST FAIL initially (the page does not yet have independent queries — it still uses the global gate). They turn green after 75-02.
  </behavior>
  <read_first>
    - taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx (lines 1-129 — vi.mock-first ordering, makeQueryClient factory, MemoryRouter render helper, delayed-loading mock)
    - taskflow/src/routes/dashboard/IssueDetailPage.tsx (current query structure lines 77-92, comments derivation line 127, ActivityTimeline call ~412)
    - .planning/phases/75-progressive-issue-detail-rendering/75-RESEARCH.md (§Validation Architecture lines 661-683)
    - .planning/phases/75-progressive-issue-detail-rendering/75-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements)
    - .planning/phases/75-progressive-issue-detail-rendering/75-PATTERNS.md (test analog, lines 429-483)
  </read_first>
  <action>
    Create `IssueDetailPage.progressive.test.tsx` following the AioTestRunsSection.test.tsx structure: `vi.mock` declarations FIRST (settings.store, auth.store, `@/services/stronghold` readSecret → 'mock-token', `@/services/jira` mocking `fetchIssueDetail`/`fetchEnrichedSubtasks`/`fetchEpicStories`, `@/services/jira/comments` mocking `fetchComments`, `@/services/jira/changelog` mocking `fetchIssueChangelog`, `@/hooks/useDelayedLoading`), then imports, then a `makeQueryClient()` factory with `retry:false`, then a `renderPage()` helper wrapping `QueryClientProvider` + `MemoryRouter initialEntries={['/issue/PROJ-123']}`.

    Write three `it` blocks binding the behaviors above: (1) base resolves + comments pending → assert header text present via `screen.getByText` AND comments not-yet-rendered; (2) `useDelayedLoading` mock returns true + comments isPending → assert `getByTestId('comments-skeleton')`; subtasks isPending → `getByTestId('subtasks-skeleton')`; (3) changelog mock returns a never-resolving / undefined-data promise → assert ActivityTimeline skeleton present.

    These reference `fetchIssueComments`/`fetchComments`, `fetchIssueChangelog`, `fetchEnrichedSubtasks`, `comments-skeleton`, `subtasks-skeleton` — identifiers that 75-02 wires up. The scaffold is EXPECTED TO FAIL now (RED). Do not weaken assertions to make it pass; mark it as the Wave 0 gate.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/IssueDetailPage.progressive.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `vi.mock` calls for `@/services/jira/comments` and `@/services/jira/changelog`.
    - Test contains assertions on `getByTestId('comments-skeleton')` and `getByTestId('subtasks-skeleton')`.
    - Running the file shows tests RED (failing) — this is the expected Wave 0 state; record the failure output in the SUMMARY.
    - The rest of the suite (`cd taskflow && npm run test`) is otherwise unaffected (no import errors break other files).
  </acceptance_criteria>
  <done>Failing progressive test scaffold exists covering PERF-DETAIL-01/02; will turn green after 75-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView → Jira REST v2 | Bearer-PAT-authenticated reads; same boundary as every existing Jira fetch |

## STRIDE Threat Register

This is a client-side rendering refactor that splits an existing authenticated read into three authenticated reads on the same REST v2 surface. No new endpoints, no new inputs, no new auth flow. Comment/changelog content is already fetched and rendered today.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Information Disclosure | PAT in query keys | accept | PAT is read inside `queryFn` via `readSecret`, never placed in query keys (existing convention preserved across all three new fetchers) — no new exposure |
| T-75-02 | Tampering | Comment/changelog field rendering (XSS) | accept | Fields are rendered by the existing wiki renderer with existing sanitization; this phase changes WHERE the data is fetched, not how it is rendered — no new sink |
| T-75-03 | Spoofing | New fetchers call wrong host | mitigate | `fetchIssueChangelog`/`fetchEnrichedSubtasks` build URLs from the same `baseUrl` arg and `apiFetch('jira', ...)` provider as every existing fetcher — no host derivation change |
| T-75-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH §Package Legitimacy Audit: N/A) |

No NEW high-severity threats are introduced by the decomposition. Existing XSS/auth handling on the rendered fields is unchanged.
</threat_model>

<verification>
- `cd taskflow && npm run build` green (tsc + vite).
- `fetchIssueChangelog`, `fetchEnrichedSubtasks` exported; `fetchIssueDetail` slimmed (no comment field, no expand=changelog, no inline enrichment).
- Both skeletons render at UI-SPEC dimensions; no `font-semibold` added.
- `IssueDetailPage.progressive.test.tsx` exists and is RED (Wave 0 gate).
- No `greenhopper` reference introduced in any detail-panel fetch path (GH-CUT-01).
</verification>

<success_criteria>
- Base fetch decomposed into base + 3 independent REST v2 paths (D-01/D-02).
- Skeletons and failing test scaffold ready for the page rewrite to consume.
- Build green; rest of suite unbroken.
</success_criteria>

<output>
Create `.planning/phases/75-progressive-issue-detail-rendering/75-01-SUMMARY.md` when done. Record the RED test output from Task 3.
</output>
