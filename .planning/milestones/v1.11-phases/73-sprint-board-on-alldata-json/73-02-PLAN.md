---
phase: 73-sprint-board-on-alldata-json
plan: 02
type: execute
wave: 2
depends_on:
  - 73-01
files_modified:
  - taskflow/src/routes/dashboard/SprintBoardTab.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
  - taskflow/src/routes/dashboard/TaskCard.tsx
  - taskflow/src/routes/dashboard/TaskCard.test.tsx
autonomous: true
requirements:
  - GH-BOARD-01
  - GH-BOARD-02
  - GH-BOARD-03
  - GH-BOARD-04
must_haves:
  truths:
    - "Opening the sprint board issues exactly ONE allData.json request (the six-query path is gone for stories+subtasks; activeSprint/epicsBasic/boardQuickFilters/projectStatuses remain per R-01/R-02)"
    - "Issues bucket into the existing 3-column UI (To Do / In Progress / Done) by statusCategory.key resolved via the Phase 71 entity-map (D-03 / D-03a)"
    - "Subtasks group under their parent story via fields.parent?.key on the AdaptedIssue (D-04)"
    - "Orphan subtasks (parentId present, parent absent from issuesData) render as standalone cards; warnOnce('orphan-subtask', parentId) fires once per parent (D-04b)"
    - "Each card whose adapted issue carries timeInColumn.enteredStatus shows a small badge (e.g. '3d') with title 'Entered status N ago' (D-05 / R-03 — Intl.RelativeTimeFormat helper)"
    - "Drag-to-transition, QuickCreateInput, epic / quick-filter / label / saved-filter, allDoneFingerprint, and sprint goal banner continue to work unchanged (GH-BOARD-04)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/SprintBoardTab.tsx"
      provides: "Sprint board now reads from useGhAllData; six legacy queries collapsed to four (allData replaces stories+subtasks; activeSprint/epics/quickfilters/statuses kept per R-01/R-02)"
      contains: "useGhAllData"
    - path: "taskflow/src/routes/dashboard/TaskCard.tsx"
      provides: "timeInColumn badge slot inside the existing shrink-0 row, after story-points chip"
      contains: "timeInColumn"
  key_links:
    - from: "taskflow/src/routes/dashboard/SprintBoardTab.tsx"
      to: "@/services/jira"
      via: "named import useGhAllData / invalidateGhAllData / buildEntityMaps / createAdapter"
      pattern: "useGhAllData"
    - from: "taskflow/src/routes/dashboard/SprintBoardTab.tsx"
      to: "AdaptedIssue[]"
      via: "useMemo adapter pass over allData.issuesData.issues with storyPointsFieldKey threaded"
      pattern: "createAdapter\\("
    - from: "taskflow/src/routes/dashboard/TaskCard.tsx"
      to: "@/lib/formatTimeAgo"
      via: "named import formatTimeAgo + formatTimeAgoStrict"
      pattern: "formatTimeAgo"
---

<objective>
Rewrite the data layer of `SprintBoardTab.tsx` so the board fetches via `useGhAllData(boardId)` (Plan 01), adapts issues via Phase 71 `createAdapter` in a `useMemo`, and surfaces `timeInColumn` on each card via a new `TaskCard` badge slot. This is the core "what the user sees" plan: after Plan 02, opening the board issues a single `allData.json` GET, columns render via `statusCategory.key`, subtasks group under stories, and each card shows its time-in-column.

Purpose: Fulfills GH-BOARD-01 (single allData call), GH-BOARD-02 (timeInColumn surfaced), GH-BOARD-03 interpreted per D-03/D-03a (GH is the data source; 3-bucket UI preserved), and the read-side of GH-BOARD-04 (existing features unchanged on the new source). The "Reload board" toolbar action, Sidebar prefetch swap, and `fetchSprintSubtasks` deletion are deferred to Plan 03 to keep this plan a focused single-concern rewrite.

Output: SprintBoardTab and TaskCard updated; existing SprintBoardTab tests updated; TaskCard badge render test added.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-sprint-board-on-alldata-json/73-CONTEXT.md
@.planning/phases/73-sprint-board-on-alldata-json/73-RESEARCH.md
@.planning/phases/73-sprint-board-on-alldata-json/73-PATTERNS.md
@.planning/phases/73-sprint-board-on-alldata-json/73-UI-SPEC.md
@.planning/phases/73-sprint-board-on-alldata-json/73-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add timeInColumn badge slot to TaskCard (D-05, R-03, GH-BOARD-02)</name>
  <files>taskflow/src/routes/dashboard/TaskCard.tsx, taskflow/src/routes/dashboard/TaskCard.test.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/TaskCard.tsx (lines 30-45 props interface; lines 140-160 the shrink-0 badge row — the exact insertion slot)
    - taskflow/src/lib/formatTimeAgo.ts (from Plan 01 — exports formatTimeAgo + formatTimeAgoStrict)
    - .planning/phases/73-sprint-board-on-alldata-json/73-UI-SPEC.md §"1. timeInColumn Badge" (markup contract + className list)
    - .planning/phases/73-sprint-board-on-alldata-json/73-PATTERNS.md §"TaskCard.tsx" (new prop signature + insertion slot)
    - .planning/phases/73-sprint-board-on-alldata-json/73-RESEARCH.md §"Finding 5" + §"Pitfall 6" (storyPointsFieldKey threading reminder for caller)
    - taskflow/src/services/jira/greenhopper/adapter.ts (line 155 — confirms `timeInColumn` is on AdaptedIssue)
    - taskflow/src/routes/dashboard/TaskCard.test.tsx if it exists; otherwise treat as Wave-0 new file
  </read_first>
  <behavior>
    - Given timeInColumn prop is undefined → no badge rendered (assert no element with title prefix "Entered status")
    - Given timeInColumn.enteredStatus = Date.now() - 90_000_000 → a span with className containing "text-[11px]" and "bg-muted" renders, text matches /^\d+[smhd]$/ (e.g. "1d"), title starts with "Entered status "
    - Badge sits inside the existing `shrink-0` row, after the story-points chip, before the showStatus badge — assert DOM order in the row
  </behavior>
  <action>
Extend `TaskCardProps` (lines ~30-45) with one new optional prop per UI-SPEC §1 and PATTERNS.md "TaskCard.tsx" — option (b) "separate prop preserves backward compatibility" (chosen over widening `issue` type):
`timeInColumn?: { enteredStatus: number; durationPreviously?: number }`

Inside the existing `<div className="flex items-center gap-1.5 shrink-0">` row (around `TaskCard.tsx:144`), append a new conditional `<span>` AFTER the story-points chip and BEFORE the `showStatus` badge. Use the exact className from UI-SPEC §1 ("text-[11px] text-muted-foreground bg-muted rounded-full px-1.5 py-0.5 font-mono leading-none"). Use `formatTimeAgoStrict(timeInColumn.enteredStatus)` for the text and `` `Entered status ${formatTimeAgo(timeInColumn.enteredStatus)} ago` `` for the `title` attribute per UI-SPEC + D-05a. No Radix Tooltip wrapper, no `aria-label` (the badge is decorative metadata).

Render condition: only when `timeInColumn?.enteredStatus` is truthy (per UI-SPEC §1).

Imports: add `formatTimeAgo, formatTimeAgoStrict` from `@/lib/formatTimeAgo` (Plan 01 artifact). Do NOT add `date-fns` (R-03).

Update `TaskCard.test.tsx` (or create if it doesn't already cover render) with three test cases matching the behavior block. Use `vi.useFakeTimers()` to make the relative time deterministic. Render with `@testing-library/react` and assert via `screen.getByTitle(/^Entered status /)` for the positive case.

Do NOT change `isSubtask`, `subtaskCount`, `onToggle`, drag/drop, status pill, or any other TaskCard surface. Storybook / consumers other than SprintBoardTab (e.g. backlog rows) continue to omit the new prop — backward compatible.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/routes/dashboard/TaskCard.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "timeInColumn\\??:" taskflow/src/routes/dashboard/TaskCard.tsx | grep -v '^#' | wc -l` ≥ 1 (prop declared)
    - `grep -F "formatTimeAgoStrict" taskflow/src/routes/dashboard/TaskCard.tsx` returns ≥1 match
    - No `date-fns` import in TaskCard.tsx (grep returns nothing)
    - Three test cases (undefined prop / present prop / DOM position) green
    - tsc clean for TaskCard.tsx and its test
  </acceptance_criteria>
  <done>TaskCard accepts optional timeInColumn prop and renders the badge per UI-SPEC §1; backward-compatible with all existing callers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite SprintBoardTab data layer onto useGhAllData (D-01, D-03, D-04, D-04b, R-01, R-02, R-04, GH-BOARD-01/03/04)</name>
  <files>taskflow/src/routes/dashboard/SprintBoardTab.tsx, taskflow/src/routes/dashboard/SprintBoardTab.test.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/SprintBoardTab.tsx (lines 14-57 imports; 599-734 query block; 731 sentinelProjectId; 721-723 setLocalIssues effect; 754-774 reload handler; 876-886 subtask grouping; 890 allDoneFingerprint; 938 savedFilterIssueKeys; 1130-1162 toolbar — toolbar replacement is Plan 03; keep BOTH existing reload buttons untouched in THIS task)
    - taskflow/src/services/jira/greenhopper/adapter.ts (createAdapter signature; adaptIssue produces AdaptedIssue with timeInColumn, fields.parent when both parentId+parentKey present, statusCategory via entity-maps)
    - taskflow/src/services/jira/greenhopper/entityMaps.ts (buildEntityMaps signature; takes allData.entityData)
    - taskflow/src/services/jira/greenhopper/warnOnce.ts (warnOnce(kind, id) signature)
    - .planning/phases/73-sprint-board-on-alldata-json/73-CONTEXT.md §"research_resolutions" R-01/R-02/R-04 (DO NOT delete activeSprint, boardQuickFilters, or `fetchBoardQuickFilters` import)
    - .planning/phases/73-sprint-board-on-alldata-json/73-PATTERNS.md §"SprintBoardTab.tsx (component, data-layer swap)"
    - .planning/phases/73-sprint-board-on-alldata-json/73-RESEARCH.md §"Pattern 2: SprintBoardTab data layer replacement" (table of keep/remove per query)
    - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx (existing mock setup — update fetchSprintStories/fetchSprintSubtasks mocks to mock useGhAllData instead)
    - taskflow/src/services/jira/greenhopper/__fixtures__/allData.real.json (fixture used to seed mock useGhAllData responses)
  </read_first>
  <behavior>
    - Mocked useGhAllData returning a fixture allData with N stories + M subtasks → SprintBoardTab renders N+M cards bucketed into the three columns by statusCategory.key (assert per-column counts)
    - Mocked useGhAllData returning a subtask whose parentId references a story NOT in issuesData → that subtask renders as a standalone card in its column; console.warn fires exactly once for the orphan parentId
    - Mocked useGhAllData with a story carrying timeInColumn.enteredStatus → TaskCard for that story shows the badge (assert via title attribute)
    - fetchSprintStories / fetchSprintSubtasks are NOT called during board open (useQuery for the legacy keys is gone)
    - activeSprint / boardQuickFilters / epicsBasic / projectStatuses queries still fire (R-01 / R-02 / D-09a)
    - sprint goal banner still renders when activeSprint.goal is present (sourced from REST per R-02)
  </behavior>
  <action>
Rewrite the data layer of `SprintBoardTab.tsx` per D-01 / D-03 / D-04 / D-04b plus R-01 / R-02 / R-04. This is a precise surgical rewrite — do not redesign unrelated surfaces.

(1) Imports (lines 14-57): from `@/services/jira` remove `fetchSprintStories` and `fetchSprintSubtasks` ONLY; add `useGhAllData`, `buildEntityMaps`, `createAdapter`, and the `AdaptedIssue` type. Keep `fetchEpicsBasic`, `fetchActiveSprint`, `fetchBoardQuickFilters`, `fetchProjectStatuses` per R-01/R-02/D-09a. Add `warnOnce` from `@/services/jira/greenhopper/warnOnce` (internal import — same package). Do NOT add `invalidateGhAllData` or remove the `Workflow` lucide import in this task — toolbar work is Plan 03.

(2) Replace the stories+subtasks `useQuery` block (lines ~599-641) with `const { data: allData, isLoading, isFetching, isError, error, dataUpdatedAt } = useGhAllData(boardId);`. Keep the remaining four queries (epics, activeSprint, boardQuickFilters, projectStatuses) verbatim — R-01/R-02 require this. Do NOT touch the `['jira-active-sprint', ...]` or `['jira-board-quickfilters', ...]` keys; D-09b does NOT fire because allData has no sprint goal (R-02) and no structured quick filters (R-01).

(3) Build adapter pipeline (D-01 returns raw envelope; caller adapts):
   - `entityMaps = useMemo(() => allData ? buildEntityMaps(allData.entityData) : null, [allData])`
   - `adapt = useMemo(() => entityMaps ? createAdapter({ storyPointsFieldKey, entityMaps }) : null, [storyPointsFieldKey, entityMaps])` — thread `storyPointsFieldKey` from `useSettingsStore` (per Pitfall 6: missing key zeros all story-point chips)
   - `adaptedIssues = useMemo<AdaptedIssue[]>(() => { if (!allData || !adapt) return []; return allData.issuesData.issues.map((gh) => { if (gh.parentId !== undefined && gh.parentKey === undefined) warnOnce('orphan-subtask', String(gh.parentId)); return adapt(gh); }); }, [allData, adapt])`

(4) Replace the `setLocalIssues(data ?? [])` effect with `setLocalIssues(adaptedIssues)` (or update the effect deps). `localIssues` becomes `AdaptedIssue[]`; assignable to `JiraIssue[]` consumer slots via structural compatibility — but where the codebase narrows by checking JiraIssue-specific fields, prefer leaving the variable typed `AdaptedIssue[]` and casting at call boundaries if tsc complains. Do not introduce `any`.

(5) `sentinelProjectId` (line ~731) per R-04: source from raw envelope, NOT adapted issues — `const sentinelProjectId = allData?.issuesData.issues[0]?.projectId ?? 0;`. Pass this number to `useGhTransitions(sentinelProjectId, sentinelIssueTypeId)`.

(6) Subtask grouping at lines ~876-886: NO CHANGE. The adapter sets `fields.parent.key` when both `parentId` and `parentKey` are present (confirmed by fixture and adapter.ts:104-112). Existing grouping by `fields.parent?.key` continues to work; orphan subtasks (parent absent from issuesData) flow into `storyIssues` as standalone cards per D-04b.

(7) allDoneFingerprint (line ~890): NO CHANGE. `categoryOf(issue)` uses `issue.fields.status.statusCategory?.key` which the adapter populates via entity-map resolution (D-03 / D-03a).

(8) Pass `timeInColumn` from each adapted issue into the existing `TaskCard` render — find the TaskCard JSX usage(s) in this file and add `timeInColumn={issue.timeInColumn}` prop (introduced in Task 1). Apply to every TaskCard render site in this file. Do NOT introduce conditional rendering — TaskCard handles `undefined` itself.

(9) Toolbar (lines 1130-1162): leave BOTH existing reload buttons (`handleReloadWorkflowTransitions` + bare RefreshCw) UNTOUCHED. Plan 03 collapses them into "Reload board". This keeps Plan 02 a pure data-layer + card-badge rewrite.

(10) Update `SprintBoardTab.test.tsx` mocks:
   - Replace `vi.mock(...)` for `fetchSprintStories` / `fetchSprintSubtasks` with `vi.mock('@/services/jira', () => ({ useGhAllData: vi.fn(...), ...other existing mocks }))`.
   - Seed `useGhAllData` mock with a trimmed allData fixture (≥3 issues spanning all three statusCategory keys: 'new', 'indeterminate', 'done'; one subtask with parentId pointing to an in-fixture story; one orphan subtask).
   - Add the three behavior assertions from the `<behavior>` block.
   - Keep existing assertions on filters, savedFilterIssueKeys, sprint goal banner — they should still pass.

Verify after each chunk with `cd taskflow && npx vitest run src/routes/dashboard/SprintBoardTab.test.tsx --reporter=dot`.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/routes/dashboard/SprintBoardTab.test.tsx --reporter=dot &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "useGhAllData(" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns ≥1 match
    - `grep -E "fetchSprintStories|fetchSprintSubtasks" taskflow/src/routes/dashboard/SprintBoardTab.tsx | grep -v '^//' | wc -l` == 0
    - `grep -E "fetchActiveSprint|fetchBoardQuickFilters|fetchEpicsBasic|fetchProjectStatuses" taskflow/src/routes/dashboard/SprintBoardTab.tsx | grep -v '^//' | wc -l` ≥ 4 (R-01/R-02/D-09a kept)
    - `grep -F "allData?.issuesData.issues[0]?.projectId" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns ≥1 match (R-04)
    - `grep -F "warnOnce(" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns ≥1 match (D-04b)
    - `grep -F "createAdapter(" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns ≥1 match
    - `grep -F "timeInColumn=" taskflow/src/routes/dashboard/SprintBoardTab.tsx` returns ≥1 match (badge wired)
    - Existing + new SprintBoardTab tests green; tsc clean
  </acceptance_criteria>
  <done>Board renders from useGhAllData; six-query path collapsed to four; timeInColumn badge wired through; orphan subtask + statusCategory bucketing behave per D-03/D-04b; activeSprint + quickFilters preserved per R-01/R-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React Query cache ↔ render | Adapted issue data flows directly to TaskCard; no user input boundary |
| GH allData envelope ↔ adapter | Untrusted shape from server is normalized by Phase 71 `adaptIssue` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-04 | Tampering | `allData.issuesData.issues[*].parentId` → orphan-subtask path | mitigate | `warnOnce('orphan-subtask', parentId)` provides observability; UI degrades gracefully (standalone card) per D-04b |
| T-73-05 | Information Disclosure | `timeInColumn.enteredStatus` exposed in card chrome | accept | Same trust class as existing status pill; no PII; UI-SPEC §1 muted styling avoids implying urgency |
| T-73-06 | Denial of Service | Per-issue adapter pass inside React render | mitigate | Adapter wrapped in `useMemo` keyed on `[allData, adapt]`; re-runs only on data ref change (avoids per-render N×156 cost) |
</threat_model>

<verification>
- `npx vitest run src/routes/dashboard/SprintBoardTab.test.tsx src/routes/dashboard/TaskCard.test.tsx` green
- `npx tsc --noEmit` clean
- `npx biome check src/routes/dashboard/SprintBoardTab.tsx src/routes/dashboard/TaskCard.tsx` clean (no regression of 0/0 baseline)
- Network-log spot-check at end of plan: open sprint board with devtools network panel; assert exactly one `/rest/greenhopper/1.0/xboard/work/allData.json` GET and zero `jira-sprint-stories` / `jira-sprint-subtasks` REST calls (deferred to Plan 03 verification if executor cannot run app)
</verification>

<success_criteria>
- GH-BOARD-01: legacy `['jira-sprint-stories', ...]` and `['jira-sprint-subtasks', ...]` useQuery blocks removed from SprintBoardTab; `useGhAllData` is the sole issue source
- GH-BOARD-02: TaskCard renders the timeInColumn badge for every adapted issue with `timeInColumn.enteredStatus` present
- GH-BOARD-03 (per D-03/D-03a): 3-bucket UI preserved; bucketing driven by `statusCategory.key` resolved through Phase 71 entity-maps
- GH-BOARD-04: filters, saved-filter, allDoneFingerprint, sprint goal banner, board-level error state all continue to work in tests
- Memory `[[project_biome_state]]` 0/0 baseline preserved
</success_criteria>

<output>
Create `.planning/phases/73-sprint-board-on-alldata-json/73-02-SUMMARY.md` when done.
</output>
