---
phase: 73-sprint-board-on-alldata-json
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/jira/greenhopper/useGhAllData.ts
  - taskflow/src/services/jira/greenhopper/useGhAllData.test.ts
  - taskflow/src/services/jira/greenhopper/index.ts
  - taskflow/src/services/jira.ts
  - taskflow/src/lib/formatTimeAgo.ts
  - taskflow/src/lib/formatTimeAgo.test.ts
autonomous: true
requirements:
  - GH-BOARD-01
  - GH-BOARD-02
must_haves:
  truths:
    - "useGhAllData(boardId) returns the raw GhAllDataResponse envelope and polls every POLL_INTERVAL_MS while the sprint-board route is active"
    - "getGhAllData warms the same cache key SprintBoardTab reads"
    - "invalidateGhAllData invalidates one board or all boards"
    - "useGhAllData, getGhAllData, invalidateGhAllData are importable from @/services/jira (the dual-file rule)"
    - "formatTimeAgoStrict / formatTimeAgo render unix-ms enteredStatus into UI-SPEC-shaped strings using Intl.RelativeTimeFormat (no date-fns)"
  artifacts:
    - path: "taskflow/src/services/jira/greenhopper/useGhAllData.ts"
      provides: "useGhAllData hook + getGhAllData + invalidateGhAllData"
      exports: ["useGhAllData", "getGhAllData", "invalidateGhAllData"]
    - path: "taskflow/src/services/jira/greenhopper/useGhAllData.test.ts"
      provides: "Hook + helper behavior tests"
    - path: "taskflow/src/lib/formatTimeAgo.ts"
      provides: "Intl.RelativeTimeFormat-based time-ago helpers per R-03"
      exports: ["formatTimeAgo", "formatTimeAgoStrict"]
    - path: "taskflow/src/services/jira/greenhopper/index.ts"
      provides: "Barrel re-export of useGhAllData module"
      contains: "export * from './useGhAllData'"
    - path: "taskflow/src/services/jira.ts"
      provides: "Public surface re-export"
      contains: "useGhAllData"
  key_links:
    - from: "taskflow/src/services/jira/greenhopper/useGhAllData.ts"
      to: "taskflow/src/services/jira/greenhopper/allData.ts"
      via: "fetchAllData(baseUrl, token, boardId)"
      pattern: "fetchAllData\\("
    - from: "taskflow/src/services/jira.ts"
      to: "taskflow/src/services/jira/greenhopper/index.ts"
      via: "named re-export of useGhAllData/getGhAllData/invalidateGhAllData"
      pattern: "useGhAllData"
---

<objective>
Ship the data-layer foundation for Phase 73 (per D-01, D-02, R-03): a typed React Query hook + imperative + invalidator around `fetchAllData`, exposed through `src/services/jira.ts`, plus a shared `Intl.RelativeTimeFormat`-based time-ago helper that TaskCard will consume in Plan 02 for the `timeInColumn` badge.

Purpose: Plan 02 (SprintBoardTab rewrite) and Plan 03 (Sidebar prefetch swap, Reload-board action) both depend on a stable public surface (`useGhAllData`, `getGhAllData`, `invalidateGhAllData`) routed through `jira.ts` per memory `[[project_jira_ts_dual_file]]`. Defining these interfaces in Plan 01 prevents a scavenger-hunt in downstream plans.

Output: New hook module + tests, barrel addition, `jira.ts` re-export, shared time-ago helper + tests.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-sprint-board-on-alldata-json/73-CONTEXT.md
@.planning/phases/73-sprint-board-on-alldata-json/73-RESEARCH.md
@.planning/phases/73-sprint-board-on-alldata-json/73-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useGhAllData hook + getGhAllData + invalidateGhAllData (D-01, D-02)</name>
  <files>taskflow/src/services/jira/greenhopper/useGhAllData.ts, taskflow/src/services/jira/greenhopper/useGhAllData.test.ts</files>
  <read_first>
    - taskflow/src/services/jira/greenhopper/transitions.ts (lines 307-358 — canonical hook analog `useGhTransitions` + `getGhTransitions` + `invalidateGhTransitions`)
    - taskflow/src/services/jira/greenhopper/allData.ts (line 21 — `fetchAllData(baseUrl, token, boardId): Promise<GhAllDataResponse>`)
    - taskflow/src/services/jira/greenhopper/types.ts (`GhAllDataResponse` shape)
    - taskflow/src/lib/query-constants.ts (`POLL_INTERVAL_MS`, `STALE_TIME_MS` — DO NOT modify per D-06)
    - taskflow/src/hooks/useIsActiveRoute.ts (route gating)
    - taskflow/src/stores/auth.store.ts (jiraBaseUrl selector)
    - taskflow/src/services/stronghold.ts (readSecret API)
    - .planning/phases/73-sprint-board-on-alldata-json/73-PATTERNS.md §"useGhAllData.ts"
    - .planning/phases/73-sprint-board-on-alldata-json/73-RESEARCH.md §"Pattern 1: useGhAllData hook"
    - taskflow/src/services/jira/greenhopper/transitions.test.ts (test scaffolding analog for the new test file)
  </read_first>
  <behavior>
    - useGhAllData(null) → query.enabled is false; fetchAllData NOT called
    - useGhAllData(123) when route active + baseUrl + token present → query.enabled is true; fetchAllData called once with (baseUrl, token, 123); returns the resolved GhAllDataResponse as `data` (raw envelope, not adapted — per D-01)
    - useGhAllData uses queryKey ['gh-all-data', boardId], refetchInterval = POLL_INTERVAL_MS, refetchIntervalInBackground = false, staleTime = STALE_TIME_MS
    - getGhAllData(qc, baseUrl, token, boardId) calls queryClient.ensureQueryData with the matching key+queryFn and returns the response (Plan 03 Sidebar prefetch reads this)
    - invalidateGhAllData(qc) invalidates ['gh-all-data'] (all boards)
    - invalidateGhAllData(qc, 7) invalidates ['gh-all-data', 7] only
  </behavior>
  <action>
Create `useGhAllData.ts` per D-01 / D-02 / R-03 mirroring the Phase 72 canonical analog `useGhTransitions` in `transitions.ts:307-358`. Module exports exactly three symbols: `useGhAllData(boardId: number | null)`, `getGhAllData(queryClient, baseUrl, token, boardId)`, `invalidateGhAllData(queryClient, boardId?)`.

Hook contract (D-01): `useQuery<GhAllDataResponse>({ queryKey: ['gh-all-data', boardId], queryFn: () => fetchAllData(jiraBaseUrl as string, token as string, boardId as number), refetchInterval: POLL_INTERVAL_MS, refetchIntervalInBackground: false, staleTime: STALE_TIME_MS, enabled: isActive && !!boardId && !!jiraBaseUrl && !!token })`. Do NOT use `staleTime: Infinity` (that's a transitions-specific choice; allData polls).

Auth pattern (must match the analog exactly): read `jiraBaseUrl` from `useAuthStore`; load token via `useEffect`+`readSecret('jira-pat')` with the cancelled-flag pattern; re-read when `jiraBaseUrl` changes. Route gate: `useIsActiveRoute('/sprint-board')`.

Return raw `GhAllDataResponse` envelope — adaptation happens caller-side in Plan 02 via `useMemo` (locked by D-01 / Discretion section). Do NOT call `adaptIssue` or `buildEntityMaps` inside this hook.

`getGhAllData` uses `queryClient.ensureQueryData` with the same key + `staleTime: STALE_TIME_MS` and returns `Promise<GhAllDataResponse>` — used by Plan 03 Sidebar prefetch warm (D-08).

`invalidateGhAllData(qc, boardId?)`: when `boardId` is undefined invalidate `['gh-all-data']`, otherwise invalidate `['gh-all-data', boardId]`. Used by Plan 03 Reload-board action (D-07 + R-01 + R-02 expanded invalidation set).

Create `useGhAllData.test.ts` using the vitest+@testing-library/react patterns established in `transitions.test.ts`. Tests cover the four behaviors above. Mock `fetchAllData`, `readSecret`, `useAuthStore`, `useIsActiveRoute`. Wrap rendered hook in a fresh `QueryClientProvider` per test (no shared cache). Assert `fetchAllData` call count is exactly 1 for the enabled case and 0 for disabled cases.

Do NOT import `warnOnce`, `buildEntityMaps`, or `adaptIssue` — keep the hook a thin data-fetch wrapper.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/services/jira/greenhopper/useGhAllData.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - File `taskflow/src/services/jira/greenhopper/useGhAllData.ts` exports exactly: `useGhAllData`, `getGhAllData`, `invalidateGhAllData`
    - Hook queryKey shape literally `['gh-all-data', boardId]` (grep: `grep -F "['gh-all-data'" taskflow/src/services/jira/greenhopper/useGhAllData.ts` returns ≥1 match)
    - Hook uses `POLL_INTERVAL_MS` and `STALE_TIME_MS` imports from `lib/query-constants.ts`; no inline numeric literals for these values
    - Hook does NOT contain `Infinity`, `adaptIssue`, `buildEntityMaps`, or `createAdapter`
    - `useGhAllData.test.ts` passes; all four behavior cases asserted; `fetchAllData` mock asserted with exact call counts
    - `npx tsc --noEmit` clean for new files
  </acceptance_criteria>
  <done>useGhAllData / getGhAllData / invalidateGhAllData defined; tests green; types compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Intl.RelativeTimeFormat-based time-ago helpers (R-03, GH-BOARD-02)</name>
  <files>taskflow/src/lib/formatTimeAgo.ts, taskflow/src/lib/formatTimeAgo.test.ts</files>
  <read_first>
    - taskflow/src/routes/dashboard/IssueDetailContent.tsx (lines 37-45 — canonical Intl.RelativeTimeFormat pattern)
    - .planning/phases/73-sprint-board-on-alldata-json/73-CONTEXT.md §"research_resolutions" R-03
    - .planning/phases/73-sprint-board-on-alldata-json/73-RESEARCH.md §"Finding 3" + §"Inline time-ago helper"
    - .planning/phases/73-sprint-board-on-alldata-json/73-PATTERNS.md §"Time formatting helpers"
    - taskflow/src/services/jira/greenhopper/__fixtures__/allData.real.json (lines 454-460 — confirms `enteredStatus` is unix milliseconds, NOT ISO)
    - taskflow/package.json (CONFIRM `date-fns` is absent — do not add a dependency)
  </read_first>
  <behavior>
    - formatTimeAgoStrict(now - 30_000) === "30s"; ...90_000 === "1m"; ...3_900_000 === "1h"; ...90_000_000 === "1d"
    - formatTimeAgo(now - 90_000_000) returns natural phrasing produced by Intl.RelativeTimeFormat (e.g. "yesterday" or "1 day ago" depending on locale defaults) — assert it is a non-empty string containing "day" or "yesterday"
    - Both helpers accept a unix-millisecond number; both treat future timestamps (enteredStatus &gt; Date.now()) by clamping to "0s" / "now"
    - No exception is thrown for boundary inputs: 0, Date.now(), Date.now() - 1
  </behavior>
  <action>
Create `src/lib/formatTimeAgo.ts` with two pure exports per R-03 (date-fns is NOT installed; supersedes UI-SPEC §1 reference to `formatDistanceToNowStrict`):

`formatTimeAgoStrict(enteredStatusMs: number): string` — compact badge text. Derive `diffSecs = Math.floor((Date.now() - enteredStatusMs) / 1000)`. Output thresholds:
  - `diffSecs < 0`: return `"0s"`
  - `< 60`: `${diffSecs}s`
  - `< 3600`: `${Math.floor(diffSecs/60)}m`
  - `< 86400`: `${Math.floor(diffSecs/3600)}h`
  - else: `${Math.floor(diffSecs/86400)}d`

`formatTimeAgo(enteredStatusMs: number): string` — natural phrasing for the badge `title` attribute. Use `new Intl.RelativeTimeFormat('en', { numeric: 'auto' })` (matches `IssueDetailContent.tsx:37-45`). Same bucket cutoffs as strict; call `rtf.format(-diffUnits, unit)` for the appropriate unit (`'second'`, `'minute'`, `'hour'`, `'day'`). Future timestamps → `"now"`.

These helpers are colocated in `lib/` so Plan 02 TaskCard imports them without coupling to the greenhopper module surface.

Create `formatTimeAgo.test.ts` with vitest. Use `vi.useFakeTimers()` + `vi.setSystemTime(new Date('2026-05-29T12:00:00Z'))` to make assertions deterministic. Cover all four buckets and the future-timestamp clamp. Do NOT assert exact locale strings for `formatTimeAgo` past containment (Node Intl output is locale-implementation-specific) — assert non-empty + presence of unit substring.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/lib/formatTimeAgo.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/formatTimeAgo.ts` exports `formatTimeAgo` and `formatTimeAgoStrict`
    - No import of `date-fns` anywhere (grep: `grep -r "date-fns" taskflow/src/lib/formatTimeAgo.ts` returns nothing)
    - All test cases pass under fake timers; no flake on real-time CI
    - `formatTimeAgoStrict(now)` returns `"0s"`; `formatTimeAgoStrict(now - 90 * 86_400_000)` returns `"90d"`
    - `grep -F "Intl.RelativeTimeFormat" taskflow/src/lib/formatTimeAgo.ts` returns ≥1 match
  </acceptance_criteria>
  <done>formatTimeAgo helpers exist, tested, framework-free; no date-fns added.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the new public surface — barrel + jira.ts re-export (D-02, memory dual-file rule)</name>
  <files>taskflow/src/services/jira/greenhopper/index.ts, taskflow/src/services/jira.ts</files>
  <read_first>
    - taskflow/src/services/jira/greenhopper/index.ts (current barrel — append-only, see PATTERNS.md)
    - taskflow/src/services/jira.ts (lines 2688-2706 — Phase 72 GH re-export block; this is the exact insertion point)
    - .planning/phases/73-sprint-band-on-alldata-json/73-CONTEXT.md §"D-02" + §"Legacy Cleanup Scope D-09"
    - .planning/phases/73-sprint-board-on-alldata-json/73-PATTERNS.md §"jira.ts (re-export surface)"
    - memory `[[project_jira_ts_dual_file]]`
  </read_first>
  <action>
Add the public surface per D-02 and the dual-file memory rule.

(a) `src/services/jira/greenhopper/index.ts`: append `export * from './useGhAllData';` to the existing barrel. Do NOT re-order the other exports. `warnOnce` stays internal (not barrel-exported).

(b) `src/services/jira.ts`: extend the named-export block at lines ~2688-2706 (Phase 72 GH block) to also re-export `useGhAllData`, `getGhAllData`, and `invalidateGhAllData` from `./jira/greenhopper`. Keep the existing alphabetical-ish ordering of the block.

DO NOT in this task delete `fetchSprintSubtasks` or `fetchBoardQuickFilters` — Plan 03 handles the `fetchSprintSubtasks` deletion after SprintBoardTab no longer calls it; `fetchBoardQuickFilters` STAYS per R-01 (allData has no structured quick filters).

DO NOT modify `src/lib/query-constants.ts` (D-06).

After edits run `npx tsc --noEmit` and `npx biome check src/services/jira.ts src/services/jira/greenhopper/index.ts` — both must be clean (memory `[[project_biome_state]]` is 0/0).
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npx tsc --noEmit &amp;&amp; npx biome check src/services/jira.ts src/services/jira/greenhopper/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "from './useGhAllData'" taskflow/src/services/jira/greenhopper/index.ts` returns 1 match
    - `grep -E "useGhAllData|getGhAllData|invalidateGhAllData" taskflow/src/services/jira.ts | grep -v '^#' | wc -l` ≥ 3
    - `fetchSprintSubtasks` re-export still present in `jira.ts` (deletion is Plan 03 — verifies we did not remove prematurely)
    - `fetchBoardQuickFilters` and `fetchAllData` re-exports unchanged
    - tsc clean, biome clean
  </acceptance_criteria>
  <done>Public surface (useGhAllData / getGhAllData / invalidateGhAllData) importable from `@/services/jira` with zero tsc/biome regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App ↔ GreenHopper API (existing) | Bearer PAT already in use; no new auth surface introduced by this plan |
| React Query cache ↔ React components | Cache holds non-PII workflow metadata only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Information Disclosure | `useGhAllData` query data in browser memory | accept | Sprint board data (issues, columns) is the same trust class already cached for `useGhTransitions`; no PII regression |
| T-73-02 | Tampering | Time-ago helpers consuming `enteredStatus` | accept | Pure functions over a number input; clamp future timestamps in formatTimeAgo to avoid NaN in UI |
| T-73-03 | Denial of Service | Polling cadence | mitigate | Reuse `POLL_INTERVAL_MS`/`STALE_TIME_MS` (D-06) — net request reduction (~6→2 per cycle) is a strict improvement |
</threat_model>

<verification>
- All three task verify commands green
- `npx tsc --noEmit` clean across taskflow/src
- `npx biome check taskflow/src/services/jira.ts taskflow/src/services/jira/greenhopper/ taskflow/src/lib/formatTimeAgo.ts` clean
- No file in `files_modified` other than those listed
</verification>

<success_criteria>
- `useGhAllData`, `getGhAllData`, `invalidateGhAllData` importable from `@/services/jira` (memory dual-file rule)
- `useGhAllData.test.ts` covers enabled / disabled / single-call / invalidation behavior; ≥4 assertions
- `formatTimeAgo` + `formatTimeAgoStrict` exported from `@/lib/formatTimeAgo`; tested with fake timers; no `date-fns` import (R-03)
- Biome and tsc clean — no regression of `[[project_biome_state]]` 0/0 baseline
</success_criteria>

<output>
Create `.planning/phases/73-sprint-board-on-alldata-json/73-01-SUMMARY.md` when done.
</output>
