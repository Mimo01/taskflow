---
phase: 71-greenhopper-adapter-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 71-01
  - 71-02
files_modified:
  - taskflow/src/services/jira/greenhopper/allData.ts
  - taskflow/src/services/jira/greenhopper/allData.test.ts
  - taskflow/src/services/jira/greenhopper/data.ts
  - taskflow/src/services/jira/greenhopper/data.test.ts
  - taskflow/src/services/jira/greenhopper/details.ts
  - taskflow/src/services/jira/greenhopper/details.test.ts
  - taskflow/src/services/jira/greenhopper/transitions.ts
  - taskflow/src/services/jira/greenhopper/transitions.test.ts
autonomous: true
requirements:
  - GH-ADAPT-01
must_haves:
  truths:
    - "Four typed fetchers (fetchAllData, fetchBacklogData, fetchIssueDetails, fetchGhTransitions) exist; each calls greenhopperFetch with its endpoint and casts the JSON to its typed response"
    - "Every fetcher 401/403 → throws ApiError('...', status, 'jira'); network failure → throws Error('Cannot reach ...'); other non-ok → throws Error with status (matches jira/transitions.ts envelope)"
    - "Each fetcher's URL contains the exact path from RESEARCH §Capture Script endpoint list, parameterized correctly"
  artifacts:
    - path: "taskflow/src/services/jira/greenhopper/allData.ts"
      provides: "fetchAllData(baseUrl, token, boardId) → Promise<GhAllDataResponse>"
      exports: ["fetchAllData"]
    - path: "taskflow/src/services/jira/greenhopper/data.ts"
      provides: "fetchBacklogData(baseUrl, token, boardId) → Promise<GhBacklogResponse>"
      exports: ["fetchBacklogData"]
    - path: "taskflow/src/services/jira/greenhopper/details.ts"
      provides: "fetchIssueDetails(baseUrl, token, boardId, issueKey, loadSubtasks) → Promise<GhDetailsResponse>"
      exports: ["fetchIssueDetails"]
    - path: "taskflow/src/services/jira/greenhopper/transitions.ts"
      provides: "fetchGhTransitions(baseUrl, token, projectId) → Promise<GhTransitionsResponse>"
      exports: ["fetchGhTransitions"]
  key_links:
    - from: "all four fetchers"
      to: "greenhopperFetch in ./client"
      via: "import + call"
      pattern: "greenhopperFetch\\("
    - from: "all four fetchers (401/403 path)"
      to: "ApiError"
      via: "throw new ApiError(..., status, 'jira')"
      pattern: "new ApiError\\("
---

<objective>
Ship the four typed fetchers covering every GreenHopper endpoint in scope: `fetchAllData`, `fetchBacklogData`, `fetchIssueDetails`, `fetchGhTransitions`. Each is a thin async function that calls `greenhopperFetch` from 71-02 and casts the JSON to the matching response type from `types.ts`. Error envelope matches the existing `jira/transitions.ts:19-41` pattern (401/403 → ApiError, network → wrapped Error). No consumers yet — these are the data-pull primitives Phases 73/74/75 will swap in.

Purpose: GH-ADAPT-01 ("typed fetchers for allData/data/details/transitions returning documented response shapes") is satisfied by this plan.

Output: Eight new files — four fetchers + four colocated vitest test files mocking `./client`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-greenhopper-adapter-foundation/71-RESEARCH.md
@.planning/phases/71-greenhopper-adapter-foundation/71-PATTERNS.md
@.planning/research/GREENHOPPER-API.md
@taskflow/src/services/jira/transitions.ts
@taskflow/src/services/aio/projects.ts
@taskflow/src/services/aio/projects.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fetchAllData + fetchBacklogData with tests</name>
  <files>taskflow/src/services/jira/greenhopper/allData.ts, taskflow/src/services/jira/greenhopper/allData.test.ts, taskflow/src/services/jira/greenhopper/data.ts, taskflow/src/services/jira/greenhopper/data.test.ts</files>
  <read_first>
    - taskflow/src/services/jira/transitions.ts (lines 12-45 — error envelope pattern: 401/403 → ApiError, network → wrapped Error, status-other → wrapped Error)
    - taskflow/src/services/aio/projects.test.ts (lines 1-60 — mock-./client + happy/401/network test triple to mirror)
    - taskflow/src/services/jira/greenhopper/client.ts (from 71-02 — confirm greenhopperFetch signature)
    - taskflow/src/services/jira/greenhopper/types.ts (GhAllDataResponse, GhBacklogResponse)
    - .planning/research/GREENHOPPER-API.md §Endpoints (URL patterns for allData + data)
    - .planning/phases/71-greenhopper-adapter-foundation/71-PATTERNS.md §"allData.ts" + §"data.ts, details.ts"
    - Path to ApiError class — locate via `grep -rn "export class ApiError" taskflow/src/`
  </read_first>
  <behavior>
    For each fetcher (allData, data):
    - Happy path (response.ok): returns parsed JSON cast to the typed response shape
    - 401 or 403: throws ApiError with status code and source 'jira'
    - Network/throw inside greenhopperFetch: caught and re-thrown as `new Error("Cannot reach ${baseUrl} — check the base URL")`
    - Other non-ok status: throws Error with endpoint name + status code (e.g., "GreenHopper allData request failed with status 500")
    - URL contains the exact query: allData → `/work/allData.json?rapidViewId={boardId}`; data → `/plan/backlog/data.json?rapidViewId={boardId}`
  </behavior>
  <action>
    Create `allData.ts`:
    - Imports: `greenhopperFetch` from `'./client'`, `GhAllDataResponse` from `'./types'`, `ApiError` from its module (resolved during read_first)
    - Export async `fetchAllData(baseUrl: string, token: string, boardId: number): Promise<GhAllDataResponse>`
    - Body follows the `jira/transitions.ts:19-41` error envelope: try/catch around `greenhopperFetch(baseUrl, token, \`/work/allData.json?rapidViewId=${boardId}\`, 'Load Sprint Board (allData)')`; catch → throw `Cannot reach ${baseUrl} — check the base URL`; if !response.ok and status 401 or 403 → throw `new ApiError('Invalid token or token has expired', response.status, 'jira')`; other non-ok → throw `new Error(\`GreenHopper allData request failed with status ${response.status}\`)`; ok path → `return (await response.json()) as GhAllDataResponse`

    Create `data.ts`: same shape, function name `fetchBacklogData`, path `/plan/backlog/data.json?rapidViewId=${boardId}`, operation label `'Load Backlog (data)'`, return type `Promise<GhBacklogResponse>`, error messages substitute "backlog data" for "allData".

    Create `allData.test.ts` and `data.test.ts`:
    - Mock setup mirroring `aio/projects.test.ts:1-15`: `vi.mock('./client', () => ({ greenhopperFetch: vi.fn(), GREENHOPPER_API_PATH: '/rest/greenhopper/1.0/xboard' }))`; import the SUT
    - `const mockedGhFetch = vi.mocked(greenhopperFetch)`; `beforeEach(() => vi.clearAllMocks())`
    - One test per behavior bullet above (4 tests per fetcher × 2 fetchers = 8 tests minimum)
    - URL assertion: `expect(mockedGhFetch).toHaveBeenCalledWith(BASE, TOKEN, expect.stringContaining('rapidViewId=42'), expect.any(String))`
    - 401: `mockedGhFetch.mockResolvedValue({ ok: false, status: 401 } as unknown as Response); await expect(fetchAllData(BASE, TOKEN, 42)).rejects.toThrow('Invalid token')`
    - Network: `mockedGhFetch.mockRejectedValue(new Error('timeout')); await expect(fetchAllData(BASE, TOKEN, 42)).rejects.toThrow('Cannot reach')`
    - Happy: `mockedGhFetch.mockResolvedValue({ ok: true, status: 200, json: async () => ({ rapidViewId: 42, statistics: {/*…*/}, entityData: {statuses:{},priorities:{},types:{},epics:{}}, columnsData:{rapidViewId:42,columns:[]}, swimlanesData:{rapidViewId:42,swimlaneStrategy:'',parentSwimlanesData:{parentIssueIds:[],inprogressCandidates:[],doneCandidates:[]}}, issuesData:{rapidViewId:42,activeFilters:[],issues:[]} }) } as unknown as Response)` (minimal valid shape, inline literal — fixtures are used by adapter tests in 71-05, not here)
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/services/jira/greenhopper/allData.test.ts src/services/jira/greenhopper/data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "export async function fetchAllData" taskflow/src/services/jira/greenhopper/allData.ts` == 1
    - Source assertion: `grep -c "export async function fetchBacklogData" taskflow/src/services/jira/greenhopper/data.ts` == 1
    - Source assertion: `grep -c "ApiError" taskflow/src/services/jira/greenhopper/allData.ts` ≥ 1 (envelope match)
    - Test command above passes; both fetcher test files exercise happy/401/network/non-ok cases
    - URL assertion present in both test files (`grep -c "rapidViewId" taskflow/src/services/jira/greenhopper/*.test.ts` ≥ 2)
  </acceptance_criteria>
  <done>fetchAllData + fetchBacklogData behave per the envelope, tests pass, URL/error semantics match jira/transitions.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fetchIssueDetails + fetchGhTransitions with tests</name>
  <files>taskflow/src/services/jira/greenhopper/details.ts, taskflow/src/services/jira/greenhopper/details.test.ts, taskflow/src/services/jira/greenhopper/transitions.ts, taskflow/src/services/jira/greenhopper/transitions.test.ts</files>
  <read_first>
    - taskflow/src/services/jira/transitions.ts (lines 12-45 — same envelope pattern)
    - taskflow/src/services/jira/greenhopper/allData.ts (from Task 1 — copy shape exactly)
    - taskflow/src/services/jira/greenhopper/types.ts (GhDetailsResponse, GhTransitionsResponse)
    - .planning/research/GREENHOPPER-API.md §Endpoints + §details.json + §transitions.json
    - .planning/phases/71-greenhopper-adapter-foundation/71-PATTERNS.md §"details.ts" + §"transitions.ts" (note: NO `data.transitions` unwrap — GH returns the whole envelope as the response)
  </read_first>
  <behavior>
    Same envelope (happy/401-403/network/other non-ok) as Task 1, plus:
    - details: function signature `fetchIssueDetails(baseUrl, token, boardId, issueKey, loadSubtasks)`; URL is `/issue/details.json?rapidViewId=${boardId}&issueIdOrKey=${issueKey}&loadSubtasks=${loadSubtasks}`; test asserts issueKey is forwarded into the path (`stringContaining('issueIdOrKey=PROJ-1')`)
    - transitions: function signature `fetchGhTransitions(baseUrl, token, projectId)`; URL is `/work/transitions.json?projectId=${projectId}`; return type is `GhTransitionsResponse` (the whole envelope — no `.transitions` unwrap unlike jira/transitions.ts which is REST shape)
  </behavior>
  <action>
    Create `details.ts`: mirror `allData.ts` exactly with these differences:
    - Signature: `fetchIssueDetails(baseUrl: string, token: string, boardId: number, issueKey: string, loadSubtasks: boolean): Promise<GhDetailsResponse>`
    - Path: `\`/issue/details.json?rapidViewId=${boardId}&issueIdOrKey=${encodeURIComponent(issueKey)}&loadSubtasks=${loadSubtasks}\`` — issueKey is URL-encoded to handle special characters
    - Operation label: `'Load Issue Details'`
    - Error messages substitute "issue details" for "allData"

    Create `transitions.ts`: mirror `allData.ts` with:
    - Signature: `fetchGhTransitions(baseUrl: string, token: string, projectId: number): Promise<GhTransitionsResponse>`
    - Path: `\`/work/transitions.json?projectId=${projectId}\``
    - Operation label: `'Load Workflow Transitions'`
    - Return: `return (await response.json()) as GhTransitionsResponse` — NO `.transitions` unwrap (key difference from `jira/transitions.ts` per PATTERNS.md deviation note)
    - Error messages substitute "transitions" for "allData"

    Create `details.test.ts` and `transitions.test.ts`:
    - Same mock + envelope coverage as Task 1
    - details: extra test asserts the URL forwards `issueIdOrKey=PROJ-1` and `loadSubtasks=true` (`expect(mockedGhFetch).toHaveBeenCalledWith(BASE, TOKEN, expect.stringContaining('issueIdOrKey=PROJ-1'), expect.any(String))`)
    - transitions: happy-path JSON uses minimal valid `GhTransitionsResponse` shape: `{ projectAndIssueTypeToWorkflow: {}, workflowToTransitions: {} }`; test asserts the response is returned as-is (no `.transitions` unwrap)
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/services/jira/greenhopper/details.test.ts src/services/jira/greenhopper/transitions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "export async function fetchIssueDetails" taskflow/src/services/jira/greenhopper/details.ts` == 1
    - Source assertion: `grep -c "export async function fetchGhTransitions" taskflow/src/services/jira/greenhopper/transitions.ts` == 1
    - Source assertion: `grep -c "encodeURIComponent(issueKey)" taskflow/src/services/jira/greenhopper/details.ts` ≥ 1
    - Source assertion: `grep -c "\.transitions" taskflow/src/services/jira/greenhopper/transitions.ts` == 0 (no REST-shape unwrap)
    - Tests pass; details test asserts issueKey forwarding; transitions test asserts whole-envelope return
  </acceptance_criteria>
  <done>fetchIssueDetails + fetchGhTransitions behave per the envelope, tests pass, URL semantics match RESEARCH endpoint table, transitions returns the whole envelope unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → Jira host | Each fetcher crosses this boundary via greenhopperFetch → apiFetch('jira',...); reuses existing trust model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-07 | Tampering | issueKey injected into URL (details.ts) | mitigate | `encodeURIComponent(issueKey)` per Task 2 action; prevents path-injection for keys containing special chars |
| T-71-08 | Denial of Service | Unhandled non-ok status crashes board | mitigate | Error envelope matches jira/transitions.ts:19-41 (401/403 → ApiError, network → wrapped, other → wrapped); never throws raw Response |
| T-71-09 | Availability | 401 cascade breaks Jira auth state | accept | Reuses apiFetch('jira', ...) so existing markDisconnected fires consistently — same model as tempo + jira services |
| T-71-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/services/jira/greenhopper` passes all eight new test cases (across this plan's four files)
- Each fetcher source contains `ApiError` and `'jira'` references
- URL semantics validated by grep + by test stringContaining assertions
</verification>

<success_criteria>
- All four fetchers exported with correct signatures
- All four `.test.ts` files green via the verify commands
- GH-ADAPT-01 satisfied: typed fetchers for allData/data/details/transitions return documented response shapes
</success_criteria>

<output>
Create `.planning/phases/71-greenhopper-adapter-foundation/71-03-SUMMARY.md` when done summarizing: four function signatures, URL templates, error envelope choice, any deviations from RESEARCH/PATTERNS.
</output>
