---
phase: 71-greenhopper-adapter-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - taskflow/scripts/capture-greenhopper.mjs
  - taskflow/src/services/jira/greenhopper/__fixtures__/allData.real.json
  - taskflow/src/services/jira/greenhopper/__fixtures__/data.real.json
  - taskflow/src/services/jira/greenhopper/__fixtures__/details.real.json
  - taskflow/src/services/jira/greenhopper/__fixtures__/transitions.real.json
autonomous: false
requirements:
  - GH-ADAPT-01
  - GH-ADAPT-03
must_haves:
  truths:
    - "A committed Node CLI captures all four GreenHopper endpoints with no live PAT in the output (per D-10)"
    - "Four redacted real-capture JSON fixtures exist under __fixtures__/ and are loadable via vitest's resolveJsonModule"
    - "Every PII surface listed in RESEARCH.md §Capture Script redaction table is rewritten before write (issue keys, summaries, usernames, avatar URLs, epic.text, details.html, project name/avatar)"
  artifacts:
    - path: "taskflow/scripts/capture-greenhopper.mjs"
      provides: "One-shot redacting capture CLI; reads JIRA_BASE_URL/JIRA_PAT/BOARD_ID/ISSUE_KEY/PROJECT_ID; writes four redacted fixtures"
      contains: "PROJ-, redact, writeFileSync, fetch"
    - path: "taskflow/src/services/jira/greenhopper/__fixtures__/allData.real.json"
      provides: "Redacted GhAllDataResponse capture for adapter + entity-map tests"
    - path: "taskflow/src/services/jira/greenhopper/__fixtures__/data.real.json"
      provides: "Redacted GhBacklogResponse capture for data.ts fetcher tests"
    - path: "taskflow/src/services/jira/greenhopper/__fixtures__/details.real.json"
      provides: "Redacted GhDetailsResponse capture for details.ts fetcher tests; section.html replaced with placeholder"
    - path: "taskflow/src/services/jira/greenhopper/__fixtures__/transitions.real.json"
      provides: "Redacted GhTransitionsResponse capture for transitions.ts fetcher tests"
  key_links:
    - from: "taskflow/scripts/capture-greenhopper.mjs"
      to: "process.env.JIRA_BASE_URL / JIRA_PAT / BOARD_ID / ISSUE_KEY / PROJECT_ID"
      via: "env-driven CLI"
      pattern: "process\\.env\\.JIRA_(BASE_URL|PAT)"
    - from: "taskflow/scripts/capture-greenhopper.mjs"
      to: "__fixtures__/{allData,data,details,transitions}.real.json"
      via: "writeFileSync after redaction"
      pattern: "writeFileSync"
---

<objective>
Establish Wave 0 fixture infrastructure that blocks every downstream Phase 71 task. This plan ships the one-shot Node CLI capture script (`scripts/capture-greenhopper.mjs` per D-10) and produces the four committed `__fixtures__/*.real.json` files, each anonymized via the redaction map locked in RESEARCH.md §Capture Script.

Purpose: Success criterion #4 ("Unit tests cover the adapter with fixtures captured from a real GreenHopper response") cannot be met without redacted real captures; tests in 71-03/04/05 import these JSON files directly.

Output: One committed script + four committed redacted JSON fixtures, ready for vitest auto-import in later plans.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-greenhopper-adapter-foundation/71-CONTEXT.md
@.planning/phases/71-greenhopper-adapter-foundation/71-RESEARCH.md
@.planning/phases/71-greenhopper-adapter-foundation/71-PATTERNS.md
@.planning/research/GREENHOPPER-API.md
@taskflow/scripts/bump-version.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement scripts/capture-greenhopper.mjs (CLI + redaction + write)</name>
  <files>taskflow/scripts/capture-greenhopper.mjs</files>
  <read_first>
    - taskflow/scripts/bump-version.mjs (CLI shebang + env validation skeleton; mirror style)
    - .planning/phases/71-greenhopper-adapter-foundation/71-RESEARCH.md §"Capture Script" (lines ~348-382) — endpoint list, redaction map, "do not use apiFetch" rationale
    - .planning/phases/71-greenhopper-adapter-foundation/71-PATTERNS.md §"scripts/capture-greenhopper.mjs" — file-extension choice, skeleton excerpts
    - .planning/research/GREENHOPPER-API.md §Endpoints (URL patterns + response shapes for verification)
    - .planning/phases/71-greenhopper-adapter-foundation/71-CONTEXT.md D-10 (locked redaction requirements)
  </read_first>
  <action>
    Create taskflow/scripts/capture-greenhopper.mjs as a Node 18+ ES module CLI (`.mjs`, not `.ts` — tsx is not a devDependency per PATTERNS.md).

    Required structure (per PATTERNS.md analog `taskflow/scripts/bump-version.mjs`):
    - `#!/usr/bin/env node` shebang
    - Header comment block listing usage: `JIRA_BASE_URL=... JIRA_PAT=... BOARD_ID=... ISSUE_KEY=... PROJECT_ID=... node scripts/capture-greenhopper.mjs`
    - Header comment must also include a "Why not apiFetch" note referencing RESEARCH Pitfall 8 (apiFetch runs in renderer, touches useAuthStore, calls markDisconnected on 401)
    - Imports: `writeFileSync` from `node:fs`, `resolve`/`dirname` from `node:path`, `fileURLToPath` from `node:url`
    - Compute `__dirname` via `fileURLToPath(import.meta.url)`; derive `TASKFLOW_ROOT = resolve(__dirname, '..')` and `FIX_DIR = resolve(TASKFLOW_ROOT, 'src/services/jira/greenhopper/__fixtures__')`
    - Validate required env: `['JIRA_BASE_URL', 'JIRA_PAT', 'BOARD_ID', 'ISSUE_KEY', 'PROJECT_ID']`; missing → `console.error` (NEVER echo PAT value) + `process.exit(1)`
    - `async function get(path)` helper using global `fetch` (Node 18+) with `Authorization: Bearer ${process.env.JIRA_PAT}` and `Content-Type: application/json`; throws on non-ok with `${path} → ${res.status}` (status only, not body)
    - Four GET calls to the endpoints listed in RESEARCH §Capture Script lines ~350-354 (allData, data, details with `loadSubtasks=true`, transitions)
    - Shared `keyMap: Map<string, string>` produces stable `PROJ-{n}` numbering across all four files (assign first time seen, reuse thereafter)
    - Implement `redactIssueKey(key)`, `redactSummary(idx)`, `redactAssignee(idx)`, `redactAvatarUrl(idx)` helpers per the RESEARCH redaction table (PATTERNS.md mirrors verbatim)
    - For each response, walk and rewrite per the table: `issue.key`/`details.key`/transition issue refs → `PROJ-{n}`; `issue.summary` → `'Sample summary {n}'`; `issue.assignee` → `'user{n}'`; `issue.assigneeName` → `'User {n}'`; `issue.avatarUrl` → `'https://example.invalid/avatar/{n}.png'`; `entityData.epics[*].epicField.text` → `'Epic {n}'`; `entityData.epics[*].epicField.epicKey` mirrors the same PROJ-{n} from keyMap; `projectName` → `'Sample Project'`; `projectAvatarUrl` → `'https://example.invalid/project.png'`
    - For `details.json`: strip query strings from every `operations[*].sections[*].operations[*].url` via `url.split('?')[0]`; replace every `Section.html` and every `inlineEditableFields[*].editHtml` with the literal string `'<!-- redacted by capture script -->'` (NEVER regex-redact HTML — RESEARCH Pitfall 7)
    - Write each redacted object via `writeFileSync(resolve(FIX_DIR, '<name>.real.json'), JSON.stringify(obj, null, 2))`
    - Log "wrote <relative path>" after each file (path only — no payload echo, no PAT value)
    - Wrap top-level in `async function main()` invoked at module bottom with `.catch(err => { console.error(err.message); process.exit(1); })`

    Do NOT add any new npm dependency. Do NOT import `apiFetch`. Do NOT log `process.env.JIRA_PAT` directly. Do NOT regex-redact HTML.
  </action>
  <verify>
    <automated>node -c taskflow/scripts/capture-greenhopper.mjs &amp;&amp; grep -q "process.env.JIRA_PAT" taskflow/scripts/capture-greenhopper.mjs &amp;&amp; grep -q "writeFileSync" taskflow/scripts/capture-greenhopper.mjs &amp;&amp; grep -q "redacted by capture script" taskflow/scripts/capture-greenhopper.mjs &amp;&amp; ! grep -E 'console\.(log|error)\([^)]*process\.env\.JIRA_PAT' taskflow/scripts/capture-greenhopper.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "^import .* from 'node:" taskflow/scripts/capture-greenhopper.mjs` ≥ 3 (fs, path, url)
    - Source assertion: file contains literal `'<!-- redacted by capture script -->'`
    - Source assertion: file does NOT contain literal `apiFetch`
    - Source assertion: file does NOT contain a `console.*` statement that prints `process.env.JIRA_PAT` (grep negative above)
    - Behavior assertion: running `node -c <file>` exits 0 (syntactically valid)
    - All five required env vars referenced by name (`grep -c "JIRA_BASE_URL\|JIRA_PAT\|BOARD_ID\|ISSUE_KEY\|PROJECT_ID" taskflow/scripts/capture-greenhopper.mjs` ≥ 5)
  </acceptance_criteria>
  <done>capture-greenhopper.mjs syntactically valid, passes the grep gates above, never prints the PAT, and replaces HTML fields with the placeholder string.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Run the capture script against the user's Jira instance and commit fixtures</name>
  <what-built>The capture CLI from Task 1 is ready; this checkpoint is the one-shot human-only run that produces the four committed fixtures. The script requires a live PAT against the team's Jira instance — Claude cannot supply this credential.</what-built>
  <how-to-verify>
    1. Confirm you have a Jira PAT, a board id (e.g. the team sprint board), one issue key from that board, and the matching project id.
    2. Run from the repo root (do NOT commit the env values):
       ```
       JIRA_BASE_URL=<your-jira> JIRA_PAT=<your-pat> BOARD_ID=<n> ISSUE_KEY=<KEY-123> PROJECT_ID=<n> \
         node taskflow/scripts/capture-greenhopper.mjs
       ```
    3. Verify four files appeared under `taskflow/src/services/jira/greenhopper/__fixtures__/`: `allData.real.json`, `data.real.json`, `details.real.json`, `transitions.real.json`.
    4. Inspect each file and confirm redaction landed (open the JSON; search for any real teammate display name, real issue summary text, real avatar host, or real PAT — there should be zero hits). The `details.real.json` `html`/`editHtml` fields must equal the placeholder string.
    5. Confirm `git status` shows ONLY the four new fixture files plus the script — no `.env` file, no log dump, no secrets.
    6. Commit the fixtures with the script.
  </how-to-verify>
  <acceptance_criteria>
    - File existence: all four `__fixtures__/*.real.json` files exist and are valid JSON (`node -e "JSON.parse(require('node:fs').readFileSync('taskflow/src/services/jira/greenhopper/__fixtures__/allData.real.json'))" ` exits 0; repeat for the other three)
    - Redaction assertion: `grep -RnE "@[a-zA-Z0-9._-]+\\.(com|net|org)" taskflow/src/services/jira/greenhopper/__fixtures__/ | grep -v "example.invalid"` returns zero lines (no real domains except the placeholder)
    - Redaction assertion: every `key` field in fixtures matches `^PROJ-\d+$` (verify with `node -e "const x=require('./taskflow/src/services/jira/greenhopper/__fixtures__/allData.real.json'); console.log(x.issuesData.issues.every(i=>/^PROJ-\\d+$/.test(i.key)))"` → prints `true`)
    - details.real.json contains the literal `<!-- redacted by capture script -->` placeholder in at least one location (`grep -c "redacted by capture script" taskflow/src/services/jira/greenhopper/__fixtures__/details.real.json` ≥ 1)
  </acceptance_criteria>
  <resume-signal>Type "fixtures captured and redaction verified" once the four JSON files are committed and the acceptance grep checks pass. If you cannot reach the Jira instance, type "blocked" with reason.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev shell → committed repo | PAT and live response data enter the dev machine and must be redacted before any file is committed |
| committed fixtures → public git history | once pushed, fixtures live forever; pre-commit redaction is the only effective control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Information Disclosure | scripts/capture-greenhopper.mjs (PAT in env) | mitigate | Script never logs `process.env.JIRA_PAT`; error messages report status code only. Acceptance criteria grep-negative on `console.*(...JIRA_PAT...)` |
| T-71-02 | Information Disclosure | __fixtures__/*.real.json (teammate PII via summaries/usernames/avatars) | mitigate | D-10 redaction map applied pre-write; redaction acceptance grep on real domains + key shape regex during human checkpoint |
| T-71-03 | Information Disclosure | details.real.json Section.html (rendered comments, attachments, mentions) | mitigate | Whole-field replacement with literal placeholder string per RESEARCH Pitfall 7 (no substring/regex redaction over HTML) |
| T-71-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; Node 18+ global `fetch` only. T-71-SC tracked for milestone-wide compliance |
</threat_model>

<verification>
- `node -c taskflow/scripts/capture-greenhopper.mjs` passes (syntactic validity)
- All four fixture files are valid JSON (parseable)
- No real domains, real usernames, or real PAT values appear anywhere under `__fixtures__/`
- `details.real.json` shows the placeholder string in every `html` / `editHtml` slot
</verification>

<success_criteria>
- One committed `taskflow/scripts/capture-greenhopper.mjs` that passes Task 1's grep gates
- Four committed redacted JSON fixtures whose existence + shape unblocks 71-03/04/05
- Zero new npm dependencies introduced
- No secrets, no real PII committed
</success_criteria>

<output>
Create `.planning/phases/71-greenhopper-adapter-foundation/71-01-SUMMARY.md` when done summarizing: script path, fixture paths, what was redacted, any deviations from D-10.
</output>
