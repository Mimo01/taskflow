---
phase: 74-backlog-on-data-json
plan: 02
type: execute
wave: 1
depends_on:
  - 74-01
files_modified:
  - taskflow/src/services/jira/greenhopper/useGhBacklogData.ts
  - taskflow/src/services/jira/greenhopper/index.ts
  - taskflow/src/services/jira.ts
autonomous: true
requirements:
  - GH-BACKLOG-01
must_haves:
  truths:
    - "useGhBacklogData(boardId) returns a React Query result reading ['gh-backlog', boardId] via fetchBacklogData with staleTime: STALE_TIME_MS, enabled gated by isActive && boardId && baseUrl && token, with NO refetchInterval (per D-02)"
    - "getGhBacklogData(qc, baseUrl, token, boardId) warms the same cache key via ensureQueryData"
    - "invalidateGhBacklogData(qc, boardId?) invalidates one board or all boards"
    - "The three new symbols are re-exported through src/services/jira.ts per [[project_jira_ts_dual_file]] (D-09b) — importers will use @/services/jira"
    - "Plan 01's RED useGhBacklogData.test.tsx flips to GREEN (8/8 passing)"
  artifacts:
    - path: taskflow/src/services/jira/greenhopper/useGhBacklogData.ts
      provides: "Hook + imperative twin + invalidator (Phase 73 mirror, no polling)"
      exports: ["useGhBacklogData", "getGhBacklogData", "invalidateGhBacklogData"]
    - path: taskflow/src/services/jira/greenhopper/index.ts
      provides: "Barrel re-export of useGhBacklogData module"
      contains: "export * from './useGhBacklogData'"
    - path: taskflow/src/services/jira.ts
      provides: "Public re-export surface (dual-file rule)"
      contains: "useGhBacklogData"
  key_links:
    - from: taskflow/src/services/jira/greenhopper/useGhBacklogData.ts
      to: taskflow/src/services/jira/greenhopper/data.ts
      via: "imports fetchBacklogData (Phase 71)"
      pattern: "fetchBacklogData"
    - from: taskflow/src/services/jira.ts
      to: taskflow/src/services/jira/greenhopper/index.ts
      via: "named re-export block"
      pattern: "from './jira/greenhopper'"
---

<objective>
Ship the cache layer: `useGhBacklogData`, `getGhBacklogData`, `invalidateGhBacklogData` — verbatim port of Phase 73's `useGhAllData` family minus polling — and re-export through `services/jira.ts`. This is the smallest possible plan that flips Plan 01's RED hook test to GREEN and unlocks Plans 03 (BacklogPage) and 04 (Sidebar) to compile against the public surface.

Purpose: Phase 71 already shipped `fetchBacklogData`. Plan 02 only adds the React Query wrapper + re-export. No call sites change yet.
Output: One new source file, one barrel modification, three new symbols re-exported from the dual-file `jira.ts`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/74-backlog-on-data-json/74-CONTEXT.md
@.planning/phases/74-backlog-on-data-json/74-RESEARCH.md
@.planning/phases/74-backlog-on-data-json/74-PATTERNS.md
@taskflow/src/services/jira/greenhopper/useGhAllData.ts
@taskflow/src/services/jira/greenhopper/data.ts
@taskflow/src/services/jira/greenhopper/index.ts
@taskflow/src/services/jira/greenhopper/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement useGhBacklogData module (hook + twin + invalidator)</name>
  <read_first>
    - taskflow/src/services/jira/greenhopper/useGhAllData.ts (full — verbatim model)
    - taskflow/src/services/jira/greenhopper/data.ts (fetchBacklogData signature)
    - taskflow/src/services/jira/greenhopper/types.ts (GhBacklogResponse after Plan 01)
    - taskflow/src/lib/query-constants.ts (STALE_TIME_MS — do not import POLL_INTERVAL_MS)
    - taskflow/src/services/jira/greenhopper/__tests__/useGhBacklogData.test.tsx (Plan 01 — the suite to GREEN)
    - .planning/phases/74-backlog-on-data-json/74-PATTERNS.md §"useGhBacklogData.ts (NEW)" + §"Pattern S6: Auth token bootstrap"
    - .planning/phases/74-backlog-on-data-json/74-RESEARCH.md §"Pattern 1: Hook + imperative twin + invalidator"
  </read_first>
  <behavior>
    - useGhBacklogData(boardId: number | null) returns UseQueryResult<GhBacklogResponse>.
    - Reads jiraBaseUrl from useAuthStore and bootstraps token via readSecret('jira-pat') in a useEffect that re-runs when jiraBaseUrl changes (WR-05 carry-forward — cancelled flag pattern, identical to useGhAllData).
    - Route gate: useIsActiveRoute('/backlog') (NOT '/sprint-board').
    - useQuery options: queryKey ['gh-backlog', boardId], queryFn calls fetchBacklogData(jiraBaseUrl as string, token as string, boardId as number), staleTime: STALE_TIME_MS, enabled: isActive && !!boardId && !!jiraBaseUrl && !!token.
    - NO refetchInterval, NO refetchIntervalInBackground (D-02 — backlog opened-on-demand).
    - getGhBacklogData(qc, baseUrl, token, boardId): Promise<GhBacklogResponse> — calls qc.ensureQueryData with the same key + staleTime.
    - invalidateGhBacklogData(qc, boardId?): when boardId is undefined, invalidate ['gh-backlog']; otherwise ['gh-backlog', boardId].
    - File-header JSDoc mirrors useGhAllData.ts:1-25 but states explicitly: "No polling — backlog is opened-on-demand. Mirrors Phase 73 D-01/D-02 with refetchInterval removed per Phase 74 D-02."
  </behavior>
  <action>
    Create `taskflow/src/services/jira/greenhopper/useGhBacklogData.ts`. Copy the structure of `useGhAllData.ts` verbatim and apply these line-precise diffs: replace import `fetchAllData` with `fetchBacklogData` from `./data`; replace type import `GhAllDataResponse` with `GhBacklogResponse`; drop the `POLL_INTERVAL_MS` import (leave `STALE_TIME_MS`); change `useIsActiveRoute('/sprint-board')` to `useIsActiveRoute('/backlog')`; change queryKey tuple to `['gh-backlog', boardId]`; delete the `refetchInterval` and `refetchIntervalInBackground` options entirely; rename all three exported symbols to `useGhBacklogData` / `getGhBacklogData` / `invalidateGhBacklogData`. Keep the cancelled-flag effect for `readSecret('jira-pat')` identical (token re-read on jiraBaseUrl change). Do NOT inline an adapter — the hook returns the raw envelope (planner default per CONTEXT "Claude's Discretion" + RESEARCH "Primary recommendation"). Do NOT export any other symbol.
  </action>
  <acceptance_criteria>
    - File exists at `taskflow/src/services/jira/greenhopper/useGhBacklogData.ts`.
    - `grep -cE "useIsActiveRoute\\('/backlog'\\)" taskflow/src/services/jira/greenhopper/useGhBacklogData.ts` returns 1.
    - `grep -cE "refetchInterval" taskflow/src/services/jira/greenhopper/useGhBacklogData.ts` returns 0 (no polling per D-02).
    - `grep -cE "'gh-backlog'" taskflow/src/services/jira/greenhopper/useGhBacklogData.ts` returns at least 3 (queryKey in hook + ensureQueryData + invalidate).
    - `grep -cE "^export (function|async function) (useGhBacklogData|getGhBacklogData|invalidateGhBacklogData)" taskflow/src/services/jira/greenhopper/useGhBacklogData.ts` returns 3.
    - `cd taskflow && pnpm vitest run src/services/jira/greenhopper/__tests__/useGhBacklogData.test.tsx --reporter=basic` exits 0 (the Plan 01 RED suite now GREEN, 8/8).
    - `cd taskflow && pnpm tsc --noEmit` exits 0.
    - `cd taskflow && pnpm biome check src/services/jira/greenhopper/useGhBacklogData.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd taskflow && pnpm vitest run src/services/jira/greenhopper/__tests__/useGhBacklogData.test.tsx --reporter=basic && pnpm tsc --noEmit && pnpm biome check src/services/jira/greenhopper/useGhBacklogData.ts</automated>
  </verify>
  <done>Hook + twin + invalidator live; 8/8 RED tests now GREEN; biome clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire greenhopper barrel + services/jira.ts dual-file re-export</name>
  <read_first>
    - taskflow/src/services/jira/greenhopper/index.ts (full — find the `export * from './useGhAllData';` line, line ~15)
    - taskflow/src/services/jira.ts (lines 2643-2664 — existing GH re-export block; verify line numbers and alphabetical positions before edit)
    - .planning/phases/74-backlog-on-data-json/74-PATTERNS.md §"index.ts (MODIFY — barrel export)" + §"services/jira.ts (additions)"
    - .planning/phases/74-backlog-on-data-json/74-CONTEXT.md D-09b ([[project_jira_ts_dual_file]])
  </read_first>
  <behavior>
    - greenhopper/index.ts gains `export * from './useGhBacklogData';` immediately after the existing `export * from './useGhAllData';` line.
    - services/jira.ts existing GH re-export block (named exports `from './jira/greenhopper'`) gains `useGhBacklogData`, `getGhBacklogData`, `invalidateGhBacklogData` inserted in alphabetical position alongside their `*GhAllData` / `*GhTransitions` siblings.
    - No other symbols added or removed in this task (deletions land in Plan 06).
  </behavior>
  <action>
    Edit `taskflow/src/services/jira/greenhopper/index.ts` to add the single `export * from './useGhBacklogData';` line. Edit `taskflow/src/services/jira.ts` GH re-export block (executor locates by searching for `from './jira/greenhopper'` near `useGhAllData,`) and add the three new names in alphabetical position: `getGhBacklogData` between `getGhAllData` and `getGhTransitions`; `invalidateGhBacklogData` between `invalidateGhAllData` and `invalidateGhTransitions`; `useGhBacklogData` between `useGhAllData` and `useGhTransitions`. No other edits to `jira.ts` in this task.
  </action>
  <acceptance_criteria>
    - `grep -cE "export \\* from './useGhBacklogData'" taskflow/src/services/jira/greenhopper/index.ts` returns 1.
    - `grep -cE "useGhBacklogData" taskflow/src/services/jira.ts` returns at least 1.
    - `grep -cE "getGhBacklogData" taskflow/src/services/jira.ts` returns at least 1.
    - `grep -cE "invalidateGhBacklogData" taskflow/src/services/jira.ts` returns at least 1.
    - `cd taskflow && pnpm tsc --noEmit` exits 0.
    - `cd taskflow && node -e "import('./src/services/jira.ts').then(m => process.exit(typeof m.useGhBacklogData === 'function' && typeof m.getGhBacklogData === 'function' && typeof m.invalidateGhBacklogData === 'function' ? 0 : 1))"` — not required (TS file), but as a fallback `pnpm vitest run --reporter=basic -t "useGhBacklogData"` resolves via the barrel without module-not-found.
  </acceptance_criteria>
  <verify>
    <automated>grep -cE "export \\* from './useGhBacklogData'" taskflow/src/services/jira/greenhopper/index.ts | grep -qx 1 && grep -q "useGhBacklogData" taskflow/src/services/jira.ts && grep -q "getGhBacklogData" taskflow/src/services/jira.ts && grep -q "invalidateGhBacklogData" taskflow/src/services/jira.ts && cd taskflow && pnpm tsc --noEmit</automated>
  </verify>
  <done>Three new public symbols available via `@/services/jira`. Plans 03/04 can import them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→GH API | Bearer PAT in `Authorization: Bearer <token>` to `/rest/greenhopper/1.0/xboard/plan/backlog/data.json`. Token sourced via `readSecret('jira-pat')` — same Stronghold path as Phase 71-73. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-03 | Information Disclosure | Token logged or leaked through query-key serialization | mitigate | queryKey is `['gh-backlog', boardId]` — no token in the cache key (mirrors Phase 73 — devtools display is safe). |
| T-74-04 | Tampering | Cache key collision with future `gh-backlog-*` namespaces | mitigate | Fixed two-element key tuple; invalidator uses prefix `['gh-backlog']` only when boardId omitted. No partial-match drift. |
| T-74-05 | Denial of Service | Hook accidentally enabled before token resolves → fetch with `null` token → 401 storm | mitigate | `enabled: isActive && !!boardId && !!jiraBaseUrl && !!token` (verified by Test 3 of Plan 01 suite). |
| T-74-SC | Tampering | npm/pnpm installs | mitigate | No new dependencies — all imports resolve from existing `@tanstack/react-query`, in-repo modules. |
</threat_model>

<verification>
- `cd taskflow && pnpm vitest run src/services/jira/greenhopper/__tests__/useGhBacklogData.test.tsx --reporter=basic` — 8/8 GREEN.
- `cd taskflow && pnpm tsc --noEmit` — exits 0.
- `cd taskflow && pnpm biome check src/services/jira/greenhopper/useGhBacklogData.ts src/services/jira/greenhopper/index.ts src/services/jira.ts` — 0 errors / 0 warnings on the edited region (existing `jira.ts` warnings elsewhere are out of scope).
- Plan 01's `types-fixture.test.ts` still GREEN.
</verification>

<success_criteria>
- `useGhBacklogData` family compiles, tests pass, and is reachable from `@/services/jira`.
- Hook does not schedule any polling fetch (no refetchInterval).
- Plan 03 and Plan 04 can now build call sites against this public surface.
</success_criteria>

<output>
Create `.planning/phases/74-backlog-on-data-json/74-02-SUMMARY.md` when done.
</output>
