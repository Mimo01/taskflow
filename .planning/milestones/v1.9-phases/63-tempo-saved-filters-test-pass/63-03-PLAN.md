---
phase: 63
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/jira.test.ts
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
tags:
  - tests
  - dead-code
  - cleanup
must_haves:
  truths:
    - "The two failing tests in jira.test.ts (`returns all four defaults when API call throws` and `returns all four defaults when response is not ok`) pass after expectation update"
    - "The full taskflow test suite passes with zero failures"
    - "No source file under taskflow/src/ contains an import or unused reference to deleted widget or workload modules"
    - "D-07: jira.test.ts discoverCustomFields toEqual expectations include flaggedFieldKey: 'customfield_10021'"
    - "D-08: full suite run after additions verifies zero failures"
  artifacts:
    - path: "taskflow/src/services/jira.test.ts"
      provides: "Updated discoverCustomFields expectations including flaggedFieldKey"
      contains: "flaggedFieldKey: 'customfield_10021'"
    - path: ".planning/phases/63-tempo-saved-filters-test-pass/63-03-DEAD-CODE-AUDIT.md"
      provides: "Grep audit log of widget/workload references with disposition for each match"
      contains: "Dead code audit"
  key_links:
    - from: "taskflow/src/services/jira.test.ts"
      to: "taskflow/src/services/jira.ts discoverCustomFields"
      via: "toEqual expectations matching the 6-key return shape"
      pattern: "flaggedFieldKey: 'customfield_10021'"
---

<objective>
Fix the two known failing tests in `jira.test.ts` by updating the `discoverCustomFields` `toEqual` expectations to include `flaggedFieldKey: 'customfield_10021'` (the 6th key the implementation now returns), and perform a scoped dead-code sweep for stale `widget`/`workload` references introduced by the v1.9 removals and Phase 62 additions. This is the QUAL-01 and QUAL-02 work stream and runs in parallel with Plan 01 (no file overlap).

Purpose: TEMPO-04/TEMPO-05 cannot be marked done unless the full suite is green (QUAL-01). The two failing tests are unrelated to Tempo — they fail because `discoverCustomFields` was extended in a prior phase to include the flagged field. QUAL-02 requires that the v1.9 removals (Phase 59) left no dead imports behind.

Output: jira.test.ts with corrected expectations, a dead-code audit log, and a green full test suite.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/63-tempo-saved-filters-test-pass/63-CONTEXT.md
@.planning/phases/63-tempo-saved-filters-test-pass/63-RESEARCH.md
@.planning/phases/63-tempo-saved-filters-test-pass/63-PATTERNS.md

<interfaces>
<!-- Current state of the two failing toEqual blocks in jira.test.ts -->

From taskflow/src/services/jira.test.ts lines 908-936 (current failing state):

Test 1 — "returns all four defaults when API call throws" (line 908):
  expect(result).toEqual({
    storyPointsFieldKey: 'customfield_10016',
    epicLinkFieldKey: 'customfield_10014',
    epicNameFieldKey: 'customfield_10015',
    sprintFieldKey: 'customfield_10020',
    epicColorFieldKey: 'customfield_10013',
  });

Test 2 — "returns all four defaults when response is not ok" (line 921):
  expect(result).toEqual({
    storyPointsFieldKey: 'customfield_10016',
    epicLinkFieldKey: 'customfield_10014',
    epicNameFieldKey: 'customfield_10015',
    sprintFieldKey: 'customfield_10020',
    epicColorFieldKey: 'customfield_10013',
  });

Both must become a 6-key object with `flaggedFieldKey: 'customfield_10021'` appended.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the two discoverCustomFields test expectations and confirm full suite green</name>
  <files>taskflow/src/services/jira.test.ts</files>
  <read_first>
    - taskflow/src/services/jira.test.ts lines 880-940 (the two failing toEqual blocks at lines ~912 and ~929)
    - taskflow/src/services/jira.ts (search for `discoverCustomFields` — confirm the implementation returns the 6 keys including `flaggedFieldKey: 'customfield_10021'`; this is the source of truth that the tests must match)
    - .planning/phases/63-tempo-saved-filters-test-pass/63-CONTEXT.md (D-07: the locked fix)
    - .planning/phases/63-tempo-saved-filters-test-pass/63-RESEARCH.md §"Pattern 4" (the exact before/after blocks)
  </read_first>
  <behavior>
    - jira.test.ts "returns all four defaults when API call throws" passes after the fix.
    - jira.test.ts "returns all four defaults when response is not ok" passes after the fix.
    - No other test in jira.test.ts regresses.
    - `cd taskflow && npm test` exits 0 with zero failing tests across the full suite.
  </behavior>
  <action>
    Per locked decision D-07:

    A) Locate the two `expect(result).toEqual({ ... });` blocks inside the `discoverCustomFields` describe — one at approximately line 912 (the "API call throws" test) and one at approximately line 929 (the "response is not ok" test). Both currently enumerate the same five keys: `storyPointsFieldKey: 'customfield_10016'`, `epicLinkFieldKey: 'customfield_10014'`, `epicNameFieldKey: 'customfield_10015'`, `sprintFieldKey: 'customfield_10020'`, `epicColorFieldKey: 'customfield_10013'`.

    B) In BOTH blocks, append a sixth key-value pair as the last entry: `flaggedFieldKey: 'customfield_10021',` (with a trailing comma). The order of the existing five keys is unchanged; the new key is appended after `epicColorFieldKey`. The exact field key value `'customfield_10021'` must match the implementation in `jira.ts`.

    C) Optional but recommended for clarity (per RESEARCH Pattern 4 note): update the two `it(...)` test description strings from "returns all four defaults when X" to "returns all defaults when X" (drop the now-incorrect number). This is a description-only change and does not affect the assertion. If the executor prefers to leave the descriptions unchanged, that is acceptable — the assertion fix is the load-bearing change.

    D) Do NOT modify any other test in jira.test.ts. Do NOT modify jira.ts production code. Do NOT touch the discoverCustomFields implementation.

    E) After the fix, run the FULL test suite (not just jira.test.ts) to confirm zero regressions across the codebase: `cd taskflow && npm test -- --reporter=verbose`. The expected outcome is the full suite green (the pre-fix state was 1298 passing + 2 failing; after the fix it must be all-passing).
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "flaggedFieldKey: 'customfield_10021'" taskflow/src/services/jira.test.ts` returns at least 2
    - `cd taskflow && npm test -- --run src/services/jira.test.ts` exits 0
    - `cd taskflow && npm test -- --run` exits 0 (full suite green — zero failing tests)
    - The two test cases "returns all .* defaults when API call throws" and "returns all .* defaults when response is not ok" both pass
  </acceptance_criteria>
  <done>The two known failing tests now pass, the full test suite is green, and no other test was modified.</done>
</task>

<task type="auto">
  <name>Task 2: Dead code audit and sweep for widget/workload references and Phase 62 unused imports</name>
  <files>.planning/phases/63-tempo-saved-filters-test-pass/63-03-DEAD-CODE-AUDIT.md, taskflow/src/** (only files where the audit identifies a stale import to remove)</files>
  <read_first>
    - .planning/phases/63-tempo-saved-filters-test-pass/63-CONTEXT.md §"Dead Code Sweep" (D-09 — scoped to v1.9 removal fallout + Phase 62 leftovers)
    - .planning/phases/63-tempo-saved-filters-test-pass/63-RESEARCH.md §"Phase Requirements → Test Map" QUAL-02 row (grep audit is the verification mechanism)
    - .planning/milestones/v1.8-ROADMAP.md and .planning/ROADMAP.md Phase 59 entry (to know what was deleted: WidgetGrid, WidgetCard, WidgetPicker, widgets/ folder, WorkloadTab, WorkloadSkeleton, `/workload` route)
  </read_first>
  <action>
    Per locked decision D-09 — scope this sweep narrowly to v1.9 removal fallout plus Phase 62 leftover imports. Do NOT touch unrelated code.

    A) Run the following grep audits from the taskflow repo root and capture the output verbatim into a new file `.planning/phases/63-tempo-saved-filters-test-pass/63-03-DEAD-CODE-AUDIT.md`:
       - `cd taskflow && grep -rn --include='*.ts' --include='*.tsx' -E "\\b(widget|Widget|WidgetGrid|WidgetCard|WidgetPicker|WorkloadTab|WorkloadSkeleton)\\b" src/`
       - `cd taskflow && grep -rn --include='*.ts' --include='*.tsx' -E "/workload" src/`
       - `cd taskflow && grep -rn --include='*.ts' --include='*.tsx' -E "from ['\"].*widgets/" src/`

    B) For each match in each grep, classify it in the audit file under one of three headings:
       - "STALE — to be removed": a literal import of, or reference to, a deleted module/component/route. List the file path, line number, and the offending code.
       - "LEGITIMATE — keep": a match that refers to live code (e.g., a variable named `widgetMode` in unrelated context, a comment referring to historical decisions, the string literal "widget" in tests of removed functionality that should also be removed but is out-of-scope, the `useWidget...` hooks if they survived). Justify each retention in one sentence.
       - "DOCS/PLANNING — out of scope": matches inside `.planning/`, `*.md`, or test snapshot fixtures that document historical state. Do not modify these.

    C) For every entry classified "STALE — to be removed", delete the import line or unused symbol from the source file. If removing the import leaves the file with no other imports from the same module, remove the entire import statement. If a removed symbol was the only thing in a `useEffect`/function body, also remove the now-empty block. Do NOT remove anything not on the STALE list.

    D) Also run a Phase 62 leftovers grep specifically targeting WorklogsPage and Sidebar additions:
       - `cd taskflow && grep -rn --include='*.ts' --include='*.tsx' -E "^import .* from .*tempo" src/`
       - For any import lines in this output, manually check (via Read) whether each imported symbol is actually used in the file (search for the symbol name in the file body). Append findings to the audit file under heading "Phase 62 leftovers". Remove unused imports.

    E) After modifications, re-run `cd taskflow && npm test -- --run` to confirm no test regressed. Re-run `cd taskflow && npx tsc --noEmit` to confirm no broken imports.

    F) Append a "Final state" section to the audit file noting: (1) how many STALE entries were found and removed, (2) the test suite result after sweep, and (3) the tsc result.

    Note on commit hygiene: the audit file lives under `.planning/` and is committed alongside the source changes so the QUAL-02 evidence is preserved.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/63-tempo-saved-filters-test-pass/63-03-DEAD-CODE-AUDIT.md` exists
    - The audit file contains all three headings: "STALE — to be removed", "LEGITIMATE — keep", "DOCS/PLANNING — out of scope"
    - The audit file contains a "Final state" section listing the STALE-removed count and the final test/tsc result
    - `cd taskflow && npx tsc --noEmit` exits 0
    - `cd taskflow && npm test -- --run` exits 0
    - `cd taskflow && grep -rn --include='*.ts' --include='*.tsx' -E "import .* from ['\"].*widgets/" src/` returns no matches (no remaining dead widget imports)
    - `cd taskflow && grep -rn --include='*.ts' --include='*.tsx' -E "from ['\"].*WorkloadTab|from ['\"].*WorkloadSkeleton" src/` returns no matches
  </acceptance_criteria>
  <done>The dead-code audit is documented, all stale widget/workload imports are removed, no Phase 62 unused imports remain, the test suite is green, and TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan modifies test expectations and removes dead imports only — no runtime behavior change, no new trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-08 | Tampering | jira.test.ts expectations | accept | Tests are CI-validated; a wrong fix value would fail the CI run immediately. The exact value `'customfield_10021'` is sourced from the production implementation in jira.ts (line 1309 area) and from RESEARCH §"Pattern 4". |
| T-63-09 | Repudiation | Dead-code removal audit trail | mitigate | All STALE removals are documented in `63-03-DEAD-CODE-AUDIT.md` with file path and line number, providing a git-history-paired evidence trail for the QUAL-02 sign-off. |
| T-63-SC | Tampering | npm package installs | mitigate | No new packages installed in this phase. |
</threat_model>

<verification>
- `cd taskflow && npm test -- --run` exits 0 (full suite green)
- `cd taskflow && npx tsc --noEmit` exits 0
- `grep -c "flaggedFieldKey: 'customfield_10021'" taskflow/src/services/jira.test.ts` returns at least 2
- `.planning/phases/63-tempo-saved-filters-test-pass/63-03-DEAD-CODE-AUDIT.md` exists and documents the audit
</verification>

<success_criteria>
1. QUAL-01: The full test suite passes with zero failures.
2. QUAL-02: No dead widget/workload imports remain in `taskflow/src/`; audit log preserved.
3. The two jira.test.ts failing tests pass with `flaggedFieldKey: 'customfield_10021'` added to both expectations.
</success_criteria>

<output>
Create `.planning/phases/63-tempo-saved-filters-test-pass/63-03-SUMMARY.md` when done.
</output>
