---
phase: 64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/worklogs/WorklogsPage.tsx
  - taskflow/src/routes/worklogs/WorklogsPage.test.tsx
autonomous: true
requirements:
  - TEMPO-08
user_setup: []
tags:
  - worklogs
  - hierarchy
  - sticky-table
  - tanstack-query
  - jira-enrichment

must_haves:
  truths:
    - "D-01: Worklog table rows are Jira issues (epic → story → subtask), not people — no person column"
    - "D-02: All hierarchy levels render expanded (no collapse state)"
    - "D-03: Cells aggregate all worklog entries for that issue+day; zero cells render blank"
    - "D-04: Person filter remains with default = current user; filter bar and saved filters kept unchanged"
    - "D-05: Batch JQL enrichment fires after worklogs load — single dependent useQuery with uniqueKeys useMemo"
    - "D-06: 3-level hierarchy: Epic → Story → Subtask; stories whose parent epic is unknown render under synthetic 'No Epic' group"
    - "D-07: Unresolvable issue keys render as the bare KEY with muted strikethrough; their hours still count in totals"
    - "D-08: Clicking a task row calls the onIssueClick prop with that issue's key (via useOutletContext)"
    - "D-09: Breadcrumbs handled automatically by handleIssueClick — no custom breadcrumb UI in the table"
    - "D-10: Leaf rows trigger onIssueClick; per Claude's discretion, epic and story rows are also clickable"
    - "Date header row stays visible while scrolling vertically; first issue-name column stays visible while scrolling horizontally"
    - "Per-issue total column, per-day totals row, and grand total are still visible at all times"
  artifacts:
    - path: "taskflow/src/routes/worklogs/WorklogsPage.tsx"
      provides: "Hierarchy table with sticky header + column, outlet context wiring, Jira enrichment query"
      contains: "useOutletContext, uniqueKeys useMemo, hierarchy useMemo, sticky top-0 left-0 z-30"
    - path: "taskflow/src/routes/worklogs/WorklogsPage.test.tsx"
      provides: "Updated test coverage for hierarchy table, outlet context mock, enrichment mock"
      contains: "useOutletContext mock, apiFetch enrichment mock, hierarchy assertions"
  key_links:
    - from: "taskflow/src/routes/worklogs/WorklogsPage.tsx"
      to: "outlet context onIssueClick"
      via: "useOutletContext"
      pattern: "useOutletContext<\\{[^}]*onIssueClick"
    - from: "taskflow/src/routes/worklogs/WorklogsPage.tsx"
      to: "/rest/api/2/search"
      via: "apiFetch with issuekey in (...) JQL"
      pattern: "issuekey in \\("
    - from: "taskflow/src/routes/worklogs/WorklogsPage.tsx"
      to: "TempoWorklog issue.key"
      via: "data.map((w) => w.issue.key)"
      pattern: "w\\.issue\\.key"
---

<objective>
Replace the person×day pivot table in `WorklogsPage.tsx` with a 3-level Jira issue hierarchy (Epic → Story → Subtask) as rows. Add a dependent TanStack Query that batch-enriches unique `w.issue.key` values from Tempo worklogs with `summary/issuetype/parent` fields. Make the date header row and first issue-name column sticky. Wire `onIssueClick` via `useOutletContext` so leaf rows navigate to `/issue/:key`. Update the test suite to match the new table structure.

Purpose: Phase 64 implements TEMPO-08 (previously deferred from v2) and the sticky table + navigation parts of the phase goal. Cell drill-down editing comes in Plan 02.
Output: Rewritten table body + first dependent enrichment query + updated test file. Filter bar, saved filters, schedule coloring, and `formatSeconds`/date helpers are KEPT unchanged.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@.planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-CONTEXT.md
@.planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-RESEARCH.md
@.planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-PATTERNS.md
@.planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-UI-SPEC.md

<interfaces>
<!-- Existing identifiers and signatures the executor MUST use. Verified by codebase inspection. -->

From taskflow/src/services/tempo/types.ts (line 18):
```typescript
export interface TempoWorklog {
  tempoWorklogId?: number;
  jiraWorklogId?: number;
  issue: { key: string; id?: string | number; projectId?: string | number; summary?: string; issueType?: { name: string } };
  author: { name: string; key?: string; displayName?: string };
  timeSpentSeconds: number;
  dateStarted: string; // YYYY-MM-DD
  comment?: string;
  // ...
}
```

From taskflow/src/services/tempo/index.ts (re-exports):
```typescript
export { fetchWorklogs, fetchUserSchedule, type ScheduleDayType } from './...';
```

From taskflow/src/lib/apiFetch.ts:
```typescript
export function apiFetch(source: 'jira' | 'tempo' | string, url: string, init: RequestInit, label: string): Promise<Response>;
```

From taskflow/src/main.tsx (line 544–552, outlet context shape):
```typescript
context={{
  onIssueClick: handleIssueClick,        // (key: string, resetTrail?: boolean) => void
  onEpicClick: handleIssueClick,
  openEdit, openClone, openAddSubtask, openCreateStory,
}}
```

From taskflow/src/routes/dashboard/BacklogPage.tsx (line 191 — reference consumer):
```typescript
const { onIssueClick } = useOutletContext<{
  onIssueClick: (key: string, resetTrail?: boolean) => void;
}>();
```

From taskflow/src/services/jira.ts (line 2244-ish — existing batch JQL pattern this enrichment mirrors):
```typescript
const jql = `issuekey in (${keys.join(',')})`;
// url: `${base}/rest/api/2/search?jql=${encodeURIComponent(jql)}&fields=...&maxResults=${keys.length}`;
```

Hierarchy node shape (NEW — define in WorklogsPage.tsx):
```typescript
type DayMap = Map<string, number>; // YYYY-MM-DD -> seconds
type SubtaskNode = { summary: string; dayMap: DayMap; entries: TempoWorklog[] };
type StoryNode  = { summary: string; dayMap: DayMap; entries: TempoWorklog[]; subtasks: Map<string, SubtaskNode> };
type EpicNode   = { summary: string; dayMap: DayMap; entries: TempoWorklog[]; stories: Map<string, StoryNode> };
type HierarchyMap = Map<string, EpicNode>; // key = epicKey or '__NO_EPIC__'
```

Enrichment query response shape:
```typescript
type EnrichedIssue = {
  key: string;
  fields: {
    summary: string;
    issuetype: { name: string; subtask: boolean };
    parent?: { key: string; fields: { summary: string } };
  };
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update WorklogsPage.test.tsx mocks and fixtures for hierarchy table</name>
  <files>taskflow/src/routes/worklogs/WorklogsPage.test.tsx</files>
  <read_first>
    - taskflow/src/routes/worklogs/WorklogsPage.test.tsx (all 669 lines — current pivot-table tests; identify keep/update/delete per Patterns map)
    - .planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-PATTERNS.md (§WorklogsPage.test.tsx — "New mock additions needed", "Updated makeWorklog factory", "Existing tests to KEEP")
    - .planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-RESEARCH.md (§Wave 0 Gaps — mock list)
    - taskflow/src/routes/dashboard/BacklogPage.test.tsx (reference: how an existing test mocks useOutletContext)
  </read_first>
  <behavior>
    - Test fixture `makeWorklog` accepts an `issueKey` argument (default `'X-1'`) so callers can pass distinct keys for hierarchy tests; the returned object's `issue.key` reflects that argument.
    - `useOutletContext` is mocked via `vi.mock('react-router-dom', ...)` so the page receives a `mockOnIssueClick` function from the test.
    - `@/lib/apiFetch` is mocked so URLs containing `/rest/api/2/search` return `{ ok: true, json: () => ({ issues: mockEnrichResult }) }`; tests can mutate `mockEnrichResult` to control hierarchy enrichment per test.
    - `@/services/jira/worklogs` is mocked exporting `createWorklog`, `updateWorklog`, `deleteWorklog` (consumed in Plan 02 but stubbed now so future cell-popover tests can compile).
    - All TEMPO-02 date-preset tests, TEMPO-03 person-filter tests, TEMPO-04 save-filter tests, TEMPO-05 load/rename/delete tests, and D-08 zero-hour blank assertions continue to pass against the new hierarchy implementation (Plan 02 will add new tests for hierarchy rendering + cell popover).
    - All TEMPO-01 "row per author" assertions are removed (the pivot table no longer exists). Existing TEMPO-07 totals assertions are updated to look for per-issue and per-day totals on the new hierarchy table.
  </behavior>
  <action>Extend `WorklogsPage.test.tsx` so it can compile and run against a hierarchy table. Add `vi.mock('react-router-dom', ...)` that exports `useOutletContext` returning `{ onIssueClick: mockOnIssueClick }` (let `mockOnIssueClick = vi.fn()` declared at module scope, reset in `beforeEach`). Add `vi.mock('@/lib/apiFetch', ...)` exporting `apiFetch` as a `vi.fn()` that branches on URL: `/rest/api/2/search` → returns `{ ok: true, json: () => ({ issues: mockEnrichResult }) }`; other URLs fall back to the existing tempo fetch handler. Declare `let mockEnrichResult: unknown[] = []` at module scope and reset in `beforeEach`. Add `vi.mock('@/services/jira/worklogs', ...)` exporting `createWorklog`, `updateWorklog`, `deleteWorklog` as `vi.fn().mockResolvedValue(undefined)`. Modify the existing `makeWorklog` factory to accept `issueKey = 'X-1'` and assign it to `issue.key`. Delete pivot-table assertions per Patterns map: any test that asserts "row per `author.name`" (TEMPO-01). KEEP TEMPO-02/03/04/05 tests and D-08 blank-cell assertion. UPDATE TEMPO-07 tests to look for per-issue total column and per-day totals row labels (the totals semantics carry forward; only the row dimension changed). Do NOT add new hierarchy/render tests in this task — those belong to Task 2 + Task 3 of this plan. Run the test file to confirm it compiles and existing kept tests still pass against the current pivot implementation (they will continue to pass because filter bar, schedule, saved filters are unchanged).</action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/worklogs/WorklogsPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/routes/worklogs/WorklogsPage.test.tsx` contains `vi.mock('react-router-dom'` block that exports `useOutletContext`
    - File contains `vi.mock('@/lib/apiFetch'` and `vi.mock('@/services/jira/worklogs'`
    - `makeWorklog` signature accepts an `issueKey` parameter (regex `function makeWorklog\([^)]*issueKey`)
    - `cd taskflow && npx vitest run src/routes/worklogs/WorklogsPage.test.tsx` exits 0
    - All TEMPO-02, TEMPO-03, TEMPO-04, TEMPO-05 tests still pass (count from `it('TEMPO-02'`, `it('TEMPO-03'`, `it('TEMPO-04'`, `it('TEMPO-05'` matches before/after)
    - No `it.skip` or `describe.skip` blocks added in this task
  </acceptance_criteria>
  <done>Test file compiles and runs against the unchanged pivot implementation; all kept tests pass; mocks for `useOutletContext`, `apiFetch`, and `jira/worklogs` are wired and reusable in subsequent tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add useOutletContext, uniqueKeys useMemo, and dependent Jira enrichment query in WorklogsPage.tsx</name>
  <files>taskflow/src/routes/worklogs/WorklogsPage.tsx, taskflow/src/routes/worklogs/WorklogsPage.test.tsx</files>
  <read_first>
    - taskflow/src/routes/worklogs/WorklogsPage.tsx (current 667 lines — keep auth effect, worklogs useQuery, schedule useQuery, filter bar JSX, saved filters JSX, formatSeconds, enumerateDays, getThisWeekRange/etc., dayColClass; replace only the pivot useMemo and the data-table tbody)
    - taskflow/src/routes/dashboard/BacklogPage.tsx lines 188–200 (reference: `useOutletContext` consumer pattern with `onIssueClick: (key: string, resetTrail?: boolean) => void`)
    - taskflow/src/services/jira.ts lines 2240–2260 (reference: existing `issuekey in (...)` JQL batch — same URL shape this enrichment uses)
    - taskflow/src/services/tempo/types.ts (confirm `w.issue.key`, NOT `w.issueKey`)
    - .planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-PATTERNS.md (§Pattern 1 useOutletContext, §Pattern 2 Dependent enrichment, §Pattern 4 keepable patterns)
    - .planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-RESEARCH.md (§Pitfall 2 — stable uniqueKeys queryKey, §Pitfall 7 — guard `uniqueKeys.length > 0`)
  </read_first>
  <behavior>
    - WorklogsPage consumes `onIssueClick` from `useOutletContext<{ onIssueClick: (key: string, resetTrail?: boolean) => void }>()` (parallel to BacklogPage.tsx line 191).
    - A `uniqueKeys` `useMemo` returns `[...new Set((data ?? []).map((w) => w.issue.key))].sort()` and a stable `uniqueKeysStr = uniqueKeys.join(',')`.
    - A second `useQuery` with `queryKey: ['jira', 'worklog-enrich', jiraBaseUrl, uniqueKeysStr]` calls `apiFetch('jira', '${baseUrl}/rest/api/2/search?jql=' + encodeURIComponent('issuekey in (KEY-1,KEY-2,...)') + '&fields=summary,issuetype,parent&maxResults=${uniqueKeys.length}', { headers: { Authorization: 'Bearer ${token}', Accept: 'application/json' } }, 'Enrich Worklog Issues')`, returning `d.issues` typed as `EnrichedIssue[]`.
    - `enabled` guard: `!!jiraBaseUrl && !!jiraToken && !!data && uniqueKeys.length > 0`.
    - `staleTime: 5 * 60 * 1000` (5 min — issue metadata rarely changes).
    - `jiraToken` MUST NOT appear in `queryKey` (T-62-06 rule — token is fetched inside `queryFn` via `readSecret`, same as future cell mutations).
    - The query returns empty array if `uniqueKeys.length === 0` (early-return in queryFn) so an empty data set never produces `issuekey in ()` JQL.
    - A test asserts the enrichment query fires once with the expected `issuekey in (...)` JQL when worklogs data resolves, and does NOT fire when `data` is empty.
  </behavior>
  <action>In `WorklogsPage.tsx`: add `import { useOutletContext } from 'react-router-dom'` and `import { apiFetch } from '@/lib/apiFetch'` (verify presence; add only if missing). Inside the `WorklogsPage` function, after the existing `useAuthStore`/`useSettingsStore` lines, add `const { onIssueClick } = useOutletContext<{ onIssueClick: (key: string, resetTrail?: boolean) => void }>()`. After the existing worklogs `useQuery` (the `['tempo', 'worklogs', ...]` one), add a `useMemo` named `uniqueKeys` that returns `[...new Set((data ?? []).map((w) => w.issue.key))].sort()` with `[data]` dependency, and a `const uniqueKeysStr = uniqueKeys.join(',')`. Then add a `useQuery` named `enrichQuery` with `queryKey: ['jira', 'worklog-enrich', jiraBaseUrl, uniqueKeysStr]`. Its `queryFn` is an async function that: returns `[]` if `uniqueKeys.length === 0`; reads `const token = await readSecret('jira-pat').catch(() => null)`; throws `new Error('No token')` if token is null; builds `const base = jiraBaseUrl!.replace(/\/$/, '')` and `const jql = encodeURIComponent('issuekey in (' + uniqueKeys.join(',') + ')')` and `const url = base + '/rest/api/2/search?jql=' + jql + '&fields=summary,issuetype,parent&maxResults=' + uniqueKeys.length`; calls `apiFetch('jira', url, { headers: { Authorization: 'Bearer ' + token, Accept: 'application/json' } }, 'Enrich Worklog Issues')`; throws `new Error('Enrichment failed: ' + response.status)` if `!response.ok`; returns `(await response.json()).issues as EnrichedIssue[]`. Set `enabled: !!jiraBaseUrl && !!jiraToken && !!data && uniqueKeys.length > 0` and `staleTime: 5 * 60 * 1000`. Add the `EnrichedIssue` type at module scope (after the existing `DatePreset` type export). Do NOT yet build the hierarchy useMemo or change the rendered table — the existing pivot render still runs in this task. In `WorklogsPage.test.tsx`, add three new tests: (a) "fires Jira enrichment query with issuekey in (...) JQL after worklogs load" — populate worklog response with two distinct issue keys, await the table render, assert the `apiFetch` mock was called with a URL containing `issuekey%20in` and both keys; (b) "does NOT fire enrichment query when no worklogs returned" — empty worklogs response; assert `apiFetch` was NOT called with a `/rest/api/2/search` URL; (c) "exposes onIssueClick via useOutletContext" — assert `mockOnIssueClick` was passed into the component (smoke: render does not throw and `useOutletContext` mock was invoked).</action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/worklogs/WorklogsPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `WorklogsPage.tsx` contains `import { useOutletContext } from 'react-router-dom'`
    - `WorklogsPage.tsx` contains `import { apiFetch } from '@/lib/apiFetch'` (verify: `grep -c "apiFetch" taskflow/src/routes/worklogs/WorklogsPage.tsx | grep -v '^#'` ≥ 2)
    - `WorklogsPage.tsx` contains `const { onIssueClick } = useOutletContext<` (regex `useOutletContext<\s*\{`)
    - `WorklogsPage.tsx` contains `queryKey: ['jira', 'worklog-enrich'` exactly once
    - `WorklogsPage.tsx` does NOT contain `jiraToken` inside any `queryKey` array (forbidden by T-62-06)
    - `WorklogsPage.tsx` contains the literal string `issuekey in (`
    - `WorklogsPage.tsx` contains `&fields=summary,issuetype,parent`
    - `WorklogsPage.tsx` contains `staleTime: 5 * 60 * 1000` on the enrichment query
    - `cd taskflow && npx vitest run src/routes/worklogs/WorklogsPage.test.tsx` exits 0
    - The three new tests ("fires Jira enrichment query…", "does NOT fire enrichment query when no worklogs returned", "exposes onIssueClick via useOutletContext") are present and pass (verify: `grep -c "fires Jira enrichment\|does NOT fire enrichment\|onIssueClick via useOutletContext" taskflow/src/routes/worklogs/WorklogsPage.test.tsx` ≥ 3)
    - `cd taskflow && npx tsc --noEmit -p taskflow/tsconfig.json` exits 0 (no new type errors)
  </acceptance_criteria>
  <done>WorklogsPage subscribes to outlet context and fires a dependent enrichment query against `/rest/api/2/search` once worklogs data is ready and at least one unique issue key exists; test suite covers query firing, empty short-circuit, and outlet context wiring; existing rendered pivot table is unchanged in this task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace pivot table with epic/story/subtask hierarchy + sticky CSS + leaf-row navigation</name>
  <files>taskflow/src/routes/worklogs/WorklogsPage.tsx, taskflow/src/routes/worklogs/WorklogsPage.test.tsx</files>
  <read_first>
    - taskflow/src/routes/worklogs/WorklogsPage.tsx (current pivot useMemo lines 261–300 — direct shape model; table render lines 564–663 — replace tbody and thead classes only; KEEP the wrapper div `flex-1 overflow-auto px-6 py-4` and `formatSeconds`, `formatDayHeader`, `enumerateDays`, `dayColClass`)
    - .planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-PATTERNS.md (§Pattern 3 hierarchy useMemo classification rule, §Row Hierarchy Visual, §Sticky Cell CSS, §Issue Type Icons, §Unresolvable Issue Key Display)
    - .planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-UI-SPEC.md (§Layout Contracts — Sticky Table; §Row Hierarchy Visual Contract; §Interaction Contracts — Issue Row Click → Navigation; §Copywriting Contract — "Issue", "No Epic")
    - .planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-RESEARCH.md (§Pitfall 3 — issuetype.subtask classification; §Pitfall 6 — overflow-auto on right container; §Architectural Responsibility Map row "Sticky table rendering")
    - taskflow/src/routes/dashboard/IssueDetailPage.tsx (no read needed for this task — reserved for Plan 02)
  </read_first>
  <behavior>
    - The `pivot` useMemo is replaced with a `hierarchy` useMemo that returns `{ hierarchy: HierarchyMap, days: string[], dayTotals: Map<string, number>, grandTotal: number, issueTotals: Map<string, number> }`.
    - Issue classification rules (NEVER use `issuetype.name === 'Epic'` — admin-renamable):
      - `enrichMap.get(key)?.fields.issuetype.subtask === true` → subtask
      - else if `enrichMap.get(key)?.fields.parent?.key` is present → story (its `parent.key` is the epic key)
      - else → epic (top of hierarchy)
      - Unresolvable key (no entry in `enrichMap`) → treated as a story under the synthetic `'__NO_EPIC__'` epic with label = the bare key, muted+strikethrough.
    - Worklog accumulation:
      - Subtask worklog: nests under `epicMap[epicKey].stories[storyKey].subtasks[subtaskKey].dayMap[date] += secs`, AND propagates the same `secs` up to `storyKey.dayMap`, `epicKey.dayMap`, and `issueTotals`.
      - Story-direct worklog: nests under `epicMap[epicKey].stories[storyKey].dayMap[date] += secs`; propagates to epic.dayMap and issueTotals.
      - Epic-direct worklog: nests under `epicMap[epicKey].dayMap[date] += secs`; propagates to issueTotals.
      - "No Epic" synthetic group: a story whose `parent` is missing/unresolvable goes under `'__NO_EPIC__'` epic key with no clickable header row.
    - Table renders one `<tr>` per node (epic, story, subtask) in this DFS order: epic → its stories (in insertion order) → each story's subtasks (in insertion order). Then the synthetic `__NO_EPIC__` group renders last.
    - Sticky CSS exactly per UI-SPEC §Layout Contracts:
      - Corner `<th>` "Issue": `sticky top-0 left-0 z-30 bg-background text-left px-4 py-3 border border-border min-w-48 font-semibold text-muted-foreground`
      - Date `<th>` cells: `sticky top-0 z-20 bg-background text-right px-4 py-3 border border-border min-w-14 font-semibold text-muted-foreground` (plus `dayColClass(dayTypeMap.get(day))`)
      - Total `<th>`: `sticky top-0 z-20 bg-background text-right px-4 py-3 border border-border min-w-18 font-semibold`
      - First-column `<td>` in data rows: `sticky left-0 z-10 bg-background px-4 py-3 border border-border` (epic adds `bg-muted/40 font-semibold` in addition to or overriding `bg-background`)
      - Totals row first `<td>`: `sticky left-0 bottom-0 z-20 bg-background px-4 py-3 border border-border font-semibold`
    - Row hierarchy visuals:
      - Epic row `<tr>`: `hover:bg-accent/50 cursor-pointer`; first `<td>` adds `bg-muted/40 font-semibold`; label has no indent (`pl-0`) and a `Layers` icon (size-3, `text-purple-500 mr-1`).
      - Story row `<tr>`: `hover:bg-accent/50 cursor-pointer`; first `<td>` label `pl-4` with `BookOpen` icon (size-3, `text-blue-500 mr-1`).
      - Subtask row `<tr>`: `hover:bg-accent/50 cursor-pointer`; first `<td>` label `pl-8 text-muted-foreground` with `GitBranch` icon (size-3, `text-muted-foreground mr-1`).
      - "No Epic" synthetic header row: `bg-muted/40 font-semibold text-muted-foreground italic`; NO `cursor-pointer`; NOT clickable.
      - Unresolvable issue rendered as `<span className="text-xs text-muted-foreground line-through">{key}</span>` (hours still counted, included in totals).
    - Navigation: clicking the sticky name `<td>` button (wrap label in `<button type="button" aria-label="Open ${issueKey}" onClick={() => onIssueClick(issueKey)}>...</button>`) for epic, story, and subtask rows calls `onIssueClick(issueKey)`. Per D-10 Claude's discretion, epic header rows ARE clickable. "No Epic" group header is NOT clickable.
    - Header text changes: "Name" → "Issue" (corner cell copy per UI-SPEC §Copywriting Contract).
    - Totals: per-day totals row, per-issue total column, grand total preserved. `tfoot` first cell uses sticky `left-0 z-20` so it stays visible while horizontally scrolling.
    - Loading state: when `isLoading && !data`, render the existing skeleton table; when `data && !enrichQuery.data && enrichQuery.isLoading`, render the hierarchy with `Skeleton` inline in place of each issue label (hours data is real).
    - Enrichment error (non-blocking): if `enrichQuery.isError`, render an `Alert` from `@/components/ui/alert` with body `"Some issues could not be loaded. Hours are still shown."` directly below the filter bar, ABOVE the table.
  </behavior>
  <action>In `WorklogsPage.tsx`: define the four hierarchy node types (`DayMap`, `SubtaskNode`, `StoryNode`, `EpicNode`, `HierarchyMap`) at module scope below the `DatePreset` export; add `const NO_EPIC = '__NO_EPIC__'`. Replace the existing `pivot, days, dayTotals, grandTotal` useMemo (current lines 262–300) with a new useMemo that builds `enrichMap: Map<string, EnrichedIssue>` from `enrichQuery.data ?? []`, then iterates over `data ?? []` and applies the classification rules in `<behavior>`. The useMemo must return `{ hierarchy, days, dayTotals, grandTotal, issueTotals }`. Dependencies: `[data, enrichQuery.data, from, to]`. In the JSX table area (currently lines 564–663): keep the wrapper `<div className="flex-1 overflow-auto px-6 py-4">`. Update the corner header cell to use the sticky classes listed in `<behavior>` and the literal label "Issue" (replacing "Name"). Update each date `<th>` and the Total `<th>` to use the sticky classes listed in `<behavior>`. Replace the data `<tbody>` content: iterate `Array.from(hierarchy.entries())` rendering per-epic group: an epic `<tr>` with `Layers` icon + epic summary (or muted strikethrough key) clickable via `onIssueClick`; then nested iteration over `epic.stories.values()` rendering story `<tr>` with `BookOpen` icon + `pl-4`; then each story's `subtasks.values()` rendering subtask `<tr>` with `GitBranch` icon + `pl-8 text-muted-foreground`. For each row, render N day cells using `formatSeconds(node.dayMap.get(day) ?? 0)` (KEEP `dayColClass` schedule coloring) and one Total cell using `formatSeconds(issueTotals.get(key) ?? 0)`. For the synthetic `NO_EPIC` group, render a non-clickable header row labeled "No Epic" then its stories. KEEP the existing `<tfoot>` totals row but update the first `<td>` to add `sticky left-0 bottom-0 z-20 bg-background`. Import `Layers, BookOpen, GitBranch, Alert, AlertDescription` (Alert from `@/components/ui/alert`). Wire the enrichment-error alert directly below the existing filter bar block (after line 561 `Save filter` button div, before the `Table area` wrapper). Update `WorklogsPage.test.tsx` with new tests: (1) "TEMPO-08 renders epic header rows with bg-muted/40" — populate enrichment so two issues are epics; assert rows match selector containing `bg-muted/40`. (2) "TEMPO-08 renders story rows indented with pl-4 + subtask rows with pl-8" — populate hierarchy and assert presence of `pl-4` and `pl-8` class strings on respective label cells. (3) "TEMPO-08 unresolvable issue key renders with line-through and is included in totals" — feed a worklog with key `KEY-X` not present in `mockEnrichResult`; assert a span with `line-through` exists containing `KEY-X` and the grand total includes its seconds. (4) "TEMPO-08 'No Epic' group appears for stories without an enriched epic" — feed a story whose parent is missing in enrichment; assert presence of text `No Epic`. (5) "clicking a subtask row calls onIssueClick with its key" — render hierarchy; click the subtask label button; assert `mockOnIssueClick` was called with the subtask key. (6) "clicking the corner header cell does NOT call onIssueClick" — click the "Issue" header; assert `mockOnIssueClick` not invoked. (7) "enrichment query error shows inline alert" — make `apiFetch` reject on the search URL; assert `Some issues could not be loaded. Hours are still shown.` appears.</action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/worklogs/WorklogsPage.test.tsx && cd taskflow && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `WorklogsPage.tsx` contains the literal `'__NO_EPIC__'` exactly once
    - `WorklogsPage.tsx` contains `issuetype.subtask` and NOT `issuetype.name === 'Epic'` (regex `issuetype\.name\s*===\s*['\"]Epic['\"]` returns no matches)
    - `WorklogsPage.tsx` no longer contains `const pivotMap` or `pivot:` (the old structure is gone)
    - `WorklogsPage.tsx` contains the strings `sticky top-0 left-0 z-30`, `sticky top-0 z-20`, `sticky left-0 z-10`, and at least once `bg-background` paired with each sticky class (verify presence via grep)
    - Corner header cell renders the literal text `Issue` (not `Name`) — verify `grep -c '>Issue<' taskflow/src/routes/worklogs/WorklogsPage.tsx | grep -v '^#'` ≥ 1
    - `WorklogsPage.tsx` imports `Layers`, `BookOpen`, `GitBranch` from `lucide-react`
    - `WorklogsPage.tsx` imports `Alert, AlertDescription` from `@/components/ui/alert`
    - All 7 new tests from this task pass and 4 existing tests from Task 2 still pass (`cd taskflow && npx vitest run src/routes/worklogs/WorklogsPage.test.tsx` exits 0)
    - `cd taskflow && npx tsc --noEmit -p tsconfig.json` exits 0
    - `cd taskflow && npx vitest run` exits 0 (full suite green — guards against incidental breakage in BacklogPage/etc.)
  </acceptance_criteria>
  <done>The rendered table is a 3-level Jira issue hierarchy with sticky header row, sticky first column, leaf-row navigation via `onIssueClick`, "No Epic" synthetic group, and inline enrichment-error alert. All TEMPO-08 hierarchy assertions pass; existing TEMPO-02/03/04/05/07 filter and totals tests continue to pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tempo API → frontend | Tempo worklog data crosses into the React app; `w.issue.key` values originate from Tempo, not user input |
| Jira API → frontend | Jira enrichment response (summary, issuetype, parent) crosses into the React app |
| User click → in-app navigation | Issue key string flows from rendered row into `onIssueClick` → route param |
| User text → DOM | Issue summaries and (in Plan 02) worklog comments rendered as React text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Tampering | JQL query construction in enrichment useQuery | mitigate | `w.issue.key` values originate from Tempo API responses (not user input); interpolation into `issuekey in (...)` JQL is bounded by Jira's own key syntax. URL-encode via `encodeURIComponent` to prevent accidental URL break-out (already required by `<action>`) |
| T-64-02 | Information disclosure | Jira issue summaries rendered to DOM | accept | React renders summary text as a text node — no `dangerouslySetInnerHTML`. Same posture as IssueDetailPage and BacklogPage |
| T-64-03 | Denial of service | Enrichment query with very large `uniqueKeys` array | accept | Date-preset ranges cap effective issue count for normal usage (RESEARCH §A3 — degrades gracefully at ~200 keys; user can narrow date range). Future enhancement: chunk in 100s if observed in production |
| T-64-04 | Repudiation | Issue navigation click | accept | `onIssueClick` is a UI-only navigation handler; no destructive action; matches existing BacklogPage/SprintBoardTab behavior |
| T-64-05 | Tampering | Outlet-context-injected `onIssueClick` | accept | `useOutletContext` is internal to React Router; route layout is the trust authority — same posture as BacklogPage line 191 |

ASVS L1 mapping: V5 Input Validation does not apply to this plan (no user input crosses to the API); applies to Plan 02 (`parseDuration` already validated in `services/jira/duration`).
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/worklogs/WorklogsPage.test.tsx` exits 0
- `cd taskflow && npx vitest run` exits 0 (full suite green)
- `cd taskflow && npx tsc --noEmit -p tsconfig.json` exits 0
- `cd taskflow && npm run build` exits 0 (Tauri/Vite build still works — sanity check)
- Manual smoke (Plan 02 will add the human-verify checkpoint): rendering the page locally shows epic/story/subtask grouping with sticky headers and a clickable subtask row navigating to `/issue/:key`.
</verification>

<success_criteria>
- WorklogsPage renders a 3-level Jira issue hierarchy (Epic → Story → Subtask) instead of a person×day pivot
- Date header row stays at the top while scrolling vertically; first issue-name column stays visible while scrolling horizontally
- Clicking a leaf row (subtask, or story without subtasks, or epic — D-10 discretion) navigates to `/issue/:key` via `onIssueClick`
- Unresolvable issue keys render as the bare KEY with muted+strikethrough; their hours still count in totals
- "No Epic" synthetic group exists for orphaned stories
- Inline non-blocking Alert appears when Jira enrichment fails; hours still show
- All previously passing tests (TEMPO-02/03/04/05/07, D-08) continue to pass
- Seven new TEMPO-08-tagged tests pass: epic styling, indents, unresolvable, "No Epic", subtask click → onIssueClick, header non-click, enrichment error alert
- The two enrichment-firing tests (Task 2) pass
</success_criteria>

<output>
Create `.planning/phases/64-redo-worklogs-page-with-epic-story-subtask-hierarchy-sticky-/64-01-SUMMARY.md` when done.
</output>
