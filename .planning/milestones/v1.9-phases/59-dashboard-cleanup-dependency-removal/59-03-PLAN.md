---
phase: 59-dashboard-cleanup-dependency-removal
plan: 03
type: execute
wave: 2
depends_on:
  - 59-01
  - 59-02
files_modified:
  - taskflow/package.json
  - taskflow/package-lock.json
autonomous: true
requirements:
  - QUAL-03

must_haves:
  truths:
    - "D-05: npm run build is the authoritative cleanup verification (Vite/Rollup catches CSS import failures that tsc misses); this plan owns running it."
    - "react-grid-layout and @types/react-grid-layout are absent from package.json (both dependencies and devDependencies sections)"
    - "react-grid-layout is absent from node_modules after npm install"
    - "npm run build completes with exit code 0 (per D-05)"
    - "The full unit-test suite passes with zero failures"
    - "No file in src/ imports from 'react-grid-layout' or 'react-grid-layout/css/styles.css'"
  artifacts:
    - path: "taskflow/package.json"
      provides: "Package manifest without react-grid-layout entries"
      contains: '"name"'
    - path: "taskflow/package-lock.json"
      provides: "Lockfile in sync with the cleaned package.json"
      contains: '"lockfileVersion"'
  key_links:
    - from: "taskflow/package.json"
      to: "react-grid-layout package"
      via: "dependency declaration (MUST NOT EXIST after this plan)"
      pattern: 'react-grid-layout'
---

<objective>
Complete the Phase 59 cleanup: uninstall `react-grid-layout` and `@types/react-grid-layout` from `package.json`, regenerate `package-lock.json`, and run the authoritative `npm run build` + full test suite to prove the codebase is whole.

Purpose: Plans 01 and 02 deleted every consumer of `react-grid-layout` (only `WidgetGrid.tsx` ever imported it). Now the package itself and its types can leave the manifest, satisfying QUAL-03 and Phase 59 success criteria 3 and 4. Per CONTEXT.md D-05, `npm run build` is the authoritative check (Vite catches CSS import failures that `tsc` misses) — this plan owns that verification step.

Output: Cleaned `package.json` and lockfile; a passing `npm run build` (D-05); a green test suite.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-dashboard-cleanup-dependency-removal/59-CONTEXT.md
@.planning/phases/59-dashboard-cleanup-dependency-removal/59-PATTERNS.md
@taskflow/package.json

<interfaces>
<!-- Verified package.json entries from grep at planning time: -->
<!-- Line 42:  "@types/react-grid-layout": "^1.3.6",   — inside devDependencies -->
<!-- Line 52:  "react-grid-layout": "^2.2.2",         — inside dependencies -->

<!-- After Plan 01, the only consumer of react-grid-layout (WidgetGrid.tsx) is gone, -->
<!-- so removing the package leaves no dangling imports. -->

<!-- Authoritative verification per CONTEXT.md D-05: -->
<!--   cd taskflow && npm run build -->
<!-- (Vite/Rollup catches CSS imports that tsc misses.) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Uninstall react-grid-layout via npm and confirm package.json + lockfile</name>
  <files>taskflow/package.json, taskflow/package-lock.json</files>
  <read_first>taskflow/package.json, .planning/phases/59-dashboard-cleanup-dependency-removal/59-CONTEXT.md</read_first>
  <action>
    Use `npm uninstall` (preferred over a manual JSON edit) so `package-lock.json` regenerates correctly in a single step:

    From the `taskflow/` directory run:
    `npm uninstall react-grid-layout @types/react-grid-layout`

    `npm uninstall` removes the entries from `dependencies` / `devDependencies` in `package.json` AND updates `package-lock.json` AND removes the package from `node_modules` in one go.

    After the command completes:
    - `package.json` must no longer contain the string `react-grid-layout`.
    - `package-lock.json` must no longer reference `react-grid-layout` outside of historic lockfile-cleanup noise (the modern lockfile format has no orphan entries after `npm uninstall`).
    - `node_modules/react-grid-layout` and `node_modules/@types/react-grid-layout` must not exist.

    If `npm uninstall` fails (e.g. network offline, registry unreachable), fall back to: (a) editing `package.json` to delete the two lines listed in `<interfaces>`, then (b) running `rm -rf taskflow/node_modules/react-grid-layout taskflow/node_modules/@types/react-grid-layout`, then (c) running `npm install --package-lock-only` to refresh the lockfile. Prefer the `npm uninstall` path — fallback is only for offline situations.
  </action>
  <verify>
    <automated>! grep -q 'react-grid-layout' taskflow/package.json &amp;&amp; test ! -d taskflow/node_modules/react-grid-layout &amp;&amp; test ! -d taskflow/node_modules/@types/react-grid-layout</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'react-grid-layout' taskflow/package.json` returns 0
    - `taskflow/node_modules/react-grid-layout` directory does not exist
    - `taskflow/node_modules/@types/react-grid-layout` directory does not exist
    - `taskflow/package-lock.json` exists and is valid JSON (verify with `cd taskflow &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('package-lock.json','utf8'))"` exits 0)
    - `grep -rE "from ['\"]react-grid-layout|react-grid-layout/css" taskflow/src 2>/dev/null` returns no matches (no leaked imports)
  </acceptance_criteria>
  <done>Both packages and their `node_modules/` directories are gone; lockfile is in sync; no source file still imports `react-grid-layout`.</done>
</task>

<task type="auto">
  <name>Task 2: Authoritative build + full test suite verification (D-05)</name>
  <files>taskflow/package.json</files>
  <read_first>taskflow/package.json, .planning/phases/59-dashboard-cleanup-dependency-removal/59-CONTEXT.md</read_first>
  <action>
    Run the two authoritative verification commands per CONTEXT.md D-05 and Phase 59 success criterion 4.

    1. From `taskflow/` run `npm run build`. This is the canonical check per D-05 — Vite/Rollup will surface any dangling CSS imports or broken module resolutions that `tsc` alone would miss. The command must exit with code 0.

    2. From `taskflow/` run the full unit-test suite: `npm test -- --run`. The command must exit with code 0 and report zero failures. (`--run` ensures vitest does not enter watch mode.)

    If either command fails:
    - Read the failure output carefully. Likely causes: a leaked `react-grid-layout` import (Plan 01 missed a consumer), a leaked `/workload` reference (Plan 02 missed a consumer), or a stale `dashboardLayout` test mock somewhere outside `Settings.test.tsx`.
    - Locate the failure with `grep -rE "react-grid-layout|WidgetGrid|WorkloadTab|dashboardLayout|WIDGET_REGISTRY" taskflow/src` and patch the surviving reference in this plan's scope.
    - Do NOT modify package.json or re-add the uninstalled packages to silence a build error — fix the source consumer instead.

    No new files are created. The only mutation in this task is potentially `taskflow/package-lock.json` if `npm install` re-runs during the build (vitest may invoke it). `package.json` content is unchanged from Task 1 onward.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npm run build &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow &amp;&amp; npm run build` exits with code 0 (Phase 59 success criterion 4, D-05)
    - `cd taskflow &amp;&amp; npm test -- --run` exits with code 0 and reports zero failing tests
    - `grep -rcE "react-grid-layout|WidgetGrid|WidgetCard|WidgetPicker|WorkloadTab|WorkloadSkeleton|getDefaultDashboardLayout|WIDGET_REGISTRY|DEV_DASHBOARD_PRESET|PM_DASHBOARD_PRESET" taskflow/src 2>/dev/null` returns 0 across the entire source tree
    - `grep -rcE "'/workload'|/workload\"" taskflow/src 2>/dev/null` returns 0
  </acceptance_criteria>
  <done>`npm run build` and `npm test -- --run` both pass (D-05); the project is free of widget, workload, and react-grid-layout references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | `npm uninstall` resolves and removes the package via the npm CLI. No new packages are installed in this plan. |
| package.json → bundled artifact | Removed devDependency must not leave a transitive dependency dangling in the bundle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-09 | Tampering | package-lock.json regeneration | mitigate | `npm uninstall` regenerates the lockfile in a single deterministic operation; the fallback path explicitly runs `npm install --package-lock-only` to keep the lockfile consistent if the network path fails. |
| T-59-10 | Denial of Service | broken build after package removal | mitigate | Task 2 explicitly runs `npm run build` (the authoritative check per D-05) and the full vitest suite; any leaked consumer surfaces here and is fixed in the same plan rather than shipped. |
| T-59-11 | Information Disclosure | lockfile content | accept | `package-lock.json` is checked into the repo and never contained secrets — removing entries reveals nothing sensitive. |
| T-59-SC | Tampering | npm/pip/cargo installs | mitigate (out of scope) | This plan only UNINSTALLS — no new packages are added. The package legitimacy gate applies to additions, not removals; no `[ASSUMED]`/`[SUS]` checkpoint is required. RESEARCH.md does not declare a Package Legitimacy Audit table for this milestone because no installs occur in Phase 59. |
</threat_model>

<verification>
- `grep -c 'react-grid-layout' taskflow/package.json` returns 0
- `taskflow/node_modules/react-grid-layout` and `taskflow/node_modules/@types/react-grid-layout` do not exist
- `cd taskflow && npm run build` exits 0 (per D-05)
- `cd taskflow && npm test -- --run` exits 0
- `grep -rcE "react-grid-layout|WidgetGrid|WidgetCard|WidgetPicker|WorkloadTab|WorkloadSkeleton|/workload|getDefaultDashboardLayout|WIDGET_REGISTRY" taskflow/src` returns 0
</verification>

<success_criteria>
- `react-grid-layout` and `@types/react-grid-layout` absent from `package.json` (QUAL-03)
- `node_modules/react-grid-layout` removed (Phase 59 success criterion 3)
- `npm run build` exits 0 (Phase 59 success criterion 4, per D-05)
- Full test suite passes (no regressions from the deletion)
- All must_haves.truths evaluate to true
</success_criteria>

<output>
Create `.planning/phases/59-dashboard-cleanup-dependency-removal/59-03-SUMMARY.md` when done.
</output>
