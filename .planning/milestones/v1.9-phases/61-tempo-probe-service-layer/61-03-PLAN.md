---
phase: 61-tempo-probe-service-layer
plan: 03
type: execute
wave: 1
depends_on:
  - 61-01
files_modified:
  - taskflow/src/stores/settings.store.ts
  - taskflow/src/stores/settings.store.test.ts
autonomous: true
requirements:
  - TEMPO-06
must_haves:
  truths:
    - "`useSettingsStore` exposes `tempoEnabled: boolean` (default `false`) and `setTempoEnabled: (v: boolean) => void`"
    - "Settings store `version` field is bumped from `19` to `20` in the `persist({...})` config"
    - "Migration guard `if (version < 20) { if (s.tempoEnabled === undefined) s.tempoEnabled = false; }` is added after the existing `if (version < 19)` block"
    - "A v19 store missing `tempoEnabled` is migrated to a v20 store with `tempoEnabled: false`"
    - "Existing migration guards (versions 1-19) are unchanged — additive-only edit"
  artifacts:
    - path: "taskflow/src/stores/settings.store.ts"
      provides: "tempoEnabled / setTempoEnabled state + v20 migration guard"
      contains: "tempoEnabled: boolean"
    - path: "taskflow/src/stores/settings.store.test.ts"
      provides: "Unit tests for tempoEnabled default, setter, and v19→v20 migration"
      contains: "tempoEnabled"
  key_links:
    - from: "taskflow/src/stores/settings.store.ts"
      to: "taskflow/src/routes/settings/IntegrationsSection.tsx"
      via: "useSettingsStore((s) => s.tempoEnabled) selector consumed in Plan 04"
      pattern: "tempoEnabled: boolean"
---

<objective>
Bump `settings.store.ts` from v19 to v20: add `tempoEnabled: boolean` (default `false`) and `setTempoEnabled: (v: boolean) => void` to the `SettingsState` interface and the `create(persist((set) => ({...})))` initial state object; bump `version: 19` → `version: 20` in the `persist` config; add a migration guard for v19→v20. Extend `settings.store.test.ts` with three new tests covering default value, setter, and migration.

Purpose: This is the persistence half of TEMPO-06. `tempoEnabled` survives app restart via zustand-persist + Tauri storage. Plan 04 (UI toggle) imports the new selectors; Phase 62 reads `tempoEnabled` to gate the Tempo sidebar link.

Output: Two modified files — `settings.store.ts` with v20 schema and `settings.store.test.ts` with new tempo-block tests. Full suite passes.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-tempo-probe-service-layer/61-CONTEXT.md
@.planning/phases/61-tempo-probe-service-layer/61-RESEARCH.md
@.planning/phases/61-tempo-probe-service-layer/61-PATTERNS.md

# Files to modify — read current state in full before editing
@taskflow/src/stores/settings.store.ts
@taskflow/src/stores/settings.store.test.ts

<interfaces>
<!-- Current settings.store.ts insertion points — verified by grep -->

Line 103: `aioEnabled: boolean;`  ← insert tempoEnabled interface block immediately after `setAioEnabled` on line 104
Line 219: `aioEnabled: false,`    ← insert tempoEnabled initial-state block immediately after `setAioEnabled` on line 220
Line 322: `version: 19,`           ← bump to `version: 20,`
Line 403-405: `if (version < 19) { ... }`  ← insert `if (version < 20) { ... }` block immediately after this guard

Existing settings.store.test.ts pattern (Phase 51 precedent at lines 124-147):
- `describe('settings.store — aioEnabled toggle (Phase 51)')` block uses `useSettingsStore.setState({ aioEnabled: false })` to reset state, then tests default / setter / state-after-set
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify settings.store.ts — add tempoEnabled state, setter, version bump, and migration guard</name>
  <files>taskflow/src/stores/settings.store.ts</files>
  <read_first>
    - .planning/phases/61-tempo-probe-service-layer/61-PATTERNS.md (settings.store.ts pattern assignments — exact insertion points and code blocks)
    - .planning/phases/61-tempo-probe-service-layer/61-CONTEXT.md (D-13)
    - taskflow/src/stores/settings.store.ts (full current state — read before editing; do NOT rewrite, edit-in-place)
  </read_first>
  <action>Make four additive edits to `taskflow/src/stores/settings.store.ts` (per D-13 and 61-PATTERNS.md Pattern: settings.store.ts insertion points). (1) Interface insertion — after the `setAioEnabled: (v: boolean) => void;` line (currently line 104), add a JSDoc comment `/** Enable Tempo Timesheets integration. Default: false. Gates all Tempo API calls. */` then the two new interface lines `tempoEnabled: boolean;` and `setTempoEnabled: (v: boolean) => void;`. (2) Initial state insertion — after `setAioEnabled: (v) => set({ aioEnabled: v }),` (currently line 220), add `tempoEnabled: false,` and `setTempoEnabled: (v) => set({ tempoEnabled: v }),`. (3) Version bump — change `version: 19,` (currently line 322) to `version: 20,`. (4) Migration guard insertion — after the existing `if (version < 19) { ... }` block (currently lines 403-405) and before the `return persisted as SettingsState;` statement, add: `if (version < 20) { if (s.tempoEnabled === undefined) s.tempoEnabled = false; }`. Do NOT touch any other line. Do NOT delete or modify existing migration guards (1-19). The variable name for the persisted object inside the `migrate` function is `s` (mirrors existing guards at lines 389-405).</action>
  <verify>
    <automated>cd taskflow &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run src/stores/settings.store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tempoEnabled: boolean;" taskflow/src/stores/settings.store.ts` returns 1 (interface line present)
    - `grep -c "setTempoEnabled: (v: boolean) => void;" taskflow/src/stores/settings.store.ts` returns 1 (interface line present)
    - `grep -c "tempoEnabled: false," taskflow/src/stores/settings.store.ts` returns 1 (initial state present)
    - `grep -c "setTempoEnabled: (v) => set({ tempoEnabled: v })," taskflow/src/stores/settings.store.ts` returns 1 (setter implementation)
    - `grep -c "version: 20," taskflow/src/stores/settings.store.ts` returns 1
    - `grep -c "version: 19," taskflow/src/stores/settings.store.ts` returns 0 (version is bumped, not duplicated)
    - `grep -c "if (version < 20)" taskflow/src/stores/settings.store.ts` returns 1
    - `grep -c "if (s.tempoEnabled === undefined) s.tempoEnabled = false" taskflow/src/stores/settings.store.ts` returns 1
    - All previous migration guards remain — `grep -cE "if \\(version < (1[0-9]?|[1-9])\\)" taskflow/src/stores/settings.store.ts` returns at least 19 (count of v1..v19 guards preserved)
    - `cd taskflow && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>settings.store.ts compiles cleanly, exposes `tempoEnabled` and `setTempoEnabled`, persists at version 20, and migrates v19 stores correctly. All four insertion points completed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend settings.store.test.ts with tempoEnabled tests (Phase 61 block)</name>
  <files>taskflow/src/stores/settings.store.test.ts</files>
  <read_first>
    - taskflow/src/stores/settings.store.test.ts (full current state — read lines 124-147 for the aioEnabled test block to mirror)
    - taskflow/src/stores/settings.store.ts (Task 1 output — confirm fields and setter exist)
    - .planning/phases/61-tempo-probe-service-layer/61-RESEARCH.md (Phase Requirements → Test Map)
  </read_first>
  <behavior>
    - Test 1 (default): A fresh store has `tempoEnabled === false`
    - Test 2 (setter): Calling `useSettingsStore.getState().setTempoEnabled(true)` updates state so `useSettingsStore.getState().tempoEnabled === true`
    - Test 3 (setter back to false): Calling `setTempoEnabled(false)` resets to `false`
  </behavior>
  <action>Append a new `describe('settings.store — tempoEnabled toggle (Phase 61)', () => { ... })` block to `taskflow/src/stores/settings.store.test.ts`, mirroring the existing `describe('settings.store — aioEnabled toggle (Phase 51)', ...)` block at lines 124-147. Use the same reset pattern in `beforeEach`: `useSettingsStore.setState({ tempoEnabled: false });`. Implement the three tests listed in `&lt;behavior&gt;` using `useSettingsStore.getState()` to read state and `useSettingsStore.getState().setTempoEnabled(...)` to mutate. Do NOT touch the existing aioEnabled or selectedAioProjectKey describe blocks. Do NOT add a separate migration test — the existing guards' migration behavior is structurally covered by the Phase 51 / Phase 55 precedent blocks; adding a tempoEnabled migration assertion would require calling the inline `migrate` function which is deferred (IN-02, see line 182-184 of the existing test file). Document this exclusion as a one-line comment above the new describe block: `// Migration guard test deferred per IN-02 (see existing comment at top of file). The v20 guard mirrors Phase 51 aioEnabled v15 guard which is also untested for the same reason.`</action>
  <verify>
    <automated>cd taskflow &amp;&amp; npm test -- --run src/stores/settings.store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe('settings.store — tempoEnabled toggle (Phase 61)'" taskflow/src/stores/settings.store.test.ts` returns 1
    - `grep -c "setTempoEnabled" taskflow/src/stores/settings.store.test.ts` returns at least 3 (one per test)
    - `cd taskflow && npm test -- --run src/stores/settings.store.test.ts` exits 0
    - Test output shows at least 3 new test cases pass under the "Phase 61" describe block
    - All previous tests in the file still pass (regression guard)
  </acceptance_criteria>
  <done>Three new tests added under "Phase 61" describe block; default, set-true, set-false behaviors verified; full settings.store.test.ts suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted v19 store (Tauri disk) → in-memory v20 store | Existing user installs cross this boundary on first launch after upgrade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05 | Tampering | Persisted store migration | mitigate | Additive guard `if (version < 20) { if (s.tempoEnabled === undefined) s.tempoEnabled = false; }` — never deletes existing fields; idempotent (won't overwrite if user already has `tempoEnabled` set). |
| T-61-06 | Denial of Service | Migration loop runs on every launch | accept | All zustand-persist migrations run synchronously and complete in <1ms; 20 versions of cumulative guards have been verified in production through prior milestones. |
| T-61-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` exits 0
- `cd taskflow && npm test -- --run src/stores/settings.store.test.ts` exits 0 (all tests including 3 new Phase 61 tests pass)
- `cd taskflow && npm test` exits 0 (full suite green)
- `grep "version:" taskflow/src/stores/settings.store.ts | head -1` shows `version: 20,`
- New tempoEnabled fields are reachable: `cd taskflow && grep "tempoEnabled" src/stores/settings.store.ts | wc -l` returns at least 4 (interface line + 2 initial state lines + migration guard line)
</verification>

<success_criteria>
- TEMPO-06 success criterion 4 satisfied: settings store version is bumped (19→20) and migration guard passes for fresh installs (default `tempoEnabled: false`) and existing v19 stores (additive guard sets `tempoEnabled: false` only when undefined)
- D-13 satisfied verbatim: version bump, new fields, additive migration guard
- Plan 04 dependency satisfied: `useSettingsStore((s) => s.tempoEnabled)` and `useSettingsStore((s) => s.setTempoEnabled)` selectors are reachable
- No regression in existing 19 migration guards
</success_criteria>

<output>
Create `.planning/phases/61-tempo-probe-service-layer/61-03-SUMMARY.md` when done, citing the four edit points and the three new test cases.
</output>
