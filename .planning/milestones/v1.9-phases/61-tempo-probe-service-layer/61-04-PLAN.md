---
phase: 61-tempo-probe-service-layer
plan: 04
type: execute
wave: 2
depends_on:
  - 61-03
files_modified:
  - taskflow/src/routes/settings/IntegrationsSection.tsx
  - taskflow/src/routes/settings/IntegrationsSection.test.tsx
autonomous: false
requirements:
  - TEMPO-06
must_haves:
  truths:
    - "Settings → Integrations renders a 'Tempo Timesheets' section below the AIO section"
    - "Tempo section contains a checkbox labeled 'Enable Tempo Timesheets' bound to `useSettingsStore((s) => s.tempoEnabled)`"
    - "Toggling the checkbox calls `setTempoEnabled(checked)` and updates the store"
    - "Section description text reads: 'Show worklog data from Jira Tempo Timesheets. Requires Tempo plugin on your Jira instance.'"
    - "No sub-UI (project picker / dropdown) appears when Tempo is enabled — bare toggle only (D-11)"
    - "Toggle state survives app restart via the v20 store (verified manually)"
  artifacts:
    - path: "taskflow/src/routes/settings/IntegrationsSection.tsx"
      provides: "Tempo Timesheets toggle section below AIO block"
      contains: "Enable Tempo Timesheets"
    - path: "taskflow/src/routes/settings/IntegrationsSection.test.tsx"
      provides: "Mock store extension + 4 new Tempo toggle tests"
      contains: "tempoEnabled"
  key_links:
    - from: "taskflow/src/routes/settings/IntegrationsSection.tsx"
      to: "taskflow/src/stores/settings.store.ts"
      via: "useSettingsStore((s) => s.tempoEnabled) and ((s) => s.setTempoEnabled) selectors"
      pattern: "useSettingsStore\\(\\(s\\) => s\\.tempoEnabled"
---

<objective>
Add a "Tempo Timesheets" section to Settings → Integrations below the existing AIO block. Use the locked HTML pattern from CONTEXT.md D-11/D-12 and the UI-SPEC typography weights. Extend the IntegrationsSection test file's `mockStore` with `tempoEnabled` + `setTempoEnabled` fields and add 4 new test cases. End with a checkpoint where the user verifies the live render + persistence-on-restart behavior in the Tauri app.

Purpose: This is the final wiring of TEMPO-06 — the visible UI surface that makes the integration discoverable. After this plan, a user can toggle Tempo on/off, the setting persists, and Phase 62 can read `tempoEnabled` to conditionally render the sidebar entry.

Output: Two modified files; full test suite green; a human-verified app restart confirming persistence.
</objective>

<execution_context>
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-tempo-probe-service-layer/61-CONTEXT.md
@.planning/phases/61-tempo-probe-service-layer/61-RESEARCH.md
@.planning/phases/61-tempo-probe-service-layer/61-PATTERNS.md
@.planning/phases/61-tempo-probe-service-layer/61-UI-SPEC.md

# Files to modify — read in full before editing
@taskflow/src/routes/settings/IntegrationsSection.tsx
@taskflow/src/routes/settings/IntegrationsSection.test.tsx

# Store contract from Plan 03
@taskflow/src/stores/settings.store.ts

<interfaces>
<!-- Verified insertion points -->

IntegrationsSection.tsx:
- Lines 14-15: existing `aioEnabled` / `setAioEnabled` selectors → add `tempoEnabled` / `setTempoEnabled` selectors immediately after
- Lines 62-80: existing AIO toggle block (inside outer `flex flex-col gap-8` container)
- Insert new Tempo toggle `<div className="flex flex-col gap-4">` block AFTER the AIO block's closing `</div>` and BEFORE the outer container's closing `</div>` (currently around line 146)

IntegrationsSection.test.tsx mockStore (current):
- Lines 9-19: type annotation `{ aioEnabled: boolean; setAioEnabled: ...; selectedAioProjectKey: ...; setSelectedAioProjectKey: ... }` and object literal
- Lines 110-118: `beforeEach` resets `mockStore.aioEnabled = false` and `mockStore.selectedAioProjectKey = null`
- Lines 125-148: existing AIO toggle describe block (4 tests — render / unchecked / checked / setter)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend IntegrationsSection.test.tsx mockStore + add 4 Tempo toggle tests</name>
  <files>taskflow/src/routes/settings/IntegrationsSection.test.tsx</files>
  <read_first>
    - taskflow/src/routes/settings/IntegrationsSection.test.tsx (full current state — lines 9-19 mock type; lines 110-118 beforeEach; lines 125-148 AIO toggle tests to mirror)
    - .planning/phases/61-tempo-probe-service-layer/61-PATTERNS.md (IntegrationsSection.test.tsx pattern — mockStore extension blocks and test cases verbatim)
    - .planning/phases/61-tempo-probe-service-layer/61-UI-SPEC.md (Copywriting Contract — checkbox `aria-label="Enable Tempo Timesheets"`)
  </read_first>
  <behavior>
    - Test 1 (renders): `screen.getByRole('checkbox', { name: /enable tempo timesheets/i })` is in the document
    - Test 2 (default unchecked): with `mockStore.tempoEnabled = false`, the checkbox is NOT checked
    - Test 3 (controlled checked): with `mockStore.tempoEnabled = true`, the checkbox IS checked
    - Test 4 (setter called on click): clicking the unchecked checkbox calls `mockStore.setTempoEnabled` with `true`
  </behavior>
  <action>Make four additive edits to `taskflow/src/routes/settings/IntegrationsSection.test.tsx` (per 61-PATTERNS.md). (1) Mock store type extension — extend the type annotation at lines 9-15 to include `tempoEnabled: boolean;` and `setTempoEnabled: ReturnType<typeof vi.fn>;` (append both after `setSelectedAioProjectKey`). (2) Mock store object literal — add `tempoEnabled: false,` and `setTempoEnabled: vi.fn(),` to the object initializer at lines 15-19. (3) beforeEach reset — add `mockStore.tempoEnabled = false;` alongside the existing `mockStore.aioEnabled = false;` reset at lines 110-118. (4) Add a new `describe('Tempo Timesheets toggle (Phase 61)', () => { ... })` block after the existing AIO toggle describe block (around line 148). Inside the new describe block, implement the 4 tests listed in `&lt;behavior&gt;` using the existing test patterns: `renderWithClient(<IntegrationsSection />)`, `screen.getByRole('checkbox', { name: /enable tempo timesheets/i })`, and `fireEvent.click(...)`. Use the exact case-insensitive regex `/enable tempo timesheets/i` for the accessible name lookup. Use the existing `setTempoEnabled` mock fn from `mockStore` to assert `toHaveBeenCalledWith(true)`. Do NOT touch other existing describe blocks. Do NOT remove or modify the existing AIO toggle tests.</action>
  <verify>
    <automated>cd taskflow &amp;&amp; npm test -- --run src/routes/settings/IntegrationsSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tempoEnabled: boolean;" taskflow/src/routes/settings/IntegrationsSection.test.tsx` returns 1 (type annotation)
    - `grep -c "tempoEnabled: false," taskflow/src/routes/settings/IntegrationsSection.test.tsx` returns at least 1 (mock initial value)
    - `grep -c "setTempoEnabled: vi.fn()" taskflow/src/routes/settings/IntegrationsSection.test.tsx` returns 1
    - `grep -c "mockStore.tempoEnabled = false;" taskflow/src/routes/settings/IntegrationsSection.test.tsx` returns at least 1 (beforeEach reset)
    - `grep -c "Tempo Timesheets toggle (Phase 61)" taskflow/src/routes/settings/IntegrationsSection.test.tsx` returns 1
    - `grep -c "/enable tempo timesheets/i" taskflow/src/routes/settings/IntegrationsSection.test.tsx` returns at least 4 (one per test)
    - `cd taskflow && npm test -- --run src/routes/settings/IntegrationsSection.test.tsx` — exits 0; output shows at least 4 new Tempo tests pass; previous AIO tests still pass (regression guard)
    - NOTE: tests will be RED initially because IntegrationsSection.tsx has not been edited yet — this is intentional TDD ordering. They go GREEN after Task 2 completes.
  </acceptance_criteria>
  <done>Test file extended with mockStore additions and 4 new failing tests that verify Tempo toggle rendering and interaction. Tests are RED awaiting Task 2 implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add Tempo Timesheets section to IntegrationsSection.tsx</name>
  <files>taskflow/src/routes/settings/IntegrationsSection.tsx</files>
  <read_first>
    - taskflow/src/routes/settings/IntegrationsSection.tsx (full current state — read lines 1-30 for imports/selectors, lines 60-95 for AIO toggle block to mirror, find outer container closing tag)
    - .planning/phases/61-tempo-probe-service-layer/61-UI-SPEC.md (Copywriting Contract + Interaction Contract + Typography weights — section heading text-lg font-semibold; toggle label text-sm font-semibold; description text-xs text-muted-foreground; checkbox h-4 w-4 accent-primary)
    - .planning/phases/61-tempo-probe-service-layer/61-PATTERNS.md (IntegrationsSection.tsx pattern — exact selector insertion + JSX block)
    - .planning/phases/61-tempo-probe-service-layer/61-CONTEXT.md (D-11: bare toggle, no sub-UI; D-12: exact section heading and description text)
  </read_first>
  <action>Make two additive edits to `taskflow/src/routes/settings/IntegrationsSection.tsx`. (1) Selector additions — after the two existing `aioEnabled` / `setAioEnabled` selector lines (currently lines 14-15), add two new lines: `const tempoEnabled = useSettingsStore((s) => s.tempoEnabled);` and `const setTempoEnabled = useSettingsStore((s) => s.setTempoEnabled);`. (2) JSX section addition — find the closing `</div>` of the AIO section block (which immediately follows the `{aioEnabled && (...)}` conditional, currently around line 144) and insert a new sibling section block BEFORE the outer container's closing `</div>`. The new section is a `<div className="flex flex-col gap-4">` wrapping: a `<h3 className="text-sm font-semibold text-muted-foreground uppercase tracking-wide">Tempo Timesheets</h3>` heading, and a `<label className="flex items-center justify-between gap-4 cursor-pointer">` containing a `<div>` with two paragraphs (the title `<p className="text-sm font-semibold">Enable Tempo Timesheets</p>` and the description `<p className="text-xs text-muted-foreground">Show worklog data from Jira Tempo Timesheets. Requires Tempo plugin on your Jira instance.</p>`) followed by an `<input type="checkbox" aria-label="Enable Tempo Timesheets" checked={tempoEnabled} onChange={(e) => setTempoEnabled(e.target.checked)} className="h-4 w-4 accent-primary" />`. Do NOT wrap the input in a conditional `{tempoEnabled && (...)}` — D-11 specifies bare toggle with no sub-UI. Per UI-SPEC Typography section, the toggle title uses `font-semibold` (not `font-medium` as appears in the AIO precedent). The exact section heading text is "Tempo Timesheets" (D-12); the exact description text is "Show worklog data from Jira Tempo Timesheets. Requires Tempo plugin on your Jira instance." (D-12). Do NOT modify the existing AIO block.</action>
  <verify>
    <automated>cd taskflow &amp;&amp; npm test -- --run src/routes/settings/IntegrationsSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tempoEnabled = useSettingsStore" taskflow/src/routes/settings/IntegrationsSection.tsx` returns 1
    - `grep -c "setTempoEnabled = useSettingsStore" taskflow/src/routes/settings/IntegrationsSection.tsx` returns 1
    - `grep -c ">Tempo Timesheets<" taskflow/src/routes/settings/IntegrationsSection.tsx` returns 1 (h3 heading)
    - `grep -c "Enable Tempo Timesheets" taskflow/src/routes/settings/IntegrationsSection.tsx` returns at least 2 (label paragraph + aria-label)
    - `grep -c "Show worklog data from Jira Tempo Timesheets. Requires Tempo plugin on your Jira instance." taskflow/src/routes/settings/IntegrationsSection.tsx` returns 1 (exact description text per D-12)
    - `grep -c "checked={tempoEnabled}" taskflow/src/routes/settings/IntegrationsSection.tsx` returns 1
    - `grep -c "onChange={(e) => setTempoEnabled(e.target.checked)}" taskflow/src/routes/settings/IntegrationsSection.tsx` returns 1
    - `grep -c "{tempoEnabled && (" taskflow/src/routes/settings/IntegrationsSection.tsx` returns 0 (no sub-UI per D-11 — guard against accidental conditional sub-UI)
    - All 4 Tempo tests from Task 1 transition RED → GREEN
    - All previous AIO tests still pass
    - `cd taskflow && npx tsc --noEmit` exits 0
    - `cd taskflow && npm test` exits 0 (full suite green)
  </acceptance_criteria>
  <done>IntegrationsSection.tsx renders a Tempo Timesheets section with the exact copy from D-12, bare-toggle pattern from D-11, and UI-SPEC typography weights. All 4 Task 1 tests are GREEN; full suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Tempo toggle renders and persists across app restart</name>
  <what-built>
    Settings → Integrations now contains a Tempo Timesheets section below the AIO block. The section has a checkbox bound to `tempoEnabled` in the settings store, which is persisted at version 20 via zustand-persist + Tauri storage. Visual styling follows the UI-SPEC: 14px font-semibold label, 12px muted-foreground description, accent-primary checkbox fill, gap-4 inner spacing. No sub-UI appears when enabled (intentional per D-11 — Phase 62 will add the sidebar link).
  </what-built>
  <how-to-verify>
    Step 1 — Launch the app: from `taskflow/`, run `npm run tauri dev` (or use the existing dev launcher).

    Step 2 — Navigate to Settings → Integrations. Confirm visually:
    - Tempo Timesheets section appears BELOW the AIO Test Management section
    - Section heading text reads: "Tempo Timesheets" (uppercase, muted)
    - Toggle title reads: "Enable Tempo Timesheets" (semibold)
    - Description reads: "Show worklog data from Jira Tempo Timesheets. Requires Tempo plugin on your Jira instance." (smaller, muted)
    - Checkbox is on the right side, unchecked by default
    - Spacing between the AIO section and Tempo section matches the rhythm between other sections (gap-8 / 32px)

    Step 3 — Toggle the checkbox ON. Confirm:
    - Checkbox visually fills with the primary accent color
    - No new UI appears below the toggle (no sub-section, no dropdown — bare toggle per D-11)

    Step 4 — Quit the app completely (Cmd+Q on macOS, not just close the window). Relaunch.

    Step 5 — Navigate back to Settings → Integrations. Confirm:
    - Tempo checkbox is STILL checked (persistence across restart)
    - This proves the v20 store migration works

    Step 6 — Toggle the checkbox OFF. Quit + relaunch one more time. Confirm checkbox is OFF after restart.

    Expected: all six steps pass. If step 5 fails (checkbox reverts to unchecked after restart), Plan 03's migration guard or version bump is broken — investigate `settings.store.ts` version field and the v20 migration block before approving.
  </how-to-verify>
  <resume-signal>Type "approved" if all six steps pass; otherwise describe which step failed and what was seen.</resume-signal>
  <action>Human verification only. Claude does not write code in this task. The previous automated tests prove unit-level correctness; this checkpoint proves end-to-end persistence (TEMPO-06 success criterion 3 final piece) which cannot be automated without a Tauri test harness.</action>
  <verify>
    <human-check>User has run the app, toggled the checkbox, quit and relaunched, and confirmed persistence.</human-check>
  </verify>
  <acceptance_criteria>
    - User has typed "approved" OR has identified a specific defect to fix before approval
    - If approved: TEMPO-06 success criterion 3 ("toggling it on persists `tempoEnabled: true` via the settings store and survives app restart") is satisfied
    - If not approved: defect description is captured and a follow-up plan or task can be created
  </acceptance_criteria>
  <done>User has approved the Tempo toggle's appearance, behavior, and cross-restart persistence in the running Tauri app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User UI interaction → store → Tauri persistence | Checkbox state crosses from React event to zustand setter to Tauri disk storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-07 | Tampering | Checkbox state binding | mitigate | Standard React controlled-input pattern (`checked={tempoEnabled}` + `onChange` setter) — no untrusted-input crossing; setter is the only mutation path |
| T-61-08 | Information Disclosure | UI copy revealing Tempo plugin presence | accept | Description text "Requires Tempo plugin on your Jira instance" is informational, not a credential; same disclosure pattern as the existing AIO section. |
| T-61-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `cd taskflow && npm test -- --run src/routes/settings/IntegrationsSection.test.tsx` exits 0 (all tests including 4 new Tempo tests pass)
- `cd taskflow && npx tsc --noEmit` exits 0
- `cd taskflow && npm test` exits 0 (full suite green)
- Human verification: Tempo toggle renders below AIO section with correct copy, bare-toggle pattern, and persists across app restart
</verification>

<success_criteria>
- TEMPO-06 success criterion 3 satisfied: Settings → Integrations shows a Tempo toggle (default off); toggling it on persists `tempoEnabled: true` via the settings store and survives app restart
- D-11 satisfied: bare checkbox toggle, no sub-UI
- D-12 satisfied: exact section heading "Tempo Timesheets" and exact description text
- UI-SPEC Typography satisfied: font-semibold weights on heading and label (not font-medium)
- Phase 61 complete: TEMPO-06 fully addressed across probe (Plan 01), service module (Plan 02), store (Plan 03), and UI (Plan 04)
</success_criteria>

<output>
Create `.planning/phases/61-tempo-probe-service-layer/61-04-SUMMARY.md` when done, citing the two file edits, the 4 new tests, and the human-verification outcome.
</output>
