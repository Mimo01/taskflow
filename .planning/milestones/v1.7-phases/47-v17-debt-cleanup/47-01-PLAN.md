---
phase: 47-v17-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/BacklogPage.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
  - taskflow/.gitignore
autonomous: true
requirements:
  - LOAD-03

must_haves:
  truths:
    - "BacklogPage uses the shared STALE_TIME_MS constant for staleTime instead of a magic number"
    - "SprintBoardTab test has no reference to the removed fetchSprintIssues function"
    - "stats.html is listed in taskflow/.gitignore"
  artifacts:
    - path: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      provides: "Shared constant usage for staleTime"
      contains: "STALE_TIME_MS"
    - path: "taskflow/.gitignore"
      provides: "Git exclusion for stats.html"
      contains: "stats.html"
  key_links:
    - from: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      to: "taskflow/src/lib/query-constants.ts"
      via: "import { STALE_TIME_MS }"
      pattern: "import.*STALE_TIME_MS.*from.*query-constants"
---

<objective>
Fix three code/artifact debt items from the v1.7 milestone audit: replace magic number staleTime literals in BacklogPage with the shared STALE_TIME_MS constant, remove the dead fetchSprintIssues mock from SprintBoardTab tests, and add stats.html to gitignore.

Purpose: Eliminate code debt before closing v1.7 milestone
Output: Clean code with no magic numbers, no dead mocks, and no untracked build artifacts
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-v17-debt-cleanup/47-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace BacklogPage staleTime literal with STALE_TIME_MS constant</name>
  <files>taskflow/src/routes/dashboard/BacklogPage.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/BacklogPage.tsx (current state — lines 260-285 contain the two staleTime: 30_000 literals)
    - taskflow/src/lib/query-constants.ts (source of STALE_TIME_MS constant, exports `STALE_TIME_MS = 30_000`)
  </read_first>
  <action>
1. Add import to BacklogPage.tsx — insert among existing imports from @/lib/:
   `import { STALE_TIME_MS } from '@/lib/query-constants';`

2. Replace both occurrences of `staleTime: 30_000,` (lines ~268 and ~278) with:
   `staleTime: STALE_TIME_MS,`

3. Do NOT change any other code. This is a behavioral no-op (STALE_TIME_MS === 30_000).

IMPORTANT: There are two separate vi.mock blocks in the test file for jira services. Do NOT touch BacklogPage.test.tsx in this task — only the source file.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit && npm test src/routes/dashboard/BacklogPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BacklogPage.tsx contains `import { STALE_TIME_MS } from '@/lib/query-constants'`
    - BacklogPage.tsx contains zero occurrences of `staleTime: 30_000`
    - BacklogPage.tsx contains exactly 2 occurrences of `staleTime: STALE_TIME_MS`
    - tsc --noEmit exits 0
    - BacklogPage.test.tsx passes
  </acceptance_criteria>
  <done>BacklogPage uses STALE_TIME_MS from query-constants for both staleTime values; no magic 30_000 literals remain</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead fetchSprintIssues mock and gitignore stats.html</name>
  <files>taskflow/src/routes/dashboard/SprintBoardTab.test.tsx, taskflow/.gitignore</files>
  <read_first>
    - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx (lines 25-45 — contains two vi.mock blocks; line 31 has the dead fetchSprintIssues mock in the `@/services/jira` block)
    - taskflow/.gitignore (current contents — stats.html is NOT yet listed)
  </read_first>
  <action>
**SprintBoardTab.test.tsx — Remove dead mock (line 31 only):**

The file has TWO separate vi.mock blocks:
- Block 1 (~lines 30-35): `vi.mock('@/services/jira', ...)` — this contains the dead `fetchSprintIssues: vi.fn().mockResolvedValue([]),` line
- Block 2 (~lines 38-41): `vi.mock('@/services/jira/issues', ...)` — this mocks fetchSprintStories and fetchSprintSubtasks

Remove ONLY the `fetchSprintIssues: vi.fn().mockResolvedValue([]),` line from Block 1. Leave Block 2 completely untouched. Ensure the remaining mock object in Block 1 has valid syntax (no trailing comma issues, proper closing braces).

**taskflow/.gitignore — Add stats.html:**

Append `stats.html` on a new line at the end of `taskflow/.gitignore`. This prevents the bundle analysis artifact (generated by `ANALYZE=true npx vite build`) from being tracked.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npm test src/routes/dashboard/SprintBoardTab.test.tsx && grep -q 'stats.html' .gitignore && ! grep -q 'fetchSprintIssues' src/routes/dashboard/SprintBoardTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SprintBoardTab.test.tsx contains zero occurrences of `fetchSprintIssues`
    - SprintBoardTab.test.tsx still contains `fetchSprintStories` (Block 2 untouched)
    - SprintBoardTab.test.tsx still contains `fetchSprintSubtasks` (Block 2 untouched)
    - SprintBoardTab.test.tsx passes all tests
    - taskflow/.gitignore contains the line `stats.html`
  </acceptance_criteria>
  <done>Dead fetchSprintIssues mock removed from SprintBoardTab test; stats.html added to gitignore</done>
</task>

</tasks>

<verification>
1. `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit` — TypeScript compiles cleanly
2. `cd /Users/mimo/Desktop/Tasker/taskflow && npm test` — Full test suite passes
3. `grep -c 'STALE_TIME_MS' taskflow/src/routes/dashboard/BacklogPage.tsx` — returns 3 (1 import + 2 usages)
4. `! grep -q 'fetchSprintIssues' taskflow/src/routes/dashboard/SprintBoardTab.test.tsx` — no dead mock
5. `grep -q 'stats.html' taskflow/.gitignore` — gitignore entry exists
</verification>

<success_criteria>
- BacklogPage.tsx imports and uses STALE_TIME_MS instead of literal 30_000
- SprintBoardTab.test.tsx has no fetchSprintIssues reference; all tests pass
- stats.html is in taskflow/.gitignore
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/47-v17-debt-cleanup/47-01-SUMMARY.md`
</output>
