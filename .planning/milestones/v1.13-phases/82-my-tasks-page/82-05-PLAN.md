---
phase: 82-my-tasks-page
plan: 05
type: execute
wave: 2
depends_on: ["82-04"]
files_modified:
  - taskflow/src/routes/routes.tsx
  - taskflow/src/components/app/sidebar-items.ts
  - taskflow/src/components/app/Sidebar.tsx
autonomous: false
requirements: [MYTASK-01]
must_haves:
  truths:
    - "A 'My Tasks' sidebar entry navigates to /my-tasks"
    - "The /my-tasks route lazy-loads MyTasksPage via the withLazy pattern"
    - "The CheckSquare icon renders for the My Tasks sidebar entry"
    - "Grouping/scope preferences survive a real Tauri app restart (manual UAT)"
  artifacts:
    - path: "taskflow/src/routes/routes.tsx"
      provides: "/my-tasks lazy route"
      contains: "/my-tasks"
    - path: "taskflow/src/components/app/sidebar-items.ts"
      provides: "My Tasks SIDEBAR_NAV_ITEMS entry"
      contains: "my-tasks"
    - path: "taskflow/src/components/app/Sidebar.tsx"
      provides: "CheckSquare in ICON_MAP"
      contains: "CheckSquare"
  key_links:
    - from: "taskflow/src/components/app/sidebar-items.ts (iconName: 'CheckSquare')"
      to: "Sidebar.tsx ICON_MAP['CheckSquare']"
      via: "render-time icon lookup"
      pattern: "CheckSquare"
    - from: "taskflow/src/routes/routes.tsx"
      to: "routes/my-tasks/MyTasksPage"
      via: "lazy() + withLazy"
      pattern: "my-tasks/MyTasksPage"
---

<objective>
Wire the My Tasks page into the app: register the `/my-tasks` lazy route, add the "My Tasks" sidebar entry, and add the `CheckSquare` icon to the ICON_MAP (MYTASK-01). Then a human UAT checkpoint covers the manual-only verifications from VALIDATION.md (real Tauri restart persistence + live inline interactions + progressive paging).

Purpose: This is the final integration wave — small, config-only wiring that makes the page reachable. The three edits must stay in lockstep (iconName string ↔ ICON_MAP key ↔ route path), and the persistence/interaction behaviors can only be confirmed against a real Tauri build, so a checkpoint follows.
Output: registered route + sidebar entry + icon; human-verified end-to-end behavior.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-my-tasks-page/82-PATTERNS.md
@.planning/phases/82-my-tasks-page/82-UI-SPEC.md
@.planning/phases/82-my-tasks-page/82-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register /my-tasks route + sidebar entry + CheckSquare icon</name>
  <files>taskflow/src/routes/routes.tsx, taskflow/src/components/app/sidebar-items.ts, taskflow/src/components/app/Sidebar.tsx</files>
  <read_first>
    - taskflow/src/routes/routes.tsx (self-analog — lazy() imports lines 10-22, withLazy wrapper lines 24-32, routes array)
    - taskflow/src/components/app/sidebar-items.ts (self-analog — SIDEBAR_NAV_ITEMS array, lines 34-60, entry shape)
    - taskflow/src/components/app/Sidebar.tsx (self-analog — lucide import block + ICON_MAP lines 10-60, render-time lookup line ~315)
    - .planning/phases/82-my-tasks-page/82-PATTERNS.md (§routes.tsx, §sidebar-items.ts, §Sidebar.tsx — exact target edits)
  </read_first>
  <action>
    (1) routes.tsx: add `const MyTasksPage = lazy(() => import('./my-tasks/MyTasksPage'));` alongside the other lazy imports, and add `{ path: '/my-tasks', element: withLazy(MyTasksPage) }` to the routes array (never a direct non-lazy element).

    (2) sidebar-items.ts: add to SIDEBAR_NAV_ITEMS in the 'main' section (after Dashboard): `{ id: 'my-tasks', label: 'My Tasks', path: '/my-tasks', iconName: 'CheckSquare', section: 'main' }`. Use exactly "My Tasks" per UI-SPEC copywriting.

    (3) Sidebar.tsx: add `CheckSquare` to the `lucide-react` import block and add `CheckSquare,` to the ICON_MAP object. The ICON_MAP key MUST exactly match the iconName string 'CheckSquare' (mismatch → silent no-icon render at the line ~315 lookup). Implements MYTASK-01.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- --run src/routes/my-tasks/MyTasksPage.test.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "routes.tsx|sidebar" | head -5; echo "tsc-done"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -q "my-tasks/MyTasksPage" taskflow/src/routes/routes.tsx` AND `grep -q "'/my-tasks'" taskflow/src/routes/routes.tsx`
    - Source assertion: `grep -q "iconName: 'CheckSquare'" taskflow/src/components/app/sidebar-items.ts` AND `grep -q "path: '/my-tasks'" taskflow/src/components/app/sidebar-items.ts`
    - Source assertion: `grep -q "CheckSquare" taskflow/src/components/app/Sidebar.tsx` (both import and ICON_MAP)
    - Consistency: the iconName string in sidebar-items.ts exactly equals an ICON_MAP key in Sidebar.tsx
    - `npx tsc --noEmit` reports no new errors in routes.tsx / Sidebar.tsx / sidebar-items.ts
  </acceptance_criteria>
  <done>The /my-tasks route lazy-loads MyTasksPage, the sidebar shows a "My Tasks" entry with the CheckSquare icon, the iconName↔ICON_MAP keys match, and typecheck is clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — real Tauri build (persistence + live interactions + progressive paging)</name>
  <action>
    Human-only verification against a real Tauri/WebKit build — covers the VALIDATION.md Manual-Only Verifications that the jsdom unit/smoke suite cannot exercise (Tauri Store restart round-trip, live PeekPanel/StatusPopover/context-menu interactions, and multi-page All-Assigned progressive paging). No code is written in this task; the executor pauses for the developer to run the app and confirm.
  </action>
  <what-built>
    A "My Tasks" sidebar entry routing to /my-tasks, the full My Tasks page (summary/filter strip, three grouping modes with nested subtasks, scope toggle, inline interactions), and grouping/scope persistence via my-tasks.json. All unit/smoke tests are green; the items below require a real Tauri build (VALIDATION.md Manual-Only Verifications).
  </what-built>
  <how-to-verify>
    1. Run the app: `cd taskflow && npm run tauri dev` (real WebKit, not jsdom).
    2. Click "My Tasks" in the sidebar → confirm it navigates to /my-tasks and loads current-sprint issues by default (My Day grouping). Confirm the CheckSquare icon renders next to the label.
    3. Row anatomy (MYTASK-05): confirm each row shows type icon, key, priority icon, summary, status pill, due date (overdue in red), story points, MR badge (where applicable), and a time bar. Confirm icon columns are NOT collapsed to 0 width.
    4. Inline interactions (MYTASK-06): click a row body → PeekPanel slideover opens; click the issue key → full-page detail; click the status pill → StatusPopover transition; right-click a row → context menu shows "Log Work", "Copy issue key", "Copy link" (and nothing else).
    5. Grouping (MYTASK-03/04): switch My Day / By Status / By Sprint & Parent; confirm subtasks always nest under their parent and a parent with an overdue subtask appears in the Overdue band.
    6. Filter strip (MYTASK-02): click a count pill → list filters; click it again → clears; only one active at a time.
    7. Scope toggle + progressive paging (MYTASK-07): switch to "All Assigned" on an account with >50 assigned issues → confirm the list grows beyond 50 and a "Loading more tasks…" indicator shows while paging.
    8. Persistence (MYTASK-08): set grouping=By Status and scope=All Assigned, quit the app completely, relaunch → confirm both are restored.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found (band misplacement, 0-width icons, missing persistence, page cap at 50, etc.).</resume-signal>
</task>

</tasks>

<threat_model>
Read-only personal-task viewer over already-authenticated data. This plan is pure client-side route/sidebar/icon wiring — no new network, storage, auth, or input surface.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Route registration and a sidebar nav entry introduce no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-09 | Spoofing / Tampering | client-side route registration | accept | SPA route is local to the already-authenticated desktop app; no server-side authorization is bypassed (Jira PAT still gates all data). No mitigation needed. |
| T-82-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH confirms zero new packages). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd taskflow && npm run test -- --run src/routes/my-tasks/MyTasksPage.test.tsx` green
- Phase gate before /gsd-verify-work: `cd taskflow && npm run check` (biome + tsc + full vitest) green
- Human UAT checkpoint approved (Tauri restart persistence + live interactions + progressive paging)
</verification>

<success_criteria>
MYTASK-01 satisfied: the My Tasks page is reachable from the sidebar via a lazy /my-tasks route with the correct icon, and the human UAT confirms the manual-only behaviors (real-restart persistence, live inline interactions, progressive All-Assigned paging).
</success_criteria>

<output>
Create `.planning/phases/82-my-tasks-page/82-05-SUMMARY.md` when done.
</output>
