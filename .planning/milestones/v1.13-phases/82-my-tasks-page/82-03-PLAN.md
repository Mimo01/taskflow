---
phase: 82-my-tasks-page
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - taskflow/src/services/jira.ts
  - taskflow/src/services/jira/client.test.ts
autonomous: true
requirements: [MYTASK-07]
must_haves:
  truths:
    - "All Assigned scope fetches every page via fetchAllSearchPages — never a single page-capped call"
    - "250 results are returned when total=250 and the first page returns 50 (criterion 6)"
    - "fetchMyTasksHierarchy includes the flagged field in both parent fields and subtaskFields so My Day band 0 can detect flagged items"
  artifacts:
    - path: "taskflow/src/services/jira.ts"
      provides: "fetchAllAssignedHierarchy + flaggedFieldKey-extended fetchMyTasksHierarchy"
      exports: ["fetchAllAssignedHierarchy", "fetchMyTasksHierarchy"]
      contains: "fetchAllAssignedHierarchy"
    - path: "taskflow/src/services/jira/client.test.ts"
      provides: "criterion-6 assertion: 250 results when total=250 and first page returns 50"
  key_links:
    - from: "taskflow/src/services/jira.ts (fetchAllAssignedHierarchy)"
      to: "fetchAllSearchPages (services/jira/client.ts)"
      via: "import + call (no hand-rolled pagination loop)"
      pattern: "fetchAllSearchPages"
---

<objective>
Extend the Jira service layer for the My Tasks page (MYTASK-07): add `fetchAllAssignedHierarchy` (All-Assigned scope) wrapping the exported `fetchAllSearchPages` (fully paginated, no page cap), and extend `fetchMyTasksHierarchy` to include the flagged field in both parent and subtask field strings. Add the criterion-6 pagination assertion. Wave 0 — service + unit tests only, no UI.

Purpose: Two verified blocking gaps. (1) The All-Assigned scope has no data function and must never use a single page-capped call (recurring fetch-once page-cap pitfall — memory project_fetch_once_pagecap_pitfall). (2) `fetchMyTasksHierarchy` currently omits the flagged field from its `fields=` query, so `isIssueFlagged` returns false for everything and the My Day "Flagged / Blocked" band silently yields nothing.
Output: extended `services/jira.ts` + criterion-6 assertion in `services/jira/client.test.ts`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-my-tasks-page/82-RESEARCH.md
@.planning/phases/82-my-tasks-page/82-PATTERNS.md
@.planning/phases/82-my-tasks-page/82-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criterion-6 pagination assertion in client.test.ts</name>
  <files>taskflow/src/services/jira/client.test.ts</files>
  <read_first>
    - taskflow/src/services/jira/client.test.ts (the file being modified — existing fetchAllSearchPages describe block at line 52, multi-page test at line 53)
    - taskflow/src/services/jira/client.ts (fetchAllSearchPages — PAGE_SIZE=200, loops until startAt >= total)
    - .planning/phases/82-my-tasks-page/82-RESEARCH.md (§Progressive Loading Pattern — criterion 6 test spec)
    - .planning/phases/82-my-tasks-page/82-VALIDATION.md (criterion 6 row)
  </read_first>
  <behavior>
    - New `it()` inside the existing `describe('fetchAllSearchPages', ...)`: mock fetch so total=250 and the first response page returns 50 issues; subsequent pages fill to 250; assert the resolved array has exactly 250 items (criterion 6 wording: "250 results returned when total=250 and first page returns 50")
    - The test must drive `fetchAllSearchPages` through multiple page fetches (proving no single-page cap)
  </behavior>
  <action>
    Add a new `it('returns all 250 results when total=250 and first page returns 50', ...)` to the existing `describe('fetchAllSearchPages')` block (do not duplicate the existing total>PAGE_SIZE test — this asserts the specific criterion-6 wording with first-page=50). Mock global fetch to return `{ total: 250, issues: [...50...] }` on the first call and additional pages until 250 accumulate, then assert `result.length === 250`. RED first if the assertion does not yet pass, then confirm GREEN against the existing implementation (the loop already supports this).
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- --run src/services/jira/client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npm run test -- --run src/services/jira/client.test.ts` passes
    - Source assertion: `grep -q "250" taskflow/src/services/jira/client.test.ts` AND a test exists referencing first page returning 50 with total 250
    - Behavior: resolved array length equals 250 (criterion 6 satisfied)
  </acceptance_criteria>
  <done>The criterion-6 pagination assertion exists, drives multiple page fetches, asserts 250 results, and is green.</done>
</task>

<task type="auto">
  <name>Task 2: fetchAllAssignedHierarchy + flagged-field extension of fetchMyTasksHierarchy</name>
  <files>taskflow/src/services/jira.ts</files>
  <read_first>
    - taskflow/src/services/jira.ts (lines 483-601: fetchMyTasksHierarchy — the self-analog; lines 494-496 fields/subtaskFields strings to extend; line 234 isIssueFlagged)
    - taskflow/src/services/jira/client.ts (fetchAllSearchPages export to import — use the client.ts export per RESEARCH assumption A3, not the private jira.ts:283 copy)
    - .planning/phases/82-my-tasks-page/82-RESEARCH.md (§fetchAllAssignedHierarchy — full spec; §Critical Gap: fetchMyTasksHierarchy Fields)
    - .planning/phases/82-my-tasks-page/82-PATTERNS.md (§src/services/jira.ts — target source for both changes)
  </read_first>
  <action>
    (1) Extend `fetchMyTasksHierarchy`: add a new trailing parameter `flaggedFieldKey = 'customfield_10021'` (non-breaking default). Append `,${flaggedFieldKey}` to the parent `fields` string (currently ends `...timetracking,duedate`) and append `,duedate,${flaggedFieldKey}` to the `subtaskFields` string (currently ends `...parent,timetracking` — it lacks both duedate and the flagged field). This unblocks My Day band 0 (flagged) and overdue-subtask classification.

    (2) Add `export async function fetchAllAssignedHierarchy(baseUrl, token, projectKey, flaggedFieldKey, storyPointsFieldKey = 'customfield_10016')` returning `{ issues: JiraIssue[]; myIssueKeys: Set<string> }`. Build the fields list including `customfield_10020` (sprintField, for By Sprint & Parent ordering D-05), the SP fields, duedate, and flaggedFieldKey. JQL: `project = ${projectKey} AND issuetype not in subtaskIssueTypes() AND assignee = currentUser() ORDER BY rank ASC` — the `assignee = currentUser()` clause is the assignee-scoping that keeps the search to the authenticated user only (T-82-04). Import and call `fetchAllSearchPages` from './jira/client' against `${base}/rest/api/2/search?jql=${jql}&fields=${fields}` — NEVER write a new pagination loop and NEVER pass a maxResults cap. Set `myIssueKeys = new Set(issues.map(i => i.key))`.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- --run src/services/jira/client.test.ts && npx tsc --noEmit -p taskflow/tsconfig.json 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -q "export async function fetchAllAssignedHierarchy" taskflow/src/services/jira.ts`
    - Source assertion: `grep -q "assignee = currentUser()" taskflow/src/services/jira.ts` (assignee-scoping enforced — T-82-04)
    - Source assertion: `grep -q "fetchAllSearchPages" taskflow/src/services/jira.ts` (no hand-rolled loop)
    - Source assertion: `fetchMyTasksHierarchy` signature contains `flaggedFieldKey` and the flagged key is appended to BOTH `fields` and `subtaskFields` (grep the two field-string lines)
    - Source assertion: no `maxResults=50` (or any single-page cap) introduced for the All-Assigned path
    - `npx tsc --noEmit` reports no new errors for jira.ts
  </acceptance_criteria>
  <done>fetchAllAssignedHierarchy exists, is assignee-scoped, paginates via fetchAllSearchPages with no cap; fetchMyTasksHierarchy includes the flagged field in both parent and subtask field strings; typecheck clean.</done>
</task>

</tasks>

<threat_model>
Read-only personal-task viewer over already-authenticated Jira data. The only realistic new surface is the All-Assigned search query scope — it must be assignee-scoped to the current user. No new credential handling (reuses existing Bearer PAT), no new write endpoints.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Jira REST search | A new JQL search query crosses to Jira. Risk is over-broad data retrieval (returning issues not assigned to the user). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-04 | Information Disclosure | fetchAllAssignedHierarchy JQL | mitigate | JQL hard-codes `assignee = currentUser()` so the All-Assigned scope can only return the authenticated user's own issues — verified by source-grep acceptance criterion. No assignee parameter is accepted from the UI. |
| T-82-05 | Denial of Service | fetchAllSearchPages full pagination | accept | Unbounded page count is the explicit D-06 requirement (no cap). fetchAllSearchPages PAGE_SIZE=200 and react-query staleTime:30s bound practical cost; acceptable for a single authenticated desktop user. |
| T-82-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH confirms zero new packages). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd taskflow && npm run test -- --run src/services/jira/client.test.ts` green
- `grep -q "assignee = currentUser()" taskflow/src/services/jira.ts`
- `git diff taskflow/package.json` empty
</verification>

<success_criteria>
MYTASK-07 service layer complete: All-Assigned data via fully-paginated fetchAllSearchPages (criterion 6 proven), assignee-scoped, and the flagged field gap closed so My Day band classification works.
</success_criteria>

<output>
Create `.planning/phases/82-my-tasks-page/82-03-SUMMARY.md` when done.
</output>
