---
phase: 82-my-tasks-page
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - taskflow/src/lib/my-tasks-sort.ts
  - taskflow/src/lib/my-tasks-sort.test.ts
autonomous: true
requirements: [MYTASK-04, MYTASK-02]
must_haves:
  truths:
    - "A parent with an overdue subtask sorts into the Overdue band, not To Do"
    - "A flagged parent sorts into band 0 regardless of subtask bands"
    - "A Done parent with an In Progress subtask sorts into the In Progress band"
    - "Summary-strip counts (To Do / In Progress / In Review / Done / Overdue) derive purely from the loaded dataset"
  artifacts:
    - path: "taskflow/src/lib/my-tasks-sort.ts"
      provides: "classifyBand, subtreeBand, groupByMyDay, deriveCounts, MY_DAY_BANDS (pure, no I/O)"
      contains: "export const MY_DAY_BANDS"
      min_lines: 80
    - path: "taskflow/src/lib/my-tasks-sort.test.ts"
      provides: "D-04 subtree-band scenarios + count derivation tests"
      contains: "subtreeBand"
  key_links:
    - from: "taskflow/src/lib/my-tasks-sort.ts"
      to: "isIssueFlagged (services/jira)"
      via: "import + call (do not re-implement)"
      pattern: "isIssueFlagged"
---

<objective>
Create the pure smart-sort and count-derivation logic for the My Tasks "My Day" view (D-04) and the summary strip (MYTASK-02). This is Wave 0 logic with zero UI and zero I/O — fully unit-testable in isolation. Establishes the contract the UI wave consumes.

Purpose: D-04 ("a parent floats to the rank of its most-urgent child") is the single most subtlety-prone piece of the phase. Isolating it as a pure function with an exhaustive test suite eliminates the highest-risk failure class (parent-only band evaluation) before any UI exists.
Output: `src/lib/my-tasks-sort.ts` (band classify + subtree band + My Day grouping + count derivation) and its co-located test file.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-my-tasks-page/82-RESEARCH.md
@.planning/phases/82-my-tasks-page/82-PATTERNS.md
@.planning/phases/82-my-tasks-page/82-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure band-classify + subtree-band + groupByMyDay + deriveCounts</name>
  <files>taskflow/src/lib/my-tasks-sort.ts, taskflow/src/lib/my-tasks-sort.test.ts</files>
  <read_first>
    - taskflow/src/lib/my-tasks-sort.test.ts (the file being created — write tests first, RED)
    - taskflow/src/lib/my-tasks-sort.ts (the file being created)
    - taskflow/src/lib/issueDisplayUtils.ts (PATTERNS.md analog — pure-function file structure, named exports, no react/store imports)
    - taskflow/src/lib/issueDisplayUtils.test.ts (PATTERNS.md analog — describe/it structure, minimal JiraIssue stub helper)
    - taskflow/src/services/jira.ts (lines 230-240: `isIssueFlagged` signature to import, not re-implement)
    - taskflow/src/services/jira/types.ts (JiraIssue type: `status.statusCategory.key`, `status.name`, `duedate`, `issuetype.subtask`, `parent.key`)
    - .planning/phases/82-my-tasks-page/82-RESEARCH.md (§My Day Smart-Sort Algorithm — full reference implementation; §Summary Strip — Count Derivation)
  </read_first>
  <behavior>
    - classifyBand: status `done` → 5; flagged (isIssueFlagged) OR status.name includes "block" → 0; duedate in past AND not done → 1; status.name includes "review" AND myOpenMRIssueKeys.has(key) → 2; statusCategory `indeterminate` → 3; statusCategory `new` → 4
    - subtreeBand(parent, subtasks): returns Math.min(classifyBand(parent), ...subtasks.map(classifyBand), 5) — parent floats to its most-urgent child (D-04)
    - Test: parent To Do (band 4) + overdue subtask (band 1) → subtreeBand returns 1
    - Test: parent Done (band 5) + In Progress subtask (band 3) → subtreeBand returns 3
    - Test: flagged parent → subtreeBand returns 0 even if all subtasks are Done
    - Test: classifyBand returns 5 for done, 0 for flagged, 1 for past-duedate non-done, 4 for new
    - groupByMyDay: separates parents from subtasks (issuetype.subtask), attaches each subtask to parent by parent.key, includes a parent only if it (or one of its subtasks) is in myIssueKeys, assigns subtreeBand, stable-sorts ascending by band, groups consecutive same-band entries into `{ band, parents: [{parent, subtasks}] }[]`
    - deriveCounts(issues, mrAwaitingMeKeys): returns { toDo, inProgress, inReview, doneSprint, overdue, mrAwaiting } per RESEARCH §Summary Strip; inProgress = indeterminate AND !name.includes('review'); inReview = indeterminate AND name.includes('review'); overdue counts non-done past-duedate issues
  </behavior>
  <action>
    Write the test file first (RED): import { classifyBand, subtreeBand, groupByMyDay, deriveCounts, MY_DAY_BANDS } from './my-tasks-sort'. Build a `makeIssue(overrides)` stub helper mirroring issueDisplayUtils.test.ts (set the flagged field dynamically via computed key `[flaggedFieldKey]: value`). Cover every behavior bullet above, passing a fixed `today` Date for determinism and an explicit `myOpenMRIssueKeys` Set.

    Then implement `my-tasks-sort.ts` per RESEARCH.md §My Day Smart-Sort Algorithm. Export `MY_DAY_BANDS` as the `as const` array ['flagged-blocked','overdue','in-review-my-mr','in-progress','to-do','done'] and `MyDayBand` type. All functions are pure, accept `today: Date = new Date()` as the last param for testability, and import `isIssueFlagged` from '@/services/jira' (do NOT re-implement flagged duck-typing). No imports from react, react-query, or any store. Implements MYTASK-04 (subtree smart-sort) and MYTASK-02 (deriveCounts).
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- --run src/lib/my-tasks-sort.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd taskflow && npm run test -- --run src/lib/my-tasks-sort.test.ts` passes with all behavior-bullet cases green
    - Source assertion: `grep -q "export const MY_DAY_BANDS" taskflow/src/lib/my-tasks-sort.ts` succeeds
    - Source assertion: `grep -q "isIssueFlagged" taskflow/src/lib/my-tasks-sort.ts` (imports, does not re-implement)
    - Source assertion: `my-tasks-sort.ts` contains no `import ... from 'react'` and no `import ... store` (pure module)
    - Behavior: subtreeBand test for "parent To Do + overdue subtask → 1" is present and green (D-04 enforcement against parent-only evaluation)
  </acceptance_criteria>
  <done>All My Day band/subtree/grouping/count functions exist as pure named exports, the test suite covers the three D-04 subtree scenarios plus count derivation, and the quick test command is green.</done>
</task>

</tasks>

<threat_model>
This phase is a read-only personal-task viewer over already-authenticated Jira/GitLab data. No new auth, no new write endpoints. This plan adds pure in-memory transform functions only — no network, no storage, no user input.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan introduces no new trust boundary — all inputs are already-fetched JiraIssue objects from the existing authenticated Jira service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Information Disclosure | my-tasks-sort.ts | accept | Pure function operates only on data already in the client; no new exposure surface, no persistence, no logging of issue data. |
| T-82-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH confirms zero new packages). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd taskflow && npm run test -- --run src/lib/my-tasks-sort.test.ts` green
- No new external packages introduced (verify `git diff taskflow/package.json` is empty)
</verification>

<success_criteria>
MYTASK-04 smart-sort logic and MYTASK-02 count derivation exist as fully unit-tested pure functions with the three D-04 subtree scenarios proven green.
</success_criteria>

<output>
Create `.planning/phases/82-my-tasks-page/82-01-SUMMARY.md` when done.
</output>
