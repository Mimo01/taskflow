---
phase: 86
slug: redesign-dashboard-to-new-screenshot-layout-and-remove-old-w
status: approved
shadcn_initialized: true
preset: base-nova / neutral / cssVariables
created: 2026-06-15
---

# Phase 86 — UI Design Contract

> Visual and interaction contract for Phase 86: redesign dashboard to new screenshot layout
> and remove old widgets. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (components.json present) |
| Preset | base-nova, baseColor: neutral, cssVariables: true |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react |
| Font | Geist Variable (font-sans token, `@fontsource-variable/geist`) |

Source: `taskflow/components.json`, `taskflow/src/index.css`

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding |
| sm | 8px | Legend item gap, label-to-bar gap |
| md | 16px | Card internal section gap, progress bar margin |
| lg | 24px | Card padding (--card-spacing default = p-4 = 16px for default Card; hero px-6 = 24px) |
| xl | 32px | Layout column gap in top row |
| 2xl | 48px | Major vertical separation between hero and card row |
| 3xl | 64px | Page-level top/bottom breathing room |

Dashboard shell layout (from current `index.tsx` — keep unchanged):
- Page horizontal padding: `px-6` (24px)
- Page top padding in header: `pt-5 pb-5` (20px each)
- Content column gap: `gap-4` (16px)

Exceptions:
- `pt-5 pb-5` (20px) — existing dashboard shell vertical padding from `index.tsx`; inherited, not a new design decision. Keep unchanged.
- Recharts chart margin `right: 40` — Recharts implementation numeric for right Y-axis label clearance; not a CSS spacing token.
- Hero header bottom border: `border-b border-border/50` (structural, not spacing exception)
- Card `--card-spacing` token: 16px (default size), 12px (size="sm") — do not override

Source: `taskflow/src/routes/dashboard/index.tsx`, `taskflow/src/components/ui/card.tsx`

---

## Typography

Exactly 4 sizes, exactly 2 weights.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Display | 36px (`text-4xl`) | 600 (`font-semibold`) | 1.2 | Big number done count ("8" in "8 of 13 done"); hero greeting ("Good evening, Milan") |
| Heading | 16px (`text-base`) | 600 (`font-semibold`) | 1.4 | ChartWrapper title, card section labels ("MY ISSUES", "UPCOMING RELEASES", "PAST 7 DAYS…") |
| Body | 14px (`text-sm`) | 400 (`font-normal`) | 1.5 | Release name, "of N done" annotation, legend labels, header-right total values |
| Label | 12px (`text-xs`) | 400 (`font-normal`) | 1.3 | Date subline, card subtitle, muted annotations, chart axis ticks, chart bar labels, "% ready", today pill |

Additional specific rules:
- Section label style (card headers): `text-xs font-normal text-muted-foreground uppercase tracking-wide` — the `uppercase tracking-wide` treatment distinguishes section labels without needing a separate weight. Applies to "MY ISSUES", "UPCOMING RELEASES", and "PAST 7 DAYS · HOURS & COMMITS PER DAY" labels.
- Big number (My Issues done count): `text-4xl font-semibold text-foreground` — the "8" in "8 of 13 done". Size = 36px (Display role), weight = 600. The size alone carries visual emphasis.
- "of N done" annotation: `text-sm font-normal text-muted-foreground` inline after big number.
- Hero greeting: `text-4xl font-semibold text-foreground` — same Display role as the big number. Both are 36px / semibold.
- Chart bar labels: `fontSize={12}`, `fill="var(--muted-foreground)"` — Label role (12px). Maps onto `text-xs` / Label. Consistent with `WeeklyTrendChart` `LabelList` pattern; 1px difference from 11px is imperceptible in rendered bars.
- Header-right totals (hours/commits): `text-sm font-normal` for values in their respective `color: var(--chart-1)` / `var(--chart-2)` — Body role; color carries the distinction, not weight.
- Today pill (X-axis): `text-xs font-normal bg-foreground text-background rounded-full px-2 py-0.5` — Label role.

Source: `taskflow/src/routes/dashboard/index.tsx` (hero), `taskflow/src/routes/dashboard/DashboardReleaseCard.tsx` (card header), `taskflow/src/routes/dashboard/WeeklyTrendChart.tsx` (chart labels)

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--background)` — oklch(1 0 0) light / oklch(0.145 0 0) dark | Page background, hero region background |
| Secondary (30%) | `var(--card)` — oklch(1 0 0) light / oklch(0.205 0 0) dark | All Card surfaces (MY ISSUES, UPCOMING RELEASES, chart card) |
| Accent — hours (blue) | `var(--chart-1)` — oklch(0.809 0.105 251.813) | Hours bar fill, "h logged" header total, left Y-axis |
| Accent — commits (green) | `var(--chart-2)` — oklch(0.623 0.214 259.815) | Commits bar fill, "commits" header total, right Y-axis |
| Accent — To Do | `var(--muted-foreground)` — oklch(0.556 0 0) light / oklch(0.708 0 0) dark | My Issues "To Do" bar segment and legend swatch |
| Accent — In Progress | `var(--chart-1)` — oklch(0.809 0.105 251.813) | My Issues "In Progress" bar segment and legend swatch |
| Accent — Done | oklch(0.623 0.214 259.815) (chart-2 / green) | My Issues "Done" bar segment and legend swatch; also readiness progress bar fill |
| Destructive | `var(--destructive)` — oklch(0.577 0.245 27.325) light / oklch(0.704 0.191 22.216) dark | Error states only; no destructive actions in this phase |
| Muted surface | `var(--muted)` | Skeleton, empty card backgrounds, To Do bar segment track |
| Timeline dot (default) | `var(--border)` — oklch(0.922 0 0) | Upcoming release dots without readiness |
| Timeline dot (ready) | `var(--chart-2)` (green) | Upcoming release dots where donePct >= 80% |

Accent reserved for: hours bars (chart-1 blue), commits bars (chart-2 green), In Progress segments (chart-1), Done segments (chart-2/green), today X-axis pill (foreground/background inversion), readiness progress bars (chart-2/green), release timeline dots.

Source: `taskflow/src/index.css` (OKLCH tokens), `taskflow/src/routes/dashboard/WeeklyTrendChart.tsx` (COLOR_MET / COLOR_UNDER), `taskflow/src/routes/dashboard/DashboardReleaseCard.tsx`

---

## Region Contracts

### Region 1 — Hero Greeting

Layout: Full-width header row with bottom border. Vertically stacks greeting + subline.

**Greeting line:**
- Text: `"{getTimeGreeting()} {firstName ?? 'there'}"` — keep existing `getTimeGreeting()` + first-name parser from `index.tsx`
- Typography: `text-4xl font-semibold text-foreground` (Display role — 36px / 600)
- Time buckets: morning (00:00–11:59), afternoon (12:00–17:59), evening (18:00–23:59)

**Subline (date + sprint position):**
- Format: `"Monday, 15 June 2026 · Sprint day 4 of 10"`
- Date: `new Date().toLocaleDateString('en-GB', { weekday:'long', day:'numeric', month:'long', year:'numeric' })` — existing pattern
- Sprint clause: `"· Sprint day {elapsed} of {total}"` where:
  - `elapsed = differenceInCalendarDays(today, sprint.startDate) + 1`
  - `total = differenceInCalendarDays(sprint.endDate, sprint.startDate) + 1`
  - Both dates via local calendar (`toLocaleDateString('en-CA')`) NOT `toISOString()`
- **Sprint clause hidden** when `activeSprint === null` (date line still shown without the bullet+sprint segment)
- Typography: `text-xs text-muted-foreground mt-1` (Label role)

Shell: `px-6 pt-5 pb-5 border-b border-border/50 shrink-0` (unchanged from current index.tsx)

**States:**
- No active sprint: subline shows date only, no sprint segment (never shows "Sprint day N of M")
- Loading sprint: render date immediately (sprint clause appears once data resolves — no skeleton on the hero text)

---

### Region 2a — MY ISSUES Card (top row, left)

**Card shell:** `<Card>` (rounded-xl ring-1 ring-foreground/10 bg-card)

**Card header:**
- Label: "MY ISSUES" — `text-xs font-normal text-muted-foreground uppercase tracking-wide` (Label role)
- Icon: lucide `CheckSquare` (size-4) in `text-chart-1` (blue) — consistent with card header pattern

**Big number row:**
- Layout: flex row, baseline aligned
- Done count: `text-4xl font-semibold text-foreground` (Display role — 36px / 600) — e.g. "8"
- Annotation: `text-sm font-normal text-muted-foreground` (Body role) — " of 13 done" (space before "of")

**Segmented horizontal bar:**
- Full-width, height 8px (`h-2`), rounded-full outer track (`bg-muted rounded-full overflow-hidden`)
- Three contiguous flex segments, each proportional to count / total, no gap between segments:
  - To Do: `bg-muted-foreground/40` (desaturated)
  - In Progress: `var(--chart-1)` blue
  - Done: `var(--chart-2)` / green — rightmost
- Minimum visible width for non-zero segment: 4px (prevent invisible slivers)
- 0% segment: render at 0 width (invisible), do not hide with conditional (avoids layout jump)

**Legend row:**
- Layout: flex row, gap-4, items-center
- Per item: color swatch (8px square, rounded-sm) + label text
  - "To Do {n}" — swatch `bg-muted-foreground/40`, label `text-xs font-normal text-muted-foreground` (Label role)
  - "In Progress {n}" — swatch `bg-chart-1`, label `text-xs font-normal text-muted-foreground` (Label role)
  - "Done {n}" — swatch `bg-chart-2`, label `text-xs font-normal text-muted-foreground` (Label role)

**Data invariant (also tested):** `toDo + inProgress + done === total` — assert in unit test.

**States:**
- Loading: Card with 3 skeleton lines (height 4px, 8px, 4px with gap-2)
- Error: `<ErrorState error={e} onRetry={refetch} viewName="My Issues" />` inside Card (per-section degradation)
- Empty (0 issues in sprint): Card renders header + empty state copy (see Copywriting)
- Zero in one bucket: segment collapses to 0 width, legend shows "To Do 0" (honest zero)

---

### Region 2b — UPCOMING RELEASES Card (top row, right)

**Card shell:** `<Card>` (same chrome as MY ISSUES)

**Card header:**
- Label: "UPCOMING RELEASES" — `text-xs font-normal text-muted-foreground uppercase tracking-wide` (Label role)
- Icon: lucide `Calendar` (size-4, `text-chart-1` blue) — matches existing DashboardReleaseCard

**Timeline container:**
- Layout: full-width horizontal track
- Track line: 1px `bg-border` horizontal line connecting dot centers, absolutely positioned at dot vertical midpoint
- Up to 3 dots, evenly spaced (flex justify-between or absolute positioning at 0%, 50%, 100% of track width for 3 dots; adjust for 1 or 2)

**Per-release dot:**
- Dot: 10px circle (`size-2.5`), `ring-2 ring-card` (white halo to cut track line visually)
  - Default fill: `bg-muted-foreground`
  - donePct >= 80%: `bg-chart-2` (green)
  - 100% ready: `bg-chart-2` (green) with checkmark indicator (optional — executor discretion)
- Below dot (vertically stacked, centered on dot):
  - Release name: `text-xs font-normal text-foreground truncate max-w-[96px]` (Label role)
  - Relative due: `text-xs font-normal text-muted-foreground` (Label role) — from `getReleaseTimingLabel()`:
    - "Today" (due-today)
    - "Tomorrow" (daysUntil === 1)
    - "in {n} days" (daysUntil > 1)
    - "overdue" in `text-amber-600 dark:text-amber-400` (overdue)
  - Readiness bar: height 4px (`h-1`), width 80px, `bg-muted rounded-full` track, `bg-chart-2 rounded-full` fill at `{donePct}%` width
  - "% ready" label: `text-xs font-normal text-muted-foreground` (Label role) — "{donePct}% ready"
- Above dot (if overdue): a lucide `AlertCircle` size-3 in `text-amber-500` (optional — executor discretion)

**Data: `getReleaseTimingLabel` relative-due label rules:**
- "Tomorrow" = daysUntil === 1 (not shown in current DashboardReleaseCard; add this case)
- "Today" = due-today
- "in N days" = daysUntil > 1
- "overdue" = releaseDate < today (amber color)

**States:**
- Loading: Card with skeleton (3 rows of h-2 rectangles with gap-2, animate-pulse)
- Error: `<ErrorState error={e} onRetry={refetch} viewName="Upcoming Releases" />` inside Card
- 0 upcoming releases with due dates: empty state copy (see Copywriting)
- 1 or 2 releases (fewer than 3): render only what exists; do not show placeholder dots
- 0% ready: readiness bar renders at 0 width fill (honest zero)
- 100% ready: readiness bar renders full-width green fill

---

### Region 3 — PAST 7 DAYS — HOURS & COMMITS PER DAY Chart Card

**Card shell:** Full-width `<Card>` with `<ChartWrapper bare>` inside (avoids double-border per `chart-wrapper.tsx` `bare` prop rule), OR use a standalone `<ChartWrapper>` with explicit height (executor chooses based on header layout needs).

**Recommended structure** (to support header-right totals alongside title):
```
<Card>
  <CardHeader>
    <div class="flex items-center justify-between">
      <CardTitle class="text-xs font-normal text-muted-foreground uppercase tracking-wide">
        PAST 7 DAYS · HOURS & COMMITS PER DAY
      </CardTitle>
      <div class="flex items-center gap-4">  {/* header-right totals */}
        <span class="text-sm font-normal" style="color: var(--chart-1)">{totalHours} h logged</span>
        <span class="text-sm font-normal" style="color: var(--chart-2)">{totalCommits} commits</span>
      </div>
    </div>
  </CardHeader>
  <CardContent>
    {/* ChartWrapper bare + explicit-height div */}
  </CardContent>
</Card>
```

**Chart body:**
- Outer div: `style={{ height: 280 }}` (explicit height — WebKit 0×0 guard, Phase 81 D-03)
- `<ChartContainer config={chartConfig} className="h-full w-full">`
- `<ComposedChart data={dayBuckets} responsive margin={{ top: 24, right: 40, left: 0, bottom: 0 }}>`
  - Note: `responsive` prop on Recharts v3 chart primitives, NOT `<ResponsiveContainer>` (D-14)
  - `isAnimationActive={false}` on all Bar elements (D-14)
  - `right: 40` margin is a Recharts implementation value for right Y-axis label clearance; not a CSS spacing token

**Axes:**
- `<XAxis dataKey="label">` — 7 weekday abbreviations ("Tue", "Wed", … "Mon")
  - Today tick: render as a pill — use a custom `tick` renderer or wrap with a `<foreignObject>`; background `bg-foreground`, text `text-background`, `rounded-full px-2 py-0.5 text-xs font-normal` (Label role)
- `<YAxis yAxisId="hours" orientation="left" tickFormatter={(v) => \`${v}h\`}>` — hours scale, `text-chart-1`
- `<YAxis yAxisId="commits" orientation="right">` — commits scale, `text-chart-2`

**Reference line:**
- `<ReferenceLine y={maxValue} yAxisId="hours" strokeDasharray="4 4" stroke="var(--muted-foreground)" label={{ ... }}>` — dashed gridline at the daily max hours value (not a fixed target; use `Math.max(...dayBuckets.map(b => b.hours))`)

**Bars (grouped, side-by-side):**
- Hours bar: `<Bar yAxisId="hours" dataKey="hours" fill="var(--chart-1)" radius={[4,4,0,0]} isAnimationActive={false}>`
  - `<LabelList position="top" formatter={(v) => v > 0 ? \`${formatHoursMinutes(v)}\` : '0h'} fontSize={12} fill="var(--muted-foreground)">` (Label role — 12px)
  - 0-value days: flat bar (height approaches 0), label "0h" still shown
- Commits bar: `<Bar yAxisId="commits" dataKey="commits" fill="var(--chart-2)" radius={[4,4,0,0]} isAnimationActive={false}>`
  - `<LabelList position="bottom" formatter={(v) => String(v)} fontSize={12} fill="var(--muted-foreground)">` (Label role — 12px)
  - 0-value days: flat bar, label "0" still shown
  - Note: commits labels positioned "bottom" (below bar) to avoid collision with hours labels above

**Today day highlight:**
- Today's column: both bars receive `stroke="var(--foreground)"` + `strokeWidth={2}` on their `<Cell>` (same pattern as `WeeklyTrendChart`)
- X-axis today tick: visual pill (see Axes above)

**Chart config object:**
```ts
const chartConfig = {
  hours:   { label: 'Hours logged', color: 'var(--chart-1)' },
  commits: { label: 'Commits',      color: 'var(--chart-2)' },
} satisfies ChartConfig
```

**States (per-section independent degradation — DASH-07):**
- Loading: `<ChartWrapper isLoading={showSkeleton}>` renders `<Skeleton className="w-full h-full rounded-md">` inside explicit-height div
- Error (Jira/Tempo fetch failure): `<ErrorState error={e} onRetry={refetch} viewName="Hours & Commits">` — chart section error never propagates to other cards
- Tempo disabled: `isEmpty={!tempoEnabled}` → `<EmptyState icon={Timer} title="Tempo not connected" subtitle="Connect Tempo in Settings to see logged hours.">` — same pattern as `WeeklyTrendChart`
- GitLab not configured: commits series shows 0 for all days (no commits bars omitted); if gitlabBaseUrl empty → commits series stays 0 (no error shown for commits independently)
- All-zero week: renders all 7 flat bars with "0h" / "0" labels — NEVER shows empty state for an all-zero but connected week (Pitfall 6 / D-12)
- Partial today (mid-day): bars render what's recorded; no special visual treatment beyond today highlight
- Weekend days with 0 activity: flat bars at 0, not omitted (D-12)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Hero greeting — morning | "Good morning, {firstName}" |
| Hero greeting — afternoon | "Good afternoon, {firstName}" |
| Hero greeting — evening | "Good evening, {firstName}" |
| Hero greeting — no name | "Good {time-of-day}, there" |
| Hero subline — with sprint | "{Day}, {D} {Month} {YYYY} · Sprint day {N} of {M}" |
| Hero subline — no sprint | "{Day}, {D} {Month} {YYYY}" |
| My Issues card label | "MY ISSUES" |
| My Issues scope annotation | "this sprint" (shown as CardDescription below label) |
| My Issues annotation | "of {total} done" (inline after big number) |
| My Issues legend item | "To Do {n}" / "In Progress {n}" / "Done {n}" |
| My Issues empty heading | "No issues assigned" |
| My Issues empty body | "You have no issues assigned in the current sprint." |
| My Issues error | "Could not load sprint issues. Tap retry to try again." |
| Upcoming Releases card label | "UPCOMING RELEASES" |
| Upcoming Releases scope annotation | "next 3 with due dates" |
| Upcoming Releases relative due — today | "Today" |
| Upcoming Releases relative due — tomorrow | "Tomorrow" |
| Upcoming Releases relative due — future | "in {n} days" |
| Upcoming Releases relative due — overdue | "overdue" |
| Upcoming Releases readiness | "{n}% ready" |
| Upcoming Releases empty heading | "No upcoming releases" |
| Upcoming Releases empty body | "No unreleased versions with a due date were found." |
| Upcoming Releases error | "Could not load releases. Tap retry to try again." |
| Chart card label | "PAST 7 DAYS · HOURS & COMMITS PER DAY" |
| Chart header total — hours | "{n} h logged" (blue, var(--chart-1)) |
| Chart header total — commits | "{n} commits" (green, var(--chart-2)) |
| Chart X-axis today pill | weekday abbreviation (e.g. "Mon") — no extra label |
| Chart empty — Tempo off | "Tempo not connected" / "Connect Tempo in Settings to see logged hours." |
| Chart empty — zero week | (no empty state — render flat 0-bars; zero is valid data) |
| Chart error | "Could not load hours & commits. Tap retry to try again." |
| Primary CTA | No primary CTA — this phase is read-only dashboard; no user action button |
| Destructive actions | None — no destructive actions in this phase |

Note: all "Tap retry" copy is rendered by the existing `<ErrorState>` component which already includes a retry button. The `viewName` prop provides the context noun.

---

## Component Inventory

Existing components to reuse (no new installs required):

| Component | Import path | Used by |
|-----------|-------------|---------|
| `<Card>`, `<CardHeader>`, `<CardTitle>`, `<CardContent>`, `<CardDescription>` | `@/components/ui/card` | All three card regions |
| `<Skeleton>` | `@/components/ui/skeleton` | Loading states in all cards |
| `<ErrorState>` | `@/components/ui/error-state` | Error states in all cards |
| `<EmptyState>` | `@/components/ui/empty-state` | My Issues empty, Releases empty, Tempo-off |
| `<Progress>` | `@/components/ui/progress` | Readiness bars in Upcoming Releases (reuse from DashboardReleaseCard) |
| `<ChartWrapper>` | `@/components/chart-wrapper` | Hours & Commits chart shell |
| `<ChartContainer>`, `<ChartConfig>` | `@/components/ui/chart` | Chart primitive |
| Recharts `<ComposedChart>`, `<Bar>`, `<Cell>`, `<LabelList>`, `<XAxis>`, `<YAxis>`, `<ReferenceLine>` | `recharts` | Hours & Commits chart bars |
| lucide `CheckSquare`, `Calendar`, `Timer` | `lucide-react` | Card headers, Tempo empty state |

New component files to create:

| File | Purpose |
|------|---------|
| `src/routes/dashboard/MyIssuesCard.tsx` | My Issues card with segmented bar |
| `src/routes/dashboard/UpcomingReleasesTimeline.tsx` | 3-dot timeline card |
| `src/routes/dashboard/HoursCommitsChart.tsx` | Full-width dual-axis chart card |

The root `src/routes/dashboard/index.tsx` is rewritten to compose only the 3 new components.

---

## Interaction Contract

This dashboard is read-only. No user-initiated mutations.

| Interaction | Behavior |
|-------------|----------|
| Error state retry button | Re-calls the relevant `refetch()` for that card only; does not reload other cards |
| Today pill on X-axis | Visual indicator only — not clickable |
| Timeline dots | Visual indicator only — not clickable |
| Segmented bar segments | Visual only — no tooltip or click action |
| Progress/readiness bars | Visual only |

No navigation actions, no modals, no destructive confirmations in this phase.

---

## Accessibility Contract

| Region | ARIA requirement |
|--------|-----------------|
| Hero header | `<header>` element or `role="banner"` on the header div; greeting as `<h1>` |
| My Issues card | `role="region" aria-label="My issues this sprint"` on Card wrapper |
| Segmented bar | `role="img" aria-label="Sprint progress: {toDo} to do, {inProgress} in progress, {done} done"` |
| Upcoming Releases card | `role="region" aria-label="Upcoming releases"` on Card wrapper |
| Chart card | `role="region" aria-label="Past 7 days hours and commits"` on Card wrapper |
| Chart | `aria-label="Hours and commits per day bar chart"` on ChartContainer |
| Loading skeleton | `aria-busy="true"` on Card during loading (matches existing StatTile pattern) |
| Error state | `<ErrorState>` component already handles `role="alert"` |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Card, Skeleton, Progress, Badge, EmptyState, ErrorState, ChartContainer | not required |
| Third-party | none | not applicable |

No third-party registries declared. Vetting gate: not applicable.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
