---
phase: 83-dashboard-stat-tiles-and-sprint-health-chart
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/dashboardMetrics.ts
  - taskflow/src/routes/dashboard/dashboardMetrics.test.ts
  - taskflow/src/routes/dashboard/widget-removal.guard.test.ts
  - taskflow/src/components/app/Sidebar.tsx
autonomous: true
requirements: [DASH-02, DASH-03, DASH-01]
must_haves:
  truths:
    - "SP Done excludes subtasks: parent(5 SP) + 2 subtasks(2 SP each) sums to 5, not 9"
    - "Personal tile counts (Open/InProgress/Overdue) derive from the warm sprint-board cache, filtered to the current user's non-subtask issues"
    - "getDaysRemaining returns a correct day count from an active-sprint endDate, 0 when ending today, null when absent"
    - "Donut data excludes zero-SP categories and is points-weighted by statusCategory"
    - "Active-sprint cache is prefetched for /dashboard so SprintHealthSection can read endDate with zero new fetch on Dashboard load"
  artifacts:
    - path: "taskflow/src/routes/dashboard/dashboardMetrics.ts"
      provides: "Pure derivation functions: computeSpDone, computeSpTotal, computePersonalTileCounts, computeDonutData, getDaysRemaining, filterNonSubtasks"
      exports: ["computeSpDone", "computeSpTotal", "computePersonalTileCounts", "computeDonutData", "getDaysRemaining", "filterNonSubtasks", "DonutSegment"]
      min_lines: 50
    - path: "taskflow/src/routes/dashboard/dashboardMetrics.test.ts"
      provides: "Unit tests including the mandated subtask-exclusion fixture (5 not 9)"
      contains: "toBe(5)"
  key_links:
    - from: "taskflow/src/components/app/Sidebar.tsx"
      to: "['jira-active-sprint', activeJiraProject, jiraBaseUrl, boardId]"
      via: "prefetchForPath('/dashboard') active-sprint prefetch (Option B per D-10)"
      pattern: "jira-active-sprint"
user_setup: []
---

<objective>
Wave 0 foundation for Phase 83. Create the pure, React-free metric-derivation module (`dashboardMetrics.ts`) that is the single source of truth for every stat tile and the donut, with its full unit-test suite anchored by the mandated criterion-2 subtask-exclusion test (parent 5 SP + 2×2 subtasks ⇒ 5, not 9). Extend the existing widget-removal guard to assert the soon-to-be-deleted widgets are no longer imported. Apply the D-10 Option B Sidebar change so the active-sprint cache is warm for `/dashboard` — satisfying the criterion-3 "zero new API calls on Dashboard load" bar.

Purpose: Lock the testable derivation contract before any UI is built; downstream UI imports these functions and never re-derives inline.
Output: `dashboardMetrics.ts` + `dashboardMetrics.test.ts`, an extended `widget-removal.guard.test.ts`, and a 1-call Sidebar prefetch extension.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-CONTEXT.md
@.planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-RESEARCH.md
@.planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-PATTERNS.md
@.planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create dashboardMetrics.ts + dashboardMetrics.test.ts (pure derivation + mandated subtask-exclusion test)</name>
  <read_first>
    - taskflow/src/routes/dashboard/DashboardSprintCard.tsx (source of getDaysRemaining lines 29-34, !subtask filter line 76, SP-sum/donePoints/totalPoints lines 78-92 — lift verbatim)
    - taskflow/src/routes/dashboard/DashboardInProgressCard.tsx (assignee.displayName personal-filter pattern lines 62-67)
    - taskflow/src/lib/my-tasks-sort.ts (timezone-safe overdue check lines 54-56; duedate index-signature cast)
    - taskflow/src/lib/my-tasks-sort.test.ts (makeIssue factory analog — pure-fn unit test structure)
    - taskflow/src/services/jira.ts (JiraIssue type: issuetype.subtask line ~166, statusCategory.key 'new'|'indeterminate'|'done' line ~156, duedate field)
    - .planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-PATTERNS.md (verbatim function bodies for all 6 exports)
    - .planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-RESEARCH.md (Q3, Q5, Q6 — exact filter expressions)
  </read_first>
  <behavior>
    - computeSpDone([parent(subtask:false,sp:5,done), sub(subtask:true,sp:2,done), sub(subtask:true,sp:2,done)], 'customfield_10016') === 5 (MANDATED criterion-2 test — not 9)
    - computeSpTotal([parent(false,5,new), sub(true,2,new)], 'customfield_10016') === 5
    - computePersonalTileCounts: open = my non-subtask issues with statusCategory.key !== 'done'; inProgress = my non-subtask issues with key === 'indeterminate'; overdue = my non-subtask issues with duedate < today AND key !== 'done'. Issues assigned to other displayNames are excluded.
    - getDaysRemaining(undefined) === null; getDaysRemaining(<NaN date>) === null; an endDate 3 days out === 3; an endDate today (ms<=0) === 0
    - computeDonutData: returns segments [{name:'todo',fill:'var(--chart-1)'},{name:'inProgress',fill:'var(--chart-2)'},{name:'done',fill:'var(--chart-3)'}], points-weighted by statusCategory over non-subtasks, with zero-value segments filtered out
    - filterNonSubtasks drops every issue where fields.issuetype.subtask is true
  </behavior>
  <action>
    Create `taskflow/src/routes/dashboard/dashboardMetrics.ts` as a pure TypeScript module importing only `import type { JiraIssue } from '@/services/jira'` — NO React, NO hooks. Export exactly: `filterNonSubtasks(issues)`, `computeSpDone(issues, spKey)`, `computeSpTotal(issues, spKey)`, `computePersonalTileCounts(issues, displayName, today)` returning `{ open, inProgress, overdue }`, `computeDonutData(issues, spKey)` returning `DonutSegment[]`, `getDaysRemaining(endDateIso)`, and the `DonutSegment` interface `{ name: string; value: number; fill: string }`. Lift the function bodies verbatim from 83-PATTERNS.md (§dashboardMetrics.ts). Use `i.fields.issuetype.subtask` (boolean — never name comparison), `i.fields.status.statusCategory?.key ?? 'new'`, `(i.fields[spKey] as number | null | undefined) ?? 0`, and `i.fields.duedate as string | null | undefined`. The donut fills MUST be the CSS-var strings `var(--chart-1)` / `var(--chart-2)` / `var(--chart-3)` — no hardcoded hex (criterion 3). `computeDonutData` filters out `value === 0` segments (Recharts pitfall). Implements D-02/D-03/D-04/D-05/D-07/D-08/D-09.

    Create `taskflow/src/routes/dashboard/dashboardMetrics.test.ts` using the `makeIssue` factory analog from `my-tasks-sort.test.ts` (adapted per 83-PATTERNS.md §dashboardMetrics.test.ts to accept `{ subtask, sp, statusCategory, assignee?, duedate? }`). Write one `describe` block per exported function. The FIRST and mandatory test asserts `computeSpDone([parent5, sub2, sub2], 'customfield_10016')` is `5` (the criterion-2 gate). Add the computeSpTotal, computePersonalTileCounts (covering Open/InProgress/Overdue plus an other-user exclusion case), getDaysRemaining (null/0/3-day cases), and computeDonutData (zero-SP exclusion + 3-category) cases per the 83-VALIDATION.md per-task map.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- dashboardMetrics --run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- dashboardMetrics --run` exits 0
    - dashboardMetrics.test.ts contains the assertion `computeSpDone([parent, sub1, sub2], 'customfield_10016')` resolving to `toBe(5)`
    - dashboardMetrics.ts contains no `import` of `react` or any hook; only `import type { JiraIssue }`
    - dashboardMetrics.ts donut fills are `var(--chart-1)`, `var(--chart-2)`, `var(--chart-3)` — grep finds no hex color in the file: `grep -v '^#' taskflow/src/routes/dashboard/dashboardMetrics.ts | grep -ciE '#[0-9a-f]{3,6}\b'` returns 0
    - dashboardMetrics.ts uses `i.fields.issuetype.subtask` (not an issuetype name comparison)
  </acceptance_criteria>
  <done>The metrics module exists with all 6 named exports, the mandated subtask-exclusion test passes asserting 5 (not 9), and the full `dashboardMetrics` test suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend widget-removal guard + add active-sprint prefetch for /dashboard (Sidebar Option B)</name>
  <read_first>
    - taskflow/src/routes/dashboard/widget-removal.guard.test.ts (existing fs.existsSync + source-string assertion patterns)
    - taskflow/src/components/app/Sidebar.tsx (prefetchForPath lines 121-206; shared resolveBoardId chain lines 129-144; /sprint-board active-sprint prefetch lines 145-155; fetchActiveSprint already imported)
    - .planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-RESEARCH.md (Q1 Option B resolution + Critical Open Question 2)
  </read_first>
  <action>
    In `taskflow/src/components/app/Sidebar.tsx` `prefetchForPath`, apply D-10 Option B: make the active-sprint prefetch fire for BOTH `/sprint-board` AND `/dashboard`. The `resolveBoardId` promise chain (lines 129-144) is already shared by both paths inside the `if (path === '/sprint-board' || path === '/dashboard')` block — lift the `resolveBoardId.then((boardId) => queryClient.prefetchQuery({ queryKey: ['jira-active-sprint', activeJiraProject, jiraBaseUrl, boardId], queryFn: () => fetchActiveSprint(jiraBaseUrl, jiraToken, activeJiraProject, boardId ?? undefined), staleTime: 5 * 60 * 1000 }))` block OUT of the `if (path === '/sprint-board')` sub-block so it runs for both. Keep the `/sprint-board`-only prefetches (epics-basic, project-statuses) inside the sub-block. Add a one-line comment noting "Phase 83 D-10 Option B: warm active-sprint for /dashboard so SprintHealthSection reads endDate with enabled:false (zero new API calls)". No new import is needed — `fetchActiveSprint` is already imported.

    In `taskflow/src/routes/dashboard/widget-removal.guard.test.ts`, add a new `describe('dashboard subtree — Phase 83 widget removal guard')` block with: (a) `fs.existsSync(path.join(DASHBOARD_DIR, 'SmokeTestChart.tsx'))` is `false`, `DashboardSprintCard.tsx` is `false`, `DashboardInProgressCard.tsx` is `false`; (b) a source-string assertion reading `index.tsx` via `fs.readFileSync` and asserting it contains no import of `SmokeTestChart`, `DashboardSprintCard`, or `DashboardInProgressCard` (match the existing source-string assertion style in this file, filtering comment lines where relevant). NOTE: these assertions will be RED until plan 83-03 performs the deletions — that is expected; mark the block with a comment "GREEN after 83-03". (Do NOT also assert DashboardReleaseCard absence — it is retained.)
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- widget-removal --run; echo "guard ran (red until 83-03 deletes files — expected)"; grep -c "jira-active-sprint" src/components/app/Sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx: the `['jira-active-sprint', ...]` prefetch is reachable for `path === '/dashboard'` (no longer nested only inside `if (path === '/sprint-board')`)
    - Sidebar.tsx still type-checks: `cd taskflow && npx tsc --noEmit` reports no new errors in Sidebar.tsx
    - widget-removal.guard.test.ts contains `existsSync` assertions toBe(false) for SmokeTestChart.tsx, DashboardSprintCard.tsx, DashboardInProgressCard.tsx
    - widget-removal.guard.test.ts asserts index.tsx source does not import SmokeTestChart / DashboardSprintCard / DashboardInProgressCard
  </acceptance_criteria>
  <done>The Sidebar prefetches active-sprint for /dashboard via the shared resolveBoardId chain, and the widget-removal guard is extended to assert the three deletions and the import removals (red until 83-03, by design).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jira REST API → TanStack Query cache | Already-authenticated data; this phase only reads from the warm cache, adds no new network input |
| Cache → derived figures (per Jira project) | Derived tile/donut numbers are scoped by the sprint-board cache key which includes `activeJiraProject` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Information disclosure | dashboardMetrics over sprint-board cache | mitigate | Cache key `['jira-issues','sprint-board',activeJiraProject,storyPointsFieldKey]` includes `activeJiraProject` — derived figures cannot leak across Jira projects; metrics functions receive only the scoped issue array |
| T-83-02 | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new packages) |
| T-83-SC | Tampering | supply chain | accept | No new dependencies introduced; recharts/react-query already installed and audited in prior phases |
</threat_model>

<verification>
- `npm run test -- dashboardMetrics --run` green (all pure-function tests, incl. the mandated 5-not-9 test).
- `npm run test -- widget-removal --run` runs (assertions red until 83-03 — expected and documented).
- `npx tsc --noEmit` shows no new errors from the Sidebar edit.
</verification>

<success_criteria>
- `dashboardMetrics.ts` exports all 6 functions, no React imports, donut fills are CSS vars only.
- The criterion-2 subtask-exclusion test asserts 5 (not 9) and passes.
- Active-sprint cache is prefetched for `/dashboard` (criterion 3 enabler).
- Widget-removal guard extended for the three deletions + index.tsx import removals.
</success_criteria>

<output>
Create `.planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-01-SUMMARY.md` when done.
</output>
