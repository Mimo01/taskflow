---
phase: 83-dashboard-stat-tiles-and-sprint-health-chart
plan: 02
type: execute
wave: 2
depends_on: ["83-01"]
files_modified:
  - taskflow/src/routes/dashboard/StatTile.tsx
  - taskflow/src/routes/dashboard/SprintHealthSection.tsx
  - taskflow/src/routes/dashboard/SprintHealthSection.test.tsx
  - taskflow/src/routes/dashboard/StatTile.test.tsx
autonomous: true
requirements: [DASH-02, DASH-03, DASH-07]
must_haves:
  truths:
    - "StatTile renders a label + value + decorative icon as a static, non-interactive region (no role=button, no cursor-pointer, no hover styling)"
    - "SprintHealthSection shows days remaining + a %-complete progress bar + a points-by-status donut, all derived via dashboardMetrics over the warm sprint-board cache"
    - "SprintHealthSection degrades independently: own Skeleton / ErrorState / EmptyState via ChartWrapper; a failure here does not blank the tiles"
    - "Donut segments use var(--chart-1..3) (no hardcoded hex); donut center shows total sprint SP; isAnimationActive is false; PieChart uses the responsive prop (not ResponsiveContainer)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/StatTile.tsx"
      provides: "Static display tile (label, value, icon)"
      contains: "role=\"region\""
    - path: "taskflow/src/routes/dashboard/SprintHealthSection.tsx"
      provides: "Sprint days-remaining + progress bar + points-by-status donut, warm-cache sourced"
      contains: "jira-active-sprint"
  key_links:
    - from: "taskflow/src/routes/dashboard/SprintHealthSection.tsx"
      to: "['jira-issues','sprint-board',activeJiraProject,storyPointsFieldKey]"
      via: "useQuery staleTime:30_000 (cache key must match SprintBoardTab exactly)"
      pattern: "'jira-issues', 'sprint-board'"
    - from: "taskflow/src/routes/dashboard/SprintHealthSection.tsx"
      to: "dashboardMetrics (computeDonutData/computeSpTotal/computeSpDone/getDaysRemaining)"
      via: "import from ./dashboardMetrics"
      pattern: "from './dashboardMetrics'"
user_setup: []
---

<objective>
Build the two new UI components that consume the Wave 1 metrics module: `StatTile` (a static, display-only tile) and `SprintHealthSection` (days-remaining + progress bar + points-by-status donut via `ChartWrapper`). Both read the warm sprint-board cache with the sacred cache key; `SprintHealthSection` additionally reads the (now-prefetched) active-sprint cache for `endDate`. Each section owns its loading/error/empty states for independent degradation (DASH-07). No `index.tsx` edits here — these components are built and unit-tested in isolation; wiring lands in 83-03.

Purpose: Produce the presentational + warm-cache-reading pieces without touching the Dashboard route, so they can be tested standalone before integration.
Output: `StatTile.tsx`, `SprintHealthSection.tsx`, and their render/smoke tests.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-CONTEXT.md
@.planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-RESEARCH.md
@.planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-PATTERNS.md
@.planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-UI-SPEC.md
@taskflow/src/routes/dashboard/dashboardMetrics.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StatTile.tsx (static display tile) + StatTile.test.tsx</name>
  <read_first>
    - taskflow/src/routes/dashboard/DashboardSprintCard.tsx (card surface, header pattern, 3-block skeleton)
    - .planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-PATTERNS.md (§StatTile.tsx — props interface, card surface, header, value display)
    - .planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-UI-SPEC.md (Component Inventory §StatTile, Interaction Contract §Stat Tiles, Accessibility, Color)
    - taskflow/src/lib/utils.ts (cn helper)
    - taskflow/src/components/ui/skeleton.tsx
  </read_first>
  <behavior>
    - Renders the label text and the numeric value
    - Renders the passed lucide icon with aria-hidden
    - The tile root is a div with role="region" and aria-label containing the label
    - The value element exposes aria-label "{value} {label}" so screen readers read "4 Open"
    - No element in the rendered output has role="button" or an onClick; no cursor-pointer class
  </behavior>
  <action>
    Create `taskflow/src/routes/dashboard/StatTile.tsx`. Props: `{ label: string; value: number; icon: LucideIcon; iconClass?: string; valueClass?: string }`. Import `type { LucideIcon } from 'lucide-react'`, `Skeleton` (for parity though parent controls skeleton — only import if used), and `cn` from `@/lib/utils`. Root element: `<div role="region" aria-label={label} className="rounded-lg border border-border bg-card p-4 flex flex-col gap-3 min-h-[80px]">`. Header row: `<div className="flex items-center gap-2">` with `<Icon className={cn('size-4', iconClass)} aria-hidden />` and `<span className="text-xs text-muted-foreground uppercase tracking-wide">{label}</span>`. Value: `<p className={cn('text-3xl font-semibold text-primary', valueClass)} aria-label={`${value} ${label}`}>{value}</p>`. Per D-06 and UI-SPEC Interaction Contract: do NOT add `role="button"`, `cursor-pointer`, `hover:bg-*`, or any click handler — static display only. Implements DASH-02 tile presentation.

    Create `taskflow/src/routes/dashboard/StatTile.test.tsx` (jsdom render via @testing-library/react, matching existing dashboard card test style e.g. DashboardSprintCard.test.tsx). Assert: label renders, value renders, the root has `role="region"`, the value has `aria-label="4 Open"` for `{label:'Open', value:4}`, and that `screen.queryByRole('button')` is null (D-06 static guard). Pass a lucide icon (e.g. Activity) as the `icon` prop.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- StatTile --run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- StatTile --run` exits 0
    - StatTile.tsx contains `role="region"` and contains NO `role="button"`, NO `cursor-pointer`, NO `onClick`, NO `hover:bg-`
    - StatTile.test.tsx asserts `queryByRole('button')` is null
    - StatTile.test.tsx asserts the value element aria-label reads "{value} {label}"
  </acceptance_criteria>
  <done>StatTile renders a static labelled value tile with correct a11y semantics and no interactive affordances; its test suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SprintHealthSection.tsx (days remaining + progress + donut) + SprintHealthSection.test.tsx</name>
  <read_first>
    - taskflow/src/routes/dashboard/DashboardSprintCard.tsx (query pattern, staleTime:30_000, array-normalise guard, progress bar, days-remaining display, useDelayedLoading, empty state)
    - taskflow/src/routes/dashboard/SmokeTestChart.tsx (ChartWrapper + ChartContainer + Recharts integration to adapt BarChart→PieChart)
    - taskflow/src/components/chart-wrapper.tsx (ChartWrapper props: title, description, height, isLoading, error, isEmpty, onRetry)
    - taskflow/src/components/chart-wrapper.test.tsx (jsdom chart render assertion: [data-slot="chart"] / .recharts-surface)
    - taskflow/src/components/ui/chart.tsx (ChartContainer + ChartConfig)
    - taskflow/src/components/ui/progress.tsx, empty-state.tsx
    - taskflow/src/hooks/useDelayedLoading.ts
    - taskflow/src/services/jira.ts (fetchSprintIssues + fetchActiveSprint signatures, JiraActiveSprint endDate)
    - .planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-PATTERNS.md (§SprintHealthSection.tsx — full imports, props, both query patterns, donut, progress, days, empty state)
    - .planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-UI-SPEC.md (Component Inventory §SprintHealthSection, Copywriting, Color chart segments, Accessibility)
    - .planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-RESEARCH.md (Q1, Q4, Q7, Pattern 3/4)
    - taskflow/src/routes/dashboard/dashboardMetrics.ts (the functions to consume)
  </read_first>
  <behavior>
    - Reads sprint-board via useQuery key ['jira-issues','sprint-board',activeJiraProject,storyPointsFieldKey] with staleTime:30_000 (cache shared with SprintBoardTab — no new fetch when warm)
    - Reads active-sprint via useQuery key ['jira-active-sprint',activeJiraProject,jiraBaseUrl,boardId], enabled:false (reads from the Sidebar-prefetched cache only — zero new fetch)
    - Renders days-remaining copy ("Sprint ends today" / "1 day remaining" / "{N} days remaining") from getDaysRemaining(activeSprint.endDate)
    - Renders a Progress bar at donePct = donePoints/totalPoints (div-by-zero guarded to 0) with caption "{N}% complete · {done} / {total} pts"
    - Renders the donut (PieChart/Pie) inside ChartWrapper with isEmpty={totalSP===0}; donut center shows total sprint SP + "pts"
    - On no active sprint (and not loading) renders EmptyState "No active sprint"; ChartWrapper surfaces error/loading independently
  </behavior>
  <action>
    Create `taskflow/src/routes/dashboard/SprintHealthSection.tsx`. Props: `{ jiraBaseUrl: string; jiraToken: string; activeJiraProject: string; storyPointsFieldKey: string; boardId: number | null }` (auth lifted in index.tsx, passed down — PATTERNS pattern 1). Imports per 83-PATTERNS.md §SprintHealthSection.tsx. Sprint-board `useQuery`: key `['jira-issues','sprint-board',activeJiraProject,storyPointsFieldKey]`, `queryFn: () => fetchSprintIssues(jiraBaseUrl, jiraToken, activeJiraProject, false, storyPointsFieldKey)`, `staleTime: 30_000`, `enabled: !!jiraBaseUrl && !!jiraToken && !!activeJiraProject`. Copy the comment "CACHE KEY MUST MATCH DashboardSprintCard / SprintBoardTab exactly" (pitfall 6). Active-sprint `useQuery`: key `['jira-active-sprint', activeJiraProject, jiraBaseUrl, boardId]`, `queryFn: () => fetchActiveSprint(jiraBaseUrl, jiraToken, activeJiraProject, boardId ?? undefined)`, `staleTime: 5*60_000`, `enabled: false` (Option B reactive read — the Sidebar prefetch from 83-01 warms it; NEVER use queryClient.getQueryData in render per project memory reactive-cache-read). Normalise: `const sprintIssues = Array.isArray(sprintIssuesRaw) ? sprintIssuesRaw : []`. Derive via dashboardMetrics: `totalSP = computeSpTotal(sprintIssues, storyPointsFieldKey)`, `donePoints = computeSpDone(...)`, `donutData = computeDonutData(...)`, `daysLeft = getDaysRemaining(activeSprint?.endDate)`. Compute `donePct = totalSP > 0 ? Math.round((donePoints/totalSP)*100) : 0` with the verbatim div-by-zero comment. `showSkeleton = useDelayedLoading(issuesLoading)`. Render the section card `<div role="region" aria-label="Sprint health" className="rounded-lg border border-border bg-card p-6 flex flex-col gap-4">` containing: days-remaining line, `<Progress value={donePct} />` + caption, and the `ChartWrapper title="Sprint Health" description="Story points by status category" height={200} isLoading={showSkeleton} error={error} isEmpty={totalSP===0} onRetry={refetch}` wrapping `ChartContainer` + `<PieChart responsive>` + `<Pie data={donutData} dataKey="value" nameKey="name" innerRadius="60%" outerRadius="80%" isAnimationActive={false} />`, with the absolute-overlay donut-center label (total SP + "pts"). Use the donutConfig with `var(--chart-1..3)`. On `!showSkeleton && !activeSprint` render `EmptyState` with the UI-SPEC copy. Add `aria-label` on the chart container per UI-SPEC Accessibility. Use the exact copy strings from UI-SPEC Copywriting Contract. Implements DASH-03, DASH-07, D-07/D-08/D-09/D-10/D-11.

    Create `taskflow/src/routes/dashboard/SprintHealthSection.test.tsx`: render inside a `QueryClientProvider` with the sprint-board cache pre-seeded via `queryClient.setQueryData(['jira-issues','sprint-board','PROJ','customfield_10016'], [makeIssue(...)])` and the active-sprint cache pre-seeded with an endDate, mirroring chart-wrapper.test.tsx render style. Assert: the donut renders (`document.querySelector('[data-slot="chart"]')` truthy), the progress bar renders (`role="progressbar"`), and days-remaining copy appears. Add an empty-cache case asserting the "No active sprint" EmptyState copy renders (DASH-07 degradation). Reuse a local makeIssue factory.
  </action>
  <verify>
    <automated>cd taskflow && npm run test -- SprintHealthSection --run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- SprintHealthSection --run` exits 0
    - SprintHealthSection.tsx contains the exact cache key `['jira-issues', 'sprint-board', activeJiraProject, storyPointsFieldKey]` and `staleTime: 30_000`
    - SprintHealthSection.tsx active-sprint useQuery has `enabled: false` (reactive read, no fetch)
    - SprintHealthSection.tsx contains NO `queryClient.getQueryData` (project memory: reactive cache-read)
    - SprintHealthSection.tsx contains `isAnimationActive={false}` and `<PieChart responsive` and NO `ResponsiveContainer` import
    - SprintHealthSection.tsx has no hardcoded hex in chart code: `grep -v '^#' taskflow/src/routes/dashboard/SprintHealthSection.tsx | grep -ciE '#[0-9a-f]{3,6}\b'` returns 0
    - SprintHealthSection.test.tsx asserts `[data-slot="chart"]` truthy and the EmptyState "No active sprint" copy in the no-sprint case
  </acceptance_criteria>
  <done>SprintHealthSection renders days-remaining + progress + donut from the warm caches with independent loading/error/empty states, uses the sacred cache key, reads active-sprint reactively with enabled:false, and its render + empty-state tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TanStack Query warm cache → rendered UI | Components only read already-authenticated cached Jira data; no new network input, no user input |
| Jira issue summary/status text → JSX | Rendered via default React JSX escaping (no dangerouslySetInnerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-03 | Information disclosure | SprintHealthSection cache reads | mitigate | Cache key includes `activeJiraProject`; figures are scoped to the current project — no cross-project leak |
| T-83-04 | Cross-site scripting | tile/donut text from Jira | mitigate | All Jira text rendered via JSX auto-escaping; no `dangerouslySetInnerHTML` introduced |
| T-83-SC | Tampering | supply chain | accept | No package installs in this phase |
</threat_model>

<verification>
- `npm run test -- StatTile --run` and `npm run test -- SprintHealthSection --run` both green.
- `npm run test -- dashboardMetrics --run` still green (no regression to the consumed module).
- Grep confirms no hardcoded hex in chart code and no `queryClient.getQueryData` in render.
</verification>

<success_criteria>
- StatTile is a static, accessible, non-interactive display tile.
- SprintHealthSection derives all figures via dashboardMetrics over the sacred cache key, reads active-sprint with enabled:false, and degrades independently.
- Donut uses var(--chart-1..3), isAnimationActive=false, responsive prop, 60%/80% radii, center total-SP label.
</success_criteria>

<output>
Create `.planning/phases/83-dashboard-stat-tiles-and-sprint-health-chart/83-02-SUMMARY.md` when done.
</output>
