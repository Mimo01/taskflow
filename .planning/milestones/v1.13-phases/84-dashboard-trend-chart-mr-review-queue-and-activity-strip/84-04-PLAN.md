---
phase: 84-dashboard-trend-chart-mr-review-queue-and-activity-strip
plan: 04
type: execute
wave: 3
depends_on: ["84-02", "84-03"]
files_modified:
  - taskflow/src/routes/dashboard/index.tsx
autonomous: false
requirements: [DASH-04, DASH-05, DASH-06, DASH-07]
must_haves:
  truths:
    - "The Dashboard mounts all three new sections (WeeklyTrendChart, MrReviewQueue, ActivityStrip) plus the retained stat tiles and sprint-health chart"
    - "index.tsx loads gitlabToken via readSecret('gitlab-pat') and reads gitlab auth fields, mirroring the existing jira-pat pattern"
    - "DashboardReleaseCard is relocated from its standalone div into a combined 'Activity & Releases' two-column section alongside ActivityStrip (D-16)"
    - "One section failing leaves all other sections rendering their own state — the Dashboard never goes fully blank (DASH-07 criterion 4)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/index.tsx"
      provides: "Full Phase 84 dashboard composition"
      contains: "WeeklyTrendChart"
  key_links:
    - from: "taskflow/src/routes/dashboard/index.tsx"
      to: "WeeklyTrendChart / MrReviewQueue / ActivityStrip / DashboardReleaseCard"
      via: "JSX mounts with auth props"
      pattern: "<WeeklyTrendChart"
    - from: "taskflow/src/routes/dashboard/index.tsx"
      to: "gitlab PAT"
      via: "readSecret('gitlab-pat') in useEffect"
      pattern: "gitlab-pat"
---

<objective>
Wire the three Phase 84 sections into the Dashboard root and relocate the next-release countdown into a combined "Activity & Releases" section (D-16). Add the gitlab auth reads and gitlab-token load that the new sections require, mirroring the existing jira-pat pattern. Then run a human UAT in a real Tauri build to confirm independent degradation, cache reuse, and the visual layout.

Purpose: This is the integration wave — all three components already exist and are unit-tested; this plan composes them and proves the runtime behaviors that can only be verified live (criterion 2 no-duplicate-request, criterion 4 independent degradation, the Tempo/GitLab empty states).

Output: Extended `index.tsx`; human UAT sign-off.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-CONTEXT.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-RESEARCH.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-PATTERNS.md
@.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-UI-SPEC.md
@taskflow/src/routes/dashboard/index.tsx
@taskflow/src/routes/dashboard/MrHealthPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire new sections into index.tsx + add gitlab auth/token + relocate release card</name>
  <files>taskflow/src/routes/dashboard/index.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/index.tsx (current root — existing jira-pat useEffect lines 49-55, useAuthStore read line 40, section mounts lines 196-214; the standalone DashboardReleaseCard div lines 207-214 is what gets relocated)
    - taskflow/src/routes/dashboard/MrHealthPanel.tsx (the gitlab-token load + tokenLoading pattern to mirror; gitlabUserId comes from useAuthStore inside MrReviewQueue, not passed as a prop)
    - taskflow/src/stores/auth.store.ts (confirm fields: gitlabBaseUrl, gitlabUserId, gitlabUsername, gitlabName, gitlabEmail, jiraUsername, activeGitlabProject)
    - taskflow/src/stores/settings.store.ts (tempoEnabled selector for the WeeklyTrendChart prop)
    - .planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-PATTERNS.md (index.tsx extension pattern lines 538-622 — auth reads, token load, section mount JSX, relocation note)
    - .planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-UI-SPEC.md (Layout — Dashboard Section Order §Layout: hero → tiles → sprint-health+trend side-by-side → MR queue → Activity & Releases)
  </read_first>
  <action>
    Extend `index.tsx` (keep `'use no memo'`). Add to the `useAuthStore()` destructure: `gitlabBaseUrl, gitlabUserId, gitlabUsername, gitlabName, gitlabEmail, jiraUsername, activeGitlabProject` (alongside existing jiraBaseUrl/activeJiraProject/jiraUserDisplayName). Add `tempoEnabled` from `useSettingsStore`.

    Add a gitlab-token load mirroring the existing jira-pat useEffect: `const [gitlabToken, setGitlabToken] = useState<string | null>(null); const [gitlabTokenLoading, setGitlabTokenLoading] = useState(true);` and a useEffect keyed on `gitlabBaseUrl` that sets loading true, calls `readSecret('gitlab-pat').then(setGitlabToken).catch(() => setGitlabToken(null)).finally(() => setGitlabTokenLoading(false))`, with an else branch setting `gitlabTokenLoading` false when no gitlabBaseUrl. Do NOT put any token string into a queryKey here.

    Import the three new components (`WeeklyTrendChart`, `MrReviewQueue`, `ActivityStrip`) from `./`.

    Restructure the section JSX to the UI-SPEC layout order (keep hero + stat tiles + the existing SprintHealthSection mount unchanged in content):
    - Replace the standalone `SprintHealthSection` section block with a two-column grid section: `<div className="relative px-6 pb-6"><div className="grid grid-cols-1 lg:grid-cols-2 gap-4"><SprintHealthSection .../><WeeklyTrendChart jiraBaseUrl={jiraBaseUrl ?? ''} jiraToken={jiraToken ?? ''} jiraUsername={jiraUsername ?? ''} tempoEnabled={tempoEnabled} /></div></div>` (preserve all existing SprintHealthSection props).
    - Add a full-width MR queue section: `<div className="relative px-6 pb-6"><MrReviewQueue gitlabBaseUrl={gitlabBaseUrl ?? ''} gitlabToken={gitlabToken ?? ''} tokenLoading={gitlabTokenLoading} /></div>`.
    - REMOVE the standalone DashboardReleaseCard div (current lines 207-214) and add a combined "Activity & Releases" two-column section (D-16): `<div className="relative px-6 pb-6"><div className="grid grid-cols-1 lg:grid-cols-2 gap-4"><ActivityStrip jiraBaseUrl={jiraBaseUrl ?? ''} jiraToken={jiraToken ?? ''} activeJiraProject={activeJiraProject ?? ''} jiraUsername={jiraUsername} gitlabBaseUrl={gitlabBaseUrl ?? ''} gitlabToken={gitlabToken ?? ''} activeGitlabProject={activeGitlabProject ?? 0} gitlabUsername={gitlabUsername} gitlabName={gitlabName} gitlabEmail={gitlabEmail} /><DashboardReleaseCard jiraBaseUrl={jiraBaseUrl ?? ''} jiraToken={jiraToken ?? ''} activeJiraProject={activeJiraProject ?? ''} /></div></div>`.

    The DashboardReleaseCard relocation is purely a mount-point move — props unchanged (Pitfall 7). Each section keeps its own degradation; no section's loading/error affects another (DASH-07). Implements DASH-04/05/06 mounting + D-16 relocation + DASH-07 composition.
  </action>
  <verify>
    <automated>cd taskflow && npm run check && npx vitest run src/routes/dashboard/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DashboardReleaseCard" taskflow/src/routes/dashboard/index.tsx` shows exactly one mount (relocated, not duplicated) plus its import
    - index.tsx imports and mounts `WeeklyTrendChart`, `MrReviewQueue`, `ActivityStrip`
    - `grep -n "gitlab-pat" taskflow/src/routes/dashboard/index.tsx` shows the readSecret load
    - No token string appears in any queryKey in index.tsx (`grep -n "queryKey" index.tsx` reviewed — only the existing sprint-board key, no token element)
    - ActivityStrip is mounted in the same grid as DashboardReleaseCard (Activity & Releases section)
    - `cd taskflow && npm run check` passes (biome + tsc)
    - `cd taskflow && npx vitest run src/routes/dashboard/` all green (including the existing index.test.tsx)
  </acceptance_criteria>
  <done>index.tsx composes all five sections in the UI-SPEC order, loads the gitlab token, relocates the release card into Activity & Releases, and the full dashboard test suite + check are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — real Tauri build: layout, independent degradation, cache reuse, empty states</name>
  <what-built>
    The complete Phase 84 Dashboard: weekly Tempo trend chart with 8h target line, MR review queue (two groups from the warm GitLab cache), and an activity strip co-located with the next-release countdown — all degrading independently. Built and unit-tested across plans 01-03 and wired in Task 1.
  </what-built>
  <files>taskflow/src/routes/dashboard/index.tsx (verified live; no file changes in this task)</files>
  <action>Manual human verification in a real Tauri build — no code changes. Execute the how-to-verify steps below and record the outcome of each in the SUMMARY. This checkpoint gates phase completion: it proves the runtime-only behaviors (independent degradation, warm-cache reuse with zero duplicate requests, Tempo/GitLab empty states, WebKit chart rendering) that unit tests cannot cover.</action>
  <how-to-verify>
    1. Build and launch the real Tauri app: `cd taskflow && npm run tauri dev` (or the project's standard dev-build command). Navigate to `/dashboard`.
    2. LAYOUT: confirm top-to-bottom order — gradient hero → stat tiles row → Sprint health + Weekly trend charts side-by-side (stacked on a narrow window) → MR review queue (full width) → Activity & Releases (ActivityStrip left, next-release countdown right). Resize the window narrow to confirm the grids stack.
    3. TREND CHART (DASH-04): with Tempo enabled, confirm Mon-Fri bars render at correct dimensions (no 0×0 collapse), the 8h "Target" reference line is visible, bar/line colors come from the theme (toggle dark/light — colors should adapt). Then toggle Tempo OFF in Settings and confirm the chart shows a graceful "Tempo not connected" empty state (NOT an error).
    4. MR QUEUE (DASH-06): confirm two labelled groups ("Awaiting my review", "My open MRs"); confirm an MR appears in only one group; click an MR row and confirm it opens in the EXTERNAL browser (web_url). If GitLab is unconfigured, confirm the "GitLab not connected" empty state; if no MRs, confirm "No MRs awaiting review".
    5. ACTIVITY STRIP (DASH-05): confirm a compact merged Jira+commits feed, newest-first, capped (~6 items) with a "+N more" indicator when there are more.
    6. CACHE REUSE (DASH-05 criterion 2 — the key test): open devtools Network (or TanStack Query devtools). Visit Standup Notes first, then navigate to Dashboard in the SAME session. Confirm ZERO new Jira-activity and commits network requests fire on the Dashboard (the activity strip reads the warm Standup cache).
    7. INDEPENDENT DEGRADATION (DASH-07 criterion 4): simulate one section failing (e.g. temporarily break GitLab connectivity or force a query error). Confirm the OTHER sections (tiles, sprint health, trend chart, activity, release) still render their own content/state — the Dashboard never goes fully blank.
  </how-to-verify>
  <resume-signal>Type "approved" if all seven checks pass, or describe the specific section/behavior that failed.</resume-signal>
  <verify><human-check>All seven UAT checks pass in a real Tauri build; user types "approved".</human-check></verify>
  <done>Human confirms layout order, trend chart + Tempo-off empty state, MR queue grouping + external open, activity strip cap, zero duplicate requests after Standup→Dashboard, and independent section degradation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Stronghold | index.tsx loads the gitlab PAT via readSecret('gitlab-pat'), mirroring the existing jira-pat load. Token held in component state, passed to children as props, never into a queryKey. |
| (composition only) | This plan adds no new fetch of its own — it mounts components that own their own (already-reviewed) data access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-02 | Information Disclosure | gitlabToken in index.tsx state | mitigate | Token stored in React state and passed as a prop only; never placed in a queryKey (T-62-06). Acceptance-criteria grep confirms no token in any key. |
| T-84-06 | Information Disclosure | gitlab PAT load timing | accept | Loaded via the same readSecret pattern already used for jira-pat; tokenLoading prop prevents premature "not connected" flash. No new storage or transport. |
| T-84-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (RESEARCH Package Legitimacy Audit: zero new packages). |
</threat_model>

<verification>
- `cd taskflow && npm run check` — biome + tsc green
- `cd taskflow && npx vitest run src/routes/dashboard/` — all dashboard component + integration tests green
- `cd taskflow && npx vitest run` — full suite green before /gsd-verify-work
- Human UAT sign-off on all seven checks (layout, trend chart + Tempo-off, MR queue + external open, activity strip, cache reuse, independent degradation)
</verification>

<success_criteria>
- All three new sections + retained tiles/sprint-health composed in the UI-SPEC order
- gitlab token loaded; DashboardReleaseCard relocated into Activity & Releases (D-16); no duplicate release card
- DASH-07 criterion 4 confirmed live: one section failing never blanks the others
- DASH-05 criterion 2 confirmed live: zero duplicate Jira-activity/commits requests after visiting Standup then Dashboard
- Full check + full vitest suite green; human UAT approved
</success_criteria>

<output>
Create `.planning/phases/84-dashboard-trend-chart-mr-review-queue-and-activity-strip/84-04-SUMMARY.md` when done
</output>
