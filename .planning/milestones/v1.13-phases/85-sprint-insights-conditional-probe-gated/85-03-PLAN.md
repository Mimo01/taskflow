---
phase: 85-sprint-insights-conditional-probe-gated
plan: 03
type: execute
wave: 2
depends_on: [85-01, 85-02]
files_modified:
  - taskflow/src/routes/dashboard/VelocityChart.tsx
autonomous: true
requirements: [INSIGHT-01]

must_haves:
  truths:
    - "User sees a Personal Velocity bar chart with committed vs completed SP per closed sprint when ≥3 qualifying sprints exist"
    - "When fewer than 3 qualifying sprints exist, the chart area shows an explanatory message (not an error, not the generic empty state)"
    - "Closed-sprint and per-sprint issue data are never refetched within a session (staleTime: Infinity)"
    - "A runtime fetch error surfaces the ChartWrapper error/retry state and never blanks the Dashboard"
  artifacts:
    - path: "taskflow/src/routes/dashboard/VelocityChart.tsx"
      provides: "Personal velocity chart component (INSIGHT-01)"
      contains: "computePersonalVelocitySeries"
      min_lines: 90
  key_links:
    - from: "taskflow/src/routes/dashboard/VelocityChart.tsx"
      to: "fetchClosedSprints / fetchSprintIssuesBySprintId"
      via: "useQuery + useQueries fan-out"
      pattern: "fetchClosedSprints|fetchSprintIssuesBySprintId"
    - from: "taskflow/src/routes/dashboard/VelocityChart.tsx"
      to: "computePersonalVelocitySeries"
      via: "import from ./dashboardMetrics"
      pattern: "computePersonalVelocitySeries"
    - from: "taskflow/src/routes/dashboard/VelocityChart.tsx"
      to: "var(--chart-1) / var(--chart-2)"
      via: "Bar fill props"
      pattern: "var\\(--chart-[12]\\)"
---

<objective>
Create `taskflow/src/routes/dashboard/VelocityChart.tsx` — the personal velocity chart (INSIGHT-01): committed vs completed story points across the last 6 closed sprints, personal-scoped by `displayName`. The component fetches closed sprints and per-sprint issues (both `staleTime: Infinity`), derives the series via `computePersonalVelocitySeries` (from 85-01), and renders a grouped `BarChart` inside `ChartWrapper` + `ChartContainer`. When fewer than 3 qualifying sprints exist it shows an explanatory message as ChartWrapper children (D-06 — NOT the generic isEmpty path). Runtime fetch errors degrade independently via ChartWrapper's error/retry state (D-10, criterion 3).

Purpose: Deliver the velocity insight UI on top of the Wave 1 data layer (85-02) and pure functions (85-01), honoring the locked charting stack (D-11) and personal-scope decisions (D-01/D-02/D-05/D-06).
Output: `VelocityChart.tsx` — props-only, `'use no memo'`, probe-documented, ready for `index.tsx` wiring in 85-04.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/85-sprint-insights-conditional-probe-gated/85-CONTEXT.md
@.planning/phases/85-sprint-insights-conditional-probe-gated/85-UI-SPEC.md
@.planning/phases/85-sprint-insights-conditional-probe-gated/85-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create VelocityChart.tsx — data fetch, series derivation, and <3-sprint guard</name>
  <files>taskflow/src/routes/dashboard/VelocityChart.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/WeeklyTrendChart.tsx — MASTER TEMPLATE. Lift: `'use no memo'` directive (line 1), props-only interface (no useAuthStore inside component), token-not-in-queryKey comment + pattern (T-84-02, ~lines 95–106), `useDelayedLoading` usage (~line 108), `role="region"` + `aria-label` outer wrapper, `ChartWrapper` with `isEmpty={false}` for a custom non-empty branch, explicit-height inner `<div style={{ height: 240 }}>` (WebKit 0×0 guard).
    - taskflow/src/components/chart-wrapper.tsx — `ChartWrapperProps`: `title`, `description?`, `height?` (default 240), `isLoading?`, `error?: unknown`, `isEmpty?`, `onRetry?`, `children`. State precedence is `error > isLoading > isEmpty > children` — so `isEmpty={false}` lets your custom children render.
    - taskflow/src/components/ui/empty-state.tsx — `EmptyState` props are `{ icon: LucideIcon; title: string; subtitle?: string; action? }`. Use `icon`/`title`/`subtitle` (NOT `description`).
    - taskflow/src/routes/dashboard/dashboardMetrics.ts — `computePersonalVelocitySeries(sprints, issuesBySprint, displayName, spKey): VelocityPoint[]` (added in 85-01); `VelocityPoint = { sprintName; committed; completed }`. Read its exact signature and the `VelocityPoint` export before importing.
    - taskflow/src/services/jira.ts — `fetchClosedSprints(baseUrl, token, boardId, n=6)` and `fetchSprintIssuesBySprintId(baseUrl, token, sprintId, spKey)` (added in 85-02). Read exact param order and return types.
    - taskflow/src/lib/concurrency.ts — confirm `getJiraLimit()` is the GLOBAL `p-limit(6)`; this component must NOT use it. Create a dedicated module-level `pLimit(3)` instead (D-05).
    - taskflow/src/hooks/useDelayedLoading.ts — signature for the loading-delay hook.
  </read_first>
  <action>
    Create component `VelocityChart` (default export). Start the file with `'use no memo';` (Phase 81 D-02 — React Compiler conflict with the `responsive` prop). Props-only interface `VelocityChartProps`: `jiraBaseUrl: string`, `jiraToken: string`, `jiraUserDisplayName: string`, `boardId: number | null`, `storyPointsFieldKey: string`, `activeJiraProject: string` (D-16 props-only — never read `useAuthStore` inside the component).

    Declare a dedicated module-level limiter `const velocityLimit = pLimit(3)` per D-05 (NOT `getJiraLimit()` from `lib/concurrency.ts` — the global cap is 6; this backfill gets its own tighter cap, criterion 1c).

    Add the mandated D-10 probe-outcome comment near the top of the component body (verbatim from UI-SPEC Copywriting): "Probe A+B PASSED 2026-06-15: closed-sprint endpoint returns sprint objects with startDate/endDate; SP field customfield_10106 confirmed on closed-sprint issues. Build is unconditional; <3-guard and error state handle runtime absence."

    Fetch closed sprints with `useQuery`: `queryKey: ['jira-closed-sprints', boardId]` (token absent — T-84-02; token referenced ONLY inside the `queryFn` closure), `queryFn: () => fetchClosedSprints(jiraBaseUrl, jiraToken, boardId!, 6)`, `staleTime: Infinity` (closed-sprint data never changes — D-05/criterion 2), `enabled: !!jiraToken && boardId != null`. Capture `data: closedSprints`, `isLoading: sprintsLoading`, `error: sprintsError`, `refetch: refetchSprints`.

    Fan out per-sprint issue fetches with `useQueries` over `closedSprints ?? []`: each query `queryKey: ['jira-sprint-issues', sprint.id, storyPointsFieldKey]` (token absent), `queryFn: () => velocityLimit(() => fetchSprintIssuesBySprintId(jiraBaseUrl, jiraToken, sprint.id, storyPointsFieldKey))`, `staleTime: Infinity`, `enabled: !!jiraToken && (closedSprints?.length ?? 0) > 0`. Each sprint gets its own cache slot so closed data is never refetched.

    Build `issuesBySprint` as a `Map<number, JiraIssue[]>` zipping each `closedSprints[i].id` to `sprintIssueQueries[i]?.data ?? []`. Compute `velocitySeries` via `computePersonalVelocitySeries(closedSprints ?? [], issuesBySprint, jiraUserDisplayName, storyPointsFieldKey)` only when all fan-out queries have settled (`sprintIssueQueries.every((q) => !q.isLoading)`), else `[]`.

    Derive `qualifyingSprints = velocitySeries.filter((p) => p.committed > 0 || p.completed > 0)` — these are the sprints where a personal SP sum is derivable (D-06 "qualifying" definition).

    Compute `showSkeleton = useDelayedLoading(sprintsLoading || !sprintIssueQueries.every((q) => !q.isLoading))`.

    Leave the JSX render for Task 2 but stub a minimal `return null` placeholder so the file type-checks at the end of Task 1 if you commit between tasks; otherwise complete Task 2 in the same pass. Do NOT put `jiraToken` in any `queryKey`. Do NOT import `ResponsiveContainer`. Do NOT call `getJiraLimit()`.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -i 'VelocityChart' | grep -c -i 'error' | grep -qx 0 && echo TSC_CLEAN</automated>
  </verify>
  <done>VelocityChart.tsx exists with `'use no memo'`, props-only interface, a dedicated `pLimit(3)` (not `getJiraLimit()`), the D-10 probe comment, the closed-sprints `useQuery` (staleTime: Infinity), the per-sprint `useQueries` fan-out (staleTime: Infinity), the `issuesBySprint` Map, `velocitySeries` via `computePersonalVelocitySeries`, and `qualifyingSprints` filter. No `jiraToken` appears in any queryKey. Type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render grouped BarChart + <3-sprint EmptyState branch + error/retry degradation</name>
  <files>taskflow/src/routes/dashboard/VelocityChart.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/WeeklyTrendChart.tsx — the `role="region"` → `ChartWrapper` → explicit-height `<div>` → `ChartContainer` → chart render structure (~lines 139–206). Lift the `chartConfig satisfies ChartConfig` declaration shape and the `isAnimationActive={false}` Bar pattern.
    - taskflow/src/components/ui/chart.tsx — `ChartContainer` and `ChartConfig` exports; `ChartContainer` takes `config` and `className`.
    - taskflow/src/index.css — confirm `--chart-1` (light blue) and `--chart-2` (medium blue) tokens exist (lines ~105–106) so `var(--chart-1)`/`var(--chart-2)` resolve.
    - 85-UI-SPEC.md — Copywriting Contract (exact title/description/EmptyState/legend strings) and Chart Visual Specifications (committed = `--chart-1` at 40% opacity behind, completed = `--chart-2` solid front; legend labels "Committed"/"Completed"; height 240; axis fontSize 11).
  </read_first>
  <action>
    Declare `const chartConfig = { committed: { label: 'Committed', color: 'var(--chart-1)' }, completed: { label: 'Completed', color: 'var(--chart-2)' } } satisfies ChartConfig`.

    Render: outer `<div role="region" aria-label="Personal velocity chart">` wrapping `<ChartWrapper>` with `title="Personal Velocity"`, `description="Committed vs completed story points · last 6 closed sprints"`, `height={240}`, `isLoading={showSkeleton}`, `error={sprintsError}`, `isEmpty={false}` (ALWAYS false — the <3 case is handled in children, NOT via the generic isEmpty path, D-06), `onRetry={refetchSprints}`.

    Inside ChartWrapper children, an explicit-height guard div `<div style={{ height: 240 }} className="w-full">` (WebKit 0×0 guard, Phase 81 D-03). Inside it branch on `qualifyingSprints.length < 3`:
    - TRUE (sparse-data safety net): render a centered `EmptyState` with `icon={BarChart2}` (from lucide-react), `title="Not enough sprint data"`, `subtitle="At least 3 closed sprints with assigned story points are needed to show your velocity trend."` (exact UI-SPEC wording). Wrap in `className="h-full w-full flex items-center justify-center"`.
    - FALSE: render `<ChartContainer config={chartConfig} className="h-full w-full" aria-label="Personal velocity bar chart — committed vs completed story points per sprint">` containing `<BarChart data={velocitySeries} responsive>` (the `responsive` prop — NEVER `ResponsiveContainer`, D-11). Children: `<XAxis dataKey="sprintName" tick={{ fontSize: 11 }} />`, `<YAxis tickFormatter={(v) => String(v)} tick={{ fontSize: 11 }} />`, `<Tooltip />`, `<Legend wrapperStyle={{ fontSize: 11 }} />`, then two grouped (not stacked) bars: `<Bar dataKey="committed" name="Committed" fill="var(--chart-1)" fillOpacity={0.4} isAnimationActive={false} />` (faint full bar behind) and `<Bar dataKey="completed" name="Completed" fill="var(--chart-2)" isAnimationActive={false} />` (solid foreground).

    Runtime error/omission (D-10, criterion 3): because `error={sprintsError}` is passed and ChartWrapper's precedence is `error > isLoading > isEmpty > children`, a failed closed-sprints fetch automatically renders ChartWrapper's `ErrorState` with the retry button — the section degrades independently and never blanks the Dashboard. No extra error handling needed beyond wiring `error`/`onRetry`.

    Use NO hardcoded hex colors anywhere (the WeeklyTrendChart per-bar semantic-hex exception does NOT apply here — there is no per-sprint target). Use `var(--chart-1)`/`var(--chart-2)` only.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i 'VelocityChart' | grep -c -i 'error' | grep -qx 0 && grep -q "'use no memo'" src/routes/dashboard/VelocityChart.tsx && grep -q 'responsive' src/routes/dashboard/VelocityChart.tsx && ! grep -q 'ResponsiveContainer' src/routes/dashboard/VelocityChart.tsx && ! grep -q 'getJiraLimit' src/routes/dashboard/VelocityChart.tsx && ! grep -qE '#[0-9a-fA-F]{6}' src/routes/dashboard/VelocityChart.tsx && grep -q 'var(--chart-1)' src/routes/dashboard/VelocityChart.tsx && grep -q 'var(--chart-2)' src/routes/dashboard/VelocityChart.tsx && grep -q 'isAnimationActive={false}' src/routes/dashboard/VelocityChart.tsx && grep -q 'staleTime: Infinity' src/routes/dashboard/VelocityChart.tsx && grep -q 'isEmpty={false}' src/routes/dashboard/VelocityChart.tsx && echo VELOCITY_CHART_OK</automated>
  </verify>
  <done>VelocityChart renders a grouped `BarChart` (committed `var(--chart-1)` 40% opacity, completed `var(--chart-2)` solid) via `responsive` prop inside `ChartContainer`/`ChartWrapper`; with `<3` qualifying sprints it shows the `EmptyState` "Not enough sprint data" message via children with `isEmpty={false}` (not the generic empty path, not an error); `sprintsError` wires to ChartWrapper's error/retry state; `isAnimationActive={false}` on both bars; no hex colors; no `ResponsiveContainer`; no `getJiraLimit`. Type-checks clean and all grep gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jira DC REST → component | Closed-sprint list and per-sprint issue arrays are external, already fetched by the 85-02 barrel functions; this component only reads/renders them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-03-V5 | Tampering/DoS | VelocityChart series derivation | mitigate | All data access is defensive: `closedSprints ?? []`, `sprintIssueQueries[i]?.data ?? []`, `velocitySeries` computed only when queries settle. The pure `computePersonalVelocitySeries` (85-01) applies `?? 0` SP fallbacks. Missing/empty series renders the `<3`-sprint EmptyState, never crashes. |
| T-85-03-ERR | Denial of Service | Runtime fetch failure | mitigate | `error={sprintsError}` + `onRetry={refetchSprints}` route to ChartWrapper's `ErrorState` (precedence error > loading > empty > children) — section degrades independently, Dashboard never blanks (D-10, criterion 3). |
| T-85-03-INFO | Information Disclosure | PAT token leakage | mitigate | `jiraToken` lives only in `queryFn` closures; never placed in any `queryKey` (T-84-02) — keeps the PAT out of TanStack Query devtools/cache keys. |

No high-severity unmitigated threats. This is a read-only render of already-fetched data.
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` reports no errors in VelocityChart.tsx.
- `cd taskflow && npm test` — existing 85-01 `computePersonalVelocitySeries` unit tests remain green (this plan consumes those pure functions; it adds no new tests since rendering behavior is covered by the consumed-and-tested derivation + ChartWrapper's existing states).
- Grep gates (Task 2 verify) confirm: `'use no memo'`, `responsive` (not `ResponsiveContainer`), no `getJiraLimit`, no hex colors, `var(--chart-1)`/`var(--chart-2)`, `isAnimationActive={false}`, `staleTime: Infinity`, `isEmpty={false}`.
- Behavior: with `<3` qualifying sprints the BarChart is not rendered and the "Not enough sprint data" EmptyState shows in its place (not an error, not the generic empty state).
</verification>

<success_criteria>
- `VelocityChart.tsx` exists, type-checks, and is a props-only default-export component.
- Closed-sprint and per-sprint issue queries both use `staleTime: Infinity`; token never appears in a queryKey.
- Per-sprint fan-out runs under a dedicated `pLimit(3)`, not the global `getJiraLimit()`.
- Grouped BarChart: committed `var(--chart-1)` @ 40% opacity (faint, behind), completed `var(--chart-2)` solid (front); `isAnimationActive={false}` on both; `responsive` prop; no hex.
- `<3` qualifying sprints → `EmptyState` "Not enough sprint data" via ChartWrapper children with `isEmpty={false}` (D-06 — NOT generic isEmpty, NOT an error).
- Runtime fetch error → ChartWrapper error/retry state; Dashboard never blanks (D-10).
- D-10 probe-outcome comment present.
- INSIGHT-01 velocity render path fully covered (wiring into `index.tsx` happens in 85-04).
</success_criteria>

<output>
Create `.planning/phases/85-sprint-insights-conditional-probe-gated/85-03-SUMMARY.md` when done.
</output>
