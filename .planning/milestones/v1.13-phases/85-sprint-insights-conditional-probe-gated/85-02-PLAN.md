---
phase: 85-sprint-insights-conditional-probe-gated
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/jira.ts
  - taskflow/src/lib/concurrency.ts
  - taskflow/src/services/jira/greenhopper/burndown.ts
  - taskflow/src/services/jira/greenhopper/types.ts
  - taskflow/src/services/jira/greenhopper/index.ts
autonomous: true
requirements: [INSIGHT-01, INSIGHT-02]

must_haves:
  truths:
    - "fetchClosedSprints returns the most-recent N closed sprints (tail of the ascending list), never the first page"
    - "fetchSprintIssuesBySprintId sources the SP field from the passed spKey, never a hardcoded customfield_10106"
    - "A dedicated pLimit(3) limiter exists for the velocity backfill, distinct from getJiraLimit()'s pLimit(6)"
    - "The burndown fetch URL is the full rapid-charts path with no doubled /xboard prefix"
    - "All three new functions are reachable from the services/jira barrel"
  artifacts:
    - path: "taskflow/src/services/jira.ts"
      provides: "fetchClosedSprints, fetchSprintIssuesBySprintId; re-export of fetchBurndown"
      contains: "fetchClosedSprints"
    - path: "taskflow/src/lib/concurrency.ts"
      provides: "getVelocityLimit() — dedicated pLimit(3) for velocity fan-out"
      contains: "getVelocityLimit"
    - path: "taskflow/src/services/jira/greenhopper/burndown.ts"
      provides: "fetchBurndown — scopechangeburndownchart fetcher via greenhopperFetch apiPath override"
      contains: "scopechangeburndownchart"
  key_links:
    - from: "taskflow/src/services/jira/greenhopper/burndown.ts"
      to: "greenhopperFetch (./client)"
      via: "apiPath='' rapid-charts path override"
      pattern: "greenhopperFetch\\("
    - from: "taskflow/src/services/jira.ts"
      to: "fetchBurndown"
      via: "barrel re-export from ./jira/greenhopper"
      pattern: "fetchBurndown"
---

<objective>
Add the Jira data-access layer for the two probe-gated Sprint Insights charts: closed-sprint pagination + per-sprint issue fetch (velocity, INSIGHT-01), a dedicated concurrency limiter for the velocity fan-out, and the GreenHopper scope-change burndown fetcher (INSIGHT-02). Pure network/service code — no React, no charting.

Purpose: The Wave 2 chart components (85-03 velocity, 85-04 burndown) consume these functions as props-driven data sources. Getting the closed-sprint ordering (Probe A landmine), the SP-field sourcing, the dedicated p-limit cap, and the GreenHopper base-path override correct here is what makes both charts trustworthy.

Output:
- `fetchClosedSprints(baseUrl, token, boardId, n)` and `fetchSprintIssuesBySprintId(baseUrl, token, sprintId, spKey)` in the `services/jira.ts` barrel.
- `getVelocityLimit()` — a dedicated `pLimit(3)` in `lib/concurrency.ts`, separate from the global `getJiraLimit()` `pLimit(6)`.
- `fetchBurndown(baseUrl, token, boardId, sprintId)` + `GreenHopperBurndown` type in a new `greenhopper/burndown.ts` domain module, re-exported through the greenhopper index and the `services/jira` barrel.
- Confirmation of the `.changes` entry-level shape and time-unit magnitude from one live read, with the consumer kept defensive either way.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts (READ FIRST)
@.planning/phases/85-sprint-insights-conditional-probe-gated/85-CONTEXT.md
@.planning/phases/85-sprint-insights-conditional-probe-gated/85-RESEARCH.md
@.planning/phases/85-sprint-insights-conditional-probe-gated/85-PATTERNS.md

# Modified file + analogs
@taskflow/src/services/jira.ts
@taskflow/src/services/jira/sprints.ts
@taskflow/src/services/jira/greenhopper/client.ts
@taskflow/src/services/jira/greenhopper/data.ts
@taskflow/src/services/jira/greenhopper/index.ts
@taskflow/src/lib/concurrency.ts

# Probe harness (read-only, re-runnable — used to confirm .changes entry shape)
@.planning/phases/85-sprint-insights-conditional-probe-gated/probe.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Closed-sprint tail paginator + per-sprint issue fetch + dedicated p-limit(3)</name>

  <read_first>
    - `taskflow/src/services/jira.ts` — file being modified. Analogs in-file: `fetchSprintsForBoard` (~line 2211; boardId-scoped agile fetch), `fetchSprintIssues` (~line 392; the `spFields = [...new Set(['customfield_10016','customfield_10028', spKey])].join(',')` dedup pattern + `assignee,issuetype,status` field selection), `fetchActiveSprint` (~line 1341; board discovery → `values[0]`, return-null-on-catch style). `apiFetch`, `JiraIssue`, `JiraActiveSprint` are already imported/declared in this file — no new imports.
    - `taskflow/src/services/jira/sprints.ts` `fetchSprintsForBoard` (lines 164–192) — the exact `base = baseUrl.replace(/\/$/, '')` + `headers = { Authorization: \`Bearer ${token}\`, 'Content-Type': 'application/json' }` + `apiFetch('jira', url, { headers }, '<op label>')` + `data?.values ?? []` shape to mirror.
    - `taskflow/src/lib/concurrency.ts` — file being modified. `getJiraLimit()` returns the global `pLimit(6)`; mirror its module-singleton pattern for a separate `pLimit(3)`. `import pLimit from 'p-limit'` already present.
  </read_first>

  <files>taskflow/src/services/jira.ts, taskflow/src/lib/concurrency.ts</files>

  <action>
In `taskflow/src/lib/concurrency.ts`, add a dedicated velocity limiter alongside the existing global one (criterion 1c / D-05): a module-level `const velocityLimit = pLimit(3)` and an exported `getVelocityLimit()` that returns it. Mirror the `getJiraLimit()` doc-comment style. This is a SEPARATE instance from `getJiraLimit()`'s `pLimit(6)` and must NOT be affected by `setJiraConcurrencyLimit`. Document inline that this tighter cap scopes the velocity backfill fan-out so it never monopolizes the Jira connection.

In `taskflow/src/services/jira.ts`, add two exported async functions near the other sprint helpers:

`fetchClosedSprints(baseUrl: string, token: string, boardId: number, n = 6): Promise<JiraActiveSprint[]>` — paginate the agile closed-sprint endpoint and return the most-recent N. Build `base`/`headers` exactly like `fetchSprintsForBoard`. Loop: starting `startAt = 0`, page size `PAGE = 50`, call `apiFetch('jira', \`${base}/rest/agile/1.0/board/${boardId}/sprint?state=closed&maxResults=${PAGE}&startAt=${startAt}\`, { headers }, 'Load Closed Sprints')`; on `!res.ok` return `[]`; read `data?.values ?? []`, push into an accumulator; break when `data?.isLast` is truthy OR `values.length < PAGE`; otherwise `startAt += PAGE`. After the loop, `return allSprints.slice(-n)`. Add the MANDATED ordering comment directly above the slice: the `state=closed` endpoint returns sprints ASCENDING (oldest first) — Probe A 2026-06-15 returned 2019 sprints (ids 44/102/106/107/227) while the active sprint is current; reading the first page silently charts 2019 data, so this function accumulates the full list and slices the LAST N. Do NOT add an `orderBy` param (the endpoint has none). `boardId` is a parameter — never hardcode 6708 (Spoofing mitigation; the caller passes the app-resolved board).

`fetchSprintIssuesBySprintId(baseUrl: string, token: string, sprintId: number, spKey: string): Promise<JiraIssue[]>` — fetch one sprint's issues for the velocity SP sums. Build `base`/`headers` as above. Source SP via the passed `spKey` with the same dedup pattern as `fetchSprintIssues`: `spFields = [...new Set(['customfield_10016', 'customfield_10028', spKey])].join(',')`. NEVER hardcode `customfield_10106` as the sole field (Tampering mitigation — `spKey` comes from `discoverCustomFields` upstream; the literal customfields are only the fallback set already used by `fetchSprintIssues`). Request fields `assignee,issuetype,status,${spFields}`; call `apiFetch('jira', \`${base}/rest/agile/1.0/sprint/${sprintId}/issue?fields=assignee,issuetype,status,${spFields}&maxResults=200\`, { headers }, 'Load Sprint Issues')`. On `!res.ok` return `[]`. Treat the response as external input (V5): `return (data?.issues ?? []) as JiraIssue[]`. Add an inline note that `maxResults=200` caps a single closed sprint's issues (closed sprints rarely exceed this); if a future instance needs more, add a pagination loop mirroring the closed-sprint paginator.

Do NOT add story-points summing, personal filtering, or subtask exclusion here — those are pure functions owned by 85-01 (`computePersonalVelocitySeries`). This task is fetch-only.
  </action>

  <verify>
    <automated>cd taskflow && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
    <automated>cd taskflow && grep -n "slice(-n)" src/services/jira.ts && grep -n "getVelocityLimit\|pLimit(3)" src/lib/concurrency.ts && grep -vn '^\s*//' src/services/jira.ts | grep -c "customfield_10106" </automated>
  </verify>

  <acceptance_criteria>
    - `fetchClosedSprints` returns `allSprints.slice(-n)` — the LAST n of the accumulated ascending list, NOT `.slice(0, n)` / the first page.
    - The closed-sprint loop increments `startAt` by 50 and terminates on `data?.isLast` or a short page (`values.length < PAGE`).
    - `fetchSprintIssuesBySprintId` builds its SP field list from the passed `spKey` (via the `new Set([...])` dedup), and the only occurrences of the literal `customfield_10106` in non-comment code are zero (the function uses `spKey`; `10016`/`10028` are the pre-existing fallback set, not `10106`).
    - `boardId` and `sprintId` are function parameters — no hardcoded `6708`, `163`, `19562`, or `227` literals in either function.
    - `lib/concurrency.ts` exports `getVelocityLimit()` returning a `pLimit(3)` instance that is a different object from `getJiraLimit()`'s `pLimit(6)` and is not mutated by `setJiraConcurrencyLimit`.
    - Both new functions return `[]` (not throw, not `null`) on `!res.ok`, and read via `data?.values ?? []` / `data?.issues ?? []` (V5 null-safe).
    - `tsc --noEmit` reports no new errors.
  </acceptance_criteria>

  <done>
`fetchClosedSprints` and `fetchSprintIssuesBySprintId` are exported from `services/jira.ts`, `getVelocityLimit()` is exported from `lib/concurrency.ts`, the ordering landmine comment is present above the tail slice, no SP key is hardcoded to `customfield_10106`, and `tsc --noEmit` passes clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: GreenHopper burndown fetcher (rapid-charts path override) + confirm .changes shape</name>

  <read_first>
    - `taskflow/src/services/jira/greenhopper/client.ts` (full) — `greenhopperFetch(baseUrl, token, path, operation, apiPath = GREENHOPPER_API_PATH, init?)`. URL = `baseUrl.replace(/\/$/,'') + apiPath + path` (line 42). `GREENHOPPER_API_PATH = '/rest/greenhopper/1.0/xboard'`. Passing `apiPath=''` yields `baseUrl + '' + path`. Uses `apiFetch('jira', …)` + Bearer PAT (Phase 71 D-04). `client.ts` is intentionally PRIVATE to `greenhopper/` (D-06) — import it only from a sibling module inside `greenhopper/`, never across the directory boundary.
    - `taskflow/src/services/jira/greenhopper/data.ts` (lines 1–60) — the domain-module template: `import { greenhopperFetch } from './client'`, `import { ApiError } from '../../../lib/api-error'`, try/catch that re-throws `ApiError` (auth → `setJiraConnected(false)`) and wraps network errors as `Cannot reach ${baseUrl}`, then `!response.ok` branch. Mirror this error envelope.
    - `taskflow/src/services/jira/greenhopper/index.ts` — barrel for the greenhopper submodule (`export * from './data'` etc.); add the new module here. `client.ts` stays unexported (D-06).
    - `taskflow/src/services/jira.ts` — the greenhopper re-export block (~lines 2497–2520, `export { … } from './jira/greenhopper'`); add `fetchBurndown` and the `GreenHopperBurndown` type to the existing re-export so app code imports from `services/jira`.
    - `taskflow/src/services/jira/greenhopper/types.ts` — where `GreenHopperBurndown` belongs (alongside the other `Gh*` response interfaces). Probe C confirmed top-level keys: `activatedTime, changes, endTime, issueToParentKeys, issueToSummary, now, openCloseChanges, startTime, statisticField, workRateData`; `statisticField: 'timeestimate'`; `changes` is `Record<string, unknown[]>` with ~496 entries.
    - `.planning/phases/85-sprint-insights-conditional-probe-gated/probe.sh` (Probe C, lines ~60–72) — read-only burndown probe; re-runnable to confirm the entry-level `.changes` shape.
  </read_first>

  <files>taskflow/src/services/jira/greenhopper/burndown.ts, taskflow/src/services/jira/greenhopper/types.ts, taskflow/src/services/jira/greenhopper/index.ts, taskflow/src/services/jira.ts</files>

  <action>
Create `taskflow/src/services/jira/greenhopper/burndown.ts` following the `data.ts` domain-module template. Export `fetchBurndown(baseUrl: string, token: string, boardId: number, sprintId: number): Promise<GreenHopperBurndown>`. Inside, call `greenhopperFetch(baseUrl, token, \`/rest/greenhopper/1.0/rapid/charts/scopechangeburndownchart?rapidViewId=${boardId}&sprintId=${sprintId}\`, 'Load Sprint Burndown', '')` — the FIFTH argument is `apiPath=''` (empty string), the explicit override (D-08). Add the MANDATED comment: the rapid-charts endpoint lives at a different root than `GREENHOPPER_API_PATH` (`/xboard`); passing any non-empty `apiPath` doubles the prefix to `…/xboard/rest/greenhopper/1.0/rapid/charts/…` → 404. `boardId` (== rapidViewId) and `sprintId` are parameters — never hardcode 6708/19562 (Spoofing mitigation; the caller passes the app-resolved board + active sprint). Reuse the `data.ts` error envelope: re-throw `ApiError` (auth bubbles to `setJiraConnected(false)` per D-04), wrap network errors as `Cannot reach ${baseUrl}`, and throw on `!response.ok` so TanStack Query surfaces it to the chart's `error` state (D-09/D-10 runtime degradation). Return `response.json()` typed as `GreenHopperBurndown`.

In `taskflow/src/services/jira/greenhopper/types.ts`, add an exported `GreenHopperBurndown` interface with the Probe-C-confirmed top-level fields: `startTime: number`, `endTime: number`, `now: number`, `activatedTime: number`, `statisticField: string` (value `'timeestimate'` on this DC — hours/seconds, not story points), `changes: Record<string, BurndownChangeEntry[]>`, and `workRateData?: { rates?: unknown[] } | unknown` (leave the guideline shape loose; the parser in 85-01 owns it). Define `BurndownChangeEntry` defensively (V5): all fields optional — `{ key?: string; statC?: { newValue?: number; oldValue?: number }; added?: boolean }` — because the entry-level shape is MEDIUM-confidence (RESEARCH A2). Add a comment that 85-01's `parseBurndownChanges` is the sole consumer and must stay null-safe (`?? 0`, `Math.max(0, …)`).

CONFIRM the unknowns before finalizing the entry type (RESEARCH open questions 1 & 2): perform ONE read-only live read of a `.changes` entry and inspect (a) the entry-level field names and (b) the value magnitude (≈28800 → seconds; ≈8 → hours). Either re-run `probe.sh` (read-only, re-runnable) or issue a single direct `curl -sS -H "Authorization: Bearer $JIRA_PAT" "$JIRA_BASE_URL/rest/greenhopper/1.0/rapid/charts/scopechangeburndownchart?rapidViewId=<app-resolved board>&sprintId=<active sprint>"` and `jq '.changes | to_entries[0]'` plus `jq '.statisticField'`. Record the observed entry field names and unit magnitude in the SUMMARY. If the observed field names differ from `statC.newValue`/`statC.oldValue`, update `BurndownChangeEntry` to match and note it for 85-01; if the live read is not possible (no creds in this environment), keep the defensive optional-field type as-is and flag in the SUMMARY that 85-01 must re-confirm before relying on exact field names. Do NOT make the parser brittle on a guess — the type stays all-optional regardless.

Wire exports: add `export * from './burndown'` to `greenhopper/index.ts` (keep `client.ts` unexported, D-06), and add `fetchBurndown` plus `type GreenHopperBurndown` to the existing `export { … } from './jira/greenhopper'` re-export block in `services/jira.ts` so consumers import both from `services/jira`.
  </action>

  <verify>
    <automated>cd taskflow && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
    <automated>cd taskflow && grep -n "scopechangeburndownchart" src/services/jira/greenhopper/burndown.ts && grep -c "rapid/charts/scopechangeburndownchart" src/services/jira/greenhopper/burndown.ts && grep -n "from './burndown'" src/services/jira/greenhopper/index.ts && grep -n "fetchBurndown" src/services/jira.ts</automated>
  </verify>

  <acceptance_criteria>
    - The burndown URL passed to `greenhopperFetch` is exactly `/rest/greenhopper/1.0/rapid/charts/scopechangeburndownchart?rapidViewId=${boardId}&sprintId=${sprintId}` AND the 5th argument is `''` (apiPath override) — so the resolved URL contains the rapid-charts path with NO `/xboard` segment and no doubled `/rest/greenhopper/1.0` prefix.
    - `greenhopperFetch` is imported from `./client` inside `burndown.ts` (a sibling within `greenhopper/`), preserving the D-06 privacy boundary; `client.ts` is not exported from `greenhopper/index.ts`.
    - `boardId` and `sprintId` are parameters — no hardcoded `6708` / `19562` literals in `burndown.ts`.
    - `GreenHopperBurndown` is exported and its `BurndownChangeEntry.statC` (and inner `newValue`/`oldValue`) fields are OPTIONAL (null-safe), matching the MEDIUM-confidence entry shape.
    - `fetchBurndown` and `GreenHopperBurndown` are reachable via `import { fetchBurndown, type GreenHopperBurndown } from '@/services/jira'`.
    - The SUMMARY records either the observed `.changes` entry field names + unit magnitude from a live read, OR an explicit flag that the live read was unavailable and 85-01 must re-confirm.
    - `tsc --noEmit` reports no new errors.
  </acceptance_criteria>

  <done>
`fetchBurndown` exists in `greenhopper/burndown.ts` using `greenhopperFetch` with `apiPath=''` and the full rapid-charts path, `GreenHopperBurndown` (with all-optional change-entry fields) is in `greenhopper/types.ts`, both are re-exported through the greenhopper index and the `services/jira` barrel, the `.changes` shape/unit is confirmed (or flagged) in the SUMMARY, and `tsc --noEmit` passes clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Jira DC (agile REST) | `boardId`/`sprintId` cross into URL construction; responses are untrusted external JSON |
| app → Jira DC (GreenHopper rapid-charts) | `rapidViewId`/`sprintId` cross into URL; `.changes` timeline is untrusted external JSON |
| caller → data layer | board/sprint/SP-field identifiers must originate from app resolution (`fetchActiveSprint` / `discoverCustomFields`), never literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01 | Spoofing | `fetchClosedSprints` / `fetchSprintIssuesBySprintId` / `fetchBurndown` boardId/sprintId/rapidViewId args | mitigate | boardId/sprintId/rapidViewId are required parameters supplied by the caller from the app-resolved board (`fetchActiveSprint` → `values[0].id`) and active sprint; no `6708`/`163`/`19562` literals anywhere in the three functions (acceptance criteria assert zero hardcoded IDs) |
| T-85-02 | Tampering | SP field key in `fetchSprintIssuesBySprintId` | mitigate | SP field sourced from the passed `spKey` (from `discoverCustomFields`) via the `new Set([...])` dedup; `customfield_10106` never hardcoded as the sole field (grep gate: zero non-comment occurrences) |
| T-85-03 | Tampering/Spoofing (Input Validation, V5) | agile + greenhopper response bodies | mitigate | Treat all responses as external input: `data?.values ?? []`, `data?.issues ?? []`, `response.json()` typed with all-optional change-entry fields; `!res.ok` → `[]` (sprints/issues) or throw (burndown, surfaced to chart error state). Numeric clamping (`Math.max(0,…)`, `?? 0`) is enforced downstream in 85-01's parser |
| T-85-04 | Information Disclosure | Bearer PAT in request | mitigate | PAT flows only through `apiFetch('jira', …)` Authorization header via `greenhopperFetch`/`apiFetch` (Phase 71 D-04); never logged, never placed in a queryKey (enforced in Wave 2 consumers) |
| T-85-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced (RESEARCH Package Legitimacy Audit: zero new packages); `p-limit` already installed and audited |

No high-severity unmitigated threats. The burndown `!res.ok` path throws by design (D-09/D-10) so a mid-session GreenHopper failure degrades the burndown section independently rather than corrupting shared state.
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` — clean, no new errors.
- `cd taskflow && grep -vn '^\s*//' src/services/jira.ts | grep -c customfield_10106` — returns `0` (SP key never hardcoded in non-comment code).
- `fetchClosedSprints` uses `slice(-n)` (tail), `startAt += PAGE` (50) pagination — verified by grep + read.
- `fetchBurndown` calls `greenhopperFetch(..., '')` with the rapid-charts path; resolved URL has no `/xboard` and no doubled `/rest/greenhopper/1.0` prefix.
- `getVelocityLimit()` exported from `lib/concurrency.ts`, distinct `pLimit(3)` instance.
- 85-01's mandated unit tests (in the parallel Wave 1 plan) exercise the tail-ordering/SP-sum semantics that consume these fetchers' outputs; this plan's functions are fetch-only and verified by tsc + grep gates.
</verification>

<success_criteria>
- `fetchClosedSprints`, `fetchSprintIssuesBySprintId`, and `fetchBurndown` (+ `GreenHopperBurndown` type) are all importable from `services/jira`.
- Closed-sprint fetch returns the most-recent N (tail), never the first page (Probe A landmine mitigated in code + comment).
- SP field key is parameter-sourced; no `customfield_10106` literal in non-comment code.
- Dedicated `pLimit(3)` velocity limiter exists, separate from the global `pLimit(6)`.
- Burndown URL uses the rapid-charts path with `apiPath=''`; no doubled `/xboard` prefix.
- `.changes` entry shape + unit magnitude confirmed from a live read (or flagged for 85-01 re-confirmation).
- No new high-severity threats; no new packages.
</success_criteria>

<output>
Create `.planning/phases/85-sprint-insights-conditional-probe-gated/85-02-SUMMARY.md` when done. Record: the confirmed `.changes` entry field names + time-unit magnitude (or the explicit "live read unavailable — 85-01 must re-confirm" flag), and any deviation of `BurndownChangeEntry` from the assumed `statC.{newValue,oldValue}` shape.
</output>
