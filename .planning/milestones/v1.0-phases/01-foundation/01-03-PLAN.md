---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/routes/onboarding/RoleStep.tsx
  - src/routes/onboarding/DoneStep.tsx
  - src/routes/settings/index.tsx
  - src/routes/settings/Settings.tsx
  - src/routes/settings/TokenSection.tsx
  - src/routes/settings/RoleSection.tsx
  - src/routes/settings/ThemeSection.tsx
  - src/components/app/Sidebar.tsx
  - src/components/app/ThemeToggle.tsx
  - src/main.tsx
  - src/routes/onboarding/RoleStep.test.tsx
  - src/routes/settings/Settings.test.tsx
  - src/services/theme.test.ts
autonomous: true
requirements:
  - ROLE-01
  - ROLE-02
  - AUTH-05
  - UI-01

must_haves:
  truths:
    - "User sees a role picker (Developer / Project Manager) on the Role wizard step and selecting one advances the wizard to Done"
    - "Selected role is persisted and survives app restart"
    - "User can change their role from the Settings page without re-running onboarding"
    - "Settings page shows Jira and GitLab tokens as masked (***...) with an eye-icon reveal toggle"
    - "Settings page has an 'Update token' button that triggers a new validation + Stronghold write"
    - "Dark/Light/System theme toggle is available in Settings and the preference persists across restarts"
    - "Theme applies immediately on toggle — no page reload required"
    - "Gear icon in sidebar/top bar navigates to /settings from anywhere in the app"
    - "Switching active Jira project in Settings clears all cached TanStack Query data and triggers a fresh reload"
  artifacts:
    - path: "src/routes/onboarding/RoleStep.tsx"
      provides: "Radio group with Developer and Project Manager options; writes to onboarding store and settings store on select"
    - path: "src/routes/settings/Settings.tsx"
      provides: "Settings page shell with sections for tokens, role, and theme"
    - path: "src/routes/settings/TokenSection.tsx"
      provides: "Masked token display with eye-toggle reveal and Update Token button"
    - path: "src/routes/settings/RoleSection.tsx"
      provides: "Role picker that reads/writes useSettingsStore().role"
    - path: "src/routes/settings/ThemeSection.tsx"
      provides: "Three-way toggle Dark/Light/System calling applyTheme() + saveTheme()"
    - path: "src/components/app/Sidebar.tsx"
      provides: "App sidebar with gear icon linking to /settings and role-aware nav placeholder"
  key_links:
    - from: "src/routes/settings/TokenSection.tsx"
      to: "src/services/stronghold.ts"
      via: "storeSecret() on Update Token; readSecret() to confirm current token exists"
      pattern: "storeSecret|removeSecret"
    - from: "src/routes/settings/TokenSection.tsx"
      to: "src/services/jira.ts or gitlab.ts"
      via: "validateJira/validateGitLab called on Update Token before storeSecret"
      pattern: "validateJira|validateGitLab"
    - from: "src/routes/settings/ThemeSection.tsx"
      to: "src/services/theme.ts"
      via: "applyTheme() + saveTheme() called on toggle"
      pattern: "applyTheme.*saveTheme|saveTheme.*applyTheme"
    - from: "src/main.tsx"
      to: "src/services/theme.ts"
      via: "loadTheme() called before first render to apply persisted theme"
      pattern: "loadTheme"
    - from: "src/routes/settings/TokenSection.tsx"
      to: "TanStack Query queryClient"
      via: "queryClient.clear() when active Jira project changes"
      pattern: "queryClient.clear|invalidateQueries"
---

<objective>
Complete Phase 1 by implementing the Role wizard step, the Done step, the full Settings page (tokens, role, theme), and the app sidebar with settings navigation. This plan closes the onboarding wizard and ensures all post-onboarding configuration is reachable from Settings without re-running onboarding.

Purpose: ROLE-01, ROLE-02, AUTH-05, and UI-01 all surface to the user in this plan. Onboarding is only first-run — settings is where returning users manage everything. Token masking and the Update Token flow are locked UX decisions from CONTEXT.md.
Output: A complete 5-step onboarding wizard and a full Settings page. Phase 1 is feature-complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 01 and 02 this plan builds on. -->

From src/services/theme.ts:
```typescript
export function applyTheme(theme: 'dark' | 'light' | 'system'): void;
export async function saveTheme(theme: 'dark' | 'light' | 'system'): Promise<void>;
export async function loadTheme(): Promise<'dark' | 'light' | 'system'>;
```

From src/services/stronghold.ts:
```typescript
export async function storeSecret(key: string, value: string): Promise<void>;
export async function readSecret(key: string): Promise<string>;
export async function removeSecret(key: string): Promise<void>;
```

From src/stores/settings.store.ts:
```typescript
interface SettingsState {
  role: 'developer' | 'pm' | null;
  theme: 'dark' | 'light' | 'system';
  setRole: (role: 'developer' | 'pm') => void;
  setTheme: (theme: 'dark' | 'light' | 'system') => void;
}
export declare const useSettingsStore: () => SettingsState;
```

From src/services/jira.ts:
```typescript
export async function validateJira(baseUrl: string, token: string): Promise<{ displayName: string; emailAddress: string }>;
export async function listJiraProjects(baseUrl: string, token: string): Promise<Array<{ id: string; key: string; name: string }>>;
```

From src/services/gitlab.ts:
```typescript
export async function validateGitLab(baseUrl: string, token: string): Promise<{ id: number; name: string; username: string }>;
export async function listGitLabGroups(baseUrl: string, token: string): Promise<Array<{ id: number; name: string; full_path: string }>>;
```

From src/stores/auth.store.ts:
```typescript
interface AuthState {
  jiraConnected: boolean;
  gitlabConnected: boolean;
  activeJiraProject: string | null;
  activeGitlabGroup: string | null;
  set: (partial: Partial<AuthState>) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Role step, Done step, and Settings page with token masking and role section</name>
  <files>
    src/routes/onboarding/RoleStep.tsx
    src/routes/onboarding/DoneStep.tsx
    src/routes/settings/Settings.tsx
    src/routes/settings/TokenSection.tsx
    src/routes/settings/RoleSection.tsx
    src/routes/settings/index.tsx
    src/routes/onboarding/RoleStep.test.tsx
    src/routes/settings/Settings.test.tsx
  </files>
  <behavior>
    RoleStep:
    - Renders two options: "Developer" and "Project Manager" using shadcn/ui RadioGroup
    - Selecting an option immediately writes to both useOnboardingStore().set({ role }) and useSettingsStore().setRole()
    - 'Continue' button is disabled until a role is selected
    - Back button is present and calls goBack()
    - Current role selection is restored from useOnboardingStore().role on back navigation

    DoneStep:
    - Simple confirmation screen: "You're all set!" with a 'Go to Dashboard' button
    - Button navigates to /dashboard via React Router useNavigate()
    - On render, marks onboarding complete in Tauri Store (set 'onboarding-complete' = true in settings store)

    Settings TokenSection:
    - Shows Jira URL (editable input) and token field (masked: value="••••••••" type="password")
    - Eye icon button next to token field: clicking reveals/hides the actual token (reads it from Stronghold on first reveal, NOT from Zustand)
    - 'Update Token' button: triggers the same validateJira flow as onboarding (spinner, error messages), then calls storeSecret to overwrite the key
    - Same error message strings as onboarding (locked decisions)
    - GitLab token section follows the same pattern
    - Switching active Jira project: renders a project Select (same as onboarding dropdown); on change, calls queryClient.clear() to invalidate all cached data

    Settings RoleSection:
    - Renders the same RadioGroup as RoleStep but reads/writes useSettingsStore().role directly
    - No wizard navigation — it's just a toggle that saves immediately

    Settings.test.tsx tests:
    - Token field initially shows masked value (not the real token string)
    - Eye icon click triggers readSecret and reveals token
    - Update Token button disabled while validation is in progress
    - Role radio group change updates settings store
  </behavior>
  <action>
    Create `src/routes/onboarding/RoleStep.tsx`:
    Use shadcn/ui `RadioGroup` and `RadioGroupItem`. Two options: value="developer" label="Developer" and value="pm" label="Project Manager". On value change: call `useOnboardingStore().set({ role: value })` AND `useSettingsStore().setRole(value)` — role must be in the settings store (persisted) not only in the transient onboarding store. 'Continue' button disabled when `!useOnboardingStore().role`. On continue: call `goNext()`.

    Create `src/routes/onboarding/DoneStep.tsx`:
    Simple layout. On mount, call `useSettingsStore().setOnboardingComplete(true)` (add this boolean field to settings store if not present). 'Go to Dashboard' button calls `useNavigate()('/dashboard')`.

    Create `src/routes/settings/Settings.tsx`:
    Tabbed or sectioned layout with sections: Credentials (TokenSection), Role (RoleSection), Appearance (ThemeSection). Use shadcn/ui `Tabs` or simple section dividers — Claude's discretion on exact layout. Import and render each section component.

    Create `src/routes/settings/TokenSection.tsx`:
    - State: `revealed: boolean` (local state, not store — security consideration)
    - Jira block: URL input bound to `useAuthStore().jiraBaseUrl` (add this field to auth store if not set), masked token field, eye icon button (`EyeIcon` / `EyeOffIcon` from lucide-react), 'Update Token' button
    - On eye click: if not revealed, call `readSecret('jira-pat')` and store result in local `revealedToken` state; set `revealed = true`. If already revealed: clear `revealedToken`, set `revealed = false`. Token is NEVER stored in Zustand — local component state only for display.
    - 'Update Token' flow: use `useMutation` calling `validateJira(baseUrl, newToken)`, on success call `storeSecret('jira-pat', newToken)`, update `jiraConnected = true` in auth store. Error display same exact strings as onboarding.
    - Project selection: shadcn/ui Select listing projects (use `useQuery` to fetch when token is available); on project change: `useAuthStore().set({ activeJiraProject: id })` then `queryClient.clear()`.
    - GitLab block: identical pattern with 'gitlab-pat' key and `validateGitLab`.

    Create `src/routes/settings/RoleSection.tsx`:
    Reads `useSettingsStore().role`, renders same RadioGroup as RoleStep, calls `useSettingsStore().setRole()` on change. No save button — immediate.

    Update `src/routes/settings/index.tsx` to render `<Settings />`.

    Fill in `src/routes/onboarding/RoleStep.test.tsx`:
    - Render RoleStep; assert Continue button is disabled initially
    - Select "developer" radio; assert Continue button is enabled
    - Click Continue; assert goNext was called
    - Assert settings store role is updated to 'developer'

    Fill in `src/routes/settings/Settings.test.tsx`:
    - Token field shows masked value on render (does not call readSecret on load)
    - Eye icon click calls readSecret and shows the returned value
    - Update Token button shows spinner while mutation is in progress
    - Role radio change calls setRole
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/onboarding/RoleStep.test.tsx src/routes/settings/Settings.test.tsx</automated>
  </verify>
  <done>
    RoleStep and Settings tests pass. Token field never shows plaintext on initial render. Eye toggle works via Stronghold read (mocked in tests). Role selection persists to settings store.
  </done>
</task>

<task type="auto">
  <name>Task 2: Theme section in Settings, Sidebar with gear icon, and app shell wiring</name>
  <files>
    src/routes/settings/ThemeSection.tsx
    src/components/app/Sidebar.tsx
    src/components/app/ThemeToggle.tsx
    src/main.tsx
  </files>
  <action>
    Create `src/routes/settings/ThemeSection.tsx`:
    Renders three buttons or a segmented control: "Light", "Dark", "System". Active option is highlighted. On click: call `applyTheme(selected)` immediately (instant DOM update) then `await saveTheme(selected)` (persist to Tauri Store). Both calls happen on every click — apply first for instant feedback, save second for persistence. Use `useSettingsStore().theme` as controlled value and `setTheme()` to update the store.

    Create `src/components/app/ThemeToggle.tsx`:
    Compact theme toggle (icon button cycling through light/dark/system) for placement in the sidebar or header. Same logic as ThemeSection but minimal UI — a single `Sun`/`Moon`/`Monitor` icon from lucide-react that cycles on click.

    Create `src/components/app/Sidebar.tsx`:
    Vertical sidebar (or top bar — Claude's discretion on orientation). Contains:
    - App name or logo at top
    - Nav placeholder items (will be filled in Phase 2): "Dashboard" link to /dashboard
    - Bottom: `ThemeToggle` component and a `Link` to /settings wrapping a `Settings` (gear) icon from lucide-react
    Role-aware placeholder: show current role label but no conditional nav yet (Phase 2 implements role-based routing).

    Update `src/main.tsx`:
    - Call `loadTheme().then(applyTheme)` BEFORE `createRoot(...).render(...)` to prevent flash of wrong theme on startup
    - Wrap app in a layout component that renders `<Sidebar />` beside the main content area when the user is past onboarding (check auth store for `activeJiraProject !== null` as a proxy)
    - Route `/` should redirect to `/dashboard` when onboarding is complete (check `useSettingsStore().onboardingComplete`)

    Run `npx vitest run` to confirm the full suite is still green after wiring changes.

    Also initialize shadcn/ui components used across all plans if not already done:
    ```bash
    cd /Users/mimo/Desktop/Tasker/taskflow
    npx shadcn@latest init
    npx shadcn@latest add button input label select radio-group tabs alert
    ```
    Run this before creating component files so shadcn primitives are available.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run</automated>
  </verify>
  <done>
    Full `npx vitest run` exits 0. App launches; sidebar is visible with gear icon navigating to /settings. Theme toggle in sidebar changes theme immediately. Theme preference survives app restart (verify by toggling to dark, restarting Tauri, confirming dark class is applied on startup).
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` exits 0 — entire suite green
2. Run `npm run tauri dev`: complete 5-step wizard works (Welcome → Jira → GitLab → Role → Done)
3. After wizard: navigates to /dashboard with sidebar visible
4. Gear icon in sidebar navigates to /settings
5. Settings: token fields show masked values; eye icon reveals token text (mocked in dev)
6. Settings: Update Token flow shows spinner and error messages matching onboarding
7. Settings: role change updates immediately with no save button
8. Settings: theme toggle Dark → app goes dark immediately; restart Tauri → opens in dark mode
9. `grep -r "jiraToken\|jiraUrl\|gitlabToken" src/stores/auth.store.ts` returns empty (no token fields in auth store)
10. `grep -r "loadTheme" src/main.tsx` matches (theme applied before render)
</verification>

<success_criteria>
- 5-step onboarding wizard complete: Welcome, Jira (with inline project select), GitLab (with inline group select), Role picker, Done
- Role selection persists across restarts (settings store backed by Tauri Store)
- Settings page provides all credential management without re-running onboarding
- Tokens are always masked in Settings; reveal requires explicit eye-toggle + Stronghold read
- Update Token flow validates before writing to Stronghold; same error strings as onboarding
- Theme toggle applies immediately and persists across restarts
- Gear icon always accessible from sidebar (one click from anywhere)
- `npx vitest run` exits 0 — all Phase 1 tests green
- All 9 Phase 1 requirements (AUTH-01 through AUTH-06, ROLE-01, ROLE-02, UI-01) verifiably implemented
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` documenting:
- Final shadcn/ui components added
- Settings page layout decision (tabs vs sections — note it was Claude's discretion)
- Sidebar layout decision (vertical vs horizontal)
- Whether ThemeToggle was placed in sidebar or top bar
- Tauri Store key names used for settings persistence
- `npx vitest run` final output
- Phase 1 complete: confirm all AUTH-01..06, ROLE-01, ROLE-02, UI-01 are implemented
</output>
