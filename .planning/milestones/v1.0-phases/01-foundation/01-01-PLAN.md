---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/lib.rs
  - src-tauri/Cargo.toml
  - src-tauri/capabilities/default.json
  - src-tauri/tauri.conf.json
  - src/main.tsx
  - src/App.tsx
  - src/routes/onboarding/index.tsx
  - src/routes/dashboard/index.tsx
  - src/routes/settings/index.tsx
  - src/services/tauri.ts
  - src/services/stronghold.ts
  - src/services/theme.ts
  - src/stores/onboarding.store.ts
  - src/stores/settings.store.ts
  - src/stores/auth.store.ts
  - src/lib/utils.ts
  - src/test/setup.ts
  - vitest.config.ts
  - tailwind.config.js
  - src/services/jira.test.ts
  - src/services/gitlab.test.ts
  - src/services/stronghold.test.ts
  - src/routes/onboarding/JiraStep.test.tsx
  - src/routes/onboarding/RoleStep.test.tsx
  - src/routes/settings/Settings.test.tsx
  - src/services/theme.test.ts
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Tauri 2 desktop app launches in dev mode without errors"
    - "Dark class toggles on <html> when theme changes and persists across reloads"
    - "Hash-based routing navigates between /, /dashboard, and /settings without 404"
    - "All test scaffold files exist and `npx vitest run` completes (no test failures — tests start failing RED in plan 02)"
  artifacts:
    - path: "src/services/tauri.ts"
      provides: "Tauri abstraction layer wrapping invoke()"
      exports: ["tauriService"]
    - path: "src/services/stronghold.ts"
      provides: "Stronghold PAT read/write/remove with save()"
      exports: ["storeSecret", "readSecret", "removeSecret"]
    - path: "src/services/theme.ts"
      provides: "applyTheme function + Tauri Store persistence"
      exports: ["applyTheme", "saveTheme", "loadTheme"]
    - path: "src/stores/onboarding.store.ts"
      provides: "Wizard step state preserved on back navigation"
      exports: ["useOnboardingStore"]
    - path: "src/stores/settings.store.ts"
      provides: "Role + theme Zustand store persisted via Tauri Store"
      exports: ["useSettingsStore"]
    - path: "src/stores/auth.store.ts"
      provides: "Auth status (boolean) + project/group selection — never stores token values"
      exports: ["useAuthStore"]
    - path: "vitest.config.ts"
      provides: "Test runner config (jsdom, globals, setup file)"
    - path: "src/test/setup.ts"
      provides: "jest-dom matchers + window.crypto shim for Vitest"
    - path: "src-tauri/capabilities/default.json"
      provides: "Permission grants for stronghold, store, http plugins"
      contains: "stronghold:default, store:default, http:default"
  key_links:
    - from: "src/main.tsx"
      to: "createHashRouter routes"
      via: "RouterProvider"
      pattern: "createHashRouter"
    - from: "src/main.tsx"
      to: "theme applied before first render"
      via: "loadTheme() call before ReactDOM.render"
      pattern: "loadTheme"
    - from: "src/services/stronghold.ts"
      to: "src-tauri/src/lib.rs"
      via: "Tauri Stronghold plugin registered in Rust"
      pattern: "tauri_plugin_stronghold"
---

<objective>
Scaffold the complete Tauri 2 + React 18 + TypeScript project with all dependencies installed, plugin registrations, routing shell, service abstraction layers, Zustand stores, and test infrastructure in place.

Purpose: Every subsequent plan builds on this foundation. The scaffold must be correct from the start — wrong routing strategy (BrowserRouter vs HashRouter), missing capability grants, or Stronghold not registered in Rust will cause cascading failures.
Output: A runnable Tauri desktop app with placeholder routes, all stores defined, all service interfaces implemented, all test scaffold files in place, and `npx vitest run` green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Tauri 2 project and install all dependencies</name>
  <files>
    src-tauri/src/lib.rs
    src-tauri/Cargo.toml
    src-tauri/capabilities/default.json
    src-tauri/tauri.conf.json
    tailwind.config.js
    vitest.config.ts
    src/test/setup.ts
    src/lib/utils.ts
    src/main.tsx
  </files>
  <action>
    Run scaffold and install all dependencies:

    ```bash
    cd /Users/mimo/Desktop/Tasker
    npm create tauri-app@latest taskflow -- --template react-ts --yes
    cd taskflow
    npm run tauri add stronghold
    npm run tauri add store
    npm run tauri add http
    npm install react-router-dom zustand @tanstack/react-query
    npm install class-variance-authority clsx tailwind-merge lucide-react
    npm install -D shadcn vitest @testing-library/react @testing-library/jest-dom jsdom @vitejs/plugin-react
    ```

    Configure `tailwind.config.js` with `darkMode: 'class'` (NOT 'media') and content paths covering `./src/**/*.{ts,tsx}`.

    Configure `vitest.config.ts`:
    - environment: 'jsdom'
    - globals: true
    - setupFiles: ['./src/test/setup.ts']
    - plugins: [react()]

    Create `src/test/setup.ts`:
    - Import `@testing-library/jest-dom`
    - Add window.crypto shim: `Object.defineProperty(window, 'crypto', { value: { getRandomValues: (buf) => randomFillSync(buf) } })` using Node `crypto.randomFillSync`

    Create `src/lib/utils.ts` with the `cn()` helper using `clsx` + `tailwind-merge`.

    In `src-tauri/src/lib.rs`, register all three plugins in the Tauri builder:
    ```rust
    use tauri_plugin_stronghold::Builder as StrongholdBuilder;
    tauri::Builder::default()
        .plugin(StrongholdBuilder::with_argon2(&salt_path).build())
        .plugin(tauri_plugin_store::Builder::default().build())
        .plugin(tauri_plugin_http::init())
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
    ```
    The `salt_path` must point to a writable app data directory path (use `tauri::path::app_data_dir` from the app handle). This is Rust-side salt for argon2 — derive it from the app data directory on startup.

    Add to `Cargo.toml` under `[profile.dev.package.scrypt]`: `opt-level = 3` (prevents multi-minute dev builds).

    In `src-tauri/capabilities/default.json`, ensure the permissions array includes:
    `"stronghold:default"`, `"store:default"`, `"http:default"`

    Update `tauri.conf.json` app identifier to `"com.taskflow.app"` and productName to `"Taskflow"`.

    Update `src/main.tsx` to use `createHashRouter` (NEVER createBrowserRouter — breaks in Tauri production builds). Wire `QueryClientProvider` wrapping `RouterProvider`. Call `loadTheme()` before rendering to avoid flash of wrong theme.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm run tauri dev` starts without Rust compile errors. `npx vitest run` exits 0 (test files don't exist yet so this just confirms the config is valid). All plugins appear in Cargo.toml dependencies.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Service layer — Tauri abstraction, Stronghold helpers, theme service; Zustand stores; test scaffolds</name>
  <files>
    src/services/tauri.ts
    src/services/stronghold.ts
    src/services/theme.ts
    src/stores/onboarding.store.ts
    src/stores/settings.store.ts
    src/stores/auth.store.ts
    src/routes/onboarding/index.tsx
    src/routes/dashboard/index.tsx
    src/routes/settings/index.tsx
    src/services/stronghold.test.ts
    src/services/theme.test.ts
    src/services/jira.test.ts
    src/services/gitlab.test.ts
    src/routes/onboarding/JiraStep.test.tsx
    src/routes/onboarding/RoleStep.test.tsx
    src/routes/settings/Settings.test.tsx
  </files>
  <behavior>
    - storeSecret('jira-pat', 'abc') + readSecret('jira-pat') round-trips to 'abc' (mocked IPC)
    - removeSecret('jira-pat') causes readSecret to throw or return empty (mocked IPC)
    - applyTheme('dark') adds 'dark' class to document.documentElement
    - applyTheme('light') removes 'dark' class from document.documentElement
    - applyTheme('system') adds 'dark' class when matchMedia prefers-color-scheme: dark is true
    - useOnboardingStore: goNext() increments step; goBack() decrements step but never below 0; field values are preserved after step changes (no clearing)
    - useSettingsStore: initial role is null; setRole('developer') sets role to 'developer'
    - useAuthStore: initial jiraConnected is false; never exposes token string fields
    - Jira test scaffold file exists (tests are placeholder/failing RED — will be fleshed out in Plan 02)
    - GitLab test scaffold file exists (placeholder RED)
    - JiraStep.test.tsx scaffold exists (placeholder RED)
    - RoleStep.test.tsx scaffold exists (placeholder RED)
    - Settings.test.tsx scaffold exists (placeholder RED)
  </behavior>
  <action>
    Create `src/services/tauri.ts`:
    Exports `tauriService` object with `invoke<T>(cmd, args?)` that delegates to `@tauri-apps/api/core`'s `invoke`. This is the ONLY place in the codebase that imports from `@tauri-apps/api/core` directly — all other code calls `tauriService.invoke`.

    Create `src/services/stronghold.ts`:
    Implement `storeSecret(key, value)`, `readSecret(key)`, `removeSecret(key)` using `@tauri-apps/plugin-stronghold`. Follow the pattern from RESEARCH.md exactly — module-level `_stronghold` and `_store` singletons, `getStore()` lazy initializer, always call `_stronghold.save()` after writes. Vault password strategy: generate a random 32-byte hex key on first launch and store it in Tauri Store under `vault-password` key. Read it back on subsequent launches. Document in a comment that this is appropriate for dev tooling (internal API tokens) but not financial credentials — migration path to keyring is replacing this file only.

    Create `src/services/theme.ts`:
    Implements `applyTheme(theme: 'dark' | 'light' | 'system')` toggling `document.documentElement.classList`. Implements `saveTheme(theme)` and `loadTheme()` using `LazyStore` from `@tauri-apps/plugin-store` (store name: `settings.json`). On 'system', check `window.matchMedia('(prefers-color-scheme: dark)').matches`.

    Create `src/stores/onboarding.store.ts`:
    Implements `useOnboardingStore` with Zustand. Fields: `step` (0-4), `jiraUrl`, `jiraToken`, `jiraProject: string | null`, `gitlabUrl`, `gitlabToken`, `gitlabGroup: string | null`, `role: 'developer' | 'pm' | null`, `jiraValidated: boolean`, `gitlabValidated: boolean`. Actions: `set(partial)`, `goNext()`, `goBack()` (floor at 0). IMPORTANT: No `reset()` on step change — all field values survive navigation.

    Create `src/stores/auth.store.ts`:
    Fields: `jiraConnected: boolean`, `gitlabConnected: boolean`, `activeJiraProject: string | null`, `activeGitlabGroup: string | null`. NEVER stores token strings — only boolean auth status and selected project/group identifiers.

    Create `src/stores/settings.store.ts`:
    Fields: `role: 'developer' | 'pm' | null`, `theme: 'dark' | 'light' | 'system'`. Persisted via Zustand's `persist` middleware backed by a custom storage adapter that reads/writes `LazyStore` from Tauri Store plugin.

    Create placeholder route components:
    - `src/routes/onboarding/index.tsx` — renders `<div>Onboarding</div>` (placeholder)
    - `src/routes/dashboard/index.tsx` — renders `<div>Dashboard</div>` (placeholder)
    - `src/routes/settings/index.tsx` — renders `<div>Settings</div>` (placeholder)

    Create test scaffolds — write failing placeholder tests for Plan 02 to implement:
    - `src/services/stronghold.test.ts` — full tests for storeSecret/readSecret/removeSecret using `mockIPC` from `@tauri-apps/api/mocks`
    - `src/services/theme.test.ts` — full tests for applyTheme behavior (these should pass now since applyTheme is pure DOM)
    - `src/services/jira.test.ts` — placeholder `it.todo()` stubs for AUTH-01 and AUTH-06 behaviors
    - `src/services/gitlab.test.ts` — placeholder `it.todo()` stubs for AUTH-02 behaviors
    - `src/routes/onboarding/JiraStep.test.tsx` — placeholder `it.todo()` stubs for AUTH-04, AUTH-06
    - `src/routes/onboarding/RoleStep.test.tsx` — placeholder `it.todo()` stubs for ROLE-01
    - `src/routes/settings/Settings.test.tsx` — placeholder `it.todo()` stubs for AUTH-05, ROLE-02
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run</automated>
  </verify>
  <done>
    `npx vitest run` exits 0. Stronghold and theme service tests pass. Placeholder todo tests are listed but don't count as failures. All service and store files exist with correct exports. No test file imports directly from `@tauri-apps/api/core` — all go through `tauriService`.
  </done>
</task>

</tasks>

<verification>
1. `npm run tauri dev` compiles and opens the Tauri window without errors
2. `npx vitest run` exits 0 with stronghold and theme tests passing
3. `cat src-tauri/capabilities/default.json` contains `stronghold:default`, `store:default`, `http:default`
4. `grep -r "createBrowserRouter" src/` returns empty (would break in production)
5. `grep -r "Authorization" src/stores/` returns empty (tokens never in Zustand)
6. `grep -r "import.*@tauri-apps/api/core" src/` returns only `src/services/tauri.ts`
</verification>

<success_criteria>
- Tauri 2 desktop app launches with placeholder routes for /, /dashboard, /settings
- Hash-based routing works (no 404 on navigation)
- All three Tauri plugins registered in Rust and granted in capabilities
- `npx vitest run` exits 0
- Dark class toggles correctly on applyTheme calls (theme service tests pass)
- Stronghold service tests pass with mocked IPC
- No PAT strings stored in Zustand state (auth store has only boolean + selection fields)
- All Wave 0 test scaffold files exist in correct paths
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Exact scaffold command used and final directory structure
- Any deviations from planned file paths
- Plugin versions installed (from package.json + Cargo.lock)
- Whether `npm run tauri dev` launched successfully
- Vitest run output (pass/fail counts)
- Any open questions surfaced during execution
</output>
