---
phase: 02-developer-dashboard
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/postcss.config.js
  - taskflow/tailwind.config.js
  - taskflow/tailwind.config.js.bak
autonomous: true
requirements:
  - UI-02
gap_closure: true

must_haves:
  truths:
    - "The app renders with full Tailwind CSS styling — tabs, badges, layout, and color tokens are all visually applied"
    - "The Vite dev server and production build produce a styled output without PostCSS overriding @tailwindcss/vite"
  artifacts:
    - path: "taskflow/vite.config.ts"
      provides: "@tailwindcss/vite plugin (already present) — sole CSS pipeline entry point after config files are removed"
  key_links:
    - from: "taskflow/vite.config.ts"
      to: "Tailwind v4 CSS output"
      via: "@tailwindcss/vite plugin — active only when postcss.config.js is absent"
      pattern: "@tailwindcss/vite"
---

<objective>
Delete the three dead config files that are stripping all Tailwind utilities from the build output.

Purpose: The root cause of the "app is missing styles" UAT failure is that postcss.config.js with an empty plugins:{} object is present on disk. Vite detects it, activates the PostCSS pipeline, and that pipeline overwrites @tailwindcss/vite's output with an unstyled CSS bundle. Removing this file (and the two dead Tailwind v3 config files) lets @tailwindcss/vite operate as the sole CSS pipeline, restoring all styles.
Output: Three files deleted. App renders with full Tailwind styling on next build/dev start.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-developer-dashboard/02-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead PostCSS and Tailwind v3 config files</name>
  <files>
    taskflow/postcss.config.js,
    taskflow/tailwind.config.js,
    taskflow/tailwind.config.js.bak
  </files>
  <action>
    Delete all three files from the taskflow directory:

    ```
    rm /Users/mimo/Desktop/Tasker/taskflow/postcss.config.js
    rm /Users/mimo/Desktop/Tasker/taskflow/tailwind.config.js
    rm /Users/mimo/Desktop/Tasker/taskflow/tailwind.config.js.bak
    ```

    Root cause: postcss.config.js contains `export default { plugins: {} }`. Its mere presence on disk causes Vite to activate the PostCSS pipeline. That pipeline runs with empty plugins, producing unstyled CSS that overwrites the output of the @tailwindcss/vite plugin (which is registered in vite.config.ts and handles CSS injection when PostCSS is absent). Tailwind v4 does not use tailwind.config.js (it reads from CSS @theme directives) so both tailwind.config.js and tailwind.config.js.bak are dead files.

    After deletion, verify vite.config.ts still contains the @tailwindcss/vite plugin entry — do not modify vite.config.ts unless it is missing the plugin (it should already have it from Phase 01).

    Do NOT create any new config files. The correct Tailwind v4 setup is: @tailwindcss/vite in vite.config.ts, @import "tailwindcss" in the root CSS file, no postcss.config.js.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && ls postcss.config.js tailwind.config.js tailwind.config.js.bak 2>&1 | grep -c "No such file" | grep -q 3 && echo "PASS: all three files deleted" || echo "FAIL: some files still exist"</automated>
  </verify>
  <done>postcss.config.js, tailwind.config.js, and tailwind.config.js.bak are absent from taskflow/. Running `npm run dev` serves the app with Tailwind CSS styles applied.</done>
</task>

</tasks>

<verification>
Confirm files are gone and build still compiles:
```
ls /Users/mimo/Desktop/Tasker/taskflow/postcss.config.js 2>&1   # should say: No such file
cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit      # should exit 0
```
</verification>

<success_criteria>
- postcss.config.js does not exist in taskflow/
- tailwind.config.js does not exist in taskflow/
- tailwind.config.js.bak does not exist in taskflow/
- npx tsc --noEmit exits 0
- App starts with `npm run dev` and CSS/Tailwind utilities are applied
</success_criteria>

<output>
After completion, create `.planning/phases/02-developer-dashboard/02-05-SUMMARY.md`
</output>
