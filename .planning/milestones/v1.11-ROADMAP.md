# Milestone v1.11: GreenHopper API Migration

**Status:** ✅ SHIPPED 2026-06-01
**Phases:** 71–75
**Total Plans:** 22

## Overview

Eliminated Jira API n+1 bottlenecks by migrating the app's Jira backbone to the on-prem GreenHopper API (`/rest/greenhopper/1.0/xboard/*`), which returns board/backlog/issue data as a single fat payload with embedded entity lookup maps. Hard cutover per surface — each phase replaced its REST path in place; the old REST board/backlog/transitions paths were deleted as their replacements shipped.

## Phases

### Phase 71: GreenHopper Adapter Foundation

**Goal:** Build the typed GreenHopper API client and adapter layer that every later phase consumes. No UI changes.

**Requirements:** GH-ADAPT-01, GH-ADAPT-02, GH-ADAPT-03

**Plans:** 6/6 plans complete

- [x] 71-01-PLAN.md — Wave 0: capture script + redacted real fixtures (blocks all)
- [x] 71-02-PLAN.md — Wave 1: greenhopperFetch client + types.ts
- [x] 71-03-PLAN.md — Wave 2: four typed fetchers (allData/data/details/transitions) + tests
- [x] 71-04-PLAN.md — Wave 2: buildEntityMaps + resolvers + warnOnce + tests
- [x] 71-05-PLAN.md — Wave 3: adaptIssue + createAdapter + fixture-driven tests
- [x] 71-06-PLAN.md — Wave 4: public barrel + jira.ts re-export (D-05) + full suite verify

**Details:**

Built `scripts/capture-greenhopper.mjs` (one-shot Node CLI that pulls 4 endpoints and redacts PII), committed 4 redacted JSON fixtures, implemented `greenhopperFetch` wrapper over `apiFetch('jira', ...)`, defined 12 GreenHopper response interfaces + `EntityMaps`, built 4 typed fetchers (`getGhAllData`, `getGhBacklogData`, `getGhDetails`, `getGhTransitions`), `buildEntityMaps` + `warnOnce` resolver helpers, `adaptIssue` + `createAdapter`, and wired everything into the project's 60-import `services/jira.ts` barrel.

**Completed:** 2026-05-28

---

### Phase 72: Workflow Transitions via GreenHopper

**Goal:** Replace per-issue REST `/transitions` calls with a cached per-project `transitions.json` map; wire it into sprint-board drag-to-transition and issue-detail status change.

**Requirements:** GH-TRANS-01, GH-TRANS-02, GH-TRANS-03

**Plans:** 3/3 plans complete

- [x] 72-01-PLAN.md — Wave 1: warnOnce extract + statuses.ts + transitions cache (useGhTransitions/getGhTransitions/invalidateGhTransitions) + jira.ts re-exports + tests
- [x] 72-02-PLAN.md — Wave 2: swap 4 call sites (StatusPopover, SprintBoardTab, BulkActionBar, QuickCreateInput) + sprint-board toolbar 'Reload workflow transitions' action
- [x] 72-03-PLAN.md — Wave 3: hard cutover — delete legacy fetchTransitions GET from services/jira.ts and services/jira/transitions.ts + tests; full suite green

**Details:**

Shipped `useGhTransitions` / `getGhTransitions` / `invalidateGhTransitions` hooks, `fetchAllJiraStatuses` helper, and the transitions cache keyed by `projectId × issueTypeId → workflow → transitions[]`. Swapped 4 call sites. Hard-deleted the legacy per-issue `fetchTransitions` GET from both `services/jira.ts` and `services/jira/transitions.ts` — zero references to the legacy fetcher name across `src/` confirmed via grep gate.

**Completed:** 2026-05-29

---

### Phase 73: Sprint Board on `allData.json`

**Goal:** Replace the multi-call sprint-board fetch with a single `allData.json` call; render columns from `columnsData` and surface `timeInColumn`.

**Requirements:** GH-BOARD-01, GH-BOARD-02, GH-BOARD-03, GH-BOARD-04

**Plans:** 3/3 plans complete

- [x] 73-01-PLAN.md — Wave 1: useGhAllData hook + getGhAllData + invalidateGhAllData + formatTimeAgo helpers + jira.ts re-exports + tests
- [x] 73-02-PLAN.md — Wave 2: SprintBoardTab data-layer rewrite onto useGhAllData (statusCategory bucketing, orphan-subtask warnOnce, R-04 projectId source) + TaskCard timeInColumn badge
- [x] 73-03-PLAN.md — Wave 3: single "Reload board" toolbar (4-key invalidation) + Sidebar prefetch swap to getGhAllData + delete fetchSprintSubtasks (GH-CUT-01)

**Details:**

`useGhAllData` + `getGhAllData` + `invalidateGhAllData` hooks shipped. `SprintBoardTab` data-layer rewritten onto `useGhAllData` with `statusCategory`-based bucketing (ACTIVE/FUTURE/DONE columns). `TaskCard` gains `timeInColumn` badge with `formatTimeAgo`. Sidebar `/sprint-board` prefetch swapped from legacy `fetchSprintStories` to `getGhAllData`. `fetchSprintSubtasks` hard-deleted. Unified "Reload board" toolbar action invalidates 4 keys. Quick filters (Jira-loaded `jira-board-quickfilters` system removed in post-verification cleanup `e1c098f0`).

**Completed:** 2026-05-29

---

### Phase 74: Backlog on `data.json`

**Goal:** Replace the paginated REST backlog fetch with a single `data.json` call.

**Requirements:** GH-BACKLOG-01, GH-BACKLOG-02

**Plans:** 6/6 plans complete

- [x] 74-01-PLAN.md — Wave 0: type widening (GhBacklogResponse per D-04a) + Wave 0 test scaffolds + static-grep guard script
- [x] 74-02-PLAN.md — Wave 1: useGhBacklogData / getGhBacklogData / invalidateGhBacklogData + greenhopper barrel + services/jira.ts re-exports (D-09b)
- [x] 74-03-PLAN.md — Wave 2: BacklogPage data-layer rewrite (single useGhBacklogData, adapter chain, sprint reverse-index, ACTIVE/FUTURE filter, mutation invalidation swap)
- [x] 74-04-PLAN.md — Wave 2: Sidebar /backlog prefetch collapse 3 → 1 (getGhBacklogData)
- [x] 74-05-PLAN.md — Wave 3: Reload backlog toolbar action + aria-live status region + human-verify checkpoint
- [x] 74-06-PLAN.md — Wave 4: hard-cutover delete (fetchBacklogIssues / fetchBacklogSprintStories / fetchBacklogView / BacklogViewData) + activate check:legacy-backlog guard + full-suite gate

**Details:**

`useGhBacklogData` / `getGhBacklogData` / `invalidateGhBacklogData` shipped. `BacklogPage` rewritten onto a single `data.json` call with adapter chain + sprint reverse-index. Sidebar backlog prefetch collapsed from 3 queries to 1. Five production cache-invalidation sites migrated from legacy keys to `invalidateGhBacklogData`. Two title-resolver caches rewired. Legacy backlog symbols hard-deleted; `npm run check:legacy-backlog` static-grep guard prevents reappearance.

**Completed:** 2026-05-29

---

### Phase 75: Progressive Issue Detail Rendering

**Goal:** Keep the existing Jira REST-based issue detail panel but eliminate the "blank panel until everything loads" feeling.

**Requirements:** PERF-DETAIL-01, PERF-DETAIL-02, PERF-DETAIL-03, GH-CUT-01, GH-CUT-02

**Plans:** 4/4 plans complete

- [x] 75-01-PLAN.md — Wave 1: decompose fetchIssueDetail (slim base + fetchIssueChangelog + fetchEnrichedSubtasks) + Comments/Subtasks skeletons + failing Wave 0 progressive test scaffold
- [x] 75-02-PLAN.md — Wave 2: remove global isLoading gate, add 3 independent section queries with 200ms-gated skeletons + per-section inline errors + TTFMP/TTI perf marks; turn progressive test green
- [x] 75-03-PLAN.md — Wave 3: mutation/invalidation fan-out to new comments/changelog keys + fix IssueDetailContent:68 key bug + invalidation tests + full-suite regression gate
- [x] 75-04-PLAN.md — Wave 4: GH-CUT-02 perf verification artifact (before/after TTFMP+TTI, per-section latencies, gating section) via human-verify checkpoint

**Details:**

`fetchIssueDetail` decomposed into a slim base fetch + 3 independent parallel queries (comments, subtask enrichment, changelog). Global `isLoading` gate removed; `IssueDetailContent` renders header immediately on base fetch. `CommentsSkeleton` + `SubtasksSkeleton` with 200ms-gated display prevent flicker on cache hits. Per-section inline error/retry isolation. Fanned-out invalidation wired to new `['issue-comments', key]` / `['issue-enriched-subtasks', key]` / `['issue-changelog', key]` cache keys. Performance: TTFMP 1180ms (header renders immediately), TTI 1682ms (changelog gates "fully loaded"). Artifact: `taskflow/docs/perf/75-issue-detail-progressive.md`.

**Completed:** 2026-05-30

---

## Milestone Summary

**Decimal Phases:** None.

**Key Decisions:**

- Hard cutover per surface — REST paths deleted in the same phase as their GreenHopper replacement (no coexistence flags, no feature flags)
- GreenHopper `details.json` migration descoped mid-milestone (Phase 75 rescoped to progressive rendering of existing REST v2 detail panel; GH-DETAIL-01..04 dropped, PERF-DETAIL-01..03 added)
- `CATEGORY_COLUMNS` 3-bucket (backlog/in-progress/done) used instead of `columnsData.columns[]` for sprint board columns — accepted per D-03/D-03a (GH-BOARD-03 design note)
- `jira-board-quickfilters` system removed post-verification (commit `e1c098f0`) when it turned out GH `allData.json` doesn't return quickfilter data; app's own saved filters preserved
- `FieldsSection.transitionMutation.onSettled` wired to call `invalidateGhAllData` after status transition (commit `2ac516c7`) to keep sprint board live after issue-detail status change

**Issues Resolved:**

- `transitionMutation.onSettled` not invalidating `gh-all-data` — sprint board not refreshing after status change from issue detail (fixed `2ac516c7`)
- `fetchIssueChangelog` not in `services/jira.ts` barrel (WARNING-01, fixed `b15b9142`)
- `buildEntityMaps` unknown cast in `BacklogPage` (WARNING-02, fixed `d038a252`)
- Orphaned components and duplicate comment functions cleaned (`68f38d59`)
- Dead GH details module removed, sprint-move invalidation fixed (`045ac7d8`)
- Stale 5-key reload doc in `73-VERIFICATION.md` fixed (`76f5b155`)

**Issues Deferred (Tech Debt — 10 items):**

- `fetchIssueDetails` dead private module in `greenhopper/details.ts` + `greenhopper/index.ts` (0 production callers; removed from public surface in `8d6f36e6`)
- Phase 71/72/73 plan SUMMARY.md files missing `requirements-completed` frontmatter
- `QuickCreateInput` + `BulkActionBar` orphaned (no production parent renders them)
- `73-VERIFICATION.md` truth #9 stale (claims 5-key Reload; `jira-board-quickfilters` removed; current code correct with 4 keys)
- GH-BOARD-03 design interpretation: `columnsData.columns[]` not consumed; fixed 3-bucket CATEGORY_COLUMNS used instead
- Biome `useExhaustiveDependencies` in `useGhAllData.ts:48` (intentional WR-05 pattern, deferred)
- Biome `useExhaustiveDependencies` in `useGhBacklogData.ts:56` (intentional WR-05 pattern, deferred)
- `FieldsSection.sprintMoveMutation.onSettled` invalidates `gh-backlog` but not `gh-all-data` — sprint board won't auto-refresh when issue moved to sprint from issue detail panel
- WR-02: subtask enrichment cache key stale on base refetch — new subtask assignees appear only after navigate-away/back
- INFO-01: Duplicate comment functions in `jira.ts` vs `jira/comments.ts` (pre-existing tech debt)

---

*For current project status, see .planning/ROADMAP.md*
