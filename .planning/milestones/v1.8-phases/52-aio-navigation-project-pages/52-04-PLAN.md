---
phase: 52-aio-navigation-project-pages
plan: "04"
type: execute
wave: 2
depends_on:
  - "52-01"
  - "52-02"
files_modified:
  - taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx
  - taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx
autonomous: true
requirements:
  - AION-03

must_haves:
  truths:
    - "AioProjectOverviewPage renders a table row per AioCycle with key, name, and status badge"
    - "Cycle name is a NavLink to /aio-cycle/:projectKey/:cycleKey (not full-row click)"
    - "Status badge uses aioCycleStatusBadgeClass from statusStyles.ts"
    - "Loading state shows AioCyclesSkeleton after 200ms delay"
    - "Empty list shows EmptyState with FlaskConical icon"
    - "Fetch error shows ErrorState with retry that invalidates cycles query"
    - "AioProjectOverviewPage.test.tsx stubs from Plan 00 turn GREEN"
  artifacts:
    - path: "taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx"
      provides: "AIO project overview page with cycle list and status badges"
      exports: ["default AioProjectOverviewPage"]
    - path: "taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx"
      provides: "5-row skeleton with no internal p-4 padding"
      exports: ["AioCyclesSkeleton"]
  key_links:
    - from: "taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx"
      to: "taskflow/src/services/aio"
      via: "import { fetchAioCycles } from '@/services/aio'"
      pattern: "fetchAioCycles"
    - from: "taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx"
      to: "taskflow/src/lib/statusStyles.ts"
      via: "import { aioCycleStatusBadgeClass } from '@/lib/statusStyles'"
      pattern: "aioCycleStatusBadgeClass"
    - from: "taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx"
      to: "React Router NavLink"
      via: "NavLink to={`/aio-cycle/${projectKey}/${cycle.key}`}"
      pattern: "/aio-cycle"
---

<objective>
Build AioProjectOverviewPage and AioCyclesSkeleton — the per-project cycle list route. This page reads :projectKey from URL params, fetches all cycles via fetchAioCycles, and renders them in a table with a NavLink on the cycle name column.

Purpose: After this plan, navigating to /aio-project/:projectKey renders the full cycle list page. AioProjectOverviewPage.test.tsx stubs (3 cases) turn GREEN. Phase 52 is feature-complete.
Output: AioProjectOverviewPage.tsx (default export), AioCyclesSkeleton.tsx (named export).
</objective>

<execution_context>
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/52-aio-navigation-project-pages/52-CONTEXT.md
@.planning/phases/52-aio-navigation-project-pages/52-PATTERNS.md
@.planning/phases/52-aio-navigation-project-pages/52-UI-SPEC.md

<interfaces>
<!-- Key contracts this page uses. Executor must not explore these — use as given. -->

From taskflow/src/services/aio (via Plan 01 barrel):
- fetchAioCycles(baseUrl: string, token: string, projectKey: string): Promise<AioCycle[]>
- AioCycle { key: string; name: string; status: string; projectKey: string }

From taskflow/src/lib/statusStyles.ts (via Plan 01):
- aioCycleStatusBadgeClass(status: string): string
  — returns Tailwind class string for badge; 'Active' → 'bg-blue-500/15 text-blue-600 dark:text-blue-400', 'Closed' → 'bg-muted text-muted-foreground', unknown → fallback muted

From react-router-dom:
- useParams<{ projectKey: string }>() — returns { projectKey: string | undefined }
- NavLink — renders an anchor; to prop set to `/aio-cycle/${projectKey}/${cycle.key}`
  — NavLink is on Name cell ONLY (D-12) — NOT full-row click; route 404s until Phase 53

From taskflow/src/stores/auth.store.ts:
- useAuthStore() returns { jiraBaseUrl: string | null }

From taskflow/src/services/stronghold:
- readSecret('jira-pat'): Promise<string>

From @tanstack/react-query:
- useQuery<AioCycle[]>({ queryKey, queryFn, enabled })
  — queryKey: ['aio', jiraBaseUrl, 'cycles', projectKey] (locked in CONTEXT.md)
  — enabled: !!jiraBaseUrl && !!token && !!projectKey (useParams null-guard per RESEARCH pitfall 3)
- useQueryClient() — for invalidateQueries with queryKey ['aio', jiraBaseUrl, 'cycles', projectKey]

From taskflow/src/hooks/useDelayedLoading.ts:
- useDelayedLoading(isLoading: boolean): boolean

UI-SPEC page layout:
- Outer: flex flex-col h-full
- Header: px-6 py-4 border-b border-border flex-shrink-0 / h1 text-xl font-semibold
  — h1 text: "Cycles — {projectKey}" (projectKey from useParams)
- Content: flex-1 overflow-auto
- Skeleton wrapper: p-4 around AioCyclesSkeleton (skeleton itself has NO internal p-4)
- Error wrapper: p-4 around ErrorState
- Table: w-full text-sm / thead border-b bg-muted/10 / tbody rows

UI-SPEC table columns (AIO Cycles per UI-SPEC + D-11):
- Key column: w-28 | px-3 py-2 text-left text-xs font-medium text-muted-foreground header | px-3 py-3 text-xs text-muted-foreground font-mono cell | content: cycle.key
- Name column: auto | px-4 py-2 text-left text-xs font-medium text-muted-foreground header | px-4 py-3 cell | content: NavLink to /aio-cycle/{projectKey}/{cycle.key}, className="hover:underline"
- Status column: w-32 | px-3 py-2 text-left text-xs font-medium text-muted-foreground header | px-3 py-3 cell | content: inline span with aioCycleStatusBadgeClass, classes: "inline-flex items-center rounded-full border border-transparent px-2 py-0.5 text-xs font-medium {aioCycleStatusBadgeClass(cycle.status)}"

Row: no cursor-pointer (Name cell is the only interactive element — D-12 anti-pattern warning)
Row classes: border-b border-border hover:bg-muted/30 transition-colors

UI-SPEC empty state copy:
- title: "No cycles found"
- subtitle: "No test cycles have been created for this project yet"
- icon: FlaskConical

UI-SPEC error state: viewName="cycles"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AioCyclesSkeleton.tsx (no internal padding)</name>
  <files>taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/AioProjectsSkeleton.tsx — read the file created in Plan 03 Task 1; AioCyclesSkeleton is identical in structure (same 5-row pattern, no internal p-4)
    - taskflow/src/routes/dashboard/EpicsSkeleton.tsx — verify the pitfall: EpicsSkeleton has internal p-4; AioCyclesSkeleton must NOT have it
  </read_first>
  <action>
    Create taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx as a named export component.

    Import: import { Skeleton } from '@/components/ui/skeleton'

    Function: export function AioCyclesSkeleton()

    Return: a div with className="flex flex-col gap-2" (NO p-4)
    containing 5 Skeleton elements: {[0, 1, 2, 3, 4].map((i) => (<Skeleton key={i} className="h-10 w-full" />))}

    This is structurally identical to AioProjectsSkeleton but with the correct name for the cycles context. The page wrapper in AioProjectOverviewPage provides the p-4 padding.
  </action>
  <verify>
    <automated>grep -n "p-4" /Users/user/Documents/Projects/taskflow/taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx exists
    - File contains: export function AioCyclesSkeleton()
    - File contains: className="flex flex-col gap-2"
    - File contains: Skeleton key={i} className="h-10 w-full"
    - grep "p-4" on the file returns empty (no internal padding — critical)
  </acceptance_criteria>
  <done>AioCyclesSkeleton.tsx exists, 5 rows h-10 w-full, no internal p-4</done>
</task>

<task type="auto">
  <name>Task 2: Create AioProjectOverviewPage.tsx and pass AioProjectOverviewPage.test.tsx</name>
  <files>taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/AioProjectsPage.tsx — read the file created in Plan 03; AioProjectOverviewPage follows the same skeleton but with useParams, NavLink, 3-column table, and status badge
    - taskflow/src/routes/dashboard/AioProjectOverviewPage.test.tsx — read the Wave 0 stub to see exactly what text/queries the tests will use (MemoryRouter initialEntries, which mock return shapes, what is queried from the DOM)
    - taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx — confirm the import path for the skeleton component
    - taskflow/src/lib/statusStyles.ts — confirm aioCycleStatusBadgeClass exists and the import path is @/lib/statusStyles
  </read_first>
  <action>
    Create taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx as a default export function component.

    Imports (in order, using @/ alias):
    - useQuery, useQueryClient from @tanstack/react-query
    - FlaskConical from lucide-react
    - useEffect, useState from react
    - NavLink, useParams from react-router-dom
    - EmptyState from @/components/ui/empty-state
    - ErrorState from @/components/ui/error-state
    - useDelayedLoading from @/hooks/useDelayedLoading
    - aioCycleStatusBadgeClass from @/lib/statusStyles
    - fetchAioCycles, and type AioCycle from @/services/aio
    - readSecret from @/services/stronghold
    - useAuthStore from @/stores/auth.store
    - AioCyclesSkeleton from ./AioCyclesSkeleton

    Component body:
    1. const { projectKey } = useParams<{ projectKey: string }>()
    2. const { jiraBaseUrl } = useAuthStore()
    3. const [token, setToken] = useState<string | null>(null)
    4. useEffect: readSecret('jira-pat').then(setToken).catch(() => setToken(null)) — dependency array []
    5. const queryClient = useQueryClient()
    6. useQuery<AioCycle[]> with:
       - queryKey: ['aio', jiraBaseUrl, 'cycles', projectKey]
       - queryFn: () => fetchAioCycles(jiraBaseUrl!, token!, projectKey!)
       - enabled: !!jiraBaseUrl && !!token && !!projectKey (null-guard for all three — RESEARCH pitfall 3)
    7. const showSkeleton = useDelayedLoading(isLoading)

    Render:
    - Outer div: className="flex flex-col h-full"
    - Page header div: className="flex items-center justify-between px-6 py-4 border-b border-border flex-shrink-0"
      containing h1 className="text-xl font-semibold" with text "Cycles — {projectKey}"
      (projectKey from useParams — may be undefined on malformed URL, in which case fallback to empty string is acceptable)
    - Content div: className="flex-1 overflow-auto"
      States in order:
      a. Error (no cached data): {isError && !data && <div className="p-4"><ErrorState error={error} onRetry={() => queryClient.invalidateQueries({ queryKey: ['aio', jiraBaseUrl, 'cycles', projectKey] })} viewName="cycles" /></div>}
      b. Skeleton: {showSkeleton ? <div className="p-4"><AioCyclesSkeleton /></div> : ...}
      c. After skeleton gate — data table or empty:
         Table: className="w-full text-sm" with thead className="border-b bg-muted/10"
         Headers: "Key" (w-28, px-3 py-2), "Name" (auto, px-4 py-2), "Status" (w-32, px-3 py-2) — all text-left text-xs font-medium text-muted-foreground
         Rows: map over data ?? [] — tr key={cycle.key} className="border-b border-border hover:bg-muted/30 transition-colors"
           (NO cursor-pointer on tr — only Name cell is interactive, per D-12 anti-pattern warning)
         Key td: px-3 py-3, text-xs text-muted-foreground font-mono, content: cycle.key
         Name td: px-4 py-3, contains NavLink to={`/aio-cycle/${projectKey}/${cycle.key}`} className="hover:underline" with cycle.name as text content
         Status td: px-3 py-3, contains span with:
           className={`inline-flex items-center rounded-full border border-transparent px-2 py-0.5 text-xs font-medium ${aioCycleStatusBadgeClass(cycle.status)}`}
           text content: cycle.status
         Empty state: when data !== undefined && data.length === 0, render EmptyState icon={FlaskConical} title="No cycles found" subtitle="No test cycles have been created for this project yet"
  </action>
  <verify>
    <automated>cd /Users/user/Documents/Projects/taskflow/taskflow && npx vitest run src/routes/dashboard/AioProjectOverviewPage.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx exists
    - File contains: export default function AioProjectOverviewPage()
    - File contains: const { projectKey } = useParams
    - File contains: queryKey: ['aio', jiraBaseUrl, 'cycles', projectKey]
    - File contains: fetchAioCycles(jiraBaseUrl!, token!, projectKey!)
    - File contains: enabled: !!jiraBaseUrl && !!token && !!projectKey
    - File contains: aioCycleStatusBadgeClass(cycle.status)
    - File contains: NavLink (on name cell only — not on tr)
    - File contains: /aio-cycle/${projectKey}/${cycle.key}
    - File contains: title="No cycles found"
    - File does NOT contain: cursor-pointer (on the tr element — tr must NOT be a clickable row)
    - npx vitest run src/routes/dashboard/AioProjectOverviewPage.test.tsx exits 0 (all 3 stubs from Plan 00 GREEN)
    - cd taskflow && npx vitest run exits with full suite green (929 existing + 13 new stubs all passing)
  </acceptance_criteria>
  <done>AioProjectOverviewPage.tsx complete. AioProjectOverviewPage.test.tsx 3/3 green. All Phase 52 stubs now green. Full suite regression-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL param → fetch | projectKey from useParams flows into fetchAioCycles — trust boundary in cycles.ts (Plan 01), not here |
| AIO API response → NavLink href | cycle.key from API response used in NavLink href — API is authenticated source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | projectKey from useParams passed to fetchAioCycles | mitigate | encodeURIComponent applied in cycles.ts (Plan 01). The page component passes projectKey! as a plain string to fetchAioCycles which handles encoding internally. No additional encoding needed at the page layer. |
| T-52-02 | Information Disclosure | ErrorState viewName in cycles error display | accept | viewName="cycles" is a static string — no credentials or tokens in error output. |
| T-52-P1 | Tampering | useParams null on malformed URL | accept | enabled: !!projectKey guard prevents fetch when projectKey is undefined. Page renders header with undefined/empty projectKey — cosmetic only, no data leak or crash. |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/routes/dashboard/AioProjectOverviewPage.test.tsx` — exits 0, 3 passed
- `grep -n "p-4" taskflow/src/routes/dashboard/AioCyclesSkeleton.tsx` — empty (no internal padding)
- `grep "queryKey.*aio.*jiraBaseUrl.*cycles.*projectKey" taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx` — 1 match
- `grep "aioCycleStatusBadgeClass" taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx` — 1 match
- `grep "NavLink" taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx` — present (on Name cell only)
- `grep "cursor-pointer" taskflow/src/routes/dashboard/AioProjectOverviewPage.tsx` — empty (NOT on tr)
- `cd taskflow && npx vitest run` — full suite: 929 original + all Phase 52 tests green (cycles: 5, projects page: 3, overview page: 3, sidebar: 2 = 13 new), 0 failures
</verification>

<success_criteria>
AioProjectOverviewPage renders cycle list with key, name NavLink, and status badge. AioCyclesSkeleton has no double-padding. AioProjectOverviewPage.test.tsx 3/3 green. All 13 Phase 52 test stubs now GREEN. Full suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/52-aio-navigation-project-pages/52-04-SUMMARY.md`
</output>
