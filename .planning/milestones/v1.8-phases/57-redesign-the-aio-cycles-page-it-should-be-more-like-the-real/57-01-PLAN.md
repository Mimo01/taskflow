---
phase: 57
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - taskflow/src/services/aio/cycles.test.ts
  - taskflow/src/services/jira/users.test.ts
  - taskflow/src/lib/aioUtils.test.ts
  - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PROBE-FINDINGS.md
autonomous: false
requirements: [AION-03]

must_haves:
  truths:
    - "Developer has captured the exact live URL for the folder tree endpoint (returns AioFolder[] shape)"
    - "Developer has captured the exact live URL for the folder cycle count endpoint (returns Record<string, number>)"
    - "Developer has captured the exact live URL + projection param for the cycle list with detail (returns paged response with allIDs)"
    - "Developer has captured the exact live URL + param convention for the batch cycle summary endpoint (returns AioCycleSummaryItem[])"
    - "Developer has captured the exact live URL + param for direct Jira user lookup by username (returns single JiraAssignableUser or 404)"
    - "57-PROBE-FINDINGS.md records all five confirmed URLs verbatim and the exact path + query string for each"
    - "RED test stubs exist for fetchAioFolderTree, fetchAioFolderCycleCounts, fetchAioCyclesWithDetail, fetchAioCycleSummaries in cycles.test.ts (file fails because exports do not exist yet)"
    - "RED test stub exists for fetchJiraUserByUsername in jira/users.test.ts (file fails because export does not exist yet)"
    - "RED test stubs exist for AIO_STATUS_MAP and normalizeStatusById in aioUtils.test.ts (existing file extended — extension fails because exports do not exist yet)"
  artifacts:
    - path: ".planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PROBE-FINDINGS.md"
      provides: "Verbatim endpoint URLs from live AIO instance + branch decisions for Wave 1"
      contains: "folder tree URL"
    - path: "taskflow/src/services/aio/cycles.test.ts"
      provides: "RED stubs (describe blocks with vi.fn mocks) for 4 new fetch functions"
      contains: "fetchAioFolderTree"
    - path: "taskflow/src/services/jira/users.test.ts"
      provides: "RED stubs for fetchJiraUserByUsername (new file)"
      contains: "fetchJiraUserByUsername"
    - path: "taskflow/src/lib/aioUtils.test.ts"
      provides: "RED stubs for AIO_STATUS_MAP + normalizeStatusById (extended existing file)"
      contains: "normalizeStatusById"
  key_links:
    - from: "57-PROBE-FINDINGS.md"
      to: "Plan 02 fetchAioFolderTree implementation"
      via: "endpoint URL constants"
      pattern: "GET /project/.+/folder"
---

<objective>
Establish the foundation for Phase 57 by:
(1) Probing the live AIO + Jira instance to confirm the exact URLs and query parameters for five endpoints whose shapes are documented in `API-EXAMPLES.md` but whose URLs are inferred (Assumptions A1–A6 in `57-RESEARCH.md`).
(2) Creating RED test stubs for every new service function and utility that downstream waves will implement, so subsequent waves run the Nyquist GREEN cycle against existing failing tests.

Purpose: Without confirmed URLs, Plan 02 would ship 404-prone service code. Without RED stubs, Plan 02/03 would write code with no failing assertion to satisfy first. Wave 0 is the BLOCKING dependency for both.

Output:
- `57-PROBE-FINDINGS.md` recording verified URLs + parameter conventions
- 3 test files containing RED stubs that compile but fail because the production exports they reference do not exist yet
</objective>

<execution_context>
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-CONTEXT.md
@.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-RESEARCH.md
@.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PATTERNS.md
@.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-UI-SPEC.md
@.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/API-EXAMPLES.md

<interfaces>
<!-- Existing exports from cycles.ts that the new functions must coexist with: -->
From taskflow/src/services/aio/cycles.ts:
  - export async function fetchAioCycles(baseUrl: string, token: string, projectKey: string): Promise<AioCycle[]>
  - export async function fetchAioCycleDetail(baseUrl: string, token: string, projectKey: string, cycleKey: string): Promise<AioCycle>

From taskflow/src/lib/aioUtils.ts:
  - export function normalizeStatus(raw: string | undefined): 'pass' | 'fail' | 'blocked' | 'notRun'
  - export function normalizeStatusLabel(raw: string | undefined): string

From taskflow/src/services/jira/users.ts:
  - export async function fetchAssignableUsers(baseUrl: string, token: string, projectKey: string, query: string): Promise<JiraAssignableUser[]>

From taskflow/src/services/jira/types.ts:
  - export interface JiraAssignableUser { displayName: string; name: string; key?: string; avatarUrls?: {...} }

<!-- New exports the Wave 0 stubs reference (do not exist yet — that is the point): -->
Target exports for Wave 1 / Plan 02 to satisfy:
  - export async function fetchAioFolderTree(baseUrl, token, projectKey): Promise<AioFolder[]>
  - export async function fetchAioFolderCycleCounts(baseUrl, token, projectKey): Promise<Record<string, number>>
  - export async function fetchAioCyclesWithDetail(baseUrl, token, projectKey, folderID?: number): Promise<AioCycleDetailPagedResponse>
  - export async function fetchAioCycleSummaries(baseUrl, token, projectKey, ids: number[]): Promise<AioCycleSummaryItem[]>
  - export const AIO_STATUS_MAP: Record<number, 'pass' | 'fail' | 'blocked' | 'notRun' | 'inProgress'>
  - export function normalizeStatusById(id: number): 'pass' | 'fail' | 'blocked' | 'notRun' | 'inProgress'

Target export for Wave 1 / Plan 03:
  - export async function fetchJiraUserByUsername(baseUrl, token, username): Promise<JiraAssignableUser | null>
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Probe live AIO + Jira endpoints and write 57-PROBE-FINDINGS.md</name>
  <files>.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PROBE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/API-EXAMPLES.md
    - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-RESEARCH.md (sections: API Analysis, Open Questions, Assumptions Log)
    - taskflow/src/services/aio/client.ts (AIO_API_PATH base path)
    - taskflow/src/services/aio/cycles.ts (existing fetchAioCycles for URL pattern)
    - taskflow/src/services/jira/users.ts (existing fetchAssignableUsers for URL pattern)
  </read_first>
  <what-built>
    Wave 0 cannot be automated — the executor does not have direct access to the live AIO/Jira instance. The developer must open the real AIO web application in a browser with DevTools (Network tab) and capture the actual HTTP requests fired when the AIO cycles page renders. The output is a markdown findings file Wave 1 consumes verbatim.
  </what-built>
  <how-to-verify>
    Developer steps (run by the human, not Claude):

    1. Open the live AIO Test Management web UI in Chrome/Firefox.
    2. Open DevTools → Network tab → filter "Fetch/XHR".
    3. Navigate to a project's Cycles overview page (the "real AIO page" Phase 57 mirrors).
    4. Capture the following five requests by URL path + query string + method. Each request shape must match one of the four files in `API-EXAMPLES.md` (or the Jira `JiraAssignableUser` shape for the fifth):

       (a) Folder tree request — response matches the `folder` shape from API-EXAMPLES.md (recursive `{ID, name, parentID, children[]}`).
           Expected URL pattern: `GET {baseUrl}/rest/aio-tcms-api/1.0/project/{projectKey}/folder` (assumption A1)

       (b) Folder cycle count request — response matches the `count` shape (`{folderID: cycleCount}` object with `-1` key).
           Expected URL pattern: `GET {baseUrl}/rest/aio-tcms-api/1.0/project/{projectKey}/testcycle/count` OR `/folder/count` (assumption A2)

       (c) Cycle list with detail request — response matches the `paged` shape (`{items, allIDs, startAt, maxResults, isLast}` with non-null `detail`).
           Expected URL pattern: `GET {baseUrl}/rest/aio-tcms-api/1.0/project/{projectKey}/testcycle?startAt=0` possibly with projection param (assumption A4)

       (d) Cycle summary batch request — response matches the `paged2` shape (array of `{ID, detail: null, summary: {totalTests, testRunDistribution}}`).
           Expected URL pattern: `GET ?projection=summary&ids=...` OR POST with body `{ids: [...]}` (assumption A3)

       (e) Folder filter convention — does request (c) above use `?folderID={id}` to filter by folder, or does the client filter all-cycles locally? (assumption A5). Click into a folder in the AIO sidebar and check whether a new request fires with a folderID query param, or whether the client just filters the already-loaded list.

       (f) Jira user lookup — navigate to any Jira issue, hover an assignee avatar, or use the Jira REST `GET /rest/api/2/user?username={DC-username}` directly. Confirm whether the param name is `username` (Jira DC) or `name`. The `ownedByID` values in the AIO sample (`JIRAUSER23429`, `ext94772`) should resolve to a JiraAssignableUser shape (`{displayName, name, key, avatarUrls}`).

    5. Write `57-PROBE-FINDINGS.md` with this exact structure (one section per finding, each with the verbatim URL, query params, method, and a paste of the first 200 chars of the actual response body):

       ```
       # Phase 57 Probe Findings

       **Probed:** {ISO timestamp}
       **Probed against:** {live instance URL, anonymised if needed}

       ## A1: Folder tree endpoint — CONFIRMED
       URL: GET {full path}
       Method: GET
       Headers: Authorization: Bearer <PAT>
       Sample response (first 200 chars): ...

       ## A2: Folder cycle count endpoint — CONFIRMED
       URL: GET {full path}
       ...

       ## A3: Cycle summary batch endpoint — CONFIRMED
       URL: GET / POST {full path}
       Param convention: ?ids=12478,12505 / POST body / paginated
       ...

       ## A4: Cycle list with detail endpoint — CONFIRMED
       URL: GET {full path}
       Detail projection param: ?projection=detail (or empty if default)
       allIDs present in response: yes/no
       ...

       ## A5: Folder filter convention — CONFIRMED
       Convention: ?folderID={id} server-side filter   |   client-side from allIDs   |   /folder/{ID}/testcycle path
       ...

       ## A6: Jira user lookup endpoint — CONFIRMED
       URL: GET {baseUrl}/rest/api/2/user?username={DC-username}
       Method: GET
       Sample username tried: JIRAUSER23429
       Response shape matches JiraAssignableUser: yes/no
       404 on unknown username: yes/no
       ```

    6. If any endpoint cannot be located (e.g. real AIO uses a totally different URL scheme), the developer records `NOT FOUND — recommendation:` and a fallback path. Wave 1 cannot proceed without all six findings recorded.
  </how-to-verify>
  <action>
    Create `.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PROBE-FINDINGS.md` per the structure above. Closes Assumptions A1–A6 from `57-RESEARCH.md` Assumptions Log. The file is consumed verbatim by Wave 1 — Plan 02 and Plan 03 paste the confirmed URLs into `aioFetch()` / `apiFetch()` call sites.
  </action>
  <acceptance_criteria>
    - File `.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PROBE-FINDINGS.md` exists
    - File contains the headings `## A1`, `## A2`, `## A3`, `## A4`, `## A5`, `## A6`
    - Each section contains either a confirmed URL string OR a `NOT FOUND — recommendation:` paragraph
    - Section A6 explicitly states whether the Jira DC param is `username` or `name`
    - Section A5 explicitly states one of: server-filter / client-filter / path-based
  </acceptance_criteria>
  <resume-signal>Type "approved" when 57-PROBE-FINDINGS.md is written and committed. Reply with "blocked" + the unresolved assumption ID(s) if any endpoint cannot be confirmed.</resume-signal>
  <verify>
    <automated>test -f .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PROBE-FINDINGS.md && grep -q '^## A1' .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PROBE-FINDINGS.md && grep -q '^## A6' .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PROBE-FINDINGS.md</automated>
  </verify>
  <done>57-PROBE-FINDINGS.md exists with all six headings A1–A6 present, each containing either a confirmed URL or an explicit NOT FOUND note.</done>
</task>

<task type="auto">
  <name>Task 2: Add RED stubs for AIO_STATUS_MAP and normalizeStatusById to aioUtils.test.ts</name>
  <files>taskflow/src/lib/aioUtils.test.ts</files>
  <read_first>
    - taskflow/src/lib/aioUtils.test.ts (existing tests for normalizeStatus and normalizeStatusLabel — preserve all)
    - taskflow/src/lib/aioUtils.ts (existing exports — note what's NOT exported yet)
    - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PATTERNS.md (aioUtils.test.ts pattern section)
    - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-UI-SPEC.md (Status Color Palette table — source of ID→status mapping)
  </read_first>
  <action>
    Extend `taskflow/src/lib/aioUtils.test.ts` by appending two new `describe` blocks AFTER the existing `describe('normalizeStatus', ...)` and `describe('normalizeStatusLabel', ...)` blocks. Do NOT modify the existing two describe blocks. The new describe blocks must:

    1. Import the new symbols at the top: `import { normalizeStatus, normalizeStatusLabel, normalizeStatusById, AIO_STATUS_MAP } from './aioUtils';` (update the existing import line — Wave 1 / Plan 02 will add these exports).

    2. Add `describe('AIO_STATUS_MAP', ...)` with one `it` per known ID mapping (D-05 from CONTEXT.md, confirmed in UI-SPEC):
       - `AIO_STATUS_MAP[901]` toBe `'pass'`
       - `AIO_STATUS_MAP[51]` toBe `'fail'`
       - `AIO_STATUS_MAP[55]` toBe `'blocked'`
       - `AIO_STATUS_MAP[53]` toBe `'notRun'`
       - `AIO_STATUS_MAP[54]` toBe `'inProgress'`

    3. Add `describe('normalizeStatusById', ...)` with `it` blocks covering:
       - Each known ID (901, 51, 55, 53, 54) maps to its expected status string
       - Unknown ID (e.g. 999) maps to `'notRun'` (safe fallback)
       - The Number-conversion pattern: `normalizeStatusById(Number('53'))` toBe `'notRun'` and `normalizeStatusById(Number('901'))` toBe `'pass'` — documents Pitfall 3 (testRunDistribution keys are JSON strings).

    This file MUST fail when run after this task because `AIO_STATUS_MAP` and `normalizeStatusById` do not exist yet. That failure is the RED state — Plan 02 turns it GREEN.

    The exact test block bodies are provided verbatim in `57-PATTERNS.md` under "taskflow/src/lib/aioUtils.test.ts (test — NEW FILE)" — copy them; only the "describe('normalizeStatus (existing — regression guard)', ...)" duplicate must NOT be added (the existing file already covers it).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/lib/aioUtils.test.ts 2>&1 | grep -E "AIO_STATUS_MAP|normalizeStatusById"</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/lib/aioUtils.test.ts` contains `describe('AIO_STATUS_MAP'` somewhere in the file
    - `taskflow/src/lib/aioUtils.test.ts` contains `describe('normalizeStatusById'` somewhere in the file
    - `taskflow/src/lib/aioUtils.test.ts` contains the string `AIO_STATUS_MAP[901]` and `AIO_STATUS_MAP[51]` and `AIO_STATUS_MAP[55]` and `AIO_STATUS_MAP[53]` and `AIO_STATUS_MAP[54]`
    - `taskflow/src/lib/aioUtils.test.ts` contains an import line referencing `normalizeStatusById` and `AIO_STATUS_MAP` from `./aioUtils`
    - Running `cd taskflow && npx vitest run src/lib/aioUtils.test.ts` FAILS (RED state — exports don't exist yet)
    - Existing tests for `normalizeStatus` and `normalizeStatusLabel` are NOT modified — `grep -c "describe('normalizeStatus'" taskflow/src/lib/aioUtils.test.ts` returns 1 (the original; new describes use different names)
  </acceptance_criteria>
  <done>Extended aioUtils.test.ts has 2 new describe blocks covering AIO_STATUS_MAP + normalizeStatusById, file fails on run (RED), and existing tests are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Create RED stub file jira/users.test.ts for fetchJiraUserByUsername</name>
  <files>taskflow/src/services/jira/users.test.ts</files>
  <read_first>
    - taskflow/src/services/aio/cycles.test.ts (test infrastructure pattern: vi.mock for apiFetch, BASE/TOKEN constants, describe→it→beforeEach)
    - taskflow/src/services/jira/users.ts (existing fetchAssignableUsers — note signature and what's NOT exported yet)
    - taskflow/src/services/jira/types.ts (JiraAssignableUser shape — `{displayName, name, key?, avatarUrls?}`)
    - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PATTERNS.md (jira/users.test.ts NEW FILE section — provides verbatim test bodies)
  </read_first>
  <action>
    Create a NEW file `taskflow/src/services/jira/users.test.ts` (does not exist yet — `find` confirmed). Structure mirrors `taskflow/src/services/aio/cycles.test.ts`:

    1. Imports: `import { beforeEach, describe, expect, it, vi } from 'vitest';` then `vi.mock('../../lib/apiFetch', () => ({ apiFetch: vi.fn() }));` then `import { apiFetch } from '../../lib/apiFetch';` and `import { fetchJiraUserByUsername } from './users';`.

    2. Constants: `const mockedApiFetch = vi.mocked(apiFetch);` `const BASE = 'https://jira.example.com';` `const TOKEN = 'test-token';`.

    3. One `describe('fetchJiraUserByUsername', ...)` block with four `it` cases (verbatim from 57-PATTERNS.md):
       - `it('returns JiraAssignableUser on 200', ...)` — mock 200 + JSON `{displayName: 'Alice', name: 'JIRAUSER23429'}`; expect result toEqual that object.
       - `it('returns null on 404', ...)` — mock `{ok: false, status: 404}`; expect result toBeNull.
       - `it('returns null on network error', ...)` — `mockedApiFetch.mockRejectedValue(new Error('timeout'))`; expect toBeNull.
       - `it('calls correct URL with encoded username', ...)` — verify the URL includes `/rest/api/2/user?username=ext94772` and Authorization header `Bearer test-token`.

    The exact `it` body source is `57-PATTERNS.md` lines under the "taskflow/src/services/jira/users.test.ts (test — NEW FILE)" heading. Copy verbatim.

    Note: the URL param name in `it('calls correct URL with encoded username')` MUST match what the developer recorded in `57-PROBE-FINDINGS.md` section A6 (likely `username` per Jira DC convention). If A6 reports `name` instead of `username`, swap the assertion accordingly.

    This file MUST fail when run after this task because `fetchJiraUserByUsername` is not exported from `./users` yet. RED state. Plan 03 turns it GREEN.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/services/jira/users.test.ts 2>&1 | grep -E "fetchJiraUserByUsername|Cannot find"</automated>
  </verify>
  <acceptance_criteria>
    - File `taskflow/src/services/jira/users.test.ts` exists
    - File contains `describe('fetchJiraUserByUsername'`
    - File contains four `it(` cases referenced above (count `grep -c "  it(" taskflow/src/services/jira/users.test.ts` returns 4)
    - File imports `fetchJiraUserByUsername` from `./users`
    - File mocks `../../lib/apiFetch`
    - Running `cd taskflow && npx vitest run src/services/jira/users.test.ts` FAILS (RED — fetchJiraUserByUsername does not exist)
  </acceptance_criteria>
  <done>jira/users.test.ts exists with 4 RED test cases for fetchJiraUserByUsername; vitest run fails because export is missing.</done>
</task>

<task type="auto">
  <name>Task 4: Extend aio/cycles.test.ts with RED stubs for 4 new fetch functions</name>
  <files>taskflow/src/services/aio/cycles.test.ts</files>
  <read_first>
    - taskflow/src/services/aio/cycles.test.ts (existing tests for fetchAioCycles — preserve all)
    - taskflow/src/services/aio/cycles.ts (existing fetchAioCycles signature + error-handling pattern lines 75–105)
    - taskflow/src/services/aio/types.ts (existing AioCycle, AioPage types — note that AioFolder/AioCycleDetailItem/AioCycleSummaryItem don't exist yet)
    - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PATTERNS.md (cycles.test.ts EXTEND section — provides describe block template)
    - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/API-EXAMPLES.md (folder + count + paged + paged2 sample responses to use as mock JSON)
    - .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PROBE-FINDINGS.md (output of Task 1 — informs URL assertion strings if added)
  </read_first>
  <action>
    Extend `taskflow/src/services/aio/cycles.test.ts` by appending new describe blocks AFTER the existing `describe('fetchAioCycles', ...)`. Do NOT modify the existing describe block. The new content:

    1. At the import line, add: `import { fetchAioCycles, fetchAioFolderTree, fetchAioFolderCycleCounts, fetchAioCyclesWithDetail, fetchAioCycleSummaries } from './cycles';` — keep `fetchAioCycles` since existing tests use it.

    2. Append `describe('fetchAioFolderTree', ...)` with these `it` cases:
       - `'returns AioFolder[] on 200'` — mock 200 with the actual folder-tree sample (simplified: `[{ID: 201, name: '2023 - DONE', description: null, parentID: null, rankOrder: null, children: [{ID: 202, name: 'Campaign A 11/2023', description: null, parentID: 201, rankOrder: null, children: []}]}]`); expect result toEqual that array.
       - `'throws ApiError with status 401 and source jira on 401'` — mock `{ok: false, status: 401}`; expect toMatchObject `{status: 401, source: 'jira'}`.
       - `'returns [] on 404'` — mock `{ok: false, status: 404}`; expect result toEqual `[]`.
       - `'throws "Cannot reach AIO" on network error'` — `mockedApiFetch.mockRejectedValue(new Error('timeout'))`; expect toThrow matching `/Cannot reach AIO/`.

    3. Append `describe('fetchAioFolderCycleCounts', ...)` with similar four cases. The 200-case mock JSON is a simplified Record<string, number>: `{"-1": 3, "201": 5, "301": 240}`. Expect `await fetchAioFolderCycleCounts(BASE, TOKEN, PROJECT_KEY)` toEqual that object.

    4. Append `describe('fetchAioCyclesWithDetail', ...)` with four cases. The 200-case uses the paged sample shape: `{items: [{ID: 1001, jiraProjectID: 2001, permission: {value: 15}, detail: {key: 'PROJ-CY-101', title: 'Cycle Alpha', ownedByID: 'user_a', folder: null, isClosed: false, startDate: null, endDate: null}, summary: null}], allIDs: [1001, 1002], startAt: 0, maxResults: 20, isLast: true}`. Expect result toEqual that object (or matchObject if extending types).

       Add a fifth case: `'accepts optional folderID parameter and includes it in the URL'` — call `fetchAioCyclesWithDetail(BASE, TOKEN, PROJECT_KEY, 201)` and assert `mockedApiFetch` was called with a URL containing the folder filter query param. The exact filter convention (`?folderID=201`, client-filter, or `/folder/201/testcycle`) MUST come from `57-PROBE-FINDINGS.md` section A5. If A5 says client-filter, this fifth case becomes `'ignores folderID param and returns full cycle list (client-side filter happens in component)'`.

    5. Append `describe('fetchAioCycleSummaries', ...)` with four cases. The 200-case uses the paged2 sample: an ARRAY (not paged wrapper) like `[{ID: 1001, jiraProjectID: 2001, permission: {value: 15}, detail: null, summary: {totalTests: 261, totalRuns: null, totalTestsWithDS: null, estimatedEffort: null, testRunDistribution: {"53": 228, "901": 30, "54": 3}, actualEffort: null, assignees: null}, objectiveAttachments: []}]`. Expect result toEqual that array. The function signature is `(BASE, TOKEN, PROJECT_KEY, [1001, 1002])` — verify the URL contains the `ids` param convention per A3 finding (e.g. `?ids=1001,1002` for GET, or `mockedApiFetch` called with POST body matching `{ids: [1001, 1002]}` if A3 says POST).

    All four describe blocks must include a `beforeEach(() => { vi.clearAllMocks(); })` clause.

    Run after this task — file MUST fail because the four new functions are not exported yet. RED state. Plan 02 turns it GREEN.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/services/aio/cycles.test.ts 2>&1 | grep -E "fetchAioFolderTree|fetchAioFolderCycleCounts|fetchAioCyclesWithDetail|fetchAioCycleSummaries|Cannot find"</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/services/aio/cycles.test.ts` contains `describe('fetchAioFolderTree'`
    - `taskflow/src/services/aio/cycles.test.ts` contains `describe('fetchAioFolderCycleCounts'`
    - `taskflow/src/services/aio/cycles.test.ts` contains `describe('fetchAioCyclesWithDetail'`
    - `taskflow/src/services/aio/cycles.test.ts` contains `describe('fetchAioCycleSummaries'`
    - Existing `describe('fetchAioCycles'` is still present and unmodified (single occurrence: `grep -c "describe('fetchAioCycles'" taskflow/src/services/aio/cycles.test.ts` returns 1)
    - Import line references all four new functions from `./cycles`
    - Running `cd taskflow && npx vitest run src/services/aio/cycles.test.ts` FAILS (RED state)
  </acceptance_criteria>
  <done>cycles.test.ts has 4 new describe blocks for the new fetch functions; existing fetchAioCycles tests unchanged; vitest run fails because new exports don't exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer browser → live AIO TCMS API | Personal Access Token in Authorization header crosses this boundary during probe |
| Developer browser → live Jira REST API | Same PAT (PAT is shared with Jira per Phase 51) |
| Disk (57-PROBE-FINDINGS.md) → version control | Probe findings file lives in `.planning/` and may be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01-01 | I (Information Disclosure) | 57-PROBE-FINDINGS.md | mitigate | Developer MUST anonymise any Bearer tokens, real usernames (replace with `JIRAUSER23429` / `ext94772` style placeholders), and customer project keys before committing. Probe task explicitly says "anonymised if needed" in the verbatim URL paste. |
| T-57-01-02 | I | Test fixture data in cycles.test.ts / users.test.ts | mitigate | Test mock JSON uses placeholder values only (`'user_a'`, `'PROJ'`, `'jira.example.com'`) — never real production data. Acceptance criteria require BASE constant = `'https://jira.example.com'`. |
| T-57-01-03 | T (Tampering) | Probe URL transcription | accept | Risk: developer mistypes URL into findings file → Wave 1 ships 404. Mitigation is verification in Plan 02 unit tests (mocks must match the recorded URL pattern). Low severity — caught immediately on first run of Plan 02 tests. |
| T-57-01-04 | R (Repudiation) | 57-PROBE-FINDINGS.md timestamp | accept | The file records `Probed:` timestamp. No cryptographic signing — appropriate for solo-dev internal planning artefact. |

No high-severity threats identified for Wave 0. All findings remain inside the `.planning/` directory or test fixture mocks; no production-facing surface area is added in this plan.
</threat_model>

<verification>
After all tasks complete, run:

```bash
cd taskflow && npx vitest run src/lib/aioUtils.test.ts src/services/jira/users.test.ts src/services/aio/cycles.test.ts
```

Expected output: all three files FAIL with errors referencing the missing exports (`fetchAioFolderTree`, `fetchAioFolderCycleCounts`, `fetchAioCyclesWithDetail`, `fetchAioCycleSummaries`, `fetchJiraUserByUsername`, `AIO_STATUS_MAP`, `normalizeStatusById`). This is the RED state Wave 1 satisfies.

The existing tests (`fetchAioCycles`, `normalizeStatus`, `normalizeStatusLabel`) must continue to PASS — confirms we did not break the regression suite.

Additionally verify:
```bash
test -f .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PROBE-FINDINGS.md
grep -c '^## A' .planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-PROBE-FINDINGS.md
# Must return >= 6 (one per assumption A1–A6)
```
</verification>

<success_criteria>
- 57-PROBE-FINDINGS.md exists with all six findings A1–A6 recorded
- Three test files (aioUtils.test.ts, jira/users.test.ts, aio/cycles.test.ts) contain RED stubs covering 7 missing exports
- Existing tests in those files (where applicable) remain passing
- Wave 1 (Plan 02 + Plan 03) has the artefacts needed to GREEN the stubs
</success_criteria>

<output>
After completion, create `.planning/phases/57-redesign-the-aio-cycles-page-it-should-be-more-like-the-real/57-01-SUMMARY.md` recording:
- Confirmed URLs from 57-PROBE-FINDINGS.md (paste sections A1–A6)
- Which assumption(s) A1–A6 are CLOSED and which (if any) escalated as blockers
- Test stub file paths and assertion counts
- Wave 1 dependencies unblocked
</output>
