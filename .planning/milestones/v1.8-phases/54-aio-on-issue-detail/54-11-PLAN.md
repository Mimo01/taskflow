---
phase: 54-aio-on-issue-detail
plan: "54-11"
type: execute
wave: 10
depends_on: ["54-10"]
files_modified:
  - taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx
  - taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx
  - taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx
  - taskflow/src/routes/dashboard/AioTestRunDetailPage.test.tsx
  - taskflow/src/routes/routes.tsx
  - .planning/phases/54-aio-on-issue-detail/54-HUMAN-UAT.md
autonomous: false
gap_closure: true
tags: [aio, tcms, impacted-executions, cross-project, status-bug, new-route, round-3-uat]

requirements: [AIOI-01, AIOI-04]

must_haves:
  truths:
    - "Cross-project impacted executions resolve their detail fetch correctly. Bug verified 2026-05-14T13:35Z via user-shared network response: URL `/project/VTE/testcycle/ESHOP-CY-759/testrun/209620?assignSteps=true` returned 'For the selected project, No Cycle found with Key: ESHOP-CY-759'. Root cause: the detail fetch used the parent issue's projectKey (VTE) for ALL impacted executions, but cross-project cycles (ESHOP-CY-759) require their OWN project key. Fix: derive the project key per-cycle from `cycleKey.split('-')[0]` (AIO convention `{PROJECT}-CY-{N}` confirmed via Phase 54 probe data: ESHOP-CY-11, ESHOP-CY-1011, etc.). The traceability response does NOT carry projectKey on the cycle object — split-on-first-dash is the only viable derivation."
    - "After the fix, impacted-execution status chips reflect the REAL run status (PASS/FAIL/BLOCKED/NOT_EXECUTED) — not always 'Not Run'."
    - "Each impacted execution row has TWO clickable targets: (a) the cycle key cell navigates to the existing in-app cycle detail page `/aio-cycle/{projectKey}/{cycleKey}`; (b) the run ID cell navigates to a NEW in-app run detail page `/aio-cycle/{projectKey}/{cycleKey}/run/{runId}`. Both use the cycle-derived projectKey so cross-project navigation works correctly."
    - "New `AioTestRunDetailPage` route at `/aio-cycle/:projectKey/:cycleKey/run/:runId` shows the run header (status chip, executed date, test case key) and reuses the existing wiki-rendering step display (the same StepTable visual contract as `AioTestRunsSection`)."
    - "Existing contract preserved — rows still render with the proper status chip color (`aioRunStatusBadgeClass`), test case key + title still visible, cycle key + run ID columns still present. The two new click targets do not change the row visual at rest (only on hover via `cursor-pointer` + `hover:bg-muted/30`)."
    - "Type-clean — `tsc --noEmit` exit 0. No new `any` casts in the modified files."
    - "Test count uplift — at least 4 new tests (cross-project detail-fetch correctness, cycle-key click navigation, run-id click navigation, new run detail page render). Current baseline 1023."

  artifacts:
    - path: "taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx"
      provides: "Per-cycle project key derivation for the detail fetch + two clickable cells inside ImpactedExecutionsList (cycle key → /aio-cycle/.../...; run ID → /aio-cycle/.../.../run/...). The fix lives in the runDetail call sites at lines 460-475 (cross-cycle) and the earlier inCycleResults block — change to `cycleKey.split('-')[0]` per row."
      contains: "cycleKey.split('-')[0]"
    - path: "taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx"
      provides: "New full-page route showing a single run's detail. Reads URL params, fetches via fetchAioTestRunDetail, renders status header + step table. Reuses normalizeStatusLabel / aioRunStatusBadgeClass from existing lib."
      contains: "AioTestRunDetailPage"
    - path: "taskflow/src/routes/routes.tsx"
      provides: "New route entry `path: '/aio-cycle/:projectKey/:cycleKey/run/:runId'` wired to lazy-loaded AioTestRunDetailPage."
      contains: "/aio-cycle/:projectKey/:cycleKey/run/:runId"
    - path: "taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx"
      provides: "Tests covering: (1) detail-fetch project key is derived from cycleKey (not issue projectKey) on cross-project rows; (2) cycle-key cell is a Link to /aio-cycle/.../...; (3) run-id cell is a Link to /aio-cycle/.../.../run/...; (4) status chip reflects detail.run.status when fetch succeeds with the corrected URL."
      contains: "cross-project"
    - path: "taskflow/src/routes/dashboard/AioTestRunDetailPage.test.tsx"
      provides: "Tests for the new run detail page: (1) renders status header + step table for a fetched run; (2) shows skeleton while loading; (3) shows error state on fetch failure."
      contains: "AioTestRunDetailPage"

  key_links:
    - from: "AioTestRunsSection.tsx::stepsQuery queryFn cross-cycle fetch loop"
      to: "fetchAioTestRunDetail signature"
      via: "swap `projectKey` argument from outer-scope `projectKey` (issue's project) to `runRef.cycleKey.split('-')[0]` so cross-project cycles are fetched correctly."
      pattern: "runRef.cycleKey.split('-')[0]"
    - from: "ImpactedExecutionsList row cells"
      to: "react-router-dom <Link>"
      via: "wrap cycle key cell content in `<Link to={`/aio-cycle/${cycleProjectKey}/${row.runRef.cycleKey}`}>` and run id cell in `<Link to={`/aio-cycle/${cycleProjectKey}/${row.runRef.cycleKey}/run/${row.runRef.runId}`}>`. `cycleProjectKey = row.runRef.cycleKey.split('-')[0]` derived per row."
      pattern: "Link to"
    - from: "routes.tsx new entry"
      to: "AioTestRunDetailPage component"
      via: "lazy-loaded same as other AIO routes; new path slot between cycle detail and merge-requests entries."
      pattern: "AioTestRunDetailPage"

---

<objective>
Round-3 UAT (user): two new bugs in the Impacted executions list. Diagnostic data shared by user confirmed root cause for the status bug.

**Bug A (status always "Not Run"):** Detail fetch URL hard-codes `projectKey = issueKey.split('-')[0]` (the parent issue's project, e.g. "VTE"). For cross-project impacted executions (e.g. ESHOP-CY-759), the API returns "No Cycle found" → `fetchAioTestRunDetail` returns null → status defaults to `NOT_EXECUTED` ("Not Run"). Verbatim user-shared response: URL `https://jira.orange.sk/rest/aio-tcms-api/1.0/project/VTE/testcycle/ESHOP-CY-759/testrun/209620?assignSteps=true` → body `"For the selected project, No Cycle found with Key: ESHOP-CY-759"`.

**Bug B (rows not clickable):** Phase 54 explicitly scoped impacted-execution rows as read-only ("No click handlers — cycle/run navigation is a phase-53 concern" — AioTestRunsSection.tsx:688). User requested clickability with TWO link targets:
1. Cycle key cell → existing in-app `/aio-cycle/{projectKey}/{cycleKey}` page (built in earlier phase)
2. Run ID cell → NEW in-app `/aio-cycle/{projectKey}/{cycleKey}/run/{runId}` page (this plan creates it)

Both targets use the cycle-derived projectKey (from `cycleKey.split('-')[0]`) so cross-project navigation works.

Scope intentionally narrow: fix the status bug + add two link targets + scaffold a minimal run detail page that reuses existing components. No other UI rework.

Purpose: Close the final UAT findings on Phase 54 so Test 4 (ROADMAP SC end-to-end) can pass.
</objective>

<execution_context>
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx
@taskflow/src/routes/dashboard/AioCycleDetailPage.tsx
@taskflow/src/routes/routes.tsx
@taskflow/src/services/aio/issue-steps.ts

<interfaces>
**Status bug fix — call sites in AioTestRunsSection.tsx:**

- **Line 357: `const projectKey = issueKey.split('-')[0];`** — keep as-is; still used for traceability fetches (which are scoped to the AIO project ID).
- **Lines 460-475: cross-cycle chunked fetch loop.** The `fetchAioTestRunDetail(jiraBaseUrl, token, projectKey, runRef.cycleKey, runRef.runId)` call uses the wrong `projectKey`. Replace with `runRef.cycleKey.split('-')[0]`.
- **Lines 437-450 (approx — find via grep "inCycleResults"):** the in-cycle fetch loop uses `projectKey` too. In-cycle cycles ARE in the same project (definition of "in-cycle"), so this is technically safe — but for consistency, also use `cycleKey.split('-')[0]`. This keeps both fetches symmetric and prevents future bugs if "in-cycle" semantics change.

**Clickability — render layer in AioTestRunsSection.tsx:**

- **Lines 690-740: `ImpactedExecutionsList`.** Replace the `<td>` content for the cycle key column (line 720-722) and run ID column (line 723-725) with `<Link>` elements. Import `Link` from `react-router-dom`. Cycle project key derived inline per row.

**New page — taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx:**

- Mirror the structure of `AioCycleDetailPage` (see lines 53-100).
- `useParams<{ projectKey, cycleKey, runId }>()` — three URL params.
- One useQuery: `fetchAioTestRunDetail(jiraBaseUrl, token, projectKey, cycleKey, runId)`.
- Header: status chip + run ID + cycle key (as breadcrumb back-link) + executed date.
- Body: step table — reuse the existing `StepTable` component from `AioTestRunsSection.tsx` (it's not yet exported; extract it OR inline the step rendering). For simplicity, INLINE the step-row render here for v1 — the StepTable export refactor is a future cleanup.
- Skeleton: `AioCycleDetailSkeleton` is too cycle-specific; create a tiny inline skeleton OR reuse `AioTestRunsSkeleton`.
- Error: `ErrorState` component.

**Route — routes.tsx:**

- Add `const AioTestRunDetailPage = lazy(() => import('./dashboard/AioTestRunDetailPage'));` near line 23.
- Add `{ path: '/aio-cycle/:projectKey/:cycleKey/run/:runId', element: withLazy(AioTestRunDetailPage) },` after the existing cycle-detail entry on line 53.

</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 — Fix cross-project detail fetch (status bug)</name>
  <files>taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx, taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx</files>
  <behavior>
- Test 1 (RED): with sentinel-dominant cycle PROJ-CY-DOMINANT and a cross-cycle ref to `OTHER-CY-9` (different project key), the cross-cycle detail fetch is called with `projectKey="OTHER"` (NOT `PROJECT_KEY="PROJ"`). Test inspects `mockFetchRunDetail.mock.calls` for the cross-cycle call.
- Test 2: with a successful cross-project detail fetch returning `testRunStatus: { name: 'Passed' }` (mocked at the fetcher level), the impacted-execution row shows a PASS chip, not Not Run.
  </behavior>
  <action>
1. Add a test inside the existing `describe('Impacted executions list (Gap 1) + attachments grid on no-runs path (Gap 2)', …)` block (around line 549). Use the existing `makeImpactedCase` and `makeRunDetail` helpers. Provide a cross-cycle key with a DIFFERENT project prefix (e.g. `OTHER-CY-9`). Assert `mockFetchRunDetail` was called with `projectKey="OTHER"` for that row.
2. RED commit: add the test (failing). Then GREEN commit: change line 464 from `projectKey` to `runRef.cycleKey.split('-')[0]`. Also change the corresponding in-cycle fetch site for consistency.
  </action>
  <verify>
    <automated>cd /Users/user/Documents/Projects/taskflow/taskflow && ./node_modules/.bin/vitest run src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- New test asserts `mockFetchRunDetail.mock.calls.some(call => call[2] === 'OTHER')` is true.
- Existing tests still GREEN (the prior PROJECT_KEY assertions still hold because in-cycle cycle keys start with `PROJECT_KEY` too).
- `tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>
Cross-project impacted executions resolve their detail fetch correctly. Status chips will now reflect real run state.
  </done>
</task>

<task type="auto">
  <name>Task 2 — Build new AioTestRunDetailPage + wire route</name>
  <files>taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx, taskflow/src/routes/dashboard/AioTestRunDetailPage.test.tsx, taskflow/src/routes/routes.tsx</files>
  <action>
1. Create `taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx` mirroring `AioCycleDetailPage` structure (see file). Read 3 URL params; call `fetchAioTestRunDetail`; render header (status chip + run ID + cycle key back-link via `NavLink` to /aio-cycle/.../...) + step table.
2. For v1, inline the step table render (4 columns: #, Step, Expected, Actual + status chip). Don't refactor StepTable out of AioTestRunsSection yet.
3. Create a small test file covering: loading skeleton, success render with status chip + steps, error state on null detail.
4. Add lazy import + route in `routes.tsx` between line 53 (cycle detail) and line 54.
  </action>
  <verify_cmd>cd taskflow && ./node_modules/.bin/vitest run src/routes/dashboard/AioTestRunDetailPage.test.tsx --reporter=dot</verify_cmd>
  <acceptance_criteria>
- File `AioTestRunDetailPage.tsx` exists, exports default component.
- Route entry in routes.tsx matches `/aio-cycle/:projectKey/:cycleKey/run/:runId`.
- New test file passes with at least 2 tests (render success, error path).
- `tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>
New full-page route shows a single test run's detail (header + steps) by reading the URL params and fetching via the existing detail API.
  </done>
</task>

<task type="auto">
  <name>Task 3 — Make ImpactedExecutionsList rows clickable (two link targets)</name>
  <files>taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.tsx, taskflow/src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx</files>
  <action>
1. Import `Link` from `react-router-dom` in AioTestRunsSection.tsx.
2. Inside `ImpactedExecutionsList`, derive `cycleProjectKey = row.runRef.cycleKey.split('-')[0]` per row.
3. Wrap the cycle key text in `<Link to={`/aio-cycle/${cycleProjectKey}/${row.runRef.cycleKey}`} className="hover:underline">`.
4. Wrap the run ID text in `<Link to={`/aio-cycle/${cycleProjectKey}/${row.runRef.cycleKey}/run/${row.runRef.runId}`} className="hover:underline">`.
5. Update the prior comment that said "No click handlers — cycle/run navigation is a phase-53 concern" to reflect the new behavior.
6. Add 2 tests: (a) cycle-key cell has an `<a>` with the expected href; (b) run-id cell has an `<a>` with the expected href.
  </action>
  <verify_cmd>cd taskflow && ./node_modules/.bin/vitest run src/routes/dashboard/issue-detail/AioTestRunsSection.test.tsx --reporter=dot</verify_cmd>
  <acceptance_criteria>
- Two `<Link>` elements per row, with correct hrefs derived from cycleKey.
- Hrefs use the cycle-derived projectKey (not the issue's projectKey).
- Existing read-only ImpactedExecutionsList tests still pass (the visible text inside the link is unchanged).
  </acceptance_criteria>
  <done>
Cycle key + run ID cells navigate to the appropriate in-app detail pages. Cross-project navigation works correctly.
  </done>
</task>

<task type="auto">
  <name>Task 4 — Update 54-HUMAN-UAT.md</name>
  <files>.planning/phases/54-aio-on-issue-detail/54-HUMAN-UAT.md</files>
  <action>
Add a "Gap 5" entry in `## Gaps` documenting:
- The status bug (cross-project detail-fetch failure) with the verbatim user-shared diagnostic URL + response body
- The clickability scope expansion (two new link targets + new run detail page)
- Fix commits from this plan
Update the Summary block. Bump `re_uat_round` to 4 (this is a separate UAT round from the parser-fix one).
  </action>
  <verify_cmd>grep -c "fix_plan: \"54-11\"" .planning/phases/54-aio-on-issue-detail/54-HUMAN-UAT.md</verify_cmd>
  <acceptance_criteria>
- Gap 5 entry present with `fix_plan: "54-11"`.
- `re_uat_round: 4` in frontmatter.
  </acceptance_criteria>
  <done>
UAT scaffold reflects the cross-project status fix + clickability additions ready for round-4 verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 — Round-4 human UAT</name>
  <what-built>
- Cross-project impacted execution detail fetch now uses the cycle-derived project key. Status chips show real run state (PASS/FAIL/BLOCKED/NOT_EXECUTED) instead of always "Not Run".
- Cycle key cell → in-app cycle detail page link.
- Run ID cell → new in-app run detail page link (`/aio-cycle/:projectKey/:cycleKey/run/:runId`).
- New `AioTestRunDetailPage` component showing run status header + step table.
  </what-built>
  <how-to-verify>
1. Hot-reload should pick up changes. If not: restart `npm run tauri dev`.
2. Open the same VTE-* story from your round-3 report (the one with ESHOP-CY-759 impacted execution).
3. Verify:
   - (a) Each impacted execution row's status chip reflects the REAL run state (PASS/FAIL/BLOCKED). The previously-"Not Run" rows now show correctly.
   - (b) Hovering the cycle key cell shows a link cursor; clicking navigates to the existing AIO cycle detail page in-app.
   - (c) Hovering the run ID cell shows a link cursor; clicking navigates to a NEW run detail page in-app showing the run header + step content.
4. Test 4 (ROADMAP SC end-to-end on a happy-path issue) — re-run if pending.
  </how-to-verify>
  <resume-signal>
Type "approved" if status chips + both link targets work. Otherwise describe issues.
  </resume-signal>
</task>

</tasks>

<verification>

After Tasks 1-3 land and before Task 5 checkpoint:

```bash
cd /Users/user/Documents/Projects/taskflow/taskflow
./node_modules/.bin/tsc --noEmit
./node_modules/.bin/vitest run --reporter=dot
```

Expected: tsc exit 0; vitest exit 0 with at least 1027 tests passed (baseline 1023 + 4 new).

</verification>

<success_criteria>
Plan 54-11 is complete when ALL true:
1. Cross-project detail fetch uses the cycle-derived project key.
2. Impacted execution rows have two working link targets (cycle key + run ID).
3. New AioTestRunDetailPage renders status header + step table for a fetched run.
4. tsc clean; vitest suite GREEN with test count uplift.
5. User confirms status chips + click navigation work on the round-3 story.

After all of the above, Phase 54 closes. Next: `/gsd:verify-work 54` to mark the phase `verified`.
</success_criteria>

<output>
After completion, create `.planning/phases/54-aio-on-issue-detail/54-11-SUMMARY.md` per the standard template.
</output>