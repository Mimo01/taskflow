---
phase: 53
plan: "02"
type: execute
wave: 1
depends_on: ["53-00"]
files_modified:
  - taskflow/src/stores/pinned-tabs.store.ts
  - taskflow/src/components/app/PinnedTabStrip.tsx
autonomous: true
requirements:
  - AIOP-01
  - AIOP-02
  - AIOP-03

must_haves:
  truths:
    - "usePinnedTabsStore has a pinnedCycleMeta field (Record<string, {name, projectKey}>) with default value {}"
    - "setPinnedCycleMeta action stores cycle metadata under the given key"
    - "clearCycleMeta action removes the given key from pinnedCycleMeta"
    - "Store version is 1; migrate callback sets pinnedCycleMeta = {} when version < 1 (for users upgrading from v0)"
    - "Fresh-install stores initialize with pinnedCycleMeta: {} from the create() factory default — not from migration"
    - "PinnedTabStrip prop is named resolvedTabs (not resolvedIssues)"
    - "PinnedTabStrip accepts Map<string, IssueTab | CycleTab> where CycleTab has type='cycle', name, projectKey"
    - "Cycle tab renders FlaskConical icon + cycle key in font-mono text-[9px] + cycle name in text-[11px]"
    - "Issue tab rendering is unchanged (IssueTypeIcon + key + summary)"
    - "Strip aria-label is 'Pinned tabs' (was 'Pinned issues')"
    - "Loader2 spinner still shown for issue tabs with no resolved data"
    - "Drag/reorder/ghost/context-menu behavior is unchanged"
  artifacts:
    - path: "taskflow/src/stores/pinned-tabs.store.ts"
      provides: "Extended pinned tabs store with cycle metadata, version 1, migration"
      contains: "pinnedCycleMeta"
    - path: "taskflow/src/components/app/PinnedTabStrip.tsx"
      provides: "Extended tab strip supporting IssueTab and CycleTab discriminated union"
      contains: "resolvedTabs"
  key_links:
    - from: "taskflow/src/components/app/PinnedTabStrip.tsx"
      to: "taskflow/src/stores/pinned-tabs.store.ts"
      via: "type definitions imported (IssueTab/CycleTab used in props)"
      pattern: "ResolvedTab"
    - from: "taskflow/src/main.tsx"
      to: "taskflow/src/components/app/PinnedTabStrip.tsx"
      via: "resolvedTabs prop (renamed from resolvedIssues) — main.tsx wired in Wave 2"
      pattern: "resolvedTabs"
---

<objective>
Wave 1 (parallel with Plan 53-01): Extend the pinned-tabs store and refactor PinnedTabStrip to support cycle tabs alongside issue tabs. This plan owns the store + component layer only; main.tsx wiring is Wave 2.

Purpose: Delivers AIOP-01/02/03 infrastructure — cycle meta persistence (AIOP-03), cycle tab rendering (AIOP-01), and the prop rename that unblocks Wave 2 main.tsx wiring.
Output: Extended pinned-tabs.store.ts (version 0→1 with migration), refactored PinnedTabStrip.tsx (discriminated union prop, cycle tab render).
</objective>

<execution_context>
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/53-cycle-detail-header-pinning/53-CONTEXT.md
@.planning/phases/53-cycle-detail-header-pinning/53-RESEARCH.md
@.planning/phases/53-cycle-detail-header-pinning/53-PATTERNS.md
@.planning/phases/53-cycle-detail-header-pinning/53-UI-SPEC.md

<interfaces>
<!-- Key existing interfaces — extracted from codebase. -->

From taskflow/src/stores/pinned-tabs.store.ts (current full file — PATTERNS.md lines 330–419):
```typescript
interface PinnedTabsState {
  pinnedKeys: string[];
  togglePin: (key: string) => void;
  removePin: (key: string) => void;
  reorder: (fromIndex: number, toIndex: number) => void;
  isPinned: (key: string) => boolean;
}
// persist version: 0
// migrate: (persisted, _version) => persisted as PinnedTabsState  (stub — no real migration)
```

From taskflow/src/components/app/PinnedTabStrip.tsx (current props — PATTERNS.md lines 423–516):
```typescript
interface PinnedTabStripProps {
  pinnedKeys: string[];
  activeKey: string | null;
  onTabClick: (issueKey: string) => void;
  onTabClose: (issueKey: string) => void;
  onReorder: (fromIndex: number, toIndex: number) => void;
  resolvedIssues: Map<string, ResolvedIssue>;  // ← renamed to resolvedTabs in this plan
}
interface ResolvedIssue {
  summary: string;
  issueTypeName: string;
}
// aria-label="Pinned issues" (line ~201)  ← updated to "Pinned tabs"
// resolvedIssues.get(key) called at: ghost render (~line 162) and main tab render (~line 263)
// Tab rendering lines ~173–191 (ghost) and ~263–280 (main)
```

Target types after this plan (per D-03 from CONTEXT.md):
```typescript
type IssueTab = { type: 'issue'; summary: string; issueTypeName: string };
type CycleTab = { type: 'cycle'; name: string; projectKey: string };
type ResolvedTab = IssueTab | CycleTab;
// PinnedTabStripProps.resolvedTabs: Map<string, ResolvedTab>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend pinned-tabs.store.ts (version 0→1, cycle metadata)</name>
  <files>taskflow/src/stores/pinned-tabs.store.ts</files>
  <read_first>
    - taskflow/src/stores/pinned-tabs.store.ts — full file; read every line; understand the create() body structure (pinnedKeys default, togglePin, removePin, reorder, isPinned), the persist config (name, storage, version, migrate); this is the complete source of truth for this surgical edit
    - taskflow/src/stores/settings.store.ts — NOT modified here; read only to understand the sequential migration guard pattern (if (version < N) blocks) to ensure the pattern is mirrored correctly
    - taskflow/src/stores/pinned-tabs.store.test.ts — read current tests to understand the beforeEach reset pattern; confirm that new actions' tests (Wave 0 stubs) are in place as todos
    - .planning/phases/53-cycle-detail-header-pinning/53-RESEARCH.md — section "Current State of usePinnedTabsStore" (lines 114–156); read the exact changes required and the pitfall about fresh-install default
  </read_first>
  <action>
    Make FIVE surgical changes to `taskflow/src/stores/pinned-tabs.store.ts`. Do NOT rewrite the file — modify in place:

    CHANGE 1 — Extend PinnedTabsState interface:
    Add after `isPinned: (key: string) => boolean;`:
    - `pinnedCycleMeta: Record<string, { name: string; projectKey: string }>;`
    - `setPinnedCycleMeta: (key: string, meta: { name: string; projectKey: string }) => void;`
    - `clearCycleMeta: (key: string) => void;`

    CHANGE 2 — Add default in create() factory body:
    After `pinnedKeys: [],` add:
    - `pinnedCycleMeta: {},`
    This MUST be in the factory (not only in migration) to handle fresh installs that never run migrate.

    CHANGE 3 — Add action implementations in create() factory body:
    After `isPinned: (key) => get().pinnedKeys.includes(key),` add:
    - `setPinnedCycleMeta: (key, meta) => set((s) => ({ pinnedCycleMeta: { ...s.pinnedCycleMeta, [key]: meta } })),`
    - `clearCycleMeta: (key) => set((s) => { const next = { ...s.pinnedCycleMeta }; delete next[key]; return { pinnedCycleMeta: next }; }),`

    CHANGE 4 — Bump persist version:
    Change `version: 0` to `version: 1`.

    CHANGE 5 — Replace migrate stub with real migration:
    Change `migrate: (persisted, _version) => persisted as PinnedTabsState,` to:
    ```
    migrate: (persisted, version) => {
      const s = persisted as PinnedTabsState;
      if (version < 1) {
        s.pinnedCycleMeta = {};
      }
      return s;
    },
    ```

    After all changes, confirm: all existing action bodies are unchanged (togglePin filter logic, removePin filter logic, reorder splice logic, isPinned includes check).
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --reporter=verbose pinned-tabs.store 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/stores/pinned-tabs.store.ts` contains `pinnedCycleMeta: Record<string, { name: string; projectKey: string }>`  in both the interface AND the create() factory default
    - File contains `setPinnedCycleMeta` and `clearCycleMeta` action implementations
    - Persist config shows `version: 1`
    - Migrate callback has `if (version < 1) { s.pinnedCycleMeta = {}; }` guard
    - All 5 previously passing tests in `pinned-tabs.store.test.ts` still pass (togglePin, isPinned, removePin, reorder)
    - The store todos (from Wave 0 plan 53-00) remain as todos — do not implement them; they will be filled in later
  </acceptance_criteria>
  <done>pinned-tabs.store.ts has pinnedCycleMeta with default, setPinnedCycleMeta, clearCycleMeta, version 1, real migration; existing 5 tests green</done>
</task>

<task type="auto">
  <name>Task 2: Refactor PinnedTabStrip.tsx (discriminated union prop, cycle tab rendering)</name>
  <files>taskflow/src/components/app/PinnedTabStrip.tsx</files>
  <read_first>
    - taskflow/src/components/app/PinnedTabStrip.tsx — full file; read all 315 lines; identify exact line numbers for: lucide-react import block (line ~13), ResolvedIssue interface (lines ~43–46), resolvedIssues prop in PinnedTabStripProps (line ~40), function signature destructure (line ~79), ghost render resolvedIssues.get(key) call (line ~162), ghost content branch (lines ~173–191), aria-label (line ~200), main tab resolvedIssues.get(key) call (line ~262), main tab content (lines ~263–280); make all 8 surgical changes
    - .planning/phases/53-cycle-detail-header-pinning/53-PATTERNS.md — section "PinnedTabStrip.tsx (MODIFY)" (lines 422–516); read all 8 touch points and the exact code for the discriminated union render switch; use PATTERNS.md code as the target state for the render blocks
    - .planning/phases/53-cycle-detail-header-pinning/53-RESEARCH.md — section "Current State of PinnedTabStrip" (lines 162–205); read ghost render lines 173–191 description and main tab render lines 263–280 description
    - .planning/phases/53-cycle-detail-header-pinning/53-UI-SPEC.md — Cycle Tab in PinnedTabStrip section; confirms tab dimensions (h-9, max-w-[180px], rounded-t-md), active state classes, and the two-line key+name layout
  </read_first>
  <action>
    Make EIGHT surgical changes to `taskflow/src/components/app/PinnedTabStrip.tsx`. Do NOT rewrite; modify in place:

    CHANGE 1 — Add FlaskConical to lucide-react import:
    Find the existing lucide-react import block. Add `FlaskConical` to the destructured list in alphabetical position (between the existing icons). The exact icons currently imported: read the file to enumerate (ArrowLeftToLine, ArrowRightToLine, etc.) per PATTERNS.md lines 447–458.

    CHANGE 2 — Replace ResolvedIssue interface with discriminated union:
    Delete the `interface ResolvedIssue { summary: string; issueTypeName: string; }` block.
    Add in its place:
    - `type IssueTab = { type: 'issue'; summary: string; issueTypeName: string };`
    - `type CycleTab = { type: 'cycle'; name: string; projectKey: string };`
    - `type ResolvedTab = IssueTab | CycleTab;`

    CHANGE 3 — Rename prop in PinnedTabStripProps interface:
    Change `resolvedIssues: Map<string, ResolvedIssue>;` to `resolvedTabs: Map<string, ResolvedTab>;`.

    CHANGE 4 — Update function signature destructure:
    Change `resolvedIssues,` to `resolvedTabs,` in the function parameter destructure.

    CHANGE 5 — Update ghost render lookup:
    Change `const resolved = resolvedIssues.get(key);` to `const resolved = resolvedTabs.get(key);`
    (this is the lookup in the ghost/drag-preview render path).

    CHANGE 6 — Replace ghost content branch with discriminated union switch:
    Replace the existing ghost tab content (the block that renders IssueTypeIcon + text when resolved exists, and Loader2 when it doesn't) with the three-way switch from PATTERNS.md lines 485–506:
    - `resolved?.type === 'cycle'`: FlaskConical icon + div flex-col (key in font-mono text-[9px] text-muted-foreground/60, name in text-[11px] leading-tight truncate)
    - `resolved?.type === 'issue'`: IssueTypeIcon + div flex-col (key + summary, same layout as current)
    - fallback (no resolved data): Loader2 + key span (existing loading state for issue tabs only)

    CHANGE 7 — Update main tab render lookup:
    Change `const resolved = resolvedIssues.get(key);` to `const resolved = resolvedTabs.get(key);`
    (this is the lookup in the main tab render path, not the ghost path).

    CHANGE 8 — Replace main tab content branch with same discriminated union switch:
    Apply the same three-way switch as CHANGE 6 to the main tab content area. The tab content structure (outer div with icon + text div) is identical for ghost and main renders per PATTERNS.md.

    BONUS — Update aria-label:
    Find `aria-label="Pinned issues"` and change to `aria-label="Pinned tabs"`.

    Verify: all drag state, pointer event handlers (onPointerDown, onPointerMove, onPointerUp), drop target logic, context menu items, and tab dimension classes (h-9, max-w-[180px], etc.) are UNTOUCHED.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --reporter=verbose PinnedTabStrip 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `taskflow/src/components/app/PinnedTabStrip.tsx` no longer contains `resolvedIssues` anywhere (grep returns 0 matches)
    - File contains `resolvedTabs: Map<string, ResolvedTab>` in the props interface
    - File contains `type IssueTab`, `type CycleTab`, `type ResolvedTab`
    - File contains `FlaskConical` in the lucide-react import
    - File contains `aria-label="Pinned tabs"` (not "Pinned issues")
    - `resolved?.type === 'cycle'` appears twice (ghost render + main render)
    - `resolved?.type === 'issue'` appears twice
    - The Loader2 fallback (no resolved data) still exists in both render paths
    - `cd taskflow && npx tsc --noEmit --skipLibCheck 2>&1 | grep PinnedTabStrip | head -5` — zero errors on this file (note: main.tsx will have an error until Wave 2 updates the prop call site)
    - PinnedTabStrip test stubs (from Wave 0) still run as todos without crashing
  </acceptance_criteria>
  <done>PinnedTabStrip.tsx uses resolvedTabs with discriminated union; cycle tabs render FlaskConical + key + name; aria-label updated; no behavior changes to drag/reorder/context-menu</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store → Tauri JSON file | pinnedCycleMeta persisted to pinned-tabs.json via createTauriStorage |
| Store migration → persisted data | v0→v1 migration reads arbitrary persisted state shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-02-01 | Tampering | Zustand persist migration | mitigate | `if (version < 1) { s.pinnedCycleMeta = {}; }` initializes to safe empty object; no user-supplied data enters migration path |
| T-53-02-02 | Information Disclosure | pinned-tabs.json | accept | File written to Tauri app data directory (sandboxed); cycle names stored at pin time are user-visible data, not secrets |
</threat_model>

<verification>
After both tasks:

```
cd taskflow && npm test -- --reporter=dot
```

Expected:
- `pinned-tabs.store.test.ts` — 5 existing tests GREEN, new todos remain as todos
- `PinnedTabStrip.test.tsx` — stub todos listed (no crashes)
- All other previously passing tests GREEN

TypeScript spot check on modified files:
```
cd taskflow && npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "pinned-tabs|PinnedTabStrip" | head -10
```
Note: main.tsx will report a TypeScript error on `resolvedIssues` prop until Wave 2 updates the call site — this is expected and acceptable at end of Wave 1.
</verification>

<success_criteria>
- usePinnedTabsStore has pinnedCycleMeta with default {}, setPinnedCycleMeta, clearCycleMeta actions, version 1, real migration guard (per D-02, D-08, AIOP-03)
- PinnedTabStrip accepts resolvedTabs Map with discriminated union; cycle tabs render FlaskConical + cycle key + cycle name (per D-03)
- All existing drag/reorder/ghost/context-menu behavior is unchanged
- No regressions in store tests
</success_criteria>

<output>
After completion, create `.planning/phases/53-cycle-detail-header-pinning/53-02-SUMMARY.md`
</output>
