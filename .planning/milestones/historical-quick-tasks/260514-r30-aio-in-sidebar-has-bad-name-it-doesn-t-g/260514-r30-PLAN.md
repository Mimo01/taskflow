---
phase: 260514-r30
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/components/app/sidebar-items.ts
  - taskflow/src/components/app/Sidebar.test.tsx
autonomous: true
requirements:
  - QT-260514-r30
must_haves:
  truths:
    - "Sidebar nav item under the Testing section reads 'AIO Cycles' (not 'AIO Projects')"
    - "Clicking the renamed item still navigates to /aio-project/{selectedAioProjectKey}"
    - "Sidebar tests assert the new label and continue to pass"
  artifacts:
    - path: taskflow/src/components/app/sidebar-items.ts
      provides: "SIDEBAR_NAV_ITEMS entry with id 'aio-projects' has label 'AIO Cycles'"
      contains: "label: 'AIO Cycles'"
    - path: taskflow/src/components/app/Sidebar.test.tsx
      provides: "Sidebar aioEnabled-gate tests updated to assert 'AIO Cycles'"
      contains: "AIO Cycles"
  key_links:
    - from: taskflow/src/components/app/sidebar-items.ts
      to: taskflow/src/components/app/Sidebar.tsx
      via: "SIDEBAR_NAV_ITEMS import — Sidebar.tsx renders {nav.label}"
      pattern: "SIDEBAR_NAV_ITEMS"
    - from: taskflow/src/components/app/Sidebar.tsx
      to: "/aio-project/:projectKey route"
      via: "navTo computed as `/aio-project/${encodeURIComponent(selectedAioProjectKey ?? '')}` for nav.id === 'aio-projects'"
      pattern: "/aio-project/"
---

<objective>
The sidebar's Testing section currently shows a nav item labeled "AIO Projects" (plural). Since Phase 55 (D-10) this item no longer points at an AIO projects list page — it deep-links to a single configured project's overview (`/aio-project/{selectedAioProjectKey}`), whose page header reads "Cycles — {projectKey}". The plural label is therefore misleading: there is no longer an "AIO Projects" page to land on. Rename the sidebar label to **"AIO Cycles"** so the label matches the destination (the AIO project's cycles overview) and the user's expectation when they click it.

Purpose: Fix the misleading sidebar label reported in quick task r30 ("AIO in sidebar has bad name, it doesn't go to 'AIO Projects' anymore"). Behavior of the link (deep-link to configured project, gating on aioEnabled + selectedAioProjectKey) is unchanged.

Output: One-word label change in `sidebar-items.ts` and the six co-located test assertions in `Sidebar.test.tsx` updated to match.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@taskflow/src/components/app/sidebar-items.ts
@taskflow/src/components/app/Sidebar.tsx
@taskflow/src/components/app/Sidebar.test.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From taskflow/src/components/app/sidebar-items.ts:
- `SidebarNavDef`: { id: string; label: string; path: string; iconName: string; section: string }
- `SIDEBAR_NAV_ITEMS`: SidebarNavDef[] — exported array; the entry with `id: 'aio-projects'` currently has `label: 'AIO Projects'` and `path: '#aio-dynamic'` (sentinel — Sidebar.tsx computes the real `to`).
- Section 'testing' is the visual group containing this item; that section gate (aioEnabled && selectedAioProjectKey) lives in Sidebar.tsx and is NOT changing.

From taskflow/src/components/app/Sidebar.tsx (lines 350–375):
- Renders `<span className={labelClass}>{nav.label}</span>` for each visible nav item.
- For `nav.id === 'aio-projects'`, computes `navTo = `/aio-project/${encodeURIComponent(selectedAioProjectKey ?? '')}`` — route behavior is unchanged by this fix.

From taskflow/src/components/app/Sidebar.test.tsx — six text assertions on the literal `'AIO Projects'` (lines 122, 136, 149, 162, 175, and the `it(...)` titles at lines 152 and 165). All must be updated to `'AIO Cycles'`. The route assertion at line 177 (`'/aio-project/PROJ'`) is unrelated and MUST be left as-is.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename sidebar label from "AIO Projects" to "AIO Cycles" and update tests</name>
  <files>taskflow/src/components/app/sidebar-items.ts, taskflow/src/components/app/Sidebar.test.tsx</files>
  <behavior>
    - Test: Sidebar renders the literal text "AIO Cycles" (not "AIO Projects") under the Testing section when aioEnabled=true AND selectedAioProjectKey is set.
    - Test: When aioEnabled is false, neither "Testing" nor "AIO Cycles" appears.
    - Test: When aioEnabled=true but selectedAioProjectKey is null, "AIO Cycles" does not appear.
    - Test: The renamed "AIO Cycles" NavLink href is still `/aio-project/<selectedAioProjectKey>` (route behavior is unchanged — only the visible label is changing).
    - No other sidebar items, sections, or behavior change.
  </behavior>
  <action>
    Step 1 (RED): Update all six occurrences of the literal string `'AIO Projects'` in `taskflow/src/components/app/Sidebar.test.tsx` to `'AIO Cycles'`. The occurrences are at lines 122, 136, 149, 152 (it-title), 162, 165 (it-title), and 175 (text lookup) — verify via `grep -n "AIO Projects" taskflow/src/components/app/Sidebar.test.tsx` before editing and confirm the count drops to 0 after. Do NOT change the route assertion `'/aio-project/PROJ'` at line 177 — only the human-readable label text is being renamed. Run `npm test -- src/components/app/Sidebar.test.tsx --run` and confirm at least one assertion now FAILS because production code still emits "AIO Projects" (this is the RED state).

    Step 2 (GREEN): In `taskflow/src/components/app/sidebar-items.ts`, in the `SIDEBAR_NAV_ITEMS` array, change the entry with `id: 'aio-projects'` from `label: 'AIO Projects'` to `label: 'AIO Cycles'`. Leave `id`, `path` ('#aio-dynamic' sentinel), `iconName` ('FlaskConical'), and `section` ('testing') unchanged — the `id` value is referenced elsewhere (settings store, Sidebar.tsx's `nav.id === 'aio-projects'` deep-link branch, preset visibility sets) and renaming it would silently break the Testing section gate and the deep-link to `/aio-project/{key}`. Re-run the Sidebar test file and confirm all assertions pass (GREEN).

    Rationale for "AIO Cycles" specifically: the destination page (`AioProjectOverviewPage` at `/aio-project/:projectKey`) renders the header `Cycles — {projectKey}` and lists AIO cycles for that project. "AIO Cycles" matches the destination content; the previous "AIO Projects" (plural) implied a list of projects that no longer exists after Phase 55 D-10.

    Verify no other files reference the old plural label outside tests: `grep -rn "AIO Projects" taskflow/src` should return only the existing test file (now updated) — there are no docs/strings to migrate.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- src/components/app/Sidebar.test.tsx --run 2>&1 | tail -30 && echo "--- grep check ---" && grep -rn "AIO Projects" src/components/app/ || echo "OK: no stale 'AIO Projects' references remain"</automated>
  </verify>
  <done>
    - `taskflow/src/components/app/sidebar-items.ts` has exactly one `label: 'AIO Cycles'` and zero `label: 'AIO Projects'`.
    - `taskflow/src/components/app/Sidebar.test.tsx` has zero occurrences of the string `'AIO Projects'` and matching occurrences of `'AIO Cycles'` at the same assertion sites.
    - `npm test -- src/components/app/Sidebar.test.tsx --run` reports all tests passing.
    - The route assertion `expect(link?.getAttribute('href')).toBe('/aio-project/PROJ')` is unchanged.
    - Manual check (post-merge, not blocking): with aioEnabled=true and an AIO project configured in Settings, the sidebar shows "AIO Cycles" under Testing, and clicking it navigates to `/aio-project/{key}` (the Cycles page).
  </done>
</task>

</tasks>

<verification>
- Unit tests pass: `cd taskflow && npm test -- src/components/app/Sidebar.test.tsx --run`
- Lint passes for touched files (project pre-commit hook will gate the commit).
- Grep confirms no remaining `'AIO Projects'` literal in `taskflow/src/components/app/`.
- `nav.id` is still `'aio-projects'` (only the user-visible `label` changed) — preserving the deep-link branch in `Sidebar.tsx` and the visibility-preset sets in `sidebar-items.ts` / `settings.store.ts`.
</verification>

<success_criteria>
- Sidebar nav item under the Testing section reads "AIO Cycles" instead of "AIO Projects".
- Clicking it still navigates to `/aio-project/{selectedAioProjectKey}` (Cycles page).
- All Sidebar tests pass with the new label.
- No other sidebar items or behavior changed.
</success_criteria>

<output>
After completion, create `.planning/quick/260514-r30-aio-in-sidebar-has-bad-name-it-doesn-t-g/260514-r30-SUMMARY.md` documenting:
- The label change (old → new) and the rationale (destination page is "Cycles — {projectKey}", Phase 55 D-10 removed the plural-projects page).
- That `nav.id` deliberately stayed `'aio-projects'` to preserve store references and the deep-link branch.
- Test file updates and pass count.
</output>
