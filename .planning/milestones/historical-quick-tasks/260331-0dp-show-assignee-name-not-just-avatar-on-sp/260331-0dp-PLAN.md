---
phase: quick
plan: 260331-0dp
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/StoryHeaderRow.tsx
autonomous: true
requirements: [quick-260331-0dp]
must_haves:
  truths:
    - "Assignee display name text is visible next to the avatar on story header rows"
    - "When no assignee is set, neither avatar nor name is shown"
    - "Name does not cause layout overflow — it truncates if too long"
  artifacts:
    - path: "taskflow/src/routes/dashboard/StoryHeaderRow.tsx"
      provides: "Assignee name text next to avatar"
      contains: "assigneeDisplayName"
  key_links: []
---

<objective>
Show the assignee's display name as text next to the avatar on sprint board story header rows.

Purpose: The avatar alone (added in quick-260331-039) is hard to identify at a glance — adding the name text makes it immediately clear who owns the story.
Output: Updated StoryHeaderRow with name label beside avatar.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/dashboard/StoryHeaderRow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add assignee display name text next to avatar</name>
  <files>taskflow/src/routes/dashboard/StoryHeaderRow.tsx</files>
  <action>
In StoryHeaderRow.tsx, update the assignee avatar block (lines 84-89) to also render the display name as text next to the avatar icon.

Current code (lines 85-89):
```tsx
{assigneeDisplayName && (
  <div className="shrink-0">
    <CachedAvatar url={assigneeAvatarUrl} name={assigneeDisplayName} size={20} />
  </div>
)}
```

Replace with:
```tsx
{assigneeDisplayName && (
  <div className="shrink-0 flex items-center gap-1.5">
    <CachedAvatar url={assigneeAvatarUrl} name={assigneeDisplayName} size={20} />
    <span className="text-xs text-muted-foreground truncate max-w-[120px]">{assigneeDisplayName}</span>
  </div>
)}
```

Key details:
- Use `text-xs text-muted-foreground` to keep it visually subtle (matches subtask count style)
- Use `truncate max-w-[120px]` to prevent long names from pushing other elements off-screen
- Use `gap-1.5` between avatar and name for a tight but readable pairing
- The `shrink-0` on the outer div is already present and prevents the whole block from collapsing; BUT since we now have a truncatable name inside, change the outer div to `shrink-0 min-w-0 flex items-center gap-1.5` — actually keep `shrink-0` so the avatar+name block doesn't shrink below its max-w. The truncate on the inner span handles overflow.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Assignee display name text appears next to avatar on story header rows. Text is muted, small, and truncates at 120px max width. No assignee = nothing rendered.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Visual: story headers with assignees show avatar + name text side by side
- Visual: long names truncate with ellipsis, do not break layout
- Visual: unassigned stories show no avatar or name
</verification>

<success_criteria>
Sprint board story header rows display the assignee's name as text next to their avatar image.
</success_criteria>

<output>
After completion, create `.planning/quick/260331-0dp-show-assignee-name-not-just-avatar-on-sp/260331-0dp-SUMMARY.md`
</output>
