---
phase: quick
plan: 260510-ju
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/package.json
  - taskflow/src-tauri/Cargo.toml
  - taskflow/src-tauri/tauri.conf.json
  - taskflow/CHANGELOG.md
autonomous: false
requirements: [release-1.7.2]
must_haves:
  truths:
    - "package.json version field reads 1.7.2"
    - "src-tauri/Cargo.toml [package] version reads 1.7.2"
    - "src-tauri/tauri.conf.json version reads 1.7.2"
    - "CHANGELOG.md has a ## [1.7.2] section at the top"
    - "Git tag v1.7.2 exists and is pushed to origin"
    - "GitHub Actions CI pipeline is triggered and building"
  artifacts:
    - path: "taskflow/CHANGELOG.md"
      provides: "Release notes for v1.7.2"
      contains: "## [1.7.2]"
  key_links:
    - from: "release.sh"
      to: "bump-version.mjs"
      via: "node scripts/bump-version.mjs 1.7.2"
      pattern: "chore: bump version to 1.7.2"
    - from: "git push origin v1.7.2"
      to: ".github/workflows/release-cross-platform.yml"
      via: "tag push trigger"
      pattern: "on: push: tags: v*"
---

<objective>
Release Taskflow v1.7.2.

Purpose: Ship the changes accumulated since v1.7.1 — draggable sidebar resize (phase 50), story points empty value fix, update-check log category, request body logging in dev tools, pre-commit quality gate, and CI release pipeline improvements.

Output: Version bumped to 1.7.2 across all version files, CHANGELOG.md updated, git tag v1.7.2 pushed, CI pipeline triggered to build macOS/Linux/Windows artifacts and publish the release.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mimo/Documents/Projects/taskflow/.planning/PROJECT.md
@/Users/mimo/Documents/Projects/taskflow/.planning/STATE.md

Current version: 1.7.1 (package.json, src-tauri/Cargo.toml, src-tauri/tauri.conf.json — all three in sync).

Release script: `taskflow/scripts/release.sh <version>` reads release notes from stdin, calls `bump-version.mjs` to update all three version files + CHANGELOG.md, commits, tags, and pushes to trigger CI.

bump-version.mjs updates:
- taskflow/package.json → `"version"` field
- taskflow/src-tauri/tauri.conf.json → `"version"` field
- taskflow/src-tauri/Cargo.toml → `version = "..."` in [package] section
- taskflow/CHANGELOG.md → prepends new ## [X.Y.Z] section

CI workflow (.github/workflows/release-cross-platform.yml) triggers on `v*` tag push and builds macOS universal, Linux x86_64, Windows x86_64, then creates the GitHub release on Mimo01/taskflow-releases.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose release notes from git log since v1.7.1</name>
  <files>taskflow/CHANGELOG.md (will be updated by bump-version.mjs in Task 2)</files>
  <action>
    Run `git log v1.7.1..HEAD --oneline` to get all commits since v1.7.1.

    Categorize commits into changelog sections. Use these groupings:

    **Added** — new capabilities visible to users:
    - Draggable sidebar resize (phase 50): users can drag the sidebar edge to resize it; drag handles on issue detail, MR detail, and releases detail side panels
    - Story points field now supports clearing to empty (no story points set)
    - Request body now logged in Dev Tools request log (POST/PUT visibility)
    - Separate "updater" log category in Dev Tools for update-check calls

    **Fixed** — bug fixes:
    - Pagination loop break when Jira returns empty issues with total > 0
    - versionId validated before JQL interpolation to prevent injection
    - GitLab milestone shows dash instead of "Loading" when no milestone matched
    - Sidebar prefetch timer cleared on unmount
    - useResizable syncs width when initialWidth changes after mount
    - Drag handle border color uses state-driven hover (not CSS :hover)
    - Issue detail panel pixel fallback computed correctly on first drag

    **Changed** — internal/tooling:
    - Release script simplified: CI now owns all builds and release publishing; release.sh is a thin version-bump-and-tag-push trigger
    - Pre-commit hook enforces full quality gate: biome check + all tests must pass before any commit

    Format the notes as Markdown following the existing CHANGELOG.md style (see top of CHANGELOG.md for reference). Do NOT include a date or version header — bump-version.mjs adds those automatically.

    Example format:
    ```
    ### Added
    - Draggable sidebar resize — drag the sidebar edge to any width; panel resize handles on issue detail, MR detail, and releases pages
    - Story points field can now be cleared to empty (no story points set)
    ...

    ### Fixed
    - ...

    ### Changed
    - ...
    ```

    Hold the composed notes in memory for use in Task 2. Do NOT write CHANGELOG.md directly — bump-version.mjs will prepend the entry.
  </action>
  <verify>Release notes composed and ready. All major feature areas covered (draggable resize, story points clear, dev tools improvements, bug fixes, tooling).</verify>
  <done>Release notes string composed covering Added / Fixed / Changed sections with accurate, user-facing descriptions.</done>
</task>

<task type="auto">
  <name>Task 2: Run release script to bump version, commit, tag, and push</name>
  <files>taskflow/package.json, taskflow/src-tauri/Cargo.toml, taskflow/src-tauri/tauri.conf.json, taskflow/CHANGELOG.md</files>
  <action>
    From the repository root `/Users/mimo/Documents/Projects/taskflow`, run:

    ```bash
    printf '%s' "<release_notes_from_task_1>" | bash taskflow/scripts/release.sh 1.7.2
    ```

    The script will:
    1. Pre-flight: verify semver format, clean working tree, git remote exists
    2. Version bump: update package.json, src-tauri/tauri.conf.json, src-tauri/Cargo.toml to 1.7.2; prepend ## [1.7.2] section to CHANGELOG.md; commit "chore: bump version to 1.7.2"; create annotated tag v1.7.2
    3. Push main and tag v1.7.2 to origin
    4. Print summary pointing to GitHub Actions

    IMPORTANT: The pre-commit hook runs `biome check` and `vitest run` before committing. This will take ~60-90 seconds. Do not interrupt.

    If the pre-commit hook fails (lint or test error), fix the error, stash or discard any partially-written files, and re-run the release command.

    After a successful push, the CI pipeline at https://github.com/Mimo01/taskflow/actions will start building. You do NOT need to wait for it to finish — the release is triggered once the tag push is confirmed.
  </action>
  <verify>
    <automated>git -C /Users/mimo/Documents/Projects/taskflow tag --list v1.7.2</automated>
    Confirm:
    - Output is `v1.7.2` (tag exists)
    - `grep '"version": "1.7.2"' taskflow/package.json` returns a match
    - `grep 'version = "1.7.2"' taskflow/src-tauri/Cargo.toml` returns a match
    - `grep '"version": "1.7.2"' taskflow/src-tauri/tauri.conf.json` returns a match
    - `grep '## \[1.7.2\]' taskflow/CHANGELOG.md` returns a match
    - `git -C /Users/mimo/Documents/Projects/taskflow log --oneline -1` shows "chore: bump version to 1.7.2"
  </verify>
  <done>
    v1.7.2 tag pushed to origin; CI pipeline triggered. All three version files read 1.7.2. CHANGELOG.md has the [1.7.2] entry at the top.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Version bumped to 1.7.2 across all files, CHANGELOG.md updated, git tag v1.7.2 pushed to origin, CI pipeline triggered on GitHub Actions to build macOS/Linux/Windows and publish the release to Mimo01/taskflow-releases.
  </what-built>
  <how-to-verify>
    1. Visit https://github.com/Mimo01/taskflow/actions — confirm a new workflow run appears for tag v1.7.2 with status "in progress" or "success"
    2. After CI completes (~15-20 min), visit https://github.com/Mimo01/taskflow-releases/releases/tag/v1.7.2 — confirm release exists with macOS (DMG + .app.tar.gz), Linux (AppImage + .deb), and Windows (NSIS exe + MSI) artifacts
    3. Verify latest.json is updated by checking the releases repo
  </how-to-verify>
  <resume-signal>Type "approved" when CI is confirmed running (or "released" when artifacts are published), or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
```bash
# All version files at 1.7.2
grep '"version"' /Users/mimo/Documents/Projects/taskflow/taskflow/package.json
grep 'version' /Users/mimo/Documents/Projects/taskflow/taskflow/src-tauri/tauri.conf.json | head -1
grep '^version' /Users/mimo/Documents/Projects/taskflow/taskflow/src-tauri/Cargo.toml

# Tag exists
git -C /Users/mimo/Documents/Projects/taskflow tag --list v1.7.2

# CHANGELOG entry
grep '## \[1.7.2\]' /Users/mimo/Documents/Projects/taskflow/taskflow/CHANGELOG.md
```
</verification>

<success_criteria>
- package.json, Cargo.toml, tauri.conf.json all read version 1.7.2
- CHANGELOG.md has a [1.7.2] section with Added / Fixed / Changed notes
- Annotated git tag v1.7.2 exists and is pushed to origin
- GitHub Actions CI run appears for v1.7.2 tag push
- (After CI) Artifacts published on Mimo01/taskflow-releases at tag v1.7.2
</success_criteria>

<output>
After completion, create `.planning/quick/260510-ju-release-1-7-2/260510-ju-SUMMARY.md` using the summary template.
</output>
