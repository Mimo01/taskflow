---
phase: quick
plan: 260317-uo1
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/notifications/NotificationPopover.tsx
  - taskflow/src/routes/notifications/NotificationDetail.tsx
  - taskflow/src/routes/notifications/NotificationPopover.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Clicking a GitLab notification closes the popover and navigates to the internal MR detail page"
    - "Clicking a Jira notification closes the popover and navigates to issue detail page"
    - "No inline notification detail panel ever expands below a notification row"
  artifacts:
    - path: "taskflow/src/routes/notifications/NotificationPopover.tsx"
      provides: "Simplified notification feed with direct navigation only"
  key_links:
    - from: "NotificationPopover.tsx"
      to: "/mr/:projectId/:iid"
      via: "onMRClick callback from AppLayout"
      pattern: "onMRClick"
---

<objective>
Remove the inline NotificationDetail expand-on-click behavior from notifications. All notification clicks should navigate directly: Jira items to issue detail, GitLab items to MR detail page. No inline detail panel.

Purpose: Simplify notification UX -- clicking always navigates to the relevant detail page rather than expanding an inline panel.
Output: Cleaned-up NotificationPopover with no inline detail, NotificationDetail.tsx deleted.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/notifications/NotificationPopover.tsx
@taskflow/src/routes/notifications/NotificationDetail.tsx
@taskflow/src/routes/notifications/NotificationRow.tsx
@taskflow/src/components/app/TopBar.tsx
@taskflow/src/main.tsx (handleMRClick, handleIssueClick, onNotifPopoverChange)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove inline NotificationDetail and simplify click handling</name>
  <files>taskflow/src/routes/notifications/NotificationPopover.tsx, taskflow/src/routes/notifications/NotificationDetail.tsx</files>
  <action>
In NotificationPopover.tsx:
1. Remove the `useState` for `selectedItemId` and `selectedItem` derivation (lines 60-71)
2. Remove the `handleDetailClose` function
3. Remove the import of `NotificationDetail`
4. Simplify `handleRowClick`: remove the fallback `setSelectedItemId(item.id)` branch. For Jira items, call `onIssueClick`. For GitLab items, call `onMRClick`. Both should markAsRead. If neither callback matches (edge case), do nothing (no inline detail).
5. In the JSX map, remove the conditional render of `<NotificationDetail>` below each row (lines 142-145). Each `sortedItems.map` entry should just render `<NotificationRow>` directly without the wrapper div.
6. Remove the `import { useState }` if no other useState remains.

Delete `taskflow/src/routes/notifications/NotificationDetail.tsx` entirely -- it is no longer used anywhere.

Also update the popover behavior: after a notification click that navigates (Jira or GitLab), the notification popover should close. The `onIssueClick` and `onMRClick` callbacks from AppLayout already handle navigation. To close the popover, add an `onClose` prop to NotificationPopover and call it after navigation clicks. In TopBar.tsx, pass `onClose={() => onNotifPopoverChange(false)}` to NotificationPopover. Update TopBar's NotificationPopoverProps and TopBarProps accordingly.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>NotificationDetail.tsx deleted. NotificationPopover has no inline detail expand. All clicks navigate via callbacks. Popover closes on navigation click. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update notification tests for simplified behavior</name>
  <files>taskflow/src/routes/notifications/NotificationPopover.test.tsx</files>
  <action>
Update NotificationPopover.test.tsx:
1. Remove any tests that assert NotificationDetail rendering or inline detail expand behavior.
2. Ensure existing tests for onIssueClick and onMRClick callbacks still pass.
3. Add or update a test verifying that the onClose callback is invoked when a Jira notification is clicked (navigates and closes popover).
4. Add or update a test verifying that the onClose callback is invoked when a GitLab notification is clicked (navigates and closes popover).
5. If any test imports NotificationDetail, remove those imports.

Run the test suite to confirm all pass.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/notifications/NotificationPopover.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All notification tests pass. No references to NotificationDetail remain. Tests verify popover close on navigation click.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npx vitest run src/routes/notifications/` passes all tests
3. NotificationDetail.tsx no longer exists on disk
4. No imports of NotificationDetail remain in the codebase
</verification>

<success_criteria>
- Clicking any GitLab notification navigates to /mr/:projectId/:iid and closes the popover
- Clicking any Jira notification navigates to /issue/:key and closes the popover
- No inline detail panel expands below notification rows
- NotificationDetail.tsx is deleted
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/quick/260317-uo1-redo-the-notifications-for-gitlab-ui-rem/260317-uo1-SUMMARY.md`
</output>
