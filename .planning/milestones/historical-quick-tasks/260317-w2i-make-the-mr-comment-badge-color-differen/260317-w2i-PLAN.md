---
phase: quick
plan: 260317-w2i
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/notifications/NotificationRow.tsx
autonomous: true
---

<objective>
Add distinct badge colors for MR comment notification types so they are visually distinguishable from the gray default fallback.

Purpose: Currently `mr-note`, `comment-mention`, and `gitlab-mention` notification types all fall through to the default gray `bg-muted text-muted-foreground` badge color, making them indistinguishable. Each comment/mention type should have its own color to help users quickly scan and identify notification types.

Output: Updated colorMap in NotificationRow.tsx with colors for all comment-related notification types.
</objective>

<context>
@taskflow/src/routes/notifications/NotificationRow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add distinct badge colors for MR comment notification types</name>
  <files>taskflow/src/routes/notifications/NotificationRow.tsx</files>
  <action>
In NotificationRow.tsx, update the `colorMap` object (around line 98) to add entries for the three comment/mention notification types that currently fall through to the gray default:

```
'mr-note': 'bg-indigo-100 text-indigo-700'        — MR comments (indigo to differentiate from jira-comment's violet)
'comment-mention': 'bg-pink-100 text-pink-700'     — Jira @mention in comments (pink stands out as "someone talked to you")
'gitlab-mention': 'bg-pink-100 text-pink-700'      — GitLab @mention in MR notes (same pink as comment-mention since both are mentions)
```

Color rationale and existing assignments for reference:
- pipeline-failure: red (danger)
- mr-approval: green (success)
- due-date-reminder: amber (warning)
- issue-assignment: blue (informational)
- issue-update: teal (change)
- jira-comment: violet (conversation)
- mr-note: indigo (MR conversation — close to violet but distinct)
- comment-mention / gitlab-mention: pink (direct attention / someone mentioned you)

This ensures every NotificationType in the union has an explicit color entry, and no type falls through to the gray default.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 9 notification types have explicit color entries in colorMap. MR comment badges (mr-note) show indigo, mention badges (comment-mention, gitlab-mention) show pink — visually distinct from each other and from other notification type badges.</done>
</task>

</tasks>

<verification>
- Open notification popover and confirm MR comment notifications show indigo badges
- Confirm mention notifications show pink badges
- Confirm no notification type falls through to gray default
</verification>

<success_criteria>
Every NotificationType value has a color entry in colorMap. MR comments are visually distinct from Jira comments and from mention notifications.
</success_criteria>
