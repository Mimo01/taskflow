---
phase: quick-260326-j2q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/components/app/Sidebar.tsx
  - taskflow/src/hooks/useSavedFilterSync.ts
  - taskflow/src/main.tsx
autonomous: true
requirements: [remove-saved-filters-sidebar]
must_haves:
  truths:
    - "Sidebar no longer shows the Saved Filters section"
    - "Saved filters store still gets populated for other consumers (dashboard widget, command palette, sprint board)"
    - "No unused imports remain in Sidebar.tsx"
  artifacts:
    - path: "taskflow/src/hooks/useSavedFilterSync.ts"
      provides: "Hook that fetches Jira favourite filters and syncs to saved filter store"
    - path: "taskflow/src/components/app/Sidebar.tsx"
      provides: "Sidebar without saved filters section"
  key_links:
    - from: "taskflow/src/main.tsx"
      to: "taskflow/src/hooks/useSavedFilterSync.ts"
      via: "hook call in AppLayout"
      pattern: "useSavedFilterSync"
---

<objective>
Remove the Saved Filters section from the sidebar while preserving data flow for other components that consume from the saved filter store.

Purpose: The sidebar saved filters list is no longer wanted in the sidebar UI.
Output: Clean sidebar without saved filters, data fetching relocated to app layout level.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/components/app/Sidebar.tsx
@taskflow/src/main.tsx
@taskflow/src/stores/saved-filter.store.ts

<interfaces>
<!-- The saved filter store is consumed by multiple components beyond the sidebar -->
<!-- SavedFiltersWidget, CommandPalette, SprintBoardTab all read from useSavedFilterStore -->
<!-- The Sidebar currently fetches favourite filters and populates the store via setSavedFilters -->
<!-- This fetch+sync must be relocated so the store remains populated -->

From taskflow/src/stores/saved-filter.store.ts:
  setSavedFilters: (filters: JiraSavedFilter[]) => void;

From taskflow/src/services/jira/filters.ts:
  export async function fetchFavouriteFilters(baseUrl: string, token: string): Promise<JiraSavedFilter[]>;

Existing hook pattern in main.tsx (lines 443-446):
  useNotificationPolling();
  useUpdatePolling();
  useCustomFieldDiscovery();
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract saved filter sync into a dedicated hook and wire into app layout</name>
  <files>taskflow/src/hooks/useSavedFilterSync.ts, taskflow/src/main.tsx</files>
  <action>
Create taskflow/src/hooks/useSavedFilterSync.ts:
- Extract the favourite filters fetching logic currently in Sidebar.tsx (lines 64-79)
- The hook should:
  - Get jiraBaseUrl from useAuthStore
  - Use useQuery with queryKey ['jira-favourite-filters', jiraBaseUrl], calling fetchFavouriteFilters
  - Use useEffect to call setSavedFilters when data arrives
  - Use same staleTime (2 * 60 * 1000) and enabled (!!jiraBaseUrl) options
  - Import from the same sources as Sidebar currently does: @tanstack/react-query, @/services/jira/filters, @/services/stronghold, @/stores/auth.store, @/stores/saved-filter.store

In taskflow/src/main.tsx:
- Import useSavedFilterSync from '@/hooks/useSavedFilterSync'
- Call useSavedFilterSync() alongside the other hooks (after useCustomFieldDiscovery on line 446)
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>useSavedFilterSync hook exists and is called in AppLayout, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Remove saved filters rendering and unused imports from Sidebar</name>
  <files>taskflow/src/components/app/Sidebar.tsx</files>
  <action>
Remove from Sidebar.tsx:
1. Delete the Saved Filters JSX block (lines 158-163): the entire {jiraBaseUrl && !sidebarCollapsed && ...} block containing SavedFilterList
2. Remove these now-unused imports:
   - import { useQuery } from '@tanstack/react-query' (line 9)
   - import { SavedFilterList } from '@/components/SavedFilterList' (line 28)
   - import { fetchFavouriteFilters } from '@/services/jira/filters' (line 29)
   - import { readSecret } from '@/services/stronghold' (line 30)
   - import { useSavedFilterStore } from '@/stores/saved-filter.store' (line 32)
3. Remove useEffect import from react (keep useMemo and useState)
4. Remove the useQuery block (lines 67-75) that fetches favouriteFilters
5. Remove the useEffect block (lines 77-79) that syncs to store
6. Remove the destructured { setSavedFilters } from useSavedFilterStore (line 65)
7. Remove { jiraBaseUrl } from useAuthStore (line 64) — check if jiraBaseUrl is used elsewhere in the component first. If not used elsewhere, remove the entire useAuthStore import and usage.
8. Remove the useAuthStore import (line 31) if no longer referenced.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit --pretty 2>&1 | head -30 && npx vitest run src/components/SavedFilterList.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Sidebar.tsx has no saved filter imports, no saved filter JSX, no unused imports, TypeScript compiles, existing tests pass</done>
</task>

</tasks>

<verification>
- `cd taskflow && npx tsc --noEmit` compiles with no errors
- `cd taskflow && npx vitest run` all existing tests pass
- Visual: sidebar should show nav items and settings but no "Saved Filters" section at bottom
</verification>

<success_criteria>
- Sidebar no longer renders SavedFilterList component
- Sidebar.tsx has no unused imports related to saved filters
- Saved filter store is still populated via useSavedFilterSync hook in main.tsx
- All existing tests pass
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260326-j2q-remove-saved-filters-from-the-sidebar/260326-j2q-SUMMARY.md`
</output>
