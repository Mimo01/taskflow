# Quick Task 260330-r56: Replace sprint board drag-and-drop with right-click status transition popup

**Status:** Complete
**Date:** 2026-03-30

## What Changed

### Task 1: Add right-click context menu with transitions to TaskCard
- Added optional `transitions`, `onTransition`, `transitionError` props to TaskCard
- Wrapped card in `ContextMenu` from `@/components/ui/context-menu` when `onTransition` is provided
- Each transition renders as a colored badge using `statusCategoryBadgeClass`
- No context menu in non-board contexts (when `onTransition` absent)
- **Commit:** d47c424

### Task 2: Remove DnD from SprintBoardTab, wire context menu transitions
- Removed all `@dnd-kit/core` imports, `DndContext`, `DragOverlay`, `DroppableCell`, sensors, drag handlers
- Deleted `DraggableCard.tsx` (no longer needed)
- Deleted `BoardColumn.tsx` (unused — not imported anywhere)
- Created `handleTransition` with optimistic update + rollback (extracted from old `handleDragEnd`)
- Created `getTransitions` helper reading from queryClient cache
- Wired `TaskCard` directly in `VirtualizedSwimlanes` with transition props
- Kept pre-fetch transitions `useEffect` for context menu cache population
- **Commit:** d874078

### Follow-up fixes
- Wrapped `ContextMenuLabel` in `ContextMenuGroup` to fix Base UI `MenuGroupRootContext` error — **Commit:** c10dd71
- Removed `cursor-pointer` and `hover:border-primary/50` from task cards (no longer draggable) — **Commit:** 74ef958

## Files Modified
- `taskflow/src/routes/dashboard/TaskCard.tsx` — context menu with transitions
- `taskflow/src/routes/dashboard/SprintBoardTab.tsx` — DnD removed, context menu wired
- `taskflow/src/routes/dashboard/DraggableCard.tsx` — deleted
- `taskflow/src/routes/dashboard/BoardColumn.tsx` — deleted

## Verification
- TypeScript: no errors (`npx tsc --noEmit`)
- Tests: 839 passed, 86 test files passed
