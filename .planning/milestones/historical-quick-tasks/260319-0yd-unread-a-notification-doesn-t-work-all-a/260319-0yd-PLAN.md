---
phase: quick
plan: 260319-0yd
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/stores/notifications.store.ts
  - taskflow/src/routes/notifications/NotificationPopover.tsx
autonomous: true
requirements: [fix-unread-toggle, fix-button-styles]
must_haves:
  truths:
    - "Clicking the mail-open icon on a read notification marks it as unread (blue bg returns, unread count increments)"
    - "Clicking the check icon on an unread notification marks it as read (blue bg removed, unread count decrements)"
    - "The 'All' toggle button and 'Mark all read' button have identical padding and visual weight"
  artifacts:
    - path: "taskflow/src/stores/notifications.store.ts"
      provides: "markAsUnread action that removes id from readIds"
      contains: "markAsUnread"
    - path: "taskflow/src/routes/notifications/NotificationPopover.tsx"
      provides: "Toggle-aware read/unread callback per row"
  key_links:
    - from: "NotificationPopover.tsx"
      to: "notifications.store.ts"
      via: "markAsUnread called when row is already read"
      pattern: "markAsUnread"
---

<objective>
Fix two notification bugs: (1) "Mark as unread" does nothing because the store has no markAsUnread action and the popover always calls markAsRead regardless of read state, and (2) the "All" toggle button and "Mark all read" button have inconsistent padding.

Purpose: Restore expected read/unread toggle behavior and visual consistency in notification header.
Output: Working unread toggle + consistent header button styles.
</objective>

<context>
@taskflow/src/stores/notifications.store.ts
@taskflow/src/routes/notifications/NotificationPopover.tsx
@taskflow/src/routes/notifications/NotificationRow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add markAsUnread store action and wire toggle in popover</name>
  <files>taskflow/src/stores/notifications.store.ts, taskflow/src/routes/notifications/NotificationPopover.tsx</files>
  <action>
1. In notifications.store.ts:
   - Add `markAsUnread: (id: string) => void` to the NotificationsState interface (next to markAsRead)
   - Implement: `markAsUnread: (id) => set((s) => ({ readIds: s.readIds.filter((rid) => rid !== id) }))`

2. In NotificationPopover.tsx:
   - Destructure `markAsUnread` from `useNotificationsStore()` alongside markAsRead
   - In `renderGroupedRows`, change the `onMarkRead` callback from:
     `onMarkRead={() => markAsRead(item.id)}`
     to:
     `onMarkRead={() => readSet.has(item.id) ? markAsUnread(item.id) : markAsRead(item.id)}`
   - This makes the existing NotificationRow icon toggle (Check vs MailOpen) actually functional

3. Fix button style consistency in the header:
   - On the "Mark all read" Button (line 242), add `px-2` to className so it matches the All/Unread toggle button
   - Change: `className="text-xs h-7"` to `className="text-xs h-7 px-2"`
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
  - markAsUnread action exists in store, removes id from readIds array
  - Popover passes toggle-aware callback: read items get markAsUnread, unread items get markAsRead
  - "Mark all read" button has px-2 matching the All/Unread toggle button
  - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles with no errors
- Manually: open notification popover, click mail-open icon on a read notification — it should become unread (blue bg, count increments)
- Manually: confirm "All" and "Mark all read" buttons have same visual padding
</verification>

<success_criteria>
- Read notifications can be toggled back to unread via the MailOpen action icon
- Header buttons are visually consistent
</success_criteria>

<output>
After completion, create `.planning/quick/260319-0yd-unread-a-notification-doesn-t-work-all-a/260319-0yd-SUMMARY.md`
</output>
