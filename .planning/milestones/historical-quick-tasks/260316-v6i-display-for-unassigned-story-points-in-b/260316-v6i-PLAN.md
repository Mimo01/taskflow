---
phase: quick
plan: 260316-v6i
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/BacklogRow.tsx
  - taskflow/src/routes/dashboard/BacklogPage.tsx
autonomous: true
must_haves:
  truths:
    - "Unestimated backlog issues show a ? badge instead of --"
    - "Issue keys like PROJ-123 never wrap to a second line in the Key column"
  artifacts:
    - path: "taskflow/src/routes/dashboard/BacklogRow.tsx"
      provides: "? badge for null story points, nowrap on key cell"
    - path: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      provides: "Matching header width for nowrap key column"
  key_links: []
---

<objective>
Two small backlog table polish fixes: (1) show a `?` badge for unestimated story points instead of the plain `--` text, and (2) prevent issue key text from wrapping in narrow columns.

Purpose: Improve visual consistency and readability in the backlog table.
Output: Updated BacklogRow.tsx and BacklogPage.tsx.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/dashboard/BacklogRow.tsx
@taskflow/src/routes/dashboard/BacklogPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Show ? badge for null story points and prevent key wrapping</name>
  <files>taskflow/src/routes/dashboard/BacklogRow.tsx, taskflow/src/routes/dashboard/BacklogPage.tsx</files>
  <action>
In BacklogRow.tsx, lines 119-127 (story points cell):
- Replace the null branch (currently `<span className="text-xs text-muted-foreground">--</span>`) with a badge identical to the existing points badge but showing `?` instead. Use the same classes: `inline-flex items-center justify-center rounded border border-border bg-muted px-1.5 py-0.5 text-xs font-medium text-foreground`. This way estimated and unestimated issues both show a consistent badge — estimated shows the number, unestimated shows `?`.

In BacklogRow.tsx, line 84 (key cell):
- Add `whitespace-nowrap` to the td className so that keys like `PROJ-123` never wrap. Keep the existing `w-24` and other classes.

In BacklogPage.tsx, line 307 (Key column header th):
- Add `whitespace-nowrap` to match the row cell behavior. Keep existing classes.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Null story points render as a ? inside the same badge style as numeric points. Issue key cells have whitespace-nowrap to prevent text wrapping.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Visual: backlog rows with no story points show `?` in a bordered badge, not `--`
- Visual: long issue keys (e.g., LONGPROJECT-1234) stay on one line
</verification>

<success_criteria>
- Unestimated issues show ? badge matching the style of estimated point badges
- No key text wrapping in the backlog table
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/quick/260316-v6i-display-for-unassigned-story-points-in-b/260316-v6i-SUMMARY.md`
</output>
