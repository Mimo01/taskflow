---
phase: quick
plan: 260317-06k
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/components/app/RecentItemsPopover.tsx
  - taskflow/src/routes/notifications/NotificationPopover.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Recent items popover is the same width as the notifications popover"
    - "Notifications popover feed can show more items before scrolling"
  artifacts:
    - path: "taskflow/src/components/app/RecentItemsPopover.tsx"
      provides: "Wider recent items popover matching notification width"
    - path: "taskflow/src/routes/notifications/NotificationPopover.tsx"
      provides: "Taller max-height for notification feed"
  key_links: []
---

<objective>
Match the recent items popover width to the notifications popover width, and increase the notifications popover max scroll height.

Purpose: Visual consistency between the two header popovers, and more visible notifications without scrolling.
Output: Two files updated with consistent sizing.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/components/app/RecentItemsPopover.tsx
@taskflow/src/routes/notifications/NotificationPopover.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align popover sizes</name>
  <files>taskflow/src/components/app/RecentItemsPopover.tsx, taskflow/src/routes/notifications/NotificationPopover.tsx</files>
  <action>
    In RecentItemsPopover.tsx:
    - Change PopoverContent className from `w-80` to `w-96` (line 142) to match notification popover width
    - Change inner scroll container from `max-h-[400px]` to `max-h-[480px]` (line 150) to also match notifications height

    In NotificationPopover.tsx:
    - Change the feed scroll container from `max-h-[480px]` to `max-h-[600px]` (line 119) to show more notifications before scrolling
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && grep -n "w-96" src/components/app/RecentItemsPopover.tsx && grep -n "max-h-\[600px\]" src/routes/notifications/NotificationPopover.tsx && grep -n "max-h-\[480px\]" src/components/app/RecentItemsPopover.tsx</automated>
  </verify>
  <done>Recent items popover is w-96 (same as notifications), notifications max-height is 600px, recent items max-height is 480px (same as old notifications)</done>
</task>

</tasks>

<verification>
- Both popovers use w-96 width
- Notifications feed scrolls at 600px max-height
- Recent items feed scrolls at 480px max-height
- App builds without errors: `cd taskflow && npx tsc --noEmit`
</verification>

<success_criteria>
Recent items and notifications popovers have consistent w-96 width. Notifications max-height increased to 600px for more visible content.
</success_criteria>

<output>
After completion, create `.planning/quick/260317-06k-make-the-popover-size-of-recent-items-th/260317-06k-SUMMARY.md`
</output>
