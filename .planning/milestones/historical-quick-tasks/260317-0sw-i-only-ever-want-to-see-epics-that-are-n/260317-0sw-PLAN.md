---
phase: quick
plan: 260317-0sw
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/jira.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "EpicsPage shows only non-Done epics"
    - "Sprint board epic filter dropdown excludes Done epics"
    - "Backlog epic badges still show Done epics if a story references one"
  artifacts:
    - path: "taskflow/src/services/jira.ts"
      provides: "JQL filtering for epic queries"
      contains: "statusCategory != Done"
  key_links:
    - from: "fetchEpicsBasic"
      to: "EpicsPage + SprintBoardTab"
      via: "react-query cache key jira-epics-basic"
      pattern: "statusCategory.*Done"
    - from: "fetchBacklogView"
      to: "BacklogPage epic names"
      via: "issuekey in (...) JQL — no status filter (requested by ID)"
---

<objective>
Filter all epic listing queries to exclude Done epics, while preserving epic name/color resolution for stories that reference a Done epic.

Purpose: User never wants to see completed epics cluttering the EpicsPage or filter dropdowns. The only exception is when a story explicitly links to a Done epic — those epic names/colors must still resolve.

Output: Updated JQL in two functions within jira.ts
</objective>

<context>
@taskflow/src/services/jira.ts (lines 1682-1707: fetchEpicsBasic, lines 1753-1773: fetchEpicsWithEnrichment)
@taskflow/src/routes/dashboard/EpicsPage.tsx (consumer of fetchEpicsBasic)
@taskflow/src/routes/dashboard/SprintBoardTab.tsx (consumer of fetchEpicsBasic for epic name map)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add statusCategory != Done to epic listing JQL queries</name>
  <files>taskflow/src/services/jira.ts</files>
  <action>
In `fetchEpicsBasic` (line 1692), change the JQL from:
  `project = ${projectKey} AND issuetype = Epic ORDER BY updated DESC`
to:
  `project = ${projectKey} AND issuetype = Epic AND statusCategory != Done ORDER BY updated DESC`

In `fetchEpicsWithEnrichment` (line 1769), apply the same change:
  `project = ${projectKey} AND issuetype = Epic AND statusCategory != Done ORDER BY updated DESC`

Do NOT modify the `fetchBacklogView` epic fetch at line 1614 — that uses `issuekey in (...)` to resolve epic names/colors for stories that reference specific epics (including Done ones). This is the "explicitly requested by ID" exception.

Do NOT modify `fetchEpicEnrichmentMap` or `fetchEpicStories` — these also operate on explicit epic keys.

Jira's `statusCategory` has three values: "To Do", "In Progress", "Done". Using `statusCategory != Done` catches all terminal statuses (e.g. "Done", "Closed", "Released") regardless of the specific status name configured in the Jira workflow.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && grep -n "statusCategory != Done" src/services/jira.ts | wc -l | xargs test 2 -eq</automated>
  </verify>
  <done>Both fetchEpicsBasic and fetchEpicsWithEnrichment JQL queries contain "statusCategory != Done". fetchBacklogView epic-by-key query is unchanged. EpicsPage and sprint board filter dropdown will only show active epics. Backlog epic badges still resolve Done epic names.</done>
</task>

</tasks>

<verification>
- `grep "statusCategory != Done" taskflow/src/services/jira.ts` returns exactly 2 matches (fetchEpicsBasic + fetchEpicsWithEnrichment)
- `grep "issuekey in" taskflow/src/services/jira.ts` still present and unmodified (backlog epic name resolution)
- TypeScript compiles: `cd taskflow && npx tsc --noEmit`
</verification>

<success_criteria>
- EpicsPage no longer displays epics whose Jira status category is "Done"
- Sprint board epic name map and filter dropdown exclude Done epics
- Backlog view still resolves epic names/colors for stories linked to Done epics
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/quick/260317-0sw-i-only-ever-want-to-see-epics-that-are-n/260317-0sw-SUMMARY.md`
</output>
