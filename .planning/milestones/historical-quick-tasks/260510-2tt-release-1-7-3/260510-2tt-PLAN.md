---
phase: quick
plan: 260510-2tt
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/package.json
  - taskflow/src-tauri/Cargo.toml
  - taskflow/src-tauri/tauri.conf.json
  - taskflow/CHANGELOG.md
autonomous: false
requirements: [release-1.7.3]
must_haves:
  truths:
    - "package.json version field reads 1.7.3"
    - "src-tauri/Cargo.toml [package] version reads 1.7.3"
    - "src-tauri/tauri.conf.json version reads 1.7.3"
    - "CHANGELOG.md has a ## [1.7.3] section at the top"
    - "Git tag v1.7.3 exists and is pushed to origin"
    - "GitHub Actions CI pipeline is triggered and building"
  artifacts:
    - path: "taskflow/CHANGELOG.md"
      provides: "Release notes for v1.7.3"
      contains: "## [1.7.3]"
  key_links:
    - from: "release.sh"
      to: "bump-version.mjs"
      via: "node scripts/bump-version.mjs 1.7.3"
      pattern: "chore: bump version to 1.7.3"
    - from: "git push origin v1.7.3"
      to: ".github/workflows/release-cross-platform.yml"
      via: "tag push trigger"
      pattern: "on: push: tags: v*"
---

<objective>
Release Taskflow v1.7.3.

Purpose: Patch release fixing the updater restart command (wrong process plugin command after update) and CI pipeline regressions (macOS Rust target and Windows package-lock version mismatch).

Output: Version bumped to 1.7.3 across all version files, CHANGELOG.md updated, git tag v1.7.3 pushed, CI pipeline triggered to build macOS/Linux/Windows artifacts and publish the release.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Run release script to bump version, commit, tag, and push</name>
  <files>taskflow/package.json, taskflow/src-tauri/Cargo.toml, taskflow/src-tauri/tauri.conf.json, taskflow/CHANGELOG.md</files>
  <action>
    From the repository root `/Users/mimo/Documents/Projects/taskflow`, run:

    ```bash
    printf '### Fixed\n- Updater restart now uses the correct process plugin command (app.restart()) after applying an update\n- CI pipeline: fixed macOS Rust target and Windows package-lock version mismatch that caused build failures\n' | bash taskflow/scripts/release.sh 1.7.3
    ```

    The script will:
    1. Pre-flight: verify semver format, clean working tree, git remote exists
    2. Version bump: update package.json, src-tauri/tauri.conf.json, src-tauri/Cargo.toml to 1.7.3; prepend ## [1.7.3] section to CHANGELOG.md; commit "chore: bump version to 1.7.3"; create annotated tag v1.7.3
    3. Push main and tag v1.7.3 to origin

    IMPORTANT: The pre-commit hook runs `biome check` and `vitest run` before committing. This will take ~60-90 seconds. Do not interrupt.

    If the pre-commit hook fails, fix the error and re-run.

    After a successful push, the CI pipeline will start building. You do NOT need to wait for it to finish.
  </action>
  <verify>
    - `git tag --list v1.7.3` returns `v1.7.3`
    - `grep '"version": "1.7.3"' taskflow/package.json` returns a match
    - `grep 'version = "1.7.3"' taskflow/src-tauri/Cargo.toml` returns a match
    - `grep '"version": "1.7.3"' taskflow/src-tauri/tauri.conf.json` returns a match
    - `grep '## \[1.7.3\]' taskflow/CHANGELOG.md` returns a match
  </verify>
  <done>
    v1.7.3 tag pushed to origin; CI pipeline triggered. All three version files read 1.7.3. CHANGELOG.md has the [1.7.3] entry at the top.
  </done>
</task>

</tasks>

<success_criteria>
- package.json, Cargo.toml, tauri.conf.json all read version 1.7.3
- CHANGELOG.md has a [1.7.3] section with Fixed notes
- Annotated git tag v1.7.3 exists and is pushed to origin
- GitHub Actions CI run appears for v1.7.3 tag push
</success_criteria>

<output>
After completion, create `.planning/quick/260510-2tt-release-1-7-3/260510-2tt-SUMMARY.md` using the summary template.
</output>
