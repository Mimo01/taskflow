---
phase: quick
plan: 260331-0ir
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/StoryHeaderRow.tsx
autonomous: true
requirements: []
---

<objective>
Fix misaligned assignee section on sprint board story header rows. Status badges
("To Do", "In Progress", "Done") have different text lengths, causing the assignee
avatar+name to not line up across rows. Fix by giving the status badge a consistent
minimum width so all rows align.

Purpose: Visual consistency across story swimlane headers on the sprint board.
Output: StoryHeaderRow with aligned columns regardless of status text length.
</objective>

<context>
@taskflow/src/routes/dashboard/StoryHeaderRow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix status badge alignment with min-width</name>
  <files>taskflow/src/routes/dashboard/StoryHeaderRow.tsx</files>
  <action>
In StoryHeaderRow.tsx, fix the alignment by giving the status badge a fixed minimum
width so all rows line up consistently:

1. On the status badge `<span>` (line 93), add `min-w-[5.5rem] text-center` to the
   className. This ensures "To Do", "In Progress", and "Done" all occupy the same
   horizontal space. The min-w value of 5.5rem (88px) accommodates "In Progress"
   (the longest common status) comfortably.

2. Keep existing classes: `shrink-0 rounded px-1.5 py-0.5 text-xs font-medium` plus
   the dynamic statusStyle. Just append `min-w-[5.5rem] text-center`.

This is a single-line CSS change. The status badge already has `shrink-0` so it won't
compress. Adding min-width and centering the text ensures uniform column width across
all story header rows.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Status badges on all story header rows occupy the same width, causing assignee and subtask count columns to align consistently across rows regardless of status text length.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Visual: on sprint board, story headers with different statuses ("To Do", "In Progress", "Done") have their assignee avatars and subtask counts aligned in the same column position
</verification>

<success_criteria>
Status badges have uniform width across all story header rows, assignee sections are vertically aligned.
</success_criteria>

<output>
After completion, create `.planning/quick/260331-0ir-fix-misaligned-assignee-on-sprint-board-/260331-0ir-SUMMARY.md`
</output>
