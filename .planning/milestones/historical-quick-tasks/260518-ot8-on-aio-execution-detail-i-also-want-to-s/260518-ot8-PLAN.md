---
phase: 260518-ot8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx
  - taskflow/src/routes/dashboard/AioTestRunDetailPage.test.tsx
autonomous: true
requirements:
  - Show run defects on AIO execution detail page

must_haves:
  truths:
    - "When viewing an execution detail, linked defects are displayed below the step table"
    - "Each defect shows: issue key (linked), title, status pill, assignee, reporter, priority, severity"
    - "Defect section is absent (or shows 'No defects') when jiraDefectIDs is empty"
  artifacts:
    - path: taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx
      provides: "Defects section using useQueries + fetchJiraIssueByKey"
      contains: "jiraDefectIDs"
    - path: taskflow/src/routes/dashboard/AioTestRunDetailPage.test.tsx
      provides: "Test covering defects section rendering"
  key_links:
    - from: AioTestRunDetailPage
      to: fetchJiraIssueByKey
      via: "useQueries per numeric ID from run.jiraDefectIDs"
      pattern: "useQueries.*jiraDefectIDs"
---

<objective>
Show the linked Jira defects for a test run on the AIO execution detail page.

Purpose: The execution detail page already fetches `run.jiraDefectIDs` from `fetchAioTestRunDetail` but never renders them. Users need to see defects inline to understand why a run failed.
Output: A defects section in `AioTestRunDetailPage.tsx` below the step table. Defects are resolved via per-ID `useQueries` calls to `fetchJiraIssueByKey`, matching the exact pattern already working in `AioCycleDetailPage`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mimo/Documents/Projects/taskflow/.planning/STATE.md
@/Users/mimo/Documents/Projects/taskflow/taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx
@/Users/mimo/Documents/Projects/taskflow/taskflow/src/routes/dashboard/AioTestRunDetailPage.test.tsx

<interfaces>
<!-- Key patterns. Executor needs these — no codebase exploration required. -->

From AioTestRunDetailPage.tsx (current):
- detailQuery.data.run is typed as AioTestRun
- AioTestRun.jiraDefectIDs?: number[]  — raw numeric Jira IDs returned by fetchAioTestRunDetail
- AioTestRun.defects: [] — always empty (service-level resolution removed)
- The page already imports: useQuery from @tanstack/react-query, NavLink, useNavigate,
  useAioCredentials, useAuthStore, normalizeStatusLabel, aioRunStatusPillClass
- token and jiraBaseUrl are already available in component scope

From AioCycleDetailPage.tsx (reference pattern to copy):
```typescript
// useQueries pattern for per-defect issue resolution
const issueQueries = useQueries({
  queries: defectsWithTriggers.map((d) => ({
    queryKey: ['jira', jiraBaseUrl, 'issue-lightweight', d.defectKey],
    queryFn: () => fetchJiraIssueByKey(jiraBaseUrl!, token!, d.defectKey),
    enabled: !!jiraBaseUrl && !!token && !tokenLoading,
  })),
});
```

// fetchJiraIssueByKey import:
import { fetchJiraIssueByKey } from '@/services/jira';

// statusPillClass import (for Jira status category styling):
import { statusPillClass } from '@/lib/statusStyles';

// CachedAvatar import (for assignee/reporter avatars):
import { CachedAvatar } from '@/components/ui/cached-avatar';

// IssueTypeIcon — can copy the small component from AioCycleDetailPage.tsx
// (uses Bug/BookOpen/CornerDownRight/CheckSquare lucide icons, typed by typeName string)

// NavLink — already imported in AioTestRunDetailPage
// Skeleton from @/components/ui/skeleton — already available

// JiraIssue type:
import type { JiraIssue } from '@/services/jira/types';
// fields used: key, fields.summary, fields.status.name, fields.status.statusCategory.key,
//              fields.assignee.displayName, fields.assignee.avatarUrls['48x48'],
//              fields.reporter.displayName, fields.reporter.avatarUrls['48x48'],
//              fields.priority.name, fields.priority.iconUrl,
//              fields.customfield_13415?.value, fields.issuetype?.name
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add defects section to AioTestRunDetailPage with tests</name>
  <files>
    taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx,
    taskflow/src/routes/dashboard/AioTestRunDetailPage.test.tsx
  </files>
  <behavior>
    - When run.jiraDefectIDs is empty or undefined, the defects section either renders "No defects linked" or is absent entirely
    - When run.jiraDefectIDs has IDs (e.g. [186227, 186230]), useQueries issues one fetchJiraIssueByKey per numeric ID (cast to String)
    - Each resolved issue row renders: issue key as NavLink to /issue/{key}, summary text, status pill via statusPillClass, assignee with CachedAvatar, reporter with CachedAvatar, priority icon+name, severity from customfield_13415
    - Loading rows show Skeleton placeholders
    - The section is placed below the step table with a "Defects" heading
    - Test: mock fetchAioTestRunDetail to return run with jiraDefectIDs: [186227], mock @/services/jira fetchJiraIssueByKey to resolve with a JiraIssue stub; assert defect key and summary are rendered
    - Test: mock fetchAioTestRunDetail to return run with jiraDefectIDs: [] or undefined; assert no defect rows (empty state or section absent)
  </behavior>
  <action>
    Write the test cases first (RED), then implement.

    TEST SETUP: The test file already mocks `@/services/aio`. Add a second mock for `@/services/jira` that mocks `fetchJiraIssueByKey`. The mock JiraIssue stub should have: key='VTE-186227', fields.summary='Login regression', fields.status.name='Open', fields.status.statusCategory.key='new', fields.assignee=null, fields.reporter=null, fields.priority=null, fields.customfield_13415=null, fields.issuetype.name='Bug'. Use data-testid="aio-run-defects-section" on the defects section container for test targeting.

    IMPLEMENTATION in AioTestRunDetailPage.tsx:
    1. Add imports: `useQueries` (alongside existing `useQuery`), `fetchJiraIssueByKey` from `@/services/jira`, `CachedAvatar` from `@/components/ui/cached-avatar`, `Skeleton` from `@/components/ui/skeleton`, `statusPillClass` from `@/lib/statusStyles`, `type JiraIssue` from `@/services/jira/types`. Add lucide icons: `Bug`, `BookOpen`, `CornerDownRight`, `CheckSquare` if using IssueTypeIcon.
    2. After the existing `detailQuery` hook, derive `defectIds` from `detailQuery.data?.run.jiraDefectIDs ?? []` mapped to `String`.
    3. Add `useQueries` call: same pattern as AioCycleDetailPage — queryKey `['jira', jiraBaseUrl, 'issue-lightweight', defectId]`, queryFn `fetchJiraIssueByKey(jiraBaseUrl!, token!, defectId)`, enabled `!!jiraBaseUrl && !!token && !tokenLoading`.
    4. Render a defects section inside the `detailQuery.data ?` branch, placed after the existing step table div. Use `data-testid="aio-run-defects-section"`. Show heading "Defects". If `defectIds.length === 0`, render nothing (omit the section entirely — no empty state needed since the run just has no defects). If there are defects, render a table with columns: Key, Title, Status, Assignee, Reporter, Priority, Severity. Each row uses `issueQueries[i]` for data, shows Skeleton when loading. Key column uses `NavLink to={/issue/${resolvedKey}}`.
    5. Do NOT duplicate the full FilterPopover/sort machinery from AioCycleDetailPage — this is a single run's defects (typically 0-3 items), so a simple table without sort/filter is appropriate.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run src/routes/dashboard/AioTestRunDetailPage.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    All existing AioTestRunDetailPage tests pass plus new defects tests pass. The defects section is visible in the component when jiraDefectIDs are present, absent when empty.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component → Jira REST | Defect IDs from AIO response are used as Jira issue keys in fetchJiraIssueByKey — untrusted numeric IDs from an authenticated API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ot8-01 | Information Disclosure | DefectRow NavLink | accept | Defect IDs come from AIO (same auth domain); no PII cross-exposure beyond what Jira already shows |
| T-ot8-SC | Tampering | npm installs | accept | No new packages installed; all imports from existing dependencies |
</threat_model>

<verification>
Run: `cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run src/routes/dashboard/AioTestRunDetailPage.test.tsx`

All tests green. TypeScript compiles without errors: `npx tsc --noEmit 2>&1 | grep -i "AioTestRunDetailPage" | head -10`
</verification>

<success_criteria>
- AioTestRunDetailPage renders a "Defects" section showing linked Jira issues when run.jiraDefectIDs is non-empty
- Each defect row shows: key (NavLink), summary, status pill, assignee, reporter, priority, severity
- Section is absent when no defects are linked
- All existing tests pass; new defects tests pass
</success_criteria>

<output>
Create `.planning/quick/260518-ot8-on-aio-execution-detail-i-also-want-to-s/260518-ot8-SUMMARY.md` when done
</output>
