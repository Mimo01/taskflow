---
phase: quick-260331-wj7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/MergeRequestDetailPage.tsx
autonomous: true
requirements: [MR-LAYOUT-REORGANIZE]

must_haves:
  truths:
    - "MR detail page has a narrower sidebar (~288px) instead of 42% width"
    - "Discussions are visually prominent — positioned below description as primary content, not buried at bottom"
    - "Open in GitLab button is in the header area, not a separate action section at the bottom"
    - "MR state badge is visible inline in the header next to the IID"
    - "Left column sections have visual separation via lightweight panel styling"
    - "Commits are collapsible when there are more than 5"
    - "Page still looks good when there are no discussions"
  artifacts:
    - path: "taskflow/src/routes/dashboard/MergeRequestDetailPage.tsx"
      provides: "Reorganized MR detail page layout"
      contains: "w-72"
  key_links:
    - from: "MergeRequestDetailPage.tsx header"
      to: "MRStateBadge component"
      via: "inline rendering in header div"
      pattern: "MRStateBadge.*state.*mr\\.state"
    - from: "MergeRequestDetailPage.tsx left column"
      to: "DiscussionThreads component"
      via: "promoted position after description"
      pattern: "DiscussionThreads"
---

<objective>
Reorganize the MR detail page layout for a cleaner, more GitLab-like look and feel.

Purpose: The current layout has discussions buried at the bottom, an oversized 42% sidebar, no visual section separation, and the "Open in GitLab" button wasting space at the bottom. This makes the page feel inconsistent after adding discussion threads.

Output: A reorganized MergeRequestDetailPage.tsx with narrower sidebar, promoted discussions, inline header elements, card-like section styling, and collapsible commits.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/dashboard/MergeRequestDetailPage.tsx
@taskflow/src/routes/dashboard/DiscussionThreads.tsx

Note: There is NO Card component in this project (no card.tsx in components/ui/).
Use lightweight `rounded-lg border bg-card p-4` divs for section panels instead.
The `bg-card` Tailwind class comes from the shadcn theme and is already available.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure MR detail page header, sections, and sidebar width</name>
  <files>taskflow/src/routes/dashboard/MergeRequestDetailPage.tsx</files>
  <action>
Restructure MergeRequestDetailPage.tsx with these specific changes (do NOT touch DiscussionThreads.tsx internals):

**1. Header area — inline state badge + "Open in GitLab" button:**
Replace the current header (lines 179-182) and bottom action buttons (lines 238-249) with a single header block:
```
<div>
  <div className="flex items-center gap-2 mb-1">
    <p className="text-xs font-mono text-muted-foreground">!{mr.iid}</p>
    <MRStateBadge state={mr.state} draft={mr.draft} />
  </div>
  <div className="flex items-center gap-3">
    <h2 className="text-xl font-semibold leading-snug flex-1">{mr.title}</h2>
    <Button variant="outline" size="sm" className="gap-1.5 text-xs shrink-0"
      onClick={() => openUrl(mr.web_url)}>
      <ExternalLink className="size-3.5" />
      Open in GitLab
    </Button>
  </div>
</div>
```
Remove the entire bottom "Action buttons" section (the flex justify-end div at the end).

**2. Sidebar width — reduce from 42% to fixed w-72 (288px):**
Change `w-[42%]` to `w-72` on the right sidebar div (line 254). Also update the skeleton sidebar to use `w-72` instead of `w-[42%]` (line 525).

**3. Section panel styling — wrap each left-column section in a light panel:**
Wrap Description, Commits, Linked Jira Issues sections each in:
`<div className="rounded-lg border bg-card p-4">`
with the section h3 header inside using `<h3 className="text-sm font-medium text-muted-foreground mb-3">`.

**4. Reorder sections — promote discussions above commits:**
New left column order within `p-6 space-y-5`:
  1. Header (with inline badge + Open in GitLab)
  2. Description panel (in border card)
  3. Discussion Threads (if any) -- full width, NOT wrapped in a card (threads already have their own border styling)
  4. Commits panel (in border card, with collapsible behavior)
  5. Linked Jira Issues panel (if any, in border card)

**5. Collapsible commits — show first 5 by default:**
Add `useState` for `showAllCommits` (default false). Define `COMMIT_PREVIEW_COUNT = 5`.
Show `commits.slice(0, COMMIT_PREVIEW_COUNT)` when collapsed.
Add a toggle button below the list:
```
{commits.length > COMMIT_PREVIEW_COUNT && (
  <button type="button" onClick={() => setShowAllCommits(v => !v)}
    className="text-xs text-muted-foreground hover:text-foreground mt-2 transition-colors">
    {showAllCommits ? 'Show less' : `Show ${commits.length - COMMIT_PREVIEW_COUNT} more commits`}
  </button>
)}
```

**6. Discussion section separator:**
Before the DiscussionThreads section, add a visual separator if discussions exist:
```
{discussions && discussions.length > 0 && (
  <div className="pt-2">
    <DiscussionThreads discussions={discussions} />
  </div>
)}
```
The DiscussionThreads component already has its own h3 header and thread card styling -- do not double-wrap it.

**7. Keep all existing imports, queries, helper components (MetaRow, PersonDisplay, MRStateBadge, PipelineStatus, CommitRow) intact.** Only add `useState` to the existing import from 'react' (it already imports useEffect). Add the `useState` for showAllCommits inside the main component.

**Important:** The `bg-card` class is available from the shadcn/tailwind theme config. Do NOT create a new Card component file.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Sidebar is w-72 (288px fixed) instead of w-[42%]
    - MR state badge appears inline next to !iid in the header
    - "Open in GitLab" button is in the header row, bottom action section removed
    - Description, Commits, Linked Jira Issues wrapped in rounded-lg border bg-card panels
    - Discussions appear after description, before commits (promoted position)
    - Commits collapse to first 5 with "Show N more commits" toggle
    - No TypeScript errors
    - Page renders correctly with and without discussions
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Reorganized MR detail page with narrower sidebar, promoted discussions, inline header elements, section panels, and collapsible commits</what-built>
  <how-to-verify>
    1. Navigate to any MR detail page in the app
    2. Verify sidebar is noticeably narrower (288px instead of ~42% of viewport)
    3. Verify MR state badge (Open/Merged/Closed/Draft) shows inline next to the !iid number
    4. Verify "Open in GitLab" button is in the header row next to the title
    5. Verify Description, Commits, and Linked Jira Issues have card-like border panels
    6. Verify Discussions section appears between Description and Commits (not buried at bottom)
    7. If there are more than 5 commits, verify "Show N more commits" toggle works
    8. Verify the page looks good on an MR with no discussions
    9. Verify the overall look feels more consistent and GitLab-like
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `cd taskflow && npx tsc --noEmit`
- Visual inspection of MR detail page confirms reorganized layout
</verification>

<success_criteria>
MR detail page has a professional, GitLab-inspired layout with narrower sidebar, promoted discussions as primary content, inline status badge and action button in header, visually grouped sections, and collapsible commits list.
</success_criteria>

<output>
After completion, create `.planning/quick/260331-wj7-reorganize-mr-detail-page-layout-and-dis/260331-wj7-SUMMARY.md`
</output>
