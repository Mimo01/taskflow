---
phase: quick
plan: 260331-039
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/StoryHeaderRow.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.tsx
autonomous: true
requirements: [show-assignee-on-story-headers]
must_haves:
  truths:
    - "Story swimlane headers on the sprint board display the assignee avatar"
    - "Unassigned stories show no avatar (no blank space or broken image)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/StoryHeaderRow.tsx"
      provides: "Assignee avatar display in story header"
      contains: "CachedAvatar"
  key_links:
    - from: "taskflow/src/routes/dashboard/SprintBoardTab.tsx"
      to: "taskflow/src/routes/dashboard/StoryHeaderRow.tsx"
      via: "assignee props passed to StoryHeaderRow"
      pattern: "assigneeAvatarUrl.*assigneeDisplayName"
---

<objective>
Add assignee avatar to sprint board story swimlane headers.

Purpose: Users want to see at a glance who owns each story without expanding the swimlane.
Output: StoryHeaderRow shows a small CachedAvatar next to the story key when an assignee exists.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/dashboard/StoryHeaderRow.tsx
@taskflow/src/routes/dashboard/SprintBoardTab.tsx
@taskflow/src/components/ui/cached-avatar.tsx
@taskflow/src/services/jira/types.ts

<interfaces>
From taskflow/src/services/jira/types.ts:
```typescript
// JiraIssue.fields.assignee
assignee: { displayName: string; avatarUrls: { '48x48': string } } | null;
```

From taskflow/src/components/ui/cached-avatar.tsx:
```typescript
interface CachedAvatarProps {
  url: string | null | undefined;
  name: string;
  size?: 20 | 24 | 32 | 40;
  className?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add assignee avatar to StoryHeaderRow and wire from SprintBoardTab</name>
  <files>taskflow/src/routes/dashboard/StoryHeaderRow.tsx, taskflow/src/routes/dashboard/SprintBoardTab.tsx</files>
  <action>
In StoryHeaderRow.tsx:
1. Import CachedAvatar from '@/components/ui/cached-avatar'
2. Add two optional props to StoryHeaderRowProps:
   - assigneeAvatarUrl?: string | null
   - assigneeDisplayName?: string
3. Render a CachedAvatar (size=20) immediately after the key+summary button and before the status badge, only when assigneeDisplayName is truthy. Use the same pattern as TaskCard.tsx line 115-117. Wrap in a conditional: `{assigneeDisplayName && (<CachedAvatar url={assigneeAvatarUrl} name={assigneeDisplayName} size={20} />)}`
4. Add `shrink-0` class wrapper to prevent the avatar from being squished.

In SprintBoardTab.tsx:
Pass the two new props at ALL THREE StoryHeaderRow call sites (approx lines 291, 371, 929):
- assigneeAvatarUrl={story.fields.assignee?.avatarUrls['48x48']}
- assigneeDisplayName={story.fields.assignee?.displayName}

For the sticky header (line 929), use stickyHeader.story instead of story.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Story headers on the sprint board display a 20px assignee avatar when the story has an assignee. Unassigned stories show no avatar element. All three StoryHeaderRow instances (virtualizer, non-virtualizer, sticky) pass assignee data.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Visual check: story headers show small circular avatar next to story info
</verification>

<success_criteria>
Assignee avatars visible on sprint board story swimlane headers for assigned stories. No visual artifacts for unassigned stories.
</success_criteria>

<output>
After completion, create `.planning/quick/260331-039-on-sprint-board-story-headers-i-also-wan/260331-039-SUMMARY.md`
</output>
