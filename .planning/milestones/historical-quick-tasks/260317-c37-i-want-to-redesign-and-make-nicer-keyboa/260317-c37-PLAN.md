---
phase: quick
plan: 260317-c37
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/components/app/KeyboardShortcutsPanel.tsx
  - taskflow/src/lib/shortcuts.ts
autonomous: true
requirements: [quick-260317-c37]
must_haves:
  truths:
    - "Keyboard shortcuts modal has polished visual design with keycap-styled badges"
    - "Modifier key combos render as separate keycaps (e.g., Cmd + / as two badges)"
    - "Categories have icons and visual separators for clear hierarchy"
    - "Modal has search/filter, backdrop blur, smooth animations, and Esc footer hint"
  artifacts:
    - path: "taskflow/src/components/app/KeyboardShortcutsPanel.tsx"
      provides: "Redesigned keyboard shortcuts modal"
    - path: "taskflow/src/lib/shortcuts.ts"
      provides: "displayKeys field for split keycap rendering"
  key_links:
    - from: "KeyboardShortcutsPanel.tsx"
      to: "shortcuts.ts"
      via: "SHORTCUTS constant + displayKeys"
      pattern: "SHORTCUTS.*displayKeys"
---

<objective>
Redesign the keyboard shortcuts modal to be visually polished with keycap-styled key badges, category icons, search/filter, backdrop blur, smooth animations, and an Esc footer hint.

Purpose: The current modal is functional but plain — this makes it feel like a premium, well-crafted feature.
Output: Redesigned KeyboardShortcutsPanel.tsx with enhanced shortcuts.ts data.
</objective>

<context>
@taskflow/src/components/app/KeyboardShortcutsPanel.tsx
@taskflow/src/lib/shortcuts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add displayKeys to shortcuts data and redesign the modal</name>
  <files>taskflow/src/lib/shortcuts.ts, taskflow/src/components/app/KeyboardShortcutsPanel.tsx</files>
  <action>
**shortcuts.ts changes:**
Add an optional `displayKeys` field to `ShortcutEntry` — an array of strings representing individual keycaps for rendering (e.g., `['⌘', '/']` instead of single `'⌘/'`). Add displayKeys to every entry in SHORTCUTS. For single-key shortcuts like J, K, Enter, Esc — just `['J']`, `['K']`, etc. For combos: `['⌘', '/']`, `['⌘', 'K']`, `['⌘', '⇧', 'S']`, `['⌘', '⇧', 'B']`, `['⌘', '⇧', 'N']`, `['⌘', ',']`, `['⌘', 'B']`.

**KeyboardShortcutsPanel.tsx full redesign:**

1. **Backdrop blur**: Change backdrop from `bg-black/50` to `bg-black/40 backdrop-blur-sm` for modern glass effect.

2. **Open animation**: Add CSS transition classes. Use a simple approach: add `animate-in fade-in-0 zoom-in-95` to the popup (Tailwind animate utilities already available via shadcn).

3. **Layout**: Keep max-w-[500px]. Add a proper header section with a title "Keyboard Shortcuts" (text-base font-semibold) and a small search input below it (border rounded-md px-3 py-1.5 text-sm with Search icon from lucide-react). The search input filters shortcuts by description text (case-insensitive, local state).

4. **Category sections**: Reorder to: General, Navigation, Lists, Actions. Each category gets:
   - A Lucide icon next to the category name: General -> `Keyboard`, Navigation -> `Compass`, Lists -> `List`, Actions -> `Zap`
   - Category header styled: `flex items-center gap-2 text-xs font-semibold uppercase tracking-wider text-muted-foreground py-2`
   - A top border separator: `border-t border-border` (except first category)

5. **Shortcut rows**: Each row is `flex items-center justify-between py-1.5 px-2 rounded-md hover:bg-muted/50 transition-colors`. Description on left (text-sm). Keycaps on right.

6. **Keycap rendering**: Use the `displayKeys` array. Each key in the array gets its own keycap badge: `inline-flex items-center justify-center min-w-[24px] h-[22px] px-1.5 text-[11px] font-medium font-mono bg-gradient-to-b from-muted to-muted/80 text-muted-foreground border border-border/80 rounded-[4px] shadow-[0_1px_0_1px_rgba(0,0,0,0.05)]`. Between keycaps in a combo, no separator needed — just a small gap (`gap-1`).

7. **Footer**: Below the scrollable content area, add a fixed footer: `border-t border-border pt-3 mt-3 flex justify-center text-xs text-muted-foreground`. Content: "Press **Esc** to close" (the "Esc" rendered as an inline keycap badge same style as above).

8. **Structure**: The popup body (between header and footer) should be the scrollable area with `overflow-y-auto max-h-[50vh]` so header and footer stay fixed.

9. **Empty search state**: If search filter matches nothing, show centered text "No shortcuts found" in text-sm text-muted-foreground with py-8.

Do NOT use useHotkeys('escape') — @base-ui/react/dialog handles Escape natively.
Do NOT use createContext/useContext — use local useState for search filter.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Keyboard shortcuts modal renders with backdrop blur, smooth animation, search input, category icons, keycap-styled badges for each key in combos, hover states on rows, border separators between categories, and "Press Esc to close" footer
    - All existing shortcuts display correctly with split keycap rendering
    - Search filters shortcuts by description text
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- Open app, press Cmd+/ to open shortcuts modal
- Verify backdrop has blur effect
- Verify each modifier combo shows separate keycaps (e.g., Cmd and Shift and S as 3 separate badges)
- Verify category icons appear (Keyboard, Compass, List, Zap)
- Type in search box and verify filtering works
- Verify "Press Esc to close" footer is visible
- Press Esc to close, verify it works
</verification>

<success_criteria>
Keyboard shortcuts modal is visually polished with keycap badges, category icons, search, backdrop blur, and footer hint. TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260317-c37-i-want-to-redesign-and-make-nicer-keyboa/260317-c37-SUMMARY.md`
</output>
