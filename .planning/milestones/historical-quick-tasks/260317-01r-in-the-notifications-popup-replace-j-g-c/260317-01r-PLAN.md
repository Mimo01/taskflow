---
phase: quick
plan: 260317-01r
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/stores/notifications.store.ts
  - taskflow/src/services/notifications.ts
  - taskflow/src/routes/notifications/NotificationRow.tsx
autonomous: true
requirements: [quick-avatar-notifications]

must_haves:
  truths:
    - "Notification rows show the author's avatar image instead of J/GL letter circles"
    - "When avatar URL is missing or fails to load, initials from the author name are shown as fallback"
    - "Source (Jira/GitLab) is still visually distinguishable via the existing left border color"
  artifacts:
    - path: "taskflow/src/stores/notifications.store.ts"
      provides: "authorAvatarUrl field on NotificationItem type"
      contains: "authorAvatarUrl"
    - path: "taskflow/src/services/notifications.ts"
      provides: "Avatar URL extraction from Jira and GitLab API responses"
      contains: "authorAvatarUrl"
    - path: "taskflow/src/routes/notifications/NotificationRow.tsx"
      provides: "Avatar image with initials fallback replacing J/GL circles"
      contains: "img"
  key_links:
    - from: "taskflow/src/services/notifications.ts"
      to: "taskflow/src/stores/notifications.store.ts"
      via: "authorAvatarUrl field populated from API responses"
      pattern: "authorAvatarUrl"
    - from: "taskflow/src/routes/notifications/NotificationRow.tsx"
      to: "NotificationItem.authorAvatarUrl"
      via: "avatar image src or initials fallback"
      pattern: "authorAvatarUrl"
---

<objective>
Replace the J/GL letter circles in notification rows with the actual avatar of the person who triggered the notification. Jira API provides avatarUrls on comment/changelog authors, GitLab provides avatar_url on note authors. Fall back to initials from the author name when no URL is available or image fails to load.

Purpose: Make notifications feel personal and scannable -- a face is recognized faster than a letter.
Output: Updated NotificationItem type, populated avatar URLs from both APIs, avatar rendering in NotificationRow.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/stores/notifications.store.ts
@taskflow/src/services/notifications.ts
@taskflow/src/routes/notifications/NotificationRow.tsx

<interfaces>
<!-- Existing NotificationItem type (from notifications.store.ts) -->
```typescript
export interface NotificationItem {
  id: string;
  source: 'jira' | 'gitlab';
  entityTitle: string;
  author: string;        // "J.Smith"
  bodyPreview: string;
  fullBody: string;
  createdAt: string;
  url?: string;
  notificationType?: NotificationType;
  entityState?: string;
}
```

<!-- Jira API author objects include avatarUrls (not currently typed in notifications.ts casts) -->
<!-- changelog.histories[].author: { displayName, avatarUrls: { '48x48': string } } -->
<!-- comment.author: { displayName?, avatarUrls?: { '48x48': string } } -->

<!-- GitLab note.author: { id, name, username, avatar_url } -->

<!-- Existing avatar pattern from TaskCard.tsx (lines 100-124): -->
<!-- img with size-5 rounded-full, onError hides img and shows initials sibling -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add authorAvatarUrl to NotificationItem and populate from APIs</name>
  <files>taskflow/src/stores/notifications.store.ts, taskflow/src/services/notifications.ts</files>
  <action>
1. In `notifications.store.ts`, add `authorAvatarUrl?: string;` to the `NotificationItem` interface (after `author`). Also update the duplicate interface in `notifications.ts` (line 23-34) to match.

2. In `notifications.ts`, update ALL type cast objects to include avatar fields from the raw API data, then populate `authorAvatarUrl` on every `results.push(...)` call:

   **Jira changelog authors** (fetchIssueUpdates, ~line 113):
   - Expand the `author` type cast to: `author: { displayName: string; avatarUrls?: { '48x48'?: string } }`
   - When building NotificationItem, set `authorAvatarUrl: history.author.avatarUrls?.['48x48']`
   - This applies to both the issue-assignment push (~line 143) and the main issue-update push (~line 174)

   **Jira comment authors** (fetchCommentMentions + fetchAllComments, ~lines 216 and 283):
   - Expand comment `author` type cast to: `author?: { displayName?: string; avatarUrls?: { '48x48'?: string } }`
   - Set `authorAvatarUrl: comment.author?.avatarUrls?.['48x48']` on push

   **Jira due-date reminders** (fetchDueDateReminders):
   - No person triggers these, set `authorAvatarUrl: undefined`

   **GitLab notes** (fetchNewGitlabNotes, ~line 426):
   - The `note.author` already has `avatar_url` from the API. Set `authorAvatarUrl: note.author?.avatar_url`
   - Applies to both system-note pushes (~line 450) and regular-note pushes (~line 468)

   **GitLab approvals** (fetchGitlabApprovals, ~line 522):
   - The `user` object in approved_by has avatar_url. Expand type to include it: `user: { id: number; name: string; username: string; avatar_url?: string }`
   - Set `authorAvatarUrl: entry.user.avatar_url`

   **GitLab pipeline failures** (fetchGitlabPipelineFailures):
   - No person, set `authorAvatarUrl: undefined`
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>NotificationItem has authorAvatarUrl field, all API fetchers populate it from Jira avatarUrls['48x48'] or GitLab avatar_url, TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Replace J/GL circles with avatar images in NotificationRow</name>
  <files>taskflow/src/routes/notifications/NotificationRow.tsx</files>
  <action>
Replace the source icon section (lines 47-60, the J/GL letter circles) with an avatar display:

1. Add a helper function `getInitials(name: string): string` that returns first letter of first and last name (e.g., "John Smith" -> "JS", "Alice" -> "A", empty/Unknown -> "?").

2. Replace the existing `{item.source === 'jira' ? ... : ...}` block (the J/GL circles) with:
   - If `item.authorAvatarUrl` exists: render an `<img>` with `src={item.authorAvatarUrl}`, `alt={item.author}`, classes `w-6 h-6 rounded-full object-cover` (slightly larger than the old 5x5 for better face visibility). Add `onError` handler that hides the img and shows the initials sibling (same pattern as TaskCard.tsx).
   - Always render an initials fallback `<span>` with classes `w-6 h-6 rounded-full flex items-center justify-center text-[10px] font-medium` and a background color based on source: `bg-orange-100 text-orange-700` for Jira, `bg-purple-100 text-purple-700` for GitLab. This span is hidden when image loads, shown when no URL or image errors.
   - Use the same `onError` toggle pattern from TaskCard: img onError sets `display: none` on itself and `display: flex` on nextElementSibling.
   - When no `authorAvatarUrl`, set initial `className` so img is hidden and initials span is visible.

3. Keep the unread blue dot positioned relative to the new avatar (it already uses absolute positioning on the parent div, which stays the same).
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Notification rows show author avatar with source-colored initials fallback. Unread dot still visible. Source still distinguishable via left border color (orange=Jira, purple=GitLab).</done>
</task>

</tasks>

<verification>
- `cd taskflow && npx tsc --noEmit` passes clean
- Notification popup shows avatar images for notifications where author avatar URL is available
- Initials fallback shows for notifications without avatar URL (e.g., pipeline failures, due-date reminders)
- Initials background color matches source (orange for Jira, purple for GitLab)
- Unread blue dot still appears for unread notifications
- Left border color still indicates source
</verification>

<success_criteria>
- NotificationItem type includes authorAvatarUrl optional field
- All 6 notification fetchers populate authorAvatarUrl from API data where available
- NotificationRow renders avatar image with initials fallback instead of J/GL circles
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260317-01r-in-the-notifications-popup-replace-j-g-c/260317-01r-SUMMARY.md`
</output>
