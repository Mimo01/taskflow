---
phase: quick-260519-05a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [taskflow/src/services/jira.ts, taskflow/src/**, taskflow/src-tauri/src/**]
autonomous: false
requirements: [QUICK-260519-05a]
must_haves:
  truths:
    - "`npm run check` passes (biome lint + tsc --noEmit) with zero errors"
    - "`npm test` passes (vitest run) with zero failing tests"
    - "`cargo fmt --check` passes in src-tauri/ with no formatting diffs"
    - "`cargo clippy -- -D warnings` passes in src-tauri/ with zero warnings"
    - "`cargo test` passes in src-tauri/ with zero failing tests"
    - "The pre-existing uncommitted change to taskflow/src/services/jira.ts is preserved (not reverted by this work) unless it is itself the cause of a failure — in which case fix forward, do not blanket-revert"
  artifacts:
    - path: "taskflow/src/services/jira.ts"
      provides: "Existing modified file remains valid TypeScript that passes tsc and biome"
  key_links:
    - from: "taskflow/.husky/pre-commit"
      to: "npm run check && npm run test"
      via: "git commit"
      pattern: "npm run (check|test)"
---

<objective>
Ensure both the JS/TS stack (biome lint + tsc + vitest) and the Rust stack (cargo fmt + clippy + cargo test) are green across the repository. Currently `taskflow/src/services/jira.ts` has uncommitted changes whose effect on lint/typecheck/tests is unknown, and the Rust side has not been verified in this session.

Purpose: Restore a known-green baseline so the next commit (any quick task or phase) can pass the pre-commit hook (`npm run check && npm run test`) without surprise failures, and so Rust changes from recent work are also verified.

Output: All six commands listed in `must_haves.truths` exit 0. Any fixes required to get there are committed in a single focused commit at the end (unless the user opts out at the human-verify checkpoint).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mimo/Documents/Projects/taskflow/.planning/STATE.md
@/Users/mimo/Documents/Projects/taskflow/taskflow/package.json
@/Users/mimo/Documents/Projects/taskflow/taskflow/biome.json
@/Users/mimo/Documents/Projects/taskflow/taskflow/src-tauri/Cargo.toml
@/Users/mimo/Documents/Projects/taskflow/taskflow/.husky/pre-commit

<interfaces>
<!-- Useful project facts the executor must use directly. -->

Working directories:
- JS/TS workspace: `/Users/mimo/Documents/Projects/taskflow/taskflow`
- Rust workspace:  `/Users/mimo/Documents/Projects/taskflow/taskflow/src-tauri`

JS/TS commands (run from taskflow/):
- Lint + typecheck: `npm run check`   (biome check ./src && tsc --noEmit)
- Auto-fix lint only: `npm run fix`   (biome check --write ./src) — does NOT fix tsc errors
- Tests:            `npm test`        (vitest run)

Rust commands (run from taskflow/src-tauri/):
- Format check:  `cargo fmt --check`
- Format apply:  `cargo fmt`
- Lint strict:   `cargo clippy --all-targets -- -D warnings`
- Tests:         `cargo test`

Pre-existing state to be aware of:
- `taskflow/src/services/jira.ts` is modified (uncommitted, -34/+1 lines per `git diff --stat`).
  Per user memory `jira.ts dual-file gotcha`: all 60 imports use legacy `jira.ts`, NOT `jira/` modules.
  Always edit `jira.ts` until unified. Do not "fix" lint errors by routing imports to `jira/`.
- Memory `feedback_no_verify_lint.md`: `--no-verify` is acceptable when the pre-commit hook fails
  on PRE-EXISTING unrelated warnings. It is NOT acceptable for failures this plan introduces or
  fails to resolve. The bar for this plan is: all six commands exit 0 WITHOUT `--no-verify`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full JS/TS gate (biome + tsc + vitest) and fix any failures</name>
  <files>taskflow/src/**, taskflow/src/services/jira.ts</files>
  <action>
  From `/Users/mimo/Documents/Projects/taskflow/taskflow`, run `npm run check` first, then `npm test`. Capture each command's full output.

  Triage in this exact order:
  1. If `npm run check` fails on biome lint, try `npm run fix` (auto-fix) and re-run `npm run check`. Do NOT use `npm run fix` if the diff would touch files outside `taskflow/src/` or would revert the existing modifications to `taskflow/src/services/jira.ts` (read the diff before accepting). If auto-fix is unsafe, fix the specific lint findings by hand.
  2. If `npm run check` fails on `tsc --noEmit` (TypeScript errors), fix the types directly — never silence with `any`, `@ts-ignore`, or `@ts-expect-error` unless the underlying type is genuinely from an untyped 3rd-party module. Prefer narrowing, generics, or correct imports.
  3. If `npm test` (vitest) fails, fix the production code or the test depending on which one encodes the correct behavior. Do not skip or `.todo` failing tests to make the gate pass.
  4. Re-run `npm run check && npm test` and confirm both exit 0.

  Constraint on `taskflow/src/services/jira.ts`: the file is currently modified (uncommitted) and the dual-file gotcha applies. If a lint/type/test failure originates inside `jira.ts`, fix it in-place in `jira.ts` (the legacy file). Do not move logic into `taskflow/src/services/jira/*` to silence errors — that breaks the 60 existing imports.

  If failures appear to be pre-existing and unrelated to recent work (e.g. dead-code warnings in files untouched for months), note them in the summary but still fix them — this plan's goal is a clean green baseline, not "no worse than before".
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npm run check && npm test</automated>
  </verify>
  <done>Both `npm run check` and `npm test` exit 0 from `taskflow/`. No tests were skipped, `.skip`-ed, `.todo`-ed, or deleted to achieve this. `taskflow/src/services/jira.ts` retains its existing modifications (verifiable: `git diff --stat taskflow/src/services/jira.ts` still shows changes, unless the executor explicitly decided the file's existing changes were themselves the bug and documented why in the summary).</done>
</task>

<task type="auto">
  <name>Task 2: Run full Rust gate (cargo fmt + clippy + test) and fix any failures</name>
  <files>taskflow/src-tauri/src/**</files>
  <action>
  From `/Users/mimo/Documents/Projects/taskflow/taskflow/src-tauri`, run in order:

  1. `cargo fmt --check` — if it fails, run `cargo fmt` (no `--check`) to auto-format, then re-run `cargo fmt --check` to confirm clean.
  2. `cargo clippy --all-targets -- -D warnings` — fix each warning. Prefer the idiomatic fix (e.g. remove `clone()`, use `&str`, use `if let`). Do NOT add `#[allow(clippy::xxx)]` to silence warnings unless the lint is genuinely a false positive for the rust edition used here (`edition = "2021"`) — if you do allow, add a `// reason:` comment on the line above.
  3. `cargo test` — fix any failing tests by correcting the production code, not by deleting or `#[ignore]`-ing the test. If a test is genuinely flaky and unrelated, note it in the summary but still attempt a fix.

  After all three pass individually, run them in one chained command to confirm: `cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test`.

  Note: `cargo test` may need to compile the full Tauri workspace and can take several minutes on a cold target/ directory. That is expected — do not abort early.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow/src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test</automated>
  </verify>
  <done>All three rust commands exit 0 from `src-tauri/`. No tests were `#[ignore]`-ed and no clippy lints were `#[allow]`-ed without a `// reason:` comment justifying why.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies the fix set and approves commit</name>
  <what-built>
  All six gate commands exit 0 (npm run check, npm test, cargo fmt --check, cargo clippy -D warnings, cargo test). Any necessary fixes have been applied to source files. The pre-existing modification to `taskflow/src/services/jira.ts` is either preserved as-is or, if it was the cause of a failure, has been adjusted with a documented reason.
  </what-built>
  <how-to-verify>
  1. Run `git status` and `git diff` from the repo root and review every changed file. The diff should be limited to the fixes needed to get the gates green plus the pre-existing jira.ts change.
  2. Confirm no test files were deleted, `.skip`-ed, `.todo`-ed, or `#[ignore]`-ed.
  3. Confirm no biome rules were disabled in `taskflow/biome.json` and no `@ts-ignore`/`@ts-expect-error` were added without justification.
  4. Re-run the full gate one more time to be sure:
     - `cd taskflow && npm run check && npm test`
     - `cd taskflow/src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test`
  5. Decide: commit the fix set as a single commit (suggested message: `chore(260519-05a): make all js and rust tests and linters pass`), or instruct the executor to revise.
  </how-to-verify>
  <resume-signal>Type "approved" to commit, or describe what to change.</resume-signal>
</task>

</tasks>

<verification>
After human approval, the executor commits the changes (if any) with the husky pre-commit hook ENABLED (no `--no-verify`). The hook itself runs `npm run check && npm run test` and must pass — this is the final independent confirmation that the JS/TS gate is green. Rust gates are not in the pre-commit hook, so they are verified by the explicit commands in Task 2 and re-run in Task 3.
</verification>

<success_criteria>
- `cd taskflow && npm run check` exits 0
- `cd taskflow && npm test` exits 0
- `cd taskflow/src-tauri && cargo fmt --check` exits 0
- `cd taskflow/src-tauri && cargo clippy --all-targets -- -D warnings` exits 0
- `cd taskflow/src-tauri && cargo test` exits 0
- No tests skipped, ignored, or deleted to achieve green
- No lint rules disabled or `@ts-ignore`/clippy `#[allow]` added without an inline reason
- Pre-commit hook passes without `--no-verify` on the resulting commit (if any fixes were needed)
</success_criteria>

<output>
Create `.planning/quick/260519-05a-make-sure-all-js-and-rust-tests-and-lint/260519-05a-SUMMARY.md` when done, documenting:
- Which gates were already green vs which required fixes
- Each fix applied (file + one-line reason)
- Final status of `taskflow/src/services/jira.ts` (preserved / adjusted / why)
- Any pre-existing issues found but consciously deferred (with rationale)
</output>
