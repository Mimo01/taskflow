---
phase: 32-time-tracking-attachments-mentions
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/jira/types.ts
autonomous: true
gap_closure: true
requirements:
  - TIME-01
  - TIME-02
  - TIME-03
  - TIME-04
  - TIME-05
  - DETAIL-06
  - DETAIL-07
  - DETAIL-08
  - DETAIL-09

must_haves:
  truths:
    - "TypeScript compiles without errors (npx tsc --noEmit passes)"
    - "All 15 original type exports are present in types.ts"
    - "All 3 phase-32 type exports are present in types.ts"
    - "All 665 existing tests still pass"
  artifacts:
    - path: "taskflow/src/services/jira/types.ts"
      provides: "All Jira type definitions — original 15 + 3 new phase-32 types"
      exports: ["JiraUser", "JiraProject", "JiraIssue", "JiraFixVersion", "JiraTransition", "JiraComment", "JiraActiveSprint", "JiraIssueLink", "JiraAttachment", "JiraIssueDetail", "JiraProjectStatus", "CreatemetaField", "IssueLinkType", "BacklogViewData", "EpicEnriched", "JiraWorklog", "JiraAssignableUser", "ParsedDuration"]
  key_links:
    - from: "taskflow/src/services/jira/backlog.ts"
      to: "taskflow/src/services/jira/types.ts"
      via: "import type { BacklogViewData, JiraActiveSprint, JiraIssue }"
      pattern: "BacklogViewData.*JiraActiveSprint.*JiraIssue"
    - from: "taskflow/src/services/jira/worklogs.ts"
      to: "taskflow/src/services/jira/types.ts"
      via: "import type { JiraWorklog }"
      pattern: "JiraWorklog"
---

<objective>
Restore all original type exports in jira/types.ts that were destroyed by Phase 32 Plan 01 commit 41699f1.

Purpose: Plan 01 replaced types.ts (15 exports used by 10 domain modules) with a file containing only 3 new phase-32 types. This causes 20 TS2305 errors across backlog.ts, client.ts, comments.ts, epics.ts, fields.ts, issues.ts, links.ts, projects.ts, sprints.ts, transitions.ts, and versions.ts.

Output: A single merged types.ts file containing all 15 original types + 3 new phase-32 types (JiraWorklog, JiraAssignableUser, ParsedDuration). TypeScript compilation passes cleanly.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-time-tracking-attachments-mentions/32-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge original and phase-32 types into types.ts</name>
  <files>taskflow/src/services/jira/types.ts</files>
  <read_first>
    - taskflow/src/services/jira/types.ts (current broken file — only 3 exports)
    - taskflow/src/services/jira/backlog.ts (verify imports resolve after fix)
    - taskflow/src/services/jira/client.ts (verify imports resolve after fix)
  </read_first>
  <action>
Replace the entire contents of `taskflow/src/services/jira/types.ts` with a merged file that contains:

1. The file header comment (update to reflect it is the single source of truth for ALL Jira types including phase-32 additions)

2. ALL 15 original type exports from the Phase 31 version (commit 82d7d13). These are the types that were destroyed:
   - `JiraUser` (displayName, emailAddress, name)
   - `JiraProject` (id, key, name)
   - `JiraIssue` (id, key, fields with summary, status, assignee, customfield_10016, issuetype, description, parent, subtasks, timetracking, index signature)
   - `JiraFixVersion` (id, name, releaseDate, released, description)
   - `JiraTransition` (id, name, to)
   - `JiraComment` (id, author, body, created, updated)
   - `JiraActiveSprint` (id, name, state, startDate, endDate, goal, originBoardId)
   - `JiraIssueLink` (id, type, inwardIssue, outwardIssue)
   - `JiraAttachment` (id, filename, content, thumbnail, mimeType)
   - `JiraIssueDetail` (id, key, fields with all properties including changelog from Phase 31)
   - `JiraProjectStatus` (id, name, statusCategory)
   - `CreatemetaField` (fieldId, name, required, autoCompleteUrl, schema)
   - `IssueLinkType` (id, name, inward, outward)
   - `BacklogViewData` (sprints, backlog, epicNames, epicColors)
   - `EpicEnriched` (key, epicName, summary, status, assignee, totalStories, doneStories, totalPoints, color)

3. The 3 NEW phase-32 type exports appended at the end, after the original types:
   - `JiraWorklog` (id, author with displayName/name/avatarUrls, updateAuthor, timeSpent, timeSpentSeconds, started, created, updated, comment)
   - `JiraAssignableUser` (displayName, name, key, avatarUrls with 48x48/24x24/16x16)
   - `ParsedDuration` (seconds, display)

IMPORTANT: The `JiraIssueDetail` interface MUST include the `changelog` property that was added in Phase 31 (commit 82d7d13). This has the shape: `changelog?: { histories: Array<{ id, created, author, items }> }`.

After writing the file, run:
- `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit` to verify zero TS2305 errors from domain modules
- `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run` to verify all 665 tests still pass
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | grep -c "TS2305" | xargs test 0 -eq && echo "PASS: No TS2305 errors" || echo "FAIL: TS2305 errors remain"</automated>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface" taskflow/src/services/jira/types.ts` returns 18 (15 original + 3 new)
    - `grep "JiraUser" taskflow/src/services/jira/types.ts` finds the interface
    - `grep "JiraProject" taskflow/src/services/jira/types.ts` finds the interface
    - `grep "JiraIssue " taskflow/src/services/jira/types.ts` finds the interface
    - `grep "JiraIssueDetail" taskflow/src/services/jira/types.ts` finds the interface
    - `grep "BacklogViewData" taskflow/src/services/jira/types.ts` finds the interface
    - `grep "EpicEnriched" taskflow/src/services/jira/types.ts` finds the interface
    - `grep "JiraWorklog" taskflow/src/services/jira/types.ts` finds the interface
    - `grep "JiraAssignableUser" taskflow/src/services/jira/types.ts` finds the interface
    - `grep "ParsedDuration" taskflow/src/services/jira/types.ts` finds the interface
    - `grep "changelog" taskflow/src/services/jira/types.ts` finds the Phase 31 changelog property
    - `npx tsc --noEmit` produces zero TS2305 errors
    - `npx vitest run` shows 665 tests passing
  </acceptance_criteria>
  <done>types.ts contains all 18 exported interfaces (15 original + 3 phase-32). TypeScript compiles without TS2305 errors. All 665 tests pass.</done>
</task>

</tasks>

<verification>
- `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit` exits cleanly (no TS2305 errors in jira/ domain modules)
- `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run` shows all tests passing
- `grep -c "export interface" taskflow/src/services/jira/types.ts` returns 18
</verification>

<success_criteria>
- TypeScript compilation passes without TS2305 errors from the 10 affected domain modules
- All 665 existing tests continue to pass
- types.ts exports all 18 interfaces (verified by grep)
</success_criteria>

<output>
After completion, create `.planning/phases/32-time-tracking-attachments-mentions/32-05-SUMMARY.md`
</output>
