---
phase: 33-board-sprint-filters
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/SprintGoalBanner.test.tsx
  - taskflow/src/services/jira/board-config.test.ts
  - taskflow/src/routes/dashboard/QuickFilterChipRow.test.tsx
  - taskflow/src/routes/dashboard/BulkActionBar.test.tsx
  - taskflow/src/services/jira/filters.test.ts
  - taskflow/src/components/SavedFilterList.test.tsx
autonomous: true
requirements:
  - BOARD-01
  - BOARD-02
  - BOARD-03
  - BOARD-04
  - BOARD-05
  - BOARD-06
  - BOARD-07
  - FILT-01
  - FILT-02
  - FILT-03

must_haves:
  truths:
    - "Test stub files exist for all Phase 33 components and services before implementation begins"
    - "Each test file has at least one describe block with pending/skipped test cases matching requirements"
    - "Running vitest produces no errors from the stub files"
  artifacts:
    - path: "taskflow/src/routes/dashboard/SprintGoalBanner.test.tsx"
      provides: "Test stubs for BOARD-01 sprint goal banner"
    - path: "taskflow/src/services/jira/board-config.test.ts"
      provides: "Test stubs for BOARD-02 board quick filters service"
    - path: "taskflow/src/routes/dashboard/QuickFilterChipRow.test.tsx"
      provides: "Test stubs for BOARD-03 label filter chips"
    - path: "taskflow/src/routes/dashboard/BulkActionBar.test.tsx"
      provides: "Test stubs for BOARD-04 through BOARD-07 bulk operations"
    - path: "taskflow/src/services/jira/filters.test.ts"
      provides: "Test stubs for FILT-01 and FILT-03 filter CRUD"
    - path: "taskflow/src/components/SavedFilterList.test.tsx"
      provides: "Test stubs for FILT-02 saved filter list"
  key_links: []
---

<objective>
Create test stub files for all Phase 33 components and services (Wave 0 Nyquist compliance).

Purpose: Every implementation task in Plans 01-05 needs a pre-existing test file to verify against. These stubs define the behavioral contract before code is written.
Output: 6 test stub files with pending test cases.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-board-sprint-filters/33-CONTEXT.md
@.planning/phases/33-board-sprint-filters/33-RESEARCH.md
@.planning/phases/33-board-sprint-filters/33-VALIDATION.md

<interfaces>
<!-- Follow existing test patterns from the codebase -->

From taskflow/src/services/jira/sprints.test.ts (test pattern):
```typescript
import { beforeEach, describe, expect, it, vi } from 'vitest';
vi.mock('../../lib/apiFetch', () => ({ apiFetch: vi.fn() }));
import { apiFetch } from '../../lib/apiFetch';
const BASE = 'https://jira.example.com';
const TOKEN = 'test-token';
```

From taskflow/src/stores/filter.store.test.ts (store test pattern):
```typescript
import { describe, expect, it, beforeEach } from 'vitest';
// Direct store import, test state transitions
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create service test stubs (board-config.test.ts, filters.test.ts)</name>
  <files>
    taskflow/src/services/jira/board-config.test.ts,
    taskflow/src/services/jira/filters.test.ts
  </files>
  <read_first>
    taskflow/src/services/jira/sprints.test.ts,
    taskflow/src/services/jira/worklogs.test.ts
  </read_first>
  <action>
**1. Create `board-config.test.ts`** — test stubs for fetchBoardQuickFilters (BOARD-02):

```typescript
import { beforeEach, describe, expect, it, vi } from 'vitest';

vi.mock('../../lib/apiFetch', () => ({ apiFetch: vi.fn() }));

describe('board-config service', () => {
  beforeEach(() => { vi.clearAllMocks(); });

  describe('fetchBoardQuickFilters', () => {
    it.todo('returns quick filters array on success');
    it.todo('returns empty array on non-ok response');
    it.todo('calls correct Jira Agile endpoint with board ID');
  });
});
```

**2. Create `filters.test.ts`** — test stubs for filter CRUD (FILT-01, FILT-03):

```typescript
import { beforeEach, describe, expect, it, vi } from 'vitest';

vi.mock('../../lib/apiFetch', () => ({ apiFetch: vi.fn() }));

describe('filters service', () => {
  beforeEach(() => { vi.clearAllMocks(); });

  describe('createJiraFilter', () => {
    it.todo('creates filter via POST /rest/api/2/filter');
    it.todo('sets favourite: true on created filter');
    it.todo('throws on non-ok response');
  });

  describe('fetchFavouriteFilters', () => {
    it.todo('returns favourite filters array on success');
    it.todo('returns empty array on non-ok response');
  });

  describe('updateJiraFilter', () => {
    it.todo('updates filter via PUT with correct body');
    it.todo('throws on non-ok response');
  });

  describe('deleteJiraFilter', () => {
    it.todo('deletes filter via DELETE');
    it.todo('throws on non-ok response');
  });

  describe('buildJqlFromFilters', () => {
    it.todo('includes project clause');
    it.todo('adds epic link clause when epics provided');
    it.todo('adds labels clause with quoted values');
    it.todo('adds assignee clause');
    it.todo('adds status clause');
    it.todo('returns project-only JQL when no filters active');
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/services/jira/board-config.test.ts src/services/jira/filters.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>Both test files exist with .todo() stubs, vitest recognizes them as pending tests with no errors</done>
</task>

<task type="auto">
  <name>Task 2: Create component test stubs (SprintGoalBanner, QuickFilterChipRow, BulkActionBar, SavedFilterList)</name>
  <files>
    taskflow/src/routes/dashboard/SprintGoalBanner.test.tsx,
    taskflow/src/routes/dashboard/QuickFilterChipRow.test.tsx,
    taskflow/src/routes/dashboard/BulkActionBar.test.tsx,
    taskflow/src/components/SavedFilterList.test.tsx
  </files>
  <read_first>
    taskflow/src/routes/dashboard/SprintBoardTab.test.tsx,
    taskflow/src/components/ui/stale-data-banner.test.tsx
  </read_first>
  <action>
**1. Create `SprintGoalBanner.test.tsx`** — stubs for BOARD-01:

```typescript
import { describe, it } from 'vitest';

describe('SprintGoalBanner', () => {
  it.todo('renders banner with goal text when goal is provided');
  it.todo('returns null when goal is null');
  it.todo('returns null when goal is empty string');
  it.todo('returns null when goal is whitespace only');
  it.todo('has role="banner" and aria-label="Sprint goal"');
  it.todo('applies border-l-4 border-primary accent styling');
});
```

**2. Create `QuickFilterChipRow.test.tsx`** — stubs for BOARD-02, BOARD-03:

```typescript
import { describe, it } from 'vitest';

describe('QuickFilterChipRow', () => {
  it.todo('renders Jira quick filter chips');
  it.todo('renders label filter chips');
  it.todo('returns null when no quick filters and no labels');
  it.todo('shows divider between QF and label sections when both exist');
  it.todo('toggles active state on chip click');
  it.todo('applies variant="default" to active chips');
  it.todo('applies variant="outline" to inactive chips');
  it.todo('has role="toolbar" with aria-label="Quick filters"');
  it.todo('each chip has role="switch" with aria-checked');
});
```

**3. Create `BulkActionBar.test.tsx`** — stubs for BOARD-04 through BOARD-07:

```typescript
import { describe, it } from 'vitest';

describe('BulkActionBar', () => {
  describe('selection display', () => {
    it.todo('shows selected count badge');
    it.todo('has role="toolbar" with selection count in aria-label');
  });

  describe('action dropdowns', () => {
    it.todo('renders Status dropdown with project statuses');
    it.todo('renders Assignee dropdown with project members');
    it.todo('renders Priority dropdown with standard priorities');
    it.todo('disables Apply Changes button when no dropdown has value');
    it.todo('enables Apply Changes button when at least one dropdown has value');
  });

  describe('bulk execution', () => {
    it.todo('uses postTransition for status changes (not updateIssueField)');
    it.todo('uses updateIssueField for assignee changes');
    it.todo('uses updateIssueField for priority changes');
    it.todo('limits concurrency to 5 parallel requests');
  });

  describe('progress indicator', () => {
    it.todo('shows progress bar during bulk operation');
    it.todo('displays success count on completion');
    it.todo('displays failure count with details on partial failure');
    it.todo('has role="progressbar" with correct aria attributes');
  });

  describe('deselect', () => {
    it.todo('clears selection on Deselect All click');
    it.todo('clears selection on Escape key');
  });
});
```

**4. Create `SavedFilterList.test.tsx`** — stubs for FILT-02:

```typescript
import { describe, it } from 'vitest';

describe('SavedFilterList', () => {
  it.todo('renders saved filters from store');
  it.todo('shows empty state when no saved filters');
  it.todo('shows loading skeletons while isLoading');
  it.todo('highlights active filter with checkmark');
  it.todo('calls onApplyFilter when filter clicked');
  it.todo('deactivates filter when active filter clicked again');
  it.todo('has role="listbox" with role="option" items');
  it.todo('shows context menu with Edit and Delete options on right-click');
});
```

All stubs use `it.todo()` (vitest marks them as pending, not failing). No imports of unwritten source files -- stubs are pure describe/it structure.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/SprintGoalBanner.test.tsx src/routes/dashboard/QuickFilterChipRow.test.tsx src/routes/dashboard/BulkActionBar.test.tsx src/components/SavedFilterList.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 component test files exist with .todo() stubs covering BOARD-01 through BOARD-07 and FILT-02, vitest reports them as pending with no errors</done>
</task>

</tasks>

<verification>
- `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run --reporter=verbose` passes (all new stubs show as pending/todo)
- All 6 test files exist at the documented paths
- No import errors (stubs do not import unwritten source files)
</verification>

<success_criteria>
- 6 test stub files created covering all Phase 33 requirements
- Each file has describe blocks matching the component/service it will test
- All test cases use it.todo() for pending status
- Vitest runs clean with no failures
- VALIDATION.md wave_0_complete can be set to true after execution
</success_criteria>

<output>
After completion, create `.planning/phases/33-board-sprint-filters/33-00-SUMMARY.md`
</output>
