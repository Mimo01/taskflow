---
phase: 18-app-icon-multi-page-settings
plan: 06
type: execute
wave: 3
depends_on:
  - "18-04"
  - "18-05"
files_modified:
  - taskflow/src/routes/developer/TaskRow.tsx
  - taskflow/src/routes/sprint/SprintBoard.tsx
  - taskflow/src/routes/backlog/BacklogRow.tsx
  - taskflow/src/components/MrRow.tsx
  - taskflow/src/components/Sidebar.tsx
autonomous: false
requirements:
  - SETTINGS-03

must_haves:
  truths:
    - "Setting density to Compact reduces vertical padding on task rows, MR rows, backlog items, sprint board cards, and sidebar nav items"
    - "Setting density to Comfortable increases vertical padding on those same surfaces"
    - "Default density shows the same spacing as before Phase 18 (no visual regression)"
    - "Density changes take effect immediately without page reload"
  artifacts:
    - path: "taskflow/src/routes/developer/TaskRow.tsx"
      provides: "density-compact and density-comfortable Tailwind variants on row padding"
    - path: "taskflow/src/routes/sprint/SprintBoard.tsx"
      provides: "density variants on sprint board card padding"
    - path: "taskflow/src/components/Sidebar.tsx"
      provides: "density variants on sidebar nav item padding"
  key_links:
    - from: "index.css @variant density-compact"
      to: "list/card components"
      via: "Tailwind class density-compact:py-X"
      pattern: "density-compact:"
---

<objective>
Apply density CSS variants (density-compact:, density-comfortable:) to all list and card surfaces so the density selector in Appearance settings has a visible effect throughout the app.

Purpose: Plan 04 created the CSS variant infrastructure. This plan applies it to the actual surfaces — task rows, MR rows, backlog items, sprint board cards, and sidebar nav items. Ends with a human visual check.
Output: Density-aware padding on all major list/card surfaces; human checkpoint verifies the effect across routes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-app-icon-multi-page-settings/18-CONTEXT.md
@.planning/phases/18-app-icon-multi-page-settings/18-RESEARCH.md
@.planning/phases/18-app-icon-multi-page-settings/18-04-SUMMARY.md

<interfaces>
<!-- Density CSS variants available after Plan 04 -->
In taskflow/src/index.css:
```css
@variant density-compact (&:is([data-density="compact"] *));
@variant density-comfortable (&:is([data-density="comfortable"] *));
```

Usage pattern:
```html
<!-- py-3 is the default; compact shrinks, comfortable expands -->
<div class="py-3 density-compact:py-1.5 density-comfortable:py-5">...</div>
```

<!-- applyDensity writes data-density="compact|comfortable" to html element
     or removes the attribute for 'default' -->
```typescript
export function applyDensity(density: Density): void { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply density variants to list rows and sidebar nav</name>
  <files>taskflow/src/routes/developer/TaskRow.tsx, taskflow/src/routes/backlog/BacklogRow.tsx, taskflow/src/components/MrRow.tsx, taskflow/src/components/Sidebar.tsx</files>
  <action>
    Read each target file first to identify the outer row/item container's padding classes.

    For each file, find the primary vertical padding (py-X or pt-X pb-X) on the outermost row/item container and add density variants:

    Pattern to apply:
    ```
    Before: className="... py-3 ..."
    After:  className="... py-3 density-compact:py-1.5 density-comfortable:py-5 ..."
    ```

    Specific surfaces to update:
    - TaskRow.tsx (My Tasks list): outer row container — apply to the element that wraps the task title, assignee, priority
    - BacklogRow.tsx (Backlog list): outer row container
    - MrRow.tsx (MR lists in Developer Dashboard): outer row container
    - Sidebar.tsx (sidebar nav items): nav link items — use smaller increments since sidebar items are more compact by default (e.g., py-2 → density-compact:py-1 density-comfortable:py-3)

    Spacing reference (for a default py-3 = 12px):
    - Compact: py-1.5 (6px) — approximately half
    - Comfortable: py-5 (20px) — approximately 65% more

    For sidebar nav items (typically py-2 = 8px):
    - Compact: py-1 (4px)
    - Comfortable: py-3 (12px)

    If the actual padding is different from py-3/py-2, adjust the compact/comfortable values proportionally — the goal is approximately +-50% change in vertical padding.

    Only modify vertical padding (py-X, pt-X, pb-X). Do NOT change horizontal padding, width, font-size, or other properties — density is purely vertical spacing.

    Also apply to sprint board cards if they are in a separate identifiable component (check if SprintBoard.tsx has a card component or imports one — apply to the card's internal padding).
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && grep -rn "density-compact:" src/routes/developer/ src/routes/backlog/ src/components/MrRow.tsx src/components/Sidebar.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <done>At least 4 files have density-compact: and density-comfortable: classes applied. TypeScript compiles clean. npx vitest run passes.</done>
</task>

<task type="auto">
  <name>Task 2: Apply density variants to sprint board cards</name>
  <files>taskflow/src/routes/sprint/SprintBoard.tsx</files>
  <action>
    Read `taskflow/src/routes/sprint/SprintBoard.tsx` to find the sprint board card component or inline card markup.

    Sprint board cards are typically rendered as a card with padding — find the card container's py-X class and apply:
    ```
    py-3 density-compact:py-1.5 density-comfortable:py-5
    ```
    or equivalent proportional adjustment.

    If board cards are in a separate file (e.g., SprintCard.tsx, BoardCard.tsx), modify that file instead and add it to the files_modified list.

    After applying, run the full settings test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>Full vitest suite passes. Sprint board cards have density variants applied. TypeScript clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify density affects all surfaces and Settings nav works end-to-end</name>
  <files></files>
  <action>Human visual verification that density selector and settings sidebar nav work correctly throughout the app.</action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>User has confirmed density affects all target surfaces and Settings sidebar nav works without page reload.</done>
  <what-built>Density infrastructure + rollout across all list/card surfaces. Selecting Compact/Default/Comfortable in Settings > Appearance now affects task rows, MR rows, backlog items, sprint board cards, and sidebar nav items.</what-built>
  <how-to-verify>
    1. Run the app: `cd /Users/mimo/Desktop/Tasker/taskflow && npm run tauri dev`
    2. Open Settings → Appearance
    3. Select "Compact" — verify:
       - My Tasks (Developer tab) rows are visibly smaller/tighter
       - Sprint Board cards have less vertical padding
       - Backlog rows are tighter
       - Sidebar nav items are slightly tighter
    4. Select "Comfortable" — verify the same surfaces expand with more breathing room
    5. Select "Default" — verify the app looks the same as before Phase 18 (no visual regression)
    6. Navigate between routes while density is set to Compact — verify density persists across route changes
    7. Verify the Settings sidebar itself has 5 items: Connections, Appearance, Notifications, Workflow, Role
    8. Click each Settings sidebar item — verify correct section renders without page reload
  </how-to-verify>
  <resume-signal>Type "approved" if density and settings navigation work correctly. If issues found, describe which surface or section is wrong.</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run 2>&1 | tail -5
cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0"
```
Full suite green. TypeScript clean. Human checkpoint approved.
</verification>

<success_criteria>
- density-compact: and density-comfortable: classes applied to task rows, MR rows, backlog items, sprint board cards, and sidebar nav items
- Default density shows no visual change from pre-Phase-18 (no regression)
- Density change is immediate — no page reload required
- Full vitest suite passes
- Human visual check approved
</success_criteria>

<output>
After completion, create `.planning/phases/18-app-icon-multi-page-settings/18-06-SUMMARY.md`
</output>
