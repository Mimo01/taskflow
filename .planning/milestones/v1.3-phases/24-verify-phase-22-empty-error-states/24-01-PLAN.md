---
phase: 24-verify-phase-22-empty-error-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-polish-empty-states-error-recovery/22-VERIFICATION.md
autonomous: true
requirements: [POLISH-01, POLISH-02, POLISH-03]

must_haves:
  truths:
    - "VERIFICATION.md exists in .planning/phases/22-polish-empty-states-error-recovery/"
    - "POLISH-01 is individually verified with file-path evidence for all 10 views"
    - "POLISH-02 is individually verified with ErrorState + StaleDataBanner evidence for all 10 views"
    - "POLISH-03 is individually verified with ApiError throw sites in jira.ts/gitlab.ts and ErrorState Reconnect CTA evidence"
    - "All Phase 22 unit tests pass (31 component tests + service tests)"
  artifacts:
    - path: ".planning/phases/22-polish-empty-states-error-recovery/22-VERIFICATION.md"
      provides: "Complete verification report for Phase 22"
      contains: "POLISH-01"
  key_links: []
---

<objective>
Write the missing VERIFICATION.md for Phase 22 (Empty States + Error Recovery) by running tests and inspecting source files to confirm POLISH-01, POLISH-02, and POLISH-03 are satisfied.

Purpose: The v1.3 milestone audit flagged Phase 22 as missing its VERIFICATION.md. This is the last gap before milestone completion.
Output: `.planning/phases/22-polish-empty-states-error-recovery/22-VERIFICATION.md`
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-polish-empty-states-error-recovery/22-01-SUMMARY.md
@.planning/phases/22-polish-empty-states-error-recovery/22-02-SUMMARY.md
@.planning/phases/22-polish-empty-states-error-recovery/22-03-SUMMARY.md
@.planning/phases/24-verify-phase-22-empty-error-states/24-RESEARCH.md
@.planning/phases/20-command-palette-recent-items/20-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather evidence and run Phase 22 tests</name>
  <files>.planning/phases/22-polish-empty-states-error-recovery/22-VERIFICATION.md</files>
  <read_first>
    - taskflow/src/lib/api-error.ts
    - taskflow/src/lib/api-error.test.ts
    - taskflow/src/components/ui/empty-state.tsx
    - taskflow/src/components/ui/empty-state.test.tsx
    - taskflow/src/components/ui/error-state.tsx
    - taskflow/src/components/ui/error-state.test.tsx
    - taskflow/src/components/ui/stale-data-banner.tsx
    - taskflow/src/components/ui/stale-data-banner.test.tsx
    - taskflow/src/routes/dashboard/MyTasksTab.tsx
    - taskflow/src/routes/dashboard/SprintBoardTab.tsx
    - taskflow/src/routes/dashboard/SprintProgressTab.tsx
    - taskflow/src/routes/dashboard/BacklogPage.tsx
    - taskflow/src/routes/dashboard/MrAttentionTab.tsx
    - taskflow/src/routes/dashboard/WorkloadTab.tsx
    - taskflow/src/routes/dashboard/ReleasesTab.tsx
    - taskflow/src/routes/dashboard/EpicsPage.tsx
    - taskflow/src/routes/notifications/NotificationPopover.tsx
    - taskflow/src/components/app/CommandPalette.tsx
    - taskflow/src/services/jira.ts
    - taskflow/src/services/gitlab.ts
    - .planning/phases/20-command-palette-recent-items/20-VERIFICATION.md
  </read_first>
  <action>
Gather all evidence needed for the VERIFICATION.md, then write the complete document. Follow the EXACT format from Phase 20's 20-VERIFICATION.md (already referenced in read_first).

**Step 1: Run Phase 22 tests and capture output**
```bash
cd taskflow && npx vitest run src/components/ui/empty-state.test.tsx src/components/ui/error-state.test.tsx src/components/ui/stale-data-banner.test.tsx src/lib/api-error.test.ts 2>&1
```
Record pass/fail counts for each test file.

**Step 2: Read each of the 10 view files and record evidence**

For each view file, document:
- The exact EmptyState import and usage (icon name, title string, subtitle if any, action if any)
- The exact ErrorState import and usage (error prop, onRetry prop)
- The exact StaleDataBanner import and usage (onRetry, onDismiss)
- The three-state detection pattern: `isError && !data -> ErrorState`, `isError && data -> StaleDataBanner`, `!isError && empty -> EmptyState`

Views to verify (10 total):
1. MyTasksTab.tsx -- ClipboardList icon, "You're all caught up!"
2. SprintBoardTab.tsx -- Columns3 icon, "No sprint issues"
3. SprintProgressTab.tsx -- BarChart3 icon, "No sprint data yet"
4. BacklogPage.tsx -- Inbox icon, "Backlog is empty", Create Issue CTA
5. MrAttentionTab.tsx -- GitMerge icon, "No merge requests need attention", Connect GitLab CTA
6. WorkloadTab.tsx -- Users icon, "No workload data"
7. ReleasesTab.tsx -- Package icon, "No releases found"
8. EpicsPage.tsx -- Layers icon, Create Epic CTA
9. NotificationPopover.tsx -- Bell icon, store-level fetchError/retryFetch (NOT useQuery pattern)
10. CommandPalette.tsx -- SearchX inline JSX in CommandEmpty (NOT shared EmptyState component)

**Step 3: Verify ApiError throw sites in services**

In jira.ts, confirm ApiError is imported and thrown on 401/403 responses. Count throw sites.
In gitlab.ts, confirm ApiError is imported and thrown on 401/403 responses. Count throw sites.

**Step 4: Verify ErrorState auth detection**

In error-state.tsx, confirm:
- `isAuthError` is imported from `@/lib/api-error`
- Auth errors show "Session expired" message
- Auth errors render a "Reconnect" button with `useNavigate` to `/settings`

**Step 5: Write VERIFICATION.md**

Create `.planning/phases/22-polish-empty-states-error-recovery/22-VERIFICATION.md` with these sections:

1. **Frontmatter** with:
   - `phase: 22-polish-empty-states-error-recovery`
   - `verified: {current ISO timestamp}`
   - `status: human_needed` (manual app testing still required)
   - `score: {N}/{N} must-haves verified`
   - `human_verification:` list with items for testing empty states, error states, and auth reconnect in running app

2. **Goal Achievement / Observable Truths table** -- one row per observable truth:
   - EmptyState component renders icon + title + optional subtitle + optional action
   - Each of the 10 views uses EmptyState with its specific icon and title
   - ErrorState component renders error message + Retry button
   - ErrorState auto-detects auth errors and shows Reconnect CTA to /settings
   - StaleDataBanner renders retry + dismiss buttons
   - Three-state detection pattern implemented in all views
   - ApiError thrown on 401/403 in jira.ts and gitlab.ts

3. **Required Artifacts** table -- file path, line count, key exports/evidence

4. **Key Link Verification** table:
   - Each view -> EmptyState (import + render)
   - Each view -> ErrorState (import + render)
   - Each view -> StaleDataBanner (import + render)
   - jira.ts -> ApiError (import + throw)
   - gitlab.ts -> ApiError (import + throw)
   - ErrorState -> isAuthError (import + usage)
   - ErrorState -> useNavigate /settings (Reconnect CTA)
   - NotificationPopover -> notifications.store fetchError/retryFetch

5. **Requirements Coverage** table:
   - POLISH-01: List all 10 views with their EmptyState icon and title
   - POLISH-02: List all 10 views with ErrorState + StaleDataBanner usage
   - POLISH-03: ApiError throw sites (count in jira.ts + gitlab.ts) + ErrorState Reconnect CTA

6. **Test Results** table -- file, test count, pass/fail

7. **Anti-Patterns Found** -- any warnings from test output

8. **Human Verification Required** -- items needing manual app testing:
   - Disconnect Jira credentials, verify ErrorState with Reconnect CTA appears
   - Verify empty states render with correct icons when no data
   - Verify StaleDataBanner appears when cached data exists but refresh fails
   - Note: CommandPalette uses inline SearchX (not shared EmptyState) -- intentional deviation

9. **Gaps Summary** -- expected: no code gaps

Use REAL line numbers, file sizes, and test counts from the source files read in Step 2. Do NOT use placeholder values.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-polish-empty-states-error-recovery/22-VERIFICATION.md && grep -c "POLISH-01" .planning/phases/22-polish-empty-states-error-recovery/22-VERIFICATION.md && grep -c "POLISH-02" .planning/phases/22-polish-empty-states-error-recovery/22-VERIFICATION.md && grep -c "POLISH-03" .planning/phases/22-polish-empty-states-error-recovery/22-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/22-polish-empty-states-error-recovery/22-VERIFICATION.md exists
    - File contains "POLISH-01" at least 2 times (requirements coverage + truths)
    - File contains "POLISH-02" at least 2 times
    - File contains "POLISH-03" at least 2 times
    - File contains "SATISFIED" for all 3 POLISH requirements
    - File contains "Observable Truths" section header
    - File contains "Requirements Coverage" section header
    - File contains "Key Link Verification" section header
    - File contains "Test Results" section header
    - File contains "Human Verification Required" section header
    - File contains "EmptyState" (component name referenced)
    - File contains "ErrorState" (component name referenced)
    - File contains "StaleDataBanner" (component name referenced)
    - File contains "ApiError" (class name referenced)
    - File contains "isAuthError" (helper function referenced)
    - File contains all 10 view names: MyTasksTab, SprintBoardTab, SprintProgressTab, BacklogPage, MrAttentionTab, WorkloadTab, ReleasesTab, EpicsPage, NotificationPopover, CommandPalette
    - File frontmatter contains "status: human_needed"
    - cd taskflow && npx vitest run src/components/ui/empty-state.test.tsx src/components/ui/error-state.test.tsx src/components/ui/stale-data-banner.test.tsx src/lib/api-error.test.ts exits 0
  </acceptance_criteria>
  <done>
    VERIFICATION.md exists with all required sections. All 3 POLISH requirements marked SATISFIED with file-path evidence. All Phase 22 component tests pass. Human verification items listed for manual app testing.
  </done>
</task>

</tasks>

<verification>
- 22-VERIFICATION.md exists in .planning/phases/22-polish-empty-states-error-recovery/
- All 3 POLISH requirements have SATISFIED status with evidence
- Phase 22 component tests (empty-state, error-state, stale-data-banner, api-error) all pass
- Document follows established VERIFICATION.md format from Phases 18-21
</verification>

<success_criteria>
- VERIFICATION.md produced with evidence for all 3 requirements
- No code changes made (verification-only phase)
- All Phase 22 tests confirmed passing
</success_criteria>

<output>
After completion, create `.planning/phases/24-verify-phase-22-empty-error-states/24-01-SUMMARY.md`
</output>
