---
phase: 27-refactoring-type-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/jira/index.ts
  - taskflow/src/services/jira/types.ts
  - taskflow/src/services/jira/client.ts
  - taskflow/src/services/jira/projects.ts
  - taskflow/src/services/jira/issues.ts
  - taskflow/src/services/jira/sprints.ts
  - taskflow/src/services/jira/fields.ts
  - taskflow/src/services/jira/comments.ts
  - taskflow/src/services/jira/epics.ts
  - taskflow/src/services/jira/backlog.ts
  - taskflow/src/services/jira/links.ts
  - taskflow/src/services/jira/worklogs.ts
  - taskflow/src/services/jira/transitions.ts
  - taskflow/src/services/jira/versions.ts
  - taskflow/src/services/jira.ts
autonomous: true
requirements: [REFAC-01, REFAC-05]

must_haves:
  truths:
    - "All 48+ import sites still resolve @/services/jira without any import path changes"
    - "Every function previously exported from jira.ts is still exported from the barrel"
    - "Error handling uses type guard function instead of double-casts"
    - "All existing tests still pass after decomposition"
  artifacts:
    - path: "taskflow/src/services/jira/index.ts"
      provides: "Barrel export re-exporting all domain modules"
      exports: ["*"]
    - path: "taskflow/src/services/jira/types.ts"
      provides: "All 15+ Jira type/interface definitions"
    - path: "taskflow/src/services/jira/client.ts"
      provides: "Shared fetch helper, fetchAllSearchPages, error type guard, constants"
      exports: ["isResponseLikeError", "fetchAllSearchPages", "PAGE_SIZE", "SUBTASK_CHUNK_SIZE"]
  key_links:
    - from: "taskflow/src/services/jira/issues.ts"
      to: "taskflow/src/services/jira/client.ts"
      via: "import fetchAllSearchPages"
      pattern: "import.*fetchAllSearchPages.*from.*./client"
    - from: "taskflow/src/services/jira/index.ts"
      to: "taskflow/src/services/jira/types.ts"
      via: "export * from './types'"
      pattern: "export \\* from './types'"
---

<objective>
Decompose the monolithic jira.ts (2,018 lines) into focused domain modules under src/services/jira/ with a barrel index.ts that preserves all existing import paths.

Purpose: Make the Jira service maintainable by splitting into ~13 focused files. Extract error handling into a shared type guard (REFAC-05).
Output: src/services/jira/ directory with barrel exports, original jira.ts deleted.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-refactoring-type-safety/27-CONTEXT.md
@.planning/phases/27-refactoring-type-safety/27-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create jira/ domain modules with types, client, and all domain files</name>
  <files>taskflow/src/services/jira/types.ts, taskflow/src/services/jira/client.ts, taskflow/src/services/jira/projects.ts, taskflow/src/services/jira/issues.ts, taskflow/src/services/jira/sprints.ts, taskflow/src/services/jira/fields.ts, taskflow/src/services/jira/comments.ts, taskflow/src/services/jira/epics.ts, taskflow/src/services/jira/backlog.ts, taskflow/src/services/jira/links.ts, taskflow/src/services/jira/worklogs.ts, taskflow/src/services/jira/transitions.ts, taskflow/src/services/jira/versions.ts</files>
  <read_first>
    - taskflow/src/services/jira.ts (full file -- the source of truth for all functions, types, constants)
    - taskflow/src/services/jira.test.ts (to understand test imports and what functions are tested)
  </read_first>
  <action>
1. Create `taskflow/src/services/jira/` directory.

2. Create `taskflow/src/services/jira/types.ts`:
   - Move ALL exported interfaces and types from jira.ts: JiraUser, JiraProject, JiraIssue, JiraFixVersion, JiraTransition, JiraComment, JiraActiveSprint, JiraIssueLink, JiraAttachment, JiraIssueDetail, JiraProjectStatus, CreatemetaField, IssueLinkType, BacklogViewData, EpicEnriched, and any other exported types.
   - Include any type aliases or utility types used across multiple domain modules.

3. Create `taskflow/src/services/jira/client.ts`:
   - Move the `apiFetch` import (from `@/lib/apiFetch`)
   - Move constants: `PAGE_SIZE`, `SUBTASK_CHUNK_SIZE`, and any other shared constants
   - Move `fetchAllSearchPages` (the internal paginated search helper used by issues, sprints, backlog, epics)
   - Move the shared headers construction pattern (the function or inline pattern that builds `{ Authorization: 'Basic ...', 'Content-Type': 'application/json' }`)
   - **Create the `isResponseLikeError` type guard** (REFAC-05) to replace the 3 identical `as unknown as { status: number; text?: () => Promise<string> }` double-casts at current jira.ts lines 334, 448, 1492:

   ```typescript
   interface ResponseLikeError {
     status: number;
     text?: () => Promise<string>;
   }

   export function isResponseLikeError(err: unknown): err is ResponseLikeError {
     return (
       err !== null &&
       typeof err === 'object' &&
       'status' in err &&
       typeof (err as { status: unknown }).status === 'number'
     );
   }
   ```

   - Export `fetchAllSearchPages`, `isResponseLikeError`, `PAGE_SIZE`, `SUBTASK_CHUNK_SIZE`, and any shared helper functions. Do NOT re-export these from barrel (fetchAllSearchPages is internal-only).
   - Actually, check if fetchAllSearchPages is used by test files. If jira.test.ts imports it, it must be in the barrel. Otherwise, keep it internal (exported from client.ts but not from index.ts).

4. Create domain modules, each importing from `./types` and `./client` as needed:

   **`projects.ts`**: `validateJira`, `listJiraProjects`
   **`issues.ts`**: `fetchSprintIssues`, `fetchMyTasksHierarchy`, `fetchIssueDetail`, `fetchIssueSummary`, `updateIssueField`, `createIssue`, `bulkUpdateIssue`, `wrapCustomFieldValue`, `searchJira`
   **`sprints.ts`**: `fetchActiveSprint`, `fetchSprintsForBoard`, `addIssuesToSprint`
   **`fields.ts`**: `discoverCustomFields`, `fetchCreatemeta`, `fetchProjectStatuses`
   **`comments.ts`**: `fetchComments`, `postComment`, `updateComment`, `deleteComment`
   **`epics.ts`**: `fetchEpicsBasic`, `fetchEpicEnrichmentMap`, `fetchEpicsWithEnrichment`, `fetchEpicStories`
   **`backlog.ts`**: `fetchBacklogIssues`, `fetchBacklogView`
   **`links.ts`**: `fetchIssueLinkTypes`, `createIssueLink`
   **`worklogs.ts`**: `fetchIssueWorklogs`
   **`transitions.ts`**: `fetchTransitions`, `postTransition`
   **`versions.ts`**: `fetchFixVersions`

   Each domain module:
   - Imports types from `./types`
   - Imports helpers/constants from `./client` (fetchAllSearchPages, isResponseLikeError, PAGE_SIZE, etc.)
   - Imports `apiFetch` from `@/lib/apiFetch` if it makes direct API calls
   - Uses `isResponseLikeError(err)` type guard in catch blocks instead of `err as unknown as { status: number; text?: () => Promise<string> }` (this fixes the 3 jira.ts double-casts)
   - Has IDENTICAL function signatures and return types as the original jira.ts

5. **Dependency direction (CRITICAL):** types.ts and client.ts are foundation. Domain modules import from them. Domain modules NEVER import from each other. The barrel index.ts imports from all modules but is NEVER imported internally.

6. **Do NOT create the barrel index.ts yet** -- that is Task 2. Do NOT delete jira.ts yet.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/services/jira/types.ts exists and contains all interface/type exports (JiraUser, JiraProject, JiraIssue, JiraIssueDetail, etc.)
    - taskflow/src/services/jira/client.ts exists and contains `export function isResponseLikeError`
    - taskflow/src/services/jira/client.ts contains `export function fetchAllSearchPages` or `export async function fetchAllSearchPages`
    - taskflow/src/services/jira/issues.ts exists and contains `export async function fetchSprintIssues`
    - taskflow/src/services/jira/sprints.ts exists and contains `export async function fetchActiveSprint`
    - taskflow/src/services/jira/projects.ts exists and contains `export async function validateJira`
    - taskflow/src/services/jira/epics.ts exists and contains `export async function fetchEpicsWithEnrichment`
    - taskflow/src/services/jira/comments.ts exists and contains `export async function fetchComments`
    - taskflow/src/services/jira/backlog.ts exists and contains `export async function fetchBacklogView`
    - taskflow/src/services/jira/fields.ts exists and contains `export async function discoverCustomFields`
    - taskflow/src/services/jira/links.ts exists and contains `export async function fetchIssueLinkTypes`
    - taskflow/src/services/jira/transitions.ts exists and contains `export async function fetchTransitions`
    - taskflow/src/services/jira/versions.ts exists and contains `export async function fetchFixVersions`
    - taskflow/src/services/jira/worklogs.ts exists and contains `export async function fetchIssueWorklogs`
    - NO file in jira/ contains `as unknown as` (the 3 double-casts replaced with isResponseLikeError)
    - NO domain module imports from another domain module (only from ./types or ./client)
    - TypeScript compiles without errors (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>All domain modules created with identical function signatures. Error handling uses isResponseLikeError type guard. No circular dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Create barrel index.ts, delete original jira.ts, verify all imports resolve</name>
  <files>taskflow/src/services/jira/index.ts, taskflow/src/services/jira.ts</files>
  <read_first>
    - taskflow/src/services/jira.ts (check which exports exist -- every one must be in the barrel)
    - taskflow/src/services/jira.test.ts (check what it imports to ensure barrel covers test needs)
  </read_first>
  <action>
1. Create `taskflow/src/services/jira/index.ts` barrel file:

```typescript
export * from './types';
export * from './projects';
export * from './issues';
export * from './sprints';
export * from './fields';
export * from './comments';
export * from './epics';
export * from './backlog';
export * from './links';
export * from './worklogs';
export * from './transitions';
export * from './versions';
```

Note: Do NOT export from `./client` in the barrel -- client.ts exports are internal (used by domain modules only). EXCEPTION: If `fetchAllSearchPages` or any other client.ts function is imported by test files or other non-jira code, add those specific exports.

Also export `isResponseLikeError` from the barrel if it is used outside of jira/ modules (check if any file outside jira/ imports it). If not, keep it internal.

2. Delete the original `taskflow/src/services/jira.ts` file.

3. Verify TypeScript resolution: `@/services/jira` and `../services/jira` imports across the 48+ consumer files will now resolve to `@/services/jira/index.ts`. This is automatic TypeScript behavior -- no import path changes needed.

4. Handle the test file `taskflow/src/services/jira.test.ts`:
   - Check its import: if it uses `import { ... } from './jira'`, this will now resolve to `./jira/index.ts` -- no change needed.
   - If it uses `import * as jiraModule from './jira'`, this also resolves correctly.
   - Verify the test file compiles and runs.

5. Run full test suite: `cd taskflow && npx vitest --run`
6. Run Biome check: `cd taskflow && npx biome check src/services/jira/`
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/services/jira/index.ts exists and contains `export * from './types'`
    - taskflow/src/services/jira/index.ts contains `export * from './issues'`
    - taskflow/src/services/jira/index.ts contains `export * from './sprints'`
    - taskflow/src/services/jira/index.ts contains `export * from './epics'`
    - taskflow/src/services/jira.ts does NOT exist (deleted)
    - `grep -r "from.*@/services/jira" taskflow/src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v test` shows imports still resolve (no TypeScript errors)
    - All tests pass (npx vitest --run exits 0)
    - npx biome check src/services/jira/ passes
  </acceptance_criteria>
  <done>jira.ts fully decomposed into 14 files under jira/. Barrel export preserves all 48+ import sites. All tests pass. Zero double-casts in jira/ modules.</done>
</task>

</tasks>

<verification>
- `cd taskflow && npx vitest --run` — all tests pass
- `cd taskflow && npx tsc --noEmit` — no TypeScript errors
- `cd taskflow && npx biome check src/services/jira/` — clean
- `ls taskflow/src/services/jira.ts` — file does not exist (deleted)
- `ls taskflow/src/services/jira/index.ts` — file exists
- `grep -r "as unknown as" taskflow/src/services/jira/` — returns nothing
- `wc -l taskflow/src/services/jira/*.ts` — each file well under 300 lines
</verification>

<success_criteria>
- jira.ts (2,018 lines) decomposed into ~14 focused domain modules
- Barrel index.ts re-exports everything
- Zero import path changes across the codebase (48+ consumer files unaffected)
- isResponseLikeError type guard replaces 3 double-casts (REFAC-05)
- All existing tests pass with no modifications
</success_criteria>

<output>
After completion, create `.planning/phases/27-refactoring-type-safety/27-02-SUMMARY.md`
</output>
