---
phase: 27-refactoring-type-safety
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/create-edit-issue/index.ts
  - taskflow/src/routes/dashboard/create-edit-issue/CreateEditIssueModal.tsx
  - taskflow/src/routes/dashboard/create-edit-issue/useCreateEditForm.ts
  - taskflow/src/routes/dashboard/create-edit-issue/useIssueMutations.ts
  - taskflow/src/routes/dashboard/create-edit-issue/IssueTypeSelector.tsx
  - taskflow/src/routes/dashboard/create-edit-issue/CustomFieldsSection.tsx
  - taskflow/src/routes/dashboard/create-edit-issue/LinkRowsSection.tsx
  - taskflow/src/routes/dashboard/CreateEditIssueModal.tsx
autonomous: true
requirements: [REFAC-02]

must_haves:
  truths:
    - "CreateEditIssueModal still opens, creates issues, edits issues, and resets on close"
    - "Form state uses useReducer instead of 21 separate useState calls"
    - "Main orchestrator file is under 250 lines"
    - "All existing tests still pass after decomposition"
  artifacts:
    - path: "taskflow/src/routes/dashboard/create-edit-issue/CreateEditIssueModal.tsx"
      provides: "Slim orchestrator component (~200 lines)"
    - path: "taskflow/src/routes/dashboard/create-edit-issue/useCreateEditForm.ts"
      provides: "useReducer-based form state hook"
      exports: ["useCreateEditForm"]
    - path: "taskflow/src/routes/dashboard/create-edit-issue/useIssueMutations.ts"
      provides: "TanStack Query mutation hooks for create/edit"
      exports: ["useIssueMutations"]
    - path: "taskflow/src/routes/dashboard/create-edit-issue/index.ts"
      provides: "Barrel re-export of CreateEditIssueModal"
  key_links:
    - from: "taskflow/src/routes/dashboard/create-edit-issue/CreateEditIssueModal.tsx"
      to: "taskflow/src/routes/dashboard/create-edit-issue/useCreateEditForm.ts"
      via: "import useCreateEditForm"
      pattern: "import.*useCreateEditForm"
    - from: "taskflow/src/routes/dashboard/create-edit-issue/CreateEditIssueModal.tsx"
      to: "taskflow/src/routes/dashboard/create-edit-issue/useIssueMutations.ts"
      via: "import useIssueMutations"
      pattern: "import.*useIssueMutations"
---

<objective>
Decompose CreateEditIssueModal.tsx (915 lines) into focused sub-components and custom hooks with useReducer replacing 21 useState calls.

Purpose: Make the modal maintainable by extracting form state, mutations, and repeated JSX sections into separate files.
Output: create-edit-issue/ subdirectory with orchestrator + hooks + sub-components.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-refactoring-type-safety/27-CONTEXT.md
@.planning/phases/27-refactoring-type-safety/27-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useCreateEditForm hook (useReducer) and useIssueMutations hook</name>
  <files>taskflow/src/routes/dashboard/create-edit-issue/useCreateEditForm.ts, taskflow/src/routes/dashboard/create-edit-issue/useIssueMutations.ts</files>
  <read_first>
    - taskflow/src/routes/dashboard/CreateEditIssueModal.tsx (full file -- understand all 21 useState calls, the reset useEffect, validation logic, and mutation handlers)
  </read_first>
  <action>
1. Create `taskflow/src/routes/dashboard/create-edit-issue/` directory.

2. Create `taskflow/src/routes/dashboard/create-edit-issue/useCreateEditForm.ts`:

   a. Define the `FormState` interface containing ALL fields currently managed by individual useState calls in CreateEditIssueModal. Read the file carefully to identify all 21 useState calls. Expected fields include (but verify against actual code):
   - selectedIssueType, summary, description
   - assigneeInputValue, selectedAssigneeName, showAssigneeResults
   - timeEstimate, priority, storyPoints
   - epicLinkKey, epicOpen, epicFilter
   - parentKey
   - customFieldValues (Record<string, string>)
   - customFieldInputValues (Record<string, string>)
   - customFieldAutoResults (Record<string, unknown[]>)
   - customFieldShowResults (Record<string, boolean>)
   - apiError (string | null)
   - linkRows (array type -- check actual type in source)

   b. Define `FormAction` discriminated union type with actions:
   - `{ type: 'SET_FIELD'; field: keyof FormState; value: FormState[keyof FormState] }`
   - `{ type: 'RESET'; initialValues?: EditInitialValues }` -- must support the reset-on-open behavior
   - `{ type: 'SET_ISSUE_TYPE'; issueType: IssueType }` -- if issue type changes affect other fields
   - Any other specific actions needed (check if changing issue type resets certain fields)

   c. Implement `formReducer(state: FormState, action: FormAction): FormState`

   d. Implement `useCreateEditForm(options: { isEdit: boolean; initialValues?: EditInitialValues })` hook:
   - Uses `useReducer(formReducer, initialState)`
   - Includes the reset `useEffect` that currently resets all 21 states when the modal opens or initialValues change
   - Includes any validation logic (summary required, etc.)
   - Returns `{ state, dispatch, isValid, resetForm }` or similar

   e. Define and export the `EditInitialValues` type if not already exported elsewhere.

3. Create `taskflow/src/routes/dashboard/create-edit-issue/useIssueMutations.ts`:

   a. Extract the `useMutation` hooks for create and edit operations from CreateEditIssueModal.
   b. Extract the `onSubmit` / form submission handler logic that calls the mutation.
   c. The hook should accept the form state and return `{ createMutation, editMutation, handleSubmit }` or similar.
   d. Import Jira service functions (createIssue, updateIssueField, etc.) from `@/services/jira`.
   e. Import TanStack Query hooks as needed.

4. Both hooks must maintain IDENTICAL behavior to the current inline code. The form must:
   - Reset all fields when modal opens in create mode
   - Pre-fill all fields when modal opens in edit mode
   - Clear assignee results when selection changes
   - Handle custom field autocomplete state
   - Validate before submit
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/dashboard/create-edit-issue/useCreateEditForm.ts exists
    - useCreateEditForm.ts contains `export function useCreateEditForm` or `export const useCreateEditForm`
    - useCreateEditForm.ts contains `useReducer(formReducer` or `useReducer(reducer`
    - useCreateEditForm.ts does NOT contain any `useState(` calls (all state is in the reducer)
    - useCreateEditForm.ts contains a `RESET` action type in the FormAction union
    - taskflow/src/routes/dashboard/create-edit-issue/useIssueMutations.ts exists
    - useIssueMutations.ts contains `export function useIssueMutations` or `export const useIssueMutations`
    - useIssueMutations.ts contains `useMutation` (from TanStack Query)
    - TypeScript compiles without errors (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>useCreateEditForm hook with useReducer and useIssueMutations hook created. Form state consolidated from 21 useState to single useReducer. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extract sub-components, create orchestrator, wire barrel export</name>
  <files>taskflow/src/routes/dashboard/create-edit-issue/IssueTypeSelector.tsx, taskflow/src/routes/dashboard/create-edit-issue/CustomFieldsSection.tsx, taskflow/src/routes/dashboard/create-edit-issue/LinkRowsSection.tsx, taskflow/src/routes/dashboard/create-edit-issue/CreateEditIssueModal.tsx, taskflow/src/routes/dashboard/create-edit-issue/index.ts, taskflow/src/routes/dashboard/CreateEditIssueModal.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/CreateEditIssueModal.tsx (full file -- identify JSX sections for each sub-component)
    - taskflow/src/routes/dashboard/create-edit-issue/useCreateEditForm.ts (from Task 1)
    - taskflow/src/routes/dashboard/create-edit-issue/useIssueMutations.ts (from Task 1)
  </read_first>
  <action>
1. Create `IssueTypeSelector.tsx`:
   - Extract the issue type dropdown/selector JSX from CreateEditIssueModal
   - Props: issue type list, selected type, onChange handler, any display props
   - Use prop threading (no context) per project convention

2. Create `CustomFieldsSection.tsx`:
   - Extract the dynamic custom fields rendering section
   - Props: createmeta fields, field values, onChange handlers, autocomplete state
   - This is the section that renders different input types based on CreatemetaField schema

3. Create `LinkRowsSection.tsx`:
   - Extract the issue link rows section (add/remove link rows)
   - Props: link rows array, onChange/add/remove handlers, link type options

4. Create the new `CreateEditIssueModal.tsx` in the subdirectory:
   - Import useCreateEditForm and useIssueMutations hooks
   - Import IssueTypeSelector, CustomFieldsSection, LinkRowsSection sub-components
   - Keep TanStack Query data-fetching hooks (useQuery for createmeta, epics, etc.) in the orchestrator
   - The orchestrator wires hooks + sub-components together
   - Target: ~200 lines (under 250 absolute max)
   - Must maintain ALL existing props interface (open, onClose, initialValues, etc.)

5. Create `index.ts` barrel:
   ```typescript
   export { CreateEditIssueModal } from './CreateEditIssueModal';
   ```
   Also re-export any types that consumers need (e.g., if any external file imports types from CreateEditIssueModal).

6. Update the ORIGINAL `taskflow/src/routes/dashboard/CreateEditIssueModal.tsx`:
   - Replace its entire content with a re-export: `export { CreateEditIssueModal } from './create-edit-issue';`
   - This ensures any direct imports of the old path still work.
   - Alternatively, delete the original file IF all imports use barrel resolution. Check: does any file import `from './CreateEditIssueModal'` or `from '@/routes/dashboard/CreateEditIssueModal'`? If so, keep the re-export shim. If all imports go through directory index resolution, the shim is unnecessary.

7. Run full test suite: `cd taskflow && npx vitest --run`
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/src/routes/dashboard/create-edit-issue/CreateEditIssueModal.tsx exists
    - `wc -l taskflow/src/routes/dashboard/create-edit-issue/CreateEditIssueModal.tsx` is under 250
    - taskflow/src/routes/dashboard/create-edit-issue/IssueTypeSelector.tsx exists
    - taskflow/src/routes/dashboard/create-edit-issue/CustomFieldsSection.tsx exists
    - taskflow/src/routes/dashboard/create-edit-issue/LinkRowsSection.tsx exists
    - taskflow/src/routes/dashboard/create-edit-issue/index.ts exists and contains `export { CreateEditIssueModal }`
    - The orchestrator file does NOT contain `useState(` (state is in useReducer via the hook)
    - All tests pass (npx vitest --run exits 0)
    - No import errors across the codebase (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>CreateEditIssueModal decomposed into orchestrator (~200 lines) + 2 hooks + 3 sub-components. All tests pass. Form state uses useReducer.</done>
</task>

</tasks>

<verification>
- `cd taskflow && npx vitest --run` — all tests pass
- `cd taskflow && npx tsc --noEmit` — no TypeScript errors
- `wc -l taskflow/src/routes/dashboard/create-edit-issue/CreateEditIssueModal.tsx` — under 250 lines
- `grep -c "useState(" taskflow/src/routes/dashboard/create-edit-issue/CreateEditIssueModal.tsx` — returns 0 (no useState in orchestrator)
- `grep "useReducer" taskflow/src/routes/dashboard/create-edit-issue/useCreateEditForm.ts` — found
</verification>

<success_criteria>
- CreateEditIssueModal (915 lines) decomposed into ~7 focused files
- Form state converted from 21 useState to useReducer
- Orchestrator under 250 lines
- All existing imports and tests continue to work
- Modal behavior identical: create, edit, reset, validate, submit
</success_criteria>

<output>
After completion, create `.planning/phases/27-refactoring-type-safety/27-03-SUMMARY.md`
</output>
