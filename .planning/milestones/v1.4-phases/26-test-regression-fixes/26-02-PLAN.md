---
phase: 26-test-regression-fixes
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
  - taskflow/src/routes/dashboard/BacklogPage.test.tsx
  - taskflow/src/routes/dashboard/MrAttentionTab.test.tsx
  - taskflow/src/routes/dashboard/IssueDetailSheet.test.tsx
  - taskflow/src/services/jira.test.ts
  - taskflow/src/services/notifications.test.ts
  - taskflow/src/routes/notifications/NotificationPopover.test.tsx
  - taskflow/src/routes/notifications/NotificationRow.test.tsx
  - taskflow/src/components/app/KeyboardShortcutsPanel.test.tsx
  - taskflow/src/components/app/RecentItemsPopover.test.tsx
autonomous: true
requirements: [TEST-03, TEST-05]

must_haves:
  truths:
    - "All 57 previously failing tests now pass"
    - "tsc --noEmit reports zero errors on test files"
    - "npm test runs with zero failures and zero warnings"
    - "No new test files were added (only existing tests fixed)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/SprintBoardTab.test.tsx"
      provides: "Fixed quickFilters mock, updated makeIssue types, all 17 tests pass"
    - path: "taskflow/src/routes/dashboard/BacklogPage.test.tsx"
      provides: "Fixed quickFilters mock, all 16 tests pass"
    - path: "taskflow/src/routes/dashboard/MrAttentionTab.test.tsx"
      provides: "Added useLocation to router mock, all 8 tests pass"
    - path: "taskflow/src/routes/dashboard/IssueDetailSheet.test.tsx"
      provides: "Fixed router context and updated assertions, all 7 tests pass"
    - path: "taskflow/src/services/jira.test.ts"
      provides: "Updated discoverCustomFields assertions for 5 fields including epicColorFieldKey"
    - path: "taskflow/src/services/notifications.test.ts"
      provides: "Updated notification matching assertions"
  key_links:
    - from: "taskflow/src/routes/dashboard/SprintBoardTab.test.tsx"
      to: "taskflow/src/stores/settings.store.ts"
      via: "vi.mock with quickFilters"
      pattern: "quickFilters.*\\[\\]"
    - from: "taskflow/src/routes/dashboard/MrAttentionTab.test.tsx"
      to: "react-router-dom"
      via: "vi.mock with useLocation"
      pattern: "useLocation"
---

<objective>
Fix all 57 failing tests across 10 test files and resolve 4 TypeScript errors in SprintBoardTab.test.tsx.

Purpose: Make the entire test suite green. This addresses the 6 Phase 8 regressions (TEST-03) plus all other failures, and fixes the remaining TS errors in test files (TEST-05). Combined with Plan 01's infrastructure fixes, this achieves all 4 success criteria for Phase 26.
Output: All 10 test files passing, tsc --noEmit clean on test files.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-test-regression-fixes/26-CONTEXT.md
@.planning/phases/26-test-regression-fixes/26-RESEARCH.md
@.planning/phases/26-test-regression-fixes/26-01-SUMMARY.md

<interfaces>
<!-- Settings store fields that ALL mocks must include (from settings.store.ts): -->
storyPointsFieldKey: string       (default: 'customfield_10016')
epicLinkFieldKey: string          (default: 'customfield_10014')
epicNameFieldKey: string          (default: 'customfield_10015')
epicColorFieldKey: string         (default: 'customfield_10013')
quickFilters: QuickFilter[]       (default: [])
addQuickFilter: (qf) => void
removeQuickFilter: (id) => void
renameQuickFilter: (id, name) => void
moveQuickFilter: (id, direction) => void

<!-- Router mock must include for MrAttentionTab/IssueDetailSheet: -->
useOutletContext, useNavigate, useLocation

<!-- JiraIssue type requires these fields (check current type definition): -->
All required fields in JiraIssue interface must be present in makeIssue() helper
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix high-volume test failures (SprintBoardTab, BacklogPage, MrAttentionTab -- 39 tests)</name>
  <files>
    taskflow/src/routes/dashboard/SprintBoardTab.test.tsx,
    taskflow/src/routes/dashboard/BacklogPage.test.tsx,
    taskflow/src/routes/dashboard/MrAttentionTab.test.tsx
  </files>
  <read_first>
    taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
    taskflow/src/routes/dashboard/BacklogPage.test.tsx
    taskflow/src/routes/dashboard/MrAttentionTab.test.tsx
    taskflow/src/routes/dashboard/SprintBoardTab.tsx
    taskflow/src/routes/dashboard/BacklogPage.tsx
    taskflow/src/routes/dashboard/MrAttentionTab.tsx
    taskflow/src/stores/settings.store.ts
    taskflow/src/types/jira.types.ts
    taskflow/src/components/filters/UnifiedFilterBar.tsx
  </read_first>
  <action>
**A. SprintBoardTab.test.tsx (15 failures + 4 TS errors):**

1. **Fix settings store mock** -- find the `vi.mock('@/stores/settings.store'` block and add these fields to the mock return value:
   - `quickFilters: []`
   - `addQuickFilter: vi.fn()`
   - `removeQuickFilter: vi.fn()`
   - `renameQuickFilter: vi.fn()`
   - `moveQuickFilter: vi.fn()`
   - `epicNameFieldKey: 'customfield_10015'`
   - `epicColorFieldKey: 'customfield_10013'`
   (Keep existing fields like `storyPointsFieldKey`, `epicLinkFieldKey`)

2. **Fix `makeIssue()` helper TS errors** -- read the current `JiraIssue` interface from `src/types/jira.types.ts`. The `makeIssue()` function returns objects missing required fields. Add ALL required fields with sensible defaults. Use proper typing -- NO `as JiraIssue` cast on partial objects. Instead, construct a complete object. Common missing fields may include `description`, `subtasks`, `timetracking`, `labels`, `components`, `parentKey`, `epicKey`, `storyPoints`, `assignee`, `reporter`, `priority`, `status`, `created`, `updated`. Check the actual interface and provide defaults for every required field.

3. **Run**: `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/SprintBoardTab.test.tsx` -- all 17 tests must pass.

**B. BacklogPage.test.tsx (16 failures):**

1. **Fix settings store mock** -- find the `vi.mock('@/stores/settings.store'` block and add the same fields as SprintBoardTab:
   - `quickFilters: []`
   - `addQuickFilter: vi.fn()`
   - `removeQuickFilter: vi.fn()`
   - `renameQuickFilter: vi.fn()`
   - `moveQuickFilter: vi.fn()`
   - `epicNameFieldKey: 'customfield_10015'`
   - `epicColorFieldKey: 'customfield_10013'`

2. **Check for any other missing mock fields** by reading the component imports and store usage. If `BacklogPage.tsx` or its children access other settings store fields, add those to the mock too.

3. **Run**: `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/BacklogPage.test.tsx` -- all 16 tests must pass.

**C. MrAttentionTab.test.tsx (8 failures):**

1. **Fix router mock** -- the existing `vi.mock('react-router-dom'` block is missing `useLocation`. Add it:
   ```typescript
   useLocation: vi.fn(() => ({ pathname: '/dashboard', search: '', hash: '', state: null, key: 'default' })),
   ```

2. **Check if any other missing exports** are needed by reading the component's imports from `react-router-dom`.

3. **Run**: `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/MrAttentionTab.test.tsx` -- all 8 tests must pass.

**Decision context (per user):** Update tests to match current production code. Only fix production code if test reveals genuine runtime bug. Use proper typing, no `as X` workarounds.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/SprintBoardTab.test.tsx src/routes/dashboard/BacklogPage.test.tsx src/routes/dashboard/MrAttentionTab.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `SprintBoardTab.test.tsx` contains `quickFilters: []`
    - `SprintBoardTab.test.tsx` contains `addQuickFilter: vi.fn()`
    - `SprintBoardTab.test.tsx` contains `epicColorFieldKey`
    - `BacklogPage.test.tsx` contains `quickFilters: []`
    - `BacklogPage.test.tsx` contains `epicColorFieldKey`
    - `MrAttentionTab.test.tsx` contains `useLocation`
    - `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/SprintBoardTab.test.tsx` exits 0
    - `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/BacklogPage.test.tsx` exits 0
    - `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/dashboard/MrAttentionTab.test.tsx` exits 0
    - `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | grep "SprintBoardTab.test"` returns empty (0 TS errors)
  </acceptance_criteria>
  <done>39 tests across SprintBoardTab, BacklogPage, and MrAttentionTab now pass. SprintBoardTab TS errors resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining test failures (IssueDetailSheet, services, notifications, misc -- 18 tests)</name>
  <files>
    taskflow/src/routes/dashboard/IssueDetailSheet.test.tsx,
    taskflow/src/services/jira.test.ts,
    taskflow/src/services/notifications.test.ts,
    taskflow/src/routes/notifications/NotificationPopover.test.tsx,
    taskflow/src/routes/notifications/NotificationRow.test.tsx,
    taskflow/src/components/app/KeyboardShortcutsPanel.test.tsx,
    taskflow/src/components/app/RecentItemsPopover.test.tsx
  </files>
  <read_first>
    taskflow/src/routes/dashboard/IssueDetailSheet.test.tsx
    taskflow/src/routes/dashboard/IssueDetailSheet.tsx
    taskflow/src/services/jira.test.ts
    taskflow/src/services/jira.ts
    taskflow/src/services/notifications.test.ts
    taskflow/src/services/notifications.ts
    taskflow/src/routes/notifications/NotificationPopover.test.tsx
    taskflow/src/routes/notifications/NotificationPopover.tsx
    taskflow/src/routes/notifications/NotificationRow.test.tsx
    taskflow/src/routes/notifications/NotificationRow.tsx
    taskflow/src/components/app/KeyboardShortcutsPanel.test.tsx
    taskflow/src/components/app/KeyboardShortcutsPanel.tsx
    taskflow/src/components/app/RecentItemsPopover.test.tsx
    taskflow/src/components/app/RecentItemsPopover.tsx
  </read_first>
  <action>
For each file below, read BOTH the test file AND its production counterpart. Compare the test assertions to the current production behavior. Update tests to match production code. Do NOT change production code unless a genuine runtime bug is found.

**A. IssueDetailSheet.test.tsx (7 failures):**
1. Add `useLocation` to the router mock if missing (same pattern as MrAttentionTab fix).
2. Read `IssueDetailSheet.tsx` to understand current API for optimistic updates, linked issues rendering, and comment threads.
3. Update each failing assertion to match current component output. Common issues: changed prop names, different rendering structure, updated callback signatures.
4. Run: `npx vitest run src/routes/dashboard/IssueDetailSheet.test.tsx`

**B. jira.test.ts (2 failures):**
1. Find the `discoverCustomFields` test(s). Current production returns 5 fields: `storyPointsFieldKey`, `epicLinkFieldKey`, `epicNameFieldKey`, `epicColorFieldKey`, and one more (check the actual function).
2. Update expected objects to include `epicColorFieldKey: 'customfield_10013'` (or whatever the actual field ID is based on the mock data/response in the test).
3. Run: `npx vitest run src/services/jira.test.ts`

**C. notifications.test.ts (4 failures):**
1. Read `notifications.ts` to understand current `fetchJiraNotifications` implementation -- specifically how assignee/reporter/watcher matching works.
2. Update test expectations to match the current production behavior for notification broadening.
3. Run: `npx vitest run src/services/notifications.test.ts`

**D. NotificationPopover.test.tsx (2 failures):**
1. Read `NotificationPopover.tsx` for current read/unread toggle behavior.
2. Update assertions to match. Check if button text, aria labels, or toggle mechanics changed.
3. Run: `npx vitest run src/routes/notifications/NotificationPopover.test.tsx`

**E. NotificationRow.test.tsx (1 failure):**
1. Read `NotificationRow.tsx` for current rendering (specifically `parentKey` chip).
2. Update assertion to match component output.
3. Run: `npx vitest run src/routes/notifications/NotificationRow.test.tsx`

**F. KeyboardShortcutsPanel.test.tsx (1 failure):**
1. Read `KeyboardShortcutsPanel.tsx` for current shortcut key badge text.
2. Update the expected text in the assertion.
3. Run: `npx vitest run src/components/app/KeyboardShortcutsPanel.test.tsx`

**G. RecentItemsPopover.test.tsx (1 failure):**
1. Read `RecentItemsPopover.tsx` for current `openUrl` call behavior.
2. Update the assertion (the function call signature or arguments may have changed).
3. Run: `npx vitest run src/components/app/RecentItemsPopover.test.tsx`

**Decision context (per user):** Minimal fixes only. Don't improve weak assertions. Don't add new tests. Just make existing tests pass by matching current production behavior.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run` exits 0 (all 493 tests pass)
    - `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run 2>&1 | grep "Tests.*failed"` returns empty (zero failures)
    - `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run 2>&1 | grep -c "Unhandled"` returns 0
    - `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | grep -c "\.test\."` returns 0 (zero TS errors in test files)
    - `jira.test.ts` contains `epicColorFieldKey`
    - `IssueDetailSheet.test.tsx` contains `useLocation`
    - No production files were modified (only .test. files changed)
  </acceptance_criteria>
  <done>All 57 previously failing tests pass. Full suite is green with zero failures, zero unhandled errors, zero TS errors in test files.</done>
</task>

</tasks>

<verification>
Run the full phase gate from taskflow directory:
1. `npm test` -- exits 0, zero failures, zero warnings
2. `npm test 2>&1 | grep -c "Unhandled"` -- returns 0
3. `npx tsc --noEmit 2>&1 | grep "\.test\."` -- returns empty
4. `npx tsc --noEmit 2>&1 | grep "jira.ts"` -- returns empty (unused var removed in Plan 01)
</verification>

<success_criteria>
- All 493 tests pass (57 previously failing now fixed)
- Zero "Unhandled Rejection" errors in test output
- Zero TypeScript errors in test files (SprintBoardTab.test.tsx TS errors resolved)
- Zero TypeScript errors in jira.ts (unused variable removed in Plan 01)
- `npm test` runs clean end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/26-test-regression-fixes/26-02-SUMMARY.md`
</output>
