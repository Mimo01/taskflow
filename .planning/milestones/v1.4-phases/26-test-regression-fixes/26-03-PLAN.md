---
phase: 26-test-regression-fixes
plan: 03
type: execute
wave: 1
depends_on: ["26-01"]
files_modified: ["taskflow/src/test/setup.ts"]
autonomous: true
requirements: [TEST-05]
gap_closure: true

must_haves:
  truths:
    - "tsc --noEmit exits with code 0 (zero TypeScript errors)"
    - "npm test still passes (489 tests, 0 failures)"
  artifacts:
    - path: "taskflow/src/test/setup.ts"
      provides: "Vitest globals type reference for vi.mock usage"
      contains: "/// <reference types"
  key_links:
    - from: "taskflow/src/test/setup.ts"
      to: "vitest/globals"
      via: "triple-slash reference directive"
      pattern: "reference types.*vitest/globals"
---

<objective>
Fix the TS2304 error in setup.ts where `vi` is used without TypeScript knowing about vitest globals.

Purpose: Close the single remaining verification gap — success criterion 3 requires `tsc --noEmit` to pass with zero errors.
Output: setup.ts with vitest globals type reference, tsc --noEmit exits 0.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-test-regression-fixes/26-VERIFICATION.md

<interfaces>
<!-- Current setup.ts content (28 lines) uses vi.mock() at line 11 without importing vi -->
<!-- tsconfig.json has no "types" array in compilerOptions -->
<!-- Fix: Add triple-slash reference to setup.ts (preferred over modifying tsconfig.json to keep vitest types scoped to test files) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vitest globals type reference to setup.ts</name>
  <files>taskflow/src/test/setup.ts</files>
  <read_first>taskflow/src/test/setup.ts, taskflow/tsconfig.json</read_first>
  <action>
Add `/// <reference types="vitest/globals" />` as the FIRST line of `taskflow/src/test/setup.ts` (before the `import '@testing-library/jest-dom'` line).

This tells TypeScript that `vi` is a known global provided by vitest, resolving the TS2304 error on line 11. Using a triple-slash directive in setup.ts is preferred over adding to tsconfig.json types array because it keeps vitest type pollution scoped to test infrastructure files only.

The resulting file should start with:
```
/// <reference types="vitest/globals" />
import '@testing-library/jest-dom';
```

Everything else in the file stays exactly as-is.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'reference types.*vitest/globals' taskflow/src/test/setup.ts` returns 1
    - `cd taskflow && npx tsc --noEmit` exits with code 0 (zero errors)
    - `cd taskflow && npm test` still passes (489 tests, 0 failures)
  </acceptance_criteria>
  <done>tsc --noEmit exits 0, npm test passes with 489 tests and 0 failures, setup.ts contains vitest globals reference</done>
</task>

</tasks>

<verification>
1. `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit` exits with code 0
2. `cd /Users/mimo/Desktop/Tasker/taskflow && npm test` reports 0 failures
3. `grep 'reference types.*vitest/globals' /Users/mimo/Desktop/Tasker/taskflow/src/test/setup.ts` matches
</verification>

<success_criteria>
- tsc --noEmit passes with zero errors (closes VERIFICATION.md gap)
- All 489 tests still pass with zero failures
- Phase 26 success criterion 3 is now satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/26-test-regression-fixes/26-03-SUMMARY.md`
</output>
