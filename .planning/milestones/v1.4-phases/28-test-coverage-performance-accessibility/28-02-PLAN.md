---
phase: 28-test-coverage-performance-accessibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/jira/issues.test.ts
  - taskflow/src/services/jira/sprints.test.ts
  - taskflow/src/services/jira/epics.test.ts
  - taskflow/src/services/jira/fields.test.ts
  - taskflow/src/services/jira/backlog.test.ts
  - taskflow/src/services/jira/client.test.ts
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "issues module has tests for fetchSprintIssues, fetchIssueDetail, createIssue, updateIssueField, searchJira (happy + error)"
    - "sprints module has tests for fetchActiveSprint, fetchSprintsForBoard, addIssuesToSprint (happy + error)"
    - "epics module has tests for fetchEpicsBasic, fetchEpicEnrichmentMap, fetchEpicsWithEnrichment, fetchEpicStories (happy + error)"
    - "fields module has tests for discoverCustomFields, fetchCreatemeta, fetchProjectStatuses (happy + error)"
    - "backlog module has tests for fetchBacklogIssues, fetchBacklogView (happy + error)"
    - "client module has tests for fetchAllSearchPages, fetchAllWorklogPages, isResponseLikeError (happy + error)"
  artifacts:
    - path: "taskflow/src/services/jira/issues.test.ts"
      provides: "issues module tests"
      contains: "vi.mock"
    - path: "taskflow/src/services/jira/sprints.test.ts"
      provides: "sprints module tests"
      contains: "vi.mock"
    - path: "taskflow/src/services/jira/epics.test.ts"
      provides: "epics module tests"
      contains: "vi.mock"
    - path: "taskflow/src/services/jira/fields.test.ts"
      provides: "fields module tests"
      contains: "vi.mock"
    - path: "taskflow/src/services/jira/backlog.test.ts"
      provides: "backlog module tests"
      contains: "vi.mock"
    - path: "taskflow/src/services/jira/client.test.ts"
      provides: "client module tests"
      contains: "vi.mock"
  key_links:
    - from: "issues.test.ts, sprints.test.ts, fields.test.ts, backlog.test.ts"
      to: "../../lib/apiFetch"
      via: "vi.mock('../../lib/apiFetch')"
      pattern: "vi\\.mock\\('../../lib/apiFetch'"
    - from: "epics.test.ts"
      to: "./client"
      via: "vi.mock('./client')"
      pattern: "vi\\.mock\\('./client'"
    - from: "client.test.ts"
      to: "../../lib/apiFetch"
      via: "vi.mock('../../lib/apiFetch')"
      pattern: "vi\\.mock\\('../../lib/apiFetch'"
---

<objective>
Write unit tests for 6 larger Jira service modules: issues, sprints, epics, fields, backlog, client.

Purpose: Complete TEST-01 for the remaining 6 Jira modules. These are more complex modules with multiple exports and pagination logic.
Output: 6 colocated test files in src/services/jira/
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-test-coverage-performance-accessibility/28-RESEARCH.md

<interfaces>
<!-- Mock targets vary by module: -->
<!-- issues.ts, sprints.ts, fields.ts, backlog.ts: import apiFetch from '../../lib/apiFetch' -->
<!-- epics.ts: imports fetchAllSearchPages from './client' (NOT apiFetch directly) -->
<!-- client.ts: imports apiFetch from '../../lib/apiFetch' -->
<!-- backlog.ts: also imports fetchAllSearchPages from './client' AND apiFetch -->

From issues.ts (9 exports — test top 5):
- fetchSprintIssues(baseUrl, token, boardId, sprintId, customFieldMap?) => Promise<JiraIssue[]>
- fetchIssueDetail(baseUrl, token, issueKey, customFieldMap?) => Promise<JiraIssue>
- createIssue(baseUrl, token, fields) => Promise<{ key: string }>
- updateIssueField(baseUrl, token, issueKey, fields) => Promise<void>
- searchJira(baseUrl, token, jql, maxResults?) => Promise<JiraIssue[]>

From sprints.ts (3 exports):
- fetchActiveSprint(baseUrl, token, boardId) => Promise<JiraSprint | null>
- fetchSprintsForBoard(baseUrl, token, boardId) => Promise<JiraSprint[]>
- addIssuesToSprint(baseUrl, token, sprintId, issueKeys) => Promise<void>

From epics.ts (4 exports — uses ./client):
- fetchEpicsBasic(baseUrl, token, projectKey) => Promise<JiraIssue[]>
- fetchEpicEnrichmentMap(baseUrl, token, projectKey) => Promise<Map<string, EpicEnriched>>
- fetchEpicsWithEnrichment(baseUrl, token, projectKey) => Promise<EpicEnriched[]>
- fetchEpicStories(baseUrl, token, epicKey) => Promise<JiraIssue[]>

From fields.ts (3 exports):
- discoverCustomFields(baseUrl, token) => Promise<Record<string, string>>
- fetchCreatemeta(baseUrl, token, projectKey, issueTypeId) => Promise<...>
- fetchProjectStatuses(baseUrl, token, projectKey) => Promise<...>

From backlog.ts (2 exports — uses both apiFetch and ./client):
- fetchBacklogIssues(baseUrl, token, projectKey, customFieldMap?) => Promise<JiraIssue[]>
- fetchBacklogView(baseUrl, token, projectKey, boardId, customFieldMap?) => Promise<BacklogView>

From client.ts (3 exports):
- fetchAllSearchPages(baseUrl, token, jql, fields, maxResults?) => async generator
- fetchAllWorklogPages(baseUrl, token, issueKey) => Promise<JiraWorklog[]>
- isResponseLikeError(err) => boolean (type guard)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for issues, sprints, fields modules</name>
  <files>
    taskflow/src/services/jira/issues.test.ts,
    taskflow/src/services/jira/sprints.test.ts,
    taskflow/src/services/jira/fields.test.ts
  </files>
  <read_first>
    taskflow/src/services/jira/issues.ts,
    taskflow/src/services/jira/sprints.ts,
    taskflow/src/services/jira/fields.ts,
    taskflow/src/services/jira/types.ts,
    taskflow/src/lib/apiFetch.ts,
    taskflow/src/lib/api-error.ts
  </read_first>
  <action>
Create 3 test files. All mock `../../lib/apiFetch`.

**issues.test.ts** — Test top 5 exports (fetchSprintIssues, fetchIssueDetail, createIssue, updateIssueField, searchJira):
1. Mock `../../lib/apiFetch` with `vi.mock('../../lib/apiFetch', () => ({ apiFetch: vi.fn() }))`.
2. `fetchSprintIssues` happy: mock apiFetch returning `{ ok: true, status: 200, json: async () => ({ issues: [{ key: 'PROJ-1', fields: { summary: 'Test' } }] }) }`. Verify returns array of issues.
3. `fetchSprintIssues` error: mock returning `{ ok: false, status: 401 }`. Expect throw.
4. `fetchIssueDetail` happy: mock returning single issue JSON `{ key: 'PROJ-1', fields: { summary: 'Detail' } }`. Verify returns issue object.
5. `fetchIssueDetail` error: mock returning `{ ok: false, status: 404 }`. Expect throw.
6. `createIssue` happy: mock returning `{ ok: true, status: 201, json: async () => ({ key: 'PROJ-2' }) }`. Verify returns `{ key: 'PROJ-2' }`. Verify apiFetch called with method 'POST'.
7. `createIssue` error: mock returning `{ ok: false, status: 400 }`. Expect throw.
8. `updateIssueField` happy: mock returning `{ ok: true, status: 204 }`. Verify method 'PUT'. Expect no throw.
9. `updateIssueField` error: mock returning `{ ok: false, status: 403 }`. Expect throw.
10. `searchJira` happy: mock returning issues array. Verify result shape.
11. `searchJira` error: mock returning `{ ok: false, status: 401 }`. Expect throw.

Note: issues.ts may use fetchAllSearchPages from ./client for some functions. Read the source carefully to determine which functions use apiFetch directly vs client. Mock both if needed: `vi.mock('./client', () => ({ fetchAllSearchPages: vi.fn() }))`.

**sprints.test.ts** — Test 3 exports:
1. Mock `../../lib/apiFetch`.
2. `fetchActiveSprint` happy: mock returning `{ ok: true, status: 200, json: async () => ({ values: [{ id: 1, name: 'Sprint 1', state: 'active' }] }) }`. Verify returns the active sprint.
3. `fetchActiveSprint` with no active sprint: mock returning empty values. Verify returns null.
4. `fetchActiveSprint` error: mock returning `{ ok: false, status: 401 }`. Expect throw.
5. `fetchSprintsForBoard` happy: mock returning sprints array. Verify returns array.
6. `fetchSprintsForBoard` error: expect throw.
7. `addIssuesToSprint` happy: mock returning `{ ok: true, status: 204 }`. Verify method 'POST'.
8. `addIssuesToSprint` error: expect throw.

**fields.test.ts** — Test 3 exports:
1. Mock `../../lib/apiFetch`.
2. `discoverCustomFields` happy: mock returning field list `[{ id: 'customfield_10001', name: 'Story Points' }]`. Verify returns mapped object.
3. `discoverCustomFields` error: expect throw.
4. `fetchCreatemeta` happy: mock returning createmeta JSON. Verify result.
5. `fetchCreatemeta` error: expect throw.
6. `fetchProjectStatuses` happy: mock returning status array. Verify result.
7. `fetchProjectStatuses` error: expect throw.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/services/jira/issues.test.ts src/services/jira/sprints.test.ts src/services/jira/fields.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - issues.test.ts contains at least 10 `it(` calls covering 5 functions with happy+error
    - sprints.test.ts contains at least 6 `it(` calls covering 3 functions
    - fields.test.ts contains at least 6 `it(` calls covering 3 functions
    - All 3 files pass: `npx vitest run src/services/jira/issues.test.ts src/services/jira/sprints.test.ts src/services/jira/fields.test.ts` exits 0
  </acceptance_criteria>
  <done>issues.test.ts, sprints.test.ts, fields.test.ts pass with happy+error cases for every tested export</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for epics, backlog, client modules</name>
  <files>
    taskflow/src/services/jira/epics.test.ts,
    taskflow/src/services/jira/backlog.test.ts,
    taskflow/src/services/jira/client.test.ts
  </files>
  <read_first>
    taskflow/src/services/jira/epics.ts,
    taskflow/src/services/jira/backlog.ts,
    taskflow/src/services/jira/client.ts,
    taskflow/src/services/jira/types.ts,
    taskflow/src/lib/apiFetch.ts
  </read_first>
  <action>
Create 3 test files. Note different mock targets:

**epics.test.ts** — epics.ts imports `fetchAllSearchPages` from `./client`, NOT apiFetch:
1. `vi.mock('./client', () => ({ fetchAllSearchPages: vi.fn() }))`.
2. `fetchAllSearchPages` is an async generator. Mock it to return issues: `vi.mocked(mockFetchAllSearchPages).mockImplementation(async function* () { yield { key: 'EPIC-1', fields: { summary: 'Epic 1', issuetype: { name: 'Epic' } } }; })`.
3. `fetchEpicsBasic` happy: verify returns array of epic issues.
4. `fetchEpicsBasic` error: mock generator to throw. Expect rejects.
5. `fetchEpicEnrichmentMap` happy: mock generator yielding epic + stories. Verify returns Map.
6. `fetchEpicStories` happy: mock returning story issues. Verify result.
7. `fetchEpicStories` error: mock throwing. Expect rejects.

**backlog.test.ts** — backlog.ts uses BOTH apiFetch AND fetchAllSearchPages from ./client:
1. Mock both: `vi.mock('../../lib/apiFetch', ...)` and `vi.mock('./client', ...)`.
2. `fetchBacklogIssues` happy: mock fetchAllSearchPages yielding backlog issues. Verify returns array.
3. `fetchBacklogIssues` error: mock throwing. Expect rejects.
4. `fetchBacklogView` happy: mock both apiFetch (for sprint data) and fetchAllSearchPages (for issues). Verify returns BacklogView shape with sprint and backlog sections.
5. `fetchBacklogView` error: expect throw on API failure.

**client.test.ts** — client.ts imports apiFetch directly:
1. Mock `../../lib/apiFetch`.
2. `isResponseLikeError` — pure function, no mock needed:
   - Pass `{ message: 'err', status: 401 }` => true.
   - Pass `new Error('nope')` => false.
   - Pass `null` => false.
3. `fetchAllSearchPages` happy: mock apiFetch returning paginated results. First call returns `{ issues: [...5 items], total: 8, startAt: 0, maxResults: 5 }`, second call returns `{ issues: [...3 items], total: 8, startAt: 5, maxResults: 5 }`. Collect all yielded issues. Verify 8 total.
4. `fetchAllSearchPages` error: mock returning `{ ok: false, status: 500 }`. Expect throw.
5. `fetchAllWorklogPages` happy: mock apiFetch returning worklogs. Verify returns array.
6. `fetchAllWorklogPages` error: mock returning `{ ok: false, status: 401 }`. Expect throw.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/services/jira/epics.test.ts src/services/jira/backlog.test.ts src/services/jira/client.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - epics.test.ts contains `vi.mock('./client'` (NOT `vi.mock('../../lib/apiFetch'`)
    - epics.test.ts contains at least 5 `it(` calls
    - backlog.test.ts mocks BOTH apiFetch and ./client
    - backlog.test.ts contains at least 4 `it(` calls
    - client.test.ts contains at least 6 `it(` calls (isResponseLikeError + fetchAllSearchPages + fetchAllWorklogPages)
    - All 3 files pass: `npx vitest run src/services/jira/epics.test.ts src/services/jira/backlog.test.ts src/services/jira/client.test.ts` exits 0
  </acceptance_criteria>
  <done>epics.test.ts, backlog.test.ts, client.test.ts pass with happy+error cases for every tested export</done>
</task>

</tasks>

<verification>
cd taskflow && npx vitest run src/services/jira/issues.test.ts src/services/jira/sprints.test.ts src/services/jira/epics.test.ts src/services/jira/fields.test.ts src/services/jira/backlog.test.ts src/services/jira/client.test.ts --reporter=verbose
</verification>

<success_criteria>
- 6 new test files exist for the larger Jira modules
- Each file tests every major exported function with happy path and error case
- epics.test.ts and backlog.test.ts correctly mock ./client (not just apiFetch)
- All 6 test files pass when run together
- Full test suite still passes
</success_criteria>

<output>
After completion, create `.planning/phases/28-test-coverage-performance-accessibility/28-02-SUMMARY.md`
</output>
