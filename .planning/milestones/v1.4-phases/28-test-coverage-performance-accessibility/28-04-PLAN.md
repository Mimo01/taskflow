---
phase: 28-test-coverage-performance-accessibility
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/package.json
  - taskflow/src/routes/dashboard/BacklogPage.tsx
  - taskflow/src/routes/notifications/NotificationPopover.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.tsx
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "BacklogPage issue list renders using useVirtualizer instead of .map()"
    - "NotificationPopover notification list renders using useVirtualizer with measureElement for variable heights"
    - "SprintBoardTab virtualizes swimlane rows using useVirtualizer, cards within visible swimlanes remain fully rendered"
    - "DragOverlay in SprintBoardTab renders outside the virtualized container"
    - "All three lists handle empty state (0 items) correctly"
    - "@tanstack/react-virtual is installed as a dependency"
  artifacts:
    - path: "taskflow/package.json"
      provides: "@tanstack/react-virtual dependency"
      contains: "@tanstack/react-virtual"
    - path: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      provides: "virtualized backlog list"
      contains: "useVirtualizer"
    - path: "taskflow/src/routes/notifications/NotificationPopover.tsx"
      provides: "virtualized notification list"
      contains: "useVirtualizer"
    - path: "taskflow/src/routes/dashboard/SprintBoardTab.tsx"
      provides: "virtualized sprint board swimlanes"
      contains: "useVirtualizer"
  key_links:
    - from: "BacklogPage.tsx"
      to: "@tanstack/react-virtual"
      via: "import { useVirtualizer }"
      pattern: "useVirtualizer"
    - from: "NotificationPopover.tsx"
      to: "@tanstack/react-virtual"
      via: "import { useVirtualizer }"
      pattern: "measureElement"
    - from: "SprintBoardTab.tsx"
      to: "@tanstack/react-virtual"
      via: "import { useVirtualizer }"
      pattern: "useVirtualizer"
---

<objective>
Install @tanstack/react-virtual and virtualize three long-scrolling lists: BacklogPage issue list, NotificationPopover list, and SprintBoardTab swimlanes.

Purpose: Satisfy PERF-01 — lists with 200+ items render without visible scroll jank by only mounting items in/near the viewport.
Output: 3 modified components using useVirtualizer, 1 new dependency installed
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-test-coverage-performance-accessibility/28-RESEARCH.md
@.planning/phases/28-test-coverage-performance-accessibility/28-UI-SPEC.md

<interfaces>
<!-- Virtualization contract from UI-SPEC.md: -->

BacklogPage:
- estimateSize: () => 44 (fixed height BacklogRow)
- overscan: 10
- No measureElement needed (fixed height)
- Scroll container: existing overflow-auto parent

NotificationPopover:
- estimateSize: () => 64 (variable height)
- overscan: 5
- measureElement callback on each rendered item
- Scope: shared list rendering for both popover and page views

SprintBoardTab:
- Virtualization unit: swimlane rows (story groups), NOT individual cards
- measureElement callback on each swimlane
- overscan: 5 swimlanes
- DragOverlay renders OUTSIDE the virtualized container
- Cards within a visible swimlane remain fully rendered
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @tanstack/react-virtual and virtualize BacklogPage</name>
  <files>
    taskflow/package.json,
    taskflow/src/routes/dashboard/BacklogPage.tsx
  </files>
  <read_first>
    taskflow/src/routes/dashboard/BacklogPage.tsx,
    taskflow/package.json
  </read_first>
  <action>
**Step 1: Install dependency:**
```bash
cd taskflow && npm install @tanstack/react-virtual
```

**Step 2: Virtualize BacklogPage issue list:**

In BacklogPage.tsx:
1. Add imports: `import { useVirtualizer } from '@tanstack/react-virtual'` and `import { useRef } from 'react'` (if useRef not already imported).
2. Find where filtered issues are rendered with `.map()` — this is the backlog issue list rendering section.
3. Add a `parentRef = useRef<HTMLDivElement>(null)` for the scroll container.
4. Create virtualizer:
```typescript
const rowVirtualizer = useVirtualizer({
  count: filteredIssues.length,
  getScrollElement: () => parentRef.current,
  estimateSize: () => 44,
  overscan: 10,
});
```
5. Replace the `.map()` rendering with virtualized rendering:
   - Wrap in a div with `ref={parentRef}` and `className="overflow-auto"` plus appropriate height (use existing container's height or `flex-1 overflow-auto` pattern).
   - Inner div with `style={{ height: \`${rowVirtualizer.getTotalSize()}px\`, position: 'relative' }}`.
   - Map over `rowVirtualizer.getVirtualItems()` instead of filteredIssues directly.
   - Each item div gets `style={{ position: 'absolute', top: 0, left: 0, width: '100%', transform: \`translateY(${virtualRow.start}px)\` }}` and `key={virtualRow.key}`.
   - Access actual issue via `filteredIssues[virtualRow.index]`.
6. Preserve existing empty state rendering — when filteredIssues.length === 0, show existing empty state component, NOT the virtualizer.
7. Preserve existing loading state — if data is loading, show existing skeleton/loading UI.

**Anti-regression rules:**
- Do NOT add horizontal scrollbar. Keep width: '100%' on items.
- Preserve all existing event handlers, drag-and-drop if present, and keyboard navigation.
- BacklogRow component receives the same props as before — only the rendering wrapper changes.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"@tanstack/react-virtual"` in dependencies
    - BacklogPage.tsx contains `import { useVirtualizer } from '@tanstack/react-virtual'`
    - BacklogPage.tsx contains `estimateSize: () => 44`
    - BacklogPage.tsx contains `overscan: 10`
    - BacklogPage.tsx contains `getVirtualItems()`
    - BacklogPage.tsx contains `getTotalSize()`
    - BacklogPage.tsx still handles empty state (0 items) without virtualizer rendering
    - Full test suite passes: `npx vitest run` exits 0
  </acceptance_criteria>
  <done>BacklogPage uses useVirtualizer for issue list rendering. @tanstack/react-virtual installed. No visual regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Virtualize NotificationPopover and SprintBoardTab swimlanes</name>
  <files>
    taskflow/src/routes/notifications/NotificationPopover.tsx,
    taskflow/src/routes/dashboard/SprintBoardTab.tsx
  </files>
  <read_first>
    taskflow/src/routes/notifications/NotificationPopover.tsx,
    taskflow/src/routes/dashboard/SprintBoardTab.tsx
  </read_first>
  <action>
**Part A: Virtualize NotificationPopover list:**

In NotificationPopover.tsx:
1. Add import: `import { useVirtualizer } from '@tanstack/react-virtual'`.
2. Add `useRef` for scroll container if not present.
3. Find where notification items are rendered with `.map()`.
4. Create virtualizer with variable-height support:
```typescript
const rowVirtualizer = useVirtualizer({
  count: notifications.length,
  getScrollElement: () => parentRef.current,
  estimateSize: () => 64,
  overscan: 5,
});
```
5. Replace `.map()` with virtualized rendering pattern:
   - Parent div with `ref={parentRef}` and overflow-auto + max-height (preserve existing container constraints).
   - Inner div with `height: rowVirtualizer.getTotalSize()` and `position: relative`.
   - Each virtual item positioned absolutely with `transform: translateY(${virtualRow.start}px)`.
   - Add `ref={rowVirtualizer.measureElement}` on each rendered item div for dynamic height measurement using `data-index={virtualRow.index}`.
6. Preserve empty state and loading state.
7. If list is shared between popover and full-page views, apply virtualization to the shared component so both benefit.

**Part B: Virtualize SprintBoardTab swimlanes:**

In SprintBoardTab.tsx:
1. Add import: `import { useVirtualizer } from '@tanstack/react-virtual'`.
2. Identify the swimlane rendering loop — these are the story groups (parent story + its subtask cards across columns).
3. Create virtualizer for swimlane rows:
```typescript
const swimlaneVirtualizer = useVirtualizer({
  count: swimlanes.length, // or however swimlane groups are structured
  getScrollElement: () => boardScrollRef.current,
  estimateSize: () => 120, // rough swimlane height — will be measured
  overscan: 5,
});
```
4. Replace swimlane `.map()` with virtualized rendering:
   - Each swimlane row positioned absolutely.
   - Add `ref={swimlaneVirtualizer.measureElement}` and `data-index={virtualRow.index}` on each swimlane container for dynamic measurement.
   - Cards WITHIN a visible swimlane remain fully rendered (NOT virtualized at card level).
5. CRITICAL: Ensure `<DragOverlay>` renders OUTSIDE the virtualized container div. DragOverlay must be a sibling of (not inside) the virtualizer's inner div. It should already be at the DndContext level — verify this is the case.
6. Keep column headers sticky (not virtualized).
7. Preserve empty state and loading state.

**Anti-regression rules for both components:**
- No horizontal scrollbars introduced.
- Keyboard J/K navigation must still work (scroll-into-view for focused item).
- Loading skeletons render during data fetch (before virtualizer has items).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - NotificationPopover.tsx contains `import { useVirtualizer } from '@tanstack/react-virtual'`
    - NotificationPopover.tsx contains `measureElement` (for variable heights)
    - NotificationPopover.tsx contains `estimateSize: () => 64`
    - NotificationPopover.tsx contains `getVirtualItems()`
    - SprintBoardTab.tsx contains `import { useVirtualizer } from '@tanstack/react-virtual'`
    - SprintBoardTab.tsx contains `measureElement`
    - SprintBoardTab.tsx contains `getVirtualItems()`
    - SprintBoardTab.tsx DragOverlay is NOT inside the virtualizer inner div
    - Full test suite passes: `npx vitest run` exits 0
  </acceptance_criteria>
  <done>NotificationPopover and SprintBoardTab use useVirtualizer. Variable heights measured dynamically. DragOverlay remains outside virtualized container. No visual regressions.</done>
</task>

</tasks>

<verification>
cd taskflow && npx vitest run --reporter=verbose
</verification>

<success_criteria>
- @tanstack/react-virtual is installed in package.json
- All 3 components (BacklogPage, NotificationPopover, SprintBoardTab) use useVirtualizer
- BacklogPage uses fixed estimateSize (44px), others use measureElement for variable heights
- SprintBoardTab virtualizes swimlanes (not individual cards), DragOverlay outside virtualizer
- Empty states still render when item count is 0
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/28-test-coverage-performance-accessibility/28-04-SUMMARY.md`
</output>
