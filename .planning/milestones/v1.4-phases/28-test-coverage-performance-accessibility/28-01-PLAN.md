---
phase: 28-test-coverage-performance-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/jira/comments.test.ts
  - taskflow/src/services/jira/transitions.test.ts
  - taskflow/src/services/jira/versions.test.ts
  - taskflow/src/services/jira/worklogs.test.ts
  - taskflow/src/services/jira/links.test.ts
  - taskflow/src/services/jira/projects.test.ts
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "comments module has tests for fetchComments, postComment, updateComment, deleteComment (happy + error)"
    - "transitions module has tests for fetchTransitions, postTransition (happy + error)"
    - "versions module has tests for fetchFixVersions (happy + error)"
    - "worklogs module has tests for fetchIssueWorklogs (happy + error)"
    - "links module has tests for fetchIssueLinkTypes, createIssueLink (happy + error)"
    - "projects module has tests for validateJira, listJiraProjects (happy + error)"
  artifacts:
    - path: "taskflow/src/services/jira/comments.test.ts"
      provides: "comments module tests"
      contains: "vi.mock"
    - path: "taskflow/src/services/jira/transitions.test.ts"
      provides: "transitions module tests"
      contains: "vi.mock"
    - path: "taskflow/src/services/jira/versions.test.ts"
      provides: "versions module tests"
      contains: "vi.mock"
    - path: "taskflow/src/services/jira/worklogs.test.ts"
      provides: "worklogs module tests"
      contains: "vi.mock"
    - path: "taskflow/src/services/jira/links.test.ts"
      provides: "links module tests"
      contains: "vi.mock"
    - path: "taskflow/src/services/jira/projects.test.ts"
      provides: "projects module tests"
      contains: "vi.mock"
  key_links:
    - from: "each test file"
      to: "../../lib/apiFetch"
      via: "vi.mock('../../lib/apiFetch')"
      pattern: "vi\\.mock\\('../../lib/apiFetch'"
---

<objective>
Write unit tests for 6 smaller Jira service modules: comments, transitions, versions, worklogs, links, projects.

Purpose: Satisfy TEST-01 for 6 of 12 Jira modules. Each module follows the same apiFetch mock pattern — happy path returns expected data, error case throws on non-ok response.
Output: 6 colocated test files in src/services/jira/
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-test-coverage-performance-accessibility/28-RESEARCH.md

<interfaces>
<!-- Mock pattern: all 6 modules import apiFetch from '../../lib/apiFetch'. Mock that module. -->
<!-- For worklogs: imports fetchAllWorklogPages from './client' — mock './client' instead -->
<!-- For versions: imports apiFetch from '../../lib/apiFetch' -->

From comments.ts (4 exports):
- fetchComments(baseUrl, token, issueKey) => Promise<JiraComment[]>
- postComment(baseUrl, token, issueKey, body) => Promise<void>
- updateComment(baseUrl, token, issueKey, commentId, body) => Promise<void>
- deleteComment(baseUrl, token, issueKey, commentId) => Promise<void>

From transitions.ts (2 exports):
- fetchTransitions(baseUrl, token, issueKey) => Promise<JiraTransition[]>
- postTransition(baseUrl, token, issueKey, transitionId) => Promise<void>

From versions.ts (1 export):
- fetchFixVersions(baseUrl, token, projectKey) => Promise<JiraVersion[]>

From worklogs.ts (1 export — uses ./client, NOT apiFetch):
- fetchIssueWorklogs(baseUrl, token, issueKey) => Promise<JiraWorklog[]>

From links.ts (2 exports):
- fetchIssueLinkTypes(baseUrl, token) => Promise<JiraIssueLinkType[]>
- createIssueLink(baseUrl, token, payload) => Promise<void>

From projects.ts (2 exports):
- validateJira(baseUrl, token) => Promise<JiraUser>
- listJiraProjects(baseUrl, token) => Promise<JiraProject[]>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for comments, transitions, versions modules</name>
  <files>
    taskflow/src/services/jira/comments.test.ts,
    taskflow/src/services/jira/transitions.test.ts,
    taskflow/src/services/jira/versions.test.ts
  </files>
  <read_first>
    taskflow/src/services/jira/comments.ts,
    taskflow/src/services/jira/transitions.ts,
    taskflow/src/services/jira/versions.ts,
    taskflow/src/services/jira/types.ts,
    taskflow/src/lib/apiFetch.ts,
    taskflow/src/lib/api-error.ts,
    taskflow/src/stores/notifications.store.test.ts
  </read_first>
  <action>
Create 3 test files following the established project test pattern:

**comments.test.ts** — Test all 4 exports:
1. Mock `../../lib/apiFetch` with `vi.mock('../../lib/apiFetch', () => ({ apiFetch: vi.fn() }))`, then import `apiFetch` and cast with `vi.mocked()`.
2. `beforeEach(() => { vi.clearAllMocks(); })`.
3. Tests for `fetchComments`:
   - Happy: mock apiFetch returning `{ ok: true, status: 200, json: async () => ({ comments: [{ id: '1', body: 'hello' }] }) }`. Expect result to be `[{ id: '1', body: 'hello' }]`.
   - Error 401: mock apiFetch returning `{ ok: false, status: 401 }`. Expect to throw (ApiError).
4. Tests for `postComment`:
   - Happy: mock apiFetch returning `{ ok: true, status: 201 }`. Expect no throw. Verify apiFetch was called with method 'POST' and body containing `{ body: 'test comment' }`.
   - Error 403: mock returning `{ ok: false, status: 403 }`. Expect throw.
5. Tests for `updateComment`:
   - Happy: mock returning `{ ok: true, status: 200 }`. Expect no throw. Verify method 'PUT'.
   - Error: mock returning `{ ok: false, status: 500 }`. Expect throw.
6. Tests for `deleteComment`:
   - Happy: mock returning `{ ok: true, status: 204 }`. Expect no throw. Verify method 'DELETE'.
   - Error 401: mock returning `{ ok: false, status: 401 }`. Expect throw.

**transitions.test.ts** — Test 2 exports:
1. Same mock pattern as comments.
2. `fetchTransitions` happy: mock returns `{ ok: true, status: 200, json: async () => ({ transitions: [{ id: '1', name: 'Done' }] }) }`.
3. `fetchTransitions` error: mock returns `{ ok: false, status: 401 }`. Expect throw.
4. `postTransition` happy: mock returns `{ ok: true, status: 204 }`. Verify method 'POST', body `{ transition: { id: 'txn-1' } }`.
5. `postTransition` error: mock returns `{ ok: false, status: 403 }`. Expect throw.

**versions.test.ts** — Test 1 export:
1. Same mock pattern.
2. `fetchFixVersions` happy: mock returns `{ ok: true, status: 200, json: async () => ([{ id: '1', name: 'v1.0', released: false }]) }`.
3. `fetchFixVersions` error: mock returns `{ ok: false, status: 401 }`. Expect throw.

All test files use `import { beforeEach, describe, expect, it, vi } from 'vitest'` at top. Use baseUrl `'https://jira.example.com'` and token `'test-token'` constants.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/services/jira/comments.test.ts src/services/jira/transitions.test.ts src/services/jira/versions.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - comments.test.ts contains `vi.mock('../../lib/apiFetch'`
    - comments.test.ts contains `describe('comments'`
    - comments.test.ts contains at least 8 `it(` calls (4 happy + 4 error for 4 functions)
    - transitions.test.ts contains at least 4 `it(` calls
    - versions.test.ts contains at least 2 `it(` calls
    - All 3 test files pass: `npx vitest run src/services/jira/comments.test.ts src/services/jira/transitions.test.ts src/services/jira/versions.test.ts` exits 0
  </acceptance_criteria>
  <done>comments.test.ts, transitions.test.ts, versions.test.ts all pass with happy+error cases for every export</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for worklogs, links, projects modules</name>
  <files>
    taskflow/src/services/jira/worklogs.test.ts,
    taskflow/src/services/jira/links.test.ts,
    taskflow/src/services/jira/projects.test.ts
  </files>
  <read_first>
    taskflow/src/services/jira/worklogs.ts,
    taskflow/src/services/jira/links.ts,
    taskflow/src/services/jira/projects.ts,
    taskflow/src/services/jira/client.ts,
    taskflow/src/services/jira/types.ts,
    taskflow/src/lib/apiFetch.ts
  </read_first>
  <action>
Create 3 test files:

**worklogs.test.ts** — IMPORTANT: worklogs.ts imports `fetchAllWorklogPages` from `./client`, NOT apiFetch. Mock `./client` instead:
1. `vi.mock('./client', () => ({ fetchAllWorklogPages: vi.fn() }))`.
2. Import and vi.mocked the mock.
3. `fetchIssueWorklogs` happy: mock `fetchAllWorklogPages` returning `[{ author: { displayName: 'Alice' }, timeSpentSeconds: 3600, started: '2026-01-01' }]`. Verify result shape.
4. `fetchIssueWorklogs` error: mock rejecting with an Error. Expect rejects.

**links.test.ts** — Test 2 exports:
1. Mock `../../lib/apiFetch`.
2. `fetchIssueLinkTypes` happy: mock returns `{ ok: true, status: 200, json: async () => ({ issueLinkTypes: [{ id: '1', name: 'Blocks', inward: 'is blocked by', outward: 'blocks' }] }) }`.
3. `fetchIssueLinkTypes` error: mock returns `{ ok: false, status: 401 }`. Expect throw.
4. `createIssueLink` happy: mock returns `{ ok: true, status: 201 }`. Verify method 'POST' and body has inwardIssue/outwardIssue/type keys.
5. `createIssueLink` error: mock returns `{ ok: false, status: 400 }`. Expect throw.

**projects.test.ts** — Test 2 exports:
1. Mock `../../lib/apiFetch`.
2. `validateJira` happy: mock returns `{ ok: true, status: 200, json: async () => ({ key: 'admin', displayName: 'Admin User' }) }`. Verify returns JiraUser object.
3. `validateJira` error 401: mock returns `{ ok: false, status: 401 }`. Expect throw.
4. `listJiraProjects` happy: mock returns `{ ok: true, status: 200, json: async () => ([{ key: 'PROJ', name: 'Project', id: '1' }]) }`. Verify returns array.
5. `listJiraProjects` error: mock returns `{ ok: false, status: 403 }`. Expect throw.

Use same patterns as Task 1: `vi.clearAllMocks()` in beforeEach, baseUrl and token constants.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/services/jira/worklogs.test.ts src/services/jira/links.test.ts src/services/jira/projects.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - worklogs.test.ts contains `vi.mock('./client'` (NOT `vi.mock('../../lib/apiFetch'`)
    - worklogs.test.ts contains at least 2 `it(` calls
    - links.test.ts contains `vi.mock('../../lib/apiFetch'`
    - links.test.ts contains at least 4 `it(` calls
    - projects.test.ts contains at least 4 `it(` calls
    - All 3 test files pass: `npx vitest run src/services/jira/worklogs.test.ts src/services/jira/links.test.ts src/services/jira/projects.test.ts` exits 0
  </acceptance_criteria>
  <done>worklogs.test.ts, links.test.ts, projects.test.ts all pass with happy+error cases for every export</done>
</task>

</tasks>

<verification>
cd taskflow && npx vitest run src/services/jira/comments.test.ts src/services/jira/transitions.test.ts src/services/jira/versions.test.ts src/services/jira/worklogs.test.ts src/services/jira/links.test.ts src/services/jira/projects.test.ts --reporter=verbose
</verification>

<success_criteria>
- 6 new test files exist, colocated with their source modules
- Each file tests every exported function with at least one happy path and one error case
- All 6 test files pass when run together
- Full test suite still passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/28-test-coverage-performance-accessibility/28-01-SUMMARY.md`
</output>
