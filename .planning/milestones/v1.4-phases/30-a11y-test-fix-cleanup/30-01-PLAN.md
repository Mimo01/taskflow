---
phase: 30-a11y-test-fix-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - A11Y-01

must_haves:
  truths:
    - "A11Y-01 checkbox in REQUIREMENTS.md is marked complete [x]"
    - "A11Y-01 traceability row in REQUIREMENTS.md shows Complete"
    - "Full test suite passes with zero failures"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated A11Y-01 status"
      contains: "[x] **A11Y-01**"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "ConnectionsSection.test.tsx"
      via: "A11Y-01 requirement tracks the test fix"
      pattern: "A11Y-01.*Complete"
---

<objective>
Mark the A11Y-01 requirement as complete in REQUIREMENTS.md and verify the full test suite is green.

Purpose: Close the sole remaining v1.4 audit gap. The actual code fix (tightening the ConnectionsSection test query) was already applied. This plan updates the tracking documentation to reflect reality.
Output: REQUIREMENTS.md with A11Y-01 checked off and traceability updated.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-a11y-test-fix-cleanup/30-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update A11Y-01 status in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (verify current checkbox and traceability state)
  </read_first>
  <action>
Make two edits to `.planning/REQUIREMENTS.md`:

1. **Line 50 — checkbox update:** Change:
   ```
   - [ ] **A11Y-01**: All form inputs in CreateEditIssueModal and ConnectionsSection have proper aria labels
   ```
   to:
   ```
   - [x] **A11Y-01**: All form inputs in CreateEditIssueModal and ConnectionsSection have proper aria labels
   ```

2. **Line 102 — traceability table update:** Change:
   ```
   | A11Y-01 | Phase 30 | Pending |
   ```
   to:
   ```
   | A11Y-01 | Phase 30 | Complete |
   ```

No other lines should be modified.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` line 50 contains `- [x] **A11Y-01**`
    - `.planning/REQUIREMENTS.md` line 102 contains `| A11Y-01 | Phase 30 | Complete |`
    - No `[ ]` (unchecked) checkboxes remain in the v1.4 requirements section (lines 12-58)
    - No `Pending` entries remain in the traceability table (lines 80-108)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "\[ \]" .planning/REQUIREMENTS.md | head -5; grep -n "Pending" .planning/REQUIREMENTS.md | head -5; echo "---"; grep "A11Y-01" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>A11Y-01 checkbox is [x] and traceability row shows Complete. Zero unchecked boxes and zero Pending rows remain in v1.4 section.</done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suite passes</name>
  <files></files>
  <read_first>
    - taskflow/vitest.config.ts (confirm test runner config)
  </read_first>
  <action>
Run the full Vitest test suite to confirm zero failures:

```bash
cd taskflow && npx vitest run
```

Expected: 60+ test files pass, 615+ tests green, 0 failures. If any test fails, diagnose and fix before marking the plan complete.

Also run the specific ConnectionsSection test to double-confirm the A11Y-01 fix:

```bash
cd taskflow && npx vitest run src/routes/settings/ConnectionsSection.test.tsx
```

Expected: 9/9 tests pass.
  </action>
  <acceptance_criteria>
    - `cd taskflow && npx vitest run` exits with code 0
    - `cd taskflow && npx vitest run src/routes/settings/ConnectionsSection.test.tsx` exits with code 0
    - Output shows 0 failed tests
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>Full test suite passes with zero failures. ConnectionsSection tests pass 9/9.</done>
</task>

</tasks>

<verification>
1. `grep "\[x\] \*\*A11Y-01\*\*" .planning/REQUIREMENTS.md` returns a match
2. `grep "A11Y-01 | Phase 30 | Complete" .planning/REQUIREMENTS.md` returns a match
3. `cd taskflow && npx vitest run` exits 0 with 0 failures
</verification>

<success_criteria>
- A11Y-01 requirement marked complete in both checkbox and traceability table
- All 27 v1.4 requirement checkboxes are [x] (checked)
- Full test suite green (615+ tests, 0 failures)
</success_criteria>

<output>
After completion, create `.planning/phases/30-a11y-test-fix-cleanup/30-01-SUMMARY.md`
</output>
