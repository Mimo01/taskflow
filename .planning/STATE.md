---
gsd_state_version: 1.0
milestone: v1.13
milestone_name: Personal Workspace
status: planning
last_updated: "2026-06-13T22:28:16.515Z"
last_activity: 2026-06-13
progress:
  total_phases: 0
  completed_phases: 0
  total_plans: 0
  completed_plans: 0
  percent: 0
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-06-02)

**Core value:** Developers and PMs can see everything they need — tasks, MRs, sprint state, notifications, and test execution health — in one place, without switching between Jira, GitLab, and AIO.
**Current focus:** Phase 80 — subtask-templates-and-bulk-creation

## Current Position

Phase: Not started (defining requirements)
Plan: —
Status: Defining requirements
Last activity: 2026-06-13 — Milestone v1.13 started

## Performance Metrics

**Velocity (v1.11 reference):**

- Plans completed: 22 (5 phases, 4 days, 284 commits)
- Average phase size: ~4.4 plans
- LOC delta: +48,340 / −16,981

**By Phase (v1.12 planned):**

| Phase | Requirements | Description |
|-------|-------------|-------------|
| 76 | VISUAL-01..05 | Done-state strikethrough + card color stripes + shared utils + rank service stub |
| 77 | PEEK-01..07, DETAIL-01..02 | Universal non-blocking peek slideover + issue-detail refinements |
| 78 | RANK-01..05 | Drag-to-rank on Backlog active-sprint list |
| 79 | TRAN-01..05 | Drag-to-transition on Sprint Board with per-transition drop zones |
| 80 | SUBTPL-01..08 | Subtask templates in Settings + bulk creation modal |
| Phase 77 P01 | 15 | 2 tasks | 4 files |
| Phase 77 P03 | 45min | 4 tasks | 7 files |
| Phase 77 P04 | 30min | 4 tasks | 8 files |
| Phase 78-drag-to-rank-on-backlog P01 | 8min | 3 tasks | 6 files |
| Phase 78-drag-to-rank-on-backlog P02 | 3min | 2 tasks | 2 files |
| Phase 78-drag-to-rank-on-backlog P03 | 3min | 1 tasks | 2 files |

## Accumulated Context

### Decisions

Key decisions for v1.12 (drawn from research + PROJECT.md):

- Peek primitive: `IssueDetailSheet` with `modal={false}` on `Sheet` — resolve `aria-hidden` suppression at Phase 77 plan step before implementation
- `@dnd-kit` install in Phase 78 (removes Phase 67 absence guard test); reused by Phase 79 — single install for both drag features
- `rankCustomFieldId` read from `GhBacklogResponse` cache — never hardcoded; unit test asserts fixture value `10105`
- Flicker mitigation: `cancelQueries` in `onMutate` + `isDraggingRef`-gated local state as rendered source of truth during drag
- `hasScreen: true` transitions filtered from drop zone targets (remain accessible via StatusPopover)
- Bulk create: sequential `for` loop (not `Promise.all`); per-row status tracking; retry-failed-only on partial failure
- DETAIL-01/02 folded into Phase 77 — both require the `onIssuePeek` click model established in that phase
- Phase 80 independent of Phases 77-79; depends only on Phase 76 shared primitives
- [Phase ?]: peekPanelWidth defaults to null (not 480) in initialSettings — mirrors issueDetailPanelWidth; PeekPanel applies ?? 480 at read time (Plan 03)
- [Phase ?]: A1 confirmed — JiraIssueDetail.fields.parent present at types.ts:152, no type change needed
- [Phase ?]: CommandPalette body-select routes to peek (onOpenIssue); key-element navigate split delivered in Plan 04 Task 3
- [Phase ?]: NotificationPopover body row routes to peek; NotificationRow key split delivered in Plan 04 Task 3
- [Phase ?]: min-h-0 required on flex-row squeeze wrapper (A5) for height containment in Safari
- [Phase ?]: [Phase 77-04]: D-10 key/body split applied universally — div role=button body→onOpenIssue, inner key button→stopPropagation→navigate; Pitfall 1 avoided by outer-button-to-div conversion
- [Phase ?]: CR-01: cross-bucket midpoint stays in lower bucket, extends before value with midChar 'i'
- [Phase ?]: CR-02: digit-by-digit BigInt parseBase36 replaces BigInt(parseInt) to avoid float64 precision loss
- [Phase 78-04]: SortableContext per-section (sprint-<id>/backlog id), localOrder Map flicker gate, DragOverlay isOverlay, justDragged 50ms guard
- [Phase 78-04]: rankMutation onMutate calls cancelQueries(['gh-backlog',boardId]) + snapshots cache; onError restores snapshot + localOrder; onSettled invalidates

### Blockers/Concerns

- Phase 77: `Sheet modal={false}` vs CSS `position:fixed` — must verify `@base-ui/react Dialog modal={false}` suppresses `aria-hidden` on document root before writing plans
- Phase 79: `hasScreen` propagation through `__adaptToJiraTransition` — confirm before writing plans
- Apple Developer ID certificate not yet acquired — blocks macOS notarization (carried from v1.7)
- Windows code signing decision pending (carried from v1.7)

### Quick Tasks Completed

| # | Description | Date | Commit | Status | Directory |
|---|-------------|------|--------|--------|-----------|
| 260602-x58 | Standup Notes: collapse story status transitions to initial → final | 2026-06-02 | 19ccf8ed | | [260602-x58-on-standup-notes-page-when-presenting-st](./quick/260602-x58-on-standup-notes-page-when-presenting-st/) |
| 260603-fb8 | Let users select which Jira board (fix wrong rapidViewId/board id) | 2026-06-03 | e3f3b752 | Verified | [260603-fb8-select-jira-board](./quick/260603-fb8-select-jira-board/) |
| 260604-knq | Backlog drag-to-rank: drop DragOverlay, drag row in place (keep autoScroll synced) | 2026-06-04 | 2a2a4c30 | | [260604-knq-phase-78-backlog-drag-to-rank-keep-dnd-k](./quick/260604-knq-phase-78-backlog-drag-to-rank-keep-dnd-k/) |
| 260605-hb4 | Notifications & dashboard issue clicks open the full page (not the peek sidebar) | 2026-06-05 | 2cc4a751 | Verified | [260605-hb4-currently-all-clicks-on-issues-except-ke](./quick/260605-hb4-currently-all-clicks-on-issues-except-ke/) |
| 260605-hx2 | Resolution control set via workflow transition — sidebar + StatusPopover + board drag & right-click picker (reworked from rejected field-PUT) | 2026-06-06 | f604bb13 | Verified (live UAT approved on ESHOP-20308) | [260605-hx2-add-resolution-field-control-to-issue-de](./quick/260605-hx2-add-resolution-field-control-to-issue-de/) |
| 260606-o5y | Sprint board: story swimlane rows open the issue peek preview like cards (PEEK-01 body→peek, PEEK-05 key→full-page); all 3 sites wired; WR-01 keyboard double-fire guard added to row + card | 2026-06-06 | b96862cc | Approved & closed | [260606-o5y-on-sprint-board-story-rows-open-issue-pr](./quick/260606-o5y-on-sprint-board-story-rows-open-issue-pr/) |
| 260606-oqf | Issue peek panel: add elevation so it stops blending in — leftward shadow + ring-1 ring-foreground/10 (matches sheet/popover/dialog conventions, theme-aware light+dark) | 2026-06-06 | a33b9b5b | Approved & closed | [260606-oqf-on-issue-peek-panel-add-more-distinction](./quick/260606-oqf-on-issue-peek-panel-add-more-distinction/) |
| 260606-oyy | Sprint board: priority now shown as Jira iconUrl icon (card footer + swimlane header) instead of left border; card left border repurposed to issue-type color via issueTypeStripeClass(); new PriorityIcon component; a11y alt text; issue-type stripe tuned to 3px per UAT | 2026-06-06 | a553b75b | Approved & closed | [260606-oyy-on-sprint-board-i-want-to-change-how-pri](./quick/260606-oyy-on-sprint-board-i-want-to-change-how-pri/) |
| 260606-pxn | Backlog view: show Jira priority icon in task rows — reuses PriorityIcon in its own column (key → priority → title, like the swimlane); explicit-px wrapper so the column holds width in the WebKit virtualized table (class-sized content collapses to 0); px-0 per UAT | 2026-06-06 | f686edd7 | Approved & closed | [260606-pxn-on-backlog-view-put-the-priority-icon-in](./quick/260606-pxn-on-backlog-view-put-the-priority-icon-in/) |
| 260606-qfn | Add issue-type icon (Story/Bug/Task/Epic) to backlog rows + sprint board story swimlane header — reuses IssueTypeIcon; dedicated column BEFORE the key in both (type → key → priority → summary, consistent per user); explicit-px wrapper on the backlog cell; issueTypeName prop threaded to all 3 SprintBoardTab call sites; null-safe issuetype?.name; npm run check GREEN | 2026-06-06 | 825ddbda | Approved & closed | [260606-qfn-add-issue-type-icon-to-backlog-issue-row](./quick/260606-qfn-add-issue-type-icon-to-backlog-issue-row/) |
| 260606-qup | Backlog rows: add horizontal edge padding so the type icon (first cell) and assignee avatar (last cell) aren't flush against the row edges — first cell px-0 → pl-4 pr-0, assignee cell px-2 → pl-2 pr-4 in RowCells (covers both render paths); explicit-px inner spans untouched to keep virtualized columns from collapsing; matches the section header's px-4 edge convention | 2026-06-06 | 91c6d386 | Approved & closed | [260606-qup-on-backlog-view-issue-rows-have-no-left-](./quick/260606-qup-on-backlog-view-issue-rows-have-no-left-/) |
| 260606-r80 | Unassigned avatar made visually distinct in CachedAvatar — unassigned branch now uses border-dashed border-muted-foreground/50 + bg-muted/40 (dimmed dashed placeholder) instead of the same solid bg-muted circle as assigned initials; User icon kept; assigned initials + image branches unchanged; existing-token reuse (no new colors); Test 9 added asserting border-dashed present/absent; 9/9 tests + npm run check GREEN | 2026-06-06 | 3c0199f5 | Approved & closed | [260606-r80-unassigned-avatar-distinct](./quick/260606-r80-unassigned-avatar-distinct/) |
| 260606-rgc | Epics page brought to backlog parity (EpicsPage.tsx) — removed the `<thead>` column-header row (headerless like the backlog table); added a `<colgroup>` to preserve column widths that previously lived only on the deleted `<th>`s; assignee cell now ALWAYS renders CachedAvatar with `name={...|| 'Unassigned'}` (distinct dashed-border unassigned treatment) matching BacklogRow, replacing the old `assignee ? … : null` conditional. UAT follow-ups: (1) status pill was visually off — statusPillClass omits a display and needs a flex-item parent for its min-w/text-center, so wrapped the span in `<div className="flex">` (1dd00a2a); (2) spacing polish — status col w-32→w-28 (excess right margin) and assignee cell px-3→pl-3 pr-6 + col w-12→w-16 so the avatar isn't flush to the container edge (8fdc867d). --full pipeline (discuss+research+check+review+verify), verified 5/5, npm run check GREEN. New memory: statusPillClass needs flex parent. | 2026-06-06 | 8fdc867d | Approved & closed | [260606-rgc-make-the-epics-page-match-more-backlog-v](./quick/260606-rgc-make-the-epics-page-match-more-backlog-v/) |
| 260606-s09 | Filter stories by "Unassigned" in Backlog + Sprint Board, merged into the existing assignee filter. New shared `taskflow/src/lib/assignee-filter.ts` holds a reserved sentinel `UNASSIGNED_FILTER = '__unassigned__'` (+ label "Unassigned"), `buildAssigneeOptions()` (pins sentinel to TOP, only when ≥1 visible issue is unassigned, with a collision guard so a real displayName equal to the sentinel is never offered), and `matchesAssigneeFilter()` (sentinel matched strictly on `assignee == null`, excluded from the named substring pass — OR semantics). Both BacklogPage and SprintBoardTab now delegate to the single shared helper (predicate drift eliminated); UnifiedFilterBar `displayMap` renders the sentinel as "Unassigned" in the dropdown, search, and active chip. `filter.store.ts` untouched (sentinel round-trips as a plain string). --full pipeline (discuss+research+check+review+verify); code-review WR-01/IN-01 fixed in a0c22947; 12/12 vitest pass; verified 6/6; npm run check GREEN. Post-approval UI polish: Unassigned dropdown option styled italic+muted with a divider via a generic `distinctValue` prop on FilterDropdown (0266f84b), + pr-0.5 to stop truncate clipping the italic trailing glyph (79bc5910). | 2026-06-06 | 79bc5910 | Approved & closed | [260606-s09-in-backlog-and-sprint-board-filters-i-wa](./quick/260606-s09-in-backlog-and-sprint-board-filters-i-wa/) |
| 260606-ubz | Peek "Open full page" now preserves the breadcrumb trail back to the page the peek was opened from. The `onNavigateFull` handler in `main.tsx` called `handleIssueClick(key, true)`, where `resetTrail=true` wiped the trail (`breadcrumbReset()` + pushed nothing); dropped the arg so the default `resetTrail=false` path runs — pushing the source list route (via `routeLabel`) or the parent issue (issue→issue drill) onto the trail before navigating to `/issue/${key}`, identical to a normal list click. Inline quick fix (1 line), tsc clean. | 2026-06-06 | 943cba44 | Approved & closed | [260606-ubz-peek-open-full-page-should-push-breadcru](./quick/260606-ubz-peek-open-full-page-should-push-breadcru/) |
| 260606-spj | Issue peek redesign — (1) header now shows IssueTypeIcon + key + truncated title (was key-only); the icon/title appear once the issue loads via a deduped `useQuery` on the SAME `['jira-issue-detail', issueKey, jiraBaseUrl]` key IssueDetailView uses (TanStack dedupes → no extra fetch), while Close + Open-full-page controls stay PeekPanel-owned and visible during load (left container `min-w-0 flex-1`, key `shrink-0`, title `truncate pr-0.5`). (2) Merge Requests moved out of the top sidebar block to the BOTTOM of the single-column peek content (below description/activity): MR fetch extracted into new `issue-detail/useLinkedMRs.ts` hook, sidebar gained `omitMergeRequests` prop gated by `layout==='single-column'`, and IssueDetailView's single-column branch renders `<MergeRequestsSection>` after the activity feed. Two-column full page is byte-identical (omitMergeRequests=false). Edited the REAL `issue-detail/IssueDetailSidebar.tsx`, not the barrel. --full pipeline (discuss+research+check+review+verify); verified 6/6; 7/7 PeekPanel tests; npm run check GREEN (465 files). Code review: 0 critical, 3 advisory warnings left as-is (pre-existing MR 20-item page cap, redundant two-column hook subscription, queryFn DRY). Post-approval follow-ups (inline, same pattern): Linked Issues also moved out of the top sidebar via a new `omitLinkedIssues` prop (6acd11f8), then both Linked Issues + Merge Requests placed JUST ABOVE the activity/comments feed in the peek (fd58e525) — final single-column order: Fields → content → Linked Issues → Merge Requests → activity/comments. Two-column full page unchanged throughout (props default false). 21/21 tests; npm run check GREEN. | 2026-06-06 | fd58e525 | Approved & closed | [260606-spj-on-issue-peek-component-show-both-key-an](./quick/260606-spj-on-issue-peek-component-show-both-key-an/) |
| 260606-ugr | Subtask parent-issue link redesigned. FINAL shipped design after several UAT placement/style iterations: (1) FULL PAGE — a "Parent" `<section>` in the body relationships region, in the same location, row style AND data as a Story's Subtasks section: a `hover:bg-accent` row button with mono key + truncating summary + assignee avatar/name + status pill (NO border/card, NO type icon, NO arrow — matches subtask rows exactly). (2) PEEK — the parent ALSO renders as that body section, PLUS a compact breadcrumb in the PeekPanel header before the current issue: parent's real `IssueTypeIcon` + mono parent key + `ChevronRight` separator, tooltip = parent summary, `onOpenIssue(parent.key)` nav. Iteration history: started as a prominent bordered card above the title → moved to sidebar Fields block → moved to body subtasks-region + peek header link (up-arrow) → arrow swapped for parent type icon + card swapped for subtask-row style → un-suppressed on peek + added assignee parity. Assignee isn't in the base `issue.fields.parent` payload, so it's enriched via a new `parentQuery` in IssueDetailView (`fetchJiraIssueByKey` → summary/status/assignee/issuetype, 30s stale) passed down as `enrichedParent`; summary/status fall back to the base parent so the row shows before enrichment lands. Type foundation: widened the CONSUMED `JiraIssueDetail.fields.parent` at `jira.ts:1239` (dual-file gotcha — NOT jira/types.ts) for optional `issuetype` + `status.statusCategory.key`. Files: IssueDetailContent.tsx, IssueDetailView.tsx, PeekPanel.tsx, jira.ts (+ transient IssueDetailSidebar.tsx edit reverted). `onOpenIssue` nav preserved (peek breadcrumb fix 943cba44 intact). --full pipeline (discuss+research+check×2+review+verify) for the base; UAT follow-ups applied inline. npm run check GREEN (465 files) at every step, no new `any`. Approved by user. | 2026-06-06 | 26b4e684 | Approved & closed | [260606-ugr-on-subtasks-redesign-how-the-parent-issu](./quick/260606-ugr-on-subtasks-redesign-how-the-parent-issu/) |
| 260606-w2l | Standup "Yesterday / Worked On" column now has a second grouping level: activity attributable to a specific sub-task nests under a clickable sub-task sub-header inside its parent story, while story-level activity stays flat directly under the story (sub-task groups follow beneath). `buildGroups` (YesterdayColumn.tsx) refactored so every `SubItem` carries an `originKey`; commit and MR-comment counts now aggregate per composite `${groupKey}::${originKey}` key (so "N commits" attaches to the right sub-task, not the whole story); a single post-loop partition pass splits each group's `subItems` into story-level vs a `bySubtask` map using `meta?.isSubtask && meta.parentKey === group.issueKey`, emitting sorted `subTaskGroups` (non-empty only — empty sub-task headers never render, and a story with no sub-task activity is byte-identical to before). Rendering: `IssueActivityGroup` gained exported `SubTaskSubGroup` interface + `subTaskGroups?`/`onOpenIssue?` props; nested block guarded by length, each sub-task header reuses the documented `div[role=button]` + inner `<button>` key pattern with `pl-6 ml-2` indent (same visual language as the story header + TodayInProgressSection); extracted `SubItemList` helper to avoid duplicating the 3-way render branch. `generateMarkdown` mirrors the hierarchy (2-space sub-task line, 4-space item bullets). Today "In Progress" column audited — already nests sub-tasks via IssueRow indented, no change. --full pipeline (discuss+research+check+review+verify); verified 7/7; 65/65 standup tests; npm run check GREEN (465 files). Code review: 0 critical, 3 advisory warnings — all FIXED post-verify (1575df83): WR-01 getColumnHeading tests now use local-component dates (TZ-independent, was a UTC instant that flips day in negative-offset CI); WR-02 extracted shared `ActivityGroupHeader` using an absolutely-positioned overlay peek `<button>` as a SIBLING of the key `<button>` (icon+summary pointer-events-none, key relative z-10) — removes the interactive-in-interactive ARIA violation on BOTH story + sub-task headers, retires two biome-ignore suppressions, preserves exact full-row→peek / key→full-page UX; WR-03 sort uses localeCompare (0 for equal keys). 65/65 tests + npm run check GREEN after fixes. | 2026-06-06 | 1575df83 | Approved & closed | [260606-w2l-standup-subtask-subgrouping](./quick/260606-w2l-standup-subtask-subgrouping/) |
| 260607-0ph | Standup Notes "Yesterday / Worked On" transitions now render as the app-standard status pills instead of plain text: two `statusPillClass()` pills (from → to) joined by a muted `→` separator, mirroring StatusPopover/TaskCard. Pills are colored by Jira `statusCategory.key` — the data-shape gap (changelog `status` items carry only `fromString`/`toString` display names, no category) was resolved by enriching transitions inside `fetchYesterdayJiraActivity`: fetch the global status list once via `fetchAllJiraStatuses`, build a name→key Map, stamp `fromCategory`/`toCategory` onto each transition (try/catch → undefined categories degrade to gray fallback, activity still renders; status-list fetch placed AFTER the JQL search so existing `calls[0]` URL-assertion tests stay valid). `SubItem` gained a structured `transition` field for the styled render while `label` (`"To Do → Done"`) is kept untouched so the clipboard markdown export is byte-identical. Geometry stays in the helper; render only supplies the required flex parent (memory: statusPillClass needs flex parent). Files: jira.ts, IssueActivityGroup.tsx, YesterdayColumn.tsx. Default quick pipeline + auto code-review; npm run check GREEN (465 files); jira-standup 17/17, YesterdayColumn 15/15, standup suite 65/65. | 2026-06-06 | 8eeb4cce | Approved & closed | [260607-0ph-on-standup-notes-page-status-transitions](./quick/260607-0ph-on-standup-notes-page-status-transitions/) |
| 260607-ixt | v1.12 tech-debt cleanup from the milestone audit. (1) VISUAL-04/05 requirement text reconciled to the SHIPPED design: `.planning/REQUIREMENTS.md` (lines 15-16) + `.planning/ROADMAP.md` Phase 76 goal/SC now state the sprint-board card left-edge stripe encodes issue **TYPE** (via `issueTypeStripeClass`) and priority is shown via the `PriorityIcon` footer image — superseding the old "priority-driven stripe" wording orphaned by quick-260606-oyy; original intent (at-a-glance priority visibility) noted as preserved; `76-VERIFICATION.md` history left intact. (2) Dead client-side LexoRank calc deleted: `taskflow/src/services/jira/rank.ts` + `rank.test.ts` (12 tests) removed — Phase 78 shipped on server-side neighbor-key ranking (`rank-api.ts` / `rankIssueApi`, untouched). (3) Dead priority-stripe machinery removed from `issueDisplayUtils.ts`: `priorityStripeClass`, `prioritySeverityFromIcon`, and constants `PRIORITY_STRIPE`/`ICON_SEVERITY_STRIPE`/`DEFAULT_STRIPE` (+ their ~25 tests) — zero production callers; `issueTypeStripeClass` (wired into TaskCard) retained. --full pipeline (discuss+research+check+review+verify); plan PASSED first check; code review clean; verified 6/6; npm run check GREEN (463 files), issueDisplayUtils suite 20/20. Note: 2 pre-existing IssueDetailContent.test.tsx (DETAIL-01) failures unrelated to this task. | 2026-06-07 | 1a6daa24 | Verified | [260607-ixt-v1-12-debt-reconcile-visual-04-05-text-r](./quick/260607-ixt-v1-12-debt-reconcile-visual-04-05-text-r/) |
| 260607-jwb | Standup Notes "Yesterday" recap day is now overridable: clicking the left-column heading opens a dropdown (base-ui `DropdownMenuRadioGroup`) listing the last 14 **calendar** days (weekends included), most-recent-first. New `buildRecentDayOptions(count)` in `standup-date.ts` builds the list TZ-safely via local `new Date(y,m,d-i)` components (mirrors `toLocalDateString`; no `toISOString`/`toLocaleDateString`). `StandupNotesPage` gained a `dateOverride` state (`useState<string|null>(null)`); `yesterdayDate = dateOverride ?? resolvedYesterday` re-keys all four data queries automatically, while the **schedule query stays independent** (drives the resolved default — re-keying would be circular). Affordance is subtle: heading is a `cursor-pointer` trigger with a `ChevronDown` that is `opacity-0` → `group-hover/yhead:opacity-60`, so the default look is unchanged. The resolved-default row is labelled "Yesterday" and rendered selected (`RadioGroup value={yesterdayDate}`); selecting it calls `onSelectDate(null)` to revert. **No persistence** — React state only (no settings store / localStorage / stronghold); reload clears the override. --full pipeline (discuss+research+check+review+verify); plan PASSED first check; code review WR-01/WR-02 fixed inline (50d675b4: prepend resolved default when it falls outside the 14-day window so the radio value always matches a row; dedup double `getScheduleLookbackRange()` call); verified 7/7; npm run check GREEN (463 files), standup suites 45/45. Post-verify UAT iterations (all inline, npm run check GREEN throughout): (1) day-row labels now lead with the date and the **default row always carries a tag** — `"Thursday, 5 June 2026 · Yesterday"` when the resolved default genuinely is calendar-yesterday, else `"Friday, 5 June 2026 · Last working day"` (gated on `getColumnHeading(date) === 'Yesterday'`, matching the column heading's own logic); regular rows show the bare date. (2) Caret no longer shifts the date label: the `<h2>` + date `<p>` now live INSIDE the `DropdownMenuTrigger` with the `ChevronDown` trailing AFTER them, so its reserved space is trailing whitespace and the date keeps its original position. APPROVED & closed by user. | 2026-06-07 | d941d3da | Approved & closed | [260607-jwb-in-standup-notes-i-want-to-be-able-to-ch](./quick/260607-jwb-in-standup-notes-i-want-to-be-able-to-ch/) |
| 260607-nm9 | Add a "Clear all app cache" option to Settings → Advanced → Data. Mirrors the existing "Clear notification cache" row (confirm Dialog + inline green "Cleared" feedback, no toast). Clears the two real transient caches in Taskflow: (1) the avatar cache — new `clearAvatarCache()` export in `avatarCache.ts` that iterates `memoryCache` revoking every blob URL, resets the map + `inflight`, and deletes all non-`__cache_version__` keys from the `avatar-cache.json` `LazyStore` then `save()`, all wrapped `.catch(() => {})` so it never throws (mirrors `evictAvatar`, CACHE_VERSION unchanged); (2) the react-query cache via `queryClient.clear()` (same idiom TokenSection uses on project switch). New row added to the existing Data subsection in `DebugModeSection.tsx` with an async `handleClearCache` (`useQueryClient`), destructive confirm dialog, 3000ms inline "Cleared" reset. Zustand preference/connection/token stores are intentionally OUT of scope (already covered by the existing Reset section). TDD: 4 new `clearAvatarCache` tests (20/20 avatarCache, 15/15 Settings); `npm run check` GREEN. Default quick pipeline + worktree executor; Task 3 human-verify checkpoint APPROVED via live UAT. | 2026-06-07 | 52239935 | Approved & closed | [260607-nm9-add-an-option-to-clear-all-app-cache-int](./quick/260607-nm9-add-an-option-to-clear-all-app-cache-int/) |
| 260607-mhc | Standup notes page can now switch the **watched person** via a subtle `Showing: <name> ▼` ghost picker in the page header (right side, mirrors the Refresh/Copy affordances) — default is the logged-in user, so existing behavior is unchanged. New pure `resolveEffectiveIdentity(auth, watchedUser)` (`effectiveIdentity.ts`) returns an "effective identity" object: for "me" it passes the full auth identity through; for any watched Jira person it maps only what a `JiraAssignableUser` carries (`jiraUsername=name`, `jiraUserDisplayName=displayName`, `jiraUserKey=key`, `gitlabName=displayName` for best-effort commit matching) and **forces `gitlabUserId`/`gitlabUsername`/`gitlabEmail` to `null`** — the load-bearing no-fallback guard, unit-tested with an explicit non-inheritance assertion. This effective identity is lifted into `StandupNotesPage` local state (transient `useState(null)`, **resets to me every load** — no persisted field) and threaded into the existing react-query keys for all four Yesterday queries + copy-markdown cache reads, and passed as new props into `TodayColumn` (which previously self-sourced identity from `useAuthStore`). Consequence: switching person re-keys/refetches the whole standup; commit matching runs best-effort by display name (empty if unmatched — OK); and the three GitLab-ID-keyed MR sections (mr-events, reviewer MRs, participating MRs) auto-disable via their existing `!!gitlabUserId` enable-gates, rendering a subtle "GitLab account not matched" hint instead of ever leaking the logged-in user's MRs. Picker copies the `MentionPopover` debounce → server-side `fetchAssignableUsers(&username=)` pattern (no fetch-once/page-cap bug), gated on `activeJiraProject`; built on the `Popover` primitive (hosts a search `Input`; a `Menu` would hijack typeahead). Files: effectiveIdentity.ts (+test), WatchedPersonPicker.tsx, StandupPageHeader.tsx, StandupNotesPage.tsx, TodayColumn.tsx (+test). --full pipeline (discuss+research+check+review+verify); plan PASSED first check; code review 0 critical (guard independently traced); verified 5/5 must-haves (status human_needed — 4 runtime/UI behaviors await live UAT: picker subtlety, live search+refetch, rendered not-matched state, reset-on-reload); npm run check GREEN (466 files), standup vitest 70/70 incl. effectiveIdentity 5/5. Post-review follow-up (commit 93d54f5c) cleared all 4 code-review warnings + 3 info items: memoized the effective identity (WR-01); added a subtle "Commits matched by display name — may be incomplete" hint to the Yesterday column when watching a teammate, parity with the Today column's GitLab not-matched hint (WR-02); NaN-guarded formatDateLabel (WR-03); keyed picker rows/selection on Jira account key to avoid username collisions (WR-04); documented the name-only commit path (IN-01); "Copied!" now only flashes after the clipboard write resolves (IN-02); trimmed a stale comment (IN-03) — npm run check GREEN, standup 70/70. APPROVED & closed by user. | 2026-06-07 | 93d54f5c | Approved & closed | [260607-mhc-standup-watched-person-picker](./quick/260607-mhc-standup-watched-person-picker/) |
| 260608-cwq | Standup notes: each worklog on a subtask/story now appears as its own row (flat, no aggregation). Each row shows duration + description on a single line (`duration · description` in muted text); worklogs with no description show duration only. Total-hours stat header unchanged; subtask click-through preserved. | 2026-06-08 | 47531165 | Approved & closed | [260608-cwq-on-standup-notes-page-all-worklogs-on-su](./quick/260608-cwq-on-standup-notes-page-all-worklogs-on-su/) |
| 260608-dhp | On standup notes page, I want to add new watched thing to 'yesterday' column - created issues | 2026-06-08 | 86353a84 | Approved & closed | [260608-dhp-on-standup-notes-page-i-want-to-add-new-](./quick/260608-dhp-on-standup-notes-page-i-want-to-add-new-/) |
| 260609-bzk | On aio cycle defects subpage, there are defects listed. Make clicking on them open the side preview as the rest of the app does (clicking on key opens full page) | 2026-06-09 | 45904afb | Approved & closed | [260609-bzk-on-aio-cycle-defects-subpage-there-are-d](./quick/260609-bzk-on-aio-cycle-defects-subpage-there-are-d/) |
| 260609-cmd | When searching for issues, always open them in full page, never just side preview | 2026-06-09 | c616c55a | Approved & closed | [260609-cmd-when-searching-for-issues-always-open-th](./quick/260609-cmd-when-searching-for-issues-always-open-th/) |
| 260609-fbi | On sprint board or backlog view, when dragging an issue and pressing esc, it should cancel the drag action | 2026-06-09 | 488bd808 | Approved & closed | [260609-fbi-on-sprint-board-or-backlog-view-when-dra](./quick/260609-fbi-on-sprint-board-or-backlog-view-when-dra/) |
| 260609-fiq | When the app is in fullscreen mode, pressing esc shouldnt exit the fullscreen | 2026-06-09 | d2007c4c | Approved & closed | [260609-fiq-when-the-app-is-in-fullscreen-mode-press](./quick/260609-fiq-when-the-app-is-in-fullscreen-mode-press/) |
| 260609-fs9 | In comments (or other places?) when showing X days ago and is is more than one year ago, make it say years + days | 2026-06-09 | a9673c73 | Approved & closed | [260609-fs9-in-comments-or-other-places-when-showing](./quick/260609-fs9-in-comments-or-other-places-when-showing/) |
| 260609-g7c | wiki renderer should render {{{TEST}}} in tt tags | 2026-06-09 | 62e5728b | Approved & closed | [260609-g7c-wiki-renderer-should-render-test-in-tt-t](./quick/260609-g7c-wiki-renderer-should-render-test-in-tt-t/) |
| 260609-hvu | In wizard process, if there is an error it prints error message. But I want to be able to see advanced error with full log of what has happened | 2026-06-09 | 73de6187 | Approved & closed | [260609-hvu-in-wizard-process-if-there-is-an-error-i](./quick/260609-hvu-in-wizard-process-if-there-is-an-error-i/) |
| 260609-iff | In wizard, save data on every step finish, not everything at the end | 2026-06-09 | 6e3a7db5 | Approved & closed | [260609-iff-in-wizard-save-data-on-every-step-finish](./quick/260609-iff-in-wizard-save-data-on-every-step-finish/) |
| 260610-ew2 | Backlog right-click menu: "Send to top"/"Send to bottom" reorder a story to the first/last position within its OWN section (its sprint, or the backlog) — never changes sprint membership. New pure `resolveSendToEdge(currentKeys, activeKey, edge)` in `backlogDragHelpers.ts` delegates to the existing `resolveIntraRankFromDrop` (overKey = first/last section key) so persisted rank matches drag semantics; null-return no-op guards for already-at-edge / missing key / single-row sections. `handleSendToEdge` in `BacklogPage.tsx` resolves the row's section via `findSectionOfKey`, takes the localOrder-aware base order (mirrors drag), and routes through the shared `rankMutation` with the identical mutate shape (optimistic localOrder override + rollback + invalidate; no `addIssuesToSprint`/`moveIssuesToBacklog` on this path). `onSendToTop`/`onSendToBottom` threaded BacklogPage → VirtualizedBacklogTable → BacklogRow; two flat ContextMenuItems in a new "Reorder" group, render guard extended so the menu shows when only these handlers exist. --full pipeline (discuss+research+check+review+verify); plan PASSED first check; verified 6/6 must-haves; code review 0 blockers (2 advisory warnings: WR-01 under an active filter top/bottom targets the unfiltered section edge — same as drag; WR-02 silent no-op at edge); helper vitest 46/46; npm run check (biome + tsc) GREEN. Post-approval menu polish (2b8c98b7): context-menu group order set to Reorder → Flag → Move to..., and the separator between the sprint list and the Backlog item removed in the shared `SprintMoveMenuItems` (affects Backlog + issue-detail Fields picker), with the now-dead `Separator` prop dropped from the component + both call sites; npm run check GREEN. | 2026-06-10 | 43476ea6 | Approved & closed | [260610-ew2-send-to-sprint-edge](./quick/260610-ew2-send-to-sprint-edge/) |
| 260610-fnk | Wiki renderer now linkifies bare active-project issue keys (`PROD-123`) in prose → clickable in-app link to `/issue/KEY`, with a `line-through` strike when the referenced issue's `statusCategory.key === 'done'`. New `IssueKeyLink` component owns a deduped `useQuery(['jira-issue-detail', key, jiraBaseUrl])` (verbatim PeekPanel queryFn/`staleTime: 30_000`) so status is resolved cache-first, app-wide-shared, non-blocking (link paints immediately, strike a beat later); unknown/unresolved keys stay a link with no strike. `isKnownPrefix(key, activeJiraProject)` gates linkification to the single persisted active project ONLY (no app-wide project list exists — isolated behind one helper so it can widen to a cached `['jira-projects']` query later) — avoids false positives like COVID-19/UTF-8. **Key deviation (auto-fixed, blocking):** react-markdown 10 dropped the planned `text` component override, so prose linkification moved to a `rehypeIssueKeys` rehype plugin (`unist-util-visit`) that runs after rehype-raw / before rehype-sanitize and emits an allowlisted, XSS-safe `<issuekeylink data-key>` element (plain string only, no raw HTML/href); plugin SKIPs text under `a`/`code`/`pre` so code spans/blocks stay literal (D-04) and existing-link labels never re-wrap. The `a` override single-owns full `browse/KEY` URLs (renders one `IssueKeyLink`, no nested `<a>` — D-05). Render-only: `preprocessJiraMarkup` and the Source/serialize round-trip untouched. --full pipeline (discuss+research+check×2[1 blocker fixed: D-05 nested-anchor guard unimplementable in flat text override → single-ownership a-override + Test E]+review+verify); verified 8/8 must-haves; 150/150 WikiRenderer tests (incl. 5 new linkify/strike/code/foreign-prefix/no-nesting cases); npm run check GREEN (467 files). Code review 0 blockers; WR-01/02/03 fixed post-verify (decc86f9): retry:false on the status query (missing-PAT/transient-miss is the D-03 unknown case, not a retry:3 network-churn error), splice prose node only when a key was actually linkified (foreign-only nodes left untouched), and `[SKIP, index+len]` resume for robustness. | 2026-06-10 | decc86f9 | Approved & closed | [260610-fnk-in-wiki-renderer-when-there-is-a-test-in](./quick/260610-fnk-in-wiki-renderer-when-there-is-a-test-in/) |
| 260612-ggx | Releases page: warn when a task HAS a merge request but that MR is not in the release's milestone (different milestone, or none). Today MRs are fetched FROM the matched milestone and linked to issues by ticket key, so a linked MR is always in-milestone by construction; a task with no in-milestone MR just showed a generic "Missing MR" indicator. New `searchProjectMRsByKey(baseUrl, token, projectId, key)` in `gitlab.ts` (project-scoped `GET /merge_requests?search=<key>&in=title&state=all`, paginated, no label enrichment, error handling mirrors `fetchMilestoneMRs`). On `ReleaseDetailPage`, a `useQueries` block fans out ONLY over `missingRows` (tasks with `r.mr === null`) — in-milestone tasks incur no extra fetch; for each, `linkMRToTask` re-confirms the key then picks the first MR whose milestone differs from (or is absent vs.) `matchedMilestone` (compare by `id`; null milestone = warn). The MR cell renders a distinct orange "Wrong milestone" indicator with a tooltip naming the offending MR's milestone; in-milestone + plain Missing-MR branches unchanged. Second surface (cache-only, no list-render fan-out): the detail page seeds a release-attributed cache entry; `ReleasesTab` renders a reactive `<WrongMilestoneBadge>` (fetch-disabled `useQuery`) showing `⚠ MR milestone` once a release's detail view has populated it. Both sides route through a shared `wrongMilestoneMRKey()` helper (`lib/query-constants.ts`, includes GitLab base URL to avoid cross-instance project-id collisions). --full pipeline (discuss+research+check[PASSED first]+review+verify); verified 5/5 must-haves; code-review WR-01 (badge read cache imperatively in render → never re-rendered; fixed via reactive subscription) + WR-02 (shared key helper) fixed post-review in e5d541a4; 71/71 gitlab tests (6 new); npm run check GREEN (467 files). Note: executor worktree branched (EnterWorktree base drift) before the pre-dispatch plan commit, so orig hashes 74d3877f/6487399d were cherry-picked onto main as c64c3387/84fd8189. **Post-approval follow-ups (user-directed, inline):** (1) `5dc57864` perf — replaced the per-missing-task GitLab `search` fan-out (N slow round-trips) with a single `fetchRecentProjectMRs` call (latest 100 project MRs from the fast list endpoint, `state=all&order_by=updated_at&sort=desc`, matched locally; `searchProjectMRsByKey` + its 6 tests removed, 6 new tests added); same commit made the "Wrong milestone" cell render the offending MR as a clickable state-colored `!iid` link (was static text). Trade-off: an MR older than the latest 100 falls back to "Missing MR". (2) `43b125ea` — removed the releases-LIST "⚠ MR milestone" summary badge per user preference (dropped `WrongMilestoneBadge`, the detail-page `setQueryData` seeding, and the `wrongMilestoneMRKey` helper); the detail-page per-task warning stays. npm run check GREEN (467 files), gitlab 71/71 + ReleasesTab 14/14 throughout. APPROVED & closed by user. | 2026-06-12 | 43b125ea | Approved & closed | [260612-ggx-on-releases-page-merge-requests-are-pair](./quick/260612-ggx-on-releases-page-merge-requests-are-pair/) |
| 260612-j9p | Issue detail Priority dropdown upgraded: each option now shows Jira priority icon + name (PriorityIcon in a flex gap-1.5 row inside SelectItem), and the SelectTrigger shows the selected priority's icon + name via a custom render (falls back to f.priority before query resolves so icon shows immediately on open). Read-only row unchanged. Reuses existing PriorityIcon + Select primitives; no new deps. 23/23 FieldsSection tests pass incl. 2 new assertions (option list icons + trigger icon). biome + tsc GREEN. | 2026-06-12 | d9038d8e | Approved & closed | [260612-j9p-priority-dropdown-icons](./quick/260612-j9p-priority-dropdown-icons/) |

## Deferred Items

Items acknowledged and deferred at milestone close on 2026-06-07 (v1.12):

| Category | Item | Status |
|----------|------|--------|
| uat | Phase 78/79 Windows/WebView2 `mouseup`-loss UAT (UAT-1) | deferred — needs a Windows host; mitigations in place (autoScroll=false, touch-action:none, portaled DragOverlay) |
| uat | Phase 80 live-Jira UAT (ordered creation, partial-failure/retry, skipped-fields badge) | deferred — untestable without a live DC; covered by unit tests |
| tech_debt | Phase 78 dnd-kit `autoScroll` disabled | accepted — upstream #1108 rect-desync; no auto-scroll while dragging the backlog |
| tech_debt | Phase 79 D-07 reversed — screen/validator transitions not pre-filtered from drop targets | accepted — app has no transition-screen flow; rollback-on-rejection covers no silent snap-back (79-CONTEXT.md) |
| code_review | Phase 80 WR-02 React key collision in failure list | non-blocking |
| code_review | Phase 80 WR-03 silent `@current` degradation when auth display name unavailable | non-blocking |
| todo | priority-stripe-rest-rank (priority stripe colored by Jira priority-scheme rank via REST /priority) | deferred — backlog idea, not in v1.12 |

### Carried from v1.10

| Category | Item | Status |
|----------|------|--------|
| code_review | WR-05 (70-REVIEW) | non-blocking — unguarded `as number\|null` SP cast in Today*Section.tsx |
| code_review | IN-01 (70-REVIEW) | benign — setCopied setTimeout not cleared on unmount in StandupNotesPage.tsx |

## Session Continuity

Last session: 2026-06-09T11:52:00.000Z
Stopped at: Phase 80 UI-SPEC approved
Resume file: None
| 5 | Store full untruncated response bodies in devtools logs | 2026-06-03 | 99036280 | — |
| 16 | Consistent story point badge width in Backlog regardless of digit count | 2026-06-06 | a0aa443b | Approved | — |

## Operator Next Steps

- Start the next milestone with /gsd-new-milestone
