---
phase: quick-260609-fs9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/IssueDetailContent.tsx
  - taskflow/src/routes/settings/UpdatesSection.tsx
  - taskflow/src/lib/formatTimeAgo.ts
  - taskflow/src/lib/formatTimeAgo.test.ts
autonomous: true
requirements:
  - FS9-01
must_haves:
  truths:
    - "Comments/activity entries older than 1 year show years + remaining days (e.g. '1 year 32 days ago')"
    - "Timestamps less than 1 year old are unchanged"
    - "Zero remaining days shows just '1 year ago' (no '0 days' suffix)"
    - "formatTimeAgo (badge title) also handles the year+day case"
    - "UpdatesSection relativeTime is consistent with the other sites"
    - "npm run check passes (biome + tsc clean)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/IssueDetailContent.tsx"
      provides: "relativeTime() with year+day branch"
    - path: "taskflow/src/lib/formatTimeAgo.ts"
      provides: "formatTimeAgo() with year+day branch"
    - path: "taskflow/src/lib/formatTimeAgo.test.ts"
      provides: "Tests for the year+day cases"
  key_links:
    - from: "IssueDetailContent.tsx:relativeTime"
      to: "IssueDetailView.tsx, WorklogEntry.tsx, ChangelogEntry.tsx, MergeRequestListPage.tsx, InlineComment.tsx"
      via: "named export re-used across 5 sites"
      pattern: "relativeTime"
---

<objective>
Fix relative-time display so timestamps older than one year render as years plus
remaining days ("1 year 32 days ago") instead of an ever-growing "N days ago".

Purpose: The current day-only fallback produces confusing strings like
"420 days ago". Rendering years + residual days gives the user an immediately
understandable age for old items.

Output: Three files updated; formatTimeAgo.test.ts extended; npm run check
remains clean.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add year+day branch to all three relativeTime implementations</name>
  <files>
    taskflow/src/routes/dashboard/IssueDetailContent.tsx,
    taskflow/src/routes/settings/UpdatesSection.tsx,
    taskflow/src/lib/formatTimeAgo.ts,
    taskflow/src/lib/formatTimeAgo.test.ts
  </files>
  <behavior>
    - formatTimeAgo(ms) where diff >= 1 year → "1 year 32 days ago" (Intl or manual)
    - formatTimeAgo(ms) where diff is exactly 1 year → "1 year ago" (no "0 days" suffix)
    - formatTimeAgo(ms) where diff >= 2 years → "2 years 5 days ago"
    - formatTimeAgo(ms) where diff is exactly 2 years → "2 years ago"
    - All sub-year cases unchanged (day/hour/minute/second)
    - Same logic applies to relativeTime(iso) in IssueDetailContent and UpdatesSection
  </behavior>
  <action>
    The year threshold is 365 * 86_400 seconds (365 days). Use integer arithmetic,
    not Intl.RelativeTimeFormat for this branch, to control the compound format precisely.

    Shared computation for all three sites (implement identically in each):

    const YEAR_SECS = 365 * 86_400;
    const years = Math.floor(diffSecs / YEAR_SECS);
    const remainingDays = Math.floor((diffSecs % YEAR_SECS) / 86_400);
    const yearLabel = years === 1 ? '1 year' : `${years} years`;
    return remainingDays === 0
      ? `${yearLabel} ago`
      : `${yearLabel} ${remainingDays} day${remainingDays === 1 ? '' : 's'} ago`;

    Apply this branch BEFORE the existing day branch in each function (i.e.
    add `if (diffSecs >= YEAR_SECS)` above the current `return rtf.format(-Math.floor(diffSecs / 86400), 'day')`
    line — or in the IssueDetailContent.tsx variant, above the final return).

    For `formatTimeAgoStrict` in formatTimeAgo.ts: this function is used for the
    compact badge text ("30s", "5m", "3h", "7d") and does NOT need a year branch —
    "365d" or "420d" is acceptable for the compact badge. Leave it unchanged.

    For `formatTimeAgo` in formatTimeAgo.ts: add the year+day branch after the
    existing day-scale check (which currently is the last return). Add YEAR_SECS
    as a module-level constant alongside the existing SECOND/MINUTE/HOUR/DAY
    constants.

    Tests to add to formatTimeAgo.test.ts (in the `formatTimeAgo` describe block):
    - 1 year exactly (365 * 86_400_000 ms): output contains "year" and does NOT
      contain "day" (zero residual)
    - 1 year + 32 days: output contains "year" and "32 day"
    - 2 years exactly: output contains "2 year" and does NOT contain "day"
    - 2 years + 1 day: output contains "2 year" and "1 day"

    UpdatesSection.tsx has its own local copy of relativeTime — apply the same
    YEAR_SECS branch there too for consistency. No test file exists for
    UpdatesSection; no new test file needed.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run src/lib/formatTimeAgo.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    All formatTimeAgo tests pass including the four new year-scale cases. npm run check
    exits 0 (biome + tsc clean). Comments and changelogs older than a year show
    "X year[s] [Y day[s]] ago" instead of "NNN days ago".
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full check</name>
  <files></files>
  <action>
    Run `npm run check` from taskflow/ to confirm biome lint + tsc are clean after the
    changes. Fix any type or lint errors before marking done.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npm run check 2>&1 | tail -20</automated>
  </verify>
  <done>npm run check exits 0 with no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ISO string input → display | Jira/GitLab ISO timestamps are external data parsed client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-fs9-01 | Tampering | relativeTime ISO parse | accept | Pure display function; no auth or data write path; malformed ISO → NaN → "now" (clamp at diffSecs <= 0) |
| T-fs9-02 | Denial of Service | formatTimeAgo year math | accept | Pure integer arithmetic; no allocation risk |
</threat_model>

<verification>
1. `npx vitest run src/lib/formatTimeAgo.test.ts` — all tests pass including 4 new year-scale cases
2. `npm run check` exits 0
3. Manual: open a Jira issue comment or changelog entry older than 1 year — timestamp reads "X year[s] [Y day[s]] ago"
</verification>

<success_criteria>
- relativeTime() and formatTimeAgo() both produce "X year[s] [Y day[s]] ago" for inputs >= 365 days old
- Sub-year behavior is byte-identical to before
- formatTimeAgoStrict compact badge is unchanged (still "Nd" for day-scale)
- npm run check GREEN
</success_criteria>

<output>
Create `.planning/quick/260609-fs9-in-comments-or-other-places-when-showing/260609-fs9-SUMMARY.md` when done
</output>
