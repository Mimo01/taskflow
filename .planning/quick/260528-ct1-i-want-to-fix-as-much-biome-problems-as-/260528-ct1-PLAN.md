---
phase: quick-260528-ct1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/components/app/AppIcon.tsx
  - taskflow/src/components/app/KeyboardShortcutsPanel.tsx
  - taskflow/src/components/ui/empty-state.test.tsx
  - taskflow/src/components/ui/label.tsx
  - taskflow/src/components/ui/input-group.tsx
  - taskflow/src/routes/dashboard/SprintGoalBanner.tsx
  - taskflow/src/routes/dashboard/AioCycleDetailPage.tsx
  - taskflow/src/routes/dashboard/BacklogFilterBar.tsx
  - taskflow/src/components/app/CommandPalette.tsx
  - taskflow/src/routes/dashboard/AuthImage.tsx
  - taskflow/src/routes/dashboard/ImageLightbox.tsx
  - taskflow/src/routes/dashboard/MentionPopover.tsx
autonomous: true
requirements: [BIOME-A11Y-01]
tags: [biome, a11y, lint]

must_haves:
  truths:
    - "Running `npm run check` from taskflow/ reports 0 warnings (down from 20)"
    - "No existing functional behavior changes — only attributes, handlers, and element name swaps"
    - "Test suite still passes after fixes"
  artifacts:
    - path: "taskflow/src/components/app/AppIcon.tsx"
      provides: "aria-hidden on decorative SVG"
    - path: "taskflow/src/components/app/KeyboardShortcutsPanel.tsx"
      provides: "aria-hidden on decorative search icon SVG"
    - path: "taskflow/src/components/ui/empty-state.test.tsx"
      provides: "type=button on test button"
    - path: "taskflow/src/components/ui/label.tsx"
      provides: "biome-ignore noLabelWithoutControl for generic primitive"
    - path: "taskflow/src/components/ui/input-group.tsx"
      provides: "biome-ignore on role=group + onKeyDown on InputGroupAddon"
    - path: "taskflow/src/routes/dashboard/SprintGoalBanner.tsx"
      provides: "<header> element replacing <div role=banner>"
    - path: "taskflow/src/routes/dashboard/AioCycleDetailPage.tsx"
      provides: "biome-ignore on <tr role=button>"
    - path: "taskflow/src/routes/dashboard/BacklogFilterBar.tsx"
      provides: "aria-pressed on <button>, no aria-selected on <li>"
    - path: "taskflow/src/components/app/CommandPalette.tsx"
      provides: "Keyboard handlers + role/tabIndex on clickable divs"
    - path: "taskflow/src/routes/dashboard/AuthImage.tsx"
      provides: "Guarded role/tabIndex/onKeyDown on clickable <img>"
    - path: "taskflow/src/routes/dashboard/ImageLightbox.tsx"
      provides: "onKeyDown on overlay + presentation role on inner"
    - path: "taskflow/src/routes/dashboard/MentionPopover.tsx"
      provides: "onKeyDown(Enter) on <div role=option>"
  key_links:
    - from: "all modified files"
      to: "Biome a11y rule engine"
      via: "npm run check"
      pattern: "Found 0 errors|warnings"
---

<objective>
Fix all 20 remaining Biome a11y warnings across 12 files in `taskflow/src` to bring the project to a clean lint baseline (0 warnings).

Purpose: Eliminate accessibility lint debt — each fix is either a proper a11y improvement (onKeyDown handlers, semantic HTML, aria-pressed for toggles, aria-hidden for decorative SVGs) or a targeted biome-ignore where semantic swap is structurally incompatible (Label primitive, role=group divs, tr role=button).

Output: Clean `npm run check` output with 0 warnings; test suite still passing.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mimo/Documents/Projects/taskflow/.planning/quick/260528-ct1-i-want-to-fix-as-much-biome-problems-as-/260528-ct1-CONTEXT.md
@/Users/mimo/Documents/Projects/taskflow/.planning/quick/260528-ct1-i-want-to-fix-as-much-biome-problems-as-/260528-ct1-RESEARCH.md
@/Users/mimo/Documents/Projects/taskflow/taskflow/biome.json

The RESEARCH.md contains a complete file-by-file fix table (lines 241-254) and per-rule fix patterns with code examples (lines 27-235). Follow it exactly — every fix has been pre-decided. The "Gotchas" section (lines 258-289) covers the four trickiest cases (CommandPalette overlay, ImageLightbox overlay, ImageLightbox inner div, InputGroupAddon).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical / single-line fixes (8 files, 8 warnings)</name>
  <files>taskflow/src/components/app/AppIcon.tsx, taskflow/src/components/app/KeyboardShortcutsPanel.tsx, taskflow/src/components/ui/empty-state.test.tsx, taskflow/src/components/ui/label.tsx, taskflow/src/components/ui/input-group.tsx, taskflow/src/routes/dashboard/SprintGoalBanner.tsx, taskflow/src/routes/dashboard/AioCycleDetailPage.tsx, taskflow/src/routes/dashboard/BacklogFilterBar.tsx</files>
  <action>
Apply low-risk targeted fixes per RESEARCH.md File-by-File Fix Summary:

1. AppIcon.tsx line ~4: Add `aria-hidden="true"` to the root `<svg>` (decorative app icon).
2. KeyboardShortcutsPanel.tsx line ~68: Add `aria-hidden="true"` to the inline magnifying-glass `<svg>`.
3. empty-state.test.tsx line ~28: Change `<button>Click me</button>` to `<button type="button">Click me</button>` in the test render call.
4. label.tsx line ~7: Add suppression directly above the `<label>` element: `{/* biome-ignore lint/a11y/noLabelWithoutControl: Generic Label primitive — callers provide htmlFor */}`.
5. input-group.tsx lines ~12 and ~48: Add suppression above each `<div role="group">`: `{/* biome-ignore lint/a11y/useSemanticElements: InputGroup layout requires div, not fieldset */}` (per RESEARCH note on fieldset styling incompatibility).
6. SprintGoalBanner.tsx line ~19: Replace `<div role="banner" aria-label="Sprint goal" ...>` with `<header aria-label="Sprint goal" ...>` (and close with `</header>`). `<header>` natively has role="banner".
7. AioCycleDetailPage.tsx line ~1019: Add suppression above the `<tr role="button" ...>`: `{/* biome-ignore lint/a11y/useSemanticElements: tr cannot be replaced by button in a table */}`.
8. BacklogFilterBar.tsx line ~86: Remove `aria-selected={selected.has(option)}` from the `<li>`, and add `aria-pressed={selected.has(option)}` to the inner `<button>` (per RESEARCH Option C — multi-select filter toggle semantics).

Do not change any other logic, attributes, or layout. Read each file first to find exact current line content before editing.
  </action>
  <verify>
    <automated>cd taskflow && npx biome check src/components/app/AppIcon.tsx src/components/app/KeyboardShortcutsPanel.tsx src/components/ui/empty-state.test.tsx src/components/ui/label.tsx src/components/ui/input-group.tsx src/routes/dashboard/SprintGoalBanner.tsx src/routes/dashboard/AioCycleDetailPage.tsx src/routes/dashboard/BacklogFilterBar.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Biome check on these 8 files reports 0 warnings related to: noSvgWithoutTitle, useButtonType, noLabelWithoutControl, useSemanticElements, useAriaPropsSupportedByRole. (input-group.tsx may still have its line ~47 onKeyDown warning — handled in Task 2.)</done>
</task>

<task type="auto">
  <name>Task 2: Keyboard handlers on clickable non-interactive elements (5 files, 12 warnings)</name>
  <files>taskflow/src/components/ui/input-group.tsx, taskflow/src/components/app/CommandPalette.tsx, taskflow/src/routes/dashboard/AuthImage.tsx, taskflow/src/routes/dashboard/ImageLightbox.tsx, taskflow/src/routes/dashboard/MentionPopover.tsx</files>
  <action>
Apply onKeyDown / role / tabIndex fixes per RESEARCH.md patterns (lines 27-82, 258-289):

1. input-group.tsx line ~47 (`InputGroupAddon` onClick): Add `onKeyDown` handler that focuses the parent input on Enter/Space:
   `onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') { e.currentTarget.parentElement?.querySelector('input')?.focus(); } }}`
   Per RESEARCH "Gotchas" — this is real keyboard parity with the existing click-to-focus behavior.

2. CommandPalette.tsx lines ~230 and ~231 (two clickable divs flagged for both noStaticElementInteractions and useKeyWithClickEvents):
   - Outer div (full-screen backdrop, line ~230): Add `role="button"`, `tabIndex={0}`, and `onKeyDown={(e) => { if (e.key === 'Escape' || e.key === 'Enter' || e.key === ' ') { e.preventDefault(); onClose(); } }}` (replace `onClose` with the actual close handler used by the existing onClick).
   - Inner div (line ~231, if it has onClick stopPropagation): Add `role="presentation"` and `onKeyDown={(e) => { e.stopPropagation(); }}` to satisfy linter without semantic harm.
   Read the file to confirm which handler is wired to onClick before mirroring it into onKeyDown.

3. AuthImage.tsx lines ~110 and ~125 (clickable `<img>` elements, onClick is optional prop):
   Add guarded attributes — only set role/tabIndex/onKeyDown when `onClick` is truthy. Pattern from RESEARCH:
   ```
   role={onClick ? 'button' : undefined}
   tabIndex={onClick ? 0 : undefined}
   onKeyDown={onClick ? (e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); onClick(e as unknown as React.MouseEvent<HTMLImageElement>); } } : undefined}
   ```
   Apply at both line ~110 and ~125 — they are two separate `<img>` instances. Use the existing `onClick` prop name from each call site (may differ between the two).

4. ImageLightbox.tsx:
   - Line ~24 (outer overlay div with role="dialog"): Add `onKeyDown={(e) => { if (e.key === 'Escape') onClose(); }}` directly on the div (mirrors the existing document-level Escape listener — see RESEARCH gotcha).
   - Line ~39 (inner white content div with onClick stopPropagation): Add `role="presentation"` and `onKeyDown={(e) => { e.stopPropagation(); }}`.

5. MentionPopover.tsx line ~109 (`<div role="option">` in listbox): Add `onKeyDown={(e) => { if (e.key === 'Enter') { e.preventDefault(); onSelect(user); } }}` — substitute `onSelect(user)` with the actual selection handler invoked by the existing onClick at that line (read file to confirm exact callback expression).

For all handlers, prefer reusing the same callback expression that the existing onClick uses, wrapped in the Enter/Space (or Escape) key guard. Do not introduce new state or refs.
  </action>
  <verify>
    <automated>cd taskflow && npx biome check src/components/ui/input-group.tsx src/components/app/CommandPalette.tsx src/routes/dashboard/AuthImage.tsx src/routes/dashboard/ImageLightbox.tsx src/routes/dashboard/MentionPopover.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Biome check on these 5 files reports 0 warnings for useKeyWithClickEvents and noStaticElementInteractions.</done>
</task>

<task type="auto">
  <name>Task 3: Full verification + regression check</name>
  <files>(no source edits — verification only)</files>
  <action>
Run the full project check and full test suite to confirm:
1. `npm run check` (Biome lint + format check across taskflow/) reports 0 warnings AND 0 errors.
2. `npm test` (Vitest) still passes — confirm no regression from the test-file edit in Task 1 (empty-state.test.tsx) or any of the runtime behavior changes (especially SprintGoalBanner div→header swap, BacklogFilterBar aria-pressed swap, and ImageLightbox onKeyDown).

If any warning remains, read its rule + file + line from Biome output and apply the same fix family (RESEARCH.md sections 1-6) as the analogous warnings already handled. Do not introduce new patterns.

If tests fail, identify the failure cause:
- Test snapshot referencing `<div role="banner">` → update to `<header>`.
- Test querying by `aria-selected` on `<li>` → update to `aria-pressed` on button.
- Test asserting absence of `role=button` on `<img>` → check the guarded attribute set.

Commit the final state with message `fix(quick-260528-ct1): resolve all biome a11y warnings`.
  </action>
  <verify>
    <automated>cd taskflow && npm run check 2>&1 | tail -5 && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>`npm run check` reports "Found 0 errors. Found 0 warnings." (or equivalent clean exit code 0); `npm test` exits 0 with all tests passing.</done>
</task>

</tasks>

<verification>
Phase-level check from `taskflow/` directory:
- `npm run check` → 0 errors, 0 warnings
- `npm test -- --run` → all tests pass
- `git diff --stat` shows changes only in the 12 files listed in files_modified
</verification>

<success_criteria>
- [ ] 0 Biome warnings remain in `npm run check`
- [ ] All 20 warnings from RESEARCH.md table addressed
- [ ] Test suite passes (no regression)
- [ ] Only the 12 files in files_modified were touched
- [ ] Suppressions limited to the 4 documented cases (label.tsx, input-group ×2, AioCycleDetailPage tr)
- [ ] Semantic swap applied: SprintGoalBanner div→header
- [ ] aria-pressed used on BacklogFilterBar button (not aria-selected on li)
- [ ] aria-hidden added to 2 decorative SVGs (AppIcon, KeyboardShortcutsPanel)
</success_criteria>

<output>
Create `.planning/quick/260528-ct1-i-want-to-fix-as-much-biome-problems-as-/260528-ct1-SUMMARY.md` when done.
</output>
