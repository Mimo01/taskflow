---
phase: quick-260606-o5y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/StoryHeaderRow.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.tsx
autonomous: true
requirements: [PEEK-01, PEEK-05]
must_haves:
  truths:
    - "Clicking a story swimlane header row body opens the peek panel (same as a card body)"
    - "Clicking the story issue key still navigates to the full /issue/KEY page"
    - "Chevron, epic pill, and right-click context menu behavior are unchanged"
    - "The row body is keyboard-activatable via Enter/Space"
    - "All three StoryHeaderRow sites (in-list x2 + sticky overlay) behave identically"
    - "npm run check stays GREEN (biome check + tsc)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/StoryHeaderRow.tsx"
      provides: "onOpenIssue prop + div[role=button] peek target + inner key button split"
      contains: "onOpenIssue"
    - path: "taskflow/src/routes/dashboard/SprintBoardTab.tsx"
      provides: "onOpenIssue wired into all three StoryHeaderRow instantiations"
  key_links:
    - from: "taskflow/src/routes/dashboard/StoryHeaderRow.tsx outer div[role=button]"
      to: "onOpenIssue(storyKey)"
      via: "onClick / onKeyDown on the row container"
      pattern: "onOpenIssue\\?\\(storyKey\\)"
    - from: "taskflow/src/routes/dashboard/StoryHeaderRow.tsx inner key <button>"
      to: "onOpenDetail(storyKey)"
      via: "onClick with stopPropagation"
      pattern: "e\\.stopPropagation\\(\\)"
    - from: "taskflow/src/routes/dashboard/SprintBoardTab.tsx (3 sites)"
      to: "StoryHeaderRow onOpenIssue prop"
      via: "onOpenIssue={onOpenIssue}"
      pattern: "onOpenIssue=\\{onOpenIssue\\}"
---

<objective>
On the sprint board, make story swimlane header rows open the issue peek panel the
same way cards do: clicking the row body opens the peek (via `onOpenIssue`), while
clicking the issue key still navigates to the full `/issue/KEY` page (via
`onOpenDetail`). This is a near-mechanical port of the card's PEEK-01/PEEK-05 split
(`TaskCard.tsx`) into `StoryHeaderRow.tsx` plus wiring at three call sites.

Purpose: Story rows currently only support full-page navigation; users expect the
same non-blocking peek preview affordance the cards already have (PEEK-01).
Output: Updated `StoryHeaderRow.tsx` (new `onOpenIssue` prop + key/body split) and
`SprintBoardTab.tsx` (3 wiring lines), with Biome baseline GREEN.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260606-o5y-on-sprint-board-story-rows-open-issue-pr/260606-o5y-CONTEXT.md
@.planning/quick/260606-o5y-on-sprint-board-story-rows-open-issue-pr/260606-o5y-RESEARCH.md

# Canonical reference pattern (PEEK-01/PEEK-05 split) — DO NOT modify, mirror it:
@taskflow/src/routes/dashboard/TaskCard.tsx

# Files to change:
@taskflow/src/routes/dashboard/StoryHeaderRow.tsx
@taskflow/src/routes/dashboard/SprintBoardTab.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add onOpenIssue prop and split key/body in StoryHeaderRow</name>
  <files>taskflow/src/routes/dashboard/StoryHeaderRow.tsx</files>
  <behavior>
    - Row body click (anywhere except key/chevron/epic pill) → calls onOpenIssue(storyKey) → peek panel (PEEK-01).
    - Issue key click → calls onOpenDetail(storyKey) → full-page nav, and does NOT also trigger peek (PEEK-05).
    - Enter/Space on the focused row body → onOpenIssue(storyKey).
    - Chevron click → toggles only, does NOT open peek.
    - Epic pill click → onEpicClick only, does NOT open peek (already stopPropagation).
    - When onOpenIssue is omitted, behavior degrades to today's: key button still navigates full-page, row body has no peek handler.
  </behavior>
  <action>
    Implement the card's PEEK-01/PEEK-05 split (mirror TaskCard.tsx lines 320, 365-390,
    and CardBody key button at 151-167) on StoryHeaderRow. Per CONTEXT D-10 / Pitfall 1,
    mirror cards exactly.

    1. Add optional prop `onOpenIssue?: (key: string) => void;` to `StoryHeaderRowProps`
       (after the existing `onOpenDetail` on line 33). KEEP `onOpenDetail` required and
       unchanged. Add `onOpenIssue` to the destructured params (in the block at lines 54-74).

    2. Introduce a switch immediately before `rowContent`: `const useKeyBodySplit = !!onOpenIssue;`
       (mirrors TaskCard.tsx:320).

    3. Convert the outer row container `<div>` (lines 76-87) into the peek target. When
       `useKeyBodySplit` is true, spread on: `role="button"`, `tabIndex={0}`,
       `onClick={() => onOpenIssue?.(storyKey)}`, and `onKeyDown` handling Enter/Space with
       `e.preventDefault()` then `onOpenIssue?.(storyKey)`. Add `cursor-pointer` to the
       className ONLY when `useKeyBodySplit` (the container currently has no cursor-pointer).
       Add the EXACT biome-ignore comment immediately above the `<div>`:
       `{/* biome-ignore lint/a11y/useSemanticElements: div[role=button] required — inner key/chevron are <button>, nested button is invalid HTML (D-10 / Pitfall 1) */}`.
       Do NOT add a `justDragged` guard — story header rows are not draggable (Research
       Finding 4); do not introduce drag logic.

    4. Add `e.stopPropagation()` to the chevron button's onClick (line 91): change
       `onClick={onToggle}` to `onClick={(e) => { e.stopPropagation(); onToggle(); }}` so a
       chevron click no longer bubbles to the new peek handler (mirror TaskCard.tsx:247).

    5. Demote the key+summary `<button>` (lines 101-118) into a key button + summary span:
       - Replace the wrapping `<button>` with a plain
         `<div className="flex items-center gap-2 flex-1 min-w-0">` (the old button provided
         this flex layout).
       - Keep the flag icon adjacent to the key.
       - The KEY becomes its own `<button type="button">` with
         `onClick={(e) => { e.stopPropagation(); onOpenDetail(storyKey); }}` (PEEK-05).
         Move the `group` class onto this key button and keep the existing
         done-state `line-through ... group-hover` / `group-hover:underline` className logic
         on it (research Code Example note: group moves from old wrapper to the key button).
       - The SUMMARY becomes a plain `<span className="text-sm font-medium truncate">{summary}</span>`
         with no click handler — clicks bubble to the outer div → peek.

    Leave assignee block, epic pill, status badge, subtask count, transitionError, and the
    ContextMenu wrapping (lines 163-219) entirely unchanged. Update the top-of-file JSDoc
    comment (lines 5-7) to note the row body now opens the peek and the key opens the full page.
  </action>
  <verify>
    <automated>cd taskflow && grep -q "onOpenIssue" src/routes/dashboard/StoryHeaderRow.tsx && grep -q "useSemanticElements" src/routes/dashboard/StoryHeaderRow.tsx && grep -q "e.stopPropagation()" src/routes/dashboard/StoryHeaderRow.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>
    StoryHeaderRow has an optional `onOpenIssue` prop; the outer row div is a
    div[role=button] peek target (with the biome-ignore comment) when onOpenIssue is set;
    the key is an inner button calling onOpenDetail with stopPropagation; the summary is a
    plain span; the chevron onClick has stopPropagation. tsc passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire onOpenIssue into all three StoryHeaderRow sites and verify Biome GREEN</name>
  <files>taskflow/src/routes/dashboard/SprintBoardTab.tsx</files>
  <behavior>
    - All three StoryHeaderRow instantiations (in-list virtualized x2 + sticky overlay)
      pass onOpenIssue, so every story row — including the sticky pinned header — opens the
      peek on body click and navigates full-page on key click.
  </behavior>
  <action>
    Add `onOpenIssue={onOpenIssue}` to each of the three `StoryHeaderRow` JSX sites,
    immediately after the existing `onOpenDetail={setSelectedIssueKey}` line. Do NOT change
    `onOpenDetail` — keep it as is (PEEK-05). Per CONTEXT, all three sites already have
    `onOpenIssue` in scope:
      - Site 1, line ~484 (inside VirtualizedSwimlanes): `onOpenIssue` is the component prop
        (declared line 212, used by cards at 561/586/728/753).
      - Site 2, line ~655 (inside VirtualizedSwimlanes): same prop source.
      - Site 3, line ~1661 (sticky header overlay in SprintBoardTab): `onOpenIssue` from the
        outlet context destructure at line 796.
    No other lines change. Cards in this same file already use the identical
    `onOpenIssue={onOpenIssue}` + `onIssueClick/onOpenDetail={setSelectedIssueKey}` pairing,
    confirming both handlers are valid in both scopes.
  </action>
  <verify>
    <automated>cd taskflow && test "$(grep -c 'onOpenIssue={onOpenIssue}' src/routes/dashboard/SprintBoardTab.tsx)" -ge 7 && npm run check</automated>
  </verify>
  <done>
    All three StoryHeaderRow sites pass `onOpenIssue={onOpenIssue}` (total
    `onOpenIssue={onOpenIssue}` occurrences in the file is >= 7: 4 prior card sites + 3 new
    story-row sites). `npm run check` (biome check + tsc) is GREEN.
  </done>
</task>

</tasks>

<verification>
- `npm run check` passes (biome check + tsc) — Biome baseline stays GREEN per CONTEXT/MEMORY.
- `grep -c 'onOpenIssue={onOpenIssue}' src/routes/dashboard/SprintBoardTab.tsx` returns >= 7.
- StoryHeaderRow.tsx contains `onOpenIssue`, the `useSemanticElements` biome-ignore comment,
  and `e.stopPropagation()` on both the chevron and the key button.
- Manual smoke (human, optional): on the sprint board, click a story row summary → peek
  panel opens; click the story key → full /issue/KEY page; click chevron → expand/collapse
  with no peek; right-click → context menu unaffected; Tab to row + Enter → peek opens.
</verification>

<success_criteria>
- Story swimlane header row body click opens the peek panel via onOpenIssue (PEEK-01).
- Story issue key click navigates full-page via onOpenDetail with stopPropagation (PEEK-05).
- Chevron, epic pill, and right-click context menu behavior unchanged; chevron no longer
  leaks a peek-open on click.
- Row body is keyboard-activatable (Enter/Space).
- All three StoryHeaderRow sites behave identically (including the sticky overlay).
- `npm run check` GREEN.
</success_criteria>

<output>
Create `.planning/quick/260606-o5y-on-sprint-board-story-rows-open-issue-pr/260606-o5y-SUMMARY.md` when done.
</output>
