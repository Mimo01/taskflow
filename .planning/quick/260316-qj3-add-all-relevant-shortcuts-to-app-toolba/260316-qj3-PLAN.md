---
phase: quick
plan: 260316-qj3
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src-tauri/src/lib.rs
  - taskflow/src/main.tsx
autonomous: true
requirements: [quick-260316-qj3]
must_haves:
  truths:
    - "macOS app menu bar has File, Edit, View, Go, Window, Help menus with relevant items"
    - "Menu items with accelerators trigger the same actions as their keyboard shortcuts"
    - "Platform-appropriate items only (macOS gets standard Edit/Window menus, all platforms get Go/View/Help)"
  artifacts:
    - path: "taskflow/src-tauri/src/lib.rs"
      provides: "Full native menu bar with all app shortcuts wired as menu items"
    - path: "taskflow/src/main.tsx"
      provides: "Event listeners for all menu item clicks that trigger corresponding app actions"
  key_links:
    - from: "taskflow/src-tauri/src/lib.rs"
      to: "taskflow/src/main.tsx"
      via: "Tauri event emitter for each menu item ID"
      pattern: "app.emit.*menu-"
---

<objective>
Replace the minimal Help-only toolbar menu with a full application menu bar containing all relevant shortcuts organized into standard OS menu categories (File, Edit, View, Go, Window, Help). Each menu item emits a Tauri event that the frontend listens for and routes to the existing action handler.

Purpose: Users can discover and trigger all app shortcuts from the native menu bar, matching standard desktop app conventions.
Output: Updated lib.rs with full menu structure, updated main.tsx with event listeners for all new menu items.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src-tauri/src/lib.rs
@taskflow/src/main.tsx
@taskflow/src/lib/shortcuts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build full native menu bar in Rust</name>
  <files>taskflow/src-tauri/src/lib.rs</files>
  <action>
Replace the current minimal menu setup in `lib.rs` with a full menu bar. Use `tauri::menu::*` APIs (Menu, Submenu, MenuItemBuilder, PredefinedMenuItem).

Build these submenus (in standard macOS order -- on Windows/Linux the app submenu is omitted by Tauri automatically):

**File menu:**
- "New Issue" — accelerator "CmdOrCtrl+N", id "menu-new-issue"
- Separator
- PredefinedMenuItem::close_window (macOS standard)

**Edit menu (macOS standard items — Tauri predefined):**
- PredefinedMenuItem::undo
- PredefinedMenuItem::redo
- Separator
- PredefinedMenuItem::cut
- PredefinedMenuItem::copy
- PredefinedMenuItem::paste
- PredefinedMenuItem::select_all

**View menu:**
- "Command Palette" — accelerator "CmdOrCtrl+K", id "menu-command-palette"
- Separator
- PredefinedMenuItem::fullscreen (macOS, no-op on other platforms)

**Go menu:**
- "Sprint Board" — accelerator "CmdOrCtrl+Shift+S", id "menu-nav-sprint"
- "Backlog" — accelerator "CmdOrCtrl+Shift+B", id "menu-nav-backlog"
- "Notifications" — accelerator "CmdOrCtrl+Shift+N", id "menu-nav-notifications"
- Separator
- "Settings" — accelerator "CmdOrCtrl+,", id "menu-nav-settings"

**Window menu (macOS standard):**
- PredefinedMenuItem::minimize
- PredefinedMenuItem::maximize (tauri v2 may not have this — if unavailable, skip)
- Separator
- PredefinedMenuItem::close_window

**Help menu:**
- "Keyboard Shortcuts" — accelerator "CmdOrCtrl+/", id "menu-keyboard-shortcuts" (existing)

Build the menu using `Menu::with_items` passing all submenus in order. Remove the old code that finds Help submenu and appends to it — we now build the entire menu from scratch.

Update `on_menu_event` to match on all custom menu item IDs and emit corresponding events:
- "menu-new-issue" -> emit "menu-new-issue"
- "menu-command-palette" -> emit "menu-command-palette"
- "menu-nav-sprint" -> emit "menu-nav-sprint"
- "menu-nav-backlog" -> emit "menu-nav-backlog"
- "menu-nav-notifications" -> emit "menu-nav-notifications"
- "menu-nav-settings" -> emit "menu-nav-settings"
- "menu-keyboard-shortcuts" -> emit "menu-keyboard-shortcuts" (already exists)

Use a single match or if-else chain in on_menu_event. The event name should equal the menu item ID for simplicity.

Important: Use `Submenu::with_items` for each submenu. On macOS, Tauri's `Menu::default` auto-adds the app-name submenu with About/Quit — since we're building from scratch with `Menu::with_items`, we need to manually add an app submenu as the first entry using `Submenu::with_items` containing PredefinedMenuItem::about, separator, PredefinedMenuItem::services_submenu (macOS only), separator, PredefinedMenuItem::hide, PredefinedMenuItem::hide_others, PredefinedMenuItem::show_all, separator, PredefinedMenuItem::quit. Gate this with `#[cfg(target_os = "macos")]` or just include it unconditionally (Tauri handles graceful no-ops on non-macOS).

Actually, simpler approach: start with `Menu::default(app.handle())?` which already has the platform-appropriate app menu, Edit, Window, and Help submenus on macOS. Then clear it and rebuild, OR build fully from scratch. The cleanest approach is building from scratch with `Menu::new(app.handle())` and then using `menu.append(&submenu)?` for each submenu, including the app submenu with predefined items for macOS.

Use `Menu::with_items(app.handle(), &[...])` passing all Submenu references as `&dyn IsMenuItem`.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <done>lib.rs compiles with full menu bar containing File, Edit, View, Go, Window, Help submenus with all app shortcuts as menu items, each emitting a named event on click.</done>
</task>

<task type="auto">
  <name>Task 2: Wire frontend event listeners for all menu actions</name>
  <files>taskflow/src/main.tsx</files>
  <action>
In the AppLayout component in main.tsx, extend the existing `useEffect` that listens for `menu-keyboard-shortcuts` to also listen for all new menu events. Create a single useEffect that sets up all menu event listeners:

```typescript
useEffect(() => {
  const listeners = [
    listen('menu-keyboard-shortcuts', () => setShortcutsOpen(true)),
    listen('menu-command-palette', () => setPaletteOpen(true)),
    listen('menu-new-issue', () => handleOpenCreate()),
    listen('menu-nav-sprint', () => navigate('/sprint-board')),
    listen('menu-nav-backlog', () => navigate('/backlog')),
    listen('menu-nav-notifications', () => setNotifPopoverOpen(true)),
    listen('menu-nav-settings', () => navigate('/settings')),
  ];
  return () => { listeners.forEach((p) => p.then((fn) => fn())); };
}, []);
```

Remove the old single-event useEffect for `menu-keyboard-shortcuts` since it's now consolidated into this one.

Note: `navigate` is already available from `useNavigate()` in AppLayout. `handleOpenCreate` is already defined. All state setters are already in scope.

The `navigate` reference in the dependency array: since navigate from react-router is stable, leaving deps as `[]` is fine (same pattern already used in the existing useEffect).
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>All menu bar items trigger the correct in-app action: New Issue opens create modal, Command Palette opens palette, Go items navigate to correct routes, Keyboard Shortcuts opens panel.</done>
</task>

</tasks>

<verification>
1. `cargo check` passes for the Rust side
2. `npx tsc --noEmit` passes for the TypeScript side
3. Manual: run `cargo tauri dev`, verify menu bar shows File/Edit/View/Go/Window/Help menus with expected items
4. Manual: click each Go menu item and confirm navigation works
5. Manual: click File > New Issue and confirm create modal opens
</verification>

<success_criteria>
- Native menu bar has 6 menus (+ app menu on macOS) with all app shortcuts as menu items
- Every menu item with an accelerator triggers the same action as its keyboard shortcut
- Standard platform items (Edit operations, Window minimize/close, Quit) work correctly
- No regressions to existing keyboard shortcuts
</success_criteria>

<output>
After completion, create `.planning/quick/260316-qj3-add-all-relevant-shortcuts-to-app-toolba/260316-qj3-SUMMARY.md`
</output>
