---
phase: quick-260518-n3s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/AioCycleDetailPage.tsx
  - taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx
autonomous: false
requirements: [QUICK-260518-N3S-01]

must_haves:
  truths:
    - "User sees a header row above the defects table with clickable column headers for sortable fields"
    - "Clicking a sortable header cycles sort: asc → desc → unsorted (and shows an indicator)"
    - "User can filter defects by Status, Priority, Severity, and Assignee via popover multi-select chips"
    - "Active filters render as removable chips above (or alongside) the toolbar with a Clear all action"
    - "Filtered + sorted defect list updates in place; empty filtered result shows a 'no matches' message instead of EmptyState"
    - "Pre-existing 'no defects' EmptyState still renders when allDefects.length === 0"
    - "Defect rows that are still loading (issueQuery pending) sort to the bottom and are excluded from filters until resolved"
  artifacts:
    - path: "taskflow/src/routes/dashboard/AioCycleDetailPage.tsx"
      provides: "Sort + filter UI for the Defects tab, refactored DefectRow data flow so parent owns issue queries"
      contains: "useQueries"
    - path: "taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx"
      provides: "Test coverage for sort cycling, filter selection, and filter+sort composition on defects tab"
      contains: "describe.*defects.*sort"
  key_links:
    - from: "Defects tab toolbar (filter popovers)"
      to: "filteredDefects useMemo"
      via: "filter state Sets passed into useMemo"
      pattern: "useMemo.*filter"
    - from: "Defects table thead (sort buttons)"
      to: "sortedDefects useMemo"
      via: "sortKey + sortDir state"
      pattern: "setSort.*sortKey|setSortDir"
---

<objective>
On the AIO cycle detail Defects tab, add sort (clickable column headers) and filter (multi-select popovers for Status / Priority / Severity / Assignee). Today the defects list is read-only and ungoverned — users with even 10+ defects in a cycle have to eyeball-scan to find what's failing on a given component or person.

Purpose: Bring the defects tab to feature parity with the kind of sort/filter affordances that already exist elsewhere in the app (e.g. UnifiedFilterBar pattern in BacklogPage/SprintBoardTab) so a tester can quickly find "open High priority bugs assigned to me in this cycle."

Output: Updated `AioCycleDetailPage.tsx` whose Defects tab renders a small filter toolbar + sortable column headers; new and updated tests in `AioCycleDetailPage.test.tsx` proving the behavior.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/dashboard/AioCycleDetailPage.tsx
@taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx
@taskflow/src/services/jira/types.ts
@taskflow/src/components/ui/popover.tsx
@taskflow/src/components/ui/badge.tsx
@taskflow/src/components/ui/button.tsx

<interfaces>
<!-- Key types & contracts the executor needs. Do not go scavenging. -->

From taskflow/src/services/jira/types.ts (JiraIssue.fields used by DefectRow):
- `summary: string`
- `status: { name: string; statusCategory?: { key: 'new' | 'indeterminate' | 'done' } }`
- `assignee: { displayName: string; avatarUrls: Record<string,string> } | null`
- `reporter: { displayName: string; avatarUrls: Record<string,string> } | null`
- `priority: { name: string; iconUrl?: string } | null`
- `customfield_13415: { value?: string; name?: string } | null`  // severity
- `issuetype?: { name: string }`

Existing DefectRow uses `useQuery<JiraIssue | null>(['jira', jiraBaseUrl, 'issue-lightweight', defectIdOrKey], fetchJiraIssueByKey)`. To support cross-row sort/filter, hoist this to the parent via `useQueries` from `@tanstack/react-query` so the parent has all `JiraIssue` objects in one place, then pass each resolved issue down to DefectRow as a prop (DefectRow becomes presentational; it still owns its loading skeleton when issue is null).

Existing chip pattern (already in the same file, lines 552-612) — use this same `Badge variant={isActive ? 'default' : 'outline'}` styling for the filter chips toolbar, and use the same `Popover` component as `UnifiedFilterBar` for the dropdown multi-selects (label, search input, scrollable list with check icons). Keep it inline in this file — do NOT extract a new shared component for a single-page filter.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor DefectRow to receive resolved issue from parent (enable sort/filter)</name>
  <files>taskflow/src/routes/dashboard/AioCycleDetailPage.tsx, taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx</files>
  <behavior>
    - DefectRow continues to render exactly as today when passed `issue: JiraIssue | null` and `isLoading: boolean` props (same skeletons, same row, same a11y, same click-to-open behavior). Existing tests in the "AIOC-03*" group MUST still pass without modification of their assertions.
    - Parent `AioCycleDetailPage` resolves all defect issues in one place via `useQueries({ queries: defectsWithTriggers.map((d) => ({ queryKey: ['jira', jiraBaseUrl, 'issue-lightweight', d.defectKey], queryFn: () => fetchJiraIssueByKey(jiraBaseUrl!, token!, d.defectKey), enabled: !!jiraBaseUrl && !!token && !tokenLoading })) })`.
    - Each defect now has shape `{ defectKey, triggeredBy, issue: JiraIssue | null, isLoading: boolean }` in the parent before rendering.
    - No behavioral change visible to users yet (this is pure refactor — sort/filter UI lands in Task 2).
  </behavior>
  <action>
    Hoist DefectRow's `useQuery` up to `AioCycleDetailPage` using `useQueries` from `@tanstack/react-query` (import it). Build a memoized `resolvedDefects` array shaped `{ defectKey, triggeredBy, issue, isLoading }`. Change `DefectRow` signature to take `issue: JiraIssue | null` and `isLoading: boolean` (drop `tokenLoading`, drop the internal `useQuery`, drop the `jiraBaseUrl`/`token` props that are no longer needed). Update the `<DefectRow ... />` call site to pass the new props. Keep `triggeredBy` and `onOpen` props identical. Skeleton elements, `data-testid` values (`defect-row-${defectIdOrKey}`, `defect-title-loading-${defectIdOrKey}`), and click/keyboard handlers MUST be byte-identical so the existing AIOC-03* tests still pass.

    Update mocks in `AioCycleDetailPage.test.tsx` only as strictly needed for the refactor — if a test mocks `fetchJiraIssueByKey` to return a particular issue, it should still work because the parent's `useQueries` calls the same function with the same key.

    Run the full file's tests before proceeding: `cd taskflow && npm test -- --run AioCycleDetailPage`. All previously passing tests must remain green.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npm test -- --run AioCycleDetailPage 2>&1 | tail -40</automated>
  </verify>
  <done>All previously passing AioCycleDetailPage tests still pass. `DefectRow` no longer calls `useQuery` internally. Parent owns the issue queries via `useQueries`. No user-visible behavior change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add sortable column headers + filter popovers to Defects tab</name>
  <files>taskflow/src/routes/dashboard/AioCycleDetailPage.tsx, taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx</files>
  <behavior>
    Tests to add to `AioCycleDetailPage.test.tsx` under a new `describe('AIOC-N3S: defects sort + filter', ...)` block. Use the existing test setup (mock `fetchJiraIssueByKey` to return distinct `JiraIssue` shapes per key so sort/filter results are deterministic). Example fixtures: defect A = status "Open" / priority "High" / severity "Critical" / assignee "Alice"; defect B = status "Done" / priority "Low" / severity "Minor" / assignee "Bob"; defect C = status "Open" / priority "Medium" / severity "Major" / assignee "Alice".

    - Test 1 (sort by Key asc/desc): renders all three rows. Click "Key" header — rows reorder ascending by key (A, B, C if keys are PROJ-1, PROJ-2, PROJ-3). Click again — descending (C, B, A). Click again — unsorted (original input order restored).
    - Test 2 (sort by Priority): clicking "Priority" header sorts by priority rank (High → Medium → Low) on first click. Defects with no priority data sort to the bottom regardless of asc/desc.
    - Test 3 (sort indicator): the active sorted column header shows a chevron-up icon when asc, chevron-down when desc, and no chevron when unsorted. data-testid `defects-sort-header-{key}` on each clickable th, `defects-sort-indicator-{key}` on the icon.
    - Test 4 (filter by Status): open Status filter popover (button has `data-testid="defects-filter-status"`), toggle "Open" — only defects A and C remain. Toggle "Done" — A, B, C all render. Toggle "Open" off — only B.
    - Test 5 (filter chip + clear all): after applying a Status="Open" filter, a removable chip "Status: Open" renders with a close button (`data-testid="defects-filter-chip-status-Open"`). A "Clear all" button (`data-testid="defects-filter-clear-all"`) appears when any filter is active and resets all filter Sets.
    - Test 6 (filter + sort compose): apply Status=Open AND sort by Key desc — visible rows: C then A.
    - Test 7 (no matches): apply a filter that excludes everything — renders the text "No defects match the selected filters." (NOT the EmptyState — EmptyState is reserved for `allDefects.length === 0`).
    - Test 8 (loading rows excluded from filter): if a defect's issue is still loading (issueQuery pending — mock returns a never-resolving promise), it does NOT appear in filtered results, but DOES appear in unfiltered (no-filter) sort results, sorted to the bottom.
  </behavior>
  <action>
    Add UI and state to `AioCycleDetailPage.tsx`'s Defects tab content (inside `<TabsContent value="defects">`):

    1. State:
       - `const [defectSortKey, setDefectSortKey] = useState<'key' | 'status' | 'priority' | 'severity' | 'assignee' | null>(null)`
       - `const [defectSortDir, setDefectSortDir] = useState<'asc' | 'desc'>('asc')`
       - `const [defectStatusFilter, setDefectStatusFilter] = useState<Set<string>>(new Set())`
       - same for `defectPriorityFilter`, `defectSeverityFilter`, `defectAssigneeFilter` (all `Set<string>`)
       - Empty set = "no filter applied to this dimension" (show all). Non-empty = only rows whose value is in the set.

    2. Derived options (useMemo): build `statusOptions`, `priorityOptions`, `severityOptions`, `assigneeOptions` from `resolvedDefects` (only include values from issues that have resolved — skip loading). De-dupe + sort alphabetically. For Priority, also build a `priorityRank` map: `{ Highest: 0, High: 1, Medium: 2, Low: 3, Lowest: 4 }` with fallback `Number.POSITIVE_INFINITY` for unknown/missing.

    3. Filter pipeline (useMemo `filteredDefects`):
       - If a defect's `issue` is still loading AND any filter set is non-empty → exclude (because we don't know its values).
       - Otherwise apply each non-empty filter: status check `issue.fields.status?.name`; priority `issue.fields.priority?.name`; severity `issue.fields.customfield_13415?.value ?? issue.fields.customfield_13415?.name`; assignee `issue.fields.assignee?.displayName`. Missing value never matches a non-empty filter set.

    4. Sort pipeline (useMemo `sortedDefects`): if `defectSortKey === null` return `filteredDefects` as-is. Otherwise stable sort by the selected key:
       - `key` → string compare on `issue?.key ?? defectKey`
       - `status` → string compare on status name
       - `priority` → numeric compare on priorityRank
       - `severity` → string compare on severity value
       - `assignee` → string compare on assignee displayName
       - Missing/loading values ALWAYS sort to the bottom regardless of `defectSortDir`. Apply `defectSortDir` only to rows that have the value.

    5. Header cycling: clicking a sortable header runs:
       ```
       if (defectSortKey !== key) { setDefectSortKey(key); setDefectSortDir('asc'); }
       else if (defectSortDir === 'asc') { setDefectSortDir('desc'); }
       else { setDefectSortKey(null); setDefectSortDir('asc'); }
       ```

    6. Toolbar UI: above the `<table>` add a `div` with `role="toolbar" aria-label="Defects filters"` containing four `<Popover>` filter buttons (Status, Priority, Severity, Assignee). Reuse the same Popover/Badge/Button look as the existing `UnifiedFilterBar` filter dropdowns (search input, scrollable list with check marks). Below the filter buttons, render active filter chips: one chip per selected value across all dimensions, formatted as `{Dimension}: {Value}` with an `X` close button. Chip removal toggles the value off in its set. When any filter is active, also render a "Clear all" button that empties all four sets.

    7. Sortable column headers: wrap "Key", "Status", "Assignee", "Priority", "Severity" `<th>` text in a `<button type="button">` with `data-testid="defects-sort-header-{key}"` and an inline `ChevronUp`/`ChevronDown` icon when this column is the active sort (no icon when not active). "Title", "Reporter", and "Triggered By" remain non-sortable plain headers.

    8. Replace the `defectsWithTriggers.map(...)` render with `sortedDefects.map(...)`. When `sortedDefects.length === 0` AND any filter is active, render `<p className="px-4 py-3 text-sm text-muted-foreground">No defects match the selected filters.</p>` instead of the table body. Keep the existing `allDefects.length === 0` EmptyState branch untouched.

    Add the new test block at the end of `AioCycleDetailPage.test.tsx` following the existing test style. Run all tests in the file plus the broader suite affected by the change.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npm test -- --run AioCycleDetailPage 2>&1 | tail -60</automated>
  </verify>
  <done>All eight new tests pass. All previously passing AioCycleDetailPage tests still pass. The Defects tab visually has: a filter toolbar with four popovers + active filter chips + clear all, and clickable sortable column headers for Key/Status/Assignee/Priority/Severity with chevron indicators.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User UAT on AIO cycle detail Defects tab sort + filter</name>
  <what-built>
    Sortable columns (Key, Status, Assignee, Priority, Severity) and multi-select filter popovers (Status, Priority, Severity, Assignee) added to the Defects tab on AIO cycle detail. Active filters render as removable chips with a Clear all action. Filter+sort compose. Loading rows sort to the bottom and are excluded from active filters.
  </what-built>
  <how-to-verify>
    1. Run the app: `cd taskflow && npm run tauri dev`.
    2. Navigate to any AIO cycle that has multiple defects (e.g. a cycle with 5+ failed runs that have linked Jira defects). Click the "Defects" tab.
    3. Sort cycle:
       - Click the "Priority" column header → defects reorder High → Low; chevron-up shows next to "Priority".
       - Click again → reverse order; chevron-down shows.
       - Click again → original order restored, no chevron.
       - Repeat for Key, Status, Assignee, Severity.
    4. Filter:
       - Click the "Status" filter button → popover opens with all observed statuses.
       - Select "Open" (or whatever fits). Only Open defects render.
       - Repeat with Priority / Severity / Assignee. Multiple dimensions should AND together.
       - Verify a filter chip appears for each selected value; clicking the `X` on a chip removes it.
       - Click "Clear all" → all filters cleared, all defects visible.
    5. Compose: apply a filter that matches some rows AND sort by a column → rows are both filtered and sorted.
    6. Empty filter: apply a filter that matches nothing → message "No defects match the selected filters." renders (not the bigger EmptyState).
    7. Loading: open a cycle whose defect issues are slow to resolve (or throttle the network) — confirm rows with skeleton bodies appear at the bottom when sorting and don't pollute the filter dropdowns.
    8. Regression: clicking a defect row still navigates to /issue/{key} after it resolves. The "no defects" EmptyState still appears for cycles with no linked defects at all.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `cd /Users/mimo/Documents/Projects/taskflow/taskflow && npm test -- --run AioCycleDetailPage` passes (existing + new tests).
- `cd /Users/mimo/Documents/Projects/taskflow/taskflow && npm run lint` passes (no new warnings introduced).
- Manual UAT per Task 3 above.
</verification>

<success_criteria>
- User can sort the defects table by Key, Status, Assignee, Priority, or Severity (click-cycle asc/desc/none with chevron indicator).
- User can filter defects by Status, Priority, Severity, and Assignee via popover multi-select.
- Active filters render as removable chips with a Clear all affordance.
- Filter + sort compose correctly.
- Loading rows sort to the bottom and are excluded from active filter results.
- Existing defect row click-to-open, keyboard a11y, and EmptyState behavior unchanged.
- All tests pass; lint clean.
</success_criteria>

<output>
Create `.planning/quick/260518-n3s-on-aio-cycle-detail-defects-page-i-want-/260518-n3s-SUMMARY.md` when done.
</output>
