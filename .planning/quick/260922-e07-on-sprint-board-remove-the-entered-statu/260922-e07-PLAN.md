---
phase: quick-260922-e07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/TaskCard.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.tsx
  - taskflow/src/routes/dashboard/TaskCard.test.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
  - taskflow/src/lib/formatTimeAgo.ts
  - taskflow/src/lib/formatTimeAgo.test.ts
autonomous: true
requirements: [QUICK-260922-e07]

must_haves:
  truths:
    - "Sprint Board cards no longer show the time-in-status chip (no element with title 'Entered status …')"
    - "Sprint Board renders, drags, and transitions exactly as before otherwise"
    - "npm run check and npm test pass with no unused-import/dead-code diagnostics"
  artifacts:
    - path: "taskflow/src/routes/dashboard/TaskCard.tsx"
      provides: "Card body without the timeInColumn badge and without the timeInColumn prop"
    - path: "taskflow/src/routes/dashboard/SprintBoardTab.tsx"
      provides: "TaskCard call sites with no timeInColumn prop"
  key_links:
    - from: "taskflow/src/services/jira/greenhopper/adapter.ts"
      to: "GhBoardIssue.timeInColumn"
      via: "service-layer passthrough (UNCHANGED — still adapted and unit-tested)"
      pattern: "timeInColumn"
---

<objective>
Remove the "entered status" time-in-column timestamp chip from Sprint Board task cards,
along with the UI-only data plumbing that exists solely to feed it.

Purpose: The user does not want the per-card time-in-status badge on the Sprint Board.
Output: TaskCard renders without the chip; SprintBoardTab no longer passes `timeInColumn`;
the `formatTimeAgo` helper (whose only consumer was this chip) is deleted; tests updated.

Scope boundary: the GreenHopper service layer keeps `timeInColumn`
(`src/services/jira/greenhopper/types.ts` + `adapter.ts` + `adapter.test.ts` are NOT touched).
It is a faithful passthrough of the upstream GH payload and has its own passing tests;
removing it would be an unrelated API-shape change.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/dashboard/TaskCard.tsx
@taskflow/src/routes/dashboard/SprintBoardTab.tsx
@taskflow/src/routes/dashboard/TaskCard.test.tsx
@taskflow/src/lib/formatTimeAgo.ts

Current state (verified by grep at plan time):
- `TaskCard.tsx:43` imports `formatTimeAgo, formatTimeAgoStrict` from `@/lib/formatTimeAgo`.
- `TaskCard.tsx:91` declares `timeInColumn?: { enteredStatus: number; durationPreviously?: number }`
  on `TaskCardProps`; `TaskCard.tsx:126` repeats it on the internal `CardBodyProps`;
  `CardBody` destructures it at line ~144 and the component forwards it to `<CardBody>`.
- `TaskCard.tsx:239-249` renders the chip: a `<span>` with `title={\`Entered status ${...} ago\`}`
  placed between the story-points chip and the `showStatus` pill inside the `shrink-0` row.
- `SprintBoardTab.tsx` passes `timeInColumn={(card as { timeInColumn?: { enteredStatus: number } }).timeInColumn}`
  at FOUR `<TaskCard>` call sites (~lines 579, 604, 751, 776 — virtualized/fallback x droppable/plain).
- `src/lib/formatTimeAgo.ts` is consumed ONLY by `TaskCard.tsx` and its own test file.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the entered-status chip and its prop from TaskCard and SprintBoardTab</name>
  <files>taskflow/src/routes/dashboard/TaskCard.tsx, taskflow/src/routes/dashboard/SprintBoardTab.tsx</files>
  <action>
In `TaskCard.tsx`:
  1. Delete the chip JSX block in `CardBody` — the comment block referencing Phase 73 Plan 02 /
     UI-SPEC §1 / D-05 / R-03 plus the `{timeInColumn?.enteredStatus != null && ( ... )}` span.
     Leave the story-points chip and the `showStatus` status pill and all surrounding
     `density-compact:` classes untouched; the row keeps its `shrink-0 flex` wrapper.
  2. Delete the `timeInColumn` field (and its doc comment) from `TaskCardProps`.
  3. Delete the `timeInColumn` field from the internal `CardBodyProps` interface, from the
     `CardBody` destructuring parameter list, from the `TaskCard` component's own destructured
     props, and from the `<CardBody ... />` invocation(s) that forward it.
  4. Delete the now-unused `import { formatTimeAgo, formatTimeAgoStrict } from '@/lib/formatTimeAgo';`.

In `SprintBoardTab.tsx`:
  5. Delete the `timeInColumn={...}` prop at all FOUR `<TaskCard>` call sites, including the
     inline `(card as { timeInColumn?: { enteredStatus: number } })` casts that exist only to
     supply it. Change nothing else on those call sites (`isDraggable`, `justDragged`,
     `transitions`, `onTransition`, `isFlagged`, `onToggleFlag` all stay).

Do not touch `src/services/jira/greenhopper/*` — the adapter/type passthrough stays.
  </action>
  <verify>
    <automated>cd taskflow && ! grep -rnE 'timeInColumn|Entered status|formatTimeAgo' src/routes/dashboard/TaskCard.tsx src/routes/dashboard/SprintBoardTab.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Neither `TaskCard.tsx` nor `SprintBoardTab.tsx` mentions `timeInColumn`, `Entered status`, or `formatTimeAgo`; the story-points chip and status pill still render in the same row.</done>
</task>

<task type="auto">
  <name>Task 2: Update tests — drop the badge specs, keep a negative regression assertion</name>
  <files>taskflow/src/routes/dashboard/TaskCard.test.tsx, taskflow/src/routes/dashboard/SprintBoardTab.test.tsx</files>
  <action>
In `TaskCard.test.tsx`:
  1. Remove the entire `describe('TaskCard — timeInColumn badge slot (Phase 73 Plan 02)', ...)`
     block (the three specs: absent-prop, present-prop, DOM-order) and the fake-timer
     `beforeEach`/`afterEach` that exist only for it.
  2. Replace it with ONE regression spec in a `describe('TaskCard — no entered-status chip')`
     block asserting `screen.queryByTitle(/^Entered status /)` is `null` for a rendered
     `<TaskCard issue={makeIssue()} showStatus />`. Keep the `makeIssue` helper and the
     `lucide-react` mock (both are used by the PEEK-05 block below).
  3. Rewrite the file's top doc-comment so it describes the current contract (PEEK-05 key/body
     click split + no entered-status chip) instead of the removed badge slot.
  4. Leave `describe('TaskCard — PEEK-05 key/body click split (Phase 77 Plan 04)', ...)` intact.

In `SprintBoardTab.test.tsx`:
  5. Remove the spec `it('forwards timeInColumn from adapted issue into TaskCard (badge wires through)')`
     (~line 457) entirely — including the `enteredStatus` fixture and the
     `(story as Record<string, unknown>).timeInColumn = ...` assignment inside it.
     Do not touch any other spec in that file.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/TaskCard.test.tsx src/routes/dashboard/SprintBoardTab.test.tsx</automated>
  </verify>
  <done>Both test files pass; `grep -rn 'Entered status' src/routes/dashboard/SprintBoardTab.test.tsx` returns nothing; TaskCard.test.tsx retains the PEEK-05 specs plus one negative chip assertion.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the now-orphaned formatTimeAgo helper and run the full gates</name>
  <files>taskflow/src/lib/formatTimeAgo.ts, taskflow/src/lib/formatTimeAgo.test.ts</files>
  <action>
  1. Re-confirm there are no remaining importers: `grep -rn "formatTimeAgo" taskflow/src`
     should list only `src/lib/formatTimeAgo.ts` and `src/lib/formatTimeAgo.test.ts`.
     If ANY other file still imports it, STOP and report instead of deleting.
  2. Delete `src/lib/formatTimeAgo.ts` and `src/lib/formatTimeAgo.test.ts` with `git rm`.
  3. Run `npm run check` (biome + tsc --noEmit) and `npm test` from the `taskflow/` directory.
     Per the project Biome baseline note: gate on "no NEW files flagged", not an absolute
     diagnostic count — pre-existing diagnostics in unrelated files are acceptable.
  4. Commit all three tasks' changes together (the pre-commit hook runs the full vitest suite,
     so a single commit avoids a red intermediate commit).
  </action>
  <verify>
    <automated>cd taskflow && npm run check && npm test</automated>
  </verify>
  <done>`src/lib/formatTimeAgo*.ts` are gone from git; `npm run check` reports no new flagged files; `npm test` is fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | UI-only deletion; no new input path, no network call, no dependency added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-e07-01 | Denial of Service | SprintBoardTab render | accept | Pure JSX/prop removal; no data-fetch or loop change. Full vitest suite is the gate. |
| T-e07-02 | Tampering | over-deletion of shared helper | mitigate | Task 3 re-greps importers and STOPs rather than deleting if any consumer remains; GH adapter/types explicitly out of scope. |
| T-e07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — N/A. |
</threat_model>

<verification>
1. `cd taskflow && npm run check && npm test` — green.
2. `grep -rn "Entered status" taskflow/src` — no matches.
3. `grep -rn "formatTimeAgo" taskflow/src` — no matches.
4. `grep -rn "timeInColumn" taskflow/src` — matches ONLY under `src/services/jira/greenhopper/`.
5. Manual smoke (optional): open the Sprint Board; cards show key, summary, assignee,
   priority icon, story points, and status pill — no small time chip.
</verification>

<success_criteria>
- Sprint Board cards render with no entered-status time chip at any density.
- No unused imports, dead props, or orphaned helper files remain from the chip.
- GreenHopper adapter/type `timeInColumn` passthrough is untouched and still tested.
- `npm run check` and `npm test` both pass.
</success_criteria>

<output>
Create `.planning/quick/260922-e07-on-sprint-board-remove-the-entered-statu/260922-e07-SUMMARY.md` when done
</output>

