---
phase: quick-260922-e07
plan: 01
subsystem: ui
tags: [react, sprint-board, taskcard, dead-code-removal]

requires: []
provides:
  - Sprint Board TaskCard no longer renders the entered-status/time-in-column chip
  - formatTimeAgo/formatTimeAgoStrict helper removed (orphaned, single consumer)
affects: [dashboard, sprint-board]

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - taskflow/src/routes/dashboard/TaskCard.tsx
    - taskflow/src/routes/dashboard/SprintBoardTab.tsx
    - taskflow/src/routes/dashboard/TaskCard.test.tsx
    - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx

key-decisions:
  - "GreenHopper service-layer timeInColumn passthrough (adapter.ts/types.ts) left untouched — it's a faithful upstream payload mirror unrelated to the UI chip, out of scope"
  - "All three tasks committed together in one commit — pre-commit hook runs the full vitest suite, avoiding a red intermediate commit (per project convention)"

patterns-established: []

requirements-completed: [QUICK-260922-e07]

duration: 25min
completed: 2026-09-22
---

# Quick Task 260922-e07: Remove entered-status chip from Sprint Board cards

**Removed the per-card time-in-status chip (and its dead `formatTimeAgo` helper) from Sprint Board TaskCards — cards now show only key, summary, assignee, priority, story points, and status pill.**

## Performance

- **Duration:** ~25 min
- **Tasks:** 3/3 completed
- **Files modified:** 6 (4 modified, 2 deleted)

## Accomplishments
- Deleted the "Entered status" chip JSX, the `timeInColumn` prop on `TaskCardProps`/`CardBodyProps`, and its forwarding at all 4 `SprintBoardTab` `<TaskCard>` call sites
- Deleted the now-orphaned `src/lib/formatTimeAgo.ts` (+ its test) — its only consumer was the removed chip
- Updated `TaskCard.test.tsx` and `SprintBoardTab.test.tsx`: removed obsolete badge specs, added a negative regression assertion (`queryByTitle(/^Entered status /)` is null)
- Verified `timeInColumn` now appears ONLY under `src/services/jira/greenhopper/*` (adapter/types passthrough, intentionally untouched)

## Task Commits

All three tasks were combined into a single commit (per plan Task 3 instruction — the pre-commit hook runs the full vitest suite, so splitting into RED/GREEN-style commits would create a red intermediate commit):

1. **Tasks 1-3: Remove entered-status chip, update tests, delete orphaned helper** - `87e1230e` (feat)

## Files Created/Modified
- `taskflow/src/routes/dashboard/TaskCard.tsx` - Removed chip JSX, `timeInColumn` prop/field (TaskCardProps + CardBodyProps + destructuring), and the `formatTimeAgo`/`formatTimeAgoStrict` import
- `taskflow/src/routes/dashboard/SprintBoardTab.tsx` - Removed `timeInColumn={...}` prop (incl. inline cast) at all 4 `<TaskCard>` call sites
- `taskflow/src/routes/dashboard/TaskCard.test.tsx` - Replaced the 3-spec timeInColumn badge `describe` block with one regression spec; rewrote file doc-comment; removed now-unused fake-timer setup and a leftover comment reference
- `taskflow/src/routes/dashboard/SprintBoardTab.test.tsx` - Removed the "forwards timeInColumn ... badge wires through" spec and its fixture; updated file doc-comment
- `taskflow/src/lib/formatTimeAgo.ts` - Deleted (orphaned)
- `taskflow/src/lib/formatTimeAgo.test.ts` - Deleted (orphaned)

## Decisions Made
- GreenHopper service-layer `timeInColumn` (in `src/services/jira/greenhopper/adapter.ts`/`types.ts`) kept untouched — faithful upstream payload passthrough, unrelated to the UI-only chip removal, has its own passing tests.
- All edits committed as a single commit rather than per-task commits, because the project's pre-commit hook runs the full vitest suite on every commit (a partial/RED intermediate commit would fail the hook or misrepresent state).

## Deviations from Plan

None — plan executed exactly as written. One small cleanup beyond the plan's explicit line items: a leftover comment in `TaskCard.test.tsx` (`makeIssue`'s story-points comment) that referenced "the timeInColumn badge" was reworded since it no longer described any real assertion — this falls under Rule 1 (accuracy of an in-code comment, not a functional change) and was verified against the plan's own success criterion #4 (`grep -rn "timeInColumn" taskflow/src` matches ONLY under `greenhopper/`).

## Issues Encountered
None.

## User Setup Required
None — no external service configuration required.

## Verification Results
- `cd taskflow && npx tsc --noEmit` — clean, no errors
- `cd taskflow && npx vitest run src/routes/dashboard/TaskCard.test.tsx src/routes/dashboard/SprintBoardTab.test.tsx` — 27/27 passed
- `cd taskflow && npm run check` — Biome reports 1 error / 40 warnings, but confirmed via `npx biome check ./src` file-by-file listing that NONE of this plan's touched files are flagged (all diagnostics are pre-existing baseline drift in `chart.tsx`, `BacklogRow.tsx`, `IssueDetailPage.progressive.test.tsx`, `MyTasksPage.test.tsx` — documented in STATE.md's Biome baseline note); `tsc --noEmit` portion is fully clean
- `cd taskflow && npm test` — 195 files / 2740 tests passed, 2 skipped, 13 todo (full suite green)
- `grep -rn "Entered status" taskflow/src` — no matches outside the intentional regression-test assertions in `TaskCard.test.tsx`
- `grep -rn "formatTimeAgo" taskflow/src` — no matches
- `grep -rn "timeInColumn" taskflow/src` — matches ONLY under `src/services/jira/greenhopper/`

## Next Phase Readiness
No blockers. Sprint Board TaskCard now renders without the time-in-status chip at every density; GreenHopper's `timeInColumn` passthrough remains available for a future consumer if ever needed.

---
*Quick task: 260922-e07*
*Completed: 2026-09-22*

## Self-Check: PASSED

- FOUND: taskflow/src/routes/dashboard/TaskCard.tsx
- FOUND: taskflow/src/routes/dashboard/SprintBoardTab.tsx
- CONFIRMED DELETED: taskflow/src/lib/formatTimeAgo.ts
- CONFIRMED DELETED: taskflow/src/lib/formatTimeAgo.test.ts
- FOUND commit: 87e1230e
