---
phase: quick
plan: 260325-k0s
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
autonomous: true
must_haves:
  truths:
    - "Every push and PR to main runs lint, typecheck, and tests automatically"
    - "Release workflow reuses the same CI checks so they cannot drift"
    - "Developers get fast feedback — CI completes in minutes, not waiting for full Tauri build"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline for PRs and pushes"
    - path: ".github/workflows/release.yml"
      provides: "Release pipeline (existing, unchanged or minimally adjusted)"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "taskflow/package.json scripts"
      via: "npm ci && npm run check && npx vitest run"
      pattern: "npm run check|vitest run"
---

<objective>
Create a CI workflow that runs on every push/PR to ensure code quality gates (lint, typecheck, tests) pass before merge. Currently only a release workflow exists (triggered on version tags), so there is no continuous feedback loop during development.

Purpose: Catch regressions early, enforce code quality on every change.
Output: `.github/workflows/ci.yml` — a fast CI pipeline.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.github/workflows/release.yml (existing release workflow — reference for setup steps)
@taskflow/package.json (scripts: check, lint, test)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CI workflow for pushes and PRs</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create `.github/workflows/ci.yml` with the following design:

**Triggers:**
- `push` to `main` branch
- `pull_request` to `main` branch

**Concurrency:** group by `ci-${{ github.ref }}`, cancel-in-progress: true (so pushing again cancels stale runs).

**Job: `ci`** — runs on `ubuntu-22.04` (cheapest, fastest):

Steps:
1. `actions/checkout@v4`
2. `actions/setup-node@v4` with `node-version: lts/*`, cache: npm, cache-dependency-path: `taskflow/package-lock.json`
3. `npm ci` in `taskflow/` working directory
4. `npm run check` in `taskflow/` — this runs `biome check ./src && tsc --noEmit` (lint + typecheck in one command)
5. `npx vitest run` in `taskflow/` — runs all tests

Do NOT include Rust compilation or Tauri build steps — those are slow and only needed for release. This CI workflow is for fast frontend quality gates only.

Do NOT include Linux system dependencies (libwebkit2gtk etc.) — not needed without Tauri build.
  </action>
  <verify>
    <automated>cat .github/workflows/ci.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" 2>/dev/null || python3 -c "import sys; print('YAML validation skipped - no pyyaml')"</automated>
  </verify>
  <done>
    - ci.yml exists with push/PR triggers on main
    - Steps: checkout, setup-node, npm ci, npm run check, vitest run
    - Valid YAML syntax
    - No Rust/Tauri build steps (fast pipeline)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify tests pass locally to confirm CI will succeed</name>
  <files></files>
  <action>
Run the exact same commands the CI will execute, in order, to confirm they all pass:

1. `cd taskflow && npm run check` — must exit 0 (biome lint + tsc --noEmit)
2. `cd taskflow && npx vitest run` — must exit 0 (all tests pass)

If either fails, fix the issue before considering the plan complete. Common issues:
- Unused imports flagged by biome: remove them
- Type errors from tsc: fix the types
- Failing tests: investigate and fix

This ensures the CI workflow will pass on its first run in GitHub Actions.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npm run check && npx vitest run</automated>
  </verify>
  <done>
    - `npm run check` exits 0 (no lint or type errors)
    - `npx vitest run` exits 0 (all tests pass)
    - CI pipeline is guaranteed to pass on push
  </done>
</task>

</tasks>

<verification>
- `.github/workflows/ci.yml` exists and is valid YAML
- Workflow triggers on push to main and PRs to main
- Pipeline runs: npm ci, npm run check, vitest run
- All checks pass locally (same commands CI will run)
</verification>

<success_criteria>
A CI workflow exists that will automatically run lint, typecheck, and tests on every push/PR to main. Local verification confirms all checks pass, so the first CI run will succeed.
</success_criteria>

<output>
After completion, create `.planning/quick/260325-k0s-set-up-a-proper-process-for-pipelines-ch/260325-k0s-SUMMARY.md`
</output>
