---
phase: quick-260617-dd2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/main.tsx
  - taskflow/src-tauri/src/lib.rs
  - taskflow/src/lib/shortcuts.ts
autonomous: true
requirements:
  - QUICK-260617-dd2

must_haves:
  truths:
    - "Pressing cmd+f opens the command palette/search in all views"
    - "Native browser find-in-page does not appear when cmd+f is pressed"
    - "Pressing cmd+k does nothing (no alias kept)"
    - "The keyboard shortcuts panel displays cmd+F (not cmd+K) for the open-palette shortcut"
    - "The macOS menu bar accelerator for Command Palette is CmdOrCtrl+F"
  artifacts:
    - path: "taskflow/src/main.tsx"
      provides: "useHotkeys registration for mod+f"
      contains: "mod+f"
    - path: "taskflow/src-tauri/src/lib.rs"
      provides: "Native menu accelerator for CmdOrCtrl+F"
      contains: "CmdOrCtrl+F"
    - path: "taskflow/src/lib/shortcuts.ts"
      provides: "Shortcut display registry entry for open-palette"
      contains: "⌘F"
  key_links:
    - from: "taskflow/src/main.tsx"
      to: "setPaletteOpen(true)"
      via: "useHotkeys('mod+f', ...)"
      pattern: "useHotkeys.*mod\\+f"
    - from: "taskflow/src-tauri/src/lib.rs"
      to: "menu-command-palette event"
      via: "accelerator CmdOrCtrl+F"
      pattern: "CmdOrCtrl\\+F"
---

<objective>
Remap the command palette / search keyboard shortcut from cmd+k to cmd+f globally across the Taskflow desktop app.

Purpose: The user wants cmd+f to open search, which is a more natural muscle-memory shortcut. cmd+k must be fully removed (no alias).

Output: Three file edits — web-layer hotkey, native menu accelerator, display registry. The existing `e.preventDefault()` in the useHotkeys handler suppresses the WKWebView find-in-page bar.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mimo/Documents/Projects/taskflow/.planning/STATE.md
@/Users/mimo/Documents/Projects/taskflow/.planning/quick/260617-dd2-i-want-to-be-able-to-open-search-with-cm/260617-dd2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remap cmd+k to cmd+f in all three shortcut registration points</name>
  <files>taskflow/src/main.tsx, taskflow/src-tauri/src/lib.rs, taskflow/src/lib/shortcuts.ts</files>
  <action>
Make exactly three edits, all in one commit:

1. `taskflow/src/main.tsx` around line 246 — change the hotkey string from `'mod+k'` to `'mod+f'`. The surrounding code (PALETTE-01 comment, e.preventDefault(), setPaletteOpen(true)) stays identical. The existing e.preventDefault() is already present and is the correct mechanism to suppress WKWebView's native find-in-page behavior; no additional capture-phase listener is needed unless manual testing reveals it fails (see fallback note below).

2. `taskflow/src-tauri/src/lib.rs` around line 178 — change the menu item accelerator string from `"CmdOrCtrl+K"` to `"CmdOrCtrl+F"`. The id, label, and build call remain unchanged.

3. `taskflow/src/lib/shortcuts.ts` around lines 62-69 — in the `open-palette` entry, change `defaultKey` from `'⌘K'` to `'⌘F'` and `displayKeys` from `['⌘', 'K']` to `['⌘', 'F']`.

Do NOT keep any `'mod+k'` registration or alias — clean break per user decision.

Fallback note (do not implement proactively): If manual testing shows cmd+f still opens the native find bar alongside the palette, the fallback is a capture-phase `document.addEventListener('keydown', handler, { capture: true })` using the pattern already established by the ESC fullscreen handler in main.tsx around line 540-549.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && grep -c "mod+f" src/main.tsx && grep -v "mod+k" src/main.tsx | grep -c "setPaletteOpen" ; grep -c "CmdOrCtrl+F" src-tauri/src/lib.rs ; grep -c "⌘F" src/lib/shortcuts.ts</automated>
  </verify>
  <done>
    - src/main.tsx contains `'mod+f'` and no remaining `'mod+k'` near setPaletteOpen
    - src-tauri/src/lib.rs contains `"CmdOrCtrl+F"` for the command palette menu item
    - src/lib/shortcuts.ts open-palette entry shows defaultKey '⌘F' and displayKeys ['⌘', 'F']
    - npm run check passes (biome + tsc clean)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Remapped the command palette shortcut from cmd+k to cmd+f in web hotkey, Tauri menu accelerator, and display registry.</what-built>
  <how-to-verify>
    1. Launch the Taskflow desktop app (Tauri dev build or installed app)
    2. Press cmd+f from any view — the command palette/search should open; the native browser find bar should NOT appear
    3. Press cmd+k — nothing should happen (no palette, no other action)
    4. Open Help → Keyboard Shortcuts panel — the "Open command palette" row should display ⌘F
    5. Check the macOS menu bar for Taskflow — Command Palette item should show Cmd+F as its accelerator
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe what failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web keydown → app action | Keyboard shortcut interception must not expose app actions to unintended triggers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-dd2-01 | Tampering | useHotkeys mod+f handler | accept | Shortcut only opens the command palette UI; no privileged action taken; no state mutation beyond setPaletteOpen(true) |
</threat_model>

<verification>
- `grep 'mod+f' taskflow/src/main.tsx` returns a match
- `grep 'mod+k' taskflow/src/main.tsx` returns no match near setPaletteOpen
- `grep 'CmdOrCtrl+F' taskflow/src-tauri/src/lib.rs` returns a match
- `grep '⌘F' taskflow/src/lib/shortcuts.ts` returns a match
- `cd taskflow && npm run check` exits 0
</verification>

<success_criteria>
cmd+f opens the command palette from any view in the desktop app. The native macOS find-in-page bar does not appear. cmd+k produces no action. The keyboard shortcuts help panel shows ⌘F for the open-palette entry. All three registration points (web hotkey, Rust menu accelerator, display registry) are consistent.
</success_criteria>

<output>
Create `.planning/quick/260617-dd2-i-want-to-be-able-to-open-search-with-cm/260617-dd2-SUMMARY.md` when done
</output>
