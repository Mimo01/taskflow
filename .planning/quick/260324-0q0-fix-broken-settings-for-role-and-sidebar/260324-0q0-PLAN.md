---
phase: quick-260324-0q0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/settings/RoleSection.tsx
  - taskflow/src/routes/settings/AppearanceSection.tsx
  - taskflow/src/routes/settings/Settings.tsx
  - taskflow/src/routes/settings/SidebarSection.tsx
  - taskflow/src/routes/settings/SidebarItemsList.tsx
  - taskflow/src/routes/onboarding/RoleStep.tsx
  - taskflow/src/components/app/sidebar-items.ts
  - taskflow/src/components/app/Sidebar.tsx
  - taskflow/src/stores/settings.store.ts
  - taskflow/src/routes/settings/Settings.test.tsx
autonomous: true
requirements: [FIX-ROLE, FIX-SIDEBAR-SECTIONS]

must_haves:
  truths:
    - "Changing role in Settings applies the corresponding sidebar + dashboard preset"
    - "Changing role in onboarding wizard applies the corresponding preset"
    - "Sidebar items in Settings have their own dedicated section (not buried in Appearance)"
    - "Sidebar renders items grouped into visual sections with labels"
  artifacts:
    - path: "taskflow/src/routes/settings/SidebarSection.tsx"
      provides: "Dedicated sidebar customization settings section"
    - path: "taskflow/src/routes/settings/RoleSection.tsx"
      provides: "Role picker that applies presets on change"
    - path: "taskflow/src/components/app/sidebar-items.ts"
      provides: "Section definitions for sidebar grouping"
    - path: "taskflow/src/components/app/Sidebar.tsx"
      provides: "Sidebar rendering items in grouped sections"
  key_links:
    - from: "taskflow/src/routes/settings/RoleSection.tsx"
      to: "settings.store applyPreset"
      via: "applyPreset() call on role change"
      pattern: "applyPreset"
    - from: "taskflow/src/components/app/Sidebar.tsx"
      to: "taskflow/src/components/app/sidebar-items.ts"
      via: "SIDEBAR_SECTIONS import"
      pattern: "SIDEBAR_SECTIONS"
---

<objective>
Fix broken role-to-preset wiring in Settings and onboarding, extract sidebar configuration into its own Settings section, and add section grouping support to the sidebar navigation.

Purpose: Currently changing role in Settings does nothing to the layout, sidebar customization is hidden inside Appearance, and the sidebar is a flat list with no visual grouping.
Output: Working role-preset integration, clean settings organization, sectioned sidebar.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/settings/Settings.tsx
@taskflow/src/routes/settings/RoleSection.tsx
@taskflow/src/routes/settings/AppearanceSection.tsx
@taskflow/src/routes/settings/SidebarItemsList.tsx
@taskflow/src/routes/settings/PresetButtons.tsx
@taskflow/src/routes/onboarding/RoleStep.tsx
@taskflow/src/components/app/sidebar-items.ts
@taskflow/src/components/app/Sidebar.tsx
@taskflow/src/stores/settings.store.ts

<interfaces>
From settings.store.ts:
```typescript
setRole: (role: 'developer' | 'pm' | 'tech-lead') => void;
applyPreset: (preset: 'dev' | 'pm') => void;
sidebarItems: SidebarItem[];
setSidebarItems: (items: SidebarItem[]) => void;
setSidebarItemVisible: (id: string, visible: boolean) => void;
reorderSidebarItem: (fromIndex: number, toIndex: number) => void;
```

From sidebar-items.ts:
```typescript
export interface SidebarNavDef {
  id: string;
  label: string;
  path: string;
  icon: ComponentType<{ className?: string }>;
}
export const SIDEBAR_NAV_ITEMS: SidebarNavDef[];
export function getDefaultSidebarItems(preset: 'dev' | 'pm'): SidebarItem[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix role-preset wiring and extract sidebar settings section</name>
  <files>
    taskflow/src/routes/settings/RoleSection.tsx
    taskflow/src/routes/settings/AppearanceSection.tsx
    taskflow/src/routes/settings/SidebarSection.tsx
    taskflow/src/routes/settings/Settings.tsx
    taskflow/src/routes/settings/Settings.test.tsx
    taskflow/src/routes/onboarding/RoleStep.tsx
  </files>
  <action>
    **Fix RoleSection.tsx:**
    - Import `applyPreset` from settings store alongside `role` and `setRole`
    - When `setRole` is called, also call `applyPreset(role === 'pm' ? 'pm' : 'dev')` — tech-lead uses dev preset
    - Add a confirmation dialog (same pattern as PresetButtons.tsx) warning that changing role will reset sidebar and dashboard layout. Use Dialog from `@/components/ui/dialog` (same imports as PresetButtons).
    - Only call `setRole` + `applyPreset` after user confirms

    **Fix onboarding RoleStep.tsx:**
    - Import `applyPreset` from settings store
    - In `handleValueChange`, after `setRole(value)`, also call `applyPreset(value === 'pm' ? 'pm' : 'dev')`
    - No confirmation needed here since it's initial setup

    **Create SidebarSection.tsx:**
    - New file at `taskflow/src/routes/settings/SidebarSection.tsx`
    - Move the sidebar items UI from AppearanceSection into this new component
    - Render: heading "Sidebar", descriptive subtitle, SidebarItemsList, PresetButtons
    - Add `data-testid="section-sidebar"` to the wrapper div

    **Update AppearanceSection.tsx:**
    - Remove SidebarItemsList and PresetButtons imports and rendering
    - Keep only ThemeSection and density picker
    - Remove the `import SidebarItemsList` and `import PresetButtons` lines

    **Update Settings.tsx:**
    - Add 'sidebar' to SettingsSection type union
    - Import SidebarSection from './SidebarSection'
    - Add sidebar entry to SECTIONS array between 'appearance' and 'notifications': `{ id: 'sidebar', label: 'Sidebar', icon: <PanelLeft className="h-4 w-4" /> }` (import PanelLeft from lucide-react)
    - Add rendering case: `{activeSection === 'sidebar' && <SidebarSection />}`
    - Now has 7 sections total: connections, appearance, sidebar, notifications, workflow, role, advanced

    **Update Settings.test.tsx:**
    - Update test "renders 5 sidebar nav buttons" -> "renders 7 sidebar nav buttons" and update count to 7
    - Update the regex in "renders sidebar buttons with correct labels" to include Sidebar and Advanced: add `screen.getByRole('button', { name: /sidebar/i })` and `screen.getByRole('button', { name: /advanced/i })`
    - Add test for clicking Sidebar button renders sidebar section with testid `section-sidebar`
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/settings/Settings.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Changing role in Settings shows confirmation dialog then applies preset (sidebar + dashboard reset)
    - Changing role in onboarding applies preset immediately
    - Sidebar settings have their own section in Settings nav
    - AppearanceSection only contains theme and density
    - All settings tests pass with updated counts
  </done>
</task>

<task type="auto">
  <name>Task 2: Add section grouping to sidebar navigation</name>
  <files>
    taskflow/src/components/app/sidebar-items.ts
    taskflow/src/components/app/Sidebar.tsx
    taskflow/src/routes/settings/SidebarItemsList.tsx
    taskflow/src/stores/settings.store.ts
  </files>
  <action>
    **Update sidebar-items.ts:**
    - Add a `section` field to `SidebarNavDef`: `section?: string` (optional for backward compat)
    - Define section constants: `'main'`, `'planning'`, `'code'`, `'tracking'`
    - Assign sections to each nav item in SIDEBAR_NAV_ITEMS:
      - main: dashboard, my-tasks
      - planning: sprint-board, backlog, epics
      - code: merge-requests, mr-attention
      - tracking: sprint-progress, workload, releases
    - Export a `SIDEBAR_SECTIONS` array defining section order and labels:
      ```typescript
      export const SIDEBAR_SECTIONS: { id: string; label: string }[] = [
        { id: 'main', label: 'Main' },
        { id: 'planning', label: 'Planning' },
        { id: 'code', label: 'Code' },
        { id: 'tracking', label: 'Tracking' },
      ];
      ```

    **Update Sidebar.tsx:**
    - Import `SIDEBAR_SECTIONS` from sidebar-items
    - Instead of rendering `visibleNavItems` as a flat list, group them by section
    - For each section that has at least one visible item, render:
      - A small muted section label (only when sidebar is NOT collapsed): `<span className="px-3 pt-3 pb-1 text-[10px] font-semibold uppercase tracking-wider text-muted-foreground/60">{section.label}</span>`
      - The nav items belonging to that section in order
    - When sidebar is collapsed, skip section labels (just render icons in order, no grouping labels)
    - Preserve the existing item ordering from sidebarItems store (user customization) but group visually by section

    **Update SidebarItemsList.tsx:**
    - Group the drag-reorder list by section to match sidebar display
    - For each section, show a small non-draggable section header label
    - Items can still be toggled on/off but drag-reorder stays within sections (use separate SortableContext per section)
    - Update reorder logic: when computing fromIndex/toIndex for `reorderSidebarItem`, use the global index in the full sidebarItems array, not the section-local index

    **Update SidebarItem type in settings.store.ts:**
    - Add optional `section?: string` to the `SidebarItem` interface
    - Update `getDefaultSidebarItems` calls: the section is already on the SidebarNavDef, so no store change needed — the Sidebar component looks up the section from SIDEBAR_NAV_ITEMS at render time

    No store migration needed — section is derived from SIDEBAR_NAV_ITEMS at render time, not stored.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Sidebar renders nav items grouped under labeled sections (Main, Planning, Code, Tracking)
    - Section labels are hidden when sidebar is collapsed
    - SidebarItemsList in Settings shows items grouped by section with section headers
    - Drag reorder works within sections
    - All existing tests pass
  </done>
</task>

</tasks>

<verification>
- Settings page shows 7 sections in nav (connections, appearance, sidebar, notifications, workflow, role, advanced)
- Clicking Role and changing it shows confirmation then resets sidebar + dashboard
- Sidebar section in Settings shows drag-reorder list with section grouping
- App sidebar shows items under section labels (Main, Planning, Code, Tracking)
- Collapsing sidebar hides section labels
- All vitest tests pass
</verification>

<success_criteria>
- Role changes in Settings and onboarding both apply the correct preset
- Sidebar customization has its own Settings section (not in Appearance)
- Sidebar navigation renders items in labeled section groups
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/quick/260324-0q0-fix-broken-settings-for-role-and-sidebar/260324-0q0-SUMMARY.md`
</output>
