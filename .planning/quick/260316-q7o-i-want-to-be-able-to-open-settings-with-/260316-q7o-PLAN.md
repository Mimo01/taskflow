---
phase: quick
plan: 260316-q7o
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/lib/shortcuts.ts
  - taskflow/src/main.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can press Cmd+, to open Settings page"
    - "Shortcut appears in keyboard shortcuts panel under Navigation"
  artifacts:
    - path: "taskflow/src/lib/shortcuts.ts"
      provides: "nav-settings shortcut entry"
      contains: "nav-settings"
    - path: "taskflow/src/main.tsx"
      provides: "useHotkeys binding for settings navigation"
      contains: "mod+comma"
  key_links:
    - from: "taskflow/src/main.tsx"
      to: "/settings"
      via: "useHotkeys('mod+comma', () => navigate('/settings'))"
      pattern: "mod.comma.*settings"
---

<objective>
Add a keyboard shortcut (Cmd+,) to open the Settings page.

Purpose: Cmd+, is the standard macOS shortcut for opening preferences/settings. This makes the app feel native.
Output: Settings shortcut registered, wired, and visible in shortcuts panel.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/lib/shortcuts.ts
@taskflow/src/main.tsx

<interfaces>
From taskflow/src/lib/shortcuts.ts:
```typescript
export type ShortcutCategory = 'Navigation' | 'Lists' | 'Actions' | 'General';
export interface ShortcutEntry {
  id: string;
  defaultKey: string;
  description: string;
  category: ShortcutCategory;
}
export const SHORTCUTS: ShortcutEntry[] = [ ... ];
```

From taskflow/src/main.tsx (existing pattern for navigation shortcuts):
```typescript
useHotkeys('mod+shift+s', () => navigate('/sprint-board'));
useHotkeys('mod+shift+b', () => navigate('/backlog'));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register settings shortcut and wire navigation</name>
  <files>taskflow/src/lib/shortcuts.ts, taskflow/src/main.tsx</files>
  <action>
1. In `taskflow/src/lib/shortcuts.ts`, add a new entry to the SHORTCUTS array in the Navigation category group (after the existing nav-notifications entry):
   ```
   {
     id: 'nav-settings',
     defaultKey: '⌘,',
     description: 'Open Settings',
     category: 'Navigation',
   }
   ```

2. In `taskflow/src/main.tsx`, inside the AppLayout component where the other navigation useHotkeys calls are (around line 117-119), add:
   ```typescript
   useHotkeys('mod+comma', () => navigate('/settings'));
   ```
   Place it after the existing mod+shift+n line. Use 'mod+comma' (code name) following the same pattern as 'mod+slash' to avoid potential normalizer issues.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Cmd+, navigates to /settings. Shortcut appears in the keyboard shortcuts panel under Navigation category.</done>
</task>

</tasks>

<verification>
- `grep 'nav-settings' taskflow/src/lib/shortcuts.ts` returns the entry
- `grep 'mod+comma' taskflow/src/main.tsx` returns the useHotkeys binding
- All existing tests pass (no regressions)
</verification>

<success_criteria>
- Pressing Cmd+, (macOS) / Ctrl+, (other) navigates to Settings
- The shortcut is listed in the Keyboard Shortcuts panel under Navigation
- No existing shortcut behavior is broken
</success_criteria>

<output>
After completion, create `.planning/quick/260316-q7o-i-want-to-be-able-to-open-settings-with-/260316-q7o-SUMMARY.md`
</output>
