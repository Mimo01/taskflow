---
quick_id: 260523-n5r
description: Check linters and fix any errors
date: 2026-05-23
mode: quick (inline execution)
---

# Quick Task 260523-n5r: Check linters and fix any errors

## Linter status

`npm run lint` (biome) reports **3 errors** + 842 warnings. TypeScript (`tsc --noEmit`) passes.

Task scope: fix the 3 errors only. Warnings are out of scope (existing debt, not introduced by this task).

## Errors to fix

1. **`src/routes/dashboard/WikiRenderer.tsx:925`** — `lint/correctness/useHookAtTopLevel`
   `useContext(OlDepthContext)` is called from the `ol` arrow function inside the components map — biome cannot statically see this is a component.
   **Fix:** extract to a named top-level component `OlRenderer` and reference it from the components map.

2. **`src/routes/worklogs/WorklogsPage.tsx:916`** — `lint/a11y/useKeyWithMouseEvents`
   `onMouseOver` on `<table>` without a matching `onFocus`.
   **Fix:** broaden `handleTableMouseOver` to accept `React.SyntheticEvent<HTMLTableElement>` (its body only reads `e.target`) and add `onFocus={handleTableMouseOver}`. The handler safely no-ops for non-focusable cells but covers focus-bubbling cases.

3. **`src/routes/worklogs/WorklogsPage.tsx:1068`** — `lint/security/noDangerouslySetInnerHtml`
   `<style dangerouslySetInnerHTML={{ __html: COLUMN_HOVER_CSS }} />`.
   **Fix:** use React 19's native children syntax `<style>{COLUMN_HOVER_CSS}</style>`. CSS string contains no characters that would be HTML-escaped problematically.

## Verification

- `npm run lint` → exit 0 (no errors; warnings unchanged)
- `npm run check` (biome check + tsc) → exit 0
- `npm test` → existing tests pass (no behavioral change for any fix)
- Commit normally (no `--no-verify`); pre-commit hook (`cd taskflow; npm run check; npm run test`) must pass
