---
phase: quick-260606-r80
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/components/ui/cached-avatar.tsx
  - taskflow/src/components/ui/cached-avatar.test.tsx
autonomous: false
requirements: [R80-UNASSIGNED-AVATAR]
must_haves:
  truths:
    - "An unassigned avatar is visually distinct from an assigned-user initials fallback at a glance"
    - "The unassigned avatar uses existing Tailwind design tokens (no new color system)"
    - "Assigned-user avatars (initials and image) are visually unchanged"
  artifacts:
    - path: "taskflow/src/components/ui/cached-avatar.tsx"
      provides: "CachedAvatar with distinct unassigned styling"
      contains: "showUnassigned"
  key_links:
    - from: "taskflow/src/components/ui/cached-avatar.tsx"
      to: "border-dashed / dimmed background tokens"
      via: "showUnassigned conditional className"
      pattern: "showUnassigned"
---

<objective>
When a story has no assignee, the Backlog/board renders `<CachedAvatar url={null} name="Unassigned" />`. Today the unassigned state uses the SAME filled `bg-muted` circle as a regular initials fallback, differing only by showing a `User` lucide icon in `text-muted-foreground` instead of initials — too subtle to read as "empty" at a glance.

Make the unassigned avatar clearly distinct with a SMALL, focused visual tweak using existing design tokens: a dashed border + dimmed/transparent fill, so it reads as a clearly empty/placeholder slot versus a solid assigned avatar.

Purpose: Improve scannability of unassigned stories without a heavy redesign.
Output: Updated `CachedAvatar` unassigned branch styling + a test asserting the distinct treatment.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mimo/Documents/Projects/taskflow/.planning/STATE.md

# The single component to change (already pinned — do NOT rediscover):
@/Users/mimo/Documents/Projects/taskflow/taskflow/src/components/ui/cached-avatar.tsx
@/Users/mimo/Documents/Projects/taskflow/taskflow/src/components/ui/cached-avatar.test.tsx

# Pinned facts from investigation:
# - cached-avatar.tsx line 63: `showUnassigned = !url && isUnassigned(name)`
# - cached-avatar.tsx lines 68-83: the fallback <div>. Unassigned and assigned-initials
#   BOTH currently use the base classes `rounded-full bg-muted flex items-center justify-center`
#   (line 71). Unassigned only differs by skipping `text-[10px] font-medium text-foreground`
#   and rendering a <User> icon in `text-muted-foreground` (lines 72, 78-82).
# - `border-dashed` is an established codebase pattern: PinnedTabStrip.tsx (border-2 border-dashed
#   border-primary/30 bg-primary/5), WorklogsPage.tsx (border border-dashed border-border),
#   ReleaseDetailPage.tsx (border-dashed border-muted-foreground). Reuse these tokens — do NOT
#   introduce new colors.
# - lucide-react `UserPlus`, `UserRound`, `UserPlus2` icons are available if a clearer
#   "add/empty person" glyph is preferred over the current `User` icon (optional).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the unassigned avatar visually distinct</name>
  <files>taskflow/src/components/ui/cached-avatar.tsx, taskflow/src/components/ui/cached-avatar.test.tsx</files>
  <behavior>
    - When `showUnassigned` is true: the fallback div renders with a dashed border
      (e.g. `border border-dashed border-muted-foreground/50`) and a dimmed/transparent
      fill instead of the solid `bg-muted` — so it is visually distinct from an
      assigned initials avatar.
    - When `showUnassigned` is false (real name, initials): the fallback div retains
      `bg-muted` and `text-[10px] font-medium text-foreground` exactly as before — assigned
      avatars are unchanged.
    - Existing Tests 1–8 in cached-avatar.test.tsx still pass (icon vs initials, sizes, img swap, onError).
    - New test (Test 9): when name="Unassigned" and url=null, the fallback element's className
      includes `border-dashed` (asserting the distinct treatment); and the assigned-initials
      case (name="Uma Thompson") className does NOT include `border-dashed`.
  </behavior>
  <action>
    Edit ONLY the fallback `<div>` in cached-avatar.tsx (lines ~68-83). Split the background/border
    styling by the existing `showUnassigned` flag rather than always applying `bg-muted`:
    - For the unassigned branch, apply a dashed-border placeholder treatment using existing tokens —
      a dashed border (reuse the `border-dashed border-muted-foreground/...` or `border-border`
      pattern seen in PinnedTabStrip/WorklogsPage/ReleaseDetailPage) plus a dimmed or transparent
      fill (e.g. `bg-muted/40` or `bg-transparent`) so the circle reads as an empty slot. Keep the
      `User` icon in `text-muted-foreground` (or optionally switch to `UserPlus`/`UserRound` if it
      reads clearer — your discretion, but keep the import tidy and remove the old icon import if you
      swap it). Keep `rounded-full flex items-center justify-center` and the size class.
    - For the assigned branch (initials), keep `bg-muted` + `text-[10px] font-medium text-foreground`
      unchanged. Do NOT alter the `<img>` branch.
    Do not introduce any new color variables, hex values, or a new icon-size scheme. Respect the
    existing `cn()` composition and `SIZE_MAP`/`ICON_SIZE_MAP` constants.
    Then add Test 9 to cached-avatar.test.tsx asserting `border-dashed` is present for the
    unassigned fallback and absent for the assigned-initials fallback (query via
    `screen.getByRole('img')` and assert `.className` / `toHaveClass`).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/components/ui/cached-avatar.test.tsx</automated>
  </verify>
  <done>
    All cached-avatar tests pass including the new Test 9. The unassigned fallback div carries a
    dashed border + dimmed/transparent fill (distinct from `bg-muted`); the assigned-initials and
    `<img>` branches are unchanged. `npm run check` (biome + tsc) stays clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The unassigned avatar in CachedAvatar now renders as a dashed-border, dimmed placeholder circle, visually distinct from solid assigned-user avatars.</what-built>
  <how-to-verify>
    1. Run the app (`cd taskflow && npm run dev` or the usual Tauri dev command).
    2. Open the Backlog page and find a story with NO assignee (renders `name="Unassigned"`).
       The Release Detail page assignee column also shows an unassigned avatar.
    3. Confirm the unassigned avatar looks clearly different from an assigned-user avatar
       (dashed/empty placeholder vs solid filled circle) at normal scanning distance, and that
       assigned avatars (initials + images) look unchanged.
    4. Check both light and dark themes if applicable.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual adjustments needed (e.g. border weight, opacity, icon).</resume-signal>
</task>

</tasks>

<verification>
- `cd taskflow && npx vitest run src/components/ui/cached-avatar.test.tsx` — all pass
- `cd taskflow && npm run check` — biome + tsc clean
- Human confirms the unassigned avatar reads as distinct in the running app
</verification>

<success_criteria>
- Unassigned avatar is visually distinct (dashed border + dimmed/transparent fill) from assigned avatars
- Only existing design tokens used (no new colors/icon scheme)
- Assigned initials and image avatars unchanged
- New + existing CachedAvatar tests pass; `npm run check` clean
- Human verification approved
</success_criteria>

<output>
Create `.planning/quick/260606-r80-unassigned-avatar-distinct/260606-r80-SUMMARY.md` when done
</output>
