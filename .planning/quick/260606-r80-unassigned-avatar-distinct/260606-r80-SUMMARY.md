---
phase: quick-260606-r80
plan: "01"
subsystem: ui-components
tags: [avatar, unassigned, visual, tailwind]
dependency_graph:
  requires: []
  provides: [distinct-unassigned-avatar]
  affects: [backlog, board, release-detail]
tech_stack:
  added: []
  patterns: [border-dashed design token reuse, cn() conditional className]
key_files:
  created: []
  modified:
    - taskflow/src/components/ui/cached-avatar.tsx
    - taskflow/src/components/ui/cached-avatar.test.tsx
decisions:
  - "Used border-dashed border-muted-foreground/50 bg-muted/40 matching PinnedTabStrip/WorklogsPage pattern — no new design tokens"
  - "Kept User icon unchanged; split bg/border/text styling by showUnassigned flag in a single cn() call"
metrics:
  duration: "~8 minutes"
  completed: "2026-06-06"
---

# Phase quick-260606-r80 Plan 01: Distinct Unassigned Avatar Summary

**One-liner:** Dashed-border + dimmed-fill unassigned avatar using existing `border-dashed border-muted-foreground/50 bg-muted/40` tokens, making unassigned slots instantly distinguishable from solid assigned-user circles.

## Tasks

| # | Name | Status | Commit |
|---|------|--------|--------|
| 1 | Make the unassigned avatar visually distinct | COMPLETE | 3c0199f5 |
| 2 | Visual verification checkpoint | APPROVED (2026-06-06) | — |

## What Was Built

Task 1 edited the fallback `<div>` in `cached-avatar.tsx` to split styling by the existing `showUnassigned` flag:

- **Unassigned branch:** `border border-dashed border-muted-foreground/50 bg-muted/40` — reads as an empty/placeholder slot
- **Assigned-initials branch:** `bg-muted text-[10px] font-medium text-foreground` — unchanged
- **Image branch:** unchanged

Test 9 added to `cached-avatar.test.tsx` asserts `border-dashed` is present on the unassigned fallback element and absent on the assigned-initials fallback.

## Verification

- `npx vitest run src/components/ui/cached-avatar.test.tsx` — 9/9 tests pass (Tests 1-8 unchanged, Test 9 new)
- `npm run check` (biome + tsc) — clean, no issues

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None — CSS-only styling change, no new endpoints or trust boundaries.

## Human Verification

Task 2 (`checkpoint:human-verify`) — **APPROVED** by user on 2026-06-06. Unassigned avatar confirmed visually distinct (dashed border + dimmed fill) from assigned avatars; assigned avatars unchanged.

## Self-Check: PASSED

- [x] `taskflow/src/components/ui/cached-avatar.tsx` modified in worktree
- [x] `taskflow/src/components/ui/cached-avatar.test.tsx` modified in worktree
- [x] Commit `3c0199f5` exists in worktree git log
- [x] 9 tests pass, `npm run check` clean
