---
phase: quick-260528-19k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/MyTasksTab.tsx
  - taskflow/src/routes/dashboard/MyTasksSkeleton.tsx
  - taskflow/src/routes/dashboard/MyTasksTab.test.tsx
  - taskflow/src/routes/routes.tsx
  - taskflow/src/components/app/sidebar-items.ts
  - taskflow/src/components/app/Sidebar.tsx
  - taskflow/src/stores/settings.store.ts
  - taskflow/src/routes/settings/WorkflowSection.tsx
  - taskflow/src/routes/dashboard/SubtasksPanel.tsx
  - taskflow/src/main.tsx
  - taskflow/src/routes/dashboard/WikiRenderer.tsx
  - taskflow/src/routes/dashboard/DiscussionThreads.tsx
  - taskflow/src/routes/dashboard/create-edit-issue/useIssueMutations.ts
  - taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx
  - taskflow/src/routes/dashboard/issue-detail/useFieldMutation.ts
  - taskflow/src/components/app/CommandPalette.tsx
  - taskflow/src/routes/dashboard/MrHealthPanel.tsx
  - taskflow/src/routes/dashboard/TaskRow.tsx
  - taskflow/src/components/app/Sidebar.test.tsx
  - taskflow/src/routes/settings/Settings.test.tsx
  - taskflow/src/routes/settings/ConnectionsSection.test.tsx
  - taskflow/src/routes/settings/SidebarItemsList.test.tsx
  - taskflow/src/components/app/CommandPalette.test.tsx
  - taskflow/src/routes/dashboard/IssueDetailSheet.test.tsx
autonomous: true
requirements: [QUICK-260528-19k]

must_haves:
  truths:
    - "The /my-tasks route no longer exists and navigating there 404s naturally (no redirect)"
    - "The 'My Tasks' item no longer appears in the sidebar navigation"
    - "The 'Show subtasks in My Tasks' toggle no longer appears in Settings > Workflow"
    - "showSubtasksInMyTasks is removed from the settings store and stripped from persisted state on upgrade (version 24 migration)"
    - "The Dashboard My Subtasks widget (SubtasksPanel) still renders correctly using fetchMyTasksHierarchy"
    - "App builds and the full test suite passes with no live my-tasks references remaining"
  artifacts:
    - path: "taskflow/src/stores/settings.store.ts"
      provides: "Settings store at version 24 with showSubtasksInMyTasks removed + migration"
      contains: "version: 24"
    - path: "taskflow/src/components/app/sidebar-items.ts"
      provides: "Sidebar nav items without my-tasks entry"
    - path: "taskflow/src/routes/routes.tsx"
      provides: "Route table without /my-tasks"
  key_links:
    - from: "taskflow/src/routes/dashboard/SubtasksPanel.tsx"
      to: "fetchMyTasksHierarchy"
      via: "useQuery — MUST be preserved (dashboard widget depends on it)"
      pattern: "fetchMyTasksHierarchy"
    - from: "taskflow/src/stores/settings.store.ts"
      to: "persisted localStorage state"
      via: "migrate() version < 24 deletes showSubtasksInMyTasks"
      pattern: "version < 24"
---

<objective>
Completely remove the My Tasks page from the TaskFlow app — route, page components, sidebar entry, settings toggle, settings-store field, and all live code references — while preserving the Dashboard My Subtasks widget (SubtasksPanel) and its `fetchMyTasksHierarchy` data source.

Purpose: The My Tasks page is being retired. Its content is superseded by the Dashboard My Subtasks widget; keeping the dead route, sidebar entry, and settings toggle is clutter.

Output: A clean removal with no remnants — `/my-tasks` 404s naturally (no redirect per locked decision), the settings store bumps to version 24 with a migration that strips the dropped key, and `npm run build` + `npm test` pass.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260528-19k-i-want-to-completely-remove-my-tasks-pag/260528-19k-CONTEXT.md
@.planning/quick/260528-19k-i-want-to-completely-remove-my-tasks-pag/260528-19k-RESEARCH.md

<notes_from_planner_verification>
The RESEARCH.md file-by-file map is accurate and HIGH confidence. Two corrections / additions
found during planning (grep + direct inspection) — these are folded into the tasks below:

1. CheckSquare is NOT globally exclusive to my-tasks. It is also imported/used in
   issue-type-icon.tsx, AioCycleDetailPage.tsx, AioTestRunDetailPage.tsx, and mocked in
   PinnedTabStrip.test.tsx. ONLY remove CheckSquare from the import list in Sidebar.tsx
   (where it was there solely for the my-tasks nav item). Do NOT touch CheckSquare anywhere else.

2. RESEARCH missed two test files that reference the my-tasks query key and become stale once
   production code stops using it:
   - CommandPalette.test.tsx:240 seeds `['jira-issues', 'my-tasks', ...]` cache. Re-key the seed
     to `'sprint-board'` so the test still exercises the (now sprint-board-only) dedup path.
   - IssueDetailSheet.test.tsx:291-314 is a self-contained test that manually invalidates a
     `['jira-issues', 'my-tasks']` key and asserts 8 invalidations. Remove the my-tasks line and
     update the count assertion 8 → 7 so the test reflects the new production behavior.

3. Stale doc-comments referencing MyTasksTab in MrHealthPanel.tsx (lines 8-9) and TaskRow.tsx
   (line 7) — clean up to honor the "no remnants" decision (cosmetic).

4. useIsActiveRoute.test.ts:29 uses '/my-tasks' only as an arbitrary non-matching path string —
   it is harmless and still passes. Leave it (changing it is pure churn). NOT in files_modified.
</notes_from_planner_verification>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete page files and remove route, sidebar, and settings references</name>
  <files>taskflow/src/routes/dashboard/MyTasksTab.tsx, taskflow/src/routes/dashboard/MyTasksSkeleton.tsx, taskflow/src/routes/dashboard/MyTasksTab.test.tsx, taskflow/src/routes/routes.tsx, taskflow/src/components/app/sidebar-items.ts, taskflow/src/components/app/Sidebar.tsx, taskflow/src/stores/settings.store.ts, taskflow/src/routes/settings/WorkflowSection.tsx</files>
  <action>Per CONTEXT locked decisions, perform a clean break.

(a) Delete the three files outright: MyTasksTab.tsx, MyTasksSkeleton.tsx, MyTasksTab.test.tsx (use rm).

(b) routes.tsx: remove the `import MyTasksTab from './dashboard/MyTasksTab';` line and the `{ path: '/my-tasks', element: <MyTasksTab /> }` route object. Do NOT add any redirect — let /my-tasks 404 naturally per locked decision.

(c) sidebar-items.ts: remove the entire `my-tasks` nav object (id 'my-tasks', label 'My Tasks', path '/my-tasks', iconName 'CheckSquare', section 'main'). Verified order: after removal, index 1 of the sorted main-section items becomes 'standup-notes' (needed for Task 3 test update).

(d) Sidebar.tsx: remove `'/my-tasks'` from the PREFETCH_ROUTES set; remove the now-meaningless comment about /my-tasks using fetchMyTasksHierarchy; remove `CheckSquare` from the lucide-react import list in THIS file only (it was imported solely for the my-tasks nav item — do NOT remove CheckSquare from issue-type-icon.tsx, AioCycleDetailPage.tsx, or AioTestRunDetailPage.tsx where it is still used).

(e) settings.store.ts: remove `showSubtasksInMyTasks: true` from initialSettings; remove `showSubtasksInMyTasks: boolean` (and its JSDoc) from the SettingsState interface; remove `setShowSubtasksInMyTasks` from both the actions interface and its implementation. Bump persist `version: 23` → `version: 24`. Add a `version < 24` migration block that deletes the `showSubtasksInMyTasks` key from persisted state. Also remove the redundant `version < 1` initializer for showSubtasksInMyTasks.

(f) WorkflowSection.tsx: remove `showSubtasksInMyTasks` and `setShowSubtasksInMyTasks` from the useSettingsStore() destructure, and remove the entire "Show subtasks in My Tasks" toggle label block. Keep the surrounding flex container and the "Collapse parent stories by default" toggle.</action>
  <verify>
    <automated>cd taskflow && npm run build 2>&1 | tail -5 && ! test -f src/routes/dashboard/MyTasksTab.tsx && grep -q "version: 24" src/stores/settings.store.ts && grep -q "version < 24" src/stores/settings.store.ts</automated>
  </verify>
  <done>The three MyTasks files are deleted; routes.tsx has no /my-tasks route; sidebar-items.ts has no my-tasks entry; Sidebar.tsx PREFETCH_ROUTES and imports are cleaned; settings.store.ts is at version 24 with the migration and no showSubtasksInMyTasks field/setter; WorkflowSection.tsx no longer renders the toggle; `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dashboard, cache-invalidation, label, and comment references</name>
  <files>taskflow/src/routes/dashboard/SubtasksPanel.tsx, taskflow/src/main.tsx, taskflow/src/routes/dashboard/WikiRenderer.tsx, taskflow/src/routes/dashboard/DiscussionThreads.tsx, taskflow/src/routes/dashboard/create-edit-issue/useIssueMutations.ts, taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx, taskflow/src/routes/dashboard/issue-detail/useFieldMutation.ts, taskflow/src/components/app/CommandPalette.tsx, taskflow/src/routes/dashboard/MrHealthPanel.tsx, taskflow/src/routes/dashboard/TaskRow.tsx</files>
  <action>Remove all remaining live references to /my-tasks and the my-tasks query key.

(a) SubtasksPanel.tsx — KEEP the component and KEEP the `fetchMyTasksHierarchy` import and query (the Dashboard My Subtasks widget depends on it). The `['jira-issues', 'my-tasks', ...]` queryKey on this query is ALSO preserved — it is the widget's own cache key and must stay. Remove only the "View all in My Tasks" link block (the `<Link to="/my-tasks">` and its hasMore text) and the stale comment referencing MyTasksTab's shared cache.

(b) main.tsx — remove the routeLabel entry `if (pathname.startsWith('/my-tasks')) return 'My Tasks';`; update the cache-shape comment that mentions my-tasks so it no longer references it (the generic `{ issues: [] }` handling code below stays and still works).

(c) WikiRenderer.tsx and DiscussionThreads.tsx — remove the `'/my-tasks': 'My Tasks',` entry from each file's staticLabels record.

(d) useIssueMutations.ts, FieldsSection.tsx, useFieldMutation.ts — remove the `queryClient.invalidateQueries({ queryKey: ['jira-issues', 'my-tasks'] })` call from each (safe: SubtasksPanel data refreshes on its normal staleTime cycle).

(e) CommandPalette.tsx — remove the `cachedMyTasks` getQueryData read and the loop that merges its issues into issuesMap. The dedup logic continues to work using only cachedSprintBoard; leave the allIssues variable and sprint-board loop intact.

(f) MrHealthPanel.tsx (lines ~8-9) and TaskRow.tsx (line ~7) — update the stale doc-comments that reference MyTasksTab so no remnant naming survives (cosmetic, per the "no remnants" decision). Do not change runtime behavior.</action>
  <verify>
    <automated>cd taskflow && npm run build 2>&1 | tail -5 && [ "$(grep -rn "to=\"/my-tasks\"\|'/my-tasks'\|\"/my-tasks\"\|'my-tasks'\|MyTasksTab" src --include='*.ts' --include='*.tsx' | grep -v '\.test\.' | grep -v useIsActiveRoute | grep -v SubtasksPanel | grep -vc fetchMyTasksHierarchy)" = "0" ]</automated>
  </verify>
  <done>SubtasksPanel still imports/uses fetchMyTasksHierarchy and keeps its own ['jira-issues','my-tasks',...] query key, but no longer links to /my-tasks; main.tsx, WikiRenderer.tsx, DiscussionThreads.tsx have no my-tasks labels; the three invalidateQueries my-tasks calls are removed; CommandPalette.tsx no longer reads the my-tasks cache; stale MyTasksTab doc-comments are cleaned; no live (non-test, non-SubtasksPanel) my-tasks references remain; `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Update test files to match the removal</name>
  <files>taskflow/src/components/app/Sidebar.test.tsx, taskflow/src/routes/settings/Settings.test.tsx, taskflow/src/routes/settings/ConnectionsSection.test.tsx, taskflow/src/routes/settings/SidebarItemsList.test.tsx, taskflow/src/components/app/CommandPalette.test.tsx, taskflow/src/routes/dashboard/IssueDetailSheet.test.tsx</files>
  <action>Update tests so the suite reflects the removed page. Run `npm test` after edits.

(a) Sidebar.test.tsx — remove the `{ id: 'my-tasks', visible: true }` entry from the sidebarItems mock.

(b) Settings.test.tsx — remove `{ id: 'my-tasks', visible: true }` from the sidebarItems mock; remove `showSubtasksInMyTasks: true` and `setShowSubtasksInMyTasks: vi.fn()` from mockSettingsStore; delete the three WorkflowSection tests that exercise the removed toggle ("renders show subtasks toggle", "subtasks toggle reflects showSubtasksInMyTasks from store", "toggling subtasks calls setShowSubtasksInMyTasks").

(c) ConnectionsSection.test.tsx — remove `showSubtasksInMyTasks: true` and `setShowSubtasksInMyTasks: vi.fn()` from mockSettingsStore.

(d) SidebarItemsList.test.tsx — the assertion at ~line 57 expects `setSidebarItemVisible` called with `('my-tasks', false)` for checkbox index 1. After removing the my-tasks nav item, index 1 is now 'standup-notes' (verified order). Update the expected id to `'standup-notes'`.

(e) CommandPalette.test.tsx — the seed at ~line 240 uses query key `['jira-issues', 'my-tasks', 'TEST', 'customfield_10016']`. Re-key it to `['jira-issues', 'sprint-board', 'TEST', 'customfield_10016']` so the test still exercises the (now sprint-board-only) cache read path in CommandPalette.

(f) IssueDetailSheet.test.tsx — in the "onSettled invalidates ..." test (~lines 291-319): remove the `await qc.invalidateQueries({ queryKey: ['jira-issues', 'my-tasks'] })` line and its matching `expect(invalidateSpy).toHaveBeenCalledWith({ queryKey: ['jira-issues', 'my-tasks'] })` assertion; change the count assertion from `toHaveBeenCalledTimes(8)` to `toHaveBeenCalledTimes(7)`. Rename the test title to drop "my-tasks" if it mentions it.</action>
  <verify>
    <automated>cd taskflow && npm test 2>&1 | tail -15</automated>
  </verify>
  <done>All six test files are updated; no test references the removed my-tasks nav item, the removed showSubtasksInMyTasks field/setter, or the removed my-tasks query invalidation; `npm test` passes with no failures.</done>
</task>

</tasks>

<verification>
- `cd taskflow && npm run build` succeeds (catches TS + CSS import errors that tsc-only misses, per carried project decision).
- `cd taskflow && npm test` passes the full suite.
- `grep -rn "/my-tasks\|MyTasksTab\|showSubtasksInMyTasks" taskflow/src --include='*.ts' --include='*.tsx' | grep -v fetchMyTasksHierarchy | grep -v useIsActiveRoute | grep -v SubtasksPanel` returns no live (non-test, non-cosmetic) matches.
- Manual sanity (optional): launch app, confirm sidebar has no "My Tasks", Settings > Workflow has no "Show subtasks in My Tasks" toggle, Dashboard My Subtasks widget still renders, and navigating to /my-tasks 404s.
</verification>

<success_criteria>
- /my-tasks route removed; no redirect added (404s naturally).
- "My Tasks" sidebar entry gone; CheckSquare removed from Sidebar.tsx imports only.
- "Show subtasks in My Tasks" toggle removed from WorkflowSection.
- settings.store.ts at version 24 with migration stripping showSubtasksInMyTasks; field and setter fully removed.
- SubtasksPanel (Dashboard My Subtasks widget) preserved and functional via fetchMyTasksHierarchy (its own my-tasks query key is intentionally kept).
- All my-tasks cache invalidations and label entries removed; CommandPalette no longer reads the my-tasks cache.
- All affected tests updated; `npm run build` and `npm test` both pass.
</success_criteria>

<output>
Create `.planning/quick/260528-19k-i-want-to-completely-remove-my-tasks-pag/260528-19k-SUMMARY.md` when done.
</output>
</content>
</invoke>
