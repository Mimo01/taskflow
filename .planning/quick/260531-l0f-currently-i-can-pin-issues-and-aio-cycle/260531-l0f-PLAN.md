---
phase: quick-260531-l0f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/stores/pinned-tabs.store.ts
  - taskflow/src/stores/pinned-tabs.store.test.ts
  - taskflow/src/main.tsx
  - taskflow/src/components/app/PinnedTabStrip.tsx
  - taskflow/src/components/app/PinnedTabStrip.test.tsx
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
  - taskflow/src/components/app/sidebar-items.ts
  - taskflow/src/components/app/Sidebar.tsx
  - taskflow/src/routes/dashboard/ReleasesTab.tsx
autonomous: true
requirements: [REL-PIN-01, REL-PIN-02, REL-PIN-03, REL-PIN-04, REL-ICON-01]
must_haves:
  truths:
    - "User can pin a release from the Release detail header; the button toggles to Unpin"
    - "A pinned release shows as a tab in the strip with a Rocket icon and the release name"
    - "Pinned releases persist across active-project switches and app restart"
    - "Clicking a pinned-release tab navigates back to /release/:versionId"
    - "The release icon is Rocket in the sidebar nav, Releases list header, and Release detail header (Labels Tag icon unchanged)"
  artifacts:
    - path: "taskflow/src/stores/pinned-tabs.store.ts"
      provides: "pinnedReleaseMeta map + setPinnedReleaseMeta/clearReleaseMeta + persist version 2 migration"
      contains: "pinnedReleaseMeta"
    - path: "taskflow/src/components/app/PinnedTabStrip.tsx"
      provides: "ReleaseTab union member + Rocket render branch in strip and ghost"
      contains: "Rocket"
    - path: "taskflow/src/main.tsx"
      provides: "three-way release/cycle/issue split for filtering, resolution, activeKey, and onTabClick"
      contains: "REL-"
    - path: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      provides: "Pin/Unpin button in header action row"
      contains: "REL-"
  key_links:
    - from: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      to: "pinned-tabs.store.ts"
      via: "togglePin('REL-'+versionId) + setPinnedReleaseMeta"
      pattern: "REL-"
    - from: "taskflow/src/main.tsx"
      to: "/release/:versionId"
      via: "onTabClick navigates on REL- prefix"
      pattern: "REL-"
    - from: "taskflow/src/main.tsx"
      to: "PinnedTabStrip.tsx"
      via: "resolvedPinnedTabs Map carries ReleaseTab"
      pattern: "type: 'release'"
---

<objective>
Add the ability to pin Jira releases (fix versions) to the pinned-tab strip, mirroring the existing AIO-cycle pinning pattern 1:1, and unify the release icon to lucide `Rocket` everywhere a release is identified.

Purpose: Users can already pin issues and cycles; releases are the missing third pinnable entity. This closes the gap using the exact same Zustand-persisted-store + three-way-split + tab-render pattern already proven for cycles.
Output: A `pinnedReleaseMeta` store map (persist v2), a `ReleaseTab` union variant rendered with `Rocket`, a Pin/Unpin button on the Release detail page, three-way issue/cycle/release routing, and three unified `Rocket` icon swaps.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260531-l0f-currently-i-can-pin-issues-and-aio-cycle/260531-l0f-CONTEXT.md
@.planning/quick/260531-l0f-currently-i-can-pin-issues-and-aio-cycle/260531-l0f-RESEARCH.md

# Pattern to mirror (cycle pinning) and target files
@taskflow/src/stores/pinned-tabs.store.ts
@taskflow/src/components/app/PinnedTabStrip.tsx
@taskflow/src/routes/dashboard/AioCycleDetailPage.tsx
@taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend store with pinnedReleaseMeta + persist v2 migration</name>
  <files>taskflow/src/stores/pinned-tabs.store.ts, taskflow/src/stores/pinned-tabs.store.test.ts</files>
  <behavior>
    - setPinnedReleaseMeta('REL-12345', {name, versionId, projectKey}) stores the entry in pinnedReleaseMeta
    - clearReleaseMeta('REL-12345') deletes the entry
    - migrate from version < 2 initializes pinnedReleaseMeta = {} (existing version < 1 cycle migration still applies)
    - existing pinnedCycleMeta tests remain green
  </behavior>
  <action>
Mirror the `pinnedCycleMeta` pattern exactly (lines 7, 12-13, 19-20, 39-46). In `PinnedTabsState` add `pinnedReleaseMeta: Record<string, { name: string; versionId: string; projectKey: string }>` plus `setPinnedReleaseMeta: (key, meta) => void` and `clearReleaseMeta: (key) => void`. Default `pinnedReleaseMeta: {}` in the initializer next to `pinnedCycleMeta: {}`. Implement `setPinnedReleaseMeta` (spread new entry into the map) and `clearReleaseMeta` (copy + delete key) identically to the cycle versions. Bump persist `version: 1` → `2` (line 51) and extend `migrate` (lines 52-58): keep the `if (version < 1) { s.pinnedCycleMeta = {}; }` branch and add `if (version < 2) { s.pinnedReleaseMeta = {}; }` (per LOCKED decision: persist store version 1→2 with migration). Then in the test file, add a `pinnedReleaseMeta` describe block mirroring the cycle meta tests (set/clear) and a v2 migration test asserting `pinnedReleaseMeta` defaults to `{}` when migrating from version < 2; leave existing v1 cycle tests intact (per Pitfall 1 & 3).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/stores/pinned-tabs.store.test.ts</automated>
  </verify>
  <done>Store exposes pinnedReleaseMeta/setPinnedReleaseMeta/clearReleaseMeta, persist version is 2 with a version<2 migration, and the store test suite passes including new release-meta + migration tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three-way split in main.tsx + ReleaseTab render in PinnedTabStrip</name>
  <files>taskflow/src/main.tsx, taskflow/src/components/app/PinnedTabStrip.tsx, taskflow/src/components/app/PinnedTabStrip.test.tsx</files>
  <behavior>
    - A pinned key starting with 'REL-' resolves to a ReleaseTab {type:'release', name, versionId, projectKey} and is excluded from the issue summary fetch
    - PinnedTabStrip renders release tabs with a Rocket icon + release name (font-mono key + truncated name layout, same as cycle)
    - Clicking a release tab navigates to /release/:versionId
    - activeReleaseKey highlights the tab when on /release/:versionId
  </behavior>
  <action>
In `PinnedTabStrip.tsx`: add `Rocket` to the lucide import (line 13). Add `type ReleaseTab = { type: 'release'; name: string; versionId: string; projectKey: string }` next to the existing IssueTab/CycleTab (lines 35-37) and widen `ResolvedTab` to `IssueTab | CycleTab | ReleaseTab`. In BOTH the ghost render branch chain (lines 147-172) and the tab render branch chain (lines 247-276), add a `resolved?.type === 'release'` branch BEFORE the `<Loader2/>` fallback, rendering `<Rocket className="w-3.5 h-3.5 shrink-0 text-muted-foreground" />` plus the same two-line key/name layout the cycle branch uses (font-mono key + truncated `resolved.name`). Match the cycle branch markup exactly, swapping only the icon and source field (per LOCKED decision: Rocket icon for pinned-release tabs).

In `main.tsx`: (1) add `pinnedReleaseMeta` to the store reads (lines 142-147). (2) Convert the binary split (lines 166-167) to three-way (per LOCKED decision: collision-safe `REL-{versionId}` key scheme, three-way split at every site): `releasePinnedKeys = pinnedKeys.filter((k) => k.startsWith('REL-'))`, `cyclePinnedKeys = pinnedKeys.filter((k) => k.includes('-CY-'))`, `issuePinnedKeys = pinnedKeys.filter((k) => !k.startsWith('REL-') && !k.includes('-CY-'))`. (3) Add `ReleaseTab` local alias (lines 188-189) and widen the `resolvedPinnedTabs` Map type (line 192) to include it. (4) Add a release resolution loop parallel to the cycle loop (lines 207-216) reading `pinnedReleaseMeta[key]` and pushing `{ type: 'release', name, versionId, projectKey }`. (5) Add `activeReleaseKey` (near lines 300-307): when `location.pathname.startsWith('/release/')`, key = `'REL-' + pathname.replace('/release/','')`; extend the `activeKey` prop (line 515) to `activeIssueKey ?? activeCycleKey ?? activeReleaseKey`. (6) Convert `onTabClick` (lines 516-523) to three-way checking `REL-` FIRST: `if (key.startsWith('REL-')) { navigate('/release/' + (pinnedReleaseMeta[key]?.versionId ?? key.slice(4))); } else if (key.includes('-CY-')) { /* existing cycle nav */ } else { handleIssueClick(key, true); }` (per Pitfall 2: REL- must be excluded from issue fetch and checked before -CY-).

In `PinnedTabStrip.test.tsx`: add a `Rocket` mock alongside the existing `FlaskConical` mock (line 8), e.g. `Rocket: () => <span data-testid="rocket-icon" />`, and add a `release tab rendering` describe block mirroring the cycle block (lines 48-99) asserting the rocket icon + release name render (per Pitfall 3).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/components/app/PinnedTabStrip.test.tsx</automated>
  </verify>
  <done>main.tsx filters/resolves/routes release keys three-ways (REL- checked first, excluded from issue fetch), PinnedTabStrip renders a Rocket release tab in both strip and ghost, and PinnedTabStrip tests pass including the new release-tab block.</done>
</task>

<task type="auto">
  <name>Task 3: Pin button on Release detail + Rocket icon unification</name>
  <files>taskflow/src/routes/dashboard/ReleaseDetailPage.tsx, taskflow/src/components/app/sidebar-items.ts, taskflow/src/components/app/Sidebar.tsx, taskflow/src/routes/dashboard/ReleasesTab.tsx</files>
  <action>
In `ReleaseDetailPage.tsx`: add the Pin/Unpin button to the existing right-aligned action row (the `flex justify-end gap-2` block at lines 754-765, alongside "Open in Jira") — this matches the issue/cycle detail Pin row pattern and satisfies the LOCKED "header actions (top-right)" decision (per Assumption A2). Wire store hooks mirroring `AioCycleDetailPage.tsx` lines 427-431 & 799-817: `const pinned = usePinnedTabsStore((s) => s.pinnedKeys.includes('REL-' + versionId))`, plus `togglePin`, `removePin`, `setPinnedReleaseMeta`, `clearReleaseMeta`. Render `<Button variant="outline" size="sm" className="gap-1.5 text-xs">` with `aria-label`/`title` toggling "Pin"/"Unpin" and a `<Pin className={cn('size-3.5', pinned && 'fill-current text-primary')} />`. onClick: `if (pinned) { removePin('REL-'+versionId); clearReleaseMeta('REL-'+versionId); } else { togglePin('REL-'+versionId); setPinnedReleaseMeta('REL-'+versionId, { name: version.name, versionId, projectKey: activeJiraProject ?? '' }); }` — `versionId` from `useParams` (line 123), `version` from `fixVersions?.find(...)` (line 176), `activeJiraProject` already destructured (line 131) (per LOCKED decision: Pin/Unpin in Release detail header actions; store name+versionId+projectKey in pinnedReleaseMeta).

Icon unification to `Rocket` (per LOCKED decision: unify release icon to Rocket in sidebar nav / Releases list header / Release detail header; leave Labels `<Tag>` alone):
- `sidebar-items.ts:70`: `iconName: 'Tag'` → `iconName: 'Rocket'`.
- `Sidebar.tsx`: add `Rocket` to the lucide import (line 24 area) and to `ICON_MAP` (lines 46-57) so `'Rocket'` resolves.
- `ReleasesTab.tsx`: import `Rocket`, change `icon={Package}` (line 315) → `icon={Rocket}`; remove the now-unused `Package` import (line 16) (per Pitfall 4 — biome noUnusedImports).
- `ReleaseDetailPage.tsx`: import `Rocket`, change the header `<Package className="size-4..." />` next to `v{version.id}` (line 449) → `<Rocket className="size-4..." />`; remove the now-unused `Package` import (line 22). DO NOT touch the `<Tag />` at line 487 (Labels section) and keep the `Tag` import.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/ReleasesTab.test.tsx && npm run check</automated>
  </verify>
  <done>Release detail header has a working Pin/Unpin button writing REL-{versionId} + pinnedReleaseMeta; sidebar nav, Releases list header, and Release detail header all use Rocket; Labels Tag icon unchanged; no unused Package imports; `npm run check` (biome + tsc) is fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted store → app state | `pinnedReleaseMeta` rehydrated from disk (pinned-tabs.json) on startup |
| pinned key → router navigation | tab click maps a stored key to a `/release/:versionId` route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l0f-01 | Tampering | persist migration v1→v2 | mitigate | `migrate` initializes `pinnedReleaseMeta = {}` for version < 2 so hydrated stores never read undefined (Pitfall 1) |
| T-l0f-02 | Denial of Service | REL- key reaching issue fetch | mitigate | `issuePinnedKeys` filter excludes `REL-`; onTabClick checks `REL-` before `-CY-` (Pitfall 2) — prevents broken/perpetual-loader tab from failed Jira call |
| T-l0f-03 | Spoofing | REL- key collision with a real `REL` Jira project | accept | No project named exactly `REL` exists in app data; three-way split routes REL-/CY- before issue fallback (Assumption A1, LOW risk) |
| T-l0f-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — code-only change using existing zustand, lucide-react, react-router-dom |
</threat_model>

<verification>
- `cd taskflow && npm run check` (biome + tsc) is fully GREEN — no unused `Package` imports, no type errors from widened unions.
- `cd taskflow && npx vitest run src/stores/pinned-tabs.store.test.ts src/components/app/PinnedTabStrip.test.tsx src/routes/dashboard/ReleasesTab.test.tsx` all pass.
- Manual sanity (optional): pin a release → tab appears with Rocket icon → switch project → tab persists → click → lands on /release/:versionId.
</verification>

<success_criteria>
- A release can be pinned/unpinned from the Release detail header; the tab strip shows it with a Rocket icon and the release name.
- Pinned releases persist across project switches and restart (persist v2 + migration).
- Clicking a pinned-release tab navigates to /release/:versionId; REL- keys never hit the issue summary fetch.
- Rocket icon unified across sidebar nav, Releases list header, and Release detail header; Labels Tag icon untouched.
- `npm run check` green and all three affected vitest files pass.
</success_criteria>

<output>
Create `.planning/quick/260531-l0f-currently-i-can-pin-issues-and-aio-cycle/260531-l0f-SUMMARY.md` when done.
</output>
