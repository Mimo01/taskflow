---
phase: quick-260618-d06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/my-tasks/MyTaskRow.tsx
  - taskflow/src/routes/my-tasks/MyTaskRow.test.tsx
autonomous: true
requirements: [QUICK-260618-d06]
must_haves:
  truths:
    - "On My Tasks, a DONE story/subtask row shows the issue KEY with strikethrough"
    - "On My Tasks, a DONE story/subtask row shows the SUMMARY text WITHOUT strikethrough"
    - "Non-DONE rows show neither the key nor the summary with strikethrough"
  artifacts:
    - path: "taskflow/src/routes/my-tasks/MyTaskRow.tsx"
      provides: "Row markup where doneSummaryClass is applied to the key only"
    - path: "taskflow/src/routes/my-tasks/MyTaskRow.test.tsx"
      provides: "Regression test asserting key-only strikethrough"
  key_links:
    - from: "taskflow/src/routes/my-tasks/MyTaskRow.tsx"
      to: "doneSummaryClass"
      via: "applied to the issue key <button> only, never the summary <span>"
      pattern: "doneSummaryClass"
---

<objective>
On the "My Tasks" page, DONE story and subtask rows currently render BOTH the issue key
AND the summary text with `line-through` strikethrough. The canonical app-wide pattern
(StoryHeaderRow.tsx, WikiRenderer.tsx, and the `doneSummaryClass` JSDoc itself) applies
strikethrough to the KEY only — the summary text stays normal.

Align `MyTaskRow` to the canonical pattern by removing `doneSummaryClass` from the two
summary spans while keeping it on the two key buttons.

Purpose: Visual consistency — My Tasks should match the rest of the app's DONE styling.
Output: Corrected `MyTaskRow.tsx` plus a regression test asserting key-only strikethrough.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# The file to fix
@taskflow/src/routes/my-tasks/MyTaskRow.tsx

# Canonical "key-only strikethrough" pattern — mirror this behavior
@taskflow/src/routes/dashboard/StoryHeaderRow.tsx
@taskflow/src/lib/issueDisplayUtils.ts

# Existing My Tasks test fixtures (makeIssue helper, render scaffolding)
@taskflow/src/routes/my-tasks/MyTasksPage.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove strikethrough from the two summary spans in MyTaskRow</name>
  <files>taskflow/src/routes/my-tasks/MyTaskRow.tsx</files>
  <action>
    In `MyTaskRow.tsx`, `doneSummaryClass(issue.fields.status.statusCategory)` is currently
    applied in FOUR places: two issue-key `<button>` elements (correct) and two summary
    `<span>` elements (incorrect). Per the canonical pattern in StoryHeaderRow.tsx and the
    `doneSummaryClass` JSDoc ("this class is applied to the issue KEY element, not the
    summary text"), strikethrough belongs on the key ONLY.

    Remove the `doneSummaryClass(...)` argument from the `cn(...)` call on the two SUMMARY
    spans:
    1. The subtask-row summary span (the span with classes
       `flex-1 min-w-0 truncate text-sm font-normal text-foreground/90`, ~lines 290-297)
       rendering `{issue.fields.summary}`.
    2. The parent-row summary span (the span with classes
       `flex-1 min-w-0 truncate text-sm font-medium text-foreground`, ~lines 363-370)
       rendering `{issue.fields.summary}`.

    For the parent summary span, after removing the only dynamic class the `cn(...)` wrapper
    becomes a single static string — prefer a plain `className="..."` string (no `cn`) to
    match the StoryHeaderRow summary span which uses a bare className. For the subtask
    summary span do the same. Do NOT touch either issue-key `<button>` — both must keep
    `doneSummaryClass(issue.fields.status.statusCategory)`.

    Leave the `doneSummaryClass` import in place (still used by the key buttons).
  </action>
  <verify>
    <automated>cd taskflow && grep -c "doneSummaryClass" src/routes/my-tasks/MyTaskRow.tsx | grep -qx 3 && echo "OK: 3 references (1 import + 2 key buttons)"</automated>
  </verify>
  <done>
    `doneSummaryClass` appears exactly 3 times in MyTaskRow.tsx: the import line and the
    two issue-key buttons. Neither summary span references it. Both key buttons still do.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression test asserting key-only strikethrough on My Tasks rows</name>
  <files>taskflow/src/routes/my-tasks/MyTaskRow.test.tsx</files>
  <behavior>
    - Rendering a DONE parent row: the issue-key element has class `line-through`; the
      summary text element does NOT have class `line-through`.
    - Rendering a DONE subtask row (isSubtask): same — key crossed out, summary not.
    - Rendering a non-DONE (indeterminate) row: neither key nor summary has `line-through`.
  </behavior>
  <action>
    Create `MyTaskRow.test.tsx` (new file) co-located with the component. Import
    `MyTaskRow` and render it directly with `@testing-library/react` (no need to drive the
    full page). Build minimal `JiraIssue` fixtures mirroring the `makeIssue` shape in
    `MyTasksPage.test.tsx` (summary, status.name, status.statusCategory.key, issuetype,
    priority, assignee:null, timetracking:null). Provide the required `MyTaskRowProps`
    (jiraBaseUrl, storyPointsFieldKey, flaggedFieldKey, onOpenPeek/onOpenIssue as vi.fn()).

    The issue key renders inside a `<button>` whose text is the key; the summary renders
    in a `<span>` whose text is `Summary for <KEY>`. Use `screen.getByText(key)` for the
    key element and `screen.getByText(summary)` for the summary element, then assert
    `.toHaveClass('line-through')` on the key and `.not.toHaveClass('line-through')` on the
    summary. Mirror the assertion style already used in
    `src/routes/dashboard/WikiRenderer.test.tsx` (lines ~1454-1467).

    Cover three cases: DONE parent (key crossed, summary not), DONE subtask
    (`isSubtask` prop, same expectation), and indeterminate parent (neither crossed).
    Wrap the test file with any required providers only if rendering fails without them;
    `MyTaskRow` is a presentational component and should render standalone.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/my-tasks/MyTaskRow.test.tsx</automated>
  </verify>
  <done>
    New test file passes: it asserts the DONE key has `line-through`, the DONE summary does
    NOT, and the indeterminate row has neither. Test fails if strikethrough is reintroduced
    on a summary span (regression guard).
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/routes/my-tasks/MyTaskRow.test.tsx` passes (from taskflow/).
- `npm run check` (biome + tsc) stays clean.
- Manual sanity (optional): My Tasks page — a DONE story shows key crossed out, summary normal.
</verification>

<success_criteria>
- DONE rows on My Tasks render the issue KEY with strikethrough and the SUMMARY without it,
  matching StoryHeaderRow / the app-wide canonical pattern.
- `doneSummaryClass` is applied only to the two issue-key buttons in MyTaskRow.tsx.
- A regression test locks the key-only behavior for parent, subtask, and non-DONE rows.
- biome + tsc clean.
</success_criteria>

<output>
Create `.planning/quick/260618-d06-on-my-tasks-page-done-stories-should-onl/260618-d06-SUMMARY.md` when done.
</output>
