---
phase: quick-260618-d06
plan: "01"
subsystem: my-tasks
tags: [strikethrough, done-state, visual-consistency, regression-test]
dependency_graph:
  requires: []
  provides: [key-only-strikethrough-on-my-tasks]
  affects: [MyTaskRow]
tech_stack:
  added: []
  patterns: [doneSummaryClass-key-only, as-unknown-cast-for-partial-fixtures]
key_files:
  modified:
    - taskflow/src/routes/my-tasks/MyTaskRow.tsx
  created:
    - taskflow/src/routes/my-tasks/MyTaskRow.test.tsx
decisions:
  - "Use as unknown as JiraIssue cast for minimal test fixtures to avoid fighting the index signature on JiraIssue.fields"
metrics:
  duration: "~4 minutes"
  completed: "2026-06-18"
  tasks_completed: 2
  files_changed: 2
---

# Phase quick-260618-d06 Plan 01: My Tasks DONE Story Strikethrough Summary

**One-liner:** Removed `doneSummaryClass` from both summary spans in `MyTaskRow`, leaving strikethrough on the issue key buttons only — matching the app-wide canonical pattern from `StoryHeaderRow`.

## What Was Built

Two tasks executed:

**Task 1 — Fix MyTaskRow.tsx (commit 30f3a7b8)**

`doneSummaryClass(issue.fields.status.statusCategory)` was applied in four places: two issue-key `<button>` elements (correct) and two summary `<span>` elements (incorrect). Removed the `cn(...)` wrapper entirely from both summary spans, replacing with plain static `className` strings. Both key buttons remain unchanged.

Result: `doneSummaryClass` appears exactly 3 times — 1 import + 2 key buttons. Neither summary span references it.

**Task 2 — Regression test MyTaskRow.test.tsx (commit 481eca3e)**

New test file with 3 cases:
- DONE parent row: key has `line-through`, summary does not
- DONE subtask row (`isSubtask` prop): key has `line-through`, summary does not
- Indeterminate parent row: neither key nor summary has `line-through`

Tests use `@testing-library/react` directly against `MyTaskRow` as a presentational component. `isIssueFlagged` mocked at module level. Fixture cast via `as unknown as JiraIssue` to avoid fighting the index signature on `JiraIssue.fields`.

## Verification

- `npx vitest run src/routes/my-tasks/MyTaskRow.test.tsx` — 3/3 passed
- `npm run check` (biome + tsc) — clean (23 pre-existing warnings in unrelated files, 0 errors)
- `doneSummaryClass` count in MyTaskRow.tsx confirmed at 3 (grep -c)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Test fixture missing required `JiraIssue.id` and `status.id` fields**
- **Found during:** Task 2 — tsc gate
- **Issue:** Initial fixture omitted `id` (top-level) and `status.id`; TypeScript error TS2741
- **Fix:** Added both fields; also switched from direct return type to `as unknown as JiraIssue` cast to handle the `[key: string]: unknown` index signature incompatibility cleanly
- **Files modified:** taskflow/src/routes/my-tasks/MyTaskRow.test.tsx
- **Commit:** 481eca3e

## Commits

| Hash | Message |
|------|---------|
| 30f3a7b8 | fix(quick-260618-d06-01): remove strikethrough from summary spans in MyTaskRow |
| 481eca3e | test(quick-260618-d06-02): add regression test for key-only strikethrough on My Tasks rows |

## Self-Check: PASSED

- [x] `taskflow/src/routes/my-tasks/MyTaskRow.tsx` modified — confirmed (30f3a7b8)
- [x] `taskflow/src/routes/my-tasks/MyTaskRow.test.tsx` created — confirmed (481eca3e)
- [x] Both commits exist in git log
- [x] Tests pass: 3/3
- [x] biome + tsc: clean
