---
phase: quick-260316-r0x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/main.tsx
  - taskflow/src/routes/dashboard/IssueDetailPage.tsx
  - taskflow/src/routes/dashboard/IssueDetailSheet.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.tsx
  - taskflow/src/routes/dashboard/MyTasksTab.tsx
  - taskflow/src/routes/dashboard/BacklogPage.tsx
  - taskflow/src/routes/dashboard/EpicsPage.tsx
  - taskflow/src/routes/dashboard/index.tsx
  - taskflow/src/routes/dashboard/SubtasksPanel.tsx
  - taskflow/src/components/app/TopBar.tsx
  - taskflow/src/components/app/PinnedTabStrip.tsx
  - taskflow/src/components/app/CommandPalette.tsx
  - taskflow/src/components/app/RecentItemsPopover.tsx
  - taskflow/src/routes/notifications/NotificationPopover.tsx
autonomous: true
requirements: [QUICK-R0X]

must_haves:
  truths:
    - "Clicking any issue anywhere navigates to /issue/:key as a full-page route"
    - "Issue detail page shows back arrow + breadcrumb showing origin page name"
    - "Back arrow returns user to the page they came from"
    - "All app chrome (Sidebar, TopBar, PinnedTabStrip) remains visible on issue page"
    - "Sheet overlay is completely removed — no dual-mode behavior"
    - "Pinned tab clicks navigate to /issue/:key route"
  artifacts:
    - path: "taskflow/src/routes/dashboard/IssueDetailPage.tsx"
      provides: "Full-page issue detail route component with back/breadcrumb nav"
      min_lines: 60
    - path: "taskflow/src/main.tsx"
      provides: "Updated router with /issue/:key route, handleIssueClick uses navigate"
      contains: "path: '/issue/:key'"
  key_links:
    - from: "taskflow/src/main.tsx"
      to: "taskflow/src/routes/dashboard/IssueDetailPage.tsx"
      via: "router config path /issue/:key"
      pattern: "path.*issue.*:key"
    - from: "handleIssueClick in main.tsx"
      to: "/issue/:key route"
      via: "navigate('/issue/${issueKey}', { state: { from } })"
      pattern: "navigate.*issue"
    - from: "IssueDetailPage.tsx"
      to: "IssueDetailContent + IssueDetailSidebar"
      via: "direct import and render (same body as old IssueDetailSheet)"
      pattern: "IssueDetailContent|IssueDetailSidebar"
---

<objective>
Replace the IssueDetailSheet (75vw slide-out panel) with a full-page route-based issue detail view at `/issue/:key`. All issue clicks navigate to this route. Add back arrow + breadcrumb navigation showing the origin page.

Purpose: Issue detail deserves full-page real estate instead of a cramped sheet overlay. Consistent navigation model.
Output: IssueDetailPage.tsx route component, updated router, all issue click handlers converted to navigate.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260316-r0x-redo-issue-detail-as-full-page-with-back/260316-r0x-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From taskflow/src/routes/dashboard/IssueDetailSheet.tsx (IssueDetailBody — reuse this logic):
```typescript
// IssueDetailBody fetches issue via useQuery(['jira-issue-detail', issueKey, jiraBaseUrl])
// Renders IssueDetailContent (left ~60%) + IssueDetailSidebar (right ~42%)
// Also fetches epicStories if issue is an Epic
```

From taskflow/src/routes/dashboard/IssueDetailContent.tsx:
```typescript
interface IssueDetailContentProps {
  issue: JiraIssueDetail
  issueKey: string
  jiraBaseUrl: string
  onOpenIssue?: (key: string) => void
  storyPointsFieldKey: string
  sprintFieldKey: string
  epicLinkFieldKey: string
  onEdit?: (initialValues: EditInitialValues) => void
  onAddSubtask?: (parentKey: string) => void
  epicStories?: JiraIssue[]
  isPinned?: boolean
  onTogglePin?: (key: string) => void
}
```

From taskflow/src/main.tsx (outlet context shape passed to all route children):
```typescript
// Outlet context: { onIssueClick, onEpicClick, openEdit, openAddSubtask, openCreateStory, selectedIssueKey }
```

From taskflow/src/components/app/PinnedTabStrip.tsx:
```typescript
interface PinnedTabStripProps {
  pinnedKeys: string[];
  activeKey: string | null;
  onTabClick: (issueKey: string) => void;
  onTabClose: (issueKey: string) => void;
  onReorder: (fromIndex: number, toIndex: number) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IssueDetailPage route component with back/breadcrumb navigation</name>
  <files>taskflow/src/routes/dashboard/IssueDetailPage.tsx</files>
  <action>
Create a new full-page issue detail route component that:

1. **Reads issueKey from route params** via `useParams<{ key: string }>()` from react-router-dom.

2. **Back/breadcrumb navigation header:**
   - Use `useLocation()` to read `location.state?.from` (an object `{ path: string; label: string }` passed by handleIssueClick).
   - Render a header bar above the issue content with:
     - A back arrow button (lucide `ArrowLeft` icon) that calls `navigate(from.path)` (or `navigate(-1)` as fallback if no state).
     - A breadcrumb trail: `{from.label} > {issueKey}` — e.g. "Sprint Board > PROJ-123". Use muted text for the origin label (clickable, same navigate behavior), separator `>` or `/`, and the issue key in normal weight.
   - If no `from` state exists (direct URL navigation), show just the back arrow (calls `navigate(-1)`) and the issue key as breadcrumb.

3. **Issue detail body:** Extract the `IssueDetailBody` logic from `IssueDetailSheet.tsx` into this component. Specifically:
   - Use `useAuthStore` for `jiraBaseUrl`, `jiraConnected`.
   - Use `useSettingsStore` for custom field keys.
   - Use `useQuery` for `jira-issue-detail` and `jira-epic-stories` (same query config as IssueDetailBody).
   - Render `IssueDetailContent` (left) and `IssueDetailSidebar` (right) in a flex row, same as the old sheet body but now full-width in the main content area.

4. **Props from outlet context:** Read `onIssueClick`, `openEdit`, `openAddSubtask` from `useOutletContext` to pass as `onOpenIssue`, `onEdit`, `onAddSubtask` to IssueDetailContent. Also read `isPinned`/`onTogglePin` — derive `isPinned` from `usePinnedTabsStore` using the current issueKey.

5. **Track recent item:** On mount (or when issueKey changes), call `pushRecentItem` from the recent-items store with the issue key and resolved title (from the fetched issue data).

6. **Skeleton loading state:** Reuse the same skeleton pattern from IssueDetailSheet (or inline a similar one).

Styling: The header (back + breadcrumb) should be a compact bar with `px-6 py-3 border-b flex items-center gap-2 text-sm`. The issue body below fills the remaining space with `flex-1 overflow-hidden`.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>IssueDetailPage.tsx exists with route param reading, back arrow, breadcrumb, and full issue detail rendering. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire router, handleIssueClick, and all entry points to use navigate</name>
  <files>
    taskflow/src/main.tsx,
    taskflow/src/routes/dashboard/IssueDetailSheet.tsx,
    taskflow/src/routes/dashboard/SprintBoardTab.tsx,
    taskflow/src/routes/dashboard/MyTasksTab.tsx,
    taskflow/src/routes/dashboard/BacklogPage.tsx,
    taskflow/src/routes/dashboard/EpicsPage.tsx,
    taskflow/src/routes/dashboard/index.tsx,
    taskflow/src/routes/dashboard/SubtasksPanel.tsx,
    taskflow/src/components/app/TopBar.tsx,
    taskflow/src/components/app/PinnedTabStrip.tsx,
    taskflow/src/components/app/CommandPalette.tsx,
    taskflow/src/components/app/RecentItemsPopover.tsx,
    taskflow/src/routes/notifications/NotificationPopover.tsx
  </files>
  <action>
**main.tsx — major changes:**

1. **Add route:** Add `{ path: '/issue/:key', element: <IssueDetailPage /> }` to the router children. Import `IssueDetailPage` from `./routes/dashboard/IssueDetailPage`.

2. **Convert handleIssueClick to navigate:** Instead of `setSelectedIssueKey(issueKey)`, call `navigate('/issue/${issueKey}', { state: { from: { path: location.pathname, label: routeLabel(location.pathname) } } })`. Keep the `pushRecentItem` + title resolution logic in handleIssueClick (the page component will also push, but handleIssueClick is the canonical entry point from list views).

3. **Add routeLabel helper** (local function in AppLayout) that maps pathname to display label:
   - `/sprint-board` -> "Sprint Board"
   - `/backlog` -> "Backlog"
   - `/my-tasks` -> "My Tasks"
   - `/epics` -> "Epics"
   - `/dashboard` -> "Overview"
   - `/mr-attention` -> "MR Attention"
   - `/sprint-progress` -> "Sprint Progress"
   - `/workload` -> "Workload"
   - `/releases` -> "Releases"
   - default -> "Home"

4. **Remove IssueDetailSheet:** Delete the `<IssueDetailSheet>` JSX from the render. Remove the `selectedIssueKey` state and `setSelectedIssueKey` useState. Remove the `IssueDetailSheet` import. Keep `isPinned` and `togglePin` — pass them via outlet context instead.

5. **Update outlet context:** Change from `{ onIssueClick, onEpicClick, openEdit, openAddSubtask, openCreateStory, selectedIssueKey }` to `{ onIssueClick: handleIssueClick, onEpicClick: handleIssueClick, openEdit: handleOpenEdit, openAddSubtask: handleOpenAddSubtask, openCreateStory: handleOpenCreateStory }`. Remove `selectedIssueKey` from context (no longer needed — the issue page reads from route params).

6. **Update PinnedTabStrip:** Change `activeKey={selectedIssueKey}` to derive active key from current URL. Use a simple check: if `location.pathname.startsWith('/issue/')`, extract the key from pathname. Otherwise null. Also change `onTabClick` to navigate: `onTabClick={(key) => handleIssueClick(key)}` (already does navigate after conversion).

7. **Update CommandPalette onIssueClick:** The `onIssueClick` prop already calls `handleIssueClick` then closes palette — this will now navigate, which is correct.

**TopBar.tsx:** The `onIssueClick` prop is passed to `RecentItemsPopover` and `NotificationPopover`. These will continue to call the same callback, which now navigates. No change needed to TopBar itself beyond updating the type if `selectedIssueKey` was in its props (it is not — only `onIssueClick`).

**RecentItemsPopover.tsx:** Its `onIssueClick` callback already delegates to the parent. No structural change needed. The callback now navigates instead of opening a sheet.

**NotificationPopover.tsx:** Same pattern — `onIssueClick` callback delegates up. No change needed.

**Route components (SprintBoardTab, MyTasksTab, BacklogPage, EpicsPage, Dashboard/index.tsx, SubtasksPanel):**
- Remove `selectedIssueKey` from any `useOutletContext` destructuring where it appears.
- The `onIssueClick` callback they receive from outlet context now navigates, so their existing usage is correct.
- In BacklogPage and MyTasksTab: remove `selectedIssueKey` from the destructured context (it was used for J/K navigation highlighting — keep the `useListNavigation` `onSelect` callback which calls `onIssueClick`, which now navigates; for J/K highlight, derive from `useLocation().pathname` or accept that navigating away from the list on J/K select is the new behavior. Per user decision: "All issue clicks navigate to the full-page issue detail route" — J/K selection should navigate to the issue page).

**PinnedTabStrip.tsx:** No structural change needed — it already receives `onTabClick` and `activeKey` as props. The parent (main.tsx) is updating what it passes.

**Delete consideration:** Do NOT delete `IssueDetailSheet.tsx` file yet — just remove its usage from main.tsx. The executor can leave the file for cleanup later, or delete it if confident all references are removed.

**Test files:** Update test mocks that provide `useOutletContext` to remove `selectedIssueKey` from the mock return value where it appears (SprintBoardTab.test.tsx, MyTasksTab.test.tsx, BacklogPage.test.tsx). Also update any test that asserts `onIssueClick` opens a sheet — it now navigates. For `IssueDetailSheet.test.tsx` — this test file can be left as-is (tests a now-unused component) or deleted.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -30 && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All issue clicks navigate to /issue/:key. Sheet is removed from AppLayout. Back arrow and breadcrumb show origin page. PinnedTabStrip highlights active issue from URL. TypeScript compiles and existing tests pass (with updated mocks).</done>
</task>

</tasks>

<verification>
- Navigate to Sprint Board, click an issue -> URL changes to `/#/issue/PROJ-123`, full-page detail renders with "Sprint Board > PROJ-123" breadcrumb
- Click back arrow -> returns to Sprint Board
- Navigate to Backlog, click an issue -> breadcrumb shows "Backlog > PROJ-456"
- Click a pinned tab -> navigates to `/issue/:key`, pinned tab shows as active
- Open Command Palette, select an issue -> navigates to issue page
- Click a notification with an issue key -> navigates to issue page
- Sidebar, TopBar, PinnedTabStrip remain visible on the issue page
- No sheet overlay appears anywhere
</verification>

<success_criteria>
- IssueDetailSheet is no longer rendered in AppLayout
- /issue/:key route exists and renders full-page issue detail
- Back arrow + breadcrumb navigation works from any entry point
- All app chrome remains visible on the issue detail page
- TypeScript compiles without errors
- Existing tests pass with updated mocks
</success_criteria>

<output>
After completion, create `.planning/quick/260316-r0x-redo-issue-detail-as-full-page-with-back/260316-r0x-SUMMARY.md`
</output>
