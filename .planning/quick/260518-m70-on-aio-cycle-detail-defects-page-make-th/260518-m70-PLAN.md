---
phase: 260518-m70-quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/AioCycleDetailPage.tsx
  - taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx
autonomous: true
requirements: [QUICK-260518-m70]

must_haves:
  truths:
    - "On AIO cycle detail defects tab, the entire defect row is clickable (not just the issue key)"
    - "Clicking anywhere on a resolved defect row navigates to /issue/{resolvedKey}"
    - "While the defect's Jira issue is still loading, the row is NOT clickable (no navigation, no pointer cursor)"
    - "Breadcrumb trail is pushed before navigation, mirroring the existing runs-row pattern (label = cycleName, path = current pathname)"
    - "Keyboard interaction matches the existing run-row pattern (Enter / Space trigger navigation; tabIndex set; role='button' when interactive)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/AioCycleDetailPage.tsx"
      provides: "DefectRow with full-row clickability gated on issue resolution"
      contains: "DefectRow"
    - path: "taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx"
      provides: "Coverage for clickable defect row + loading-gate behavior"
      contains: "defect row clickable"
  key_links:
    - from: "DefectRow tr onClick"
      to: "react-router navigate(`/issue/${resolvedKey}`)"
      via: "useNavigate() passed via prop"
      pattern: "navigate\\(`/issue/"
    - from: "DefectRow tr onClick"
      to: "useBreadcrumbStore.getState().push(...)"
      via: "matches openRun() pattern at line 347"
      pattern: "useBreadcrumbStore.getState\\(\\)\\.push"
---

<objective>
On the AIO cycle detail Defects tab, make each defect row fully clickable (not just the Jira key) so users can navigate to the issue detail by clicking anywhere on the row. The row must only become clickable AFTER the underlying Jira issue (`issueQuery`) finishes loading — while loading, the row stays non-interactive (no pointer cursor, no onClick, no role="button"). This prevents navigation to an unresolved/raw numeric ID before we know the real key.

Purpose: Improves click target ergonomics on the Defects tab to match the existing Runs tab UX (already row-clickable per lines 629-644), while avoiding the "click before resolved" trap where a user could be sent to `/issue/186227` instead of `/issue/PROJ-1234`.

Output: Updated `DefectRow` component + corresponding tests in `AioCycleDetailPage.test.tsx`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@taskflow/src/routes/dashboard/AioCycleDetailPage.tsx
@taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx

<interfaces>
<!-- Key contracts the executor will use. Already in codebase — do NOT redefine. -->

Existing DefectRow signature (taskflow/src/routes/dashboard/AioCycleDetailPage.tsx, lines 81-95):
```typescript
function DefectRow({
  defectIdOrKey,   // numeric Jira internal ID string OR real key (e.g. "PROJ-1234")
  jiraBaseUrl,
  token,
  tokenLoading,
  triggeredBy,
}: {
  defectIdOrKey: string;
  jiraBaseUrl: string | undefined;
  token: string | null;
  tokenLoading: boolean;
  triggeredBy: string;
})
```

Inside DefectRow (lines 96-103):
- `issueQuery` = useQuery<JiraIssue | null>({ ... fetchJiraIssueByKey ... })
- `issueQuery.isLoading` — TRUE while issue resolving (gate on this for clickability)
- `issueQuery.data?.key` — resolved canonical Jira key (only safe to navigate to once present)
- `linkTarget = `/issue/${issueQuery.data?.key ?? defectIdOrKey}`` — currently used by the inner NavLink

Existing row-clickable pattern (same file, lines 629-644, the runs table):
```
<tr
  className="border-b border-border hover:bg-muted/30 transition-colors cursor-pointer"
  role="button"
  tabIndex={0}
  data-testid={`run-row-${run.id}`}
  aria-label={`Open run for ${run.testCase?.title ?? run.testCaseKey}`}
  onClick={() => openRun(run)}
  onKeyDown={(e) => {
    if (e.key === 'Enter' || e.key === ' ') {
      e.preventDefault();
      openRun(run);
    }
  }}
>
```

Existing breadcrumb-push-then-navigate pattern (line 346-349):
```
const openRun = (run: AioTestRun) => {
  useBreadcrumbStore.getState().push({ label: cycleName, path: location.pathname });
  navigate(`/aio-cycle/${projectKey}/${cycleKey}/run/${run.id}`);
};
```

DefectRow is defined OUTSIDE the AioCycleDetailPage component, so it does not have access to `navigate`, `location`, or `cycleName` via closure. New props will be needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make DefectRow fully row-clickable, gated on issue resolution</name>
  <files>taskflow/src/routes/dashboard/AioCycleDetailPage.tsx, taskflow/src/routes/dashboard/AioCycleDetailPage.test.tsx</files>
  <behavior>
    Tests to add in AioCycleDetailPage.test.tsx (mirror existing AIOC-03 defect tests, around lines 314-450):
    - Test A (loading state): With `fetchJiraIssueByKey` returning a never-resolving promise, after clicking the Defects tab, the rendered defect row MUST NOT have `role="button"`, MUST NOT have `cursor-pointer` class, MUST NOT navigate when clicked (use a navigate spy or assert URL unchanged via MemoryRouter), AND its keyDown handler MUST be a no-op (Enter/Space do nothing). Assert the loading skeleton (`defect-title-loading-186227`) is present.
    - Test B (resolved + click): With `fetchJiraIssueByKey` resolving to `{ key: 'PROJ-1234', fields: { summary: 'x', status: { name: 'Open', statusCategory: { key: 'new' } } } }`, after clicking the Defects tab and awaiting `PROJ-1234` to render, the row MUST have `cursor-pointer` class and `role="button"`. Clicking the row (any cell area, e.g. the Title cell or Triggered By cell — NOT the inner NavLink) MUST navigate to `/issue/PROJ-1234`. Use a navigate spy via `vi.mock('react-router-dom', ...)` already present in the test file, OR assert via location.pathname using MemoryRouter + a location-watcher pattern already used elsewhere in this file (whichever the file already uses — do not introduce a new mock style).
    - Test C (resolved + keyboard): With the same resolved mock, pressing Enter on the focused row MUST navigate to `/issue/PROJ-1234`. Pressing Space MUST also navigate (and `e.preventDefault()` is called to suppress page-scroll). Verify by spying navigate.
    - Test D (breadcrumb push): On a successful row click (after resolution), `useBreadcrumbStore.getState().push` is called with `{ label: cycleName, path: location.pathname }` BEFORE navigate. Either spy on the store's push action or assert post-click trail content. Pattern already established by the existing run-row tests — copy that approach.

    Tests MUST be written FIRST and MUST FAIL before implementation. Place new tests inside the existing `describe` block that contains the AIOC-03 defect tests.
  </behavior>
  <action>
    Modify `DefectRow` in `taskflow/src/routes/dashboard/AioCycleDetailPage.tsx` (lines 81-204) to make the entire `<tr>` clickable, gated on `issueQuery` having resolved. Steps:

    1. Extend `DefectRow` props to accept the navigation primitives it needs (DefectRow lives outside the page component, so it cannot close over them):
       - `onOpen: (resolvedKey: string) => void` — single callback passed by the parent that performs the breadcrumb push + navigate. Parent will define it inline next to `openRun` (around line 346-349) as e.g. `const openDefect = (resolvedKey: string) => { useBreadcrumbStore.getState().push({ label: cycleName, path: location.pathname }); navigate(`/issue/${resolvedKey}`); };`. Pass `onOpen={openDefect}` in the JSX at lines 740-748.

    2. Inside DefectRow, compute `const resolvedKey = issueQuery.data?.key ?? null;` and `const isClickable = !issueQuery.isLoading && !!resolvedKey;`. Do NOT treat the raw `defectIdOrKey` numeric fallback as clickable — only navigate when we have the actual Jira key, to avoid sending the user to `/issue/186227`.

    3. Update the `<tr>` (line 106) to conditionally apply interactive attributes:
       - `className`: keep existing `border-b border-border hover:bg-muted/30 transition-colors`, append ` cursor-pointer` ONLY when `isClickable`.
       - When `isClickable`: add `role="button"`, `tabIndex={0}`, `aria-label={`Open defect ${resolvedKey}`}`, `data-testid={`defect-row-${defectIdOrKey}`}`, `onClick={() => onOpen(resolvedKey!)}`, and `onKeyDown` matching the run-row pattern (Enter/Space → preventDefault + onOpen). When NOT clickable: omit `role`, `tabIndex`, `onClick`, `onKeyDown`; still include the `data-testid` so tests can locate the loading row.

    4. Keep the inner `<NavLink>` on the Key cell (line 112-114) AS-IS — it still works for direct key clicks, and is correctly skipped (no `stopPropagation` needed because NavLink already calls `e.preventDefault` on its own anchor; however, to keep behavior identical and avoid double-navigation when the outer `<tr>` is also clickable, add `onClick={(e) => e.stopPropagation()}` to the NavLink so clicking it does NOT also fire the row's onClick — they navigate to the same place anyway, but stopping propagation keeps the click chain clean and matches the runs-table behavior where inner interactive elements should not double-fire).

    5. In the parent component (around line 346-349), add `openDefect` next to `openRun` using the exact same shape: `useBreadcrumbStore.getState().push({ label: cycleName, path: location.pathname }); navigate(`/issue/${resolvedKey}`);`. Wire it into the `<DefectRow>` JSX (lines 740-748).

    Do NOT touch: column structure, skeleton-loading cells, status pill rendering, assignee/reporter/priority/severity cells, or the existing `linkTarget`/NavLink href. Scope is strictly: outer `<tr>` becomes clickable when resolved, and only when resolved.

    Use existing project conventions (Tailwind classes, react-router `useNavigate`, `useBreadcrumbStore` from `@/stores/breadcrumb.store`). No new dependencies.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- --run src/routes/dashboard/AioCycleDetailPage.test.tsx</automated>
  </verify>
  <done>
    - All 4 new test cases (A loading, B click resolved, C keyboard resolved, D breadcrumb push) pass.
    - All pre-existing AIOC-03 defect tests still pass (key link still works, triggered-by column still works, raw numeric ID still passed to fetchJiraIssueByKey).
    - Manual sanity (deferred to UAT): clicking anywhere on a resolved defect row in the running app navigates to `/issue/{key}` and the breadcrumb shows the cycle name when going back.
    - No new lint errors introduced (`npm run lint` clean for the modified file).
  </done>
</task>

</tasks>

<verification>
- `npm test -- --run src/routes/dashboard/AioCycleDetailPage.test.tsx` passes (all old + new tests green).
- Grep guard: outer `<tr>` inside DefectRow now contains `role="button"` only inside a conditional branch — confirm via `grep -n "role=\"button\"" taskflow/src/routes/dashboard/AioCycleDetailPage.tsx` returns at least 2 hits (runs row + defect row).
- No regression in cycle runs row clickability (existing run-row tests still pass).
</verification>

<success_criteria>
- Defect row is fully clickable across all 8 columns once the underlying Jira issue resolves.
- During the loading phase, the row remains visually identical to today (skeletons render, no pointer cursor), and clicks/keypresses do NOT navigate.
- Navigation target uses the RESOLVED Jira key (`issueQuery.data.key`) — never the raw numeric `defectIdOrKey` — to match the existing inner NavLink semantics.
- Breadcrumb trail is pushed with the cycle name + current pathname before navigation, so the back button on the issue detail page returns the user to the same cycle/Defects tab.
- Tests are written first (RED), then implementation makes them GREEN.
</success_criteria>

<output>
Create `.planning/quick/260518-m70-on-aio-cycle-detail-defects-page-make-th/260518-m70-SUMMARY.md` when done, summarizing the modified files, the gate behavior (loading vs resolved), and any test additions.
</output>
