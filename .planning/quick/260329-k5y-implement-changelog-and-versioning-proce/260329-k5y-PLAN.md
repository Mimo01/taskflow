---
phase: quick-260329-k5y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/cliff.toml
  - taskflow/CHANGELOG.md
  - taskflow/scripts/bump-version.mjs
  - taskflow/scripts/release.sh
  - taskflow/scripts/generate-changelog.sh
  - taskflow/package.json
autonomous: true
requirements: [QUICK-260329-K5Y]

must_haves:
  truths:
    - "Running `node scripts/bump-version.mjs X.Y.Z` updates all 3 version files, regenerates CHANGELOG.md, commits, tags with annotation, and pushes"
    - "CHANGELOG.md contains categorized history from all existing tags (v0.1.0 through current)"
    - "GSD planning commits (docs(quick, docs(gsd) and bump commits are excluded from changelog"
    - "Tag annotations contain only the current version's notes (for GitHub Release workflow extraction)"
    - "release.sh runs tests and lint before calling bump-version.mjs"
  artifacts:
    - path: "taskflow/cliff.toml"
      provides: "git-cliff configuration for conventional commit parsing"
      contains: "commit_parsers"
    - path: "taskflow/scripts/bump-version.mjs"
      provides: "Unified bump + commit + tag + push workflow"
      min_lines: 80
    - path: "taskflow/CHANGELOG.md"
      provides: "Full project changelog generated by git-cliff"
      contains: "## ["
    - path: "taskflow/scripts/release.sh"
      provides: "Thin wrapper: tests + lint + bump"
      min_lines: 10
  key_links:
    - from: "taskflow/scripts/bump-version.mjs"
      to: "taskflow/cliff.toml"
      via: "npx git-cliff --config"
      pattern: "git-cliff.*--config.*cliff\\.toml"
    - from: "taskflow/scripts/release.sh"
      to: "taskflow/scripts/bump-version.mjs"
      via: "node scripts/bump-version.mjs"
      pattern: "node.*bump-version\\.mjs"
    - from: ".github/workflows/release.yml"
      to: "git tag annotation"
      via: "%(contents:body) extraction"
      pattern: "contents:body"
---

<objective>
Replace custom shell-based changelog generation with git-cliff and create a unified bump-version.mjs script that updates all version files, regenerates CHANGELOG.md, commits, tags with annotation, and pushes -- all in one command.

Purpose: Adopt the proven pmkar versioning workflow (git-cliff + bump script) so releases are consistent, changelogs are auto-generated, and the process is a single command.
Output: cliff.toml, bump-version.mjs, updated release.sh, generated CHANGELOG.md, removed generate-changelog.sh
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260329-k5y-implement-changelog-and-versioning-proce/260329-k5y-CONTEXT.md
@.planning/quick/260329-k5y-implement-changelog-and-versioning-proce/260329-k5y-RESEARCH.md

Reference implementations (read these during execution):
@~/Desktop/pmkar/scripts/bump-version.mjs
@~/Desktop/pmkar/cliff.toml

Existing scripts to replace/modify:
@taskflow/scripts/release.sh
@taskflow/scripts/generate-changelog.sh
@taskflow/scripts/inject-version.cjs (KEEP UNCHANGED)
@taskflow/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cliff.toml and generate initial CHANGELOG.md</name>
  <files>taskflow/cliff.toml, taskflow/CHANGELOG.md</files>
  <action>
    Create `taskflow/cliff.toml` adapted from pmkar's cliff.toml. Use the exact same config structure:
    - header = "" (empty)
    - body template: version header as `## [version]`, grouped commits with `### Group` headers, bullet list of first-line commit messages
    - trim = true
    - [git] section: conventional_commits = true, filter_unconventional = true, split_commits = false
    - commit_parsers: skip `^docs\\(quick`, `^docs\\(gsd`, `^chore: bump version`; group feat/fix/refactor/perf/test/docs/chore/ci same as pmkar
    - filter_commits = false, tag_pattern = "v[0-9]*", sort_commits = "newest"

    Then generate the initial CHANGELOG.md by running git-cliff from the REPO ROOT (important -- taskflow/ is NOT the git root):
    ```
    cd /Users/mimo/Desktop/Tasker && npx git-cliff@2.12.0 --config taskflow/cliff.toml -o taskflow/CHANGELOG.md
    ```

    Verify the output contains entries for existing tags (v1.6.1, v1.6.0, etc.) and that GSD docs commits are excluded.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker && test -f taskflow/cliff.toml && grep -q "commit_parsers" taskflow/cliff.toml && test -f taskflow/CHANGELOG.md && grep -q "## \[" taskflow/CHANGELOG.md && ! grep -q "docs(quick" taskflow/CHANGELOG.md && echo "PASS"</automated>
  </verify>
  <done>cliff.toml exists with correct config. CHANGELOG.md is generated with categorized history from all tags. No GSD planning commits appear in the changelog.</done>
</task>

<task type="auto">
  <name>Task 2: Create bump-version.mjs and update release.sh + package.json</name>
  <files>taskflow/scripts/bump-version.mjs, taskflow/scripts/release.sh, taskflow/package.json</files>
  <action>
    Create `taskflow/scripts/bump-version.mjs` based on pmkar's script with these key differences for Tasker:

    1. **REPO_ROOT detection**: The script lives in `taskflow/scripts/` but git root is one level above `taskflow/`. Use `const TASKFLOW_ROOT = resolve(__dirname, '..')` and `const REPO_ROOT = resolve(TASKFLOW_ROOT, '..')`. All git commands use `{ cwd: REPO_ROOT }`, all file paths relative to TASKFLOW_ROOT.

    2. **Version validation**: Same as pmkar -- `/^\d+\.\d+\.\d+$/` with clear error message.

    3. **Update 3 files** (same logic as pmkar's script, paths relative to TASKFLOW_ROOT):
       - `package.json`: JSON parse, set .version, JSON.stringify with 2-space indent + trailing newline
       - `src-tauri/tauri.conf.json`: JSON parse, set .version, same format
       - `src-tauri/Cargo.toml`: regex replace `version = "X.Y.Z"` in [package] section only (same careful approach as pmkar)

    4. **Regenerate CHANGELOG.md** (full history): `execSync('npx git-cliff@2.12.0 --config taskflow/cliff.toml --tag v${newVersion} -o taskflow/CHANGELOG.md', { cwd: REPO_ROOT, stdio: 'inherit' })`

    5. **Generate tag body** (just this version's notes): `execSync('npx git-cliff@2.12.0 --config taskflow/cliff.toml --tag v${newVersion} --unreleased --strip header', { cwd: REPO_ROOT })` -- capture stdout into a variable.

    6. **Git commit**: `git add -A && git commit -m "chore: bump version to ${newVersion}"` from REPO_ROOT.

    7. **Git tag with annotation**: Use the tag body from step 5. Format: subject line = `v${newVersion}`, blank line, then tag body. Pipe through `git tag -a v${newVersion} -F -`. Use the same printf pattern as current release.sh: `printf "%s\n\n%s\n" "v${newVersion}" "${tagBody}"`.

    8. **Git push**: `git push origin main && git push origin v${newVersion}` from REPO_ROOT.

    9. **Error handling**: If any step fails, exit with error. Use try/catch around each file update (same as pmkar). For git operations, let execSync throw naturally.

    IMPORTANT: The tag annotation format MUST match what `.github/workflows/release.yml` extracts via `git tag -l --format='%(contents:body)'`. The body must be the changelog content (no version header), so the `--strip header` flag is critical.

    Then update `taskflow/scripts/release.sh` to be a thin wrapper:
    - Accept version as `$1` (e.g., `1.7.0`, NOT `v1.7.0` -- bare semver)
    - Validate format: must match `^[0-9]+\.[0-9]+\.[0-9]+$`
    - Check for uncommitted changes (same as current)
    - Run tests: `npx vitest run`
    - Run lint+typecheck: `npm run check`
    - Call bump script: `node scripts/bump-version.mjs "$VERSION"`
    - Print success message

    Then update `taskflow/package.json` scripts:
    - Change `"release"` to: `"bash scripts/release.sh"`  (already correct, keep as-is)
    - Add `"bump"`: `"node scripts/bump-version.mjs"`

    Finally, delete `taskflow/scripts/generate-changelog.sh` -- it is fully replaced by git-cliff. Per discretion: no fallback needed, git-cliff failure should be loud and immediate.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && test -f scripts/bump-version.mjs && test -f scripts/release.sh && ! test -f scripts/generate-changelog.sh && node -c scripts/bump-version.mjs && grep -q '"bump"' package.json && grep -q 'bump-version.mjs' scripts/release.sh && echo "PASS"</automated>
  </verify>
  <done>bump-version.mjs exists with all 8 steps (validate, update 3 files, changelog, tag body, commit, tag, push). release.sh is a thin wrapper that runs tests+lint then calls bump. generate-changelog.sh is deleted. package.json has both "release" and "bump" scripts. All JS files parse without syntax errors.</done>
</task>

</tasks>

<verification>
1. `taskflow/cliff.toml` contains commit_parsers with skip rules for docs(quick, docs(gsd, and chore: bump version
2. `taskflow/CHANGELOG.md` has categorized entries from existing tags, no GSD commits
3. `taskflow/scripts/bump-version.mjs` parses cleanly (`node -c`) and contains all steps: file updates, git-cliff, commit, tag, push
4. `taskflow/scripts/release.sh` runs tests+lint then calls bump-version.mjs
5. `taskflow/scripts/generate-changelog.sh` is deleted
6. `taskflow/package.json` has "bump" and "release" scripts
7. Tag annotation format is compatible with `.github/workflows/release.yml` extraction (printf "%s\n\n%s\n" pattern)
</verification>

<success_criteria>
- git-cliff generates correct changelog from existing commit history
- bump-version.mjs is a complete, self-contained release automation script
- release.sh provides the safety net of tests+lint before bumping
- The release.yml CI workflow will still correctly extract tag annotations for GitHub Release notes
- Old generate-changelog.sh is removed
</success_criteria>

<output>
After completion, create `.planning/quick/260329-k5y-implement-changelog-and-versioning-proce/260329-k5y-SUMMARY.md`
</output>
