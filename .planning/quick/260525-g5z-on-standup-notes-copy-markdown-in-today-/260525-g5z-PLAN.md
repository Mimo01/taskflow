---
phase: quick-260525-g5z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/standup-notes/TodayColumn.tsx
  - taskflow/src/routes/standup-notes/TodayColumn.markdown.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Copy Markdown output describes participating MRs as a readable sentence, not 'Participating: !3961 mr-name'"
    - "Reviewing MRs nested under a story also read as a sentence"
    - "Standalone 'Participating MRs' section lines read naturally"
  artifacts:
    - path: "taskflow/src/routes/standup-notes/TodayColumn.tsx"
      provides: "generateTodayMarkdown with sentence-style MR lines"
      contains: "generateTodayMarkdown"
    - path: "taskflow/src/routes/standup-notes/TodayColumn.markdown.test.ts"
      provides: "Unit test asserting the new MR sentence format"
  key_links:
    - from: "taskflow/src/routes/standup-notes/StandupNotesPage.tsx"
      to: "generateTodayMarkdown"
      via: "import + handleCopyMarkdown call"
      pattern: "generateTodayMarkdown"
---

<objective>
Rewrite how participating (and reviewing) merge requests are rendered in the
Today section of the standup-notes Copy Markdown output so they read as natural
sentences instead of the current terse `Participating: !3961 mr-name`.

Purpose: The current output is not a well-formed sentence and reads awkwardly
when pasted into a standup channel.
Output: Updated `generateTodayMarkdown` MR line formatting + a focused unit test
locking the new format.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@taskflow/src/routes/standup-notes/TodayColumn.tsx
@taskflow/src/routes/standup-notes/StandupNotesPage.tsx

<interfaces>
<!-- Contracts the executor needs — already in the codebase. No exploration required. -->

The markdown generator lives in TodayColumn.tsx:

  export function generateTodayMarkdown(sources: TodayMarkdownSources, todayDate: string): string

Two locations emit MR lines today (the strings to change):

1. Nested under an In Progress story (line ~131) — uses NestedMr:
   `- ${mr.kind === 'review' ? 'Reviewing' : 'Participating'}: !${mr.iid} ${mr.title}`
   where NestedMr = { iid: number; title: string; kind: 'review' | 'participating' }

2. Standalone "Participating MRs" section (lines ~152-158) — uses ParticipatedMR:
   `- !${mr.mrIid}: ${mr.title}`
   where the unmatched participating items expose `mrIid: number` and `title: string`.

Note: nested uses `mr.iid`, standalone uses `mr.mrIid`. Do not swap these field names.

The standalone "MRs Awaiting Review" section (line ~144-149) uses `- !${mr.iid}: ${mr.title}`
on GitLabMR (`iid`, `title`) — leave its wording consistent if you touch nearby code,
but the user complaint is specifically about Participating, so changing it is optional.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite participating/reviewing MR lines as sentences</name>
  <files>taskflow/src/routes/standup-notes/TodayColumn.tsx, taskflow/src/routes/standup-notes/TodayColumn.markdown.test.ts</files>
  <behavior>
    - Given a nested participating MR { iid: 3961, title: 'Fix login redirect', kind: 'participating' } under a story, the markdown contains a line that reads as a sentence including both the MR number (!3961) and the title (e.g. `- Participated in MR !3961 (Fix login redirect)`).
    - Given a nested reviewing MR { kind: 'review' }, the line reads as a review sentence (e.g. `- Reviewed MR !3961 (<title>)`).
    - Given an unmatched participating MR { mrIid: 4012, title: 'Update docs' } in the standalone "Participating MRs" section, its line reads as the same sentence form using mrIid (e.g. `- Participated in MR !4012 (Update docs)`).
    - The literal old substring `Participating: !` never appears in the output.
  </behavior>
  <action>
    In `generateTodayMarkdown` (TodayColumn.tsx), update the two MR-line templates.

    For the nested loop (currently the line that pushes
    `${mr.kind === 'review' ? 'Reviewing' : 'Participating'}: !${mr.iid} ${mr.title}`):
    emit a sentence form. Use "Reviewed MR !{iid} ({title})" for kind 'review' and
    "Participated in MR !{iid} ({title})" for kind 'participating'. Keep the leading
    `- ` list-item dash. Use `mr.iid` here (NestedMr field).

    For the standalone "Participating MRs" section (currently
    `- !${mr.mrIid}: ${mr.title}`): emit "- Participated in MR !{mrIid} ({title})"
    using `mr.mrIid` (ParticipatedMR field). Keep the section header "### Participating MRs".

    Wrap the title in parentheses so it reads cleanly even when the title contains
    colons or punctuation. Do not change field names, query keys, the matching logic,
    or any other section. Keep the markdown structure (headers, blank lines) intact.

    Then create a focused unit test at TodayColumn.markdown.test.ts that imports
    `generateTodayMarkdown` and `todayQueryKeys` are NOT needed — only the generator.
    Build minimal `TodayMarkdownSources` fixtures that exercise: (a) a participating MR
    nested under an in-progress story, (b) a reviewing MR nested under a story, and
    (c) an unmatched participating MR in the standalone section. Assert the sentence
    strings above appear and that `Participating: !` does NOT appear. Use the existing
    JiraIssue / GitLabMR / ParticipatedMR shapes already imported by TodayColumn;
    mirror fixture construction from TodayColumn.test.tsx / mrMatching.test.ts if a
    shape detail is unclear.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/standup-notes/TodayColumn.markdown.test.ts</automated>
  </verify>
  <done>
    New test file passes; `generateTodayMarkdown` emits sentence-form MR lines for
    both nested (review + participating) and standalone participating MRs; the string
    `Participating: !` no longer appears anywhere in TodayColumn.tsx output.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Rewrote the participating/reviewing MR lines in the Today section of the
    standup-notes Copy Markdown output to read as sentences (e.g.
    "Participated in MR !3961 (mr-name)" / "Reviewed MR !3961 (mr-name)").
  </what-built>
  <how-to-verify>
    1. Run the app (`cd taskflow && npm run tauri dev`) and open the Standup Notes page.
    2. Ensure GitLab is connected so participating MRs appear in the Today column.
    3. Click "Copy Markdown" in the page header.
    4. Paste into a text editor and inspect the Today section.
    5. Confirm participating MRs read as a sentence (number + title), with no
       "Participating: !3961 mr-name" terse form remaining. Confirm reviewing MRs
       nested under a story also read naturally.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any wording you'd prefer.</resume-signal>
</task>

</tasks>

<verification>
- `cd taskflow && npx vitest run src/routes/standup-notes/TodayColumn.markdown.test.ts` passes.
- `grep -n "Participating: !" taskflow/src/routes/standup-notes/TodayColumn.tsx` returns nothing.
- Existing standup-notes tests still pass: `cd taskflow && npx vitest run src/routes/standup-notes`.
</verification>

<success_criteria>
- Participating and reviewing MR lines in the Today copy-markdown read as complete
  sentences containing both the MR number and title.
- The old `Participating: !{iid} {title}` form is gone.
- A unit test locks the new format; no other markdown sections or matching logic changed.
</success_criteria>

<output>
Create `.planning/quick/260525-g5z-on-standup-notes-copy-markdown-in-today-/260525-g5z-SUMMARY.md` when done.
</output>
