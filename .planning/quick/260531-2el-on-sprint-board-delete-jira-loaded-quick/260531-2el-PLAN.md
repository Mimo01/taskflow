---
phase: quick-260531-2el
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/jira/board-config.ts
  - taskflow/src/services/jira/board-config.test.ts
  - taskflow/src/services/jira/types.ts
  - taskflow/src/stores/filter.store.ts
  - taskflow/src/routes/dashboard/QuickFilterChipRow.tsx
  - taskflow/src/routes/dashboard/QuickFilterChipRow.test.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
autonomous: true
requirements: [QUICK-260531-2el]
must_haves:
  truths:
    - "The Sprint Board no longer fetches or renders Jira GreenHopper editmodel quick-filter chips."
    - "The Sprint Board label-chip row still renders and toggles app label filters."
    - "Epic / label / assignee / status filtering and the UnifiedFilterBar saved-preset QuickFilter still work."
    - "The project type-checks, lints (Biome 0 errors/0 warnings), and the test suite passes after removal."
  artifacts:
    - path: "taskflow/src/routes/dashboard/QuickFilterChipRow.tsx"
      provides: "Label-only chip row (Jira QF chips, divider, and JQL helpers removed)"
      contains: "toggleLabelFilter"
    - path: "taskflow/src/stores/filter.store.ts"
      provides: "Filter store without Jira-QF fields/actions; label-filter API intact"
      contains: "toggleLabelFilter"
    - path: "taskflow/src/services/jira/types.ts"
      provides: "Jira types without JiraBoardQuickFilter interface"
  key_links:
    - from: "taskflow/src/routes/dashboard/SprintBoardTab.tsx"
      to: "taskflow/src/routes/dashboard/QuickFilterChipRow.tsx"
      via: "QuickFilterChipRow render (label-only, no quickFilters prop)"
      pattern: "QuickFilterChipRow"
    - from: "taskflow/src/routes/dashboard/QuickFilterChipRow.tsx"
      to: "taskflow/src/stores/filter.store.ts"
      via: "useFilterStore label-filter selectors"
      pattern: "toggleLabelFilter"
---

<objective>
Surgically remove the Jira-loaded (GreenHopper editmodel) Sprint Board quick
filters and every piece of code that exists solely to support them, while keeping
the app's own filters fully working.

This reverts the feature added by commit 79efbb39 ("fix(board): load sprint board
quick filters from GreenHopper editmodel"). Scope and removal inventory are LOCKED
in CONTEXT.md — this plan executes that inventory, it does not re-discover it.

Purpose: The Jira editmodel quick filters are being retired; only the app's native
filters (label chips, epic/label/assignee/status, UnifiedFilterBar saved presets)
remain.

Output: Two deleted files, six trimmed source/test files, and a clean type-check /
lint / test run (Biome baseline: 0 errors / 0 warnings).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260531-2el-on-sprint-board-delete-jira-loaded-quick/260531-2el-CONTEXT.md
@.planning/STATE.md

Files being modified (read before editing — line numbers in CONTEXT.md are pre-edit/approximate):
@taskflow/src/services/jira/board-config.ts
@taskflow/src/services/jira/types.ts
@taskflow/src/stores/filter.store.ts
@taskflow/src/routes/dashboard/QuickFilterChipRow.tsx
@taskflow/src/routes/dashboard/SprintBoardTab.tsx
@taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
@taskflow/src/routes/dashboard/QuickFilterChipRow.test.tsx

Verified scope facts (confirmed during planning grep, 2026-05-31):
- board-config.ts is NOT re-exported from src/services/jira/index.ts; its only
  production importer is SprintBoardTab.tsx (line ~44) — safe to delete entirely.
- useQuickFilteredIssues (QuickFilterChipRow.tsx) has NO production consumer (only
  self-references) — delete it along with the JQL helpers.
- filter.store.test.ts has ZERO Jira-QF references already — do NOT touch it.
- settings.store.test.ts "quickfilter" hits are the app's saved-preset filters —
  KEEP, do NOT touch.
- Commands (from taskflow/package.json): `npm run check` = biome check + tsc
  --noEmit; `npm test` = vitest run. Run both from inside the taskflow/ directory.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the editmodel fetcher and its test; remove the JiraBoardQuickFilter type</name>
  <files>taskflow/src/services/jira/board-config.ts (delete), taskflow/src/services/jira/board-config.test.ts (delete), taskflow/src/services/jira/types.ts</files>
  <action>
    Delete both taskflow/src/services/jira/board-config.ts and
    taskflow/src/services/jira/board-config.test.ts entirely (prefer `git rm`).
    These are sole-purpose files whose only production consumer is SprintBoardTab.tsx
    (rewired in Task 3).

    In taskflow/src/services/jira/types.ts, remove the JiraBoardQuickFilter
    interface — the "// --- Phase 33 additions: Board quick filters ---" comment
    line plus the `export interface JiraBoardQuickFilter { ... }` body. Leave the
    surrounding ParsedDuration and JiraSavedFilter types untouched.

    Do NOT remove anything from src/services/jira/index.ts (board-config is not
    re-exported there). Do NOT touch the app-native QuickFilter type in
    filter.store.ts / settings.store.ts (different concept — KEEP per CONTEXT.md).
  </action>
  <verify>
    <automated>cd taskflow && test ! -e src/services/jira/board-config.ts && test ! -e src/services/jira/board-config.test.ts && [ "$(grep -c JiraBoardQuickFilter src/services/jira/types.ts)" = "0" ]</automated>
  </verify>
  <done>board-config.ts and board-config.test.ts no longer exist; JiraBoardQuickFilter is gone from types.ts. Residual references in SprintBoardTab/QuickFilterChipRow are removed in Tasks 2-3; the cross-file zero-reference check is in Task 3 verify.</done>
</task>

<task type="auto">
  <name>Task 2: Strip Jira-QF state from filter.store.ts and Jira chips/JQL from QuickFilterChipRow</name>
  <files>taskflow/src/stores/filter.store.ts, taskflow/src/routes/dashboard/QuickFilterChipRow.tsx, taskflow/src/routes/dashboard/QuickFilterChipRow.test.tsx</files>
  <action>
    filter.store.ts — remove ONLY the Jira-QF members from the FilterState
    interface, the store initializer, and clearAll: the
    `activeJiraQuickFilters: Set<number>` field (interface + initializer + the
    `activeJiraQuickFilters: new Set<number>()` line inside clearAll),
    `toggleJiraQuickFilter` (signature + implementation), and `clearJiraQuickFilters`
    (signature + implementation). KEEP activeLabelFilters, toggleLabelFilter,
    clearLabelFilters, the QuickFilter interface, applyQuickFilter, and all
    epic/label/assignee/status members.

    QuickFilterChipRow.tsx — strip the Jira parts, keep it a working label-only row:
    remove the `import type { JiraBoardQuickFilter }` line; delete the JQL helpers
    parseSimpleJql, evaluateCondition, evaluateQuickFilter and the entire
    useQuickFilteredIssues hook (no production consumer — confirmed); drop the
    now-unused JiraIssue import if nothing else uses it after the hook is gone.
    In QuickFilterChipRowProps, remove the quickFilters and issues props (issues
    is unused once the hook is gone) so the component accepts only `{ labels: string[] }`.
    Remove activeJiraQuickFilters and toggleJiraQuickFilter from the useFilterStore()
    destructure (keep activeLabelFilters, toggleLabelFilter). Delete the Jira chip
    .map() block and the QF/label divider (hasQFs logic); compute totalChips from
    labels.length only and make the early-return guard `if (labels.length === 0) return null;`.
    Re-index the label chips so they are the FIRST chips: drop the `quickFilters.length + j`
    offset — use j directly for chipRefs/tabIndex so the first label chip gets
    tabIndex 0 and arrow-key navigation works. Update the file's top doc-comment to
    describe label-only behavior (no Jira QFs).

    QuickFilterChipRow.test.tsx — this is an it.todo stub. Remove the Jira-QF todo
    lines ("renders Jira quick filter chips", "shows divider between QF and label
    sections when both exist"). Keep the label-chip and a11y todo lines and a valid
    describe block.

    Do NOT touch filter.store.test.ts (already zero Jira-QF refs).
  </action>
  <verify>
    <automated>cd taskflow && [ "$(grep -hE 'activeJiraQuickFilters|toggleJiraQuickFilter|clearJiraQuickFilters|useQuickFilteredIssues|parseSimpleJql|evaluateQuickFilter|evaluateCondition' src/stores/filter.store.ts src/routes/dashboard/QuickFilterChipRow.tsx | grep -vE '^[[:space:]]*(//|\*)' | wc -l | tr -d ' ')" = "0" ] && npx vitest run src/routes/dashboard/QuickFilterChipRow.test.tsx src/stores/filter.store.test.ts</automated>
  </verify>
  <done>filter.store.ts exposes only label/epic/assignee/status filter API; QuickFilterChipRow renders a label-only chip row (first chip focusable) with no Jira QF code or JQL helpers; the two named test files pass.</done>
</task>

<task type="auto">
  <name>Task 3: Remove all Jira-QF wiring from SprintBoardTab, fix its tests, and verify clean check/test</name>
  <files>taskflow/src/routes/dashboard/SprintBoardTab.tsx, taskflow/src/routes/dashboard/SprintBoardTab.test.tsx</files>
  <action>
    SprintBoardTab.tsx — remove all Jira-QF wiring while leaving label/epic/assignee/
    status filters and the (now label-only) QuickFilterChipRow render intact:
    - Remove `import { fetchBoardQuickFilters } from '@/services/jira/board-config'`
      (line ~44) and `import type { JiraBoardQuickFilter }` (line ~48). Keep the
      QuickFilterChipRow import (line ~54).
    - Remove the boardQuickFilters useQuery block (lines ~712-717, queryKey
      ['jira-board-quickfilters', boardId]) and its invalidateQueries block
      (lines ~798-801, same queryKey). Leave the jira-statuses and jira-active-sprint
      invalidations.
    - Remove `activeJiraQuickFilters` from the useFilterStore() destructure (~981);
      keep activeEpics/activeLabels/activeAssignees/activeStatuses/activeLabelFilters.
    - Remove the qfMatch block (~1056-1068) and drop qfMatch from the applyFilters
      return (~1077) so it returns
      `epicMatch && labelMatch && assigneeMatch && statusMatch && labelChipMatch`.
    - Delete the local parseSimpleJql (~1081-1086) and evaluateQfCondition
      (~1088-1115) functions (used only by qfMatch).
    - Remove the `activeJiraQuickFilters.size > 0 ||` line from the local-filters
      guard (~1137); keep the other size checks.
    - On the QuickFilterChipRow render (~1298-1302), remove the
      `quickFilters={boardQuickFilters ?? []}` and `issues={localIssues}` props,
      leaving `<QuickFilterChipRow labels={filterOptions.labels} />`.

    SprintBoardTab.test.tsx — remove the now-dangling Jira-QF mock and assertion:
    delete the `vi.mock('@/services/jira/board-config', ...)` block (lines ~99-101;
    also the "Sprints + board-config still hit REST" comment if it now only refers
    to sprints), and remove the `['jira-board-quickfilters', 163]` invalidation
    assertion (lines ~560-562). Adjust the nearby "Five invalidations" / "three
    legacy keys" comments to match the reduced count. Keep the sprint and
    gh-all-data / gh-transitions / jira-statuses assertions.

    Discretion: if removing these props/imports leaves any variable unused (e.g.
    localIssues only fed the removed prop — verify it is still used elsewhere before
    touching it), resolve to keep tsc/biome clean without changing unrelated behavior.
  </action>
  <verify>
    <automated>cd taskflow && [ "$(grep -rE 'activeJiraQuickFilters|fetchBoardQuickFilters|JiraBoardQuickFilter|jira-board-quickfilters|evaluateQfCondition' src --include=*.ts --include=*.tsx | wc -l | tr -d ' ')" = "0" ] && npm run check && npm test</automated>
  </verify>
  <done>SprintBoardTab has no Jira-QF imports, query, store field, qfMatch, JQL helpers, or quickFilters prop; its tests pass; repo-wide grep for the Jira-QF identifiers returns zero; `npm run check` passes (tsc + Biome 0/0) and `npm test` is green.</done>
</task>

</tasks>

<verification>
After all tasks, from inside the taskflow/ directory:
- `npm run check` passes — tsc --noEmit clean AND Biome reports 0 errors / 0 warnings (preserving the baseline).
- `npm test` (vitest run) is fully green.
- Repo-wide grep across src/**/*.ts,tsx for `activeJiraQuickFilters`, `fetchBoardQuickFilters`, `JiraBoardQuickFilter`, `jira-board-quickfilters`, `evaluateQfCondition`, `useQuickFilteredIssues` returns zero matches.
- Files taskflow/src/services/jira/board-config.ts and board-config.test.ts do not exist.
- KEPT and still present: QuickFilterChipRow component + render, activeLabelFilters/toggleLabelFilter/clearLabelFilters, the QuickFilter saved-preset type, epic/label/assignee/status filtering, settings.store.test.ts untouched.
</verification>

<success_criteria>
- Jira-loaded (editmodel) quick filters and their sole-purpose support code are fully removed.
- App-native filters (label chips, epic/label/assignee/status, UnifiedFilterBar saved presets) remain fully functional.
- Project type-checks, lints clean (Biome 0/0), and the test suite passes.
- No scope creep: the shared QuickFilterChipRow component and the app's QuickFilter type are preserved.
</success_criteria>

<output>
Create `.planning/quick/260531-2el-on-sprint-board-delete-jira-loaded-quick/260531-2el-SUMMARY.md` when done.
</output>
