---
phase: quick-260601-1ou
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx
  - taskflow/src/routes/dashboard/create-edit-issue/useIssueMutations.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "After a status transition, sprint board columns refresh (GH all-data invalidated)"
    - "No dead invalidateQueries(['jira-sprint-stories']) calls remain in either file"
    - "No dead invalidateQueries(['jira-issues','sprint-board']) calls remain in either file"
  artifacts:
    - path: taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx
      provides: "transitionMutation.onSettled calls invalidateGhAllData"
      contains: "invalidateGhAllData"
    - path: taskflow/src/routes/dashboard/create-edit-issue/useIssueMutations.ts
      provides: "Dead legacy invalidation calls removed"
  key_links:
    - from: "FieldsSection.tsx"
      to: "invalidateGhAllData (jira.ts re-export)"
      via: "import { invalidateGhAllData } from '@/services/jira'"
      pattern: "invalidateGhAllData"
---

<objective>
Fix FieldsSection.transitionMutation.onSettled to also call invalidateGhAllData(queryClient, boardId) after a status transition, mirroring the existing invalidateGhBacklogData call in the same handler. Remove dead invalidateQueries(['jira-sprint-stories']) and invalidateQueries(['jira-issues','sprint-board']) calls from FieldsSection and useIssueMutations — those query keys have no producers since the Phase 73/74 GreenHopper cutover.

Purpose: Sprint board columns do not refresh after a status change because the GH all-data query is not invalidated; meanwhile the dead query keys add noise and mislead future readers.
Output: Two modified files, biome-clean, tests pass.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mimo/Documents/Projects/taskflow/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix transitionMutation.onSettled in FieldsSection and remove dead keys from both files</name>
  <files>
    taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx,
    taskflow/src/routes/dashboard/create-edit-issue/useIssueMutations.ts
  </files>
  <action>
FieldsSection.tsx — three changes:

1. Import: add `invalidateGhAllData` to the existing import from `@/services/jira` (line 31 currently imports `invalidateGhBacklogData, isIssueFlagged`). Result: `import { invalidateGhAllData, invalidateGhBacklogData, isIssueFlagged } from '@/services/jira';`

2. transitionMutation.onSettled (currently lines ~260-270): remove the two dead lines:
   - `queryClient.invalidateQueries({ queryKey: ['jira-issues', 'sprint-board'] });`
   - `queryClient.invalidateQueries({ queryKey: ['jira-sprint-stories'] });`
   Then add `invalidateGhAllData` call immediately after (or instead of, if that branch already covers it) the existing `invalidateGhBacklogData` call. The new block should read:
   ```
   if (boardId) {
     invalidateGhAllData(queryClient, boardId);
   } else {
     invalidateGhAllData(queryClient);
   }
   ```
   replacing / sitting alongside the current `if (boardId) invalidateGhBacklogData(queryClient, boardId); else invalidateGhBacklogData(queryClient);` block. Keep the `invalidateGhBacklogData` call only if it covers a distinct query key not covered by `invalidateGhAllData`; remove it if `invalidateGhAllData` is a strict superset (check useGhAllData.ts — it invalidates `['gh-all']` while `invalidateGhBacklogData` invalidates `['gh-backlog']`; they are distinct, so KEEP both).

   Final onSettled for transitionMutation should contain:
   - `queryClient.invalidateQueries({ queryKey: ['jira-issue-detail', issueKey, jiraBaseUrl] })`
   - `queryClient.invalidateQueries({ queryKey: ['jira-issue-changelog', issueKey, jiraBaseUrl] })`
   - `if (boardId) invalidateGhAllData(queryClient, boardId); else invalidateGhAllData(queryClient);`
   - `if (boardId) invalidateGhBacklogData(queryClient, boardId); else invalidateGhBacklogData(queryClient);`
   - `queryClient.invalidateQueries({ queryKey: ['jira-epics-basic'] })`
   - `queryClient.invalidateQueries({ queryKey: ['jira-fixversion-issues'] })`
   - `queryClient.invalidateQueries({ queryKey: ['jira-version-counts'] })`
   - `queryClient.invalidateQueries({ queryKey: ['jira-transitions', issueKey] })`

3. sprintMutation.onSettled (lines ~288-293): also remove the dead `['jira-sprint-stories']` line from there if present. Leave `invalidateGhBacklogData` in that handler (it's correct scope for sprint moves).

useIssueMutations.ts — two changes:

1. createMutation.onSuccess (line ~119): remove `queryClient.invalidateQueries({ queryKey: ['jira-issues', 'sprint-board'] });`

2. editMutation.onSuccess (lines ~191-192): remove both:
   - `queryClient.invalidateQueries({ queryKey: ['jira-issues', 'sprint-board'] });`
   - `queryClient.invalidateQueries({ queryKey: ['jira-sprint-stories'] });`

Do NOT remove `invalidateGhBacklogData(queryClient)` from editMutation.onSuccess — that call is still valid.

Also remove the now-unused `invalidateGhBacklogData` import from useIssueMutations.ts if and only if no remaining call uses it after the cleanup (check: editMutation still calls `invalidateGhBacklogData(queryClient)` so the import stays).
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npm run check 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `grep -n "jira-sprint-stories\|jira-issues.*sprint-board" taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx` returns 0 lines
    - `grep -n "jira-sprint-stories\|jira-issues.*sprint-board" taskflow/src/routes/dashboard/create-edit-issue/useIssueMutations.ts` returns 0 lines
    - `grep -n "invalidateGhAllData" taskflow/src/routes/dashboard/issue-detail/FieldsSection.tsx` shows at least one call in transitionMutation.onSettled
    - `npm run check` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client query cache | Internal React Query cache manipulation — no external trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1ou-01 | Tampering | queryClient.invalidateQueries | accept | Internal cache-only; no user data written, no network calls introduced |
</threat_model>

<verification>
- No `['jira-sprint-stories']` query keys remain in either modified file
- No `['jira-issues', 'sprint-board']` query keys remain in either modified file
- `invalidateGhAllData` is called in transitionMutation.onSettled with boardId when available
- `npm run check` (biome + tsc) exits 0
</verification>

<success_criteria>
After a status transition in the issue detail panel, the sprint board refreshes because the GH all-data cache is invalidated. Dead query keys are gone from both files. Biome and TypeScript are clean.
</success_criteria>

<output>
Create `.planning/quick/260601-1ou-fix-fieldssection-transitionmutation-ons/260601-1ou-SUMMARY.md` when done.
</output>
