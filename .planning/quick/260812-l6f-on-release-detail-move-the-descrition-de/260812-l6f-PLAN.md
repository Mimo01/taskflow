---
phase: quick-260812-l6f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
  - taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx
  - taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx
autonomous: true
requirements: [QUICK-L6F-01]

must_haves:
  truths:
    - "On the release detail page, the release description(s) render inside the right sidebar, not in the main content column"
    - "The Jira/GitLab description split (Jira Description + GitLab Description headings, or the collapsed single 'Description' block when a milestone is matched and both are empty) behaves exactly as before"
    - "The main content column still shows title heading, label summary, unified task table, and the Pin / Open in Jira actions in the same order"
    - "All existing sidebar rows (Status, Release Date, GitLab Milestone, Release Branch, Merged back, MR Labels, MRs, Issues, Story points) and the Edit button are unchanged"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx"
      provides: "Sidebar rendering DescriptionsSection using its existing version + matchedMilestone + gitlabMatch props"
      contains: "DescriptionsSection"
    - path: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      provides: "Left column without the DescriptionsSection block or its import"
  key_links:
    - from: "taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx"
      to: "./DescriptionsSection"
      via: "import + render inside sidebar container"
      pattern: "DescriptionsSection"
---

<objective>
Relocate the release description block on the Release Detail page from the main (left) content column into the right sidebar. Pure relocation — no behavior, data, or copy changes.

Purpose: The descriptions are reference metadata, not primary content; the main column should be dominated by the unified task table.
Output: `DescriptionsSection` rendered by `ReleaseDetailSidebar` instead of `ReleaseDetailPage`, plus a regression test locking the new location.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
@taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx
@taskflow/src/routes/dashboard/release-detail/DescriptionsSection.tsx
@taskflow/src/routes/dashboard/release-detail/MetaRow.tsx
@taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx

Key facts already established (do not re-derive):
- `DescriptionsSection` props: `gitlabMatchType: ReleaseMatch['type']`, `matchedMilestone: GitLabMilestone | null`, `versionDescription: string | null | undefined`. Its file needs NO changes.
- `ReleaseDetailSidebar` ALREADY receives every input the section needs: `gitlabMatch` (use `gitlabMatch.type`), `matchedMilestone` (currently destructured as the unused `matchedMilestone: _matchedMilestone`), and `version` (use `version.description`). No new props are required, and `ReleaseDetailSidebarProps` does not change.
- The sidebar body is a `<div className="space-y-4 text-sm">` containing the `Details` header row and the `MetaRow` stack; `MetaRow` is a fixed 28-unit label column, so descriptions must NOT be wrapped in `MetaRow` — they render as full-width sections.
- In `ReleaseDetailPage.tsx` the block to remove is the `<DescriptionsSection .../>` element (with its comment) between `<ReleaseTitleHeading />` and `<LabelSummarySection />`, plus the now-unused import on line 20.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render DescriptionsSection in the sidebar and remove it from the main column</name>
  <files>taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx, taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <action>
In `ReleaseDetailSidebar.tsx`: add `import { DescriptionsSection } from './DescriptionsSection';` alongside the existing sibling imports, and rename the destructured `matchedMilestone: _matchedMilestone` back to plain `matchedMilestone` so the value is consumed. Inside the `<div className="space-y-4 text-sm">` body, after the final `hasStoryPoints` MetaRow block (i.e. as the last child of that container), render a wrapper element carrying `data-testid="sidebar-descriptions"` with class `space-y-4 border-t pt-4` containing `<DescriptionsSection gitlabMatchType={gitlabMatch.type} matchedMilestone={matchedMilestone} versionDescription={version.description} />`. The wrapper supplies the separator and inter-section spacing that `space-y-6` gave it in the old left-column position; the sidebar is min-240px wide, so add no fixed widths of your own — `DescriptionsSection` already wraps text and its GitLab markdown block already uses `max-w-none` prose.

In `ReleaseDetailPage.tsx`: delete the `<DescriptionsSection ... />` element together with its preceding `{/* Description(s) — ... */}` comment, and delete the now-unused `DescriptionsSection` import. Change nothing else — `ReleaseTitleHeading`, `LabelSummarySection`, `UnifiedTaskTable`, the action buttons, the `<ReleaseDetailSidebar>` prop list, and all dialogs stay byte-identical.
  </action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit && npx biome check ./src/routes/dashboard/ReleaseDetailPage.tsx ./src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx && grep -c "DescriptionsSection" src/routes/dashboard/ReleaseDetailPage.tsx; test "$(grep -c 'DescriptionsSection' src/routes/dashboard/ReleaseDetailPage.tsx)" = "0" && grep -q 'DescriptionsSection' src/routes/dashboard/release-detail/ReleaseDetailSidebar.tsx</automated>
  </verify>
  <done>`tsc --noEmit` and `biome check` pass on both files; `DescriptionsSection` no longer appears anywhere in `ReleaseDetailPage.tsx` and does appear in `ReleaseDetailSidebar.tsx`; no `_matchedMilestone` identifier remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the new location with sidebar tests</name>
  <files>taskflow/src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx</files>
  <behavior>
    - With `gitlabMatch` = the existing `noneMatch` and a version whose `description` is set, the sidebar renders a `Description` heading and the description text.
    - With no version description and `noneMatch`, the sidebar renders `Description` and the `No description` italic copy.
    - With an exact `gitlabMatch` plus a `matchedMilestone` that has a `description`, the sidebar renders both `Jira Description` and `GitLab Description` headings.
    - The descriptions render inside the `sidebar-descriptions` testid container (assert via `within(screen.getByTestId('sidebar-descriptions'))`).
  </behavior>
  <action>
Append a new `describe('ReleaseDetailSidebar — descriptions', ...)` block to the existing test file, reusing the file's `renderSidebar` helper, `makeVersion` factory and `noneMatch` constant. Pass `branchState: { kind: 'blocked-no-milestone' }` in each case since `renderSidebar` requires it and it is irrelevant here. For the two-heading case, override `gitlabMatch` with an exact `ReleaseMatch` (`{ type: 'exact', candidateName: '33.5.0', candidateUrl: 'https://gitlab.example/milestones/1' }`) and `matchedMilestone` with a minimal object satisfying `GitLabMilestone` — mirror the shape from `@/services/gitlab`, casting only if a full literal is impractical. Scope every assertion through `within(screen.getByTestId('sidebar-descriptions'))` so the test fails if the block moves back to the main column. Do not modify the existing describe blocks or the shared helpers' defaults.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx && npx biome check ./src/routes/dashboard/release-detail/ReleaseDetailSidebar.test.tsx</automated>
  </verify>
  <done>New description tests pass, all pre-existing tests in the file still pass, and biome reports no new diagnostics.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The release description(s) now render at the bottom of the Release Detail right sidebar, below Story points, separated by a top border. The main column starts at the title and goes straight into the label summary and unified task table.</what-built>
  <how-to-verify>
    1. Run `cd taskflow && npm run tauri dev` (or the usual dev command) and open any release detail page via Releases → a release.
    2. Confirm the description text appears in the RIGHT sidebar, below Story points, and no longer in the main column.
    3. Open a release that has a matched GitLab milestone: confirm both `Jira Description` and `GitLab Description` appear in the sidebar, with GitLab markdown (lists, links) still rendered.
    4. Open a release with a matched milestone where neither side has text: confirm a single `Description` / `No description` block, not two.
    5. Drag the sidebar resize handle to its narrowest (240px) and widest: confirm the description text wraps and does not overflow or clip.
    6. Confirm filters, the unified task table, Pin, Open in Jira, and the Edit modal all behave as before.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitLab API → renderer | Milestone `description` markdown is remote-controlled text rendered via ReactMarkdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l6f-01 | Tampering | `DescriptionsSection` markdown rendering | accept | Pure relocation — the component and its ReactMarkdown config are unchanged; no `rehype-raw`, so raw HTML stays inert exactly as today |
| T-l6f-SC | Tampering | package installs | accept | No new dependencies are added by this plan |
</threat_model>

<verification>
- `cd taskflow && npx tsc --noEmit` passes
- `cd taskflow && npx vitest run src/routes/dashboard/release-detail/` passes
- `cd taskflow && npx biome check ./src/routes/dashboard` reports no NEW flagged files versus the pre-change baseline
</verification>

<success_criteria>
- Descriptions render only in the sidebar; zero references to `DescriptionsSection` remain in `ReleaseDetailPage.tsx`
- `DescriptionsSection.tsx` is unmodified
- `ReleaseDetailSidebarProps` gained no new fields
- All three description display modes (single, Jira-only, Jira+GitLab) behave identically to before
- Human verification approved
</success_criteria>

<output>
Create `.planning/quick/260812-l6f-on-release-detail-move-the-descrition-de/260812-l6f-SUMMARY.md` when done
</output>
