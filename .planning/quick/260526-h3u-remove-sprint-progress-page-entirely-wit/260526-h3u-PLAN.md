---
phase: quick-260526-h3u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/SprintProgressTab.tsx
  - taskflow/src/routes/dashboard/SprintProgressSkeleton.tsx
  - taskflow/src/routes/dashboard/SprintHealthPanel.tsx
  - taskflow/src/routes/dashboard/SprintProgressTab.test.tsx
  - taskflow/src/routes/dashboard/SprintHealthPanel.test.tsx
  - taskflow/src/routes/routes.tsx
  - taskflow/src/components/app/sidebar-items.ts
  - taskflow/src/main.tsx
  - taskflow/src/routes/dashboard/WikiRenderer.tsx
  - taskflow/src/routes/dashboard/DiscussionThreads.tsx
  - taskflow/src/routes/dashboard/DashboardSprintCard.tsx
  - taskflow/src/routes/dashboard/DashboardInProgressCard.tsx
  - taskflow/src/routes/worklogs/WorklogsPage.tsx
  - taskflow/src/components/app/Sidebar.test.tsx
  - taskflow/src/routes/settings/Settings.test.tsx
  - taskflow/src/routes/standup-notes/TodayColumn.test.tsx
autonomous: true
requirements: [QUICK-260526-h3u]

must_haves:
  truths:
    - "The Sprint Progress page no longer exists — navigating to /sprint-progress resolves to no route"
    - "No 'Sprint Progress' item appears in the sidebar navigation"
    - "The app builds cleanly with no dangling imports or references to SprintProgressTab/SprintProgressSkeleton/SprintHealthPanel"
    - "The full test suite passes with no references to the deleted components"
  artifacts:
    - path: "taskflow/src/routes/routes.tsx"
      provides: "Route table with /sprint-progress route and lazy import removed"
      contains_not: "SprintProgressTab"
    - path: "taskflow/src/components/app/sidebar-items.ts"
      provides: "Sidebar nav definitions without the sprint-progress entry"
      contains_not: "sprint-progress"
  key_links:
    - from: "taskflow/src/routes/routes.tsx"
      to: "(removed) SprintProgressTab"
      via: "lazy import + route entry deleted"
      pattern: "SprintProgressTab"
    - from: "taskflow/src/components/app/sidebar-items.ts"
      to: "(removed) /sprint-progress nav item"
      via: "SIDEBAR_NAV_ITEMS entry deleted"
      pattern: "sprint-progress"
---

<objective>
Remove the Sprint Progress page and the orphan SprintHealthPanel component from TaskFlow entirely — no replacement UI, no redirect. Delete five files and clean every dangling reference (route, sidebar, breadcrumb labels, JSDoc comments, and test fixtures) across the codebase.

Purpose: The Sprint Progress page is being retired. Leaving it in place is dead weight; partial removal would leave broken imports or phantom nav entries.
Output: Five deleted files; eleven edited files with all SprintProgress/SprintHealthPanel/`/sprint-progress` references removed. App builds and tests pass clean.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260526-h3u-remove-sprint-progress-page-entirely-wit/260526-h3u-CONTEXT.md
@.planning/quick/260526-h3u-remove-sprint-progress-page-entirely-wit/260526-h3u-RESEARCH.md

# Project convention (from STATE.md): verify cleanup with `npm run build`, NOT just tsc — CSS imports fail silently in TypeScript-only checks.

<interfaces>
<!-- Verified by grep of full taskflow/src/ on 2026-05-26. Executor needs no codebase exploration. -->

DELETE these 5 files outright (no edits — just remove):
  - taskflow/src/routes/dashboard/SprintProgressTab.tsx       (the page being removed; default export only)
  - taskflow/src/routes/dashboard/SprintProgressSkeleton.tsx  (used only by SprintProgressTab; exports SprintProgressSkeleton fn)
  - taskflow/src/routes/dashboard/SprintHealthPanel.tsx       (orphan — never rendered in production; exports SprintHealthPanelProps + default fn)
  - taskflow/src/routes/dashboard/SprintProgressTab.test.tsx
  - taskflow/src/routes/dashboard/SprintHealthPanel.test.tsx

VERIFIED: No exported type from the deleted files (SprintHealthPanelProps, SprintProgressSkeleton) is imported anywhere outside these five files. No barrel index.ts re-exports. No keyboard-shortcut or command-palette binding targets /sprint-progress.

EDIT these files — remove the listed reference(s):
  - taskflow/src/routes/routes.tsx
        L16: const SprintProgressTab = lazy(() => import('./dashboard/SprintProgressTab'));  → delete line
        L44: { path: '/sprint-progress', element: withLazy(SprintProgressTab) },            → delete line
  - taskflow/src/components/app/sidebar-items.ts
        The `sprint-progress` object in SIDEBAR_NAV_ITEMS (id/label/path/iconName/section, ~lines 78-83 incl. braces & trailing comma) → delete the whole object
  - taskflow/src/main.tsx
        L291: if (pathname.startsWith('/sprint-progress')) return 'Sprint Progress';  → delete line
  - taskflow/src/routes/dashboard/WikiRenderer.tsx
        L877: '/sprint-progress': 'Sprint Progress',  (staticLabels map entry) → delete line
  - taskflow/src/routes/dashboard/DiscussionThreads.tsx
        L59: '/sprint-progress': 'Sprint Progress',  (staticLabels map entry) → delete line
  - taskflow/src/routes/dashboard/DashboardSprintCard.tsx
        L11 JSDoc: "...cache keys with SprintBoardTab/SprintProgressTab/SprintHealthPanel." → change to "...cache keys with SprintBoardTab."
  - taskflow/src/routes/dashboard/DashboardInProgressCard.tsx
        L13 comment: "DashboardSprintCard, and SprintHealthPanel — no extra API call when warm." → "DashboardSprintCard — no extra API call when warm."
        L42 comment: "// CACHE KEY MUST MATCH DashboardSprintCard / SprintHealthPanel / SprintBoardTab exactly" → "// CACHE KEY MUST MATCH DashboardSprintCard / SprintBoardTab exactly"
  - taskflow/src/routes/worklogs/WorklogsPage.tsx
        L315 comment: "// ─ Auth token effect (SprintProgressTab pattern) ─..." → "// ─ Auth token effect ─..." (drop the SprintProgressTab parenthetical)
  - taskflow/src/components/app/Sidebar.test.tsx
        L78 fixture: { id: 'sprint-progress', visible: true },  → delete line (mock sidebarItems array)
  - taskflow/src/routes/settings/Settings.test.tsx
        L128 fixture: { id: 'sprint-progress', visible: false },  → delete line (mock sidebarItems array)
  - taskflow/src/routes/standup-notes/TodayColumn.test.tsx
        L13 comment: "* Pattern source: SprintHealthPanel.test.tsx + YesterdayColumn.tempo-disabled.test.tsx" → trim "SprintHealthPanel.test.tsx + " so it reads "* Pattern source: YesterdayColumn.tempo-disabled.test.tsx"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the five Sprint Progress / Sprint Health files</name>
  <files>
    taskflow/src/routes/dashboard/SprintProgressTab.tsx,
    taskflow/src/routes/dashboard/SprintProgressSkeleton.tsx,
    taskflow/src/routes/dashboard/SprintHealthPanel.tsx,
    taskflow/src/routes/dashboard/SprintProgressTab.test.tsx,
    taskflow/src/routes/dashboard/SprintHealthPanel.test.tsx
  </files>
  <action>Delete all five files listed in `<files>` using `rm`. These are the page component, its skeleton, the orphan SprintHealthPanel, and the two associated test files (per locked CONTEXT decisions: full delete + delete orphan SprintHealthPanel). No edits to these files — remove them. Do NOT delete any other Sprint* file (SprintBoardTab and DashboardSprintCard remain — they are unrelated and still in use).</action>
  <verify>
    <automated>cd taskflow && test ! -e src/routes/dashboard/SprintProgressTab.tsx && test ! -e src/routes/dashboard/SprintProgressSkeleton.tsx && test ! -e src/routes/dashboard/SprintHealthPanel.tsx && test ! -e src/routes/dashboard/SprintProgressTab.test.tsx && test ! -e src/routes/dashboard/SprintHealthPanel.test.tsx && echo DELETED_OK</automated>
  </verify>
  <done>All five files no longer exist on disk; SprintBoardTab.tsx and DashboardSprintCard.tsx are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Remove all route, sidebar, and breadcrumb references</name>
  <files>
    taskflow/src/routes/routes.tsx,
    taskflow/src/components/app/sidebar-items.ts,
    taskflow/src/main.tsx,
    taskflow/src/routes/dashboard/WikiRenderer.tsx,
    taskflow/src/routes/dashboard/DiscussionThreads.tsx
  </files>
  <action>Remove the live code references to SprintProgress / `/sprint-progress` per the `<interfaces>` block: in routes.tsx delete both the lazy import line and the `/sprint-progress` route entry; in sidebar-items.ts delete the entire `sprint-progress` object from SIDEBAR_NAV_ITEMS (including its braces and trailing comma); in main.tsx delete the breadcrumb `if (pathname.startsWith('/sprint-progress'))` line; in WikiRenderer.tsx and DiscussionThreads.tsx delete the `'/sprint-progress': 'Sprint Progress',` entry from each `staticLabels` map. Do NOT add a redirect — the route should simply cease to exist (per CONTEXT: no replacement, no redirect). Note: no settings-store migration is needed — the sidebar render filters stored items against SIDEBAR_NAV_ITEMS by id, so any persisted `sprint-progress` entry is silently ignored.</action>
  <verify>
    <automated>cd taskflow && (grep -rn "sprint-progress\|SprintProgressTab" src/routes/routes.tsx src/components/app/sidebar-items.ts src/main.tsx src/routes/dashboard/WikiRenderer.tsx src/routes/dashboard/DiscussionThreads.tsx | grep -v '^#' && echo FOUND_REFS && exit 1) || echo NO_REFS_OK</automated>
  </verify>
  <done>None of the five files contain `sprint-progress` or `SprintProgressTab`; no redirect was added.</done>
</task>

<task type="auto">
  <name>Task 3: Clean comment/test references, then build and test clean</name>
  <files>
    taskflow/src/routes/dashboard/DashboardSprintCard.tsx,
    taskflow/src/routes/dashboard/DashboardInProgressCard.tsx,
    taskflow/src/routes/worklogs/WorklogsPage.tsx,
    taskflow/src/components/app/Sidebar.test.tsx,
    taskflow/src/routes/settings/Settings.test.tsx,
    taskflow/src/routes/standup-notes/TodayColumn.test.tsx
  </files>
  <action>Apply the remaining surgical edits per the `<interfaces>` block: DashboardSprintCard.tsx JSDoc — drop `/SprintProgressTab/SprintHealthPanel` so it reads "...cache keys with SprintBoardTab."; DashboardInProgressCard.tsx — fix the two comments (L13 and L42) to drop `SprintHealthPanel`; WorklogsPage.tsx L315 — drop the `(SprintProgressTab pattern)` parenthetical from the comment; Sidebar.test.tsx L78 and Settings.test.tsx L128 — delete the `{ id: 'sprint-progress', ... }` mock fixture lines (these are dead references per the locked "clean all dead references" decision — verified they are not asserted on, so removing them is safe); TodayColumn.test.tsx L13 — trim `SprintHealthPanel.test.tsx + ` from the pattern-source comment. Then run `npm run build` (NOT just tsc — CSS imports fail silently in TS-only checks, per STATE.md convention) and the test suite to prove no dangling references remain.</action>
  <verify>
    <automated>cd taskflow && (grep -rn "SprintProgress\|SprintHealthPanel\|sprint-progress" src/ | grep -v '^#' && echo RESIDUAL_REFS && exit 1) || echo NO_RESIDUAL_OK && npm run build && npm run test</automated>
  </verify>
  <done>`grep -rn "SprintProgress\|SprintHealthPanel\|sprint-progress" src/` returns nothing; `npm run build` succeeds; the test suite passes.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `cd taskflow && grep -rn "SprintProgress\|SprintHealthPanel\|sprint-progress" src/` → zero matches (full codebase clean).
2. `cd taskflow && ls src/routes/dashboard/Sprint*` → only SprintBoardTab.tsx and any unrelated Sprint* files remain; no SprintProgressTab/SprintProgressSkeleton/SprintHealthPanel.
3. `cd taskflow && npm run build` → completes without errors.
4. `cd taskflow && npm run test` → full suite green.
</verification>

<success_criteria>
- The five target files are deleted.
- Every SprintProgress / SprintHealthPanel / `/sprint-progress` reference is removed from `src/` (code, comments, test fixtures).
- No redirect or replacement UI was added.
- `npm run build` and the test suite both pass.
</success_criteria>

<output>
Create `.planning/quick/260526-h3u-remove-sprint-progress-page-entirely-wit/260526-h3u-SUMMARY.md` when done.
</output>
