---
phase: quick-260521-u4u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/index.tsx
  - taskflow/src/routes/dashboard/index.test.tsx
autonomous: false
requirements: [QUICK-260521-U4U]
must_haves:
  truths:
    - "Dashboard greeting uses ONLY the user's first name (e.g., 'Alice', not 'Alice Doe')"
    - "Greeting falls back gracefully when displayName is null/empty"
    - "Dashboard hero feels visually warmer — decorative SVG element is present"
    - "Existing tests pass with updated assertions (greeting text changed)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/index.tsx"
      provides: "Updated hero section: first-name greeting + decorative SVG + warmer styling"
      contains: "Welcome back"
    - path: "taskflow/src/routes/dashboard/index.test.tsx"
      provides: "Tests updated for first-name greeting + fallback"
      contains: "Welcome back, Alice"
  key_links:
    - from: "taskflow/src/routes/dashboard/index.tsx"
      to: "useAuthStore().jiraUserDisplayName"
      via: "split on whitespace, take first token"
      pattern: "jiraUserDisplayName.*split"
---

<objective>
On the dashboard, make the welcome hero feel more welcoming and personal:
1. Greet by first name only (extract from `jiraUserDisplayName`: "Alice Doe" → "Alice").
2. Add a light, decorative SVG element (e.g., subtle wave/sun/sparkle) to lift the plain gradient hero.
3. Optionally warm the copy slightly (time-of-day-aware greeting verb like "Good morning, Alice" — Claude's discretion).

Purpose: The current hero is a plain centered `<h1>` on a gradient. "Welcome back, Alice Doe" feels formal and HR-system-like. Users wanted a first-name, friendlier feel with a touch of personality.

Output: Updated `dashboard/index.tsx` hero section + updated tests asserting the new greeting format.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/dashboard/index.tsx
@taskflow/src/routes/dashboard/index.test.tsx

<interfaces>
Current hero (taskflow/src/routes/dashboard/index.tsx lines 32–39):

- Outer `<div className="flex flex-col">` wraps hero + card grid.
- Hero section: `<section className="bg-gradient-to-br from-primary/5 to-background dark:from-primary/10 dark:to-background px-6 py-10 text-center">`
- Heading: `<h1 className="text-3xl font-semibold tracking-tight">Welcome back, {jiraUserDisplayName ?? 'there'}</h1>`
- Subtitle: `<p className="text-sm text-muted-foreground mt-1">{today}</p>` where `today` is `toLocaleDateString('en-GB', { weekday, day, month, year })`.

Auth store hook returns `jiraUserDisplayName: string | null`. Example values: "Alice Doe", "Jane Doe", possibly single token ("Alice") or null.

Tailwind utilities already in use across the app: `text-primary`, `text-muted-foreground`, `bg-gradient-to-br`, `from-primary/10`, `dark:` variants. Use existing palette — no new colors.

Existing test (index.test.tsx) asserts EXACT strings:
- "Welcome back, Alice Doe" (line 85) — will need to change to first-name form
- "Welcome back, there" (line 107) — fallback assertion, may keep or adapt
- Mocked `jiraUserDisplayName: 'Alice Doe'` (line 32, 75) — feeds first-name extraction
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: First-name greeting + warmer hero with decorative SVG</name>
  <files>taskflow/src/routes/dashboard/index.tsx, taskflow/src/routes/dashboard/index.test.tsx</files>
  <behavior>
    - Test 1: When `jiraUserDisplayName` is "Alice Doe", heading renders "Welcome back, Alice" (or greeting variant containing the substring "Alice" with no "Doe").
    - Test 2: When `jiraUserDisplayName` is "Jane Doe", heading contains "Jane" and does NOT contain "Doe".
    - Test 3: When `jiraUserDisplayName` is null, heading renders a graceful fallback (e.g., "Welcome back, there" — exact string preserved or updated; either is acceptable as long as a friendly fallback exists).
    - Test 4: When `jiraUserDisplayName` is a single token like "Alice", heading renders "Alice" (no crash on missing space).
    - Test 5: Hero section contains a decorative `<svg>` element (assert via `document.querySelector('section svg')` not null).
    - Existing DASH-05 (no widget markers) and three-card-stubs tests continue to pass unchanged.
    - Today's date in en-GB format still renders.
  </behavior>
  <action>
    Update `taskflow/src/routes/dashboard/index.tsx` hero section:

    1. Derive first name: extract the first whitespace-delimited token from `jiraUserDisplayName`. Implement inline (no new utility file needed) — e.g., `jiraUserDisplayName?.trim().split(/\s+/)[0] ?? null`. Falsy result → use the existing 'there' fallback (or a warmer alternative — Claude's discretion).

    2. Replace the bare `<h1>Welcome back, {name}</h1>` with a warmer hero. Suggested structure (adapt as needed — keep it tasteful, no emoji noise):
       - Keep the existing `<section>` gradient wrapper but make it `relative overflow-hidden` so the SVG can sit absolutely behind/beside the text without affecting layout.
       - Inline a light decorative SVG (sun, sparkles, abstract blob, or wave). Use `currentColor` with `text-primary/15` or `text-primary/20` opacity utilities so it auto-themes with light/dark mode. Place it absolutely positioned (e.g., `absolute right-6 top-1/2 -translate-y-1/2 opacity-60 pointer-events-none`) — decorative only, must be `aria-hidden="true"`.
       - Heading: `<h1 className="text-3xl font-semibold tracking-tight">Welcome back, {firstName ?? 'there'}</h1>` (or a time-of-day-aware variant — Claude's discretion: e.g., "Good morning, Alice" if local hour < 12, "Good afternoon" < 18, else "Good evening". If you take the time-of-day path, ensure tests still match by checking substring "Alice" rather than the exact greeting prefix.).
       - Keep the date subtitle exactly as-is (en-GB format) — tests rely on it.

    3. Keep the same outer `<div className="flex flex-col">` wrapper and the cards grid untouched. Only the `<section>` hero changes.

    4. Update `taskflow/src/routes/dashboard/index.test.tsx`:
       - Adjust Test 1 ("greeting renders displayName") to assert first-name behavior. Use a regex matcher or partial text matcher: `expect(screen.getByText(/Alice/)).toBeTruthy()` AND `expect(screen.queryByText(/Alice Doe/)).toBeNull()` (proves no last name).
       - Keep Test 2 (today's date) unchanged.
       - Update Test 3 (fallback when displayName null) to match whatever fallback string you ship (default: keep "Welcome back, there").
       - Add Test 4: single-token displayName ("Alice") still renders "Alice".
       - Add Test 5: decorative SVG present — `expect(document.querySelector('section svg')).not.toBeNull()` (with `aria-hidden` assertion if desired).
       - Keep Test 4 (DASH-05 widget markers) and Test 5 (three cards) unchanged — they should be re-numbered to 6 and 7 if you renumber, but renumbering is not required.

    Do NOT introduce new utility files, no new dependencies, no animation libraries. Inline SVG only. Use existing Tailwind classes — no new color tokens.

    Note: project skills directories (.claude/skills, .agents/skills) do not exist for this project; no project-specific skill rules apply.
  </action>
  <verify>
    <automated>cd taskflow && npm test -- src/routes/dashboard/index.test.tsx</automated>
    <human-check>Run `cd taskflow && npm run dev`, open dashboard. Verify: (a) greeting shows only first name; (b) SVG decoration is visible but tasteful (not garish); (c) date still renders below; (d) dark mode looks good (toggle theme or check system theme).</human-check>
  </verify>
  <done>
    - `npm test src/routes/dashboard/index.test.tsx` passes (all assertions including new first-name and SVG presence checks).
    - Dashboard renders "Welcome back, {FirstName}" (or warmer time-of-day variant containing first name only).
    - Decorative `<svg>` element exists inside the hero `<section>` with `aria-hidden="true"`.
    - No regressions in DASH-05 (no widget controls) or three-card rendering.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of new dashboard hero</name>
  <what-built>First-name greeting + decorative SVG in dashboard hero section.</what-built>
  <how-to-verify>
    1. Run `cd taskflow && npm run dev` and open the dashboard route (the app's default landing page after login).
    2. Confirm the greeting reads "Welcome back, {YourFirstName}" (e.g., "Welcome back, Jane" — NOT "Welcome back, Jane Doe").
    3. Confirm a light SVG decoration is visible in the hero — subtle, themed to the primary color, not distracting.
    4. Confirm the date line below the greeting still renders in en-GB format (e.g., "Thursday, 21 May 2026").
    5. Toggle dark mode (system settings or app theme toggle if exists). Verify the SVG and gradient still look balanced — no harsh contrast issues.
    6. Reload the page. Verify nothing flickers or shifts unexpectedly.
    7. If the implementer chose a time-of-day greeting variant ("Good morning/afternoon/evening"), confirm it matches your local hour.
  </how-to-verify>
  <resume-signal>Type "approved" if the hero looks welcoming and the first-name greeting works. If the SVG is too loud / too quiet, or the greeting feels off, describe the issue and Claude will iterate.</resume-signal>
</task>

</tasks>

<verification>
- Unit tests pass: `cd taskflow && npm test -- src/routes/dashboard/index.test.tsx`
- Full test suite still green: `cd taskflow && npm test` (sanity check — no unrelated breakage)
- Visual verification by user (Task 2 checkpoint)
</verification>

<success_criteria>
- Dashboard greeting uses first name only (e.g., "Welcome back, Alice" — not "Welcome back, Alice Doe").
- Hero contains a decorative inline SVG, `aria-hidden="true"`, themed via Tailwind primary color with opacity.
- Date subtitle preserved in en-GB format.
- Fallback works when `jiraUserDisplayName` is null/single-token.
- Tests updated and passing.
- User approves visual result.
</success_criteria>

<output>
Create `.planning/quick/260521-u4u-on-the-dashboard-make-the-welcome-more-w/260521-u4u-SUMMARY.md` when done. Update STATE.md "Quick Tasks Completed" table with this task's row.
</output>
