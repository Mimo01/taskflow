---
phase: quick-260916-rk1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx
autonomous: true
requirements: [QUICK-260916-RK1]

must_haves:
  truths:
    - "A task row on the Releases detail page shows the Jira issue-type icon (Bug/Story/Subtask/Epic/default) to the left of the Key cell"
    - "The column header strip shows a blank header cell aligned over the new icon column so Key/Summary/etc. headers stay aligned with their cells"
    - "An issue with no issuetype renders an empty, aria-hidden icon slot instead of a broken/missing icon"
  artifacts:
    - path: "taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx"
      provides: "COL_ICON width constant, icon cell in TaskRow, matching blank cell in ColumnHeaderStrip"
      contains: "IssueTypeIcon"
  key_links:
    - from: "taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx"
      to: "taskflow/src/components/ui/issue-type-icon.tsx"
      via: "import { IssueTypeIcon } from '@/components/ui/issue-type-icon'"
      pattern: "IssueTypeIcon"
---

<objective>
Add the Jira issue-type icon (Bug/Story/Subtask/Epic/default, from the existing `IssueTypeIcon` mapper) to each task row on the Releases detail page's primary (task) table, matching the visual pattern already used in `BacklogRow.tsx`.

Purpose: The Releases page task list is the only major issue-list surface still missing the type icon (Backlog, Recently Visited, and Search results already show it) — visual consistency and faster scanability.
Output: `TaskRow` in `UnifiedTaskTable.tsx` renders an 18x18 type-icon cell before the Key cell; `ColumnHeaderStrip` gains a matching blank header cell so columns stay aligned.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mimo/Documents/Projects/taskflow/taskflow/src/components/ui/issue-type-icon.tsx
@/Users/mimo/Documents/Projects/taskflow/taskflow/src/routes/dashboard/BacklogRow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add type-icon column to UnifiedTaskTable's primary task rows</name>
  <files>taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx</files>
  <action>
    In `taskflow/src/routes/dashboard/release-detail/UnifiedTaskTable.tsx`:

    1. Add `import { IssueTypeIcon } from '@/components/ui/issue-type-icon';` to the import block (alongside the other `@/components/ui/*` imports near the top).

    2. Add a new column width constant next to the existing `COL_KEY`/`COL_SUMMARY`/etc. constants (around line 73-77): `const COL_ICON = 'flex-none w-[1.125rem]';` (18px = 1.125rem, matching the BacklogRow 18x18 icon slot exactly, no gap-eating padding since the row already uses `gap-2`).

    3. In `ColumnHeaderStrip` (~line 375-394), add a blank header `<span className={COL_ICON} aria-hidden="true" />` as the very first child, before the existing `<span className={COL_KEY}>Key</span>`. This is a decorative-only column (the icon itself conveys the type without needing a text label — same as BacklogRow's header, which also has no "Type" label), so mark it `aria-hidden="true"` rather than giving it visible text.

    4. In `TaskRow` (~line 828-913), add the icon cell as the first child inside the row div, before the full-row overlay button and the Key button. Follow the `pointer-events-none relative` sibling convention used by the Summary/Assignee/Status cells (NOT the `relative z-10` button convention — this cell is not interactive), and copy the exact 18x18/aria-hidden/conditional-render pattern from `BacklogRow.tsx`'s issue-type icon cell:
       - Wrapping element: `<span className={`pointer-events-none relative ${COL_ICON} flex items-center justify-center`} style={{ width: 18, height: 18 }} aria-hidden={!issue.fields.issuetype}>`
       - Inside: `{issue.fields.issuetype?.name && <IssueTypeIcon typeName={issue.fields.issuetype.name} />}`
       - Close the span.

    Do not modify `issue-type-icon.tsx`. Do not touch the secondary (uncovered-MRs) table, `SecondaryHeaderStrip`, or `MrSubLine` — WR-06 established those are a deliberately different shape (Key / Merge request / BR / MS) with no room in scope for this quick task; adding the icon there is out of scope.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx tsc --noEmit -p . 2>&1 | grep -i UnifiedTaskTable || true</automated>
  </verify>
  <done>UnifiedTaskTable.tsx imports IssueTypeIcon, defines COL_ICON, ColumnHeaderStrip renders a blank aria-hidden icon header cell first, and TaskRow renders the 18x18 conditional IssueTypeIcon cell first (before the Key button), with no new TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Issue-type icons (Bug/Story/Subtask/Epic/default) added to each task row on the Releases detail page's primary task table, positioned before the Key column.</what-built>
  <how-to-verify>
    1. Run the app (`npm run tauri dev` or your usual dev command) and open a release with tasks in Jira (e.g. via the Releases page -> a release with issues).
    2. Confirm each task row shows a small colored icon (bug icon for Bug, book icon for Story, corner-down-right icon for Subtask, checksquare for default) immediately to the left of the Jira key.
    3. Confirm the icon column aligns cleanly across all rows and the header strip's Key/Summary/Assignee/Status/MR labels are still aligned over their columns (no visual shift).
    4. Confirm density settings (compact/comfortable) still render correctly — the icon cell shouldn't cause row height changes.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this is a purely presentational change reading already-fetched, already-rendered `issue.fields.issuetype` data (same field already displayed as row content elsewhere on this page). No new trust boundary is crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick260916rk1-01 | Information Disclosure | IssueTypeIcon cell in TaskRow | accept | Renders only `issue.fields.issuetype.name`, data already fetched and displayed elsewhere on the same page (Key, Summary, Status); no new data exposure. |
</threat_model>

<verification>
- `npx tsc --noEmit` shows no new errors in UnifiedTaskTable.tsx
- Manual visual check per the checkpoint task above
</verification>

<success_criteria>
- Releases detail page task rows show the issue-type icon before the Key cell, matching BacklogRow's visual pattern (18x18, aria-hidden when absent, conditional render)
- Header strip stays aligned with the new column
- No changes to issue-type-icon.tsx or the secondary (uncovered-MRs) table
</success_criteria>

<output>
Create `.planning/quick/260916-rk1-on-releases-page-add-task-type-icon-patt/260916-rk1-SUMMARY.md` when done
</output>
