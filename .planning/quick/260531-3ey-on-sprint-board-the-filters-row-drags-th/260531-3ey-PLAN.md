---
phase: quick-260531-3ey
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/components/UnifiedFilterBar.tsx
autonomous: true
requirements: [QUICK-260531-3ey]

must_haves:
  truths:
    - "On Sprint Board, when filter content (presets + chips) overflows, the left content region scrolls horizontally instead of widening the page (D: scroll scope)."
    - "Right-side action buttons (Save / Save Filter / Filter+count, and the savingName input) stay pinned on the right and never scroll (D: right pinned shrink-0)."
    - "The expanded filtersOpen row (FilterDropdowns + chips) also scrolls horizontally instead of dragging the page (D: apply to both rows)."
    - "QuickFilterChipRow remains untouched and unregressed (D: leave as-is)."
    - "npm run check (biome check + tsc) stays green."
  artifacts:
    - path: "taskflow/src/components/UnifiedFilterBar.tsx"
      provides: "Primary row + expanded row restructured with scrollable left region and pinned right group"
      contains: "flex-1 min-w-0"
  key_links:
    - from: "UnifiedFilterBar primary row left region"
      to: "no-scrollbar @utility (src/index.css)"
      via: "overflow-x-auto no-scrollbar classes"
      pattern: "overflow-x-auto no-scrollbar"
    - from: "UnifiedFilterBar primary row"
      to: "right action button group"
      via: "shrink-0 wrapper holding Save / Save Filter / savingName input / Filter toggle"
      pattern: "shrink-0"
---

<objective>
Fix the Sprint Board filters row so it no longer drags the entire page horizontally when its
content overflows. Restructure both the primary row and the expanded `filtersOpen` row in
`UnifiedFilterBar.tsx` so the left content (preset pills + active chips, plus dropdowns in the
expanded row) lives in a scrollable `flex-1 min-w-0 ... overflow-x-auto no-scrollbar` region with
`flex-nowrap` inner content, and the right-side action buttons sit in a pinned `shrink-0` group.

Purpose: A flex item refuses to shrink below its intrinsic content width unless it has `min-w-0`,
so today overflow forces the row wider than the viewport and the ancestor `overflow-auto` scrolls
the whole board. Adding the scroll region contains overflow locally.
Output: Modified `taskflow/src/components/UnifiedFilterBar.tsx`. CSS-only / structural — no logic,
state, props, or testid changes.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260531-3ey-on-sprint-board-the-filters-row-drags-th/260531-3ey-CONTEXT.md
@.planning/quick/260531-3ey-on-sprint-board-the-filters-row-drags-th/260531-3ey-RESEARCH.md
@taskflow/src/components/UnifiedFilterBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure the primary filters row into a scrollable left region + pinned right group</name>
  <files>taskflow/src/components/UnifiedFilterBar.tsx</files>
  <action>
In the primary row (currently lines ~329-562), keep the outer `<div className="flex items-center gap-1.5 px-3 py-1.5">` as the row container — KEEP `px-3` on this OUTER row only (do not move padding into the scroll wrapper, or scrolled content loses end padding).

Wrap the left content — the empty-state hint (`quickFilters.length === 0 && !hasActiveFilters` span), the `quickFilters.map(...)` preset pills, the active-chips `<>` block (the `w-px h-4` divider + the chip container) — inside a single new scrollable left region:
`<div className="flex-1 min-w-0 flex flex-nowrap items-center gap-1.5 overflow-x-auto no-scrollbar"> ... </div>`.
The `flex-1 min-w-0` is the critical bit (per RESEARCH §2/§Pitfalls): without `min-w-0` the flex child will not shrink and `overflow-x-auto` does nothing.

Inside that left region, change the active-chip container at line ~450 from `flex flex-wrap items-center gap-1` to `flex flex-nowrap items-center gap-1` (single-row, no wrapping — per D: chip behavior on overflow). Add `shrink-0` to each individual chip `<span>` (line ~455) and to the empty-state hint span and the divider so they keep intrinsic width and do not squash inside the nowrap region. Leave the inner Clear button as-is.

DELETE the `flex-1` spacer `<div className="flex-1" />` at line ~481 — the new left region's `flex-1` now fills the available space.

Wrap the right-side action controls in a single pinned group:
`<div className="shrink-0 flex items-center gap-1.5"> ... </div>`.
This group must contain ALL of: the Save button (`hasActiveFilters && !savingName`), the Save Filter button (`hasActiveFilters && !savingName && jiraBaseUrl`), the `savingName` span (input + Save + cancel — keep it here so it stays pinned when it replaces the Save buttons, per RESEARCH §Pitfalls), and the Filter toggle Button (with `activeCount` badge).

Do NOT change any logic, handlers, state, props, conditional-render guards, or `data-testid` values (e.g. line ~454). This is structural/className only.
  </action>
  <verify>
    <automated>cd taskflow && [ "$(grep -F -c 'flex-1 min-w-0 flex flex-nowrap items-center gap-1.5 overflow-x-auto no-scrollbar' src/components/UnifiedFilterBar.tsx)" = "2" ] && echo "LEFT_REGION_OK_BOTH_ROWS"</automated>
    <automated>cd taskflow && [ "$(grep -F -c 'className="flex-1" />' src/components/UnifiedFilterBar.tsx)" = "0" ] && echo "SPACER_REMOVED"</automated>
    <automated>cd taskflow && grep -qF 'shrink-0 flex items-center gap-1.5' src/components/UnifiedFilterBar.tsx && echo "RIGHT_GROUP_OK"</automated>
  </verify>
  <done>Primary row left content is inside a `flex-1 min-w-0 ... overflow-x-auto no-scrollbar` region with `flex-nowrap` chips; the `flex-1` spacer is deleted; Save/Save Filter/savingName/Filter toggle are inside one `shrink-0` group; no testids or logic changed.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the same scroll region to the expanded filtersOpen row and verify the gate</name>
  <files>taskflow/src/components/UnifiedFilterBar.tsx</files>
  <action>
In the expanded `filtersOpen` row (currently lines ~575-637), keep the outer
`<div className="flex items-center gap-1.5 px-3 py-1.5 border-t border-border/50 bg-muted/20">` as-is (keep `px-3` on the outer row).

Wrap the four `FilterDropdown` components (Epic / Label / Assignee / Status) AND the active-chips block (the `activeChips.length > 0` fragment containing the `w-px h-4` divider + chip container) inside a single scrollable left region matching Task 1:
`<div className="flex-1 min-w-0 flex flex-nowrap items-center gap-1.5 overflow-x-auto no-scrollbar"> ... </div>`.

Change the chip container at line ~607 from `flex flex-wrap items-center gap-1` to `flex flex-nowrap items-center gap-1`. Add `shrink-0` to each chip `<span>` (line ~612) and to the `w-px h-4` divider so they keep intrinsic width. There are no right-pinned buttons in this row, so no `shrink-0` action group is needed — the scroll region alone prevents page drag (per RESEARCH §4 expanded row).

Do NOT touch `QuickFilterChipRow.tsx` (it is a sibling row, already self-contained, and is NOT the page-drag culprit per RESEARCH §3). Do NOT change any FilterDropdown props, logic, or `data-testid` values (e.g. line ~611).

Then run the repo gate to confirm no type/lint regressions.
  </action>
  <verify>
    <automated>cd taskflow && [ "$(grep -F -c 'flex flex-nowrap items-center gap-1"' src/components/UnifiedFilterBar.tsx)" = "2" ] && echo "BOTH_CHIP_CONTAINERS_NOWRAP"</automated>
    <automated>cd taskflow && [ "$(grep -F -c 'flex flex-wrap items-center gap-1"' src/components/UnifiedFilterBar.tsx)" = "0" ] && echo "NO_WRAP_CHIP_CONTAINERS_LEFT"</automated>
    <automated>cd taskflow && [ "$(grep -F -c '.replace(/^(epic|label|assignee|status)-/' src/components/UnifiedFilterBar.tsx)" = "2" ] && echo "TESTIDS_INTACT"</automated>
    <automated>cd taskflow && npm run check</automated>
  </verify>
  <done>Expanded row dropdowns + chips live in a `flex-1 min-w-0 ... overflow-x-auto no-scrollbar` region with `flex-nowrap` chips; both rows now have nowrap chip containers and zero flex-wrap chip containers; QuickFilterChipRow untouched; chip testids intact (2 occurrences); `npm run check` (biome + tsc) is green.</done>
</task>

</tasks>

<verification>
- `npm run check` (run from `taskflow/`) passes — biome check + tsc clean, matching the repo GREEN baseline.
- Both rows contain exactly one `flex-1 min-w-0 ... overflow-x-auto no-scrollbar` left region each (2 total).
- The `flex-1` spacer `<div className="flex-1" />` is gone (0 occurrences).
- The right action group (`shrink-0 flex items-center gap-1.5`) wraps Save / Save Filter / savingName input / Filter toggle.
- Both chip containers are `flex-nowrap`; zero `flex-wrap` chip containers remain.
- Chip `data-testid` replace patterns (lines ~454 and ~611) are unchanged — 2 occurrences, no test fixture impact.
- `QuickFilterChipRow.tsx` is unmodified.
</verification>

<success_criteria>
- Sprint Board filters no longer drag the page horizontally on overflow; the left content region scrolls horizontally (trackpad / shift-wheel) with a hidden scrollbar.
- Right-side action buttons remain pinned right and unaffected by scroll.
- Both the primary and expanded `filtersOpen` rows behave this way.
- No logic, props, state, or testid changes; `npm run check` green.
</success_criteria>

<output>
Create `.planning/quick/260531-3ey-on-sprint-board-the-filters-row-drags-th/260531-3ey-SUMMARY.md` when done.
</output>
