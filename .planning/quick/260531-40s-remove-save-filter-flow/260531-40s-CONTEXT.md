# Quick Task 260531-40s: Remove the "Save Filter" (Jira) button and flow - Context

**Gathered:** 2026-05-31
**Status:** Ready for planning

<domain>
## Task Boundary

Remove the **"Save Filter"** button and its **entire flow** from the filters. This is the
Jira-backend saved-filter system. Only keep the **"Save"** button that saves filters
**locally** (the QuickFilters system persisted to `settings.json`).

Source root: `taskflow/` (i.e. `taskflow/src/...`), NOT the repo root.

</domain>

<decisions>
## Implementation Decisions

### Worklogs scope
- **Leave WorklogsPage alone.** Its "Save filter" button is a separate Tempo system
  (`useTempoFiltersStore`) that merely shares the label. Do NOT touch
  `src/routes/worklogs/WorklogsPage.tsx` or `useTempoFiltersStore`.

### Removal depth
- **Full delete.** Remove the dialogs, store, jira/filters service, sync hook, and ALL
  downstream consumers (CommandPalette "Saved Filters" group, SprintBoard active-filter
  banner + JQL-intersection logic, the startup favourite-filters fetch). No dead code left.

### What to KEEP (do not break)
- The local **"Save"** QuickFilters flow in `UnifiedFilterBar.tsx` (pills, inline name
  input, `addQuickFilter`), backed by `settings.store.ts` (`quickFilters`) and
  `filter.store.ts` (active epic/label/assignee/status). This is the whole point — it stays.

### Claude's Discretion
- Whether to keep the `src/services/jira/index.ts` barrel file (remove only the
  `export * from './filters'` line) — keep the barrel, drop the re-export.
- Exact test-file handling: delete tests that target removed code; keep the suite green.

</decisions>

<specifics>
## Specific Ideas

Confirmed Jira-saved-filter surface to DELETE (taskflow/src):
- `components/SaveFilterDialog.tsx` (+ test if any)
- `components/EditFilterDialog.tsx`
- `components/SavedFilterList.tsx` + `components/SavedFilterList.test.tsx` (only rendered in tests today)
- `stores/saved-filter.store.ts` + `stores/saved-filter.store.test.ts`
- `hooks/useSavedFilterSync.ts`
- `services/jira/filters.ts` + `services/jira/filters.test.ts`

Confirmed references to REMOVE/EDIT:
- `components/UnifiedFilterBar.tsx:26` import + `:571` `<SaveFilterDialog>` render + the
  "Save Filter" button (BookmarkPlus, label "Save Filter") + `saveDialogOpen` state.
  KEEP the "Save" (local QuickFilter) button and inline input in the same file.
- `components/app/CommandPalette.tsx:37,75,76` + the "Saved Filters" group render (~295-313).
- `routes/dashboard/SprintBoardTab.tsx:50,965-967` + saved-filter query, JQL intersection,
  and active-filter banner (~1232-1243).
- `hooks/useSavedFilterSync.ts` call in `main.tsx:34` (import) and `main.tsx:495` (call).
- `services/jira/index.ts:10` `export * from './filters';` — remove that line.

</specifics>

<canonical_refs>
## Canonical References

No external specs — requirements fully captured in decisions above. Distinguish the two
"Save" systems carefully: label "Save Filter" = delete; label "Save" = keep.

</canonical_refs>
