---
phase: quick-260531-40s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/components/SaveFilterDialog.tsx
  - taskflow/src/components/EditFilterDialog.tsx
  - taskflow/src/components/SavedFilterList.tsx
  - taskflow/src/components/SavedFilterList.test.tsx
  - taskflow/src/stores/saved-filter.store.ts
  - taskflow/src/stores/saved-filter.store.test.ts
  - taskflow/src/hooks/useSavedFilterSync.ts
  - taskflow/src/services/jira/filters.ts
  - taskflow/src/services/jira/filters.test.ts
  - taskflow/src/services/jira/index.ts
  - taskflow/src/components/UnifiedFilterBar.tsx
  - taskflow/src/components/app/CommandPalette.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
  - taskflow/src/main.tsx
autonomous: true
requirements: [QT-260531-40s]

must_haves:
  truths:
    - "The Jira 'Save Filter' button no longer renders in the Sprint Board filter bar (only the local 'Save' QuickFilter button remains)"
    - "The CommandPalette no longer shows a 'Saved Filters' group (both the default-state and always-visible branches are gone)"
    - "The Sprint Board no longer fetches Jira saved-filter issue keys, intersects JQL, or renders an active-saved-filter banner"
    - "App startup no longer calls useSavedFilterSync (no favourite-filters fetch)"
    - "The local 'Save' QuickFilters flow (inline name input, addQuickFilter, quickFilter pills) still works unchanged"
    - "WorklogsPage / Tempo savedFilters flow is untouched"
    - "npm run check is GREEN (biome check + tsc --noEmit) and npm run test passes"
  artifacts:
    - path: "taskflow/src/services/jira/index.ts"
      provides: "jira barrel WITHOUT the filters re-export"
      contains: "export"
    - path: "taskflow/src/components/UnifiedFilterBar.tsx"
      provides: "Filter bar with local Save flow only"
      contains: "addQuickFilter"
  key_links:
    - from: "taskflow/src/services/jira/index.ts"
      to: "(deleted) ./filters"
      via: "removed re-export"
      pattern: "export \\* from './filters'"
    - from: "taskflow/src/main.tsx"
      to: "(deleted) useSavedFilterSync"
      via: "removed import + call"
      pattern: "useSavedFilterSync"
---

<objective>
Remove the Jira-backed "Save Filter" saved-filter subsystem entirely — dialogs, store,
service, sync hook, and all downstream consumers — while preserving the local "Save"
QuickFilters flow and leaving the Tempo (WorklogsPage) saved-filters subsystem untouched.

Purpose: Eliminate dead/confusing dual-Save UX; the Jira saved-filter flow is being retired
in favor of the local QuickFilters system.
Output: 7 source files + 3 test files deleted, 5 consumer files edited, suite GREEN.

This is a pure deletion/refactor. The removal map in
`260531-40s-RESEARCH.md` is HIGH-confidence and file:line-precise — execute it directly.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260531-40s-remove-save-filter-flow/260531-40s-CONTEXT.md
@.planning/quick/260531-40s-remove-save-filter-flow/260531-40s-RESEARCH.md

Source root is `taskflow/` — all paths below are under `taskflow/src/...` and ALL
build/test/lint commands run from inside `taskflow/`.

CRITICAL — two independent "Save" systems share labels:
- DELETE: label "Save Filter", gated on `jiraBaseUrl`, backed by `useSavedFilterStore` / `services/jira/filters`.
- KEEP: label "Save" (local QuickFilters), `addQuickFilter`, `settings.store` quickFilters, `filter.store`.
- DO NOT TOUCH: Tempo `savedFilters`/`activeFilterId` in `WorklogsPage.tsx` + `tempo-filters.store.ts` (separate subsystem, false-positive name matches only).
- KEEP shared icons: `BookmarkPlus` (both Save buttons), and `Bookmark` in UnifiedFilterBar (filter chips @356/394). Only drop `Bookmark` from CommandPalette and SprintBoardTab imports.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the Jira saved-filter files and drop the barrel re-export</name>
  <files>
    taskflow/src/components/SaveFilterDialog.tsx (delete),
    taskflow/src/components/EditFilterDialog.tsx (delete),
    taskflow/src/components/SavedFilterList.tsx (delete),
    taskflow/src/components/SavedFilterList.test.tsx (delete),
    taskflow/src/stores/saved-filter.store.ts (delete),
    taskflow/src/stores/saved-filter.store.test.ts (delete),
    taskflow/src/hooks/useSavedFilterSync.ts (delete),
    taskflow/src/services/jira/filters.ts (delete),
    taskflow/src/services/jira/filters.test.ts (delete),
    taskflow/src/services/jira/index.ts (edit)
  </files>
  <action>
    Delete the 7 source files and 3 test files listed above (use `rm` from `taskflow/`).
    These form a self-contained delete-set per RESEARCH "DELETE entirely" table — every
    importer is either in the set or edited in Task 2. In `services/jira/index.ts`, remove
    ONLY the line `export * from './filters';` (RESEARCH index.ts:10); keep all other barrel
    exports including `export * from './types'`. The `JiraSavedFilter` interface in
    `services/jira/types.ts:262-269` becomes orphaned dead code — leaving it is harmless
    (per Assumption A2); do not spend effort removing it. Do NOT touch the `tempo-filters`
    store or any WorklogsPage file.
  </action>
  <verify>
    From `taskflow/`: `test ! -e src/stores/saved-filter.store.ts && test ! -e src/services/jira/filters.ts && test ! -e src/hooks/useSavedFilterSync.ts && echo DELETED`; and `grep -rn "from './filters'" src/services/jira/index.ts` returns no match.
  </verify>
  <done>All 10 files removed; `services/jira/index.ts` has no `./filters` re-export; barrel otherwise intact.</done>
</task>

<task type="auto">
  <name>Task 2: Strip saved-filter references from the 4 consumers + main.tsx</name>
  <files>
    taskflow/src/components/UnifiedFilterBar.tsx,
    taskflow/src/components/app/CommandPalette.tsx,
    taskflow/src/routes/dashboard/SprintBoardTab.tsx,
    taskflow/src/main.tsx
  </files>
  <action>
    Apply the RESEARCH "EDIT" + "FLAGGED" removals exactly. Re-grep each symbol before
    deleting in case line numbers drifted.

    UnifiedFilterBar.tsx: remove the `import { SaveFilterDialog }` (~:26); remove
    `const [saveDialogOpen, setSaveDialogOpen] = useState(false)` (~:199); remove the
    SECOND "Save Filter" `<Button>` block gated on `&& jiraBaseUrl` (~:500-510) that calls
    `setSaveDialogOpen(true)` — KEEP the FIRST local "Save" button (~:488-499, calls
    `handleStartSave`); remove the `{jiraBaseUrl && <SaveFilterDialog .../>}` block (~:569-577);
    remove the now-orphaned `currentJql`/`currentJqlClauses` derivation (~:218-243); and
    remove `jiraBaseUrl` from the `useAuthStore()` destructure (~:198) after confirming via
    grep it has no other consumer in this file (Assumption A1). KEEP `BookmarkPlus`,
    `Bookmark`, the local QuickFilter inline-input/`savingName` block, and `addQuickFilter`.

    CommandPalette.tsx: remove `import { useSavedFilterStore }` (~:37); remove the
    `savedFilters` + `setActiveFilter` selectors (~:75-76); remove BOTH "Saved Filters"
    `<CommandGroup>` blocks — the default-state one (~:294-313) AND the always-visible
    second one (~:432-450, the FLAGGED extra); drop `Bookmark` from the lucide import (~:15,
    `Bookmark, SearchX`), keeping `SearchX`.

    SprintBoardTab.tsx: remove `import { useSavedFilterStore }` (~:50); drop `Bookmark` from
    the lucide import (~:16) and remove the `fetchAllSearchPages` import (~:44); remove the 3
    saved-filter selectors + `activeFilter` derive (~:965-968); remove the
    `savedFilterIssueKeys` `useQuery` block (~:970-981); remove the JQL-intersection
    `if (savedFilterIssueKeys...)` block (~:1050-1060) so `filteredSwimlanes` flows from
    `swimlanes` straight into the KEPT local-filter block (~:1062-1078); remove the active
    saved-filter banner JSX (~:1231-1247). KEEP `filteredSwimlanes` and the `useFilterStore`
    local selectors (`activeEpics` etc.).

    main.tsx: remove `import { useSavedFilterSync }` (~:34) and the `useSavedFilterSync();`
    call (~:495).
  </action>
  <verify>
    From `taskflow/`: `grep -rn "useSavedFilterStore\|useSavedFilterSync\|SaveFilterDialog\|fetchAllSearchPages\|savedFilterIssueKeys" src/components/UnifiedFilterBar.tsx src/components/app/CommandPalette.tsx src/routes/dashboard/SprintBoardTab.tsx src/main.tsx` returns no matches; then `npm run fix && npm run check` is GREEN.
  </verify>
  <done>Zero saved-filter references in the 4 consumers; local "Save" QuickFilter button + inline input retained; `npm run check` GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Clean stale test mock and run full suite</name>
  <files>
    taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
  </files>
  <action>
    In `SprintBoardTab.test.tsx`, remove the now-stale `@/services/jira/client` mock for
    `fetchAllSearchPages` (~:138-141) and its "saved filter integration" comment (~:138) —
    the only source consumer was removed in Task 2, so the mock is dead. Re-grep first; if
    the mock path differs, remove whatever stubs `fetchAllSearchPages`. Do NOT alter any
    other mock or assertion in this file. The 3 saved-filter test files were already deleted
    in Task 1; CommandPalette.test.tsx and UnifiedFilterBar.test.tsx have no saved-filter
    refs (per RESEARCH Test Impact) and need no edits. Leave the dangling comment in
    WorklogsPage.test.tsx alone (harmless, out of scope). Then run the full suite.
  </action>
  <verify>
    From `taskflow/`: `grep -c "fetchAllSearchPages" src/routes/dashboard/SprintBoardTab.test.tsx` returns 0; then `npm run test` passes (vitest run) and `npm run check` is GREEN.
  </verify>
  <done>Stale `fetchAllSearchPages` mock removed; `npm run test` and `npm run check` both pass.</done>
</task>

</tasks>

<verification>
From `taskflow/`:
1. `npm run check` — biome check + tsc --noEmit, expect GREEN (run `npm run fix` first to auto-clear any unused imports).
2. `npm run test` — vitest run, expect all pass.
3. `grep -rn "useSavedFilterStore\|useSavedFilterSync\|SaveFilterDialog\|EditFilterDialog\|SavedFilterList" src` returns only — confirmed empty (no live references; Tempo names are unaffected).
4. Manual confirm preserved: `grep -rn "addQuickFilter" src/components/UnifiedFilterBar.tsx` still present; WorklogsPage.tsx + tempo-filters.store.ts unchanged in git diff.
</verification>

<success_criteria>
- 7 source + 3 test files deleted; `services/jira/index.ts` filters re-export removed.
- 4 consumers + main.tsx have zero Jira saved-filter references, no orphaned imports/vars.
- Local "Save" QuickFilters flow intact; Tempo/WorklogsPage untouched.
- `npm run check` GREEN and `npm run test` passes from `taskflow/`.
</success_criteria>

<output>
Create `.planning/quick/260531-40s-remove-save-filter-flow/260531-40s-SUMMARY.md` when done.
</output>
