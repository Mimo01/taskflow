---
phase: quick-260813-1k3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/stores/recent-items.store.ts
  - taskflow/src/stores/recent-items.store.test.ts
  - taskflow/src/routes/dashboard/IssueDetailView.tsx
  - taskflow/src/main.tsx
  - taskflow/src/components/app/RecentItemsPopover.tsx
autonomous: false
requirements: [QUICK-1k3]

must_haves:
  truths:
    - "Each Jira row in the Recent Items popover shows the issue-type icon (Bug/Story/Task/Sub-task/Epic) instead of the generic clock"
    - "Each GitLab MR row in the popover shows a merge-request icon"
    - "The type icon still renders after an app restart with a cold react-query cache (persisted on the recent item)"
    - "A recent item whose type cannot be resolved falls back to the clock icon (no wrong default icon)"
  artifacts:
    - path: "taskflow/src/stores/recent-items.store.ts"
      provides: "RecentItem.issueType persisted field, preserved on dedup like title"
      contains: "issueType"
    - path: "taskflow/src/components/app/RecentItemsPopover.tsx"
      provides: "Type icon per row via shared IssueTypeIcon primitive"
      contains: "IssueTypeIcon"
  key_links:
    - from: "taskflow/src/components/app/RecentItemsPopover.tsx"
      to: "taskflow/src/components/ui/issue-type-icon.tsx"
      via: "import { IssueTypeIcon }"
      pattern: "components/ui/issue-type-icon"
    - from: "taskflow/src/routes/dashboard/IssueDetailView.tsx"
      to: "recent-items.store pushItem"
      via: "issueType: issue.fields.issuetype?.name"
      pattern: "issuetype\\?\\.name"
---

<objective>
Render the issue-type icon in the Recent Items popover rows, reusing the existing
`IssueTypeIcon` primitive rather than inventing a new icon mapping.

Purpose: The popover currently shows an identical clock glyph on every row, which
carries zero per-row information. The type icon is the app's established scanning
affordance (Backlog rows, pinned tabs, my-tasks rows all use `IssueTypeIcon`).

Output: Type icon on every Jira row, merge-request icon on every MR row, with the
type name persisted on the recent item so icons survive a cold start.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@taskflow/src/components/app/RecentItemsPopover.tsx
@taskflow/src/components/ui/issue-type-icon.tsx
@taskflow/src/stores/recent-items.store.ts

Interface contracts already in the codebase (do NOT redefine):

- `IssueTypeIcon` — `taskflow/src/components/ui/issue-type-icon.tsx`
  `export function IssueTypeIcon({ typeName, className }: { typeName: string; className?: string })`
  Default className is `'w-3.5 h-3.5 shrink-0'`; unknown type names fall through to a
  blue `CheckSquare`. Reference usage: `BacklogRow.tsx:99`, `PinnedTabStrip.tsx:177`.

- `RecentItem` — `taskflow/src/stores/recent-items.store.ts`
  `{ type: 'jira' | 'gitlab'; id: string; url?: string; title?: string; timestamp: number }`
  `pushItem(item: Omit<RecentItem, 'timestamp'>)` dedups by `type`+`id`, preserves the
  previous `title` when the caller omits it, caps at 10. Persisted via
  `createTauriStorage('recent-items.json')`, `version: 0`.

- `JiraIssue.fields.issuetype` — `taskflow/src/services/jira.ts:160`
  `{ id?: string; name: string; subtask: boolean }`

- gh-backlog cache (`['gh-backlog', boardId]`) holds a raw `GhBacklogResponse`:
  `.issues` are `GhIssue { key, summary, typeId, ... }` (type name is NOT on the issue;
  it lives in `entityData.types[typeId].typeName` — see
  `taskflow/src/services/jira/greenhopper/types.ts:26` and `entityMaps.ts:98`).

Jira push call sites (only two): `main.tsx:467`, `IssueDetailView.tsx:214`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist issue type on recent items</name>
  <files>taskflow/src/stores/recent-items.store.ts, taskflow/src/stores/recent-items.store.test.ts, taskflow/src/routes/dashboard/IssueDetailView.tsx, taskflow/src/main.tsx</files>
  <behavior>
    - pushItem with `issueType: 'Bug'` stores that value on the item
    - pushItem for an existing type+id WITHOUT issueType preserves the previously stored issueType (same preservation rule as `title`)
    - pushItem for an existing type+id WITH a new issueType overwrites the stored one
    - Items pushed without issueType leave the field undefined (no default value invented)
  </behavior>
  <action>
    Add an optional `issueType?: string` field to the `RecentItem` interface in
    `recent-items.store.ts`, documented as "Jira issue type name (Story/Bug/...) cached so
    the popover icon survives a cold react-query cache". Extend the `pushItem` reducer to
    preserve it across dedup using exactly the pattern already used for `title`
    (`const issueType = item.issueType ?? existing?.issueType;` then spread conditionally so
    an undefined value never writes an explicit `undefined` key). Do NOT bump the persist
    `version` or add a migration — the field is optional and the existing
    `migrate` passthrough handles old payloads.

    Add the behavior cases above to `recent-items.store.test.ts` following the existing
    describe/renderHook/act style in that file.

    Update the two Jira push call sites to supply the type:
    - `IssueDetailView.tsx:214` — pass `issueType: issue.fields.issuetype?.name`. Add
      `issue.fields.issuetype?.name` to that effect's dependency array alongside
      `issue?.fields.summary`.
    - `main.tsx:467` — the `handleIssueClick` helper already walks the react-query caches to
      resolve `resolvedTitle`. Resolve `resolvedTypeName` in the SAME passes (no extra cache
      scans): from the `jira-issues` and `jira-issue-detail` entries read
      `match.fields?.issuetype?.name`; from the `gh-backlog` envelope read
      `entityData?.types?.[match.typeId]?.typeName` (widen the local generic to include
      `typeId` on issues and an `entityData.types` record). Pass
      `issueType: resolvedTypeName` to `pushRecentItem`. Leave the two GitLab push sites
      untouched.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/stores/recent-items.store.test.ts</automated>
  </verify>
  <done>Store tests pass including the three new issueType cases; both Jira push sites pass a type name.</done>
</task>

<task type="auto">
  <name>Task 2: Render the type icon in RecentItemsPopover rows</name>
  <files>taskflow/src/components/app/RecentItemsPopover.tsx</files>
  <action>
    In `RecentItemRow`, replace the per-row `<Clock className="w-4 h-4 ..." />` leading glyph
    with a type icon in the same leading slot (do not change row layout, gap, or density
    classes):

    - `item.type === 'jira'`: resolve the type name as
      `cachedTypeName ?? item.issueType`. `cachedTypeName` comes from the existing
      `findJiraIssueInCache` result — extend that helper to also surface the type name:
      for full `JiraIssue` hits return `fields.issuetype?.name`; for the gh-backlog branch
      resolve `entityData?.types?.[issue.typeId]?.typeName` and carry it on the existing
      `isPartial: true` variant as an optional `typeName` field (keep the WR-02
      discriminated-union guarantee — do NOT cast the partial to `JiraIssue`).
      Render `<IssueTypeIcon typeName={resolvedType} />` when a name resolved.
    - `item.type === 'gitlab'`: render lucide `GitMerge` with
      `className="w-3.5 h-3.5 shrink-0 text-muted-foreground"`.
    - Unresolved Jira type (no cache hit, no persisted `issueType`): keep the existing
      `Clock` glyph as the fallback so an unknown item is never mislabeled as a Task by
      `IssueTypeIcon`'s default branch.

    Import `IssueTypeIcon` from `@/components/ui/issue-type-icon`. Keep the `Clock` import
    (still used by the popover trigger and the fallback). Update the file's top-of-file
    doc comment to mention the type icon.
  </action>
  <verify>
    <automated>cd taskflow && npm run check &amp;&amp; npx vitest run src/stores/recent-items.store.test.ts src/components/app/CommandPalette.test.tsx</automated>
  </verify>
  <done>`npm run check` (biome + tsc) is clean, no new biome-flagged files, and the popover renders IssueTypeIcon for Jira rows / GitMerge for MR rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Recent Items popover rows now lead with the issue-type icon (Jira) or a merge icon (MRs) instead of a repeated clock.</what-built>
  <how-to-verify>
    1. Run `cd taskflow && npm run tauri dev`.
    2. Open a Story, a Bug, a Sub-task, and one merge request so they land in recents.
    3. Click the clock button in the top bar.
    4. Expect: green book (Story), red bug (Bug), blue corner-arrow (Sub-task), and a merge
       icon on the MR row — icon sizes match the pinned-tab strip, single line per row,
       no layout shift or truncation change.
    5. Restart the app (cold react-query cache) and reopen the popover — icons must still
       be correct from the persisted `issueType`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jira/GitLab API → react-query cache → UI | Server-controlled `issuetype.name` / `typeName` strings rendered in the popover |
| Persisted store file (`recent-items.json`) → UI | Locally-persisted, user-writable JSON deserialized into `RecentItem[]` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1k3-01 | Tampering | `recent-items.json` `issueType` value | accept | Value is only compared in a `switch` inside `IssueTypeIcon` and never rendered as text or HTML; an unknown string hits the default branch |
| T-1k3-02 | Information disclosure | Popover row rendering | accept | Type name is already visible on the issue itself; no new data class surfaced |
| T-1k3-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies — `lucide-react` and `IssueTypeIcon` already in the tree; no install task in this plan |
</threat_model>

<verification>
- `cd taskflow && npm run check` clean (biome + tsc); gate on "no NEW files flagged", not an absolute diagnostic count.
- `cd taskflow && npx vitest run src/stores/recent-items.store.test.ts` passes.
- `grep -v '^\s*[/*]' taskflow/src/components/app/RecentItemsPopover.tsx | grep -c 'IssueTypeIcon'` ≥ 2 (import + usage).
</verification>

<success_criteria>
- Jira recent rows show the correct `IssueTypeIcon`; MR rows show a merge icon.
- Type name persists in `recent-items.json` and drives the icon on a cold start.
- No new icon mapping introduced — `IssueTypeIcon` is the single source of truth.
- No changes to row height, density classes, or the one-row-one-line rule.
</success_criteria>

<output>
Create `.planning/quick/260813-1k3-in-the-recently-visited-also-put-the-typ/260813-1k3-SUMMARY.md` when done
</output>

