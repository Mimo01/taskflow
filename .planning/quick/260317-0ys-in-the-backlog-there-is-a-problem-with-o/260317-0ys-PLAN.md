---
phase: quick
plan: 260317-0ys
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/BacklogPage.tsx
  - taskflow/src/routes/dashboard/BacklogRow.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Long epic names and summaries do not cause horizontal overflow in the backlog table"
    - "Epic badge text truncates with ellipsis when too long"
    - "Summary text truncates with ellipsis when too long"
  artifacts:
    - path: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      provides: "table-fixed layout on backlog table"
    - path: "taskflow/src/routes/dashboard/BacklogRow.tsx"
      provides: "Overflow-safe epic and summary cells"
  key_links: []
---

<objective>
Fix horizontal overflow in the backlog table when epic names or issue summaries are too long.

Purpose: Long epic badges or summary text push the table wider than its container, causing unwanted horizontal scrolling.
Output: Backlog table enforces fixed column widths; epic and summary cells truncate with ellipsis.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/dashboard/BacklogPage.tsx
@taskflow/src/routes/dashboard/BacklogRow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix backlog table overflow with table-fixed and proper truncation</name>
  <files>taskflow/src/routes/dashboard/BacklogPage.tsx, taskflow/src/routes/dashboard/BacklogRow.tsx</files>
  <action>
**BacklogPage.tsx** (line ~343):
- Change `<table className="w-full text-sm">` to `<table className="w-full text-sm table-fixed">` to enforce column widths.
- The Summary `<th>` (currently no width) will auto-fill remaining space, which is correct with table-fixed.

**BacklogRow.tsx**:
- Epic cell (line ~95): Add `overflow-hidden` to the `<td>` so `className="w-32 px-2 py-2 density-compact:py-1 density-comfortable:py-3 overflow-hidden"`. The button inside already has `truncate max-w-full` which will now work correctly since the td enforces 8rem.
- Summary cell (line ~114): Add `overflow-hidden` to the `<td>` so `className="px-2 py-2 density-compact:py-1 density-comfortable:py-3 overflow-hidden"`. Change the inner `<span>` from `className="text-sm text-left truncate w-full"` to `className="block text-sm text-left truncate"` -- the span must be `block` (not inline) for `truncate` (text-overflow: ellipsis) to work, and remove `w-full` since `block` auto-fills width.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Backlog table uses table-fixed layout. Epic and summary cells have overflow-hidden on td elements. Summary span is block-level with truncate. No horizontal overflow occurs with long text.</done>
</task>

</tasks>

<verification>
- Open the backlog page with issues that have long epic names or summaries
- Verify no horizontal scrollbar appears
- Verify epic badges truncate with ellipsis
- Verify summary text truncates with ellipsis
- All other columns (key, points, status, assignee) remain properly sized
</verification>

<success_criteria>
- table-fixed applied to backlog table
- Epic and summary cells truncate long text without causing overflow
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260317-0ys-in-the-backlog-there-is-a-problem-with-o/260317-0ys-SUMMARY.md`
</output>
