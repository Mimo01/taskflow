---
phase: quick-260531-qx3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
autonomous: true
requirements: [QX3-01, QX3-02, QX3-03]
must_haves:
  truths:
    - "When a GitLab milestone is matched and has MRs, the release page shows a compact MR-state distribution (merged/open/closed counts) per D-01"
    - "When a milestone is matched with MRs, a contributor section lists unique MR authors as avatars with names per D-02"
    - "The Issues section shows a status distribution (new / in progress / done) derived from loaded issues per D-03"
    - "When at least one issue carries a positive story-point value, an effort line shows completed-vs-total story points per D-03; otherwise it is omitted"
    - "All new sections render nothing when their underlying data is absent (no '—' placeholders, no zero-states)"
    - "npm run check (biome + tsc) is clean after the change"
  artifacts:
    - path: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      provides: "MR-state distribution, contributor list, issue status distribution + story-point effort line, plus customfield_10016 in fetch fields"
      contains: "customfield_10016"
  key_links:
    - from: "fetchFixVersionIssues fields string"
      to: "issue.fields.customfield_10016"
      via: "added field in fields= list"
      pattern: "summary,status,assignee,issuetype,customfield_10016"
    - from: "MR-state + contributor sections"
      to: "releaseMrs / gitlabMatch.type !== 'none'"
      via: "conditional render guard"
      pattern: "gitlabMatch\\.type !== 'none'"
    - from: "status distribution + effort"
      to: "releaseIssues[].fields.status.statusCategory / customfield_10016"
      via: "derived counts"
      pattern: "statusCategory\\?\\.key"
---

<objective>
Surface three already-available info groups on the release detail page: MR state distribution, contributor list, and issue status distribution + story-point effort — reusing existing components and patterns. All groups hide gracefully when their data is absent.

Purpose: Make the release page show data already loaded (GitLab MR state/author, Jira issue status) plus story points (one fetch-field addition), without new queries.
Output: An updated `ReleaseDetailPage.tsx` with the three new info groups and `customfield_10016` added to the issue fetch fields.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260531-qx3-i-want-to-add-more-info-to-the-release-d/260531-qx3-CONTEXT.md
@.planning/quick/260531-qx3-i-want-to-add-more-info-to-the-release-d/260531-qx3-RESEARCH.md
@taskflow/src/routes/dashboard/ReleaseDetailPage.tsx

Interface notes (verified from source — do NOT re-explore):
- `releaseMrs` (line 324) = `milestoneMRs ?? []`, type `GitLabMR[]`. Each MR: `state: 'opened'|'closed'|'merged'|'locked'`, `author: { id: number; name: string; username: string; avatar_url: string }`.
- `releaseIssues` (line 323) = `fixVersionIssues ?? []`, type `JiraIssue[]`. Each issue: `fields.status.statusCategory?: { key: 'new'|'indeterminate'|'done' }`, `fields.customfield_10016: number | null`.
- Graceful-hide guard for MR-derived sections: `gitlabMatch.type !== 'none'` AND `milestoneMRs` loaded AND `releaseMrs.length > 0` (mirror existing label-summary guard at line 628).
- Components: `Badge` from `@/components/ui/badge` — props `tone?: ChipTone` (`'blue'|'green'|'red'|'orange'|'amber'|'purple'|'muted'`) + `variant?`; `CachedAvatar` from `@/components/ui/cached-avatar` — `url`, `name`, `size?: 20|24|32|40`; both already imported.
- `ChipTone` has NO `gray` — use `tone="muted"` for closed MRs (CONTEXT says gray=closed). green=merged, blue=opened.
- `Users` icon is NOT imported — add it to the lucide-react import block (lines 13-28). `GitMerge` already imported.
- Existing dedupe pattern to mirror: `labelMap` aggregation (lines 343-360). Existing avatar usage: lines 872-877. Existing Progress usage: lines 664-670. `MetaRow` (line 1262): `label` + `children`.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add story-points field and derive the three info-group datasets</name>
  <files>taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <behavior>
    - fetchFixVersionIssues now requests customfield_10016, so loaded issues carry story points when configured.
    - mrStateCounts: { merged, opened, closed } where closed buckets everything that is not merged/opened (incl. 'locked') — exhaustive, no switch.
    - contributors: unique MR authors deduped by author.id (Map<number, author>), only when releaseMrs is non-empty.
    - issueStatusCounts: { new, indeterminate, done } over releaseIssues using statusCategory?.key with a default bucket for undefined.
    - storyPoints: { total, completed } summing customfield_10016 guarded by typeof === 'number'; hasStoryPoints = some issue has a number > 0.
  </behavior>
  <action>In the `fields` constant inside `fetchFixVersionIssues` (line 108), change the string from `'summary,status,assignee,issuetype'` to `'summary,status,assignee,issuetype,customfield_10016'` (per D-03 / RESEARCH gap). Do NOT touch the React Query key (line 297) — it does not include fields, which is intentionally safe.

Add `Users` to the lucide-react import block (lines 13-28), keeping alphabetical order.

In the component body, after the existing `labelCoverage` block (around line 372) and before `startEditing`, derive four memo-free constants (plain consts, recomputed each render like the existing `labelMap`/`labelCoverage`):
  1. `mrStateCounts` — iterate `releaseMrs`: count `merged` when `mr.state === 'merged'`, `opened` when `mr.state === 'opened'`, else increment `closed` (this folds `'closed'` and `'locked'` together — keeps the math exhaustive per RESEARCH pitfall, avoids a non-exhaustive switch). Result shape `{ merged: number; opened: number; closed: number }`.
  2. `contributors` — build a `Map<number, GitLabMR['author']>` keyed on `author.id` over `releaseMrs` (mirror the `labelMap` shape, lines 343-360), then `Array.from(map.values())`. Sort by `author.name.localeCompare`.
  3. `issueStatusCounts` — iterate `releaseIssues`: bucket by `issue.fields.status.statusCategory?.key`; use a default of `'new'` for undefined (per RESEARCH — statusCategory is optional). Result shape `{ new: number; indeterminate: number; done: number }`.
  4. `storyPoints` — `total` = sum of `i.fields.customfield_10016` across `releaseIssues` guarded by `typeof i.fields.customfield_10016 === 'number'`; `completed` = same sum but only for issues whose `statusCategory?.key === 'done'`. Also compute `hasStoryPoints = releaseIssues.some(i => typeof i.fields.customfield_10016 === 'number' && i.fields.customfield_10016 > 0)` (per RESEARCH graceful-hide guard). Guard arithmetic with `typeof === 'number'` — never coerce null (RESEARCH pitfall, keeps tsc/biome clean).

Do NOT render anything in this task — only the data derivations and the two import/fetch edits. This keeps the task self-contained and the next task purely presentational.</action>
  <verify>
    <automated>cd taskflow && npx tsc --noEmit 2>&1 | head -20; grep -c 'summary,status,assignee,issuetype,customfield_10016' src/routes/dashboard/ReleaseDetailPage.tsx</automated>
  </verify>
  <done>`customfield_10016` is present in the fetch fields string; `mrStateCounts`, `contributors`, `issueStatusCounts`, `storyPoints`/`hasStoryPoints` consts exist and compile; `Users` imported; `npx tsc --noEmit` reports no new errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render the three info groups with graceful hiding</name>
  <files>taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <behavior>
    - MR-state distribution renders only when gitlabMatch.type !== 'none' && milestoneMRs && releaseMrs.length > 0; shows merged/opened/closed counts as Badge tone=green/blue/muted, omitting zero buckets.
    - Contributor section renders under the same guard; shows "N contributors" header with Users icon and a wrapping row of CachedAvatar + name.
    - Issue status distribution renders when releaseIssues.length > 0; three counts (new/in progress/done) as Badge tone, omitting zero buckets.
    - Effort line renders only when hasStoryPoints; shows "Story points: {completed} / {total}". No "—" placeholders anywhere.
  </behavior>
  <action>Add the three info groups, choosing placement per CONTEXT (Claude's discretion):

1. MR state distribution (D-01): Render a sidebar `MetaRow label="MRs"` placed in the right-sidebar metadata block (after the existing `MR Labels` MetaRow, around line 1075). Guard the entire MetaRow with `gitlabMatch.type !== 'none' && milestoneMRs && releaseMrs.length > 0` — when false, render NOTHING (do NOT add a "—" row; CONTEXT overrides the existing MR-Labels "—" pattern for new sections). Inside, render small `Badge` counts only for non-zero buckets: `tone="green"` for `{mrStateCounts.merged} merged`, `tone="blue"` for `{mrStateCounts.opened} open`, `tone="muted"` for `{mrStateCounts.closed} closed`. Use `GitMerge` icon optionally for visual consistency. Tabular-nums class for the numbers.

2. Contributor list (D-02): Render a left-column `<section>` placed after the Labels section (after line 650, before the Issues section at 653), mirroring the Labels section header style. Guard with the same MR condition AND `contributors.length > 0`. Header: `<Users className="size-3.5" />` + "Contributors" with a count Badge (`{contributors.length}`). Body: a `flex flex-wrap gap-2` row of `<span class="inline-flex items-center gap-1.5 text-xs">` each containing `<CachedAvatar url={c.avatar_url} name={c.name} size={20} />` + `{c.name}` (exact pattern from lines 872-877).

3. Issue status distribution + effort (D-03): Render inside the existing Issues `<section>`, directly under the existing Progress bar (after line 670, before the milestone warning at 672). Guard with `releaseIssues.length > 0`. Show a `flex flex-wrap gap-1.5` row of `Badge` counts for non-zero buckets: `tone="blue"`/secondary for `{issueStatusCounts.new} new`, `tone="amber"` for `{issueStatusCounts.indeterminate} in progress`, `tone="green"` for `{issueStatusCounts.done} done`. Below it, render the effort line ONLY when `hasStoryPoints`: a small line "Story points: {storyPoints.completed} / {storyPoints.total}" (tabular-nums, text-xs text-muted-foreground). Omit the effort line entirely when `hasStoryPoints` is false (per CONTEXT) but still show the status distribution.

Match existing density: text-xs, gap-1.5/gap-2, muted-foreground headers. Do not overcrowd the sidebar — only the compact MR-state counts go there; richer groups (contributors, status distribution) go in the left column per RESEARCH placement recommendation.</action>
  <verify>
    <automated>cd taskflow && npm run check 2>&1 | tail -20</automated>
  </verify>
  <done>The three groups render under their guards; zero buckets and absent-data cases render nothing (no "—"); `npm run check` (biome + tsc) is clean.</done>
</task>

</tasks>

<verification>
- `npm run check` passes (biome check + tsc) — the locked baseline stays green.
- MR-state distribution, contributor list, and issue status distribution render when their data is present; nothing renders when it is absent.
- Effort line appears only when at least one issue has a positive story-point value.
- No new React Query was added; only the issue `fields=` string changed.
- No milestone-timeline group was added (explicitly out of scope).
</verification>

<success_criteria>
- D-01 MR state distribution: COVERED (Task 1 counts, Task 2 sidebar badges).
- D-02 contributor list: COVERED (Task 1 dedupe, Task 2 left-column avatars).
- D-03 issue breakdown + effort: COVERED (Task 1 status counts + story-point sum and customfield_10016 fetch, Task 2 distribution badges + conditional effort line).
- Graceful hide: COVERED (all sections conditionally rendered, no "—" placeholders).
- Component reuse: COVERED (Badge tone, CachedAvatar, MetaRow, existing patterns).
- `npm run check` clean: COVERED (verify steps run it).
</success_criteria>

<output>
Create `.planning/quick/260531-qx3-i-want-to-add-more-info-to-the-release-d/260531-qx3-SUMMARY.md` when done
</output>
