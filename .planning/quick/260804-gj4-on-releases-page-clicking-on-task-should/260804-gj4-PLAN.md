---
phase: quick-260804-gj4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
autonomous: false
requirements: [QUICK-GJ4]
must_haves:
  truths:
    - "Clicking anywhere on a task row in the release issues table opens the PeekPanel preview (side panel), not a full page navigation"
    - "Clicking the task KEY in that row navigates to the full /issue/{key} page and does NOT open the peek"
    - "The breadcrumb trail pushed on full navigation still carries the release name (so Back returns to the release)"
    - "The MR link buttons inside the row still open the MR externally and do not open the peek"
    - "Release rows on /releases still navigate to /release/{versionId} (unchanged)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      provides: "Peek-on-row / full-nav-on-key click behavior for the release issues table"
      contains: "useOutletContext"
  key_links:
    - from: "taskflow/src/routes/dashboard/ReleaseDetailPage.tsx"
      to: "Outlet context onOpenIssue (main.tsx handleOpenPeek)"
      via: "useOutletContext destructure + row onClick"
      pattern: "onOpenIssue"
---

<objective>
On the release detail page (`/release/:versionId`), the issues table currently makes the ENTIRE row navigate to `/issue/{key}` — inconsistent with the rest of the app, where a row click opens the PeekPanel preview and only the task key navigates full-page.

Fix the issues table in `ReleaseDetailPage.tsx` to mirror the `BacklogRow.tsx` pattern:
- whole row click → `onOpenIssue(key)` (peek preview)
- task key click → `e.stopPropagation()` + full navigation to `/issue/{key}`

Purpose: consistent issue-opening interaction across every list surface in the app.
Output: modified `ReleaseDetailPage.tsx`.

**Scoping note (verified during planning):** `ReleasesTab.tsx` (the `/releases` list) contains ONLY release rows, no task rows — its `handleReleaseClick` → `navigate('/release/{id}')` is correct release-level navigation and MUST NOT be touched. The actual task rows the bug refers to live in `ReleaseDetailPage.tsx` lines 807-818.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mimo/Documents/Projects/taskflow/taskflow/src/routes/dashboard/ReleaseDetailPage.tsx
@/Users/mimo/Documents/Projects/taskflow/taskflow/src/routes/dashboard/BacklogRow.tsx
@/Users/mimo/Documents/Projects/taskflow/taskflow/src/routes/dashboard/BacklogPage.tsx
@/Users/mimo/Documents/Projects/taskflow/taskflow/src/main.tsx

**Interface contract already wired in `main.tsx` (do not change main.tsx):**
- `<Outlet context={{ onIssueClick: handleIssueClick, onOpenIssue: handleOpenPeek, ... }} />` at main.tsx ~line 628-639.
- `handleOpenPeek(issueKey: string) => void` sets `peekIssueKey`, which mounts `<PeekPanel>` beside the `<main>` outlet (main.tsx ~641-659). `ReleaseDetailPage` is rendered inside that same Outlet (`routes.tsx:46`), so the peek works there with no extra wiring.
- Consumer precedent: `BacklogPage.tsx:317` — `const { onIssueClick, onOpenIssue, openCreateStory } = useOutletContext<{...}>();`
- `EpicsPage.tsx:93` shows the defensive `useOutletContext<...>() ?? {}` form; use that so the component still renders if mounted outside an Outlet (tests).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Row click opens peek, key click navigates full-page in the release issues table</name>
  <files>taskflow/src/routes/dashboard/ReleaseDetailPage.tsx</files>
  <action>
Import `useOutletContext` from `react-router-dom` (add to the existing `useNavigate, useParams` import on line 32).

Inside `ReleaseDetailPage`, near the other hook calls (after `useNavigate`/`useQueryClient`, ~line 145), destructure the outlet context defensively, mirroring EpicsPage: read `onOpenIssue` typed as an optional `(key: string) => void` from `useOutletContext<{ onOpenIssue?: (key: string) => void; [key: string]: unknown }>() ?? {}`.

Extract the existing full-navigation body of the `<tr onClick>` (currently lines 811-814) into a named local handler — e.g. `openIssueFull(issueKey: string)` — that keeps the CURRENT behavior verbatim: `breadcrumbPush({ path: `/release/${versionId}`, label: version.name })` then `navigate(`/issue/${issueKey}`)`. Do NOT swap this for the outlet's `onIssueClick`: the outlet's `routeLabel()` maps `/release/` to the generic literal "Release", whereas the local push carries the actual release name in the breadcrumb — preserving it is a must-have.

Note `version` is only in scope inside the `isLoading || !version ? ... : (...)` branch that contains the table, so define `openIssueFull` where `version` is narrowed non-null (or capture `version.name` at the call site) — do not introduce a non-null assertion.

Change the `<tr>` (line 808) `onClick` to open the preview instead of navigating: call `onOpenIssue` with `row.issue.key` when it is available, and fall back to `openIssueFull(row.issue.key)` when the context did not supply it, so the row is never dead. Keep `key`, `className`, and all cell markup unchanged.

Change the Key cell (lines 816-818) to render the key as an inner `<button type="button">` around `{row.issue.key}`, exactly following the BacklogRow precedent at BacklogRow.tsx:105-117: its `onClick` receives the event, calls `e.stopPropagation()` first, then `openIssueFull(row.issue.key)`. Keep the cell `<td>` classes as-is and give the button the font-mono/text-xs/text-primary look it currently has plus `hover:underline cursor-pointer`, so the key remains visually a link and the row's peek handler does not also fire.

Leave every other interactive element in the row untouched — the MR `<button>`s at lines 853-868 and 897-912 already call `e.stopPropagation()`, which now correctly suppresses the peek as well. Leave the "Unmatched MRs" section (lines 940-1029) untouched: those are MR rows, not task rows, and its inline key buttons already stopPropagation + navigate full-page.

Do NOT modify `ReleasesTab.tsx`, `main.tsx`, or `routes.tsx`.
  </action>
  <verify>
    <automated>cd taskflow && npm run check && npm test -- src/routes/dashboard</automated>
    <automated>cd taskflow && grep -v '^\s*[/*]' src/routes/dashboard/ReleaseDetailPage.tsx | grep -c 'useOutletContext' | grep -qv '^0$'</automated>
    <automated>cd taskflow && grep -v '^\s*[/*]' src/routes/dashboard/ReleasesTab.tsx | grep -c 'handleReleaseClick' | grep -q '^3$'</automated>
  </verify>
  <done>`npm run check` passes clean (biome + tsc); the dashboard test suite passes; `ReleaseDetailPage.tsx` consumes `onOpenIssue` via `useOutletContext`; the `<tr>` onClick no longer calls `navigate('/issue/...')` directly; the key cell contains a `stopPropagation` button that does; `ReleasesTab.tsx` is byte-identical to before.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Release detail issues table now behaves like Backlog and Sprint Board: row click previews, key click opens full view.</what-built>
  <how-to-verify>
1. Run `cd taskflow && npm run tauri dev` (or the already-running dev app).
2. Navigate to Releases, click any release row → you should still land on the full release detail page (`/release/{id}`). This must be unchanged.
3. In the "Issues" table, click anywhere on a task row that is NOT the key (e.g. the Summary or Assignee cell) → the PeekPanel preview should slide in on the right; the URL should stay on `/release/{id}`.
4. With the peek open, click a different task row → the peek should swap to that issue without navigating away.
5. Click the task KEY (leftmost mono cell) → you should navigate to `/issue/{KEY}` full page, and NO peek should be open.
6. On that full issue page, use the back arrow / breadcrumb → it should return to the release, and the breadcrumb should show the release NAME (e.g. "Releases / v2.4.0"), not the generic word "Release".
7. Click an MR link (`!1234`) inside a task row → it should open the MR in the browser and NOT open the peek.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what misbehaved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jira API → renderer | Issue keys rendered into click handlers and route paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-gj4-01 | Tampering | `navigate('/issue/' + key)` in ReleaseDetailPage | accept | Key comes from the authenticated Jira search response and only feeds an in-app react-router path; no eval, no external URL construction. Behavior is unchanged from current code. |
| T-gj4-02 | Elevation of Privilege | PeekPanel opened with an arbitrary key | accept | PeekPanel refetches the issue through the same authenticated Jira client; no additional authority is granted by previewing versus full-page viewing. |
| T-gj4-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies are installed by this plan; if any install becomes necessary, stop and raise a blocking human checkpoint. |
</threat_model>

<verification>
- `cd taskflow && npm run check` — biome + tsc clean (repo baseline is GREEN; any new error is from this change).
- `cd taskflow && npm test -- src/routes/dashboard` — existing dashboard tests, including `ReleasesTab.test.tsx`, still pass.
- `git diff --stat` shows exactly one modified file: `taskflow/src/routes/dashboard/ReleaseDetailPage.tsx`.
</verification>

<success_criteria>
- Task-row click on `/release/:versionId` opens the PeekPanel preview instead of navigating.
- Task-key click navigates to `/issue/{key}` with the release-name breadcrumb preserved and no peek open.
- MR buttons and the Unmatched MRs section behave exactly as before.
- `/releases` release-row navigation is untouched.
- `npm run check` and the dashboard test suite pass.
</success_criteria>

<output>
Create `.planning/quick/260804-gj4-on-releases-page-clicking-on-task-should/260804-gj4-SUMMARY.md` when done
</output>
