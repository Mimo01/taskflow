---
phase: quick-260606-w2l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/standup-notes/YesterdayColumn.tsx
  - taskflow/src/routes/standup-notes/IssueActivityGroup.tsx
  - taskflow/src/routes/standup-notes/YesterdayColumn.test.ts
autonomous: true
requirements: [STANDUP-SUBTASK-SUBGROUP]

must_haves:
  truths:
    - "On the Yesterday 'Worked On' column, activity attributable to a sub-task nests under a clickable sub-task sub-header within its parent story group"
    - "Story-level activity (commits/MRs keyed to the story, story worklogs, transitions/comments) renders flat directly under the story header, above any sub-task sub-groups"
    - "A sub-task sub-header is only rendered when that sub-task has at least one activity item (never an empty header)"
    - "A story with no sub-task activity renders exactly as it does today (no regression)"
    - "The Copy-markdown export mirrors the nesting: story heading → story-level bullets → 2-space-indented sub-task line → 4-space-indented sub-task bullets"
    - "Commits and MR-comments attributable to a sub-task show under that sub-task, not lumped at the story level"
    - "The Today 'In Progress' column is audited and confirmed already-nesting (no change required)"
  artifacts:
    - path: "taskflow/src/routes/standup-notes/YesterdayColumn.tsx"
      provides: "buildGroups origin-key tagging + partition pass producing IssueGroup.subTaskGroups; generateMarkdown nested emit"
      contains: "subTaskGroups"
    - path: "taskflow/src/routes/standup-notes/IssueActivityGroup.tsx"
      provides: "Nested sub-task sub-group rendering with clickable sub-headers + onOpenIssue prop"
      contains: "subTaskGroups"
    - path: "taskflow/src/routes/standup-notes/YesterdayColumn.test.ts"
      provides: "Tests for sub-task nesting (worklog/commit/MR), markdown mirror, and no-regression cases"
      contains: "subtask"
  key_links:
    - from: "taskflow/src/routes/standup-notes/YesterdayColumn.tsx"
      to: "taskflow/src/routes/standup-notes/IssueActivityGroup.tsx"
      via: "subTaskGroups prop on IssueActivityGroup"
      pattern: "subTaskGroups="
    - from: "buildGroups partition pass"
      to: "issueMeta sub-task metadata"
      via: "meta.isSubtask && meta.parentKey === group.issueKey"
      pattern: "isSubtask"
---

<objective>
Add a second level of grouping to the Standup Notes Yesterday "Worked On" column: activity attributable to a specific sub-task nests under a clickable sub-task sub-header within its parent story group, while story-level activity stays flat directly under the story header. Mirror the nesting in the Copy-markdown export. Audit the Today column (already nests — verify, no change expected).

Purpose: Sub-task worklogs/commits/MR-activity currently roll up FLAT to the parent story and lose sub-task attribution. This restores attribution as a clean nested view.
Output: Refactored `buildGroups` (origin-key tagging + partition pass + per-(group,origin) count aggregation), nested rendering in `IssueActivityGroup`, nested markdown emit, and extended tests.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260606-w2l-standup-subtask-subgrouping/260606-w2l-CONTEXT.md
@.planning/quick/260606-w2l-standup-subtask-subgrouping/260606-w2l-RESEARCH.md
@taskflow/src/routes/standup-notes/YesterdayColumn.tsx
@taskflow/src/routes/standup-notes/IssueActivityGroup.tsx
@taskflow/src/routes/standup-notes/TodayInProgressSection.tsx
@taskflow/src/routes/standup-notes/YesterdayColumn.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor buildGroups — origin-key tagging + sub-task partition pass</name>
  <files>taskflow/src/routes/standup-notes/YesterdayColumn.tsx, taskflow/src/routes/standup-notes/IssueActivityGroup.tsx</files>
  <behavior>
    - A sub-task worklog (issueMeta marks it isSubtask with parentKey = story) produces a `subTaskGroups` entry on the parent story group keyed by the sub-task key, holding the worklog SubItem; `group.subItems` (story-level) does NOT contain it.
    - A commit whose extracted Jira key is a sub-task of a story aggregates into a per-(story, subtask) count and lands in that sub-task's sub-group as "N commits".
    - An MR-comment whose extracted key is a sub-task aggregates per (story, subtask, mrIid) and lands in that sub-task's sub-group.
    - Story-level activity (origin === story key, OR origin has no meta / is not a sub-task / parent ≠ this story) stays in `group.subItems`.
    - When a story has no sub-task activity, `group.subTaskGroups` is an empty array and `group.subItems` is identical to today's output.
    - `subTaskGroups` is sorted by `issueKey` (stable order).
    - Sub-task summary falls back to the bare key when `issueMeta[subtaskKey]` is missing (graceful, no crash).
  </behavior>
  <action>
    In IssueActivityGroup.tsx: add an optional `originKey?: string` field to the `SubItem` interface (distinct from the existing `issueKey` which stays purely for click affordance, per RESEARCH Open Question 1). Export a new `SubTaskSubGroup` interface: `{ issueKey: string; summary: string; issueType?: string; subItems: SubItem[] }`.

    In YesterdayColumn.tsx: extend the internal `IssueGroup` interface with `subTaskGroups: SubTaskSubGroup[]` (import the type from IssueActivityGroup) and initialise it to `[]` in `ensureGroup`.

    Set `originKey` on EVERY SubItem at attach time to the ORIGIN issue key (worklog: `worklog.issue.key`; jira activity: `activity.issueKey`; commit: the extracted `key`; MR approval/comment: the extracted `key`). Keep the existing `issueKey` population for worklogs unchanged (click affordance). Do NOT thread buckets through the four source loops — keep pushing to `group.subItems` with `originKey` set.

    Change commit aggregation from `commitCountByGroup` keyed by `group.issueKey` to a composite key `${group.issueKey}::${originKey}` (the originKey being the extracted commit key), so commit counts are per-(group, origin). Same for `keyedCommentCounts`: nest by composite `(group.issueKey, originKey)` so MR-comment counts attribute to the sub-task. When emitting these aggregated SubItems, set their `originKey` accordingly.

    Add ONE partition pass after the four source loops, before the return (follow RESEARCH "Partition pass" shape): for each group, split `group.subItems` into story-level vs `Map<subtaskKey, SubItem[]>`. An item belongs to a sub-task when `origin && origin !== group.issueKey && meta?.isSubtask && meta.parentKey === group.issueKey` (meta from `issueMeta?.[origin]`); otherwise it is story-level. Reassign `group.subItems` to the story-level list and build `group.subTaskGroups` from the map: `{ issueKey, summary: issueMeta?.[key]?.summary ?? key, issueType: issueMeta?.[key]?.type, subItems }`, sorted by `issueKey` ascending. Only non-empty sub-tasks become entries (the map only contains keys with items).

    Do NOT create a separate top-level IssueGroup per sub-task (RESEARCH anti-pattern — re-introduces flat-story regression). Do NOT render an empty sub-task header.
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/standup-notes/YesterdayColumn.test.ts</automated>
  </verify>
  <done>buildGroups tags every SubItem with originKey, aggregates commit/MR counts per (group, origin), and produces `IssueGroup.subTaskGroups` (sorted, non-empty only) via a single partition pass; existing YesterdayColumn tests still pass (the "subtask worklog under parent" test may need its assertions updated in Task 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Nested rendering in IssueActivityGroup + wire onOpenIssue through YesterdayColumn</name>
  <files>taskflow/src/routes/standup-notes/IssueActivityGroup.tsx, taskflow/src/routes/standup-notes/YesterdayColumn.tsx</files>
  <behavior>
    - When `subTaskGroups` is non-empty, each sub-group renders below the story's own sub-item list as a `pl-6 ml-2`-indented block: a clickable sub-task header (type icon + key button + summary) followed by its sub-items indented a further `pl-6 ml-2`.
    - Sub-task header body click calls the resolved open handler (onOpenIssue ?? onIssueClick) with the sub-task key; the key button calls onIssueClick(subtaskKey) and stops propagation.
    - When `subTaskGroups` is empty/undefined, output is identical to today (story header + flat sub-items only).
    - Sub-task header uses the documented `div[role=button]` wrapper with an inner `<button>` for the key (no nested-button invalid HTML).
  </behavior>
  <action>
    In IssueActivityGroup.tsx: add an optional `subTaskGroups?: SubTaskSubGroup[]` prop and an optional `onOpenIssue?: (key: string) => void` prop. After the existing story-level sub-item list, when `subTaskGroups && subTaskGroups.length > 0`, map each entry to an indented block wrapped in `pl-6 ml-2`. Each block renders a sub-task header reusing the EXACT existing header markup pattern (the `div[role=button]` + inner key `<button>` with `stopPropagation`, per Pitfall 1 / the existing lines ~91-116 and TodayInProgressSection IssueRow) — body click → `(onOpenIssue ?? onIssueClick)?.(st.issueKey)`, key button → `onIssueClick?.(st.issueKey)`. Below the header, render the sub-group's `subItems` using the SAME sub-item rendering logic as the story-level list (extract a small `SubItemList` helper inside the file to avoid duplicating the three-way clickable-MR / clickable-issue / plain branches), wrapped in a further `pl-6 ml-2`. Use `st.issueKey` as the React key for each sub-group block (stable, not array index — Pitfall 2).

    In YesterdayColumn.tsx render loop: pass `subTaskGroups={group.subTaskGroups}` and `onOpenIssue={onOpenIssue}` to each `IssueActivityGroup`. The existing `onClick`, `onIssueKeyClick`, `onIssueClick`, `onMRClick` props stay as-is.

    Keep the no-subtask-activity path visually identical to today (guard the entire nested block behind the length check).
  </action>
  <verify>
    <automated>cd taskflow && npm run check</automated>
  </verify>
  <done>IssueActivityGroup renders sub-task sub-groups (indented, clickable headers, further-indented items) only when present; YesterdayColumn passes subTaskGroups + onOpenIssue; `npm run check` (biome + tsc) is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Nested markdown emit + extend/adjust tests</name>
  <files>taskflow/src/routes/standup-notes/YesterdayColumn.tsx, taskflow/src/routes/standup-notes/YesterdayColumn.test.ts</files>
  <behavior>
    - generateMarkdown emits story-level `group.subItems` as `- label` (unchanged), then for each `subTaskGroups` entry emits `  - {issueKey}: {summary}` (2-space indent) followed by its items as `    - {label}` (4-space indent).
    - A story with no sub-task activity produces byte-identical markdown to today (no trailing nested block).
    - The existing "subtask worklog under parent" test is updated to ALSO assert the new nested `  - ESHOP-2: Wire up form` line; the worklog label format `1h · ESHOP-2 Wire up form` stays unchanged.
    - New tests cover: commit attributed to a sub-task nests under it; MR-comment attributed to a sub-task nests under it; a story with only story-level activity produces no nested block (regression guard).
  </behavior>
  <action>
    In generateMarkdown (YesterdayColumn.tsx): after emitting `group.subItems`, iterate `group.subTaskGroups` (follow RESEARCH "Markdown nested emit" shape): push `  - ${st.issueKey}: ${st.summary}` then for each `item` of `st.subItems` push `    - ${item.label}`. Keep existing worklog label format (do NOT change `formatDuration(...) · KEY summary`) to avoid churning unrelated assertions.

    In YesterdayColumn.test.ts: update the existing "groups a subtask worklog under its parent story but still lists the subtask" test to assert the nested line `  - ESHOP-2: Wire up form` is present (in addition to the existing `1h · ESHOP-2 Wire up form` and `### ESHOP-1` assertions, and still no `### ESHOP-2` heading). Add new tests: (a) a commit on a sub-task (commitsData with a message carrying the sub-task key + issueMeta marking it a sub-task of the story) produces `  - {subtaskKey}: {summary}` then `    - 1 commit`; (b) an MR-comment on a sub-task nests similarly; (c) a story with ONLY story-level activity (e.g. one story worklog, no sub-task meta) produces NO line starting with two-space `  - ` — regression guard that no nested block leaks. Keep `npx vitest run src/routes/standup-notes` fully green (including TodayColumn.markdown.test.ts, untouched).
  </action>
  <verify>
    <automated>cd taskflow && npx vitest run src/routes/standup-notes && npm run check</automated>
  </verify>
  <done>generateMarkdown mirrors the nesting with 2-space/4-space indents; the existing worklog test is updated for nesting; new commit/MR/no-regression tests pass; the full standup suite and `npm run check` are GREEN.</done>
</task>

</tasks>

<verification>
- `cd taskflow && npx vitest run src/routes/standup-notes` — full standup suite passes (Yesterday + Today markdown).
- `cd taskflow && npm run check` — biome + tsc GREEN (per MEMORY: `check`, not bare `lint`).
- Manual audit note: Today "In Progress" column (`TodayInProgressSection.tsx`) already nests sub-tasks under stories via `row.subtasks.map(... indented)` — it is sprint-membership-based and two-level already. No change required; record this in the SUMMARY.
</verification>

<success_criteria>
- Yesterday "Worked On" column nests sub-task-attributable activity under clickable sub-task sub-headers within the story group; story-level activity stays flat above the sub-groups.
- Sub-task sub-headers only appear when the sub-task has activity; stories without sub-task activity are unchanged.
- Copy-markdown export mirrors the nesting (2-space sub-task line, 4-space items).
- Commits and MR-comments attributable to a sub-task are counted/shown under that sub-task.
- Today column audited and confirmed unchanged.
- `npm run check` and the standup test suite are GREEN.
</success_criteria>

<output>
Create `.planning/quick/260606-w2l-standup-subtask-subgrouping/260606-w2l-SUMMARY.md` when done.
</output>
