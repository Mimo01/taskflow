---
phase: quick-260804-g1l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/IssueDetailContent.tsx
autonomous: false
requirements: [QUICK-260804-G1L]

must_haves:
  truths:
    - "On the issue detail page, an icon-only button sits immediately next to 'Open in Jira'"
    - "Clicking it copies the issue's Jira browse URL to the clipboard without opening a browser"
    - "The icon swaps to a check mark for ~2s after a successful copy, then reverts"
    - "The button is keyboard/screen-reader reachable via aria-label and shows a native title tooltip"
    - "'Open in Jira' still works unchanged"
  artifacts:
    - path: "taskflow/src/routes/dashboard/IssueDetailContent.tsx"
      provides: "Copy-Jira-link icon button + copied state + unmount-safe timer"
      contains: "navigator.clipboard.writeText"
  key_links:
    - from: "taskflow/src/routes/dashboard/IssueDetailContent.tsx"
      to: "navigator.clipboard"
      via: "onClick handler writing the browse URL"
      pattern: "navigator\\.clipboard\\.writeText"
---

<objective>
Add a small icon-only "Copy Jira link" sub-button next to the existing "Open in Jira"
button on the issue detail page. It copies the same URL that "Open in Jira" opens
(`${jiraBaseUrl.replace(/\/$/, '')}/browse/${issueKey}`) to the clipboard, with a brief
check-mark confirmation, and does not replace or alter the existing button.

Purpose: Sharing an issue link currently requires opening Jira in a browser and copying
from the address bar.
Output: Modified `IssueDetailContent.tsx` (single-file, additive UI change, no new deps).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@taskflow/src/routes/dashboard/IssueDetailContent.tsx
@taskflow/src/routes/standup-notes/StandupNotesPage.tsx
@taskflow/src/components/ui/button.tsx
</context>

<interface_notes>
Facts already verified — do not re-explore:

- `IssueDetailContent.tsx:3` already imports from lucide-react:
  `Copy, ExternalLink, LayoutList, Pencil, Pin, Plus`. **`Copy` is already taken by the
  Clone button** (line 464) — the new button must use a different icon (`Link2`) and
  `Check` for the success state. Extend the existing import line; do not add a second
  lucide-react import.
- `IssueDetailContent.tsx:4` already imports `useMemo, useState` from react. `useRef` and
  `useEffect` must be added to that same import for the unmount-safe timer.
- The button row is `IssueDetailContent.tsx:398-478`, a `<div className="flex justify-end gap-2">`.
  "Open in Jira" is the last child at lines 467-477.
- `Button` size variants available (`src/components/ui/button.tsx:22-33`):
  `default | xs | sm | lg | icon | icon-xs | icon-sm | icon-lg`. The sibling buttons use
  `size="sm"` (h-7). Use `size="icon-sm"` so the icon button matches that height.
- There is **no Tooltip component** in `src/components/ui/` — use the native `title`
  attribute (same as the Pin button at line 405).
- Clipboard pattern to mirror: `StandupNotesPage.tsx:495-550` — `copied` state + a
  `copiedTimer` ref, `setCopied(true)` only inside `.then()` (never optimistically), and a
  `useEffect` cleanup clearing the timer on unmount. Known prior review finding IN-01 was
  "setCopied setTimeout not cleared" — this plan must not repeat it.
</interface_notes>

<tasks>

<task type="auto">
  <name>Task 1: Add copy-Jira-link icon button to the issue detail action row</name>
  <files>taskflow/src/routes/dashboard/IssueDetailContent.tsx</files>
  <action>
    Extend the existing lucide-react import (line 3) with `Check` and `Link2`; extend the
    existing react import (line 4) with `useEffect` and `useRef`.

    Inside the `IssueDetailContent` component, next to the other hooks, add:
    a `copiedLink` boolean state (default false), a `copiedLinkTimer` ref holding
    `ReturnType<typeof setTimeout> | null`, and a `useEffect` with an empty dependency
    array whose cleanup clears the pending timeout.

    Add a `handleCopyJiraLink` callback that builds the URL exactly as the "Open in Jira"
    button does — `${jiraBaseUrl.replace(/\/$/, '')}/browse/${issueKey}` — and calls
    `navigator.clipboard.writeText(url)`. Set `copiedLink` to true only in the resolved
    `.then()` branch, clear any pending timer first, then start a 2000ms timeout that
    resets `copiedLink` to false and nulls the ref. In `.catch()`, do nothing (leave the
    button idle) — a rejected clipboard in the webview must not show a false success.

    Render a new `Button` as the last child of the `flex justify-end gap-2` row, placed
    immediately AFTER the existing "Open in Jira" button. Props:
    `variant="outline"`, `size="icon-sm"`, `onClick={handleCopyJiraLink}`,
    `aria-label="Copy Jira link"`, and `title={copiedLink ? 'Copied!' : 'Copy Jira link'}`.
    Its only child is the icon: `Check` with `className="size-3.5 text-primary"` when
    `copiedLink` is true, otherwise `Link2` with `className="size-3.5"`.

    Do not modify the existing "Open in Jira" button, the Clone button's `Copy` icon usage,
    or any other row member. Do not introduce a Tooltip component or any new dependency.
    Update the row comment on line 398 to include the new action.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npm run check</automated>
    <automated>grep -v '^\s*//' taskflow/src/routes/dashboard/IssueDetailContent.tsx | grep -c 'navigator.clipboard.writeText' # expect 1</automated>
    <automated>grep -c 'aria-label="Copy Jira link"' taskflow/src/routes/dashboard/IssueDetailContent.tsx # expect 1</automated>
    <automated>grep -c 'clearTimeout' taskflow/src/routes/dashboard/IssueDetailContent.tsx # expect >= 2 (handler + unmount cleanup)</automated>
  </verify>
  <done>
    `npm run check` (biome + tsc) passes clean; the icon button renders after "Open in Jira"
    with `aria-label="Copy Jira link"`; the copy handler writes the browse URL and only
    flashes success on a resolved clipboard promise; the timeout is cleared on unmount.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    An icon-only "Copy Jira link" button (Link2 icon) immediately to the right of
    "Open in Jira" in the issue detail action row. It copies the issue's Jira browse URL
    to the clipboard and swaps to a check mark for 2 seconds.
  </what-built>
  <how-to-verify>
    1. Run `cd taskflow &amp;&amp; npm run tauri dev` and open any issue's detail page.
    2. Confirm the action row reads: Pin | Log Work | Edit | Clone | Open in Jira | [icon].
       The icon button should be square and visually match the height of its neighbours,
       reading as a sub-action rather than a full button.
    3. Hover it — the native tooltip shows "Copy Jira link".
    4. Click it — the icon swaps to a check mark, then reverts to the link icon after ~2s.
    5. Paste into any text field — the clipboard contains
       `&lt;your-jira-base-url&gt;/browse/&lt;ISSUE-KEY&gt;` with no trailing-slash duplication,
       and no browser window opened.
    6. Click "Open in Jira" — it still opens the same URL in the browser as before.
    7. Tab to the icon button with the keyboard and press Enter — same copy behavior.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → OS clipboard | App-composed string written to the system clipboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-g1l-01 | Information Disclosure | clipboard write in `IssueDetailContent.tsx` | accept | Only the issue browse URL (base URL + key) is copied — same data already rendered in the UI and opened by "Open in Jira"; no tokens or PII |
| T-g1l-02 | Tampering | `jiraBaseUrl` / `issueKey` interpolation | accept | Both come from app settings and the routed issue, identical to the existing `openUrl` call; no new input source introduced |
| T-g1l-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies — `lucide-react` and the Web Clipboard API are already in use; no install step in this plan |
</threat_model>

<verification>
- `cd taskflow && npm run check` passes (biome check + tsc --noEmit)
- `cd taskflow && npm test` shows no new failures
- Human verification checkpoint approved
</verification>

<success_criteria>
- A "Copy Jira link" icon button sits next to "Open in Jira" on the issue detail page
- Clicking it copies `<jiraBaseUrl>/browse/<ISSUE-KEY>` to the clipboard without navigating
- Success feedback (check icon) appears for ~2s and the timer is cleared on unmount
- The button is labelled for assistive tech and reachable by keyboard
- "Open in Jira" and all other row actions are unchanged
- Exactly one file modified; zero new dependencies
</success_criteria>

<output>
Create `.planning/quick/260804-g1l-add-a-copy-link-sub-button-next-to-open-/260804-g1l-SUMMARY.md` when done
</output>
