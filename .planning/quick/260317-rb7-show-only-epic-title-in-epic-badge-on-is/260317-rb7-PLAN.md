---
phase: quick
plan: 260317-rb7
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/IssueDetailSidebar.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Epic badge on issue detail sidebar shows only the epic title, not the epic key/ID"
    - "If no epic name is available, falls back to displaying the epic key"
  artifacts:
    - path: "taskflow/src/routes/dashboard/IssueDetailSidebar.tsx"
      provides: "Epic badge rendering without epic ID"
  key_links: []
---

<objective>
Show only the epic title/name in the epic badge on the issue detail sidebar, removing the epic key/ID prefix.

Purpose: Cleaner epic badge -- the epic ID adds noise and the title alone is sufficient.
Output: Updated IssueDetailSidebar.tsx with simplified epic badge rendering.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/dashboard/IssueDetailSidebar.tsx (lines 407-426)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove epic ID from epic badge, show title only</name>
  <files>taskflow/src/routes/dashboard/IssueDetailSidebar.tsx</files>
  <action>
In IssueDetailSidebar.tsx around lines 419-421, replace the current epic badge content:

Current code inside the button (lines 420-421):
```tsx
<span className="opacity-70 mr-1">{epicLink}</span>
{epicName && epicName !== epicLink ? epicName : null}
```

Replace with:
```tsx
{epicName || epicLink}
```

This removes the dedicated epic ID span entirely. It shows epicName when available, falling back to epicLink (the key) only when no name exists.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Epic badge shows only the epic title. Epic key/ID no longer appears. Falls back to epic key if no name available.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Epic badge in issue detail sidebar shows only epic name text
</verification>

<success_criteria>
Epic badge on issue detail sidebar displays only the epic title, with no epic key/ID prefix visible.
</success_criteria>

<output>
After completion, create `.planning/quick/260317-rb7-show-only-epic-title-in-epic-badge-on-is/260317-rb7-SUMMARY.md`
</output>
