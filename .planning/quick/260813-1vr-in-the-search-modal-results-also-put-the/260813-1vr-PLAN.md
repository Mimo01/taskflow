---
phase: quick-260813-1vr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/components/app/CommandPalette.tsx
  - taskflow/src/components/app/CommandPalette.test.tsx
autonomous: true
requirements: [QUICK-1vr]

must_haves:
  truths:
    - "Each Jira issue row in the command palette search results leads with the issue-type icon (Bug/Story/Task/Subtask/Epic), matching the Recent Items popover"
    - "Direct Match, Issues, Jira Search Results, and Closed Jira Tasks groups all show the icon"
    - "Merge Request rows lead with a merge icon, consistent with the popover"
    - "Recent Items rows in the palette default state show the same issue-type icon, resolved from cache or the persisted issueType, with a Clock fallback"
    - "No layout shift: icons are shrink-0 and the summary still truncates on one line"
  artifacts:
    - path: "taskflow/src/components/app/CommandPalette.tsx"
      provides: "Issue-type icon rendering in all palette result rows"
      contains: "IssueTypeIcon"
    - path: "taskflow/src/components/app/CommandPalette.test.tsx"
      provides: "Regression tests for icon rendering in search-result and recent-item rows"
  key_links:
    - from: "taskflow/src/components/app/CommandPalette.tsx"
      to: "taskflow/src/components/ui/issue-type-icon.tsx"
      via: "import { IssueTypeIcon } from '@/components/ui/issue-type-icon'"
      pattern: "IssueTypeIcon"
---

<objective>
Render the issue-type icon in the command palette (search modal) result rows, mirroring the treatment
just shipped for the Recent Items popover (commits 5b9c4184 / d53558a5).

Purpose: search results currently show only `KEY  summary` with no type affordance, so a user
scanning results cannot tell a Bug from a Story without opening it. The Recent Items popover already
solved this; the palette must match.

Output: `CommandPalette.tsx` renders the shared `IssueTypeIcon` primitive on every Jira row and a
`GitMerge` glyph on MR rows, plus regression tests.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@taskflow/src/components/app/CommandPalette.tsx
@taskflow/src/components/app/CommandPalette.test.tsx
@taskflow/src/components/ui/issue-type-icon.tsx
@taskflow/src/components/app/RecentItemsPopover.tsx
@taskflow/src/stores/recent-items.store.ts
</context>

<interfaces>
Shared primitive (already exists, do NOT create a new icon component):

  `IssueTypeIcon({ typeName: string, className?: string })` from `@/components/ui/issue-type-icon`
  — switches on `typeName` over Bug / Story / Subtask / Sub-task / Epic, default CheckSquare.
  Default className is `w-3.5 h-3.5 shrink-0`; it colors the glyph itself.

Type source on a `JiraIssue`: `issue.fields.issuetype?.name`.

Type source on a persisted recent item: `item.issueType` (`RecentItem.issueType?: string`,
recent-items.store.ts:10 — added by d53558a5, preserved across dedup).

Reference row markup — RecentItemsPopover.tsx:158-166 (icon → key → truncating title, Clock
fallback for Jira with no resolvable type, `GitMerge` for MRs).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render issue-type icon on all palette search-result rows</name>
  <files>taskflow/src/components/app/CommandPalette.tsx, taskflow/src/components/app/CommandPalette.test.tsx</files>
  <behavior>
    - Searching with a result whose `fields.issuetype.name` is `Bug` renders a bug glyph inside that Issues row
    - A `Story` result renders the story glyph (BookOpen), not the default CheckSquare
    - The Direct Match row (key-pattern query) renders the icon for `keyMatchResult.fields.issuetype.name`
    - A result with `fields.issuetype` undefined still renders (default CheckSquare), no crash
    - Merge Request rows render a merge glyph
    - The issue key button and truncating summary remain present and unchanged in each row
  </behavior>
  <action>
    Import `IssueTypeIcon` from `@/components/ui/issue-type-icon` and `GitMerge` from `lucide-react`
    into CommandPalette.tsx.

    Add the icon as the FIRST child of the `CommandItem` in all four Jira result groups, before the
    existing key `<button>`: Direct Match (line ~350), Issues (line ~371), Jira Search Results
    (line ~426), Closed Jira Tasks (line ~467). Use
    `<IssueTypeIcon typeName={issue.fields.issuetype?.name ?? ''} className="w-3.5 h-3.5 shrink-0" />`
    — the primitive's `default:` branch already handles an unknown/empty name, so no conditional is
    needed for issues (unlike recents, which have no cached issue at all).

    Add `<GitMerge className="w-3.5 h-3.5 shrink-0 text-muted-foreground" />` as the first child of
    the Merge Requests `CommandItem` (line ~392), matching RecentItemsPopover.tsx:165.

    Extract the four Jira rows' shared JSX into one local `IssueResultRow` sub-component or a small
    render helper inside the file if it removes duplication cleanly — but do NOT change the existing
    `value=`/`key=` strings on any `CommandItem` (cmdk filtering and the Direct-Match / live / closed
    prefixes depend on them) and do NOT change `handleIssueSelect` / `handleIssueKeyClick` wiring
    (PEEK-05 key-button split must survive).

    Do not touch `src/components/ui/issue-type-icon.tsx` — reuse it as-is.

    Tests: the palette test file does NOT mock `lucide-react`, so real SVGs render. Assert with a
    class-substring selector that is stable across lucide naming schemes, e.g.
    `container.querySelector('[class*="lucide-bug"]')`, scoped to the row found via
    `screen.getByText('Fix login bug').closest('[cmdk-item]')`. Reuse the existing fixture style at
    CommandPalette.test.tsx:235-278 (seeded sprint-board query cache) and :303-325 (mocked
    `fetchJiraIssueByKey`) rather than inventing new harnesses.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/components/app/CommandPalette.test.tsx</automated>
  </verify>
  <done>Every Jira result group and the MR group render a leading icon; new tests pass; all pre-existing CommandPalette tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Icon on Recent Items rows in the palette default state</name>
  <files>taskflow/src/components/app/CommandPalette.tsx, taskflow/src/components/app/CommandPalette.test.tsx</files>
  <behavior>
    - A recent Jira item with `issueType: 'Bug'` renders the bug glyph in its palette row
    - A recent Jira item whose key is in the seeded `jira-issues` sprint-board cache uses the cached `fields.issuetype.name` in preference to the persisted `issueType`
    - A recent Jira item with no cached issue and no `issueType` renders the Clock fallback glyph
    - A recent GitLab item renders the merge glyph
    - The existing label text (`KEY summary` / `!iid title`) is unchanged for all four cases
  </behavior>
  <action>
    The Recent Items group (CommandPalette.tsx:324-341) currently renders the bare string from
    `getRecentItemLabel(item)`. Change the `CommandItem` body to render icon + label while keeping
    the label text identical (existing tests at :193-203 and :279-285 assert on it).

    Resolve the type name the same way RecentItemsPopover.tsx:131-142 does, but using the palette's
    already-computed `issuesMap`: `issuesMap.get(item.id)?.fields.issuetype?.name ?? item.issueType`.
    Do NOT copy `findJiraIssueInCache` into this file — the palette already holds the sprint-board
    cache in `issuesMap`; the persisted `item.issueType` is the fallback.

    Render `<IssueTypeIcon typeName={resolved} .../>` when a type resolved, `<Clock className="w-4
    h-4 text-muted-foreground shrink-0" />` when it did not (import `Clock` from lucide-react), and
    `<GitMerge .../>` for `item.type === 'gitlab'` — same three-way branch as
    RecentItemsPopover.tsx:158-166.

    Wrap the label in a `<span className="truncate">` so the added icon cannot push the text into a
    second line.

    Test harness note: the inline `mockRecentItems` type at CommandPalette.test.tsx:95-100 has no
    `issueType` field — extend that inline type with `issueType?: string` and `title?: string` as
    needed so fixtures type-check under `tsc --noEmit`.
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/components/app/CommandPalette.test.tsx &amp;&amp; npx tsc --noEmit &amp;&amp; npx biome check src/components/app/CommandPalette.tsx src/components/app/CommandPalette.test.tsx</automated>
  </verify>
  <done>Recent Items rows in the palette show type icons with cache-then-persisted-then-Clock resolution; typecheck and biome clean on both touched files; full CommandPalette suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jira API → palette render | `fields.issuetype.name` is remote-controlled string data rendered into a UI switch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1vr-01 | Tampering | `IssueTypeIcon` typeName switch | accept | Value is only compared against a fixed case list; unmatched values fall to a default glyph and are never rendered as text or HTML |
| T-1vr-02 | Denial of Service | undefined `fields.issuetype` on partial cache entries | mitigate | Optional chaining + `?? ''` so a missing type renders the default icon instead of throwing during render |
| T-1vr-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies — `lucide-react` and the local `IssueTypeIcon` primitive are already in the tree |
</threat_model>

<verification>
- `cd taskflow && npx vitest run src/components/app/CommandPalette.test.tsx` — green
- `cd taskflow && npx tsc --noEmit` — clean
- `cd taskflow && npx biome check src/components/app/CommandPalette.tsx` — no NEW diagnostics (see the biome-baseline-drift rule: gate on new files flagged, not an absolute count)
- `grep -c IssueTypeIcon taskflow/src/components/app/CommandPalette.tsx` — at least 2 (import + usage)
- No new files created under `src/components/ui/`
</verification>

<success_criteria>
- All four Jira result groups (Direct Match, Issues, Jira Search Results, Closed Jira Tasks) and the Recent Items group render a leading issue-type icon
- MR rows in both search state and recents render the merge glyph
- Icon resolution matches RecentItemsPopover semantics: cache first, persisted `issueType` second, Clock fallback last (recents only)
- Zero behavior change to selection, key-button full-page navigation, cmdk `value`/`keywords` filtering, and existing label text
- One row stays one line at every density
</success_criteria>

<output>
Create `.planning/quick/260813-1vr-in-the-search-modal-results-also-put-the/260813-1vr-SUMMARY.md` when done
</output>
