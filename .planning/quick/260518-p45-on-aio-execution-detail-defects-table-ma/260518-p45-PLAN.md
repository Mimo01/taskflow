---
phase: quick-260518-p45
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx
autonomous: true
requirements:
  - make-row-clickable-execution-detail-defects

must_haves:
  truths:
    - "Clicking anywhere on a defect row in the execution detail page navigates to /issue/<key>"
    - "Row is only clickable after the Jira issue has loaded (isLoading=false and key resolved)"
    - "Clicking the issue-key NavLink still works and does not double-navigate"
    - "Keyboard navigation (Enter / Space) triggers row click"
  artifacts:
    - path: taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx
      provides: "Updated DefectRow with clickable-row pattern"
      contains: "role=\"button\""
  key_links:
    - from: "DefectRow tr"
      to: "/issue/<key>"
      via: "onClick → navigate"
      pattern: "navigate.*linkTarget"
---

<objective>
Make every row in the execution-detail defects table clickable, navigating to the Jira issue detail page — matching the identical pattern already in AioCycleDetailPage.DefectRow.

Purpose: Consistent UX across AIO defect tables; users can click anywhere on the row instead of only the small key link.
Output: Updated DefectRow in AioTestRunDetailPage with full-row navigation, disabled until the issue has loaded.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mimo/Documents/Projects/taskflow/.planning/STATE.md

<!-- Reference implementation — DO NOT COPY WHOLESALE; adapt props to local component shape -->
<!-- AioCycleDetailPage.DefectRow (lines 194-330) has the exact pattern to follow:
  - isClickable = !isLoading && !!resolvedKey
  - <tr> spread: role="button", tabIndex, aria-label, onClick, onKeyDown when isClickable
  - className includes `cursor-pointer` when isClickable
  - NavLink inside first <td> adds onClick={(e) => e.stopPropagation()}
  - onOpen callback receives resolvedKey; caller passes navigate(linkTarget)
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add clickable-row pattern to AioTestRunDetailPage.DefectRow</name>
  <files>taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx</files>
  <action>
    Modify the DefectRow component in AioTestRunDetailPage.tsx to mirror the clickable-row pattern from AioCycleDetailPage.DefectRow. Specific changes:

    1. Add `onOpen: (resolvedKey: string) => void` prop to DefectRow's props type. The existing props are: defectId, issue, isLoading.

    2. Derive `resolvedKey = issue?.key ?? null` and `isClickable = !isLoading && !!resolvedKey` inside DefectRow.

    3. Update the `<tr>` element:
       - Add `hover:bg-muted/30 transition-colors` to className (already has `border-t border-border align-top` — append these).
       - When isClickable: spread `role="button"`, `tabIndex={0}`, `aria-label={\`Open defect \${resolvedKey}\`}`, `onClick={() => onOpen(resolvedKey!)}`, `onKeyDown` handler that calls `onOpen(resolvedKey!)` on Enter or Space with `e.preventDefault()`. Add `cursor-pointer` to className.

    4. On the NavLink inside the first `<td>`: add `onClick={(e) => e.stopPropagation()}` so clicking the key link does not also fire the row handler.

    5. At the call site (inside `defectIds.map`): import `useNavigate` is already present. Derive `linkTarget = \`/issue/\${issue?.key ?? defectId}\`` (same as existing displayKey logic). Pass `onOpen={(key) => navigate(\`/issue/\${key}\`)}` to each DefectRow. The `navigate` function from `useNavigate()` is already used in the component (handleBack) — reuse it.

    Per D-xx: no decisions locked; mirror AioCycleDetailPage pattern exactly.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx tsc --noEmit 2>&1 | grep AioTestRunDetailPage || echo "no type errors in target file"</automated>
  </verify>
  <done>
    DefectRow tr has role="button" when issue is loaded, cursor-pointer styling, full-row onClick navigates to /issue/&lt;key&gt;, NavLink has stopPropagation, keyboard Enter/Space works. TypeScript compiles without errors in AioTestRunDetailPage.tsx.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → router | Navigation target is derived from resolved Jira issue key — no user-controlled input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-p45-01 | Tampering | DefectRow onClick | accept | linkTarget is computed from trusted API response (issue.key), not from URL params or user text |
| T-p45-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this task |
</threat_model>

<verification>
1. Open AIO execution detail for a run that has linked defects.
2. Wait for defect rows to finish loading (skeletons disappear).
3. Hover over a defect row — cursor should change to pointer and row background should highlight.
4. Click anywhere on the row body (not the key link) — should navigate to /issue/&lt;KEY&gt;.
5. Click the issue-key NavLink — should also navigate to /issue/&lt;KEY&gt; without double-firing.
6. Tab to a defect row and press Enter — should navigate.
7. Rows still loading (skeleton visible) should NOT have pointer cursor or be clickable.
</verification>

<success_criteria>
Full defect rows in AioTestRunDetailPage are clickable to issue detail once loaded, matching AioCycleDetailPage behavior exactly. No regression in the key-cell NavLink. TypeScript compiles clean.
</success_criteria>

<output>
Create `.planning/quick/260518-p45-on-aio-execution-detail-defects-table-ma/260518-p45-SUMMARY.md` when done.
</output>
