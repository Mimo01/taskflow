---
phase: quick-260518-p45
plan: "01"
subsystem: aio
tags: [clickable-row, navigation, ux, defects]
dependency_graph:
  requires: []
  provides: [clickable-defect-rows-execution-detail]
  affects: [AioTestRunDetailPage]
tech_stack:
  added: []
  patterns: [clickable-row-with-role-button, stopPropagation-on-nested-NavLink]
key_files:
  modified:
    - taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx
decisions:
  - Mirrored AioCycleDetailPage.DefectRow pattern exactly — no new decisions needed
metrics:
  duration: "~5 minutes"
  completed: "2026-05-18"
  tasks_completed: 1
  tasks_total: 1
  files_changed: 1
---

# Phase quick-260518-p45 Plan 01: Clickable Defect Rows on AIO Execution Detail Summary

**One-liner:** Full-row click navigation to `/issue/<key>` on AioTestRunDetailPage.DefectRow, matching AioCycleDetailPage pattern with keyboard support and disabled-until-loaded guard.

## Tasks Completed

| # | Task | Commit | Status |
|---|------|--------|--------|
| 1 | Add clickable-row pattern to AioTestRunDetailPage.DefectRow | ffd9c434 | Done |

## What Was Built

Added the identical clickable-row pattern from `AioCycleDetailPage.DefectRow` to `AioTestRunDetailPage.DefectRow`:

- `onOpen: (resolvedKey: string) => void` prop added to `DefectRow`
- `resolvedKey = issue?.key ?? null` and `isClickable = !isLoading && !!resolvedKey` derived inside component
- `<tr>` gets `hover:bg-muted/30 transition-colors` always; `cursor-pointer` + `role="button"` + `tabIndex={0}` + `aria-label` + `onClick` + `onKeyDown` (Enter/Space) spread when `isClickable`
- `NavLink` inside first `<td>` gets `onClick={(e) => e.stopPropagation()}` to prevent double-navigation
- Call site passes `onOpen={(key) => navigate('/issue/${key}')}` — reuses existing `navigate` from `useNavigate()`

## Deviations from Plan

None - plan executed exactly as written.

## Self-Check: PASSED

- File modified: taskflow/src/routes/dashboard/AioTestRunDetailPage.tsx — confirmed
- Commit ffd9c434 — confirmed
- TypeScript: no errors in target file
