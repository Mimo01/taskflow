# Quick Task 260528-20i: I want to fix as much biome problems as possible - Context

**Gathered:** 2026-05-27
**Status:** Ready for planning

<domain>
## Task Boundary

Fix as many biome lint issues as possible in the taskflow/src directory. Current state: 53 errors, 1031 warnings, 12 infos across 418 files.

Top issue categories:
- 650 lint/style/useNamingConvention
- 293 lint/style/noNonNullAssertion
- 20 lint/suspicious/noArrayIndexKey
- 18 lint/correctness/useExhaustiveDependencies
- 6 lint/style/useTemplate
- 3 lint/style/useNodejsImportProtocol
- 3 lint/complexity/useLiteralKeys
- 1 lint/style/useImportType
- 1 lint/complexity/noCommaOperator

</domain>

<decisions>
## Implementation Decisions

### Scope
- Target auto-fixable rules first (`biome check --write`)
- Then address noNonNullAssertion and useNamingConvention manually

### noNonNullAssertion (293 warnings)
- Add null guards (`?.`, `if (foo)`) where the intent is obvious and safe
- Do NOT add biome-ignore suppression comments — prefer real fixes
- Skip assertions that require deep domain knowledge to safely guard

### useNamingConvention (650 issues)
- Rename variables/properties to camelCase where clearly wrong
- Add `// biome-ignore lint/style/useNamingConvention: intentional` for cases that are intentional (e.g. PascalCase class names used as object keys, component exports)
- Do NOT rename everything strictly — preserve intentional patterns

### Claude's Discretion
- noArrayIndexKey (20): Use judgment on whether the list items have stable unique identifiers available
- useExhaustiveDependencies (18): These require understanding hook logic — fix only where the missing dep is clearly safe to add
- Other small rule groups (useTemplate, useNodejsImportProtocol, useLiteralKeys, useImportType, noCommaOperator): fix all

</decisions>

<specifics>
## Specific Ideas

- Run `npx biome check --write` first to catch all safe auto-fixes
- Then batch-process noNonNullAssertion by scanning for `!.` and `!,` and `!)` patterns
- For useNamingConvention, the most common culprit is object property shorthand with PascalCase identifiers (e.g. mock classes returned as `{ LazyStore }`)

</specifics>
