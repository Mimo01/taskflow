---
phase: quick-260528-20i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "taskflow/src/**/*.ts"
  - "taskflow/src/**/*.tsx"
autonomous: true
requirements: [BIOME-FIX]
must_haves:
  truths:
    - "Total biome diagnostic count is materially lower than the 1084 baseline (target <500)"
    - "All format and organizeImports errors are eliminated (53 errors -> 0 errors)"
    - "No new biome-ignore suppressions on suspicious/noExplicitAny or a11y rules were added"
    - "tsc --noEmit passes and the test suite still passes after fixes"
  artifacts:
    - path: "taskflow/biome.json"
      provides: "Biome config (unchanged unless objectLiteralMember override is verified-safe)"
      contains: "useNamingConvention"
  key_links:
    - from: "executor"
      to: "taskflow/ working dir"
      via: "all biome/npm commands run from inside taskflow/"
      pattern: "npx biome check"
---

<objective>
Reduce biome lint diagnostics in `taskflow/src` from the 1084 baseline (53 errors, 1031 warnings, 9 infos) by working through categories in safety order: auto-fixes first, then small mechanical rule groups, then the two dominant manual categories (noNonNullAssertion 293, useNamingConvention 650).

Purpose: Lower lint noise so real signal surfaces; clear all blocking format/import errors.
Output: Modified source/test files across `taskflow/src`; biome error count driven to 0 and warning count materially reduced. No regressions (tsc + tests still pass).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md

ALL commands run from inside `/Users/mimo/Documents/Projects/taskflow/taskflow` (where biome.json and package.json live).
</execution_context>

<context>
@/Users/mimo/Documents/Projects/taskflow/.planning/quick/260528-20i-i-want-to-fix-as-much-biome-problems-as-/260528-20i-CONTEXT.md
@/Users/mimo/Documents/Projects/taskflow/.planning/quick/260528-20i-i-want-to-fix-as-much-biome-problems-as-/260528-20i-RESEARCH.md

Key locked decisions (CONTEXT.md):
- Auto-fixable rules first via `biome check --write`.
- noNonNullAssertion: add real null guards (`?.`, `?? ''`, `if (foo)`) where intent is obvious/safe. NO biome-ignore suppressions for this rule. Skip assertions needing deep domain knowledge.
- useNamingConvention: rename to camelCase where clearly wrong; use `// biome-ignore lint/style/useNamingConvention: intentional` for genuinely intentional cases (API-shape interfaces, PascalCase component/class lookup maps, mock contracts). Do NOT rename everything strictly.
- a11y rules are OUT OF SCOPE — do not touch them.

Critical pitfalls (RESEARCH.md):
- Do NOT run `biome check --write --unsafe` globally — it applies unsafe fixes to noNonNullAssertion/useExhaustiveDependencies that may be wrong. If used for useTemplate/useLiteralKeys/useNodejsImportProtocol, run on specific files only.
- For removing `!` from function arguments use `?? ''` / `?? 0` / `?? []` to preserve non-nullable types; use `?.` only for property-access chains (e.g. JSX rendering) where `T | undefined` is acceptable.
- JSX noArrayIndexKey suppression comment must sit on the line IMMEDIATELY before the `key={i}` line, not before the opening JSX tag.
- Never rename snake_case TypeScript interface properties that mirror external API shapes (gitlab.ts, jira.ts, aio/types.ts) — suppress instead.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-fixes + small mechanical rule groups</name>
  <files>taskflow/src/**/*.ts, taskflow/src/**/*.tsx</files>
  <action>
    Run `npm run fix` (= `biome check --write ./src`) to clear all auto-fixable diagnostics: ~37 format errors, ~16 organizeImports errors, and the 1 useImportType warning (src/routes/onboarding/IntegrationsStep.test.tsx). This drives error count toward 0.

    Then manually fix the small mechanical rule groups (~14 diagnostics), all low-risk transforms:
    - useTemplate (6): convert string concatenation to template literals in src/hooks/useIsActiveRoute.ts, src/services/internalLinks.ts, WikiRenderer.tsx (x2) and the remaining flagged sites. Pattern: '/route/' + key + '/x' becomes a backtick template `/route/${key}/x`.
    - useLiteralKeys (3): obj['status'] becomes obj.status in src/routes/dashboard/AioProjectOverviewPage.test.tsx (~lines 278, 321).
    - useNodejsImportProtocol (3): from 'path' becomes from 'node:path' (and equivalent for any other node builtins flagged).
    - noCommaOperator (1): in src/routes/dashboard/BacklogPage.tsx, split the comma-operator expression into separate statements.
    - suppressions/unused (4): fix misplaced biome-ignore comments. In src/routes/standup-notes/IssueActivityGroup.tsx (~lines 107, 120) move each comment to immediately precede the key={i} line per the JSX placement rule. In src/stores/settings.store.test.ts (~lines 18, 20) remove the now-redundant organizeImports suppressions if import order is corrected post-fix.

    Do NOT run --write --unsafe globally. If you choose to auto-fix the useTemplate/useLiteralKeys/useNodejsImportProtocol group, scope it to individual files only and review the diff.

    After edits, run `npm run fix` once more to normalize formatting of touched files.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx biome check ./src --reporter=summary 2>&1 | tail -20</automated>
  </verify>
  <done>Biome reports 0 errors (format + organizeImports cleared). useTemplate, useLiteralKeys, useNodejsImportProtocol, noCommaOperator, and suppressions/unused counts are all 0. tsc --noEmit still passes.</done>
</task>

<task type="auto">
  <name>Task 2: noArrayIndexKey + useExhaustiveDependencies</name>
  <files>taskflow/src/**/*.tsx, taskflow/src/**/*.ts</files>
  <action>
    Address the two medium-effort judgment categories.

    noArrayIndexKey (15): For static/non-reorderable lists with no item identity, add a biome-ignore for lint/suspicious/noArrayIndexKey on the line immediately before the key={i} line — KeyboardShortcutsPanel.tsx (~123), StepIndicator.tsx (~26), AioCyclesSkeleton.tsx (~16), dashboard/index.tsx (~77, AMBIENT_CURVES). For dynamic/reorderable lists, replace the index key with a stable unique id: AioProjectOverviewPage.tsx (~467), DiscussionThreads.tsx (~255, thread.id), MergeRequestListPage.tsx (~279, mr.iid), AioTestRunsSection.tsx (~299, testRun.id/key), NotificationRow.tsx (~297, notification.id). For ChangelogEntry.tsx (~41), inspect whether entries carry an id — use it if present, otherwise suppress.

    useExhaustiveDependencies (18): Apply only clearly-safe fixes per RESEARCH Section C.
    - Add missing safe deps: useResizable.ts:69 (add direction), CreateEditIssueModal.tsx:96 (add dispatch).
    - Replace over-specific deps with the parent object: DiscussionThreads.tsx:76 (linkCtx.activeGitlabProjectPath becomes linkCtx), useCreateEditForm.ts:189 (initialValues?.summary becomes initialValues), useMentionUserMap.ts:22 (derive fingerprint inside the effect; add wikiTexts/initialMap).
    - Ref-as-dep false positives: SprintBoardTab.tsx:192 (stickyHeaderInnerRef.current/.style) — add a biome-ignore for lint/correctness/useExhaustiveDependencies explaining ref.current is intentionally not a dep.
    - For cases needing deeper judgment (cached-avatar.tsx:54 extra url, AioProjectOverviewPage.tsx:364 extra projectKey, BacklogPage.tsx:552 non-memoized applyFilters, MentionPopover.tsx:34 debounced dep, SprintBoardTab.tsx:728 localIssues.length): only fix if the change is provably behavior-preserving; otherwise leave with a precise biome-ignore explaining why, or skip. Prefer moving a non-memoized function inside the effect body over wrapping in useCallback.

    Run the test suite for any component you changed a hook dependency in, to confirm no behavioral regression.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx biome check ./src --reporter=summary 2>&1 | grep -iE "noArrayIndexKey|useExhaustiveDependencies|Found" | tail -10 && npm run test 2>&1 | tail -5</automated>
  </verify>
  <done>noArrayIndexKey and useExhaustiveDependencies counts are substantially reduced (remaining ones carry justified biome-ignore comments). Test suite passes. No a11y rules touched.</done>
</task>

<task type="auto">
  <name>Task 3: noNonNullAssertion + useNamingConvention (dominant categories)</name>
  <files>taskflow/src/**/*.ts, taskflow/src/**/*.tsx</files>
  <action>
    Work the two dominant manual categories, batching by file.

    noNonNullAssertion (293) — NO biome-ignore allowed (locked decision). Replace ! with real guards:
    - Function-argument assertions inside enabled-guarded queryFns: use null-coalescing to preserve types, e.g. fetchSprintData(jiraBaseUrl ?? '', jiraToken ?? '', activeJiraProject ?? '') and (jiraBaseUrl ?? '').replace(/\/$/, ''). Do NOT use ?. here (breaks non-nullable signatures).
    - Property-access chains (JSX/render paths): convert d.issue!.fields.status?.name to d.issue?.fields.status?.name where T | undefined is acceptable; or add an inline type predicate in the preceding .filter((d): d is ... => ...).
    - Test assertions: convert row!.subtasks to row?.subtasks, or add `if (!row) throw new Error('row should exist')` before use.
    - Start with highest-volume source files: AioCycleDetailPage.tsx (34), SprintBoardTab.tsx (26), BacklogPage.tsx (21), AioProjectOverviewPage.tsx (20), WorklogsPage.tsx (20); then remaining source files; then the 62 test-file occurrences.
    - SKIP assertions inside complex business-logic pipelines where guard semantics interact with multiple conditions, or where the enabling guard is far from the use site (e.g. cycleKey!/projectKey! threaded across a large block). Leave those untouched (do not suppress).

    useNamingConvention (650) — mix of renames and intentional suppressions:
    - The 1 variable violation src/services/stronghold.ts:25: rename to camelCase.
    - "property name" interface properties (~100) that mirror external REST API shapes (gitlab.ts ~58, aio/types.ts ~5, jira.ts, etc.): add a biome-ignore for lint/style/useNamingConvention noting it mirrors the API response shape, above each. Do NOT rename (breaks all access sites + API destructuring).
    - "object property name" shorthand (~549): for PascalCase component-icon lookup maps (e.g. Sidebar ICON_MAP entries used as string-key lookups) and PascalCase class/component names in vi.mock return objects, add a per-entry biome-ignore for lint/style/useNamingConvention noting the intentional PascalCase. Batch by high-volume file: gitlab.test.ts (87), jira.ts (42), linkEngine.test.ts (23), AioCycleDetailPage.test.tsx (22). Only rename to camelCase where the name is genuinely a mistyped local property with no external/string-key contract.

    biome.json override (objectLiteralMember selector) is [ASSUMED] in RESEARCH — do NOT add it unless you first verify the selector syntax against the Biome 2.4.8 schema and confirm a dry npx biome check ./src still passes with it. Default to per-entry suppression.

    After all edits run `npm run fix` to normalize formatting, then verify counts and run tsc + tests.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx biome check ./src --reporter=summary 2>&1 | grep -iE "noNonNullAssertion|useNamingConvention|Found|error" | tail -15 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>noNonNullAssertion count is materially reduced via real null guards (no biome-ignore for this rule). useNamingConvention reduced via targeted renames + justified suppressions on API-shape and PascalCase-contract entries. tsc --noEmit passes. Total biome diagnostics under ~500 and 0 errors.</done>
</task>

</tasks>

<verification>
Run from inside /Users/mimo/Documents/Projects/taskflow/taskflow:
- `npx biome check ./src` — 0 errors; total diagnostics materially below 1084 baseline (target <500).
- `npx tsc --noEmit` — passes.
- `npm run test` — passes.
- `git diff` — confirm no a11y rule lines or biome.json a11y/noExplicitAny changes; confirm no biome-ignore was added for noNonNullAssertion.
</verification>

<success_criteria>
- Biome error count: 0 (down from 53).
- Total biome diagnostics: materially reduced from 1084 (target under 500).
- No regressions: tsc --noEmit and vitest both pass.
- Locked decisions honored: noNonNullAssertion fixed with real guards (no suppressions), useNamingConvention handled with renames + justified suppressions, a11y rules untouched.
</success_criteria>

<output>
Create `.planning/quick/260528-20i-i-want-to-fix-as-much-biome-problems-as-/260528-20i-SUMMARY.md` when done, recording before/after diagnostic counts per category and any categories deliberately skipped.
</output>
