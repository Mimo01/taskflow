---
phase: quick-260330-wj6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/TaskCard.tsx
  - taskflow/src/routes/dashboard/StoryHeaderRow.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Task cards show cursor-pointer and a subtle hover effect indicating clickability"
    - "Story header rows show cursor-pointer on the clickable text area"
    - "Issue keys in both cards and headers appear link-like (underline on hover)"
  artifacts:
    - path: "taskflow/src/routes/dashboard/TaskCard.tsx"
      provides: "Interactive hover affordances on task cards"
    - path: "taskflow/src/routes/dashboard/StoryHeaderRow.tsx"
      provides: "Interactive hover affordances on story headers"
  key_links:
    - from: "TaskCard onClick"
      to: "setSelectedIssueKey"
      via: "visual cursor+hover signals user can click"
---

<objective>
Add visual affordances to sprint board task cards and story header rows so users can
immediately tell these elements are clickable and will navigate to issue details.

Purpose: Users currently have no visual cue that left-clicking cards or story headers
opens the issue detail sheet. The recent removal of cursor-pointer (74ef958) was correct
in that hover-border was misleading for right-click context menus, but the underlying
clickability for detail navigation still needs to be communicated.

Output: Updated TaskCard.tsx and StoryHeaderRow.tsx with hover/cursor affordances.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/dashboard/TaskCard.tsx
@taskflow/src/routes/dashboard/StoryHeaderRow.tsx
@taskflow/src/routes/dashboard/SprintBoardTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add clickability affordances to TaskCard and StoryHeaderRow</name>
  <files>taskflow/src/routes/dashboard/TaskCard.tsx, taskflow/src/routes/dashboard/StoryHeaderRow.tsx</files>
  <action>
In TaskCard.tsx:
1. On the outer card div (line ~87, the one with role="button"), add `cursor-pointer` and `hover:bg-accent/50 transition-colors` to the className. This replaces the old hover:border-primary/50 with a more subtle background tint that signals "this whole card is clickable" without the misleading border highlight.
2. On the issue key span (line ~98, the `text-xs font-mono text-muted-foreground` span), add `group-hover:underline` and wrap the card div in a group by adding `group` to the card div's className. This makes the issue key underline on hover, giving a link-like affordance.

So the card div className becomes:
```
'border rounded-lg px-2 py-2 density-compact:py-1 density-comfortable:py-3 bg-card w-full flex flex-col gap-1 cursor-pointer hover:bg-accent/50 transition-colors group'
```
And the issue key div becomes:
```
<div className="text-xs font-mono text-muted-foreground group-hover:underline">{issue.key}</div>
```

In StoryHeaderRow.tsx:
1. On the button that wraps the key + summary (line ~48-55), add `cursor-pointer` to the className (it may already inherit from button element, but be explicit). Also add `group` class.
2. On the story key span inside that button, add `group-hover:underline` so the key underlines on hover, signaling clickability.
3. The outer row div already has `hover:bg-muted/60` which is good — keep it.

So the button className becomes:
```
'flex items-center gap-2 flex-1 min-w-0 text-left group cursor-pointer'
```
And the storyKey span becomes:
```
<span className="font-mono text-xs text-muted-foreground shrink-0 group-hover:underline">{storyKey}</span>
```
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Task cards show cursor-pointer + subtle bg hover + key underline on hover. Story header rows show key underline on hover of the clickable area. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Visual clickability affordances on sprint board task cards and story headers: cursor-pointer, subtle background highlight on card hover, and issue key underline on hover for both cards and story headers.</what-built>
  <how-to-verify>
    1. Open the app and navigate to the Sprint Board tab
    2. Hover over a task card — confirm: cursor changes to pointer, card background gets a subtle tint, issue key underlines
    3. Hover over a story header row — confirm: the issue key underlines when hovering the text area
    4. Left-click a task card — confirm it opens issue detail sheet
    5. Left-click a story header key/summary — confirm it opens issue detail sheet
    6. Right-click a task card — confirm context menu still works as before
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Visual hover states are subtle but clear
- No regression on context menu right-click behavior
</verification>

<success_criteria>
- Cards and story headers clearly communicate clickability through cursor, hover background, and key underline
- No visual regression on existing styles
- Context menu functionality preserved
</success_criteria>

<output>
After completion, create `.planning/quick/260330-wj6-better-communicate-that-sprint-board-tas/260330-wj6-SUMMARY.md`
</output>
