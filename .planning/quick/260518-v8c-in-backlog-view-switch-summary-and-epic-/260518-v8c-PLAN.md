---
phase: quick-260518-v8c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/BacklogPage.tsx
  - taskflow/src/routes/dashboard/BacklogRow.tsx
autonomous: false
requirements:
  - QT-260518-v8c-01
  - QT-260518-v8c-02

must_haves:
  truths:
    - "On the Backlog view the column order is: Key, Summary, Epic, Points, Assignee (Summary comes before Epic)."
    - "The Epic column header is right-aligned and its badge cell content is right-aligned."
    - "Clicking the epic badge still navigates to the epic detail; clicking elsewhere on the row still opens the story."
    - "The Summary cell still truncates with ellipsis when the text overflows and still shows the OverdueBadge."
  artifacts:
    - path: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      provides: "Updated <thead> column order (Key, Summary, Epic, Points, Assignee) with Epic header right-aligned"
      contains: "text-right"
    - path: "taskflow/src/routes/dashboard/BacklogRow.tsx"
      provides: "Updated RowCells with Summary <td> rendered before Epic <td>, Epic cell right-aligned"
      contains: "text-right"
  key_links:
    - from: "BacklogPage.tsx <thead>"
      to: "BacklogRow.tsx RowCells <td> order"
      via: "Column order must match header order exactly"
      pattern: "Key.*Summary.*Epic.*Points.*Assignee"
---

<objective>
Swap the Summary and Epic columns in the Backlog table view so the order becomes
Key | Summary | Epic | Points | Assignee, and right-align the Epic column (both
the header label and the epic badge in the body cell).

Purpose: User-requested layout tweak — Summary is the primary read field and
should sit immediately after the key; the epic badge looks tidier when right-aligned
next to the Points column.

Output: Updated `<thead>` in `BacklogPage.tsx` and updated `RowCells` in
`BacklogRow.tsx`; no service, store, or test contract changes.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@taskflow/src/routes/dashboard/BacklogPage.tsx
@taskflow/src/routes/dashboard/BacklogRow.tsx

<interfaces>
<!-- Current column structure (BEFORE) — taken from BacklogPage.tsx lines 140-154 and BacklogRow.tsx lines 67-131 -->

Header order today:
  <th>Key</th>              (w-24, text-left)
  <th>Epic</th>             (no width, text-left, whitespace-nowrap)
  <th>Summary</th>          (no width, text-left)
  <th>Points</th>           (w-14, text-right)
  <th>Assignee</th>         (w-10)

Body cell order today (RowCells in BacklogRow.tsx):
  <td>key</td>              (w-24 ... whitespace-nowrap)
  <td>epic badge</td>       (no width, whitespace-nowrap)
  <td>summary</td>          (max-w-0 w-full ... overflow-hidden whitespace-nowrap text-ellipsis)
  <td>story points</td>     (w-14 ... text-right)
  <td>assignee avatar</td>  (w-10)

Target order (AFTER):
  Key | Summary | Epic | Points | Assignee
  - Summary keeps its current truncation behavior and the OverdueBadge.
  - Epic <th> becomes text-right; Epic <td> becomes text-right so the badge
    sits flush with the right edge of its column (the Points column to its
    right is already w-14 text-right).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Summary/Epic column order in backlog header and rows; right-align Epic</name>
  <files>
    taskflow/src/routes/dashboard/BacklogPage.tsx,
    taskflow/src/routes/dashboard/BacklogRow.tsx
  </files>
  <action>
    In `taskflow/src/routes/dashboard/BacklogPage.tsx`, edit the `<thead>` block
    (around lines 140-154) so the `<th>` order becomes Key, Summary, Epic, Points,
    Assignee. Move the existing Summary `<th>` (currently `<th className="px-2 py-2
    text-left text-xs font-medium text-muted-foreground">Summary</th>`) so it sits
    immediately after the Key `<th>` and before the Epic `<th>`. On the Epic
    `<th>`, change `text-left` to `text-right` (keep `whitespace-nowrap` and the
    rest of the classes unchanged). Do NOT touch the Key, Points, or Assignee
    `<th>` definitions other than relocating Summary above Epic.

    In `taskflow/src/routes/dashboard/BacklogRow.tsx`, edit the `RowCells`
    component (lines 50-133) so the `<td>` order matches the new header order:
    Key cell, Summary cell, Epic cell, Story points cell, Assignee cell. Move the
    Summary `<td>` block (the one with `max-w-0 w-full ... overflow-hidden
    whitespace-nowrap text-ellipsis` containing `issue.fields.summary` and the
    `<OverdueBadge>`) so it sits between the Key `<td>` and the Epic `<td>`. On
    the Epic `<td>` (currently `<td className="px-2 py-2 density-compact:py-1
    density-comfortable:py-3 whitespace-nowrap">`), append `text-right` to its
    className so the cell content right-aligns. Do NOT change the Skeleton width,
    the epic button markup, the conditional rendering of the epic badge, or any
    other cell. Leave the Summary cell's max-width/overflow/ellipsis classes
    exactly as they are — moving the cell, not restyling it.

    Do not change props, JiraIssue typing, the BacklogRow forwardRef wrapper, the
    context menu structure, or any imports. Keep the existing comments
    ("Summary cell -- takes remaining space..."; "Epic badge cell -- right after
    key") in sync with the new positions (rename the Epic comment to drop "right
    after key" and the Summary comment can stay, or simply update both to reflect
    the new layout).
  </action>
  <verify>
    <automated>cd taskflow &amp;&amp; npx vitest run src/routes/dashboard/BacklogPage.test.tsx --reporter=dot</automated>
  </verify>
  <done>
    BacklogPage.test.tsx passes. Header order in the rendered table is
    Key | Summary | Epic | Points | Assignee. Epic `<th>` and Epic `<td>` both
    have `text-right` applied. No other column class lists have changed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visually verify backlog column swap and Epic right-alignment</name>
  <what-built>
    Reordered the Backlog table columns to Key | Summary | Epic | Points |
    Assignee, and right-aligned the Epic column header label and the epic
    badge in each row.
  </what-built>
  <how-to-verify>
    1. Run the app (`cd taskflow &amp;&amp; npm run dev`) and open the Backlog page
       for any project that has a populated backlog (issues with and without
       epics, mix of long and short summaries).
    2. Confirm the column header reads, left to right:
       Key, Summary, Epic, Points, Assignee.
    3. Confirm in the body rows that the summary text appears right after the
       issue key, and the colored epic badge appears to the right of the
       summary, immediately before the Points column.
    4. Confirm the Epic header label ("Epic") is right-aligned, and the epic
       badge in each body cell is flush with the right edge of its column
       (close to the Points column).
    5. Click a summary on a row — story detail still opens. Click an epic
       badge — epic detail still opens (event still stops propagation, the row
       click does NOT fire).
    6. Right-click a row — the "Move to..." context menu still appears and
       behaves the same way.
    7. Resize the window narrow enough that summaries must truncate — the
       Summary cell still truncates with an ellipsis and the Epic badge remains
       fully visible on the right.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run src/routes/dashboard/BacklogPage.test.tsx` is green.
- `git diff` shows changes only in `BacklogPage.tsx` (thead block) and
  `BacklogRow.tsx` (RowCells body), with no churn outside those regions.
- Human checkpoint approved.
</verification>

<success_criteria>
- Backlog table renders columns in the new order: Key, Summary, Epic, Points,
  Assignee.
- Epic header and epic badge cell are right-aligned.
- All existing behaviors preserved: row click → story detail, epic badge click
  → epic detail (no row navigation), context menu "Move to..." actions,
  Summary truncation + OverdueBadge.
- No TypeScript or lint errors introduced.
</success_criteria>

<output>
Create `.planning/quick/260518-v8c-in-backlog-view-switch-summary-and-epic-/260518-v8c-SUMMARY.md` when done.
</output>
