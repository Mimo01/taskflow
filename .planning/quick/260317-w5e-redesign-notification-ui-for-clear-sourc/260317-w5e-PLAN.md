---
phase: quick
plan: 260317-w5e
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/notifications/NotificationRow.tsx
  - taskflow/src/routes/notifications/NotificationPopover.tsx
  - taskflow/src/routes/notifications/NotificationRow.test.tsx
autonomous: true
requirements: [NOTIF-REDESIGN]

must_haves:
  truths:
    - "Each notification row prominently displays its source (Jira or GitLab) via a visible labeled source tag"
    - "Source identification is immediate at a glance without relying solely on border color"
    - "Notification rows have improved visual hierarchy with clear separation between metadata and content"
    - "Existing functionality (click navigation, mark as read, unread styling) is preserved"
  artifacts:
    - path: "taskflow/src/routes/notifications/NotificationRow.tsx"
      provides: "Redesigned notification row with prominent source badge, improved layout"
      min_lines: 80
    - path: "taskflow/src/routes/notifications/NotificationPopover.tsx"
      provides: "Notification popover with optional source grouping headers"
      min_lines: 50
  key_links:
    - from: "taskflow/src/routes/notifications/NotificationRow.tsx"
      to: "notifications.store"
      via: "NotificationItem type import"
      pattern: "import.*NotificationItem"
    - from: "taskflow/src/routes/notifications/NotificationPopover.tsx"
      to: "NotificationRow"
      via: "renders NotificationRow for each item"
      pattern: "<NotificationRow"
---

<objective>
Redesign notification UI for clear source identification and improved styling.

Purpose: Users currently struggle to identify which source (Jira vs GitLab) a notification comes from at a glance. The thin left border is too subtle. The redesign adds a prominent source indicator and improves overall visual quality.

Output: Redesigned NotificationRow with clear source badges, improved visual hierarchy, and better styling throughout the notification popover.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/src/routes/notifications/NotificationRow.tsx
@taskflow/src/routes/notifications/NotificationPopover.tsx
@taskflow/src/routes/notifications/NotificationRow.test.tsx
@taskflow/src/components/app/TopBar.tsx
@taskflow/src/stores/notifications.store.ts

<interfaces>
From notifications.store.ts:
```typescript
export type NotificationType =
  | 'comment-mention'
  | 'issue-update'
  | 'mr-note'
  | 'gitlab-mention'
  | 'jira-comment'
  | 'mr-approval'
  | 'pipeline-failure'
  | 'issue-assignment'
  | 'due-date-reminder';

export interface NotificationItem {
  id: string;
  source: 'jira' | 'gitlab';
  entityTitle: string;
  author: string;
  authorAvatarUrl?: string;
  bodyPreview: string;
  fullBody: string;
  createdAt: string;
  url?: string;
  notificationType?: NotificationType;
  entityState?: string;
  parentKey?: string;
  parentSummary?: string;
  mrProjectId?: number;
  mrIid?: number;
}
```

From TopBar.tsx:
```typescript
<PopoverContent className="p-0 w-[28rem]">
  <NotificationPopover onIssueClick={onIssueClick} onMRClick={onMRClick} onClose={() => onNotifPopoverChange(false)} />
</PopoverContent>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign NotificationRow with prominent source identification and improved styling</name>
  <files>taskflow/src/routes/notifications/NotificationRow.tsx</files>
  <action>
Redesign the NotificationRow component with these specific changes:

1. **Source badge (primary change):** Replace the subtle left border-only approach with a prominent inline source tag. Add a small pill/badge in the top-right or top-left metadata line that reads "Jira" or "GitLab" with distinct, saturated background colors:
   - Jira: `bg-orange-500 text-white` pill with text "Jira"
   - GitLab: `bg-purple-600 text-white` pill with text "GitLab"
   The badge should be small (`text-[10px] font-semibold px-1.5 py-0.5 rounded-full`) but visually distinct.

2. **Keep the left border** as a secondary indicator (it reinforces the source at a glance when scrolling) but make it thinner: change `border-l-4` to `border-l-3` or keep as-is (the badge is now the primary identifier).

3. **Improved metadata line layout:** Restructure the top metadata area to show source badge + type badge + relative timestamp all on a single line:
   ```
   [Jira] [Issue update]                   2h ago
   ```
   Move the timestamp from the bottom to the metadata line (right-aligned via `ml-auto`). This tightens the row and reduces vertical space waste.

4. **Author line:** Below the metadata badges, show the author name in `text-xs text-muted-foreground` like: "by J.Smith". Keep the avatar to the left as-is.

5. **Entity title:** Keep as-is (bold when unread, normal when read), but ensure it is directly below the author line for clean reading order.

6. **Body preview:** Keep the existing arrow-format logic for status changes and the linkified plain text fallback. No changes needed here.

7. **Entity state chip + parent story context:** Keep as-is.

8. **Hover state improvement:** Change from basic `hover:bg-muted` to a slightly more polished `hover:bg-accent/60` with `transition-colors duration-150`.

9. **Unread indicator:** Keep the blue dot but move it to be positioned on the source badge instead of the avatar (to draw the eye to the source first).

Preserve all existing props interface and callback behavior. Do NOT change the component's public API.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/notifications/NotificationRow.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>NotificationRow renders with prominent "Jira" or "GitLab" source badge, improved metadata layout with timestamp on top line, and cleaner hover states. All existing tests pass (update test assertions if the DOM structure changes for border classes).</done>
</task>

<task type="auto">
  <name>Task 2: Add source section headers to NotificationPopover and update tests</name>
  <files>taskflow/src/routes/notifications/NotificationPopover.tsx, taskflow/src/routes/notifications/NotificationRow.test.tsx</files>
  <action>
**NotificationPopover changes:**

1. **Visual grouping by source:** Group notifications into two sections when both sources have items. Add a subtle section divider between Jira and GitLab notifications:
   - Sort all items newest-first as before, then within the rendering, group by source
   - Add a sticky mini-header for each group: a thin row with the source name in `text-xs font-semibold text-muted-foreground uppercase tracking-wider` and a hairline bottom border
   - Format: `JIRA` and `GITLAB` section headers
   - If only one source has items, skip the section header entirely (no need when unambiguous)
   - Within each group, maintain newest-first sort order

2. **Header visual polish:** Update the popover header from plain `border-b` to include a subtle background: `bg-muted/30 border-b`. Keep "Notifications" title and "Mark all as read" button as-is.

3. **Stale data banner area:** Keep the existing `fetchError` handling unchanged.

**Test updates (NotificationRow.test.tsx):**

Update test assertions to match the new DOM structure:
- The border class assertions (`border-orange-500`, `border-purple-500`) should still pass if left borders are kept
- Update the 'Comment mention' text assertion if the labelMap changed (current code maps 'comment-mention' to 'Mentioned' but test expects 'Comment mention' -- fix the test to match current labelMap)
- Add a new test: renders source badge text "Jira" for jira source and "GitLab" for gitlab source
- Add a new test: renders the relative timestamp in the metadata line

Run all notification tests to ensure nothing breaks.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/notifications/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Notification popover groups items by source with section headers when both sources present. Tests updated and passing, including new assertions for source badge text rendering.</done>
</task>

</tasks>

<verification>
1. `cd /Users/mimo/Desktop/Tasker/taskflow && npx vitest run src/routes/notifications/ --reporter=verbose` -- all notification tests pass
2. `cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit` -- no TypeScript errors
3. Visual: Each notification row shows a prominent colored "Jira" or "GitLab" badge that is immediately identifiable at a glance
4. Visual: Notifications are grouped by source with section headers when both sources have items
</verification>

<success_criteria>
- Source identification is prominent and immediate (colored "Jira"/"GitLab" pill badge on every row)
- Metadata line consolidates source badge + type badge + timestamp on one line
- Notification popover groups by source with section headers
- All existing notification tests pass (with updated assertions where DOM changed)
- New tests cover source badge rendering
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/quick/260317-w5e-redesign-notification-ui-for-clear-sourc/260317-w5e-SUMMARY.md`
</output>
