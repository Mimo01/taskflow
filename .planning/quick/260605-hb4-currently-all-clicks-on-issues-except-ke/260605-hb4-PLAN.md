---
phase: quick-260605-hb4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/app/TopBar.tsx
  - src/routes/dashboard/index.tsx
  - src/routes/dashboard/DashboardInProgressCard.test.tsx
  - src/routes/notifications/NotificationPopover.test.tsx
autonomous: true
requirements: [HB4-01]
must_haves:
  truths:
    - "Clicking a notification row body in the TopBar NotificationPopover navigates to the full issue page (/issue/:key), not the PeekPanel sidebar"
    - "Clicking the body of a Dashboard home In-Progress card row navigates to the full issue page, not the PeekPanel sidebar"
    - "Dashboard full-page issue opens start a fresh breadcrumb trail (resetTrail=true)"
    - "Notification full-page opens still mark the item as read and close the popover"
    - "Issue-key button clicks on both surfaces still navigate full-page (no regression)"
    - "Sprint Board, Backlog, Standup Notes, Command Palette, and issue-detail panels keep their existing peek behavior (untouched)"
    - "npm run check (biome + tsc) stays green"
  artifacts:
    - path: "src/components/app/TopBar.tsx"
      provides: "NotificationPopover element no longer receives onOpenIssue, so row-body clicks fall through to onIssueClick (full-page)"
      contains: "NotificationPopover"
    - path: "src/routes/dashboard/index.tsx"
      provides: "DashboardInProgressCard receives a resetTrail=true onIssueClick wrapper and no onOpenIssue"
      contains: "onIssueClick={(key)"
    - path: "src/routes/dashboard/DashboardInProgressCard.test.tsx"
      provides: "test 3 asserts body click → onIssueClick (full-page), no onOpenIssue"
      contains: "onIssueClick"
    - path: "src/routes/notifications/NotificationPopover.test.tsx"
      provides: "test asserting row-body click with onIssueClick (no onOpenIssue) calls onIssueClick + markAsRead + onClose"
      contains: "onIssueClick"
  key_links:
    - from: "src/components/app/TopBar.tsx"
      to: "NotificationPopover onIssueClick (handleIssueClick(key, true) via main.tsx)"
      via: "onOpenIssue prop removed → handleRowClick falls through to onIssueClick branch"
      pattern: "NotificationPopover"
    - from: "src/routes/dashboard/index.tsx"
      to: "handleIssueClick(key, true) from outlet context"
      via: "onIssueClick wrapper passing resetTrail=true; onOpenIssue removed"
      pattern: "onIssueClick={\\(key\\)"
---

<objective>
Make issue clicks from the TopBar NotificationPopover and the Dashboard home In-Progress card always open the full issue page (`/issue/:key`) instead of the PeekPanel quick sidebar, with a fresh breadcrumb trail for dashboard opens. Leave every other surface (Sprint Board, Backlog, Standup Notes, Command Palette, issue-detail panels) unchanged.

Purpose: Notifications and the Dashboard home are "entry point" surfaces; users expect a full-page issue view, not a transient peek sidebar.
Output: Edited TopBar + dashboard wiring (Option B — stop passing `onOpenIssue`), updated tests, green `npm run check`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260605-hb4-currently-all-clicks-on-issues-except-ke/260605-hb4-CONTEXT.md
@.planning/quick/260605-hb4-currently-all-clicks-on-issues-except-ke/260605-hb4-RESEARCH.md

# Source files (already read by planner; executor confirms exact lines)
@src/components/app/TopBar.tsx
@src/routes/dashboard/index.tsx
@src/routes/dashboard/DashboardInProgressCard.tsx
@src/routes/notifications/NotificationPopover.tsx
@src/routes/dashboard/DashboardInProgressCard.test.tsx
@src/routes/notifications/NotificationPopover.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stop passing onOpenIssue at the two call sites; reset breadcrumb trail for dashboard</name>
  <files>src/components/app/TopBar.tsx, src/routes/dashboard/index.tsx</files>
  <action>
Apply Option B (RESEARCH Finding 1) — make body clicks fall through to the full-page `onIssueClick` handler by removing `onOpenIssue` at the two parent call sites. Do NOT edit the leaf components (`NotificationPopover.tsx`, `NotificationRow.tsx`, `DashboardInProgressCard.tsx`) — their `onOpenIssue ?? onIssueClick` / `if (onOpenIssue) … if (onIssueClick) …` fallbacks already resolve to full-page when `onOpenIssue` is undefined, and `handleRowClick` preserves `markAsRead`/`onClose` in the fallback branch (RESEARCH Pitfall 4).

1. `src/components/app/TopBar.tsx` (~L102-106): On the `<NotificationPopover>` element only, remove the `onOpenIssue={onOpenIssue}` line. Leave `onIssueClick`, `onMRClick`, `onClose` intact. Do NOT remove `onOpenIssue` from TopBar's own props or from any other element (e.g. RecentItemsPopover) — scope is the NotificationPopover JSX only. Notifications already get `resetTrail=true` because TopBar's `onIssueClick` is wired in main.tsx as `(key) => handleIssueClick(key, true)` — no breadcrumb work needed here (RESEARCH Pitfall 2).

2. `src/routes/dashboard/index.tsx`:
   - Widen the local outlet-context type (L34-37) `onIssueClick` to `(key: string, resetTrail?: boolean) => void`. The global outlet context already provides the full `handleIssueClick`; only this local annotation narrows it (RESEARCH Finding 2 / TypeScript Implications).
   - On the `<DashboardInProgressCard>` element (L111-119): remove `onOpenIssue={onOpenIssue}`, and change `onIssueClick={onIssueClick}` to `onIssueClick={(key) => onIssueClick(key, true)}` so dashboard full-page opens start a fresh breadcrumb trail per the LOCKED CONTEXT decision (resetTrail=true).
   - Remove `onOpenIssue` from the `useOutletContext` destructure (L34) since it is now unused — Biome flags unused vars and the baseline is GREEN (RESEARCH Pitfall 5). If `onOpenIssue` is still consumed elsewhere in the file, keep it; otherwise drop it.

Leave all stopPropagation on issue-key buttons untouched (RESEARCH Pitfall 3). Do NOT edit main.tsx.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npm run check</automated>
  </verify>
  <done>npm run check (biome + tsc) is green. TopBar's NotificationPopover element no longer passes onOpenIssue. dashboard/index.tsx passes `onIssueClick={(key) => onIssueClick(key, true)}` to the card, no longer passes onOpenIssue to it, and has no unused-var lint error.</done>
</task>

<task type="auto">
  <name>Task 2: Update DashboardInProgressCard test 3 and add NotificationPopover full-page test</name>
  <files>src/routes/dashboard/DashboardInProgressCard.test.tsx, src/routes/notifications/NotificationPopover.test.tsx</files>
  <action>
Lock the new behavior in tests (RESEARCH Finding 3).

1. `src/routes/dashboard/DashboardInProgressCard.test.tsx` — rewrite **test 3** ("test 3: click navigation…", ~L189-217):
   - Render the card WITHOUT `onOpenIssue` (i.e. `<DashboardInProgressCard {...defaultProps} />`) to mirror the new dashboard call site. Remove the local `onOpenIssue = vi.fn()`.
   - Key-button assertion stays: clicking the `PROJ-101` key button calls `defaultProps.onIssueClick` with `'PROJ-101'`.
   - Body-click assertion: clicking the body summary text now calls `defaultProps.onIssueClick` with `'PROJ-101'` (full-page), NOT a peek handler. Delete the obsolete `onOpenIssue` peek expectation.
   - Update the test name/description so it states body click → onIssueClick (full-page) rather than → onOpenIssue (peek).
   - Note `defaultProps` (L101-108) already contains `onIssueClick: vi.fn()` and no `onOpenIssue`. Leave `beforeEach` resets intact.
   - Do NOT change test 6 (~L234-281) — it already renders without onOpenIssue and asserts onIssueClick; it stays green and confirms consistency.

2. `src/routes/notifications/NotificationPopover.test.tsx` — add a new test asserting the new notification behavior: render `<NotificationPopover onIssueClick={fn} onClose={closeFn} />` (provide `onIssueClick`, OMIT `onOpenIssue`) with a Jira-issue notification item, click the row body, and assert `onIssueClick` is called with the issue key, the item is marked read (markAsRead/readIds path as existing tests assert it), and `onClose` is called. Reuse the existing test file's render helpers, mock setup, and notification-item fixtures (match the pattern of the existing row-click tests at ~L45-99). This guards the markAsRead + onClose + full-page side-effect chain (RESEARCH Pitfall 4).
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run src/routes/dashboard/DashboardInProgressCard.test.tsx src/routes/notifications/NotificationPopover.test.tsx</automated>
  </verify>
  <done>DashboardInProgressCard test 3 asserts body click → onIssueClick (full-page) with no onOpenIssue; the new NotificationPopover test asserts row-body click → onIssueClick + markAsRead + onClose. Both target test files pass.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression gate</name>
  <files>(none — verification only)</files>
  <action>
Run the full gate to confirm no out-of-scope surface regressed (Sprint Board, Backlog, Standup Notes, Command Palette, issue-detail panels are untouched per LOCKED CONTEXT scope) and the baseline stays GREEN. If `npm run check` or the suite surfaces a failure, fix it within the scope of Tasks 1-2 (do not expand scope to other surfaces). Confirm DashboardInProgressCard is rendered only by the dashboard home and NotificationPopover only by TopBar (RESEARCH Finding 4) so no other consumer was affected.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npm run check && npx vitest run</automated>
  </verify>
  <done>npm run check is green and the full vitest suite passes with no new failures.</done>
</task>

</tasks>

<verification>
- `npm run check` (biome + tsc) green.
- `npx vitest run src/routes/dashboard/DashboardInProgressCard.test.tsx src/routes/notifications/NotificationPopover.test.tsx` passes.
- Full `npx vitest run` passes (no regression on out-of-scope surfaces).
- Manual spot-check (optional): clicking a notification row body and a Dashboard In-Progress row body each navigate to `/issue/:key` full page; dashboard open shows a fresh breadcrumb trail.
</verification>

<success_criteria>
- Notification row-body clicks and Dashboard home In-Progress card body clicks open the full issue page, not the PeekPanel.
- Dashboard full-page opens reset the breadcrumb trail (resetTrail=true).
- Notification clicks still markAsRead and close the popover; issue-key buttons still navigate full-page.
- No edits to Sprint Board, Backlog, Standup Notes, Command Palette, issue-detail panels, or main.tsx.
- `npm run check` and full vitest suite green.
</success_criteria>

<output>
Create `.planning/quick/260605-hb4-currently-all-clicks-on-issues-except-ke/260605-hb4-SUMMARY.md` when done.
</output>
