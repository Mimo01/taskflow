---
phase: quick-260617-e3z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/components/app/CommandPalette.tsx
autonomous: true
requirements:
  - QUICK-260617-e3z
must_haves:
  truths:
    - "Typing 2+ characters in the search palette automatically fires a debounced Jira text search (no button click required)"
    - "Issue results from the live API appear inline in the existing 'Issues' group — no separate section header"
    - "Sprint-board cached issues and live API results are merged and deduplicated by issue key; cap at 10 total"
    - "Search is scoped to the active project only (no global Jira query)"
    - "Closing or resetting the palette (query < 2 chars) clears the debounced query and cancels any pending API call"
  artifacts:
    - path: "taskflow/src/components/app/CommandPalette.tsx"
      provides: "Debounced text search wired into Issues group"
      contains: "debouncedQuery"
  key_links:
    - from: "debouncedQuery state"
      to: "useQuery(['search', 'text', debouncedQuery, activeJiraProject])"
      via: "enabled: debouncedQuery.length >= 2"
      pattern: "search.*text.*debouncedQuery"
    - from: "textSearchResults"
      to: "issuesMap"
      via: "for loop adding keys not already present"
      pattern: "issuesMap\\.has\\(issue\\.key\\)"
---

<objective>
Wire automatic debounced issue text search into the CommandPalette. When the user types 2+ characters, a 300ms debounced useQuery fires searchJira scoped to the active project. Results merge into the existing "Issues" group (deduped by key, capped at 10) — no new section header or tab. The existing manual "Search Jira for …" tail item and "Search closed tasks for …" tail item are left untouched.

Purpose: Users can find any issue by typing without needing to click a search trigger.
Output: Modified CommandPalette.tsx with debounced auto-search merged into the Issues group.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mimo/Documents/Projects/taskflow/.planning/STATE.md
@/Users/mimo/Documents/Projects/taskflow/taskflow/src/components/app/CommandPalette.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add debounced text search query and merge results into Issues group</name>
  <files>taskflow/src/components/app/CommandPalette.tsx</files>
  <action>
Make three surgical changes to CommandPalette.tsx, all within the existing component body. Do not touch any other logic, the live search tail item, or the closed search tail item.

**Change 1 — Debounced query state** (add after the existing `trimmed` declaration, around line 69):

Add a `debouncedQuery` state initialized to `''`. Wire a useEffect on `[trimmed]` that: if `trimmed.length < 2`, calls `setDebouncedQuery('')` immediately and returns; otherwise sets a 300ms timeout to call `setDebouncedQuery(trimmed)` and returns the clearTimeout cleanup. Also extend the existing `useEffect` that resets state on `!open` to also call `setDebouncedQuery('')`.

**Change 2 — Auto text search useQuery** (add in the "Live Jira search" section, after the existing `liveResults` query):

Add a new useQuery with:
- queryKey: `['search', 'text', debouncedQuery, activeJiraProject]`
- queryFn: reads jira-pat secret via `readSecret('jira-pat')`, calls `searchJira(jiraBaseUrl ?? '', token, activeJiraProject ?? '', debouncedQuery)`
- enabled: `debouncedQuery.length >= 2 && !!jiraBaseUrl && !!activeJiraProject`
- staleTime: `30_000`
- placeholderData: `keepPreviousData`

Destructure as: `const { data: textSearchResults } = useQuery({ … })`

**Change 3 — Merge into issuesMap** (replace the existing `allIssues` derivation block, lines 101-105):

After building `issuesMap` from `cachedSprintBoard?.issues` (unchanged), add a second loop that iterates `textSearchResults ?? []` and inserts each issue into `issuesMap` only if `!issuesMap.has(issue.key)`. Then derive `allIssues` as `Array.from(issuesMap.values()).slice(0, 10)`.

The existing "Issues" `CommandGroup` JSX (which already maps over `allIssues`) requires no changes. cmdk's built-in value-based filtering still applies on top of the merged+capped list.

Do NOT change the `queryKey` or `enabled` condition of the existing `['search', 'live', query]` query — it must remain opt-in via the "Search Jira for …" tail item click.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npm run check 2>&1 | tail -20</automated>
  </verify>
  <done>
`npm run check` exits 0 (biome + tsc clean). The component contains `debouncedQuery` state, a useQuery with key `['search', 'text', debouncedQuery, activeJiraProject]`, and `issuesMap` populated from both sprint-board cache and `textSearchResults`. The existing liveSearch and closedSearch tail-item flows are intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → JQL query | The `debouncedQuery` value is interpolated into a `text ~ "..."` JQL expression inside `searchJira`; malformed input could produce a JQL syntax error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-e3z-01 | Tampering | JQL interpolation in searchJira | accept | `text ~` is a read-only search; no state mutation possible; Jira rejects malformed JQL with a 400 error which searchJira already catches and returns [] |
| T-e3z-02 | Information Disclosure | search results leaked to other projects | mitigate | `enabled` guard requires `activeJiraProject`; searchJira always includes `project = ${projectKey}` in JQL |
| T-e3z-03 | Denial of Service | rapid keystrokes flood API | mitigate | 300ms debounce + `enabled: debouncedQuery.length >= 2` prevents fires on short/rapid input |
</threat_model>

<verification>
1. Open the command palette (Cmd+F).
2. Type a word that appears in a Jira issue summary (e.g. a known issue title word). Wait ~400ms.
3. Confirm matching issues appear in the "Issues" group without clicking "Search Jira for …".
4. Type a 1-character query — confirm no API call fires (query resets, Issues group shows only cache).
5. Confirm "Search Jira for …" tail item still appears and still triggers the manual live search.
6. Close and reopen the palette — confirm no stale debounced state from the previous session.
</verification>

<success_criteria>
- Typing 2+ chars auto-searches Jira after 300ms debounce with no user gesture
- Results appear inline in the "Issues" group (no new heading)
- Total "Issues" group items capped at 10 (cache + live, deduplicated by key)
- `npm run check` passes with zero errors
- Manual "Search Jira for …" and "Search closed tasks for …" tail items remain functional
</success_criteria>

<output>
Create `.planning/quick/260617-e3z-in-the-app-search-i-should-also-be-able-/260617-e3z-SUMMARY.md` when done.
</output>
