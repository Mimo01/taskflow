---
phase: quick-260804-bh3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/services/jira.ts
  - taskflow/src/routes/dashboard/issue-detail/aggregateTimeTracking.ts
  - taskflow/src/routes/dashboard/issue-detail/aggregateTimeTracking.test.ts
  - taskflow/src/routes/dashboard/issue-detail/WorklogProgressBar.tsx
  - taskflow/src/routes/dashboard/issue-detail/WorklogProgressBar.test.tsx
  - taskflow/src/routes/dashboard/IssueDetailView.tsx
autonomous: true
requirements: [QUICK-260804-BH3]
must_haves:
  truths:
    - "On a Subtask issue, the Worklog filter view shows a progress bar of that subtask's own logged vs estimated time"
    - "On a Story/Task issue, the bar shows the story's own logged+estimate PLUS all its subtasks' logged+estimate combined"
    - "On an Epic, no progress bar is shown in the Worklog view"
    - "The bar looks like the standup notes bar: ui/Progress with green/amber/red indicator and a '<spent> / <estimate>' caption"
    - "No new paginated/capped fetch is introduced — subtask time comes from the existing subtask enrichment query"
  artifacts:
    - path: "taskflow/src/routes/dashboard/issue-detail/aggregateTimeTracking.ts"
      provides: "Pure aggregation of own + subtask time tracking seconds"
      exports: ["aggregateTimeTracking"]
    - path: "taskflow/src/routes/dashboard/issue-detail/WorklogProgressBar.tsx"
      provides: "Standup-style logged-vs-estimated progress bar for the worklog view"
      exports: ["WorklogProgressBar"]
  key_links:
    - from: "taskflow/src/routes/dashboard/IssueDetailView.tsx"
      to: "WorklogProgressBar"
      via: "rendered when timelineFilter === 'worklog'"
      pattern: "WorklogProgressBar"
    - from: "taskflow/src/services/jira.ts"
      to: "Jira search fields param"
      via: "fetchEnrichedSubtasks requests timetracking"
      pattern: "fields=assignee,status,timetracking"
---

<objective>
Add a logged-vs-estimated time progress bar to the Worklog view of the Issue Detail page, reusing the exact visual pattern from the standup notes page (`ui/Progress` + `formatDuration` caption).

Purpose: give an at-a-glance burn view of time on the issue where worklogs are read/edited.
Output: a shared `WorklogProgressBar` component, a tested pure aggregation helper, subtask `timetracking` added to the existing enrichment query, and wiring in `IssueDetailView`.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/taskflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Working directory for all paths below: `/Users/mimo/Documents/Projects/taskflow/taskflow`

Reference pattern (the standup notes bar to copy — do NOT invent a new look):
@taskflow/src/routes/standup-notes/TodayInProgressSection.tsx  (see `ProgressBar`, lines ~58-82)

Existing pieces the executor must reuse:
- `Progress` from `@/components/ui/progress` (accepts `value` 0-100 and `indicatorClassName`)
- `formatDuration(seconds)` from `@/services/jira/duration`
- `TimeTrackingSummary` (sidebar variant) at `src/routes/dashboard/issue-detail/TimeTrackingSummary.tsx` — do NOT modify it; the new bar is the standup-style variant
- `fetchEnrichedSubtasks` in `src/services/jira.ts` (line ~1577) — the ONLY subtask fetch; it uses `maxResults=subtasks.length`, so it is already uncapped. Extend it; do not add a new query.
- `subtaskEnrichmentQuery` in `src/routes/dashboard/IssueDetailView.tsx` (line ~160) — already wired, `staleTime: 30_000`, keyed by `['jira-subtask-enrichment', issueKey, jiraBaseUrl, subtaskSignature]`
- `timelineFilter` state in `IssueDetailView.tsx` (line 79, `TimelineFilter` = `'all' | 'comment' | 'change' | 'worklog'`), updated via `onFilterChange` from `ActivityTimeline`
- Issue-type discrimination pattern (from `IssueDetailContent.tsx` lines 209-210):
  `const isEpic = issue.fields.issuetype.name === 'Epic'` and `const isSubtask = issue.fields.issuetype.subtask`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add subtask timetracking to enrichment + pure aggregation helper</name>
  <files>
    taskflow/src/services/jira.ts,
    taskflow/src/routes/dashboard/issue-detail/aggregateTimeTracking.ts,
    taskflow/src/routes/dashboard/issue-detail/aggregateTimeTracking.test.ts
  </files>
  <behavior>
    aggregateTimeTracking(issueTimetracking, subtasks, { isSubtask }):
    - Subtask issue (isSubtask true): returns only the issue's own seconds; subtasks argument ignored.
    - Story/Task: returns own spent+estimate PLUS the sum of every subtask's spent/estimate.
    - Missing `timetracking` on issue or any subtask contributes 0, never NaN.
    - Subtasks array undefined (enrichment still pending) → own values only, no crash.
    - Returns `{ spentSeconds, estimateSeconds }` as numbers.
  </behavior>
  <action>
    1. In `src/services/jira.ts`, extend `fetchEnrichedSubtasks` (around line 1577):
       - change the search URL `fields=assignee,status` to `fields=assignee,status,timetracking`
       - widen the `subtasks` parameter type and the `enrichData.issues[].fields` type with an optional
         `timetracking?: { originalEstimateSeconds?: number; timeSpentSeconds?: number; remainingEstimateSeconds?: number }`
       - in the final `subtasks.map(...)` merge, carry `timetracking: fresh?.timetracking ?? sub.fields.timetracking` alongside the existing assignee/status merge.
       - `maxResults` stays `subtasks.length` — do NOT introduce pagination or a page cap.
    2. In the same file, extend the `JiraIssueDetail['fields']['subtasks'][number]['fields']` type (line ~1477) with the same optional `timetracking` shape so consumers type-check.
    3. Create `src/routes/dashboard/issue-detail/aggregateTimeTracking.ts` exporting a pure
       `aggregateTimeTracking(own, subtasks, opts)` implementing the behavior block above. No React, no fetching, no query-client access.
    4. Create `aggregateTimeTracking.test.ts` covering: subtask-only case, story+subtasks sum, missing timetracking fields, undefined subtasks array.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run src/routes/dashboard/issue-detail/aggregateTimeTracking.test.ts</automated>
  </verify>
  <done>Enrichment requests `timetracking`; aggregation helper exists with passing unit tests; `npx tsc --noEmit` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WorklogProgressBar component (standup-style) with tests</name>
  <files>
    taskflow/src/routes/dashboard/issue-detail/WorklogProgressBar.tsx,
    taskflow/src/routes/dashboard/issue-detail/WorklogProgressBar.test.tsx
  </files>
  <behavior>
    - Renders nothing (null) when the issue type name is 'Epic'.
    - Renders nothing when the aggregated estimateSeconds is 0 or missing (mirrors standup ProgressBar early-return).
    - Renders a `Progress` bar with fill = min(100, round(spent/estimate*100)).
    - Indicator color: `bg-red-500` when spent >= estimate, `bg-amber-500` when fill >= 75, else `bg-green-500` — identical thresholds to `TodayInProgressSection.ProgressBar`.
    - Caption text is `{formatDuration(spent)} / {formatDuration(estimate)}` in `text-xs text-muted-foreground tabular-nums`.
    - Subtask issue: caption reflects only its own numbers.
    - Story with subtasks: caption reflects own + subtask totals.
  </behavior>
  <action>
    Create `WorklogProgressBar.tsx` exporting `WorklogProgressBar({ issue, subtasks })`.
    Compute totals by calling `aggregateTimeTracking` from Task 1 (pass `isSubtask: issue.fields.issuetype.subtask`).
    Copy the markup/classes from `TodayInProgressSection.ProgressBar` verbatim (import `Progress` from `@/components/ui/progress`, `formatDuration` from `@/services/jira/duration`); the only allowed differences are container width (use a full-width wrapper suitable for the detail pane instead of the fixed `w-20`) and an optional leading label. Do not restyle colors, heights, or caption typography.
    Add a `role="progressbar"`-reachable accessible name or reuse whatever `Progress` already renders — do not hand-roll a second bar element.
    Write `WorklogProgressBar.test.tsx` with React Testing Library covering every bullet in the behavior block (epic hidden, no-estimate hidden, subtask own-only, story aggregate, color thresholds).
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npx vitest run src/routes/dashboard/issue-detail/WorklogProgressBar.test.tsx</automated>
  </verify>
  <done>Component renders standup-identical bar; all behavior cases covered by passing tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the bar into the Worklog view of IssueDetailView</name>
  <files>taskflow/src/routes/dashboard/IssueDetailView.tsx</files>
  <action>
    In `activitySectionNode` (around line 517-604), render `&lt;WorklogProgressBar issue={issue} subtasks={subtaskEnrichmentQuery.data} /&gt;` immediately above the `ActivityTimeline` / `CommentsSkeleton` block, gated on `timelineFilter === 'worklog'`.
    Reuse the existing `subtaskEnrichmentQuery` data — do NOT add a new useQuery, and do NOT fetch subtask worklogs individually.
    While enrichment is pending, the bar falls back to the issue's own totals (helper already handles `undefined` subtasks); no skeleton needed.
    Epic suppression is handled inside the component — do not duplicate the check here.
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/taskflow/taskflow && npm run check &amp;&amp; npx vitest run src/routes/dashboard</automated>
  </verify>
  <done>Selecting the Worklog filter on a story/subtask shows the bar; on an epic it does not; `npm run check` clean and dashboard tests pass.</done>
</task>

</tasks>

<verification>
- `cd taskflow && npm run check` (biome + tsc) is clean
- `cd taskflow && npx vitest run src/routes/dashboard src/services/jira.test.ts` passes
- `grep -n "fields=assignee,status,timetracking" src/services/jira.ts` returns a match
- `grep -rn "useQuery" src/routes/dashboard/issue-detail/WorklogProgressBar.tsx` returns nothing (no new fetch)
</verification>

<success_criteria>
- Worklog view of a Subtask shows a bar of its own logged vs estimated time
- Worklog view of a Story shows story + all subtasks combined
- Worklog view of an Epic shows no bar
- Visual identical to the standup notes progress bar (same Progress primitive, thresholds, caption format)
- No new network query, no capped/paginated subtask fetch
</success_criteria>

<output>
Create `.planning/quick/260804-bh3-on-issue-detail-in-the-worklog-tab-add-a/260804-bh3-SUMMARY.md` when done
</output>
