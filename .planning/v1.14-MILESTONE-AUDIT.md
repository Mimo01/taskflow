---
milestone: v1.14
name: Release Management
audited: 2026-08-13T07:05:07Z
status: tech_debt
scores:
  requirements: 31/31   # 33 defined, 2 intentionally descoped (DRIFT-09, MERGE-03)
  phases: 7/7           # all verified; 2 carry open human-verification items
  integration: 10/10    # all cross-phase wiring paths confirmed at HEAD
  flows: 3/3            # branch-create, drift-fix, merge-back verdict all traced end-to-end
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 87-release-detail-decomposition
    items:
      - "WR-01: `jira-version-counts` cache key written by two divergent code paths — ReleasesTab.tsx uses a local raw-fetch fetcher with a different shape and no versionId guard, instead of the newly shared `fetchVersionIssueCounts`"
      - "Page shell is 322 LOC vs D-06's aspirational 150-250 target"
      - "WR-04: newly exported branching functions are undertested"
      - "WR-06: pre-existing duplicate-breadcrumb-key bug carried over verbatim"
  - phase: 88-release-branch-milestone-creation
    items:
      - "WR-01 (open at HEAD): gitlab.ts:1056-1070, 1119-1133 — 401/403/400 `message` cast flattens only `string`/`string[]`, not GitLab's object-keyed Grape validation-error shape; a duplicate-title rejection renders as '[object Object]' in the dialog, which is the only error surface (D-15 forbids toasts)"
      - "88-11 Tasks 2-4 (live-GitLab create-branch, create-milestone, restricted-PAT error surfacing) explicitly waived by the user — no write path has ever executed against a real GitLab instance; coverage is mocked-fetch unit tests only"
  - phase: 89-three-channel-drift-detection
    items:
      - "WR-04: a failed Channel A/B/C query renders as 'no drift' (isError discarded, `?? []` swallows failure) — partially superseded by 91.1's drift-partial-banner but not closed at the channel level"
      - "WR-06: list page and detail page resolve 'matched milestone' via different tie-break rules and date windows; can diverge when >1 milestone candidate falls in the ±7d window"
      - "WR-07/WR-08/WR-09/WR-10/WR-11/WR-12 + IN-01..IN-06: narrow edge cases and test-quality items, triaged as non-blocking"
  - phase: 90-per-mr-corrective-actions
    items:
      - "Biome baseline drift: 16 diagnostics across 5 files (BacklogPage, BacklogRow, chart.tsx, MyTasksPage.tsx/.test.tsx) — pre-existing from Phases 81/82, zero introduced by Phase 90; needs `biome check --write` plus human judgment on noArrayIndexKey / noStaticElementInteractions"
      - "D-16 probe never run: 90-PROBE-RESULTS.md is `status: not-run`, RESEARCH Open Question A1 UNRESOLVED — no GitLab PAT was reachable in any execution environment. Per D-16 this changes no UI regardless of outcome"
      - "CR-01 rollback fix (commit 8e8e4676) landed after the blanket live UAT approval — the fixed code has never been watched against real GitLab data (regression test exists and passes)"
  - phase: 91-post-release-merge-back-verification
    items:
      - "WR-01: releaseBranch.ts:120-123 doc comment claims `tagChannel` is type-system-enforced REQUIRED, but the sole producer `resolveBranchState` takes it as an optional defaulted param (:178) — documentation-accuracy / future-regression risk, current single call site passes it correctly"
  - phase: 91.1-unified-release-detail-task-table
    items:
      - "Three human-verification items approved without step-by-step walkthrough: badge-filter end-to-end flow, WR-01 breadcrumb regression via the changed click surface, and CR-01/CR-02 load-flash on hard reload. Unit coverage exists for all three; live observation does not"
  - phase: 91.2-epics-page-redesign
    items:
      - "WR-06/WR-07: progress cell `w-32` tight at 3-digit counts; assignee cell `w-10` content-box narrower than the avatar — cosmetic, not fixed"
      - "WR-08: EpicProgressCells.tsx:20-27 duplicates structurally-coupled count types from jira.ts:2552-2559 with no shared import"
      - "IN-04: two identical whole-batch retry controls per row"
      - "IN-05: percentage segment widths can leave sub-pixel muted slivers (last-segment flex-1 fix not applied)"
nyquist:
  compliant_phases: [88, 89, 90, 91.2]
  partial_phases: [87, 91, 91.1]
  missing_phases: []
  overall: partial
human_verification_open: 6
---

# Milestone Audit — v1.14 Release Management

**Goal:** Turn the Releases view from a read-only Jira↔GitLab date-match into a working release-coordination
surface that detects git-flow drift and lets the user fix it per-MR.

**Verdict:** `tech_debt` — every in-scope requirement is satisfied, all cross-phase wiring is confirmed at HEAD,
and all three primary E2E flows trace end-to-end. No critical blockers. What remains is accumulated debt plus
six live-environment verification items that were waived or approved without a walkthrough.

## Requirements Coverage

33 requirements defined; 2 intentionally descoped by the user; **31/31 in-scope satisfied**.

| REQ | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|-----|-------|--------------|---------------------|-----------------|-------|
| FOUND-01 | 87 | passed | 87-01…87-06 | `[x]` | satisfied |
| RELBR-01…05 | 88 | human_needed (9/9 code-level) | 88-01/02/03/08/09/10 | `[x]` | satisfied |
| RELMS-01…04 | 88 | human_needed (9/9 code-level) | 88-01/02/03/04/06/09/10 | `[x]` | satisfied |
| DRIFT-01…08 | 89 | passed (9/9) | 89-01…89-05, 91.1-01…10 | `[x]` | satisfied |
| DRIFT-09 | 89 | — | 89-02, 89-04 (then removed) | `[~]` | **descoped** (user, UAT 2026-08-11) |
| MRFIX-01…04 | 90 | human_needed (4/4 code-level) | 90-01…90-04, 91.1-03/04/05 | `[x]` | satisfied |
| MERGE-01…02 | 91 | passed (7/7) | 91-01…91-09 | `[x]` | satisfied |
| MERGE-03 | 91 | override accepted | 91-06, 91-08 | `[x]` | **descoped** (user D-12) |
| EPIC-01…07 | 91.2 | passed (11/11) | 91.2-01…91.2-04 | `[x]` | satisfied |

**Orphan detection:** none. Every REQ-ID in the traceability table appears in at least one phase VERIFICATION.md
and at least one SUMMARY `requirements-completed` list.

**Traceability note (non-blocking):** Phase 91.1 owns no REQ-IDs of its own — it re-implements DRIFT-01…08 and
MRFIX-01…04 as a consolidated table and is credited against those IDs. The traceability table has no row for
Phase 91.1. Worth a line in REQUIREMENTS.md at milestone close so the phase isn't read as unmapped later.

**Descope precedent:** DRIFT-09 and MERGE-03 follow the same pattern as DASH-06 (P84) and DRIFT-09's own
sibling — built, then removed at UAT on explicit user decision, recorded in ROADMAP/CONTEXT. Absence is not a gap.

## Phase Verification Status

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 87 release-detail-decomposition | passed | 3/3 | 7 review warnings routed forward as follow-ups |
| 88 release-branch-milestone-creation | human_needed | 9/9 code-level | Re-verified from `gaps_found` 6/9; 3 CRs closed by 88-07/08/09. 3 live-GitLab checkpoints waived by user |
| 89 three-channel-drift-detection | passed | 9/9 | CR-01 + WR-01/02/03/05 fixed and re-read at source; 9 warnings triaged open |
| 90 per-mr-corrective-actions | human_needed | 4/4 | CR-01 rollback bug fixed post-UAT; D-16 probe never run (no PAT) |
| 91 post-release-merge-back | passed | 7/7 | 1 override (MERGE-03 descope); re-verified from `gaps_found` 6/7, gap closed by 91-09 |
| 91.1 unified task table | human_needed | 6/6 | Re-verified from `gaps_found` 4/6; 12 findings closed. 3 walkthroughs approved without live observation |
| 91.2 epics-page-redesign | passed | 11/11 | 2 CRs + 5 high-impact WRs fixed post-review |

No phase is missing a VERIFICATION.md. No phase reports `gaps_remaining`.

## Cross-Phase Integration

Integration checker verified against source at HEAD (not against SUMMARY narratives), with `tsc --noEmit` clean
and 420/420 vitest green on the release-detail and Epics suites.

| Path | Result |
|------|--------|
| P87 decomposition → all later phases | WIRED — `ReleaseDetailPage.tsx` imports exclusively from `./release-detail/*`; no monolith code reintroduced |
| P88 `releaseBranch.ts` → P90 MRFIX-04 gating | WIRED — `mrFixReadiness()` (UnifiedTaskTable.tsx:153-163) gates retarget on `releaseBranchExists && releaseBranchName != null` |
| P88 branch state → P91 tag channel health | WIRED — `tagChannel` ('pending'/'failed'/'resolved') threaded into both `branchState` and `resolveMergeBackVerdict` |
| P89 channels → P90 mutations → P91.1 table | WIRED — `mrChannelKeys.ts` centralizes the three channel keys; `useReleaseDetail.ts` queries and `useMrFixMutation.ts` patches/invalidates through the same module |
| P91.1 consolidation orphans | NONE — `MrDriftSection` and pre-91.1 table code fully removed, zero references |
| P91.2 `src/services/jira/epics.ts` removal | CLEAN — zero remaining importers |

**E2E flows: 3/3 complete.**
1. Branch missing → create → sidebar flips: `createBranchMutation.onSuccess` invalidates both the detail key
   `['gitlab-branch', project, branchName]` and the cross-page `['gitlab-release-branches', project]`.
2. Drifted MR → retarget → row + counts update: optimistic `patchMrInChannelCaches` across all three channel
   caches; `flaggedMrCount` / `brFlaggedCount` / `msFlaggedCount` recompute reactively from the same cache.
3. Fix version released → merge-back verdict: every verdict kind rendered including tag-channel-degraded states
   (ReleaseDetailSidebar.tsx:384-430).

Notable: `mrChannelKeys.ts` exists specifically to kill the hidden query-key-string coupling between query sites
and invalidation sites — the exact bug class (CR-02, P88) this audit was designed to catch, fixed structurally.

## Nyquist Coverage

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Status |
|-------|---------------|-------------------|-----------------|--------|
| 87 | exists | false | false | PARTIAL |
| 88 | exists | true | false (closed by 88-01 at exec time) | COMPLIANT |
| 89 | exists | true | false | COMPLIANT |
| 90 | exists | true | true | COMPLIANT |
| 91 | exists | false | false | PARTIAL |
| 91.1 | exists | false | false | PARTIAL |
| 91.2 | exists | true | true | COMPLIANT |

**Overall: partial** — 4 compliant, 3 partial, 0 missing. Phases 87, 91 and 91.1 can be closed retroactively with
`/gsd-validate-phase 87|91|91.1` if you want full Nyquist coverage before shipping. All three passed goal
verification independently, so this is a coverage-record gap, not evidence of untested code.

## Open Human Verification (6 items)

These are the honest outstanding items — none is a code-level defect, all are "never observed against a live
GitLab instance" or "approved without a walkthrough."

**Phase 88 (3, all explicitly waived by the user at the wave-4 checkpoint):**
1. Create a real release branch against live GitLab — confirm correct ref, sidebar flip, actionable 403 text.
2. Create a real GitLab milestone — confirm cross-view propagation to the Releases list without manual refresh.
3. Restricted-PAT 401/403 error surfacing in both dialogs — this one has a **known code gap** (WR-01: object-keyed
   GitLab validation-error bodies render as `[object Object]`). Worth fixing opportunistically since it directly
   affects what this checkpoint would observe.

**Phase 90 (2):**
4. Re-confirm MRFIX-03 independence + the CR-01 rollback fix on live GitLab (the blanket UAT approval predates
   commit 8e8e4676). A dedicated regression test covers the exact sequence and passes.
5. Run the D-16 probe with a real PAT to populate 90-PROBE-RESULTS.md and resolve RESEARCH A1.

**Phase 91.1 (3, folded into one line — see tech_debt):**
6. Badge-filter end-to-end, WR-01 breadcrumb regression on the changed click surface, and CR-01/CR-02 load-flash
   on hard reload — all unit-tested, none observed live.

## Recommendation

Ship it. The milestone goal is achieved and structurally sound. Two things are worth doing first, in priority order:

1. **Fix 88 WR-01** (`gitlab.ts` object-keyed error message flattening) — small, self-contained, and it is the only
   item on this list that is a real user-visible defect rather than a verification gap.
2. **One live-GitLab smoke pass** covering the 88 write paths and the 90 CR-01 sequence — the release-branch and
   milestone create paths have never executed against a real instance.

The Biome drift (16 diagnostics, 5 files, all pre-existing from Phases 81/82) is a good candidate for a standalone
quick task; per the project's own baseline rule, gate on "no new files flagged," not an absolute count.
