---
phase: 47-v17-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskflow/src/routes/dashboard/BacklogPage.tsx
  - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx
  - taskflow/.gitignore
autonomous: true
requirements: [LOAD-03]
must_haves:
  truths:
    - "BacklogPage uses shared STALE_TIME_MS constant for staleTime instead of literal 30_000"
    - "SprintBoardTab.test.tsx has no unused fetchSprintIssues mock"
    - "stats.html is listed in taskflow/.gitignore"
  artifacts:
    - path: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      provides: "STALE_TIME_MS constant usage"
      contains: "STALE_TIME_MS"
    - path: "taskflow/src/routes/dashboard/SprintBoardTab.test.tsx"
      provides: "Clean mock without dead fetchSprintIssues"
    - path: "taskflow/.gitignore"
      provides: "stats.html ignore rule"
      contains: "stats.html"
  key_links:
    - from: "taskflow/src/routes/dashboard/BacklogPage.tsx"
      to: "taskflow/src/lib/query-constants.ts"
      via: "import { STALE_TIME_MS }"
      pattern: "import.*STALE_TIME_MS.*query-constants"
---

<objective>
Fix two code debt items and one artifact cleanup identified by the v1.7 milestone audit.

Purpose: Eliminate stale literal constants and dead test mocks before closing the v1.7 milestone.
Output: BacklogPage using shared constant, clean test mock, stats.html ignored.
</objective>

<execution_context>
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/Tasker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@taskflow/.planning/phases/47-v17-debt-cleanup/47-RESEARCH.md
@taskflow/.planning/phases/47-v17-debt-cleanup/47-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace literal 30_000 with STALE_TIME_MS in BacklogPage and remove dead mock</name>
  <files>taskflow/src/routes/dashboard/BacklogPage.tsx, taskflow/src/routes/dashboard/SprintBoardTab.test.tsx</files>
  <read_first>
    - taskflow/src/routes/dashboard/BacklogPage.tsx (find the two staleTime: 30_000 usages near lines 268 and 278)
    - taskflow/src/lib/query-constants.ts (verify STALE_TIME_MS export exists with value 30_000)
    - taskflow/src/routes/dashboard/SprintBoardTab.test.tsx (find the vi.mock block for @/services/jira at lines 30-35, identify fetchSprintIssues at line 31)
  </read_first>
  <action>
**BacklogPage.tsx changes:**
1. Add import at top of file: `import { STALE_TIME_MS } from '@/lib/query-constants';`
   - If STALE_TIME_MS is already imported (check existing imports), skip this step
2. Replace `staleTime: 30_000,` at line ~268 (jira-sprint-stories query) with `staleTime: STALE_TIME_MS,`
3. Replace `staleTime: 30_000,` at line ~278 (jira-sprint-subtasks query) with `staleTime: STALE_TIME_MS,`
4. No other changes to BacklogPage.tsx

**SprintBoardTab.test.tsx changes:**
1. Locate the `vi.mock('@/services/jira', ...)` factory block (lines ~30-35)
2. Remove the line `fetchSprintIssues: vi.fn().mockResolvedValue([]),` (line ~31)
3. Do NOT touch the separate `vi.mock('@/services/jira/issues', ...)` block (lines ~38-41) — that mocks fetchSprintStories and fetchSprintSubtasks and must remain
4. Verify the remaining mock object still has valid syntax (no trailing comma issues, no empty object)
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/Tasker/taskflow && npx tsc --noEmit && npm test src/routes/dashboard/BacklogPage.test.tsx src/routes/dashboard/SprintBoardTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BacklogPage.tsx contains `import { STALE_TIME_MS } from '@/lib/query-constants'` (or STALE_TIME_MS in an existing import from that module)
    - BacklogPage.tsx does NOT contain `staleTime: 30_000` (grep returns no matches)
    - BacklogPage.tsx contains `staleTime: STALE_TIME_MS` (grep returns 2 matches)
    - SprintBoardTab.test.tsx does NOT contain `fetchSprintIssues` anywhere
    - SprintBoardTab.test.tsx still contains `fetchSprintStories` and `fetchSprintSubtasks` mocks
    - `npx tsc --noEmit` exits 0
    - `npm test src/routes/dashboard/BacklogPage.test.tsx` exits 0
    - `npm test src/routes/dashboard/SprintBoardTab.test.tsx` exits 0
  </acceptance_criteria>
  <done>BacklogPage uses STALE_TIME_MS constant (behavioral no-op), dead fetchSprintIssues mock removed from SprintBoardTab.test.tsx, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add stats.html to .gitignore</name>
  <files>taskflow/.gitignore</files>
  <read_first>
    - taskflow/.gitignore (current contents)
  </read_first>
  <action>
1. Append to `taskflow/.gitignore`:
```
# Bundle analysis artifact
stats.html
```
2. If `taskflow/stats.html` exists on disk, delete it (it is a generated artifact from `ANALYZE=true npx vite build`)
  </action>
  <verify>
    <automated>grep -q "stats.html" /Users/mimo/Desktop/Tasker/taskflow/.gitignore && echo "PASS: stats.html in gitignore" || echo "FAIL: stats.html not in gitignore"</automated>
  </verify>
  <acceptance_criteria>
    - taskflow/.gitignore contains the line `stats.html`
    - taskflow/stats.html does not exist on disk (deleted if it was present)
  </acceptance_criteria>
  <done>stats.html is gitignored and removed from disk</done>
</task>

</tasks>

<verification>
- `cd /Users/mimo/Desktop/Tasker/taskflow && npm test` — full test suite passes
- `grep -c "staleTime: 30_000" src/routes/dashboard/BacklogPage.tsx` returns 0
- `grep -c "fetchSprintIssues" src/routes/dashboard/SprintBoardTab.test.tsx` returns 0
- `grep -q "stats.html" .gitignore` succeeds
</verification>

<success_criteria>
All three code debt / artifact items from the v1.7 milestone audit are resolved, full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/47-v17-debt-cleanup/47-01-SUMMARY.md`
</output>
