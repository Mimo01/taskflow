// AUTH-05: Token management in settings — Plan 02
// ROLE-02: Role switching in settings — Plan 02
import { describe, it } from 'vitest';

describe('Settings', () => {
  it.todo('AUTH-05: Jira token is displayed as masked by default');
  it.todo('AUTH-05: eye icon reveals the token');
  it.todo('AUTH-05: "Update token" button triggers store write');
  it.todo('AUTH-05: removes Jira token and sets jiraConnected to false on revoke');
  it.todo('ROLE-02: role picker reads current role from useSettingsStore');
  it.todo('ROLE-02: switching role updates useSettingsStore');
});
