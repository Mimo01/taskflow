/**
 * SubtasksSection -- placeholder for subtask list display.
 *
 * Note: The original IssueDetailSidebar does NOT render subtasks inline.
 * Subtask display is handled elsewhere (IssueDetailContent).
 * This file exists per the plan but exports an empty/no-op component.
 */

export function SubtasksSection() {
  // Subtasks are rendered by IssueDetailContent, not the sidebar.
  // This component exists as a placeholder for potential future sidebar subtask display.
  return null;
}
